# Hwao direction after Tori solo-overstep correction — 20260706T0614Z

Marker: `HWAO_POST_SOLO_CORRECTION_DIRECTION_20260706T0614Z`

Correction accepted. The user's "You seem to work SOLO again" is a valid procedural correction. The Hwao-led contract is restored effective now: Hwao plans and assigns, Lana/Goru/Kun execute only on assignment, Tori relays/records/verifies. Tori's containment work (Gemini/goru-agy kills, OpenClaw LaunchAgent disable-only audit) is ratified retroactively because it was user-directed containment under the "stop Google-credit burn first" priority — but it does not create a precedent for Tori-led sequencing.

## 1. Tori may continue — in relay/verify capacity only

Tori is not paused, but until the user gives a fresh go-ahead, Tori's scope is: safety verification, receipt/marker checking, and relaying this direction. No new sequencing initiative from Tori.

## 2. P1/P3/P4 docs-only sequencing — exact next move

- Next move is verification, not production. The three specs under `docs/hwao_morning_blocker_specs_20260706T0308Z/` are the canonical spec source. Tori verifies read-only that each of P1/P3/P4 plus `BLOCKER_SPECS_RESULT.md` still carries the `NO ACTIVE EXECUTION PHRASE` wording and matches the read-only API snapshot, and reports discrepancies to Hwao. Nothing is rewritten.
- Sequencing decision (P1 → P3 → P4) is frozen as proposed but not activated. No spec advances to an execution-ready packet until the user, when quiet, chooses P2/P5/spec sequencing per their earlier stated intent. That choice belongs to the user, not to Tori and not to Hwao unilaterally.

## 3. Lana / Goru / Kun assignments

- No re-brief now. All three remain unassigned and idle. Goru in particular stays down — its `agy --model Gemini` process was a credit-burn suspect and must not be restarted until the user confirms the Google-credit containment is settled.
- When the user green-lights sequencing, Hwao will issue assignments explicitly; Tori should not pre-stage briefs for them.

## 4. Tori artifact-writing permission

- Tori may write exactly one new artifact: a short relay/receipt note recording this direction in the existing handoff directory. Nothing else — no new packets, no new specs, no promotion of Tori solo-scratch material into `docs/`.
- Existing Tori solo-scratch work is quarantined as-is; Hwao will decide later whether any of it is worth inspecting or reusing.

## 5. Cockpit / public update

- Not authorized. Cockpit and all public/user-facing surfaces remain unchanged. The safety-state line stays `NO ACTIVE EXECUTION PHRASE`. A cockpit update happens only after the user has seen this direction and the containment result, and only if they ask for one.

## 6. Scope / exclusion wording to preserve

- `NO ACTIVE EXECUTION PHRASE`
- DB writes: 0 · SQL/apply/rollback: 0 · trust recompute: 0 · prose/wiki/page_versions publish: 0 · product code patch: 0 · git/deploy/restart: 0 · cloud/GCP/API mutation: 0
- OpenClaw LaunchAgents: disabled/booted out only — plist files not deleted. Reversible; deletion requires explicit user approval.
- Specs under `docs/hwao_morning_blocker_specs_20260706T0308Z/` are docs-only and confer no execution approval.

Blocking condition: everything beyond safety verification and this receipt waits on the user's sequencing choice and their confirmation that Google-credit containment is closed.

`HWAO_POST_SOLO_CORRECTION_DIRECTION_20260706T0614Z`
