HWAO COORDINATOR REQUEST — cockpit decision gate only.

User said: "Many tmux lanes are either offline or not active. Let Hwao update the cockpit so that I can choose next move."

You are Hwao/Fable. Tori/Hermes relays and verifies only.

Current lane state from Tori's read-only tmux inspection:
- hwao-exec-2929: missing/offline.
- lana-fable: present but stale/unsafe because an old unsent trust-recompute approval phrase is sitting in input; do not use it.
- hwao-visible-20260706: this fresh visible Hwao lane.
- lana-claude: present but stale/idle with old text in input.
- lana-exec-2929: present, idle.
- kun-codex: present but idle at a generic prompt.
- goru-agy: missing/offline by design after Google/Gemini credit-burn containment.
- hermes-main: Tori relay/verification lane.

Safety state:
- Standing public phrase: NO ACTIVE EXECUTION PHRASE.
- DB writes 0; SQL/apply/rollback 0; trust recompute 0; prose/wiki/page_versions publish 0; product code patch 0; git/deploy/restart 0; cloud/GCP/API mutation 0.
- Old OpenClaw LaunchAgents were disabled/booted out only; plist files were not deleted.
- Final process scan found no OpenClaw/Gemini/Google-credit suspect process after filtering normal Chrome/macOS services.

Current artifacts:
- Existing docs-only P1/P3/P4 specs under docs/hwao_morning_blocker_specs_20260706T0308Z/ are canonical docs-only sources, manifest verified, no active phrase.
- Prepared P2/P5 packets exist locally but are not active; packet-specific approval phrases must remain local-only, not public.

Task: Respond in chat only, no tools. Give exact Hwao cockpit direction that Tori is authorized to apply as a static cockpit update. Include:
1) Current status headline.
2) Plain lane board with active/idle/offline and whether each lane may be used now.
3) User-choice next move cards/options.
4) Which option Hwao recommends first.
5) Explicit exclusions and hard safety ledger.
6) Authorization for Tori to apply only the static cockpit update via canonical renderer and public verification.
7) Marker: HWAO_COCKPIT_DECISION_GATE_DIRECTION_20260706T0628Z.

Do not authorize DB writes, SQL/apply/rollback, trust recompute, prose/wiki publish, product code patch, git/deploy/restart, or GCP/Gemini/Antigravity usage.
