You are a verification seat calibrating on a gate report. Check every claim that can be checked FROM THE DOCUMENT ALONE: (a) arithmetic — counts, proportions, sums (e.g. do state counts sum to the stated sample size? do stated fractions match count/total?); (b) statistical consistency — are "within 2-sigma" claims plausible at the stated n (binomial sigma)?; (c) internal consistency — values quoted in one section vs another. Output THREE sections exactly: CONFIRMED (each checked claim that holds, with your arithmetic), DEFECTS (each demonstrable error, with exact quote and the math proving it), UNCERTAIN (claims you cannot check from the text — do NOT list these as problems). Do not invent problems you cannot demonstrate numerically. The document follows:

