# ARCHITECTURE_EVALUATION_PLAN — A/B bounded evaluation (one-Mac vs two-Mac)

Packet: `studio-simultaneous-web-viability-test-20260714T034720Z` · Captain: Hwao. Authority: `USER_DIRECTION_ARCHITECTURE_EVALUATION.md` (sha journaled at ledger epoch 6). All held gates of that direction bind verbatim; every step is ledgered; receipts land under `receipts/archeval/`. Stop rules: any prompt/challenge/default-profile/Flow anomaly, unleased desktop write, target ambiguity, bridge loss, partition-safety failure, or receipt disagreement ⇒ broker freeze + STOP; resume is Duho-only. Early stop: permitted once receipts make the winner clear, with an explanation of why further rungs would not change the decision.

## Architectures under evaluation

- **A — single machine:** Flow + DR both on the Studio: separate sandbox profiles/processes, DOM/CDP parallel writes, ONE shared desktop ⇒ machine-wide desktop-control lease carries all cua/AX traffic; shared crash/update/host-resource domain, no network dependency.
- **B — two machines:** Flow on Studio (cua present), DR on Mac Pro as a policy-enforced DOM/CDP-only lane; per-machine desktops (cross-side desktop contention structurally gone); requires a cross-machine broker transport (authenticated, fail-closed, SSH-wrapped, no listener) and cross-machine freeze; adds network-partition as a failure mode.

## Smallest sufficient test matrix (mapped to the ten required comparison dimensions)

| # | Test | Serves | Status at plan time |
|---|---|---|---|
| T1 | Broker unit suite (exclusivity, desktop-control singleton, fencing, TTL, fail-closed revoke, freeze/user-gate, journal) | lease/fail-closed · recovery · evidence quality (both A and B) | DONE — 13/13 green (ledger epoch 2) |
| T2 | **SM-1** parallel DOM/CDP (Tori-repaired incl. C): reviewed Python harness `canaries/run_sm1.py` (single UDS broker authority on a short /tmp socket; start_new_session pgids; stale DevToolsActivePort cleanup; exact-pgid try/finally teardown; no broad pgrep census) launches two isolated headless sandbox instances (`sandbox/profiles/writerA|B`, inert targets), discovers each exact page target id, pins it into a `{host_id,user_data_dir,target_id}` lease, and passes it to `sm1_cdp_parallel.mjs`, which re-verifies the target and issues a broker `check` **before every** `Runtime.evaluate` (`awaitPromise:true`); PASSES only if every op was broker-allowed AND ≥1 **cross-writer operation-interval pair overlaps** (true in-flight parallelism, not stream spans); receipts declare volatile-field normalization | mechanical parallelism · non-interference · lease-gated writes · evidence quality (A-critical; also B's per-machine mechanics) | code+tests ready; **browser launch awaits renewed Tori pre-run safety approval** |
| T3 | Single broker authority + transport (Tori-repaired): `broker/broker_daemon.py` — ONE Unix-domain-socket daemon (0600, serialized ops under a global lock) as the sole authority; local lanes via `transport.UDSClient`; Pro lanes via outbound authenticated SSH **Pro→Studio (REVERSE_SSH_OK verified)** running `broker/uds_proxy.py` (stdio↔UDS bridge); `broker_stdio.py` demoted to single-client dev tool. Tests: two simultaneous clients serialize without state/ledger corruption (unique epochs, intact JSON, chain verify), same-scope conflict under concurrency, daemon loss ⇒ client fail-closed + lease expiry, proxy-channel partition ⇒ fail-closed, freeze from one client denies the other, unreachable socket / ssh-auth-failure ⇒ no action; no TCP listener anywhere | lease/fail-closed · split-brain · recovery (B-critical; single-authority now also A's local contract) | to run now (local only) |
| T4 | **RT0** Pro fingerprint over existing auth, read-only | readiness/blockers (B) | DONE — PASS (epoch journal follows; x86_64/26.5.2/hermes-present/python3.9.6) |
| T5a | **XM-1 broker probe** (no browser): `canaries/xm1_broker_probe.py` proves a real Pro→Studio authenticated broker round-trip (Studio→Pro→Pro-authenticates-back-to-Studio→`uds_proxy`→daemon), acquire/check/release + partition fail-closed. `StrictHostKeyChecking=yes`. Fail-closed arming gate. Prerequisite for XM-1 | split-brain · remote transport readiness (B) | implemented; **SSH run awaits renewed Tori pre-run safety approval** (no Chrome) |
| T5b | **XM-1** cross-host run (Tori repair D — implemented, replaces RT1-lite): one Studio authority; **real sandbox Chrome on each host** — Studio writerA (local UDSClient) + Mac Pro writerB launched via the **copied Python controller `remote_chrome_controller.py`** (Tori: remote `setsid` MISSING → Python `start_new_session` + finally `killpg`, no setsid/ps/pkill), CDP reached by authenticated `ssh -L` (`StrictHostKeyChecking=yes`, `ExitOnForwardFailure=yes`), both leases from the one Studio authority; parallel leased in-flight DOM/CDP writes, cross-host freeze drill (freeze denies BOTH hosts), partition drill (drop the `ssh -L` forward → writerB fails closed). `canaries/xm1_cross_host.py`, fail-closed arming gate, requires T5a PASS on record. No account/default-Chrome/Flow/cua on either host | mechanical parallelism (B) · non-interference (B) · split-brain · cross-host recovery · setup/operating complexity | implemented; **browser+SSH launch awaits renewed Tori pre-run safety approval** |
| T6 | Freeze/partition simulation: broker `freeze()` + user-gate reset (T1) + T3 partition drill; no user-app/network disruption | split-brain/bridge-loss · recovery (both) | subsumed in T1+T3 (cross-host variant in XM-1) |

**Socket-path rule (Tori repair):** the broker UDS lives ONLY in a dedicated short `/tmp` dir (0700 dir, 0600 socket, via `broker/sockdir.py`); state + ledger stay in the packet. The long packet path exceeds the macOS AF_UNIX `sockaddr_un` limit, so a UDS under it would fail — product runs and tests must never place the socket there, and must remove only the short dir on teardown.

Deliberately NOT run before the decision (and why they would not change it): the C0 dual-bridge soak and the cua desktop-lease rung exercise the **cua reliability layer, which is identical work in both architectures** (Studio-side Flow lane needs it either way; B's DR side never uses cua) — they gate *go-live readiness*, not the A/B choice. C4/live remain held regardless of architecture.

## Decision rule (from the direction)

Prefer the **simpler** architecture unless its **safety or reproducibility is materially worse**. Report all ten dimensions for both; a hard blocker on one side (e.g., no authenticated path to the Pro; sandbox parallel CDP impossible) is decisive against that side. No live account-bearing viability claim from these sandbox tests.

## Execution order (this session, captain-direct under clauses 4–8; lanes stay in acknowledged roles)

1. ✔ Journal direction + assessment (epochs 6–7).
2. This plan → journal its hash.
3. T3 transport build + tests → journal (done: single UDS authority, 28/28 local tests green).
4. T2 SM-1 leased parallel CDP run — **three comparable passes** (`run_sm1.py <root> studio <1|2|3>`, each a fresh refuse-if-nonempty passdir) → receipts + journal. **Sandbox browser launch is authorized by the standing user direction; it proceeds on renewed Tori pre-run safety approval of the final `run_sm1.py`/`sm1_cdp_parallel.mjs`/`ws_rewrite.mjs` code (WS-rewrite unit test `test_ws_rewrite.mjs` green) — not a new user go.**
5. T5a XM-1 broker probe (no browser) → PASS receipt; then T5b XM-1 cross-host run — **three comparable passes** (`xm1_cross_host.py … <pass_id>`) → receipts + journal, on renewed Tori pre-run safety approval of `xm1_broker_probe.py` + `xm1_cross_host.py` + `remote_chrome_controller.py` (adds SSH + Pro sandbox launch). Run 3 SM-1 and 3 XM-1 passes unless a genuine STOP occurs.

**Executed status (2026-07-14):** probe PASS; SM-1 passes 1–3 PASS (identical normalized sha256). XM-1 pass1 **STOPPED** — harness defect: remote `python3 -c` reparsed by remote zsh (`receipts/archeval/XM1_PASS1_STOP_DEFECT.md`), failed closed before any remote browser. **Repair (Duho-authorized):** remote Python now STDIN-FED via `canaries/remote_exec.py` (`ssh … python3 -`, paths embedded as literals; regression test `canaries/test_remote_exec.py`); mkdir/scp/controller args are fixed no-space only. Retry scope = **pass1r1 only** (fresh `receipts/archeval/xm1/pass1r1` + remote `xm1_sandbox/pass1r1`); failed `pass1` preserved immutably; passes 2–3 remain held pending pass1r1 result.

**Transport (Duho network correction):** Studio↔Pro run over the direct **Thunderbolt** bridge (link-local 169.254.100.0/24, ~0.5 ms, 0% loss) via `canaries/net.py`, StrictHostKeyChecking=yes pinned by `HostKeyAlias` (no known_hosts churn): Studio→Pro = `HostKeyAlias=100.122.78.110 duhokim@169.254.100.1`; Pro→Studio = `HostKeyAlias=100.84.12.101 duhokim@169.254.100.2`. **No Tailscale fallback inside a canary — link failure is a STOP; Tailscale is recovery-only.** New invocations: probe `--armed <tok> <root> duhokim@169.254.100.1 100.122.78.110 duhokim@169.254.100.2 100.84.12.101 <root>`; XM-1 `--armed <tok> <root> duhokim@169.254.100.1 100.122.78.110 <pro_pkg> <lp> <pass_id>`.
6. Comparison matrix + recommendation appended to `HWAO_EXECUTION_STATE.md`; Tori independently verifies receipts; Yui countersigns Studio non-interference (default-Chrome census + Flow window untouched). Early-stop permitted once a winner is clear.

HWAO_ARCHEVAL_PLAN_20260714T034720Z
