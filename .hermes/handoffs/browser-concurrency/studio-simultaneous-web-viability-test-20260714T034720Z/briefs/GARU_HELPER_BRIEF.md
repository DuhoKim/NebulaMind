# GARU brief — helper lane: bridge observer 2 / DOM-CDP writerB (ACK required)

Packet: `studio-simultaneous-web-viability-test-20260714T034720Z` · Issued by Hwao. First engagement in this role table: write `briefs/acks/GARU_ACK.md` acknowledging this brief and the standing protocols (no solo lanes; lane-scoped temp files `_tmp_*`; never free-text tmux send-keys; ledger is sole shared state) BEFORE any participation. Quota cap mirrors Goru's (≤40% window per rung), logged per receipt.

Scope by rung: **C0** — idle bridge #2, scheduled non-mutating samples only. **C1** — read lease on non-browser target B. **C2** — read lease on sandbox browser target B. **C3** — DOM/CDP **writerB**: deterministic, non-submitting scripted actions against YOUR leased CDP target in `sandbox/profiles/writerB` only, under live broker lease + per-action `check()` + target re-verification; overlap with writerA expected/allowed; never cua/AX/pointer/keyboard. No lease → no action; lease loss → stop, release, `bridge_loss` entry, never frontmost fallback. Write areas: `receipts/c*/pass*/GARU_*`, `canaries/_tmp_garu_*`, ledger appends (actor `garu`). Prohibitions: default Chrome/Flow window, credentials, sign-in surfaces, submissions, downloads outside `sandbox/downloads/writerB`, desktop-control lease requests. STOP authority: yes.

GARU_VIABILITY_BRIEF_ISSUED_20260714T034720Z
