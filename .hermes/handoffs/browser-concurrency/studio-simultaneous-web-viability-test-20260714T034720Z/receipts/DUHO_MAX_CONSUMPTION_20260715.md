# DUHO_MAX_CONSUMPTION — ramp DR + Flow toward the throttle to burn Ultra quota

Authorized by Duho (2026-07-15). GOAL: consume the ONE Google Ultra account's quota as fast as SUSTAINABLE. One account (NOT a separate one), both lanes, two distinct egress IPs (Pro=DR, Studio=Flow).

## Authorization
1. UNHOLD DR RE-RESEARCH: Goru/Tori (Pro) may now run round-2 DEEP RESEARCH re-research on papers 01..09 (deeper research addressing round-1 review gaps) — reference-only, save packets, delete own conversations.
2. Flow (Yui, Studio): after the M1 narration, keep generating — M2/M3 method-video narrations + more character/cosmic clips.
3. RUN BOTH IN PARALLEL. The broker serializes only the submit INSTANTS; each host uses its own IP for per-IP rate headroom.

## RAMP policy (each lane, independently, on its own IP)
- Start at the current gap; on each SUCCESSFUL accepted submit, DECREASE the Start-to-Start gap (step ~30 -> 20 -> 15 -> 10 -> 8 min).
- On the FIRST unaccepted / soft-throttle submit: step the gap back UP one notch and HOLD there (= max sustainable rate). Do NOT retry into it.
- On a HARD block (google.com/sorry / redirect / real challenge): STOP + freeze + hold + wake Hwao->Duho (account-challenge policy). NEVER interact with the challenge.

## Work queue (keep both lanes fed)
- DR (Pro): round-2 re-research each paper; then gap-filling research on the open questions.
- Flow (Studio): M1 narration -> M2, M3 method narrations -> more clips.

Rails unchanged: reference-only DR, drafts-only, invariants preserved, no publish/commit, receipts each run. WonE's account-free round-2 (06/07/08) + verification continue regardless.
