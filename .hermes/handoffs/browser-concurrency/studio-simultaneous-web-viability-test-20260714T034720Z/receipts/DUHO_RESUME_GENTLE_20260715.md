# DUHO_RESUME_GENTLE — block cleared; resume DR loop + Flow character at GENTLE pace

Authorized by Duho (2026-07-15 ~09:00 KST). Duho MANUALLY CLEARED the google.com/sorry block in Chrome.

## Authorization
1. Clear the broker emergency freeze (from the e2565 account-challenge stop) under this authorization; append the ledger resume entry (broker-only); VERIFY_OK.
2. Resume the DR manuscript loop — **GENTLE PACE ONLY** (the overnight back-to-back batch is what flagged the account):
   - DR review the NOT-YET-REVIEWED papers 03-08, **ONE AT A TIME, spaced ~20-30 min between submits** — NOT back-to-back.
   - **Back off IMMEDIATELY on the FIRST unaccepted / soft-throttle submit** — do NOT retry into it; pause and report to Hwao.
   - If google.com/sorry reappears: STOP + freeze + hold for Duho (never clear/interact); Hwao wakes Duho.
   - After 03-08 reviews: continue re-research + round-2 revisions, still gently.
3. Resume Flow CHARACTER work (Yui): finish the Ingredients-to-Video clip from clips/character/master_portrait.png + a couple more character shots as an intro host. Gently — do NOT pile concurrent Flow submits on top of the DR runs (broker serializes; keep spacing).

## Rails unchanged
Serialized submits via broker lease; DR reference-only (never edits .tex); drafts-only (no publish/commit); every measured invariant preserved; ledger discipline (only broker writes during submits); ACCOUNT CHALLENGE = STOP + hold + wake Duho.
