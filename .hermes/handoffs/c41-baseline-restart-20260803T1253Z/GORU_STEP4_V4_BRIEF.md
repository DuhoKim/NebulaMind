# GORU BRIEF — Step 4 V4: the amendment unlocks coverage WITHOUT dishonesty. Hold both.

Context: V1 starved (4), V2 covered but falsified (80), V3 honest but starved again (6). Each
round over-corrected against the last critique. V4 holds BOTH constraints simultaneously —
**Duho has approved contract amendment v1.1** (read
`docs/claim_ledger_contract_v1_agn_20260703T0830Z/AMENDMENT_v1_1_unknown_zone.md` and use
`artifacts/ledger_enums_v1_1.json` in your validator):

1. **Coverage returns (V2's floor)**: content-based zone-agnostic eligibility over the p4/p3
   cohort — every one of those 68 papers yields ≥1 entry or a named per-paper reason.
2. **Zones stay honest (V3's discipline)**: `unknown` stays `unknown` — it is now ENUM-VALID.
   No casts, ever.
3. **Stances obey rule 7 as amended**: `supports` ONLY on finding/interpretation spans or
   per-span-adjudicated spans (receipts in `STEP4_ZONE_ADJUDICATION.jsonl`, as your V3 did — 
   adjudicate where an entry genuinely warrants supports, budget ~20–40 adjudications for the
   strongest claims). Unknown-zone entries carry `qualifies` / `mixed` / `no_info` — an honest
   contested-frontier ledger is MOSTLY these, and that is fine.
4. **Certainty keeps V3's honesty**: single-source ⇒ `emerging_sample_limited` ceiling;
   `widely_supported` only with real cross-source corroboration; countercase entries stay
   first-class (mine the strict-tension spans).
5. Keep your V3 6 entries as the seed (they are good); extend, don't restart. Rebuild → validate
   against enums v1.1 → receipt v4 → `## Repair round (V4)` section with histograms +
   adjudication stats + per-paper no-entry reasons if any. Marker: `GORU_STEP4_V4_COMPLETE_20260804`.
Expected shape: honest DOZENS, mostly qualifies/mixed at emerging/debated certainty. Do not
over-correct again in either direction.
