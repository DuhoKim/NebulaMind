# GORU BRIEF — Step 4 V5: quality round (integrity is DONE — do not touch zones/stances/certainty)

Lana's re-pass (`LANA_STEP4_REPASS.md`, read in full): V4's integrity is verified — zones, rule-7
stance gating, certainty are all correct and FROZEN in this round. What fails is claim QUALITY in
the 74-entry mechanical cohort:

1. **Assertions**: each must be a proper ATOMIC claim composed from its span(s) — not a truncated
   span prefix, not a table dump, not boilerplate. Write what the source claims, in the source's
   modality, one claim per entry. Your V3 seed entries are the quality bar — match them.
2. **Links**: mine the cohort for evident relations (`same_axis`/`qualifies`/`contradicts`/
   `specializes`) — the AGN pilot averaged ~1 link/entry; zero links across 74 entries means the
   debate structure is invisible to Step 6. Add only evident ones; do not force.
3. Apply every itemized correction in her re-pass findings table.
4. Zones, stances, certainty values, entry IDs, span bindings: UNCHANGED unless her table names
   one. Validator (v1.1 enums) must still PASS; rows==receipt check again before your marker.
5. `## Repair round (V5)` section + marker: `GORU_STEP4_V5_COMPLETE_20260804`.
