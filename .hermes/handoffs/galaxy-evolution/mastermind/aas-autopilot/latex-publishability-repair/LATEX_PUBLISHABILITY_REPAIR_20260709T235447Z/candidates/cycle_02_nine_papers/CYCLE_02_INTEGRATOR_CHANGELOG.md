# Cycle 02 Integrator Changelog

- Added explicit `Table~\ref{tab:selection-cascade}` callouts in all nine candidate manuscripts.
- Added explicit figure references in the main result paragraphs, including the RP-1 `fig-bpt` and `fig-offsets` callouts.
- Reworded a few dense interpretation sentences to improve AAS line breaking in the manuscripts with layout warnings.
- Standardized bibliography list widths from `\begin{thebibliography}{}` to `\begin{thebibliography}{99}` across all nine files.
- Kept all measured values, sample sizes, and caveats unchanged.
