Cycle 03 integrator changelog

- Rephrased repeated optical-baseline boilerplate across all nine manuscripts so each introduction reads more naturally while preserving association-only language.
- Added spaces after commas in dense `\citep{...}` blocks to improve AAS line breaking and reduce underfull box risk.
- Smoothed the RP-1 measurements prose to help narrow-column wrapping without changing any measured values or thresholds.
- Kept all numeric results, sample sizes, and real-data limits unchanged.
