Cycle 04 integrator edits:
- Normalized the shared SDSS selection-table redshift row in all nine candidate TeX files by moving `0.02 < z < 0.12` into math mode.
- Replaced raw `\rm sSFR` formatting with `\mathrm{sSFR}`-based math in all affected manuscripts.
- Tightened the environmental-quenching result paragraph to address the lone underfull `\hbox` warning.
- Preserved all reported counts, fractions, offsets, and other measured values.
