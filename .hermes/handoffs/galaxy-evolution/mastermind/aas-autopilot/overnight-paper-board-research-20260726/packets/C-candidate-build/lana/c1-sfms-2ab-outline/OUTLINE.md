# C1 — Star-Forming Main Sequence (run `2ab3c92eea8a`): Structural Outline

AI_DRAFT_NOT_HUMAN_GOLD

STRUCTURAL OUTLINE — NOT A CANDIDATE PAPER; NO SYNTHETIC RESULTS

- Dispatch marker: `OVERNIGHT_PAPER_BOARD_PACKET_C_LANA_BRIEF_V1`
- Implements Hwao `HWAO_PACKET_A_CANONICAL_DECISION_V1` (this run is a separate SFMS output; it is NOT the MZR candidate target).
- Purpose: a section skeleton that scaffolds an SFMS paper from run `2ab3c92eea8a`. Every quantitative slot is a placeholder. No slope, normalization, scatter, turnover, or SFR / M* / O/H value is stated or invented.

## Source facts (the ONLY science the source provides — verbatim from `2ab3c92eea8a.json` / result)
- **method** = star-forming main sequence (`spec.method = scaling-relation-evolution`; median relations)
- **data** = TNG100 (`spec.data_sources = ['tng']`)
- **N** = 23,060 galaxies
- one figure: `result.png` (`artifacts = ["result.png","history.json"]`) — referenced, not copied or modified
- `spec.outputs = []` — **no draft was ever produced** for this run
- `result.summary` (verbatim): "Star-forming main sequence — median relations for TNG100 (23,060 gals)."

## Provenance metadata (verbatim, non-result — from run JSON)
- `id = 2ab3c92eea8a`, `status = done`, `spec.topic = main-sequence-quenching`, `spec.topic_source = frontier-map`
- run log (verbatim, provenance for data=TNG100): `"loading TNG100-1 z=0 fields: sfr"` … `"done"`
- Source integrity: `2ab3c92eea8a.json`, `2ab3c92eea8a/result.png`, `2ab3c92eea8a/history.json` SHA-256 re-verified against `baseline/INPUT_SHA256.txt` = OK (no drift).

## Placeholder convention
Any number, relation, slope, normalization, scatter, turnover mass, or SFR / M* / O/H value reads literally:
`TO BE COMPUTED — NOT IN SOURCE`

## Section skeleton

### Title
`TO BE COMPUTED — NOT IN SOURCE`  *(working scope only: the z=0 star-forming main sequence in TNG100; not a finalized title)*

### Abstract
*(placeholder)* Scope: star-forming main sequence, median relations, TNG100, N = 23,060 galaxies; one figure (`result.png`). Quantitative summary (slope / normalization / scatter / turnover): `TO BE COMPUTED — NOT IN SOURCE`.

### 1. Introduction
*(placeholder)* Motivation for the star-forming main sequence as a galaxy scaling relation and where this z=0 TNG100 measurement sits. Any literature or comparison values: `TO BE COMPUTED — NOT IN SOURCE`. No citations are asserted — the source run carries no citation/reference list for `2ab3c92eea8a`.

### 2. Data and Method
*(placeholder)* Data: TNG100 (`spec.data_sources = ['tng']`; run log records TNG100-1 z=0 SFR fields). Sample: N = 23,060 galaxies. Method: star-forming main sequence via `scaling-relation-evolution`; median relations. Mass range, binning, SFR/M* definitions, selection cuts: `TO BE COMPUTED — NOT IN SOURCE`.

### 3. Result
*(placeholder)* The source provides exactly one figure and one summary line:
> "Star-forming main sequence — median relations for TNG100 (23,060 gals)." (`result.png`)

- Main-sequence slope: `TO BE COMPUTED — NOT IN SOURCE`
- Normalization / zero-point: `TO BE COMPUTED — NOT IN SOURCE`
- Scatter: `TO BE COMPUTED — NOT IN SOURCE`
- Turnover / high-mass bending mass: `TO BE COMPUTED — NOT IN SOURCE`
- Any SFR, M*, or sSFR value: `TO BE COMPUTED — NOT IN SOURCE`

### 4. Caveats
*(placeholder)* Automated single-selection measurement; the source run produced **no draft** (`spec.outputs = []`); this outline invents no result. Calibration/systematics, resolution/aperture effects, comparison to observations: `TO BE COMPUTED — NOT IN SOURCE`. This is a scaffold, not a finding.

## No-invention attestation
No slope, normalization, scatter, turnover, or SFR / M* / O/H number is stated anywhere above. Only the source facts and verbatim provenance are asserted; every quantitative slot is `TO BE COMPUTED — NOT IN SOURCE`. No source, public, DB, or product byte was changed; the source figure is referenced only.
