# Cycle 26 Review Response

Implemented in the candidate-copy TeX files:

- Updated the flagship title to label RP-1 as a `Selection-Aware Pilot Matched-Control Study`.
- Recast the flagship abstract and conclusion to describe the result as a pilot association paper while preserving the existing claim boundary.
- Moved the moderate mass-redshift caliper dimensions into the flagship main text: `|\Delta\log M_\star|\leq0.05` and `|\Delta z|\leq0.002`.
- Added the matched-pair count to the flagship Figure 2 caption: `N=8,146` pairs.
- Kept the flagship numeric results unchanged: `8,146` pairs, `-1.309 dex`, `[-1.334,-1.283]`, `60,000` cached rows, `249,917` strict parent, and `24.0%` coverage.

Supplement status:

- I did not make additional edits to the supplementary atlas because it already reads as one coherent atlas, not eight standalone papers.
- I left all table counts, figure paths, and citation roles unchanged.

Refused as needing new data:

- Running the flagship on the full `249,917`-galaxy parent sample.
- Adding morphology or aperture-fraction controls.
- Resolving Seyfert versus LINER contamination with additional diagnostic lines.
- Converting denominator or proxy notes into causal physical-feedback claims.
- Changing any reported offsets, pair counts, or coverage numbers.

Safety note:

- No source package outside the approved candidate root was modified.
- No public/live roots, databases, deployments, or external submission systems were touched.
