# CYCLE 41 Actual Research Response

Edited only the allowed candidate-copy TeX files:
- `flagship_rp1/aastex/rp1_flagship_polished.tex`
- `supplementary_denominator_atlas/aastex/supplementary_denominator_atlas.tex`

## What changed
- Normalized the flagship wording to use `catalog median sSFR proxy` and `catalog median sSFR-proxy offset` where the manuscript was still using looser `catalog sSFR` phrasing.
- Tightened the flagship scope language so the offset is described as a `fiber-centered` and `morphology-uncontrolled` association inside the fixed optical denominator.
- Kept the numerical claims unchanged, including `8,146` pairs, `-1.309 dex`, `[-1.334,-1.283]`, `60,000` cached rows, `249,917` strict-parent rows, and `24.0%` coverage.
- Added one small clarification in the supplement package-decision section so it explicitly reads as a `catalog-proxy atlas` rather than a mechanism paper.

## What I refused
- I did not add any mock, synthetic, fake, placeholder, or toy data.
- I did not add any new quantitative claims.
- I did not add any new citations.
- I did not change figure paths, core association-only boundaries, or any numeric invariants.
- I did not edit any files outside the two allowed TeX manuscripts and this response file.

## Verification
- I read back the modified spans to check for LaTeX-safe prose changes.
- I did not run a full TeX compile in this environment.
- The package remains framed as an optical association pilot plus denominator/proxy atlas, not a causal mechanism paper.
