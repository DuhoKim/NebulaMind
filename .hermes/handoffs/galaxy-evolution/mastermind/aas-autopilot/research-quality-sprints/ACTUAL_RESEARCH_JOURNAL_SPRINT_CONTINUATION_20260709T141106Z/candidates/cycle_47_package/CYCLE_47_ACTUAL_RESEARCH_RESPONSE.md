# CYCLE_47_ACTUAL_RESEARCH_RESPONSE

## What changed
- Tightened the flagship abstract and scope language to foreground `fiber-centered` and `morphology-uncontrolled` framing.
- Added explicit dropped-cache structural columns in the flagship and supplement prose: `petroR50` and `petroR90` alongside the existing structural proxies.
- Strengthened the flagship conclusion to separate the SDSS optical association pilot from future multiwavelength validation using a clearer `Future physical validation requires...` lead-in.
- Elevated the supplement's 10th-neighbor discussion into a standalone `Fiber-collision warning` paragraph and clarified that the statistic is an ordinal projected-rank proxy with no line-of-sight clipping beyond the redshift slice.
- Recast the supplement's maintenance-heating and CO/HI follow-up language so the radio, X-ray, gas, and IFU references remain explicitly future-data motivation rather than measured results.

## What I refused
- I did not add any new numbers, sample sizes, results, or derived metrics.
- I did not introduce any mock, synthetic, fake, placeholder, or toy data.
- I did not change figure paths, selection counts, or the core association-only claim boundary.
- I did not add new citations that were not already present in the manuscript/bibliography or supported by the review reports.

## Verification
- The requested edits were limited to the two candidate-copy TeX files and this response file.
- I attempted a local TeX compile check, but `latexmk` and `pdflatex` are not installed in this environment, so compile verification could not be completed here.
