# CYCLE_15_ACTUAL_RESEARCH_RESPONSE

## What changed
- Updated the RP-1 abstract to state that the `-1.309 dex` offset is highly degenerate with the mass-morphology relation because morphology is not controlled in the match.
- Tightened the RP-1 interpretation section to state that the Kewley et al. (2006) demarcation removes the retired/LINER-like low-ionization tail, so the larger offset is driven in part by that broader branch rather than only Seyfert-like excitation.
- Added an explicit fiber-collision warning in the supplement neighbor-count baseline: the SDSS `55-arcsec` limit biases the index in dense environments and prevents use as a physical density metric without forward-modeled corrections.
- Added an explicit maintenance-heating wording guard in the supplement: the broad optical BPT fraction is an observational baseline pool, not the active duty cycle.
- Corrected the supplement gas-follow-up citation language to point to real survey benchmarks for CO and HI follow-up: xCOLD GASS and xGASS.
- Added the xCOLD GASS bibliography entry to the supplement so the new CO citation compiles cleanly.

## What was refused
- No new quantitative claims were added.
- No mock, synthetic, fake, placeholder, or toy data were introduced.
- No new result, sample count, interval, or causal statement was invented.
- No file outside the two approved TeX candidate copies was edited.

## Safety and provenance
- The RP-1 manuscript remains an association-only SDSS optical BPT/sSFR pilot.
- The supplement remains a denominator/proxy atlas, not a causal feedback paper.
- The core invariants were left intact: `8,146` matched pairs, `-1.309 dex`, `[-1.334,-1.283]`, `60,000` cached rows, `249,917` strict parent, and `24.0%` coverage.
- Real-data-only wording was preserved throughout.
