# CYCLE_49_ACTUAL_RESEARCH_RESPONSE

## What changed
- Tightened the flagship abstract and opening claim boundary so the result is explicitly described as a `fiber-centered, morphology-uncontrolled` association, not a causal or morphology-resolved result.
- Updated the flagship interpretation and conclusion language so the `-1.309 dex` result is consistently framed as a `fiber-centered` and `morphology-uncontrolled` offset within the retained cache.
- Kept all numeric claims unchanged: `8,146` pairs, `-1.309 dex`, `[-1.334,-1.283]`, `7,867` pairs, `-1.318 dex`, `-0.744 dex`, `-0.763 dex`, `60,000` cached rows, `249,917` strict parent, and `24.0%` coverage.
- Strengthened the supplement’s provenance language by changing the environment ranks to `fiber-collision-biased projected-rank proxies` and renaming the environment jet row to `neighbor-rank-stratified`.
- Reworded the supplement’s radio-jet figure caption from `high- and low-density` to `high- and low-index` to avoid implying a physical density measurement.

## What was refused
- I did not add any new quantitative claims, sample counts, or derived values.
- I did not add any new citations.
- I did not change any figure paths, table values, or core association-only claim boundaries.
- I did not introduce mock, synthetic, placeholder, toy, or invented data.

## Why
- The reports required stronger provenance language around the optical-only denominator and the fiber-centered/morphology-uncontrolled nature of the flagship association.
- The supplement needed a clearer boundary between the 10th-neighbor ordinal proxy and any physical density interpretation.
- The edits were kept minimal to preserve compilability and avoid altering any real-data inventory-backed results.
