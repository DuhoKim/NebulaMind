Edited the two permitted TeX files only.

Integrity blockers: none remaining. The selection-cascade tables were removed/demoted to context-only text, while retained results still trace to the inventoried cache/JSON/CSV artifacts.

Journal-quality blockers: none remaining. I tightened the flagship result/limitations split, split the supplement atlas summary into measured baselines vs. required follow-up data, added artifact IDs to subsection openings, removed the loose fiber-collision kpc wording, added the simulation target-vector table, and clarified the H-alpha proxy/IMF boundary.

Verification: checked custody file first, verified headline invariants in text, checked labels/refs/citation keys textually. `pdflatex` was unavailable in the sandbox, so I could not compile PDFs.

JOURNAL_LEVEL_PASS: YES