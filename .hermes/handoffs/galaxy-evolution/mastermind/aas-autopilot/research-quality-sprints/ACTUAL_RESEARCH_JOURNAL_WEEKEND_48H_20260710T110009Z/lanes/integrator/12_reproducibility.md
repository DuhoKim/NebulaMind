Edited both allowed TeX files.

Integrity blockers: the candidate still lacks executable analysis code or a frozen command recipe to regenerate the JSON/CSV/bootstrap outputs from the package alone. I added explicit manuscript wording that full regeneration from the candidate is not claimed.

Journal-quality blockers addressed: added explicit flagship/supplement provenance hashes, added the flagship anchor rows to the supplement provenance table, tightened Data Availability, added missing-observable caveats and literature pointers with identifiers, and marked unverified bibliography rows explicitly instead of inventing identifiers.

Validation: citation targets resolve by static check; numeric invariants and custody hashes remain preserved. PDF build could not be run: `pdflatex` is unavailable and `tectonic` panicked before compilation.

JOURNAL_LEVEL_PASS: NO