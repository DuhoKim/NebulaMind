# Hwao-director progress — prose-rich evidence/trust wiki upgrade supervision

Order marker: AUTOPILOT_PROSE_EVIDENCE_TRUST_WIKI_UPGRADE_20260708T041216Z
Continuation marker: GE_AUTOPILOT_IDLE_CONTINUATION_V1
Role: Hwao-director — SUPERVISOR + final no-apply packet owner (NOT solo content author; prose-rich candidate authoring is method-lane work). Snapshot 2026-07-08 ~04:14Z (13:14 KST).
Class: bounded docs/static, additive, no-apply. Hard gates closed.

## State at start
Order fresh: 0 candidates in any `prose-evidence-trust-upgrade/` dir; autopilot watch-ticking (idle-continuation 04:13Z); method panes idle. Estimated run ~2h. Method lanes author; director coordinates + assembles the final packet.

## Corrected caveats to carry into the final packet (from RESOURCE_SURGE_SYNTHESIS_20260708T022147Z — supersede my earlier wording)
1. **RESTART/VISIBILITY (important — my prior packets were wrong):** new static subdirectories under the running Next `public/` tree may keep returning **404 after a live-root mirror until a separately approved `:3000` restart**. Confirmed: previously-mirrored `same-format-rebuild/` files exist on disk yet still 404 through the running `next start`. The final packet must **NOT** promise "served immediately / no restart"; it must use the corrected approval wording (restart is a separate deploy/restart gate).
2. **M1 label-fix:** use the surge's additive P1 label-fix wording (`…/evidence-trust-rebuild/evidence-trust-preview-p1-label-fix-20260708T022147Z.html`, 38,442 B) — 27 unbound chips relabeled from confusing `provenance` → `no local evidence / unbound`. Only 3/30 chips have local evidence; the 27 are unbound-here, NOT high-trust.
3. **M2 totals:** evidence `28060` is an `accepted_limited` caution with **target None** (no current claim); global totals (`accepted_limited: 20`, `cited_positions: 22`) are defensible if a note explains per-claim arrays sum lower because 28060 is not assigned to a claim. Keep the 7 cite-unmatched honest.
4. **M3:** docs-only debate-map trust framing + local provenance navigation; show unmatched / PENDING_RECHECK explicitly; **no** product claim/cite binding (P3 stays a separate CLOSED gate).

## Binding quality bar (every lane)
Prose-first (explanatory lead + per-section/claim narrative + limitations), evidence boxes (supported / limited / unbound-unmatched), plain trust labels with on-page vocabulary (do NOT pretend the three methods share a trust scale), and strict no-invent (no fabricated evidence/IDs/DOI/ADS/product-cite/claim/trust — say "unbound here" / "requires product DB or P3 gate"). Static-safe only (no scripts/fetch/XHR/WebSocket/inline-handlers/product-or-DB routes/external runtime deps); M1 arXiv links allowed only where already in local data, marked external. Output must be an openable HTML page, not a JSON dump.

## Expected per method (new `prose-evidence-trust-upgrade/` dir)
`wiki-prose-evidence-trust-upgrade-…html` + `page-content-…md` + `evidence-trust-coverage-map-…json` + `manifest-…json`, plus method Goru (coverage/link/safety counts), Kun (static/link/checksum), Lana (no-overclaim/prose), Tori (receipt) checks.

## Plan to final no-apply packet
1. Autopilot dispatches the method lanes (idle now). Each authors its prose-rich upgrade candidate + method checks.
2. Director independently verifies each: prose present (not a dump), static-safe, no-invent, honest trust labels + limitations, M1 uses label-fix wording, M2 totals note, M3 docs-only.
3. Write the final packet at the required path with exact source paths + bytes + sha256, the **restart/visibility caveat**, and the **corrected approval wording** (per the surge synthesis). Expected `READY_FOR_USER_APPROVAL`.

## Safety ledger (this pass)
Read-only inspection + this one progress note. Zero live-root writes, restart/deploy, product DB/SQL, `/api/pages`, `page_versions`, live-wiki publish, git, cockpit/global/Baseline/shared-parent, cloud/GCP/OAuth/secrets, browser, cron; zero invented data; zero Method3 P3 binding; zero keystrokes into panes. Hard gates closed.

AUTOPILOT_PROSE_EVIDENCE_TRUST_WIKI_UPGRADE_20260708T041216Z
