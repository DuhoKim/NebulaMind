# HWAO_ROOT_CAUSE — C1r Deep Research failure, offline root-cause investigation

Brief: `dr-c1r-root-cause-20260712T163156Z/HWAO_BRIEF.md` ("investigate on why it failed")
Sealed packet (read-only, unmodified): `../gemini-dr-revised-canary-20260712T045317Z`
Investigator: Hwao (coordinator-led, offline/local-only; no browser, no network, no Gemini action, no retry)
Written: 2026-07-13 · This file is the single report required by the brief.

## 0. Custody and reproducibility (verified before analysis)

All sha256 recomputed locally and matched `runs/c1r/RUN_RECEIPT.json` custody block exactly:
`body.md` = `8a130c5a…6bc00`, `structured_capture.json` = `2d10e34a…ec468`, `validator_result.json` = `34f525a5…c91f4`, `rendered_body.html` = `78ed129c…2bbc`, and `prompt/C1r.md` is byte-identical to `runs/c1r/prompt_submitted.md` (`fffac44f…e1ef`).
The sealed validator was re-run unmodified from the immutable inputs (`validator/run_validator.py --body runs/c1r/body.md --structured runs/c1r/structured_capture.json --spec validator/contract_spec.json`; output written only to this investigation dir as `_tmp_revalidation.json`): **85 findings, 54 FAIL, byte-order-identical to the sealed `validator_result.json`, overall FAIL.** The pipeline is deterministic; nothing below disputes what the validator computed — only what its inputs failed to represent and what its heuristics over-matched.

The 54 = C2: 8 `EMPTY_TABLE_CELL` + 1 `BAD_STRUCTURE` · C4: 33 `UNCITED_CELL_CLAIM` + 2 `UNCITED_CLAIM` · C6: 3 `MISSING_QUALIFIER` + 6 `UNLABELED_COMPARISON` · C7: 1 `MISSING_FROM_BODY` (37-URL evidence set). Plus 28 MANUAL_REVIEW_REQUIRED (not failures) and passes on C1, C5, C8.

## 1. Decisive discovery — the citations were there, in the DOM, at per-cell granularity (§2 Result cells excepted — see §2a)

`runs/c1r/rendered_body.html` (capture root `.markdown.markdown-main-panel`, `capture_metadata.json`) contains **108 Gemini Deep-Research citation chips** — `<source-footnote><sup class="superscript" data-turn-source-index="N">` — attached inside the logical units the validator scored as uncited, with one class excepted (§2 Result cells, §2a):

| Region | Chips | Placement evidence (byte offsets in rendered_body.html) |
|---|---|---|
| §1 Calibration ledger | **40** = 8 rows × 5 cells — every cell incl. every one of the 32 flagged claim-bearing cells | region 586–63919 |
| §2 validation ledger | **8** = one inside each of the 8 "empty" Citation cells (`<td>…<source-footnote…data-turn-source-index="27">` etc.; cell text empty); **zero** chips in any Result cell (index 2) | e.g. offset ~66650 (IllustrisTNG row, cell 5, chip 27) |
| §3 bullets | **3** = one per bullet, incl. flagged `paragraph_25` | offset ~83424 (chip after "…theoretical validation of the underlying physical model.") |
| §4 observables map | **9** = one on every CALIBRATED/EMERGENT status cell | offset ~94438 ("EMERGENT " + chip 35) |
| §5 Gaps | **2** — GAP1→chip 30, GAP3→chip 36; GAP2/GAP4 carry the absence token instead | S5 event sequence: GAP1@135→chip30@1198; GAP3@2073→chip36@3125; tokens @2028, @4601 |
| Links ledger | **46 chips + 46 `<a href>` anchors** (the only anchors in the document; `td` elements containing `<a>`: **0**) | region 115452–221062 |

The chips are **text-empty and carry no `href`** — they reference the Deep-Research source panel by index. `validator/structured_capture.js:18-23` serializes links via `element.querySelectorAll('a[href]')` **only**, so every body cell/bullet/GAP captured `links: []` and chip-only cells captured `text: ""`. The validator then behaved correctly on blind inputs: 8 `EMPTY_TABLE_CELL` (chip-only Citation cells), 33+2 C4 uncited findings (chips invisible: no `UNCITED_NOT_USABLE`, no `links`, no `[`, validator.py:122,142), and C7 `MISSING_FROM_BODY` with body_urls=∅ vs 37 unique ledger URLs.

**The chip→URL mapping is fully recoverable offline from the sealed capture itself.** Each ledger row renders as chip(N) + literal anchor: walking the ledger region yields 46 (index→URL) pairs, 37 unique indices (1–37), **zero inconsistent pairings** — a deterministic map (e.g. 27→`oamonitor.ireland.openaire.eu/...stx3040`, 30→`arxiv.org/abs/2410.19905`, 36→`arxiv.org/html/2508.04907v1`). Every chip in Sections 1–5 therefore resolves to a checkable URL without any network access. `postmortem/INDEPENDENT_AUDIT_ADDENDUM.md` §"Representation boundary" pre-committed the packet's own interpretation: *"A link attached to the same logical DOM cell counts as a checkable same-cell citation under C1r."* Under that pre-declared rule, 41 of the citation-family findings — those on chip-bearing units (8 C2 empty cells, 31 §1 cells, 2 paragraphs) — are **capture-layer false positives**, not model omissions. The rule does not cover the two §2 Result-cell findings (no chip exists in those cells) or the whole of the C7 finding — see §2a.

## 2. Ranked root-cause table

| Rank | Root cause | Findings explained | Share | Class | Confidence |
|---|---|---|---|---|---|
| RC-1 | **Capture blind spot for DR chip citations** — `structured_capture.js` serializes only `a[href]`; Gemini emitted per-unit citations as text-empty `source-footnote/sup[data-turn-source-index]` chips (108 total, per-cell granularity, mapping recoverable) | 8 C2 `EMPTY_TABLE_CELL` + 31 §1 C4 `UNCITED_CELL_CLAIM` + 2 C4 `UNCITED_CLAIM` = **41/54** | 75.9% | capture/DOM-representation defect (contract citation-channel ambiguity contributing, §4-Q3) | CONFIRMED |
| RC-2 | **Genuine model noncompliance — citation locality + comparability** — (a) §2 Result cells, rows 21/22: validation claims with no same-cell citation in any form; all 8 §2 chips sit in the dedicated Citation cells, which C4 (C1r.md:86-92) verbatim rules insufficient ("a citation in the Simulation cell or a dedicated Citation cell does not satisfy the other claim-bearing cells"); (b) §1 rows 6/7/10/11/12: simulation-observation *agreement* claims in the "Explicitly emergent" cells (body.md:48, 71, 136, 161, 182) without the token C6 requires outside §2 (C1r.md:110-113, no attribution exemption); (c) GAP1 (body.md:263) predictive-failure comparison without token — chip 30 cures citation, not comparability | 2 C4 `UNCITED_CELL_CLAIM` (rows 21/22, cell 2) + 5 C6 `UNLABELED_COMPARISON` (§1) + 1 C6 (paragraph_41) = **8/54** (+6 identical §2 Result-cell defects false-negatived, rows 15–20) | 14.8% | model output defects (the 5 §1 hits carry row-level-anchoring imprecision and a contract-design-tension caveat, §5-R4) | CONFIRMED |
| RC-3 | **Validator false positives** — (a) four-qualifier trigger fires on the *word* "fraction/incidence" (validator.py:206) with no quoted numeric value present (row 6 "fraction of available Type II Supernovae energy"; rows 21/22 "cluster gas fractions" as observable names); (b) `BAD_STRUCTURE`: `c2_order_ok` is zeroed by every empty-cell hit (validator.py:56-57) and re-reported as a structure failure (validator.py:71-72) though heading order was verifiably perfect (blocks heading_3→13→23→30→40→42; evidence string literally "Order bad or missing: set()") | 3 `MISSING_QUALIFIER` + 1 `BAD_STRUCTURE` = **4/54** | 7.4% | validator defects | CONFIRMED |
| RC-4 | **C7 mixed — genuine verdict, capture-inflated evidence** — chip-aware mapping still leaves 12 ledger sources never cited in the body ({2,5,8,9,13,16,18,23,24,29,31,33}), plus 9 duplicate rows (46 rows / 37 sources) and 46 blank short-name fields; but 25 of the 37 URLs in the sealed evidence set are capture artifacts (their chips exist in-body) | 1 `MISSING_FROM_BODY` = **1/54** | 1.9% | genuine clause failure, overstated evidence | CONFIRMED |

**Bottom line (re-adjudicated 2026-07-13): 45 of 54 findings are pipeline artifacts (41 capture + 4 validator); 8 are genuine model noncompliance; 1 is a genuine C7 failure with two-thirds-inflated evidence. Six further genuine §2 Result-cell defects (rows 15–20) were false-negatived, so the corrected deterministic genuine set is ≈15 findings (8 C4 + 6 C6 + 1 C7) plus ledger hygiene. The earlier headline "exactly 1 of 54 genuine" is withdrawn.**

### 2a. Cross-check adjudication vs `TORI_ROOT_CAUSE_CORRECTED.md` (Duho-directed, 2026-07-13)

(1) **§2 Result-cell C4 findings (rows 21/22): GENUINE — accepted.** C1r.md:86-92 is explicit, and the raw HTML shows zero chips in any Result cell; rows 15–20 share the identical defect undetected because C4's cell regex requires a calibration keyword that only rows 21/22's Result text contains — 6 false negatives. (2) **C7: MIXED — accepted.** §3 already carried the 12-orphan/duplicate/short-name residue; the ranked table and headline now match it. (3) **Consequential correction by the same literal-contract logic:** the 5 §1 `UNLABELED_COMPARISON` findings, previously classed as pure validator over-breadth, are genuine — each flagged row's emergent cell asserts agreement with observations, C6 has no attribution exemption, and rows 5/8/9 escaped because they genuinely lack such claims (the row-level hits align with true cell-level violations). This converges with Tori's 45-artifact/9-genuine split; remaining differences are framing only (the 3 qualifier false positives; remediation architecture).

## 3. Genuine model defects (chip-aware inventory — what a corrected pipeline would still fail or flag)

1. **C6 GAP1 missing comparability token** (RC-2 above) — deterministic FAIL, stands.
2. **C4 §2 Result-cell citation locality — 8 cells (rows 21/22 flagged; rows 15–20 false-negatived):** every §2 Result cell asserts a validation result with no same-cell citation in any form; the populated dedicated Citation cell does not cure it (C1r.md:86-92). The 5 §1 emergent-cell unlabeled comparisons are likewise genuine (§2a).
3. **C7 bidirectionality, real residue:** 12 of 37 ledger sources are never chip-cited anywhere in the body (indices {2,5,8,9,13,16,18,23,24,29,31,33}) — genuine orphan rows under the C1r bidirectional rule (C1r.md:115-117). The sealed finding's 37-URL evidence set is 68% capture artifact, but a corrected run still fails C7 on this 12-source set.
4. **Ledger hygiene (violates C7 "unique / no duplicates" and the ledger line format, C1r.md:58-61):** 46 rows for 37 sources — duplicate rows ×9 (497/1/146 ×3, 526/4/6103 ×3, 475/1/648, 450/2/1937, simba.roe.ac.uk, 470/1/1121, 465/3/2936 each ×2); `<short name>` field empty in **all 46 rows** (a chip occupies the slot; innerText `" | https://… | QUARANTINED_PENDING_LOCAL_CHECK"`, body.md:270-315); index 14 vs 29 are the same paper as `article/` vs `article-abstract/` URL variants (a normalizer-invisible near-duplicate). *Not counted in the 54* (the sealed C7 check uses sets and has no duplicate/short-name logic — silent under-enforcement).
5. **Trivial:** `NONE_FOUND.` with a period (body.md:88) vs the exact-token rule (escaped detection only via the ≤3-word guard, validator.py:141).
6. **Manual-layer semantic concerns (never reviewed — gate correctly recorded NOT_ELIGIBLE after deterministic FAIL, `manual_review_gate.json`):** COMPARABILITY = `MATCHED_SELECTIONS` in all 8 §2 rows — uniform token-satisficing; at minimum the FLAMINGO kSZ-stacking row is doubtful. All 8 "Overlap with a Section-1 calibration target" cells say "No" while the FLAMINGO row itself names the §1 calibration target in its Result text. §2 citation quality: IllustrisTNG row resolves to an OpenAIRE aggregator page (chip 27), and BAHAMAS shares chip 30 (`arxiv.org/abs/2410.19905`) with the FLAMINGO row — a probable mis-citation. These stay manual-review items, per contract.

Model *positives* the 54-count obscures (relevant to Q6): C1 meta header emitted verbatim incl. the exact Model line (the C1-run "Gemini 1.5" hallucination did not recur); C5 zero banned-register hits (C1 had "firmly established"); C8 marker exactly once as final line; all six section headings exact and ordered; §4 uses the composite `NOT_REPORTED — NONE_FOUND` everywhere applicable (the addendum's item-3 fix worked); `UNCERTAINTY_NOT_QUOTED_BY_SOURCE` applied ~24×; the four-qualifier `TRACER=…; SELECTION=…; DENOMINATOR=…; REDSHIFT=…` syntax used 6× where numeric-ish fractions appear (e.g. body.md:71,149,174); every GAP line carries citation-or-token (chip-aware); per-unit citation attachment at 102 body chip placements.

## 4. Answers to the brief's questions

**Q1 — independent defects vs repeated manifestations.** The 54 collapse to the 4 root causes of §2: RC-1 is one blind spot manifested 41 times; RC-3 is two heuristic defects producing 4 findings (incl. the BAD_STRUCTURE double-count); RC-2 is 8 genuine findings from three defect families (§2 Result-cell citation locality, §1 emergent-cell comparability, GAP1); RC-4 is one mixed C7 finding. The *complete* independent genuine-defect list is ≈5 families: §2 Result-cell citations (8 cells incl. 6 false negatives); unlabeled comparisons (6); ledger orphans (12); ledger duplicates/short-name format; semantic-layer items pending manual review. Nothing supports "54 independent defects."

**Q2 — classification.** Genuine model noncompliance: 8/54 (RC-2), plus the mixed C7 (RC-4), 6 false-negatived §2 Result cells, and the out-of-54 ledger items in §3. Capture/DOM-representation defects: 41/54 (RC-1; chip serialization + chip-only cells captured empty; secondary capture quirks: §3 bullets double-captured as `li`+nested-`p` twins — blocks 24/25, 26/27, 28/29 — and the DR answer rendered all four GAP lines as spans of a single `<p data-path-to-node="11">`, so per-GAP-line checks operated on one merged block). Prompt/contract design defects: contributing, not primary — the citation-channel ambiguity (Q3) and the §1-wording collision + "any quoted fraction" vagueness (Q5). Validator defects: 4/54 as over-counting (RC-3) plus silent *under*-enforcement the other direction (all 10 §4 status cells skipped by the ≤3-word guard; C4 skips `bullet`-type blocks entirely — validator.py:121 — and only caught §3 bullet 1 via its duplicated paragraph twin; C4 keyword lottery: the cell regex requires a calibration keyword, so §2 Result-cell rows 21/22 tripped while rows 15–20 with the identical defect escaped (6 false negatives); C2 GAP check satisfied for the merged block by GAP2/GAP4's token; C7 has no duplicate/short-name checks and its normalizer misses `pdf/` and `article-abstract` variants; C5's attributed-quote rule is "any `"` in block" — latent hole, no impact here).

**Q3 — were citation links present but lost, or truly omitted?** **Present and lost — except in §2 Result cells, where they were truly absent.** Gemini attached per-unit citations at per-unit granularity in four of five sections (§1 table: one chip in every cell of all 8 rows; §2: one chip inside each dedicated Citation cell only — the claim-bearing Result cells carry none, which C4 (C1r.md:86-92) rules a genuine same-cell omission, see §2a; §3: per bullet; §5: on both non-token GAP lines; §4: on all 9 status cells) — but in the Deep-Research chip channel (`data-turn-source-index`), not as literal text in the four forms C4 enumerates (arXiv ID / DOI / ADS bibcode / URL), and not as `a[href]` (zero anchors inside any `<td>`). The capture serialized neither the chips nor their index→URL mapping, so the validator saw nothing. Under the addendum's pre-declared rule (same-cell DOM link counts), these are same-cell checkable citations and the findings are capture false positives; under a strict literal-text reading of C4, the model deviated — but as **one systemic channel choice**, not 40+ independent omissions. Either way the mapping is recoverable offline from the sealed HTML with zero inconsistencies (§1), so nothing about this requires a new run to resolve.

**Q4 — was `BAD_STRUCTURE` genuine?** No. Validator logic (RC-3). Section presence and order were perfect (all six exact titles, capture blocks heading_3/13/23/30/40/42 strictly ascending; `c2_missing = set()` in the finding's own evidence). The flag was flipped by the eight chip-empty Citation cells at validator.py:56-57 and re-emitted as a second, derived failure at validator.py:71-72. It double-counts RC-1 and names a defect ("order bad or missing") that did not exist.

**Q5 — were the C6 `UNLABELED_COMPARISON`/`MISSING_QUALIFIER` findings required by the literal prompt?** Split verdict — 3 of 9 over-broad, 6 genuine (§2a item 3). The 3 `MISSING_QUALIFIER` hits fire on the *word* "fraction" with no quoted numeric fraction in the row (row 6: "fraction of available Type II Supernovae energy"; rows 21/22: "cluster gas fractions" as a named observable); the prompt's rule binds "any **quoted** fraction or incidence" (C1r.md:104-106) — no value is quoted, and the model demonstrably used the four-qualifier syntax where fraction-like quantities do appear (6 occurrences). The 6 `UNLABELED_COMPARISON` findings **were** required by the literal prompt: the five flagged §1 rows assert simulation-observation agreement in their "Explicitly emergent" cells (body.md:48, 71, 136, 161, 182) and GAP1 asserts a predictive-failure tension (body.md:263), each without the token C6 requires outside §2 (C1r.md:110-113, no attribution exemption). The residual validator defect there is anchoring only: hits are row-level while the violation lives in cell 3, and calibration-target wording alone (rows 5/8/9, which genuinely lack agreement claims) must not fail — §5-R2(c).

**Q6 — did the one-shot exceed a practical complexity/capacity boundary?** Not in the "model collapsed under clause load" sense — the evidence points the other way. Chip-aware, the model satisfied 7 of 8 clause families end-to-end, including the highest-drudgery repetitions (~24 uncertainty tokens; 8/8 comparability tokens; 4/4 GAP citation-or-token; exact header/marker/section order; the §4 composite tokens the C1 run had botched), and it attached citations at per-unit granularity 102 times in the body — *more* mechanical work than pasting literal IDs. What actually broke: (i) the pipeline's representation of the answer, and (ii) one channel conflict — the contract demanded literal in-cell citation text while the Deep-Research surface's native idiom is chips + end-of-report source list, and the model followed its product idiom (literal URLs appear only in the ledger, 46 anchors). The C1 run failed the same citation-locality family while passing most else — a stable attractor across two runs, which is behavioral/product-shaped, not stochastic capacity noise. Mild strain markers do exist (all four GAP lines emitted as one paragraph; uniform `MATCHED_SELECTIONS`; ledger duplicates/orphans/empty short names), so the one-shot is *near* a practical boundary for peripheral bookkeeping — but the 54-failure verdict measures pipeline blindness plus heuristic over-reach, not model capacity. Confidence: HIGH that "54" massively overstates noncompliance; HIGH that the citation failure is channel-mismatch rather than omission; MODERATE on how much residual bookkeeping sloppiness a maximally-tightened one-shot could remove.

**Q7 — smallest safe remediation sequence:** §5.

## 5. Remediation sequence before any future live run (all offline; no retry, no browser, no network)

R0 **Governance unchanged.** The sealed run's `RUN_FAILED` / `FAIL_CLOSED` verdict stands (fail-closed was correct given what the pipeline could see); the sealed packet stays immutable. This report changes the diagnosis, not the acceptance. No retro-acceptance of the C1r answer as evidence; all URLs remain `QUARANTINED_PENDING_LOCAL_CHECK`.

R1 **Chip-aware capture** (edit `structured_capture.js` in a NEW packet, TDD against the sealed `rendered_body.html` as fixture): serialize `source-footnote`/`sup[data-turn-source-index]` per cell/span as `chips: [N,…]`; capture the chip→URL map (from the ledger chip+anchor pairing, and from the DR source panel when live); dedupe the `li`+nested-`p` double capture; split the ledger paragraph into per-row units; leave the immutable innerText channel untouched.

R2 **Validator corrections** (same new packet; RED fixture = sealed C1r capture with pinned expected corrected findings, GREEN fixture = realistic §1 wording — the current `fixtures/clean_c1*` are sanitized and masked the §1 collision): (a) decouple `BAD_STRUCTURE` from cell/GAP findings — order/presence only, with concrete evidence text; (b) `MISSING_QUALIFIER` only when a numeric fraction/incidence value is present; (c) comparison scan per-cell, exempting §1 calibration-register statements (or downgrading §1 hits to MANUAL); (d) C4: count same-unit chips (with resolved URL) as citations, recognize `ASSERTED_ABSENCE_NOT_SYSTEMATICALLY_VERIFIED` on GAP lines, scan bullet-type blocks, drop the ≤3-word guard for §4 status cells, widen keyword regexes (calibrating|calibration|validate|tuning; match|converge); (e) C2 GAP checks per text line, not per merged block; (f) C7: chip-aware bidirectionality, duplicate-row and empty-short-name checks, normalizer extended to `pdf/` and `article-abstract` variants; (g) C5 attributed-quote triage tighter than "any quote character".

R3 **Offline re-adjudication of the sealed C1r capture** with the corrected pipeline, written to the new packet (sealed packet untouched). Expected residue if §1–§4 above are right: C4 FAIL ×8 (§2 Result cells, rows 15–22); C6 FAIL ×6 (5 §1 emergent cells + GAP1); C7 FAIL (12 orphans + 9 duplicates + short-name format); everything else deterministic-PASS with the 28+ manual items (comparability semantics, overlap column, chips 27/30 citation quality) going to a real manual review — consistent with the chip-aware replay in `TORI_ROOT_CAUSE_CORRECTED.md` (12 fails = these 9 sealed-finding survivors plus its 3 qualifier false positives, before the 6 false negatives are restored). This validated prediction doubles as the acceptance test for R1+R2.

R4 **Contract r3 surgical deltas** (no new clause families): define "simulation-observation comparison" as agreement/tension claims about results, with §1 calibration-target statements explicitly exempt; scope the four-qualifier rule to quoted numeric fractions/incidences; make the citation channel explicit — require a literal same-cell ID *and* declare whether a captured chip-with-resolved-URL satisfies C4 (recommend: literal ID required, chip accepted as MANUAL-flagged fallback); forbid duplicate ledger rows; require non-empty short names; require each GAP as its own paragraph.

R5 **Full dry-run rehearsal** on both sealed captures (C1 legacy + C1r) + GREEN fixture: corrected capture → corrected validator → manual-gate rehearsal; require zero false positives on GREEN, pinned findings on both REDs, TDD RED/GREEN receipts.

R6 Only after R1–R5 receipts: propose a **new gated live-run packet** for separate Duho approval (single supervised run, same T0/safety locks, fail-closed, no retry). Outside this brief's scope; nothing is armed by this report.

## 6. Method + safety attestation

Evidence read (all sealed, read-only): the nine briefed files plus `rendered_body.html`, `capture_metadata.json`, `manual_review_gate.json`, `run_validator.py`, capture/fixture/test listings. Local-only tooling; no browser/network/Gemini/account/billing action; no DB/API/wiki/product/git/deploy/cron writes; no retry; sealed packet byte-untouched (hash-verified before and used read-only throughout). Files written, all inside this investigation dir only: `HWAO_ROOT_CAUSE.md` (this report), `_tmp_analysis.py`, `_tmp_revalidation.json`. Validator re-run used immutable inputs and unmodified sealed code; no code was patched during the investigation.

HWAO_DR_C1R_ROOT_CAUSE_DONE_20260712T163156Z
