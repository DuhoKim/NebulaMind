# HWAO_T14_DEVIATION_ADJUDICATION — bounded rulings on the two C6 clarifications

Ref: `receipts/T14_DEVIATION.md` (Tori stop was correct and protocol-compliant). Direction-only; Hwao edits nothing; no live/network/browser/git/DB/deploy/dashboard action authorized.

**Adjudication principle:** the detector and the pin both serve the literal contract of record (`prompt/C1r.md`). Where they disagree, fix whichever one contradicts the contract — never bend the contract to preserve either.

## Rule A — Deviation 1 (FLAMINGO emergent cell): **APPROVED as proposed**

For typed S1 `emergent` cells only, a named observable object — exactly the phrases `cluster scaling relations` and `thermodynamic density and temperature profiles` — counts as an observational-comparison reference when the same cell also contains a simulation reference and a result verb. Basis: the FLAMINGO emergent cell ("calibrated simulations successfully reproduce … strictly excluded from … calibration") is a genuine out-of-sample agreement claim under C6 (C1r.md:110-113); the pin was right and Lana §4.2's word list was under-inclusive. This change adds only the pinned FLAMINGO unit; the T14 pin is unchanged on this point. The phrase list is fixture-scoped: it may be extended only via a logged contract-r3 design note, never silently.

## Rule B — Deviation 2 (SIMBA ∼10% parameter fraction): **REJECTED as proposed; pin amended instead**

The role-based exemption (excluding `feedback_params`/`calibration_target` from the numeric four-qualifier gate) is **rejected**. C6's literal text binds "any quoted fraction or incidence, anywhere in the body" and supplies the `NOT_APPLICABLE` device precisely for cases where individual qualifiers do not apply — a tuned ∼10% accretion fraction is a quoted fraction, and the model emitted no qualifiers and no `NOT_APPLICABLE`. The same literal standard that made the S2 Result-cell and emergent-cell findings genuine applies here; intent-scoping the rule to population statistics is a **contract-r3 prompt change**, not a sealed-run adjudication change. Therefore, under the direction's expectation-change path (Lana+Hwao sign-off), the **T14 pin is amended**: add exactly one genuine `MISSING_QUALIFIER` finding (SIMBA `feedback_params` cell, ∼10%), tagged in `RESIDUE_REPORT.md` as "masked in the sealed run by row-level granularity; surfaced at cell granularity" (parallel to the 6 C4 false negatives). Detector unchanged.

**Amended T14 deterministic residue (the new pin):** C2 sentinel 1 · C4 S2 Result-cell 8 · C6 = 6 `UNLABELED_COMPARISON` + 1 `MISSING_QUALIFIER` · C7 integrity 1 — **17 findings total**, all prior artifact regressions still absent, C1/C5/C8 still PASS.

## Lana countersign: **REQUIRED before Tori resumes**

Rule A modifies Lana's design document and Rule B amends a pinned expectation — both fall under the logged Lana+Hwao sign-off requirement. Sequence: (1) Lana writes `design/LANA_T14_COUNTERSIGN.md` explicitly covering Rule A, Rule B, and the amended 17-finding pin (any Lana objection ⇒ STOP, back to Hwao/Duho); (2) only then Tori patches **only** the C6 role-aware detector per Rule A and the T14 expectation per Rule B; (3) full unchanged suite reruns (T0–T15), receipts reference this adjudication and the countersign; (4) all other stop conditions remain in force — any further deviation is a new STOP, not a judgment call.

HWAO_T14_DEVIATION_ADJUDICATION_DONE_20260713T010203Z
