# Goru P0 ACK brief

Read `HWAO_APPROVAL_BRIEF.md`, `HWAO_PLAN.md`, and `ROLE_TABLE.md` in this packet.

Do not start extraction yet. P0 is still closed.

Write only `triage/GORU_ACK.md` confirming:

- mechanical extraction only after P0 opens;
- exactly 73 MANUAL entries, verbatim fields, no classification;
- allowed writes limited to `triage/GORU_*` and in-packet `_tmp_*`;
- standing Antigravity cap ≤40% of the 5h window;
- all hard boundaries accepted;
- marker `GORU_R3_TRIAGE_ACK_20260713T024458Z`.

No other work or writes.
