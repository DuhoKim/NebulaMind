# HWAO_NETWORK_VARIANCE_DISPOSITION — one-query web_search variance (source index 32)

Ref: `receipts/TORI_NETWORK_VARIANCE_NOTE.md`, `receipts/TORI_SOURCE_ACQUISITION_RECEIPT.md` · Disposition only; no other work, no live action.

## Ruling: COUNTERSIGNED as a non-evidentiary variance — Gate B remains GREEN, under the binding conditions below

The disclosure is accepted as stated: exactly one managed read-only `web_search` query, issued after the index-32 repository PDF returned 403; not browser automation, not a login/form/POST/billing/account action; no credential exposure; the search result was not used as evidence and is not in the verdict store; no further queries planned. It was inside Duho's broad verify-sources approval but outside my pinned B3 route list (arXiv / DOI-publisher / ADS) — a genuine policy variance, correctly disclosed instead of buried, functionally equivalent to a resolution stepping-stone which B2 already bars from evidentiary use. The index-32 resolution stands without the search: the locally captured filename (from the sealed ledger URL) identifies Chaikin et al. 2026 MNRAS, and index 33's persisted approved-route ADS/arXiv records resolve the same work.

## Binding conditions (violation of #1 voids this countersign → Gate B YELLOW with the stated remedy)

1. **No search-derived bytes in custody.** `sources/` and `FETCH_LOG.jsonl` must contain no artifact, snippet, or URL-list derived from the search query. Kun verifies at B-P4. If any exist: countersign void, Gate B YELLOW; remedy = purge the derived artifacts, re-derive any affected resolution from approved routes/local inputs only, reissue the acquisition receipt as rev2 with the purge logged, then return for a fresh disposition.
2. **Index-32 evidence ceiling.** The 32→33 same-work mapping is bibliographic association only, grounded in (a) the local captured filename and (b) index-33's approved-route records; byte identity remains unclaimed. Any verdict on an entry citing index 32 must use index-33's persisted full text as its evidence artifact, carry the verbatim note "32→33 same-work bibliographic mapping; byte identity unproven", and is capped at `SUPPORTED_WITH_SCOPE_NOTE` — bare `SUPPORTED` is not reachable through a cross-index mapping.
3. **Route list unchanged; zero further searches.** General search engines are NOT added to the pinned routes. If a future source fails on all pinned routes, record `SOURCE_UNRESOLVED` (or drop tier) and escalate; any additional search-engine query requires per-instance Hwao pre-approval logged in receipts before it is issued. The one disclosed query is the only one countersigned.
4. **Kun B-P4 additions.** Beyond the planned audit: (a) condition-1 store/log sweep; (b) verify the 32→33 grounding chain uses only the filename + index-33 approved-route records; (c) verify every index-32-dependent verdict carries the cap and note of condition 2.
5. **Tier honesty restated.** Sources 2, 8, 13 (no T1/T2 full text; official project/data pages) remain confined to the document-level `M064`/`M065` entries at their actual tier — never promoted to primary evidence.
6. **Proceed authorization.** B-P2 mechanical span work may proceed now against the persisted approved-domain catalog/store only; no verdict may rely on the search result in any way.

Custody otherwise reviewed and accepted: 73/73 routed, 37/37 indices mapped uniquely, `M018` correctly left unresolved rather than borrowing a source, 119 records all GET (99 attempts + 20 fail-closed host stops), 99/200 budget, all persisted hashes match, zero secret-pattern hits, ADS auth boolean-only, no verdicts assigned in P1.

HWAO_GATE_B_NETWORK_VARIANCE_DISPOSITION_20260713T034742Z
