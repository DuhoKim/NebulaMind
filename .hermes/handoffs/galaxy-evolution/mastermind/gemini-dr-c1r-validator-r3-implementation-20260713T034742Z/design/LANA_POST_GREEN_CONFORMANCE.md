# LANA_POST_GREEN_CONFORMANCE — Gate A A-P5 conformance review

Packet: `gemini-dr-c1r-validator-r3-implementation-20260713T034742Z` · Lane: Lana (A-P5, RED-pin author doing independent post-GREEN conformance). Coordinator: Hwao.
Method: read-only inspection of the Gate A packet only — I traced `validator/validator_v3.py`, `validator/contract_spec_v3.json`, `capture/structured_capture_v3.js` (schema), the r3/baseline tests, `readjudication/validator_result_v3.json` + `structured_capture_v3.json` + `RESIDUE_REPORT_R3.md`, and the Tori/Kun/Hwao receipts, against the frozen pin (`design/LANA_R3_RED_PIN.md`) and its countersign (`design/HWAO_R3_RED_PIN_COUNTERSIGN.md`) and `../gemini-dr-c1r-contract-r3-manual-triage-20260713T024458Z/design/CONTRACT_R3_DRAFT.md`. No edits, no tests run, no network/live/DB/dashboard/deploy/git action.

## Verdict: CONFORMANT

The r3 implementation is the faithful mechanical embodiment of the countersigned D1–D5 decisions. The deterministic/manual/pass multisets reproduce the frozen pin exactly with no semantic drift, the code implements each device by typed role (not keyword-guessing), the D3 preserved guard is intact, and the diagnostic/no-retro-acceptance boundary is honored. Residual risks (all confined to future r3-native bodies or downstream framing, none affecting the sealed-body result) are recorded in §8.

## 1. Result conformance (independently reconfirmed from `validator_result_v3.json`)
- Overall FAIL · **19 FAIL / 82 MANUAL / 4 PASS** (PASS = C1_OK, C2 STRUCTURE_OK, C5_OK, C8_OK) — matches the frozen 19/82/4.
- FAIL multiset byte-for-byte to the pin: `C2:SENTINEL_FORMAT_DEFECT`1@`[tr7,2]` · `C2:GAP_MULTIPLE_PER_PARAGRAPH`1@parent`11` · `C6:MISSING_CALIBRATION_TARGET_PREFIX`9@`[tr4..11,1]+[tr11,2]` · `C6:UNLABELED_COMPARISON`6@`[tr5,3][tr6,3][tr9,3][tr10,3][tr11,3][gap_line_1]` · `C6:MISSING_QUALIFIER`1@`[tr6,2]` · `C7:C7_INTEGRITY_FAILURE`1.
- MANUAL 82 = 18 `C3:UNCERTAINTY_CHECK` + 48 `C4:CITED_CELL_CLAIM_REVIEW` (40 + 8 D3) + 5 `C4:CITED_CLAIM_REVIEW` + 1 `C4:CITATION_QUALITY_REVIEW` + 1 `C4:SOURCE_FIDELITY_REVIEW` + 8 `C6:COMPARISON_LABEL_REVIEW` + 1 `C7:C7_NEAR_DUPLICATE`.
- Required absences hold: `C4:UNCITED_CELL_CLAIM`=0, `C4:EMPTY_CITATION_CELL`=0 on the sealed body. No finding code outside the frozen S4 vocabulary is emitted.

## 2. D5→D4→D2→D1→D3 behavior vs frozen design — CONFORMANT, no drift
- **D5** (`_check_c2` L219-237): gap_lines grouped by `parent_path`; `C2:GAP_MULTIPLE_PER_PARAGRAPH` emitted once per parent holding >1 gap, anchored `[parent_path]`. Sealed body: all four gaps share `parent_path="11"` → exactly 1 finding. Matches S2 freeze (1-per-over-full-paragraph, parent field).
- **D4** (`_check_c7` L482-519, `_normalise_url` L90-101, `_near_duplicate_indices` L456-468): orphan(chip-index bidirectionality)/inline-only/duplicate-row(normalized-URL)/blank-short-name → single `C7_INTEGRITY_FAILURE`; near-dup a **separate** `C7_NEAR_DUPLICATE` MANUAL. arXiv `abs|html|pdf`→`/abs/<id>` canonicalized. Matches D4 + S3.
- **D2** (`_check_c6` L432-437, `_has_numeric_fraction` L116, patterns L41-47): four-qualifier requirement gated on a **quoted numeric** fraction/incidence; SIMBA `~10%`@`[tr6,2]`→`MISSING_QUALIFIER`; qualifier/`MODEL_PARAMETER` syntax→`SEMANTIC_QUALIFIER` MANUAL; bare fraction word→no finding. Matches D2.
- **D1** (`_check_c6` L390-407, `_requires_calibration_target_prefix` L120-124): scoped to S1 `calibration_target`/`feedback_params` roles; prefix required iff observable-reference regex AND calibration-action regex both fire and the cell is not prefixed/`NONE_FOUND`. Detector-gated (per CONTRACT_R3_DRAFT D1), not role-mandatory. Matches D1 + S1.
- **D3** (`_check_c4` L304-378): S2 `result` cell excluded from claim-gating; row citation owned by the `citation` cell. Matches D3.
Clause homes match the S4 freeze exactly: prefix→C6, GAP granularity→C2, empty-cite→C4, near-dup→C7.

## 3. D1 negation-blind policy/regex — CONFORMANT (exactly the frozen nine); FP/FN risk noted
`_requires_calibration_target_prefix` performs no negation parsing, so FIRE `[tr7,1]` ("no ... calibration ... to match ... observational properties") matches both `CALIBRATION_OBSERVABLE_RE` and `CALIBRATION_ACTION_RE` and is FLAGGED — honoring the S1 negation-blind freeze. The nine are exactly `[tr4..11,1]+[tr11,2]`; the six S1 col-2 exempt cells lack an observation reference (regex fails) and `[tr7,2]` is `NONE_FOUND.` (prefix-exempt, charged once under C2). Outside the fixture: (a) **FN risk** — `CALIBRATION_ACTION_RE` omits verbs like "constrain(ed) to", and `CALIBRATION_OBSERVABLE_RE` is a fixed observable list, so novel calibration phrasing could be missed; (b) **FP risk** — a `feedback_params` cell naming an observable plus a result verb in a non-calibration sense could mis-flag. Both are fixture-scoped and require a logged re-pin, never a silent change (frozen); zero effect on the sealed-body count.

## 4. D3 row-owned citation — CONFORMANT; preserved-guard attested
`_check_c4` L323-354: for each S2 row, `citation_valid = citation present AND no unresolved_chips AND _is_cited(citation)`. When **not** valid → `C4:EMPTY_CITATION_CELL` FAIL anchored to the citation cell (the D3 hard-fail guard, covering both empty and unresolvable citations); when valid → `C4:CITED_CELL_CLAIM_REVIEW` MANUAL anchored `[row, result_index]`. The eight Result manual reviews are preserved exactly at `[tr14..21, 2]`; the Result cell is never independently citation-gated (L317 `continue`). The C2 empty-cell scan explicitly exempts the S2 citation cell (L244-248 `d3_owned_empty_citation`), so an empty citation cell is charged once by D3, never double-counted. **Attestation: D3's `FAIL_CLOSED_IMPACT: YES` relaxation retains its preserved guard — an empty/unresolvable authoritative Citation cell still hard-fails (verified in code; 0 on the sealed body, exercised by the negative D3 fixture).**

## 5. D4 near-duplicate/manual vs mechanical split + normalization — CONFORMANT
`_near_duplicate_indices` keys on the `/article-abstract/`→`/article/` collapse and flags when the same key has >1 distinct exact URL and >1 index → `[14,29]` as a MANUAL `C7_NEAR_DUPLICATE`. Because `_normalise_url` keeps `article` vs `article-abstract` distinct, 14↔29 are **not** counted among the 9 duplicate rows and are **not** part of the deterministic `C7_INTEGRITY_FAILURE` — the exact mechanical/manual split the pin froze. Normalization deltas = none on the sealed ledger (the only variant pair is 14↔29; every `arxiv.org/abs/*` index is distinct), matching §6 of the pin.

## 6. Test migrations + D3 filter correction — CONFORMANT, coverage preserved
Two legacy v2 expectations conflicted with the countersigned r3 pin and were migrated narrowly (per `TORI_A_P3_BASELINE_TEST_MIGRATION.md`, independently confirmed by Kun and Hwao): (1) `test_t9` required the removed v2 S2 `UNCITED_CELL_CLAIM` → migrated to row-owned citation + manual Result review; (2) `test_t14` pinned the v2 17/73 residue with near-dup embedded in the C7 hard failure → migrated to the r3 19/82 pin with manual `C7_NEAR_DUPLICATE`. No coverage deleted (S2 typed result/citation, exact residue multiset, C7 counts, near-dup routing, determinism all still asserted). The disclosed **test-only D3 filter correction** — a `table_row_1*`-prefix selector over-matching rows 10/11 (`table_row_1` is a prefix of `table_row_10/11`), narrowed to the exact `[tr14..21]` set — is a legitimate selector bug-fix, not an expectation change; the stronger 82-item multiset assertion predated it. This is an expectation migration for changed requirements, not a weakening to make the implementation pass.

## 7. 19/82/4 result + diagnostic/no-retro boundary — CONFORMANT, semantically faithful
The counts are confirmed three independent ways (my trace of the code+result ≡ Tori residue ≡ Kun GREEN ≡ Hwao recompute), with byte-for-byte capture/validator regeneration. `RESIDUE_REPORT_R3.md` carries the required framing verbatim: mechanical/diagnostic only, no science/source-fidelity certification, no quarantine release, no retro-acceptance; the sealed C1r run remains FAIL_CLOSED. This honors §0/§9/§11 of the pin. Implementation-vs-contract wording: the v3 `contract_spec_v3.json.r3_policies` and validator behavior map one-to-one onto `CONTRACT_R3_DRAFT.md` D1 (typed prefix + comparison scope), D2 (universal numeric fraction, MODEL_PARAMETER fill accepted by `QUALIFIER_RE`), D3 (authoritative Section-2 citation role, empty-cite hard-fail), D4 (index bidirectionality, uniqueness, blank names, abs|html|pdf normalization, near-dup manual), D5 (one GAP per parent paragraph).

## 8. Residual risks (recorded; none is a STOP)
1. **Diagnostic-vs-genuine framing (highest):** 10 of the 19 deterministic findings (9 `MISSING_CALIBRATION_TARGET_PREFIX` + 1 `GAP_MULTIPLE_PER_PARAGRAPH`) are legitimate artifacts of the new r3 devices re-scoring a **pre-r3** body — they are NOT sealed-run model defects (those remain the v2 17-finding set). Any Gate-C synthesis or downstream report must keep the two framings separate and must not present the r3 19 as sealed-run model noncompliance.
2. **D1 lexicon FP/FN (future bodies only):** §3 — fixture-scoped, negation-blind by frozen choice; re-pin required for novel phrasing. Prefix honesty itself remains a manual-layer concern (same class as "Label honestly").
3. **D3 relaxation now live in code:** S2 Result cells can no longer deterministically fail for a missing in-cell citation; the safety of that relaxation now rests on (a) the tested `EMPTY_CITATION_CELL` hard-fail and (b) the 8 mandatory manual row reviews actually being performed — i.e. Gate B's source-verification territory. The relaxation is contained but not self-certifying.
4. **D2 `MODEL_PARAMETER` fill** is exercised only by synthetic fixture (no compliant instance in the sealed body); future-body behavior is unit-tested, not end-to-end observed.
5. **Schema bump `NM_GEMINI_RENDERED_DOM_V3` (+`parent_path`):** any consumer of v2 captures must not be pointed at v3 output without accounting for the schema change (packet-local today).
6. **D5 parent_path completeness (minor):** the validator groups only gap_lines that carry a non-empty `parent_path` (L224 `if parent_path:`); a future body where capture failed to populate it on some gap_line would silently escape D5 grouping. On the sealed body all four carry `parent_path="11"` and the capture guard/tests assert it; recommend a defensive validator-side assertion (missing `parent_path` on a `gap_line` ⇒ capture flag) in any later hardening. Not a STOP — the frozen S2 behavior is correctly implemented and observed.

## Boundary
Read-only conformance review; no implementation/test edits, no artifacts run, no network/live/browser/DB/dashboard/deploy/cron/git/publication action. C1r remains FAIL_CLOSED; this review certifies the pipeline's conformance to the frozen r3 design, not the C1r answer. The A-P5 closure items (Tori final packet receipt; completion marker written LAST) remain with their owners; this file satisfies the plan's Lana post-GREEN conformance requirement.

LANA_GATE_A_POST_GREEN_CONFORMANCE_DONE_20260713T034742Z
