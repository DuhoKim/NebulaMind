# User direction and boundaries — Deep Research for Galaxy Evolution content

Packet: `gemini-dr-content-expert-gate-20260713T160239Z`
User direction: **“yeah for the content use Deep Research!”**
Status: **APPROVED FOR HWAO PLAN AND ONE-CANARY PREPARATION; NOT YET ARMED**

## Objective

Use one supervised browser-based Deep Research canary to improve the scientific-content evidence position, not the already-settled nine-H2 article structure.

Start from the completed Gate B record:

- 17 `SUPPORTED`;
- 17 `SUPPORTED_WITH_SCOPE_NOTE`;
- 38 `AMBIGUOUS_NEEDS_EXPERT`;
- M018 `SOURCE_UNRESOLVED`;
- M023 galaxy-sizes observation;
- all eight comparability tokens uncorroborated.

Hwao must choose and justify the highest-value bounded canary target. Prefer a target that tests content truth rather than repeating the completed mechanical validator work. One recommended candidate is the eight-entry comparability cluster because it is load-bearing for r3 C6, but Hwao owns the final bounded choice.

## Role lock

- Hwao: coordinator and plan/contract author only.
- Tori: sole browser owner, exact-tab custodian, prompt submitter, capture owner, hard-stop executor, and independent verifier.
- Goru: local-only prompt/schema/hash/marker preflight; no browser or network.
- Lana: content/method review only if Hwao explicitly includes it; no browser.
- Kun: reproducibility/validator check only if Hwao explicitly includes it; no browser.

No concurrent browser drivers, duplicate tabs, retries, or second canary.

## Required Hwao deliverable

Write `HWAO_ONE_CANARY_PLAN.md` in this packet. It must specify:

1. exact content target and why it is the highest-value first canary;
2. exact input records from the completed Gate B packet;
3. paste-ready prompt and structured answer contract;
4. exact completion marker and validation clauses;
5. single-tab/single-browser-owner custody;
6. Deep Research mode and best-model/max-thinking checks;
7. quota evidence and hard-stop thresholds;
8. plan-ready/start ownership;
9. capture and validator artifacts;
10. fail-closed stops and no-retry rule;
11. explicit recommendation whether the user's direction is sufficient to arm after clean preflight, absent account verification or operator doubt.

## Hard boundaries

No DB/SQL, product/wiki/trust mutation, publication, deploy/restart, git write, cron, billing/account/OAuth action, CAPTCHA or verification handling, alternate browser profile, API fallback, parallel Deep Research, quarantine release, or automatic prose application.

If account verification, unusual traffic, billing/upsell, missing quota, wrong model/mode, uncertain tab identity, or operator doubt appears: stop NOT_ARMED and report.

USER_DEEP_RESEARCH_CONTENT_DIRECTION_RELAYED_20260713T160239Z
