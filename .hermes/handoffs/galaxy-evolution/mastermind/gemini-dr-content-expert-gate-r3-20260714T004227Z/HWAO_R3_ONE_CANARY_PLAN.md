# HWAO_R3_ONE_CANARY_PLAN — fresh-process custody for the one Deep Research content canary

Packet: `gemini-dr-content-expert-gate-r3-20260714T004227Z` · **Supersedes, does not resume,** R2 `gemini-dr-content-expert-gate-r2-20260714T002603Z` (closed BLOCKED/NOT_ARMED; its preflights were GREEN and its one permitted UI configuration sequence was **never started or consumed** — the sole failure was the detached R2 process losing its process-local computer-use bridge before any UI mutation, with `hermes computer-use doctor` fully GREEN). Status: **NOT ARMED**. This pass is plan-only: no browser/network action. All R3 direction boundaries bind verbatim.

## 1. Carried unchanged from R2 (by reference, binding)

The following carry byte-for-byte / clause-for-clause from `../gemini-dr-content-expert-gate-r2-20260714T002603Z/HWAO_R2_ONE_CANARY_PLAN.md` and its R1 antecedent; the new process must read that chain in full before acting:

- **Prompt:** the frozen prompt carries as a byte copy into R3 `prompt/GE_COMPARABILITY_CANARY.md`, SHA-256 pinned `4d57fd71b49e74760cc1be1acc6fb50ce1e777b4c43907612830b346b471f42a` (verified identical in R1, R2, and by the R2 blocker receipt). Its completion marker is referred to only indirectly — it is the frozen prompt's final non-empty line, pinned by that hash; the literal string must occur nowhere in this packet except the frozen prompt file (marker-uniqueness lint).
- **Target and inputs:** the eight-entry comparability cluster M066–M073 with in-cluster citation corrections; Gate B input hashes (`a4821a54…7b3f`, `1fb3165d…4442`, `5b56a549…58cf`, `71de8129…fc6b`) and local evidence texts.
- **Validation V1–V9** incl. chip-aware capture script `dd2a9670…a924`, fail-closed chip→URL recovery, reject-without-retry.
- **Pre-submit UI configuration contract (R2 §5, unconsumed and carried fresh):** exactly one bounded configuration sequence — Deep Research mode → highest DR-compatible model tier → maximum thinking/effort if exposed — then stop-and-check verification (screenshots, UI labels, quota, tab pin, empty composer); at most one new conversation/tab only if the UI requires it, identity pinned after mode selection and before paste; **no second configuration attempt, no fix-loops**; arming on clean verification under the standing user direction with no further approval round-trip; paste-equality against the frozen file hash as the last pre-submit check.
- **Quota and hard stops:** pre/post usage screenshots; abort on usage ≥80%, billing/upsell, account-verification or unusual-traffic challenge, CAPTCHA, login wall, uncertain tab identity, or operator doubt — NOT_ARMED pre-submit, capture-partial + VOID mid-run.
- **Custody constants:** one browser, one profile, one authenticated tab/conversation, **one submission, one start, no retry**; one neutral logged "continue" on visible truncation only; capture set and post-run sequence (Kun V1–V9 rerun → Lana content review → Hwao synthesis) exactly as R1 §9/R2 §6; answer advisory-only, everything quarantined; a VOID or rejected run needs a new packet and fresh gate.
- **Prohibitions:** no DB/wiki/product/trust mutation, publication, dashboard edit, git write, deploy/restart, cron, billing/OAuth/account action, alternate profile, API fallback, parallel research, quarantine release, or automatic prose application.

## 2. The sole R3 change — browser-process custody

1. **Exit the failed R2 process first.** Before the R3 owner launches: the failed detached R2 Tori process is terminated and its tmux session removed by exact-target command (`tmux kill-session -t <exact R2 session name>` — never free-text send-keys into any pane), absence verified (`tmux has-session` non-zero), and the action recorded with session name + prior PID in `preflight/TORI_R3_PROCESS_CUSTODY.md`. **The user-facing Terminal window is not closed or replaced** — only the detached worker session is removed.
2. **One new detached Hermes/Tori process** (detached tmux worker — the approved recovery mechanism) is then launched and becomes the **sole browser owner**. Its tmux session name and PID are recorded in the same custody receipt. No other browser process, fallback adapter, or computer-use client may act concurrently at any point; concurrency doubt = operator doubt = hard stop.
3. **First-task-turn rule.** In its FIRST bounded task turn the new process must, in order: (a) read the complete R3/R2 custody chain (R3 direction, this plan, R2 plan + corrections + preflight receipts + blocker, frozen prompt + `.sha256`); (b) run the **process-local bridge gate** — `computer_use list_apps` returns a non-empty application list including Google Chrome AND a read-only Chrome accessibility capture returns non-zero dimensions and elements — BEFORE any UI mutation (this converts the R1/R2 failure mode into an explicit tested precondition); (c) execute the carried R2 §5 configuration sequence → stop-and-check verification → arming (replace `markers/NOT_ARMED_20260714T004227Z` with `markers/TORI_ARMED_20260714T004227Z`) → paste-equality → the single submission → supervised run → capture → validation handoff, all within that first bounded turn.
4. **Fresh-process failure handling.** If the bridge gate fails in the new process: write `preflight/TORI_R3_BRIDGE_BLOCKER.md`, keep NOT_ARMED, exit — **no second process, no relaunch loop under R3**; a further attempt needs a new packet and fresh user gate. If the first task turn is interrupted before submission: NOT_ARMED stop with receipt (the pre-submit phase is never resumed in a later turn). If interrupted after submission: capture what exists, `CANARY_VOID_20260714T004227Z` with reason, standing no-retry rule applies.

## 3. Sequence and markers (R3-stamped)

Goru R3 preflight, local-only (byte-copy prompt into R3 `prompt/` and verify the R1==R2==R3==pin hash chain; eight-row check; marker-once-per-packet lint; input + capture-script hashes; markers state) → `GORU_R3_PREFLIGHT_GREEN_20260714T004227Z` or NOT_GREEN → §2.1 process swap with custody receipt → §2.3 first-turn bounded operation by the new sole owner → exactly one of `CANARY_CAPTURED_20260714T004227Z` / `CANARY_VOID_20260714T004227Z` → Kun reproducible V1–V9 rerun → Lana content review → Hwao synthesis. Capture artifacts land under R3 `runs/canary/` exactly per the carried set (body.md, rendered_body.html, structured_capture_v3.json, validation_result.json + checklist, meta.md with conversation URL / UI labels / quota values / continue events, screenshots/, CAPTURE_RECEIPT.md with per-file sha256, RUN_RECEIPT.json).

HWAO_CONTENT_DR_R3_PLAN_DONE
