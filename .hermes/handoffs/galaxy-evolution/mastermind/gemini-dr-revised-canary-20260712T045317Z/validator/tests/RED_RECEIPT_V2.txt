/Users/duhokim/Library/Python/3.9/lib/python/site-packages/urllib3/__init__.py:35: NotOpenSSLWarning: urllib3 v2 only supports OpenSSL 1.1.1+, currently the 'ssl' module is compiled with 'LibreSSL 2.8.3'. See: https://github.com/urllib3/urllib3/issues/3020
  warnings.warn(
.FFFFF.FFFFFFFFF                                                         [100%]
=================================== FAILURES ===================================
_____________________________ test_c1_header_fail ______________________________

    def test_c1_header_fail():
        innertext = "Missing everything"
        structured = {"blocks": [create_block("1", "paragraph", innertext)]}
        res = validate_document(innertext, structured)
        c1_findings = [f for f in res["findings"] if f["clause"] == "C1" and f["status"] == "FAIL"]
>       assert len(c1_findings) > 0
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:29: AssertionError
___________________________ test_c2_order_and_empty ____________________________

    def test_c2_order_and_empty():
        # Pass if order correct and NONE_FOUND used. Fail if NONE used or out of order
        innertext = "Calibration ledger\nNONE_FOUND\nOut-of-sample validation ledger\nDouble-counting warnings\nFeedback-relevant observables map\nGaps\nLinks ledger"
        structured = {"blocks": [create_block(str(i), "heading", t) for i, t in enumerate(innertext.split("\n"))]}
        res = validate_document(innertext, structured)
        c2_fails = [f for f in res["findings"] if f["clause"] == "C2" and f["status"] == "FAIL"]
        assert len(c2_fails) == 0
    
        # Fail case
        innertext_fail = "Calibration ledger\nNONE\nOut-of-sample validation ledger"
        structured_fail = {"blocks": [create_block(str(i), "heading", t) for i, t in enumerate(innertext_fail.split("\n"))]}
        res2 = validate_document(innertext_fail, structured_fail)
        c2_fails2 = [f for f in res2["findings"] if f["clause"] == "C2" and f["status"] == "FAIL"]
>       assert len(c2_fails2) > 0
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:44: AssertionError
_____________________ test_c3_exemptions_and_bare_quantity _____________________

    def test_c3_exemptions_and_bare_quantity():
        # Should flag 10^51 as bare quantity unless it has UNCERTAINTY_NOT_QUOTED_BY_SOURCE or +/-
        txt1 = "Energy is 10^51 ergs."
        struct1 = {"blocks": [create_block("1", "paragraph", txt1)]}
        res1 = validate_document(txt1, struct1)
>       assert any(f["clause"] == "C3" and f["status"] in ["FAIL", "MANUAL_REVIEW_REQUIRED"] for f in res1["findings"])
E       assert False
E        +  where False = any(<generator object test_c3_exemptions_and_bare_quantity.<locals>.<genexpr> at 0x10bd17350>)

tests/test_validator.py:51: AssertionError
__________________________ test_c4_uncited_paragraph ___________________________

    def test_c4_uncited_paragraph():
        # Prose without [] or UNCITED_NOT_USABLE should fail C4
        txt = "This is a calibration statement without a citation."
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct)
        c4_fails = [f for f in res["findings"] if f["clause"] == "C4" and f["status"] == "FAIL"]
>       assert len(c4_fails) > 0
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:64: AssertionError
__________________________ test_c4_uncited_table_cell __________________________

    def test_c4_uncited_table_cell():
        txt = ""
        struct = {"blocks": [create_block("1", "table_row", txt, cells=[{"text": "A claim with no cite"}])]}
        res = validate_document(txt, struct)
        c4_fails = [f for f in res["findings"] if f["clause"] == "C4" and f["status"] == "FAIL"]
>       assert len(c4_fails) > 0
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:71: AssertionError
________________________ test_c5_own_voice_banned_term _________________________

    def test_c5_own_voice_banned_term():
        txt = "This proves that the simulation works."
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct)
>       assert any(f["clause"] == "C5" and f["status"] == "FAIL" for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_c5_own_voice_banned_term.<locals>.<genexpr> at 0x10bd17d60>)

tests/test_validator.py:83: AssertionError
___________________________ test_c5_attributed_quote ___________________________

    def test_c5_attributed_quote():
        txt = 'The authors state that "it proves the theory".'
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct)
>       assert any(f["clause"] == "C5" and f["status"] == "MANUAL_REVIEW_REQUIRED" for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_c5_attributed_quote.<locals>.<genexpr> at 0x10bcef0b0>)

tests/test_validator.py:89: AssertionError
__________________________ test_c6_missing_qualifiers __________________________

    def test_c6_missing_qualifiers():
        txt = "Quenched fraction is 0.5."
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct)
>       assert any(f["clause"] == "C6" and f["status"] in ["FAIL", "MANUAL_REVIEW_REQUIRED"] for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_c6_missing_qualifiers.<locals>.<genexpr> at 0x10bcef3c0>)

tests/test_validator.py:95: AssertionError
_______________________________ test_c7_one_way ________________________________

    def test_c7_one_way():
        txt = "See [http://a.com] and [http://b.com].\nLinks ledger\n[http://a.com]"
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct)
        c7_fails = [f for f in res["findings"] if f["clause"] == "C7" and f["status"] == "FAIL"]
>       assert len(c7_fails) > 0 # b.com missing
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:102: AssertionError
____________________________ test_c7_bidirectional _____________________________

    def test_c7_bidirectional():
        txt = "Links ledger\n[http://a.com]\n[http://b.com]"
        struct = {"blocks": [create_block("1", "paragraph", txt)]}
        res = validate_document(txt, struct, spec={"bidirectional_c7": True, "marker": ""})
        c7_fails = [f for f in res["findings"] if f["clause"] == "C7" and f["status"] == "FAIL"]
>       assert len(c7_fails) > 0 # neither a nor b are in body
E       assert 0 > 0
E        +  where 0 = len([])

tests/test_validator.py:109: AssertionError
___________________________ test_c8_marker_not_final ___________________________

    def test_c8_marker_not_final():
        marker = "DONE"
        txt = "DONE\nSome trailing text"
        struct = {"blocks": []}
        res = validate_document(txt, struct, spec={"marker": marker})
>       assert any(f["clause"] == "C8" and f["status"] == "FAIL" for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_c8_marker_not_final.<locals>.<genexpr> at 0x10bd17eb0>)

tests/test_validator.py:116: AssertionError
___________________________ test_c8_marker_duplicate ___________________________

    def test_c8_marker_duplicate():
        marker = "DONE"
        txt = "DONE\nDONE"
        struct = {"blocks": []}
        res = validate_document(txt, struct, spec={"marker": marker})
>       assert any(f["clause"] == "C8" and f["status"] == "FAIL" for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_c8_marker_duplicate.<locals>.<genexpr> at 0x10bd173c0>)

tests/test_validator.py:123: AssertionError
_____________________________ test_failed_fixture ______________________________

    def test_failed_fixture():
        # Will test against a full JSON mock of the failed body
        txt = (FIXTURES_DIR / "failed_c1.md").read_text()
        if not (FIXTURES_DIR / "failed_c1_structured.json").exists():
            pytest.skip("JSON fixture not generated yet")
        struct = json.loads((FIXTURES_DIR / "failed_c1_structured.json").read_text())
        res = validate_document(txt, struct)
        assert res["overall"] == "FAIL"
        # C5 L117 'established'
>       assert any(f["clause"] == "C5" and f["status"] == "FAIL" for f in res["findings"])
E       assert False
E        +  where False = any(<generator object test_failed_fixture.<locals>.<genexpr> at 0x10bcd8f20>)

tests/test_validator.py:134: AssertionError
______________________________ test_clean_fixture ______________________________

    def test_clean_fixture():
        if not (FIXTURES_DIR / "clean_c1_structured.json").exists():
            pytest.skip("JSON fixture not generated yet")
        txt = (FIXTURES_DIR / "clean_c1.md").read_text()
        struct = json.loads((FIXTURES_DIR / "clean_c1_structured.json").read_text())
        res = validate_document(txt, struct)
>       assert res["overall"] == "MANUAL_REVIEW_REQUIRED"
E       AssertionError: assert 'FAIL' == 'MANUAL_REVIEW_REQUIRED'
E         
E         - MANUAL_REVIEW_REQUIRED
E         + FAIL

tests/test_validator.py:142: AssertionError
=========================== short test summary info ============================
FAILED tests/test_validator.py::test_c1_header_fail - assert 0 > 0
FAILED tests/test_validator.py::test_c2_order_and_empty - assert 0 > 0
FAILED tests/test_validator.py::test_c3_exemptions_and_bare_quantity - assert...
FAILED tests/test_validator.py::test_c4_uncited_paragraph - assert 0 > 0
FAILED tests/test_validator.py::test_c4_uncited_table_cell - assert 0 > 0
FAILED tests/test_validator.py::test_c5_own_voice_banned_term - assert False
FAILED tests/test_validator.py::test_c5_attributed_quote - assert False
FAILED tests/test_validator.py::test_c6_missing_qualifiers - assert False
FAILED tests/test_validator.py::test_c7_one_way - assert 0 > 0
FAILED tests/test_validator.py::test_c7_bidirectional - assert 0 > 0
FAILED tests/test_validator.py::test_c8_marker_not_final - assert False
FAILED tests/test_validator.py::test_c8_marker_duplicate - assert False
FAILED tests/test_validator.py::test_failed_fixture - assert False
FAILED tests/test_validator.py::test_clean_fixture - AssertionError: assert '...
14 failed, 2 passed in 0.06s
