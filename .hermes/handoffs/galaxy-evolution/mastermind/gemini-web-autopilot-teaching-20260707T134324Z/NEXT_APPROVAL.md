# Next approval gate — supervised Gemini web/app one-packet pilot

Marker: GEMINI_WEB_NEXT_APPROVAL_20260707T134324Z

Browser automation is still disabled until this exact gate is approved.

Approval phrase:

APPROVE EXECUTE ONE SUPERVISED GEMINI WEB OPERATOR PILOT: Use the already logged-in Gemini web/app only for one prompt packet. Paste, wait, capture full output, verify marker, save artifacts, then stop. Stop on login, 2FA, CAPTCHA, payment, API key, GCP/project/billing, OAuth code, token, cookie, account-change prompt, URL drift, UI uncertainty, prompt-only capture, missing marker, or any click/type/key/scroll/focus error. No repo/DB/deploy/git/cron/cloud/account/secret changes.

If not approved, use the manual copy/paste/capture loop only.
