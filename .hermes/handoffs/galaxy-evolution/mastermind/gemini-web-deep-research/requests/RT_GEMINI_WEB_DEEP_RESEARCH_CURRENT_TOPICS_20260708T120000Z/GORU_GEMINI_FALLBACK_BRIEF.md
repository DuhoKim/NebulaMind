# Goru/Gemini fallback advisory brief — current RT professionalization

Marker: GEMINI_AGY_FALLBACK_CURRENT_RT_PROFESSIONALIZATION_20260708T120000Z

User request: use Gemini Deep Research help to revise the current Galaxy Evolution research-topic pages because they do not look like professional-level research proposals.

Context: The intended Gemini-web GUI sidecar is blocked because `computer_use` returns 0x0 captures for screen/Chrome/Safari despite `hermes computer-use doctor` OK. Do not try browser automation. This fallback uses the authenticated Gemini/Antigravity lane as advisory research support only.

Read this prompt packet:
`/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/galaxy-evolution/mastermind/gemini-web-deep-research/requests/RT_GEMINI_WEB_DEEP_RESEARCH_CURRENT_TOPICS_20260708T120000Z/GEMINI_WEB_PROMPT_CURRENT_RT.md`

Task:
- Produce a professional proposal-quality critique and revision blueprint for the 18 current RT cards.
- Use the prompt packet’s output shape.
- If your environment has safe built-in public web/source lookup, you may use read-only public web lookup for arXiv/ADS/DOI/source pages only. Do not use browser automation, API keys, GCP/Vertex/billing, OAuth, tokens, cookies, secrets, account settings, or any private profile data.
- If web lookup is unavailable, say so explicitly and label source suggestions as `DO_NOT_USE_UNTIL_VERIFIED`.
- Do not edit RT pages. Write only the advisory output file below.

Allowed writes:
`/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/galaxy-evolution/mastermind/gemini-web-deep-research/outputs/GEMINI_AGY_FALLBACK_CURRENT_RT_PROFESSIONALIZATION_20260708T120000Z.md`

Hard gates closed:
No product DB/SQL, `/api/pages`, page_versions/live wiki publish, trust recompute, deploy/restart, git, cron, cloud/GCP/API/billing/OAuth/token/secrets, or browser automation.

Required final marker in the output file:
`GEMINI_AGY_FALLBACK_CURRENT_RT_PROFESSIONALIZATION_DONE`

Also include `GEMINI_WEB_RT_DEEP_RESEARCH_OUTPUT_DONE` only if you actually satisfy the prompt's requested output shape. If you cannot, explain the blocker and do not fake it.

GEMINI_AGY_FALLBACK_CURRENT_RT_PROFESSIONALIZATION_20260708T120000Z
