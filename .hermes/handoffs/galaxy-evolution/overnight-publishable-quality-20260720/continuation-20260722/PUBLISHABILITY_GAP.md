# Per-paper publishability gap — after scratch cycles 1-6 (03:29 KST 2026-07-22)
All three are descriptive, human-review-ready, NOT validated (0 human sign-offs). This states the
SINGLE remaining blocker between human-review-ready and genuinely publishable, per paper.

## Paper A — unlensed z~9-10 metallicity deficit
State: -0.68 +/- 0.16 dex weighted; sign secure (bootstrap P<0 = 100%, ~4sigma); robust to sample
growth (z=9.3-10.6, 2 programs incl. GN-z11), weighting, local anchor; no mass/z trend (pure
normalization offset); dominant caveat = absolute Te zero-point (~0.15 dex), NOT a scale mismatch (KE08-grounded).
BLOCKER: the absolute Te-scale zero-point is irreducible with current data. Two real paths, both need
new data (not local): (i) a larger unlensed direct-Te z>7 sample (N=6 today; ~15-20 would halve the SEM),
(ii) an independent absolute-Te anchor (e.g., recombination-line O/H cross-calibration on a high-z subset).
=> Publishable AS A BOUNDED DESCRIPTIVE RESULT now (it is careful and honest); a *precise* value awaits (i)/(ii).
RECOMMEND: submit-ready as "an independent, unlensed, systematic-limited deficit" IF a human accepts the
descriptive framing. This is the strongest of the three.

## Paper B — z~4-6 massive-galaxy abundance vs IllustrisTNG
State: no robust number-count tension (erasable by <=0.93 dex M* shift across the full excess x slope grid,
within the ~1 dex budget); the ONLY residual is spectroscopic quiescent galaxies at z>6 (~2 dex, needs >1.4 dex -> exceeds budget).
BLOCKER: the quiescent-galaxy residual rests on ~1 object (RUBIES-UDS-QG-z7) -> Poisson/cosmic-variance
dominated; not yet quantified in B. Needs the observed quiescent number density + its (large) single-object error
bar vs TNG's ~0, to state honestly whether the residual is a real tension or a statistics-of-one artifact.
=> Publishable AS "the number-count tension is not robust; the sharp residual is quiescent galaxies, statistics-limited".
RECOMMEND: one more cycle to bound the quiescent residual's Poisson/CV error (real data pull), then it is complete.

## Paper C — aperture/calibration practitioner's framework (REVIEW)
State: methods/review; central numbers grounded (0.7 dex calibration offset = KE08-verified; aperture >0.15 dex
below 20% covering fraction; DIG 30-60% Halpha). Review-cleared by astrosage.
BLOCKER: none for a *review* — it is a synthesis, not a new measurement. Its publishability is a GENRE decision
(does the Lab publish methods/reviews?), not a science gap. Every quantitative claim should be spot-checked to a
primary source (cycle 6 did the headline one); ~3-4 more spot-checks would fully source-audit it.
RECOMMEND: human decides the review genre; if yes, a short source-audit pass finishes it.

## Bottom line
A = strongest, publishable-as-descriptive now. B = one data pull from complete. C = genre decision + source audit.
None crosses the bar without a HUMAN sign-off (by design). No new shaky papers were manufactured (per the publishable-bar feedback).
