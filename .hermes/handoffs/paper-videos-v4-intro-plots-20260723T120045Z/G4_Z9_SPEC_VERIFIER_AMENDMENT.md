# G4 AMENDMENT — z9 spec verifier corrections applied and re-signed

Coordinator: Hwao · Written: 2026-07-23 ~15:05 KST (06:05Z) · Lane-only receipt.
Scope of change: exactly the three verifier-supplied narration replacements in `V4_Z9_CANARY_SPEC.json` (slots 0, 3, 8). No other field, wording, card, claim, anchor, or asset was touched. No media/public/git/runtime action occurred. No audio existed yet, so the exact-audio downstream rule invalidates nothing — the hold landed before G5 audio, as intended.

## Rulings on the three corrections — all ACCEPTED

**Slot 0 — ACCEPT (verifier is factually right; my text was wrong).**
"Nine hundred million years after the Big Bang" was unanchored and incorrect for z≈9–10 (that age corresponds to a substantially lower redshift; z≈9–10 is roughly half that age). The replacement wisely states no age at all — "at redshift nine to ten, very early in cosmic history" — which is exactly what the freeze supports. "Give a clear answer" → "provide a cleaner clue" also removes an overclaim that sat badly beside the not-a-detection boundary; "unlensed, directly measured" is more precise than "rare, cleanly measured".

**Slot 3 — ACCEPT.**
"The most defensible technique" was an unsupported superlative not present in the freeze. The replacement narrates only what `sources-v4/z9-metallicity.md:33` supports: direct electron-temperature measurements on one consistent scale.

**Slot 8 — ACCEPT.**
My original paraphrased the boundary; the spec's own acceptance check demands "boundary text verbatim in slot 8". The replacement carries the contract `boundary_text` word-for-word ("Machine-generated by an autonomous research pipeline; descriptive, not validated; automated review is not journal or human peer review. This is not a formal statistical detection."). The dropped "robust direction, systematics-limited size" phrasing survives in the slot's title and cards, so no content is lost.

## Lana semantic re-sign — PASS

- All three replacements verified against the current freeze: no number, claim, or anchor changed; slot 0/3/8 anchors remain valid as recorded in the spec.
- Word-count deltas are within ±2 words per scene at 134.4 wpm — duration budget (≤3:00) unaffected.
- Cards, claims, axis captions, figure shas, layout, contract block, and acceptance checks: byte-identical to the signed version.
- Prior sign-off (`G3_G4_Z9_SIGNOFF_AND_CANARY_INSTRUCTION.md`) remains in force with this amendment layered on top; the Kun CROP ruling is untouched.

## Gate state

Verifier HOLD is resolved by this amendment. The previously issued release (RELEASE_G5_LOCAL_Z9_CANARY) stands: **Tori may begin G5 audio/build against the amended spec as scoped — local, lane-only (`canary-v4-z9/`), hard stop after QA receipts.** Public/YouTube/site/DB/git/runtime actions remain prohibited; G6 (Duho watches and approves) still precedes any batch work.

HWAO_V4_Z9_SPEC_VERIFIER_AMENDMENT_COMPLETE
