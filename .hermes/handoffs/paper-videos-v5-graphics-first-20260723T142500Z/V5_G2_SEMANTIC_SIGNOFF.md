# V5-G2 SEMANTIC SIGN-OFF — z9 graphics-first storyboard

Coordinator: Hwao · Lana semantic hat (honest record: ruling executed by Hwao in-lane, falsifiable via the cited anchors) · 2026-07-24 KST
Scope: `V5_G2_Z9_STORYBOARD.json` only. Released gate: V5-G2 draft+sign. No audio, animation, video, YouTube, visibility, website, DB, git, runtime, or cockpit action this turn.

## Contract compliance — PASS

| Contract item | Where satisfied |
|---|---|
| 105–125 planned WPM, ≤3:30 | 378 words / 199.4 s = 113.7 WPM; 3:19 total. Pacing grounded in the measured V5-G1 receipt (116.5 WPM at speed 1.0 + 1.0 s pauses), not in a guess |
| Narrative order: meaning → question → obstacle → method → Figure-1 progressive read → stack/GN-z11 cross-check → uncertainty → verbatim boundary → recap | Rows 3–5 → 6 → 7–9 → 10–11 → 12–19 → 20–23 → 24–27 → 28–29 → 30 (intro rows 1–2, outro row 31) |
| Per-sentence rows with sentence / start–end / one action / asset+view-state / anchors / visual class / presenter flag | All 31 rows carry all seven fields |
| Presenter intro ≤15 s, outro ≤10 s, science presenter-free | Intro 0–12.2 s (rows 1–2), outro 194.5–199.4 s (row 31, 4.9 s); rows 3–30 presenter=false; presenter share 8.6% |
| ≥70% plots/source-grounded graphics | Planned 72.8% (real_plot 33.1% + conceptual 39.7%); text cards (18.6%) deliberately excluded from the count |
| Figure 1 ≥5 distinct view states | 6 defined and used in order: axes → curves → red points → −0.69 gap → anchor swap → blue stack (VS1–VS6); recap returns to VS4 only after the full progression — no identical repeated evidence beats |
| Five conceptual graphics, labeled | CG_A oxygen clock, CG_B lensing bias, CG_C benchmark choice, CG_D stacked cross-check, CG_E systematic budget — each anchored; binding rule prints "CONCEPTUAL — illustration, not data" on every non-data diagram |
| No invented values/geometry | Every on-screen number appears in the freeze (−0.69±0.03, ≈−0.65, ~1500, −0.5…−0.6, z=10.6, 0.1–0.2 dex, five, z 9.3–9.9); zoom windows are declared presentation choices pinned at build QA against the sha-verified crop, and overlays may not cover data |
| One action per sentence, onset at sentence start | 31 sentences ↔ 31 actions; onset == planned start, ±0.3 s build tolerance |
| Cold-reader questionnaire + key in a separate field | `cold_reader` block: 8 questions, key, pass rule (≥7/8 with Q8 mandatory) — answers live only inside the JSON, per instruction |

## Semantic audit — CLEAN

- **Claims:** all drawn from the G1-confirmed set for z9 (which passed unchanged against the current freeze); no claim from the superseded V3 material of other papers leaks in.
- **Arithmetic:** "near one fifth" ↔ 10^−0.69 ≈ 0.204 ✓ (row 16); "smaller than the shortfall" ↔ 0.1–0.2 < 0.69 dex ✓ (row 26).
- **Careful negatives preserved:** GN-z11 is a text chip, not a figure pointer — its presence inside Figure 1 is unverified, so the storyboard never points at the plot for it (row 23). "Small spread" (row 17) stands for the anchored 0.04 dex leave-one-out without inventing an on-screen number.
- **Avoid-list:** no cover page, no cosmic-age conversion, no clear-answer/certainty language (row 30 claims only what the anchor-swap test showed), no method superlatives.
- **Boundary:** rows 28–29 carry the contract boundary text verbatim, split at its natural sentence break for per-sentence synthesis.
- **V4 root causes addressed by construction:** no bounce-loopable gesture track exists (presenter ≈17 s total at the ends); zero static-slot design (every sentence has its own action); Figure 1 progresses through six states instead of three identical crops.

## Ruling

**SIGNED — the storyboard is approved as the V5-G2 cold-reader input exactly as written.** Any sentence or claim change voids this sign-off. Facial animation remains prohibited until the cold-reader gate passes.

Next bounded step: run the cold-reader comprehension check per the JSON's `cold_reader` block (reader must be paper-naive; closed book; ≥7/8 with Q8 correct). On PASS → V5-G3 motion-graphics canary (no faces). On FAIL → narration rewrite and V5-G2 repeat.

HWAO_V5_G2_Z9_STORYBOARD_SIGNED_COMPLETE
