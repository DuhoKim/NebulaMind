# Tori -> Goru dispatch

Target: %217
Timestamp: 20260714T035938Z

## Payload

```text
GORU / ANTIGRAVITY BRIEF — Tori-dispatched — 20260714T035938Z

Use Gemini/Antigravity quota for this scoped Tori helper task.

Safety boundary:
- Stay inside the explicit scope in the brief below.
- Prefer read-only mechanical checks, counts, inventories, source maps, repro checks, and draft reviews.
- Do not do DB writes, deploy/restart, git commit/push/merge, cloud/GCP/Gemini API config changes, secrets inspection, or live publication unless the user gives a separate explicit gate.
- If command permission is needed, ask for the smallest exact command; Tori will approve only scope-matching safe commands.
- Write/report exact files, counts, commands, and blockers. Do not self-certify unverified external facts.

Assigned brief:

# GORU brief — helper lane: bridge observer 1 / DOM-CDP writerA (ACK required)

Packet: `studio-simultaneous-web-viability-test-20260714T034720Z` · Issued by Hwao. Write `briefs/acks/GORU_ACK.md` (role-table ACK phrase) BEFORE any participation. Standing quota cap: ≤40% of the 5h Antigravity window per rung; log usage in each receipt.

Scope by rung (nothing outside it): **C0** — hold idle computer-use bridge #1 for the soak; scheduled non-mutating `list_apps` + capture-metadata samples only; no app activation/click/type/navigation. **C1** — read lease on non-browser app/window target A; captures only. **C2** — read lease on sandbox browser target A. **C3** — DOM/CDP **writerA**: deterministic, non-submitting scripted actions against YOUR leased CDP target in `sandbox/profiles/writerA` only, under a live broker target lease with per-action `check()` and target re-verification; overlap with writerB is expected and allowed — you never touch cua/AX/pointer/keyboard (that is WonE's serialized lane). Every action via the broker; no lease → no action; lease loss → stop, release, `bridge_loss` entry, NEVER frontmost fallback. Write areas: `receipts/c*/pass*/GORU_*`, `canaries/_tmp_goru_*`, ledger appends (actor `goru`). Prohibitions: default Chrome/Flow window, credentials, sign-in surfaces, submissions, downloads outside `sandbox/downloads/writerA`, desktop-control lease requests. STOP authority: yes.

GORU_VIABILITY_BRIEF_ISSUED_20260714T034720Z

Done marker: TORI_GORU_DISPATCH_DONE_20260714T035938Z

```
