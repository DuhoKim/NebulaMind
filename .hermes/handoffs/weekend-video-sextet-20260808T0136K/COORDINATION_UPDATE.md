# Coordination update — Hwao order is authoritative

Recorded 2026-08-08 after Hwao issued `HWAO_WEEKEND_ORDER.md`.

Hwao's order supersedes any conflicting path or writer detail in `LANE_CONTRACT.md` and the earlier `lanes/*/BRIEF.md` files.

## Official mapping

- `lanes/brightend` -> `lane-c41-uvlf/worker-yui/`
- `lanes/mzr-anchor` -> `lane-c41-mzr/worker-yui/`
- `lanes/fesc` -> `lane-fesc-zsweep/worker-yui/`
- `lanes/mzr-census` -> `lane-mzr-census/worker-yui/`
- `lanes/spin` -> `lane-spin-parity/worker-yui/`
- `lanes/integration` -> `integrator/reviews/yui/`

Keep all already-created `lanes/*` artifacts as pre-order receipts. Do not delete or overwrite them. Continue future findings, visual diagnoses, storyboards-as-proposals, and requests only under the official mapped destination.

## Single-writer correction

Hwao is currently acting as the designated integrator and sole writer for shared tools, TTS invocation, and candidate bundles. Paper Yui lanes must not:

- edit shared tools or storyboards of record;
- invoke TTS;
- produce or overwrite official candidate bundles;
- touch another lane.

A paper lane that needs a shared change writes a request to `integrator/requests/` and links its evidence in its own `worker-yui/` receipt.

The Yui integration session is a read-only visual/scientific QA adviser. It writes only to `integrator/reviews/yui/` until Hwao explicitly delegates the writer seat.

## Continue-usefully rule

The narration-only spin canary is real and machine-valid, but it does not by itself answer Duho's complaint that the videos do not look like scientific presentations. While Hwao controls candidate rendering, every paper Yui should continue source/status freeze, encoded-frame analysis, graphics-first slide grammar, figure/axes/provenance design, and implementable requests. Do not wait idly for publication or generic acceptance.

Alloy through Nous at speed 1.18 is the current narration route. Paper lanes record that fact but do not invoke it.

All closed gates in `HWAO_WEEKEND_ORDER.md` remain closed.
