# HWAO ORDER — next research plan, four steps in order

Stamped 2026-08-10 17:13 KST. Duho, having watched the spin cut: *"proceed as your order."*

## The finding this plan is built on

Five lanes produced five method-only videos and zero results. Every one of them re-derives from
published catalogues — which is exactly the bar that rejected nine papers: *assembly of published
values is not a study*. Choosing a better frontier does not fix that; it relocates it.

## Step 1 — close spin honestly, and write up the one original thing we have

Lana's Path C, near-zero cost. Record `FRAME_UNSTATED` as **terminal**, not pending.

**And treat the provenance failure itself as the result.** Galaxy Zoo 1's stored mirrored-condition
direction fields — `pcS1/paS1/pcS2/paS2`, `pcSm/paSm` — have no documented recording convention.
Lintott 2011 and Land 2008 are procedural throughout and never state whether a stored "clockwise"
is as-displayed or de-mirrored. **Kun reached `FRAME_UNSTATED` independently, from the source texts,
before opening the drafted record; Lana reached it separately.** Two seats, primary sources, same
conclusion.

That is a real, checkable, useful negative result about a widely used catalogue, and it is the only
genuinely original output of this week. It is also **not a claim about the sky** — it is a claim
about documentation — so it sits entirely outside the freeze's forbidden scope.

Deliverable: a short written note, primary-source quoted, stating what is undocumented, what was
searched, and what it forecloses. Plus the lane's permitted statement — the fenced conditional
instrument reading, **no Land-comparative phrasing anywhere**: not "contradicts", not "confirms",
not "in tension with".

## Step 2 — finish the four V5 freeze packets

Mechanical, hours. Not because it unlocks results, but because it converts four *unknown* lanes
into *known-clear* or *known-blocked*. Cheap information. Outstanding from the overnight run:
a receipt-bearing seal operation; the `manifest_created_at → sealed_at` relation recorded; and
per-path exact provenance replacing the boilerplate `historical-or-initial` string.

## Step 3 — a provenance feasibility gate, before any new question

Before designing a study, verify from primary documentation that **every field it depends on has a
documented meaning**. Spin spent a week and died because nobody had written down what a column
meant. This check would have killed it in a day.

Scoped after step 1, by Lana.

## Step 4 — then JWST high-z metallicity

Top of the frontier map: score 0.957, n=1321, **81% galaxy-evolution dominant** — contested and
within this corpus's competence. The Hubble-tension cluster scores higher on contestedness (0.44)
but is 5% GA-dominant; it is outside what we can honestly attack, and we will not chase it.

**Only with a question whose answer does not reduce to re-deriving someone's published number.**
That constraint is the point of the step, not a caveat on it.

## Not doing

Five parallel lanes again. More videos before there is a result worth narrating — the videos are no
longer the bottleneck.

## Gates

Unchanged. No lane states a result while its `SOURCE_FREEZE` is absent; spin stays
`BLOCK_SUBSTANTIVE_RESULT_RENDER`. Step 1 asserts nothing about handedness. No upload, publication,
public/frontend change, DB, deploy, or Git without Duho. Nothing is `accepted_by_duho` but Duho.
