# Tailnet 8766 retired — single route restored

Acted 2026-08-09 02:07 KST by the **Claude-macbook** seat (Directors board, pane %30), on Duho's
direct instruction in that pane: *"retire 8766"*.

## What was retired

- PID `38142`, started 00:08:52 KST, **PPID 1** (detached — no supervisor, will not restart).
- Command: `HermesOps/scripts/serve_videos_tailnet.py --host 100.84.12.101 --port 8766
  --root …/weekend-video-sextet-20260808T0136K/integrator/canaries`
- Recorded in `reviews/TORI_OVERHAUL.md` §21 as the verified in-place private route for `c5e7deed…`.

**Why:** two live routes served the same candidate. Duplication is not a gate problem, but a
superseded route is exactly what caused the confusion this pair of repoints existed to fix — the
12:54 server kept serving the rejected `0204` cut for hours after it was rejected.

## Verification order — 8765 checked healthy BEFORE 8766 was stopped

Retiring both would have taken the accepted cut offline, so 8765 was confirmed first:

- before: `HTTP 200`, `16,065,978` bytes on
  `http://100.84.12.101:8765/spin-method-overhaul-canary-20260808T1959K.mp4`
- after: served bytes still hash `c5e7deed0dc243ccff170fdb72b128f4816a85e1ed4dbc185543e53496baa240`

8766 after: no listener, direct probe returns `HTTP 000` (connection failure), PID gone.

## Surviving route — the only one

`http://100.84.12.101:8765/spin-method-overhaul-canary-20260808T1959K.mp4`

PID `13053`, bound to `100.84.12.101` (non-global CGNAT `100.64.0.0/10`), serving the authoritative
candidate directory in place with no copies.

## Note for Tori

8766 was your listener and it was **stopped deliberately**, not lost. Nothing was deleted; the
candidate directory it served is untouched and remains reachable on 8765. If a sibling-rollout
candidate needs a route later, prefer re-verifying 8765 or opening a new port rather than assuming
8766 is still up — your §21 route row is now stale and should be re-pointed at 8765 on next append.

## Gates

Process lifecycle only. No file written outside this handoff root, no copy into any protected root,
no upload, no publication, no Git write, no deletion. `video_reportable_now` remains `false`.
