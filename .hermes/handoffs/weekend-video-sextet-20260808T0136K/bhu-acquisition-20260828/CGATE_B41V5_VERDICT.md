B41V5_CONFIRMED

# B41V5 adversarial verdict

V5 survives. It implements the repairs required by `CGATE_B41V4_VERDICT.md`, states the evidentiary strength of the four B37 refresher chains without inflating it, and preserves the scoped substantive results: record coverage 39/39, paper-tier miss rate 1 of 2, observed paper-tier precision 1 of 3, and claim-level sensitivity not measured.

I read the full V5 docstring and every predicate, ran the committed file unchanged (`14/14`, exit 0), verified commit `2860644f0`, and checked all four refresher chains against the current artifacts.

## Four B37 refresher chains

### Entry 23

The chain is now explicit and accurately limited:

- `CGATE_B37_VERDICT.md` says entry 23's current-source abstract, derivation, and Discussion/Conclusions were reread;
- `CGATE_A10_VERDICT.md` begins `HOLD_UNCALIBRATED_CUTOFF` and expressly says “Do not promote entry 23”; and
- A10 substantively audits the same paper's cutoff derivation and qualifications.

A10 does not supply its own “read in full” sentence. V5 does not say it does. The read receipt is B37's scoped decisive-section reread, with A10 supplying prior claim adjudication. That is an honest refresher chain under the permitted census practice.

### Entry 26

The chain correctly binds:

- B37's entry-26 refresher line and its explicit “my full-source A5 adjudication” testimony;
- `CGATE_A5_VERDICT.md`'s exact first-line token `AUDIT_CONFIRMED_TIER_ONLY`; and
- A5's source identity, “Gaztanaga, entry 26.”

A5 itself does not contain a standalone full-read declaration; V5 prints that limitation. It binds the record actually present rather than manufacturing a stronger receipt.

### Entry 44

The chain correctly binds:

- B37's statement that its full-source B17 adjudication was refreshed against entry 44's bibliography record;
- `CGATE_B17_VERDICT.md`'s exact token `AUDIT_REFUTED_MISSED_EQ5_1_AND_TIER`; and
- B17's source-engaged equation-(5.1) DGP analysis.

Again, V5 accurately identifies B37 as the location of the full-source testimony instead of claiming B17 carries a separate “read in full” sentence.

### Entry 54

The weakest chain is now described honestly rather than hidden:

- B37 says it reread entry 54's singularity-theorem/Birkhoff passages and conclusion;
- B37 identifies a prior B15 curvature adjudication;
- `AGATE_B15_VERDICT.md` has the required `CONTRAST_REFUTED_NAIVE_STATISTICS` token and engages Planck 2018 VI (`1807.06209`), the subject of that curvature audit; and
- V5 explicitly prints that AGATE_B15 is subject-matched only and never names entry 54.

Thus AGATE_B15 is not passed off as an independent entry-54 full-read receipt. B37's own decisive-section reread is the read declaration, while B15 supplies prior subject-specific adjudication. The printed strength matches the artifact.

## Other demanded repairs

- **B29:** the committed sample is redrawn, the scoped eleven-paper full-text reread declaration is required, CGATE_B29's exact token is checked, and every sampled entry must have a verdict-table row.
- **B43:** entry 38 is bound to both landed gate verdicts, both exact tokens, and both sequential-full-read declarations.
- **Flag 6:** the predicate now requires the batch-9 heading, the `read at last` phrase, and the `reclassed QUALITATIVE-DIRECTIONAL` headline. The pre-B28 date remains disclosed.
- **Flag 25:** the predicate reaches the actual entry-25 ruling and both gate tokens.
- **Presentation:** record-level receipt binding is distinguished from certification of a human act, and the two claim-level observations are separately labelled rather than combined into a denominator.

## Predicate audit

The V5 checks compute or bind what the closer needs:

- the live criterion is rerun over its expressly bounded mapped-source pool and equality with `{6,22,25}` is required;
- entry 5's actual pool-external source is independently scored `(0,0,0)`;
- the sample is reproducibly redrawn;
- every receipt row uses ALL-of fragments, exact tokens, and per-entry identity checks;
- coverage and remainder are computed in both directions; and
- current paper tiers are parsed from the bibliography before the scoped metrics are calculated.

There are minor residual limitations, but none makes a stated substantive result false:

1. The standalone `COVERAGE` predicate checks set equality and does not literally include `all_ok`. Receipt integrity is enforced by the immediately preceding `RECEIPTS` check, and any failure enters the common `fails` list and makes the program exit nonzero. The closer as a whole therefore cannot go green with a failed receipt row, though conjoining `all_ok` would make the individual check name more self-contained.
2. Flag 6's three phrases are searched in the whole notes file rather than a parsed batch-9 block. In the current artifact they occur in the claimed entry-6 context; block scoping would merely harden against future textual relocation.
3. The source map remains a pragmatic parser rather than a full title/DOI/hash identity proof. The pool boundary and record-vs-reality limitations are explicitly disclosed.
4. The claim-level carrier lists remain observations rather than computed census metrics. V5 says exactly that.

These are hardening opportunities, not another free-rider or false receipt.

## Final ruling

Confirm B41V5 as the census closer of record:

- **readable corpus receipt coverage:** 39/39;
- **paper-tier miss rate:** 1 of 2—entry 22 hit, entry 5 missed;
- **observed paper-tier precision among the three mapped corpus flags:** 1 of 3;
- **claim-level sensitivity:** not measured, pending a frozen claim table and multiple-claims-per-paper rule.

The two operational causes of entry 5's miss are also fairly recorded: it was outside the scanned pool, and its actual text scores `(0,0,0)` under the vocabulary criterion.
