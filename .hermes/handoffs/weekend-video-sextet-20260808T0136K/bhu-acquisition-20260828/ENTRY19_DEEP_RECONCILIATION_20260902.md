# Entry-19 deep audit — reconciliation (Tori, 2026-09-02, STEP 3, queue draw #21)

**Entry 19 — I. Dymnikova (2019), "Universes Inside a Black Hole with the de Sitter Interior," Universe 5, 111.** Tier
**CONSISTENCY-ONLY**. Brief `ENTRY19_AUDIT_BRIEF_20260902.md` (full MDPI text, 1,333 lines).

## Verdict — BOTH SEATS AGREE, tier holds; no MUST-STOP; Easson map = INAPPLICABLE
| seat | token |
|---|---|
| codex (`ENTRY19_codex_DEEP_RESULT.md`) | `AUDIT_HOLDS_CONSISTENCY_ONLY` |
| claude-seat (`ENTRY19_claude_DEEP_RESULT.md`, blind; re-derived eqs. 13–18) | `AUDIT_HOLDS_CONSISTENCY_ONLY` |

## What both derived independently (Tori verified the citations)
1. **Premise vs derivation:** the regular Λ-black-hole parent with de Sitter core is imported (the author's own
   earlier solution); universe birth is a minisuperspace Wheeler–DeWitt/WKB tunnelling with a computed barrier and
   penetration factor — a conditional consistency result.
2. **"Favours a FLAT birth" — confirmed as the paper's stated conclusion (lines 1172–1176) but not a dynamical
   preference:** the tunnelling probability depends on k only through (k − B_s), which is fixed at ~10⁻⁶ by an
   IMPORTED anisotropy bound ΔT/T ≤ 10⁻⁵ "as in our Universe" (lines 1118–1122) before k is chosen, so it is the same
   for k = 0, ±1; "flat" wins by parsimony (|B_s| ≈ 3×10⁻⁶ vs ≈ 1). The one observed number is consumed as input.
3. **The claim-level exclusion is derived:** a barrier exists iff k − B_s > 0 (eq. 13), so flat/open births need
   B_s < −k — as stamped on 08-30.
4. **Our universe:** "speculate (at least not exclude)" that our Universe lies inside a regular black hole (lines
   1178–1181); no identification, no predicted number, no threshold.
5. **Easson map (entry 22, Prop. 2): INAPPLICABLE** — the parent satisfies its parent-side hypotheses, but the
   daughters are minisuperspace wave functions never matched to the parent as comoving no-shell closed-FRW regions;
   the proposition is silent on the one step (embedding) it is about. Neither restricts nor spares.
6. Minor arithmetic notes (claude-seat): a 3/2 constant in (17); (18) matches (k − B_s) = 10⁻⁶ not 3×10⁻⁶; dropped
   n-dependence; Cauchy-horizon instability unaddressed. No tier weight.

## Applied
Dated deep-audit annotation on entry 19 (tier word untouched). Queue recomputed.
