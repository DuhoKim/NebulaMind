# Entry-20 deep audit — reconciliation (Tori, 2026-09-02, STEP 3, queue draw #14)

**Entry 20 — Bronnikov, Melnikov & Dehnen (2007), "Regular black holes and black universes," GRG 39, 973–987**
(gr-qc/0611022). Tier **CONSISTENCY-ONLY**. Brief `ENTRY20_AUDIT_BRIEF_20260902.md`.

## Verdict — BOTH SEATS AGREE, tier holds; no MUST-STOP; Easson map = INAPPLICABLE
| seat | token |
|---|---|
| codex (`ENTRY20_codex_DEEP_RESULT.md`) | `AUDIT_HOLDS_CONSISTENCY_ONLY` |
| claude-seat (`ENTRY20_claude_DEEP_RESULT.md`, blind; re-derived eqs. 8, 10, 12–15, the throat criterion, Λ₋ = 3πρ₀/b³) | `AUDIT_HOLDS_CONSISTENCY_ONLY` |
| kimi | no output in 10 min on the 755-line source (its ceiling ~600 lines; recorded) |

## What both derived independently (Tori verified the citations)
1. **Exact existence witness:** regular static spherical solutions with a wrong-sign (phantom) scalar and a
   reconstructed potential, whose T-region is an expanding Kantowski–Sachs universe with a de Sitter far end
   ("black universes"); the geometric necessity r'' > 0 requires NEC violation (2r''/r = −φ'², line 219 with the
   phantom sign). Stability acknowledged, not solved; the paper's own Sec. 4.2 shows a w < −1 perfect fluid cannot
   support the solution — a tuned field-theoretic phantom is required (claude-seat, lines 566–571).
2. **"Our Universe originated from phantom-dominated collapse in another universe"** is "looks possible" (lines
   25–27) and "lead[s] to the idea … could appear" (lines 592–594); no collapse modelled, no FRW matching, no
   parameters; isotropisation "e.g., due to particle creation" and a viability condition imported by citation
   (z ≳ 1000, lines 595–597).
3. **Easson map (entry 22, Proposition 2): INAPPLICABLE, not restricting and not sparing on the merits.** Two
   daughter-side hypotheses fail by construction: the interior is Kantowski–Sachs (R × S², anisotropic), not a
   comoving no-shell closed-FRW daughter; and ANEC is violated on every radial null geodesic (T_kk ∝ −φ'² < 0),
   which is precisely what makes the throat exist. Entry 20 is a worked instance of Proposition 2's contrapositive.
4. **Directional content:** Λ_eff > 0 at the far end is the branch-selection criterion (C > 0 vs the AdS-wormhole
   branch, lines 287–296) — an input choice, not a risky output (A(a)); the Kantowski–Sachs-anisotropy remark is a
   "not excluded … if" compatibility clause. claude-seat flags Λ_eff > 0 as the one borderline item; both seats
   and Tori read it as selection, not prediction. No packet.

## Applied
Dated deep-audit annotation on entry 20 (tier word untouched), including the Easson map result the 08-19 ranked
list asked for.
