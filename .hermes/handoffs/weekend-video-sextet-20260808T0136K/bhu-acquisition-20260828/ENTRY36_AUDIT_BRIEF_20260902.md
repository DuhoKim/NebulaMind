# Entry-36 deep audit — blind-double brief (Tori, 2026-09-02, STEP 3, queue draw #19 under `DEPTH_QUEUE_RULE_20260902.md`)

**To:** codex + claude-seat, INDEPENDENT. Do NOT read each other's result or any file named ENTRY36_*_RESULT.
**Authority:** Duho, relay via Blanc 17:23 KST ("RESUME THE DEEP AUDIT"). **Boundary:** verdict + the case; change
no tier; a tier-adjacent outcome returns to Duho as a packet.

## The entry
**Entry 36 — J. Smoller & B. Temple (2000), "Cosmology with a shock-wave," Commun. Math. Phys. 210, 275–308**
(astro-ph/9812063). Tier **CONSISTENCY-ONLY**. Pinned source: `../bhu-reading-20260823/sources/smoller_temple_2000_clean.txt`
(3,797 lines — long; read it in full, in chunks). Read ONLY this file. Quote with line receipts.

## Disclosed prior findings — RE-DERIVE, do not rubber-stamp
- 2026-08-28 blind flag (QUALITATIVE-DIRECTIONAL) NOT adjudicated; 2026-09-01 sweep (codex + agy CONFIRMED): "derived
  distances normalize to observed H₀/T₀, indexed by unmeasured R*; no independent observable". **This audit is the
  proper adjudication of that flag** — and it must engage the paper's own strongest sentences: "the shock position at
  present time, as predicted by this model … The constraint on the shock position can [be] … no parameters in the
  problem other than the experimentally determined values of the Hubble constant and the background radiation
  temperature" (lines ~98–104).
- Entry 37 (PNAS 2003, audited today, tier held): the later shock-inside-a-black-hole construction; its bounds are
  conditional on a first-visibility epoch and a free parameter.

## The deep-audit axis: does the model PREDICT the present shock position from H₀ and T₀ alone?
1. **The construction.** The k = 0 FRW interior with the standard-model equation of state (radiation → matter),
   matched across a shock to a TOV exterior; which theorems are proved (existence, uniqueness, entropy conditions);
   what is R* (the "unmeasured" parameter the sweep named) — is it the shock's position at some reference time, and
   is it free or fixed?
2. **The claim at lines ~98–104.** Trace exactly what is computed: given H₀ and the CMB temperature T₀ (2.7 K →
   4000 K at decoupling, lines ~112, ~213), does the paper derive a NUMBER for the present shock radius (in Hubble
   lengths or Mpc), or a CONSTRAINT (inequality/window), or a one-parameter family indexed by R*? Quote the result
   with its equation number and give the number if one exists.
3. **Observability.** Where would the shock be relative to our past light cone / the Hubble length at present — inside
   or beyond? Does the paper say whether it is observable (a temperature edge, a density jump, an anisotropy) and by
   what? Is there any sentence stating a signature and its sign/magnitude?
4. **What "accounts for the observed Hubble constant and CMB temperature" means:** are H₀ and T₀ INPUTS used to
   normalise the FRW side (so the model reproduces them by construction), or OUTPUTS? Under the scheme, a number
   borrowed from the data it is compared against does not calibrate.
5. **Tier consequence, argued** (CONSISTENCY-ONLY / QUALITATIVE-DIRECTIONAL / PROSPECT / CALIBRATED-FALSIFIER; A(a);
   the lane may own a missing threshold, never a missing number). Say explicitly whether the 08-28 flag survives, and
   if a genuine present-shock prediction exists, state it as a candidate for Duho.

## Deliverable — write `ENTRY36_<seat>_DEEP_RESULT.md` in this directory
First line ONE token: `AUDIT_HOLDS_CONSISTENCY_ONLY` / `AUDIT_CHALLENGE_QUALITATIVE_DIRECTIONAL` /
`AUDIT_CHALLENGE_PROSPECT` / `AUDIT_CHALLENGE_CALIBRATED_FALSIFIER` / `UNDETERMINED_NEEDS_<resource>`. Then answers
1–5 with receipts and arithmetic, then one plain-language paragraph. Change no other files.
