# Entry-39 sweep audit — reconciliation (Tori, 2026-09-01)

**Entry 39 — N. J. Popławski (2012), "Big bounce from spin and torsion," GRG 44, 1007–1014**
(arXiv 1105.6127). Current tier **CONSISTENCY-ONLY**. Blind-double, Duho-authorized via Blanc ~15:30
(burn clamp overridden for this one audit). Brief: `ENTRY39_AUDIT_BRIEF_20260901.md`.

## Verdict — BOTH SEATS AGREE, tier holds

| seat | verdict |
|---|---|
| codex (`ENTRY39_codex_RESULT.md`, 49 lines) | `TIER_CONFIRMED_CONSISTENCY_ONLY` |
| agy (`ENTRY39_agy_RESULT.md`, 31 lines) | `TIER_CONFIRMED_CONSISTENCY_ONLY` |

**No tier change, no substance split → not a MUST-STOP.** Recorded as an annotation; **tier UNCHANGED**.

## What the two seats independently converged on

Both read all 451 source lines, enumerated every quantity the paper states, and classified each into
exactly one of the sweep's non-observational buckets — and neither found an observation-facing
number+threshold or a signed directional prediction:

- **Internal-consistency / bounce-background:** the bounce condition (`ε = −ε_S`, zero expansion,
  `1105.6127:90-102`), the minimum scale factor `a_bb = 4.9×10⁻⁵ m` (`:238`) and the three
  `a_bb/a₀ ≈ 1.2–2×10⁻³²` estimates (`:189,214,229`), `Ω_Pl = 5.5×10¹²²` (`:250`). Minimum-radius
  properties of the *unobservable* bounce, not present instrument-facing quantities.
- **Above-Planck / self-conceded validity limit:** the Standard-Model bounce density
  `ε_bb = 15.4 ε_Pl` (`:155-157`) — the paper itself says this is where classical ECSK "should be
  replaced by a quantum theory of gravity" (`:159-160`). So it cannot be a calibrated observational
  falsifier; it is a bounce condition exactly where the derivation withdraws classical authority.
- **Borrowed-from-data / external inputs:** SM degree counts `g≈100` / `g_b=28,g_f=90`, `m_t≈175 GeV`;
  `Ω_R = 8.8×10⁻⁵` and `a₀ = 2.9×10²⁷ m` from WMAP; `Ω_S = −8.6×10⁻⁷⁰` inserted from prior papers.
- **Hypothetical:** `g_f = 10⁵` (preonic substructure) → `ε_bb = 1.4×10⁻² ε_Pl` (`:161-164`), an
  explicit *example* conditional on unobserved fermions, not a prediction of their existence.

**The decisive point (both seats, independently):** the paper supplies only homogeneous-**background**
Friedmann + conservation equations and thermal/`a⁻⁴`,`a⁻⁶` scaling relations. It gives **no
perturbation variables, no mode equation, no bounce matching/transfer, no conversion to a late-time
observable** — so it cannot derive any CMB anisotropy, power-spectrum tilt/amplitude/feature,
gravitational-wave background, or relic abundance. The observation-facing passages it contains (GR
tests, bulk flows, GRB photons, WMAP) are retrospective motivation/inputs, never a derived prospective
number+threshold.

## Cross-checks that make the agreement load-bearing (not a shared blind spot)

- **Consistent with RQ-B.** Entry 39 is the same Popławski torsion-bounce mechanism family (8–12/51).
  RQ-B already found that mechanism has background field equations but no derivable perturbation
  transfer (settled UNDETERMINED). Entry 39 supplies nothing RQ-B found missing — independent
  corroboration of "background only, no observable."
- **Consistent with b36** (2026-08-30, both seats): entry 39 is a constructive bounce paper, not an
  obstruction. This audit addressed the *other* axis (concealed prediction) and also came up empty.
- **Both seats ran the absence discipline**, each naming a blind-spot class its keyword pattern would
  miss (an indirectly-worded / coined observable — an "echo"/"imprint") and mitigating it by reading
  the full derivation, not just grepping. Different wordings, same enumeration, same buckets — genuine
  convergence.

## Outcome

CONSISTENCY-ONLY is **correct**; the tier stands. Entry 39 annotated in the bibliography with this
finding, tier UNCHANGED. Nothing returns to Duho — the audit confirmed the existing tier, which is the
non-tier-adjacent outcome. Next parked-queue entry by the density rule: **27**.
