TIER_CONFIRMED_CONSISTENCY_ONLY

## Scope and conclusion

The pinned source contains no concealed calibrated falsifier and no qualitative-directional observational prediction. It constructs a homogeneous spin-fluid background bounce and calculates its internal conditions. Its sharpest numbers are either (i) conditions or sizes at the bounce, (ii) at/above the paper's conceded classical-validity limit, (iii) illustrative values conditional on unobserved particle content, or (iv) obtained by inserting present observational/cosmological inputs. No equation evolves perturbations through the bounce or maps the background solution to a CMB, matter-spectrum, relic-abundance, gravitational-wave, or other observable quantity. CONSISTENCY-ONLY therefore holds.

## The paper's numbers and their classification

I inventory the scientific quantities in the paper's title/abstract/body (not journal pagination, address, equation labels, or bibliography metadata).

1. **Model setup and exact internal relations — internal-consistency, not observational predictions.** The universe is assumed closed, `k=1`, homogeneous and isotropic, with fermions macroscopically averaged as a spin fluid (`1105.6127_clean.txt:63-64`). The torsion contribution is `epsilon_S=-(1/4) kappa s^2` and `p_S=epsilon_S` (`:74-80`); for unpolarized spins the cited spin-fluid result is `s^2=(1/8)(hbar c n)^2` (`:81-87`). Ultrarelativistic matter is approximated by `p≈epsilon/3`, while curvature and Lambda are neglected (`:88-90`). Setting `da/dt=0` defines the bounce and yields `epsilon=(1/4)kappa s^2=(1/32)kappa(hbar c n)^2`, or, with `hbar=c=1`, `epsilon=(pi/4)n^2/m_Pl^2` (`:90-102`). These coefficients (`1/4`, `1/8`, `1/32`, `pi/4`) and the zero-expansion condition are elements of the internal derivation, not observation-facing thresholds.

2. **Thermal formulae and constants — derivational inputs/internal consistency.** The equilibrium density is `epsilon(T)=(pi^2/30) g_*(k_B T)^4/(hbar c)^3`; `g_*=g_b+(7/8)g_f`; a species is counted as relativistic when `m_i c^2<k_B T` (`:104-120`). The fermion density is `n(T)=[zeta(3)/pi^2]g_n(k_B T)^3/(hbar c)^3`, with `zeta(3)≈1.202` and `g_n=(3/4)g_f` (`:121-131`). Substitution gives the exact conditional bounce-temperature formula, including `(64/270)^(1/2)`, `pi^(5/2)/zeta(3)`, and its dependence `(g_b+7g_f/8)^(1/2)/g_f`, in units of `T_Pl` (`:133-139`), and the bounce-density formula `epsilon_bb=xi (g_b+7g_f/8)^3/g_f^4 epsilon_Pl`, with `xi=(1/30)[64 pi^6/(270 zeta^2(3))]^2` (`:140-150`). These are background-state mappings conditional on particle content, not thresholds on an observable.

3. **Planck density — physical constant/validity marker, not a prediction.** The paper states `epsilon_Pl=5.1×10^96 kg m^-3 c^2` (`:146`). It is the normalization and the classical-validity boundary used by the paper, not a newly predicted measurement.

4. **Standard-Model particle inputs — external theoretical inputs.** For `T>m_t`, with `m_t≈175 GeV`, all known particles are treated as relativistic; assuming only known Standard-Model particles at equal temperatures gives `g_b=28`, `g_f=90` (`:152-154`; summarized only roughly as `g≈100` at `:19-20`). The top mass and degree counts are inputs, not predictions of ECSK.

5. **Standard-Model bounce density — above-Planck/validity-limit internal number.** Those inputs give `epsilon_bb=1.54×10^1 epsilon_Pl` (`:155-157`; “about 15” at `:20`). The paper immediately says this is at the Planck scale, where classical ECSK “should be replaced by a quantum theory of gravity” (`:159-160`). Thus 15.4 Planck densities cannot be a calibrated observational falsifier under the brief's bar; it is a bounce condition precisely where the derivation concedes loss of classical authority.

6. **Extra-fermion example — hypothetical internal number.** If `g_f=10^5` and `g_b/g_f` is held constant, the formula gives `epsilon_bb=1.4×10^-2 epsilon_Pl` (`:161-164`). This is explicitly an example conditional on speculative additional fermions/preonic substructure (`:161-163`), not a prediction of their existence, an observable threshold, or a unique model output. The claimed direction “more fermionic degrees of freedom -> lower bounce density” (`:161`) concerns an unobserved internal bounce condition, not a signed present observable.

7. **Radiation evolution and present degree counts — internal evolution plus standard inputs.** Radiation is assumed to scale as `epsilon_R~a^-4` (`:172-176`); present relativistic counts are inserted as `g_b=2` (photon) and `g_f=6` (neutrinos/antineutrinos) (`:178-181`). These counts are thermal-history inputs. Likewise the torsion contribution scales as `epsilon_S~a^-6` and obeys `epsilon_S|bb=-epsilon_bb` (`:193-204`). These relations determine homogeneous background bookkeeping, not an independently specified observable.

8. **Current critical density and radiation fraction — borrowed from data.** The calculation inserts `epsilon_cr=9.24×10^-27 kg m^-3 c^2` (`:181`) and `Omega_R=8.8×10^-5`, attributed to earlier work/WMAP (`:187`). It then obtains `a_bb/a_0=2×10^-32` (`:183-191`). The ratio is an inferred minimum-scale background quantity, not a directly measurable present observable, and it is anchored to the inserted present radiation data.

9. **Spin-torsion density parameter route — borrowed/inherited input and internal result.** The paper inserts `Omega_S=-8.6×10^-70` from prior papers (`:206-214`) and obtains `a_bb/a_0=1.7×10^-32` (`:208-216`), which it selects as the more accurate normalized minimum (`:233-235`). Because `Omega_S` is “put” into the calculation rather than derived here from an independent observable, this does not turn the resulting bounce ratio into a testable forecast.

10. **Combined route — internal cross-check.** Eliminating densities gives equation (22), refining the older `sqrt(-Omega_S/Omega_R)` expression (`:220-226`); inserting the same `Omega_R` and `Omega_S` yields `a_bb/a_0=1.2×10^-32` (`:227-231`). The paper explains differences among `2×10^-32`, `1.7×10^-32`, and `1.2×10^-32` through pair-annihilation treatment and powers of `Omega_R` (`:218`, `:233-235`). These are alternative internal estimates, not rival observational predictions.

11. **Absolute minimum scale — borrowed-from-data normalization plus internal bounce inference.** The present scale `a_0=2.9×10^27 m` is explicitly said to be derived from WMAP data; multiplying it by the selected ratio gives `a_bb=4.9×10^-5 m` (`:235-240`; rounded to `≈50 micrometres` and `10^32` times smaller than today at `:21`). The absolute FLRW scale factor also depends on the paper's closed-universe normalization. It is a minimum-radius property of the unobservable bounce, not a present instrument-facing quantity or a threshold observation could cross.

12. **Huge Planck density parameter — derived normalization/internal explanation.** The paper defines `Omega_Pl=epsilon_Pl/epsilon_cr` and states `Omega_Pl=5.5×10^122`, producing `|Omega_S|<<1` in its relation (25) (`:242-250`). The numeric value follows from the Planck density and the inserted current critical density; it is not an observational forecast. The qualitative claims that this small torsion parameter can explain flatness/horizon behavior (`:249`) and that prior work explains present flatness, homogeneity, and isotropy (`:46-48`) supply no signed residual, spectrum, or threshold against which data can contradict this paper.

13. **Numbers reported from prior work or other frameworks — context, not predictions of this calculation.** Earlier torsion work found a bounce density “a few orders of magnitude” above Planck (`:57-60`), while LQG is said to bounce at the Planck scale (`:60`, `:159-160`). These are comparisons, not outputs tested here. The statements that high-energy gamma-ray-burst observations suggest continuity below the Planck length and possibly classical behavior above Planck (`:168-170`) use observations as motivation for extrapolating classical ECSK; they do not predict a photon time delay, energy dependence, or numerical bound. The large-scale bulk-flow measurements are likewise cited as a suggestion of interaction with other spacetime regions (`:53-55`), with no derived magnitude, sign, angular pattern, or threshold.

## Deciding derivation and assumptions

The strict chain is: assume a closed homogeneous/isotropic FLRW background; model fermions by a macroscopically averaged, unpolarized spin fluid; adopt the cited dispersion `s^2=(hbar c n)^2/8`; assume ultrarelativistic kinetic equilibrium and `p≈epsilon/3`; neglect `k` and `Lambda` at the bounce; define the bounce by zero background expansion; use equilibrium Standard-Model degree counts and equal temperatures; and propagate homogeneous radiation/torsion densities with `a^-4`/`a^-6` scalings. Equations (5), (7), and (9) then algebraically fix `T_bb` and `epsilon_bb` as functions of `g_b,g_f` (`:90-150`). Equations (15)-(24) infer a minimum scale using present cosmological inputs (`:172-240`). The spin averaging is asserted with citations, not derived in this paper (`:34-35`, `:81-85`).

That chain is strict enough to calculate a **background bounce condition**, but not an observable. The Standard-Model value lies at `15.4 epsilon_Pl`, where the author expressly withdraws the adequacy of classical ECSK (`:155-160`). Avoiding that limit requires either hypothetical `g_f=10^5` (`:161-164`) or the observationally motivated but unproved extrapolation that spacetime remains classical beyond the Planck regime (`:168-170`). Neither option fixes a present measurable signature.

Most decisively, the paper supplies only Friedmann/background conservation equations (`:63-100`) and thermal/background scaling relations (`:104-150`, `:172-250`). It gives no perturbation variables, initial perturbation state, mode equation, matching/transfer prescription across the bounce, or conversion to late-time observables. Therefore it cannot derive a scalar/tensor spectrum, tilt, amplitude, feature, non-Gaussianity, CMB anisotropy, gravitational-wave background, or relic abundance. “LQG would not be able to provide cosmological signatures” (`:165-166`) is a conditional statement about suppressing a competing mechanism, not a positive ECSK observable with a sign or falsification threshold.

## Absence-search discipline

After reading all 451 source lines, I searched case-insensitively for the broad pattern family `perturb|spectrum|spectral|anisotrop|cmb|microwave|relic|abundance|nucleosynth|gravitational wave|tensor|scalar|non.?gauss|observable|observation|measure|signature|photon|galaxy|bulk flow|WMAP|density parameter|redshift|temperature|scale factor|Planck|GeV|10^|×|≈|= [0-9]|<|>|≪`. A keyword search could miss a prediction described indirectly (for example, an angular-correlation feature without the words “CMB” or “spectrum”), so I separately inspected the complete derivation and every prose claim in the full read. The only observation-facing passages found are retrospective/supportive uses of existing GR tests, bulk flows, gamma-ray photons, and WMAP inputs (`:39-40`, `:53-61`, `:168-181`, `:236`); none is a model-derived prospective number+threshold or signed observable.

Accordingly, there is no tier-adjacent finding to return: the current CONSISTENCY-ONLY classification is correct.
