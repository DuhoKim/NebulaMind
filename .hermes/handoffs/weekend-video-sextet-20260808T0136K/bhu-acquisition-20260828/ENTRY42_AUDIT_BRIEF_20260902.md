# Entry-42 deep audit — blind-double brief (Tori, 2026-09-02, STEP 3, queue draw #12 under `DEPTH_QUEUE_RULE_20260902.md`)

**To:** codex + kimi, INDEPENDENT. Do NOT read each other's result or any file named ENTRY42_*_RESULT or ENTRY_4247_*.
**Authority:** Duho, relay via Blanc 17:23 KST ("RESUME THE DEEP AUDIT"). **Boundary:** verdict + the case; change
no tier; a tier-adjacent outcome returns to Duho as a packet.

## The entry
**Entry 42 — P. F. González-Díaz (1991), "Baby universe metric equivalent to an interior black-hole metric," Phys.
Lett. B 261, 357–362.** Tier **CONSISTENCY-ONLY**. Pinned source (OCR of the 1991 scan; expect OCR noise):
`../bhu-reading-20260823/sources/gonzalez-diaz_1991_plb261_357_clean.txt` (503 lines). Read ONLY this file. Quote with
line receipts.

## Disclosed prior findings — RE-DERIVE, do not rubber-stamp
- 2026-09-01 sweep (codex DIRECTIONAL vs agy + kimi CONSISTENCY-ONLY; Duho ruling 42a → CONSISTENCY-ONLY): a conformal
  equivalence between the maximally extended n = 1 wormhole metric and the Schwarzschild interior (gravitational
  radius = half the neck radius), reinterpreting evaporation as baby-universe branching; its observable-looking result
  — a real observer sees positive Hawking flux (T₀₀) = (129πM²)⁻¹ while an ideal observer sees 0 — was judged
  standard Hawking radiation restated. That split is what the deep audit should settle with the equations.

## The deep-audit axis
1. **The equivalence.** State the two metrics and the conformal map; is the equivalence DERIVED (with the factor
   relating r_g to the neck radius shown), or asserted? Is it exact or only in some limit? What is "n = 1"?
2. **What "baby universe sector" means physically:** does the paper argue that the Schwarzschild interior IS a baby
   universe (an interior cosmology), or that a wormhole's baby-universe sector is mathematically equivalent to it?
   Does it claim anything about OUR universe being such an interior?
3. **The flux result.** Re-derive or at least check (T₀₀)_bh = (129πM²)⁻¹ against the standard Hawking flux for a
   Schwarzschild hole (L ∝ 1/M², with the standard coefficient); is "129π" a typo, a different normalisation, or a
   genuinely different number? Is the "ideal observer sees 0" a derived statement or an interpretive one? Does any
   of this differ from standard Hawking radiation in a way an observation could detect?
4. **Observation-facing content:** any signed direction or number + threshold for our universe? If the only
   numbers are S = πM², T = 1/(4πM) with M free, say so.
5. **Tier consequence, argued** (CONSISTENCY-ONLY / QUALITATIVE-DIRECTIONAL / PROSPECT / CALIBRATED-FALSIFIER; A(a);
   the lane may own a missing threshold, never a missing number).

## Deliverable — write `ENTRY42_<seat>_DEEP_RESULT.md` in this directory
First line ONE token: `AUDIT_HOLDS_CONSISTENCY_ONLY` / `AUDIT_CHALLENGE_QUALITATIVE_DIRECTIONAL` /
`AUDIT_CHALLENGE_PROSPECT` / `AUDIT_CHALLENGE_CALIBRATED_FALSIFIER` / `AUDIT_FLAG_MEMBERSHIP` /
`UNDETERMINED_NEEDS_<resource>`. Then answers 1–5 with receipts and arithmetic, then one plain-language paragraph.
Change no other files.
