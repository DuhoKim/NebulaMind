# Entry-4 deep audit — reconciliation (Tori, 2026-09-02, STEP 3, queue draw #28) — SPLIT, third seat, PACKET TO DUHO

**Entry 4 — H. Knutsen (2009), "The idea of the universe as a black hole revisited," Grav. Cosmol. 15, 273.** Tier
**THEORETICAL-OBSTRUCTION**, stamped by Duho 2026-09-02 ("a for both") with the domain: smooth FLRW-perfect-fluid /
vacuum matching at Pathria's r_b = 1 "forces a static sphere merged with its horizon ('cannot describe our expanding
universe')"; shells and other boundaries not excluded. Brief `ENTRY4_AUDIT_BRIEF_20260902.md`.

## Verdict — SPLIT 1–1, third seat rules DOMAIN_NARROWER; tier and stamped domain UNCHANGED pending Duho
| seat | token |
|---|---|
| codex (`ENTRY4_codex_DEEP_RESULT.md`) | `AUDIT_HOLDS_THEORETICAL_OBSTRUCTION` (domain as stamped) |
| claude-seat (`ENTRY4_claude_DEEP_RESULT.md`, blind) | `AUDIT_DOMAIN_NARROWER` |
| third seat (`ENTRY4_THIRD_SEAT_ADJUDICATION.md`, read both + source, sanity checks mandated) | `DOMAIN_NARROWER` |

## The algebra (third seat, with line receipts; Tori read the passage, lines 1063–1088, and did not independently
re-derive the two-branch step — recorded honestly)
1. Knutsen's junction (continuity of first and second fundamental forms, eqs. 17–43) is displayed and correct; it
   forces p = 0 and m = (4πG/3c²)ρa³r_b³ for every boundary — dust and the mass formula are genuinely derived.
2. At r_b = 1, combining eqs. 34 and 36 gives Ȧ²/c² = (1 − r_b²) − B(A) → B·(Ȧ² + c²B) = 0: two branches. The
   "static" branch (B = 0, Ȧ = 0) solves only the B-multiplied equations and violates Knutsen's own eq. 19 (a
   horizon surface is null) and eq. 24 (0/0, not > 0). The other branch (Ṫ = 0, B = −Ȧ²/c² ≤ 0) satisfies the full
   junction: a non-static zero-Killing-energy Oppenheimer–Snyder ball expanding to A_max = R_s and recollapsing, its
   surface on or inside the horizon at all times.
3. Knutsen's words (lines 1084–1088: "static … merges with its event horizon … cannot describe our expanding
   universe") are supported by the algebra only as "at/inside the horizon", not "static": the static reading divides
   ṪB = 0 by Ṫ under an assumption (eq. 24) the result violates. codex's verdict rests on that step. claude-seat's
   over-reach ("never visible from outside") is corrected by the third seat: white-hole-phase light reaches the
   exterior; what survives is that the surface itself never enters the static exterior region.

## What survives as a derived obstruction (third seat's replacement wording, for Duho)
At r_b = 1 no smooth matching puts the fluid surface in the T-timelike exterior region (B > 0); the surface touches
the horizon only at maximum expansion, A_max = R_s. The clauses "forces a static sphere merged with its horizon" and
"cannot describe our expanding universe" are not derived. **Note:** the surviving content realises Pathria's
a_max = R_s identity as the junction condition rather than refuting it — which makes the obstruction LABEL itself
tier-adjacent.

## Applied
No change to the stamped domain or tier (Duho's stamp). Annotation on entry 4: "domain under challenge, packet
filed". Packet in `OPEN_QUESTIONS_FOR_DUHO.md`.
