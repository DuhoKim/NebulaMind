# Entry-52 deep audit — blind-double brief (Tori, 2026-09-02, STEP 3, queue draw #5 under `DEPTH_QUEUE_RULE_20260902.md`)

**To:** codex + kimi, INDEPENDENT. Do NOT read each other's result or any file named ENTRY52_*_RESULT.
**Authority:** Duho, relay via Blanc 17:23 KST ("RESUME THE DEEP AUDIT"). **Boundary:** verdict + the case; change
no tier; a tier-adjacent outcome returns to Duho as a packet.

## The entry
**Entry 52 — G. Unger & N. J. Popławski (2019), "Big Bounce and Closed Universe from Spin and Torsion," ApJ 870,
78** (arXiv 1808.08327). Tier **CONSISTENCY-ONLY**. Pinned source: `../bhu-reading-20260823/sources/1808.08327_clean.txt`
(717 lines). Quote with line receipts. Read ONLY this file.

## Disclosed prior findings — RE-DERIVE, do not rubber-stamp
- 2026-08-30 (Q7 ruling, CGATE_Q7): the headline "a closed universe exists only when the product of the scale factor
  and temperature exceeds a threshold" is an existence exclusion inside the construction (two thresholds, see the
  record); tier stays CONSISTENCY-ONLY. That axis is settled.
- 2026-09-01 sweep (batch 1/2, ruling A(a)): "closed" counts as directional only when DERIVED; here it was read as an
  assumed ansatz → tier holds.
- Entries 9/10/11 (audited today): shared closures; printed numbers with unstated inputs in each.

## The deep-audit axis
1. **The threshold result.** Trace the derivation of the closed-universe existence condition (the product aT
   threshold; eqs. ~21 onward, lines ~186–255): which steps are equations, which closures are borrowed (spin fluid,
   s² ∝ n², kinetic equilibrium, fixed g_*), and is "closed" now DERIVED anywhere (a dynamical selection of k = +1),
   or still assumed with a condition attached?
2. **Numbers.** Extract every printed number (a_cr, T thresholds, expansion factors, particle-production
   parameters) with inputs; recompute at least one. Any unstated inputs?
3. **The CMB-parameter claim, line 54:** *"This expansion also predicts the cosmic microwave background radiation
   parameters that are consistent with the Planck 2015 observations, as was shown in SD"* (SD = Desai & Popławski,
   Phys. Lett. B 755, 183 (2016), reference line ~593). Answer precisely: (a) does THIS paper derive any CMB
   parameter (n_s, r, amplitude) itself, or only cite SD? (b) which quantities does it say SD predicted, and with
   what free inputs (β? number of bounces?), as far as this paper states; (c) does this paper use the Planck
   consistency as a constraint on its own parameters anywhere (e.g. to fix β or the number of bounces)? Do NOT
   fetch SD; report only what the pinned text says.
4. **Observation-facing content of THIS paper:** any derived signed direction (curvature sign DERIVED?), any
   number + threshold an observation could hit (e.g. a present-day Ω_k window, a bounce count constrained by
   data), or none?
5. **Tier consequence, argued** (CONSISTENCY-ONLY / QUALITATIVE-DIRECTIONAL / PROSPECT / CALIBRATED-FALSIFIER; the
   lane may own a missing threshold, never a missing number; a cited prediction in another paper belongs to that
   paper, not to this entry — say so explicitly if that is the situation).

## Deliverable — write `ENTRY52_<seat>_RESULT.md` in this directory
First line ONE token: `AUDIT_HOLDS_CONSISTENCY_ONLY` / `AUDIT_CHALLENGE_QUALITATIVE_DIRECTIONAL` /
`AUDIT_CHALLENGE_PROSPECT` / `AUDIT_CHALLENGE_CALIBRATED_FALSIFIER` / `UNDETERMINED_NEEDS_<resource>`. Then
answers 1–5 with receipts and arithmetic, then one plain-language paragraph. Change no other files.
