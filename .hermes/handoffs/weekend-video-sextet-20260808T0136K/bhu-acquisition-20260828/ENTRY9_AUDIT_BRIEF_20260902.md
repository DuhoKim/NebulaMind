# Entry-9 deep audit — blind-double brief (Tori, 2026-09-02, STEP 3, queue draw #2 under `DEPTH_QUEUE_RULE_20260902.md`)

**To:** codex + claude-seat, INDEPENDENT. Do NOT read each other's result or any file named ENTRY9_*_RESULT.
**Authority:** Duho, relay via Blanc 17:23 KST ("RESUME THE DEEP AUDIT"). **Boundary:** verdict + the case; change
no tier; a tier-adjacent outcome returns to Duho as a packet.

## The entry
**Entry 9 — N. J. Popławski (2010), "Cosmology with torsion: an alternative to cosmic inflation," Phys. Lett. B
694, 181–185** (arXiv 1007.0587; erratum PLB 701, 672). Tier **PROSPECT** ("names a verification route —
inherited corrections coupling to other fields — but defines no sensitivity floor or forecast amplitude").
Ranked target 1 of the bibliography: "the only published multi-paper BHU mechanism with explicit field equations".
Pinned source: `../bhu-reading-20260823/sources/1007.0587_clean.txt` (355 lines). Quote with line receipts. If the
erratum text is pinned beside it (search the sources directory for "erratum"/"701"), use it; if not, say so.

## Disclosed prior findings — RE-DERIVE, do not rubber-stamp
- Record: "derives present-day torsion density Ω_S = −8.6×10⁻⁷⁰" (line ~145); "WMAP: Ω = 1.002" quoted (line ~140).
- RQ-B (2026-09-01): background bounce has field equations, no perturbation action; β free; fluctuation
  derivation "remains to be done". Entry 11 (audited today) shares the closures: Papapetrou spin fluid,
  random-spin averaging, s² = (ħc n_f)²/8, k = +1 written in.

## The deep-audit axis: is what is claimed DERIVED, and is any number concealed or misplaced?
1. **Ω_S = −8.6×10⁻⁷⁰.** Trace its derivation: inputs (present fermion number density? which species? the
   spin-fluid coefficient), the formula, the arithmetic. Recompute it. Does the erratum change it? Is it a
   quantity any instrument could measure (compare to the precision of Ω_total from CMB, ~10⁻³)? By the scheme, a
   number "placed beyond observability" does not earn CALIBRATED-FALSIFIER — say explicitly whether that is
   the case here and why.
2. **Flatness and horizon "solved".** Is the flatness argument the same bounce-epoch Ω_min − 1 = 4cτ/a_i
   construction as entry 11 (i.e. never propagated to a present-day Ω_k), or something else? Is "closed"
   derived or assumed? Does line ~140's "WMAP Ω = 1.002" function as a prediction, a consistency check, or a
   selection of data?
3. **The PROSPECT route.** What exactly does the paper name as testable ("inherited corrections coupling to
   other fields"? something else)? Quote it. Does it give any amplitude, scale, or instrument? Does the tier
   PROSPECT fit ("names a route without a target"), or is the route so unspecified that CONSISTENCY-ONLY is
   the honest bin — or so specified that QUALITATIVE-DIRECTIONAL (a signed direction on an observable,
   DERIVED) is earned?
4. **Anything else observation-facing** (a signed curvature prediction that is derived rather than assumed,
   a relic, a bound on the bounce scale that an experiment could reach)? Extract with inputs.
5. **Tier consequence, argued** under the scheme (PROSPECT / CONSISTENCY-ONLY / QUALITATIVE-DIRECTIONAL /
   CALIBRATED-FALSIFIER; the lane may own a missing threshold, never a missing number; A(a): assumed closure
   is not directional).

## Deliverable — write `ENTRY9_<seat>_RESULT.md` in this directory
First line ONE token: `AUDIT_HOLDS_PROSPECT` / `AUDIT_CHALLENGE_CONSISTENCY_ONLY` /
`AUDIT_CHALLENGE_QUALITATIVE_DIRECTIONAL` / `AUDIT_CHALLENGE_CALIBRATED_FALSIFIER` /
`UNDETERMINED_NEEDS_<resource>`. Then answers 1–5 with receipts and arithmetic, then one plain-language paragraph.
Change no other files.
