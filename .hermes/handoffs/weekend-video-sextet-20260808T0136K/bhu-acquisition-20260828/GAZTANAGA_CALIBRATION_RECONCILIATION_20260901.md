# Gaztañaga cutoff calibration — result (Tori, 2026-09-01)

Duho RELAY "calibrate the Gaztañaga cutoff (clamp overridden)." Independent blind derivations: codex
(`GAZTANAGA_CALIBRATION_codex_RESULT.md`, 103 lines, ran CAMB) + agy (`..._agy_RESULT.md`, 80 lines, ran CAMB).

## Verdict — UNDETERMINED (both seats agree). Tier stays QUALITATIVE-DIRECTIONAL; NO calibration achievable.

| seat | verdict |
|---|---|
| codex | `UNDETERMINED_NEEDS_PRIMORDIAL_AMPLITUDE_AND_UNIQUE_CUTOFF_TRANSFER` |
| agy | `UNDETERMINED_NEEDS_SHAPE` |

The cutoff cannot be turned into a calibrated falsifier from the published Gaztañaga model. This is an
honest, non-circular negative (the RQ-B pattern), independently reached by both seats with agreeing numerics
— not a fabricated number.

## What IS fixed (the directional content, a priori — this is why the tier is already DIRECTIONAL)

The cutoff **location**: θ_cut ≈ 60° (ℓ_cut ≈ 3; k_cut = π/R), derived from the background causal horizon
χ_§ = η(a_i) via ρ_§ = ρ_Λ, i.e. from H₀/Ω_Λ and r_S = 2GM = H_Λ⁻¹ — before the CMB check, no free parameter.
codex `2003.11544:300-343,405-416`; `2204.11608:175-188,292-306`.

## What is NOT fixed (why it cannot be calibrated)

Both seats, independently, found the model supplies neither the amplitude nor a unique shape:
1. **Primordial amplitude A** — the papers call the origin of δ_T ≈ 10⁻⁵ a "remaining mystery" and say further
   work is needed "to estimate the perturbations" produced by the bounce (`2204.11608:330-340`); other pins only
   "assume small random amplitude fluctuations" (`sym14101984:93,520`). So C_ℓ = A·F_ℓ and S₁/₂ = A²·G with A free.
2. **A unique cutoff window/shape** — sharp vs smooth is never chosen (`2003.11544:251-253`).
3. **Which space, and they are NOT equivalent** — the papers give a Fourier statement (sharp k<π/R,
   `2204.11608:292-295`), an angular statement (no correlations θ>60°, `2003.11544:405-418`), and an illustrative
   ℓ<5 suppression — and **a sharp k-cutoff does NOT produce C(θ)=0 for θ>60°** (projection leakage). Plus a
   bounce→curvature transfer and the late-time ISW/lensing transfer, neither supplied.

## The numerics (two independent CAMB runs, agreeing)

- **ΛCDM mean-curve S₁/₂ ≈ 34,913 (agy) / 34,916 (codex) μK⁴**; quadrupole D₂ ≈ 1022 μK².
- **Planck measured S₁/₂ ≈ 1142–1209 μK⁴** (SMICA 1142.4 / SEVEM 1146.2 / NILC 1156.6 / Commander 1209.2;
  `1906.02552:2416-2431`); ΛCDM tail **>99.9%** (fewer than 0.1% of ΛCDM sims at/below observed) — the known anomaly.
- **Model prediction SLIDES with the unsupplied choice:**
  - sharp **angular** cutoff C(θ>60°)=0 → S₁/₂ = **0** (accommodated by Planck's low value);
  - sharp **k** cutoff P(k)=0, k<π/R → S₁/₂ ≈ **6,905 μK⁴** — which Planck's ~1,150 would **REFUTE**;
  - smooth suppression / free A → anywhere **0 → ~34,900**.

Because the model's own descriptions disagree (sharp-k vs sharp-θ give 6,905 vs 0), and A is free, no single
number or threshold follows. Forcing one interpretation to match Planck would be tuning to the observation the
model is meant to predict — the brief's non-circularity bar forbids it.

## What a real calibration would require (the future-theory target, now precisely named)

(i) a predicted primordial normalization A; (ii) one explicit window W(k) with the epoch/meaning of R;
(iii) the bounce→adiabatic/isocurvature transfer + phase statistics; (iv) the late-time observed-temperature
transfer/covariance (ISW, lensing). Supply these and F_ℓ, G, a likelihood, and a refutation threshold become
computable. This is original theory the published papers do not contain.

## Outcome

**Tier UNCHANGED — entries 23/24/25/26/27 stay QUALITATIVE-DIRECTIONAL.** The cutoff is a genuine a-priori
*scale* prediction (that is the directional content) but is NOT calibratable from the model. Returned to Duho:
the honest answer to "calibrate the cutoff" is that it can't be, and here is exactly why + what original theory
it would take. No bibliography tier edit (nothing changed).
