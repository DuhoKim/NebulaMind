# BHU lane — for Duho at 07:00, 2026-09-02

First written 23:18 KST, **rewritten 23:30 after you asked about the seats** — the earlier version's
"seat outage blocker" is gone because the cause turned out to be mine and is fixed. Plain language;
receipts are in the commits. **Nothing below is decided. Three things need your word.**

---

## The short version

You asked me to start (A) — calibrate or kill the 60° cutoff. I did. **My method was refuted by both
gate seats. I rebuilt it, and both seats refuted the rebuild too.** Program (A)'s constructive output
is currently **zero**. What survives is two verification findings and a much sharper idea of what a
real attempt would have to do.

Yesterday's score, plainly: **six of my constructions were refuted** — the spin-parity amendment,
Program (A)'s admissible class, its decision rule, my reading of the source, the "at most ~3%" bound,
and the six-times-overshoot finding. **I refuted one of a gate's** (its "22°, not 60°" charge against
the paper) and **one of my own diagnoses** (the "seat outage", which was my bug). Nothing was
published, nothing was claimed outside this lane, and no tier moved — the gating caught all of it
first, which is the system working, but it is not a good yield.

## What actually got established

**1. The corpus's 60° prediction is sound as a scale prediction.** The paper derives its causal scale
from Ω_Λ = 0.69 — measured from supernovae and BAO, *not* from the CMB correlation being tested — and
gets ~57°. Non-circular. A gate charged that the paper's own numbers give 22°; **I checked and that
charge is wrong** (it substituted a different quantity for the causal scale). The "one real a-priori
prediction" label holds.

**2. The cutoff cannot be calibrated, in the author's own words.** There is no model of the initial
conditions in the paper — he writes that quantifying it is "impossible without a model for the
initial conditions," and the paper he cites for one resolves to "In preparation." Three prior
attempts didn't fail for lack of effort; there is nothing there to complete.

**3. MY ONE CONSTRUCTIVE RESULT WAS REFUTED BY BOTH SEATS — it does not survive.** I had claimed
the paper's own most favourable implementation leaves six times more correlation than observed, so
the causal cut only partially suppresses. **Both gates refuted it, independently and for the same
core reason:** a sharp cut in Fourier space is not the same condition as "no correlation beyond 60°"
in real space — it produces ringing and long tails by construction — so the leftover correlation I
measured is probably an artifact of *my implementation*, not a property of the model. The honest
sentence is "this particular Fourier-cut implementation gives 6,897," not "the paper's model gives
6,897." Codex added a point neither I nor agy saw: `S₁/₂ = 0` demands the correlation vanish across
the whole 60°–180° range — an infinite set of constraints — which tuning one cutoff scale cannot
generally achieve.

I also withdrew an earlier claim that the cut "moves the anomaly to at most ~3%": the gate broke it
with my own printed table, where a smoothed cut suppresses the statistic *more* than the hard cut. I
had read one column and stated a conclusion the neighbouring column contradicted.

**And the two seats disagreed on one physics point, where codex is right:** agy said including ISW
would *strictly* worsen the model's fit; codex pointed out the cross term in the squared integral can
be negative, so that isn't guaranteed. I had repeated agy's version — it is withdrawn.

**So Program (A)'s constructive output so far is zero.** What survives is points 1 and 2 above, both
verification findings, plus a much clearer specification of what a real attempt would require: build
the causal condition in real space, apply the actual sky mask and estimator to every simulation,
separate the primordial/ISW/cross contributions, and derive the cutoff scale and its convention from
the source instead of choosing it.

## What needs your word

1. **Program (A)'s direction** — and option one is weaker than it looks. **(1)** do the repairs the
   gates listed (real-space construction, actual mask, ISW/cross terms); **(2)** write it up as the
   documented no-go; **(3)** stop. **Both step-2 seats say the repair does not exist**: codex —
   *"there is no principled repair available from this source alone"*, and doing it anyway *"would
   not turn either constraint into a prediction licensed by the paper."* **My recommendation is (2)**,
   in the shape kimi specified: report the underdetermination, exhibit the two incompatible readings,
   emit no number from either alone. Full costs in `OPEN_QUESTIONS_FOR_DUHO.md`.
2. **Any tier movement on entries 23–27.** Point 3 bears on them, and so does something sharper that
   surfaced in a recovered verdict: **codex argues the paper's core inference may be a non-sequitur**
   — *"equating absence of causal influence with zero correlation; common initial conditions can
   correlate causally disconnected regions."* That is the standard inflationary point, and it attacks
   the 60° prediction's **logic** rather than its arithmetic. Related, from kimi: the paper's one
   genuinely derived condition, `Φ(χ>χ§)=0`, **was never imposed on the perturbed solution** by
   anyone, including me. **I have moved nothing** and will not without you.
3. **The 5 paywalled papers** (Pathria, Good, Stuckey, Knutsen, Frolov–Markov–Mukhanov) — your fetch;
   drop them in `~/Downloads` and I pin and sweep.

## The seat problem — resolved, and the answer got worse for me twice

You asked whether agy was prompting. **It was not** — its pane sits idle at a normal prompt, and a
trivial call returns `PONG` in 11 seconds. Run in the foreground, all three seats answer (agy 11 s,
codex 6 s, kimi 10 s).

**But the final answer is not "backgrounding killed them", which is what I told you first.** A later
check found that **all four dispatches I had recorded as silent failures had written complete
verdicts** — they were sitting untracked in the lane the whole time. The seats kept working after the
harness reported the wrapper finished; I checked within seconds, saw an absent or empty file, and
declared failure. **I had measured a race, not an outcome**, and then built three theories on top of
it (huge file → file reading → total outage), each more confident than the last while the evidence
never improved.

**Two things came out of that recovery, and you should have both:**

1. **My conclusions were better supported than I reported to you.** `READING_C` — that the paper
   licenses no sharp perturbation condition — is **4-seat unanimous**. The refutation of the
   spin-parity amendment is **3-seat unanimous**. I had described both as thinner.
2. **The warning that would have prevented my two biggest errors was in a file I called empty.**
   kimi's verdict closes: *"do not let the tractability of A masquerade as a prediction of the
   paper."* I then did exactly that — built both refuted claims on the tractable reading.

**Rules recorded:** an empty output file after a completion signal is not evidence of failure —
re-check on a delay; list the whole directory before declaring a seat dead; never build an
explanatory theory on one negative observation of an asynchronous process; and **untracked files are
unread files** (`git status` showed these for hours and I filtered them out as noise every time).

**Nothing here blocks you. The lane gates normally, and always could.**

## Where to look

- `PROGRAM_A_PVALUE_RESULT_20260901.md` — the numbers, the gate verdict, and the retraction.
- `AGATE_PROGRAM_A_PVALUE_agy.md` and `CGATE_PROGRAM_A_PVALUE_codex.md` — both gates, in
  their own words; independent (codex was barred from reading agy's).
- `PROGRAM_A_CHARTER_20260901.md` — carries the refutation of my method at the top.
- `PROGRAM_A_STEP2_FINDING_20260901.md` — why the question changed.
- `HARNESS_DEFECT_REGISTER.md` §1ak–§1aq — yesterday's lessons, including the seat misdiagnosis.
- `OPEN_QUESTIONS_FOR_DUHO.md` — standing decisions, with (B) marked withdrawn.
- `AGATE_PROGRAM_A_STEP2_codex.md`, `KGATE_PROGRAM_A_STEP2_kimi.md` — the two recovered step-2
  verdicts; the sharpest material in the whole program is in these.

## What I did overnight, and under what authority

Blanc relayed two input-box lines ("re-aim (A) at the p-value framing", then "rest until the 07:00
handover") and was explicit that **your presence could not be verified**, so neither was treated as a
ruling. Blanc scoped the first as analysis-only and I stayed inside that: no tier moved, no question
closed, nothing outward, nothing deleted. The later work in this file was done on your **direct**
questions about the seats. If you did not write those two lines, nothing needs undoing — only
analysis and receipts exist.
