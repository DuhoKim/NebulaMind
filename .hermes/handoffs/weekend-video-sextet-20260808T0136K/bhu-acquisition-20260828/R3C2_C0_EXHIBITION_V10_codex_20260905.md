ACCESS_SHA=5c69ae471edb1b1970594255e970fb07274fbb814dce1096de81c9d647ad1a5b
C0_REACHABILITY=PASS

The examples below are reachability witnesses, not findings about the corpus and not physics judgments. “Claim” means an included §1 passage. The active V10 text has no held operative definition: §10.4 says option (c) was ruled, the HELD marker was removed, and §1–§3 now implement one pass/two tallies. The old HELD wording appears only as preserved history. Thus none of these reachability results depends on an unresolved reading. `REPRO_AFTER_CHOICE` is retired and is not a declared §3 outcome in V10; its information is now the script-computed `rests_on` field.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Included claim E prints “10.0 m” and states `L = 2x`; ledger input `x=5.0 m`, status `PRINTED`, origin `CHOSEN`. Mechanical calculation gives `2×5.0=10.0 m`, within stated precision. | §1 includes the paper’s own printed numeral; §2.3 records `x` as `PRINTED`; §2.4 consumes every `PRINTED`/`STANDARD` value regardless of origin; no earlier §3 terminal condition holds; §3 arithmetic group applies and the number follows, so `REPRO_EXACT`. The script separately files `rests_on=USES_CHOSEN`. | yes |
| `REPRO_FAILED` | Included claim F prints “11.0 m” and states `L = 2x`; ledger input `x=5.0 m`, status `PRINTED`, origin `DERIVED`. Calculation gives `10.0 m`, not 11.0 m. | §1 inclusion → §2 inputs sufficient and stated → §2.4 mechanical attempt → no earlier §3 terminal condition → arithmetic group; because the arithmetic does not give the printed number, §3 files `REPRO_FAILED` and reports 11.0 m and 10.0 m. | yes |
| `REPRO_BLOCKED` | Included claim B prints “6.0” and states `q=2z`; it states that `z=3.0` is in Supplement S hosted outside the lane, but after two retrieval attempts Supplement S cannot be obtained. There is an equation and no input is absent from the paper’s stated sourcing; the referenced input value is blocked. | §1 inclusion → §2 extracts `q=2z` and identifies `z` → §3 precedence: no “no derivation” and no `ABSENT` input; then the clause “an input traces to a source outside this lane that we cannot obtain” applies before timeout/arithmetic → `REPRO_BLOCKED`, naming Supplement S and `z`. | yes |
| `REPRO_NOT_EVALUABLE` | Included claim N prints “0.742” and supplies a complete symbolic recipe `r=RootOf(P_500000)(...)` plus all coefficients as `PRINTED`; evaluation starts but reaches the 120-second symbolic cap without a result. | §1 inclusion → complete recipe and stated inputs under §2 → no no-derivation, absent-input, or blocked condition → §3 precedence next reaches the 120-second cap condition → `REPRO_NOT_EVALUABLE`, printing `SYMBOLIC_TIMEOUT` and the point reached. | yes |
| `REPRO_NO_DERIVATION_STATED` | Included claim D says “Our resulting radius is 7.3 km,” but gives no equation or computational procedure for that result. | It meets §1 because it prints a numeral asserted as the paper’s own result → §2 finds no producing recipe → the first terminal condition in §3 precedence applies → `REPRO_NO_DERIVATION_STATED`, naming that passage. | yes |
| `REPRO_INPUT_ABSENT` | Included claim A prints “12.0 J” and states `E=mc²`; it states `c=2.0 m/s` but nowhere states the required mass `m`. Ledger: `c`=`PRINTED`, `m`=`ABSENT`. | §1 inclusion → §2.2 identifies `m,c` → §2.3 marks `m` `ABSENT` → §2 rule forbids supplying it → no earlier no-derivation condition; §3 precedence reaches absent before blocked/timeout/arithmetic → `REPRO_INPUT_ABSENT`, naming `m`. | yes |
| `CENSUS_COMPLETE` | Corpus has exactly claims E and F above; each has exactly one filed arithmetic-group outcome (`REPRO_EXACT`, `REPRO_FAILED`), with no non-arithmetic claim. Both tallies and denominator 2 are reported. All controls/audit pass. | §3 defines the arithmetic group as exactly those two outcomes → §4.1 requires every included claim to carry exactly one such outcome → §4.2 does not trigger because no included claim has a non-arithmetic outcome → file `CENSUS_COMPLETE` with reproduction and `rests_on` tallies. | yes |
| `CENSUS_PARTIAL` | Corpus has claims E and A above. After two attempts E is `REPRO_EXACT` and A is `REPRO_INPUT_ABSENT`. | §3 files one outcome per claim → A is a non-arithmetic outcome → §4.2 says at least one such claim after two attempts files `CENSUS_PARTIAL`; its express precedence over `CENSUS_COMPLETE` controls. Report A and missing `m`; result is inconclusive. | yes |
| `CENSUS_AUDIT_FAILED` | Sealed census files claim E as `REPRO_EXACT` from `x=5.0 m`, but the independent §6 auditor re-derives the cited source and obtains `x=4.0 m`, hence 8.0 m, and cannot reproduce the filed outcome. | §6 requires re-derivation of every claim asserting reproduction and says any outcome the audit cannot reproduce files `CENSUS_AUDIT_FAILED` → §4.3 makes the census void and requires the claim be reported. | yes |
| `R3C2_NO_CLASS` | Before dispatch, control C5 is attempted twice by each attempting seat; in every seat both attempts fail because `python3 -c "import sympy"` exits nonzero, so `C5_HARNESS_PINNED` cannot pass. No seat passes it. | §5 defines the live C5 command and PASS requirement → after two failed attempts in every attempting seat, §4.4’s exact condition holds → `R3C2_NO_CLASS`. Later limbs are unreached/`NOT RUN`. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` | Candidate X prints “9.1”; seat 1 includes it as the paper’s own result, seat 2 excludes it as attributed-not-derived. They still disagree after two reconciliation attempts and list X. | §1 requires independent inclusion decisions and says disagreement stops under this class → §6 Limb A fixes tolerance zero and says to stop after two failed reconciliation attempts → §4.5 files `CENSUS_DENOMINATOR_DISPUTED`; census does not proceed. | yes |
| `CENSUS_ORIGIN_DISPUTED` | Corpus denominator is 10. Inputs affecting claims 1 and 2 (20%) are classified `DERIVED` by seat 1 with quoted equations and `IMPORTED` by seat 2 with quoted citations; disagreement remains and all disputed inputs/quotations are listed. Controls otherwise pass. | C3 supplies origin classes/evidence → §6 says origin disagreement is reported, not reconciled → 2/10 is more than 10% → §4.6 files `CENSUS_ORIGIN_DISPUTED` and stops before reproduction. | yes |
| `CENSUS_CONTROL_SPLIT` | On C5, after two attempts seat 1’s required commands and hashes pass, while seat 2’s two attempts fail at importing SymPy. | C5 provides the named control → the results are pass in one seat and fail in another after two attempts → §4.7 files `CENSUS_CONTROL_SPLIT`, reports both outputs, and stops without adopting the passing result. | yes |

Direct answer on `CENSUS_COMPLETE`: **REACHABLE**, including for a real multi-paper corpus, but only on an input corpus in which every included claim lands in `REPRO_EXACT` or `REPRO_FAILED`. For example, a 100-paper corpus containing only claims structurally like E or F satisfies §4.1, regardless of its mix of exact and failed arithmetic and regardless of `rests_on` provenance. Conversely, one included `REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, or `REPRO_NOT_EVALUABLE` claim triggers §4.2 after two attempts and its stated precedence forces `CENSUS_PARTIAL`; it prevents `CENSUS_COMPLETE` for that realized corpus. That makes completeness stringent, not unreachable in practice: reachability asks whether an allowed concrete input exists, and the all-arithmetic corpus above is one.

There are no UNREACHABLE active §3 outcomes or §4 classes, so there is no blocking clause to quote. The historical `REPRO_AFTER_CHOICE` does not enter C0 coverage because §3 expressly labels it “RETIRED at V10,” §10.4 says its content moved into `rests_on`, and the arithmetic group is now exactly `REPRO_EXACT` and `REPRO_FAILED`.

R3C2_C0_EXHIBITION_COMPLETE
