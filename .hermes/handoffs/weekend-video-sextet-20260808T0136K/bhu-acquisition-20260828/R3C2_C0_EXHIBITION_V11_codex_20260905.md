ACCESS_SHA=d6695c06c78c4735cb155269737175fbe55b7cdff0161cfccd0e85a71b21aca1
C0_REACHABILITY=PASS

This is a reachability exhibition, not a gate and not a judgment of the design or its physics. The examples below are concrete possible records under the operative text. A quoted passage is an included claim only on the §1 determination that it is “a passage in a pinned source that prints a numeral the paper asserts as a result of its own”; thus every per-claim path depends on that marked §1 boundary judgment. Each example stipulates that both independent seats include the passage under §1. Candidate exclusions are not outcomes and therefore are not additional §3 verdicts. `REPRO_AFTER_CHOICE` is explicitly retired and is not a live outcome to exhibit.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Included claim C1 prints: “With x = 3, y = 2x = 6.” Required input `x=3` is `PRINTED`; the stated recipe is `y=2x`; mechanical evaluation gives 6, which equals the printed 6 at its printed precision. | §1 includes C1 → §2.1 extracts 6 and `y=2x` → §§2.2–2.4 list and consume printed `x=3` → none of the first four §3 terminal conditions holds → arithmetic group → `REPRO_EXACT` because the result follows within stated precision. | yes |
| `REPRO_FAILED` | Included claim C2 prints: “With x = 3, y = 2x = 7.” Required input `x=3` is `PRINTED`; the stated recipe gives 6, not the printed 7. | §1 includes C2 → §§2.1–2.4 provide a stated recipe and all needed inputs → no no-derivation, blocked, absent, or not-evaluable condition → arithmetic group → `REPRO_FAILED`; report reproduced 6 and printed 7 as “unreproduced from the stated inputs.” | yes |
| `REPRO_BLOCKED` | Included claim C3 prints: “Using y = 2x and the value of x in Smith (2020), we obtain y = 8.” The paper does not print `x`; it names Smith (2020), but that cited source is outside the lane and cannot be obtained. | §1 includes C3 → a recipe exists, so no-derivation does not apply → §3 says an unprinted input with a named citation outside the lane that cannot be obtained is `REPRO_BLOCKED` → BLOCKED precedes INPUT_ABSENT and the attempt terminates; name `x` and Smith (2020). | yes |
| `REPRO_NOT_EVALUABLE` | Included claim C4 prints: “Running ProprietarySolver P with a = 2 and tolerance 10^-6 gives z = 4.731.” The computational procedure and all inputs (`a=2`, tolerance `10^-6`) are printed, but the lane does not have ProprietarySolver P. | §1 includes C4 → procedure and inputs are stated → no no-derivation, blocked, or absent condition → §3 machinery-unavailable limb applies → `REPRO_NOT_EVALUABLE`; print `SYMBOLIC_TIMEOUT` and the point reached, as §3 requires for this outcome. | yes |
| `REPRO_NO_DERIVATION_STATED` | Included claim C5 prints: “Our resulting characteristic scale is 5 km,” but gives no equation or computational procedure capable of producing 5 km. | §1 includes C5 as the paper’s own printed result → §2.1 finds no producing equation/procedure → the first item in §3 terminal precedence is `REPRO_NO_DERIVATION_STATED` → name the passage and file it before every later condition. | yes |
| `REPRO_INPUT_ABSENT` | Included claim C6 prints: “For y = 2x we obtain y = 8.” The equation needs `x`, but the paper neither prints `x` nor traces it to a named source. | §1 includes C6 → recipe exists, so no-derivation does not apply → no named source, so BLOCKED does not apply → §2.3 classifies `x` as `ABSENT`; §2 says a seat may not supply it → §3 precedence files `REPRO_INPUT_ABSENT` and names `x`. | yes |
| `CENSUS_COMPLETE` | A completed multi-paper corpus has three included claims: C1a “x=3; y=2x=6” (`REPRO_EXACT`), C1b “a=4; b=a+1=5” (`REPRO_EXACT`), and C2 “x=3; y=2x=7” (`REPRO_FAILED`). Both seats agree on the denominator and origins; controls pass; receipt verification and audit pass. Thus all 3/3 included claims have exactly one arithmetic-group outcome. | Controls do not trigger `R3C2_NO_CLASS` or `CENSUS_CONTROL_SPLIT` → enumerations agree, so no denominator dispute → origin disagreement is not above 10% → audit and receipts pass → no included claim has a non-arithmetic outcome, so `CENSUS_PARTIAL` is false → §4.1 and the final position in §4 precedence file `CENSUS_COMPLETE`; report the reproduction tally and separate `rests_on` tally. | yes |
| `CENSUS_PARTIAL` | A corpus has included C1 (`REPRO_EXACT`) and C6 (`REPRO_INPUT_ABSENT`) after two attempts. Both seats agree on enumeration and origins; controls, audit, and receipts pass. | No earlier §4 stop class applies → at least one included claim (C6) has a non-arithmetic outcome → §4.2 files `CENSUS_PARTIAL` as INCONCLUSIVE, before `CENSUS_COMPLETE`; report C6 and why. | yes |
| `CENSUS_AUDIT_FAILED` | For a claim printing “With x = 3, y = 2x = 7,” the sealed ledger correctly contains printed `x=3`, but the original tally mistakenly files `REPRO_EXACT`; the independent §6 audit mechanically obtains 6 and therefore cannot reproduce that per-claim outcome. All controls, enumeration, and origin agreement before audit otherwise pass. | No earlier control, split, denominator, or >10%-origin stop applies → §6 audit cannot reproduce the sampled per-claim outcome → §4.3 condition holds → §4 precedence files `CENSUS_AUDIT_FAILED` before PARTIAL/COMPLETE; census void, identify the claim. An independently sufficient concrete alternative is a missing or mismatched §7 receipt. | yes |
| `R3C2_NO_CLASS` | Control C5 is attempted twice by each of two seats; every attempt fails because `python3 --version` cannot execute. | §5 defines the live harness control → after two attempts the control fails in every seat that attempted it → §4.4 condition holds → it is first in §4 filing precedence, so file `R3C2_NO_CLASS`; later limbs and controls are `NOT_RUN`. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` | Candidate D prints “The value 12 is from Smith (2020).” Seat A excludes it as attributed-not-derived; seat B includes it as the paper’s own result. The disagreement remains after two reconciliation attempts; all earlier controls pass and are not split. | §1 assigns inclusion independently and says disagreement stops the study → §6 specifies tolerance zero after two reconciliation attempts → neither `R3C2_NO_CLASS` nor `CENSUS_CONTROL_SPLIT` applies → §4 precedence files `CENSUS_DENOMINATOR_DISPUTED`; list D and do not proceed. | yes |
| `CENSUS_ORIGIN_DISPUTED` | There are 9 included claims. Both seats agree on the denominator. For input `h=0.70` affecting 1 claim, Seat A cites wording as `CHOSEN`, while Seat B cites wording as `IMPORTED`; 1/9 = 11.1%, which is more than 10%. Controls pass and no control split occurs. | No earlier control/split/denominator class applies → §§3/C3 retain both independent origin classifications → disagreement affects more than 10% of included claims → §4.6 applies → §4 precedence files `CENSUS_ORIGIN_DISPUTED` before audit/tally classes; list the input, classifications, and quotations. | yes |
| `CENSUS_CONTROL_SPLIT` | After two attempts, Seat A’s C5 harness executions pass, while Seat B’s two C5 attempts fail because SymPy cannot import. | A control passes in one seat and fails in another after two attempts → §4.7 condition holds → `R3C2_NO_CLASS` is false because the control did not fail in every attempting seat → the second item in §4 precedence files `CENSUS_CONTROL_SPLIT`; report both outputs and stop without adopting Seat A. | yes |

Direct answer to the `CENSUS_COMPLETE` suspicion: **REACHABLE.** The text does not require a blocked, absent-input, no-derivation, or not-evaluable claim to exist merely because the corpus contains many papers. The concrete three-claim, multi-paper input above makes every included claim arithmetic-group and reaches `CENSUS_COMPLETE` after all earlier classes are false. But a single included claim anywhere in the actual corpus carrying `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE` makes §4.2 true after two attempts and therefore forces `CENSUS_PARTIAL` (unless an earlier-precedence §4 stop class applies). Because §4 gives `CENSUS_PARTIAL` precedence over `CENSUS_COMPLETE`, that one non-arithmetic claim prevents a COMPLETE filing. The preregistration alone, without inspecting the pinned corpus, establishes conditional reachability, not which condition the actual corpus satisfies.

There are no UNREACHABLE live §3 outcomes or §4 classes, so there is no blocking clause to quote.

R3C2_C0_EXHIBITION_COMPLETE
