ACCESS_SHA=b293f14016f20aca40c50f258ab52c53a9a16524623107b1eeefc5a51174762c
C0_REACHABILITY=PASS

This exhibition applies the operative V14 wording. The old `REPRO_AFTER_CHOICE` is not a declared §3 outcome: §3 labels it “RETIRED,” and §10.4 says its information is now the script-computed `rests_on` field. The former HELD wording is history, not an operative clause. The live §1 definition remains a marked dependence: each example below becomes an included claim only if both seats classify its numeral as “the paper’s own result” under §1; unresolved inclusion disagreement instead invokes `CENSUS_DENOMINATOR_DISPUTED`.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Included claim: “Using `y=2x` with printed `x=3`, we obtain `y=6`.” Input ledger: `x`, status `PRINTED`, value 3. Mechanical result: 6; printed result: 6. | §1 includes the asserted result → §2.1–3 extracts recipe/input and classifies `x` → §2.4 consumes `PRINTED` → §3 precedence reaches arithmetic group → reproduced value equals/rounds to the printed numeral → `REPRO_EXACT`. | YES |
| `REPRO_FAILED` | Included claim: “Using `y=2x` with printed `x=3`, we obtain `y=7`.” Input ledger: `x`, `PRINTED`, 3. Mechanical result: 6; printed result: 7. | §1 → §2.1–4; all needed inputs are consumable and no earlier terminal condition applies → §3 arithmetic group → arithmetic does not give the paper’s number → `REPRO_FAILED` (“unreproduced from the stated inputs”). | YES |
| `REPRO_BLOCKED` — named source outside enumerable manifest | Included claim: “With `y=2a` and `a` from Smith (1999), we obtain `y=10`.” The claiming paper prints no `a`; Smith (1999) is not an enumerable pinned manifest text. Ledger records `a` as `BLOCKED`, `IMPORTED`, no value. | §1 → §2.2–3 → §2 final paragraph says named source not enumerable files `REPRO_BLOCKED` and forbids consumption → §3 precedence (`NO_DERIVATION` false, then `BLOCKED`) → `REPRO_BLOCKED`. | YES |
| `REPRO_BLOCKED` — pinned source lacks cited-line match | Included claim uses `y=2a`, says `a` is at a cited line of an enumerable pinned Smith source, but that line contains no machine-matchable value for `a`. Ledger: `a=BLOCKED`, no value. | §2 final paragraph’s second blocked limb → no value may be supplied → §3 `REPRO_BLOCKED` definition and precedence → `REPRO_BLOCKED`. | YES |
| `REPRO_NOT_EVALUABLE` — timeout | Included claim prints result `Q=1.23` and a complete recipe whose symbolic reduction is still unfinished at the 120-second cap; all inputs are `PRINTED`/`STANDARD`. Record `SYMBOLIC_TIMEOUT` and the last expression reached. | §1 → §2 → no no-derivation, blocked, or absent condition → §3 arithmetic cannot be completed within 120 seconds → precedence reaches `REPRO_NOT_EVALUABLE` before arithmetic group. | YES |
| `REPRO_NOT_EVALUABLE` — unavailable machinery | Included claim prints result `Q=1.23`, gives a complete procedure and inputs, but explicitly requires a proprietary solver unavailable to the lane. | §1 → §2 → earlier terminal conditions false → §3 “requires machinery this lane does not have” → `REPRO_NOT_EVALUABLE`; print `SYMBOLIC_TIMEOUT` and point reached as required. | YES |
| `REPRO_NO_DERIVATION_STATED` | Included passage: “Our analysis yields `M=5.0 kg`,” asserted as the paper’s own result, with no equation or computational procedure anywhere for `M`. | §1 includes the printed own-result numeral → §2.1 finds no producing recipe → §3 first and highest per-claim terminal condition is no derivation/procedure → `REPRO_NO_DERIVATION_STATED`. | YES |
| `REPRO_INPUT_ABSENT` | Included claim: “From `y=ax` we obtain `y=8`.” The paper prints `x=2` but neither prints `a` nor traces `a` to any named source. Ledger: `x=PRINTED`, `a=ABSENT`. | §1 → §2.2–3 → §2 forbids supplying `ABSENT` → no no-derivation or blocked condition; §3 precedence reaches `REPRO_INPUT_ABSENT` → stop and name `a`. | YES |
| `CENSUS_COMPLETE` | Concrete corpus realization: the complete agreed denominator contains exactly two included claims—the `y=2x, x=3, y=6` exact claim and the `y=2x, x=3, y=7` failed claim above—and any other numerals are agreed §3 exclusions. All C0–C5b controls pass in every seat; enumerations agree; origin disputes affect at most 10%; audit reproduces all records; both seal receipts verify. | No earlier §4 stop class applies → neither claim has a non-arithmetic outcome → both carry exactly one member of §3’s arithmetic group (`REPRO_EXACT`, `REPRO_FAILED`) → `CENSUS_PARTIAL` false → final precedence limb `CENSUS_COMPLETE`; file reproduction and `rests_on` tallies. | YES |
| `CENSUS_PARTIAL` | Concrete corpus realization: the agreed denominator contains the exact claim above plus the absent-input claim `y=ax`, `x=2`, printed `y=8`, missing untraced `a`. Controls, enumeration, origin threshold, audit, and receipts otherwise pass. | The second claim files non-arithmetic `REPRO_INPUT_ABSENT` → no earlier §4 stop class applies → §4 says at least one listed non-arithmetic outcome files `CENSUS_PARTIAL`, which precedes `CENSUS_COMPLETE`; report the claim and why. | YES |
| `CENSUS_AUDIT_FAILED` — sampled outcome/ledger | Corpus includes the exact `y=2x` claim, but the sealed ledger records `x=3` while the auditor finds the cited pinned line actually prints `x=4`, so the auditor cannot reproduce the sampled ledger/outcome. Earlier controls and dispute thresholds passed. | C6 says an outcome the audit cannot reproduce or any ledger incompleteness files `CENSUS_AUDIT_FAILED` → §4 audit-failure limb applies before partial/complete → no tally filed. | YES |
| `CENSUS_AUDIT_FAILED` — receipt failure | Arithmetic claims and audit material are otherwise valid, but receipt T records a tally hash different from the tally re-hashed by the verifier (equivalently, P or T is missing). | §7 says any missing receipt or mismatch files `CENSUS_AUDIT_FAILED` → §4 expressly includes failed seal-receipt verification and excludes it from `R3C2_NO_CLASS` → precedence reaches `CENSUS_AUDIT_FAILED`; no tally filed. | YES |
| `CENSUS_AUDIT_FAILED` — missing audit seed | Tally is receipted, but the external custodian never supplies or records the required 64-hex seed. | C6 mandates `C6_AUDIT_SAMPLE=NOT_RUN` and says the study files `CENSUS_AUDIT_FAILED` naming the missing seed → §4 audit-failure limb. | YES |
| `R3C2_NO_CLASS` — control failure in every attempting seat | For the exact `y=2x` corpus, `/usr/bin/python3 --version` exits nonzero in seat A and seat B on both attempts, so `C5_HARNESS_PINNED=FAIL` everywhere it is attempted. | C5 defines the nonzero exit as FAIL → §4: a C0–C5b control fails in every attempting seat after two attempts → first filing-precedence class `R3C2_NO_CLASS`; later limbs `NOT_RUN`, no census tally. | YES |
| `R3C2_NO_CLASS` — pre-dispatch failure | The mechanically built seat packet fails its forbidden-string assertion twice and therefore is not written/dispatched. | C4 defines `C4_PACKET_REDACTED=FAIL` and no dispatch → §4 explicitly says a packet failure before dispatch stops without a census tally → `R3C2_NO_CLASS` (C4 lies within C0–C5b). | YES |
| `CENSUS_DENOMINATOR_DISPUTED` | Candidate passage: “The calibration table gives `5.0`.” Seat A treats it as the paper’s own result; seat B treats it as attributed-not-derived. They remain opposed after two reconciliation attempts; all earlier controls pass/no split. | §1’s independent inclusion rule → disagreement survives two reconciliations → §1 and §6 order the stop → no earlier `R3C2_NO_CLASS`/`CENSUS_CONTROL_SPLIT` → §4 precedence files `CENSUS_DENOMINATOR_DISPUTED`; list the candidate and ledgers. | YES |
| `CENSUS_ORIGIN_DISPUTED` | Agreed denominator has 10 included claims. For inputs affecting 2 claims (20%), seat A cites wording as `CHOSEN` while seat B cites it as `FITTED`; every input and both quotations are listed. Controls and denominator agree. | C3 independently classifies origins and merge preserves alternatives → C6 reports rather than reconciles → 2/10 is more than 10% → no earlier stop → §4 files `CENSUS_ORIGIN_DISPUTED`; census does not proceed. | YES |
| `CENSUS_CONTROL_SPLIT` | On both attempts, seat A’s C2 ledger validation exits 1 because a required field is missing; seat B’s independently complete C2 ledger exits 0. | C2 maps exits to FAIL/PASS → failure in one seat and pass in another after two attempts → `R3C2_NO_CLASS` is false because failure is not in every attempting seat → next §4 precedence limb files `CENSUS_CONTROL_SPLIT`; report both outputs and stop. | YES |

`CENSUS_COMPLETE` suspicion—answer: **REACHABLE**, including for a real multi-paper corpus, but only if every included claim in the agreed denominator receives `REPRO_EXACT` or `REPRO_FAILED`. Corpus size does not itself bar the class. The routing is §1 agreed inclusion → one §3 outcome per included claim → all outcomes in the arithmetic group → no earlier §4 class → `CENSUS_COMPLETE`. Conversely, a single included `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE` claim makes the §4 `CENSUS_PARTIAL` condition true and its precedence blocks `CENSUS_COMPLETE`, provided no still-earlier study-level stop class applies. If an earlier stop class does apply, that earlier class—not `CENSUS_PARTIAL`—is filed. The preregistration alone does not establish which situation the pinned real corpus will realize because it is expressly “NOT RUN.”

There are no UNREACHABLE declared §3 outcomes or §4 classes, so no blocking-clause quotation is required.

R3C2_C0_EXHIBITION_COMPLETE
