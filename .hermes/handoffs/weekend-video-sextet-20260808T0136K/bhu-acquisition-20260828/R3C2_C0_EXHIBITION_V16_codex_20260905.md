ACCESS_SHA=47c55a74af9520586af75e31d408c969a0e81f831c94f8eac59b59ead0a91381
C0_REACHABILITY=PASS

This is a reachability exhibition, not a physics judgment and not a gate. Each hypothetical passage below is a specific included claim under §1: a pinned passage prints the stated numeral and asserts it as the paper's own result. That qualification matters because §1 expressly makes inclusion a two-seat judgment (“a passage in a pinned source that prints a numeral the paper asserts as a result of its own”); the examples establish reachability conditional on the seats applying that marked boundary as stated. All otherwise-mentioned controls pass and all earlier-precedence §4 conditions are false unless a row says otherwise.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Claim E prints “Area = 12.0 cm²,” gives `A = l × w`, and prints `l = 3.0 cm`, `w = 4.0 cm`; mechanical result `12.0 cm²`, which rounds to the printed numeral. | §1 includes Claim E → §2 lists both inputs as `PRINTED` and mechanically applies the recipe → no earlier §3 terminal condition holds → arithmetic group → §3 `REPRO_EXACT` because the reproduced and printed values agree at stated precision; report both numbers and `rests_on`. | yes |
| `REPRO_FAILED` | Claim F prints “Area = 13.0 cm²,” gives `A = l × w`, and prints `l = 3.0 cm`, `w = 4.0 cm`; mechanical result `12.0 cm²`. | §1 includes Claim F → all recipe inputs are stated and consumable under §2/§3 → no earlier terminal condition holds → arithmetic group → §3 `REPRO_FAILED`, worded “unreproduced from the stated inputs,” with both numbers and `rests_on`. | yes |
| `REPRO_BLOCKED` — named source outside enumerable manifest | Claim B1 prints “Q = 10.0,” gives `Q = 2x`, does not print `x`, and says “use x from Smith (2010)”; Smith (2010) is not an enumerable text pinned in the manifest. | §1 includes Claim B1 → §2 classifies `x` as `BLOCKED`; it may not be supplied or consumed → §3 precedence reaches `REPRO_BLOCKED` before absent/not-evaluable/arithmetic → name `x` and Smith (2010). | yes |
| `REPRO_BLOCKED` — no machine match at pinned cited line | Claim B2 prints “Q = 10.0,” gives `Q = 2x`, and cites line 40 of a pinned enumerable Smith text for `x`, but line 40 contains no machine-matchable value for `x`. | §2's imported-`PRINTED` route requires a match at the cited line; without it `x` is `BLOCKED` → §3 named-source limb and precedence file `REPRO_BLOCKED`. | yes |
| `REPRO_NOT_EVALUABLE` — timeout | Claim T prints “I = 7.1,” states a symbolic integration procedure, and supplies every required input as `PRINTED`; the committed wrapper reaches 120 seconds before completing it. | §1 includes Claim T → sufficient consumable inputs; no no-derivation, blocked, or absent condition → §3 `REPRO_NOT_EVALUABLE`, print `SYMBOLIC_TIMEOUT` and the point reached. | yes |
| `REPRO_NOT_EVALUABLE` — machinery unavailable | Claim M prints “S = 4.2,” states that S is produced from printed input `d = 8` by a proprietary solver, and the lane lacks that solver. | §1 includes Claim M → recipe and required input exist → earlier §3 conditions are false → machinery limb of `REPRO_NOT_EVALUABLE`; print `MACHINERY_UNAVAILABLE` and the point reached. | yes |
| `REPRO_NO_DERIVATION_STATED` | Claim N prints “The index is 6.4” as its own result but gives no equation or computational procedure for the index. | §1 includes Claim N despite the missing recipe → §3 says there is nothing to attempt → this is first in the per-claim precedence → `REPRO_NO_DERIVATION_STATED`, naming the passage. | yes |
| `REPRO_INPUT_ABSENT` | Claim A prints “Q = 10.0,” gives `Q = 2x`, but neither prints `x` nor traces it to any named source. | §1 includes Claim A → §2 lists `x` as `ABSENT`, forbids supplying it, and ends the attempt → no no-derivation or blocked condition → §3 precedence files `REPRO_INPUT_ABSENT`, naming `x`. | yes |
| `CENSUS_COMPLETE` | Agreed denominator is two included claims: Claim E above (`REPRO_EXACT`) and Claim F above (`REPRO_FAILED`). Both enumerations and input lists agree; origins agree; controls and audit pass; receipts verify. | §3 arithmetic group is exactly `REPRO_EXACT` plus `REPRO_FAILED` → every included claim has exactly one such outcome → no earlier §4 class applies → §4 filing precedence reaches `CENSUS_COMPLETE`; report the reproduction and `rests_on` tallies. | yes |
| `CENSUS_PARTIAL` | Agreed denominator is Claim E (`REPRO_EXACT`) plus Claim A (`REPRO_INPUT_ABSENT`); the §2 attempts finish, all controls/audit/receipts pass, and there are no disputes. | Claim A has a non-arithmetic outcome → §4 `CENSUS_PARTIAL`; with every earlier-precedence class false, it is filed before `CENSUS_COMPLETE`, and Claim A and missing `x` are reported. | yes |
| `CENSUS_AUDIT_FAILED` — audit mismatch | Sealed census contains Claim E as `REPRO_EXACT` from `3.0 × 4.0 = 12.0`, but the independent C6 audit finds the sealed ledger omitted the equation's printed scale input `s = 2.0`, so it cannot reproduce that ledger/outcome. Earlier controls, enumeration, and origins have no stop condition. | C6 says any outcome the audit cannot reproduce, or any ledger incompleteness, files `CENSUS_AUDIT_FAILED` → §4 definition applies → filing precedence reaches it before tally classes; no tally is filed and Claim E is identified. | yes |
| `CENSUS_AUDIT_FAILED` — seal/seed condition | Claims E and F otherwise yield arithmetic outcomes, but receipt T is missing (alternatively, the external 64-hex seed is not supplied and recorded, making `C6_AUDIT_SAMPLE=NOT_RUN`). | §7 says a missing receipt or mismatch files `CENSUS_AUDIT_FAILED`; C6 says a missing recorded seed does the same → §4 expressly routes seal-receipt/C6 failures here, not `R3C2_NO_CLASS` → precedence files `CENSUS_AUDIT_FAILED`. | yes |
| `R3C2_NO_CLASS` — common control failure | On both independent seats, C5's live `/usr/bin/python3 --version` command exits nonzero on attempt 1 and attempt 2; no packet/seat split exists because both seats fail. | C5 predicate is `FAIL` on nonzero exit → a C0–C5b control fails in every seat that attempted it after two attempts → §4 `R3C2_NO_CLASS`, first in filing precedence; later limbs are `NOT_RUN`. | yes |
| `R3C2_NO_CLASS` — pre-dispatch failure | The mechanically built seat packet contains a forbidden string, so `C4_PACKET_REDACTED=FAIL` before dispatch and no seat is dispatched. | C4 says the packet is not written/dispatched on that failure → §4 explicitly says a packet or seat-isolation failure before dispatch files `R3C2_NO_CLASS` → it is first in precedence. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — candidate inclusion | Candidate D prints “r = 5.0” in ambiguous prose. Seat A classifies it as the paper's own result; seat B classifies it as attributed-not-derived; the disagreement survives two reconciliation attempts. Controls pass. | §1 independently assigns inclusion and routes surviving disagreement here; §4 definition matches → after `R3C2_NO_CLASS` and control split are false, precedence files `CENSUS_DENOMINATOR_DISPUTED`; disputed candidate and complete ledgers are reported. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — input lists | Both seats include Claim E, but A says its stated equation needs inputs `l,w`, while B says it needs `l,w,s`; `merge` exits 1 and the difference survives the one C3 reconciliation. | C3 routes surviving input-set difference to §4 → §4's second limb expressly covers it → precedence files `CENSUS_DENOMINATOR_DISPUTED` and lists inputs and quotations. | yes |
| `CENSUS_ORIGIN_DISPUTED` | There are 10 included claims. For Claim O1 and O2, both seats agree on inputs and values but seat A classifies a root input `CHOSEN` from `ORIG_CHOICE_STATED`, while seat B classifies it `MEASURED` from `ORIG_MEASURED`, each with its quotation. Thus disputed origins affect 2/10 = 20% of claims; controls and denominator agree. | C3 independently records origins and merge preserves alternatives → §4 threshold is more than 10% of included claims → with earlier classes false, precedence files `CENSUS_ORIGIN_DISPUTED`; all disputed inputs, classifications, and quotations are listed. | yes |
| `CENSUS_CONTROL_SPLIT` | After two attempts, Seat A's C5 commands all exit 0 with full stdout (`PASS`), while Seat B's Python-version command exits nonzero both times (`FAIL`). | C5 yields the stated seat results → §4 requires a control that fails in one seat and passes in another after two attempts → `CENSUS_CONTROL_SPLIT`; it precedes denominator and later classes, both outputs are reported, and the study stops. | yes |

Direct answer to the `CENSUS_COMPLETE` suspicion: **REACHABLE.** It can actually be filed for a many-paper corpus if, after the agreed §1 enumeration, every included claim lands in exactly `REPRO_EXACT` or `REPRO_FAILED`, all earlier-precedence study conditions are false, and the audit and receipts succeed. Corpus size does not itself bar the class. Conversely, a single included claim anywhere with `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or final `REPRO_NOT_EVALUABLE` makes the universal `CENSUS_COMPLETE` condition false and satisfies `CENSUS_PARTIAL`; if no earlier stop class applies, §4's precedence files `CENSUS_PARTIAL`. Thus complete is reachable but fragile, not practically unreachable by definition. This is conditional reachability only: because the study is “NOT RUN” and this exhibition was forbidden to inspect the corpus files, it does not assert that the fixed real corpus will satisfy the condition.

UNREACHABLE verdicts: none. Therefore there is no blocking-clause quotation to supply.

R3C2_C0_EXHIBITION_COMPLETE
