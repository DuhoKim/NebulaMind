ACCESS_SHA=fe194fb4aee7603dbeecbfeda62dc8507aba2a994e3b9a206c8089490700e1c9
C0_REACHABILITY=PASS

This is an existential reachability exhibition, not a prediction about the pinned corpus and not a physics assessment. Each quoted “paper passage” below is a concrete possible passage in an enumerable pinned text. Section 1’s inclusion boundary remains a two-reader judgment: every per-claim example depends first on both seats agreeing that its numeral is “the paper’s own result.” That marked dependence does not eliminate any path because the exhibited passages expressly assert their numerals as their own results.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Included claim E: “Using y = 2x with x = 3, we obtain y = 6.” Inputs: `x=3`, status `PRINTED`; recipe `y=2x`; mechanical result 6; printed result 6. | §1 includes the asserted result → §2 lists a sufficient `PRINTED` input and applies the stated recipe → none of §3’s earlier terminal conditions holds → arithmetic group → reproduced value rounds to the printed numeral → `REPRO_EXACT`. | yes |
| `REPRO_FAILED` | Included claim F: “Using y = 2x with x = 3, we obtain y = 7.” Input: `x=3`, status `PRINTED`; recipe result 6; printed result 7. | §1 inclusion → sufficient stated input under §2 → no earlier §3 terminal condition → arithmetic group → arithmetic does not give the paper’s number → `REPRO_FAILED` (“unreproduced from the stated inputs”). | yes |
| `REPRO_BLOCKED` — named source not enumerable | Included claim B1: “With y = 2x and x from Smith (2010), we find y = 8.” The paper does not print `x`; Smith (2010) is named but is not an enumerable text in the manifest. | §1 inclusion → §2 classifies the named-source input `BLOCKED`, forbids consuming it, and ends the attempt → §3 precedence: no-derivation is false; `REPRO_BLOCKED` is the first true condition → `REPRO_BLOCKED`. | yes |
| `REPRO_BLOCKED` — no machine match at pinned cited line | Included claim B2 states the same result and recipe, citing `x` to line 20 of an enumerable pinned Smith text; line 20 says “the calibrated input is discussed below” and contains no machine-matchable value. | §1 inclusion → §2 imported-value exception fails because no value machine-matches at the cited line → status `BLOCKED`, no value consumed → §3 precedence reaches `REPRO_BLOCKED` before later conditions. | yes |
| `REPRO_NOT_EVALUABLE` — timeout | Included claim T: “Evaluate the stated symbolic integral I(a) by procedure P with a = 1; we obtain I = 4.” `a=1` is `PRINTED`; all inputs are present; P is launched through the committed wrapper but exceeds 120 seconds and prints `SYMBOLIC_TIMEOUT` at the point “integral expansion.” | §1 inclusion → §2 recipe and inputs exist, with no `BLOCKED`/`ABSENT` record → arithmetic cannot complete within the cap → §3 precedence reaches `REPRO_NOT_EVALUABLE` before the arithmetic group → report `SYMBOLIC_TIMEOUT` and the point reached. | yes |
| `REPRO_NOT_EVALUABLE` — unavailable machinery | Included claim M: “Applying algorithm A to the printed vector v = (1,2), the detector returns q = 0.4.” All required numeric inputs are `PRINTED`, but A requires a named detector emulator the lane does not have; point reached: “emulator invocation.” | §1 inclusion → stated computational procedure and sufficient inputs avoid the first three terminal conditions → machinery is unavailable → §3 files `REPRO_NOT_EVALUABLE` with `MACHINERY_UNAVAILABLE` and the point reached. | yes |
| `REPRO_NO_DERIVATION_STATED` | Included claim N: “Our analysis gives a radius of 5 km.” The passage asserts the paper’s own numeral but gives no equation or computational procedure anywhere for it. | §1 inclusion → §2 cannot extract a producing equation/procedure → §3’s first-precedence terminal condition holds → `REPRO_NO_DERIVATION_STATED`, irrespective of any other missing information. | yes |
| `REPRO_INPUT_ABSENT` | Included claim A: “Using y = x + z and x = 2, we obtain y = 5.” Required `z` is neither printed nor traced to a named source. | §1 inclusion → §2 lists `x` as `PRINTED`, `z` as `ABSENT`, forbids supplying `z`, and ends the attempt → derivation exists and no input is `BLOCKED` → §3 precedence reaches `REPRO_INPUT_ABSENT`; name `z`. | yes |
| `CENSUS_COMPLETE` | A many-paper corpus has 100 agreed included claims: 60 instances shaped like E and 40 shaped like F; every claim has a stated recipe and sufficient `PRINTED`/`STANDARD` inputs, and each files exactly one arithmetic-group outcome. Both seats agree on candidates, input lists, and origins; C0–C5b pass in both; audit and both seal receipts pass. | §4: no earlier stop class applies; no non-arithmetic claim exists, so `CENSUS_PARTIAL` is false; every included claim carries exactly one of `REPRO_EXACT`/`REPRO_FAILED` → filing precedence reaches `CENSUS_COMPLETE`. | yes |
| `CENSUS_PARTIAL` | Corpus has two agreed included claims: E files `REPRO_EXACT`; A files `REPRO_INPUT_ABSENT` for `z`. Controls, enumeration, origins, audit, and receipts otherwise pass. | No earlier §4 class applies → at least one included claim has a listed non-arithmetic outcome after its §2 attempt → `CENSUS_PARTIAL`; its explicit precedence over `CENSUS_COMPLETE` also makes complete unavailable for this input. | yes |
| `CENSUS_AUDIT_FAILED` — sampled result/ledger failure | Corpus initially files E as `REPRO_EXACT`, but C6 independently recomputes its cited input line as `x=4`, yielding 8, so it cannot reproduce the sealed per-claim outcome/ledger. Earlier controls, denominator, and origin-dispute thresholds do not trigger. | Earlier §4 classes are false → §6/C6 audit cannot reproduce the outcome or ledger → `CENSUS_AUDIT_FAILED`; no tally is filed. | yes |
| `CENSUS_AUDIT_FAILED` — receipt verification failure | Arithmetic records would otherwise form a complete census, but Blanc’s independently re-hashed tally differs from receipt T (equivalently, receipt P/T is missing); earlier classes do not apply. | §7 says any missing receipt or mismatch files `CENSUS_AUDIT_FAILED`; §4 expressly assigns seal-receipt failure here, not to `R3C2_NO_CLASS`; precedence reaches `CENSUS_AUDIT_FAILED`. | yes |
| `CENSUS_AUDIT_FAILED` — missing audit seed | Tally is receipted, but the external custodian supplies no recorded 64-hex seed. Earlier classes do not apply. | C6 sets `C6_AUDIT_SAMPLE=NOT_RUN` and directs the study to file `CENSUS_AUDIT_FAILED` naming the missing seed → §4’s audit-failure limb applies. | yes |
| `R3C2_NO_CLASS` — all attempting seats fail a pre-audit control | On two attempts in each dispatched seat, `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"` exits nonzero, so C5 fails in every seat that attempts it; no seat passes it. | C5’s predicate returns FAIL → §4 says a C0–C5b control failing in every attempting seat after two attempts files `R3C2_NO_CLASS` → it is first in study-level precedence; later limbs are unreached. | yes |
| `R3C2_NO_CLASS` — packet/isolation failure before dispatch | Mechanical packet build leaves a forbidden string, so `C4_PACKET_REDACTED=FAIL` and the packet is not dispatched. | C4 defines the packet failure and forbids dispatch → §4 explicitly assigns a packet or seat-isolation failure before dispatch to `R3C2_NO_CLASS` → first-precedence filing. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — candidate disagreement | Candidate D says “Table 1 lists 9,” with context that seat A reads as the paper’s own result and seat B reads as attributed-not-derived. They still disagree after two reconciliation attempts; all prior controls pass in both. | §1’s two-seat inclusion rule stops the study after the surviving disagreement → §4’s denominator-dispute condition holds → after earlier no-class/split checks are false, precedence files `CENSUS_DENOMINATOR_DISPUTED`; list D and both dispositions. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — input-list disagreement | Seats agree claim L is included and its printed procedure is `q=f(a,b)`. Seat A lists `a,b`; seat B lists only `a`; the input-id sets still differ after the single C3 reconciliation. Prior controls pass. | C3 merge exits 1 → one input-list reconciliation fails → §4 expressly treats surviving disagreement on agreed claims’ input lists as `CENSUS_DENOMINATOR_DISPUTED`; precedence files it before origin/audit/tally classes. | yes |
| `CENSUS_ORIGIN_DISPUTED` | There are 10 agreed included claims with identical input lists. For inputs affecting claims O1 and O2, seat A cites text as `CHOSEN` while seat B cites it as `FITTED`; all controls and enumeration agree, and no earlier class applies. Two affected claims out of 10 = 20%, which is more than 10%. | C3 carries both origin classifications without reconciliation → §4 threshold is exceeded → the census stops and files `CENSUS_ORIGIN_DISPUTED`, listing every disputed input and both quotations. | yes |
| `CENSUS_CONTROL_SPLIT` | C5’s SymPy command succeeds in seat A on both attempts but fails in seat B on both attempts; packet/isolation did not fail before dispatch. | A control passes in one seat and fails in another after two attempts → §4 defines `CENSUS_CONTROL_SPLIT` and says not to adopt the passing result → precedence files it before denominator and later classes. `R3C2_NO_CLASS` is false because the control did not fail in every attempting seat. | yes |

Direct answer on `CENSUS_COMPLETE`: **REACHABLE.** It is demanding but not logically or practically impossible merely because the corpus contains many papers. The concrete 100-claim corpus above reaches it: every included claim has a recipe, all required values are consumable `PRINTED`/`STANDARD` records, and every outcome is `REPRO_EXACT` or `REPRO_FAILED`. A failed arithmetic result still belongs to the arithmetic group and therefore does not prevent completeness. Conversely, if even one included claim files `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE`, §4’s clause “at least one included claim carries a non-arithmetic outcome” becomes true; because `CENSUS_PARTIAL` precedes `CENSUS_COMPLETE`, that concrete corpus must file `CENSUS_PARTIAL` (unless an even earlier study-level class applies). Thus one such claim forces partial for that corpus, but the document does not require every possible real corpus to contain one.

No verdict is unreachable, so there is no blocking-clause quotation to report.

R3C2_C0_EXHIBITION_COMPLETE
