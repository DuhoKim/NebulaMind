ACCESS_SHA=e67339905813549fec1c2cebb58cad264e9cc1b94a95aa0d2715866f9f356b6e
C0_REACHABILITY=PASS

This is a reachability exhibition, not a physics judgment and not a claim about which outcomes the pinned corpus will actually produce. Each synthetic passage below is a specific candidate that satisfies §1 by printing a numeral asserted as the paper's own result. That status depends on the marked §1 boundary—whether the paper asserts the numeral as its own result—and therefore requires the two seats' agreement. The examples stipulate clear assertion language so that the boundary does not prevent the exhibited paths.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Included claim E: “Using z = a + b, with a = 2.0 and b = 3.0, we obtain z = 5.0.” Ledger: `a=2.0 PRINTED`, `b=3.0 PRINTED`; reproduced `5.0`, printed `5.0`; no uncertainty. | §1 includes the paper's asserted result. §2.1–4 extracts the recipe and consumes both `PRINTED` inputs. No earlier §3 terminal condition holds. The reproduced value rounds to the printed numeral at its printed precision, so §3 `REPRO_EXACT` applies. | yes |
| `REPRO_FAILED` | Included claim F: “Using z = a + b, with a = 2.0 and b = 3.0, we obtain z = 6.0.” Ledger: `a=2.0 PRINTED`, `b=3.0 PRINTED`; reproduced `5.0`, printed `6.0`; no uncertainty. | §1 inclusion → §2 complete input list and mechanical arithmetic → no no-derivation, blocked, absent, or not-evaluable condition → arithmetic group. The sufficient stated inputs yield `5.0`, not `6.0`, so §3 files `REPRO_FAILED` and reports both numbers. | yes |
| `REPRO_BLOCKED` | Included claim B: “We obtain y = 8.0 from y = kx, using x = 2.0 and k from Smith (2011).” `x=2.0 PRINTED`; `k` is unprinted and the named Smith source is not an enumerable text pinned in the manifest. Ledger records `k` as `BLOCKED`, `origin=IMPORTED`, no value. | §1 inclusion → §2 names required inputs → §2 named-source rule says a cited, unprinted value whose source is not enumerable files `REPRO_BLOCKED`; a seat may not supply or consume it. In §3 precedence there is a stated derivation, and `REPRO_BLOCKED` precedes absent/not-evaluable/arithmetic. | yes |
| `REPRO_NOT_EVALUABLE` | Included claim N: “Solving procedure Q with the printed data vector d = [1,2] gives q = 4.20.” The paper fully states procedure Q and all inputs, but Q requires a proprietary solver unavailable to the lane. Record `MACHINERY_UNAVAILABLE` and the point reached before solver invocation. | §1 inclusion → §2 extracts the stated procedure and complete `PRINTED` input → no blocked or absent input → §3 `REPRO_NOT_EVALUABLE` because required machinery is unavailable. Its precedence comes before the arithmetic group. | yes |
| `REPRO_NO_DERIVATION_STATED` | Included claim D: “Our resulting mean radius is 7.0 km.” The passage gives no equation or computational procedure and hence no input ledger records. | The sentence is an own-result numeral under §1. At §2.1 no producing equation/procedure can be extracted. §3 expressly files `REPRO_NO_DERIVATION_STATED`; it is first in the per-claim precedence, so nothing later is considered. | yes |
| `REPRO_INPUT_ABSENT` | Included claim A: “Using area A = lw, we obtain A = 10.0 m².” The paper prints `l=2.0 m` but neither prints `w` nor traces it to a named source. Ledger: `l=2.0 PRINTED`, `w ABSENT`, no value. | §1 inclusion → §2 identifies `l,w` → `w` meets the §3 definition of `ABSENT`, not `BLOCKED`, because no source is named → §2 forbids supplying it. A derivation is stated and no blocked input exists, so §3 precedence reaches and files `REPRO_INPUT_ABSENT` before not-evaluable/arithmetic. | yes |
| `CENSUS_COMPLETE` | Corpus C has three agreed included claims: E above (`REPRO_EXACT`), F above (`REPRO_FAILED`), and C3: “p=uv; u=2.0, v=4.0; p=8.0” (`REPRO_EXACT`). Any equation/reference/date candidates are put in the separate exclusion ledger. Both seats agree on candidates, inputs, outcomes, and origins; all C0–C5b controls and C6 pass; both seal receipts verify. | No earlier §4 stop class applies. Every included claim has exactly one outcome in the §3 arithmetic group, which is exactly `REPRO_EXACT` and `REPRO_FAILED`. `CENSUS_PARTIAL` is false because there is no non-arithmetic outcome. The §4 filing precedence therefore reaches `CENSUS_COMPLETE`. | yes |
| `CENSUS_PARTIAL` | Corpus P has E (`REPRO_EXACT`) and B (`REPRO_BLOCKED`). Both seats agree; controls, origins, audit, and receipts otherwise pass. The permitted repeat does not change B's missing enumerable cited value. | No earlier §4 class applies. B carries a listed non-arithmetic outcome after the §2 attempt, satisfying `CENSUS_PARTIAL`. Under §4 precedence, `CENSUS_PARTIAL` is filed before `CENSUS_COMPLETE`; the latter is false in any event because not every included claim is arithmetic-group. | yes |
| `CENSUS_AUDIT_FAILED` | The sealed corpus contains E with `a=2.0`, `b=3.0`, printed/reproduced `z=5.0` and sealed `REPRO_EXACT`. Controls through C5b, enumeration, origins, and receipts pass. The independent C6 auditor re-reads the pinned source and finds the sealed ledger instead recorded `b=4.0`, so `C6_AUDIT.json` marks the ledger `MISMATCH`. | Earlier §4 stop classes are false. §6 says any sampled outcome the audit cannot reproduce or any ledger incompleteness files `CENSUS_AUDIT_FAILED`; §4 also defines that class for an unreproducible sampled outcome or ledger. In §4 precedence it is reached before tally classes, and no tally is filed. | yes |
| `R3C2_NO_CLASS` | Planned claim E has the inputs above, but before enumeration both independent seats run C5 twice and `/usr/bin/python3 -c "import sympy"` exits nonzero on both attempts because SymPy is unavailable. Thus the same C0–C5b control fails in every seat that attempted it. | C5's predicate makes the control `FAIL`. §4 `R3C2_NO_CLASS` applies to a C0–C5b control failing in every attempting seat after two attempts. It is first in filing precedence; later limbs are unreached and their controls are `NOT_RUN`. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` | Candidate X says “The fitted radius is 7.0 km.” Seat A treats it as the paper's asserted own result and includes it with its fit inputs; seat B treats it as attributed-not-derived and excludes it. The disagreement survives two reconciliation attempts; all earlier controls pass. | §1 explicitly routes an inclusion disagreement surviving two reconciliation attempts to `CENSUS_DENOMINATOR_DISPUTED`; §4 defines the same class. No earlier no-class/control-split applies, so §4 precedence files it and the census does not proceed. | yes |
| `CENSUS_ORIGIN_DISPUTED` | Nine agreed included claims all have agreed input lists and outcomes. In claim O, “We adopt h = 0.70 from our fit,” both seats record the same `PRINTED` value and arithmetic, but seat A classifies its origin `FITTED` from the fit wording while seat B classifies it `CHOSEN` from “adopt”; quotations are recorded. The disputed input affects 1 of 9 claims = 11.11%; controls, denominator, and input lists agree. | No earlier §4 class applies. §3/C3 requires independent origin classification and does not reconcile origin disputes. Because the disagreement affects more than 10% of included claims, §4 files `CENSUS_ORIGIN_DISPUTED`; it precedes audit and tally classes. | yes |
| `CENSUS_CONTROL_SPLIT` | Planned claim E is in scope. After two C5 attempts, seat A prints successful Python/SymPy/hash commands and passes; seat B's SymPy command exits nonzero twice and fails. | C5 supplies the pass/fail predicate. The same control passes in one seat and fails in another after two attempts, satisfying §4 `CENSUS_CONTROL_SPLIT`. `R3C2_NO_CLASS` is false because the control did not fail in every seat. The second position in §4 precedence files the split and stops. | yes |

Direct answer to the `CENSUS_COMPLETE` suspicion: **REACHABLE.** Corpus C above is a concrete many-claim input in which every included claim has exactly one arithmetic-group outcome, all earlier stop conditions are false, `CENSUS_PARTIAL` is false, and precedence reaches `CENSUS_COMPLETE`. The word “every” makes completeness fragile, not impossible. If even one included claim anywhere in the actual corpus carries `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE` after the allowed attempt/repeat, then—provided no earlier stop class applies—§4 makes `CENSUS_PARTIAL` true and files it ahead of `CENSUS_COMPLETE`. Excluded candidates do not have per-claim outcomes and do not defeat completeness. This exhibition cannot and need not assert whether the pinned corpus actually contains such a claim; reachability asks whether a licensed input can produce the class.

No declared §3 outcome or §4 class is unreachable, so there is no blocking-clause section to quote.

R3C2_C0_EXHIBITION_COMPLETE
