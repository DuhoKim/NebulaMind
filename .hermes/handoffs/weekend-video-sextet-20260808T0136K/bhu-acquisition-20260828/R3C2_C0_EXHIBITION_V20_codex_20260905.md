ACCESS_SHA=e8ba4a7438d61f028e3c46c7308d7078f011e53886e332b946bd7e21f6a1c6c8
C0_REACHABILITY=PASS

Scope: reachability only, not gating and not a physics judgment. Each illustrative passage below is an included quantitative claim under §1: it prints a numeral asserted as the paper's own result. That premise depends on the two seats making the marked §1 judgment that the numeral is “the paper's own result”; the examples do not treat inclusion as mechanical. `REPRO_AFTER_CHOICE` is retired and is therefore not a declared §3 outcome to exhibit.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Claim E prints “area = 12.0 cm²,” states `A = l*w`, and prints `l = 3.0 cm`, `w = 4.0 cm`. Mechanical evaluation gives 12.0 cm². | §1 included own result → §2 lists two `PRINTED` inputs → §2 step 4 consumes both → no earlier §3 terminal condition → 12.0 rounds to printed 12.0 → §3 arithmetic group, `REPRO_EXACT`. | YES |
| `REPRO_FAILED` | Claim F prints “area = 13.0 cm²,” states `A = l*w`, and prints `l = 3.0 cm`, `w = 4.0 cm`. Evaluation gives 12.0 cm². | §1 included → sufficient `PRINTED` inputs → arithmetic completes → no earlier §3 terminal condition → result does not match at printed precision → §3 arithmetic group, `REPRO_FAILED`, reported as “unreproduced from the stated inputs.” | YES |
| `REPRO_BLOCKED` — source outside enumerable manifest | Claim B1 prints “speed = 10 m/s,” states `v=d/t`, prints `t=2 s`, and says distance `d` is from citation X; X is not an enumerable text pinned in the manifest. | §1 included → `t` is `PRINTED`; unprinted `d` names source X → §2 named-source rule makes `d` `BLOCKED` and forbids consumption → §3 precedence (`NO_DERIVATION` first is false, then `BLOCKED`) → `REPRO_BLOCKED`. | YES |
| `REPRO_BLOCKED` — pinned source has no cited-line match | Claim B2 is as B1, but X is an enumerable pinned text and the value of `d` does not machine-match at X's cited line. | §2 imported-value exception fails its required cited-line machine match → `d` has status `BLOCKED` → §3 `REPRO_BLOCKED` before absent/not-evaluable/arithmetic. | YES |
| `REPRO_NOT_EVALUABLE` — timeout | Claim T prints “N = 42,” gives a fully specified symbolic procedure and all inputs, but the enforced computation exceeds 120 seconds; record the reached expression as the point reached. | §1 included → recipe and inputs exist → no no-derivation, blocked, or absent condition → 120-second cap exceeded → §3 precedence reaches `REPRO_NOT_EVALUABLE`; print `SYMBOLIC_TIMEOUT` and point reached. | YES |
| `REPRO_NOT_EVALUABLE` — unavailable machinery | Claim M prints “N = 42,” gives a fully specified procedure and all inputs, but explicitly requires hardware/software machinery the lane does not have; record the last completed step. | §1 included → earlier terminal conditions false → required machinery unavailable → §3 `REPRO_NOT_EVALUABLE`; print `MACHINERY_UNAVAILABLE` and point reached. | YES |
| `REPRO_NO_DERIVATION_STATED` — no recipe | Claim N1 prints “the fitted radius is 7.2 kpc” as its own result and states neither an equation nor a computational procedure. | §1 included own result → §2 cannot extract a producing recipe → first terminal condition in §3 precedence is true → `REPRO_NO_DERIVATION_STATED`, passage named. | YES |
| `REPRO_NO_DERIVATION_STATED` — named but unspecified procedure | Claim N2 prints “the pipeline yields 7.2 kpc” as its own result and merely names “our pipeline,” with no operations a seat could attempt. | §3 expressly treats a named-but-unspecified procedure as no computational procedure → first precedence limb → `REPRO_NO_DERIVATION_STATED`. | YES |
| `REPRO_INPUT_ABSENT` | Claim A prints “speed = 10 m/s” and states `v=d/t`; it prints `t=2 s` but neither prints `d` nor traces `d` to any named source. | §1 included → equation requires `d` → §2 classifies `d` `ABSENT` and forbids supplying it → no-derivation false and blocked false → §3 precedence reaches `REPRO_INPUT_ABSENT`; name `d`. | YES |
| `CENSUS_COMPLETE` | A concrete 100-claim census: claims E1–E50 each have the Claim E inputs above and file `REPRO_EXACT`; claims F1–F50 each have Claim F inputs and file `REPRO_FAILED`. The seats agree on enumeration, input lists, origins, and outcomes; C0–C5b pass; C6 reproduces every sampled outcome/ledger and prints `C6_AUDIT_SAMPLE=PASS`; receipt verification passes. | No earlier §4 stop class applies → no audit failure → every included claim has exactly one member of the §3 arithmetic group → no non-arithmetic claim, so `CENSUS_PARTIAL` is false → `C6_AUDIT_SAMPLE=PASS` → §4 precedence reaches `CENSUS_COMPLETE`; report denominator 100 and full tally (plus `rests_on` tally). | YES |
| `CENSUS_PARTIAL` | A concrete 100-claim census has E1–E99 as Claim E (`REPRO_EXACT`) and A1 as Claim A (`REPRO_INPUT_ABSENT`); all controls, reconciliations, origins, C6 audit, and receipt pass. | Earlier §4 classes false → A1 is an included claim with a non-arithmetic outcome after its permitted attempt → §4 `CENSUS_PARTIAL` is true and precedes `CENSUS_COMPLETE`; report A1 and why. | YES |
| `CENSUS_AUDIT_FAILED` — sampled outcome/ledger mismatch | Ten included arithmetic claims survive earlier stages, but C6 recomputation of sampled E3 yields 11.0 while its sealed ledger/outcome says 12.0/`REPRO_EXACT`. | Controls C0–C5b, enumeration, and origin stages pass → C6 cannot reproduce sampled outcome/ledger → §4 `CENSUS_AUDIT_FAILED`; its precedence is before partial/complete and no tally is filed. | YES |
| `CENSUS_AUDIT_FAILED` — audit otherwise not PASS | The external seed is never supplied, so C6 cannot run and records `C6_AUDIT_SAMPLE=NOT_RUN`, with that cause named. | Earlier stages pass → audit “does not run to PASS for any cause” → §4 `CENSUS_AUDIT_FAILED`. | YES |
| `CENSUS_AUDIT_FAILED` — seal receipt | All claim work and C6 pass, but verification of the seal receipt fails because the received commit identifier differs from the sent identifier. | Earlier stop classes false → §4 expressly routes failed receipt verification to `CENSUS_AUDIT_FAILED`, not `R3C2_NO_CLASS` → precedence files it before tally classes. | YES |
| `R3C2_NO_CLASS` — control failure in every attempting seat | At C2, both dispatched seats submit ledgers with a `PRINTED` input lacking `source_line`; validation fails in each seat on both attempts. | A C0–C5b control fails in every seat that attempted it after two attempts → first class in §4 precedence, `R3C2_NO_CLASS`; later limbs are unreached/`NOT_RUN`. | YES |
| `R3C2_NO_CLASS` — pre-dispatch packet/isolation failure | Before dispatch, the built seat packet is missing the manifest (or the packet redaction builder returns `C4_PACKET_REDACTED=FAIL`), so no seat is dispatched. | §4 explicitly says packet or seat-isolation failure before dispatch files `R3C2_NO_CLASS` → it is first in filing precedence. | YES |
| `CENSUS_DENOMINATOR_DISPUTED` — enumeration | Seat A includes candidate Q (“7.0 kpc” asserted as own result); Seat B excludes Q as attributed-not-derived. The disagreement survives both reconciliation attempts; controls have otherwise passed. | No prior control class → §1 two-seat inclusion disagreement after two reconciliations stops the study → §4 `CENSUS_DENOMINATOR_DISPUTED`; disputed Q and complete candidate/exclusion ledgers reported. | YES |
| `CENSUS_DENOMINATOR_DISPUTED` — input lists | Both seats include Claim F, but A lists inputs `{l,w}` and B lists `{l,w,k}` from the stated equation; the difference survives the one C3 reconciliation. | Controls and enumeration pass → §4 expressly includes surviving disagreement in input lists for agreed claims → `CENSUS_DENOMINATOR_DISPUTED`, before origin/audit/tally classes. | YES |
| `CENSUS_ORIGIN_DISPUTED` | In a 20-claim included census, the seats agree on claims, inputs, and outcomes, but for inputs affecting claims 1–3, A files `CHOSEN` with quotations and B files `IMPORTED` with quotations; 3/20 = 15%, and no earlier class applies. | Input lists agree → independent origins disagree on inputs affecting more than 10% of included claims → §4 `CENSUS_ORIGIN_DISPUTED`; all disputed inputs, classifications, and quotations reported; census does not proceed. | YES |
| `CENSUS_CONTROL_SPLIT` | At C3, Seat A's validator exits 0 on both attempts; Seat B's validator exits 1 on both attempts because its ledger contains an extra schema field. | The control passes in one seat and fails in another after two attempts → `R3C2_NO_CLASS` is false because failure is not in every attempting seat → next §4 precedence limb files `CENSUS_CONTROL_SPLIT`; report both outputs and stop. | YES |

Direct answer on the suspicion: `CENSUS_COMPLETE` is REACHABLE, including in a real corpus of many papers, on the concrete 100-claim input above (or any nonempty corpus) if every included claim files exactly one of `REPRO_EXACT` or `REPRO_FAILED`, C6 passes, and no earlier study-level class applies. It is not guaranteed. A single included claim filing `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE` makes `CENSUS_PARTIAL` true; because §4 places `CENSUS_PARTIAL` before `CENSUS_COMPLETE`, that one claim forces `CENSUS_PARTIAL` provided no still-earlier class applies. Thus blocked/absent/no-derivation anywhere does not make `CENSUS_COMPLETE` globally unreachable; it makes it unavailable for that particular realized corpus/tally.

All six declared §3 outcomes and all seven declared §4 classes have at least one licensed concrete path. There are no UNREACHABLE verdicts, so no blocking-clause quotation is required.

R3C2_C0_EXHIBITION_COMPLETE
