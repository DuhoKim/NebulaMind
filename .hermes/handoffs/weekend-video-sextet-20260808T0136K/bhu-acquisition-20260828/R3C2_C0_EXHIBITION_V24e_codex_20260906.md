ACCESS_SHA=944300dc68257c705be1da503660559242256ced061b3ef1503ce44c2e4030cc
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| REPRO_WITHIN_STATED_PRECISION | Hypothetical pinned paper P, line 1: “We choose a = 2.” Line 2: “Our result is y = 4.0, calculated as y = a + a.” Printed/reproduced pair: 4.0 / 4. | §1 includes the own-result numeral; §2 records a as PRINTED; C3 records CHOSEN with ORIG_CHOICE_STATED quoting line 1. §2 step 4 consumes it. No earlier §3 terminal condition holds; 4 rounds to 4.0. | yes |
| REPRO_WITHIN_STATED_PRECISION — precision branches | Replace P's input with a = 2.025 and result with 4.1: reproduction 4.050 rounds half away from zero to 4.1. Alternatively a = 2.05, result 4.0 ± 0.1: reproduction 4.10 lies exactly at the allowed boundary. For asymmetric uncertainty, result 4.0 (+0.2, −0.1), a = 2.075: reproduction 4.15 uses the upper half-width 0.2. | §3's no-uncertainty rounding rule, symmetric uncertainty rule, and side-specific asymmetric rule respectively. All values are printed; no earlier terminal condition applies. | yes |
| REPRO_FAILED | P instead prints “Our result is y = 5.0, calculated as y = a + a,” with chosen a = 2. Printed/reproduced pair: 5.0 / 4. | §1 → §2 sufficient PRINTED input → §3 arithmetic group. 4 does not round to 5.0. Report “unreproduced from the stated inputs.” | yes |
| REPRO_BLOCKED — source outside enumeration | P prints “Our result is y = 4.0, using y = a + a; a is taken from Q, line 8.” No value for a appears in P; Q is outside the pinned enumerable corpus (including the alternative that Q is marked RAW). | §2 named-source rule → §3 BLOCKED; C3 records status BLOCKED, origin IMPORTED, ORIG_CITATION quoting P's naming sentence, with no value. A recipe exists, so NO_DERIVATION does not precede it. | yes |
| REPRO_BLOCKED — pinned source without match | Same claim, but Q is pinned and enumerable; Q line 8 says only “The parameter is described below,” with no machine-matchable a value. | §2 imported-value match requirement fails → §3's second BLOCKED branch. No value may be supplied. | yes |
| REPRO_INPUT_ABSENT | P prints “Our result is y = 4.0, calculated as y = a + a.” It nowhere prints a or names a source for it. | §1 inclusion → §2 required-input list {a}; status ABSENT, no value. No named source means BLOCKED does not apply; §3 INPUT_ABSENT stops the attempt. C3 records UNDECLARED/ORIG_SILENT with the actual source search, including symbol and name variants. | yes |
| REPRO_NOT_EVALUABLE — timeout | P prints chosen n = 1000000000000 and its own result y = 0.0, with the explicit recipe “Initialize s = 0; for each integer j from 1 through n, add j to s; return s.” The mandated mechanical attempt reaches j = 1000000 when the wrapper's 120.0-second deadline expires, before returning y. | All inputs PRINTED; a specified procedure exists; no BLOCKED or ABSENT input. §9 wrapper prints SYMBOLIC_TIMEOUT and exits 124 → §3 NOT_EVALUABLE, with the point reached. No completed arithmetic comparison is available. | yes |
| REPRO_NOT_EVALUABLE — machinery | P prints n = 1000, an explicit 1000-qubit gate sequence and the instruction to execute it on a 1000-qubit processor and return the frequency of the all-zero result after n shots; it asserts y = 0.250. The lane has no such processor or available simulator for this procedure. | §2 lists the printed shot count and specified circuit; §3's machinery-unavailable branch applies before the arithmetic group. Print MACHINERY_UNAVAILABLE and “circuit specified; execution not started.” | yes |
| REPRO_NO_DERIVATION_STATED — no recipe | P line 1 says only “Our result is y = 4.0.” No equation or computational procedure occurs elsewhere. There are no ledger records; rests_on is NOT_COMPUTED. | §1 includes the asserted result despite missing derivation; §3's first terminal condition files NO_DERIVATION, naming line 1. | yes |
| REPRO_NO_DERIVATION_STATED — named-only procedure | P line 1 says “Our simulation gives y = 4.0,” without operations specifying the simulation. | §3 expressly says a named but unspecified procedure states no computational procedure that could produce the number. First terminal condition wins. | yes |
| CENSUS_COMPLETE | A hypothetical corpus of 100 pinned enumerable papers P001–P100. Each has exactly P's two lines from the first row, with its own claim and input IDs. Both seats agree on all 100 inclusions, inputs, origins and WITHIN outcomes. Controls pass; receipts verify; the external seed is supplied; the auditor checks every candidate and independently obtains 4 for all 100 claims, printing the required audit artefact with no mismatch. | §1 denominator 100 → §2 attempts → §3 100 arithmetic outcomes. C6: N = 100, R = 0, k = 0; all 100 claims audited under limb (i), C6_AUDIT_SAMPLE=PASS. No earlier §4 filing condition applies → §4 COMPLETE. Reproduction tally: 100 WITHIN; rests_on tally: 100 USES_CHOSEN. | yes |
| CENSUS_COMPLETE — failed arithmetic still complete | Same 100-paper corpus, but P100 prints 5.0 while its recipe produces 4. Both seats and auditor agree on that FAILED outcome; all other conditions above hold. | §3 defines the arithmetic group as WITHIN and FAILED. §4 COMPLETE receives 99 WITHIN plus 1 FAILED; C6 audits all 100 and passes. | yes |
| CENSUS_PARTIAL — non-arithmetic claim | Use 99 first-row papers plus one paper from the BLOCKED, INPUT_ABSENT, NO_DERIVATION, or NOT_EVALUABLE rows, separately. Seats agree; for NOT_EVALUABLE the permitted repeat also fails to complete. All earlier controls pass, audit and receipts pass. | §2 attempt → §3 exactly one non-arithmetic outcome. C6: N = 100, R = 1, k = min(20,1) = 1, so audit the remaining claim as well as the 99 arithmetic claims. §4 PARTIAL applies and precedes COMPLETE. | yes |
| CENSUS_PARTIAL — empty denominator | The hypothetical pinned corpus contains only the candidate “Publication date: 2026.” Both seats exclude it as DATE; candidate count 1, included 0, excluded 1, attempts 0, exclusion count 1. Controls, receipts and the completeness audit pass. | §1 exclusion → C1 consistent counts → C6 N = R = k = 0, candidate/exclusion completeness still audited → §4 explicitly routes denominator zero to PARTIAL. | yes |
| CENSUS_AUDIT_FAILED — outcome or ledger mismatch | Single first-row claim; both seats seal 4.0 / 4 and WITHIN, with C0–C5b passing and no disputes. Auditor cannot reproduce the sealed result, reporting 5 and FAILED with MISMATCH. Alternative: the full-source audit finds an omitted second own-result passage “Our second result is z = 7.0.” | C6 cannot reproduce the outcome, or finds ledger incompleteness → §4 AUDIT_FAILED. No earlier stop applies. No tally is filed. Auditor disagreement with the sealed record is this audit branch, not the two original seats' OUTCOME_DISPUTED branch. | yes |
| CENSUS_AUDIT_FAILED — missing seed | Single first-row claim, agreed and receipted; earlier controls pass, but the external custodian supplies no seed. | C6 expressly assigns C6_AUDIT_SAMPLE=NOT_RUN and CENSUS_AUDIT_FAILED with missing seed named. It precedes COMPLETE. | yes |
| CENSUS_AUDIT_FAILED — seal verification | Single first-row claim; earlier controls and audit pass. Receipt T records the merged candidate file's digest, but independent verification finds different bytes and a different digest for that file. | §7 receipt mismatch → §4's seal-verification branch of AUDIT_FAILED; interpretation NOT_RUN. This is expressly excluded from R3C2_NO_CLASS. | yes |
| R3C2_NO_CLASS — all attempting seats fail | Intended corpus is the first-row paper. In each of two seats, both C5 attempts exit nonzero on importing SymPy, with ModuleNotFoundError printed. | C5_HARNESS_PINNED=FAIL in every attempting seat after two attempts → §4 R3C2_NO_CLASS, first filing priority. Arithmetic and later controls are NOT_RUN. | yes |
| R3C2_NO_CLASS — before dispatch | Intended corpus is the first-row paper. The dispatch copy lacks the pinned ledger tool, so the required packet inventory fails before dispatch. | C4 says a copy missing a required file is not dispatched; §4 explicitly routes a pre-dispatch packet failure to R3C2_NO_CLASS. No claim attempt is needed. | yes |
| CENSUS_DENOMINATOR_DISPUTED — inclusion | P line 1: “Following Q, our estimate is 4.0.” No derivation resolves whether this is P's own result or an attributed value. Seat A includes it; seat B excludes it as ATTRIBUTED_NOT_DERIVED. Both positions survive two reconciliation attempts; earlier controls pass. | §1 independent inclusion rule → §6 limb A zero tolerance → §4 DENOMINATOR_DISPUTED. Report both candidate and exclusion ledgers and the disputed passage; later controls NOT_RUN. | yes |
| CENSUS_DENOMINATOR_DISPUTED — input lists | P prints “We choose a = 2; a′ = 2 is defined by a′ = a; our result y = 4.0 follows from y = a + a′.” Both seats include y; A lists {a,a′}, B treats a′ as an alias and lists {a}. Each submitted ledger validates; the input-ID difference survives the one reconciliation. | C3 merge exits 1 for different input-ID sets → one reconciliation → §4 DENOMINATOR_DISPUTED. C3 distinguishes this input-set dispute from a repeated validator failure; input-list completeness is seat-authored. | yes |
| CENSUS_OUTCOME_DISPUTED | A single claim explicitly requires a long finite summation with all bounds printed and asserts 1.0. Seat A completes the recipe in 119 seconds and obtains 1, filing WITHIN (pair 1.0 / 1). Seat B reaches the 120-second cap without a result and files NOT_EVALUABLE (printed 1.0; reproduced value unavailable), retaining that outcome after one reconciliation and a permitted repeat. Earlier controls pass; input lists agree. | §2 step 5 and §3 permit the two operational outcomes; §4 OUTCOME_DISPUTED applies when they survive reconciliation. Report both outcomes, the completed pair, B's unavailable reproduced value and point reached. No invented numerical pair for B; C1 requires pairs only for arithmetic outcomes. | yes |
| CENSUS_ORIGIN_DISPUTED | Ten included claims, each y = a + a = 4.0 with a printed as 2. In two papers one line says “We choose a = 2,” another “Fitting the calibration data gives a = 2.” Seat A cites the first line as CHOSEN/ORIG_CHOICE_STATED; B cites the second as FITTED/ORIG_FIT_STATED. Their input sets agree, both quote their lines, and arithmetic outcomes agree. Other eight origins agree; earlier controls pass. | C3 independent classifications and valid quoted evidence → disputes carried without reconciliation. Two of ten claims = 20%, strictly more than 10% → §4 ORIGIN_DISPUTED, before audit. No need to assume both provenance interpretations are correct; the declared condition is surviving independent disagreement. | yes |
| CENSUS_CONTROL_SPLIT | Intended corpus is the first-row paper. After two C5 attempts A has live outputs from all five commands, all exits 0 and matching dispatch path/digest; B's SymPy import fails on both attempts. All other earlier controls pass. | C5 passes A, fails B → §4 CONTROL_SPLIT. R3C2_NO_CLASS does not apply because this control did not fail in every attempting seat. Stop without adopting A's result; later limbs NOT_RUN. | yes |

These are constructed inputs for reachability, not findings about the actual pinned corpus. P and Q name hypothetical source passages, not files opened. Only the requested preregistration was read, in full; no other existing directory file was read or sought. PASS here is this exhibition's result; it does not assert that the second independent C0 seat has verified it or that a gate or census has run.

For the study rows, stated passing controls, agreement, audit records and receipts are components of the hypothetical execution input. A row introducing a stop excludes all earlier stop conditions. Filing precedence is exactly: R3C2_NO_CLASS → CENSUS_CONTROL_SPLIT → CENSUS_DENOMINATOR_DISPUTED → CENSUS_OUTCOME_DISPUTED → CENSUS_ORIGIN_DISPUTED → CENSUS_AUDIT_FAILED → CENSUS_PARTIAL → CENSUS_COMPLETE. Later unreached controls are NOT_RUN, not failures that retroactively override the stop.

**Direct answer to the suspicion: CENSUS_COMPLETE is REACHABLE.** The 100-paper construction exhibits a many-paper input. Corpus size does not itself require a non-arithmetic outcome. The result need not be correct relative to the printed number: an agreed, audited REPRO_FAILED outcome still belongs to the arithmetic group. Conversely, one included blocked, absent-input, no-derivation-stated, or not-evaluable claim prevents COMPLETE. With earlier stops absent and audit/receipt checks passing, it routes to PARTIAL. If an earlier stop applies, that class wins instead; “one such claim always forces the filed class PARTIAL” would ignore §4 precedence. Whether the actual pinned corpus permits COMPLETE cannot be established without enumerating it, which this task does not authorize. Practical frequency is not logical unreachability.

The exact completeness requirement is: “every included claim carries exactly one outcome from the arithmetic group of §3, with `C6_AUDIT_SAMPLE=PASS`.” The operative arithmetic-group definition is: “`REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`”. These clauses constrain completeness; neither says every corpus must contain a non-arithmetic claim.

**Definition dependence:** §1's operative question is whether “the paper's own number follow from the paper's own recipe applied to the inputs it states.” Its SEAT-REDACT markers hide the provenance question from the seat; they do not hold the operative definition open. §3 explicitly consumes PRINTED or STANDARD records irrespective of origin, and §10.4 records option (c)'s adoption and removal of the held marker. The chosen-input witnesses therefore reproduce mechanically and carry script-computed USES_CHOSEN separately. REPRO_AFTER_CHOICE is explicitly retired, not a current unreachable verdict. Historical HELD passages and the old REPRO_EXACT name do not add current classes. The inclusion judgement in §1 does matter: agreement admits the first-row claim; surviving disagreement in the inclusion row produces DENOMINATOR_DISPUTED.

**UNREACHABLE verdicts: none.** All six current §3 outcomes and all eight §4 classes have concrete witnesses. There is therefore no clause blocking every path to a current declared verdict to quote.

R3C2_C0_EXHIBITION_COMPLETE
