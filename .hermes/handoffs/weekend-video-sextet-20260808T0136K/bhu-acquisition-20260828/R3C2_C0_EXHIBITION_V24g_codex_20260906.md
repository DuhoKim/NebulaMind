ACCESS_SHA=c232b4ceb32be109e73cfb247ed911a6080f893a787eb28a233ad0a84104deb3
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_WITHIN_STATED_PRECISION` | Paper passage P: “We choose a = 2. Our result is q = 4, using q = a + a.” Input a is `PRINTED`, value `2`, origin `CHOSEN`, supported by the first sentence. Printed/reproduced pair: 4 / 4. | §1 includes q as the paper's own numerical result → §2 extracts recipe and input → C3 records `ORIG_CHOICE_STATED` with matching quotation → §2 step 4 and §3 consume `PRINTED` regardless of origin → no earlier terminal condition → §3 precision test passes. Script-computed `rests_on=USES_CHOSEN` does not change the outcome. | yes |
| `REPRO_WITHIN_STATED_PRECISION` — rounding branch | P instead chooses a = 1.25, states q = a + a, and prints its result q = 3 without uncertainty. Pair: 3 / 2.5. Negative variant: a = -1.25, printed q = -3, reproduced -2.5. | Same status-based route → §3 uses printed integer precision and rounds half away from zero: 2.5 → 3 and -2.5 → -3. | yes |
| `REPRO_WITHIN_STATED_PRECISION` — uncertainty branches | P chooses a = 2.1 and prints q = 4.0 ± 0.2 for q = a + a. Pair: 4.0 / 4.2. Asymmetric variants print q = 4.0 with lower uncertainty 0.1 and upper 0.2: a = 2.1 gives 4.2; a = 1.95 gives 3.9. | §3 takes the uncertainty once, without rounding: differences 0.2, 0.2 and 0.1 respectively meet the inclusive bound; asymmetric cases select the upper and lower half-width respectively. | yes |
| `REPRO_FAILED` | P chooses a = 2, specifies q = a + a, but prints its own result q = 5 without uncertainty. Pair: 5 / 4. | §1 → §2 sufficient `PRINTED` input → no earlier §3 condition → reproduced 4 does not round to printed 5 → arithmetic-group `REPRO_FAILED`; report “unreproduced from the stated inputs.” | yes |
| `REPRO_BLOCKED` — non-enumerable source | P prints q = 4 and q = a + a, and says “Use a from Source S, line L”; P prints no value for a. S is outside the hypothetical pinned enumerable corpus, or is listed only as RAW. | §1 → §2 named-source rule → §3 blocked-source limb → C3 records a as `BLOCKED`, `IMPORTED`, `ORIG_CITATION`, quotation from P's naming sentence, no value → attempt stops. Recipe exists, so no-derivation precedence does not intercept. | yes |
| `REPRO_BLOCKED` — cited-line mismatch | Same claim and citation, but S is pinned and enumerable; its cited line L says only “Parameter values will be supplied later,” with no matchable value for a. | §2 requires a machine match at the cited source line → §3 second blocked limb → same no-value `BLOCKED` record. No value is supplied by the seat. | yes |
| `REPRO_INPUT_ABSENT` | P prints “Our result q = 4, computed by q = a + a,” but gives neither a value nor a named source for a anywhere. | §1 → §2 lists a → status `ABSENT`, no value; C3 records `UNDECLARED` with `ORIG_SILENT` and an adequate recorded search including symbol and name variants → §3: recipe exists, no blocked input, then absent-input stop. | yes |
| `REPRO_NOT_EVALUABLE` — timeout | P prints its own result q = 1.0 and specifies: initialize x = 1; perform 10^100 sequential updates x ← sin(x); output x. All initialization and iteration inputs are printed, with stated chosen provenance. The actual wrapped attempt reaches only a finite initial portion before the 120.0-second deadline. | §1 → §2 fully specified procedure and `PRINTED` inputs → no no-derivation, blocked or absent condition → §9 wrapper prints `SYMBOLIC_TIMEOUT`, exit 124, and point reached → §3 `REPRO_NOT_EVALUABLE`. The witness includes the observed timeout, not an assertion that every implementation must time out. | yes |
| `REPRO_NOT_EVALUABLE` — unavailable machinery | P prints q = 4, a = 2, and a fully specified instruction sequence for a proprietary arithmetic accelerator: load a into register R, ADD R,R, return R. The required execution device/emulator is unavailable to the lane. | §2 has an executable specification and all numerical inputs; the missing item is machinery, not an unspecified operation or numerical input → §3 machinery limb, print `MACHINERY_UNAVAILABLE` and “stopped before executing accelerator instructions.” | yes |
| `REPRO_NO_DERIVATION_STATED` — no recipe | P's entire substantive passage is “Our result is q = 4.” There is no equation or computational procedure elsewhere. | §1 includes the asserted own result despite absent derivation → §3 first terminal condition, name P's passage; no arithmetic attempt. With no ledger records, script reporting uses `rests_on=NOT_COMPUTED`. | yes |
| `REPRO_NO_DERIVATION_STATED` — named but unspecified procedure | P says “Our result q = 4 was obtained using our reconstruction method,” but never states its operations. | §1 inclusion → §3 explicit named-but-unspecified-procedure sentence → first terminal outcome; do not invent inputs or a procedure. | yes |
| `CENSUS_COMPLETE` | A hypothetical pinned corpus contains 100 papers. Each says, in words, “Our result q = 4 is obtained by counting the entries in the list [red, blue, green, gold].” Each has exactly one numerical result candidate, q = 4. The two seats agree on all 100 inclusions, input lists, origins and outcomes; every pair is 4 / 4. All earlier controls pass, C6 audits all 100 claims and passes, and seal receipts verify. | §1 yields N = 100 → §2 applies the stated counting recipe to the printed list → §3 gives 100 arithmetic-group outcomes. C3 records the list as a `PRINTED` input, its value matching the source text, with `UNDECLARED` origin and the required recorded search. C6 has R = 0, k = 0; every claim is audited under its arithmetic limb. No earlier §4 condition holds → §4 `CENSUS_COMPLETE`, denominator 100 and both tallies reported. | yes |
| `CENSUS_COMPLETE` — arithmetic failure allowed | Same 100-paper corpus, except one paper prints q = 5 for the same recipe, producing pair 5 / 4. Seats and auditor agree: 99 within-precision outcomes and one `REPRO_FAILED`; all controls and receipts pass. | §3 explicitly puts both outcomes in the arithmetic group → every included claim still has one arithmetic-group outcome → §4 complete. Completeness does not require successful reproduction of every number. | yes |
| `CENSUS_PARTIAL` — each non-arithmetic branch | Starting with the 100-paper complete witness, replace one paper's sole result passage with, separately, the blocked, absent, timeout/unavailable-machinery, or no-derivation witness above. Both seats agree; optional repeat, if used for not-evaluable, leaves that outcome unchanged. All earlier controls, C6 and receipts pass. | §3 gives 99 arithmetic outcomes plus one specified non-arithmetic outcome → C6 audits all 99 arithmetic claims and the sole remaining claim: N = 100, R = 1, k = 1 → §4 partial condition holds and complete condition fails. No higher-priority stop applies. | yes |
| `CENSUS_PARTIAL` — zero denominator | Every enumerable source contains only prose and dated headings, e.g. “2026”; both seats record each numeral as `DATE`. There are no included claims. Candidate/exclusion counts agree and validate; the complete ledger audit passes with no included claims to re-derive; receipts verify. | §1 exclusions → C1 records N = 0 → C6 R = 0 and k = 0, with completeness still audited → §4 explicitly routes the empty enumeration to `CENSUS_PARTIAL`, never vacuous completeness. | yes |
| `CENSUS_AUDIT_FAILED` — audit mismatch | One-paper version of the counting witness: both original seats file q's pair 4 / 5 and `REPRO_FAILED` after making the same arithmetic mistake. Their schemas, input lists and origins agree and controls pass. The third seat obtains 4 / 4 and `REPRO_WITHIN_STATED_PRECISION`. | No original-seat outcome dispute → C6 audits the arithmetic claim and prints `MISMATCH` → §4 audit failure. Original-seat agreement does not require that their shared calculation be correct. No tally is filed. | yes |
| `CENSUS_AUDIT_FAILED` — missing seed | One-paper complete arithmetic witness, valid receipt T, but the external custodian supplies no seed. All earlier controls and agreements pass. | C6 expressly requires an externally supplied, recorded seed → `C6_AUDIT_SAMPLE=NOT_RUN` with missing seed named → §4 `CENSUS_AUDIT_FAILED` precedes completeness. | yes |
| `CENSUS_AUDIT_FAILED` — seal failure | One-paper complete arithmetic witness and C6 PASS, but independent seal verification finds the tally's actual hash differs from the hash acknowledged in receipt T. | §7 receipt mismatch → §4 expressly assigns seal failure to `CENSUS_AUDIT_FAILED`, not `R3C2_NO_CLASS`; interpretation `NOT_RUN`, no tally filed under this class. | yes |
| `R3C2_NO_CLASS` — failed control | Intended claim is the counting witness, q = 4. In both seats the live C5 SymPy import exits nonzero on both attempts because SymPy is absent; other earlier controls pass. No claim arithmetic is reached. | C5 nonzero exit gives FAIL → §4 control in C0–C5b fails in every attempting seat after two attempts → first study class, `R3C2_NO_CLASS`; later controls `NOT_RUN`. | yes |
| `R3C2_NO_CLASS` — before dispatch | Intended corpus contains the same q = 4 claim, but the built packet's digest differs from its pin at the pre-dispatch check. | C4 forbids dispatch of a packet whose digest does not match → §4 explicit pre-dispatch packet-failure limb → `R3C2_NO_CLASS`; later limbs `NOT_RUN`. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — inclusion | Candidate passage: “Our estimate is q = 4, as reported by Source S,” with no derivation. Seat A treats it as an asserted own result; seat B treats it as attributed-not-derived. They keep those inclusion/exclusion dispositions through two reconciliations; each candidate/exclusion ledger is internally valid, and earlier controls pass. | §1 independent inclusion judgement → §6 limb A disagreement survives two reconciliations → §4 denominator dispute; no arithmetic or audit is reached. The two readings are the observed input to this disagreement class, not two asserted correct classifications. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — input lists | Both include P's q = 4 for q = a + b, with printed a = 2 and b = 2. Seat A records input IDs a,b; seat B mistakenly records only a. Their individual records validate, and the input-set difference survives one reconciliation. | C3 says input-list completeness is not machine-checked → merge exits 1 for differing input-ID sets → C3's specific one-reconciliation route → §4 denominator dispute. This stop occurs before a twice-failed control in every seat could establish `R3C2_NO_CLASS`. | yes |
| `CENSUS_OUTCOME_DISPUTED` | Both seats include the counting witness q = 4 and agree on its inputs and origins. Seat A counts correctly, reports 4 / 4 and `REPRO_WITHIN_STATED_PRECISION`; seat B counts incorrectly, reports 4 / 5 and `REPRO_FAILED`. The disagreement survives the one required reconciliation. Earlier controls pass. | §2 step 5 → §4 outcome-dispute condition. Report both pairs and each seat's completed counting step. No denominator or earlier control stop applies; C6 is later and `NOT_RUN`, so its potential correction cannot intercept this class. | yes |
| `CENSUS_ORIGIN_DISPUTED` | N = 1; P prints “We choose a = 2” and separately “We fitted a = 2 to our observations.” Both seats include q = 4 from q = a + a, agree on input a and on 4 / 4. Seat A cites the first sentence as `CHOSEN`/`ORIG_CHOICE_STATED`; seat B cites the second as `FITTED`/`ORIG_FIT_STATED`. Both quotations and values match their lines; earlier controls pass. | C3 independent origin classifications are carried, not reconciled; sentence-level reason-code precedence does not make these two different quoted sentences identical → one affected claim out of one = 100%, strictly above 10% → §4 origin dispute before audit and complete. | yes |
| `CENSUS_CONTROL_SPLIT` | Intended claim is the counting witness q = 4. On both C5 attempts seat A's live outputs match the dispatch pin and all five commands exit 0; seat B's site-package manifest digest differs from its dispatch pin on both attempts. Other earlier controls pass. | C5 PASS in A and FAIL in B → not failure in every attempting seat, so `R3C2_NO_CLASS` does not apply → §4 control split, report both outputs and stop. Later limbs `NOT_RUN`; the passing seat is not adopted. | yes |

These are concrete hypothetical source passages and execution records for existential reachability, not claims discovered in the actual corpus. Only the specified pre-registration was read, in full; no manifest, source paper, tool, prior report or other directory file was read or sought. The table does not certify actual harness or validator executions. Where a study witness reaches a later class, it includes successful earlier controls, valid candidate accounting, the specified seat agreements, and successful receipts/audit unless that row explicitly supplies their failure. Stop witnesses intentionally include observed mistakes or disagreements because the declared stop classes take those records as inputs.

The counting witness avoids assuming an exclusion category for author-chosen numerical inputs: its list has four named entries but prints no input numeral. Its only numerical result candidate is q. The list is the printed input, recorded literally; the recipe counts its entries directly. C3's printed schema imposes no numeric-only restriction on `PRINTED` input values. The list's unstated provenance is recorded as `UNDECLARED` with the required adequate search, and the script reports `USES_UNDECLARED`. Neither provenance nor that field prevents arithmetic completeness under option (c). The small per-claim witnesses using chosen numerical inputs establish q's outcome; they do not purport to resolve enumeration of every other numeral in those illustrative passages.

`CENSUS_COMPLETE` is **REACHABLE**. The 100-paper witness exhibits a nonempty many-paper input, and the variant demonstrates that even a correctly identified arithmetic failure permits completeness. The operative requirement is coverage by arithmetic-group outcomes, with the audit and higher-priority conditions satisfied. There is no clause requiring every corpus to contain a blocked, absent-input, or no-derivation claim.

A single included claim carrying `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, or `REPRO_NOT_EVALUABLE` does prevent `CENSUS_COMPLETE` for that particular census. With earlier stop conditions absent and C6 PASS, it files `CENSUS_PARTIAL`. If a higher-priority stop applies, that class is filed instead: §4 orders `R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`. Thus “one non-arithmetic claim always forces PARTIAL” needs that precedence qualification; “one such claim makes COMPLETE unavailable for this corpus” is correct. Whether the actual pinned corpus contains such a claim is not established by this exhibition.

The §1 dependence is explicit: the witnesses use the current operational criterion, “prints a numeral the paper asserts as a result of its own,” and the current question about the paper's recipe applied to its stated inputs. A bare own-result numeral remains included and routes to `REPRO_NO_DERIVATION_STATED`; it cannot be excluded merely to obtain completeness. The `SEAT-REDACT` markup separates what appears in a seat packet; it is not an operative HELD marker. The historical held wording in §10 was superseded by the option-(c) ruling. Under the standing §2–§3 status rule, chosen inputs can produce arithmetic outcomes, with provenance recorded separately. `REPRO_AFTER_CHOICE` is explicitly retired and is not a seventh active outcome; `REPRO_EXACT` is the former name, not another active outcome. Exclusion kinds and `rests_on` values are not §3 per-claim outcomes or §4 study classes.

UNREACHABLE verdicts: none among the six active per-claim outcomes and eight active study-level classes. Accordingly, there is no universal blocking clause to quote. This PASS is this exhibition's reachability finding; it does not assert completion of the separately required independent second-seat verification, a gate, or a physics judgement.

R3C2_C0_EXHIBITION_COMPLETE
