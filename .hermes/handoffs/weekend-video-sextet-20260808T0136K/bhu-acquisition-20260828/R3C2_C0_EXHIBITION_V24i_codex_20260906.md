ACCESS_SHA=9c4b873d4281419a93b8aac9208e5a5de22481217fdd124b1dcfa6a7ae4d88be
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| REPRO_WITHIN_STATED_PRECISION | Synthetic pinned paper P, L1: “From Reference Alpha, without deriving it, we quote input a = 2.0.” L2: “Our result is q = 4.0, calculated by q = a + a.” Ledger: a has status PRINTED, value 2.0, origin IMPORTED, ORIG_CITATION quoting L1, source P:L1, no parents. Computation finishes in 0.01 seconds: printed 4.0; reproduced 4.0. | §1 includes P:L2 and excludes the attributed-not-derived P:L1; §2.1–4 extracts the recipe and consumes the locally printed input; C3 matches value and evidence to L1. §3 has no earlier terminal condition; 4.0 rounds to 4.0 at the printed precision. Origin does not bar consumption. | yes |
| REPRO_FAILED | Same P:L1 and ledger; P:L2 instead prints “Our result is q = 5.0, calculated by q = a + a.” Printed 5.0; reproduced 4.0 in 0.01 seconds. | §1 inclusion → §2 complete stated inputs → §3 no earlier terminal condition → arithmetic misses the printed precision. Report “unreproduced from the stated inputs,” with both numbers. | yes |
| REPRO_BLOCKED — source outside enumerable corpus | P:L1: “Use input a from Reference Alpha.” P:L2: “Our result is q = 4.0, calculated by q = a + a.” No value for a appears in P; Alpha is outside the manifest's enumerable texts. Ledger: a BLOCKED, IMPORTED, ORIG_CITATION at P:L1, no value. | §1 includes P:L2 → §2 named-source rule → §3 REPRO_BLOCKED. A recipe exists, so NO_DERIVATION does not preempt. A named source makes this BLOCKED rather than ABSENT. No outside source is opened. | yes |
| REPRO_BLOCKED — pinned source lacks match | Same claim, but Alpha is enumerable and its cited L7 says only “The parameter is tabulated elsewhere,” without a machine-matchable value for a. | §2's imported-value exception requires a match at the cited line; none exists → §3's second BLOCKED limb. Record BLOCKED without supplying a value. | yes |
| REPRO_NOT_EVALUABLE — SYMBOLIC_TIMEOUT | P:L1 quotes input n = 100000000000000000000 from Alpha without derivation. P:L2 prints own result q = 1.0 and specifies: initialize an accumulator to unity, then perform n sequential additions of zero, and return it. n is PRINTED/IMPORTED with matching citation evidence. The specified sequential implementation is still looping at the wrapper's 120.0-second deadline; the recorded point is the unfinished loop. | §1 includes the result → §2 has an explicit recipe and all its inputs → no NO_DERIVATION, BLOCKED or ABSENT condition → §9 wrapper prints SYMBOLIC_TIMEOUT and exits 124 → §3 NOT_EVALUABLE. The arithmetic outcome is not inferred from an uncompleted mandated recipe. | yes |
| REPRO_NOT_EVALUABLE — MACHINERY_UNAVAILABLE | P prints q = 4.0 and fully specifies an operation sequence for hardware device H: load a quoted, printed input register a = 2.0, execute its defined ADD instruction on a and a, then read its output register. The claiming paper explicitly requires executing H; the lane has neither H nor machinery implementing its specified execution interface. | Explicit operations distinguish this from an unspecified procedure. All numerical inputs are PRINTED, with evidence → §3's earlier terminal conditions absent → required execution machinery unavailable → print MACHINERY_UNAVAILABLE and the point “before loading H's input register.” | yes |
| REPRO_NO_DERIVATION_STATED — no recipe | P:L1: “Our result is q = 4.0.” No equation, procedure, inputs or derivation appears anywhere in P. | §1 includes an own-result numeral even without a derivation → §2 extraction finds no recipe → §3's first terminal condition. Name P:L1. No ledger record means rests_on NOT_COMPUTED. | yes |
| REPRO_NO_DERIVATION_STATED — named but unspecified procedure | P:L1: “Our result is q = 4.0, obtained using our calibration procedure.” P gives no operations defining that procedure. | §1 inclusion → §3 explicitly says a procedure named but not specified states no computational procedure that could produce the result → NO_DERIVATION, first in precedence. | yes |
| REPRO_INPUT_ABSENT | P:L1: “Our result is q = 4.0, calculated by q = a + a.” P never prints a's value and never names any source for it. Ledger: a ABSENT, no value; UNDECLARED/ORIG_SILENT with an adequate recorded search covering a and its textual variants across P, finding the equation but no value or source. | §1 inclusion → §2 lists a → no printed value and no named source → ABSENT. A recipe exists and no BLOCKED input exists → §3 INPUT_ABSENT; name a and stop without substitution. | yes |
| CENSUS_COMPLETE | Corpus consists of 89 synthetic enumerable papers P001–P089, each containing exactly the two passages in the first row. Each own-result passage is included; each quoted-input passage is excluded as ATTRIBUTED_NOT_DERIVED. Counts: 178 candidates, 89 included, 89 excluded, 89 attempts, 89 exclusion rows. Both seats agree on every disposition, input and outcome. All 89 computations give 4.0. All applicable C0–C5b controls pass; merge/compute succeed; receipts P/T and their verification succeed. An independently supplied, post-receipt 64-hex seed is recorded; C6 audits all 89 arithmetic claims and every candidate/exclusion, prints its complete matching artefact, and returns PASS. | §1 defines the denominator without silently dropping input passages → §2/§3 give 89 arithmetic outcomes → C6: N=89, R=0, k=0, with all 89 claims audited under limb (i) → §4 precedence finds no earlier class → §4 COMPLETE. The script's rests_on tally is 89 USES_IMPORTED; that does not change the reproduction tally or completeness. | yes |
| CENSUS_COMPLETE — arithmetic failures allowed | Same 89-paper corpus and successful controls/audit, but every result passage prints 5.0; each reproduction gives 4.0. Both seats and the auditor agree on REPRO_FAILED for all 89 claims. | §3 defines the arithmetic group as exactly WITHIN_STATED_PRECISION and FAILED → all included claims still carry arithmetic outcomes → no earlier §4 class → COMPLETE. Completion does not require successful reproduction. | yes |
| CENSUS_PARTIAL — non-arithmetic claim | One-paper corpus containing the absent-input claim above; both seats agree on its inclusion, ABSENT ledger and INPUT_ABSENT outcome. N=1, R=1, k=1. All earlier controls, agreements and receipts pass; the auditor confirms the missing input and complete ledgers, prints the matching artefact and returns PASS. | §2 stops without supplying a → §3 INPUT_ABSENT → §4 earlier stop conditions absent → §4 PARTIAL because one included claim is non-arithmetic. An audit can confirm a non-arithmetic outcome. Replacing this claim with the BLOCKED, NO_DERIVATION or persistently NOT_EVALUABLE witness gives the same study route after a matching audit. | yes |
| CENSUS_PARTIAL — zero denominator | P contains only “The observation date was 2020.” Both seats enumerate one candidate, exclude it as DATE, and agree on no included claims. Counts: 1 candidate, 0 included, 1 excluded, 0 attempts, 1 exclusion. Applicable controls and seal checks pass; C6 verifies the complete candidate/exclusion ledger, with N=R=k=0 and no claim re-derivations, and prints PASS. | §1 DATE exclusion → §3 denominator counts included claims only → §4 explicit zero-denominator limb → PARTIAL. The nonempty arithmetic condition cannot be satisfied vacuously. | yes |
| CENSUS_AUDIT_FAILED — audit mismatch | One-paper first-row corpus; both original seats and the sealed ledger report WITHIN_STATED_PRECISION with 4.0/4.0. Earlier controls and agreements pass. C6 audits this arithmetic claim, but its independent output reports FAILED with printed 4.0/reproduced 3.0; the artefact records MISMATCH. No origin disagreement occurs. | C6 audits every arithmetic claim → an outcome cannot be reproduced by the audit → §4 AUDIT_FAILED, ahead of COMPLETE. No tally is filed. This witness specifies an auditor failure; it does not assert that 2.0+2.0 mathematically equals 3.0. | yes |
| CENSUS_AUDIT_FAILED — missing seed | Same one-paper arithmetic corpus and successful earlier controls and receipts, but the external custodian supplies no seed after receipt T. | C6 explicitly sets C6_AUDIT_SAMPLE=NOT_RUN and files AUDIT_FAILED with the missing seed named → §4 precedence selects AUDIT_FAILED. C6 NOT_RUN does not become R3C2_NO_CLASS. | yes |
| CENSUS_AUDIT_FAILED — seal verification | Same one-paper arithmetic corpus; all earlier classes absent and C6 PASS. Receipt T records the original tally hash, but the tally subsequently verified contains printed_value 5.0 instead of 4.0, producing a different hash. | §7 independent rehash detects receipt mismatch → §4 seal-failure limb → AUDIT_FAILED, even though C6 previously passed; interpretation NOT_RUN and no tally filed. | yes |
| R3C2_NO_CLASS — repeated control failure | Intended corpus contains the first-row claim. In each of two seats, C5's live SymPy import exits nonzero with ModuleNotFoundError on both attempts; other earlier controls have passed. | C5 nonzero exit is FAIL → §4 a C0–C5b control fails in every attempting seat after two attempts → NO_CLASS, first in study precedence. Enumeration and arithmetic are unreached; later controls NOT_RUN. | yes |
| R3C2_NO_CLASS — before dispatch | Intended corpus contains the first-row claim, but the proposed seat copy lacks the pinned manifest script required by C4's dispatch inventory. The packet failure is recorded before launch. | C4 says a copy missing a required item is not dispatched → §4 explicitly assigns a pre-dispatch packet or seat-isolation failure to NO_CLASS. The two-attempt control limb is not needed for this separate limb. | yes |
| CENSUS_DENOMINATOR_DISPUTED — inclusion | P:L1: “We recover q = 4.0 as reported by Alpha.” No derivation is supplied. Seat A treats “we recover” as an own-result assertion and includes it; seat B treats it as attributed-not-derived and excludes it. Their recorded disagreement survives both reconciliation attempts. Earlier controls pass. | §1 independent inclusion decisions → §6 Limb A zero-tolerance stop after two reconciliations → §4 DENOMINATOR_DISPUTED. Both readings need not be correct: persistent reader disagreement is the declared input condition. Later controls NOT_RUN. | yes |
| CENSUS_DENOMINATOR_DISPUTED — input lists | P quotes a = 2.0 and b = 0.0 from Alpha without derivation; its own result passage prints q = 4.0 with q = a+a+b. Both seats agree on inclusion. Seat A lists input IDs {a,b}; seat B lists only {a}, regarding the zero term as unnecessary. Both keep those lists after the one C3 reconciliation; other controls up to this point pass. | C3 says input completeness against the equation is not machine-checked; merge exits 1 on different input-ID sets → one input-list reconciliation → surviving difference explicitly routes to §4 DENOMINATOR_DISPUTED. The specified reconciliation route handles this merge failure before any repeated all-seat control-failure route. | yes |
| CENSUS_OUTCOME_DISPUTED | First-row claim and identical agreed ledgers. Seat A's attempt completes in 0.01 seconds and files WITHIN_STATED_PRECISION, numbers 4.0/4.0. Seat B's child process is not scheduled to execute the addition before the 120.0-second deadline; its wrapper exits 124 and it files NOT_EVALUABLE/SYMBOLIC_TIMEOUT, printed 4.0 and no reproduced number. Both preserve their actual execution records after one reconciliation. Earlier controls and input/inclusion agreements pass. | §2.5 requires agreement claim by claim after one reconciliation; the filed outcomes still differ → §4 OUTCOME_DISPUTED, before ORIGIN_DISPUTED, audit and PARTIAL. Report both records and reached steps; the missing reproduced number for B is explicitly “not produced,” not an invented numeric pair. | yes |
| CENSUS_ORIGIN_DISPUTED | One-paper corpus: L1 “We choose a = 2.0 from Reference Alpha, quoting its value without derivation.” L2 is the first-row q=4.0 recipe. Both seats agree on dispositions, input a, its PRINTED status and arithmetic outcome 4.0/4.0. A records IMPORTED/ORIG_CITATION; B records CHOSEN/ORIG_CHOICE_STATED, each quoting L1. Their machine-valid records merge with the alternate origin retained; all earlier controls pass. | C3 says misapplication of reason-code precedence is not machine-caught; its citation-first rule makes A's classification the prescribed one, but does not make a reader split impossible. Origin disagreement is carried, not reconciled. One affected claim out of one = 100%, strictly above 10% → §4 ORIGIN_DISPUTED, ahead of audit and COMPLETE. | yes |
| CENSUS_CONTROL_SPLIT | Intended corpus contains the first-row claim. Seat A passes all five C5 harness commands with matching live pins on both attempts; seat B's SymPy import exits nonzero on both attempts. No control fails in every attempting seat, and there is no pre-dispatch failure. | C5 fails in B and passes in A after two attempts → NO_CLASS predicate false → §4 CONTROL_SPLIT, second in precedence. Report both outputs and stop; do not adopt A's pass. Later controls NOT_RUN. | yes |

These are concrete hypothetical inputs and execution records for an exhibition, not observations from the pinned corpus. Only the requested pre-registration was read; its full text, including history, was read. The synthetic source labels above do not refer to files opened or created. Successful-control and receipt states in the study witnesses are stipulated parts of those hypothetical executions, not claims that this turn ran the census harness or obtained custody receipts. This is one seat's exhibition; the document's separate independent verification remains separate.

The operative inventory is six per-claim outcomes and eight study-level classes. Rows also exhibit the alternative terminal branches that matter to their routing. REPRO_AFTER_CHOICE is explicitly retired; REPRO_EXACT is the historical name of REPRO_WITHIN_STATED_PRECISION. Neither is an additional current verdict. The script-computed rests_on values are a separate field and tally.

The §3 order applied here is NO_DERIVATION_STATED → BLOCKED → INPUT_ABSENT → NOT_EVALUABLE → arithmetic group. For example, a q=4.0 passage with no specified operations stays NO_DERIVATION even if it names an unavailable source. A specified q=a+b recipe with a traced to a non-enumerable source and b wholly absent files BLOCKED. A specified recipe with b absent and unavailable machinery files INPUT_ABSENT. Earlier conditions must be absent to exhibit a later outcome.

The §4 filing order is R3C2_NO_CLASS → CENSUS_CONTROL_SPLIT → CENSUS_DENOMINATOR_DISPUTED → CENSUS_OUTCOME_DISPUTED → CENSUS_ORIGIN_DISPUTED → CENSUS_AUDIT_FAILED → CENSUS_PARTIAL → CENSUS_COMPLETE. Each study witness makes every earlier predicate false or uses the expressly prescribed stop at its stage. Later controls are NOT_RUN after a stop. A persistent dispute is an input consisting of readers' recorded disagreement; the text does not require both conflicting classifications to be substantively correct before filing the dispute.

**Direct answer to the suspicion: CENSUS_COMPLETE is REACHABLE.** The 89-paper witness exhibits it with a positive denominator, no non-arithmetic claims, successful audit and verified receipts. It remains reachable even with 89 REPRO_FAILED outcomes because those belong to the arithmetic group. Corpus size alone imposes no clause requiring a missing input, blocked source, unspecified derivation or timeout.

A single included BLOCKED, INPUT_ABSENT, NO_DERIVATION_STATED or NOT_EVALUABLE claim does make CENSUS_COMPLETE unavailable for that particular census. If earlier stop classes are absent and the audit and seal succeed, it files CENSUS_PARTIAL. If an earlier class applies, that class wins instead: for example, a missing audit seed gives CENSUS_AUDIT_FAILED despite a blocked claim. Thus “one such claim forces PARTIAL” needs the precedence qualification; “therefore COMPLETE is unreachable” does not follow.

For an explicit many-paper partial variant, replace P089's result passage in the completion witness with the absent-input witness and remove its quoted input passage. There are 88 arithmetic claims and one INPUT_ABSENT claim: N=89, R=1, k=min(max(1,ceil(17.8)),1)=1. A matching audit covers all 88 arithmetic claims plus that remaining claim and all candidate/exclusion dispositions. With earlier predicates false, §4 files PARTIAL. No permitted repeat supplies the absent value.

**Dependence on §1:** the operative inclusion definition is a passage that “prints a numeral the paper asserts as a result of its own”. It does not require a derivation or available inputs; otherwise the NO_DERIVATION and missing-input witnesses would be excluded before §3. The completion witness explicitly excludes its quoted input passages under the enumerated attributed-not-derived kind. It does not silently omit a standalone chosen-input numeral or assume an unlisted exclusion kind. Inclusion disagreement instead follows §1's two-reconciliation route.

The marked SEAT-REDACT spans in §1 separate the provenance question from the seat-visible reproduction question. They are redaction markers, not a held admissibility clause. Under the settled option (c), §2 and §3 admit arithmetic by PRINTED/STANDARD status, independently of origin. The opening historical “without choosing anything” framing does not reinstate the retired derivation-only restriction. The imported roots in the completion witness are therefore permitted; changing that reading would change the exhibited route.

This exhibition establishes existential reachability under the stated rules, not whether the actual pinned corpus satisfies the completion predicate. The latter cannot be determined without the corpus census, which was outside this request.

UNREACHABLE verdicts: none among the current §3 outcomes and §4 classes. Consequently there is no clause blocking every path to a current verdict to quote.

R3C2_C0_EXHIBITION_COMPLETE
