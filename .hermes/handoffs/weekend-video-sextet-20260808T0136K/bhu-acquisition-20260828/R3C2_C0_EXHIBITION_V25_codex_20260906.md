ACCESS_SHA=ab6352d35a0e02fbc92173971ada554230ad5fb0578440a9728fc0f1494942e7
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| REPRO_WITHIN_STATED_PRECISION | Synthetic enumerable paper P, line 1: “For this calculation we choose a = 2 and b = 3.” Line 2: “Our result is q = a + b = 5.” No uncertainty. Inputs a and b are PRINTED, origin CHOSEN, ORIG_CHOICE_STATED quoting line 1, with empty derived_from lists. Printed 5; reproduced 5. | §1 includes the asserted result q; §3 excludes the author-set input numerals as AUTHOR_SPECIFIED_INPUT but retains them in the exclusion ledger. §2 steps 1–4 and §3 consume their PRINTED records regardless of origin. No earlier terminal condition holds; 5 rounds to 5. File the precision outcome and report both numbers; script computes rests_on = USES_CHOSEN separately. | yes |
| REPRO_WITHIN_STATED_PRECISION — rounding boundary | P instead chooses a = 1.25 and states “Our result q = a is 1.3,” without uncertainty. Printed 1.3; reproduced 1.25. A negative variant chooses a = -1.25 and prints q = -1.3. | Same inclusion and input route; §3 rounds half away from zero at the printed tenths precision: 1.25 → 1.3 and -1.25 → -1.3. | yes |
| REPRO_WITHIN_STATED_PRECISION — stated uncertainty | P chooses a = 5.2 and prints its result q = a as 5.0 ± 0.2. Asymmetric variants print 5.0 with upper uncertainty 0.2 and lower uncertainty 0.1, using a = 5.2 or a = 4.9 respectively. | §3 uses the stated uncertainty once, without rounding: differences 0.2, 0.2 and 0.1 equal their applicable half-widths. All pass. | yes |
| REPRO_FAILED | P line 1 is unchanged; line 2 instead prints “Our result is q = a + b = 6.” No uncertainty. Printed 6; reproduced 5. | §1 includes q; §2 has sufficient PRINTED inputs. No earlier §3 terminal condition holds. At units precision, 5 does not round to 6. File REPRO_FAILED, “unreproduced from the stated inputs,” and both numbers. It remains an arithmetic-group outcome. | yes |
| REPRO_BLOCKED — source outside enumerable corpus | P line 1: “Use a from External Table E, line 7.” P line 2: “Our result is q = a + a = 4.” No value for a appears in P; E is not an enumerable pinned text. | §1 includes q; the equation is stated. §2 named-source rule and §3 REPRO_BLOCKED apply. C3 records a as BLOCKED, IMPORTED, no value, with ORIG_CITATION quoting P line 1. No substitution or arithmetic is permitted. Whether E exists elsewhere is irrelevant. | yes |
| REPRO_BLOCKED — cited line has no matching value | Same P, but E is enumerable and its cited line 7 says only “The calibration is described below,” with no value for a. | §2 requires a machine-match at the cited source line; §3's second BLOCKED limb applies. The ledger cites P's naming sentence under C3 and carries no value. | yes |
| REPRO_INPUT_ABSENT | P line 1 chooses a = 2. Line 2: “Our result is q = a + b = 5.” P never prints b or names any source for it. | §1 includes q; §2 lists a and b. There is a procedure and no BLOCKED input. Record b as ABSENT with no value; document the provenance search under C3. §2 forbids supplying b; §3 files INPUT_ABSENT before any arithmetic. | yes |
| REPRO_NOT_EVALUABLE — SYMBOLIC_TIMEOUT | P chooses n = 100000000000000000000 and prints its own result S = 0 from this explicit procedure: initialize S = 0; for each integer j from 1 through n, add sin(j²) to S; report S. The prescribed computation is launched under §9's wrapper; the concrete execution trace is still in the loop at the 120.0-second deadline, with no completed result. | All numerical inputs are PRINTED; the operations are specified; no earlier §3 terminal condition holds. §9 terminates the unfinished operation, prints SYMBOLIC_TIMEOUT and exits 124. §3 files NOT_EVALUABLE with the loop position reached. This is a stipulated execution witness, not a claim that this computation was run here. | yes |
| REPRO_NOT_EVALUABLE — MACHINERY_UNAVAILABLE | P chooses n = 100000000000000000000 and supplies complete GPU source implementing the preceding loop, prescribing execution of that implementation and printing S = 0 as its result. In this witness the lane has the required Python/SymPy harness but has no compatible GPU or GPU runtime; the point reached is launch preparation. | Inputs and computational operations are stated, so NO_DERIVATION_STATED, BLOCKED and INPUT_ABSENT do not apply. C5 can pass while this additional machinery is absent. §3 files NOT_EVALUABLE with MACHINERY_UNAVAILABLE and the point reached. | yes |
| REPRO_NO_DERIVATION_STATED — no recipe | P line 1: “Our result for q is 5.” No equation or computational procedure appears anywhere in P. There are no identifiable recipe inputs and no ledger records. | §1 includes an asserted own result even though it lacks a derivation. §2 extraction finds none; §3's first terminal class files NO_DERIVATION_STATED and names P line 1. rests_on = NOT_COMPUTED. | yes |
| REPRO_NO_DERIVATION_STATED — procedure merely named | P line 1: “Our simulation gives q = 5.” No simulation operations, equation or algorithm are specified. | §1 includes q; §3 expressly treats a procedure named but not specified as no computational procedure that could produce the number. This precedes machinery or input diagnoses. | yes |
| CENSUS_COMPLETE | A synthetic corpus has 100 enumerable papers P001–P100, each containing exactly P's two original lines above. Each has one own-result claim q = 5 and two author-set input numerals. Both seats agree on all dispositions, inputs, origins and outcomes. Counts: 300 candidates, 100 included, 200 excluded, 100 attempts, 200 exclusion rows. All 100 outcomes are REPRO_WITHIN_STATED_PRECISION. The successful filing conditions described below hold. | §1 and V25's §3 exclusion rule give N = 100. §2 and §3 produce 100 arithmetic-group outcomes. C6 audits every claim: R = 0, k = 0, all 100 outcomes and ledgers MATCH, all candidates and exclusions complete, printed audit artefact present. C6_AUDIT_SAMPLE = PASS; receipts verify. No earlier §4 class applies, so COMPLETE files. rests_on tally: USES_CHOSEN = 100. | yes |
| CENSUS_COMPLETE — arithmetic failures allowed | Same 100-paper corpus, except P100 prints q = 6 from a = 2, b = 3, q = a + b. Both seats and the auditor obtain 5 and agree on REPRO_FAILED for P100; the other 99 claims reproduce. | §3 defines the arithmetic group as exactly the precision outcome and REPRO_FAILED. C6 matches all 100 outcomes, including the failure. With the same successful filing conditions and N = 100, §4 still files COMPLETE. | yes |
| CENSUS_PARTIAL — each non-arithmetic alternative | Replace P100's result passage in the 100-paper witness, separately, with the BLOCKED, INPUT_ABSENT, NO_DERIVATION_STATED or NOT_EVALUABLE witness above. Both seats agree; any permitted repeat leaves the outcome unchanged. The other 99 claims reproduce. C6 passes and receipts verify. | Each replacement is an included own-result claim with a non-arithmetic outcome. C6 audits the 99 arithmetic claims and the remaining claim: N = 100, R = 1, k = min(20, 1) = 1. After excluding earlier stop classes, §4 files PARTIAL, INCONCLUSIVE; COMPLETE's universal arithmetic condition is false. | yes |
| CENSUS_PARTIAL — empty denominator | The sole synthetic enumerable paper contains only “For this calculation we choose a = 2.” Both seats exclude its sole numeral as AUTHOR_SPECIFIED_INPUT. Counts: 1 candidate, 0 included, 1 excluded, 0 attempts, 1 exclusion row. C6's completeness audit matches the exclusion; both claim lists and the sample are empty; its printed artefact passes. Receipts verify. | §1 plus §3 gives N = 0. C6 has R = 0 and k = 0. With no earlier stop, §4 explicitly routes the zero denominator to PARTIAL and excludes vacuous COMPLETE. | yes |
| CENSUS_AUDIT_FAILED — outcome mismatch | One-paper P prints q = 6 while a = 2, b = 3 and q = a + b. Both original seats mistakenly file the precision outcome with number pair (6, 6), and keep matching, schema-valid input ledgers. No outcome split exists. The auditor computes 5, files REPRO_FAILED with pair (6, 5), and records MISMATCH. | C1/C3 do not certify arithmetic correctness; their checks can pass. C6 audits every arithmetic claim and cannot reproduce the sealed outcome. §4 files AUDIT_FAILED after earlier stop classes are excluded; no tally is filed. | yes |
| CENSUS_AUDIT_FAILED — ledger or completeness mismatch | Original P's q outcome is correctly sealed, but both seats omit the excluded candidate a = 2 and its exclusion row, adjusting declared counts consistently. The input ledger still correctly supplies a = 2. The auditor finds the omitted candidate in the source. | C1's internal count/bijection checks can pass; C6 audits full-source completeness and finds an omission. C6 cannot pass; §4 files AUDIT_FAILED. | yes |
| CENSUS_AUDIT_FAILED — audit not run | Original P is agreed and sealed correctly, with valid receipt T, but no external seed is supplied or recorded. | C6 explicitly assigns C6_AUDIT_SAMPLE = NOT_RUN and CENSUS_AUDIT_FAILED for the missing seed. With no earlier §4 stop, this is the filed class; no tally is filed. | yes |
| CENSUS_AUDIT_FAILED — seal verification failure | Original P is agreed and audited successfully. Both receipts exist, but the protocol hash independently re-computed during §7 verification differs from receipt P. | §7's mismatch rule and §4's seal-failure limb file AUDIT_FAILED, with interpretation NOT_RUN. This takes precedence over the otherwise satisfied COMPLETE conditions and is expressly excluded from R3C2_NO_CLASS. | yes |
| R3C2_NO_CLASS — repeated control failure | Intended source is original P, with a = 2, b = 3 and q = 5. Both dispatched seats run C5 twice; each time its mandatory SymPy import exits nonzero because SymPy is absent. No source arithmetic is reached. | C5_HARNESS_PINNED = FAIL in every attempting seat after two attempts. §4's first class files R3C2_NO_CLASS. Later limbs and controls are NOT_RUN, so the unrun audit does not override this stop. | yes |
| R3C2_NO_CLASS — before dispatch | Intended source is original P, but the packet's actual digest differs from its pin during the required pre-dispatch check. | C4 forbids dispatch of the mismatched packet. §4 expressly assigns a packet failure before dispatch to R3C2_NO_CLASS; later controls are NOT_RUN. No claim outcome is invented. | yes |
| CENSUS_DENOMINATOR_DISPUTED — inclusion | Synthetic P line 1: “We adopt q = 5 as our estimate.” No recipe or other inputs are given. Seat A records an own-result claim; seat B records an AUTHOR_SPECIFIED_INPUT exclusion. They retain those dispositions after two reconciliation attempts. Earlier controls pass. | §1 leaves the own-result boundary to independent classification. §1, §6 limb A and §4 route the surviving inclusion split to DENOMINATOR_DISPUTED; report both ledgers and the disputed passage. Later outcomes and audit are unreached. | yes |
| CENSUS_DENOMINATOR_DISPUTED — input lists | P chooses a = 2 and b = 3, then prints its result q = a + b = 5. Both seats include q. A lists input ids a and b; B lists only a. Their lists remain different after the single C3 reconciliation; individual records otherwise validate. | C3 says completeness against the equation is not machine-checked. merge detects unequal input-id sets and exits 1; the specific C3 reconciliation rule and §4 route the surviving difference to DENOMINATOR_DISPUTED. A merge failure initiating this route is not a substitute for the required two-attempt, every-seat control failure defining R3C2_NO_CLASS. | yes |
| CENSUS_OUTCOME_DISPUTED | P chooses a = 1.25 and prints its result q = a as 1.3, with no uncertainty. Both seats agree on inclusion, input list and CHOSEN origin. Both report number pair (1.3, 1.25). A files REPRO_WITHIN_STATED_PRECISION; B incorrectly files REPRO_FAILED and retains that outcome after the required reconciliation. Both reached the rounding step. Earlier controls pass. | §2 step 5 and §4 test the seats' filed outcomes after reconciliation. Their disagreement triggers OUTCOME_DISPUTED before origin, audit or completeness. The class does not require both conflicting outcomes to be correct; B's error is precisely a possible unresolved seat-output state. Report both outcomes, pairs and steps. | yes |
| CENSUS_ORIGIN_DISPUTED | P line 1: “For use as an input, we fit a = 2.” Line 2: “For this calculation we choose a = 2.” Line 3: “Our result is q = a + a = 4.” Both seats agree the input numerals are excluded, q is included, the input list is {a}, status is PRINTED and q reproduces. A cites line 1 with FITTED/ORIG_FIT_STATED; B cites line 2 with CHOSEN/ORIG_CHOICE_STATED. Both quotations machine-match; classifications disagree. | C3 classifies origins independently, retaining disagreements without reconciliation. Both evidence sentences support their respective codes; the disagreement concerns which provenance governs the input. One disputed input affects 1 of 1 included claims: 100%, strictly greater than 10%. With earlier stops absent, §4 files ORIGIN_DISPUTED before audit or COMPLETE. | yes |
| CENSUS_CONTROL_SPLIT | Intended claim is original P. Seat A's two C5 attempts produce all required successful live outputs and matching environment pins. Seat B's two C5 attempts report an environment-manifest mismatch and FAIL. No control fails in every attempting seat; no pre-dispatch failure occurs. | C5 establishes the differing control results. §4's R3C2_NO_CLASS predicate is false; its next class, CONTROL_SPLIT, applies. Report both outputs and stop without adopting A's result. Later controls are NOT_RUN. | yes |

These are synthetic C0 inputs and execution/seat-output states, not assertions about the contents or results of the actual pinned corpus. Hypothetical paper and source labels above are not files I opened or created. I read the specified 1,175-line preregistration in full and no other source file in this directory. This report exhibits all six current per-claim outcomes and all eight current study-level classes, including their alternative terminal branches. It is this seat's exhibition; it does not assert that the separate independent verification required by C0 has occurred.

The successful filing conditions used above are concrete procedural states: required C0–C5b controls pass with their printed artefacts; both seats agree on candidates, input lists, outcomes and origins; the source values and quotations match their ledger records; the lane's merge and compute runs exit 0; receipt P precedes limb A; receipt T names the sealed files; an external custodian supplies an independently generated 64-lowercase-hexadecimal seed after T; the required C6 artefact is printed with complete candidate/exclusion dispositions and only MATCH entries; and receipt verification succeeds. For example, the supplied seed may be 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef. The complete witness has no remaining claims to sample; the partial witness has exactly one, so either sample is fully determined regardless of that seed.

The §4 filing precedence used in every study row is: R3C2_NO_CLASS → CENSUS_CONTROL_SPLIT → CENSUS_DENOMINATOR_DISPUTED → CENSUS_OUTCOME_DISPUTED → CENSUS_ORIGIN_DISPUTED → CENSUS_AUDIT_FAILED → CENSUS_PARTIAL → CENSUS_COMPLETE. Each row excludes earlier predicates unless explicitly describing their priority. An audit marked NOT_RUN because an earlier stop prevented reaching it does not replace that earlier class.

The suspicion has a direct answer: **CENSUS_COMPLETE is REACHABLE.** The 100-paper witness supplies a nonempty many-paper input that files it; a mixture of arithmetic successes and arithmetic failures can also file it. The governing clause is:

> **every included claim carries exactly one outcome from the arithmetic group of §3, with `C6_AUDIT_SAMPLE=PASS`. A
>    denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing.**

A single included blocked, absent-input, no-derivation-stated or not-evaluable claim makes COMPLETE unavailable for that particular corpus. If earlier stop classes are absent and the audit and seal pass, it files PARTIAL. If an earlier stop applies, that earlier class files instead. Thus such a claim does not unconditionally force the filed class PARTIAL, and its possible presence does not make COMPLETE globally unreachable. Whether the actual pinned corpus contains such a claim is not established by this exhibition and cannot be inferred from corpus size alone.

The dependence on §1 is explicit. Its operational inclusion condition is “prints a numeral the paper asserts as a result of its own”; it does not require that a derivation already exist. Consequently “Our result for q is 5” is included and reaches NO_DERIVATION_STATED. Conversely, V25's AUTHOR_SPECIFIED_INPUT definition requires a numeral the paper “sets rather than derives and does not assert as a result of its own.” Consequently “For this calculation we choose a = 2” can be excluded as a candidate claim while its value remains a PRINTED input consumed when reproducing q. The COMPLETE witness depends on applying those two clauses together, including the stated input exclusions in the full audited enumeration. If seats disagree on that boundary after reconciliation, the route is DENOMINATOR_DISPUTED.

The bold §1 question and the SEAT-REDACT spans do not reinstate a held derivation-only definition. The current §2–§3 rule consumes PRINTED/STANDARD values by status, independently of provenance. The marked provenance question contributes the separate rests_on tally. REPRO_AFTER_CHOICE is expressly retired, REPRO_EXACT is a historical name, and exclusions and rests_on values are not additional current per-claim outcomes.

UNREACHABLE verdicts: none. No blocking clause excludes every path to any of the fourteen current verdicts.

R3C2_C0_EXHIBITION_COMPLETE
