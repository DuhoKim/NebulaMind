ACCESS_SHA=57de1252d5f9d4592181517e70d0bca1c715830ccbb595f4bc9622a18e4779fd
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_WITHIN_STATED_PRECISION` | P:1, “We choose a = 2”; P:2, “Our result is y = a + a = 4.” The sole required input is a, `PRINTED`, value `2`, `CHOSEN`, with the P:1 quotation and no parents. Printed/reproduced: `4`/`4`. | §1 includes the result and excludes the author-set input numeral from judgement; §2 consumes the input record nevertheless; §3 arithmetic group and printed-precision rule yield this outcome; C3 computes `USES_CHOSEN`. | yes |
| `REPRO_FAILED` | Same input and recipe, but P:2 asserts y = 5. Printed/reproduced: `5`/`4`. | §2 completes arithmetic; no earlier §3 terminal condition applies; 4 does not round to 5. File “unreproduced from the stated inputs” and report both numbers. | yes |
| `REPRO_BLOCKED`: source not enumerable | P:1, “For a use the value in Source X”; P:2, “Our result is y = a + a = 4.” P prints no a; X is outside the enumerable manifest, including the variant where X is listed RAW. | §2 named-source rule → C3 record `BLOCKED`, empty value, `IMPORTED`, `ORIG_CITATION` at P:1 → §3 `REPRO_BLOCKED`. Availability elsewhere cannot change the route. | yes |
| `REPRO_BLOCKED`: enumerable source without value | Same claim; X is verified and enumerable, but its cited line says “a is tabulated separately” and no usable numeric a token is present. | §2 requires a machine-matching value; §3's second blocked branch applies. Name a and X; never consume a. | yes |
| `REPRO_INPUT_ABSENT` | P:1, “Our result is y = a + a = 4”; no value for a, no source naming it, and no provenance statement anywhere in the fixture. Record a as `ABSENT`, empty value, `UNDECLARED`/`ORIG_SILENT`, with searches for a and textual variants over all fixture texts and their actual matches. | §2 prohibits filling the missing value; C3 permits this empty-value record and records the search; §3 has a recipe, no blocked input, and an absent input → this outcome. | yes |
| `REPRO_NOT_EVALUABLE`: timeout | P prints integer n = 10^100 as an input and specifies: start s = 0; execute exactly n sequential iterations, each replacing s by s + 1; report its claimed result s = 10^100. The wrapped literal implementation is still iterating at 120.0 seconds. | All required inputs are `PRINTED`; §9 wrapper reaches its deadline, emits `SYMBOLIC_TIMEOUT`, exit 124, with the iteration reached; §3 files `REPRO_NOT_EVALUABLE`. One repeat reaching the same deadline preserves it. | yes |
| `REPRO_NOT_EVALUABLE`: machinery | P supplies a complete gate-by-gate quantum-circuit procedure, 200-qubit initial state, shot count, and estimator, and asserts estimated probability 0.50. The lane has neither the required device nor a usable simulator. | A specified procedure and supplied inputs avoid the earlier §3 outcomes; the required machinery is unavailable → `MACHINERY_UNAVAILABLE`, point reached “circuit specified; execution unavailable” → this outcome. | yes |
| `REPRO_NO_DERIVATION_STATED`: no recipe | P:1, “Our resulting dimensionless value is 4.” There is no equation or computational procedure anywhere in P; the claim has no input records. | §1 includes the asserted result; §3's first terminal condition applies before any input/arithmetic condition. | yes |
| `REPRO_NO_DERIVATION_STATED`: procedure only named | P:1, “Our simulation gives the dimensionless result 4.” No simulation operations, equations, or algorithm are specified. | §3 explicitly treats a named but unspecified procedure as no computational procedure that could produce the result. | yes |
| `CENSUS_COMPLETE` | Corpus W89 defined below: 89 enumerable papers, each with one result `4` from printed a = 2 and y = a + a. N = 89; both seats agree; all 89 outcomes are arithmetic; the independent audit reproduces every one and its ledger; both receipts verify. | C1/C1B/C2/C3/C4/C5/C5b/C5C pass → §§2–3 arithmetic outcomes → C6 selects all 89 arithmetic claims, R = 0, k = 0, and passes → §4.1. No earlier §4 stop applies. | yes |
| `CENSUS_COMPLETE`: failures allowed | W89, but every paper prints 5 for a + a with a = 2. All 89 seat and audit records agree on `REPRO_FAILED`, printed/reproduced `5`/`4`; C6 passes. | §3 defines the arithmetic group as exactly the success and failure outcomes; §4.1 requires arithmetic classification, not universal reproduction success. | yes |
| `CENSUS_PARTIAL`: one non-arithmetic claim | W89 with P89 replaced, separately, by each blocked, absent-input, no-derivation, or persistent not-evaluable fixture above; the other 88 claims remain arithmetic. The auditor agrees on all enumerations and reconstructed selected claims. | N = 89, R = 1, k = min(18,1) = 1; C6 audits that remaining claim as well as all arithmetic claims and passes. §4.2 applies to each replacement and precedes `CENSUS_COMPLETE`. | yes |
| `CENSUS_PARTIAL`: empty enumeration | Every enumerable text has no numeral candidate at all. Both seats and auditor enumerate zero candidates, zero included and zero excluded; ledgers are empty; a recorded external seed, matching seals, selection and printed audit artefact satisfy C6. | C6's zero-denominator failure requires a passage on at least one side; here its union is empty and there are no disputes, omissions or mismatches. §4.1–2 explicitly assign the empty enumeration to `CENSUS_PARTIAL`. | yes |
| `CENSUS_AUDIT_FAILED`: arithmetic mismatch | W89 seats mistakenly seal `REPRO_WITHIN_STATED_PRECISION`, `5`/`5`, for one a = 2, y = a + a claim printing 5. Auditor independently computes 4 and files `REPRO_FAILED`, `5`/`4`. All earlier controls and seat agreement pass. | C6 selects every arithmetic claim and compares outcomes and both numbers → `MISMATCH`, no audit PASS → §4.3. No tally is filed. | yes |
| `R3C2_NO_CLASS`: repeated control failure | W89 intended claims; in both seats the live C5 SymPy import exits nonzero on each of two attempts. | C5 requires every harness command to exit 0; control fails in every attempting seat after two attempts → §4.4. Later limbs and their controls are `NOT_RUN`. | yes |
| `R3C2_NO_CLASS`: before dispatch | W89 intended claims, but packet construction uses bytes different from C4's pinned builder, yielding `C4_PACKET_REDACTED=FAIL`; no seat is dispatched. Equivalently, the required isolation probes fail before dispatch. | C4 packet/isolation condition → §4.4's explicit pre-dispatch branch. | yes |
| `CENSUS_DENOMINATOR_DISPUTED`: inclusion | P:1, “We adopt 4 as our estimate.” Seat A includes 4 as an asserted result; B excludes it as `AUTHOR_SPECIFIED_INPUT`. The readers retain their dispositions after two reconciliations; controls pass. | §1 and §6 impose zero tolerance on surviving inclusion disagreement → §4.5; disputed passage and both ledgers reported. | yes |
| `CENSUS_DENOMINATOR_DISPUTED`: input lists | Both include P's printed result y = a + b = 5, with a = 2 and b = 3 printed. A lists a and b; B lists only a and retains that list after the one reconciliation. Each submitted record validates; completeness against the recipe is not machine-checked. | C3 `merge` detects different input-id sets, exits 1 → one input-list reconciliation → surviving difference → §4.5. | yes |
| `CENSUS_OUTCOME_DISPUTED` | Both agree on a = 2 and claim y = a + a = 5. A files `REPRO_FAILED`, `5`/`4`; B files `REPRO_WITHIN_STATED_PRECISION`, `5`/`5`, and retains its computation after one reconciliation. Both reached addition; origin and input lists agree. | §2.5 and §4.6 explicitly route surviving outcome disagreement to this class; both pairs and reached steps are reported. C1 checks fields, not the truth of B's arithmetic. | yes |
| `CENSUS_ORIGIN_DISPUTED` | Ten agreed arithmetic claims. For two independent a = 2 inputs, P says on separate lines “We choose a = 2” and “Our measurement of a gives 2 by direct counting.” A cites the choice sentence as `CHOSEN`; B cites the measurement sentence as `MEASURED`. Each input affects just its own claim. | C3 accepts the quoted evidence records and carries origin alternatives without reconciliation; 2/10 = 20% of included claims affected → §4.7, before C6. | yes |
| `CENSUS_CONTROL_SPLIT` | W89 intended claims; A's C5 harness fails twice because SymPy cannot import, while B's complete live harness passes its attempts with matching dispatch pins. No control fails in every seat. | §4.8's surviving fail/pass split → report both outputs and stop; never adopt B's tally. | yes |
| `CENSUS_AUDIT_FAILED`: sealed-included absent from auditor | W89 sealed N = 89. Auditor's otherwise valid enumeration omits P01's result passage entirely; it can still reconstruct the subsequently assigned claim. | C6 union row for that passage is `OMISSION`, even if reconstruction matches → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: auditor-listed absent from sealed | In W89, P01:3 also prints “Equation 7.” Auditor lists numeral 7 as excluded `EQUATION_NUMBER`; both census seats omitted that passage from both ledgers. Each census's internal counts nevertheless agrees with its own rows. | C6 union includes the auditor-only excluded passage → `OMISSION` → §4.3. The same rule covers an auditor-only included result. | yes |
| `CENSUS_AUDIT_FAILED`: inclusion disputes above 10% | Sealed N = 10 arithmetic results, each as in the success fixture. Two additional passages “We adopt 4 as our estimate” are sealed exclusions but auditor inclusions; both sides list them. All reconstructed selected claims match. | C6 records two `AUDIT_INCLUSION_DISPUTED` rows; 2/10 = 20% > 10% → audit FAIL → §4.3. This is auditor inclusion disagreement, not the seats' origin-dispute stop. | yes |
| `CENSUS_AUDIT_FAILED`: sealed exclusions absent from auditor | Same N = 10 arithmetic results and two excluded passages; auditor omits the two exclusions entirely. | C6 explicitly classifies sealed-EXCLUDED/absent-auditor as disputes, not omissions; 2/10 > 10% → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: seedless selection | W89 tally receipted as T and auditor enumeration sealed, but custodian supplies no external seed. | C6 seed clause: audit does not run, `C6_AUDIT_SAMPLE=NOT_RUN`, missing seed named → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: zero denominator with passage | Enumerable P:1 says “We choose a = 2” and asserts no result. Both sides correctly exclude numeral 2, so N = 0 but the passage union is nonempty. | C6: a sealed denominator of zero with any passage on either side fails → §4.3 precedes §4.2's zero-denominator partial condition. | yes |
| `CENSUS_AUDIT_FAILED`: incomplete reconstruction | W89; auditor supplies P01's a record but omits its `origin_evidence` field and never replaces it with a complete reconstruction. | C6 `audit seal-rederivation` refuses the incomplete schema; audit never reaches PASS → §4.3. If an incomplete record reaches comparison, missing fields are also `MISMATCH`. | yes |
| `CENSUS_AUDIT_FAILED`: missing dependency | Fixture D below; auditor reconstructs selected q01's x record but omits its parent record P#a owned by unselected q02. | C6 requires the auditor's own complete closure; §3 and C6 forbid borrowing the sealed parent → missing dependency, `MISMATCH` (or seal refusal), no PASS → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: fabricated off-sample evidence | D; auditor keeps every field of P#a except replacing its evidence quotation “We choose a = 2” by “Our measurement gives a = 2,” which occurs nowhere in P. q02 is unselected, q01 depends on P#a. | §3 validates and compares every closure record, including unselected claims' records; fabricated quotation is unsupported evidence → `MISMATCH` on q01, or earlier reconstruction rejection → §4.3. Equal numeric values and roots do not waive the check. | yes |
| `CENSUS_AUDIT_FAILED`: duplicate input id | D; auditor includes P#a under q02 and a second record with the same input id under q03, leaving the otherwise correct q01 dependency intact. | §3 requires uniqueness across the entire reconstruction before sealing/comparing; duplicate cannot shadow either record. Seal rejection or comparison `MISMATCH` for dependent q01 → no audit PASS → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: wrong sealed claim | D; auditor moves the unique P#a record from enclosing q02 to q03 and changes its explicit `claim_id` to q03, retaining all evidence and value fields. | Internal key agreement alone is insufficient: §3 comparison binds the enclosing claim to the sealed record's claim, which is q02. Identity conflict → `MISMATCH` on q01 → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: explicit identity conflict | D; P#a stays enclosed under q02, but its explicit `claim_id` says q03 (or its explicit `input_id` says P#a_other). | §3 requires explicit identity fields to agree with enclosing keys before sealing/comparing → rejection or `MISMATCH` → §4.3. | yes |
| Complete declared alternative; `C6_AUDIT_SAMPLE=PASS`; `CENSUS_COMPLETE` | Fixture A below, N = 20 arithmetic claims. Auditor reconstructs x as the entire declared alternative: `CHOSEN`, Echoice, parents `[]`; every other record and outcome matches. | C3 carries the original dispute; only 1/20 = 5% of claims is affected, so §4.7 does not stop. §3 matches the COMPLETE alternative before roots; C6 compares matching branch roots and passes → §4.1. No `CENSUS_AUDIT_FAILED`. | yes |
| `CENSUS_AUDIT_FAILED`: hybrid branch with equal roots | A; auditor reconstructs x with alternative `CHOSEN` and Echoice, but retains primary parents `[P#a]`. Primary and alternative roots are both `{CHOSEN}`, as are this hybrid's roots. | §3 requires joint equality with one complete branch. Hybrid matches neither primary `(DERIVED, Ederive, [P#a])` nor alternative `(CHOSEN, Echoice, [])`; equal roots do not rescue it → `MISMATCH` → C6 FAIL → §4.3. | yes |
| Complete parent-only alternative; audit PASS | A variant: x has primary `DERIVED`, Ederive, parents `[P#a]`, and alternative parents `[P#b]`; a = b = 2 are quoted chosen inputs, and Ederive is the same quoted equation line supporting either dependency reading. Auditor uses `[P#b]` with unchanged origin and evidence; all closure records match. | §3 explicitly accepts an unchanged-origin parent alternative only with unchanged origin and evidence; construct the matched graph before roots. With only 1/20 claims affected and all arithmetic audited, C6 passes → §4.1. | yes |
| `CENSUS_AUDIT_FAILED`: selection/seal/artefact failures | W89, otherwise correct audit: separately, omit q01 from the handed-out arithmetic selection; corrupt a stage-1 or reconstruction seal digest; or never write/print `C6_AUDIT.json`. | C6 requires recomputed selection agreement, both seals matching, and an existing printed artefact. Each independent change prevents PASS → §4.3. | yes |
| `CENSUS_AUDIT_FAILED`: seal receipt verification | W89 with C6 PASS, but independent rehash finds the tally bytes differ from receipt T. Alternatively, the required receipt is missing. | §7's receipt verification fails → §4.3 explicitly includes this case; interpretation `NOT_RUN`, no tally filed. | yes |
| `C1B_BATCH_COVERAGE=FAIL` → `R3C2_NO_CLASS` | W89 ownership data assigns P01 to two batches; both seats retain that defect through two tries. Alternative individual faults: P01's bytes do not verify, or one batch report lacks `ACCESS_SHA`. | C1B requires exactly one owner, matching source bytes and every batch's access proof; each fault stops the seat tally. Repeated failure in every attempting seat → C1B's §4 contract → §4.4. | yes |
| `C1B_BATCH_COVERAGE=FAIL` → `CENSUS_CONTROL_SPLIT` | Same duplicate ownership or missing batch access proof persists in A through two tries; B's complete batch coverage passes. | C1B explicitly assigns a surviving fail/pass split to §4.8; later limbs are unreached. | yes |
| `JOIN=FAIL` → `R3C2_NO_CLASS` | W89 batches; batch 2's predecessor digest names a value other than batch 1's seal, in both seats through two tries. | C1B requires an intact ordered chain from a root without predecessor. JOIN failure stops tally; all-attempting-seat failure after two tries → §4.4. | yes |
| `JOIN=FAIL` → `CENSUS_CONTROL_SPLIT` | A retains that broken predecessor twice; B has a fully matching ordered chain, correct ownership, manifest evidence sources, unique ids and acyclic resolving parents. | C1B's explicit split contract → §4.8. | yes |
| `C5C_NO_FALLBACK=FAIL` → `R3C2_NO_CLASS` | W89 intended claims; both seats' session-identified provider logs contain a fallback entry on both tries. Missing logs are an independent failure variant. | §9 explicitly makes C5C a pre-tally control under the no-class/split rules; persistent failure in every attempting seat → §4.4. | yes |
| `C5C_NO_FALLBACK=FAIL` → `CENSUS_CONTROL_SPLIT` | A's fallback entry or missing log persists twice; B supplies complete session logs with no fallback and passes. | §9 → §4.8. No passing-seat tally is adopted. | yes |
| Merge field disagreement stops tally; persistent C3 failure → `R3C2_NO_CLASS` | P prints “We choose a = 2” at line 1 and “For the other run we choose a = 3” at line 2. Same input id P#a: A records `2`, line 1; B records `3`, line 2. Both individual records validate. The shared merge artefact retains these disagreements on each of two C3 attempts for both seats. | C3 prints `FIELD_DISAGREEMENTS`, exits 1, writes no merged file; C3 PASS requires exit 0 from every artefact run. Persistent common C3 failure in every attempting seat → §4.4. A single disagreement only orders open resolution before tally; it is not automatically an input-set or outcome dispute. | yes |
| Merge field disagreement resolved; `CENSUS_COMPLETE` | Same initial records; open resolution establishes that the claim uses the first run, a = 2. Corrected seat ledgers match, merge and compute exit 0, and the completed C3 artefact passes on retry; all claims are arithmetic and C6 passes. | C3 explicitly requires open resolution before tally; with no surviving two-attempt control failure, follow §§2–3, C6, §4.1. The initial merge failure is retained in the record, not converted into an automatic terminal class. | yes |
| `rests_on=NOT_COMPUTED` | The no-recipe claim “Our resulting dimensionless value is 4” has no ledger records. Candidate is included with `REPRO_NO_DERIVATION_STATED`; the merged input ledger is empty. | §3 explicitly validates an empty ledger and assigns `NOT_COMPUTED` to included candidates without records; C3/§9 three-argument `compute` emits that row. With C6 PASS, §4.2 files `CENSUS_PARTIAL`. | yes |
| Inclusion disputes at threshold; audit PASS | N = 10 arithmetic claims and one additional passage sealed-excluded but auditor-included; both list the passage, so there is no omission. The ten selected arithmetic reconstructions match and all audit prerequisites hold. | C6 reports 1/10 = 10%, which is at or below the limit; no audit failure. With all sealed included claims arithmetic, §4.1 permits `CENSUS_COMPLETE`. | yes |

These are concrete hypothetical inputs to the declared method, not claims about the contents of the actual pinned papers. Only the requested preregistration was read, in full; no other directory file was read or sought. Section 10 was read as history and supplies no routing authority. This is one seat's exhibition; it does not assert that the separate second-seat verification has occurred.

All rows use the following completion conventions unless a row changes them: fixture texts are enumerable with verified manifest rows, all 89 texts are independently enumerated, ownership is partitioned into the required 12 batches, identifiers begin with their owned filename, and every candidate numeral has a recorded disposition. Printed input numerals are retained in the exclusion ledger when author-specified, while their input records remain available to arithmetic. Positive one-based coordinates, exact numeric tokens, verbatim evidence, empty parent lists for roots, complete candidate counts, permitted attempt counts, and printed/reproduced strings are supplied. All earlier controls, seat agreements, provider logs, batch chains and receipts pass unless the row explicitly supplies their failure. Audit reconstructions are independently made and sealed before comparison; a named audit defect is introduced in that reconstruction or enumeration, not in an earlier census-seat record that would cause a prior stop.

W89 is a concrete many-paper witness: P01 through P89 each have exactly the two lines of the first row, with file-prefixed unique claim and input ids. Both seats record each input numeral as `AUTHOR_SPECIFIED_INPUT` and each y result as included. Their joined counts are 178 candidates, 89 included, 89 excluded, 89 attempts and 89 exclusions after one attempt per result. All roots are `CHOSEN`, so the second tally has 89 `USES_CHOSEN` claims. The independent audit enumerates the same passages, receives all 89 arithmetic claims, reconstructs them completely, and prints matching comparisons. Its external seed is supplied and recorded after T even though R = 0 makes the random remainder empty. This witness can be expanded to any finite number of such claims per paper without changing the route.

Fixture D makes the off-sample dependency examples explicit. One enumerable file P contains these passages; the other enumerable texts add no candidate numerals. P:1 is “We choose a = 2.” P:2 is “The intermediate x = a + a = 4 is an input for this calculation.” P:3 is “For b use Source X,” where X is not enumerable and b is not printed. P:4 is “Our result y = x = 4,” claim `P#q01`. P:5 is “Our result z = a + b = 9,” claim `P#q02`. Eighteen further result passages assert `z_i = b_i = 9` and each names the unenumerable Source X for its unprinted b_i; these are `P#q03` through `P#q20`. The intermediate numeral at P:2 is recorded as an author-specified calculation input, not an additional included result. q01 has the input record `P#x`, `PRINTED`, value `4`, `DERIVED`, equation evidence at P:2, parents `[P#a]`. The record `P#a` belongs to q02, is `PRINTED`, value `2`, `CHOSEN`, P:1 value/evidence, and has no parents; q02 also has its correctly evidenced blocked b record. The global ledger is acyclic and q01's provenance closure reaches q02's a record. q01's arithmetic uses its printed x = 4; q02 and q03–q20 are blocked.

Thus D has N = 20, one arithmetic claim, R = 19 and k = 4. With the concrete external seed consisting of `01` repeated 32 times (64 lowercase hexadecimal characters), the stated Python sampling expression on sorted `P#q02` through `P#q20` selects `P#q11`, `P#q14`, `P#q15`, `P#q07`. This selection was calculated directly. q01 is selected automatically; q02 is not selected, but its P#a record must still be independently reconstructed, validated and compared as part of q01's closure. The unchanged fixture audits to PASS and files `CENSUS_PARTIAL`; the indicated reconstruction mutations instead file `CENSUS_AUDIT_FAILED` before partial can be filed.

Fixture A supplies complete-branch matching without an earlier origin-dispute stop. There are 20 included arithmetic claims; 19 are independent copies of the first success fixture. The remaining claim in P prints y = x = 4. P:1 says “We choose a = 2”; P:2 says “For this calculation x = a + a = 4”; P:3 says “We choose x = 4.” Its ledger contains a and x. Both seats agree on x's symbol, `PRINTED` status, value `4`, and value coordinates P:2. Seat A gives x `DERIVED`, Ederive = (`ORIG_EQUATION`, P, 2, “For this calculation x = a + a = 4”), parents `[P#a]`. Seat B gives x `CHOSEN`, Echoice = (`ORIG_CHOICE_STATED`, P, 3, “We choose x = 4”), parents `[]`. Each cited line supports its own reason code, and C3 permits evidence coordinates different from value coordinates. The merged record carries both origin/evidence and parent alternatives. Only this claim reaches the disputed x record: 1/20 = 5%. Its original `rests_on` remains a reported `DISPUTED` pair, even though both branches have `{CHOSEN}` roots and compute `USES_CHOSEN`. Matching the complete alternative does not reconcile or erase this dispute. The auditor must reconstruct a as well as x because both are input records for the selected claim. Every arithmetic claim is selected; all other records match. For the parent-only variant, P additionally prints chosen b = 2 and the shared equation-evidence line “For this calculation x = a + a = b + b = 4”; origin and evidence stay identical while the alternative parent list changes in full.

The suspicion has a direct answer: **`CENSUS_COMPLETE` is REACHABLE.** W89 exhibits it for many papers, and the all-`REPRO_FAILED` variant demonstrates that complete does not mean every printed number was reproduced successfully. It means every included claim received an arithmetic determination and the audit passed. Nothing in the operative text requires at least one included claim to lack a recipe or an input.

For any particular corpus containing even one blocked, absent-input, no-derivation-stated or still-not-evaluable included claim, **`CENSUS_COMPLETE` is unavailable for that run**. The §3 precedence puts that claim outside the arithmetic group. With a passing audit and no earlier stop, §4.2 files `CENSUS_PARTIAL`; with an audit or earlier control/dispute failure, the applicable earlier §4 class is filed instead. Therefore “one such claim forces partial” is conditional on reaching a passing audit, while “complete is unreachable in practice for every possible many-paper input” does not follow. Determining whether the actual pinned corpus contains such a claim requires its census, not this reachability exhibition.

The operative completeness clause is:

> **every included claim carries exactly one outcome from the arithmetic group of §3, with `C6_AUDIT_SAMPLE=PASS`. A
>    denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing.**

The operative arithmetic group is:

> **The arithmetic group** is the set of outcomes that state whether the arithmetic reproduced the number: **exactly
> `REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`**.

For merge disagreements, the immediate rule is precisely the C3 exit-1/no-output/open-resolution rule. Its persistent common-control-failure witness above uses §4.4 and C3's requirement that every printed run succeed; it does not invent a ninth study class or treat a value disagreement as an input-id-set disagreement. The text supplies no unconditional §4 terminal filing for a single unresolved first merge attempt. Likewise, the three explicitly named C1B/C5C controls require the stated two-attempt failure or split before their terminal study filing.

No active §3 outcome or §4 class is UNREACHABLE: all six per-claim outcomes and all eight study-level classes are exhibited. There are no universally blocking clauses to quote for an unreachable active verdict. `REPRO_AFTER_CHOICE` is explicitly retired and is not a seventh outcome; its live chosen-input route is the first table row's arithmetic outcome plus `rests_on=USES_CHOSEN`. Exclusion kinds and `rests_on` labels are not additional reproduction verdicts.

R3C2_C0_EXHIBITION_COMPLETE
