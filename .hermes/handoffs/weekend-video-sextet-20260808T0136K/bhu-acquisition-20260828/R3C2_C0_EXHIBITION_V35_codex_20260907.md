ACCESS_SHA=b2e2073c49936328a533c5926465354ea01db0a63aaec7cca3626acc07e39872
C0_REACHABILITY=PASS

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_WITHIN_STATED_PRECISION` | P line 1: “We choose a = 2 for this calculation.” Line 2: “Our result is q = 2a = 4.0.” Input P#a is `PRINTED`, value `2`, `CHOSEN`, with line-1 `ORIG_CHOICE_STATED` quotation and no parents. Printed `4.0`; reproduced `4`. | §1 includes the result and excludes the input numeral as `AUTHOR_SPECIFIED_INPUT`; §2 consumes the printed input regardless of origin; §3 rounds 4 to 4.0. `rests_on=USES_CHOSEN`, independently. | yes |
| `REPRO_WITHIN_STATED_PRECISION` — rounding boundary | P states a = 2.025 and q = 2a = 4.1, with no uncertainty. Reproduced 4.05. A second claim uses a = −2.025 and prints −4.1. | §3 default printed precision and half-away-from-zero rule give 4.1 and −4.1 respectively. | yes |
| `REPRO_WITHIN_STATED_PRECISION` — stated uncertainty | P states a = 2.05, q = 2a = 4.0 ± 0.1. Reproduced 4.10; difference 0.10. Alternatively print 4.0 with lower uncertainty 0.02 and upper uncertainty 0.10. | §3 uses the uncertainty once, without rounding or doubling; equality passes. In the asymmetric case the reproduced value lies above the printed value, so the upper half-width applies. | yes |
| `REPRO_FAILED` | Same a = 2 and q = 2a, but P prints its result as 5.0. Both numbers reported: printed `5.0`, reproduced `4`. | §2 has sufficient consumable inputs; no earlier terminal condition; §3 arithmetic does not reproduce the numeral. File “unreproduced from the stated inputs.” | yes |
| `REPRO_FAILED` — uncertainty exceeded | P states a = 2.055 and prints q = 2a = 4.0 ± 0.1. Reproduced 4.11. | §3 difference 0.11 exceeds 0.1; neither doubling uncertainty nor rounding the difference is licensed. | yes |
| `REPRO_BLOCKED` — non-enumerable source | P prints “Our result is q = 2a = 4.0; use a from X,” but gives no value for a. X is outside the enumerable manifest, including the possible case that X is listed only as RAW. Input P#a has `BLOCKED`, `IMPORTED`, empty value, and `ORIG_CITATION` quoting P's naming sentence. | §2 named-source rule → C3 valid blocked record → §3 `REPRO_BLOCKED`. Obtaining X elsewhere is irrelevant and not permitted. | yes |
| `REPRO_BLOCKED` — enumerable source without matching value | Same claim, but X is a verified enumerable source whose cited line says “a is defined below” and carries no numeric value; no symbol-and-value line supplies it. | §2 machine-match requirement fails → §3 second blocked limb; name a and X and consume no value. | yes |
| `REPRO_INPUT_ABSENT` | P prints “Our result is q = 2a = 4.0” but nowhere prints a or names a source for it. P#a is `ABSENT`, empty value, `UNDECLARED`/`ORIG_SILENT`, with an adequate recorded search using symbol and prose variants. | Equation exists; no named-source blocked input; §2 prohibits supplying an absent value → §3 absent-input outcome. | yes |
| `REPRO_NOT_EVALUABLE` — `SYMBOLIC_TIMEOUT` | P prints its own result t = 0.5 from the fully specified iteration x₀ = 0, xᵢ₊₁ = (xᵢ + 1)/2, t = x_M, M = 10^12, explicitly directing sequential iteration. Both x₀ and M are printed. The wrapped attempt is still running at 120.0 seconds, at iteration 10^8. | Inputs and procedure exist; §9 wrapper reaches its monotonic deadline, prints `SYMBOLIC_TIMEOUT`, exits 124; §3 files `REPRO_NOT_EVALUABLE` and the point reached. No earlier terminal condition holds. | yes |
| `REPRO_NOT_EVALUABLE` — `MACHINERY_UNAVAILABLE` | P prints t = 3.0 from a specified operation F(2), with the full operator instructions and input 2 printed, but F requires a dedicated optical apparatus unavailable to the lane. The seat records inability to execute the first apparatus operation. | A specified procedure and sufficient inputs avoid no-derivation/blocked/absent; §3 machinery limb files `MACHINERY_UNAVAILABLE` and the point reached. | yes |
| `REPRO_NO_DERIVATION_STATED` — no recipe | P line 1: “Our result is q = 4.0.” No equation or computational procedure occurs anywhere in P. There are no ledger records for this claim. | §1 includes the asserted result; §3 no-derivation condition is first in precedence. C3 permits an empty ledger; script reports `rests_on=NOT_COMPUTED`. | yes |
| `REPRO_NO_DERIVATION_STATED` — named but unspecified procedure | P says “Our simulation gives q = 4.0” and gives no operations, algorithm, or equation for that simulation. | §3 expressly says that naming a procedure without specifying operations is no computational procedure that could produce the number. | yes |
| §3 precedence, exactly one outcome | P supplies q = a + b = 4.0; a is unprinted and traced to non-enumerable X, while b is neither printed nor traced. | §3 `REPRO_BLOCKED` precedes `REPRO_INPUT_ABSENT`; file only blocked. If the passage instead states no operations at all, no-derivation precedes both. | yes |
| `CENSUS_COMPLETE` | Baseline corpus B: 89 hypothetical enumerable papers, each containing the a = 2, q = 2a = 4.0 example above as its only included claim. Every other numeral is enumerated and correctly excluded. N = 89. Both seats agree; controls and receipts pass; the independent auditor lists the same passages, reconstructs all 89 claims exactly, and prints the matching audit artefact. | §2 and §3 give 89 arithmetic outcomes; C6 selects all arithmetic claims, R = 0 and k = 0 for the additional sample; valid seals, external seed, complete records and all matches give C6 PASS. No earlier §4 condition holds → `CENSUS_COMPLETE`. | yes |
| `CENSUS_COMPLETE` — arithmetic failures allowed | In B change one paper's printed result from 4.0 to 5.0; both seats and the auditor report printed 5.0, reproduced 4, `REPRO_FAILED`. | §3 arithmetic group contains both success and failure. C6 reproduces the failure and both numbers; all 89 outcomes remain arithmetic → §4 complete. | yes |
| `CENSUS_PARTIAL` — each non-arithmetic member | In B replace exactly one included result with, separately, the blocked-source, absent-input, no-recipe, or persistent timeout example above. Preserve that claim in the denominator and let C6 reproduce its outcome. | N = 89, R = 1, k = 1: all 88 arithmetic claims and the remaining claim are audited. C6 PASS; §4 non-arithmetic condition holds → partial, ahead of complete. The timeout can remain after the permitted repeat; a repeat is not required for the other cases. | yes |
| `CENSUS_PARTIAL` — genuinely empty enumeration | All enumerable texts have no candidate numeral passages. Both census seats and auditor produce empty candidate, exclusion and input ledgers, all five C1 counts zero. Valid seed, seals and an empty printed audit artefact; no mismatches. | C6's zero-denominator failure requires a passage on either side, absent here. R = k = 0; C6 can PASS. §4 expressly assigns the empty denominator to partial, never vacuous complete. | yes |
| `CENSUS_AUDIT_FAILED` — sampled outcome/number mismatch | B is sealed with P's q = 2a = 4.0 outcome and values 4.0/4. The auditor's sealed reconstruction reports reproduced 5 and `REPRO_FAILED` for that selected claim. | C6 compares outcome and both arithmetic numbers → `MISMATCH`; §4 audit-failed precedes partial/complete. No tally filed. | yes |
| `CENSUS_AUDIT_FAILED` — sealed included passage absent from auditor | B includes P line 2, numeral 4.0. The auditor omits that passage entirely, with internally consistent declared counts for its own shorter enumeration. | C6 union by file/line/numeral → `OMISSION` for a sealed INCLUDED passage absent from the auditor → audit failure irrespective of percentage. | yes |
| `CENSUS_AUDIT_FAILED` — auditor-listed passage absent from sealed ledgers | Both original seats omit P line 1, numeral 2, entirely. The auditor records it as excluded `AUTHOR_SPECIFIED_INPUT`. Alternatively both original seats omit P line 2's included 4.0 and the auditor includes it. | C6 calls either auditor-listed included OR excluded passage missing from the sealed ledgers an `OMISSION` → audit-failed. Earlier C1 checks can pass because they check supplied rows/counts, not semantic enumeration completeness. | yes |
| `CENSUS_AUDIT_FAILED` — inclusion dispute rate above 10% | N = 10 agreed arithmetic claims, all reconstructed exactly. The sealed exclusion ledger additionally contains two “we choose a = 2” passages that the auditor's otherwise internally valid enumeration omits. | C6 treats sealed EXCLUDED passages absent from the auditor as `AUDIT_INCLUSION_DISPUTED`, not omission. 2/10 = 20% exceeds 10% → audit-failed. Census-seat origin and denominator disagreements are absent. | yes |
| `CENSUS_AUDIT_FAILED` — both present, different dispositions | N = 10 sealed arithmetic claims. The auditor lists two of their result passages as excluded, while still reconstructing the assigned computations and matching the arithmetic records. | C6 both-present disposition differences produce two `AUDIT_INCLUSION_DISPUTED` rows; 20% > 10% → audit-failed. This is an auditor-versus-sealed dispute, not an earlier disagreement between the census seats. | yes |
| C6 PASS at the inclusion-dispute boundary → `CENSUS_COMPLETE` | Same N = 10 arithmetic corpus, but exactly one sealed excluded input passage is absent from the auditor, all included claims and reconstructions matching. | C6 dispute rate 1/10 = 10% is reported and allowed. No omission, other failure, or earlier stop; all included outcomes arithmetic → complete. | yes |
| `CENSUS_AUDIT_FAILED` — seedless selection | B reaches receipt T and the auditor's stage-1 enumeration seal; the external custodian never supplies or records a seed. | C6 explicitly orders audit `NOT_RUN` and `CENSUS_AUDIT_FAILED` with missing seed named. No successful seedless selection is invented. | yes |
| `CENSUS_AUDIT_FAILED` — zero denominator with a passage | N = 0, but both sides enumerate P line 1 “We choose a = 2” and exclude it as `AUTHOR_SPECIFIED_INPUT`; no included result exists. Controls, receipts and seed otherwise pass. | C6: a sealed denominator zero with any passage on either side fails, even when the exclusion agrees. §4 audit-failed precedes the zero-denominator partial condition. | yes |
| `CENSUS_AUDIT_FAILED` — incomplete reconstruction | In B the auditor submits P#a with symbol, value, status, origin and evidence but omits required `derived_from`, and never supplies a complete replacement. | C6 `seal-rederivation` refuses the incomplete schema; audit never reaches PASS → §4 audit-failed. If a required dependency is absent at comparison, C6 instead reports `MISMATCH`; neither path borrows it from the sealed ledger. | yes |
| `CENSUS_AUDIT_FAILED` — off-sample dependency fabricated quotation | Fixture D below: selected P#q depends on Q#r, whose owner claim is outside the selection. Auditor reconstructs Q#r with the same id, owner, status, value 2, coordinates, origin and parents, but changes its evidence quotation from “We choose r = 2 for this calculation.” to “We choose r = 2 for every calculation.” | §3 and C6 compare every record in the selected claim's complete closure, including records owned by unselected claims. Unsupported evidence quotation → unconditional record `MISMATCH`, propagated to P#q → audit-failed even though the numerical roots agree. | yes |
| `CENSUS_AUDIT_FAILED` — duplicate input id | In B's reconstruction P#a occurs once under P#q and again under Q#q, each carrying input id P#a; the second copy has a changed quotation. | §3 pre-seal duplicate-id refusal prevents one copy shadowing the other. With no corrected reconstruction C6 cannot PASS → §4 audit-failed; comparison need not be reached. | yes |
| `CENSUS_AUDIT_FAILED` — explicit identity inconsistent with enclosing keys | Auditor puts P#a under P#q but explicitly writes `claim_id=Q#q`, or gives an explicit input id different from its enclosing input key. | §3 refuses inconsistent explicit identity before sealing; no completed audit → §4 audit-failed. | yes |
| `CENSUS_AUDIT_FAILED` — record reconstructed under wrong claim | In D move the unique Q#r reconstruction into enclosing claim R#s and make its explicit owner agree with R#s. Its id remains Q#r and P#q still depends on it; the sealed owner is Q#t. | Internal consistency does not cure sealed-owner disagreement. §3 comparison records a `MISMATCH` of Q#r and propagates it to selected P#q; overall audit fails. | yes |
| `CENSUS_AUDIT_FAILED` — hybrid direction 1 | Fixture H below preserves branch U = (`DERIVED`, equation evidence, parents [P#r]) and branch V = (`CHOSEN`, choice evidence, parents []). Auditor submits U's origin/evidence with V's empty parents, keeping all common fields equal. | The reconstruction has the required fields but matches neither complete branch. §3 unconditional full-record `MISMATCH` → C6 FAIL → audit-failed; any additional derived-without-parent diagnostic cannot turn this into a match. | yes |
| `CENSUS_AUDIT_FAILED` — hybrid direction 2 | Same H; auditor submits V's `CHOSEN` origin and choice evidence with U's parents [P#r]. | §3 requires origin, evidence, parent list and any required search to match jointly. This reciprocal hybrid is neither U nor V → unconditional `MISMATCH` → audit-failed. | yes |
| `CENSUS_AUDIT_FAILED` — changed `origin_search` CONTENT | Fixture S below has two preserved `UNDECLARED`/`ORIG_SILENT` branches S1 and S2, differing only in search query content. Auditor uses the common record fields but changes the query to “x OR adjustable OR tuning,” equal to neither declared query. | §3 preserves search-only alternatives, compares the matched branch's search as JSON structure, and requires a complete branch match. Content differs from both → `MISMATCH` → audit-failed. No >10% origin-classification stop applies because origin labels agree. | yes |
| Complete `ORIG_SILENT` alternative → C6 PASS → `CENSUS_COMPLETE` | In S the auditor reconstructs exactly the entire canonical alternative branch, including its `origin_search_alt` content, evidence, parents and all common fields. Every other record and claim matches. | §3 merge retains the complete silent alternative even with equal origin labels. C6 matches that branch and compares its own search, not the primary search. Search-only difference is not an origin-classification disagreement. All outcomes arithmetic → complete; **does not file audit-failed**. | yes |
| JSON member-order change only → C6 PASS → `CENSUS_COMPLETE` | Same S alternative reconstruction, but reverse JSON object member order at the top level and inside its evidence/search objects; change no key, value or array order. | §3 searches compare as JSON structure; merge recursively sorts object keys. Structural equality remains a complete branch match → C6 PASS → complete. | yes |
| Complete provenance alternative → C6 PASS → `CENSUS_COMPLETE` | In H, N = 20 and only P#q depends on the differing origin record. Auditor reconstructs complete U; in a separate witness it reconstructs complete V. All other claims match. | One affected claim out of 20 is 5%, below the §4 origin-dispute stop. §3 keeps the original dispute and computes its pair; C6 matches a complete branch, recomputes roots under it, and passes. Disputed provenance does not remove an arithmetic outcome from §3's arithmetic group. | yes |
| Complete parent-only alternative → C6 PASS → `CENSUS_COMPLETE` | N = 20. P has r = 2 and s = 2 with measurement evidence, x = r = s = 2 with equation evidence, and q = 2x = 4.0. The seats give x identical `DERIVED` origin/evidence but parents [P#r] versus [P#s]. Auditor reconstructs the second complete branch and both roots. | §3 preserves parent-only alternatives and complete graphs. Matching parent branch is supported, not a hybrid; root recomputation agrees. No origin-label disagreement; C6 PASS, arithmetic census complete, `rests_on` reported as disputed with equal clean classifications. | yes |
| V35 complete graph U or V → C6 PASS; `rests_on=DISPUTED` with equal pair | Fixture G below: U has x→y→r; V has y→x→r; r is measured, x and y derived, all values 2. Auditor reconstructs complete U, or separately complete V. | §3 preserves both whole validated graphs in `provenance_graphs`; compute uses those graphs and reports two `DERIVED_STANDARD_OR_MEASURED_ONLY` classifications for the disputed claim. C6 uses the complete matched graph. Any cyclic unmatched mixed diagnostic view cannot add a mismatch. All arithmetic outcomes and other predicates pass → complete. | yes |
| Matched-graph cycle → `CENSUS_AUDIT_FAILED` | In G auditor uses the individually preserved x→y branch from U and y→x branch from V, with r present. Each record is a complete local branch, but the reconstructed graph has x↔y. | §3 explicitly fails a cycle in the **matched** comparison graph. This is different from an unused diagnostic graph: C6 FAIL → audit-failed. | yes |
| Missing reconstructed dependency → `CENSUS_AUDIT_FAILED` | In G auditor reconstructs x→y but omits y entirely. | C6 requires the auditor's own complete closure and reports a missing input/dependency as `MISMATCH`; §3 also fails a missing dependency in the matched graph → audit-failed. | yes |
| `CENSUS_AUDIT_FAILED` — sampling/seal/artefact failures | In B, separately: hand out only 88 of the 89 arithmetic claims; change the reconstruction after its digest seal; omit the printed `C6_AUDIT.json`; or try selection without the stage-1 enumeration seal and never complete it correctly. | C6 respectively fails recomputed selection, seal matching, required printed artefact, or refuses selection. Each prevents C6 PASS; §4 names audit-failed when the audit does not run to PASS for any cause. | yes |
| `CENSUS_AUDIT_FAILED` — receipt verification | B passes numerical comparison, but the protocol re-hash differs from receipt P, or receipt T is missing at verification. | §7 requires both receipts and four verified values; any missing receipt or mismatch → §4 audit-failed; interpretation `NOT_RUN`, no tally filed. | yes |
| `R3C2_NO_CLASS` | B's claim inputs are intact, but both attempted seats twice fail C5 because the live environment manifest digest differs from the dispatch pin. | C5 FAIL in every attempted seat after two attempts → §4 no-class. Later enumeration/arithmetic/audit controls are `NOT_RUN`. | yes |
| `R3C2_NO_CLASS` — before dispatch | B is intended, but the packet builder's output retains a forbidden string, or the dispatch copy lacks a required pinned source and is not dispatched. | C4 packet failure before dispatch → §4 explicit pre-dispatch no-class limb. No arithmetic claim needs to be attempted. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — candidate inclusion | P prints “We take q = 4.0.” One seat treats it as an asserted own result, the other as an author-specified input; that difference remains after both reconciliation attempts. | §1/§6 candidate inclusion disagreement after two reconciliations → §4 denominator-disputed; disputed passage and ledgers reported, no arithmetic. | yes |
| `CENSUS_DENOMINATOR_DISPUTED` — input lists | Both seats include P's q = a + b = 4.0 with printed a = b = 2; seat U lists P#a and P#b, seat V lists only P#a, and the input-set difference survives the one C3 reconciliation. | C3 states equation-relative completeness is seat-authored, so individual machine validation need not detect the omission. Merge id sets differ → one reconciliation → §4 denominator-disputed, ahead of generic control failure for this expressly routed case. | yes |
| `CENSUS_OUTCOME_DISPUTED` | P has a = 2, q = 2a = 4.0. Inputs, inclusion and machine controls agree. Seat U reports 4.0/4 and within precision; seat V reports 4.0/5 and failed, and the disagreement persists after the prescribed reconciliation. | §2 step 5 and §4 outcome-dispute clause require reporting both verdicts, number pairs and steps and stopping. C1 checks fields, not correctness of arithmetic; one reader's wrong calculation is a concrete possible input to this dispute rule. | yes |
| `CENSUS_ORIGIN_DISPUTED` | N = 10 arithmetic claims. For two independent inputs the paper prints both an explicit choice sentence and a measurement-description sentence for value 2; one seat uses `CHOSEN` with its exact choice quotation, the other `MEASURED` with its exact measurement quotation. Each affects one distinct claim. All common fields and outcomes agree. | C3 independently records the classifications without reconciliation and merge preserves both; each quoted sentence supports its submitted code and contains no competing higher-precedence code. §4 affected-claim rate 2/10 = 20% → origin-disputed. Audit is `NOT_RUN`, not audit-failed. | yes |
| `CENSUS_CONTROL_SPLIT` | In B seat U passes C5 live pin verification; seat V still has a mismatching manifest digest after two attempts. | §4 surviving fail/pass control split → report both and stop; do not adopt U's tally. Later controls `NOT_RUN`. | yes |
| `C1B_BATCH_COVERAGE=FAIL` → `R3C2_NO_CLASS` | B's batches are intended, but every attempted seat's batch 3 report lacks its packet `ACCESS_SHA`, on both tries. | C1B requires every report's access proof; failure stops each seat tally. C1B explicitly maps universal persistent failure to §4 no-class. | yes |
| `C1B_BATCH_COVERAGE=FAIL` → `CENSUS_CONTROL_SPLIT` | Same missing batch-3 proof persists only for seat V; U has all proofs and passes after the allowed attempts. | C1B's explicit surviving fail/pass split rule → §4 control-split. | yes |
| `JOIN=FAIL` → `R3C2_NO_CLASS` | Every attempted seat's limb-B batch-2 predecessor digest names a different predecessor from sealed batch 1; both tries retain the broken chain. | C1B JOIN checks the ordered chain and limb-A binding; FAIL stops tally; persistent failure in all attempted seats → §4 no-class. | yes |
| `JOIN=FAIL` → `CENSUS_CONTROL_SPLIT` | U's limb-B seals and predecessors match; V's batch-2 predecessor stays wrong after two attempts. | C1B JOIN fail/pass split → §4 control-split. The passing seat is not substituted. | yes |
| `C5C_NO_FALLBACK=FAIL` → `R3C2_NO_CLASS` | Every attempted seat has a session-identified provider log containing a fallback entry on both tries; alternatively every attempted seat lacks one required session log on both tries. | §9 explicitly makes missing logs or any fallback entry FAIL and places C5C under the pre-tally §4 no-class/control-split rules. Universal persistent failure → no-class. | yes |
| `C5C_NO_FALLBACK=FAIL` → `CENSUS_CONTROL_SPLIT` | U has complete logs without fallback; V still lacks its batch-3 session log, or has a fallback entry, after two attempts. | §9 explicit C5C routing → §4 control-split; stop without adopting U. | yes |
| Merge `FIELD_DISAGREEMENTS` → tally stopped; persistent C3 failure → `R3C2_NO_CLASS` | P prints a = 2 on line 1 and a = 3 on line 2 as conflicting instructions for the same q = 2a claim. The seats submit the same P#a id but values `2`/`3` and value coordinates 1/2; each value token and evidence validates locally. Both lane-side C3 merge attempts retain the disagreement and produce no merged file. | C3 merge prints disagreements, `FIELD_DISAGREEMENTS`, exits 1, writes no merged file, and requires open resolution before tally. C3 PASS requires exit 0 from every printed run, including merge. With the common required merge still failing after two attempts and no passing C3 artefact, §4 universal control-failure rule gives no-class. This is not an input-id-set dispute. | yes |
| Merge field disagreement resolved → `CENSUS_COMPLETE` | Same initial conflict, but open resolution establishes line 2 as the paper's explicitly superseding instruction. Both seats submit a = 3 at line 2 before the second completed C3 attempt. P prints q = 2a = 6.0; both reproduce 6, all other B claims and controls match. | C3 explicitly licenses open resolution before tally. Successful corrected merge/compute and all later checks including C6 PASS → §4 complete. One initial merge failure alone does not immediately license a terminal filing. | yes |
| `rests_on=NOT_COMPUTED` | P's sole result is “Our result is q = 4.0,” with no recipe and no input record. Keep it among B's included claims. | §3 and §9 compute use the candidate file and emit a row for this claim despite the empty claim ledger: `NOT_COMPUTED`. Its reproduction outcome is no-derivation; with C6 PASS the study files partial. | yes |

The table exhibits six active §3 outcomes and all eight §4 classes. These are hypothetical witness corpora, source passages, reader records and execution states supplied to the operative rules, not assertions about the contents of the actual pinned corpus. Only the requested preregistration was read, in full. No other file in the directory was read or sought, and no referenced script was opened or executed. Section 10 was read as history and supplies no binding premise here. This is a textual reachability exhibition, not a gate, physics assessment, census run, or claim that a second independent C0 seat has verified this report.

For each table row, all prerequisites not deliberately varied hold: identifiers have their owned-file prefix; sources used as enumerable inputs are verified manifest texts; candidate and exclusion rows retain file, line and numeral; declared counts match their rows; quoted evidence is exact and positively located unless the row deliberately corrupts it; no `ABSENT` or `BLOCKED` record supplies a value; both seats pass the required controls and reconciliations unless a failure is the witness. Successful audits have the independent full enumeration, stage-1 seal before selection, receipt T, an independently supplied and receipted seed `0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef`, identifier-only handout, complete reconstruction sealed before release, matching seals and recomputed selection, and a printed comparison artefact. Audit-failure witnesses leave no earlier §4 stop condition true unless a row expressly explains that precedence. The zero-denominator witnesses explicitly override B rather than quietly dropping its claims.

The fixture labels in the table have these concrete meanings:

- **D (off-sample dependency).** P prints q = 2r = 4.0 and directs use of r from enumerable Q. Q line 1 says “We choose r = 2 for this calculation.” This is an excluded author-specified input. Q line 2 states an included claim t = r + u = 5.0, with u neither printed nor sourced, so Q#t is `REPRO_INPUT_ABSENT`; Q#r is a `PRINTED`/`CHOSEN` record owned by Q#t, with the line-1 choice evidence and no parents. P's input is a verified external `PRINTED`/`IMPORTED` record with claiming-paper citation evidence and dependency Q#r, so Q#r belongs to P#q's closure. Use N = 20: one arithmetic claim P#q and 19 non-arithmetic claims with ids Q#t and R#s01 through R#s18. Each R claim prints its own result s = 4.0 with no recipe. C6 selects P#q plus k = 4 remaining claims. With the exact seed printed above, the stated Python sampling expression selects R#s14, R#s07, R#s13 and R#s04; I evaluated that expression with the standard library, without opening any census script or source file. Q#t is therefore concretely off-sample, yet the auditor must reconstruct Q#r. R#s in the wrong-owner row denotes unselected R#s01. All other records match and there are no census-seat disputes.
- **H (two complete provenance branches).** P line 1: “We measure r = 2 by counting two pulses.” Line 2: “x = r = 2.” Line 3: “We choose x = 2 for this calculation.” Line 4: “Our result is q = 2x = 4.0.” Both seats' x record uses value 2 at line 2. U cites line 2 as `ORIG_EQUATION`, `DERIVED`, with parent P#r; V cites line 3 as `ORIG_CHOICE_STATED`, `CHOSEN`, with no parents. Both ledgers contain the same measured P#r record and input-id set. Evidence-line and value-line equality is not required by C3. Embed this one affected claim among 20 arithmetic claims, keeping origin disagreement at 5%. Merge chooses PRIMARY by the canonical record hash; U and V denote record contents, never seat positions. Both hybrid directions are exhibited regardless of which content becomes PRIMARY.
- **S (complete silent alternatives).** P line 1 is “x = 2”; line 2 is “Our result is q = 2x = 4.0.” There is no provenance statement for x. Its record is `PRINTED`, value `2`, source P:1, `UNDECLARED`, evidence reason `ORIG_SILENT` with the same schema-valid empty evidence payload in both records, and `derived_from=[]`. Both searches cover all pinned enumerable texts and retain their actual value-use matches, including P:1 and P:2; neither finds provenance evidence. S1 uses query “x OR parameter x OR chosen x OR measured x”; S2 uses “x OR coefficient x OR adopted x OR fitted x.” Each includes variants, and both are adequate recorded searches in this witness. Their `files` and `matches` arrays are fully populated identically; only `query` differs. The smaller canonical record hash determines PRIMARY; the other complete search is retained as `origin_search_alt`. Reconstructing that other record exactly supplies the requested complete silent alternative, without an origin-label dispute or any seat-order assumption. Replacing the query by a third string changes JSON content; merely reordering object keys does not.
- **G (V35 whole graphs).** P prints measured r = 2 with its counting procedure, the relations x = y = 2, y = r = 2, y = x = 2 and x = r = 2 on distinct lines, and its result q = x + y = 4.0. Both seats use the same numeric value coordinates for each symbol, but quote the appropriate relation for each derived record. U: x depends on y, y depends on r. V: y depends on x, x depends on r. Both complete graphs are valid and acyclic, with the same id sets and origin labels. If these relation passages add included results under §1, retain and reproduce every such result too; all are arithmetic. A claim reaching x or y is disputed through the alternative evidence/parents but has the equal clean classification pair from the two complete graphs. The positive witness does not require a particular SHA ordering to create a mixed cycle: complete U and complete V must pass either way. The additional table row deliberately supplies the locally supported but globally cyclic matched graph and therefore fails under the explicit matched-graph rule.

The remaining provenance values also have concrete homes under the same arithmetic rule. For q = 2a = 4.0, a locally printed a = 2 with “We fit a = 2 to the displayed observations” yields `USES_FITTED`; “We adopt a = 2 from X” yields `USES_IMPORTED` with local-import citation evidence; bare a = 2 with an adequate silent search yields `USES_UNDECLARED`; a = 2 reported as the paper's own described measurement yields `DERIVED_STANDARD_OR_MEASURED_ONLY`. For a closed-list example, P literally prints c = `2.99792458e8` and q = 2c = 599584916 m/s: c is `STANDARD` with `ORIG_CONSTANT`, its own value coordinates in P, and the exact ledger-key/value strings. For a transitive example, measured r = 2, derived a = 2r = 4, and q = 2a = 8.0 yield the same clean field through parent r. These all reproduce within stated precision. Combining roots uses §3's fixed severity order; none of these provenance fields is a seventh reproduction outcome. A closed-list value printed only in a named enumerable external source instead follows §2's verified external-import route, with citation evidence in the claiming paper and the first symbol-and-value source line, not the local `STANDARD` route.

**Direct answer to the suspicion: `CENSUS_COMPLETE` is REACHABLE.** B is a many-paper witness, not a vacuous single-record construction: all 89 included claims have arithmetic outcomes and all 89 are audited. No operative clause requires a corpus to contain a non-arithmetic claim. Nor must every number be successfully reproduced: an independently reproduced `REPRO_FAILED` result remains in the arithmetic group. Chosen, fitted, imported and undeclared origins do not themselves prevent completion when their required numeric inputs have consumable status and all controls pass.

A single included blocked, absent-input, no-derivation or not-evaluable claim anywhere in a particular corpus does prevent that corpus from filing complete while that outcome persists. The precise route is §3 non-arithmetic outcome → §4 partial condition → `CENSUS_PARTIAL` **if C6 passes and no earlier stop applies**. If C6 fails, the filing is `CENSUS_AUDIT_FAILED`; if an earlier control or dispute stop applies, that earlier class wins and later checks are `NOT_RUN`. Thus “forces partial” needs the successful-audit/no-earlier-stop qualification. This report does not establish whether the actual pinned corpus satisfies completion, since the user expressly restricted access to the preregistration.

For the requested merge stop, the operative clause is: “where the two seats' value, status, symbol or source coordinates differ on the same input id, `merge` prints every such disagreement, reports `FIELD_DISAGREEMENTS`, exits 1 and writes no merged file — the lane resolves it in the open before the tally.” The immediate result is a stopped tally pending that resolution, not an invented new §4 class. A corrected second attempt can proceed; persistent failure of the common required C3 artefact after two attempts takes the existing no-class route. The specific input-set reconciliation route applies to different id sets, not to this same-id field disagreement. A control split requires an actual passing and failing control result; two contradictory field values alone are not a `CENSUS_CONTROL_SPLIT`.

Seat order does not change any exhibited branch verdict. Section 3 defines merge over the set of seat records, orders complete records by their canonical hashes, retains complete alternatives and whole provenance graphs, and serializes recursively with sorted object keys. Holding the auditor reconstruction fixed while swapping the two census seats therefore retains each positive match and each negative hybrid/search-content mismatch, together with the whole audit outcome. No row assumes that “seat B” means ALT, that equal origin labels imply equal searches, or that an independently mixed graph was supplied by a seat.

**UNREACHABLE active §3 outcomes: none. UNREACHABLE §4 classes: none.** Consequently there is no blocking clause to quote for an unreachable active verdict. `REPRO_AFTER_CHOICE` is explicitly retired under the operative §3 wording; `REPRO_EXACT` is a former name, and candidate exclusions are expressly not per-claim outcomes. They are not additional active verdicts whose retirement would cause C0 failure. `NOT_COMPUTED` is exhibited as a script-computed field, not an extra reproduction class. Every requested additional failure and positive condition has an explicit row and filing route above.

R3C2_C0_EXHIBITION_COMPLETE
