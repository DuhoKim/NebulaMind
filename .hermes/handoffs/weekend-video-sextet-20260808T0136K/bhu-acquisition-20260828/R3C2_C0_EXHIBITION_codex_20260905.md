ACCESS_SHA=19a075c66696c4a0793c2fb13a45247f9e174f4b2194ec3a9f540ed001597000
C0_REACHABILITY=PASS

This is an existential reachability exhibition, not a physics judgment and not a claim about what the actual corpus contains. Each hypothetical passage below is a specific included claim under §1. Unless a row says otherwise, the paper states the displayed equation or computational procedure.

The held clause matters. Section 3 says: “HELD PENDING DUHO'S RULING” and “No later reader should read the current wording as a settled choice.” It also says, “The wording standing here is option (b), derivation-only.” Accordingly, the rows below apply the standing provenance rule—`DERIVED` and `STANDARD` are admissible; `CHOSEN`, `FITTED`, `IMPORTED`, and `UNDECLARED` are inadmissible—without pretending that rule is settled. The individual named outcomes are reachable under that standing wording. The precise membership of the “arithmetic group,” and hence the §4 label assigned to a completed corpus containing a disputed-borderline arithmetic outcome, depends on Duho's ruling. The `CENSUS_COMPLETE` exhibition avoids that dependency by using only `REPRO_EXACT`, an outcome that expressly says admissible-only arithmetic reproduced the number.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `REPRO_EXACT` | Claim E prints “y = 6” and `y = 2x`; its ledger gives `x = 3`, status `PRINTED`, origin `DERIVED`, with the paper's derivation of `x`. Computing `2×3` gives 6 at the printed precision. | §1 includes the paper's own printed numeral → §2 extracts equation and input → standing §3 makes `DERIVED` admissible → admissible-only arithmetic gives the printed number → `REPRO_EXACT`. | Yes |
| `REPRO_AFTER_CHOICE` | Claim C prints “y = 10” and `y = 2a`; it prints `a = 5` and says “we choose a = 5,” so the ledger origin is `CHOSEN`. Using it gives `2×5 = 10`; without it the result cannot follow. | §1 included → §2 ledger records printed `a` and `CHOSEN` origin → standing §3 makes `CHOSEN` inadmissible → the number follows only once that input is used → `REPRO_AFTER_CHOICE`. | Yes, under the standing option-(b) wording; the held ruling can change the core admissibility consequence. |
| `REPRO_FAILED` | Claim F prints “y = 7” and `y = 2x`; `x = 3` is `PRINTED`, origin `DERIVED`. All required inputs are admissible, but the arithmetic gives 6, not 7. | §1 included → no earlier terminal condition applies → admissible inputs are sufficient → arithmetic differs from the printed number → `REPRO_FAILED`, reporting 6 and 7 as “unreproduced from the stated inputs.” | Yes, under the standing admissibility wording; a ruling that changes which inputs are admissible can change which concrete cases enter this class. |
| `REPRO_BLOCKED` | Claim B prints “y = 14,” `y = 2x`, and `x = 7`, while stating that the provenance/derivation of `x` is in an external supplementary source. The value is not absent, but the named outside-lane source cannot be obtained. | §1 included → equation and input are stated, so neither no-derivation nor input-absent applies → input traces to an unobtainable source outside the lane → precedence order reaches `REPRO_BLOCKED` before later outcomes. | Yes |
| `REPRO_NOT_EVALUABLE` | Claim T prints “r = 1.234” and specifies a symbolic integral `r = I(p)`; every required parameter is printed with `DERIVED` origin, but evaluating `I` exceeds 120 seconds (or needs unavailable machinery). | §1 included → derivation exists, no input is absent, and no outside source is blocked → 120-second cap/machinery condition occurs → precedence order files `REPRO_NOT_EVALUABLE`, with `SYMBOLIC_TIMEOUT` and the point reached. | Yes |
| `REPRO_NO_DERIVATION_STATED` | Claim N states “our result is 42 km” but gives no equation or computational procedure capable of producing 42 km. | It is included by §1 because it is the paper's own printed quantitative result → §2 finds no producing equation/procedure → first terminal condition in §3 is `REPRO_NO_DERIVATION_STATED`. | Yes |
| `REPRO_INPUT_ABSENT` | Claim A prints “y = 8” and `y = ax`; it prints admissibly derived `a = 2` but never states required input `x`. | §1 included → derivation exists → §2 marks `x` `ABSENT` → §2 says an `ABSENT` input ends the attempt → §3 precedence files `REPRO_INPUT_ABSENT` before blocked/not-evaluable/arithmetic outcomes. | Yes |
| `CENSUS_COMPLETE` | A one-claim corpus containing Claim E above; after the reproduction attempt, its sole included claim has exactly one `REPRO_EXACT` outcome and the full tally denominator is 1. | Limb A agrees on the denominator → Limb B completes → every included claim has exactly one outcome from the arithmetic group (`REPRO_EXACT`) and no non-arithmetic claim exists → §4 `CENSUS_COMPLETE`. | Yes. In a many-paper corpus it remains reachable iff every included claim receives an arithmetic-group outcome. Any single `REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, or `REPRO_NOT_EVALUABLE` claim forces `CENSUS_PARTIAL`; such a claim is possible, not textually inevitable. |
| `CENSUS_PARTIAL` | A corpus with Claim E and Claim A; after two attempts, E is `REPRO_EXACT` and A remains `REPRO_INPUT_ABSENT`. | At least one included claim carries one of §4's enumerated non-arithmetic outcomes after two attempts → `CENSUS_PARTIAL`; §4 declares it inconclusive and gives it precedence over `CENSUS_COMPLETE`. | Yes |
| `CENSUS_AUDIT_FAILED` | The original seat files Claim E as `REPRO_EXACT` using `x = 3`, but the independent §6 auditor re-derives from the cited line and obtains `x = 4`, so it cannot reproduce that filed outcome. | Limb B reaches §6 audit → audit cannot reproduce a sampled/mandatorily audited per-claim outcome → §4 `CENSUS_AUDIT_FAILED`; census void. | Yes |
| `R3C2_NO_CLASS` | Control C5 is attempted twice by each participating seat; in every seat both attempts fail to execute the required live Python/SymPy/hash harness. | A control fails after two attempts in every seat that attempted it → §4 `R3C2_NO_CLASS`. | Yes |
| `CENSUS_DENOMINATOR_DISPUTED` | Seat 1 includes passage D (“we obtain 5 m”); seat 2 excludes it as attributed-not-derived. They still disagree after two reconciliation attempts. | Limb A enumeration disagreement on a candidate's inclusion persists after two attempts, with tolerance zero → stop under §6 → §4 `CENSUS_DENOMINATOR_DISPUTED`; census does not proceed. | Yes |
| `CENSUS_ORIGIN_DISPUTED` | In a 9-claim corpus, input `a` affects Claim C and Claim D (2/9 = 22.2%). One seat cites wording as `CHOSEN`; the other cites wording as `DERIVED`, and the independent classifications remain unreconciled. | Independent origin classifications disagree on inputs affecting more than 10% of included claims → §4 `CENSUS_ORIGIN_DISPUTED`; list every disputed input and both quotations; census does not proceed. | Yes |
| `CENSUS_CONTROL_SPLIT` | After two attempts, Seat 1's C5 harness fails both times, while Seat 2's C5 harness passes and prints all required live outputs. | Same control fails in one seat and passes in another after two attempts → §4 `CENSUS_CONTROL_SPLIT`; report both outputs and stop without adopting the passing result. | Yes |

Suspicion resolved: `CENSUS_COMPLETE` is REACHABLE. The document imposes no universal fact that a real multi-paper corpus must contain a blocked, absent-input, no-derivation, or not-evaluable claim. A corpus—even a many-paper corpus—in which every included claim receives `REPRO_EXACT` routes to `CENSUS_COMPLETE`. Conversely, one included claim with any listed non-arithmetic outcome routes to `CENSUS_PARTIAL`, whose precedence blocks `CENSUS_COMPLETE` for that realized corpus. Thus one such claim is sufficient to prevent completeness, but the preregistration does not make such a claim necessary. This conclusion does not require resolving the held arithmetic-group boundary because the positive exhibition uses only `REPRO_EXACT`, while the partial route uses outcomes §4 expressly names as non-arithmetic.

UNREACHABLE verdicts: none. Therefore there is no blocking-clause quotation to supply.

R3C2_C0_EXHIBITION_COMPLETE
