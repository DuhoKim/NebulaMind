Arithmetic executed for real: `python3 -c` is blocked in this mode (policy), so I ran the check as a script file (`python3 /tmp/c0_arith.py`). Output: t_H = 1/H₀ = 4.580875e+17 s = 14.515918 Gyr; H₀(SI) = 2.182989e-18 s⁻¹; ρ_c = 3H₀²/(8πG) = 8.522730e-27 kg/m³. No other file was read; everything else below routes through the inlined document only.

ACCESS_SHA=b41136025d1a567f4f0557ee92b87ecdadb5bc5e00a62fded6d164b0b367bd3a
C0_REACHABILITY=FAIL

Reason in one line: six of seven §3 outcomes and all seven §4 classes are exhibitable; REPRO_AFTER_CHOICE is not — the method as written contains no procedure that can ever file it, and §10.2 already files exactly that.

EXHIBITION TABLE — (A) §3 per-claim outcomes

verdict | concrete input | clause path | reachable
---|---|---|---
REPRO_EXACT | P-A p.4 l.12 prints "from t_H = 1/H_0, t_H = 14.5 Gyr". Sole input H₀ = 67.36 km/s/Mpc, status STANDARD, origin STANDARD via ORIG_CONSTANT — the value is a literal row of C3's printed closed list. Arithmetic: 1/H₀ = 4.580875e17 s = 14.516 Gyr ≈ 14.5 Gyr within the printed 3-s.f. precision. | §2(1) extract number/units/equation → §2(2) inputs = {H₀} → §2(3) STANDARD on C3's closed list → §2(4) admissible per §3's ADMISSIBLE definition → arithmetic matches → §3 REPRO_EXACT. No terminal condition holds, so §3 precedence falls through to the arithmetic group. | YES
REPRO_AFTER_CHOICE | No concrete input exists. Nearest candidate shape (P-B: equation stated; every input printed; one input origin CHOSEN, e.g. §3's own entry 59 "β = 1/929.25 is printed and chosen"; printed values do yield the printed number) cannot be routed to any outcome — see UNREACHABLE section. | None licensed by the text. | NO
REPRO_FAILED | P-C p.2 l.30 prints "ρ_c = 3H_0²/(8πG) = 9.2e-27 kg m⁻³". Inputs H₀ = 67.36, G = 6.67430e-11, both STANDARD on C3's list; admissible inputs are sufficient. Arithmetic: H₀ = 2.182989e-18 s⁻¹, ρ_c = 8.5227e-27 ≠ 9.2e-27 printed, outside the stated 2-s.f. precision. | §2(1)–(4) admissible-only attempt completes, result ≠ paper's number → §3 REPRO_FAILED; report both numbers; wording "unreproduced from the stated inputs," not "error". | YES
REPRO_BLOCKED | P-D p.7 l.3 prints "using K from the MegaSurvey DR2 calibration pipeline (in preparation), M = 4.1e14 M_sun"; equation stated, all other inputs DERIVED/STANDARD; K traces to an external source the lane cannot obtain (§11: published sources only, nothing from another lane). | §2(2) list K → §2(3) K's evidence is external and unobtainable → §3 REPRO_BLOCKED; name it. Precedence: derivation stated (not NO_DERIVATION), K stated-with-source (not INPUT_ABSENT), so BLOCKED is the first applicable terminal class. | YES
REPRO_NOT_EVALUABLE | P-E p.9 l.21 prints "solving det(M(x)) = 0 for the stated 12×12 symbolic matrix gives x = 0.773"; all inputs DERIVED/STANDARD; the symbolic determinant does not complete within the cap. | §2(4) attempt → §9's 120-second cap on symbolic operations with SYMBOLIC_TIMEOUT reportable → §3 REPRO_NOT_EVALUABLE; print SYMBOLIC_TIMEOUT and the point reached. | YES
REPRO_NO_DERIVATION_STATED | P-F p.3 l.14 prints "we find the halo mass to be 2.3e12 M_sun", asserted as the paper's own result, with no equation or computational procedure anywhere in the paper; not attributed to another work, so §1 includes it (numeral with units, not an excluded kind). | §1 inclusion → §2(1) finds nothing to attempt → §3 REPRO_NO_DERIVATION_STATED; name the passage. It is first in §3's precedence order, so it is filed even where other terminal conditions co-occur. | YES
REPRO_INPUT_ABSENT | P-G p.5 l.8 prints "t_ff = √(3π/(32 G ρ)) = 4.7e5 yr"; equation stated; G STANDARD on C3's list; ρ never appears anywhere in the paper. | §2(2) inputs {G, ρ} → §2(3) ρ ABSENT → §2's rule "A seat may not supply a value for an ABSENT input. Encountering one ends that claim's attempt." → §3 REPRO_INPUT_ABSENT; name the input ρ. Derivation is stated, so NO_DERIVATION does not fire first. | YES

EXHIBITION TABLE — (B) §4 study-level classes

verdict | concrete input | clause path | reachable
---|---|---|---
CENSUS_COMPLETE | Corpus: 3 papers, 4 included claims, each of the REPRO_EXACT shape above (derivation stated; every input DERIVED or STANDARD-on-list; arithmetic matches within stated precision). Exclusions ledgered separately (C1 denominator printed: 4 included); the two seats' enumerations agree; no origin disputes; C1–C6 pass in both seats; audit reproduces every arithmetic-asserting outcome. | §3 files an arithmetic-group outcome on all 4 claims → §4.2's trigger (≥1 non-arithmetic outcome) does not fire → §4.1 "every included claim carries exactly one outcome from the arithmetic group of §3" → CENSUS_COMPLETE; tally 4/4 reported with its denominator. | YES — see suspicion answer below
CENSUS_PARTIAL | Corpus: 10 included claims; 9 of the REPRO_EXACT shape; claim 10 is the REPRO_INPUT_ABSENT exemplar (ρ ABSENT), filed identically after two attempts. | §3 files a non-arithmetic outcome on claim 10 → §4.2: "at least one included claim carries a non-arithmetic outcome … INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" → CENSUS_PARTIAL; report each and why (claim 10, input ρ absent, both attempts). | YES
CENSUS_AUDIT_FAILED | The t_H claim filed REPRO_EXACT (14.5 Gyr). The C6 third seat, without seeing prior work and re-classifying every origin from the pinned sources, re-derives and cannot reproduce the filed outcome (obtains 14.6 Gyr against the filed precision). | §5 C6: "Any outcome the audit cannot reproduce, or any ledger incompleteness, files CENSUS_AUDIT_FAILED" → §4.3: the census is void; report which claim. (Second licensed trigger: §7 relay digest mismatch.) | YES
R3C2_NO_CLASS | Control C3, machine-checked: a ledger record's PRINTED value fails machine-match against its cited source line; C3_NO_SUBSTITUTION=FAIL on both attempts in seat A and both attempts in seat B (including pre-dispatch, per §10.2's extension of this class to pre-dispatch control failures). | §5 C3 PASS/FAIL/NOT_RUN → FAIL "in every seat that attempted it after two attempts" → §4.4 R3C2_NO_CLASS. | YES
CENSUS_DENOMINATOR_DISPUTED | Candidate passage P-H p.6 l.9 prints "Ω_m = 0.315": seat A reads it as the paper's own result (include), seat B reads it as attributed-not-derived (excluded kind); two reconciliation attempts fail to agree. | §1: "disagreement on any candidate stops the study under CENSUS_DENOMINATOR_DISPUTED"; §6 Limb A: disagreement "after two reconciliation attempts — tolerance zero, measured in candidate passages — stop with CENSUS_DENOMINATOR_DISPUTED (§4)" → §4.5; disputed candidates listed; census does not proceed. §1's own codex note concedes the boundary "remains a judgement", so this input is a live possibility. | YES
CENSUS_ORIGIN_DISPUTED | Corpus: 20 included claims; on 3 of them (15% > 10%) the seats' origin classifications disagree on an input — seat A files ORIG_CHOICE_STATED→CHOSEN from the paper's "we take β = …" text; seat B finds no stating text and files ORIG_SILENT→UNDECLARED. | §5 C6: disagreement filed ORIGIN_DISPUTED, "not reconciled"; "Above 10% of included claims, CENSUS_ORIGIN_DISPUTED" → §4.6: census does not proceed; every disputed input listed with both classifications and both quotations. | YES
CENSUS_CONTROL_SPLIT | Control C3: seat A files FAIL on both attempts (a PRINTED value machine-mismatch); seat B files PASS on both attempts (its ledger clean). | §4.7: "a control fails in one seat and passes in another after two attempts" → CENSUS_CONTROL_SPLIT; report both seats' outputs and stop; "do not adopt the passing seat's result." | YES

(C) THE NAMED SUSPICION — CENSUS_COMPLETE: REACHABLE.

Answer: reachable, in principle — exhibited above — and fragile exactly as suspected. Routing of the force the suspicion names: §4.2 files CENSUS_PARTIAL the moment "at least one included claim carries a non-arithmetic outcome" and gives it precedence in its own words: "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE." §5's C0 note concedes the same: "CENSUS_COMPLETE requires every included claim to carry an arithmetic-group outcome, which a single blocked or absent input in the whole corpus is enough to prevent." So one REPRO_INPUT_ABSENT, REPRO_BLOCKED, REPRO_NO_DERIVATION_STATED, or REPRO_NOT_EVALUABLE claim anywhere in the corpus forces CENSUS_PARTIAL, and in any realistic many-paper corpus that is the likely filing. But that is a property of the corpus, not a clause defect: no text bars the all-arithmetic corpus, and the class CAN be filed — which is the C0 question. Verdict: reachable; exhibit in the table; expected to be rare in practice by the document's own admission. One caveat that IS ruling-dependent: the arithmetic group's "membership is fixed by the clause held above" (§3), so the boundary of CENSUS_COMPLETE shifts with Duho's ruling even though its reachability does not.

UNREACHABLE VERDICT — REPRO_AFTER_CHOICE — BLOCKING CLAUSES, QUOTED VERBATIM

Blocking clause 1, §2 step 4 — the only arithmetic attempt the method licenses:
"Attempt the arithmetic with ADMISSIBLE inputs only (§3's definition: origin DERIVED or STANDARD). A printed value whose origin is CHOSEN or FITTED is not admissible."

Blocking clause 2, §10.2 — the document's own filed finding (kimi finding 2a):
"§2 orders only an admissible-only arithmetic attempt, so no stated procedure ever establishes 'the number follows only once an inadmissible input is used'. REPRO_AFTER_CHOICE — the class §3 says this census exists to detect — cannot be reached from the method as written."

Why no path exists: §3 defines the class as "it follows only once an inadmissible input is used … including one printed in the paper", but §2 contains no step that ever performs that use, so the defining condition is unestablishable by any stated procedure. The P-B claim shape above demonstrates the fall-through: it is not REPRO_EXACT (not admissible-only), not REPRO_FAILED ("admissible inputs are sufficient" fails — the chosen value is needed), and no terminal class applies (derivation stated; no input ABSENT; no external source; evaluable). The claim exits §3 carrying no outcome at all, despite §3's "Exactly one outcome is filed per claim." §4 then has no class for it either: it blocks CENSUS_COMPLETE (not an arithmetic outcome) without triggering CENSUS_PARTIAL (not a non-arithmetic outcome) — the §4-side shadow of the same filed defect.

DEPENDENCE ON THE HELD CLAUSE, STATED EXPLICITLY PER THE BRIEF'S NOTE

- REPRO_AFTER_CHOICE: reachability DEPENDS on the ruling. The standing wording is option (b); §10.2 states kimi's repair (a second arithmetic attempt using the printed inadmissible value) "is only required if the ruling keeps the three-member arithmetic group. Under option (c) … REPRO_AFTER_CHOICE is retired and the defect disappears with it." So under (b)-as-written the class is unreachable, and under (c) it does not exist — no reading of the text as it stands licenses an exhibition, and the document itself files this as an input to Duho's ruling rather than repairing it.
- CENSUS_COMPLETE / CENSUS_PARTIAL: their shared boundary depends on the ruling (the arithmetic group's membership is fixed by the held clause); their reachability as exhibited does not — the exhibited corpora fall inside and outside the group under any reading.
- REPRO_FAILED: §10.1 records that the class's width (kimi V4 finding 4) turns on the held clause; reachability does not.
- All other rows: no dependence on the held clause.

Execution honesty: `python3 -c` was attempted and is blocked in this mode; the arithmetic was executed as a script file (`python3 /tmp/c0_arith.py`) with the real output shown above. All other content is clause-routing through the inlined document; no other file was read.

R3C2_C0_EXHIBITION_COMPLETE
