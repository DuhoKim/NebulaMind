ACCESS_SHA=53fa3ab52d3cd7dbbd9e2c4283ecc5124d3a850c22ea43686b4b604651dea4f7
SEAT=kimi
CANDIDATE_D1=SOUND_WITH_REPAIRS
CANDIDATE_D7=SOUND_WITH_REPAIRS
BATCH_PREP=SOUND_WITH_REPAIRS
TOOLING_MATCHES_CLAUSE=NO
COUNTEREXAMPLE_HANDLED=YES
IMPORTED_RULE_BREAKS_PARTITION=NO
STAGED_TESTS=PASS

# R3C2 candidate REVISION 2 review — seat kimi, 2026-09-06

Adversarial, independent review of REVISION 2 of `R3C2_D1_D7_CANDIDATE_CLAUSES_20260906.md` (access proven above, digest
computed by me as the first act of this review), `R3C2_BATCH_PREPARATION_UNADOPTED_20260906.md` and the staged kit
`r3c2_staged_d1d7/`. V23 (`R3C2_V23_DESIGN_OF_RECORD_55b466fa.md`) and the living draft
(`R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md`) were read for context only and are not reviewed; their section-10
histories are not findings here. The reconciliation and the two round-1 reports were read to check the repairs (T1–T13
below). No file starting with `R3C2_GATE_` or `R3C2_C0_` was opened; no other review file was opened. Nothing in this
report adopts, freezes, installs or runs anything. All my own fixtures, drivers and outputs are under
`r3c2_staged_d1d7/_review2_kimi/` (`run_probes.py`, `attack_seedless_v2.py`, `attack_d1_nosym.py`, `work/` with
`command_log.json` recording every command, stdout and exit code). No other file was modified by me, with one mandated
exception: the required kit invocation rewrites `_ctl/` and `C6_COUNTEREXAMPLE_EXHIBIT.txt` (the README discloses this);
the pin sheet verified before and after my run, so the exhibit regenerated byte-identical.

Summary: revision 2 is a real repair of revision 1 — the decisive batch defect (T1) is genuinely fixed and executed, the
C6 completeness machinery now matches its clause in both omission directions, and my own both-seats-omit counterexample
fails and is listed exactly as the clause requires. It is not done: I exhibit two new word-for-word gaps between clause
and code (a selection that omits the seed skips recomputation and a zero-claim audit PASSes; an import whose symbol never
co-occurs with its numeral passes the D1 floor the clause asserts), one pin-sheet path regression against the mandated
verification command, and a set of stale counts in the kit's self-description (the T13 family, in the revision that
repaired T13).

## A. Pin sheet

Mandated command, run from the working directory, printed in full:

```
$ shasum -a 256 -c r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256
shasum: r3c2_ledger_tools_STAGED.py: No such file or directory
r3c2_ledger_tools_STAGED.py: FAILED open or read
shasum: r3c2_batch_tools_STAGED.py: No such file or directory
r3c2_batch_tools_STAGED.py: FAILED open or read
shasum: r3c2_staged_tests.py: No such file or directory
r3c2_staged_tests.py: FAILED open or read
shasum: README_STAGED_UNADOPTED.md: No such file or directory
README_STAGED_UNADOPTED.md: FAILED open or read
shasum: partition_12_of_89.json: No such file or directory
partition_12_of_89.json: FAILED open or read
shasum: partition_12_of_89.txt: No such file or directory
partition_12_of_89.txt: FAILED open or read
shasum: C6_COUNTEREXAMPLE_EXHIBIT.txt: No such file or directory
C6_COUNTEREXAMPLE_EXHIBIT.txt: FAILED open or read
shasum: WARNING: 7 listed files could not be read
EXIT=1
```

The sheet's paths are bare filenames, so they resolve against the CALLER's directory, not the sheet's. Run from inside
`r3c2_staged_d1d7/` the same sheet verifies 7/7 OK (exit 0), and it verifies 7/7 OK again AFTER I re-ran the kit (the
exhibit regenerates byte-identical — the kit is deterministic). Both round-1 reports show revision 1's sheet verifying
from the working directory with `r3c2_staged_d1d7/`-prefixed output lines, so the path form regressed between revisions.
Finding F1.

## B. The kit, run by me — and my own counterexample

`cd r3c2_staged_d1d7 && /usr/bin/python3 -E r3c2_staged_tests.py`. Last three lines, verbatim:

```
exhibit written: /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3c2_staged_d1d7/C6_COUNTEREXAMPLE_EXHIBIT.txt
controls=84 passed=84 failed=0
STAGED_TESTS=PASS
```

Exit 0. I counted the controls in the source: 53 `check(` calls + 27 `probe(` calls + 4 manual `results.append` = 84,
matching the printed count. The deletion probes number 27, not the 29 the documents print (finding F4).

MY OWN counterexample (fixtures written by me, driver `_review2_kimi/run_probes.py`). Source `k2paper.txt`:

```
K2 synthetic paper (review2 fixture, written by seat kimi)
As an input assumption we fix the coupling lambda = 5 for the whole study.
Our integration of the full system gives the dimensionless drift D = 0.625.
A second independent check yields E = 42.0 for the same system.
As figure 12 summarises, both are stable.
```

Line 3 ("Our integration of the full system gives the dimensionless drift D = 0.625.") is unambiguously a numeral the
paper asserts as a result of its own — §1 requires it. FORWARD case: BOTH seat ledgers enumerate only lines 4 (included,
`REPRO_NO_DERIVATION_STATED`) and 2 (excluded, `AUTHOR_SPECIFIED_INPUT`) and omit line 3; my auditor's enumeration
includes it (`a2`). Commands run, in order (full log: `_review2_kimi/work/command_log.json`):

```
audit seal-enumeration f_aud_c.json f_aud_x.json f_stage1.txt   -> rc 0 (census-gated seal, AUDITOR_CENSUS_STDOUT_SHA256=266d57d7…)
audit select f_sealed_c.json deadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef f_stage1.txt f_sel.json
                                                                -> rc 0, N=1 R=1 k=1 audited=1
audit handout f_sel.json f_sealed_c.json f_handout.json          -> fields claim_id/source_file/source_line only
audit seal-rederivation f_rd.json f_stage2.txt                   -> rc 0 (first-write)
audit compare f_stage1.txt f_aud_c.json f_aud_x.json f_sealed_c.json f_sealed_x.json f_sealed_l.json f_sel.json f_stage2.txt f_rd.json f_C6_AUDIT.json
```

Emitted completeness result and final token (verbatim):

```
FAIL: COMPLETENESS audit_included_absent_from_sealed: ['k2paper.txt', 3, '0.625']
C6_AUDIT_SAMPLE=FAIL
```

The row in `C6_AUDIT.json`: `{"key": ["k2paper.txt", 3, "0.625"], "result": "OMISSION_AUDIT_INCLUDED_ABSENT_FROM_SEALED",
"in_sealed": false, "in_audit": true, "sealed_included": null, "audit_included": true, ...}` — FAIL and listed.

REVERSE omission (sealed includes line 3 as included; the auditor's enumeration lacks it), same staging:

```
FAIL: COMPLETENESS sealed_included_absent_from_audit_enumeration: ['k2paper.txt', 3, '0.625']
C6_AUDIT_SAMPLE=FAIL
```

Row: `"result": "OMISSION_SEALED_INCLUDED_ABSENT_FROM_AUDIT"`. So COUNTEREXAMPLE_HANDLED=YES.

WORD FOR WORD against the clause — what matches: (i) the both-directions omission rule (a sealed INCLUDED passage absent
from the auditor; a passage the auditor lists, included OR excluded, that the sealed ledgers omit — each fatal): executed
by me both directions and by kit probes 1/2/5/6. (ii) The `AUDIT_INCLUSION_DISPUTED` 10% rule — strictly above 10% of the
sealed included denominator fails, at 10% it passes listed-and-counted (kit probes 3/4: 2/20 PASS, 3/20 FAIL), and the
two dispute classes match the clause (both-sides-list-dispose-differently; sealed-EXCLUDED-absent-from-auditor). (iii)
Zero sealed denominator with any passage on either side fails (kit zero control). (iv) Disputed rows carry both
dispositions (kit assertion). (v) The stage predicates — census-gated first-write stage-1 seal, select refusing without
it, first-write re-derivation seal, recomputation of a seed-carrying selection (kit negatives+probes all ok in my run).
What does NOT match, executed by me: the recomputation is skipped when the supplied selection omits `seed_hex` and a
zero-claim audit then PASSes (finding F2), and the code emits a fourth row-result the clause's vocabulary does not name,
`MATCH_KIND_DIFFERS` (finding F5). Hence TOOLING_MATCHES_CLAUSE=NO — jointly with the D1 gap F3.

## T1–T13: is every accepted round-1 repair actually in revision 2?

Evidence cited is in the documents, the code, and my own runs (kit output above; my probes; my recomputations).

| topic | verdict | evidence |
|---|---|---|
| T1 batch partition breaks §2's IMPORTED rule | REPAIRED | Batch doc §1/§3: ownership partitioned, access not — every session holds all 89 texts; lookups logged. Kit controls executed by me: the joined ledger validates against the FULL corpus (`C3_NO_SUBSTITUTION=PASS`); the same record in a batch-1-only directory FAILS (`t4.txt absent from the sources directory` / `cannot read external value line t4.txt:2`). The mechanism is in doc AND code. |
| T2 join corrupts cross-batch `derived_from` | REPAIRED | Global source-based ids; join checks ownership (`owner_of`), id collisions, unresolved references and cycles (`walk`, r3c2_batch_tools_STAGED.py:70–93); no prefixing. Kit negatives executed: dangling cross-batch parent FAILS; non-global id FAILS. |
| T3 compare narrower than the clause | REPAIRED (one wording gap, F5) | Union of keys; per-key both presences/dispositions/kinds; MATCH / three OMISSION_* / AUDIT_INCLUSION_DISPUTED; both included-omission directions fatal; auditor-excluded-absent fatal (kimi's stricter reading, adopted); sealed-excluded-absent = dispute. Executed by me (section B) and kit probes. |
| T4 zero denominator; disputed rows lack dispositions | REPAIRED | Code line 288 zero check; kit zero control + probe ok; disputed rows carry `sealed_included`/`audit_included` (kit assertion ok). |
| T5 auditor `census` PASS not enforced | REPAIRED | `cmd_audit_seal` runs census and refuses on FAIL; seal records `AUDITOR_CENSUS_STDOUT_SHA256`; kit negative + probe ok. |
| T6 selection trusted, not recomputed | PARTLY | select refuses without a stage-1 seal (executed by kit); a seed-carrying selection emptied of ids fails recomputation (kit); stage-1 first-write. BUT a selection that omits `seed_hex` skips recomputation entirely and a zero-claim audit PASSes — my executed attack, finding F2. |
| T7 source-only independence procedural | REPAIRED as stated | Tool side landed: first-write seals, `handout` (ids+file+line only — executed), `seal-rederivation` checked in compare. Clause's "What PASS means" states the custody bound and the C4 prior-exposure floor. Custody remains custody — section D. |
| T8 D1 code weaker than its claim | PARTLY | Token match, manifest-required + exact row + verified bytes, claiming-file binding through the candidate file, non-empty quotation, first-line tie-break — all present, each with a negative + probe, all ok in my run; my D1a–D1e2 executed. BUT the floor conjunct "that line is the first carrying symbol and numeral" is unchecked when NO line carries both (my executed D1h, finding F3); and the reconciliation's described tie-break variant (borrower-designated locator, ORIGIN_DISPUTED fallback) never landed — replaced by the deterministic first-line rule without disclosure (finding F8). |
| T9 the two wordings don't reach identical filings | REPAIRED | Candidate's first D1 paragraph now says: same classification, evidence records differ, the staged tool implements the second wording only. My D1f executed: a Wording-A-style record FAILS the B-only tool (claiming-file + self-file). |
| T10 D1 consequence paragraph overstates | REPAIRED | Rev 2 says ATTEMPTED, arithmetic outcome conditional, `rests_on` USES_IMPORTED unless a more severe root, "the census does not promise to follow provenance across papers" — the reviewers' required replacements in substance. |
| T11 seals bind partition/order; coverage bytes; join "no parameters"; "cannot alter" | REPAIRED | Seal binds partition digest, owned texts' verified digests, artefacts, predecessor seal; refuses out-of-order and re-seal (executed). Coverage verifies owned bytes (executed: tampered text FAILS). Batch §5 states the two policies; §4 states detection-not-prevention. |
| T12 D7/batch overclaims | PARTLY | "strongest statement" replaced with the bounded sentence; cost "runtime is unmeasured"; drift "does not identify within-reader drift"; 12 = "a workload hypothesis, not a demonstrated capacity"; engine wording fixed; F14/F15 applied. Retained/new: "a census that can run … which batch 1 will be" and "(Blocks a first run: without it, no seat completes.)" (finding F7); "Batch 1, the lightest" is false on the doc's own table (finding F6). |
| T13 stale pins and counts | PARTLY | Per-file kit hashes removed from both documents (pin sheet only); `partition_12_of_89.txt` now pinned (F16 repaired); the batch doc's printed manifest hash verified by me against the real manifest (`300d4da1…` ✓) and the partition recomputes BYTE-IDENTICAL (15b69217…, matches the pin). BUT the pin sheet's path form regressed (finding F1) and the probe/check counts are wrong (finding F4). |

No finding of either round-1 reviewer was rejected by the reconciliation; two accepted repairs landed incompletely (T6,
T8) and the T13 count family recurred.

## C. D1 — do the two wordings reach identical filings?

Constructed cases (my fixtures `work/d1src/`; borrower `k2borrow.txt` claim `k2borrow.txt#1`, input `g` not printed by
the borrower; the borrower's line 3 names the source), each run through the staged `validate` (which implements Wording
B only):

| case | source line | Wording B filing (executed) |
|---|---|---|
| D1a | "For this calculation we choose g = 9." | C3_NO_SUBSTITUTION=PASS (PRINTED/IMPORTED, evidence at the borrower's sentence) |
| D1b | "We fit g = 9 to the sample." | PASS |
| D1c | "We measure g = 9 in the apparatus." | PASS |
| D1d | "We adopt g = 9 from Kepler (1619)." | PASS |
| D1e1 | value machine-matches at TWO lines (2 and 4); FIRST line (2) filed | PASS |
| D1e2 | same source; SECOND line (4) filed | FAIL: "external value line 4 is not the first line of k2s_two.txt carrying both g and 9 (that is line 2)" |
| D1f | Wording-A-style record (evidence at the SOURCE's own line) under the staged tool | FAIL: "citing sentence is in k2s_choose.txt, but claim k2borrow.txt#1 belongs to k2borrow.txt" + "names its own file as the external source" |
| D1g | the import misfiled CHOSEN (origin CHOSEN, ORIG_CHOICE_STATED, quoting the source's "we choose" line) | C3_NO_SUBSTITUTION=PASS |
| D1h | value a numeric token at the cited line but the symbol `g` absent from the whole source | C3_NO_SUBSTITUTION=PASS — finding F3 |

Answer: on CLASSIFICATIONS (status PRINTED, origin IMPORTED, value) the two wordings reach identical filings on every
case I can construct, including all four verb forms and the two-line source (where both wordings file the first line —
the clause's tie-break). They never reach identical RECORDS: the evidence site differs by construction (the source's own
line vs the borrower's citing sentence), so `origin_evidence.source_file/source_line/verbatim` differ. Revision 2 says
exactly this ("They assign the same classification … their EVIDENCE RECORDS differ") — accurate, and it discloses that
the tool implements the second wording only (my D1f proves the disclosure true: accepting Wording A without re-staging
the tool fails every D1 record).

Which wording is safer against a seat filing CHOSEN for an import: THE V25 REVIEW'S (Wording B) — but the safety lives
entirely at the second-reader level, and the candidate should say so. At the MACHINE level the two wordings are equally
blind: my D1g misfiles the import as CHOSEN under B's own schema (quoting the source's "we choose" line through the
ordinary PRINTED branch, which has no claiming-file binding) and it PASSes. The candidate's machine-floor sentence is
about quotation genuineness ("a genuine citation of THAT value … caught by the second seat and by C6's
re-classification, never by the machine"), not about reason-code substitution; the master draft already carries the
precedence floor for that ("never by the machine"). At the second-reader level B is strictly safer: under B the correct
filing's evidence is the BORROWER'S sentence, which names an external source, so C3's precedence (ORIG_CITATION first —
"a sentence naming an external source for the value is a citation whatever else it says") makes the second seat's and
C6's re-classification to IMPORTED mechanical; under A the correct filing and the misfiling quote the SAME source line,
and telling them apart needs the clause's special exemption that no reason code is applied to the source's line at the
borrower — a seat-applied, machine-uncheckable carve-out A must carry and B does not need. Two seats misfiling
identically escape under either wording — the shared-reader-error floor the candidate states. The recommendation of B is
right; the mechanism above is why.

Residual D1 defect: F3 (the zero-symbol-match pass) — the one machine-floor conjunct the code does not check.

## D. D7 — is source-only enumeration before ANY ledger exposure enforced?

Only by the dispatch layout, and the clause now says so. What the staged procedure enforces in CODE is fixity and
intra-tool order: the enumeration is frozen at the first-write stage-1 seal and compare fails any later change
(`C6_STAGE_ORDER`, kit probe ok); the re-derivations are frozen at a first-write stage-2 seal and compare fails any
later change (kit probe ok); `select` refuses when no stage-1 seal file exists (kit probe ok); the selection is bound to
the seal and recomputed from the sealed candidates and the seed — except when the seed field is absent (finding F2).
What nothing in the kit enforces is the TEMPORAL precedence itself — that the enumeration and the re-derivations were
committed BEFORE the auditor saw any sealed ledger. The seals carry no timestamp and no external receipt; compare checks
that present bytes match the seals, not when the seals were written.

Every way an auditor could see a sealed ledger before its stage-1 seal that the design does not close, classified:

1. The custodian runs `audit seal-enumeration` LATE (after exposure). The tool cannot tell. CUSTODY — the clause states
   the bound ("the dispatch record — inventoried and access-proven like the seats' — fixes WHEN, relative to release").
2. The seal FILE is deleted and re-created after exposure (first-write only stops the TOOL overwriting an existing file;
   it does not stop deletion-plus-rewrite, and a re-written seal over a post-exposure-modified enumeration is
   self-consistent at compare). CUSTODY — closed only if the dispatch record receipts each seal's digest at its creation
   event; the clause gestures at that record but does not require the receipt explicitly. The one design-level addition
   still available short of custody: require it in the clause.
3. The auditor is dispatched with the sealed ledgers (or excerpts) inside its working directory, prompt, attachments or
   tool responses. CUSTODY — the clause requires "no seat candidate, exclusion, input or outcome ledger in its dispatch
   inventory"; no auditor-specific inventory or access-proof control is staged (the seats' C4 floor is self-reported).
4. Reused/resumed auditor context, an earlier session of the same engine, or training exposure to the lane's committed
   tally. CUSTODY — the clause states it: "prior exposure cannot be excluded — the same floor C4 states for the seats."
5. The custodian hands the auditor the selection JSON (which exposes the groups, N, R, k) instead of the ids-only
   handout. The tool emits the right artefact (`audit handout`, executed: claim_id/source_file/source_line only);
   release discipline is the custodian's. CUSTODY.
6. Sealed ledgers released BEFORE `seal-rederivation` is run (the stage-2 seal proves the re-derivations' bytes at
   sealing, not that sealing preceded release). CUSTODY — the clause's PASS-meaning bounds it.
7. Runtime surfaces — logs, shared mounts, inherited handles, connectors/network responses, environment/startup
   content. CUSTODY — no staged probes; T7's reconciliation assigns the inventory/probe record to the run plan, not the
   clause.

None of 1–7 is a clause defect in revision 2: the clause's "What PASS means" states exactly this bound, which is the
repair the round-1 reviews demanded (T7). The stronger-vs-weaker comparison the candidate makes is CORRECT: under the
weaker version a both-seats-missed passage is structurally invisible to an anchored auditor; under the stronger my
section-B run finds it and fails the census. The "LESS LIKELY … direction only — no magnitude has been measured" hedge
is honest.

## E. Batch preparation — does the partition argument now make ONE census with ONE denominator?

The rev-1 attack ("the IMPORTED rule reads a named source's line in ANOTHER paper, which may sit in a DIFFERENT batch")
is ANSWERED in revision 2 by re-scoping what a batch is: OWNERSHIP of candidate passages is partitioned; ACCESS to
evidence is not — every session holds all 89 pinned texts, enumerates only its owned texts (which is what bounds the
reading load), and may look up any manifest text for §2/D1 with the lookup logged. Executed by me via the kit: a batch-1
claim whose IMPORT evidence is a batch-2 line joins cleanly and the joined ledger validates against the FULL corpus
(`C3_NO_SUBSTITUTION=PASS`); the same record in a directory holding only batch 1's texts FAILS — the failure mode the
repair exists to eliminate, exhibited as a control.

Attacking "per-passage rule, therefore union of partition = whole" in revision 2:
- §1's inclusion rule is per passage — granted, and ownership is disjoint and exhaustive under `C1B_BATCH_COVERAGE`
  (executed: ownership-once, owned-bytes and access-proof checks pass; the duplicate/missing/tampered negatives fail).
- The cross-text rules I can name: §2's IMPORTED rule (the named source's value line in another paper) and C3's pair
  rule (the citing sentence at the claiming paper — same text as the claim, owned; the value line at the source —
  cross-text, served by full access); `derived_from` edges across claims (resolved globally at join, cycle-checked).
  Cross-PAPER consistency rules: none exist in §1/§2 (both round-1 reviewers found none; I found none either). The one
  rule that broke the partition in rev 1 is now served by access-not-partitioned, and the joined full-corpus validate is
  the machine-match point (executed). So IMPORTED_RULE_BREAKS_PARTITION=NO for revision 2.
- The argument's own honesty is now right: "What the mechanism establishes is one joined denominator; calling it one
  independently completed census additionally needs the reading, source-access and reconciliation evidence of the run
  record", and the within-reader-drift loss is stated plainly with no measured-consistency claim.
- The real partition recomputes BYTE-IDENTICAL from the real manifest (my run; sha256 15b69217…, matching the pin
  sheet), and the manifest hash printed in the document verifies (`300d4da1…`). Batch sizes: 89 = 5×8 + 7×7, sizes
  differ by at most one ✓.

Residual batch findings (none touches the denominator mechanism; each is in F): the "Batch 1, the lightest" factual
error (F6); the two retained overclaims (F7); the id-uniqueness join checks (candidate_id and input_id collisions) have
no negative control and no deletion probe — `PROBE:ID_COLLISION` is the one marker in the tools with no probe call (F4);
and the per-lookup logging rule ("every such lookup is logged with manifest digest, file and line") is bound by no seal
and exercised by no control — it is a dispatch-record promise and §1/§4 should say so in words (folded into F7's
replacement demand).

## F. Findings (verbatim quote — defect — exact replacement — consequence)

F1. Quote: the mandated verification — "run `shasum -a 256 -c r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256` from the working
directory (paths in the sheet are relative to it)"; the sheet lists bare filenames.
Defect: from the working directory all seven entries report "FAILED open or read", exit 1 (printed in section A); from
inside `r3c2_staged_d1d7/` the sheet verifies 7/7 OK. Both round-1 reports show revision 1's sheet verifying from the
working directory with `r3c2_staged_d1d7/`-prefixed lines — the path form regressed between revisions.
Exact replacement: write the sheet's paths relative to the working directory (`r3c2_staged_d1d7/<file>`), as revision 1
had them — or state the verification CWD in the README and in the dispatch text.
Consequence: a verifier following the prescribed command concludes the pinned kit is unreadable or tampered; the
stale-copy alarm trips on itself. No census-conclusion impact; trust-in-pins impact.

F2. Quote (clause): "Only then are the sealed (merged) candidate, exclusion and input ledgers opened to the comparison
(`audit compare`), which recomputes the selection from the sealed candidates and seed and fails on any disagreement".
Defect (EXECUTED): the recomputation is guarded by `if all(k in S for k in ("seed_hex",))`
(r3c2_ledger_tools_STAGED.py:264). A selection file that OMITS `seed_hex` — retaining the correct sealed-candidates and
stage-1-seal digests (both computable by anyone holding the files), with `audited_ids`/`sampled_ids` emptied and `k`
zeroed — is never recomputed. My attack: seals all matching, `audit compare` exits 0 and writes
`C6_AUDIT_SAMPLE=PASS` with `audited = {}` — a zero-claim audit PASSes, while the honest selection over the same sealed
candidates and the same seed assigned `N=1 R=1 k=1 audited=1`. The kit's "selection emptied with the digest retained"
negative retains the seed and is caught; the absent-seed variant has no control. A supplied selection without the seed
is not detected as a disagreement: there is no seed to recompute from, and the code does not refuse.
Exact replacement (code): in `cmd_audit_compare`, require the selection to carry a well-formed `seed_hex` (64 lowercase
hexadecimal) and FAIL otherwise ("C6_SELECTION: no external seed in the selection; recomputation impossible"); add the
negative and its deletion probe.
Consequence: as staged, C6's frozen-frame guarantee is voidable from the custodian side — an audit that audited nothing
reports PASS, and `CENSUS_COMPLETE` rests on `C6_AUDIT_SAMPLE=PASS`. Until repaired, the audit token is attainable
without the audit.

F3. Quote (clause, machine floor): "`validate` checks: … the value is a numeric token at the cited source line and that
line is the first carrying symbol and numeral."
Defect (EXECUTED): when NO line of the source carries both the symbol and the numeral, `first_line_with` returns None
and the first-line check is skipped (r3c2_ledger_tools_STAGED.py:142–143). My D1h: the source contains `9` only at a
symbol-free line ("The total is 9 on the ninth page."), the symbol `g` nowhere; the record validates
(`C3_NO_SUBSTITUTION=PASS`). The clause's floor asserts a check the code does not perform in the zero-match case; an
import can cite a numeral that appears in the source only incidentally — machine-accepted provenance for the wrong
quantity.
Exact replacement (code): FAIL when `first_line_with` returns None ("no line of <source> carries both <symbol> and
<value>; the cited line cannot be the first") and add the negative + deletion probe. (The multi-match conjunct itself
works: my D1e2 fails with the exact first-line message.)
Consequence: the D1 machine floor is one conjunct short of its own statement; the catch for this class is pushed back
to the second seat and C6 re-derivation — the second-reader floor the clause enumerates, but one its floor sentence
claims is machine-checked.

F4. Quotes: "29 deletion probes, each showing the matching negative turns to PASS when its check is neutralised"
(candidate, test-run bullet); "29 deletion probes" (README); "Deletion probes for each of the eight load-bearing checks"
(candidate, D1 bullet); "checks id uniqueness across batches" (batch doc §5).
Defect (COUNTED and executed): the kit has 27 `probe(` calls (7 D1 + 10 D7 + 10 batch), all passing in my run
(`controls=84 passed=84 failed=0` = 53 checks + 27 probes + 4 manual assertions). The D1 bullet's "eight" has seven
probes. The join's candidate_id-collision check (`PROBE:ID_COLLISION`, r3c2_batch_tools_STAGED.py:80) and the
input_id-collision check (lines 82–84) have NO negative control and NO deletion probe — `ID_COLLISION` is the only
probe marker in the tools with no probe call.
Exact replacement: print "27 deletion probes" and "seven load-bearing checks" — or better, add the two missing controls
(the cross-batch id-collision negative + probe; a dedicated probe for the eighth D1 check — the self-file or
source-bytes check) so the printed numbers become true.
Consequence: the id-uniqueness join policy that T2's repair relies on is asserted but never exercised; a regression in
it is invisible to the kit. This is the T13 stale-count family, present in the revision that repaired T13.

F5. Quote: "and a result: `MATCH`, `OMISSION`, or `AUDIT_INCLUSION_DISPUTED`".
Defect: the code emits a fourth row-result the clause does not name — `MATCH_KIND_DIFFERS`, for a both-excluded passage
whose exclusion kinds differ (r3c2_ledger_tools_STAGED.py:285). Harmless in effect (not fatal, not counted as a
dispute, both kinds carried on the row), but the clause's vocabulary does not provide for it.
Exact replacement (clause): "a result: `MATCH`, `OMISSION`, `AUDIT_INCLUSION_DISPUTED`, or `MATCH_KIND_DIFFERS` (both
sides excluded, kinds differ; reported, never fatal, never a dispute)" — or have the code emit `MATCH` and let the
kind fields speak.
Consequence: a small describe-vs-compute mismatch of the family this lane keeps filing; no census-conclusion impact.

F6. Quote: "Batch 1, the lightest, therefore confirms less about capacity than a heavy batch would."
Defect: false on the document's own printed table — batch 1 (4030 non-blank lines, 278483 bytes) is heavier than batch
9 (3799 lines, 198317 bytes) and batch 11 (3321 lines, 197654 bytes) on both measures. The design point (a light first
batch confirms little) stands; the superlative is wrong.
Exact replacement: "Batch 1 is light but not the lightest (batches 9 and 11 are smaller on both measures); a light
first batch confirms less about capacity than a heavy one would — a heavier first batch is the stronger pilot, at a
higher risk of the failure it is testing for."
Consequence: none to the mechanism; the printed superlative would be quoted.

F7. Quotes: "It is the difference between a census that can run and one that cannot — a necessity established by one
observed failure, not by a demonstrated successful session, which batch 1 will be."; "(Blocks a first run: without it,
no seat completes.)"
Defect: the first asserts the batched census "can run" — undemonstrated, as the document itself states ("Twelve is a
workload hypothesis, not a demonstrated capacity") — and asserts batch 1 "will be" a demonstrated successful session, a
prediction stated as fact. The second elevates one observed seat death (at eleven texts) into "no seat completes" the
89, and this plan is not shown to be the only remedy.
Exact replacements: "It is the difference between a census that could not run (one observed failure) and one that
might — whether it can run is what batch 1 is designed to test."; "(Blocks a first run on the evidence of the limb-B
death finding: the unbatched seat died; whether this batching is the remedy is what batch 1 tests.)"
Consequence: the necessity/feasibility framing overshoots the evidence by one observed failure in each direction; the
run record should not inherit these as established.

F8. Quote (reconciliation, T8 repair text): "clause adds the multi-occurrence rule (the locator the borrower
designates; otherwise the first line carrying symbol and numeral, the others listed; seats disagreeing →
ORIGIN_DISPUTED as already defined)."
Defect: the landed clause carries only "the seat files the first line carrying both the symbol and the numeral;
`validate` fails any other line" — the borrower-designated locator and the ORIGIN_DISPUTED fallback never landed. The
landed rule is simpler and strictly deterministic (no seat discretion), so it is arguably the better rule; but the
candidate's header claim "every accepted repair is in this revision and in the staged kit" is inaccurate for the
described T8 form, and the substitution is nowhere disclosed.
Exact replacement: add one sentence to the candidate's D1 section: "The reconciliation's designated-locator variant was
replaced by the deterministic first-line rule: no seat discretion, no ORIGIN_DISPUTED case, no others-listed
requirement."
Consequence: documentation accuracy only; the landed rule is stricter than the described one.

## What verified clean in revision 2 (tested, not asserted)

- The pin sheet verifies 7/7 from inside the kit directory, before and after my kit run; the exhibit regenerates
  byte-identical (deterministic kit). `STAGED_TESTS=PASS`, `controls=84 passed=84 failed=0`, run by me.
- My own both-seats-omit counterexample FAILs and is listed (`audit_included_absent_from_sealed`), and the reverse
  FAILs and is listed (`sealed_included_absent_from_audit_enumeration`) — the clause's both-directions rule, executed
  on my files.
- The 10% dispute boundary, the zero-denominator fail, both-dispositions rows, census-gated first-write stage-1 seal,
  select's seal requirement, re-derivation seal, seed-carrying selection recomputation — all ok in my kit run, each
  with a deletion probe proving the check is load-bearing.
- D1: the four verb forms validate identically (PRINTED/IMPORTED); the first-line tie-break fails a second-line filing
  with the exact message; the claiming-file binding fails a Wording-A record — the candidate's "tool implements the
  second wording only" disclosure is true.
- Batch: the real partition recomputes byte-identical from the real manifest; the printed manifest hash verifies; the
  joined cross-batch import validates against the full corpus and fails in a one-batch directory; seal order,
  re-seal refusal, predecessor binding, join determinism, coverage positives and all ten batch negatives verify.
- The T1 repair is real; the candidate's consequence paragraphs (D1, D7, batch) are now bounded in the way the round-1
  reviews demanded; the stronger-vs-weaker D7 verdict is correct.

## Rulings requested of the principal, restated as tested

- D1: accept WORDING B (safer against a CHOSEN-filed import at the re-classification level, section C; the machine is
  equally blind under both, executed). Before installation, repair F3 (the zero-symbol-match floor conjunct) or restate
  the floor to exclude it; disclose F8. If Wording A is ever preferred, the validate rule must be re-staged first
  (executed: A-style records fail the B-only tool).
- D7: accept the stronger version AFTER F2 is repaired (the absent-seed recomputation bypass — a zero-claim audit
  currently PASSes) and F5's vocabulary is aligned; the custody floor is already stated correctly.
- Batch: acceptable in mechanism after the T1 repair (executed); repair the wording (F6, F7) and add the missing
  id-collision controls (F4) before adoption.
- Kit/documents: re-issue the pin sheet with working-directory-relative paths (F1); correct the probe counts (F4).

Nothing in this review adopts, freezes, installs or runs anything. All fixtures and outputs are mine, under
`r3c2_staged_d1d7/_review2_kimi/`; the candidate documents, the staged kit's pinned files and every other file are
untouched (the mandated kit run's own rewrites of `_ctl/` and the exhibit are disclosed above and re-verified).

R3C2_CANDIDATE_REVIEW2_kimi — UNADOPTED-REVIEW
