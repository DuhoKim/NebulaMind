ACCESS_SHA=5c69ae471edb1b1970594255e970fb07274fbb814dce1096de81c9d647ad1a5b
PACKET_SHA=e265d3f8e96454a9297f205f6a964dd2273b7d07770644ab003578cc3ff9bdbc
GATE=PREREG_UNSOUND
LEAK=The packet reveals that the lane has an expectation and that an unreproduced result is consequential while a favourable audit sample is possible, although it does not identify the comparison model or the expected quantitative pattern.

## 1. Outcome classes

The per-claim outcomes are exhaustive and mutually exclusive because the terminal-condition precedence resolves genuine overlaps. A claim with no stated procedure and an absent input satisfies the prose of both conditions, but the declared order files `REPRO_NO_DERIVATION_STATED`. A claim with a stated procedure, a needed absent input, and an unobtainable supposed external source files `REPRO_INPUT_ABSENT` before `REPRO_BLOCKED`. A completed attempt then files exactly one of `REPRO_EXACT` and `REPRO_FAILED`.

**Q1 — DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?** Yes at the per-claim level. Hardest cases:

- Printed-but-chosen input: the paper prints `β = 1/929.25` and says it chose that value. The record is `status=PRINTED`, `origin=CHOSEN`, with a matching choice quotation. The arithmetic consumes it. If the displayed result follows within stated precision, exactly `REPRO_EXACT` is filed and the script must compute `rests_on=USES_CHOSEN` (assuming no more severe root); otherwise exactly `REPRO_FAILED` and the same `rests_on` are filed.
- Stated fitted parameter: `status=PRINTED`, `origin=FITTED`; it is consumed, giving exactly one arithmetic outcome and `rests_on=USES_FITTED` absent a more severe root.
- Imported value printed and directed for use: `status=PRINTED`, `origin=IMPORTED`; it is consumed, giving exactly one arithmetic outcome and `rests_on=USES_IMPORTED` absent an undeclared root. If the value is merely attributed to another work and is not the paper's own derived claim, the candidate-claim rule excludes that passage, but the same printed value can still be an input to another included claim.

Each non-arithmetic outcome is also constructible: no procedure; absent input; stated external input that cannot be obtained; or a 120-second/machinery stop. `INCONCLUSIVE` is genuinely reachable through any included claim with one of those outcomes, which makes `CENSUS_PARTIAL` apply.

The study-level classes are not mutually exclusive, however. Quote: **“`CENSUS_COMPLETE` — every included claim carries exactly one outcome from the arithmetic group of §3.”** Quote: **“`CENSUS_AUDIT_FAILED` — the audit of §6 cannot reproduce a sampled per-claim outcome.”** A real tally in which every claim initially has `REPRO_EXACT` or `REPRO_FAILED`, followed by an audit that cannot reproduce one sampled outcome, satisfies both definitions. Likewise, a denominator dispute can coexist with a control failure, and an audit failure can coexist with an origin dispute. Only `CENSUS_PARTIAL` is expressly given precedence over `CENSUS_COMPLETE`; there is no rule saying exactly one study-level class is filed or resolving the other overlaps.

**Exact replacement:** append to §4: **“Exactly one study-level outcome is filed. Apply the first applicable class in this order: `R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`. Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`.”** If a different substantive precedence is intended, declare it now; the essential repair is a total precedence with exactly-one filing.

## 2. Controls

Several controls lack the promised three-valued exact code. Quote: **“Controls in an unreached limb are `NOT RUN`, never passes.”** This conflicts lexically with C3's `NOT_RUN`, and C0, C1, C2, C4 packet, C4 pattern blind, C5, C5b, and C6 print only a `=PASS` form. A runner cannot file the required failure or unreached spelling uniformly.

**Exact replacement:** replace the concluding code on every control with `CONTROL_NAME=PASS|FAIL|NOT_RUN`, including `C0_REACHABILITY`, `C1_DENOMINATOR_PRINTED`, `C2_INPUT_LEDGER`, `C4_PACKET_REDACTED`, `C4_PATTERN_BLIND`, `C5_HARNESS_PINNED`, `C5B_NO_CROSS_LANE`, and `C6_AUDIT_SAMPLE`; replace “`NOT RUN`” with “`NOT_RUN`”.

C0 is internally inconsistent. Quote: **“The exhibitions are authored by a seat and only verified by Tori.”** The operative header says C0 is by two pattern-blind seats who must agree. Tori-only verification permits the expectation-holder to pass reachability.

**Exact replacement:** **“The exhibition is authored independently by one pattern-blind seat and independently verified by a second pattern-blind seat; both must return `C0_REACHABILITY=PASS`. Tori checks only that every declared outcome and refuting condition has a row and does not judge reachability.”**

C1 and C2 can be passed by printing/assertion alone: neither names a validator or independent check. C4 pattern blindness and C5b similarly accept a self-reported path list. C5 at least requires live command output, and C6 requires independent re-performance.

**Exact replacement for C1/C2:** **“A named validator, `r3c2_validate_ledgers.py`, exits zero only if the candidate, exclusion, attempt, and input ledgers satisfy the frozen JSON schema and the printed counts equal recomputed counts; print its command, stdout, exit status, and input hashes. `C1_DENOMINATOR_PRINTED` and `C2_INPUT_LEDGER` are `PASS` only on exit zero.”** The schema and script must be delivered before freeze.

**Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?** No. The location and verbatim text are machine-checkable, and a script can mechanically map a seat-selected reason code to an origin. The semantic choice of reason code is still asserted. Test record: a line says “we set β=1/929.25” and the seat records `origin=FITTED`, `reason_code=ORIG_FIT_STATED`, with that exact line as `verbatim`. The quotation matcher passes because the text exists; the reason-code mapper returns `FITTED`; neither mechanism determines that “set” denotes a choice rather than a fit. A second initial seat may disagree, and C6 may catch it by reclassification, but C6 audits all filed reproduced claims and only a sample of the remainder, so this wrong plausible record is not guaranteed to be examined if its claim is non-reproduced/non-arithmetic. Even a full audit remains an independent human classification, not a machine assertion.

**Exact replacement:** replace **“`origin` must be cited, not asserted”** with **“Every `origin` is a human classification supported by machine-matched evidence; it is independently classified by both blind seats.”** Then require comparison of every input record before arithmetic: **“Any origin disagreement is retained as `ORIGIN_DISPUTED`; no claim depending on it proceeds to an undisputed `rests_on` tally. The script checks quotation identity, allowed reason-code/origin pairs, graph closure, and computed `root_origins`/`rests_on`; it does not claim to validate the quotation's semantics.”**

## 3. Circularity

Packet-only Q3 found three leaks:

1. **“for any condition whose failure would refute this lane's own expectation”** reveals that an expectation exists and that at least one outcome is directional evidence against it.
2. **“the class in which a result unreproduced from the stated inputs is both consequential and invisible”** reveals that an unreproduced result is consequential and lets a seat infer that a filed reproduction can conceal an outcome adverse to the lane.
3. **“until a favourable sample appeared”** reveals that audit samples can be favourable or unfavourable to the lane, hence that the lane has a preferred evidentiary direction.

The packet does not disclose ΛCDM by name, the magnitude/shape topic, or the exact mapping from the two tallies to the hypothesis. Thus I cannot reconstruct the comparison model or precise expected pattern from the packet alone, but I can reconstruct the existence of a stake and the favorable/adverse direction around reproduction audit failures. That fails a strict “stake of each outcome is unavailable” blind.

The expectation can therefore reach subjective inclusion and origin classification through motivated judgment. A contrary claim cannot be excluded quietly by one seat: two enumerations, zero-tolerance disagreement, full candidate/exclusion audit, and printed exclusions are meaningful safeguards. But two seats exposed to the same leaked stake can agree on a marginal “paper's own result” exclusion, and the third seat receives the same leaking packet. Origin classification is also interpretive and directly controls the later `rests_on` tally.

**Exact replacement:** remove from the seat packet the three quoted clauses or neutralize them as follows: (i) C0: **“and every declared condition”**; (ii) C6: **“(i) every claim in the arithmetic group, and (ii) …”**; (iii) delete the parenthetical history about a “favourable sample.” Keep rationale only in the master inside `SEAT-REDACT` spans.

## 4. The blind

The redaction is described as machine-asserted, but the delivered packet falsifies the described assertion. Master quote: **“asserts that no string on a forbidden list survives anywhere in the output — … the names of the people in the custody chain.”** Packet quote: **“The exhibitions are authored by a seat and only verified by Tori.”** Section 7 makes Tori part of the custody chain. Therefore either the forbidden list does not contain every custody-chain name as claimed, or the assertion could not have passed. The packet also leaks the stake as shown in §3 of this report.

**Exact replacement:** add every custody-chain name, including `Tori`, to the builder's forbidden list; mark the C0 verifier sentence for redaction and replace it in the packet with the two-blind-seat neutral procedure. Add exact forbidden phrases for `expectation`, `refute`, `consequential`, `invisible`, `favourable`, and `unfavourable`, with a required deletion probe for each.

The C4 mechanism does not enforce all it claims. Quote: **“That is enforceable, and it is the control.”** Merely running from a copied directory and pointing a wrapper at it does not establish a filesystem deny rule; the document itself says the wrapper's `--add-dir` grants rather than restricts. The path list remains self-report and cannot prove that unprinted paths were not opened.

**Exact replacement:** **“Dispatch each seat in a filesystem sandbox whose readable roots are exactly the copied seat directory plus enumerated runtime dependencies. Save the sandbox policy and an OS-level file-access trace; `C4_PATTERN_BLIND=PASS` requires zero reads outside the allowlist. A printed path list is secondary evidence only.”** If that facility is unavailable, replace “enforceable” with **“procedural but not enforceable”** and do not label C4 a machine control.

The stated limitation concerning training or earlier-session exposure is honest and should remain.

## 5. The seal

The receipted relay binds the tally and protocol before the comparison is filed, but it does not bind the protocol before limb A, which is the step it must precede to show it was not adapted to the observed tally. Quote: **“The interpretation step lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which … is itself fixed and committed before limb A begins, so it cannot be written to suit the tally.”** Yet the operative relay begins: **“Before the interpretation protocol is opened, Tori commits the tally, then sends four digests … to Blanc.”** By then the tally exists. Section 7 itself correctly says a local commit in Tori's custody can be amended or reset. Thus the word “so” is unsupported: the receipt proves existence before comparison, not fixation before evidence.

**Exact replacement:** **“Before limb A begins, Tori commits the interpretation protocol and sends its hash and commit id to Blanc. Limb A may begin only after Blanc acknowledges and timestamps those two values in receipt P. After the tally is committed and before the protocol is opened, Tori sends the tally hash and commit id to Blanc and obtains receipt T. Blanc then independently verifies the protocol against receipt P and the tally against receipt T. The interpretation report prints both receipts and all four verified values. Any missing receipt or mismatch files `CENSUS_AUDIT_FAILED`; interpretation is `NOT_RUN`.”**

## 6. Fairness

The operative negative reproduction outcome uses the required phrase: **“unreproduced from the stated inputs,” not “error.”** `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`, `REPRO_NO_DERIVATION_STATED`, and `REPRO_INPUT_ABSENT` state procedural conditions rather than accusing the paper. No operative negative clause calls a discrepancy an error.

One label overstates its definition. Quote: **“`REPRO_EXACT` — the paper's number follows, within its own stated precision.”** “Exact” asserts identity while the definition permits a tolerance interval.

**Exact replacement:** rename `REPRO_EXACT` everywhere in operative sections and scripts to `REPRO_WITHIN_STATED_PRECISION`. This is not cosmetic because it changes the class filed and removes an overclaim; its numerical test is unchanged.

## 7. Stall / executability

**Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?** Yes.

Checks performed without opening any file beyond the packet and master:

- Ran the literal C5 commands. Results: `Python 3.9.6`; SymPy `1.14.0`; `/usr/bin/python3` SHA-256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`. The commands execute.
- Metadata existence check: `r3c2_build_seat_packet.py` exists.
- Metadata existence check: root `R3C2_SEAT_PACKET.sha256` exists; `r3c2_seat_packet/R3C2_SEAT_PACKET.sha256` does not. The prose names the pin only by basename, so the existing root path is a plausible intended path, but the exact path should be stated.
- Metadata existence check: `R3C2_INTERPRETATION_PROTOCOL_20260904.md` exists.
- Metadata existence check: `nm_referee_dispatch.sh` is missing at the only delivered working-directory path a seat is instructed to use.
- The seat packet exists and its computed hash is the `PACKET_SHA` above.

Unexecutable specifications:

1. Quote: **“a script computes `root_origins`”** and **“The same script computes each claim's `rests_on`.”** No script name, command line, input/output schema file, cycle handling, missing-parent behavior, or exit-code contract is given. This central option-(c) computation cannot be executed from the preregistration as delivered.
2. Quote: **“A script asserts that every value used appears in the ledger …”** Again the script and invocation are unnamed.
3. C6 says `rests_on` is recomputed **“by the same script”**; because that script is unidentified, the audit cannot invoke a pinned implementation.
4. Quote: **“the seat brief and the pinned sources”** does not name the brief, source manifest, source paths, or hashes. A seat cannot determine the census corpus or prove completeness from this document/packet.
5. Quote: **“third seat via `nm_referee_dispatch.sh`”** names a script that is absent at the instructed working-directory path.
6. Quote: **“one-page check sheet”** and **“Kimi arithmetic with a no-fallback control”** name no artefact, command, exact code, or executable rule.
7. The builder is named but no literal build command, arguments, output path contract, or required-content assertion output is specified. Its mere existence does not make C4 reproducible by a seat.
8. `random.Random(seed).sample(sorted(claim_ids), k)` is executable only after the seed serialization/type and eligible “remaining” frame are fixed. A decimal string, hexadecimal string, bytes, and integer seed can produce different samples; claims already included in limb (i) must explicitly be removed before computing limb (ii).

**Exact replacement:** add an operative “Delivered artefacts and commands” table containing exact relative paths and SHA-256 digests for the source manifest, every pinned source, seat brief, check sheet, builder, ledger validator/provenance script, dispatch script, and interpretation protocol. Give literal commands and expected exit-code meanings. Define the audit seed as, for example, **“the custodian supplies 64 lowercase hexadecimal characters; set `seed_int=int(seed_hex,16)` and sample `remaining_ids=sorted(set(all_included_ids)-set(all_arithmetic_reproduced_ids))` using `random.Random(seed_int).sample(remaining_ids,k)`.”** Deliver the missing `nm_referee_dispatch.sh` and the named C3 validator before freeze.

These are substantive defects: they change class uniqueness, control passability, blindness, custody timing, and the ability to compute the settled second tally. They are not cosmetic.

R3C2_V10_GATE_COMPLETE
