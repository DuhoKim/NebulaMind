ACCESS_SHA=5c69ae471edb1b1970594255e970fb07274fbb814dce1096de81c9d647ad1a5b
PACKET_SHA=e265d3f8e96454a9297f205f6a964dd2273b7d07770644ab003578cc3ff9bdbc
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=The packet alone lets a seat infer that the lane's conclusion rides on the rests_on provenance tally (packet: "The interpretation step reads rests_on, never the reproduction verdict"), that DERIVED_ONLY is the clean pole of a graded severity order, that the corpus is cosmology (the Planck 2018 baseline is the only STANDARD list), and that the lane has an expectation (C0: "any condition whose failure would refute this lane's own expectation") — the channel and the existence of an expectation leak; the comparison target does not.

Seat: kimi (fresh adversarial referee, V10). Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md. Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md (read FIRST, before the master, per the brief). No other file opened for content; existence checks were metadata-only. Section 10 treated as history; §3 judged as written under option (c).

═══════════════════════════════════════════════
Q3 (PACKET ALONE, BEFORE THE MASTER) — WHAT THE LANE EXPECTS OR HOPES
═══════════════════════════════════════════════
I read the packet first and reconstructed the stake before opening the master. Every inference, with the leaking packet sentence:

1. The conclusion will be drawn from provenance, not from whether arithmetic works. Packet §3: "The interpretation step reads `rests_on`, never the reproduction verdict (§7)." A seat now knows which field carries the weight downstream.
2. There is a virtue gradient with a clean pole. Packet §3: "`DERIVED_ONLY` when every root origin is `DERIVED` or `STANDARD`; otherwise the most severe root origin present, in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` > `USES_FITTED` > `USES_CHOSEN`". Derived-only is best; undeclared is worst.
3. The corpus is cosmology. Packet C3's closed STANDARD list is exactly the Planck 2018 TT,TE,EE+lowE+lensing baseline ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline"). Any corpus paper using a different H₀/Ω will file CHOSEN or IMPORTED, so the packet pre-shapes a rests_on distribution tilted away from DERIVED_ONLY.
4. The lane has an expectation — stated outright. Packet C0: "and for any condition whose failure would refute this lane's own expectation". The expectation's content is not stated.
5. The lane once wanted a whole outcome class for chosen-input dependence. Packet §3: "(REPRO_AFTER_CHOICE — RETIRED at V10 ... What it recorded — that the number rests on a chosen, fitted, imported or undeclared input — is now the rests_on field ...)". The interest persists as the tally.
6. Failures are anticipated and their wording policed. Packet §3: "Wording: 'unreproduced from the stated inputs,' not 'error.'"
7. Provenance ambiguity is itself an anticipated finding. Packet §4: "if two blind readers cannot agree from the paper's own text what a number's provenance is, that is a finding about the corpus".
8. A comparison exists but is withheld by design. Packet header: "Sections concerning why the study exists and what its results will be compared against have been removed deliberately and mechanically", plus the term "pattern-blind" (×3) — a seat learns that a pattern and a comparison exist, with a non-inference instruction.

Reconstructed stake: the census is built to show that corpus numbers reproduce mechanically while resting on chosen, fitted, imported or undeclared inputs — "the arithmetic works, but the numbers are not derived." I could NOT infer the comparison target (threshold, prior lane result, or pattern content). The blind holds on the target; the channel (rests_on) and the existence of an expectation leak.

═══════════════════════════════════════════════
1. OUTCOME CLASSES (+ Q1)
═══════════════════════════════════════════════
Exhaustiveness (per claim): every included claim reaches exactly one outcome — no equation → REPRO_NO_DERIVATION_STATED; an ABSENT input → REPRO_INPUT_ABSENT (attempt ends, §2); arithmetic over PRINTED/STANDARD completes → REPRO_EXACT/REPRO_FAILED; cap/machinery → REPRO_NOT_EVALUABLE. The precedence sentence resolves genuine co-occurrence (absent + unobtainable-source; timeout + partial result).

Mutual exclusivity (per claim): holds as written, via the precedence order — with ONE fatal exception, FINDING 1 below.

FINDING 1 (REQUIRED) — REPRO_BLOCKED is unreachable as written.
Quotes: "**`REPRO_BLOCKED`** — an input traces to a source **outside this lane that we cannot obtain**." ; precedence: "`REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`, then the **arithmetic group**." ; the note: "*(Precedence is stated because these conditions genuinely co-occur — an absent input whose source is also unobtainable satisfied two classes with no rule to choose between them.)*"
Defect: §2 step 3 makes status PRINTED ("given in the paper"), STANDARD (closed list), else ABSENT. C3 requires a PRINTED value to machine-match text at its cited line — a citation line contains no value, so a cited-but-unprinted input is ABSENT. Any input that could "trace to a source we cannot obtain" is therefore ABSENT, and the precedence note's own example ("an absent input whose source is also unobtainable") files it REPRO_INPUT_ABSENT. A non-ABSENT input is consumed by §2 step 4 — nothing blocks. BLOCKED's exclusive domain is empty: no input files it without first satisfying INPUT_ABSENT, which precedes it. Same declared-versus-actual family C0 found in REPRO_AFTER_CHOICE; V10 retired that class and left this one. C0's own rule — "An outcome for which no such input can be exhibited is UNREACHABLE, and this preregistration does not freeze until it is" — then forbids freezing.
Exact replacement (REPRO_BLOCKED bullet): "**`REPRO_BLOCKED`** — an input whose value the paper does not print, but for which the paper names a source (a citation), where that source is outside this lane and cannot be obtained. Name it. *(Distinct from `REPRO_INPUT_ABSENT`, which is an input the paper neither prints nor traces to any named source.)*"
Exact replacement (precedence sentence): "**Exactly one outcome is filed per claim. Where more than one terminal condition holds, file the first in this order:** `REPRO_NO_DERIVATION_STATED`, `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, then the **arithmetic group**." (Traced-unobtainable → BLOCKED; silent, or traced-but-obtainable-without-value → INPUT_ABSENT; the seat may not supply the value either way. Both reachable, mutually exclusive on the named-source test.)

Study level: INCONCLUSIVE is genuinely reachable — CENSUS_PARTIAL is explicitly "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE", reachable via any non-arithmetic per-claim outcome.

FINDING 6 (RECOMMENDED) — §4 never states that exactly one study-level outcome is filed, and has no precedence among co-occurring conditions.
Construct: one claim files REPRO_BLOCKED (post-F1) → CENSUS_PARTIAL's condition holds; the audit then cannot reproduce a sampled outcome → CENSUS_AUDIT_FAILED's condition holds. §4 orders PARTIAL only over COMPLETE. Likewise R3C2_NO_CLASS + CENSUS_DENOMINATOR_DISPUTED can co-hold (a control fails in both seats while enumerations also disagree); and an audit-time origin disagreement affecting >10% of included claims satisfies CENSUS_ORIGIN_DISPUTED's wording while the unreproduced rests_on satisfies CENSUS_AUDIT_FAILED's. §3 fixed exactly this for per-claim outcomes; §4 has no equivalent.
Exact replacement (append to §4): "**Exactly one study-level outcome is filed. Where more than one condition holds, file the first in this order:** `CENSUS_DENOMINATOR_DISPUTED`, `R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`."

FINDING 7 (RECOMMENDED) — ORIGIN_DISPUTED at ≤10% leaves rests_on undefined for affected claims.
Quote: "An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` ... it is **not** reconciled."
Defect: rests_on is computed from root origins by fixed severity; a disputed root gives the script no single input, and below the 10% stop the census proceeds with no rule for those claims' field.
Exact replacement (append to C6): "**A claim whose root-origin set contains an `ORIGIN_DISPUTED` input carries `rests_on` computed under both classifications, printed as a pair and marked `DISPUTED`; the `rests_on` tally reports a `DISPUTED` row.**"

Q1 ANSWER: Reachability — NO, not every declared outcome is reachable as written: REPRO_BLOCKED's exclusive domain is empty (FINDING 1); every other §3 outcome and every §4 class is reachable, and INCONCLUSIVE is genuinely reachable. Decidability — YES: my hardest cases each trace to exactly one outcome and one rests_on under option (c):
  (a) printed-but-chosen input — "our adopted H₀ = 73.0" (not on C3's list), recipe yields the printed D_L: status PRINTED (machine-matched to its line), origin CHOSEN (ORIG_CHOICE_STATED, quoted), consumed → REPRO_EXACT; rests_on = most severe root present (USES_UNDECLARED if any root is a silently printed measured input, else USES_CHOSEN), root set printed beside it. One outcome, one rests_on. The V9 unreachable class is genuinely closed: the chosen input no longer blocks the attempt, and what REPRO_AFTER_CHOICE recorded survives as the field.
  (b) fitted parameter the paper states — "our fit gives τ = 0.09": PRINTED/FITTED, consumed → EXACT or FAILED on the arithmetic alone; rests_on USES_FITTED unless a more severe root coexists.
  (c) imported value — "σ₈ = 0.85 from Author (2019)", numeral printed: PRINTED/IMPORTED, consumed → EXACT/FAILED; rests_on USES_IMPORTED. (Cited without a numeral → ABSENT → INPUT_ABSENT as written; FINDING 1's repair routes the traced-unobtainable subcase to BLOCKED.)
The old tension resolves in favor of decidability at the price of one hollow non-arithmetic class (FINDING 1) and two soft spots (FINDINGS 7, 11).

FINDING 11 (RECOMMENDED) — "within its own stated precision" is undefined when the claim states no precision.
Defect: the paper prints "13.8 Gyr" with no uncertainty; the recipe yields 13.797. One seat files REPRO_EXACT (rounds at printed precision), another REPRO_FAILED (no stated tolerance → mismatch). Both readings defensible; the split spends the third-seat tiebreak on a mechanical question. Changes a per-claim class filed → not cosmetic.
Exact replacement (append to REPRO_EXACT): "**Where the paper states no precision for the claim, the printed precision is the claim's stated precision: the reproduced value must round to the printed numeral at that precision.**"

═══════════════════════════════════════════════
2. CONTROLS (+ Q2)
═══════════════════════════════════════════════
Pass-by-assertion sweep: C1 (printed denominator before tally), C2 (JSON ledger), C3 (machine checks), C5 (live execution; transcribing expected values fails), C6 (audit artefacts) all require printed artefacts. C4/C5b path lists are self-report — but C4 makes that secondary detection under a structural control (redacted out-of-lane packet copy) and says so. Unreached-limb controls: "Controls in an unreached limb are `NOT RUN`, never passes." ✓. Every control names an exact code with PASS/FAIL/NOT_RUN (C3 prints `PASS|FAIL|NOT_RUN` explicitly; C0/C1/C2/C4/C5/C5b/C6 name their codes; §9 supplies NOT_RUN).

FINDING 2 (REQUIRED) — C0's operative assignment contradicts the header and its own rationale.
Quote: "**The exhibitions are authored by a seat and only verified by Tori** — deciding what counts as reachable is where an author's prior would enter, so the author does not decide it."
Defect: the header binds "C0 by two pattern-blind seats who must agree" and §10.4 repeats it; the control text keeps a single author plus verification by Tori — the author of this document — which the clause's own rationale forbids. §10.2 narrates this repair as applied; the operative section kept the pre-repair sentence — the "second live copy" failure §6's own note describes.
Exact replacement: "**The exhibitions are authored by one pattern-blind seat and independently verified by a second pattern-blind seat; the two must agree. Tori performs only a coverage check that every §3 outcome and §4 class appears in the table — the author of this document does not decide what counts as reachable.**"

FINDING 4 (REQUIRED) — the origin reason-code CHOICE is asserted, not checked, where codes co-apply (the Q2 attack).
Quote: "Every record carries `origin_evidence` with a reason code — `ORIG_EQUATION`→`DERIVED`, ... `ORIG_CITATION`→`IMPORTED`, `ORIG_SILENT`→`UNDECLARED` — and, except for `ORIG_SILENT`, a **verbatim quotation machine-matched to the cited line**."
Test record (wrong but plausible): the paper prints "We adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹ from Planck (2018)" and computes a distance. Seat files: status PRINTED (value machine-matches the line ✓), origin CHOSEN, reason_code ORIG_CHOICE_STATED, verbatim quotation machine-matched ✓. Correct origin is IMPORTED (the value's provenance is the cited work). The record passes every machine check in C3, and rests_on reports USES_CHOSEN instead of USES_IMPORTED — a less severe label, i.e., the error runs in the flattering direction. Which mechanism catches it? (a) the second seat — only if it classifies differently; a shared plausible reading of "adopt" defeats it; (b) C6's audit re-classification — guaranteed for claims whose outcome asserts reproduction (100% audit), 20% sampling for the rest; a mislabelled input on a non-sampled, non-reproduced claim is never re-examined. As written, the mechanism cannot reliably catch it. That is the answer.
(What IS checkable: nothing can be asserted cleaner than its evidence — DERIVED/STANDARD/CHOSEN/FITTED/IMPORTED all require matched text; UNDECLARED, the only assertable code, is the default and the most severe, so assertion cannot launder; an ORIG_SILENT record must print the seat's search. The dangerous direction is closed.)
Exact replacement (append to C3): "**Where more than one reason code matches the cited sentence, file the first in this order: `ORIG_CITATION`, `ORIG_FIT_STATED`, `ORIG_CHOICE_STATED`, `ORIG_EQUATION`, `ORIG_CONSTANT`, `ORIG_SILENT` — a sentence that names an external source for the value is a citation whatever else it says.**"

FINDING 5 (REQUIRED) — derived_from acyclicity and referential integrity are never asserted; an empty root set launders to DERIVED_ONLY.
Quote: "Every `DERIVED` record lists its `derived_from` ids; **a script computes `root_origins`, the origins at the leaves of that chain, and no seat writes that field.** A chain cannot be made to look clean by classifying only its last step." ; "`DERIVED_ONLY` when every root origin is `DERIVED` or `STANDARD`".
Defect: C3's script assertions cover value-in-ledger, no-ABSENT-values, PRINTED match, STANDARD membership — nothing asserts that every derived_from id names a ledger record, or that the derived_from graph is acyclic. Construct: A (DERIVED, equation quoted, derived_from [B]); B (DERIVED, equation quoted, derived_from [A]). No leaves → empty root_origins → "every root origin is DERIVED or STANDARD" vacuously true → rests_on = DERIVED_ONLY, the cleanest label, for provenance that bottoms out nowhere. A dangling (typo) id reaches the same undefined state innocently. The audit recomputes rests_on "by the same script", so it reproduces the vacuous result — the shared-script design cannot catch it. This defeats the very sentence that claims the protection: a cycle has no last step.
Exact replacement (append to C3's assertions): "**The script asserts that every `derived_from` id names a record in the ledger and that the `derived_from` relation is acyclic; a dangling id or a cycle fails `C3_NO_SUBSTITUTION`. A claim whose inputs are present but whose root-origin set is empty fails this control.**"

FINDING 8 (RECOMMENDED) — C6's sample formula errors in the all-reproduced census.
Quote: "a sample of `max(1, ceil(0.20 × N))` of the remaining included claims".
Defect: if every included claim asserts reproduction, group (i) already audits them all; zero remain; `random.Random(seed).sample([], max(1, ceil(0.20 × N)))` raises ValueError — a described procedure that cannot be executed as delivered in a reachable (indeed expected) state.
Exact replacement: "a sample of `min(max(1, ceil(0.20 × N)), R)` of the remaining included claims, where `R` is the number of remaining claims; when `R` is zero the sample is empty and every included claim is already audited under (i)."

Q2 ANSWER: partially checkable. Checkable: quotation-gated non-UNDECLARED codes, script-computed root_origins and rests_on (seat-written or absent rests_on fails C3), audit re-classifies origin from the sources, UNDECLARED is the default and prints its search. Not reliably checkable as written: the code choice among co-applicable quoted codes (FINDING 4 — catch is probabilistic: seat disagreement, or audit coverage that is total only for reproduced claims), and the transitivity guarantee against cycles/dangling ids (FINDING 5 — the same-script audit shares the blind spot).

═══════════════════════════════════════════════
3. CIRCULARITY
═══════════════════════════════════════════════
Inclusion: the §1 rule is hypothesis-free (printed numeral asserted as the paper's own; excluded kinds enumerated); two blind seats assign it independently; disagreement stops the census (CENSUS_DENOMINATOR_DISPUTED); the third seat audits the full candidate and exclusion ledgers against every pinned source. A contrary claim cannot be quietly excluded: a wrongful exclusion must survive both blind seats agreeing AND the completeness audit ("any ledger incompleteness, files CENSUS_AUDIT_FAILED"). The exclusion ledger is printed beside the denominator. This channel is closed.
Origin classification: quotation-gated and dual-seated; disputes are reported, never reconciled; the audit re-classifies from the sources. Residual channels: FINDING 4 (co-applicable codes), FINDING 5 (cycles), OBSERVATION 2 below.
Interpretation step: reads rests_on + reproduction tallies after the tally is sealed; no seat may open the protocol. BUT see FINDING 3 (§5 below): as written, the seal does not custody the protocol before the tally exists, so the mapping could still be written to suit the tally — the one channel the redesign claims to have removed. With FINDING 3 repaired (pre-Limb-A receipted relay), the channel closes.
Packet channel: the packet leaks the channel (rests_on) but not the target (Q3); a seat could in principle lean on origin classification, but quotations gate it and the target is unknown. Bounded.

OBSERVATION 2 (not a finding): a measured-but-silent input (an observed redshift, a measured flux printed without provenance) files ORIG_SILENT→UNDECLARED — the most severe root origin — because the code set has no ORIG_MEASUREMENT. Both seats would do this identically (mechanical, no dispute), so the ledger consistently reports ordinary measurements as UNDECLARED and the rests_on distribution shifts upward in severity by construction. Whether that misrepresents them depends on the sealed interpretation; flagged because it is a designed-in pressure on the field the interpretation reads.

═══════════════════════════════════════════════
4. THE BLIND
═══════════════════════════════════════════════
Redaction machine-asserted as claimed — VERIFIED by me on the artefact: I extracted every SEAT-REDACT span from the master and probed the packet for distinctive strings from each ("moving work into blind seats", "entry 59", "929.25", "had no outcome to file", "NOT_ATTEMPTED", "verdict about the pattern", "Added by Duho", "swept for every other occurrence", "--add-dir", "SEAT_PACKET.sha256", "custodian is Blanc", "Amendments get a new version", "HELD PENDING") and for dropped-section/forbidden content ("ΛCDM", "Blanc", "Duho", "INTERPRETATION_PROTOCOL", "shape/magnitude", "51 rows", "R3C_MAGNITUDE", "Q-R3C2"): ZERO survivals. The only builder references in the packet are its own header lines naming the builder and embedding the master hash — and the embedded hash equals ACCESS_SHA (verified). Key operative rules appear in the packet with text identical to the master's (severity order, C3 schema and table, C6 sampling formula — compared line by line). C4's mechanism (redacted out-of-lane copy directory + asserted forbidden-list redaction + path-list secondary detection) prevents what it claims: the seat is not given the pattern record and cannot read it from its working directory.
The limit is stated honestly: "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can."
OBSERVATION 3: the packet discloses the EXISTENCE of redacted sections and uses "pattern-blind" (×3); existence-of-pattern and existence-of-comparison are knowable, content is not; a non-inference instruction is present. Bounded, but C4's forbidden-list description ("the pattern's name and topic, the comparison model, gate history") should be read as covering content, not existence.
OBSERVATION 4: engine-name parentheticals survive in the packet ("codex:" §1; "kimi found it" C3; "a gap codex found" §4; "the defect codex found in R3D's C5/C5b" C5b; "Kimi arithmetic" §9), and "R3D" is named twice. Methodological history, no hypothesis content — but "gate history" in C4's description is broader than what the list evidently caught; the document should say which.

═══════════════════════════════════════════════
5. THE SEAL (§7)
═══════════════════════════════════════════════
What works: the relay is receipted, not sent ("The relay is complete only when it is receipted"); it precedes the step it must precede (the interpretation protocol "is not opened without that recorded acknowledgement"); after opening, Blanc re-hashes tally and protocol against the receipt; the tally is bound before interpretation; the honesty paragraph is real ("It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind").

FINDING 3 (REQUIRED) — the seal does not custody the interpretation protocol before the tally exists, while §7 claims it does.
Quotes: "The interpretation step lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which **no seat may open** and which is itself fixed and committed before limb A begins, **so it cannot be written to suit the tally.**" ; "**Before the interpretation protocol is opened, Tori commits the tally, then sends four digests — tally hash, tally commit id, protocol hash, protocol commit id — to Blanc**" ; "**a local commit in Tori's own custody can be amended or reset, so it is tamper evidence, not an external custodian**".
Defect: the only relay §7 describes runs AFTER the tally is committed. Between "committed before limb A" (a local commit — tamper evidence by §7's own words) and "receipted after the tally", the protocol sits in Tori's custody, amendable. Tori can compute the tally, amend the protocol to suit, commit both, then relay; the receipt then "proves" a protocol shaped after the tally was known. The "so it cannot be written to suit the tally" clause asserts more than the mechanism delivers. §10.2 records this exact overclaim as found at V8 and announces the repair ("The relay now happens before limb A"), but no pre-Limb-A relay exists in the operative §7 — the announced repair is absent from the section it repairs. (The protocol file EXISTS on disk — verified by existence check; existence is not custody.)
Exact replacement (the seal paragraph): "**(1) Before Limb A begins, Tori sends the protocol hash and protocol commit id to Blanc; Blanc acknowledges and timestamps them in a preserved receipt; Limb A does not begin without that receipt. (2) After the tally is committed, Tori sends the tally hash and tally commit id; Blanc receipts them; the interpretation protocol is not opened without both receipts. After opening, Blanc independently re-hashes the tally and the protocol and verifies all four values against the two receipts; any mismatch files the seal-failure outcome, leaves the interpretation unrun, and voids the comparison.**" And in the protocol sentence, delete "so it cannot be written to suit the tally" or replace with "and the relay of step (1) receipts it before any tally exists."

FINDING 9 (RECOMMENDED) — a §7 seal mismatch files CENSUS_AUDIT_FAILED, whose §4 definition covers only the C6 audit.
Quotes: "Any mismatch files `CENSUS_AUDIT_FAILED`, leaves §7 `NOT RUN` and voids the comparison." vs "**`CENSUS_AUDIT_FAILED`** — the audit of §6 cannot reproduce a sampled per-claim outcome."
Defect: a receipt-verification failure is not an audit reproduction failure; the class is filed outside its definition ("§7 NOT RUN" also borrows control vocabulary for a section with no control code). Changes a class's filing condition → not cosmetic, but small.
Exact replacement (§4 class 3): "**`CENSUS_AUDIT_FAILED`** — the audit of §6 cannot reproduce a sampled per-claim outcome or ledger, **or the §7 receipt verification fails**. The census is void; report which."

═══════════════════════════════════════════════
6. FAIRNESS
═══════════════════════════════════════════════
Every negative outcome is worded "unreproduced from the stated inputs," never "error" — verified by sweep: the word "error" appears in the master only inside the fairness rule itself (§3, quoting the forbidden word to forbid it) and in §10 history; the packet is clean. ORIGIN_DISPUTED is reported with both quotations, never reconciled. CENSUS_PARTIAL is labelled INCONCLUSIVE. "The census is void" (AUDIT_FAILED) is procedural, not accusatory.
FINDING 10 (COSMETIC for the name; optional micro-repair) — REPRO_EXACT asserts exactness its definition does not deliver, and unlike FAILED it need not print the recomputed number.
Quote: "**`REPRO_EXACT`** — the paper's number follows, within its own stated precision" vs FAILED's "Report both numbers."
Defect: the name claims exactness; the fact is "matches at stated precision". The name alone is COSMETIC by the brief's test (changes nothing filed); the asymmetric evidence requirement is a small gap — the clean outcome is less evidenced than the negative one.
Optional repair: rename to `REPRO_STATED_PRECISION`, or add "Report both numbers." to the EXACT bullet.
No other class name or clause asserts more than its definition (rests_on labels are factual given the ledger; DERIVED_ONLY matches its rule; CENSUS_COMPLETE/PARTIAL describe procedure, not the corpus).

═══════════════════════════════════════════════
7. STALL / EXECUTABILITY (+ Q4)
═══════════════════════════════════════════════
Stall: two-attempt caps on reproduction and reconciliation, then named stop classes; third seat via dispatch on a split; SYMBOLIC_TIMEOUT files REPRO_NOT_EVALUABLE; control failures file R3C2_NO_CLASS (both seats) or CENSUS_CONTROL_SPLIT (one seat each — "do not adopt the passing seat's result"); unreached limbs NOT RUN. A missing Blanc receipt stalls only the interpretation (out of this document's scope by design — §7 is "deliberately NOT specified here"); the census tally still files. No unbounded wait inside the census itself.

Q4 ANSWER — checked, each named:
- C5's literal commands: all execute on this machine — `python3 --version` → Python 3.9.6; sympy 1.14.0 present (so the import command runs); `shasum -a 256 $(command -v python3)` → printed (b8763cf2…610e9, /usr/bin/python3). The "transcribing expected values fails" rule stands.
- `r3c2_build_seat_packet.py` — EXISTS in the lane directory.
- `R3C2_SEAT_PACKET.sha256` — EXISTS (existence verified; content not read — my two-file constraint; the packet's self-embedded master hash matches ACCESS_SHA, and C4 states Tori re-runs the builder and re-checks the pin before dispatch).
- `R3C2_INTERPRETATION_PROTOCOL_20260904.md` — EXISTS (not opened — no seat may open it; existence is not custody, FINDING 3).
- `nm_referee_dispatch.sh` (§9, third-seat dispatch) — DOES NOT EXIST: searched the repository by name, no match anywhere. FINDING 12 below.
- Fields a script is said to compute: root_origins/rests_on computable as specified EXCEPT the empty-root/cycle case (FINDING 5) and disputed roots (FINDING 7); the C6 sample EXCEPT the empty-remaining case (FINDING 8); the severity order and closed-list membership are fully mechanical (list printed literally in both documents — verified identical).
- Paths a seat is told to use: the packet says "Work only from this file, the seat brief, and the pinned sources in this directory" — as delivered, `r3c2_seat_packet/` contains ONLY the packet (OBSERVATION 5: brief and sources belong to the dispatch copy C4 describes; noted so dispatch assembly checks for them).
- The ledger/root script is never named, committed, or digest-pinned — unlike the packet builder, which is named and whose output is pinned; the audit's "same script" claim is then unverifiable. FINDING 12.

FINDING 12 (RECOMMENDED) — the provenance script is unnamed and unpinned; the named dispatch script does not exist as delivered.
Quotes: "a script computes `root_origins`" / "The same script computes each claim's `rests_on`" (C3) ; "third seat via `nm_referee_dispatch.sh` on a split" (§9).
Exact replacement: name and commit the ledger script (e.g., `r3c2_ledger.py`) and record its sha256 beside C3, exactly as the packet is pinned in C4; and either create `nm_referee_dispatch.sh` or replace the reference with the actual dispatch mechanism.

═══════════════════════════════════════════════
VERDICT
═══════════════════════════════════════════════
PREREG_SOUND_WITH_REPAIRS.
The option-(c) core is coherent: the reproduction question is decidable per claim; provenance is recorded-not-filtered; rests_on is script-computed; the blind's redaction property VERIFIED mechanically on the artefact; inclusion/exclusion/origin channels are quotation-gated, dual-seated, dispute-reported, and completeness-audited; fairness wording is clean; stall paths are classed. Not UNSOUND: no defect breaks decidability, and every defect has an exact repair. Not SOUND: FINDINGS 1 (a declared outcome unreachable — the document's own C0 forbids freezing), 2 (operative C0 contradicts the header and its own rationale), 3 (the seal does not bind the protocol before the tally while claiming it does), 4 (provenance code assertable among co-applicable codes), 5 (cycles launder to DERIVED_ONLY) are required before freeze; FINDINGS 6–12 before Limb A. FINDING 10's name-part is COSMETIC.

R3C2_V10_KIMI_COMPLETE
