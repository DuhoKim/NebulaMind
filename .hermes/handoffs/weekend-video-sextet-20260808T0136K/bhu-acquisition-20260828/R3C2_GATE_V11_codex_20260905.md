ACCESS_SHA=d6695c06c78c4735cb155269737175fbe55b7cdff0161cfccd0e85a71b21aca1
PACKET_SHA=a3516349cb5a55fcca2de3694fe3468fd45dbb75dd3c583a8e98c60d4e26786d
GATE=PREREG_UNSOUND
LEAK=NONE — the packet reveals design history and procedural stop consequences, but no hypothesis, comparison model, preferred empirical outcome, or expected pattern.
CONSEQUENCE_VISIBLE=CENSUS_AUDIT_FAILED — “The census is void; report which.” makes that outcome procedurally more consequential than completion.

## 1. Outcome classes

The stated precedence makes the listed per-claim terminal conditions mutually exclusive once one applies, and the study-level precedence similarly selects one class when listed conditions overlap. `CENSUS_PARTIAL` is genuinely reachable and explicitly `INCONCLUSIVE`: any included claim ending in a non-arithmetic outcome after two attempts produces it, subject to earlier stop classes.

The classes are not exhaustive, however. A real case is a claim whose equation needs parameter `x`; the claiming paper does not print `x` but says “we take x from Table 2 of Smith (2020)”; Smith (2020) is a pinned, obtainable source in the lane. Under §2, `x` is not `PRINTED` in the claiming paper and is not `STANDARD`, so it is `ABSENT` and cannot be consumed. It is not `REPRO_BLOCKED`, because its named source can be obtained, and not `REPRO_INPUT_ABSENT`, because it is traced to a named source. Arithmetic therefore cannot begin, yet no per-claim outcome applies. A study containing it consequently also cannot be filed cleanly under `CENSUS_COMPLETE` or `CENSUS_PARTIAL`.

Defect (SUBSTANTIVE). Quote: “`REPRO_BLOCKED` — an input whose value the paper does not print, but for which the paper **names a source (a citation)**, where that source is outside this lane and cannot be obtained.” The obtainable-citation case has no outcome.

Exact replacement: “`REPRO_BLOCKED` — an input whose value the claiming paper does not print, but for which the claiming paper names a source (a citation). Because values not printed by the claiming paper and not on C3’s closed `STANDARD` list may not be supplied, the attempt stops whether or not the named source is obtainable in the lane. Name the input and source.”

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE? No. A printed-but-chosen input traces cleanly: a paper prints “we choose β = 1/929.25,” the ledger records `status=PRINTED`, `origin=CHOSEN`, and matching evidence; arithmetic consumes it; rounding uniquely files `REPRO_EXACT` or `REPRO_FAILED`; the script computes `rests_on=USES_CHOSEN`. A paper-printed fitted parameter similarly yields exactly one arithmetic outcome and `USES_FITTED`; a paper-printed imported value yields exactly one arithmetic outcome and `USES_IMPORTED`. But the unprinted, cited, obtainable imported value above yields no outcome. Thus option (c) handles printed-but-chosen/fitted/imported inputs, but not every declared input state, and the reproduction question is not total as written.

At study level, the precedence prevents double filing: for example, a denominator dispute plus an audit failure files only `CENSUS_DENOMINATOR_DISPUTED`. No listed study condition inherently fits two final classes after precedence. The uncovered per-claim state is instead a route to no valid study tally.

## 2. Controls

Most controls name exact `PASS|FAIL|NOT_RUN` codes and globally state that unreached controls are `NOT_RUN`. C0 is the exception.

Defect (SUBSTANTIVE). Quote: “`C0_REACHABILITY=PASS`.” This supplies no exact `FAIL` or `NOT_RUN` token even though the prose defines an unreachable failure and C0 may itself be unreached after a prior dispatch failure.

Exact replacement: “`C0_REACHABILITY=PASS|FAIL|NOT_RUN`; PASS only when every required row has been independently exhibited and verified, FAIL when any required row is absent or cannot produce its declared condition, and NOT_RUN when C0 was not reached.”

C3 is internally impossible at the compute boundary. Quote: “No seat writes `root_origins` or `rests_on`; the script rejects a ledger that arrives with either set.” Later: “A `rests_on` value written by a seat, or absent, fails this control.” The required compute input must omit `rests_on`, but the latter sentence says its absence fails the same control.

Exact replacement: “A `rests_on` value present in the seat-authored input ledger fails this control; after a successful `compute` run, a `rests_on` value absent from the script-produced output ledger fails this control.”

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? Partly, and sufficiently for an honest disagreement to be exposed, but not machine-decided. Test record: `{status:"PRINTED", origin:"CHOSEN", origin_evidence:{reason_code:"ORIG_CHOICE_STATED", source_file:"paper.md", source_line:41, verbatim:"We adopt x = 67.4 from Planck (2018)."}}`. The quotation can machine-match while the plausible `CHOSEN` classification is wrong: the declared precedence makes `ORIG_CITATION`/`IMPORTED` controlling. The script’s text match cannot understand that semantic error. The second seat’s independent classification and C6’s fresh reclassification catch it by producing an `origin` disagreement, preserving both quotations, marking affected `rests_on` values `DISPUTED`, and—above the threshold—filing `CENSUS_ORIGIN_DISPUTED`. If all human checks make the same semantic mistake, no written mechanism catches it; the design does not claim semantic classification is machine-proved.

Two path controls can pass through omission in a self-reported list. Quote: “`C4_PATTERN_BLIND=PASS` requires that printed path list; any path outside the copy directory is `FAIL`.” Quote: “Print every path opened, each marked `IN_SCOPE` or `OUT_OF_SCOPE`; any `OUT_OF_SCOPE` row fails the control.” A seat can omit an opened path and still present a passing artefact. The master honestly admits this limit for C4, but the tokens still overstate what was checked.

Exact replacement: “`C4_PATTERN_BLIND=SELF_REPORTED_PASS|FAIL|NOT_RUN`; SELF_REPORTED_PASS means the printed list contains no outside path and makes no claim that the list is complete.” And: “`C5B_NO_CROSS_LANE=SELF_REPORTED_PASS|FAIL|NOT_RUN`; SELF_REPORTED_PASS has the same completeness limitation.”

## 3. Circularity

The seat-visible inclusion definition contains no hypothesis, comparison model, magnitude criterion, or interpretation mapping. Inclusion/exclusion is duplicated independently at tolerance zero and fully audited, so a contrary quantitative claim cannot quietly disappear without appearing in the candidate/exclusion ledgers. Origins are independently classified and audited; disagreements remain visible rather than reconciled. The interpretation protocol is withheld until after tally sealing. I find no design route by which the lane’s expected empirical result can alter arithmetic, inclusion, exclusion, or origin without leaving a failed/split/disputed artefact. The uncovered cited-input gap is a completeness defect, not evidence of directional circularity.

## 4. The blind

Packet-only Q5 and Q3 were answered before opening the master. The packet contains none of the master’s stated forbidden consequence words or custody/history names when tested case-insensitively. It nevertheless exposes unequal procedural consequences through “The census is void; report which.” That sentence is not an empirical stake or preferred result, but it falsifies an absolute reading of “every word that signals consequence” being forbidden.

Defect (SUBSTANTIVE to the claimed assertion, not to empirical blindness). Quote: “asserts that no string on a forbidden list survives anywhere in the output” and the list includes “every word that signals consequence rather than content.” The produced packet contains the consequence-bearing word `void`, showing that the described list is not exhaustive enough to establish the broader claim.

Exact replacement: “The forbidden list blocks the enumerated strings; it does not establish that all consequence-bearing language has been removed. Procedural consequences of control and study-stop outcomes remain visible, while hypothesis mappings, comparison-model preferences, and empirical stakes are removed.”

C4 does not prevent out-of-directory reads; it detects only declared ones. The master states this honestly: “This is procedural, not enforced by the filesystem” and “It cannot prove a seat has no prior exposure from training or an earlier session.” That limitation is soundly disclosed.

## 5. The seal

The two-receipt relay binds the protocol hash and commit id before limb A, then binds the tally hash and commit id after tally commit but before protocol opening. Independent re-hashing checks all four values against preserved receipts. The required temporal ordering is explicit, and missing/mismatched receipts file `CENSUS_AUDIT_FAILED`. The document correctly limits the claim to existence of the stated forms before comparison and does not claim to bind the custodian’s mind. The seal is sound as designed.

## 6. Fairness

The operative negative outcome says “unreproduced from the stated inputs,” not “error,” and the audit language does not convert disagreement into an accusation of paper error. No negative class overclaims beyond its definition.

COSMETIC defect. Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision.” The name `EXACT` asserts exact equality while the definition permits rounded agreement.

Exact replacement: rename `REPRO_EXACT` everywhere in operative sections and scripts to `REPRO_WITHIN_STATED_PRECISION`. This changes no tested value, reachability, control passability, conclusion, or which outcome a claim receives; it only makes the label honest.

## 7. Stall / executability

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Yes: C2 gives an unresolved seat path, and C3’s contradictory `rests_on` requirements cannot both be executed. I checked, without opening any unauthorized file, filesystem existence/type for the master, packet, `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, and `R3C2_INTERPRETATION_PROTOCOL_20260904.md`; all exist as regular files. I checked existence of `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`; it exists. I checked command resolution for `python3` and `shasum`; both resolve. I machine-searched the authorized packet for the master’s enumerated forbidden terms; none occurred. I did not open, hash, or execute the other named files because the gate instruction expressly forbade opening any file other than the packet and master; consequently their contents, pinned digest claims, schemas, output lines, and actual computation behavior are not verified by this report.

Defect (SUBSTANTIVE). Quote: “`/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> <sources_dir>`.” The delivered seat packet never assigns a literal path to `<sources_dir>`; it says only that pinned sources are “in this directory.” A seat cannot print the required literal command without inventing that argument.

Exact replacement: “Run `/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> .` from the printed seat working directory; `.` is the sole allowed `sources_dir`.”

The template commands using `<ledger.json>`, `<out.json>`, `<candidates.json>`, and `<exclusions.json>` appropriately stand for seat-created artefacts, but the protocol should require their resolved paths to be printed. Exact replacement appended to C1–C3: “Before execution, print the fully resolved command with every angle-bracket placeholder replaced by the actual in-scope path.”

The 120-second symbolic cap has a named `SYMBOLIC_TIMEOUT` report and `REPRO_NOT_EVALUABLE`; unreached controls have `NOT_RUN`; denominator and origin splits have stop classes. Apart from the defects above, the stall paths are fileable.

R3C2_V11_GATE_COMPLETE
