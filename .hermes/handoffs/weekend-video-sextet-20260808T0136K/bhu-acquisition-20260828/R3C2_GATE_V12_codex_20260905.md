ACCESS_SHA=065dc0e48090d7d56625e41e8f517a782640e2b97d5df08423fd38dc4c8e8ee0
PACKET_SHA=535173e6c48fc6d160dd8daf8a3d55d03d24fd4047380015f294816623057f29
GATE=PREREG_UNSOUND
LEAK=The packet reveals that the design compares mechanical reproduction with provenance/rests_on and treats chosen, fitted, imported, and undeclared roots as a salient pattern, but reveals no directional hypothesis or preferred empirical result.
CONSEQUENCE_VISIBLE=Lane-procedural consequence is visible: `CENSUS_AUDIT_FAILED` says “No tally is filed,” unlike `CENSUS_COMPLETE`, which reports the tally; no empirical outcome is said to matter more outside the lane.

## 1. Outcome classes

The precedence rules make the listed per-claim outcomes mutually exclusive once an input has a valid ledger status, and the study-level precedence makes the listed study classes mutually exclusive. `CENSUS_PARTIAL` is genuinely reachable and expressly labelled INCONCLUSIVE: one included claim with `REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, or `REPRO_NOT_EVALUABLE` produces it after two attempts, absent an earlier stop class.

Q1 — NO. The settled option-(c) rule keeps ordinary arithmetic decidable for inputs that enter the ledger, but it does not make every real input case total. The hardest cases trace as follows:

- Printed but chosen: a paper prints “we choose β = 1/929.25” and directs its use. Status `PRINTED`, origin `CHOSEN`; arithmetic yields exactly one of `REPRO_EXACT` or `REPRO_FAILED`; the computed field is `USES_CHOSEN`.
- Fitted parameter stated by the paper: status `PRINTED`, origin `FITTED`; arithmetic yields exactly one arithmetic outcome; `rests_on=USES_FITTED`.
- Unprinted value cited from another pinned manifest text: the special §2 rule makes it `PRINTED`, origin `IMPORTED`; arithmetic yields exactly one arithmetic outcome; `rests_on=USES_IMPORTED`.
- But an unprinted input cited to an external source that is obtainable, though outside the pinned corpus, has no status and no outcome. It is not `PRINTED` under the special rule, not `ABSENT` because §3 defines that as “neither printed nor traced to any named source,” and not `REPRO_BLOCKED` because that requires a source that “cannot be obtained.” A real example is a claim instructing the reader to use a tabulated particle mass from the current PDG review without printing the mass; the review is obtainable but is outside this lane.

SUBSTANTIVE DEFECT 1. Quote: “`REPRO_BLOCKED` — an input whose value the paper does not print, but for which the paper names a source (a citation), where that source is outside this lane and cannot be obtained.” Together with: “`REPRO_INPUT_ABSENT` — ... neither printed nor traced to any named source.” These leave the obtainable-but-out-of-corpus cited input unclassified, so per-claim exhaustiveness and Q1 fail.

Exact replacement: “`REPRO_BLOCKED` — an input whose value the paper does not print and for which the paper names a source, where that source is not an enumerable text pinned in `R3C2_CORPUS_MANIFEST.md`; whether the source is publicly obtainable is irrelevant because the lane may not open or consume it. Name the source. `REPRO_INPUT_ABSENT` remains limited to an input neither printed nor traced to a named source.” Replace every “outside this lane and cannot be obtained” occurrence consistently.

At study level, the existing precedence cleanly resolves real overlaps: for example, both seats failing a control and a later partial tally files `R3C2_NO_CLASS`; a one-pass/one-fail split plus a denominator dispute files `CENSUS_CONTROL_SPLIT`. I found no study tally fitting no class once all prerequisite objects exist, but Defect 1 can prevent those objects from existing.

## 2. Controls

Each control has an exact `PASS|FAIL|NOT_RUN` code, and the global sentence “Controls in an unreached limb are `NOT_RUN`, never passes” handles unreached controls. C0 and C1 require printed artefacts rather than a bare assertion; C2/C3 require script exits; C5 requires live output; C6 requires a re-audit. C4/C5b are explicitly self-reported and make no completeness claim, which is honest rather than an overclaim.

Q2 — PARTLY YES, but not machine-decidable. Test record: source line says “We adopt H₀ = 67.4 from Planck (2018).” A plausible wrong ledger record is `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, with that exact quotation. Machine matching proves only that the quotation exists; it cannot enforce the semantic precedence that `ORIG_CITATION` must win. The second seat's independent classification and C6's source-based reclassification can catch the error by returning `IMPORTED`/`ORIG_CITATION`, creating `origin_alt`/`ORIGIN_DISPUTED`; if all human readers make the same error, it stands. The master states that limitation honestly. Thus origin is auditable rather than wholly asserted, but its correctness is not machine-guaranteed.

SUBSTANTIVE DEFECT 2. Quote: “The input ledger is a JSON file, one record per input: `{... root_origins: […], ...}`” and “No seat writes `root_origins`” / “the script rejects a ledger that arrives with `root_origins` ... already set.” The mandatory-looking schema contradicts the authored-input rule, so a literal conforming ledger fails C3.

Exact replacement: “The seat-authored input ledger schema is `{claim_id, input_id, symbol, status, origin, origin_evidence, derived_from, value, source_file, source_line}` and MUST omit `root_origins`, `rests_on`, and `origin_alt`. The script-produced output schema adds `root_origins` and per-claim `rests_on`. A separately specified comparison step may add `origin_alt` before recomputation.”

SUBSTANTIVE DEFECT 3. Quote: “where the two seats' `origin` classifications differ the record carries `origin_alt`” and “an `ORIG_SILENT` record prints the search the seat ran.” Neither `origin_alt` nor a search field appears in the declared JSON schema, and no exact merge command creates `origin_alt`. Consequently the disputed-origin path and the `ORIG_SILENT` evidence requirement are not executable as specified.

Exact replacement: add `origin_search: {query, files, matches}` as required for `ORIG_SILENT`; define an exact command and schema for merging the two independently validated ledgers, with `origin_alt` plus both evidence objects on disagreement; then make `compute` accept only that script-produced merged form when `origin_alt` is present.

## 3. Circularity

The packet does not reveal the hypothesis mapping, comparison-model preference, or preferred empirical outcome. Inclusion can still involve human judgement over “the paper asserts as a result of its own,” but two independent enumerations, zero-tolerance disagreement, and C6's full candidate/exclusion audit make quiet exclusion detectable. A contrary claim cannot be silently dropped without falsifying multiple ledgers and surviving the full-source audit. Origin classification is independently repeated and disputes are preserved. The interpretation protocol is kept from seats and receipted before limb A. I find no demonstrated route by which a directional lane expectation reaches inclusion, exclusion, arithmetic, or origin classification.

Packet-only Q3: I can infer the comparison structure from “two questions from one pass” and the repeated contrast between derived/standard/measured roots and chosen/fitted/imported/undeclared roots. I can infer that these provenance categories are intended to be examined as a pattern. I cannot infer whether the lane expects many or few reproductions, which provenance pole it prefers, what hypothesis is at stake, or what comparison model matters, because no packet sentence maps any outcome or `rests_on` value to a hypothesis or external conclusion.

## 4. The blind

The master describes a mechanical builder, a forbidden-list assertion, a pinned packet, and a pre-dispatch digest check. The delivered packet omits §§0, 7, 8, and 10 and the marked spans. Its remaining text does expose procedural stop consequences and the two-tally comparison, exactly within the master’s stated limit: “procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed.” Therefore Q5 is yes only as to lane procedure: “No tally is filed” makes audit failure procedurally more consequential than completion. No sentence makes one empirical result more consequential to a hypothesis or person. C4 itself cannot prevent an absolute-path read and says so; it provides a redacted directory plus self-reported detection, not filesystem enforcement. That limit is honest.

## 5. The seal

Receipt P binds the interpretation protocol's hash and commit id before limb A; receipt T binds the tally's hash and commit id after commitment and before protocol opening. Independent re-hashing checks all four values, and missing/mismatched receipts force `CENSUS_AUDIT_FAILED` and `NOT_RUN` interpretation. This binds the existence and identity claims at the necessary times. The text correctly disclaims proof about what Tori may have guessed. I find no seal defect.

## 6. Fairness

The operative negative wording is “unreproduced from the stated inputs,” never “error.” `REPRO_FAILED` asserts an arithmetic mismatch under its definition and does not assert scientific error. `REPRO_EXACT` is slightly stronger in name than its rule, which permits agreement merely within stated/printed precision, but the definition is explicit and tested values are unchanged.

COSMETIC. Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision.” Exact replacement: rename the token everywhere to `REPRO_WITHIN_STATED_PRECISION`. This changes no filing, passability, reachability, conclusion, or tested value.

## 7. Stall / executability

Q4 — NO, not everything is executable as delivered, because Defects 1–3 leave one input domain without a status/outcome and leave the disputed-origin/ORIG_SILENT data path without a complete schema or merge command.

Within the instruction not to open files other than the packet and master, I checked existence by filesystem metadata, not contents. Present: `r3c2_ledger_tools.py` (10,132 bytes), `r3c2_build_seat_packet.py` (7,292), `R3C2_CORPUS_MANIFEST.md` (21,182), `R3C2_SEAT_PACKET.sha256` (200), `r3c2_seat_packet/SEAT_BRIEF.md` (1,770), the packet (20,412), and `R3C2_INTERPRETATION_PROTOCOL_20260904.md` (2,796). The absolute dispatch path `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh` exists (3,291). `/usr/bin/python3` and `/usr/bin/shasum` exist; `python3` resolves to `/usr/bin/python3`. I checked the literal command forms named in the master: builder; ledger `census`, `validate`, and `compute`; Python/SymPy/hash harness; and the dispatch path. I did not execute scripts or open referenced files because the brief expressly prohibited opening any file beyond the two named files. Therefore existence is established, but script behavior and pinned digests are not independently verified in this gate.

The angle-bracket ledger commands are templates and C2 expressly requires replacement before execution, so that is not a defect. The builder command is literal. No named artefact was missing as delivered. The substantive executability failures are specification failures, not missing-file failures.

R3C2_V12_GATE_COMPLETE
