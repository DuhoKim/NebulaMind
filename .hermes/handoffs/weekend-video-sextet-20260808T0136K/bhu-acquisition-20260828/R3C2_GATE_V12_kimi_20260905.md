ACCESS_SHA=065dc0e48090d7d56625e41e8f517a782640e2b97d5df08423fd38dc4c8e8ee0
PACKET_SHA=535173e6c48fc6d160dd8daf8a3d55d03d24fd4047380015f294816623057f29
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is inferable as cosmology (the packet prints the Planck 2018 baseline literally: "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.") and the printed-but-chosen/fitted case is anticipated and pre-defended ("chosen constants included"; "A paper can direct you to use its own chosen constant, and following that instruction is reproducing the paper."), so chosen/fitted/undeclared provenance is evidently expected to be live in the corpus; no hypothesis, comparison model, preferred outcome, or "pattern" is inferable, and no sentence says which tally would help or hurt the lane.
CONSEQUENCE_VISIBLE=NO

STEP 1 — PACKET-ALONE ANSWERS (given before the master was read)

Q5. No sentence in the packet tells me any outcome is more consequential than any other, to the lane or to anyone. The only consequence-adjacent sentence is §11: "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane. Paper HOLD; nothing outward." — it denies movement for every outcome equally; it does not rank them. §4.2's "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" attaches a label and a filing order to one outcome, not a consequence to anyone. Stop classes name procedural stops ("The census does not proceed"), which are consequences of procedure, not of result. CONSEQUENCE_VISIBLE=NO.

Q3. Three inferences are reachable; none reconstructs a stake.
  (i) The corpus is cosmology. Packet C3 prints the Planck 2018 baseline with values and uncertainties and says so: "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." This is content a seat must have (the STANDARD membership test); it predicts nothing about the tally.
  (ii) The design expects the printed-but-chosen/fitted case to be live. §1 builds it into the question: "chosen constants included". §2 step 4 pre-defends it: "(A paper can direct you to use its own chosen constant, and following that instruction is reproducing the paper.)" §3 motivates the dual tally with it: "a value can be printed in the paper and still have been chosen or fitted, and under (c) both facts survive: the arithmetic reproduces AND the ledger says what it rested on." So the lane evidently anticipates claims whose arithmetic reproduces while resting on chosen/fitted/undeclared inputs — the `rests_on` tally is where the design expects information. But the packet never says which `rests_on` distribution the lane wants, so this is an expectation about corpus content, not a preferred outcome.
  (iii) A selection among lettered options occurred: "under (c)". That reveals a decision point, not what was at stake in it.
  What the packet gives me nothing on: no comparison model is named anywhere; no outcome is preferred over another; no hypothesis is stated; §1's inclusion rule is pattern-free on its face. Adversarial reconstruction of the stake fails at the last step: I can see THAT the lane cares about provenance, not WHICH provenance result helps it.

1. OUTCOME CLASSES
Per-claim: exhaustive and mutually exclusive on every case I could construct. An included claim either states no procedure (REPRO_NO_DERIVATION_STATED), or its inputs are all stated (arithmetic group: REPRO_EXACT / REPRO_FAILED — these cannot co-occur, since the stated-precision rounding rule is a total predicate), or some input is unstated (traced-to-manifest → PRINTED/IMPORTED and the attempt proceeds; traced, unobtainable → REPRO_BLOCKED; untraced → REPRO_INPUT_ABSENT), or the attempt cannot complete (REPRO_NOT_EVALUABLE). Co-occurrences resolve to exactly one outcome under the total precedence "REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group". Study-level: seven classes with a total precedence ("R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT, CENSUS_DENOMINATOR_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL, CENSUS_COMPLETE"); I could not construct a tally fitting two. INCONCLUSIVE is genuinely reachable: one blocked or absent input anywhere in the corpus files a non-arithmetic outcome and CENSUS_PARTIAL (declared INCONCLUSIVE) results.

Q1 — YES, with one boundary crack (Finding 1). Hardest cases, each traced to exactly one outcome and one rests_on:
  (a) Printed-but-chosen input (the entry-59 shape: "we take β = …"): status PRINTED, origin CHOSEN via ORIG_CHOICE_STATED with machine-matched verbatim; the arithmetic consumes it (admissibility is by status, not origin); number follows → REPRO_EXACT; script-computed root_origins = {CHOSEN} → rests_on = USES_CHOSEN. One outcome, one rests_on.
  (b) A fitted parameter the paper states and reuses: PRINTED/FITTED → arithmetic proceeds → REPRO_EXACT or REPRO_FAILED; rests_on = USES_FITTED.
  (c) An imported value (cited, not printed, source in the manifest): §2's rule makes it PRINTED from that source, origin IMPORTED, value machine-matched at the cited line; the attempt proceeds; rests_on = USES_IMPORTED. REPRO_BLOCKED's exclusive domain (named source, unobtainable) is non-empty.
  (d) A derived chain: derived_from ids, script computes root_origins; dangling id, cycle, or parentless DERIVED → exit 1; pre-set root_origins/rests_on → exit 2. Decidable by construction.
  The reachability/decidability tension is resolved as written: admissibility is a two-value machine-checked status, provenance never gates the attempt, and the verdict is mechanical arithmetic against a stated-precision rounding rule. The one crack is totality, not the tension:

FINDING 1 (substantive; one exact repair). Quoted: §2 "A value the paper does not print but traces to a named source that is itself a text in `R3C2_CORPUS_MANIFEST.md` is classified `PRINTED` from that source … and the value machine-matched there."; §3 REPRO_BLOCKED: "an input whose value the paper does not print, but for which the paper **names a source (a citation)**, where that source is outside this lane and cannot be obtained." Defect: a value traced to a named source that is NOT outside the lane but yields no machine-matchable value falls through every class: (i) the source is a file listed as RAW in the manifest — pinned and present, but "not enumerable", and arguably not "a text in" the manifest; (ii) the source is enumerable but the cited line carries no matchable numeral. In both, the PRINTED-from-source rule's conditions fail, REPRO_BLOCKED's "outside this lane" fails, and REPRO_INPUT_ABSENT's "nor traced to any named source" fails — the attempt stops with nothing to file, the defect family REPRO_INPUT_ABSENT was added to close; two seats can also split (broad vs narrow reading of "text in the manifest") with no per-claim class to adjudicate it. Reachability is contingent on corpus content (a claim citing a RAW file or a dud citation line), which I cannot verify under the two-file constraint. Exact replacement for the REPRO_BLOCKED condition: "an input whose value the paper does not print, but for which the paper **names a source (a citation)**, where the value cannot be obtained from that source — because the source is outside this lane, is listed as RAW in `R3C2_CORPUS_MANIFEST.md`, or yields no machine-matchable value at the cited line. Name it." This keeps INPUT_ABSENT = untraced, BLOCKED = traced-but-unobtainable: total and exclusive again.

2. CONTROLS
No control is passable by assertion alone: C0 requires the exhibited table, authored and verified by two independent seats returning C0_REACHABILITY=PASS; C1/C2/C3 pass only on exit 0 of the pinned script's census/validate/compute runs with commands, stdout and exit status printed; C4/C5b require the printed path list; C5 is live execution with transcription failing. Every control names PASS|FAIL|NOT_RUN. Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes." and §4's stop-class sentence match. C3's reject-both-ways rule (seat-written rests_on fails; post-compute absence fails) closes both directions.

Q2 — checkable, with the floor the document itself states. Attack record: {claim_id: c1, input_id: i1, symbol: H₀, status: PRINTED, origin: CHOSEN, origin_evidence: {reason_code: ORIG_CHOICE_STATED, source_line: 412, verbatim: "We adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹ from Planck (2018)."}}. The quotation machine-matches its line and the code plausibly matches its sentence, so C3's machine checks pass. What catches it: the stated reason-code precedence ("`ORIG_CITATION` … a sentence that names an external source for the value is a citation whatever else it says") enforced by the second seat's independent origin classification (disagreement → origin_alt, rests_on computed under both and marked DISPUTED; >10% of included claims → CENSUS_ORIGIN_DISPUTED) and by C6's re-classification of every input's origin from the pinned sources — never by the machine, and the document says exactly that ("caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands"). A second attack — a PRINTED value not present at its cited line — is caught by the machine ("each `PRINTED` value machine-matches the text at its cited source line"). A third — laundering a CHOSEN leaf behind one DERIVED step — is caught by script-computed root_origins plus exit-1 on a dangling derived_from id. Q2 answer: YES; the residue (identical misclassification by every reader) is stated, not implied away.

3. CIRCULARITY
No channel found. Inclusion: two seats independently from the §1 rule alone, which contains no hypothesis content; zero-tolerance disagreement stops the study (CENSUS_DENOMINATOR_DISPUTED), so one seat cannot quietly drop a contrary claim; every excluded candidate is recorded in the exclusion ledger with a closed kind vocabulary, and C6's third seat "first audits the full candidate and exclusion ledgers against every pinned source — completeness, not just outcomes". Origin: cited, machine-matched, independently double-classified, disputes reported and never reconciled. Arithmetic: mechanical, pinned-script, machine-matched values. Interpretation: sealed off-packet (§7), committed and receipted before limb A. The lane's expectation cannot reach the evidence through inclusion, exclusion, origin, or the interpretation step.

4. THE BLIND
Machine-asserted as claimed, and verified within my constraint: the builder "drops §0, §7, §8 and §10 whole, strips every span marked `SEAT-REDACT` … and then asserts that no string on the forbidden list survives anywhere in the output", failing the build otherwise. My own grep of the packet for the enumerated forbidden content (pattern, magnitude, ΛCDM/LCDM, the custody-chain names, engine names, gate/ruling/principal/version tokens, the consequence words, the β = 1/929.25 example, SEAT-REDACT markers) found no survivor; the only hits were the benign substrings "attempt"/"expected". The packet's section structure (1,2,3,4,5,6,9,11) confirms the four dropped sections. The C4 mechanism prevents what it claims — the seat is not given the hypothesis record and cannot read it from its working directory — and the limit is stated at true strength in seat-visible text: PASS "means only that the list contains no outside path; it makes no claim that the list is complete", and the master states the procedural-not-enforced and no-prior-exposure limits. The forbidden-list claim is also stated at its true strength ("blocks the enumerated strings and does not establish that every consequence-bearing word is gone"). Noted, not a finding: "the numbering has shifted twice" survives in the packet — design history of the kind already ruled acceptable, and it grounds the cite-by-name rule.

FINDING 3 (COSMETIC). Packet-visible §4.3: "**or the §7 receipt verification fails**". §7 is dropped from the packet, so the seat meets a dangling pointer to a withheld section; it reveals only that a custody step exists, which the packet already discloses ("extracted mechanically"; receipt-based seed custody in C6). No class, passability, or reachability change. Exact replacement: "or the receipt verification fails".

5. THE SEAL (§7)
Binds what it claims, before the step it must precede, in both directions. (P) the interpretation protocol's hash and commit id are receipted by the external custodian before limb A begins — "limb A does not begin without receipt P"; (T) the tally's hash and commit id are receipted after the tally is committed and before the protocol is opened — "the interpretation protocol is not opened without both receipts". Post-opening, the custodian re-hashes and verifies all four values; any miss files CENSUS_AUDIT_FAILED, which §4 now names. The local-commit insufficiency is argued, and the overclaim guard is present: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."

6. FAIRNESS
Every negative per-claim outcome is worded on the paper's own terms: REPRO_FAILED carries "Wording: \"unreproduced from the stated inputs,\" not \"error.\""; REPRO_BLOCKED names the source; REPRO_INPUT_ABSENT names the input; REPRO_NO_DERIVATION_STATED names the passage; REPRO_NOT_EVALUABLE is about the lane's machinery. "error" occurs nowhere in operative text except inside that prohibition. No class name asserts more than its definition; REPRO_EXACT's definition contains the stated-precision clause, and the rename question is escalated to the principal, not claimed here. The rests_on vocabulary is factual provenance. Sound.

7. STALL / EXECUTABILITY
Q4 — nothing described fails to exist as delivered, within the two-file constraint. Checked by stat (existence only; opening anything else was forbidden): r3c2_ledger_tools.py EXISTS; r3c2_build_seat_packet.py EXISTS; r3c2_seat_packet/SEAT_BRIEF.md EXISTS; R3C2_CORPUS_MANIFEST.md EXISTS; R3C2_SEAT_PACKET.sha256 EXISTS; R3C2_INTERPRETATION_PROTOCOL_20260904.md EXISTS (consistent with §7's "fixed and committed before limb A begins"); /Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh EXISTS at the absolute path §9 claims; /usr/bin/python3 EXISTS and is executable; shasum EXISTS. Every literal command is well-formed with stated exit semantics (census/validate/compute → 0 pass, 1 fail on dangling id/cycle/parentless DERIVED, 2 reject on pre-set fields); the C6 sampling formula handles R = 0 ("when `R` is zero the sample is empty"); seed serialization is specified (64 lowercase hex, int(seed_hex, 16), random.Random(seed_int).sample over sorted remaining ids); sources_dir is fixed to "." with resolved commands printed. The packet's embedded master hash equals the ACCESS hash I computed independently. Digests of the script, manifest, and pin file, and the builder's runtime behavior, are unverifiable under the two-file constraint — they are dispatch-time checks, not defects. The 120-second cap names no timeout command but has a fileable outcome (SYMBOLIC_TIMEOUT → REPRO_NOT_EVALUABLE), so no stall has nowhere to go.

FINDING 2 (COSMETIC). §4.2 "after two attempts" (also §4.4, §4.7): unspecified whose two attempts these are, and a repeat attempt is meaningless for REPRO_NO_DERIVATION_STATED or REPRO_BLOCKED. Nothing filed changes either way. Exact replacement: "after the §2 attempt (one repeat permitted, meaningful only for `REPRO_NOT_EVALUABLE`)".

FINDING 4 (COSMETIC). Packet C4: "Any path outside the copy directory is `FAIL`." — "copy directory" appears nowhere else in the packet, which elsewhere says "working directory". Same referent; one-word wobble. Exact replacement: "Any path outside the working directory is `FAIL`."

VERDICT: the settled §3 definition under option (c) is reachable and decidable on every hardest case constructed; controls are artefact-backed with named three-valued codes; no circularity channel; the blind is machine-asserted and its limits stated; the seal binds in the right order with its overclaim guard; fairness wording holds. One substantive totality crack at the BLOCKED/IMPORTED/ABSENT boundary (Finding 1, exact one-clause repair given) and three cosmetics (Findings 2-4) stand between this and PREREG_SOUND.

R3C2_V12_KIMI_GATE_COMPLETE