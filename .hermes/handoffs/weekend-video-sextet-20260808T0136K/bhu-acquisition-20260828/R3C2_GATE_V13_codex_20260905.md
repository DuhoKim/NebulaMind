ACCESS_SHA=22355a08b2d9cb98163427f5d2647181e48912f794852ac498e4e5e9ff23f11f
PACKET_SHA=3eb3879f7cbb9b0cd4a53bee909ba6230768f85361e9d8fa6d86d74e11db681d
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO

## 1. Outcome classes; Q1

The arithmetic outcomes themselves have a stated precedence and are mutually exclusive once an input ledger can be formed. `CENSUS_COMPLETE` and `CENSUS_PARTIAL` are exclusive; the study-level precedence resolves other overlaps. `INCONCLUSIVE` is genuinely reachable: any included claim with a derivation but an input neither printed nor traced to a source files `REPRO_INPUT_ABSENT`, hence `CENSUS_PARTIAL`.

**Q1 — NO.** Option (c) makes printed chosen and fitted inputs decidable, but it does not make every declared outcome executable through the required ledger.

Hard cases that do work:

- A paper prints a chosen constant and a recipe. The input is `status=PRINTED`, `origin=CHOSEN`; arithmetic files exactly `REPRO_EXACT` or `REPRO_FAILED`, and the computed field is `rests_on=USES_CHOSEN`.
- A paper prints a fitted parameter. It is `status=PRINTED`, `origin=FITTED`; arithmetic files exactly one arithmetic outcome and `rests_on=USES_FITTED`.
- A paper cites a value that machine-matches the cited line of an enumerable pinned source. The exception in §2 makes it `status=PRINTED`, `origin=IMPORTED`; it files exactly one arithmetic outcome and `rests_on=USES_IMPORTED`.

The failure case is a real and expressly declared one: the claiming paper says “Using the value X from Smith et al. (2019), we obtain Y=4.2,” but does not print X, and Smith et al. is not an enumerable manifest text. Section 3 says this must file `REPRO_BLOCKED`. Yet §2 requires every input to be only `PRINTED`, `STANDARD`, or `ABSENT`; X is not `PRINTED`, is not `STANDARD`, and is expressly not `ABSENT`, because `REPRO_INPUT_ABSENT` is limited to an input “**neither printed nor traced to any named source**.” No valid C2/C3 ledger record can represent X. Consequently the required pass cannot reach one valid outcome plus one script-computed `rests_on` value.

Defect 1 (SUBSTANTIVE). Quote: “**Classify each input** as `PRINTED` (given in the paper), `STANDARD` ... or `ABSENT`.” Quote: “`REPRO_BLOCKED` — an input whose value the claiming paper does not print, and for which the claiming paper **names a source (a citation)** that is **not an enumerable text pinned in `R3C2_CORPUS_MANIFEST.md`**.” These rules create a fourth semantic state but give it no ledger status.

Exact replacement: replace every schema/status enumeration `PRINTED|STANDARD|ABSENT` with `PRINTED|STANDARD|ABSENT|BLOCKED`; add: “`BLOCKED` means the claiming paper traces the input to a named source but the value cannot be machine-matched at the cited line of an enumerable pinned source. A `BLOCKED` record carries no value, carries `origin=IMPORTED` and `ORIG_CITATION` evidence from the claiming paper, may not be consumed, and terminates the claim as `REPRO_BLOCKED`. `validate`, `compute`, and the used-value assertion accept and enforce this rule; `compute` emits the claim’s roots and `rests_on` from all recorded provenance, including `USES_IMPORTED` for this blocked root.” Also change “A seat may not supply a value for an `ABSENT` input” to “A seat may not supply a value for an `ABSENT` or `BLOCKED` input.”

There is also a study-level reachability collision. Quote: “`CENSUS_AUDIT_FAILED` — the audit of §6 cannot reproduce a sampled per-claim outcome or ledger, **or the receipt verification of the seal fails**.” Quote: “`R3C2_NO_CLASS` — a control fails **in every seat that attempted it** after two attempts.” C6 is a control. If its audit twice cannot reproduce an outcome, C6 fails in every seat that attempted it, and the declared precedence files `R3C2_NO_CLASS` before `CENSUS_AUDIT_FAILED`. Thus the audit-failure limb has no exclusive domain as written.

Exact replacement: replace the `R3C2_NO_CLASS` definition with: “`R3C2_NO_CLASS` — a control among C0 through C5b fails in every seat that attempted it after two attempts, except that packet/seat-isolation failure stops dispatch without a census tally. C6 audit failure and seal-receipt failure file `CENSUS_AUDIT_FAILED`, not `R3C2_NO_CLASS`.”

## 2. Controls; Q2

Every control has an exact `PASS|FAIL|NOT_RUN` token, and unreached controls are expressly `NOT_RUN`. C0 requires an exhibition table; C1–C3 and C4 packet redaction require named script output; C5 requires live output; C6 requires an audit record. C4 and C5b are self-reported path-list controls and can omit an unreported access, but the master explicitly limits their claim to the printed list rather than presenting completeness as proven. No control is specified to pass on a bare prose assertion without its stated artefact, although some artefacts require human independent judgment.

**Q2 — CHECKABLE, WITH AN HONEST HUMAN-JUDGMENT FLOOR.** Test record: a paper says “We choose β=0.25,” while the ledger records `status=PRINTED`, `origin=FITTED`, `reason_code=ORIG_FIT_STATED`, and quotes that sentence verbatim. The validator can confirm that the quotation and value occur, but cannot semantically turn “choose” into `ORIG_CHOICE_STATED`. The second seat independently classifies it `CHOSEN`; `merge` records `origin_alt` and both evidence objects; `compute` marks the claim `DISPUTED`; C6 re-classifies from the source and catches the same mismatch. If every human independently makes the same semantic mistake, it is not caught; the master states that limit rather than claiming full automation. `root_origins` and `rests_on` are script-derived and cannot be seat-authored.

The BLOCKED-status defect in §1 prevents C2/C3 from validating all inputs and therefore remains a control defect too.

## 3. Circularity

The packet contains no hypothesis, comparison model, preferred outcome, or empirical pattern. Inclusion uses a neutral printed-numeral rule, exclusions are fully ledgered and independently audited, and any seat disagreement stops at `CENSUS_DENOMINATOR_DISPUTED`. Origin classification is independently duplicated and audited rather than reconciled. The interpretation protocol is inaccessible to seats and externally receipted before limb A. A contrary claim cannot be quietly excluded without appearing in the complete candidate/exclusion audit. I find no operative circularity channel.

## 4. The blind

The design makes the redaction machine-asserted through a named builder, required first-line result, forbidden-list check, committed packet, and separate pin. The supplied packet contains the operative seat instructions while omitting the master-only interpretation and history. C4 does not technically prevent deliberate absolute-path access; it uses a redacted out-of-lane directory plus self-reported path lists. The master states this exact limit: “**This is procedural, not enforced by the filesystem**.” It also honestly limits the forbidden-list assertion and prior-exposure claim. The mechanism supports only the bounded claim it makes.

Q5, answered from the packet before reading the master: **No.** No packet sentence says any outcome is more consequential to the lane or anyone. “INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`” is filing precedence, not consequence.

Q3, answered from the packet before reading the master: **No substantive inference.** No sentence leaks a hypothesis, comparison model, preferred outcome, or hoped-for pattern. “Neither contaminates the other” and the required wording “unreproduced from the stated inputs,” not “error,” reveal methodological concerns only, not which result the lane wants.

## 5. The seal

The two-receipt relay binds the protocol hash and commit id before limb A, then the tally hash and commit id before opening the protocol. The external custodian receipts both, independently re-hashes both artefacts, and the report must print the receipts and four verified values. Missing or mismatched receipts file `CENSUS_AUDIT_FAILED`. This binds the artefact versions before the relevant steps. The master accurately says it proves existence and ordering, not that the lane owner had not guessed the answer.

## 6. Fairness

Negative result wording is consistently “unreproduced from the stated inputs,” never “error,” in the operative sections. `REPRO_FAILED`, `REPRO_BLOCKED`, and `REPRO_INPUT_ABSENT` do not accuse authors of error.

Defect 2 (SUBSTANTIVE UNDER THE REQUESTED CLASS-NAME TEST). Quote: “**`REPRO_EXACT`** — the paper's number follows, within its own stated precision”. The class name asserts exactness while the definition permits rounding agreement.

Exact replacement: globally rename `REPRO_EXACT` to `REPRO_WITHIN_STATED_PRECISION`, including the arithmetic-group definition and every consumer. This changes the filed class name and therefore is not marked cosmetic under the requested test, although it does not change any tested numeric value.

## 7. Stall / executability; Q4

**Q4 — NO, not everything is executable as delivered, because the declared `REPRO_BLOCKED` input cannot be encoded in the delivered three-status ledger contract.** That is the executable failure detailed in §1.

Within the instruction not to open any file other than the packet and master, I checked filesystem metadata rather than contents for every named delivered dependency: `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, `r3c2_seat_packet/SEAT_BRIEF.md`, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`, and `/usr/bin/python3`; all exist. The dispatch script and `/usr/bin/python3` are executable. Live commands returned Python 3.9.6 and SymPy 1.14.0; `command -v python3` returned `/usr/bin/python3`, and `command -v shasum` returned `/usr/bin/shasum`. I did not execute or inspect the other scripts, pin, manifest, brief, or protocol because doing so would open files expressly forbidden by the gate instructions. Therefore their internal behavior and pinned digests were not independently verified in this review. Placeholder ledger/output files are run products, not claimed pre-existing deliverables.

All literal seat-facing paths in the packet are relative to the printed working directory or use `/usr/bin/python3`; the one lane-side absolute dispatch path exists. The literal commands are syntactically specified, but C1–C3 cannot cover the blocked-input case until Defect 1 is repaired.

R3C2_V13_GATE_COMPLETE
