ACCESS_SHA=b293f14016f20aca40c50f258ab52c53a9a16524623107b1eeefc5a51174762c
PACKET_SHA=fab99a5e4983678fe51f7d2e94df8a0a0bd3e267b8ec706a463bfe7196c3a2df
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=R3C2_NO_CLASS, from “a packet or seat-isolation failure before dispatch stops dispatch without a census tally.”

## 1. Outcome classes

The per-claim classes are exhaustive and mutually exclusive as written because the terminal precedence resolves genuine overlaps, and the final arithmetic group is binary. A real overlap is a printed claim that states no producing procedure and also cites an unavailable input: it satisfies the descriptive facts behind `REPRO_NO_DERIVATION_STATED` and `REPRO_BLOCKED`, but the explicit order files only `REPRO_NO_DERIVATION_STATED`. I found no included claim that fits no class.

The study-level conditions can overlap, but the total precedence makes the filed class unique. For example, a denominator dispute plus a later-discovered audit mismatch would satisfy two descriptions if both facts exist, but `CENSUS_DENOMINATOR_DISPUTED` files first and makes the later limb unreached. I found no reachable study state with no class. `CENSUS_PARTIAL` is genuinely reachable, hence INCONCLUSIVE is reachable: one included claim with a stated recipe and an input neither printed nor traced to a named source files `REPRO_INPUT_ABSENT`, which is non-arithmetic, so the study files `CENSUS_PARTIAL` absent a higher-precedence condition.

Q1. YES. Under option (c), §3 makes every declared per-claim outcome reachable while keeping the reproduction question decidable. Hard cases trace cleanly:

- Printed-but-chosen input: status `PRINTED`, origin `CHOSEN`; consume it. Matching arithmetic files `REPRO_EXACT`; nonmatching arithmetic files `REPRO_FAILED`. The script traces the chosen leaf and computes `rests_on=USES_CHOSEN`.
- A fitted parameter the paper states: status `PRINTED`, origin `FITTED`; consume it. It files exactly one arithmetic outcome, and the script computes `rests_on=USES_FITTED`.
- Imported value: if the named pinned enumerable source machine-matches the value at the cited line, status `PRINTED`, origin `IMPORTED`; consume it, file exactly one arithmetic outcome, and compute `rests_on=USES_IMPORTED`. If the source is outside the enumerable manifest or has no match at the cited line, status `BLOCKED`, origin `IMPORTED`, no value; precedence files `REPRO_BLOCKED`, and the input is not consumed.

The reproduction verdict and provenance tally therefore do not compete for the same class slot.

## 2. Controls

All controls have exact `PASS|FAIL|NOT_RUN` codes, and the document globally says unreached controls are `NOT_RUN`. C0 has an exhibition table; C1–C3 have JSON and command output; C5 has live command output; C6 has a re-audit record. C4/C5b deliberately use printed path lists and expressly disclaim completeness, so their PASS is self-report about the list, not proof of all access. That limitation is honestly stated.

Q2. The provenance field is checkable, but not infallibly machine-decided. Test record: for the sentence “We adopt H₀ = 67.4 from Planck (2018),” write `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and quote that sentence. This is plausible and wrong under the declared precedence: naming the external source makes `ORIG_CITATION`/`IMPORTED` win. Substring matching alone will not catch the semantic error. The second seat catches it by independently classifying the same source sentence; merge records the disagreement as `origin_alt`/`origin_evidence_alt`, and compute marks the claim `DISPUTED`. C6 independently re-classifies from the pinned source and can also catch it. If all human readers make the same mistake, it stands; the master expressly acknowledges that floor. `root_origins` and `rests_on` are script-computed rather than asserted.

Finding 2.1 — substantive, not cosmetic. Quote: “`/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json>` — exit 0 only if every candidate carries exactly one disposition and the printed counts equal the recomputed counts”. Defect: the stated candidate schema contains only row fields, and the command receives only the two ledgers. No declared field or argument supplies the previously printed counts, so the validator cannot compare those external printed counts to its recomputation as claimed. C1 can establish row disposition and recomputed counts, but its decisive printed-versus-recomputed predicate is not executable from the specified inputs.

Exact replacement: “The candidate file is a JSON object `{declared_candidate_count, declared_included_count, declared_excluded_count, candidates:[...]}` and the exclusion file is `{declared_exclusion_count, exclusions:[...]}`. Before the tally, print those four declared counts verbatim from the files. Run `/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json>`. PASS requires exit 0 after the script verifies every candidate has exactly one disposition, every exclusion names one excluded candidate, and each declared count equals the count recomputed from the rows; stdout prints both declared and recomputed counts.” Update the script/schema accordingly.

## 3. Circularity

The packet exposes no hypothesis, comparison model, preferred result, or anticipated empirical pattern. Its line “No tier, warrant token, standing or stamp moves” supplies no directional stake. The procedural difference visible in Q5 is real but does not identify which empirical tally the lane wants.

The inclusion rule is outcome-neutral, two seats enumerate independently, every exclusion is ledgered, and C6 audits candidate/exclusion completeness against every pinned source. A contrary claim therefore cannot be quietly excluded without falsifying or omitting an auditable candidate record. Origin can affect later interpretation through `rests_on`, but each origin requires cited evidence, independent classification, scripted transitive computation, and C6 re-classification. The pre-receipted interpretation protocol cannot be rewritten after seeing the tally. I find no operative route by which the lane's expectation reaches evidence selection or classification.

## 4. The blind

The packet states no redaction claim about itself; the master claims the builder strips named sections/spans and rejects enumerated forbidden strings. That is a machine assertion of a finite forbidden list, not a semantic proof, and the master says exactly that: “the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone”. C4 likewise says filesystem isolation is procedural and self-reported. Thus the mechanism can prevent dispatch of output containing a listed literal string, but cannot prove the absence of paraphrased stakes, outside absolute-path access, or prior exposure. Those limits are honest.

Packet-only Q5. YES, procedurally. The sentence “a packet or seat-isolation failure before dispatch stops dispatch without a census tally” makes `R3C2_NO_CLASS` operationally more consequential to the lane than an outcome that permits a tally. No packet sentence makes one scientific reproduction result more consequential to the lane or anyone else.

Packet-only Q3. I can infer no hypothesis, comparison model, preferred scientific outcome, or expected pattern. The packet gives symmetric reproduction/provenance rules, does not map any tally to an interpretation, and says “No tier, warrant token, standing or stamp moves.” The stop/no-tally clauses reveal workflow consequences only; they do not reveal which empirical result is hoped for.

## 5. The seal

Receipt P binds the protocol hash and commit id in an external custodian's timestamped record before limb A may begin. Receipt T binds the tally hash and commit id after tally commit and before protocol opening. Independent re-hashing verifies all four values, and a missing receipt or mismatch files `CENSUS_AUDIT_FAILED`. This binds the protocol and tally versions, in the required order, before comparison. The stated limitation is accurate: it proves chronology and identity of records, not the author's mental ignorance.

## 6. Fairness

The required negative prose is “unreproduced from the stated inputs,” never “error,” and blocked/absent/non-evaluable outcomes do not accuse the paper of error.

Finding 6.1 — substantive under the requested class-name test, not cosmetic. Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision”. Defect: equality after rounding within stated precision is not exact equality; the class name asserts more than its definition and can label a numerically different reproduced value “EXACT.”

Exact replacement: replace `REPRO_EXACT` with `REPRO_WITHIN_STATED_PRECISION` everywhere, including the arithmetic-group definition, precedence consumers, packet, scripts, schemas, C0 rows, and reports. Keep the existing numerical definition and the requirement to report both numbers.

## 7. Stall / executability

Q4. NO: C1's printed-count comparison cannot be executed from its declared command inputs and schema, as Finding 2.1 shows. The exact replacement there makes it executable.

Checks performed without opening any file beyond the packet and master, as required:

- Ran the mandated master SHA command and the packet SHA command; both files were readable and hashes are printed above.
- Read the packet first and fixed Q5/Q3 before reading the master; then read the master in full.
- By filesystem metadata, confirmed existence of `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_build_seat_packet.py`, `r3c2_seat_packet/SEAT_BRIEF.md`, root-level `R3C2_SEAT_PACKET.sha256`, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`, and `/usr/bin/python3`; confirmed `/usr/bin/shasum` resolves. The named pin is root-level; `r3c2_seat_packet/R3C2_SEAT_PACKET.sha256` does not exist, but the operative master names the root-level path, so this is not a finding.
- Checked the literal ledger interfaces `census`, `validate`, `compute`, and `merge`, their placeholders, `sources_dir='.'`, computed fields (`root_origins`, `rests_on`), merge-added fields, status/origin enums, and stated exit meanings against the written design. I did not execute them because doing so requires opening the prohibited script and source/ledger files, and the placeholder ledgers do not yet exist.
- Checked the builder command, its expected output, packet path, pin path, dispatch path, harness commands, sampling expression, receipt fields, and 120-second timeout specification against the written design. I did not execute the builder because it would read the master and script and may rewrite the committed packet; I did not execute the harness's hash/import commands because they open files beyond the two-file review scope.
- Future run products such as `<candidates.json>`, `<exclusions.json>`, `<ledger.json>`, `<out.json>`, merged ledgers, receipts, tally, interpretation report, and dated check sheet are not asserted to exist before the run and are therefore not missing-deliverable findings.

No other delivered artifact named by the operative sections was missing by metadata. Digest/content correctness of third files was not claimed checked, because the review instruction prohibited opening them.

R3C2_V14_GATE_COMPLETE
