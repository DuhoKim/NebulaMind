ACCESS_SHA=b293f14016f20aca40c50f258ab52c53a9a16524623107b1eeefc5a51174762c
PACKET_SHA=fab99a5e4983678fe51f7d2e94df8a0a0bd3e267b8ec706a463bfe7196c3a2df
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology benchmarked to a printed Planck-2018 STANDARD list, and the design anticipates printed recipes that consume chosen/fitted constants and is built so reproduction and provenance are both recorded (packet §1 "chosen constants included", §2 step 4 parenthetical); plus process-history tells (iterated repair). No hypothesis, comparison-model preference, or preferred outcome is inferable.
CONSEQUENCE_VISIBLE=NO

R3C2 V14 GATE — kimi seat, 2026-09-05. Design judged only; no derivation run; §10 treated as history
binding nothing; no gate files opened; only the two named files were read. The packet header's claim
"Built from master sha256 b293f140…" matches the ACCESS_SHA above, so the packet I read was built
from the master I read.

======================================================================
STEP 1 — PACKET ALONE (answered before the master was opened)
======================================================================

Q5 (answered first, as ordered). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE
CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

No sentence does. The only consequence-bearing sentences point the other way or neutralize:
- Packet §11: "No tier, warrant token, standing or stamp moves. Published sources only; nothing from
  another lane. Paper HOLD; nothing outward." — states that no outcome moves anything, whichever
  way the tally falls.
- Packet §3, REPRO_FAILED: "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" — this
  reveals the document anticipates a REPRO_FAILED being read as an accusation and pre-defangs it;
  it constrains how a negative is phrased, not whether it is found, and assigns no consequence to
  filing one.
- Packet §4 labels CENSUS_PARTIAL "INCONCLUSIVE" and gives a filing precedence; a label and a filing
  order are not a consequence ranking.
- The parenthetical repair notes ("…how the last defect got in", "Previously…", "Added because…")
  tell me the design has been through adversarial repair rounds — process history, no stake.
CONSEQUENCE_VISIBLE=NO.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

Everything I can infer, each with the sentence that leaks it:

(i) The corpus is cosmology, and provenance will be tallied against the Planck-2018 baseline as the
    notion of "standard". Leak: packet C3's closed STANDARD table (H₀ = 67.36, Ω_m = 0.3153, …) and
    "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline. **A value not in this
    table is not `STANDARD`**, whatever its provenance." A seat cannot classify STANDARD inputs
    without these values, so this is content the task requires — but it also fixes the comparison
    baseline every paper's constants will be sorted against.

(ii) The lane expects claims whose printed recipes consume chosen or fitted constants, and has ruled
    that following such a recipe counts as reproduction. Leaks: §1 "…applied to the inputs it
    states — chosen constants included"; §2 step 4 "…chosen and fitted values included" and the
    parenthetical "(A paper can direct you to use its own chosen constant, and following that
    instruction is reproducing the paper.)"; §3 "a value can be printed in the paper and still have
    been chosen or fitted, and under (c) both facts survive: the arithmetic reproduces AND the
    ledger says what it rested on." The two-tally architecture exists to make one specific
    combination reportable — arithmetic that reproduces while resting on chosen/fitted/imported
    inputs. That machinery is dead weight unless the lane expects that combination to occur. This
    is an expectation about what the corpus CONTAINS; it is not a stated hope about which way the
    tally lands, and I cannot infer one — the same apparatus records a mostly-DERIVED/STANDARD tally
    and a mostly-CHOSEN one with equal fidelity.

(iii) The lane expects cross-text value import inside the corpus. Leak: §2's PRINTED-from-source
    IMPORTED machinery ("A value the paper does not print but traces to a named source that is
    itself an enumerable text in R3C2_CORPUS_MANIFEST.md…") only matters if corpus papers cite each
    other's values.

(iv) The lane expects origin-classification disagreement but tolerates it up to a bound. Leak: §4
    CENSUS_ORIGIN_DISPUTED triggers only "above 10% of included claims" — a chosen tolerance.

(v) Process history: the design has survived multiple adversarial repair rounds, and a defect once
    entered through a duplicated rule. Leaks: §6 "(This sentence previously survived here after
    being repaired in §2: the second live copy of a repaired rule is how the last defect got in…)",
    and the "Added because…/Previously…" parentheticals in §3/§4. No hypothesis content; a seat
    learns this document is iterated and defect-hunted.

(vi) §11 "Paper HOLD; nothing outward" tells me the lane anticipates results that could be carried
    outward and forbids it in advance — neutral as to direction.

What I CANNOT infer: any hypothesis statement, any named comparison model, any preferred study-level
outcome, and any preferred direction of either tally. The stake cannot be reconstructed beyond
inferences (i)-(vi).

======================================================================
1. OUTCOME CLASSES
======================================================================

Per-claim (§3): six outcomes plus a total precedence (REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED,
REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group {REPRO_EXACT, REPRO_FAILED}).
Candidate exclusions are a separate ledger with five enumerated kinds, not outcomes — good; that
removes the old denominator-moving channel.

Exhaustiveness and exclusivity, tested by construction:
- A claim with a stated recipe and all inputs PRINTED/STANDARD, arithmetic completing: EXACT if it
  rounds to the printed numeral at the stated-or-printed precision, else FAILED. The two are
  complements over completed attempts; the precedence makes every co-occurrence single-filed.
- No procedure stated: NO_DERIVATION_STATED. Unprinted input, named source not an enumerable pinned
  text, or pinned but no machine-match at the cited line: BLOCKED. Unprinted and traced to no named
  source: INPUT_ABSENT. Timeout or missing machinery: NOT_EVALUABLE. Every constructed case lands in
  exactly one class; every two-class overlap I constructed is resolved by the stated precedence
  (e.g., an absent input whose would-be source is also unobtainable: BLOCKED precedes ABSENT).
- One boundary case does NOT land uniquely — it is F1 below, developed under Q1.

Study-level (§4): seven classes with a total precedence (R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT,
CENSUS_DENOMINATOR_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL,
CENSUS_COMPLETE) and "Once a stop class applies, later limbs are unreached and their controls are
NOT_RUN." A tally satisfying two classes (e.g., a split control plus a both-seats control failure)
files the first deterministically. I could construct no tally fitting zero classes: controls fail
(NO_CLASS/SPLIT), enumerations disagree (DENOMINATOR_DISPUTED), origins diverge >10%
(ORIGIN_DISPUTED), audit or seal fails (AUDIT_FAILED), some claim non-arithmetic (PARTIAL), else
COMPLETE. A pre-limb-A stall (receipt P never arrives) is a missing receipt and files
CENSUS_AUDIT_FAILED by §7's "Any missing receipt or mismatch files CENSUS_AUDIT_FAILED" — covered.
INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL files whenever at least one included claim
carries a non-arithmetic outcome — one named-but-unobtainable source anywhere in the corpus
suffices, a plausible state, not a degenerate one.

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND
KEEP THE REPRODUCTION QUESTION DECIDABLE?

Answer: NO as written — every hardest case I constructed traces to exactly one outcome and one
rests_on value EXCEPT one boundary case (F1), which two live rules route to two different outcomes
with no precedence and no dispute mechanism. With F1's one-sentence repair, YES on both.

The cases that trace cleanly:
- Printed-but-chosen input (the old tension's home): paper prints "we take β = 1/929.25" and its
  recipe prints a result. Status PRINTED; origin CHOSEN via ORIG_CHOICE_STATED; the arithmetic
  consumes it (PRINTED is consumable "whatever its origin"); outcome REPRO_EXACT or REPRO_FAILED;
  the script computes rests_on = USES_CHOSEN from the root set. One outcome, one rests_on. The
  retired REPRO_AFTER_CHOICE's content survives as a field, and decidability is untouched because
  provenance is recorded, never filtered.
- A fitted parameter the paper states: "our fit gives n_s = 0.96, whence …". The downstream input
  is PRINTED, origin FITTED; where the same sentence also matches ORIG_EQUATION, the reason-code
  precedence (ORIG_FIT_STATED before ORIG_EQUATION) files FITTED deterministically. If the paper
  states the fit as a derivation, the record is DERIVED with derived_from, and the script's
  transitivity computes the root set — exit 1 on a dangling id, a cycle, or a DERIVED record with
  no parents, so an empty or laundered root set cannot pass silently.
- An imported value, all four sub-cases: (a) named source is an enumerable pinned text with a
  machine-match at the cited line → PRINTED from that source, origin IMPORTED, consumed; (b) pinned
  enumerable, no match at the cited line → REPRO_BLOCKED; (c) named source not in the manifest →
  REPRO_BLOCKED; (d) named source listed RAW in the manifest → "not an enumerable text" →
  REPRO_BLOCKED. Each lands in exactly one outcome; the BLOCKED record carries status BLOCKED,
  origin IMPORTED, ORIG_CITATION to the naming sentence, no value, and the arithmetic never
  consumes it.
- A cited-and-printed standard value: "We adopt H₀ = 67.4 from Planck (2018)" — status PRINTED in
  the claiming paper; the sentence names an external source, so the reason-code precedence files
  IMPORTED ("a sentence that names an external source for the value is a citation whatever else it
  says"); rests_on USES_IMPORTED; arithmetic consumes the printed 67.4. Deterministic.

The case that does NOT trace cleanly — F1 (SUBSTANTIVE):
  Verbatim, §2 step 3: "3. **Classify each input** as `PRINTED` (given in the paper), `STANDARD` (a
  measured constant **on C3's closed list — that list, verbatim, and no other value**), `BLOCKED`
  (traced to a named source but carrying no machine-matchable value, §3), or `ABSENT`."
  Defect: consider an input the paper NAMES but does not PRINT whose value is on the closed list —
  e.g., "assuming the Planck 2018 cosmology", with the recipe then using H₀ and no numeral printed.
  As written, step 3's STANDARD clause applies (67.36 is a measured constant on the closed list,
  verbatim — the clause carries no requirement that the paper print it), so a seat may file STANDARD,
  take the value from the list, and run the arithmetic to an arithmetic-group outcome. Simultaneously,
  §2's named-source paragraph applies ("A value the paper does not print but traces to a named
  source … whose named source is not an enumerable text of the manifest, files REPRO_BLOCKED under
  §3"), routing the same input to status BLOCKED and the claim to REPRO_BLOCKED. Both readings are
  textually available; nothing states a precedence between step 3's disjunction and the following
  paragraph's rule, and the packet orders seats to "apply the rules below exactly as written." The
  choice flips the per-claim outcome between the arithmetic group and REPRO_BLOCKED, and therefore
  flips the study-level class between CENSUS_COMPLETE and CENSUS_PARTIAL. Nor is a two-seat
  divergence recoverable: the merge is specified only for input_id-set differences (exit 1) and
  origin differences (origin_alt); status divergence has no named merge behaviour, and
  CENSUS_ORIGIN_DISPUTED covers origin only. The steelman — "the specific named-source rule beats
  the general status disjunction" — is an interpretive convention the document never states; a
  two-seat split on exactly this reading is the case the dispute machinery exists for, and that
  machinery does not cover status.
  Exact replacement (append to §2 step 3, after the classification sentence):
  "`STANDARD` applies only when the value appears in the claiming paper (or in a pinned enumerable
  text under the `IMPORTED` rule of §2): a value the paper does not print is classified by the
  named-source rule alone and is never `STANDARD`. Where a value the paper prints is on the closed
  list verbatim, file `STANDARD`; otherwise `PRINTED` — the two routes are outcome-identical, and
  this rule keeps both seats on the same one."
  This also deterministically resolves the otherwise-harmless PRINTED/STANDARD overlap for a
  paper-printed closed-list value (both consumable, but the two seats could file different
  statuses).

======================================================================
2. CONTROLS
======================================================================

Pass-by-assertion audit, per control:
- C0 (reachability): artefact-based (an exhibition table with concrete inputs and paths through the
  document), authored by one independent seat and independently verified by a second; both must
  PASS; the lane owner checks coverage only and "does not judge reachability." PASS/FAIL/NOT_RUN
  predicates stated. Cannot be passed by bare assertion — the second seat must reproduce each row.
- C1 (denominator): machine-validated — "exit 0 only if every candidate carries exactly one
  disposition and the printed counts equal the recomputed counts; print its command, stdout and
  exit status." PASS only on exit 0. Not assertion.
- C2 (input ledger): validated by a named command with the sole allowed sources_dir ".", the fully
  resolved command printed before execution; exit 0 = PASS, every failure printed. Not assertion.
- C3 (no substitution): machine-checked schema; the seat-authored ledger MUST omit root_origins,
  rests_on, origin_alt and origin_evidence_alt and "validate fails a ledger that carries any of
  them" — so the computed channels cannot be planted at intake and the merge is the only source of
  the _alt fields; machine assertions named: PRINTED value machine-matches its cited line, each
  verbatim quotation is a substring of that line, STANDARD values are members of the literally
  printed closed list, no ABSENT/BLOCKED record carries a value. Input-list completeness is
  explicitly seat-authored and C6-audited, "not machine-checked" — stated at true strength.
- C4 (seat isolation): PASS requires the printed path list and "means only that the list contains
  no outside path; it makes no claim that the list is complete." Self-report, labelled so (see §4
  of this report).
- C5 (harness): three literal commands, all must exit 0 with full stdout printed; "a transcribed
  value in place of live output is FAIL." Live, not assertible.
- C5b (cross-lane): same printed-list discipline as C4, completeness not claimed.
- C6 (audit): third seat audits the full candidate/exclusion ledgers first (completeness), then
  re-derives every arithmetic-group claim and a frozen min(max(1, ceil(0.20×N)), R) sample of the
  rest — the R = 0 case is handled; the seed is external, post-receipt, 64 lowercase hex, and
  non-delivery files CENSUS_AUDIT_FAILED with C6_AUDIT_SAMPLE=NOT_RUN. Disagreement is reported,
  never reconciled, with a DISPUTED rests_on row.
Every control names an exact code with PASS|FAIL|NOT_RUN, and "Controls in an unreached limb are
NOT_RUN, never passes." Unreached controls are handled.

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?
Answer: YES, up to two floors the document itself states — and I verified the boundary by attack.

Attack, as ordered. Paper line: "We adopt the value H₀ = 67.4 km s⁻¹ Mpc⁻¹ from the SH0ES
collaboration (Riess et al. 2022)." I write a ledger record: status PRINTED (value 67.4 machine-
matches the cited line — passes), origin CHOSEN, reason_code ORIG_CHOICE_STATED, verbatim quotation
"We adopt the value H₀ = 67.4 km s⁻¹ Mpc⁻¹" — a substring of the cited line, so the substring
check passes; the truncation omits the citation. Every machine check passes, and the root is
reported as the less severe CHOSEN instead of IMPORTED. Which mechanism catches it? Never the
machine: the reason-code precedence (ORIG_CITATION first, "a sentence that names an external source
for the value is a citation whatever else it says") is a rule seats apply, not a check the script
runs, and the substring test cannot see a truncated quotation. The catchers are (a) the second
seat's independent origin classification — if it quotes the full sentence it files IMPORTED, and
the merge marks origin_alt and the claim carries the DISPUTED pair; and (b) the C6 audit's
re-classification of origin from the pinned sources, whose failure to reproduce files
CENSUS_AUDIT_FAILED — for arithmetic-group claims the audit is exhaustive, for the rest it is the
frozen sample, so a misclassified origin on a non-arithmetic claim has only the sampled chance of
audit. The master states this floor verbatim: "A reason code that matches its quotation but
misapplies the precedence is caught only by the second seat's independent classification and the C6
re-classification, never by the machine; if every reader misclassifies identically, the record
stands — that floor is stated here rather than implied away." (That sentence sits in a SEAT-REDACT
span, correctly — it describes the floor to the master reader, not to the seat.) The second floor:
ORIG_SILENT's origin_search {query, files, matches} is required and printed, but the search's
adequacy is judgement, dual-read and audited — same machinery, same floor. What IS machine-checked
is real: quotation-to-line match, value-to-line match, closed-list membership, no-value on
ABSENT/BLOCKED, intake rejection of seat-written computed fields, script-computed root_origins and
rests_on with referential integrity and acyclicity. Conclusion: checkable, not merely asserted,
with the residual human-judgement floor named and bounded by independent re-classification.

======================================================================
3. CIRCULARITY
======================================================================

Can the lane's expectation reach the evidence? The channels, one by one:
- Inclusion/exclusion: §1's rule is operational (a printed numeral asserted as the paper's own
  result, five enumerated excluded kinds), applied independently by two seats, tolerance zero, two
  reconciliation attempts, then CENSUS_DENOMINATOR_DISPUTED with both ledgers reported. A contrary
  claim cannot be quietly excluded: every candidate carries a disposition in a machine-validated
  ledger (C1 exit 0 requires exactly one disposition per candidate and recomputed counts), the
  exclusion ledger names file, line, numeral and kind, and C6's first act is auditing the FULL
  candidate and exclusion ledgers against every pinned source — completeness, including candidates
  that never made the ledger. An exclusion survives only with a public row and a kind, audited.
- Origin classification: cited reason code + machine-matched quotation + deterministic code
  precedence + dual independent classification + disputes reported-never-reconciled + audit
  re-classification. The tunable surface is reason-code choice on ambiguous sentences; it is
  dual-read and audited, with the identical-misclassification floor stated.
- The arithmetic: mechanical over status-PRINTED/STANDARD records; provenance is never an input
  filter, so the expectation cannot suppress a reproduction by disliking its provenance — the
  exact failure mode of the retired design.
- rests_on: computed by the pinned script from the ledger; seats cannot write it (intake rejection),
  and a missing script-produced rests_on fails C3.
- The interpretation step: excised to a protocol no seat may open, committed and receipted (P)
  BEFORE limb A, so the mapping cannot be written to suit the tally; the tally is receipted (T)
  before the protocol is opened. The study-level classes contain no hypothesis mapping.
Residual observation, not a defect: the master states the rests_on rule the script implements
(DERIVED_ONLY, then the fixed severity order USES_UNDECLARED > USES_IMPORTED > USES_FITTED >
USES_CHOSEN) in a SEAT-REDACT span, while C4's own description puts the script itself in the seat's
dispatch directory. A seat that reads the script can therefore see the severity order even though
the document withholds it. It cannot act on that knowledge — it never writes rests_on, the
arithmetic ignores origin, and origin classifications are cited, dual-read and audited — so the
channel is informational only; but the redaction boundary and the dispatch set are not perfectly
aligned, and the lane should know that. (Deduced from the master's own text; I did not open the
script, per the two-file confinement.)
Conclusion: the design closes the expectation-to-evidence channels as far as a human-executed
census can; the residual judgement surfaces are dual-read, audited, and their floor is stated.

======================================================================
4. THE BLIND
======================================================================

Machine-asserted as claimed? YES, and I corroborated the delivered artefact. The builder
(r3c2_build_seat_packet.py, present in the working directory) drops §0, §7, §8, §10 whole, strips
every SEAT-REDACT span, and asserts no forbidden-list string survives, else the packet is not
written and C4_PACKET_REDACTED=FAIL. The delivered packet matches that description: sections
present are exactly 1, 2, 3, 4, 5, 6, 9, 11; C4 appears only as the seat-visible half ("what the
seat must do"); and my own scan of the packet (one of the two named files) found ZERO occurrences
of every forbidden-list item and redacted-span vocabulary I tested: pattern, expectation, refute,
consequential, invisible, favourable/unfavourable (either spelling), stake, prior, tempting, warn,
the custody-chain names, both engine names, R3D, ΛCDM/LCDM, magnitude, shape, hypothesis,
interpretation, severe, DERIVED_ONLY, and all four USES_* tokens. Survivors are neutral process
words: "verdict" ×3 (used for the per-claim reproduction outcome itself), "previously" ×3,
"defect" ×1, "repair" ×1 — the repair-history parentheticals analysed under Q3(v); none carries
hypothesis content. The packet's redaction claim is therefore consistent with the delivered file,
and the pin verifies it (below).
Does C4's mechanism prevent what it claims? It claims: the seat is not given the pattern record,
cannot read it from its working directory, and any outside path it prints fails the control. The
mechanism (redacted out-of-lane copy directory + printed path lists + dispatch-record digest
listing) delivers exactly that, and the document states its true strength three times: "This is
procedural, not enforced by the filesystem", "C4_SEAT_ISOLATION is a self-reported control with a
structural aid, and is labelled so", and PASS "makes no claim that the list is complete." A seat
that opens an absolute lane path and omits it from the printed list defeats detection — the
document says so. The forbidden-list claim is also stated at true strength: "the list blocks the
enumerated strings and does not establish that every consequence-bearing word is gone." The honest
limit is stated verbatim: "It cannot prove a seat has no prior exposure from training or an earlier
session — nothing available here can. The record states that limit rather than implying a stronger
blind." No defect.

======================================================================
5. THE SEAL (§7)
======================================================================

Does the receipted relay bind what it claims to bind, before the step it must precede? YES.
- Receipt P: the interpretation protocol's hash and commit id go to the external custodian, who
  acknowledges and timestamps them in a preserved receipt, and "limb A does not begin without
  receipt P." The protocol is therefore bound BEFORE any enumeration exists — the tally cannot
  influence it. The V8-era overclaim (commit-before-tally "so" it cannot suit the tally) is gone;
  the binding now runs where the claim requires it.
- Receipt T: the tally hash and commit id are receipted "after the tally is committed and before
  the protocol is opened"; "the interpretation protocol is not opened without both receipts." The
  tally is bound before the comparison is filed.
- Post-opening, the custodian independently re-hashes both artefacts and verifies all four values
  against the two receipts; the interpretation report must print both receipts and the four
  verified values; "Any missing receipt or mismatch files CENSUS_AUDIT_FAILED … leaves the
  interpretation NOT_RUN and voids the comparison." §4's CENSUS_AUDIT_FAILED names the seal case,
  and the class precedence places it correctly.
- The commit id alone is correctly discounted ("a local commit in Tori's own custody can be amended
  or reset, so it is tamper evidence, not an external custodian"); the custodian's re-hash makes
  amendment detectable.
The overclaim guard is honest and verbatim: "this proves the tally and the protocol both existed,
in the stated form, before the comparison was filed. **It does not prove Tori had not already
guessed the answer** — nothing available here could prove that." What it claims = what it binds.
No defect.

======================================================================
6. FAIRNESS
======================================================================

- Negative wording: REPRO_FAILED carries "Wording: \"unreproduced from the stated inputs,\" not
  \"error.\"" — the only accusation-shaped word is pre-forbidden, and both numbers are reported.
  REPRO_BLOCKED, REPRO_INPUT_ABSENT and REPRO_NO_DERIVATION_STATED describe the census's access and
  the paper's text, not fault. "UNDECLARED is the default, not the residue" removes the
  guilt-by-silence reading. The rests_on tokens (DERIVED_ONLY, USES_*) are descriptive of
  provenance; the severity collapse for mixed roots picks one label but prints "the full
  root-origin set beside it", so nothing is hidden by the collapse.
- F2 (SUBSTANTIVE under this gate's class-name test; already escalated in-document to the
  principal): Verbatim, §3: "**`REPRO_EXACT`** — the paper's number follows, within its own stated
  precision, **from the paper's own recipe applied to the inputs it states**". The class NAME
  asserts exact equality; the DEFINITION is within-stated-precision, or rounds-to-the-printed-
  numeral where no precision is stated. A claim that reproduces as 13.797 against a printed
  "13.8 Gyr" files REPRO_EXACT, and the tally row carries the word EXACT into every downstream
  reader's hands. The name therefore asserts more than its definition — this is the document's own
  recorded escalation (a rename is the principal's call, noted in the redacted §3 span), and I file
  it as a required repair under the brief's direct question. Mitigant: "Report both numbers" keeps
  the evidence beside the label, so no tested value is misstated; the defect travels in the label.
  Exact replacement: rename the class `REPRO_WITHIN_STATED_PRECISION` at every occurrence (§3 class
  head, the §3 precedence list, the arithmetic-group definition, §4 CENSUS_COMPLETE, C0's row set).
- No other class name or clause asserts more than its definition: CENSUS_PARTIAL is labelled
  INCONCLUSIVE and means exactly its definition; CENSUS_AUDIT_FAILED is about the audit; the
  severity ORDER is declared, fixed, script-computed and master-documented — declared-now, not
  tuned post-hoc.

======================================================================
7. STALL / EXECUTABILITY
======================================================================

Stall surfaces: the 120-second symbolic cap files REPRO_NOT_EVALUABLE with SYMBOLIC_TIMEOUT and the
point reached; one repeat is permitted, "meaningful only for REPRO_NOT_EVALUABLE"; reconciliation
is bounded at two attempts (enumeration) and two attempts (controls); a control failing in every
seat after two attempts files R3C2_NO_CLASS, a split files CENSUS_CONTROL_SPLIT with "do not adopt
the passing seat's result"; a seed that never arrives files CENSUS_AUDIT_FAILED with
C6_AUDIT_SAMPLE=NOT_RUN; unreached limbs' controls are NOT_RUN. No unbounded wait state remains.

Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Checks run, each named:
- R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md — exists; sha256 = ACCESS_SHA (line 1).
- r3c2_seat_packet/R3C2_SEAT_PACKET.md — exists; sha256 = PACKET_SHA (line 2); verified against the
  pin file by `shasum -a 256 -c R3C2_SEAT_PACKET.sha256` from the working directory:
  "r3c2_seat_packet/R3C2_SEAT_PACKET.md: OK" and "r3c2_seat_packet/SEAT_BRIEF.md: OK". The pin
  covers both artefacts the master says it covers.
- r3c2_ledger_tools.py — exists in the working directory; sha256
  c2b62e31029171adebfdc6aa7d77ff61a82749237d627ff46ba6e1a0457788d1, matching the pin printed in both
  named files. Its four subcommands (census/validate/compute/merge), exit-code behaviour (2 on
  pre-set computed fields, 1 on dangling derived_from/cycle/DERIVED-without-parents and on
  input_id-set mismatch) and its computed fields (root_origins, rests_on, origin_alt,
  origin_evidence_alt) are internally consistent across §2/§3/C1/C2/C3 — but I did NOT open or
  execute the script (the brief confines me to two files), so functional behaviour is verified to
  pin-identity only. That is the honest limit of this check.
- R3C2_CORPUS_MANIFEST.md — exists; verified sha256
  300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24, matching the pin printed in §2
  of both named files. The corpus's own files were not opened (two-file confinement).
- r3c2_build_seat_packet.py — exists. Its expected first line (C4_PACKET_REDACTED=PASS) was not
  re-run (executing the builder is outside the two-file confinement); the delivered packet is
  consistent with its asserted behaviour (section-4 scan above) and matches its pin.
- r3c2_seat_packet/SEAT_BRIEF.md — exists; pin-verified OK (see above). Note the packet tells the
  seat to work from "the seat brief" without naming its path; the master names it
  (r3c2_seat_packet/SEAT_BRIEF.md). Cosmetic; the dispatch set makes it unambiguous in practice.
- R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists on disk. §7 requires it committed before
  limb A with receipt P; the study is pre-freeze, so its presence is consistent. Not opened —
  no seat may open it, and neither may I.
- /Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh — exists at the absolute path §9 names.
- C5's literal commands: `/usr/bin/python3 --version` → "Python 3.9.6", exit 0;
  `shasum -a 256 /usr/bin/python3` → exit 0 (digest b8763cf2…); the sympy import — my sandbox
  blocked the `-c` form, but a module probe (`python3 -m sympy`) returned "'sympy' is a package",
  i.e., sympy is installed and importable under the pinned interpreter. All three C5 commands are
  executable as delivered in this environment.
- Paths seats are told to use: the working directory and r3c2_seat_packet/ both exist; "." as the
  sole sources_dir is coherent with C2's resolved-command printing.
- Fields the script is said to compute (root_origins, rests_on, origin_alt, origin_evidence_alt)
  are named consistently in the C3 schema, the intake-rejection list, the compute and merge
  descriptions; the candidate schema (candidate_id, source_file, source_line, numeral, included)
  and the five exclusion kinds are stated. Consistent as written; behaviour pin-verified only.
No described artefact is missing or digest-mismatched as delivered. Findings F1–F4 below are
defects of the text, not of delivery.

FINDINGS (verbatim quote — defect — exact replacement):
- F1 (SUBSTANTIVE): quoted and developed under Q1 in section 1. §2 step 3's STANDARD clause admits
  a named-but-unprinted closed-list input that §2's named-source rule routes to REPRO_BLOCKED; no
  precedence, no status-divergence mechanism; flips per-claim outcome and study-level class.
  Replacement given in section 1.
- F2 (SUBSTANTIVE by the class-name test; the rename is already the principal's escalated call):
  quoted and developed in section 6. Replacement: rename to REPRO_WITHIN_STATED_PRECISION
  everywhere.
- F3 (MINOR): Verbatim, §3 REPRO_EXACT: "The claim's `rests_on` is reported beside it." and
  REPRO_FAILED: "`rests_on` is reported beside it." The other four outcomes say nothing about
  rests_on, while §4 reports "the `rests_on` tally beside it" — for a CENSUS_PARTIAL the tally's
  membership is undefined (does a REPRO_BLOCKED claim, which has a ledger record, enter the
  rests_on tally? does a REPRO_NO_DERIVATION_STATED claim, which has none?). Defect: a reported
  tally's denominator is unspecified; two seats could compose it differently and the interpretation
  step reads that tally. Exact replacement (append after the arithmetic-group paragraph in §3):
  "`rests_on` is computed and reported for every included claim that has at least one ledger
  record, whatever its outcome; a claim with no ledger record carries `rests_on` `NOT_COMPUTED`,
  and the `rests_on` tally reports a `NOT_COMPUTED` row."
- F4 (COSMETIC): Verbatim, §3: "**`REPRO_NOT_EVALUABLE`** — the arithmetic could not be completed
  within the 120-second cap, or requires machinery this lane does not have. Print `SYMBOLIC_TIMEOUT`
  and the point reached." For the machinery limb no timeout occurred; the required token
  misdescribes the cause. Exact replacement: "Print `SYMBOLIC_TIMEOUT` when the 120-second cap is
  exceeded, or `MACHINERY_UNAVAILABLE` when the lane lacks the machinery, and the point reached."
  Marked COSMETIC: same class filed, no control's passability, reachability, conclusion or tested
  value changes.

VERDICT RATIONALE: the core design holds — inclusion is operational, dual-read, machine-validated
and completeness-audited; provenance is cited, machine-matched, dual-classified, script-computed
and audited with its floor stated; the arithmetic is mechanical and provenance-blind; the two-tally
separation removes the reachability/decidability tension everywhere except F1's boundary; the blind
is machine-asserted and honestly limited; the seal binds the protocol before limb A and the tally
before interpretation, with its own limit stated. F1 is one boundary with a one-sentence repair;
F2 is a label repair already owned by the principal; F3/F4 are small. No defect defeats the design,
so: PREREG_SOUND_WITH_REPAIRS. Not frozen, not run — C0 by two independent seats, then the freeze
decision, after these repairs or the principal's disposition of them.

R3C2_V14_KIMI_GATE_COMPLETE