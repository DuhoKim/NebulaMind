ACCESS_SHA=f997fce89cce1749648e9751cb24ff40c4e78f1cd7ced92067d3098c8638877d
PACKET_SHA=8aa2ad8682f0114b333fe924cf2453b8bda5bda65b83cece49abe201a7208be9
GATE=PREREG_UNSOUND
LEAK=The packet defensively foregrounds the expected/important pattern that arithmetic may reproduce while resting on chosen, fitted, or imported inputs.
CONSEQUENCE_VISIBLE=NO

## 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive once the stated precedence is applied. I could not construct a claim that files two outcomes or none. Co-occurrence is possible but filing is unique: a claim with no procedure, one cited-but-unavailable input, and one wholly absent input satisfies several terminal facts, but the sentence “Where more than one terminal condition holds, file the first in this order” files `REPRO_NO_DERIVATION_STATED` only. If a procedure exists, `REPRO_BLOCKED` wins over `REPRO_INPUT_ABSENT`; if neither applies, machinery failure wins before the arithmetic group; completed arithmetic files exactly `REPRO_EXACT` or `REPRO_FAILED`.

The seven study-level classes are likewise exhaustive and mutually exclusive under the total precedence. A tally with both a non-arithmetic claim and a failed audit files `CENSUS_AUDIT_FAILED`, not `CENSUS_PARTIAL`. A control split plus a denominator dispute files `CENSUS_CONTROL_SPLIT`. I found no tally fitting none. A zero-claim census would vacuously file `CENSUS_COMPLETE`; that is odd but decidable, not a gap.

`INCONCLUSIVE` is genuinely reachable: one included claim with a stated equation and an input neither printed nor traced to a source files `REPRO_INPUT_ABSENT`, making `CENSUS_PARTIAL` applicable.

### Q1 — Does section 3, as written under option (c), make every declared outcome reachable and keep reproduction decidable?

Yes. Hard cases route uniquely:

- Printed but chosen: the paper says “Set β = 1/929.25” and uses it in a stated formula. Ledger status is `PRINTED`, origin is `CHOSEN`; arithmetic consumes it. Matching arithmetic files `REPRO_EXACT`; nonmatching arithmetic files `REPRO_FAILED`. The script gives `rests_on=USES_CHOSEN` from the `CHOSEN` root.
- Printed fitted parameter: “A fit gives α = 0.73,” used in the recipe, is `PRINTED`/`FITTED`; it reaches exactly one arithmetic verdict and `rests_on=USES_FITTED`.
- Imported value: the claiming paper cites a value at a machine-matching line of an enumerable pinned source. It is `PRINTED`/`IMPORTED`; it reaches exactly one arithmetic verdict and `rests_on=USES_IMPORTED`. If the cited source is not enumerable, or the value does not match at its cited line, it is `BLOCKED`, is not consumed, and uniquely files `REPRO_BLOCKED`.

The reproduction verdict remains decidable because admissibility is determined by status, while provenance is a separately computed tally. No repair required.

## 2. Controls

Every control has an exact `PASS|FAIL|NOT_RUN` code, and the document globally says, “Controls in an unreached limb are `NOT_RUN`, never passes.” C1, C2, C3, C5, and C6 require printed artifacts or live command output. C0 requires an exhibition table and two independent judgments. C4 and C5b remain self-reported path-list controls, but the master expressly limits their claim; they cannot be mistaken for filesystem enforcement.

### Q2 — Is the provenance field checkable rather than asserted?

Yes, but by layered human reclassification rather than semantic machine validation. Test record: source sentence `We adopt H₀ = 67.4 from Planck (2018).`; plausible but wrong ledger fields `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, with that exact sentence as `verbatim`. The validator can machine-match the quotation and accept the internally consistent reason-code mapping, so it does not catch the semantic error. The fixed precedence requires `ORIG_CITATION`/`IMPORTED`; the second seat's independent classification and C6's source-based reclassification catch the error by producing the competing classification and quotation, leading to a disputed record or audit failure. The master honestly states the residual floor: identical human misclassification can stand. Thus origin is auditable, not merely asserted, but not mechanically decided.

## 3. Circularity

SUBSTANTIVE DEFECT. The packet lets the lane's stake reach the people who create the provenance evidence that §7 later interprets. The leak is cumulative:

> “Neither contaminates the other.”

> “PROVENANCE IS RECORDED, NOT FILTERED”

> “A paper can direct you to use its own chosen constant, and following that instruction is reproducing the paper.”

> “a value can be printed in the paper and still have been chosen or fitted, and both facts survive”

Those sentences do not disclose an explicit hypothesis or comparison model, but they allow an adversarial reader to reconstruct the defended/anticipated pattern: reproduction may succeed while the provenance tally carries chosen, fitted, or imported roots. That matters because origin classification is judgment-bearing, all provenance classifiers and the auditor receive the same framing, and §7 says the interpretation reads `rests_on`. Independence does not remove a shared prompt-induced direction. A contrary claim cannot be silently excluded without two-seat agreement and a full-ledger audit, which is strong; the remaining circularity channel is origin classification.

Exact replacement in the seat packet and the seat-visible master text: replace the first quoted sentence with `The reproduction verdict and provenance fields are recorded separately.`; replace the second with `Arithmetic consumes records according to status PRINTED or STANDARD.`; delete the two explanatory sentences about chosen/fitted values. Keep the status rules, origin taxonomy, precedence, and script behavior, which are sufficient to execute the task without defending a particular cross-tally pattern.

## 4. The blind

The redaction is described as a machine assertion over a fixed forbidden-string list, and the delivered packet contains no explicit hypothesis mapping, comparison model, or named preferred outcome. The C4 mechanism does what it narrowly claims—builds a redacted copy, rejects enumerated forbidden strings, and adds procedural isolation—but it does not prevent semantic leakage, absolute-path access, incomplete path reporting, or prior exposure. The master states those limits honestly. The substantive semantic leak identified in §3 is outside what a blacklist can prove and is demonstrated by the packet itself.

## 5. The seal

Sound. Receipt P binds the protocol hash and commit id with an outside custodian before limb A begins. Receipt T binds the tally hash and commit id after tally commitment and before protocol opening. Independent re-hashing checks all four values, and missing or mismatched receipts have an explicit class. The text correctly limits the proof to existence of those forms before comparison and does not claim to bind the author's private expectations.

## 6. Fairness

The operative negative reproduction wording is “unreproduced from the stated inputs,” never “error.” `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, and `REPRO_NO_DERIVATION_STATED` describe procedure states rather than blame. Study-level stop classes do not overstate empirical meaning.

COSMETIC DEFECT. `REPRO_EXACT` includes values that merely round to the printed precision, so the name asserts more than the definition. Quote: “Where the paper states no precision for the claim, the printed precision is the claim's stated precision: the reproduced value must round to the printed numeral at that precision.” Exact replacement: rename `REPRO_EXACT` to `REPRO_WITHIN_STATED_PRECISION` everywhere. This changes no filing boundary, control, reachability, conclusion, or tested value.

## 7. Stall / executability

### Q4 — Can anything described not be executed as delivered?

No missing described artifact was found. In compliance with the instruction not to open any file beyond the packet and master, I checked existence/readability by filesystem predicates only. Present: `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, `r3c2_seat_packet/SEAT_BRIEF.md`, `r3c2_seat_packet/R3C2_SEAT_PACKET.md`, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`, `/usr/bin/python3`, and `/usr/bin/shasum`.

I checked the literal command surfaces named in the operative sections: `census`, `validate`, `compute`, and `merge` invocations of `r3c2_ledger_tools.py`; the builder invocation; the three C5 harness commands; the `random.Random(seed_int).sample(...)` formula; and the absolute dispatch path. Their named executables/artifacts exist, and placeholders are expressly required to be resolved before execution. I did not execute or inspect those additional files because the gate instruction forbids opening them; consequently this gate does not independently verify script contents, embedded hashes, stdout contracts, or runtime behavior.

One execution ambiguity warrants repair but does not establish a missing artifact: “120-second cap on symbolic operations” names no enforcement wrapper or clock semantics. Exact replacement: `Each symbolic attempt is launched by a committed timeout wrapper using a 120.0-second monotonic wall-clock deadline; timeout exit records SYMBOLIC_TIMEOUT and the wrapper command, stdout, stderr, and exit status are printed.` Add and pin that wrapper beside `r3c2_ledger_tools.py`. This can change whether `REPRO_NOT_EVALUABLE` is filed and is therefore substantive.

Because the packet leaks the provenance stake into a judgment-bearing evidence channel, and because the timeout rule lacks a delivered executable enforcement mechanism, the design is not ready to freeze.

R3C2_V15_GATE_COMPLETE
