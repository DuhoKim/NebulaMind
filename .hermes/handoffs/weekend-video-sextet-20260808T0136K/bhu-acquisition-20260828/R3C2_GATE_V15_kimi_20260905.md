ACCESS_SHA=f997fce89cce1749648e9751cb24ff40c4e78f1cd7ced92067d3098c8638877d
PACKET_SHA=8aa2ad8682f0114b333fe924cf2453b8bda5bda65b83cece49abe201a7208be9
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the packet frames a clean-vs-suspect provenance dichotomy ("derived, standard or measured inputs only, or a chosen, fitted, imported or undeclared one") as the census's second tally, reveals the corpus is cosmology (the closed STANDARD list is the Planck 2018 baseline), anticipates the printed-but-chosen-constant case, and shows a held paper exists; no hypothesis direction, no comparison-model preference, no preferred outcome is inferable from any packet sentence.
CONSEQUENCE_VISIBLE=NO — no packet sentence makes any outcome more consequential than any other; §11 states "No tier, warrant token, standing or stamp moves", and the only consequence-adjacent string, "Paper HOLD", names a held artifact with no outcome-dependent disposition attached.

METHOD NOTE. I read exactly two files: the master and the seat packet. I did not open any gate file, C0 report, the interpretation protocol's content, or any corpus source. For the executability judgment (Q4) I checked existence, hashes, and git-tracking of artefacts the document names, and I ran the pinned scripts as black boxes on synthetic fixtures of my own in /tmp/r3c2_kimi_gate — never in the lane directory. §10 of the master was treated as inert history; nothing below relies on its narrated findings.

════════════════════════════════════════════
STEP 1 — PACKET ALONE (answered before the master was opened)
════════════════════════════════════════════

Q5 (answered first, from the packet alone): CAN I TELL THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER?
NO. I read all 287 lines of the packet before opening the master. No sentence ranks any outcome's consequence to the lane or to anyone. The candidate sentences and why each fails:
- "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4) — a filing precedence, not a consequence.
- "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" (§3) — wording discipline, not consequence.
- "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane. Paper HOLD; nothing outward." (§11) — this affirmatively DENIES that any outcome moves anything. "Paper HOLD" tells me a paper exists and is held; no sentence says which outcome releases, kills, or changes it.
No sentence does what Q5 asks; the strongest string in the packet ("Paper HOLD") is quoted above and it carries no outcome-dependent disposition.

Q3 (from the packet alone): WHAT CAN I INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?
(a) The census's load-bearing output is a provenance split. Leaking sentence, packet §1: "two questions from one pass: (i) does the paper's own number follow from the paper's own recipe applied to the inputs it states — chosen constants included; and (ii) what does that number rest on — derived, standard or measured inputs only, or a chosen, fitted, imported or undeclared one?" The "only … or" construction grades provenance from a clean pole to a suspect one, and the entire C3 machinery (two-seat origin classification, reason-code precedence, transitive root_origins, rests_on under both classifications on dispute, a >10% origin-dispute stop) exists to measure that split. I infer the lane is testing whether the corpus's claims rest on chosen/fitted/imported/undeclared inputs. That is hypothesis-shaped.
(b) The printed-but-chosen case is anticipated. Leaking sentence, packet §2 step 4: "(A paper can direct you to use its own chosen constant, and following that instruction is reproducing the paper.)" The lane has pre-argued the defense of counting chosen-constant reproductions as reproductions — it expects such claims to exist.
(c) The corpus is cosmology. The closed STANDARD list (packet §5/C3 table: H₀=67.36, Ω_m=0.3153, … "the Planck 2018 TT,TE,EE+lowE+lensing baseline") is content the seat needs, but it discloses the domain.
(d) Iterated repair happened in this lane: "(Added because the enumeration stop had no class.)", "(Added because this reachable state landed in no class.)", "(STANDARD was missing from the `origin` enumeration …)", "the numbering has shifted twice" — process knowledge, not hypothesis content.
(e) A paper is held (§11). The census plausibly gates something about it; direction unstated.
What I could NOT infer, trying adversarially: which way the lane hopes either tally falls. No packet sentence states or implies a preferred reproduction tally, a preferred rests_on split, or a comparison-model verdict. The dichotomy in (a) tells me what will be measured, not which pole anyone wants.

════════════════════════════════════════════
1. OUTCOME CLASSES (Q1 placed here)
════════════════════════════════════════════

Per-claim: six outcomes, exclusion ledger separately, and a total precedence (REPRO_NO_DERIVATION_STATED > REPRO_BLOCKED > REPRO_INPUT_ABSENT > REPRO_NOT_EVALUABLE > arithmetic group {REPRO_EXACT, REPRO_FAILED}), "Exactly one outcome is filed per claim."
Exhaustive: for any included claim — no stated procedure → NO_DERIVATION_STATED; stated procedure with an input printed nowhere and traced to no source → INPUT_ABSENT; input traced to a named source that is not manifest-enumerable or does not machine-match at the cited line → BLOCKED; all inputs PRINTED/STANDARD → arithmetic attempted → within stated precision (printed precision when none stated) → EXACT else FAILED; cap exceeded or machinery missing → NOT_EVALUABLE (SYMBOLIC_TIMEOUT / MACHINERY_UNAVAILABLE). Every state of a claim lands somewhere.
Mutually exclusive: I tried to construct a real claim fitting two outcomes. (i) No derivation AND an absent input → precedence files NO_DERIVATION_STATED. (ii) A blocked input AND an absent input → BLOCKED. (iii) An absent input that would also time out → the attempt never starts; even construed as co-occurring, precedence files INPUT_ABSENT. (iv) A printed value also on the closed list → §2 step 3's tiebreak files STANDARD, "the two routes are outcome-identical". (v) A printed value also traceable to a pinned source → §2's IMPORTED rule routes it to PRINTED with origin IMPORTED. No real claim fits two outcomes with no rule to choose. NONE constructed.
Study-level: seven classes with total precedence R3C2_NO_CLASS > CENSUS_CONTROL_SPLIT > CENSUS_DENOMINATOR_DISPUTED > CENSUS_ORIGIN_DISPUTED > CENSUS_AUDIT_FAILED > CENSUS_PARTIAL > CENSUS_COMPLETE. A tally is COMPLETE-eligible iff every included claim is arithmetic-group, else PARTIAL — a dichotomy over the same denominator. An all-arithmetic tally whose audit then fails fits two class conditions; precedence files AUDIT_FAILED. A control split during Limb A/B fits CONTROL_SPLIT; precedence puts it above the tally classes. No real tally fits two classes with no rule. NONE constructed.
INCONCLUSIVE (CENSUS_PARTIAL) genuinely reachable: any single blocked/absent/no-derivation/timed-out claim in the corpus yields it; COMPLETE is reachable exactly when no claim hits a stop condition. Both live.

Q1: DOES §3 UNDER OPTION (c) MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?
YES — traced, not asserted:
- Printed-but-chosen input (hardest case one): paper prints "we adopt γ = 0.55" and derives f. Status PRINTED, origin CHOSEN via ORIG_CHOICE_STATED with machine-matched verbatim. The arithmetic consumes it (admissibility is by status, not origin) → exactly one outcome, REPRO_EXACT or REPRO_FAILED; rests_on root set contains CHOSEN → USES_CHOSEN (or more severe). One outcome, one rests_on. The old tension — filtering on provenance made the class unreachable; not filtering felt like endorsement — is gone because provenance no longer gates the attempt; it is recorded beside it.
- A fitted parameter the paper states: identical path with origin FITTED → arithmetic group + USES_FITTED. One each.
- An imported value: cited to a pinned enumerable text, machine-matching at the cited line → PRINTED from that source, origin IMPORTED → consumed → EXACT/FAILED + USES_IMPORTED. No match at the cited line, or source not enumerable → REPRO_BLOCKED, status BLOCKED, origin IMPORTED, no value, never consumed; rests_on still computed from the ledger ("whatever its outcome"). One outcome, one rests_on.
- An undeclared input: ABSENT with ORIG_SILENT and a printed search → REPRO_INPUT_ABSENT; rests_on USES_UNDECLARED from the record. One each.
- A claim with no ledger record (no derivation stated): rests_on NOT_COMPUTED with its own tally row. Defined, not a hole.
Decidability is mechanical end to end: closed-list membership, machine match at the cited line, rounding at stated precision, a 120-second cap. Every §3 outcome and every §4 class has an exhibitable input (each traced above). The two requirements no longer pull against each other: reachability needed no filter once admissibility moved from origin to status, and decidability needed no judgment once the attempt stopped depending on provenance. Q1 = YES.

════════════════════════════════════════════
2. CONTROLS (Q2 placed here)
════════════════════════════════════════════

Every control names an exact three-valued code: C0_REACHABILITY, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE — each PASS|FAIL|NOT_RUN, with "Controls in an unreached limb are NOT_RUN, never passes."
Passed by assertion rather than a printed artefact? C4_SEAT_ISOLATION and C5B_NO_CROSS_LANE — yes, and the document says so to their faces: "a self-reported control with a structural aid, and is labelled so" and "PASS means the printed list contains no such row and makes no claim that the list is complete." A seat that opened an outside path and omitted it from its printed list passes both; the document states that limit instead of hiding it. Every other control requires a printed artefact: C0 the exhibition table with two independent PASSes; C1/C2/C3 the resolved command, stdout and exit status; C5 three live command outputs (a transcribed value is FAIL); C4_PACKET_REDACTED the builder's asserted output; C6 the third seat's re-derivation. No control is assertable beyond the two honestly labelled self-reports.

Q2: IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?
YES, with the floor the document itself declares. I tested the mechanisms live against the pinned script (sha e7f053b9… verified = the §3/C3 pin) using synthetic fixtures in /tmp:
- My wrong-but-plausible record: input Ω_b h², the source line "We take the baryon density from Planck (2018) as Omega_b h^2 = 0.02237.", filed with reason_code ORIG_CHOICE_STATED → origin CHOSEN (the precedence in C3 requires ORIG_CITATION → IMPORTED: "a sentence that names an external source for the value is a citation whatever else it says"). The quotation machine-matches the cited line, the code/origin pair is an allowed pair, the value matches. RESULT: `C3_NO_SUBSTITUTION=PASS`, exit 0 — the machine does NOT catch it, exactly as the master states: a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away."
- The catching mechanism, tested: a second seat filing IMPORTED on the same input → `merge` exit 0 with "origin disagreements=1" → the merged record carries origin_alt → `compute` prints `rests_on=['USES_CHOSEN', 'USES_IMPORTED'] root_origins=['CHOSEN'] DISPUTED`. The misclassification cannot silently launder into the tally: it surfaces as a DISPUTED pair, counted in the rests_on tally's DISPUTED row, and above 10% of included claims the study stops as CENSUS_ORIGIN_DISPUTED. The C6 third seat re-classifies every input's origin from the pinned sources as the final backstop. Identical misclassification by all readers stands — declared, not implied away.
- Supporting checks, all firing as written: value not at the cited line → FAIL exit 1 ("value 0.56 not at cited line"); verbatim not a substring of the cited line → FAIL exit 1; STANDARD value off the closed list → FAIL exit 1 ("not on the closed list"); ABSENT or BLOCKED record carrying a value → FAIL exit 1; seat-authored ledger carrying root_origins → validate FAIL exit 1 AND compute REJECT exit 2; dangling derived_from id → FAIL exit 1 at both validate and compute; cycle → FAIL; DERIVED with no derived_from → FAIL ("an empty root set cannot occur" holds).
The field is computed and machine-checked where a machine can check, independently double-read where it cannot, audited by a third seat, and the uncatchable residue is named in the document. Q2 = YES.

════════════════════════════════════════════
3. CIRCULARITY
════════════════════════════════════════════

Can the lane's expectation reach the evidence?
- Inclusion: the §1 rule is operational (a numeral the paper asserts as its own result, five enumerated excluded kinds), assigned independently by two seats, tolerance zero, disagreement stops the study (CENSUS_DENOMINATOR_DISPUTED) with both ledgers reported. The residual judgment — whether a numeral is "the paper's own result" — is real but is borne by two readers who must agree, not one.
- Quiet exclusion of a contrary claim: every excluded candidate is recorded with file, line, numeral and kind; "the census denominator is the count of included claims; the exclusion ledger is reported alongside it and audited under C6, so nothing is hidden by being excluded." C6's FIRST step audits the full candidate and exclusion ledgers against every pinned source — completeness, not sampling. A contrary claim silently dropped would have to vanish from both seats' candidate ledgers and from the auditor's source-by-source completeness pass. Channel closed to anything but three-way identical omission, which no mechanism available here could prevent and which the design does not claim to prevent.
- Origin classification: independent double classification, a stated reason-code precedence, DISPUTED-pair reporting, a >10% stop, and C6 re-classification. One lane member cannot tilt provenance alone.
- rests_on: computed by the pinned script from the merged ledger; seats cannot write it (validate and compute both reject it — tested).
- The interpretation step: lives in a protocol no seat may open, committed before Limb A under receipt P; the tally is receipted (T) before the protocol is opened. The expectation's mapping from tally to conclusion is physically outside everything the evidence-producers see.
Verdict: inclusion, exclusion, origin and interpretation are each either mechanical, double-read with a stop rule, script-computed, or sealed. Circularity has no live channel.

════════════════════════════════════════════
4. THE BLIND
════════════════════════════════════════════

Machine-asserted as claimed? YES — tested. I ran the delivered builder (`/usr/bin/python3 r3c2_build_seat_packet.py`) in a scratch COPY of the lane directory (the lane itself untouched): first output line `C4_PACKET_REDACTED=PASS`, then the master and packet digests — exactly the C4-described output — and the packet it wrote is byte-identical to the pinned packet (sha 8aa2ad86…, matching R3C2_SEAT_PACKET.sha256). It reports dropping §§0, 7, 8, 10 whole and redacting 32 inline spans, then asserts the forbidden list over the output. With SEAT_BRIEF.md absent the builder printed `C4_PACKET_REDACTED=FAIL` and exited 1 without writing a packet — the assertion is load-bearing, not decorative.
Does C4 prevent what it claims? It claims: the seat works from a redacted copy outside the lane, prints every path it opens, and any outside path FAILs. What it cannot do — a seat silently opening an absolute path and not listing it — it states: "This is procedural, not enforced by the filesystem … a self-reported control with a structural aid, and is labelled so." The forbidden list's own limit is stated: "the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone." The training-prior limit is stated: "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." Every limit is declared at its true strength. The blind does what it says and says what it cannot do.

════════════════════════════════════════════
5. THE SEAL (§7)
════════════════════════════════════════════

Two receipts, each preceding the step it must bind:
- P: before Limb A, the interpretation protocol's hash and commit id go to the external custodian, who acknowledges and timestamps them; "limb A does not begin without receipt P." The protocol therefore cannot be written to suit the tally — it is receipted before the census produces one.
- T: after the tally is committed and before the protocol is opened, the tally hash and commit id are receipted; "the interpretation protocol is not opened without both receipts." The tally therefore cannot be reshaped to suit the protocol.
- After opening, the custodian independently re-hashes both artefacts and verifies all four values against the two receipts; the interpretation report must print both receipts and the four verified values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED — and §4's definition of that class names the seal case ("or the receipt verification of the seal fails"), so the route is wired, not dangling.
- Ordering is coherent with C6: the audit seed comes from the custodian "After the tally digests are receipted", and a seed never supplied files CENSUS_AUDIT_FAILED with C6_AUDIT_SAMPLE=NOT_RUN.
- The overclaim guards are present: a local commit is called "tamper evidence, not an external custodian"; "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."
The relay binds the protocol before the evidence exists and the tally before the mapping is opened. Sound.

════════════════════════════════════════════
6. FAIRNESS
════════════════════════════════════════════

- Every negative outcome is worded "unreproduced from the stated inputs," not "error" (§3, REPRO_FAILED; present verbatim in the packet). I swept both documents for "error/wrong/mistake": the only operative-section hit is the fairness rule itself; all other occurrences are in §10 history, which binds nothing.
- CENSUS_PARTIAL is labelled "INCONCLUSIVE" — accurate: a non-arithmetic outcome means the reproduction question went unanswered for at least one claim.
- "No tier, warrant token, standing or stamp moves" (§11) — the scope disclaims consequence rather than implying it.
- Class names versus definitions: `REPRO_EXACT` names exactness while its definition is "within its own stated precision … must round to the printed numeral at that precision" — the name asserts more than the definition delivers. I note this independently; the master already records the rename as escalated to the principal, and since the operative definition text is precise and both numbers are always reported, no conclusion and no filed class is affected. COSMETIC here; the rename decision is the principal's, where the document already put it.
- `rests_on` token `DERIVED_ONLY` is defined as "every root origin is DERIVED, STANDARD or MEASURED" — a claim resting solely on a measured input carries the token DERIVED_ONLY. The definition is precise; the label is narrower than its domain. COSMETIC.
- `R3C2_NO_CLASS` names a control-failure class, not the absence of a class. Defined precisely. COSMETIC.
No operative clause asserts more than its definition in a way that changes a class filed, a control's passability, or a conclusion.

════════════════════════════════════════════
7. STALL / EXECUTABILITY (Q4 placed here)
════════════════════════════════════════════

Q4: CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?
Each named item, and how I checked it:
(a) `r3c2_ledger_tools.py` — exists beside the master; `shasum -a 256` = e7f053b9b98b2ba55639104b151ce4c09f174dcde46a1ead367377429c71f394, identical to the pin printed in §3 and C3.
(b) Its four subcommands, run as a black box on my own synthetic fixtures in /tmp: `validate` (PASS exit 0 on a well-formed ledger including a well-formed BLOCKED record; FAIL exit 1 with named failure lines on value/verbatim/closed-list/carried-value/computed-field/dangling-parent/cycle/parentless-DERIVED violations), `compute` (root_origins and per-claim rests_on printed; REJECT exit 2 on pre-set computed fields; FAIL exit 1 on graph violations), `merge` (exit 1 on differing input_id sets; origin_alt on disagreement; compute then prints the DISPUTED pair), `census` (declared vs recomputed counts printed; exit 0 PASS / exit 1 FAIL on mismatch). Every field the document says the script computes — root_origins, rests_on, origin_alt, origin_evidence_alt — was observed in actual output.
(c) `R3C2_CORPUS_MANIFEST.md` — exists; sha 300d4da1… = the §2 pin.
(d) `r3c2_seat_packet/R3C2_SEAT_PACKET.md` — exists; sha 8aa2ad86… = the value in R3C2_SEAT_PACKET.sha256; its embedded master hash f997fce8… = the actual master hash. `r3c2_seat_packet/SEAT_BRIEF.md` — exists; sha e1f20d1e… = the pin file's second value.
(e) `r3c2_build_seat_packet.py` — exists; executed in a scratch copy of the directory (lane untouched): first line C4_PACKET_REDACTED=PASS then both digests, as C4 describes; produced packet byte-identical to the pinned one; FAIL exit 1 without writing when SEAT_BRIEF.md was absent.
(f) `R3C2_INTERPRETATION_PROTOCOL_20260904.md` — exists and is git-tracked (metadata check only; content not opened — the document forbids seats from opening it and the design only requires existence and pre-Limb-A commitment).
(g) All seven named artefacts are git-tracked (git ls-files --error-unmatch clean): master-side script, builder, manifest, pin file, packet, brief, protocol. The packet is indeed "a committed path, not scratch".
(h) C5's literal commands: /usr/bin/python3 exists (3.9.6; sha b8763cf2…); sympy 1.14.0 imports under it (run from a file because my harness's approval layer blocks `python3 -c`; same interpreter, same import).
(i) §9's `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh` — exists at the stated absolute path.
(j) Paths seats are told to use: `.` as the sole sources_dir in C2/C3 is consistent with the seat's redacted working directory; C2's requirement to print the fully resolved command is stated.
(k) The C6 sampling frame — `min(max(1, ceil(0.20 × N)), R)`, `random.Random(int(seed_hex,16)).sample(sorted(set(included_ids) − set(arithmetic_group_ids)), k)`, 64 lowercase hex seed — is stdlib-executable as written; k is capped by R and the R=0 case is handled in text.

FINDING 1 (REQUIRED REPAIR — a reachable state with no named class):
Verbatim sentence (C3): "the two seats' independently validated ledgers are merged by `/usr/bin/python3 r3c2_ledger_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>` (exit 1 if their `input_id` sets differ)".
Defect: the exit-1 state is reachable and unwired. Input-list completeness is expressly "seat-authored and audited under C6, not machine-checked", and Limb A's tolerance-zero reconciliation covers only "any candidate's inclusion" — candidate passages, not inputs. Two seats can therefore agree on every candidate, each produce a valid ledger, and still differ on an input (one seat lists a normalization constant the paper's recipe needs; the other omits it). `merge` then exits 1 — I confirmed this exit fires as described — and no study-level class receives the state: CENSUS_DENOMINATOR_DISPUTED is defined over enumerations ("The two enumerations disagree"), not input lists; CENSUS_ORIGIN_DISPUTED is defined over origin classifications; R3C2_NO_CLASS requires a control failing "in every seat that attempted it" — here both seats PASSED every per-seat control, and the merge is a single joint step, not a per-seat attempt; the C6 audit is unreached. The study stalls in Limb B with exactly nothing fileable. This changes a class filed (a real state lands in no class), so it is not cosmetic.
Exact replacement: append to the C3 merge sentence — "If `merge` exits 1, the two seats reconcile their input lists against the paper's stated equation once; an input-set difference surviving that reconciliation stops the study under `CENSUS_DENOMINATOR_DISPUTED` (§4), the disputed inputs listed with both seats' quotations." — and extend §4 class 5 to: "`CENSUS_DENOMINATOR_DISPUTED` — the two enumerations disagree after two reconciliation attempts, or the two seats' input lists for the agreed claims disagree after the one C3 reconciliation. The census does not proceed; the disputed candidates or inputs are listed." The same two edits belong in the packet's §3/§4 (the packet text is identical there).

FINDING 2 (COSMETIC — changes nothing filed or passable): the script's usage text still reads "status in {PRINTED,STANDARD,ABSENT}" although BLOCKED is a valid status — validate accepts a well-formed BLOCKED record (tested: PASS exit 0) and rejects a BLOCKED record carrying a value (tested: FAIL exit 1). Behavior is correct; only the help line lags. Exact replacement: "status in {PRINTED,STANDARD,ABSENT,BLOCKED}".
FINDING 3 (COSMETIC, recorded for completeness): the `REPRO_EXACT` name versus its within-stated-precision definition, and the `DERIVED_ONLY` token covering STANDARD/MEASURED-only claims — both definitions are precise and operative; the names alone overreach. The rename is already recorded in the master as escalated to the principal; nothing here changes a filed class or a tested value.

Everything else the document describes — every literal command, every named script, every pinned file, every computed field, every path — exists and executes as delivered.

R3C2_V15_KIMI_GATE_COMPLETE
