ACCESS_SHA=47c55a74af9520586af75e31d408c969a0e81f831c94f8eac59b59ead0a91381
PACKET_SHA=b12b8887098e9b957bf17801266b83f1594aaefe4407a1ba058ae22319dc277f
GATE=PREREG_UNSOUND
LEAK=The packet reveals a planned comparison between mechanical reproduction and provenance (`rests_on`), and specifically anticipates that printed chosen inputs may reproduce; it reveals no hypothesis, comparison model, preferred empirical outcome, or direction of the hoped-for tally.
CONSEQUENCE_VISIBLE=NO

## Packet-first questions

### Q5 — consequence visibility

No. No packet sentence says that any outcome is more consequential than another to the lane or to anyone. The stop rules disclose procedural consequences, but not empirical stakes or outcome valence.

### Q3 — hypothesis reachability

From the packet alone I can infer only the comparison the census is designed to make. It asks both whether “the paper's own number follow[s] from the paper's own recipe applied to the inputs it states” and “what does that number rest on — derived, standard or measured inputs only, or a chosen, fitted, imported or undeclared one?” The packet also specifically anticipates the crossed case: “Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_EXACT` or `REPRO_FAILED` with its `rests_on`.” These sentences reveal an intended pattern analysis contrasting arithmetic reproduction with provenance and make printed-but-chosen reproduction a salient case. They do not reveal a hypothesis, a comparison model, a preferred result, a direction, or what any resulting pattern would mean. Thus the hypothesis itself is not reachable, although the two-axis comparison structure is.

## 1. Outcome classes

The per-claim outcomes are exhaustive and mutually exclusive after applying the express precedence. A claim with no stated procedure and an absent input superficially fits both `REPRO_NO_DERIVATION_STATED` and `REPRO_INPUT_ABSENT`; the precedence assigns only the former. A claim requiring both a cited-but-unavailable value and an uncited missing value superficially fits `REPRO_BLOCKED` and `REPRO_INPUT_ABSENT`; the precedence assigns only `REPRO_BLOCKED`. Once all necessary inputs are consumable and machinery completes, rounding to the printed precision partitions the arithmetic group into `REPRO_EXACT` and `REPRO_FAILED`. I find no real included claim that fits none after those rules.

The study-level classes are likewise total under their precedence. For example, a control split followed by a denominator disagreement satisfies two raw conditions, but files only `CENSUS_CONTROL_SPLIT`. `CENSUS_COMPLETE` and `CENSUS_PARTIAL` are exclusive because the former requires every included claim to be in the arithmetic group and the latter requires at least one non-arithmetic outcome. `INCONCLUSIVE` is genuinely reachable: one included claim with a named but unavailable cited input files `REPRO_BLOCKED`, which makes the study `CENSUS_PARTIAL` and expressly “INCONCLUSIVE.”

### Q1 — reachability and decidability

Yes. Section 3 as written under option (c) makes every declared per-claim outcome reachable and keeps the reproduction question decidable.

Hard case 1, printed but chosen: suppose a paper states “Set β = 0.20” and states `y = 2β`, printing `y = 0.40`. The β ledger record is `status=PRINTED`, `origin=CHOSEN`, with `ORIG_CHOICE_STATED` evidence. Arithmetic must consume it; 0.40 rounds to the printed number, so exactly `REPRO_EXACT` files. The script follows the leaf origin and computes exactly `rests_on=USES_CHOSEN`.

Hard case 2, fitted: suppose the paper states “A fit gives a = 3.0,” gives `z = a²`, and prints 9.0. The record is `PRINTED/FITTED`; exactly `REPRO_EXACT` files and the script computes `USES_FITTED`. If the paper prints 8.0 instead, exactly `REPRO_FAILED` files, still with `USES_FITTED`.

Hard case 3, imported: if the claiming paper names a pinned enumerable source and the value machine-matches at the cited line, the record is `PRINTED/IMPORTED`; arithmetic proceeds and files exactly one arithmetic outcome, while the script computes `USES_IMPORTED`. If the source is outside the enumerable manifest or the value does not match at the cited line, the record is `BLOCKED/IMPORTED`, is not consumed, and precedence files exactly `REPRO_BLOCKED`, with `USES_IMPORTED` computed when the record exists. An uncited missing input instead files `REPRO_INPUT_ABSENT`. These routes settle the hardest boundary without sacrificing decidability.

## 2. Controls

Most controls name an exact code and all name `PASS|FAIL|NOT_RUN`; the global sentence “Controls in an unreached limb are `NOT_RUN`, never passes” handles unreached controls. C1, however, can pass without satisfying one of its announced predicates.

### Finding 2.1 — C1 does not validate or define “attempts made”

Quoted sentence: “C1 — denominator. Claims **included**, claims **excluded** (with the exclusion ledger of §3), and attempts made, all printed before any tally.”

Defect: the subsequently defined candidate and exclusion schemas contain no attempt field, and `C1_DENOMINATOR_PRINTED=PASS` is made to depend only on the `census` command's exit 0. A seat can therefore omit “attempts made” and still obtain PASS. This changes a control's passability and is not cosmetic.

Exact replacement: “**C1 — denominator.** Claims included and excluded are printed before any tally. The candidate file is a JSON object `{declared_candidate_count, declared_included_count, declared_excluded_count, declared_attempt_count, candidates: [...], attempts: [...]}`; every attempt row carries `claim_id`, `attempt_number`, and `outcome`. Before the tally, print all four declared counts. The `census` command recomputes and prints all four counts and exits non-zero unless every included claim has the number of attempts permitted by §4, every attempt names an included claim, and every declared count equals the recomputed count. `C1_DENOMINATOR_PRINTED=PASS` only on that exit 0.”

### Q2 — provenance checkability

It is checkable by independent readers, but semantic origin classification is not fully machine-decided. Test record: for an input supported by “We adopt H₀ = 67.4 from Planck (2018),” write `status=PRINTED`, `origin=CHOSEN`, and `reason_code=ORIG_CHOICE_STATED`, quoting that exact line. This is plausible because “adopt” denotes a choice. The document's precedence nevertheless requires `ORIG_CITATION` and `IMPORTED` because the sentence names an external source. The machine can verify that the value and quotation occur, but it is not said to understand the citation or enforce reason-code precedence. The second seat's independent classification should produce `IMPORTED`; `merge` records the disagreement, and C6 independently re-classifies it. If both seats and the auditor make the same semantic mistake, nothing catches it. The master states that floor honestly: “if every reader misclassifies identically, the record stands.” Therefore provenance is auditable rather than merely a naked assertion, but its semantic classification rests on replicated human judgment, not a machine proof. No repair is required for that honestly bounded design.

## 3. Circularity

The packet does not disclose the hypothesis, comparison model, outcome valence, or interpretation mapping. Inclusion and exclusion are governed by a neutral numeral rule, disagreements stop, full candidate/exclusion ledgers are exposed, and C6 audits their completeness against every pinned source. A contrary claim cannot quietly disappear without a ledger omission that the full-ledger audit is designed to catch. Origin is independently classified twice, disagreements are preserved, and C6 re-classifies again. The interpretation protocol is sealed before limb A. These are sound barriers against expectation reaching inclusion, exclusion, origin, or arithmetic.

The claimed blind itself is overstated, however, and that weakens the barrier materially.

### Finding 3.1 — the design claims proof from an acknowledged incomplete, self-reported path list

Quoted sentences: “`C4_SEAT_ISOLATION=PASS` requires that printed path list and means only that the list contains no outside path; it makes no claim that the list is complete.” “This is procedural, not enforced by the filesystem: nothing here denies a seat an absolute path into the lane, so the seat's printed path list is the detection.” “What the blind proves and does not: it proves the seat was not given the pattern record and did not read it from its working directory.”

Defect: an incomplete self-reported list cannot detect an omitted absolute-path read. The redacted working directory proves only what was furnished there; it cannot prove what the seat did not read when the filesystem permits the read and the log is not complete. This is substantive because a silent read allows the lane's expectation to reach inclusion and origin classification while C4 and C5b still PASS.

Exact replacement: replace the last quoted sentence with: “**What the blind supports and does not:** the dispatch record proves that the seat was not furnished the pattern record in its working directory. The self-reported path list supplies secondary evidence but, because it is not complete and filesystem access is not denied, this design does not prove that the seat did not read an absolute path into the lane. `C4_SEAT_ISOLATION=PASS` therefore certifies only the contents of the printed list and the dispatch copy, not actual non-access.”

If actual non-access is required for validity rather than merely honest reporting, the exact structural repair is: “Launch each seat in a filesystem sandbox whose only readable study directory is the sealed dispatch copy; retain the sandbox's complete file-open audit log. `C4_SEAT_ISOLATION=PASS` requires both enforcement and a log containing no read outside the allowlist.”

## 4. The blind

The packet hash is fixed above. The packet actually inspected contains none of the master’s enumerated forbidden strings (`expectation`, `refute`, `consequential`, `invisible`, `favourable`, `unfavourable`, `stake`, `prior`, `tempting`, `warn`, or `ΛCDM`/`LambdaCDM`). The packet also has the master digest in its header. This is evidence consistent with the claimed builder assertion. Under the imposed two-file restriction I did not open or execute the builder, pin, or brief, so I do not independently certify their contents or behavior.

The C4 mechanism does prevent the pattern record from being included in the declared dispatch copy, assuming the builder and dispatch checks behave as specified. It does not prevent absolute-path access, and the master mostly states that limit honestly. Finding 3.1 identifies the one sentence that crosses from an honest procedural claim into an unsupported proof claim. The packet itself exposes comparison structure but no consequence or hypothesis mapping.

## 5. The seal

Sound as designed. Receipt P binds the interpretation protocol's hash and commit id in external custody before limb A begins. Receipt T binds the tally's hash and commit id after the tally is committed and before the protocol opens. The interpretation cannot run without both; Blanc re-hashes both objects, verifies all four values, and the report prints both receipts and values. Missing or mismatched custody evidence files `CENSUS_AUDIT_FAILED`. The document accurately limits the result to existence of the two records in the stated forms before comparison and does not claim to bind anyone's thoughts.

## 6. Fairness

The negative result is expressly worded “unreproduced from the stated inputs,” not “error,” and no operative negative clause calls the paper erroneous. Two labels nevertheless assert more than their definitions.

### Finding 6.1 — `REPRO_EXACT` is not exact

Quoted sentence: “`REPRO_EXACT` — the paper's number follows, within its own stated precision”.

Defect: equality only after rounding within stated precision is not exact equality. The class name overstates its definition. Because the requested class-name test treats what is filed as substantive, this is not cosmetic.

Exact replacement: replace `REPRO_EXACT` with `REPRO_WITHIN_STATED_PRECISION` everywhere, including the arithmetic-group definition and script/output schemas.

### Finding 6.2 — `DERIVED_ONLY` includes non-derived roots

Quoted sentence: “`DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`”.

Defect: a claim resting only on measured or standard roots is not derived-only. The summary label asserts a narrower provenance than its definition.

Exact replacement: replace `DERIVED_ONLY` with `NO_CHOSEN_FITTED_IMPORTED_OR_UNDECLARED_ROOT` everywhere, or equivalently with the shorter neutral token `BASELINE_ROOTS_ONLY`, while retaining the enumerated definition.

## 7. Stall / executability

### Q4 — executable as delivered

Not completely as specified. I checked existence and regular-file type, without opening any third file, for `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_timeout.py`, `r3c2_build_seat_packet.py`, `r3c2_seat_packet/R3C2_SEAT_PACKET.md`, `r3c2_seat_packet/SEAT_BRIEF.md`, `R3C2_SEAT_PACKET.sha256`, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, `R3C_MAGNITUDE_CENSUS_PREREG_20260904.md`, `/usr/bin/python3`, and `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`; every one exists as a regular file. I computed the two mandated hashes. I also checked the enumerated forbidden strings directly against the already-open packet; none survived.

I checked the literal command families named by the document against their named executables and argument shapes: `census`, `validate`, `compute`, and `merge` all point to the delivered `r3c2_ledger_tools.py`; the builder command points to the delivered `r3c2_build_seat_packet.py`; the timeout command points to delivered `r3c2_timeout.py`; C5 points to existing `/usr/bin/python3`; and the split dispatch names an existing regular file at its absolute path. I did not execute or inspect those third files because the gate instruction forbids opening anything other than the packet and master. Thus this review establishes delivery and syntactic resolvability, not implementation correctness or the stated embedded digests.

### Finding 7.1 — the mandatory timeout wrapper is not among the required dispatch-copy contents

Quoted sentences: “each seat is run from a redacted copy directory outside the lane, containing the seat packet … the script `r3c2_ledger_tools.py`, and the pinned sources” and “every symbolic operation [is] launched through the committed wrapper `r3c2_timeout.py`”.

Defect: the required dispatch contents guarantee the ledger script but do not guarantee the timeout wrapper. A conforming copy can contain every item in the C4 list yet be unable to execute §9's mandatory wrapper from the seat working directory. Existence beside the master does not cure omission from the out-of-lane dispatch copy.

Exact replacement: “each seat is run from a redacted copy directory outside the lane, containing the seat packet, `SEAT_BRIEF.md`, `r3c2_ledger_tools.py`, **`r3c2_timeout.py`**, and the pinned sources of `R3C2_CORPUS_MANIFEST.md`; the dispatch record lists and hashes every one, and a copy missing any is not dispatched.”

No other named delivered artefact was missing. Future run products such as candidate, exclusion, seat ledger, merged ledger, computed output, receipts, seed, and tally files are outputs to be created and are appropriately not claimed to pre-exist.

The design is therefore not ready to freeze. Finding 3.1 is the decisive validity defect; Findings 2.1 and 7.1 affect control passability and execution, and Findings 6.1–6.2 are fairness overclaims under the requested class-name test.

R3C2_V16_GATE_COMPLETE
