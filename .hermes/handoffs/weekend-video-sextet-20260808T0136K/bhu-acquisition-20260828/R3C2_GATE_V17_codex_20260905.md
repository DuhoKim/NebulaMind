ACCESS_SHA=fe194fb4aee7603dbeecbfeda62dc8507aba2a994e3b9a206c8089490700e1c9
PACKET_SHA=7fb7076b1b7fba9ae1e5544ce538e92345f8ffb14e80ca8d81ba254bba45a493
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=It is independently re-classified and origin disagreements can determine CENSUS_ORIGIN_DISPUTED, from “Every input's `origin` is classified independently by both seats” and the C6 instruction to re-classify origin.

## 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive after applying the explicit precedence. I could not construct a claim fitting none. A claim with no stated derivation and an absent input superficially fits two terminal descriptions, but the stated order files only `REPRO_NO_DERIVATION_STATED`. A claim with both a cited-but-unmatchable input and an uncited absent input files only `REPRO_BLOCKED`. Timeout plus either missing-input condition likewise resolves to the earlier missing-input class.

The seven study-level classes are exhaustive for the declared study states and mutually exclusive after their explicit precedence. A run with both a denominator dispute and a control split superficially fits two classes, but files only `CENSUS_CONTROL_SPLIT`. A run with a non-arithmetic claim and an audit failure files only `CENSUS_AUDIT_FAILED`. I found no declared terminal state fitting none. `CENSUS_PARTIAL` is genuinely reachable and expressly “INCONCLUSIVE”: one included claim with a stated recipe but one uncited, unprinted required input files `REPRO_INPUT_ABSENT`, making the study `CENSUS_PARTIAL`.

### Q1 — Does option (c), as written, make every outcome reachable and keep reproduction decidable?

Yes at the classification level.

- Printed-but-chosen input: a paper says “we choose b = 2,” uses b in y = 2b, and prints y = 4. Ledger: status `PRINTED`, origin `CHOSEN`, quotation machine-matched. Arithmetic consumes it. It files exactly `REPRO_EXACT` under the present names and lane computation gives exactly `rests_on=USES_CHOSEN`.
- Stated fitted parameter: “the fit gives a = 3,” followed by y = a + 1 and printed y = 5. Ledger: `PRINTED`/`FITTED`; it is consumed; the one result is `REPRO_FAILED`, with `rests_on=USES_FITTED`.
- Imported value: the claiming paper cites a named enumerable pinned source and the value machine-matches at its cited line. It is `PRINTED`/`IMPORTED`, is consumed, files exactly one arithmetic outcome, and computes `USES_IMPORTED`. If it does not machine-match, it is `BLOCKED`/`IMPORTED`, is not consumed, and files exactly `REPRO_BLOCKED`.

Thus provenance does not make the reproduction verdict undecidable. Human judgments remain in claim enumeration, input-list completeness, and semantic origin classification, but the design exposes them to two-seat and C6 review rather than leaving a filing gap.

Defect 1.1 — the arithmetic success class overclaims exactness.

Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision”.

Defect: equality after rounding within stated precision is not exact equality. The class name asserts more than its definition and can misdescribe a non-identical reproduced value. This is not COSMETIC under the requested test because it changes the class token filed.

Exact replacement: replace every operative occurrence of `REPRO_EXACT` with `REPRO_WITHIN_STATED_PRECISION`, and replace the definition lead with: “`REPRO_WITHIN_STATED_PRECISION` — the reproduced value rounds to the paper's printed numeral at the paper's stated precision; where no precision is separately stated, use the printed numeral's precision. Report both numbers.” Update the arithmetic-group enumeration and every schema/validator permitted-token list accordingly.

## 2. Controls

C0, C1, C2, C3, C4, C4 packet redaction, C5, C5b, and C6 each have a named `PASS|FAIL|NOT_RUN` code, and the global rule says unreached controls are `NOT_RUN`. C1, C2, C4, C5, and C5b require printed evidence. C4/C5b honestly remain self-reported path-list controls rather than proof of complete access history.

Defect 2.1 — C3 can be passed without a control-specific printed execution artefact.

Quote: “`C3_NO_SUBSTITUTION=PASS|FAIL|NOT_RUN`.”

Defect: C2 requires the validator command and exit status, but C3 states machine assertions without requiring the exact validator command, stdout, stderr, and exit status to be printed as the C3 artefact or defining PASS solely from that run. A token can therefore be asserted from the prose.

Exact replacement: immediately before the C3 code add: “Run `/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> .` with all placeholders resolved; print the working directory, resolved command, complete stdout and stderr, and exit status. `C3_NO_SUBSTITUTION=PASS` only on exit 0 from that printed run; `FAIL` on any nonzero exit or missing required output; `NOT_RUN` only when unreached.”

Defect 2.2 — C6 lacks a mandatory audit artefact and PASS predicate.

Quote: “Any outcome the audit cannot reproduce, or any ledger incompleteness, files `CENSUS_AUDIT_FAILED`.”

Defect: the design specifies what the auditor does but not a required row-level audit file, its fields, or printed evidence from which `C6_AUDIT_SAMPLE=PASS` is determined. C6 can be passed by the auditor's assertion.

Exact replacement: add before the C6 code: “The auditor writes and prints a C6 JSON artefact containing the sealed denominator, receipt and seed, sorted arithmetic-group ids, sorted remaining ids, computed k, sampled ids, a completeness disposition for every candidate and exclusion, and for every audited claim the independently reconstructed inputs, origins with quotations, arithmetic, prior filed outcome, audit outcome, and match boolean. `C6_AUDIT_SAMPLE=PASS` only if the artefact contains every required row, all completeness dispositions pass, every match is true, and the printed sampling recomputation exactly matches the recorded sample; `FAIL` on any mismatch or missing row; `NOT_RUN` only under an expressly stated unreached or missing-seed condition.”

### Q2 — Is origin checkable rather than asserted?

Qualified yes: the evidence is checkable, while semantic classification is deliberately human-audited rather than fully machine-decided. Test record: source sentence “We adopt H0 = 67.4 from Planck (2018).” A plausible wrong ledger records `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and quotes that sentence. Substring matching alone will not catch it. The reason-code precedence requires `ORIG_CITATION` and `IMPORTED`; the second seat's independent classification can expose the disagreement, and C6's independent source re-classification catches it when the ledger is compared in the required audit. If all human readers make the identical semantic mistake, no written mechanism catches it; the master states that limit. The quotation, search, tie-break, second seat, and C6 make the field auditable rather than bare assertion, but Defect 2.2 must be repaired to make the final check receiptable.

## 3. Circularity

The packet does not reveal a hypothesis, comparison model, preferred outcome, or desired pattern. Inclusion uses a content rule; both inclusion and exclusion are recorded, disagreements stop the study, and C6 audits the complete candidate and exclusion ledgers against every pinned source. A contrary quantitative claim therefore cannot be quietly excluded without leaving an audited exclusion row. Origin classification is exposed to independent classification and audit. The interpretation mapping is withheld from seats and fixed before limb A. I find no operative route by which the lane's expectation reaches evidence classification.

## 4. The blind

The packet I read is consistent with the described section and span removals and contains none of the stated hypothesis/consequence mapping. The design does not, however, prove semantic redaction merely by forbidden-string absence. It says so exactly: “the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone.” That is an honest limit.

C4 also does not technically prevent absolute-path access; it requires a redacted dispatch directory and uses printed paths as detection. The master states the limitation exactly: “This is procedural, not enforced by the filesystem” and “this design does not prove that the seat did not read an absolute path into the lane.” The mechanism therefore supports only the modest claim it makes. Q5: no packet sentence makes one outcome more consequential than another. Q6: the packet does reveal that origin is independently classified, audited, and can trigger an origin-dispute class; it does not reveal `rests_on` or the later comparison mapping.

## 5. The seal

Receipt P binds the interpretation-protocol hash and commit id before limb A. Receipt T binds the tally hash and commit id after tally commitment and before protocol opening. The external custodian later re-hashes both and verifies all four values, and a missing receipt or mismatch files `CENSUS_AUDIT_FAILED`. This binds the two artefacts, in those forms, before the required stages. It does not prove lack of advance knowledge or prevent private early reading, and the document expressly limits its claim to existence in stated form before comparison. I find no seal overclaim.

## 6. Fairness

The negative arithmetic outcome is expressly worded “unreproduced from the stated inputs,” not “error.” `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NO_DERIVATION_STATED`, and `REPRO_NOT_EVALUABLE` describe procedural states rather than faults. Study classes likewise do not call a paper erroneous. The sole fairness defect is Defect 1.1: `REPRO_EXACT` claims exactness while accepting rounded agreement within precision.

## 7. Stall / executability

### Q4 — Can everything described be executed as delivered?

No.

Defect 7.1 — the operative master contains stale, impossible commands for the deliberately reduced seat tool.

Quote: “the seat runs `/usr/bin/python3 r3c2_ledger_tools.py compute <ledger.json> <out.json>`” and “the two seats' independently validated ledgers are merged by `/usr/bin/python3 r3c2_ledger_tools.py merge ...`”.

Conflicting operative quote: “The seat's tool is `r3c2_ledger_tools.py` ...; the seat runs its `census` and `validate` subcommands only.” Section 9 instead assigns lane computation to “`r3c2_lane_tools.py` ...; merge, then compute”.

Defect: the redacted master-only C3 block still instructs `compute` and `merge` through the seat tool that V17 expressly says has only `validate` and `census`. Those literal commands cannot be executed as described and conflict with the lane command assignment.

Exact replacement: replace that whole master-only command block with: “After both seats exit, the lane owner runs `/usr/bin/python3 r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>`, prints command, stdout, stderr and exit status, performs the stated one reconciliation if merge reports differing input-id sets, and then runs `/usr/bin/python3 r3c2_lane_tools.py compute <merged.json> <out.json>`, again printing command, stdout, stderr and exit status. `merge` alone may add `origin_alt` and `origin_evidence_alt`; `compute` alone adds `root_origins` and per-claim `rests_on`; either command rejects those computed fields when present prematurely.”

Items checked from the two permitted documents: the master and packet hashes; every command string stated in operative §§5 and 9; every named control code and its three-valued status; the candidate, exclusion, input, merged, computed, audit, dispatch, receipt and interpretation artefacts described; the fields `origin_alt`, `origin_evidence_alt`, `root_origins`, and `rests_on`; the seat paths `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_timeout.py`, `r3c2_seat_packet/SEAT_BRIEF.md`, `R3C2_SEAT_PACKET.sha256`, and the packet; the lane paths `r3c2_lane_tools.py`, the interpretation protocol, and the absolute referee-dispatch path; and the literal Python, SymPy, shasum, builder, census, validate, merge, compute, and timeout commands. I did not open or execute any third file, as the gate instruction expressly prohibited opening any file other than the packet and master. Consequently I do not claim filesystem-existence or digest verification for those third-file artefacts; the affirmative executability finding above follows solely from the master's internal command contradiction. C0 must not pass until the corrected command and the repaired control artefacts are exhibited.

R3C2_V17_GATE_COMPLETE
