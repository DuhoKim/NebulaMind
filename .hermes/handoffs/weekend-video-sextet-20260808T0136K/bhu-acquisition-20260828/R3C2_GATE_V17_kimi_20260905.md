ACCESS_SHA=fe194fb4aee7603dbeecbfeda62dc8507aba2a994e3b9a206c8089490700e1c9
PACKET_SHA=7fb7076b1b7fba9ae1e5544ce538e92345f8ffb14e80ca8d81ba254bba45a493
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology on the Planck 2018 baseline (STANDARD table), provenance is a first-class recorded attribute beside the verdict (seven-value taxonomy, quotations, two readers, dispute stop), and the chosen-input case is anticipated and pre-defended into the arithmetic group — but no hypothesis, no comparison model, no directional expectation, and no preferred outcome is inferable from any packet sentence.
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE — the packet states origin's handling (cited quotation, classified independently by both seats, re-classified by the C6 audit, disputes >10% of included claims stop the study) but no packet sentence says what consumes origin or what it decides; §1: "The reproduction verdict and the provenance fields are recorded separately."

=====================================================================
STEP 1 — PACKET-ONLY ANSWERS (answered before the master was read;
packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md, sha256 above)
=====================================================================

Q5 (answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY
OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER?
No. The only sentences that touch consequence at all are:
  (a) §4, CENSUS_PARTIAL: "INCONCLUSIVE, and it takes precedence over
      `CENSUS_COMPLETE`." — this labels what a partial census means for the
      study's conclusiveness; it does not make any outcome weightier than
      another for anyone, and a completeness definition is something any
      census must carry.
  (b) §11: "No tier, warrant token, standing or stamp moves." — this
      affirmatively denies that any outcome moves anything.
  (c) §3, REPRO_FAILED: "Wording: \"unreproduced from the stated inputs,\"
      not \"error.\"" — this constrains how a seat writes a negative outcome,
      not whether it finds one; it singles out REPRO_FAILED as the
      reputationally sensitive class but states no consequence.
No packet sentence ranks outcomes by consequence to the lane or to anyone.

Q6 (answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE
RECORDED `origin` OF AN INPUT IS FOR?
No — only its handling, never its use. What the packet lets me state:
  "Record its `origin` with the evidence C3 requires." (§2 step 3)
  "`origin` is one recorded attribute of a ledger record, beside `status`,
  `value`, `source_file` and `source_line`" (§3)
  "Every input's `origin` is classified independently by both seats." (C3)
  C6: the audit re-derives "re-classifying every input's `origin` from the
  pinned sources"; §4(6): disagreement "on inputs affecting more than 10% of
  included claims" files CENSUS_ORIGIN_DISPUTED; disputed inputs are
  "listed with both seats' classification and both quotations."
So after recording, origin is double-classified, machine-cited,
audit-reclassified, carried through disputes, and can stop the study. What
it is FOR — what consumes it, what conclusion it feeds, what it decides —
no packet sentence says. The one sentence that bears on it, §1's "The
reproduction verdict and the provenance fields are recorded separately.",
tells me only that the verdict does not consume it.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS
WILL SHOW? (hypothesis-reachability test, adversarial)
What I can infer, each with its leaking sentence:
  1. Domain. The corpus is cosmology, staked to the Planck 2018 baseline:
     "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing
     baseline." (C3 table note; H0/Ωm/σ8/τ/age 13.797 Gyr rows.) This is
     subject-matter content, not an expectation.
  2. Provenance is a first-class product. A seven-value origin taxonomy,
     machine-matched quotations, two independent classifiers, a >10%
     dispute stop, and audit re-classification tell me the census's output
     includes a provenance record that the lane cares about as much as the
     arithmetic — "The reproduction verdict and the provenance fields are
     recorded separately." (§1). What that record will be used to conclude:
     not stated anywhere.
  3. Contested terrain, side unknown. "Distinct from a claim whose inputs
     the paper DOES state, chosen or not — that claim is attempted and
     files `REPRO_EXACT` or `REPRO_FAILED`." (§3) — the lane anticipated an
     attempt to push chosen/fitted-input claims out of the arithmetic group
     and pre-committed against it. "Wording: 'unreproduced from the stated
     inputs,' not 'error.'" — the lane anticipated failure outcomes and
     pre-regulated their description. The IMPORTED machinery (a value cited
     from a pinned enumerable text becomes PRINTED from that source) — the
     lane expected cross-citing texts and built a rescue so those claims
     are attempted, not blocked. "INCONCLUSIVE, and it takes precedence
     over CENSUS_COMPLETE" — the lane pre-labelled the mixed outcome.
What I cannot infer: any hypothesis, any comparison model, any expected
tally or fraction, and — adversarially, trying both directions — whether
the lane hopes for REPRO_EXACT or REPRO_FAILED on the chosen-input claims.
The defensive clauses cut both ways (a lane expecting EXACTs guards them
from deflation; a lane expecting FAILEDs pre-softens the wording); no
packet sentence disambiguates. The stake I can reconstruct is the terrain
(chosen/fitted inputs stay in; failures are worded carefully; a partial
census is inconclusive), never the direction.

=====================================================================
SECTION 1 — OUTCOME CLASSES (with Q1)
=====================================================================
Per-claim (§3): six outcomes with a total first-match precedence
(REPRO_NO_DERIVATION_STATED > REPRO_BLOCKED > REPRO_INPUT_ABSENT >
REPRO_NOT_EVALUABLE > arithmetic group). I could not construct a real
claim fitting two outcomes: the BLOCKED/ABSENT boundary is the
named-source test (traced to a named source vs not), the precedence is
total, and every mixed state I built (blocked+absent, no-derivation+absent,
timeout+absent) files exactly one. Exhaustive: equation stated or not;
inputs consumable (PRINTED/STANDARD), BLOCKED, or ABSENT; attempt
completes or not. Every included claim lands somewhere.
Study-level (§4): seven classes with a total precedence; every collision
I constructed files exactly one. INCONCLUSIVE is genuinely reachable — a
single blocked or absent input anywhere in the corpus files CENSUS_PARTIAL
— hair-trigger by design, and stated.

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY
DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?
YES at the per-claim level, with one decidability seam (R2) and one
study-level hole that is §4's, not §3's (R1). Hardest cases traced:
  - Printed-but-chosen input ("we take β = 1/929.25"): status PRINTED
    (printed, not verbatim on the closed list), origin CHOSEN via
    ORIG_CHOICE_STATED; consumed by status; arithmetic completes → exactly
    one of REPRO_EXACT/REPRO_FAILED; lane-side rests_on USES_CHOSEN. One
    outcome, one rests_on.
  - Fitted parameter the paper states: PRINTED, origin FITTED; consumed;
    EXACT/FAILED; USES_FITTED. Decidable.
  - Imported value, named source is a pinned enumerable text, value
    machine-matches at the cited line: PRINTED from that source, origin
    IMPORTED; consumed. No match at the cited line, or source not in the
    manifest: REPRO_BLOCKED, status BLOCKED, origin IMPORTED, no value,
    never consumed. One outcome each way; the machine-match makes the
    boundary mechanical.
  - Value on the closed list the paper does NOT print but traces to a
    named non-manifest source: never STANDARD (§2 step 3 rule), BLOCKED →
    REPRO_BLOCKED. Value the paper prints verbatim on the list: STANDARD,
    string-compared. The two routes are outcome-identical by construction;
    no seat split is possible because the routing rule is explicit.
  - DERIVED chain: derived_from referential integrity, acyclicity, and
    "DERIVED needs parents" are script-asserted (verified in the delivered
    tool's usage text); an empty root set cannot occur.
  - No stated procedure: REPRO_NO_DERIVATION_STATED. Timeout:
    SYMBOLIC_TIMEOUT via the wrapper (smoke-tested, §7 below). Missing
    machinery: MACHINERY_UNAVAILABLE.
All six §3 outcomes are reachable; all seven §4 classes are reachable.
The two findings this section carries:
  R2 (seam): the stated-precision limb of REPRO_EXACT is not a mechanical
  test (see exact quote and repair below).
  R1 (hole): the per-claim outcome has no carrier artefact, no merge rule,
  and no disagreement class (see below).

FINDING R1 (substantive — §4 not exhaustive over two-seat outcome splits;
the sealed tally has no named assembly rule).
Verbatim: "5. **Record the outcome**, per claim, as one of §3" (§2) — no
artefact is named. The C1 candidate schema is "each candidate carries
`candidate_id`, `source_file`, `source_line`, `numeral`, `included`" plus
`attempts` — no outcome field. The C3 ledger schema is per-input — no
outcome field. Verbatim: "1. **`CENSUS_COMPLETE`** — **every included
claim carries exactly one outcome from the arithmetic group of §3.**" —
"carries" presumes one outcome per claim; the document never says whose
(seat A's, seat B's, merged) or by what rule.
Constructed state with no class: both seats agree on every candidate,
every input list, and every origin; every control passes identically in
both seats; on claim k, seat A files REPRO_EXACT and seat B files
REPRO_FAILED (a boundary value under the R2 seam, or a transcription
difference in the unschema'd outcome rows). Filing: not R3C2_NO_CLASS (no
control failed), not CENSUS_CONTROL_SPLIT (controls agree), not
CENSUS_DENOMINATOR_DISPUTED (enumeration and input lists agree), not
CENSUS_ORIGIN_DISPUTED (origins agree), not CENSUS_AUDIT_FAILED (the audit
runs after the tally is sealed and audits whichever outcome was filed —
it cannot see the disagreement), not CENSUS_PARTIAL (both outcomes are
arithmetic-group), and CENSUS_COMPLETE is undecidable — the claim carries
two outcomes. The design's own discipline — inclusion disagreement
classed, input-list disagreement classed, origin disagreement carried and
classed, control disagreement classed — has no outcome-level analogue,
although the reproduction outcome is the study's primary output.
Exact replacement (three parts):
  (a) In C1, replace "The candidate and exclusion ledgers use the script's
  candidate schema: each candidate carries `candidate_id`, `source_file`,
  `source_line`, `numeral`, `included`; every exclusion row names a
  candidate and a `kind`;" with "The candidate and exclusion ledgers use
  the script's candidate schema: each candidate carries `candidate_id`,
  `source_file`, `source_line`, `numeral`, `included`; every included
  candidate additionally carries `outcome` — one of the six §3 tokens, or
  `PENDING` before limb B — and every included candidate whose `outcome`
  is in the arithmetic group carries `printed_value` and
  `reproduced_value`; every exclusion row names a candidate and a `kind`;
  the `census` subcommand verifies after limb B that every included
  candidate carries exactly one §3 outcome and that arithmetic-group
  outcomes carry both values;"
  (b) In §4, insert a new class between 5 and 6: "**`CENSUS_OUTCOME_DISPUTED`**
  — the two seats' filed per-claim outcomes on an agreed included claim
  differ after one reconciliation against the printed numeral and the
  stated-precision rule of §3. The census does not proceed; the claim is
  listed with both seats' outcomes, both number pairs, and the step each
  seat reached." and replace the precedence order with "`R3C2_NO_CLASS`,
  `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`,
  `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`,
  `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`."
  (c) In §2 step 5, replace "**Record the outcome**, per claim, as one of
  §3" with "**Record the outcome**, per claim, as one of §3, in the
  candidate file's `outcome` field; the sealed reproduction tally is the
  merged candidate file on which the two seats' outcome fields agree,
  claim by claim, after the one reconciliation named in §4."

FINDING R2 (minor — stated-precision test not mechanical).
Verbatim: "**`REPRO_EXACT`** — the paper's number follows, within its own
stated precision, **from the paper's own recipe applied to the inputs it
states** (`PRINTED` or `STANDARD`)." The mechanical rounding rule is given
only for the no-stated-precision case ("Where the paper states no
precision for the claim, the printed precision is the claim's stated
precision"). When the paper prints an uncertainty ("67.4 ± 0.5"),
"within its own stated precision" does not say whether the test is
interval membership at 1×, at 2×, or rounding; two mechanical readers can
implement different tolerances and split — which is exactly the state R1
must class.
Exact replacement: append to the REPRO_EXACT definition: "**Where the
paper states an uncertainty, the test is |reproduced − printed| ≤ the
stated uncertainty, taken once — not doubled, not rounded; where the paper
states no precision, the rounding rule above applies.**"

=====================================================================
SECTION 2 — CONTROLS (with Q2)
=====================================================================
Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY,
C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION,
C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED,
C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. Unreached controls are NOT_RUN by two
converging clauses ("Once a stop class applies, later limbs are unreached
and their controls are `NOT_RUN`."; "Controls in an unreached limb are
`NOT_RUN`, never passes.").
Pass-by-assertion check: C1 and C2 are script-gated with printed command,
stdout, exit status (verified the subcommands exist in the delivered pinned
tool). C3's machine checks are script-asserted (usage text verified:
schema-only fields, allowed reason_code/origin pairs, no value on
ABSENT/BLOCKED, verbatim substring of cited line, STANDARD string equality
with the closed list, derived_from integrity/acyclicity, origin_search
required for ORIG_SILENT). C5 demands live output and makes a transcribed
value FAIL. C0 is an authored+independently-verified exhibition table.
C4_SEAT_ISOLATION and C5B are self-reported path lists — and the document
says so at full strength ("a self-reported control with a structural aid,
and is labelled so"; "PASS means the printed list contains no such row and
makes no claim that the list is complete"). C3's one asserted limb —
"completeness of a claim's input list against the paper's equation is
seat-authored and audited under C6, not machine-checked" — is declared,
not hidden.

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? YES, with
one stated floor. Test record (wrong but plausible): paper prints "We
adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹ from Planck (2018)"; I file origin CHOSEN
with ORIG_CHOICE_STATED quoting that sentence. The quotation
machine-matches the cited line, the reason_code/origin pair is allowed,
validate passes it — the machine checks that a quotation matches, not
that the precedence-correct code was chosen (the tie-break makes the
citation win: ORIG_CITATION precedes ORIG_CHOICE_STATED). What catches it:
(a) the second seat's independent classification — merge carries
origin_alt and the claim's rests_on is computed under both and marked
DISPUTED; (b) the C6 audit's re-classification of every input's origin
from the pinned sources — a disagreement is filed ORIGIN_DISPUTED,
reported with both quotations, and above 10% of included claims stops the
study. What does not catch it: the script. If both seats misclassify
identically, the record stands — and the document states exactly this
floor in the C3 text ("caught only by the second seat's independent
classification and the C6 re-classification, never by the machine; if
every reader misclassifies identically, the record stands — that floor is
stated here rather than implied away"). The floor is honest; the
mechanism is as strong as two independent readers plus a blind third can
make a citation-vs-choice judgement.

=====================================================================
SECTION 3 — CIRCULARITY
=====================================================================
Can the lane's expectation reach the evidence? I probed four channels:
  - Inclusion: assigned independently by two seats from the §1 rule alone,
    tolerance zero, disputes stop the study with the full ledgers
    reported. The §1 rule's content is hypothesis-free (verified: nothing
    in it references any lane conclusion).
  - Exclusion: kind-enumerated (five kinds), recorded with file/line,
    count-closed by the census script (declared vs recomputed, exit 0
    required), and — decisively — C6 audits "the full candidate and
    exclusion ledgers against every pinned source — completeness, not just
    outcomes". A contrary claim quietly excluded, or never enumerated, is
    ledger incompleteness → CENSUS_AUDIT_FAILED. This channel is closed.
  - Origin classification: machine-matched quotations, two seats, carried
    disputes, audit re-classification; the tie-break order favors the more
    severe root (citation first), which is the conservative direction.
  - Interpretation: lives outside this document, committed and receipted
    (receipt P) before limb A begins; the tally is receipted (receipt T)
    before the protocol is opened. The lane's hope cannot tune the
    protocol to the tally (P precedes the tally's existence) nor the tally
    to the protocol (T precedes opening). Closed at the record level; the
    mind-level limit is stated (§7, quoted in §5 below).
Q3's packet inferences (above) confirm the blind from the seat side: the
seat can see that provenance is classified with care, and cannot see what
it is compared against or hoped to show.

=====================================================================
SECTION 4 — THE BLIND
=====================================================================
Claim: the packet's redaction is machine-asserted by
r3c2_build_seat_packet.py against a forbidden list; if any forbidden
string survives, the packet is not written and C4_PACKET_REDACTED=FAIL.
Verified at the output level (the two-file constraint bars opening the
builder itself — stated here rather than implied away):
  - 48 SEAT-REDACT spans in the master; chunked span-content scan against
    the packet: zero leaks. (The only chunk hits are fragments of the
    ledger tool's sha256, which the packet prints legitimately and
    seat-visibly as the tool pin — not redacted content.)
  - Dropped sections: "## 0.", "## 7.", "## 8.", "## 10." and their
    distinctive strings all absent from the packet.
  - Redaction markers: no "SEAT-REDACT", "<!--", "-->" in the packet.
  - Forbidden-list tokens: no ΛCDM/LCDM, no pattern/magnitude/shape
    vocabulary, no custody-chain names (Tori/Blanc/Duho), no engine names
    (codex/kimi/agy), no consequence words (expectation, refute,
    consequential, invisible, favourable/unfavourable/favorable, stake,
    prior, tempting, warn), no HOLD, R3D, R3C_, UNSOUND/SOUND, no
    rests_on/root_origins/DERIVED_ONLY/USES_/two-tallies/interpretation/
    comparison/severity/leak/gate/version-history/REPRO_AFTER_CHOICE/
    ruling/principal/lane_tools. Benign hits only: "holds"/"placeholder",
    "--version"/"sympy.__version__" (the C5 commands themselves), and the
    role-word "custodian" with no name attached (the C6 seed mechanism is
    procedural content a seat must have).
  - Fidelity: every non-blank packet body line is byte-faithful to the
    span-stripped master (0 of N lines unaccounted); the packet embeds the
    master sha256 fe194fb4… which matches ACCESS_SHA exactly; the pin file
    R3C2_SEAT_PACKET.sha256 binds the packet digest, the ledger-tool
    digest, and names both the packet and SEAT_BRIEF.md (quiet greps; pin
    file not opened for content).
Does the C4 mechanism prevent what it claims? It claims the seat is not
furnished the pattern record and that the packet carries no forbidden
string — both verified above. It does not claim actual non-access, and
says so: "This is procedural, not enforced by the filesystem";
"C4_SEAT_ISOLATION is a self-reported control with a structural aid, and
is labelled so"; "It cannot prove a seat has no prior exposure from
training or an earlier session — nothing available here can." The limits
are stated honestly and at full strength.
FINDING R4 (minor hardening). Verbatim: "**The seat packet is built
mechanically, by `r3c2_build_seat_packet.py`, and its redaction is
asserted.**" The builder whose self-assertion carries the entire redaction
guarantee is itself unpinned in the document (verified: exists, tracked,
sha256 f2f6c9ab4225630d3ac8c80d75501375a89496ff53a91a5f585dd0d6f27f7d67 —
not printed anywhere in the master). The pin binds the packet's bytes to
the dispatch record, but "no forbidden string survives" rests on an
unpinned script.
Exact replacement: append to C4: "**The builder is pinned beside this
document, sha256 `f2f6c9ab4225630d3ac8c80d75501375a89496ff53a91a5f585dd0d6f27f7d67`;
a dispatch built by any other bytes files `C4_PACKET_REDACTED=FAIL`.**"

=====================================================================
SECTION 5 — THE SEAL (§7)
=====================================================================
Ordering verified against the text: receipt P (protocol hash + commit id,
acknowledged and timestamped by the external custodian) must exist before
limb A begins; receipt T (tally hash + commit id) must exist before the
protocol is opened; "The relay is complete only when it is receipted."
After opening, the custodian independently re-hashes tally and protocol
and verifies all four values against both receipts; the interpretation
report must print both receipts and the four verified values; any missing
receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation
NOT_RUN, and voids the comparison. The relay binds what it claims, before
the step it must precede: P precedes the tally's existence (the protocol
cannot be written to suit the tally) and T precedes the protocol's opening
(the tally cannot be tuned to the protocol, nor the protocol swapped after
the tally is known). The custody rationale is stated without overclaim:
"a local commit in Tori's own custody can be amended or reset, so it is
tamper evidence, not an external custodian", and "It does not prove Tori
had not already guessed the answer — nothing available here could prove
that. It bounds the record, not the mind." The C6 seed is chained to the
same receipt ("supplied ... after the tally digests are receipted ... and
recorded with the receipt"), and a seed never supplied files
CENSUS_AUDIT_FAILED with the missing seed named — no silent stall.
Observation (no defect): a withheld receipt P before limb A has no named
class — the study simply never begins. Nothing runs and nothing can
mis-file, so this is recorded as a property, not a repair: if the lane
wants it named, one sentence in §4.4 suffices ("a seal receipt withheld
before limb A leaves the study NOT RUN and is reported as such").

=====================================================================
SECTION 6 — FAIRNESS
=====================================================================
Negative outcomes: REPRO_FAILED carries the mandated wording
"unreproduced from the stated inputs," not "error"; REPRO_BLOCKED and
REPRO_INPUT_ABSENT name the input and the source, attributing nothing;
REPRO_NO_DERIVATION_STATED names the passage; REPRO_NOT_EVALUABLE names
the lane's own limitation (timeout or machinery), not the paper's. The
word "error" appears nowhere in any operative outcome definition. No
class clause asserts more than its definition, with one named exception:
the class name `REPRO_EXACT` asserts exactness while its definition is
stated-precision-bounded (round-to-printed-numeral, or R2's interval
test). The document itself records that the rename is escalated to the
principal; a rename is a redefinition and is not this gate's to apply.
Effect inside this document: none — the definition governs filing and is
printed beside the name in every report context. COSMETIC in effect;
recorded, principal's.
The INCONCLUSIVE label on CENSUS_PARTIAL is a study-level characterization
stronger than some states it covers (a 99%-reproduced census with one
blocked input files INCONCLUSIVE), but §4.2 requires "Report each and
why", so the composition is always visible beside the label. Noted, not
a defect.

=====================================================================
SECTION 7 — STALL / EXECUTABILITY (with Q4)
=====================================================================
Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?
Checked, each with how:
  - r3c2_ledger_tools.py: exists, tracked, sha256 matches pin 3519ca61…
    exactly; usage executed — subcommands census and validate present and
    match C1/C2's command shapes, exit codes 0/1/2 as documented.
  - r3c2_lane_tools.py: exists, tracked, sha256 matches pin 6e00b13b…;
    usage executed — merge and compute present; rejects a ledger arriving
    with root_origins/rests_on set (exit 2); disputed roots carried as a
    DISPUTED pair; matches the C3/§9 descriptions.
  - r3c2_timeout.py: exists, tracked, sha256 matches pin fbb9bef7…;
    live smoke tests — fast child: WRAPPER_COMMAND/stdout/stderr/
    WRAPPER_EXIT=0/WRAPPER_ELAPSED printed, exit 0; `sleep 3` under a
    1.0 s cap: SYMBOLIC_TIMEOUT printed, exit 124, elapsed 1.004 s —
    exactly the documented contract.
  - R3C2_CORPUS_MANIFEST.md: exists, tracked, sha256 matches pin
    300d4da1… exactly.
  - R3C2_SEAT_PACKET.sha256: exists, tracked; quietly verified it binds
    the packet digest (7fb7076b…), the ledger-tool digest (3519ca61…),
    and names R3C2_SEAT_PACKET.md and SEAT_BRIEF.md.
  - r3c2_seat_packet/SEAT_BRIEF.md: exists, tracked (not opened — the
    two-file constraint; its redaction assertion is the same builder
    contract whose packet output I verified).
  - r3c2_build_seat_packet.py: exists, tracked (see R4 — unpinned).
  - R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md: exists (named in C4).
  - R3C2_INTERPRETATION_PROTOCOL_20260904.md: exists (named in §7; receipt
    P binds it before limb A).
  - /Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh: exists at the
    named absolute path.
  - C5 harness, live on this machine: /usr/bin/python3 --version →
    Python 3.9.6, exit 0; sympy import → 1.14.0, exit 0; shasum of the
    interpreter printed, exit 0.
  - C6 sampling frame: pure stdlib (random.Random(int(seed,16)).sample on
    sorted ids, k = min(max(1, ceil(0.20×N)), R)) — executable as written;
    bounded by R; deterministic under the C5-pinned interpreter.
FINDING R3 (executability — two operative commands cannot run as
delivered). Verbatim (C3, inside a SEAT-REDACT span): "**The script is
`r3c2_ledger_tools.py`, committed beside this document, sha256
`3519ca617434ddca222c0a85e8a5630f30710b8a9c7d74d79bfc4fa56973b959`; the
seat runs `/usr/bin/python3 r3c2_ledger_tools.py compute <ledger.json>
<out.json>` ... the two seats' independently validated ledgers are merged
by `/usr/bin/python3 r3c2_ledger_tools.py merge <ledger_seatA.json>
<ledger_seatB.json> <merged.json>`". Verified against the delivered,
pin-matching tool: it has `census` and `validate` only; `compute` and
`merge` exit 2 with usage. Those subcommands live in the delivered
r3c2_lane_tools.py (sha 6e00b13b…, pin-matching), which the same document
(§3 lane note, §9) correctly says is lane-side and never given to a seat,
and which the packet corroborates ("the seat runs its `census` and
`validate` subcommands only"). The span is a stale second live copy of a
rule that moved — the failure mode §6's own note names. Seat-safe:
verified the span is redacted from the packet; it cannot mislead a seat.
It would stall the lane owner at the merge step (usage error, loud, no
wrong result), and it mis-pins: the paragraph attaches the ledger tool's
sha to lane-tool functions.
Exact replacement: in that span, replace "The script is
`r3c2_ledger_tools.py`, committed beside this document, sha256
`3519ca617434ddca222c0a85e8a5630f30710b8a9c7d74d79bfc4fa56973b959`; the
seat runs `/usr/bin/python3 r3c2_ledger_tools.py compute <ledger.json>
<out.json>`" with "The lane-side script is `r3c2_lane_tools.py`, committed
beside this document, sha256
`6e00b13b2dc2c54babc2b5b39c4cc6a81ce003e21a862ca0ea03e5b19dee3462`; after
both seats exit, the lane owner runs `/usr/bin/python3
r3c2_lane_tools.py compute <merged.json> <out.json>`", and replace
"`/usr/bin/python3 r3c2_ledger_tools.py merge <ledger_seatA.json>
<ledger_seatB.json> <merged.json>`" with "`/usr/bin/python3
r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json>
<merged.json>`", keeping the exit-1 reconciliation sentence unchanged.
Stall inventory otherwise: every per-claim stall state has an outcome
(SYMBOLIC_TIMEOUT, MACHINERY_UNAVAILABLE), every study-level stop has a
class, every stop makes later controls NOT_RUN, and the one silent state
(withheld receipt P) mis-files nothing.

COSMETIC (changes no class filed, no control's passability, no
reachability, no conclusion, no tested value):
  C-1. Packet §1 redaction scar: "applied to the inputs it states **The
       reproduction verdict and the provenance fields are recorded
       separately.**" — the bold opened before the redaction point closes
       inside the removed span, leaving a dangling `**` in the seat's
       copy. Meaning intact. Optional repair: in master §1, move the
       closing `**` of the question's bold outside the SEAT-REDACT span.

=====================================================================
FAILED ATTACKS (what I tried that held)
=====================================================================
  - Two-outcome collisions per claim (blocked+absent, no-derivation+
    absent, timeout+absent, printed-and-cited, printed-and-on-list):
    exactly one filing each, via the named-source boundary and the total
    precedence.
  - Two-class collisions at study level (control split + denominator
    dispute; origin dispute + audit failure; audit failure + partial):
    exactly one filing each via the §4 precedence.
  - STANDARD-as-selectable-family: closed list is printed literally, value
    compared as exact string, and a value not in the table "is not
    STANDARD whatever its provenance"; the unprinted-list-value routing
    rule removes the seat-split route.
  - DERIVED laundering: cycle, dangling parent, and DERIVED-without-
    parents are all script-rejected (usage text verified).
  - Planted computed fields: a seat-authored ledger carrying rests_on,
    root_origins, origin_alt, or any out-of-schema field fails validate
    (usage text verified).
  - Seed gaming: seed is external, post-receipt, not a function of the
    tally; frame frozen (sorted ids, serialized seed, bounded k).
  - Quiet exclusion of a contrary claim: count closure at C1 plus C6's
    full-ledger completeness audit against every pinned source.
  - Seal overclaim: none found; every limit I probed for is stated in
    prose at full strength (filesystem non-enforcement, self-report,
    prior exposure, guessed-answer).
  - Blind at the byte level: 48 spans, zero leaks; dropped sections gone;
    markers gone; forbidden tokens absent; packet body byte-faithful.

=====================================================================
EVIDENCE LEDGER
=====================================================================
Files read for content (exactly the two named): the master (899 lines,
full) and the seat packet (267 lines, full). The pin file
R3C2_SEAT_PACKET.sha256 was matched with quiet greps only (no content
printed). No gate files, no version-history artefacts, no third content
file opened; §10 of the master was read as history and binds nothing in
this verdict.
Commands run: shasum -a 256 on the master, the packet, and each named
artefact; test -e on each named path; git ls-files --error-unmatch on the
committed-beside claims (tracked/untracked only, no metadata); quiet
grep -q pin checks; usage executions of r3c2_ledger_tools.py and
r3c2_lane_tools.py with no arguments; two live r3c2_timeout.py smoke
tests; the three C5 commands (sympy via a /tmp script because bare
`python3 -c` is shell-blocked here); a read-only Python checker
(/tmp/r3c2_gate_blind_check.py, /tmp/r3c2_gate_fidelity_check.py) over
exactly the two named files for span leakage, dropped sections, forbidden
tokens, and packet fidelity.
Constraint compliance: no writes outside this report and /tmp scratch; no
file in the handoff directory modified; the master and packet shas at
report time are the shas on lines 1-2.

Repairs required before freeze: R1 (outcome carrier + CENSUS_OUTCOME_DISPUTED
+ merge rule), R2 (stated-precision test), R3 (C3 span tool-name swap),
R4 (pin the builder). Cosmetic: C-1 (packet bold scar). Principal's,
already escalated in-document: the REPRO_EXACT rename.

R3C2_V17_GATE_COMPLETE
