ACCESS_SHA=e67339905813549fec1c2cebb58cad264e9cc1b94a95aa0d2715866f9f356b6e
PACKET_SHA=37786e5e2c37166ff21fd7d430491afd14584b62b511185ff49007da2cd706a2
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=independent provenance comparison/audit; the packet says the auditor re-derives “without sight of earlier work and re-classifying every input's `origin` from the pinned sources” and records “`MATCH` or `MISMATCH` against the sealed record”

## Packet-first questions

### Q5 — consequence visibility

No. No sentence in the packet says that any outcome is more consequential than another to the lane or to anyone. The packet states possible empirical and procedural outcomes symmetrically; stop and audit consequences do not identify an empirically preferred result.

### Q6 — purpose of `origin`

Yes. The packet says the third seat re-derives “without sight of earlier work and re-classifying every input's `origin` from the pinned sources” and that the audit records “`MATCH` or `MISMATCH` against the sealed record.” It also says an origin disagreement is reported and, above the specified threshold, files `CENSUS_ORIGIN_DISPUTED`. Thus `origin` is retained for independent provenance checking, comparison with the sealed record, and origin-dispute classification.

### Q3 — hypothesis reachability

I can infer no hypothesis, comparison model, preferred empirical outcome, or expected pattern from the packet alone. The packet supplies a census question, outcome taxonomy, provenance taxonomy, controls, and stop rules, but no sentence predicts how many claims will reproduce, ranks empirical outcomes, identifies a comparison model, or maps an outcome to a lane conclusion. The detailed provenance requirements show that provenance matters to the study, but do not reveal a hypothesis or preferred provenance category.

## 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive after applying the stated precedence. Co-occurring terminal conditions do not create double filing: for example, a claim with no computational procedure and also no usable input files `REPRO_NO_DERIVATION_STATED`; a claim with one cited-but-unavailable input and another wholly absent input files `REPRO_BLOCKED`; a fully supplied calculation that times out files `REPRO_NOT_EVALUABLE`; and a completed calculation files exactly one arithmetic outcome under the precision rule. A real included claim with a procedure but an input neither printed nor traced to a source has exactly `REPRO_INPUT_ABSENT`; none of the six lacks a domain.

The study-level classes are not exhaustive. The document itself specifies a reachable state with no class:

> “Open (§10.12): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — the gate found §4 not exhaustive over that state; a class is not added or redefined by this document's author, so the gap is recorded here as open rather than written in.”

Take an agreed included claim and agreed input list for which seat A files `REPRO_EXACT` and seat B files `REPRO_FAILED`, with the disagreement surviving the one permitted reconciliation. No C0–C5b control need fail or split; the denominator and input lists agree; origins can agree; the audit has not been reached; and neither `CENSUS_PARTIAL` nor `CENSUS_COMPLETE` can be filed from a merged candidate file because §2 requires agreement claim by claim. This tally fits none of the seven study-level classes. Consequently “Exactly one study-level outcome is filed” is false for a reachable state. `INCONCLUSIVE` is reachable—any included claim with, for example, a genuinely absent required input yields `REPRO_INPUT_ABSENT`, hence `CENSUS_PARTIAL`—but reachability of `CENSUS_PARTIAL` does not cure the uncovered disagreement state.

Defect 1.1 — SUBSTANTIVE. Exact replacement: replace the quoted open paragraph with:

> **`CENSUS_OUTCOME_DISPUTED`** — the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5. The census does not proceed; every disputed claim is listed with both outcomes, both reproduced values where applicable, and the cited source passages.

Then insert `CENSUS_OUTCOME_DISPUTED` in the study-level precedence immediately after `CENSUS_DENOMINATOR_DISPUTED`, and add it to C0's required exhibition table.

### Q1 — option (c), reachability, and decidability

Yes at the per-claim level: option (c) makes every declared per-claim outcome reachable and keeps the reproduction question decidable. The hardest traces are:

- Printed-but-chosen input: a paper says “we choose β = 0.1,” states `y = 2β`, and prints `y = 0.2`. The ledger record is `status=PRINTED`, `origin=CHOSEN`, `ORIG_CHOICE_STATED`, with machine-matched value and quotation. Arithmetic consumes β and files exactly `REPRO_EXACT`; lane computation gives root `CHOSEN` and exactly `rests_on=USES_CHOSEN`.
- Stated fitted parameter: a paper states that fitting gives `a = 3`, directs `y = a²`, and prints `9`. The record is `PRINTED/FITTED` with `ORIG_FIT_STATED`; it is consumed, files exactly `REPRO_EXACT`, and computes exactly `USES_FITTED`. If the paper printed `8`, only the outcome changes to `REPRO_FAILED`.
- Imported value: if the claiming paper cites a named enumerable pinned source and the value machine-matches at the cited line, the record is `PRINTED/IMPORTED`, is consumed, files exactly one arithmetic outcome, and computes `USES_IMPORTED`. If the source is outside the enumerable manifest or the cited-line value does not machine-match, the record is `BLOCKED/IMPORTED` with no value, files exactly `REPRO_BLOCKED` by precedence, and computes the imported root where a ledger record exists.

Thus the option-(c) tension is resolved for individual claims. The preregistration remains unsound because the reachable cross-seat disagreement has no study-level class.

## 2. Controls

Each C0–C6 control has an exact `PASS|FAIL|NOT_RUN` code, and the document says unreached controls are `NOT_RUN`. C0 requires an exhibition table and independent verification; C1, C2, C3, and C5 require printed command output and exit status; C6 requires a printed JSON artefact. These cannot validly pass by a bare token under the written predicates. C4 and C5b can pass only on printed path-list evidence, but the list is self-reported and may be incomplete; the master states that limit honestly rather than claiming enforcement.

### Q2 — is provenance checkable rather than asserted?

Yes, but semantically by independent readers rather than fully by code. Test record: the source line says “We adopt H0 = 67.4 from Planck (2018),” while a plausible wrong ledger records `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and uses that full line as `verbatim`. The substring and value machine checks can pass. The written reason-code precedence requires `ORIG_CITATION` and `IMPORTED` because the sentence names an external source. The second seat's independent classification and C6's source-based re-classification are the mechanisms that catch the planted error, producing an origin disagreement/`MISMATCH`; `validate` alone cannot establish the semantic applicability of the reason code. If both independent readers make the same semantic mistake, it survives. That residual human-audit limit is real, but this is not provenance by unsupported assertion: the source location, quotation, machine match, deterministic tie-break, second classification, and audit make the field checkable.

No additional control defect found.

## 3. Circularity

The packet-only inclusion rule contains no hypothesis, model, magnitude pattern, or outcome preference. Inclusion and exclusion are independently recorded by two seats; surviving inclusion disagreement stops the study; the full candidate/exclusion ledgers are audited. A contrary quantitative claim cannot quietly disappear without an exclusion row and full-source completeness audit.

Origin classification can influence the later interpretation through lane-computed `rests_on`, but the seats do not see the mapping, severity order, interpretation protocol, or hypothesis. Origin is supported by quotations, independently classified twice, and re-classified in C6. The interpretation protocol is committed and externally receipted before limb A. No route is specified by which the lane's expectation can tune inclusion, exclusion, provenance, or arithmetic after seeing the tally.

No circularity defect found.

## 4. The blind

The master does not overclaim machine-enforced filesystem blindness. It says:

> “The builder drops §0, §7, §8 and §10 whole, strips every span marked `SEAT-REDACT` in this document, and then asserts that no string on the forbidden list survives anywhere in the output — the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone...”

That is a machine assertion of specified transformations and forbidden strings, not a proof that all semantic leakage is absent. The supplied packet passes the requested adversarial tests: it exposes neither relative outcome consequence nor a hypothesis/comparison/preferred result. It does reveal that origin is later independently audited, contrary to the master’s statement that “the packet no longer reveals ... what the study does with it.” That sentence is inaccurate, because C6 visibly says origin is re-classified and matched against the sealed record. However, the master immediately defines the accepted floor as revealing that provenance matters, and the packet does not reveal the hidden `rests_on` comparison or interpretive mapping. The inaccurate sentence does not change a filed class, control passability, reachability, conclusion, or tested value.

Defect 4.1 — COSMETIC. Replace:

> “the packet no longer reveals that provenance is compared with reproduction, what a summary of it would be called, or what the study does with it.”

with:

> “the packet reveals that recorded provenance is independently re-classified and checked against the sealed record, but does not reveal that provenance is combined with the reproduction tally, what that summary is called, or how the interpretation uses it.”

C4 itself relies on a redacted out-of-lane copy plus dispatch digests and self-reported path lists; it does not prevent deliberate absolute-path access. The document explicitly says so. Its claimed limit is honest.

## 5. The seal

The two-receipt relay binds what it claims to bind in time. Receipt P contains the interpretation protocol hash and commit id before limb A may begin. Receipt T contains the tally hash and commit id after tally commitment but before protocol opening. The protocol cannot be opened without both receipts, and the custodian independently re-hashes both objects and checks all four values. This establishes prior existence of the receipted bytes, not ignorance or mental blindness; the document states that limitation explicitly. Missing or mismatching receipts route to `CENSUS_AUDIT_FAILED` and interpretation `NOT_RUN`.

No seal defect found.

## 6. Fairness

The negative arithmetic outcome is expressly worded “unreproduced from the stated inputs,” not “error.” Blocked, absent, no-derivation, not-evaluable, dispute, control, and audit classes describe procedural states rather than scientific fault.

The name `REPRO_EXACT` asserts exactness although its definition includes agreement only within stated uncertainty or rounding precision:

> “`REPRO_EXACT` — the paper's number follows, within its own stated precision...”

A reproduced value can differ numerically from the printed value and still enter this class. The label therefore asserts more than its definition.

Defect 6.1 — SUBSTANTIVE under the requested class-name test. Exact replacement: replace `REPRO_EXACT` everywhere in operative sections and executable schemas with `REPRO_WITHIN_STATED_PRECISION`, including the arithmetic-group definition and C1 permitted outcome tokens. Preserve the current membership and precision test unchanged.

## 7. Stall / executability

### Q4 — can everything be executed as delivered?

Not as a complete study, because the reachable surviving per-claim outcome disagreement has no executable filing instruction; Defect 1.1 is a stall state.

Within the restriction not to open any file other than the packet and master, I checked named delivered artefacts by filesystem metadata only. These all exist at the stated paths: `R3C2_CORPUS_MANIFEST.md`; `r3c2_ledger_tools.py`; `r3c2_lane_tools.py`; `r3c2_timeout.py`; `r3c2_build_seat_packet.py`; `r3c2_seat_packet/R3C2_SEAT_PACKET.md`; `r3c2_seat_packet/SEAT_BRIEF.md`; `R3C2_SEAT_PACKET.sha256`; `R3C2_INTERPRETATION_PROTOCOL_20260904.md`; `/usr/bin/python3`; and `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`. I did not inspect their contents. The Python harness commands execute as delivered: Python reports `3.9.6`, SymPy imports and reports `1.14.0`, and `/usr/bin/python3` hashes to `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`.

I checked the literal command specifications in the master: packet build; ledger `census`; ledger `validate` with `.` as source directory; lane `merge`; lane `compute`; timeout wrapper with `120.0 -- <command>`; the three harness commands; and the absolute dispatch path. Their named programs/interpreter exist, their working-directory requirements are stated, and placeholders are required to be resolved and printed. Candidate, exclusion, input, merged, computed, audit, dispatch, check-sheet, and receipt files are run outputs, so their absence before any derivation is not a missing delivered artefact. The document defines the fields said to be produced by `merge`, `compute`, and C6. I could not behavior-test scripts without opening/executing files forbidden by the review scope, and no claim to such a test is made.

Defect 7.1 — SUBSTANTIVE; same defect and exact replacement as 1.1. A surviving per-claim outcome split stops execution without a filing class.

Overall verdict: `PREREG_UNSOUND`. The design must not freeze until the reachable outcome-disagreement state has a declared class and precedence. The `REPRO_EXACT` overclaim should also be renamed, and the master’s description of what the packet reveals about origin should be corrected.

R3C2_V18_GATE_COMPLETE
