ACCESS_SHA=e67339905813549fec1c2cebb58cad264e9cc1b94a95aa0d2715866f9f356b6e
PACKET_SHA=37786e5e2c37166ff21fd7d430491afd14584b62b511185ff49007da2cd706a2
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=STATABLE_ONLY_AS_A_CHECK: both seats classify it independently and disagreements can stop the census ("Every input's `origin` is classified independently by both seats", C3; `CENSUS_ORIGIN_DISPUTED` §4) and the C6 audit re-classifies it from the pinned sources; what conclusion it feeds is nowhere in the packet — "The reproduction verdict and the provenance fields are recorded separately." (§1)

Seat: kimi. Fresh adversarial gate of the DESIGN as written. Two files opened, in this order: the seat packet first (answered Q5, Q6, Q3 from it alone), then the master. No gate file, no script source, no manifest content, no third document was opened; all other checks were existence/digest/git metadata, and the one content exception is the digest pin file the packet itself points to (Q4 requires verifying it; it contains only digests). Section 10 of the master is treated as non-binding history throughout; every judgement below is from the operative text (§1–§7, §9, §11) and my own constructions.

=====================================================================
STEP 1 — ANSWERS FROM THE PACKET ALONE (given before the master was read)
=====================================================================

Q5 — CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER?
No. No packet sentence weights any outcome above another. The only sentence that addresses consequence at all denies it for every outcome: "No tier, warrant token, standing or stamp moves." (§11). The two precedence sentences — "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4) and the filing order of §4 — are parts of the exactly-one filing rule, stated as such; precedence is not weight. "No tally is filed; report which." (`CENSUS_AUDIT_FAILED`) is procedural. CONSEQUENCE_VISIBLE=NO.

Q6 — CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` IS FOR?
Partially, and the boundary is exact. What the study DOES with it after recording is stated: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed `ORIGIN_DISPUTED` and "disagree on inputs affecting more than 10% of included claims" stops the census as `CENSUS_ORIGIN_DISPUTED` (§4); the C6 auditor re-derives claims "re-classifying every input's `origin` from the pinned sources". What it is FOR beyond those checks is not stated anywhere: the arithmetic consumes status, never origin ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`."), §1 asks only the reproduction question, and "The reproduction verdict and the provenance fields are recorded separately." No tally of origins, no comparison, no consumer of provenance appears in the packet. So: the checks are statable; the purpose is not.

Q3 — WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?
Nothing about any hypothesis, comparison model, preferred outcome, or "pattern". Adversarial reconstruction attempts and what each actually yields:
(a) The corpus is cosmology. The closed STANDARD list is "the Planck 2018 TT,TE,EE+lowE+lensing baseline" (H₀, Ω_m, Ω_Λ, Ω_b h², Ω_c h², n_s, σ₈, τ, ln(10¹⁰ A_s), age) plus G, c, ħ, k_B. That leaks the DOMAIN of the corpus. It says nothing about which tally the lane wants; the list only fixes canonical strings, and the reproduction test compares against the paper's own printed numeral, never against the list.
(b) The lane anticipates that some claims may not reproduce and pre-commits to neutral wording: "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" That constrains how a negative is described, not whether one is found; any census implies failures are possible. No direction.
(c) An external system of tiers, warrant tokens, standing and stamps exists, and this census is fenced from it (§11). Fencing implies stakes exist somewhere in the wider system; it gives no direction — nothing here moves them whatever the tally.
(d) The document has been reviewed before: "the gate found §4 not exhaustive over that state" (§4 open note) and "an open decision recorded in §10.12" (§2 step 5) tell the seat that prior review occurred and one decision is open. Process residue, no outcome direction (see finding C4 — cosmetic).
(e) The elaborate dispute machinery (inclusion, origin, control splits) shows the lane expects disagreements are possible; the machinery is symmetric and stops the study rather than steering it.
Stake reconstruction fails: I cannot say which tally helps or hurts anything or anyone. LEAK=NONE, with the content-level residue (a)–(d) recorded here because it is visible; none of it reaches a hypothesis, a comparison model, a preferred outcome, or a pattern.

=====================================================================
1. OUTCOME CLASSES — and Q1
=====================================================================

Per-claim (§3): six outcomes plus a total precedence (`REPRO_NO_DERIVATION_STATED`, `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, then the arithmetic group). I tried to construct a claim fitting two outcomes or none:
- Blocked input + absent input in one claim: precedence files `REPRO_BLOCKED`. One.
- Timeout on a claim whose inputs are all present: `REPRO_NOT_EVALUABLE` precedes the arithmetic group. One.
- Paper prints an uncertainty AND the reproduced value also rounds to the printed numeral: "where it states none, the rounding rule above applies" makes the two tests mutually exclusive by whether an uncertainty is stated. One test applies.
- Claim with no stated procedure at all vs one whose procedure the lane cannot run: both conditions can be argued for a named-but-unspecified procedure — see finding R2. Even there, both candidate classes are non-arithmetic, so the study-level class (`CENSUS_PARTIAL`) is identical either way, and the precedence order is total.
- No state with zero outcomes found: every §1 claim either states a derivation or not; every input is printed/cited-or-not; the attempt terminates, times out, or is impossible — each branch has a named class.
Exhaustive and mutually exclusive as written, via the stated precedence. `CENSUS_PARTIAL` (the INCONCLUSIVE class) is genuinely reachable — a single blocked or absent input anywhere in the corpus reaches it — and it "takes precedence over `CENSUS_COMPLETE`".

Study-level (§4): seven classes plus a total filing order. A tally satisfying two (e.g., an audit mismatch after a partial census) files exactly one by the order (`CENSUS_AUDIT_FAILED` before `CENSUS_PARTIAL`). One reachable state has no class AS WRITTEN: a per-claim outcome split surviving the one §2 step-5 reconciliation. The document discloses this in operative text — "the class filed when a disagreement survives that reconciliation is **an open decision recorded in §10.12**; this document is not frozen until that decision is recorded" (§2 step 5) and the §4 open note — and refuses to freeze over it. This is D0 below: disclosed by the document itself, not a fresh finding, and it caps the verdict.

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?
YES, with two named boundary repairs (R1, R2) that do not disturb either limb. Hardest cases traced to exactly one outcome and one `rests_on`:
- PRINTED-BUT-CHOSEN: paper prints "we adopt H₀ = 70 km s⁻¹ Mpc⁻¹" (not verbatim on the closed list). §2 step 3: status `PRINTED`; `ORIG_CHOICE_STATED` → origin `CHOSEN`. §2 step 4 consumes it (status, not origin, gates consumption). Arithmetic gives the paper's number or not → `REPRO_EXACT` or `REPRO_FAILED` by the stated-precision rule. Script-computed roots {CHOSEN} → `rests_on` `USES_CHOSEN`. One outcome, one `rests_on`.
- FITTED AND PRINTED: "our fit gives β = 1.92 ± 0.04" → `PRINTED` + `FITTED`; consumed; tested as |reproduced − printed| ≤ 0.04 once; `USES_FITTED`. One each.
- IMPORTED WITH A MATCH: value not printed in the claiming paper, named source enumerable in the manifest, machine-match at the cited line → "classified `PRINTED` from that source, with `origin` `IMPORTED`" → consumed; `USES_IMPORTED`. IMPORTED WITHOUT A MATCH or source not enumerable → status `BLOCKED` with no value, claim files `REPRO_BLOCKED` (precedence ahead of `REPRO_INPUT_ABSENT`); the `BLOCKED` record's `IMPORTED` origin still enters the roots. One each.
- PRINTED, CITED, AND VERBATIM ON THE CLOSED LIST: "Ω_m = 0.3153 from Planck (2018)" → status `STANDARD` (verbatim rule), and the reason-code tie-break files `ORIG_CITATION` first → origin `IMPORTED` → `USES_IMPORTED`; the same numeral printed without a citation → `ORIG_CONSTANT` → `STANDARD`, `DERIVED_ONLY`-eligible. The two routes diverge in `rests_on` only, both mechanically, and the tie-break is a stated total order both seats apply identically. Decidable.
- UNDECLARED BUT PRINTED: `PRINTED` + `UNDECLARED` (with the printed `origin_search`); consumed; `USES_UNDECLARED`. One each.
- NO DERIVATION: files `REPRO_NO_DERIVATION_STATED`, first in precedence even when inputs are also absent. TIMEOUT: the committed wrapper exits 124 and prints `SYMBOLIC_TIMEOUT` → `REPRO_NOT_EVALUABLE`; one repeat permitted.
Reachability: all six per-claim outcomes and all seven study-level classes have constructed paths above and in §4/§5. Decidability: the per-claim test is a numeric comparison against a stated rule; classification is machine-checked; filing is by total precedence. The tension that failed earlier rounds — admissibility gating reachability — is gone under option (c), because provenance no longer gates consumption.

FINDING R1 (minor): ASYMMETRIC UNCERTAINTY UNNAMED.
Verbatim: "Where the paper states an uncertainty, the test is |reproduced − printed| ≤ the stated uncertainty, taken once — not doubled, not rounded; where it states none, the rounding rule above applies."
Defect: a claim printed as "13.8 +0.2/−0.1 Gyr" has two "stated uncertainties"; one seat can test against the larger half-width (`REPRO_EXACT`) and the other against the smaller (`REPRO_FAILED`). Both outcomes are arithmetic-group, and a surviving split routes into the disclosed open decision, so nothing is unreachable or undecidable — but the rule does not name the case, and it can change a tested value.
Exact replacement (append): "**Where the stated uncertainty is asymmetric, the stated uncertainty is the half-width on the side the reproduced value falls.**"

FINDING R2 (minor): NAMED-BUT-UNSPECIFIED PROCEDURE.
Verbatim: "**`REPRO_NO_DERIVATION_STATED`** — the paper prints the claim as its own result but **states no equation or computational procedure that could produce it**, so there is nothing to attempt."
Defect: "the value follows from our N-body simulation" NAMES a computational procedure without stating one a seat could attempt; one seat can file `REPRO_NO_DERIVATION_STATED` (nothing attemptable stated) and another `REPRO_NOT_EVALUABLE`/`MACHINERY_UNAVAILABLE` (a procedure stated, machinery lacking). Precedence orders the two classes but not the judgement of which condition holds. Study-level impact is nil (both are non-arithmetic → `CENSUS_PARTIAL`), and the split routes to reconciliation/audit; the per-claim label is what can diverge.
Exact replacement (append to the class): "**A procedure named but not specified — a sentence that says where the number came from without stating operations a seat could attempt — states no computational procedure that could produce it; file this class and name the passage.**"

FINDING D0 (disclosed by the document, verdict-capping, no replacement text mine to write): the surviving per-claim outcome split has no study-level class as written (§2 step 5 and the §4 open note say so and declare the document not freezable until the principal records the decision). §4 is therefore not exhaustive over that one reachable state; the document itself blocks the freeze over it. Full SOUND is not certifiable while it stands; it is the principal's recorded ruling, not a gate finding.

=====================================================================
2. CONTROLS — and Q2
=====================================================================

Every control names an exact code with PASS|FAIL|NOT_RUN: `C0_REACHABILITY`, `C1_DENOMINATOR_PRINTED`, `C2_INPUT_LEDGER`, `C3_NO_SUBSTITUTION`, `C4_PACKET_REDACTED`, `C4_SEAT_ISOLATION`, `C5_HARNESS_PINNED`, `C5B_NO_CROSS_LANE`, `C6_AUDIT_SAMPLE`. Unreached controls are `NOT_RUN` ("Controls in an unreached limb are `NOT_RUN`, never passes."). Assertion-passing is closed where it matters: C1 PASS requires exit 0 of the printed `census` run with declared-vs-recomputed counts; C2/C3 PASS only on exit 0 of the printed `validate` runs — "a token asserted without the printed run is FAIL"; C5 requires live full stdout — "a transcribed value in place of live output is FAIL"; C6 PASS requires the printed `C6_AUDIT.json` artefact — "a token without the artefact is FAIL"; C4_PACKET_REDACTED FAILS any dispatch built by other bytes. The two self-reported controls (C4_SEAT_ISOLATION, C5B) can be passed by a seat that simply omits a path from its printed list — and the document says exactly that, twice: "makes no claim that the list is complete" and "a self-reported control with a structural aid, and is labelled so". Stated limit, not a hidden hole.

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?
YES at the value layer, and the human layer is dual-checked with its floor stated. Attack 1 — wrong-but-plausible origin: the paper's line is "We adopt H₀ = 70 km s⁻¹ Mpc⁻¹ following the distance-ladder determination"; I file origin `DERIVED` with `ORIG_EQUATION` and quote that very line. The machine checks pass (the quotation IS a substring of the cited line; `validate` checks matching, not appropriateness). What catches it: the second seat's independent classification files `IMPORTED` (the tie-break — "a sentence that names an external source for the value is a citation whatever else it says"), the merge carries `origin_alt`, the input is filed `ORIGIN_DISPUTED` and counted toward the 10% stop; and the C6 auditor re-classifies every origin from the sources, MISMATCH → `CENSUS_AUDIT_FAILED`. The machine alone never catches it — and the master states exactly that floor: "caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." Attack 2 — laundering through derivation: a `DERIVED` record must list `derived_from`; `validate` fails dangling ids, cycles, and parentless `DERIVED` records, and the lane script recomputes roots from the whole chain while REJECTING any ledger that arrives with `root_origins`/`rests_on` pre-set — so a one-step derivation cannot launder a chosen root. Attack 3 — substitution: `PRINTED` values machine-match the cited source line; `STANDARD` values are string-compared against the literally printed closed list; `ABSENT`/`BLOCKED` records carrying values fail the script. Checkable, with the two human floors (reason-code appropriateness; identical misclassification) stated.

FINDING C1 (COSMETIC): the `ORIG_SILENT` floor is the same family and is not stated. Verbatim: "an `ORIG_SILENT` record prints the search the seat ran." A weak search (one query, no variants) downgrades `CHOSEN` to `UNDECLARED`; the printed `origin_search {query, files, matches}` makes adequacy inspectable by the second seat and the auditor, but the document does not say that is the mechanism. Exact replacement (append): "**Adequacy of an `ORIG_SILENT` search is judged by the second seat and by the C6 auditor from the printed `origin_search`, never by the machine.**" COSMETIC: no class filed, no control's passability, no reachability, no conclusion, no tested value changes; the search is already printed.

=====================================================================
3. CIRCULARITY — can the lane's expectation reach the evidence?
=====================================================================

Q3 is answered in STEP 1 above: from the packet the seats actually receive, no hypothesis, comparison model, preferred outcome, or pattern is reachable (LEAK=NONE). Channels tested against the master:
- INCLUSION: rule-based ("prints a numeral the paper asserts as a result of its own", excluded kinds enumerated), assigned independently by two seats, zero tolerance, stop class on surviving disagreement. A contrary claim cannot be quietly dropped: every exclusion must carry a `kind` from the fixed five-token list with file/line/numeral, and the C6 auditor gives "a completeness disposition for every candidate and every exclusion" against every pinned source — a mislabelled exclusion is incompleteness → `CENSUS_AUDIT_FAILED`. Quiet exclusion requires both blind seats AND the auditor to mislabel identically — the stated floor.
- ORIGIN CLASSIFICATION: cannot touch the verdict — the arithmetic consumes status, never origin; origin feeds only `rests_on` and the dispute stops, and the verdict is insensitive to it.
- INTERPRETATION STEP: reads the sealed tallies after the two-receipt seal (§7), from a protocol no seat may open, committed before limb A. The seats never see the mapping.
- THE CLOSED LIST ITSELF: lane-authored, but it only fixes canonical strings/metadata; the reproduction test compares reproduced vs the PAPER's printed numeral, so tuning the list cannot flip `REPRO_EXACT`/`REPRO_FAILED` — it touches only the provenance taxonomy, which is dual-classified and audited.
- The surviving residues are the packet-visible process traces (finding C4) and the stated human floors. No channel from expectation to evidence found.

FINDING C4 (COSMETIC): the packet's §4 open note tells the seat prior review occurred. Verbatim (packet line 122, master §4): "the gate found §4 not exhaustive over that state". The builder's assertion is against an enumerated forbidden list, and bare "gate" is evidently not on it, so this is not a builder violation; the master's forbidden-list claim is stated at its true strength ("blocks the enumerated strings and does not establish that every consequence-bearing word is gone"). The clause leaks that a gate exists and found something — process residue with no outcome direction — and points the seat at "§10.12", a section the seat does not have. Exact replacement: "§4 is not exhaustive over that state". COSMETIC: no class, control, reachability, conclusion, or tested value changes; it only reduces process-history visibility.

=====================================================================
4. THE BLIND
=====================================================================

Machine-asserted as claimed, and verified empirically within my two-file mandate:
- The builder `r3c2_build_seat_packet.py` exists and its sha256 (`f2f6c9ab…7d67`) matches the pin stated in C4; the packet it produced is exactly the file whose digest the pin file records (`37786e5e…`), and the packet header's embedded master sha256 equals ACCESS_SHA — so the packet was built from precisely the master bytes I gated. "A dispatch built by any other bytes files `C4_PACKET_REDACTED=FAIL`" is therefore checkable, and is checked.
- Structural claims verified against the packet itself: §0, §7, §8, §10 are absent (the packet runs §1–§6, §9, §11, exactly "drops §0, §7, §8 and §10 whole"); every `SEAT-REDACT` span I compared against the master is stripped (the "one pass, two tallies" block, the `rests_on` sentences, the severity order, the custodian's name, the R3D/C0 history, the leak-floor ruling — all absent from the packet).
- My own forbidden-string sweep of the packet (pattern/topic names, the comparison model, engine names, custody-chain names, the ten consequence words, "rests_on", "root_origins", "severity", "interpretation", "hypothesis", "comparison") found exactly one residual hit: "the gate found" in the §4 open note — finding C4, cosmetic. No consequence-bearing word survives.
- Does the C4 mechanism prevent what it claims? It claims the dispatch record proves the seat was not furnished the pattern record in its working directory — supported: the dispatch copy's contents and digests are listed before launch, the copy is outside the lane, and the redaction is machine-asserted. It claims NO more: "This is procedural, not enforced by the filesystem", "`C4_SEAT_ISOLATION` is a self-reported control with a structural aid, and is labelled so", and "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The forbidden list's own limit is stated: it "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible". The accepted residual floor (a seat can tell provenance is measured with care) is disclosed in the master's redacted span with the principal's ruling cited. Honest at every claim boundary I tested.

=====================================================================
5. THE SEAL (§7)
=====================================================================

The receipted relay binds what it claims, before the step it must precede:
- Receipt (P) binds the interpretation protocol's hash AND commit id, custodied outside the lane by Blanc, and "limb A does not begin without receipt P" — the protocol's bytes are fixed before any evidence exists, closing the write-to-suit-the-tally channel at the right end.
- Receipt (T) binds the tally's hash AND commit id after the tally is committed and "the interpretation protocol is not opened without both receipts" — the tally's bytes are fixed before the mapping is read.
- After opening, "Blanc independently re-hashes the tally and the protocol and verifies all four values against the two receipts"; any missing receipt or mismatch files `CENSUS_AUDIT_FAILED` — and §4's definition of that class names the seal case, so the stop class and the seal agree.
- The audit seed is supplied by the custodian only "after the tally digests are receipted", so the sampling frame is frozen before the sample exists; a seed never supplied files `CENSUS_AUDIT_FAILED` with the missing seed named.
- Limits stated, not implied: a local commit "is tamper evidence, not an external custodian"; "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind".

FINDING R3 (minor): receipt (T) names a singular "tally" the document elsewhere makes two, and the merged ledger's place in the sealed record is unstated. Verbatim: "**(T) After the tally is committed and before the protocol is opened, Tori sends the tally hash and tally commit id; Blanc receipts them as T**" — against §4's "Report the full tally with its denominator, and the `rests_on` tally beside it — two tallies from one pass" and C6's "MATCH or MISMATCH against the sealed record", where the MATCH covers re-classified origins that live in the merged ledger, not in the merged candidate file that §2 step 5 calls the sealed tally. As written, the commit id arguably binds the whole commit (amendment changes the id, and verification then fails), but no operative sentence says the tally commit contains the candidate file, the exclusion ledger, and the merged input ledger with its `compute` output; the interpretation reads `rests_on`, so what the seal binds should be named exactly.
Exact replacement: "After the tally is committed and before the protocol is opened" → "After the tally — one commit containing the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output — is committed and before the protocol is opened, Tori sends the tally hash and tally commit id; **the receipt names every file in that commit**".

=====================================================================
6. FAIRNESS
=====================================================================

Every negative outcome is worded "unreproduced from the stated inputs," never "error" — the rule is stated at `REPRO_FAILED` and the operative text contains no contrary use (the word appears operatively only inside that wording rule itself). Class names vs definitions: `REPRO_FAILED` asserts only non-reproduction from stated inputs, which is its definition; `REPRO_BLOCKED`/`REPRO_INPUT_ABSENT` name the paper's underspecification, not a paper mistake; `CENSUS_PARTIAL` is labelled INCONCLUSIVE, not failure; `CENSUS_AUDIT_FAILED` names the audit, not the corpus.

FINDING C2 (COSMETIC): `REPRO_EXACT` asserts more than its definition. Verbatim: "**`REPRO_EXACT`** — the paper's number follows, within its own stated precision". The test is within-stated-precision (rounding, or |Δ| ≤ stated uncertainty), not exactness; the definition is precise and "Report both numbers" prints the evidence beside every claim, so the artefact does not overclaim — the name does, mildly. Exact replacement: rename to `REPRO_WITHIN_STATED_PRECISION` at every occurrence. COSMETIC by the gate's own test: the definition governs the test, both numbers are reported, and renaming changes no class filed, no control's passability, no reachability, no conclusion, and no tested value. (A post-preregistration class rename is a redefinition; routing it is the principal's call, and the document's header already records a pending class ruling.)

=====================================================================
7. STALL / EXECUTABILITY — and Q4
=====================================================================

Stall: every loop is bounded — the 120-second monotonic wrapper (`SYMBOLIC_TIMEOUT`, exit 124) caps every symbolic operation; one repeat, meaningful only for `REPRO_NOT_EVALUABLE`; two reconciliation attempts on enumeration; one on outcomes; one on input lists; controls attempted twice; every dispute lands in a named stop class; `MACHINERY_UNAVAILABLE` names the non-timeout limb. No unbounded wait exists as written.

Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Everything I checked, and how:
1. `r3c2_ledger_tools.py` — EXISTS; sha256 `8e286817…f7ac` matches the digest stated in §3 of both documents, byte for byte.
2. `r3c2_timeout.py` — EXISTS; `fbb9bef7…f331` matches §9/C4.
3. `r3c2_lane_tools.py` — EXISTS; `6e00b13b…3462` matches the C3 lane block and §9.
4. `r3c2_build_seat_packet.py` — EXISTS; `f2f6c9ab…7d67` matches C4's pin ("a dispatch built by any other bytes files C4_PACKET_REDACTED=FAIL" is thus enforceable).
5. `R3C2_CORPUS_MANIFEST.md` — EXISTS; `300d4da1…9b24` matches §2 of both documents.
6. `r3c2_seat_packet/R3C2_SEAT_PACKET.md` — EXISTS; digest matches the pin file; tracked and clean in git, so "a committed path, not scratch" is TRUE; its header embeds the master's own sha256, which equals ACCESS_SHA — packet and master are mutually consistent as delivered.
7. `R3C2_SEAT_PACKET.sha256` — EXISTS; its contents pin the packet, `SEAT_BRIEF.md`, the wrapper, the ledger tool and the builder, and all five digests verify against the bytes on disk.
8. `r3c2_seat_packet/SEAT_BRIEF.md` — EXISTS and pinned (`1f3ef407…e3ab`).
9. `R3C2_INTERPRETATION_PROTOCOL_20260904.md` — EXISTS, tracked and clean; §7's "lives in" is satisfied now, and receipt P is still required before limb A.
10. `R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md` — EXISTS (named in C4).
11. `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh` — EXISTS at the absolute path §9 names (existence only; not opened, not executed).
12. `/usr/bin/python3` — EXISTS; `--version` runs: Python 3.9.6; a `sympy` package directory is present in the Python 3.9 user site-packages. Executing the import was not permitted in my environment; C5 runs all three harness commands LIVE at dispatch and any miss is a printed FAIL, never a silent pass.
13. Literal commands: `shasum -a 256` ran (it produced ACCESS_SHA/PACKET_SHA); `census`, `validate`, `merge`, `compute` subcommand invocations as printed in C1/C2/C3 — the pinned scripts exist at the stated digests; I did not execute them (my mandate opens exactly two files, and executing a script opens it); any interface mismatch surfaces at run time as a non-zero exit on a printed control run, which is FAIL by each control's own predicate.
14. Fields the scripts are said to compute — declared-vs-recomputed counts (`census`), schema/machine-match/closed-list/`derived_from` checks (`validate`), `origin_alt`/`origin_evidence_alt` (`merge`), `root_origins`/`rests_on` (`compute`) — each names its producing script, the scripts exist at the pinned digests, and every one of those claims is exercised by a printed-artefact control or the C6 audit, so a described-but-uncomputed field cannot pass silently.
15. Paths seats are told to use: `.` as the sole `sources_dir`; the dispatch copy's contents (packet, brief, ledger tool, wrapper, manifest sources) are listed by the lane owner before launch under C4; the lane-side originals all exist per items 1–8. The manifest's per-source availability is exercised by C1/C2/C3/C6 at run time with printed failures.
16. Not executed, deliberately: the builder (it would rewrite the packet — a side effect), the dispatch script, and the ledger/lane tools.
Not found as delivered: nothing. Every artefact the document says exists, exists, with the stated digest.

FINDING C3 (COSMETIC): pin-file location wording. Verbatim: "pinned beside the packet in `R3C2_SEAT_PACKET.sha256`" (§3, both documents). The pin file sits in the working-directory root, beside the packet's DIRECTORY, not beside `r3c2_seat_packet/R3C2_SEAT_PACKET.md`. Exact replacement: "pinned at `R3C2_SEAT_PACKET.sha256` in the seat working directory". COSMETIC: the pin exists, verifies, and the dispatch record lists it; wording only.

FINDING C5 (COSMETIC, observation): a zero-denominator corpus files `CENSUS_COMPLETE` vacuously ("every included claim carries exactly one outcome from the arithmetic group"). The C1 artefact prints the declared and recomputed counts, so a zero denominator is visible rather than hidden; the corpus is pinned and non-trivial. Optional clause: "**A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named.**" COSMETIC: changes nothing for the pinned corpus.

=====================================================================
VERDICT BASIS
=====================================================================

PREREG_SOUND_WITH_REPAIRS. Every declared per-claim outcome and study-level class is reachable under option (c) as written, and the reproduction question is decidable per claim (Q1 YES, with R1/R2 naming the two boundary rules that should be written down rather than left to the disclosed split machinery). Controls are artefact-bound with stated self-report limits; provenance is checkable with its human floors stated (Q2 YES). No channel from the lane's expectation to the evidence survives inclusion, exclusion, origin classification, or the interpretation step (Q3/LEAK=NONE; CONSEQUENCE_VISIBLE=NO). The blind is machine-asserted and verified against the delivered bytes, with its limits stated honestly. The seal binds the protocol before limb A and the tally before the protocol is opened, with an outside custodian and stated limits (R3 names what the receipt should enumerate). Fairness holds; the one name that outruns its definition is cosmetic (C2). Everything the document describes exists as delivered, digests matching (Q4). Not PREREG_SOUND because of R1, R2, R3 — and because the document itself declares one reachable study-level state unclassed and refuses to freeze until the principal records that decision (D0); no UNSOUND-level defect found: no unreachable outcome, no undecidable question, no assertion-passable machine control, no hidden circularity channel, no missing artefact.

R3C2_V18_GATE_COMPLETE
