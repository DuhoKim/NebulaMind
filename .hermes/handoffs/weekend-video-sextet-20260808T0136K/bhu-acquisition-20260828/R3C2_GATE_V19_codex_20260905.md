ACCESS_SHA=a0cf4d5cae4a2b744fead10a7ee09d72479cf6e8a8ddc7ef622b81167f6ea9af
PACKET_SHA=1db4b5a3a44e3dc5f359ddea8af97240011cab5eb49d770c8869ea0ac6d992fe
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=The packet says origin is recorded provenance that is independently classified and later checked by re-classification; it does not reveal any downstream comparison or interpretation use. Sentence: “the auditor re-classifies the record from the pinned sources.”

Packet-first answers (fixed before the master was read)

Q5. No. No packet sentence says any outcome is more consequential to the lane or to anyone. The packet specifies precedence, stop effects, and whether a tally is filed, but gives no empirical stake, preferred result, or hypothesis mapping.

Q6. The packet permits this limited statement: `origin` is a separately recorded provenance attribute, independently classified by both seats and checked when “the auditor re-classifies the record from the pinned sources.” Arithmetic consumes by `status`, not by `origin`. The packet does not say that `origin` feeds a second tally or an interpretation.

Q3. I infer no hypothesis, comparison model, preferred outcome, or hoped-for pattern. The packet asks whether each printed number follows from the stated recipe and inputs, defines neutral outcome classes, and specifies provenance checks. None of its sentences maps an outcome or origin to a lane conclusion. The Planck-specific closed list identifies subject matter, not a hoped-for result.

## 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive under the stated precedence. Hard overlap: a claim gives a recipe, cites an unpinned source for input `x`, omits uncited input `y`, and also requires unavailable machinery. It satisfies BLOCKED, ABSENT, and NOT_EVALUABLE conditions, but the explicit order files exactly `REPRO_BLOCKED`. A claim with no stated procedure files `REPRO_NO_DERIVATION_STATED` before any input defect. Once all required consumable inputs exist, the arithmetic group partitions the claim by the stated precision test.

Hard option-(c) trace: suppose a paper prints “we choose x=2,” states `y=3` as a fitted parameter, imports `z=5` from the cited line of another enumerable pinned text, states `q=x+y+z`, and prints `q=10`. Records are `x`: status `PRINTED`, origin `CHOSEN`; `y`: status `PRINTED`, origin `FITTED`; `z`: status `PRINTED`, origin `IMPORTED`. All three are consumed. The reproduced value 10 files exactly `REPRO_EXACT`; the lane script traces the leaves and computes the unique `rests_on` result according to its fixed severity rule (`USES_IMPORTED`, because IMPORTED outranks FITTED and CHOSEN). If the printed result is 11, exactly `REPRO_FAILED` files with the same `rests_on`. A paper-stated value is not removed merely because it was chosen, fitted, or imported, so reproduction remains decidable.

`INCONCLUSIVE` is genuinely reachable: one included claim with a printed result but no stated equation or executable procedure files `REPRO_NO_DERIVATION_STATED`; therefore the study files `CENSUS_PARTIAL`, labelled INCONCLUSIVE. A zero-claim tally fits `CENSUS_COMPLETE` vacuously and no other tally class absent a control/audit stop. A nonempty all-arithmetic tally fits `CENSUS_COMPLETE`; a tally containing any non-arithmetic outcome fits `CENSUS_PARTIAL` and not COMPLETE.

Q1. YES for the per-claim definitions under option (c): every declared per-claim outcome is reachable and every included claim is decidable through the precedence rule. NO for the preregistration as a whole, because the study-level classes are explicitly non-exhaustive.

Defect 1.1 — SUBSTANTIVE. Quote: “Open (§10.12): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — §4 is not exhaustive over that state”. The design commands exactly one study-level outcome but supplies none for a reachable surviving outcome split. That makes the gate unfreezable and the study result undecidable in that state.

Exact replacement: replace the quoted open note with:

“6. **`CENSUS_OUTCOME_DISPUTED`** — the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5. The census does not proceed; every disputed claim is listed with both outcomes and both supporting artefacts.”

Renumber the current items 6 and 7 as 7 and 8, and replace the precedence sentence with:

“`R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`.”

## 2. Controls

C0 requires an exhibition table, independent authorship and verification, and two explicit tokens; it cannot pass by a bare lane-owner assertion. C1, C2, C3, C4, C5, C5b, and C6 each state exact PASS/FAIL/NOT_RUN codes and predicates. Unreached controls are explicitly `NOT_RUN`. C4/C5b remain self-report rather than enforcement, but the text accurately limits what their PASS proves.

Q2. YES, with a stated human-audit floor. Adversarial record: the source line says “We adopt H0 = 67.4 from Planck (2018),” but the ledger records origin `CHOSEN`, reason `ORIG_CHOICE_STATED`, and a matching quotation. This is plausible and may pass the validator's substring and reason-to-origin checks. The second seat independently classifies it; C3's precedence makes `ORIG_CITATION` win, so disagreement exposes the wrong record. C6 independently re-classifies every audited input from pinned sources and records MATCH/MISMATCH, catching it even if the sealed record survived. If both independent readers make the same semantic error, the machine cannot catch it; the master states this limit honestly.

Defect 2.1 — SUBSTANTIVE. Quote: “the `census` subcommand run again after limb B with the word `final` verifies”. The initial command is literal, but the final invocation does not specify where `final` goes. A control whose PASS depends on that run must give one exact command.

Exact replacement:

“After limb B run `/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json> final`, with all placeholders resolved; this verifies that every included candidate carries exactly one §3 outcome, none is `PENDING`, and arithmetic-group outcomes carry both values.”

## 3. Circularity

The packet exposes neither the hypothesis nor the comparison model nor a mapping from outcomes/provenance to interpretation. Inclusion uses a fixed numeral-based rule; both inclusion and exclusion are fully ledgered and independently assigned, with unresolved disagreements stopping the study. Origin classifications require quotations, independent classification, and audit re-classification. A contrary quantitative claim cannot be quietly excluded: it must appear as an included candidate or as a cited exclusion, and C6 audits completeness against every pinned source. The later interpretation protocol is sealed before limb A. I find no operative path by which the lane expectation can select the evidence.

## 4. The blind

The packet actually inspected contains none of the master-described forbidden strings (case-insensitive search covered `ΛCDM`, pattern, comparison, gate/engine/person names, consequence terms, and `rests_on`). The builder is described as mechanically stripping marked spans and failing before writing if a forbidden string survives. This asserts the packet's textual redaction, not filesystem non-access. C4 does not prevent a seat from opening an absolute lane path; the master expressly calls the control procedural/self-reported and says PASS certifies only the printed list and dispatch copy. The claimed limit is honest.

## 5. The seal

Receipt P binds the interpretation protocol hash and commit before limb A may begin. Receipt T binds the tally hash and commit after the named tally files and computed ledger are committed but before the protocol is opened; it names every file. Independent re-hashing verifies both hashes and both commit identifiers, and missing/mismatched receipts file `CENSUS_AUDIT_FAILED`. This binds the record in the required order. The text appropriately disclaims proof about prior mental exposure.

## 6. Fairness

The negative arithmetic outcome is explicitly reported as “unreproduced from the stated inputs,” never “error.” BLOCKED, ABSENT, NOT EVALUABLE, and NO DERIVATION describe limitations rather than paper error.

Defect 6.1 — SUBSTANTIVE (the filed class name asserts more than its definition). Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision”. Equality after rounding or agreement within a stated uncertainty is not exact equality.

Exact replacement: replace every operative and schema occurrence of `REPRO_EXACT` with `REPRO_WITHIN_STATED_PRECISION`; define the arithmetic group as exactly `REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`. No predicate changes.

## 7. Stall / executability

Q4. NO: not everything is executable as described without repair.

Checks performed without opening any file other than the packet and master: existence checks found `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_lane_tools.py`, `r3c2_timeout.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, `r3c2_seat_packet/R3C2_SEAT_PACKET.md`, `r3c2_seat_packet/SEAT_BRIEF.md`, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, and `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`. I checked the seat packet and master hashes. I ran all three literal C5 commands: Python 3.9.6, SymPy 1.14.0, and `/usr/bin/python3` sha256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`; all exited zero. Runtime outputs such as candidate/exclusion/input ledgers, merged/compute outputs, receipts, and `C6_AUDIT.json` are correctly described as future artefacts, not pre-existing deliverables. Placeholder ledger commands cannot properly be run before those runtime inputs exist. I did not execute scripts whose execution would open additional files, honoring the instruction to open only the two named documents.

Defect 7.1 — SUBSTANTIVE. Quote: “containing the **seat packet** — not this document — the seat brief `r3c2_seat_packet/SEAT_BRIEF.md` ..., the script `r3c2_ledger_tools.py`, the wrapper `r3c2_timeout.py`, and the pinned sources of `R3C2_CORPUS_MANIFEST.md`”. This promised dispatch inventory omits both `R3C2_SEAT_PACKET.sha256`, which the packet tells the seat pins its tool, and the manifest itself, which the seat must use to identify enumerable texts. Both exist in the lane, but the operative dispatch list does not require their delivery; a conforming dispatch can therefore leave the seat unable to verify the tool pin or enumerate the corpus as instructed.

Exact replacement:

“containing the **seat packet** — not this document — the seat brief `r3c2_seat_packet/SEAT_BRIEF.md`, `R3C2_SEAT_PACKET.sha256`, `R3C2_CORPUS_MANIFEST.md`, the script `r3c2_ledger_tools.py`, the wrapper `r3c2_timeout.py`, and every pinned source listed in `R3C2_CORPUS_MANIFEST.md`”.

Conclusion: `PREREG_UNSOUND`. Defect 1.1 alone leaves a reachable execution state with no study-level class and is expressly admitted by the operative text. Defects 2.1, 6.1, and 7.1 also affect control passability, the truth of a filed class name, and delivered executability respectively. None is cosmetic under the requested test.

R3C2_V19_GATE_COMPLETE
