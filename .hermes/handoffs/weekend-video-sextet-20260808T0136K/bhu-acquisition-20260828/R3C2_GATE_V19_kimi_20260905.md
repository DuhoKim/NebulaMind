ACCESS_SHA=a0cf4d5cae4a2b744fead10a7ee09d72479cf6e8a8ddc7ef622b81167f6ea9af
PACKET_SHA=1db4b5a3a44e3dc5f359ddea8af97240011cab5eb49d770c8869ea0ac6d992fe
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader integrity check, not a verdict input: origin is classified independently by both seats, disagreements on inputs affecting more than 10% of included claims stop the census (packet §4, "CENSUS_ORIGIN_DISPUTED — ... The census does not proceed"), and the C6 auditor re-classifies every input's origin from the pinned sources; what the recorded origin is compared or combined with after that is not stated anywhere in the packet

STEP 1 — PACKET-FIRST ANSWERS (fixed in this order — Q5, Q6, Q3 — after reading the packet alone and before the master was opened; nothing below this line was allowed to revise them)

Q5. No. No sentence in the packet makes any outcome more consequential than any other, to the lane or to anyone. The only sentence that touches consequence at all is packet §11: "No tier, warrant token, standing or stamp moves." — and it denies consequence uniformly to every outcome rather than ranking them. The stop sentences ("The census does not proceed", "No tally is filed") attach procedure to dispute/audit classes, not stakes; "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" is a filing order, not a weight. I tried to reconstruct a stake from the class inventory and could not: the arithmetic group (REPRO_EXACT / REPRO_FAILED) is described with identical evidentiary demands ("Report both numbers" on both), and the wording rule "unreproduced from the stated inputs, not error" constrains how a negative is written, not whether one is found.

Q6. Partly statable. The packet tells me three things the study does with a recorded origin, and withholds the fourth. (i) It is recorded separately from the verdict — packet §1: "The reproduction verdict and the provenance fields are recorded separately." (ii) It is cross-checked between readers and can stop the study — packet §4: "CENSUS_ORIGIN_DISPUTED — the two seats' independent origin classifications disagree on inputs affecting more than 10% of included claims. The census does not proceed; every disputed input is listed with both seats' classification and both quotations." (iii) It is independently re-derived at audit — packet §5, C6: the auditor re-derives "without sight of earlier work and re-classifying every input's origin from the pinned sources". What I cannot state from the packet: what the recorded provenance is compared against or combined with downstream — no packet sentence says origin feeds any tally, grading, or interpretation. The arithmetic consumes records by status (PRINTED/STANDARD), never by origin (packet §3: "Arithmetic consumes records according to status PRINTED or STANDARD"). So: origin is for a two-seat dispute stop and an audit re-classification; anything further is not in the packet.

Q3. Adversarial reconstruction, every inference with its leaking sentence:
(a) The corpus is cosmology. Leak: packet §5, C3 — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." A closed STANDARD list of G, c, ħ, k_B plus exactly the Planck 2018 baseline (H₀, Ω_m, Ω_Λ, Ω_b h², Ω_c h², n_s, σ₈, τ, ln(10¹⁰ A_s), age) tells me the enumerable texts use these values. This is content the seats need to run the membership test; it carries no preferred verdict.
(b) The lane expects provenance to matter. The taxonomy exists at all — seven origins (CHOSEN, DERIVED, FITTED, IMPORTED, MEASURED, STANDARD, UNDECLARED), machine-matched quotations, two independent classifiers, disputes carried rather than reconciled, a 10% stop. A lane that did not care whether numbers rest on chosen or fitted inputs would not build this. But "the lane measures provenance carefully" is not a hypothesis about what the census will show; the packet never says which measurement would be welcome.
(c) The lane anticipates the chosen/fitted/imported case and has pre-cleared arithmetic reachability. The STANDARD table is pre-printed so common constants cannot stall a claim; symmetric machinery exists for the opposite (REPRO_BLOCKED, REPRO_INPUT_ABSENT). Preparation is symmetric; no expectation of CENSUS_COMPLETE over CENSUS_PARTIAL is inferable — if anything, "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" hardens against the clean outcome.
(d) A faint process trace: "an open decision recorded in §10.12" (packet §2 step 5 and §4 note) implies a §10 with at least twelve subsections, i.e. a long revision history exists. It reveals no content of that history and no hypothesis. (Filed as defect D6 for the dangling reference, not as a leak.)
What I could not reconstruct: any hypothesis, any comparison model, any preferred outcome, any "pattern". No packet sentence states or implies what the lane hopes the tally will be, in either direction. The §1 question is symmetric; the classes are symmetric; the consequence words are absent. LEAK=NONE.

STEP 2 — THE MASTER, JUDGED AS WRITTEN (operative sections 1, 2, 3, 4, 5, 6, 7, 9, 11; §3's core definition judged under option (c) as settled; §10 treated as history that binds nothing)

1. OUTCOME CLASSES
Per-claim: six outcomes with a total precedence — "Exactly one outcome is filed per claim. Where more than one terminal condition holds, file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group." I could not construct a real claim fitting two per-claim outcomes after precedence, nor one fitting none: a claim with an untraceable input and no stated equation files REPRO_NO_DERIVATION_STATED (precedence); a claim with one BLOCKED and one ABSENT input files REPRO_BLOCKED (precedence); a timeout on arithmetic that would have failed files REPRO_NOT_EVALUABLE (precedence); a figure-only value is not in an enumerable text, so it is ABSENT or BLOCKED by the named-source rule, never unclassified.
Study-level: seven classes with a total precedence — "Exactly one study-level outcome is filed. Where more than one condition holds, file the first in this order: R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT, CENSUS_DENOMINATOR_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL, CENSUS_COMPLETE." A control failing everywhere plus a split on another files R3C2_NO_CLASS; a blocked claim plus an audit mismatch files CENSUS_AUDIT_FAILED. Two real exceptions, both found in the operative text:
(i) A tally in which the two seats' per-claim outcomes on an agreed included claim still differ after the one §2 step-5 reconciliation fits NO study-level class. The document says so itself: §4's note — "Open (§10.12): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — §4 is not exhaustive over that state; a class is not added or redefined by this document's author, so the gap is recorded here as open rather than written in." — and §2 step 5 gates the freeze on it: "this document is not frozen until that decision is recorded." Reachable (two mechanically defensible arithmetics can survive a reconciliation whose objects are only "the printed numeral and the stated-precision rule of §3"), unclassified, declared, and freeze-gated. Defect D1.
(ii) A zero-denominator census: with no included claims, CENSUS_COMPLETE's condition "every included claim carries exactly one outcome from the arithmetic group of §3" is vacuously true, and C6's sample formula degrades consistently (R = 0, empty sample). Exactly one class still files, so exclusivity survives; the issue is a degenerate class filing, declared pending as a sub-option of the same ruling. Noted under D1's ruling dependency.
INCONCLUSIVE is genuinely reachable: one included claim whose equation needs an input the paper neither prints nor traces (e.g. an age printed from Friedmann with H₀ nowhere stated or cited) files REPRO_INPUT_ABSENT, and CENSUS_PARTIAL "takes precedence over CENSUS_COMPLETE".

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE? YES, with the two declared exceptions above. Hardest cases, each traced to exactly one outcome and one rests_on:
(a) Printed-but-chosen input: the paper prints "we adopt β = 1/929.25" and uses it. Status PRINTED (given in the paper); origin CHOSEN via ORIG_CHOICE_STATED with the quotation machine-matched to the cited line. §2 step 4 consumes it ("every ledger record with status PRINTED or STANDARD"). The claim files exactly one of REPRO_EXACT / REPRO_FAILED by the stated-precision rule; the lane-side script computes rests_on from the roots (USES_CHOSEN absent a more severe root). One outcome, one rests_on. Decidable — this is the case that broke the derivation-only wording, and option (c) closes it by consuming printed values whatever their origin.
(b) Fitted parameter the paper states: "our fit yields σ₈ = 0.811" used as an input. 0.811 is not verbatim on the closed list (whose sigma8 row is 0.8111), so §2's rule files PRINTED, origin FITTED via ORIG_FIT_STATED; consumed; one arithmetic outcome. Had the paper printed 0.8111 verbatim, the same sentence files STANDARD — "the two routes are outcome-identical, and this rule keeps both seats on the same one." Decidable either way; the stated reason for the tie rule (seat alignment) is real.
(c) Imported value, all three limbs: printed with a citation ("We adopt H₀ = 67.4 from Planck (2018)") → PRINTED/IMPORTED (the precedence "a sentence that names an external source for the value is a citation whatever else it says" beats CHOSEN); not printed but traced to a named enumerable pinned text with a machine-match at the cited line → PRINTED from that source, origin IMPORTED, consumed; named source not enumerable, or no match at the cited line → status BLOCKED, never consumed, claim files REPRO_BLOCKED; no named source at all → ABSENT → REPRO_INPUT_ABSENT. Each limb decidable, one outcome.
(d) Named-but-unspecified procedure: the paper prints 13.8 Gyr and says only that it "follows from the standard cosmological computation". The V19 clause files REPRO_NO_DERIVATION_STATED and names the passage: "A procedure named but not specified — a sentence that says where the number came from without stating operations a seat could attempt — states no computational procedure that could produce it; file this class and name the passage." Decidable; substituting a referee-supplied "standard" equation is exactly what C3 forbids.
(e) Boundary the seats will hit, decided not defective: an equation imported by citation ("using equation (4) of [12]") where [12] is a pinned enumerable text. The IMPORTED rule covers values, not procedures; the named-but-unspecified clause files REPRO_NO_DERIVATION_STATED. The asymmetry is grounded in machine-matchability — a value matches exactly, a procedure's symbol mapping does not — so it is a defensible decided boundary, not a gap.
(f) Stated uncertainty vs printed precision: "13.797 ± 0.5 Gyr" — an uncertainty is stated, so the test is |reproduced − printed| ≤ 0.5, taken once; the rounding rule applies only "where it states none". Asymmetric case: "the stated uncertainty is the half-width on the side the reproduced value falls." One rule each; no double application constructible.
Every declared per-claim outcome and study-level class (with D1's declared exception) has a concrete exhibiting input; C0 requires the exhibition table from two independent seats before freeze.

2. CONTROLS
Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. None is passable by bare assertion: C1/C2/C3 PASS only on a printed script run's exit 0 ("a token asserted without the printed run is FAIL"); C5 requires full live stdout of three named commands ("a transcribed value in place of live output is FAIL"); C6 PASS only if C6_AUDIT.json "exists, is printed, and carries no MISMATCH and no incompleteness; a token without the artefact is FAIL"; C0's artefact is the exhibition table from two independent seats with the lane owner reduced to a coverage check. C4/C5b are self-reported path lists and say so at their true strength ("makes no claim that the list is complete"). Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes", and §4: "Once a stop class applies, later limbs are unreached and their controls are NOT_RUN." R3C2_NO_CLASS is correctly bounded to C0–C5b with C6/seal failures routed to CENSUS_AUDIT_FAILED.
One specification imprecision (defect D4, minor, backstopped): C1 prints "those four declared counts verbatim from the files" and the script verifies "that each of the four declared counts equals the count recomputed from the rows", but the two files carry five declared counts — the candidates file's four plus the exclusion file's declared_exclusion_count — and the verified list never names the fifth, nor a bijection between excluded candidates and exclusion rows ("every exclusion names one excluded candidate" is one direction only). As written, a fabricated declared_exclusion_count, or an excluded candidate with no exclusion row, passes C1; C6's completeness disposition "for every candidate and every exclusion" catches it at audit. Exact replacement: "…that each of the five declared counts equals the count recomputed from the rows, that every candidate whose disposition is excluded is named by exactly one exclusion row, and that every exclusion row names one excluded candidate…".

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? YES, with a stated floor. Attack, as instructed — a ledger record whose origin is wrong but plausible: the paper prints at line 212 "We adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹ from Planck (2018)"; the seat files {symbol H0, status PRINTED, origin CHOSEN, origin_evidence {ORIG_CHOICE_STATED, source_line 212, verbatim "We adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹"}, value "67.4"}. Every machine check passes: the quotation is a substring of the cited line, the value machine-matches, the schema is clean. The machine does not catch it — and the document does not claim it does. What catches it: (1) the stated reason-code precedence both seats must apply — "Where more than one reason code matches the cited sentence, file the first in this order: ORIG_CITATION, ORIG_FIT_STATED, ORIG_CHOICE_STATED, … — a sentence that names an external source for the value is a citation whatever else it says" — the sentence names Planck (2018), so ORIG_CITATION→IMPORTED is the only rule-conformant filing; (2) the second seat's independent classification applying the same precedence, with disagreement carried as origin_alt and counted toward the >10% CENSUS_ORIGIN_DISPUTED stop; (3) the C6 auditor re-classifying every input's origin from the pinned sources, MISMATCH filing CENSUS_AUDIT_FAILED. The residual floor — every reader misclassifying identically — is stated in the document (in a master-only span) rather than implied away. Second attack: an ORIG_SILENT record parked at UNDECLARED behind a one-query search is caught by the printed origin_search adequacy mechanism — "a search of one query with no variants is not adequate, and the auditor re-classifies the record from the pinned sources." The field is cited, double-read, audit-re-derived, and its enforcement floor is declared: checkable, not asserted.

3. CIRCULARITY
The lane's expectation has no described channel to the evidence, and I could not construct one that survives the written machinery. Inclusion: the §1 rule is pattern-free ("a passage printing a numeral the paper asserts as a result of its own", with excluded kinds enumerated); two seats enumerate independently from the rule alone; any surviving disagreement stops the census (CENSUS_DENOMINATOR_DISPUTED, tolerance zero); every exclusion is ledgered with a kind and audited for completeness against every pinned source. A contrary claim can be quietly excluded only if both seats exclude it identically AND the third-seat auditor's per-candidate completeness disposition misses it — three readers misapplying the same mechanical rule on the same passage, which is the design's stated floor family. Reconciliation is bounded (two attempts, then stop), so it cannot be an endless persuasion channel; what happens inside an inclusion reconciliation is the one place the document does not name the reconciliation's object (contrast §2 step 5, which names "the printed numeral and the stated-precision rule of §3") — minor, noted, not filed, because the audit's completeness pass backstops it. Origin classification: quoted, precedence-ruled, double-read, dispute-carried, audit-re-derived (Q2 above). rests_on and root_origins are computed by the lane-side script after both seats exit, from the merged ledger, and compute "REJECTS (exit 2) a ledger that arrives with root_origins or rests_on already set" — a seat cannot plant the summary field, and a planted origin_alt is outside the seat schema so "validate fails a ledger that carries any other field". The interpretation mapping lives in a protocol no seat may open, committed before limb A under receipt P, and is not opened until the tally is receipted as T — the mapping cannot be tuned to the tally. One genuine residual discretion (defect D3, minor, backstopped): merge semantics are specified for input-id sets (exit 1) and origin disagreements (origin_alt) but NOT for derived_from disagreements — if seat A records X derived_from [Y] and seat B records X derived_from [Z], the merged record's parent list is the lane owner's undocumented choice, and root_origins can shift with it. The channel is narrow (both records are DERIVED; only the parents differ) and backstopped — compute output is receipted in T and the audit re-derives — but the document's own philosophy ("Disagreement about provenance is reported, never reconciled") says it should be carried, not chosen. Exact replacement, appended to the lane-side merge sentence: "where the two derived_from lists differ the merged record carries both parent lists marked PARENTS_DISPUTED, and compute derives root_origins under both, printed as a pair, as for a disputed origin."
Boundary, stated not filed: corpus selection (which texts the manifest pins enumerable vs RAW) is upstream of this document; the document pins the manifest by digest and makes RAW exclusions visible, and the census is over every claim in the pinned corpus with no further selection.

4. THE BLIND
The redaction is machine-asserted as claimed, and I verified the claim empirically on the delivered packet (one of the two files this gate permits). The builder is pinned (sha256 f2f6c9ab…, DIGEST_MATCH); it drops §0/§7/§8/§10 whole, strips every SEAT-REDACT span, and asserts no forbidden-list string survives, failing the build otherwise ("If any survives, the packet is not written and C4_PACKET_REDACTED=FAIL; the study does not proceed on a hand-checked copy"). My independent sweep of the delivered packet for the whole forbidden family — the pattern's name and topic, the comparison model, hypothesis/comparison/gate/history words, the consequence words (expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting, warn), every custody-chain name, every engine name, R3D, leftover redaction markers, and pointers to the dropped sections — returned exactly two hits, both the "§10.12" citation (defect D6). No other forbidden string survives. The packet header embeds the master's digest, which matches ACCESS_SHA exactly: the packet I read was built from the bytes I gated.
Does C4 as described prevent what it claims, and does it state its limit honestly? Yes on both. It claims: the seat was not furnished the pattern record (dispatch copy outside the lane, contents and digests listed before launch), and the packet's content is forbidden-list-clean (machine-asserted). It disclaims, in its own words: the tooling "cannot enforce blindness to a lane-resident file"; the measure "is procedural, not enforced by the filesystem"; the path list "is the detection" and "makes no claim that the list is complete"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The forbidden list's own strength is stated exactly: "the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed." My packet-first results (Q5: no consequence visible; Q6: origin's purpose statable only as an integrity check; Q3: no hypothesis reconstructible) are consistent with that stated strength and with the accepted floor the document declares.

5. THE SEAL (§7)
The receipted relay binds what it claims, before the step it must precede. Order: receipt P (protocol hash + commit id, acknowledged and timestamped by the external custodian) precedes limb A — "limb A does not begin without receipt P" — so the interpretation mapping is fixed before any enumeration exists to tune it. Receipt T (tally hash + commit id, the receipt naming every file in the tally commit: merged candidate file, exclusion ledger, merged input ledger with compute output) precedes the opening of the protocol — "the interpretation protocol is not opened without both receipts" — so the tally is fixed before the interpretation runs. After opening, the custodian independently re-hashes and verifies all four values against both receipts; "Any missing receipt or mismatch files CENSUS_AUDIT_FAILED …, leaves the interpretation NOT_RUN and voids the comparison." §4.3 names the case. The audit seed is supplied only after the tally digests are receipted, so the sampling frame is frozen before the seed exists and the lane cannot shape the tally to the sample; a missing seed does not stall — it files CENSUS_AUDIT_FAILED with C6_AUDIT_SAMPLE=NOT_RUN. The git commit alone is correctly disclaimed ("tamper evidence, not an external custodian"). The limit is stated plainly: "this proves the tally and the protocol both existed, in the stated form, before the comparison was filed. It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." No overclaim found.

6. FAIRNESS
Every negative outcome is worded "unreproduced from the stated inputs," never "error" — the mandate sits in REPRO_FAILED's definition, and the operative text (sections 1–9, 11) contains no use of "error" as a verdict on a claim. One class name asserts more than its definition, and the document declares it pending: "REPRO_EXACT" names a test that is "follows, within its own stated precision" — exactness is not what is tested. The rename (REPRO_WITHIN_STATED_PRECISION) is escalated to the principal and rides with D1's ruling; I charge it as declared-pending, not as a fresh defect. The vacuous zero-denominator CENSUS_COMPLETE (section 1, item (ii)) is the second fairness edge: a class named COMPLETE filing on an empty census asserts more than the situation warrants; declared-pending as a sub-option. No other class name or clause asserts more than its definition: REPRO_BLOCKED names its blockage, CENSUS_PARTIAL is explicitly labelled INCONCLUSIVE, CENSUS_AUDIT_FAILED names the failure, and the rests_on severity order lives only in master-only spans, not in any name a seat sees.

7. STALL / EXECUTABILITY
Stall-safety: every stop state has a class — enumeration disagreement (CENSUS_DENOMINATOR_DISPUTED), input-list disagreement surviving merge exit 1 (same class, named), origin disagreement >10% (CENSUS_ORIGIN_DISPUTED), control split (CENSUS_CONTROL_SPLIT, "do not adopt the passing seat's result"), control failure everywhere (R3C2_NO_CLASS, including pre-dispatch packet/isolation failure), audit failure, seal-receipt failure, and a missing seed (all CENSUS_AUDIT_FAILED). Per-claim stalls: SYMBOLIC_TIMEOUT via the wrapper's 120.0 s monotonic deadline (exit 124, "the reportable outcome") and MACHINERY_UNAVAILABLE, both filing REPRO_NOT_EVALUABLE with the point reached; one repeat permitted, meaningful only for that class; attempts counted in {0,1,2} and validated. NOT_RUN discipline stated for unreached limbs.

Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Everything I checked executed or verified as delivered; checks and method, each named:
- r3c2_ledger_tools.py — EXISTS beside the master; sha256 DIGEST_MATCH against the §3 pin 8e286817… (the seat tool; census and validate subcommands per the packet).
- r3c2_lane_tools.py — EXISTS; DIGEST_MATCH against the C3/§9 pin 6e00b13b… (merge, compute).
- r3c2_timeout.py — EXISTS; DIGEST_MATCH against the §9 pin fbb9bef7…; invoked as "/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>".
- r3c2_build_seat_packet.py — EXISTS; DIGEST_MATCH against the C4 pin f2f6c9ab…; build command "/usr/bin/python3 r3c2_build_seat_packet.py" with expected first line C4_PACKET_REDACTED=PASS. NOT executed: running it would rewrite the pinned packet under gate.
- R3C2_CORPUS_MANIFEST.md — EXISTS; DIGEST_MATCH against the §2 pin 300d4da1… (89 enumerable texts per the document; content not opened — outside the two permitted files).
- R3C2_SEAT_PACKET.sha256 — EXISTS; contains the packet digest (1db4b5a3…), the seat-tool digest (8e286817…), the wrapper digest (fbb9bef7…), and the seat-brief digest (1f3ef407…), each exactly once (presence counted mechanically; content not dumped).
- r3c2_seat_packet/R3C2_SEAT_PACKET.md — EXISTS; my PACKET_SHA matches its pin; its header line 9 quotes the master digest a0cf4d5c… = ACCESS_SHA, so the delivered packet was built from the gated master bytes.
- r3c2_seat_packet/SEAT_BRIEF.md — EXISTS; digest pinned in R3C2_SEAT_PACKET.sha256.
- R3C2_INTERPRETATION_PROTOCOL_20260904.md — EXISTS (§7 says the interpretation step "lives in" it; content not opened; receipt P before limb A is the custody mechanism).
- R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md — EXISTS (cited in C4's floor paragraph).
- /Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh — EXISTS at the absolute path §9 names ("it exists there, not in this directory").
- /usr/bin/python3 — EXISTS and runs (Python 3.9.6); the C5 live-harness commands are well-formed.
- Fields the scripts are said to compute (validate's schema/quotation/STANDARD-membership/derived_from checks; census's disposition/counts/final checks; merge's input-id and origin_alt behavior; compute's root_origins/rests_on with exit-2 rejection) — verified by digest-match to the pins, the document's own binding mechanism; script internals were not read (the gate confines file access to the two named documents), and every control requires a printed run at execution time, so any behavioral drift fails publicly at C1/C2/C3. Stated as the limit of this check, not implied away.
- Literal commands in the operative text (census/validate invocations with resolved placeholders, the wrapper form, the build command, the C5 triple) — all syntactically executable as written; "." as the sole allowed sources_dir is consistent between C2 and C3.
- The packet's "§10.12" citation (packet §2 step 5 and §4 note) is NOT resolvable from the delivered packet, which drops §10 — a seat is told a decision is "recorded in §10.12" of a section it does not have. It asks nothing of the seat and the freeze-gate sentence stands on its own, so this is D6, COSMETIC.

DEFECTS, verbatim, with exact replacements

D1 (substantive; DECLARED OPEN and freeze-gated; the principal's pending ruling). §4 is not exhaustive over a reachable state: a two-seat per-claim outcome split surviving the one reconciliation files no class. Verbatim, §4: "*Open (§10.12): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — §4 is not exhaustive over that state; a class is not added or redefined by this document's author, so the gap is recorded here as open rather than written in.*" and §2 step 5: "the class filed when a disagreement survives that reconciliation is **an open decision recorded in §10.12**; this document is not frozen until that decision is recorded". Defect: a reachable, unclassified study-level state — the exact "real tally fitting none" this gate is instructed to construct. Mitigation as written: the gap is declared in the operative text and the document conditions its own freeze on the ruling, so it cannot misfile a census. Had it been undeclared, this verdict would be PREREG_UNSOUND on §4 exhaustiveness alone. Exact replacement: none from this seat — the document's own rule reserves class addition to the principal; the ruling (add a class such as CENSUS_OUTCOME_DISPUTED, or extend an existing one, with the zero-denominator sub-option) must land before any freeze, which the freeze-gate already enforces.

D2 (minor wording; §4.1/§4.3). CENSUS_COMPLETE's condition — "every included claim carries exactly one outcome from the arithmetic group of §3" — does not require the audit, and CENSUS_AUDIT_FAILED names only an audit that "cannot reproduce a sampled per-claim outcome or ledger, or the receipt verification of the seal fails"; the missing-seed clause shows the intended mapping for one not-run cause, but an audit that never runs for any other cause is covered only by a charitable reading of "cannot reproduce". Exact replacement: in §4.1 append "with C6_AUDIT_SAMPLE=PASS"; in §4.3 append "or the audit does not run to PASS for any cause, the cause named". Changes no reachability and no precedence; closes a wording seam the seed clause already implies.

D3 (minor substantive, backstopped; C3 merge). Verbatim: "`merge` exits 1 if the two `input_id` sets differ — …; where the two `origin` classifications differ the merged record carries `origin_alt` and `origin_evidence_alt`." No rule exists for a derived_from disagreement between seats, leaving the merged parent list to an undocumented lane-side choice that can shift root_origins. Backstopped by receipt T and C6 re-derivation. Exact replacement (append to the merge sentence): "where the two derived_from lists differ the merged record carries both parent lists marked PARENTS_DISPUTED, and compute derives root_origins under both, printed as a pair, as for a disputed origin."

D4 (minor wording, backstopped; C1). Verbatim: "PASS requires exit 0 after the script verifies that every candidate has exactly one disposition, that every exclusion names one excluded candidate, that every included candidate carries a permitted attempts value, and that each of the four declared counts equals the count recomputed from the rows" — five declared counts exist across the two files (the exclusion file is "{declared_exclusion_count, exclusions: [...]}"), and the fifth plus the excluded-candidate→exclusion-row direction are not named. Exact replacement: "…that each of the five declared counts equals the count recomputed from the rows, that every candidate whose disposition is excluded is named by exactly one exclusion row, and that every exclusion row names one excluded candidate…". C6's per-candidate and per-exclusion completeness dispositions backstop it.

D5 (COSMETIC; packet §1). Verbatim: "For every quantitative claim in the corpus, **does the paper's own number follow from the paper's own recipe applied to the inputs it states **The reproduction verdict and the provenance fields are recorded separately.**" — the redaction left an unterminated bold and a missing sentence boundary. Exact replacement: "…applied to the inputs it states.** The reproduction verdict and the provenance fields are recorded separately." Changes no rule, class, control, reachability, or tested value: COSMETIC.

D6 (COSMETIC; packet §2 step 5 and §4 note). Verbatim: "**an open decision recorded in §10.12**" and "*Open (§10.12): the class filed…*". The citation is unresolvable from the delivered packet (§10 is dropped) and faintly signals a deep revision history. Exact replacement, both places: "an open decision recorded by the lane owner" (drop the section pointer). Changes no class filed, no control's passability, no reachability, no conclusion: COSMETIC — and, as the only forbidden-family strings my sweep found in the packet, they are the blind's total residual surface.

DECLARED-PENDING, NOT CHARGED (the document declares them and reserves them to the principal): the REPRO_EXACT rename (section 6); the zero-denominator sub-option (section 1 (ii)); D1's class. The document is self-declared NOT FROZEN and NOT RUN, with the freeze conditioned on the D1 ruling; C0 (two independent seats, both PASS) precedes the freeze as written.

VERDICT: PREREG_SOUND_WITH_REPAIRS. Under option (c) as written, every declared outcome is reachable and the reproduction question is decidable on every hardest case constructed (Q1 YES); controls require printed artefacts; provenance is checkable with a stated floor (Q2 YES); no circularity channel survives the written machinery, with D3 the one narrow residual; the blind is machine-asserted, empirically clean on the delivered packet except two cosmetic citation remnants, and its limits are stated honestly; the seal binds protocol before limb A and tally before interpretation, with its own limit stated; fairness holds with two declared-pending naming edges; everything described exists as delivered, digest-matched (Q4). The repairs are D2–D4 (minor), D5–D6 (cosmetic), plus the principal's three pending items, of which D1 is freeze-gating by the document's own declaration.

R3C2_V19_GATE_COMPLETE
