ACCESS_SHA=e8ba4a7438d61f028e3c46c7308d7078f011e53886e332b946bd7e21f6a1c6c8
PACKET_SHA=ae995d69fb36732a693f243186150cc3ac05d676ebf04215264336c422a63286
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=It records input provenance separately from the reproduction verdict and is independently classified, disputed, and audit-reclassified; “The reproduction verdict and the provenance fields are recorded separately.”

## Packet-first answers

Q5. No. No packet sentence says that any outcome is more consequential than another to the lane or to anyone. The packet specifies procedural precedence and stop effects, but never maps an outcome to a hypothesis, comparison, preference, or consequence.

Q6. From the packet alone, `origin` is for recording and checking input provenance independently of the reproduction verdict. The packet says: “The reproduction verdict and the provenance fields are recorded separately.” It also says: “Every input's `origin` is classified independently by both seats,” and C6 requires the auditor to be “re-classifying every input's `origin` from the pinned sources.” Thus it supports independent provenance comparison, disputes, and audit reclassification. The packet does not say that `origin` is combined with the reproduction tally or used in a later interpretation, so that downstream purpose cannot be stated from the packet.

Q3. I can infer no hypothesis, comparison model, preferred empirical outcome, or expected pattern from the packet. The question—“does the paper's own number follow from the paper's own recipe applied to the inputs it states”—reveals an interest in reproducibility, and the controls reveal a preference for complete, source-grounded records, but neither implies what the corpus is hoped or expected to show. No packet sentence maps any reproduction or provenance result to a lane conclusion.

## 1. Outcome classes

The six per-claim outcomes are mutually exclusive after the stated precedence and collectively cover a single seat's completed classification. Hard case: a paper prints a chosen fitted parameter and directs its use. The record is `status=PRINTED`; the evidence tie-break makes `origin=FITTED` if the fitting statement applies before a choice statement; arithmetic consumes it and files exactly `REPRO_EXACT` or `REPRO_FAILED`, while the lane script computes a root containing `FITTED` and `rests_on=USES_FITTED`. A merely chosen printed input similarly has `origin=CHOSEN` and `rests_on=USES_CHOSEN`. A value imported from a named enumerable source that machine-matches there is `status=PRINTED`, `origin=IMPORTED`, is consumed, files exactly one arithmetic outcome, and yields `rests_on=USES_IMPORTED`. If the cited value cannot machine-match, it is `BLOCKED` and precedence files `REPRO_BLOCKED`.

`INCONCLUSIVE` is genuinely reachable: any included claim assigned one of the four non-arithmetic outcomes makes `CENSUS_PARTIAL` apply, and that class expressly says “INCONCLUSIVE.”

Q1. YES for the option-(c) per-claim definition: every declared per-claim outcome is reachable and the reproduction question stays decidable because admissibility is controlled by `status`, not `origin`. NO for the preregistration as a whole, because §4 deliberately leaves a reachable inter-seat state without a study-level class.

Defect 1 (fatal, non-cosmetic). Quote: “Open (recorded by the lane owner): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — §4 is not exhaustive over that state.” A reachable state has no declared class, contradicting “Exactly one study-level outcome is filed.” For example, both seats agree that a claim is included and agree on its input list, but after reconciliation one applies the printed-precision rule as `REPRO_EXACT` and the other files `REPRO_FAILED`. No existing class covers this: it is not an enumeration/input-list dispute, an origin dispute, a control split, or yet an audit failure.

Exact replacement: replace the quoted open paragraph with: “`CENSUS_OUTCOME_DISPUTED` — the two seats' per-claim outcomes on any agreed included claim differ after the one reconciliation of §2 step 5. The census does not proceed; every disputed claim is listed with both outcomes, both reproduced values where applicable, and both applications of the stated-precision rule.” Add `CENSUS_OUTCOME_DISPUTED` to the precedence immediately after `CENSUS_DENOMINATOR_DISPUTED` and before `CENSUS_ORIGIN_DISPUTED`; add it to C0's required exhibition table.

Study-level classes are otherwise mutually exclusive under the precedence. A zero-denominator corpus can reach `CENSUS_COMPLETE` if C6 passes: “every included claim” is vacuously arithmetic-group and the audit explicitly permits an empty remaining sample when `R=0`. A non-arithmetic claim reaches only `CENSUS_PARTIAL` among the tally classes.

## 2. Controls

No control may pass on a bare token under its wording. C0 requires an exhibition table plus independent verification; C1-C3 and C5 require printed commands/output/status; C4/C5b require printed path lists; C4 packet redaction requires the builder result; C6 requires a printed JSON artefact. Each control has an exact `PASS|FAIL|NOT_RUN` code, and unreached controls are expressly `NOT_RUN`.

Q2. YES, with a stated human-semantic floor. Test record: a paper line says “We adopt H0 = 67.4 from Planck (2018),” but the ledger records `status=PRINTED`, `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, with that verbatim line. This is plausible and its quotation machine-matches, so the validator alone may accept it. The explicit tie-break says an external-source sentence is `ORIG_CITATION` and therefore `IMPORTED`; the second seat's independent classification and C6's source-based reclassification catch the wrong origin and produce a dispute or mismatch. The master honestly states the limit: identical semantic misclassification by every reader can stand. `root_origins` and `rests_on` are script-computed, preventing a seat from asserting those derived fields.

Defect 2 (non-cosmetic). Quote: “Before the tally, print those four declared counts verbatim from the files,” while the same paragraph defines five declared counts and later says “each of the five declared counts” is checked. This leaves whether `declared_exclusion_count` must be printed before the tally textually inconsistent and weakens the printed control artefact.

Exact replacement: “Before the tally, print these five declared counts verbatim from the files: `declared_candidate_count`, `declared_included_count`, `declared_excluded_count`, `declared_attempt_count`, and `declared_exclusion_count`; then run …”

## 3. Circularity

The lane's expectation cannot reach the evidence through the stated design. Inclusion uses a content-neutral numeral rule independently in two seats; exclusions are enumerated and fully audited; origin classification uses quoted source evidence, a fixed tie-break, independent seats, and audit reclassification; and the interpretation protocol is externally receipted before limb A. A contrary claim cannot be quietly excluded because every candidate and exclusion is ledgered and C6 audits completeness against every pinned source. The remaining human judgments are visible disagreement/failure paths, not silent lane-owner discretion—except for Defect 1, which must be closed before freezing.

## 4. The blind

The packet redaction is machine-asserted as claimed only against the enumerated forbidden list: the builder is pinned, its output is pinned separately, dispatch requires a live PASS, and forbidden strings prevent output. That mechanism can prevent listed strings from surviving; it cannot establish that every synonym or implication is absent. The master states this limit honestly: “the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone.” It also honestly labels filesystem isolation as procedural and self-reported rather than enforced. The packet-first answers above found no hypothesis or consequence leak.

## 5. The seal

The two-receipt relay binds the intended record in the required order. Receipt P binds the protocol hash and commit id before limb A. Receipt T binds the committed tally after computation and before protocol opening, names every file in the commit, and is followed by independent re-hashing of both objects. Missing or mismatched receipts force `CENSUS_AUDIT_FAILED` and leave interpretation `NOT_RUN`. The master correctly limits the claim to record existence/form before comparison and does not claim to bind anyone's mental prior.

## 6. Fairness

The operative negative arithmetic outcome mandates “unreproduced from the stated inputs,” not “error.” Blocked, absent, no-derivation, timeout, dispute, and audit-failure labels do not accuse the paper of error.

Defect 3 (non-cosmetic because it changes the filed class token). Quote: “`REPRO_EXACT` — the paper's number follows, within its own stated precision”. The name `EXACT` asserts more than the definition: values that differ numerically but round to the printed numeral, or fall anywhere within a stated uncertainty, qualify.

Exact replacement: rename `REPRO_EXACT` to `REPRO_WITHIN_STATED_PRECISION` everywhere in operative §§1–7, 9, the candidate schema, script-accepted outcome tokens, C0 exhibitions, and audit output. Replace “The arithmetic group … exactly `REPRO_EXACT` and `REPRO_FAILED`” with “The arithmetic group … exactly `REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`.”

## 7. Stall / executability

Q4. NO: one described operational dependency is expressly outside the delivered directory, and Defect 2 makes one literal print instruction internally inconsistent.

I checked by filesystem metadata, without opening any file beyond the packet and master as instructed. Present beside the master: `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_timeout.py`, `r3c2_lane_tools.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, and `R3C2_INTERPRETATION_PROTOCOL_20260904.md`. Present in `r3c2_seat_packet/`: `R3C2_SEAT_PACKET.md` and `SEAT_BRIEF.md`. I ran the three literal C5 commands successfully: `/usr/bin/python3 --version` returned Python 3.9.6; the SymPy command returned 1.14.0; `shasum -a 256 /usr/bin/python3` returned `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`. I checked that the literal ledger, lane, builder, and timeout script paths named in the master resolve as delivered. Commands containing `<candidates.json>`, `<ledger.json>`, seat-ledger paths, and output paths are templates expressly required to be resolved at runtime, not missing delivered artefacts. `C6_AUDIT.json`, candidate/exclusion ledgers, merged outputs, receipts, and the check sheet are declared outputs, so their pre-run absence is not a delivery defect.

Defect 4 (non-cosmetic executability/dependency finding). Quote: “On a split, a third seat is dispatched by the lane owner via `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh` (absolute path; it exists there, not in this directory)”. By its own words, this required split-path command is not delivered with the preregistration, is outside the pinned corpus/delivery, and has no digest here. The instruction forbidding access to any third file prevented external verification, but no external check can make an outside, unpinned dependency part of the delivered design.

Exact replacement: commit the dispatcher beside the master, pin its sha256 in §9, include it in the dispatch record, and replace the sentence with: “On a split, the lane owner runs `/usr/bin/env sh ./nm_referee_dispatch.sh` from this directory; `nm_referee_dispatch.sh` is committed beside this document with sha256 `<64-hex digest>`, and the printed command, stdout, stderr, and exit status are retained.” If dispatch is intentionally infrastructure rather than a reproducible study command, delete the command/path claim and say: “Third-seat dispatch is an external administrative action and is not claimed executable from this packet.”

The 120-second wrapper rule is executable in form and maps exit 124 to `SYMBOLIC_TIMEOUT`; machinery absence maps to `MACHINERY_UNAVAILABLE`; unreached controls map to `NOT_RUN`. No additional stall state lacks a declared handling rule once Defect 1 is repaired.

R3C2_V20_GATE_COMPLETE
