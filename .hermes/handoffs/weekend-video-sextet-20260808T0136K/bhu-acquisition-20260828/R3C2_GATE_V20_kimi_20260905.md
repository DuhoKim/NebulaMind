ACCESS_SHA=e8ba4a7438d61f028e3c46c7308d7078f011e53886e332b946bd7e21f6a1c6c8
PACKET_SHA=ae995d69fb36732a693f243186150cc3ac05d676ebf04215264336c422a63286
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE

Seat: kimi. Design-only gate of R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V20). No derivation has been run; section 10 was read as history and binds nothing here; every finding below is built from the operative text (sections 1,2,3,4,5,6,7,9,11) and from live execution of the delivered tools. The packet was read and answered BEFORE the master.

## 0. Packet phase — answered from r3c2_seat_packet/R3C2_SEAT_PACKET.md ALONE, before the master was read

Q5 (answered before Q3): CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER?
NO. Four candidate sentences were found and weighed:
  1. packet line 294 (§11): "No tier, warrant token, standing or stamp moves." — the only sentence that speaks to consequence, and it declares consequence FLAT: nothing moves, for any outcome.
  2. packet line 77 (§3 REPRO_FAILED): "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" — consequence-awareness, but it strips consequence from the wording rather than assigning any.
  3. packet line 115 (§4): "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`." — filing precedence, a conservative tie-break, not a consequence ranking.
  4. packet lines 249-252 (C6): the arithmetic group is audited in full, "no sampling discount", the rest at 20% — an audit-LABOR asymmetry by outcome class, not a consequence to the lane or anyone.
No sentence tells me any outcome is more consequential than any other. CONSEQUENCE_VISIBLE=NO.

Q6 (answered before Q3): CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR?
CANNOT_STATE. Everything the packet does with `origin` is process: packet lines 62-63 — "`origin` is one recorded attribute of a ledger record, beside `status`, `value`, `source_file` and `source_line`"; line 61 — "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" (origin never enters the arithmetic); line 13 — "The reproduction verdict and the provenance fields are recorded separately."; line 192 — "Every input's `origin` is classified independently by both seats."; lines 129-131 — disagreement affecting ">10% of included claims" files `CENSUS_ORIGIN_DISPUTED` and "The census does not proceed."; line 249 — the C6 auditor re-classifies every input's origin "from the pinned sources". So the study records it, double-classifies it, halts on dispute, and audit-re-classifies it. No sentence states what origin is FOR: no tally, comparison, verdict, or any downstream consumer is named. The only statable purpose is a cross-reader integrity check, which is mechanism, not purpose. ORIGIN_PURPOSE=CANNOT_STATE.

Q3: WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?
Six adversarial reconstruction attempts:
  (a) DOMAIN: the C3 STANDARD table (packet lines 209-228) plus line 226, "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline," tells me the corpus is post-2018 quantitative cosmology whose claims consume Planck-2018-baseline parameters, and that the lane holds prior corpus knowledge (it knew which constants recur). Domain, not a hypothesis.
  (b) EXPECTED FAILURE MODES: 4 of 6 per-claim outcomes are non-arithmetic, elaborated with anticipating detail — lines 88-90 ("A procedure named but not specified — a sentence that says where the number came from without stating operations a seat could attempt — states no computational procedure that could produce it; file this class and name the passage.") and line 81 ("whether that source is obtainable elsewhere is irrelevant"). I infer the lane expects provenance-only number statements, citations to non-corpus sources, and unprinted inputs to OCCUR — i.e., expects that many claims may not reproduce from stated inputs alone. No fraction, no direction, no preferred class is stated.
  (c) WORDING ARMOR: line 77 tells me the lane treats REPRO_FAILED as a live outcome and fears its overclaiming. It steers toward caution, not toward a science conclusion.
  (d) PROVENANCE SALIENCE: origin carries the heaviest machinery in the packet (7 values, machine-cited evidence, tie-break order, double classification, 10% dispute stop, audit re-classification) while — per Q6 — NOTHING consumes it analytically. I infer the lane regards provenance classification as contentious and credibility-critical. I tried to reconstruct the further stake (e.g., "chosen/fitted inputs reproduce worse") and CANNOT: no sentence connects origin to any outcome, tally, or comparison.
  (e) STUDY-LEVEL ARCHITECTURE: 5 of 7 classes are stop/failure modes; the only tally-filing success class is the narrowest; CENSUS_PARTIAL is pre-declared INCONCLUSIVE with precedence. The design expects completion to be hard; it does not prefer one science result over another.
  (f) CONSEQUENCE SEVERANCE: line 294 cuts any payoff differential, consistent with "no preferred outcome".
CONCLUSION: I can infer corpus domain, prior corpus knowledge, and that the lane EXPECTS non-arithmetic outcomes to occur and provenance to be contentious. I cannot infer any hypothesis, comparison model, quantified expectation, or preferred outcome — the two strongest steering channels (origin→verdict, outcome→consequence) are explicitly cut by lines 61, 13 and 294. The residual signals steer at most toward underclaiming, which is outcome-neutral. LEAK=NONE.

## 1. OUTCOME CLASSES (Q1 placed here)

Per-claim (six outcomes). Exhaustive: every terminal path of the §2 method lands in exactly one class — no derivation stated (incl. named-but-unspecified) → REPRO_NO_DERIVATION_STATED; input traced-but-unobtainable/unmatched → REPRO_BLOCKED; input untraced → REPRO_INPUT_ABSENT; attempt exceeds the cap or machinery → REPRO_NOT_EVALUABLE; attempt completes → REPRO_EXACT or REPRO_FAILED by the stated-precision/uncertainty tests. I could not construct a real claim fitting none. Mutually exclusive: every co-occurrence I constructed (a BLOCKED input beside an ABSENT one; a timeout that would follow a BLOCKED input; no-procedure beside absent inputs) is resolved by the stated precedence ("file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group") plus "Exactly one outcome is filed per claim." No real claim fits two after the rule is applied. Candidate exclusions are not outcomes and are machine-bijected to excluded candidates (verified live, M1-M4).
Study-level (seven classes). Mutually exclusive via the stated filing order; reachable states are covered EXCEPT one the document itself declares open (D1 below). INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL files it whenever at least one included claim carries a non-arithmetic outcome — one BLOCKED input anywhere in the corpus suffices — and it takes precedence over CENSUS_COMPLETE.

Q1 (asked directly): DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?
YES, with two named seams (D1, D3), neither of which is the old tension. Hardest cases constructed and traced to exactly one outcome and one rests_on value:
  - PRINTED-BUT-CHOSEN input ("we choose beta = 0.5" printed in the paper): status PRINTED → consumed by the arithmetic (status governs, origin does not) → REPRO_EXACT or REPRO_FAILED. Live run (O3): root_origins=['CHOSEN'], rests_on=USES_CHOSEN. One outcome, one rests_on. The old four-round tension (admissibility gating vs reproduction) is gone: following the paper's own chosen value IS reproducing the paper, and provenance is recorded beside the verdict, not gated on.
  - FITTED parameter the paper states: status PRINTED, origin FITTED (ORIG_FIT_STATED) → consumed → EXACT/FAILED; rests_on=USES_FITTED.
  - IMPORTED value, three routes: printed in the claiming paper with a citation (tie-break ORIG_CITATION wins: "a sentence that names an external source for the value is a citation whatever else it says") → PRINTED/IMPORTED → consumed; NOT printed but cited to a pinned enumerable text with a machine-match at the cited line → PRINTED from that source, IMPORTED, consumed; NOT printed and no machine-match (or source not enumerable) → REPRO_BLOCKED, never consumed. Each route is deterministic.
  - CLOSED-LIST values: printed verbatim and on the list → STANDARD (both seats forced onto the same route by the §2 step-3 rule); printed but off the exact string (67.4 vs 67.36) → PRINTED, never STANDARD; not printed at all → the named-source rule alone, never STANDARD (verified live: N1 pass, N6 fail "not on the closed list").
  - GENUINE TWO-SEAT SPLIT on origin: carried as origin_alt/origin_evidence_alt (verified O8) and computed as a DISPUTED pair under both classifications (verified O9: rests_on=['USES_CHOSEN','USES_FITTED'], marked DISPUTED) — not one rests_on value, deliberately, with a 10%-of-claims stop. Declared behavior, not a gap.
Reachability: each of the six per-claim outcomes and each of the seven study-level classes has a concrete input path (and C0 exists to exhibit them pre-freeze, two independent seats, coverage-only check by the lane owner). Decidability: every case above is mechanical EXCEPT at one boundary — the rounding midpoint (D3) — where the stated-precision rule does not fix the convention; and the surviving per-claim outcome split (D1) is reachable but classless, as the document itself declares.

## 2. CONTROLS (Q2 placed here)

Assertion-vs-artefact sweep, control by control:
  C0_REACHABILITY: artefact is the exhibition table, authored by one independent seat, independently verified by a second, both must PASS; lane owner checks coverage only and "does not judge reachability". Not assertion-passable.
  C1_DENOMINATOR_PRINTED: PASS only on exit 0 of the printed census run; command, stdout, exit status printed. Verified live (M1 PASS with declared+recomputed printed; M3 wrong declared count FAILs with the mismatch named; M2 final PASS; M4 final rejects PENDING with the candidate named).
  C2_INPUT_LEDGER / C3_NO_SUBSTITUTION: PASS only on exit 0 from every printed validate run; "a token asserted without the printed run is FAIL". C3's print predicate is the strong one; C2's own predicate is thinner (D4, COSMETIC — same run serves both).
  C4_SEAT_ISOLATION and C5B_NO_CROSS_LANE: self-reported path lists — and the document says so: "PASS means the printed list contains no such row and makes no claim that the list is complete" (C5b) and C4 "is a self-reported control with a structural aid, and is labelled so." These two CAN be passed by assertion — by design, with the limit stated in the text and a structural aid (out-of-lane copy, dispatch inventory, builder assertion) beside them.
  C4_PACKET_REDACTED: machine-asserted by the pinned builder; any surviving forbidden string → packet not written, FAIL; "the study does not proceed on a hand-checked copy."
  C5_HARNESS_PINNED: three live commands, "a transcribed value in place of live output is FAIL".
  C6_AUDIT_SAMPLE: PASS only if C6_AUDIT.json "exists, is printed, and carries no MISMATCH and no incompleteness; a token without the artefact is FAIL"; seed external, post-receipt; missing seed → NOT_RUN → CENSUS_AUDIT_FAILED with the missing seed named.
Every control names an exact code with PASS|FAIL|NOT_RUN; unreached controls are NOT_RUN, never passes ("Controls in an unreached limb are `NOT_RUN`, never passes.", and §4's stop-class rule).

Q2 (asked directly): IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?
YES — with one floor the document states itself, which I verified empirically.
TEST PERFORMED: I wrote the record {"symbol":"alpha","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_line":4,"verbatim":"we set alpha = 0.5"},"value":"0.5"} against the source line "Following refs. [12], we set alpha = 0.5 for this test". The origin is WRONG but plausible: the sentence names an external source, so the reason-code precedence ("ORIG_CITATION first — a sentence that names an external source for the value is a citation whatever else it says") requires IMPORTED; but the quotation IS a verbatim substring of the cited line and the value machine-matches.
WHICH MECHANISM CATCHES IT: not the machine. `validate` PASSED it (probe N2, exit 0, C3_NO_SUBSTITUTION=PASS) — the machine checks that the evidence is real (quotation substring of the cited line, value machine-match, STANDARD exact-string membership, no ABSENT/BLOCKED value, schema-only fields, derived_from integrity/acyclicity — each verified live: N1,N3,N4,N5,N6,N7), not that the reason code is semantically right. The catch is the classification layer: the second seat's independent classification (a FITTED/IMPORTED filing there produces origin_alt in the merge — verified O8 — and a DISPUTED pair in compute — verified O9), and the C6 auditor's re-classification of every audited input's origin from the pinned sources, with MATCH/MISMATCH per claim and CENSUS_AUDIT_FAILED on mismatch; above 10% of included claims the census stops under CENSUS_ORIGIN_DISPUTED. IF EVERY READER MISCLASSIFIES IDENTICALLY, the record stands — and the master states exactly this floor: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." An ORIG_SILENT record must print its origin_search {query, files, matches}, and "a search of one query with no variants is not adequate" — the adequacy mechanism is named. So: origin is cited-not-asserted at the evidence layer (machine), and double-checked at the classification layer (second seat + auditor), with the irreducible floor declared.

## 3. CIRCULARITY

I hunted every channel by which the lane's expectation could reach the evidence:
  INCLUSION: the §1 rule contains no pattern content (a numeral the paper asserts as its own result; five enumerated exclusion kinds). Two seats include independently, tolerance zero; any surviving disagreement stops the study (CENSUS_DENOMINATOR_DISPUTED) with the complete ledgers reported. The lane cannot steer inclusion without producing a visible stop.
  EXCLUSION: a contrary claim cannot be quietly excluded — it must appear in the candidate file with included=false and exactly one exclusion row naming one of five kinds; the script verifies the bijection (verified M1-M4); the C6 auditor then dispositions EVERY candidate and EVERY exclusion against every pinned source (completeness, not sampling). A bogus kind is an incompleteness finding → CENSUS_AUDIT_FAILED.
  ORIGIN CLASSIFICATION: double-blind classification, disputes carried never reconciled, audit re-classification, evidence quotations machine-matched. The lane cannot tune a classification post-hoc without a MISMATCH.
  THE VERDICT-PROVENANCE SEAM: the seats never compute rests_on (the seat tool has no merge/compute — verified from its usage text; a seat-authored rests_on fails validate — verified N4; the lane tool rejects any ledger arriving with rests_on — verified O6 exit 2). The lane's merge/compute runs happen after both seats exit, and the C6 auditor re-derives outcomes and origins independently, so a lane-side tamper is an audit failure.
  INTERPRETATION STEP: lives in R3C2_INTERPRETATION_PROTOCOL_20260904.md, which no seat may open, committed and receipted (P) BEFORE limb A, opened only after the tally is receipted (T). The seats never learn what is compared. The corpus itself is pinned by digest before any enumeration.
  RECONCILIATION/Open decisions: the one place a human records a decision after seeing outcomes is the open state of D1 — it is recorded, visible, and escalated; it cannot silently change a filed outcome (the merged candidate file is receipted at T).
No channel found. A contrary claim cannot be quietly excluded; it can only be excluded loudly, through an audited ledger.

## 4. THE BLIND

Machine-asserted as claimed? YES. The builder is pinned (sha256 f2f6c9ab4225630d3ac8c80d75501375a89496ff53a91a5f585dd0d6f27f7d67 — verified against disk), drops §0/§7/§8/§10 whole, strips every SEAT-REDACT span, and asserts no forbidden-list string survives; a surviving string → packet not written, C4_PACKET_REDACTED=FAIL; a dispatch built by any other bytes fails. The delivered packet confirms it at content level: my own scan of the packet found ZERO occurrences of the consequence words (expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting, warn), of "pattern", "ΛCDM", "comparison", "model", "hypothesis", of every engine and custody-chain name (kimi, codex, Tori, Blanc, Duho), of "R3D", and of every comparison token (rests_on, root_origins, DERIVED_ONLY, USES_); packet section headers are exactly 1,2,3,4,5,6,9,11; the packet header cites master sha256 e8ba4a74... == ACCESS_SHA, and the pin file's packet entry == PACKET_SHA, so the packet is built from exactly the bytes I gated. The residual hits are benign ("merged candidate file" — the tally's assembly rule; "recomputed" — C1's count check; "--version"/"__version__" — C5; DISPUTED/tally — class names and procedure).
Does the C4 mechanism prevent what it claims? It claims: the seat is not FURNISHED the pattern (dispatch copy inventoried by the lane owner before launch, contents listed in the redacted C4 block — and the copy contents now include the pin file, the manifest and every pinned source); the packet/brief redaction is machine-asserted; the seat's printed path list is detection. It does NOT claim filesystem enforcement, and says so: "This is procedural, not enforced by the filesystem"; "C4_SEAT_ISOLATION is a self-reported control with a structural aid, and is labelled so."
Is the limit stated honestly? YES, verbatim: "this design does not prove that the seat did not read an absolute path into the lane; C4_SEAT_ISOLATION=PASS certifies the contents of the printed list and of the dispatch copy, not actual non-access. **It cannot prove a seat has no prior exposure from training or an earlier session** — nothing available here can." The forbidden list's own limit is also stated: "the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed." The accepted residual floor (a seat can tell provenance is measured with care, per the packet's taxonomy) is exactly the floor my Q3 reconstruction hit — and nothing beyond it was recoverable.

## 5. THE SEAL (§7)

Does the receipted relay bind what it claims to bind, before the step it must precede? YES on both limbs:
  (P) The interpretation protocol's hash and commit id are sent to Blanc (outside the lane, reports to the principal) and receipted BEFORE limb A begins; "limb A does not begin without receipt P." So the protocol exists, in receipted form, before any enumeration — it cannot be written to suit the tally.
  (T) The tally — one named commit containing the merged candidate file, the exclusion ledger, and the merged input ledger with its compute output, the receipt naming every file — is committed and receipted BEFORE the protocol is opened. So the tally exists, in receipted form, before the comparison is computed — it cannot be tuned to the protocol, nor the protocol swapped to suit it.
  VERIFICATION: after opening, Blanc independently re-hashes BOTH artefacts and verifies all four values against the two receipts; the interpretation report must print both receipts and the four verified values; "Any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4, whose definition now names this case), leaves the interpretation NOT_RUN and voids the comparison." A post-receipt substitution of either artefact is caught. "The relay is complete only when it is receipted" — a send without a receipt is an assertion of custody, not custody, and the document says so.
  The local-commit objection is answered in text: "a local commit in Tori's own custody can be amended or reset, so it is tamper evidence, not an external custodian."
  The limit is stated honestly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."
  One imprecision, COSMETIC: "The interpretation step lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which **no seat may open** and which is itself fixed and committed before limb A begins, so it cannot be written to suit the tally." Under the document's own standard the "so" is supported only by receipt P (commitment alone is tamper evidence, as the same section states two paragraphs later). The mechanism IS specified and ordered correctly; only this sentence's compression of it is loose. Optional replacement: "which is itself fixed, committed and receipted (P) before limb A begins, and so cannot be written to suit the tally." COSMETIC — changes no binding, no order, no filed class.

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" — the rule is stated inside the REPRO_FAILED definition itself, and the audit classes name process states (CENSUS_AUDIT_FAILED names the audit's failure cause, not the paper; CENSUS_PARTIAL is pre-declared INCONCLUSIVE). REPRO_BLOCKED explicitly refuses to fault the paper for an unobtainable source ("whether that source is obtainable elsewhere is irrelevant, because the census may not open or consume it"). No clause I found asserts paper error.
Class names under the class-name test (a name must not assert more than its definition):
  D2 (escalated to the principal): REPRO_EXACT asserts exactness; the definition admits any difference within the stated precision or stated uncertainty (reproduced 13.79 vs printed 13.8 files REPRO_EXACT; a value within a stated ±0.5 files REPRO_EXACT at any offset in the band). The master-only label DERIVED_ONLY likewise covers STANDARD- and MEASURED-only roots. Verbatim: "- **`REPRO_EXACT`** — the paper's number follows, within its own stated precision, **from the paper's own recipe applied to the inputs it states**". Defect: the name asserts more than the definition; a reader of the tally over-reads "EXACT". Replacement: the already-escalated rename (REPRO_WITHIN_STATED_PRECISION; the label e.g. DERIVED_STANDARD_OR_MEASURED) — the principal's, because a class rename is a redefinition. NOT cosmetic under the brief's own test, because a tally reader's conclusion can change; no filed class, control, reachability or tested value changes.
  All other class names (REPRO_FAILED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NO_DERIVATION_STATED, REPRO_NOT_EVALUABLE; the seven study-level classes) match their definitions.

## 7. STALL / EXECUTABILITY (Q4 placed here)

STALL: every symbolic operation runs through the pinned wrapper with a 120.0-second monotonic deadline; on the deadline it prints SYMBOLIC_TIMEOUT and exits 124 (verified live, probe L: /bin/sleep 2 under a 0.5s cap → SYMBOLIC_TIMEOUT printed, WRAPPER_ELAPSED=0.504, exit 124; probe K: fast child → WRAPPER_COMMAND, stdout, stderr, WRAPPER_EXIT=0, exit 0). The non-timeout limb files MACHINERY_UNAVAILABLE with the point reached. One repeat is permitted, meaningful only for REPRO_NOT_EVALUABLE, and attempts are machine-bounded to {0,1,2} (verified in the census schema runs). Stop classes have a total precedence; unreached controls are NOT_RUN. No unbounded wait exists.

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?
NO — everything I checked exists and executes as delivered. Each thing checked, and how:
  FILES (existence + sha256 against the document's pins; contents not opened beyond the two named files):
    r3c2_ledger_tools.py — digest == 8e286817... pin ✓
    r3c2_lane_tools.py — digest == 2aa1ea7d... pin ✓
    r3c2_timeout.py — digest == fbb9bef7... pin ✓
    r3c2_build_seat_packet.py — digest == f2f6c9ab... pin ✓
    R3C2_CORPUS_MANIFEST.md — digest == 300d4da1... in-text pin ✓
    r3c2_seat_packet/R3C2_SEAT_PACKET.md — digest == PACKET_SHA == the pin file's packet entry ✓
    r3c2_seat_packet/SEAT_BRIEF.md — exists; digest 8f853bef... == the pin file's brief entry ✓
    R3C2_SEAT_PACKET.sha256 — exists; contains exactly five entries (packet, brief, timeout, ledger tool, builder), every one matching the bytes on disk ✓ (so §3's "pinned at R3C2_SEAT_PACKET.sha256" for the seat tool is literally true)
    R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists and is git-tracked ✓ (§7's "lives in")
    R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md — exists ✓ (named in C4)
    /Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh — exists at the stated absolute path ✓ (§9)
    C6_AUDIT.json, R3C2_CHECK_SHEET_<date>.md — to-be-authored artefacts, correctly not required to exist.
  COMMANDS:
    C5 triple — /usr/bin/python3 --version → Python 3.9.6, exit 0; sympy → 1.14.0, exit 0 (the literal string uses `python3 -c`, which this shell blocks; verified by the equivalent file-based import under the same interpreter — disclosed substitution); shasum -a 256 /usr/bin/python3 → b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9, exit 0.
    census — pre-tally PASS (declared AND recomputed counts printed, reconciled=YES) ✓; final PASS ✓; a wrong declared count FAILs with the mismatch named ("FAIL: declared_included_count=2 but recomputed 1") ✓; final with a PENDING outcome FAILs with the candidate named ✓.
    validate — good ledger PASS ✓; ABSENT record carrying a value FAILs (named) ✓; a field outside the schema (rests_on) FAILs (named: "seat-authored ledger carries a field outside the schema: ['rests_on']") ✓; DERIVED with no derived_from FAILs ✓; a STANDARD value off the exact table string FAILs ("not on the closed list") ✓; a PRINTED value not at its cited line FAILs ("value 0.7 not at cited line") ✓.
    merge — identical input-id sets exit 0 ✓; differing sets exit 1 with the diff named ✓; an origin disagreement is carried as origin_alt/origin_evidence_alt and counted ✓; the PARENTS_DISPUTED counter is present in the delivered tool ✓.
    compute — derives root_origins per record and rests_on per claim, printing both (USES_CHOSEN for a chosen root; DERIVED_ONLY for a STANDARD-only claim) ✓; a disputed root yields the pair under both classifications marked DISPUTED ✓; a ledger arriving with rests_on set is REJECTED, exit 2 ✓; a DERIVED record with no parents FAILs, exit 1 ✓.
    timeout wrapper — see STALL above; behavior matches §9 verbatim (SYMBOLIC_TIMEOUT + exit 124 on the deadline).
  DELIBERATELY NOT RUN: r3c2_build_seat_packet.py — running it would rewrite the pinned packet and pin file mid-gate. Verified by the digest chain instead: the packet header cites master sha256 e8ba4a74... == ACCESS_SHA; the pin file matches the packet, brief, timeout, ledger-tool and builder bytes on disk; the builder's digest matches its pin. NOT OPENED: SEAT_BRIEF.md, R3C2_CORPUS_MANIFEST.md, the protocol, and every pinned source (my brief limits content reads to the two named files); their existence/pins were verified as above. Substitutions disclosed: tools were invoked by absolute path from /tmp/r3c2_v20_gate_kimi with synthetic sources, not from a dispatch copy directory.

## DEFECTS (each: verbatim quote, defect, exact replacement)

D1 — ESCALATED TO THE PRINCIPAL (class addition is his); the document declares it itself.
  Verbatim (§2 step 5): "the class filed when a disagreement survives that reconciliation is **an open decision recorded by the lane owner**; this document is not frozen until that decision is recorded"
  Verbatim (§4, item 5 note): "*Open (recorded by the lane owner): the class filed when the two seats' per-claim outcomes on an agreed included claim differ after the one reconciliation of §2 step 5 — §4 is not exhaustive over that state; a class is not added or redefined by this document's author, so the gap is recorded here as open rather than written in.*"
  Defect: a reachable state — two seats agree the claim and its inputs but file different per-claim outcomes after the one reconciliation — has no study-level class, so §4 as written is not exhaustive; the document says so and self-blocks freeze. Independently constructed companion case: a ZERO-denominator census vacuously satisfies CENSUS_COMPLETE as written ("every included claim carries exactly one outcome from the arithmetic group" over zero claims; C6's sample is empty; nothing can MISMATCH) — a degenerate "complete" that belongs in CENSUS_PARTIAL.
  Replacement: the principal's ruling — a stop class for the surviving split (listing both seats' outcomes and adopting neither, in the pattern of CENSUS_CONTROL_SPLIT), with the zero-denominator clause attached. Not written in by me; adding or redefining a class is not the gate's.
D2 — ESCALATED TO THE PRINCIPAL (rename is a redefinition). Verbatim, defect and replacement in §6 above (REPRO_EXACT name asserts more than "within stated precision"; DERIVED_ONLY covers STANDARD/MEASURED roots).
D3 — NEW, MINOR, non-escalated (wording; boundary-only; does not touch option (c)'s settled definition).
  Verbatim (§3, REPRO_EXACT): "**Where the paper states no precision for the claim, the printed precision is the claim's stated precision: the reproduced value must round to the printed numeral at that precision.**"
  Defect: the rounding convention is unspecified, and at an exact midpoint of the last printed digit the two standard conventions file different outcomes — e.g., a claim printed "2" (integer precision, no stated uncertainty) with reproduced value exactly 2.5 rounds to 3 under half-away-from-zero (≠2 → REPRO_FAILED) and to 2 under half-even (=2 → REPRO_EXACT). One tested value flips, so not cosmetic; and because the §2-step-5 reconciliation is anchored on "the stated-precision rule of §3", two seats applying different conventions would produce a split the rule cannot settle — landing in the currently classless state of D1. Probability low, but the rule was added to make exactly this mechanical.
  Exact replacement: "**Where the paper states no precision for the claim, the printed precision is the claim's stated precision: the reproduced value must round to the printed numeral at that precision, rounding half away from zero.**"
D4 — COSMETIC.
  Verbatim (C2): "validated by `/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> .` run from the printed seat working directory (`.` is the sole allowed `sources_dir`); before execution the seat prints the fully resolved command with every angle-bracket placeholder replaced by the actual in-scope path (exit 0 = PASS; every failure printed)."
  Defect: C2's own predicate does not require the run's stdout/exit status to be printed; C3 — the same validate run — does ("a token asserted without the printed run is FAIL"). No passability change in practice because the printed C3 run is the shared artefact. Optional replacement: append "the printed C3 run is this control's artefact." Marked COSMETIC: no class filed, no control's passability, no reachability, no conclusion, no tested value changes.
D5 — COSMETIC (§7 "so" compression; see §5 above). Optional replacement stated there.

## FAILED ATTACKS (what I tried that held)

- Claims engineered to fit two per-claim outcomes (blocked+absent co-occurrence; timeout-after-blocked; no-procedure+absent): precedence plus "exactly one outcome" resolves each to one. Claims engineered to fit none: none found.
- Printed-but-chosen, fitted-stated, imported (printed / unprinted-matched / unprinted-unmatched / closed-list-verbatim / closed-list-off-string): each traces to exactly one status, one outcome path, one rests_on (live-verified end to end).
- Wrong-but-plausible origin with a genuine quotation: passes the machine (N2) but is caught by the second seat and the auditor; identical-misclassification floor is stated by the document itself before I could claim it as a finding.
- Quiet exclusion of a contrary claim: impossible without a machine-checked exclusion row and an audited completeness disposition.
- Audit-sample steering: the seed is external, post-receipt, 64 hex, not a function of the tally.
- Post-receipt substitution of tally or protocol: caught by the custodian's post-opening re-hash of both against two receipts (four values); any mismatch files CENSUS_AUDIT_FAILED.
- Lane-side provenance computation smuggled to seats: seat tool has only census/validate (usage-verified); planted rests_on fails validate (N4) and is REJECTED by compute (O6, exit 2).
- Packet leak reconstruction: six attempts (section 0, Q3); no hypothesis, comparison model or preferred outcome recoverable; my forbidden-string scan of the delivered packet independently confirms the builder's assertion.
- Every pinned digest: all match; the pin file's five entries all match disk; the packet header's master hash matches ACCESS_SHA.
- The one attack that held against the text: the rounding midpoint (D3).

## EVIDENCE LEDGER AND CONSTRAINTS HELD

Opened for content (2 files only, per the brief): R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (read in full, 973 lines) and r3c2_seat_packet/R3C2_SEAT_PACKET.md (read in full, 295 lines, answered BEFORE the master). Metadata only: R3C2_SEAT_PACKET.sha256 (five hash lines); directory name listings. Digests computed on the six pinned files. No gate report, C0 report, manifest, seat brief, interpretation protocol, or pinned source was opened. Live probes: 6 harness/wrapper commands, 4 census runs, 7 validate runs, 3 merge runs, 3 compute runs, all on synthetic inputs under /tmp/r3c2_v20_gate_kimi (paper.txt, candidates*.json, exclusions.json, ledger_*.json, helper scripts). No writes to the lane directory; the report file beside the master is the only write. Section 10 was read as history; nothing above relies on its narrated findings — D1 and D2 stand on the operative text (§2 step 5, §4 item 5 note, §3 class definition), D3/D4/D5 are new from this read.

R3C2_V20_GATE_COMPLETE
