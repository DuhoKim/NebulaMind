ACCESS_SHA=b146c8c45ad2dd9affc6a94b0be5ed5886d231c8d4eeaf3bc6c03ce6146e36bd
PACKET_SHA=ac58435883b117e4f60030e33dd2b1d2177f064fc7e65352c890199e937cd912
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=the auditor re-classifies each origin from pinned sources and checks it against the sealed record; packet sentence: “a third independent seat … re-classifying every input's `origin` from the pinned sources”

## Packet-first answers

Q5. No. From the packet alone I cannot tell that any outcome is more consequential than any other, to the lane or to anyone. No sentence says so. Procedural stop effects and the rule that a negative is worded “unreproduced from the stated inputs” do not assign an empirical or institutional stake to an outcome.

Q6. Yes, but only as an integrity-check purpose. The packet says a third seat works while “re-classifying every input's `origin` from the pinned sources,” and that `C6_AUDIT.json` records “the re-classified `origin` of each of its inputs, and `MATCH` or `MISMATCH` against the sealed record.” It also uses cross-seat origin disagreement to reach `CENSUS_ORIGIN_DISPUTED`. The packet does not disclose `rests_on`, the provenance summary, its ordering, or its later interpretive use.

Q3. I infer no hypothesis, comparison model, preferred substantive outcome, or expected pattern. The only substantive question disclosed is neutral: “does the paper's own number follow from the paper's own recipe applied to the inputs it states.” The packet supplies exhaustive procedures and possible failure classes but does not forecast their distribution, identify a comparison, say which result helps the lane, or map any result to a conclusion. The detailed provenance taxonomy shows that provenance will be checked, but not what pattern is hoped for or how any provenance category would bear on a hypothesis.

## 1. Outcome classes

The six per-claim outcomes are exhaustive for an included claim and mutually exclusive after the explicit priority rule. A passage with no usable computational procedure files `REPRO_NO_DERIVATION_STATED`; otherwise a named but unavailable/unmatched source files `REPRO_BLOCKED`; an unnamed missing input files `REPRO_INPUT_ABSENT`; a machinery failure or timeout files `REPRO_NOT_EVALUABLE`; and completed arithmetic files exactly one of `REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`. A claim that simultaneously lacks one input and times out on another would superficially fit two descriptions, but the stated priority files only `REPRO_INPUT_ABSENT`. I found no claim state that fits none.

At study level, overlapping states are likewise resolved by the total priority order. For example, a denominator dispute plus a later-discovered audit defect files only `CENSUS_DENOMINATOR_DISPUTED`; the audit limb is unreached. I found no reachable tally/control state that fits none. `CENSUS_PARTIAL`—the document's expressly labelled INCONCLUSIVE result—is genuinely reachable whenever at least one included claim has a non-arithmetic outcome, and also by the explicit zero-denominator clause.

Q1. Yes. Option (c) makes every declared outcome reachable while keeping filing decidable. Hard cases:

- Printed-but-chosen input: a paper says “we choose β = 1/929.25” and gives a complete formula. The ledger record is `status=PRINTED`, `origin=CHOSEN`, `ORIG_CHOICE_STATED`; arithmetic consumes it. The claim files exactly one arithmetic result, and lane computation gives exactly `rests_on=USES_CHOSEN` (assuming it is the relevant root).
- Stated fitted parameter: “a fit gives a = 2.1,” followed by a complete formula. It is `PRINTED`/`FITTED`, is consumed, files exactly one arithmetic outcome, and gives `USES_FITTED`.
- Imported value: the claiming paper cites a named enumerable pinned source and the value machine-matches at the cited line. It is `PRINTED`/`IMPORTED`, is consumed, files exactly one arithmetic outcome, and gives `USES_IMPORTED`. If the source is outside the enumerable manifest or the cited-line value does not match, the same boundary instead deterministically files `REPRO_BLOCKED` before arithmetic.

One cosmetic consistency repair is needed. Quote: “`CENSUS_PARTIAL` — after the §2 attempt … **at least one included claim carries a non-arithmetic outcome**”. Elsewhere the zero-denominator clause orders `CENSUS_PARTIAL`, although zero included claims cannot satisfy that sentence. Defect: the exception is operative and decisive, but it is located only inside the preceding class's definition. Exact replacement: “**`CENSUS_PARTIAL` — either the sealed denominator is zero, with the empty enumeration named, or, after the §2 attempt (one repeat permitted, meaningful only for `REPRO_NOT_EVALUABLE`), at least one included claim carries a non-arithmetic outcome** (`REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`).” Classification: COSMETIC under the requested test because the existing zero-denominator clause already files the same class; this changes no filing, control, reachability, conclusion, or tested value.

## 2. Controls

No control can validly pass by a bare assertion. C0 requires the exhibition table and two independent results; C1–C3 require printed validator commands/output/status; C4 requires the builder result and/or stated path-list artefacts at their respective codes; C5 requires three live command outputs; C5b requires the classified printed path list; and C6 requires the printed `C6_AUDIT.json`. Every control has an exact `PASS|FAIL|NOT_RUN` code, including both C4 codes, and the document says unreached controls are `NOT_RUN`, never PASS.

Q2. The provenance field is checkable, though semantic classification is intentionally reader-audited rather than fully machine-decided. Adversarial record: for the source sentence “We adopt H0 = 67.4 from Planck (2018),” submit `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and quote that exact sentence. This is plausible because “adopt” sounds like choice, but wrong under the mandatory precedence: the named external source makes `ORIG_CITATION`/`IMPORTED` win. Machine substring matching alone need not catch that semantic misuse. The second seat's independent classification catches it as a disagreement, preserves both quotations/classifications, and the C6 auditor independently re-classifies it from the pinned source and emits `MISMATCH` against a wrongly sealed single classification. The document honestly states the remaining floor: identical semantic mistakes by all readers can stand.

## 3. Circularity

The lane's expectation cannot reach the evidence through the operative inclusion rule, which depends only on whether a passage prints a numeral asserted as the paper's own result and uses enumerated neutral exclusions. Included claims all receive a filing; exclusions are separately ledged and fully audited, so a contrary claim cannot be quietly dropped. Status consumption is independent of origin, so origin classification cannot suppress arithmetic. The packet hides the comparison, `rests_on` computation and interpretation mapping; the interpretation protocol is separately precommitted and receipted before limb A. I found no operative hypothesis-to-evidence channel.

## 4. The blind

The redaction claim is machine-asserted by a pinned builder: it removes whole sections and marked spans, then rejects a forbidden-token survivor. The packet I read disclosed no hypothesis, comparison, preferred result, empirical stake, `rests_on` rule, or interpretation mapping. C4 does not literally prevent an agent from opening an absolute lane path; it prevents furnishing the lane files in the dispatch copy and uses a self-reported path list as secondary detection. The master states this limit exactly: “This is procedural, not enforced by the filesystem,” and later says the design “does not prove that the seat did not read an absolute path into the lane.” Thus the mechanism supports the limited claim it makes and does not overclaim a filesystem-enforced blind.

## 5. The seal

Receipt P binds the interpretation protocol's hash and commit id with an external custodian before limb A may begin. Receipt T binds one named tally commit and its named files after tally computation but before the protocol is opened. Independent re-hashing verifies both hashes and both commit ids against the receipts, and a missing receipt/mismatch files `CENSUS_AUDIT_FAILED`. The relay therefore binds the record before each relevant opening/comparison step. The document accurately limits the inference to prior existence of the stated bytes, not absence of guesses in a person's mind.

## 6. Fairness

The negative arithmetic outcome is explicitly required to be worded “unreproduced from the stated inputs,” not “error.” `REPRO_FAILED` asserts only that the stated-input arithmetic did not yield the printed number; blocked, absent, unavailable-machinery, and no-derivation cases have separate non-accusatory names. No class name or operative clause asserts misconduct or more than its predicate. The old `REPRO_EXACT` token has no governing use: grep found only the preamble rename notice, the redacted §3 rename notice, and §10 history; the permitted rename audit likewise reports zero governing references.

## 7. Stall / executability

Q4. Yes, everything required at design time can be executed as delivered; run-produced ledgers, receipts and audit outputs correctly do not exist before a run. I checked:

- Master and packet: read fully; SHA-256 values are the two report header values.
- Rename: `grep -n REPRO_EXACT` on the master; every non-notice occurrence is under a `## 10` history heading. I then read the expressly permitted `R3C2_V21_RENAME_AUDIT_20260905.md`, which independently classifies all hits and reports zero governing references.
- Delivered named files, by existence/type/size metadata without opening prohibited study files: `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_lane_tools.py`, `r3c2_timeout.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, `r3c2_seat_packet/SEAT_BRIEF.md`, the packet, and `R3C2_INTERPRETATION_PROTOCOL_20260904.md` all exist as regular files.
- Literal live harness commands: `/usr/bin/python3 --version` returned Python 3.9.6; `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"` returned 1.14.0; `shasum -a 256 /usr/bin/python3` returned `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`.
- Builder: the exact command `/usr/bin/python3 r3c2_build_seat_packet.py` is fully resolved and its file exists. I did not rerun it because that would rewrite the already pinned packet during a read-only gate.
- Seat validator commands: `census <candidates.json> <exclusions.json>`, `census ... final`, and `validate <ledger.json> .` name exact interpreter/script/subcommand syntax and require only run-produced JSON paths to be resolved and printed.
- Lane commands: `merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>` and `compute <merged.json> <out.json>` are exact and their script exists; all placeholders are necessarily run-produced.
- Timeout wrapper: `/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>` is exact apart from the claim-specific command, which must be supplied at run time; the wrapper exists.
- Seat working path: the packet consistently requires the dispatched working directory, `.` as the sole validator source directory, and paths contained within that copy. The master enumerates every required dispatch-copy constituent.
- Run-produced artefacts: candidate/exclusion/input JSON files, merged/output JSON, `C6_AUDIT.json`, dispatch record, receipts P/T, and the dated check sheet are specified outputs rather than falsely claimed pre-existing inputs.

No described pre-existing artefact is missing as delivered. No stall or executable-path defect found.

R3C2_V21_GATE_COMPLETE
