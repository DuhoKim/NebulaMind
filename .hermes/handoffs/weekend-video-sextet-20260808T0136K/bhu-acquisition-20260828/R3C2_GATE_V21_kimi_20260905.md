ACCESS_SHA=b146c8c45ad2dd9affc6a94b0be5ed5886d231c8d4eeaf3bc6c03ce6146e36bd
PACKET_SHA=ac58435883b117e4f60030e33dd2b1d2177f064fc7e65352c890199e937cd912
GATE=PREREG_SOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a recorded provenance attribute used only as a cross-reader integrity check — classified independently by both seats, disputes >10% of included claims file CENSUS_ORIGIN_DISPUTED, and the C6 auditor re-classifies it (packet §3 blockquote, §4 class 7, C6); the arithmetic never consumes it and no verdict or tally uses it anywhere the packet shows.

Seat: kimi. Design judged as written, V21, option (c) settled. Packet answered BEFORE the master was read (Q5, Q6, Q3 in that order). §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Only the packet, the master, and (after the master) the rename audit were opened; all other named artefacts were verified by digest/execution/existence only.

======================================================================
1. OUTCOME CLASSES — and Q1
======================================================================
Per-claim (§3): six outcomes. Raw conditions genuinely co-occur (a claim can have a BLOCKED and an ABSENT input), but the declared total precedence — REPRO_NO_DERIVATION_STATED → REPRO_BLOCKED → REPRO_INPUT_ABSENT → REPRO_NOT_EVALUABLE → arithmetic group — files exactly one. Exhaustive: any included claim either states no procedure (NO_DERIVATION_STATED, incl. the named-but-unspecified-procedure clause), or has a BLOCKED input, or an ABSENT input, or is attempted and lands in the arithmetic group or NOT_EVALUABLE. Mutually exclusive by the order. I could not construct a real claim fitting two filed outcomes or none.

Study-level (§4): eight classes, total precedence R3C2_NO_CLASS → CENSUS_CONTROL_SPLIT → CENSUS_DENOMINATOR_DISPUTED → CENSUS_OUTCOME_DISPUTED → CENSUS_ORIGIN_DISPUTED → CENSUS_AUDIT_FAILED → CENSUS_PARTIAL → CENSUS_COMPLETE. A tally can satisfy two raw conditions (e.g., a control fails in both seats AND the enumerations disagree); the order files exactly one. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and fires on any non-arithmetic per-claim outcome, and the V21 zero-denominator clause routes an empty census there too ("A denominator of zero files CENSUS_PARTIAL with the empty enumeration named; no census is complete over nothing") — verified present in both master (line 176) and packet (line 111).

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE? YES, on every hardest case I constructed:

(a) Printed-but-chosen input ("We take β = 1/929.25"): status PRINTED, origin CHOSEN via ORIG_CHOICE_STATED quotation. Arithmetic consumes it (consumption is by status, not origin). If the recipe yields the printed numeral within stated precision → REPRO_WITHIN_STATED_PRECISION, else REPRO_FAILED. Script computes root_origins=[CHOSEN] → rests_on=USES_CHOSEN. Exactly one outcome, one rests_on. (Verified live: my fixture ledger with a CHOSEN root computed rests_on=USES_CHOSEN.)

(b) Fitted parameter the paper states: PRINTED/FITTED → consumed → arithmetic group; rests_on=USES_FITTED. One outcome, one rests_on.

(c) Imported value, five routings, each unique: printed in the claiming paper and cited → PRINTED (STANDARD if verbatim on the closed list), origin IMPORTED because the reason-code tie-break puts ORIG_CITATION first ("a sentence that names an external source for the value is a citation whatever else it says"); not printed but cited to a pinned enumerable text with a machine-match at the cited line → PRINTED from that source; cited but no machine-match at the cited line → BLOCKED status → REPRO_BLOCKED, never consumed; cited to a source not pinned in the manifest → REPRO_BLOCKED ("whether that source is obtainable elsewhere is irrelevant"); neither printed nor traced to any named source → ABSENT → REPRO_INPUT_ABSENT. The BLOCKED/ABSENT boundary is the named-source test; disjoint.

The old tension is gone: reachability comes from status-based consumption (chosen/fitted/imported inputs are attempted, not gated), decidability from mechanical precision tests (stated uncertainty taken once; asymmetric = half-width on the side the reproduced value falls; otherwise round at printed precision, half away from zero) plus the total precedence. Every §3 outcome and every §4 class has a constructible input; C0 will exhibit them empirically pre-freeze, which is process, not a design gap.

======================================================================
2. CONTROLS — and Q2
======================================================================
Every control names an exact code with PASS|FAIL|NOT_RUN. None can be passed by bare assertion: C1/C2/C3 PASS only on exit 0 of a printed run ("a token asserted without the printed run is FAIL"); C5 requires live stdout, a transcribed value is FAIL; C6 PASS requires the printed C6_AUDIT.json artefact; C4_PACKET_REDACTED is machine-asserted by the pinned builder. C4_SEAT_ISOLATION and C5b are self-reported path lists — and say so at their true strength ("PASS means only that the list contains no outside path; it makes no claim that the list is complete"), with the structural aid (out-of-lane redacted copy, dispatch inventory listed before launch) stated as "procedural, not enforced by the filesystem". Unreached controls are NOT_RUN (§4 precedence note and §5 closing line).

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? YES, with the floor stated. Attack executed: a record whose origin is wrong but plausible —
{"symbol":"H0","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","verbatim":"We adopt H₀ = 67.4 from Planck (2018)", ...},"value":"67.4", ...}
The quotation IS a substring of the cited line, so the machine passes it (my bad_verbatim fixture confirms the machine checks exactly that substring property and fails a false quotation). The misclassification — the declared precedence makes ORIG_CITATION beat ORIG_CHOICE_STATED — is caught only by (i) the second seat's independent classification (disagreement → origin_alt → ORIGIN_DISPUTED, counted toward the >10% CENSUS_ORIGIN_DISPUTED stop) and (ii) the C6 auditor re-classifying from the pinned sources. If every reader misclassifies identically, the record stands. The master states exactly this floor rather than implying it away: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." Every other substitution route is machine-closed and I executed each closure: planted rests_on field rejected at intake; BLOCKED-with-value rejected; off-list STANDARD rejected; DERIVED-without-parents rejected; cycles rejected; ORIG_SILENT without origin_search rejected; lane compute REJECTS (exit 2) a ledger arriving with root_origins/rests_on set; transitive roots computed by script so one-step laundering fails (my DERIVED-from-CHOSEN record computed root CHOSEN).

======================================================================
3. CIRCULARITY
======================================================================
No channel found. Inclusion/exclusion: the §1 rule is non-semantic (numeral asserted as the paper's own result; excluded kinds are equation/reference/page-line/date/attributed-not-derived), applied independently by two blind seats, with the exclusion ledger printed and audited for completeness by the C6 seat against every pinned source — a quiet exclusion must pass two blind readers and a third auditor and leaves a named row. Origin classification: blind seats cannot aim provenance at a conclusion they cannot see; disputes are carried, never reconciled. The lane-side compute is script-bound and rejects pre-set fields. The interpretation reads rests_on + tally only after receipt P (protocol sealed before limb A) and receipt T (tally sealed before the protocol is opened), so the mapping cannot be tuned to the tally. The packet reveals the taxonomy (seven origins, quotations, two readers) — that is the principal's accepted floor (18:52 KST ruling), not a defect.

======================================================================
4. THE BLIND
======================================================================
Machine-asserted as claimed, and verified by execution. I ran the pinned builder (sha 4ed52d4b…, matches C4 and the pin file) in a sandbox copy: with SEAT_BRIEF.md absent it prints "FAIL: C4_PACKET_REDACTED=FAIL — SEAT_BRIEF.md missing beside the packet", exit 1, and writes nothing (fails closed); with the brief present it prints first line "C4_PACKET_REDACTED=PASS", prints master sha b146c8c4… (== ACCESS_SHA), drops §0/§7/§8/§10, redacts 50 inline spans, and writes a packet whose sha is ac5843… — byte-identical to the pinned packet I was given (== PACKET_SHA). Redaction is reproducible, not hand-checked. My own forbidden-string sweep over the packet found no occurrence of the consequence words, the comparison model, engine names, custody names, gate history, rests_on, root_origins, or severity (only the literal command strings --version/__version__). The C4 mechanism prevents what it claims — the seat is not furnished the pattern material, and the dispatch record proves the copy's contents — and the document states its limit honestly: filesystem access is not denied, the path list is self-reported and not certified complete, and "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can."

======================================================================
5. THE SEAL (§7)
======================================================================
Binds what it claims, before the steps it must precede. Receipt P (protocol hash + commit id, receipted by Blanc outside the lane) precedes limb A — "limb A does not begin without receipt P" — so the interpretation mapping is fixed before any evidence exists. Receipt T (one named commit: merged candidate file, exclusion ledger, merged input ledger with compute output; the receipt names every file) precedes the opening of the protocol — "the interpretation protocol is not opened without both receipts." Post-opening, Blanc re-hashes and verifies four values against the two receipts; any miss files CENSUS_AUDIT_FAILED, which §4's definition names. The C6 seed is supplied only after the tally digests are receipted, so the audit sample cannot be a function of the tally. The overclaim check passes: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."

======================================================================
6. FAIRNESS
======================================================================
REPRO_FAILED carries the mandated wording in master and packet: 'Wording: "unreproduced from the stated inputs," not "error."' The word "error" appears in the packet only inside that rule, quoting the forbidden word; every operative "error" in the master sits under ## 10 (history). No class name asserts more than its definition in seat-visible text: REPRO_WITHIN_STATED_PRECISION (the V21 rename) matches its definition exactly; CENSUS_COMPLETE is pinned to its definition (all arithmetic-group outcomes + C6_AUDIT_SAMPLE=PASS + non-zero denominator).

One COSMETIC observation, seat-invisible: the lane-only rests_on rule (master §3, SEAT-REDACT span) reads "DERIVED_ONLY when every root origin is DERIVED, STANDARD or MEASURED" — the label names a stricter set than its membership rule. It changes no class filed, no control's passability, no reachability, no conclusion and no tested value (the reported value is computed by the stated rule; the label appears only in lane-side text and the tally) — COSMETIC. Exact replacement, when the principal authorizes: rename DERIVED_ONLY → DERIVED_STANDARD_MEASURED_ONLY in the master-only §3 rule, r3c2_lane_tools.py, and the interpretation protocol. Disposition: the principal's — §10.15 records this rename as "Not authorized and therefore NOT done"; I do not count a ruled item against the gate.

======================================================================
7. STALL / EXECUTABILITY — and Q4
======================================================================
Stall: every loop is bounded — enumeration two reconciliations then CENSUS_DENOMINATOR_DISPUTED; input lists one reconciliation then the same; outcomes one reconciliation then CENSUS_OUTCOME_DISPUTED (V21's added class, present in master §2/§4 and packet §2/§4, precedence third); arithmetic capped at 120.0 s by the pinned wrapper → SYMBOLIC_TIMEOUT → REPRO_NOT_EVALUABLE, one repeat permitted; a missing seed files CENSUS_AUDIT_FAILED rather than hanging.

Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO. Each check and how:
(1) r3c2_ledger_tools.py — exists; sha f9b7d3c8… matches §3, the packet, and R3C2_SEAT_PACKET.sha256 (shasum).
(2) r3c2_lane_tools.py — exists; sha 2aa1ea7d… matches C3/§9 (shasum).
(3) r3c2_timeout.py — exists; sha fbb9bef7… matches §9 and the pin file (shasum).
(4) r3c2_build_seat_packet.py — exists; sha 4ed52d4b… matches C4 and the pin file (shasum); executed, see §4 above.
(5) R3C2_CORPUS_MANIFEST.md — exists; sha 300d4da1… matches §2/packet (shasum).
(6) R3C2_SEAT_PACKET.sha256 — exists; all five pins verify against disk (packet ac5843…, brief 8f853bef…, timeout fbb9…, ledger f9b7…, builder 4ed5…).
(7) r3c2_seat_packet/SEAT_BRIEF.md — exists, digest matches pin (ls/shasum; not opened).
(8) R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists (ls only; opening it is forbidden to seats and outside my read set).
(9) R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md — exists (ls only).
(10) C5 harness — /usr/bin/python3 runs (3.9.6); sympy prints 1.14.0 (run via a script file; the -c form is the seat's to print live).
(11) validate on a good 3-record ledger (PRINTED/CHOSEN, STANDARD, DERIVED) → C3_NO_SUBSTITUTION=PASS, exit 0.
(12) validate negatives, all FAIL exit 1 with named lines: rests_on field planted; BLOCKED carrying a value; STANDARD value off the closed list; DERIVED with no derived_from; a two-record cycle; ORIG_SILENT without origin_search; verbatim not at the cited line.
(13) census: good files → C1_DENOMINATOR_PRINTED=PASS exit 0, declared and recomputed counts printed; `census … final` → PASS; declared-count mismatch → FAIL naming it; PENDING accepted pre-final and rejected in final ("outcome still PENDING in final census").
(14) Seat tool has census and validate only: `merge` and `compute` print usage, exit 2 — as §3's blockquote states.
(15) Lane tool: merge identical ledgers exit 0; compute gives transitive root_origins (DERIVED-from-CHOSEN → CHOSEN) and rests_on=USES_CHOSEN; merge with differing input_id sets exits 1 naming the ids (the CENSUS_DENOMINATOR_DISPUTED trigger); origin disagreement → origin_alt and compute prints the pair [USES_CHOSEN, USES_IMPORTED] marked DISPUTED; derived_from disagreement → PARENTS_DISPUTED computed under both; compute on a ledger arriving with root_origins/rests_on set → REJECT exit 2.
(16) Wrapper: `/usr/bin/python3 r3c2_timeout.py 120.0 -- /usr/bin/true` → exit 0, prints command/stdout/stderr/exit/elapsed; `… 1.0 -- sleep 5` → prints SYMBOLIC_TIMEOUT, exit 124, elapsed 1.004 — exactly §9's description.
(17) C6 sampling formula — stdlib random.Random(int(seed_hex,16)).sample over sorted remaining ids, k = min(max(1, ceil(0.20×N)), R); executable as written, bounded by R.
(18) Rename verification (per the note): `grep -n REPRO_EXACT` on the master returns exactly the 17 lines the audit classifies — 2 rename notices (header line 3; §3 line 118 "(named `REPRO_EXACT` until V21; renamed by the principal's ruling)") and 15 under ## 10 headings (533, 695, 751, 796, 819, 845, 863, 891, 920, 942, 950, 961, 975, 984, 986). GOVERNING REFERENCES TO THE OLD TOKEN: 0. The seat tool, builder and lane tool carry 0 occurrences of the old token; the new token is present in the seat tool's outcome set and the builder's required-content assertion. The audit's disclosure that the interpretation protocol carried the old token 3× and was renamed (its V3) covers a file outside my read set; the file exists and was re-timestamped 23:02.
(19) Every literal command in the packet and master resolves as printed: census/validate/merge/compute invocations, the C5 triple, the shasum calls, the wrapper form, the builder invocation (run from the master's directory in a sandbox copy — first output line C4_PACKET_REDACTED=PASS as §5 predicts).

No described artefact is missing as delivered; no described computation failed to run as written.

R3C2_V21_GATE_COMPLETE
