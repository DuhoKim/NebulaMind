ACCESS_SHA=5cd4e6da543d2c0dd4b301a9ebdbed17336257cd6ed731ae615d97230f14832e
PACKET_SHA=a87af75a2d7d0e4853bf3a0136a16c95d5dae3f92a975a8e5616b9a850626c3d
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=The packet says origin is independently classified, re-classified by the auditor, and used to detect and report origin disagreement; it does not disclose the lane-side rests_on comparison or interpretation mapping.

## Packet-first answers

### Q5

NO. No packet sentence says that any per-claim or study-level outcome is more consequential than another to the lane or to anyone. The packet describes procedural consequences neutrally (for example, “The census does not proceed”), but supplies no hypothesis mapping, preference, or differential substantive stake.

### Q6

From the packet alone I can state only its disclosed checking purpose. The packet says: “Every input's `origin` is classified independently by both seats.” It also says the auditor works while “re-classifying every input's `origin` from the pinned sources,” and: “An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations.” Thus origin is recorded for independent provenance checking and dispute detection. The packet does not say that lane-side code computes `root_origins` or `rests_on`, nor that interpretation reads that result.

### Q3

I infer no hypothesis, comparison model, preferred outcome, or expected empirical pattern. The packet gives a symmetric census question, neutral outcome definitions, provenance categories in non-valuative form, and audit/stop rules. None maps an outcome or origin to support or damage for a hypothesis. The closed cosmological constants reveal subject matter, not a comparison or hoped-for result.

## 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive as filed. Natural factual conditions can co-occur, but the explicit terminal precedence makes the filed result unique. A claim with no stated computational procedure and also missing an input files `REPRO_NO_DERIVATION_STATED`; a claim with a procedure, one cited-but-unmatchable input, and another wholly absent input files `REPRO_BLOCKED`; the named-source/no-named-source distinction itself separates BLOCKED from ABSENT. If no earlier terminal applies, failure to finish or unavailable machinery files `REPRO_NOT_EVALUABLE`; completed arithmetic then files exactly one of the two complementary precision outcomes.

The eight study-level classes are likewise exhaustive and mutually exclusive as filed because §4 supplies a total precedence. A real overlap is a seat control split followed by a denominator disagreement: both factual conditions hold, but `CENSUS_CONTROL_SPLIT` is filed first. A zero-denominator run reaches `CENSUS_PARTIAL`, not vacuous completion. `CENSUS_PARTIAL` is genuinely reachable whenever an included claim files any non-arithmetic outcome, or when the denominator is zero; it is expressly “INCONCLUSIVE.”

### Q1

YES. Under option (c), every declared per-claim outcome and study-level outcome is reachable, while the reproduction question remains decidable.

Hardest traces:

- Printed-but-chosen input: the paper prints `beta = 1/929.25`, says to use it, and calls it adopted. The ledger record is `status=PRINTED`, `origin=CHOSEN`, with `ORIG_CHOICE_STATED` evidence. Arithmetic must consume it. Completed arithmetic files exactly one of `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`; lane computation gives root origin `CHOSEN` and exactly one `rests_on=USES_CHOSEN`.
- Stated fitted parameter: a printed best-fit `a=2.1` is `status=PRINTED`, `origin=FITTED`, with `ORIG_FIT_STATED`. It is consumed and files exactly one arithmetic outcome; its root yields `rests_on=USES_FITTED`.
- Imported value: a claiming paper cites a named enumerable pinned source and the value machine-matches at the cited line. The record is `status=PRINTED`, `origin=IMPORTED`, `ORIG_CITATION`; it is consumed and yields exactly one arithmetic outcome and `rests_on=USES_IMPORTED`. If the source is outside the enumerable manifest or the value fails the cited-line match, the record is `status=BLOCKED`, no value is consumed, and the unique outcome is `REPRO_BLOCKED`.

The provenance tally does not gate arithmetic admissibility, so reachability is preserved without making the reproduction verdict depend on a subjective provenance ranking.

## 2. Controls

No control can pass by a bare assertion. C0 requires an exhibition table plus independent author and verifier; C1–C3 require printed script runs and exit zero; C4 requires the builder result and dispatch/pin checks while honestly labeling seat isolation as self-report; C5 requires live outputs; C5b requires the classified path list; C6 requires a printed audit JSON with no mismatch or incompleteness. Every control names an exact `PASS|FAIL|NOT_RUN` code, and the document states: “Controls in an unreached limb are `NOT_RUN`, never passes.”

### Q2

YES, subject to the expressly stated human-audit floor. Test record: suppose the source line reads “We adopt H0 = 67.4 from Planck (2018),” but a seat records `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and quotes that line. This is plausible and a substring check alone may accept it. The operative precedence says a sentence naming an external source is `ORIG_CITATION` and therefore `IMPORTED`. The second seat independently classifies the input, producing an origin disagreement, and C6 re-classifies it from the pinned source; a mismatch prevents C6 PASS and leads to the applicable dispute or audit-failure path. The master honestly says that if every reader makes the same misclassification, it can stand. Thus provenance is checkable rather than merely asserted, without claiming infallibility. `root_origins` and `rests_on` are script-computed and seat-authored versions are rejected.

## 3. Circularity

SOUND. Inclusion uses a content-only operational definition, records both inclusions and exclusions, requires two independent seats, and stops on surviving disagreement. A contrary quantitative claim cannot quietly disappear: every candidate has a disposition, every exclusion has a closed kind, counts are recomputed, and C6 audits the complete candidate and exclusion ledgers against all pinned sources. Origin classification is evidence-bound, independently repeated, and audit-reclassified. The seat packet contains neither hypothesis mapping nor comparison preference. Interpretation occurs only after the separately receipted protocol and sealed tally, so the expectation cannot select evidence or retrospectively rewrite the mapping.

## 4. The blind

SOUND within its stated limit. The builder is specified to strip whole sections and marked spans, then refuse to write a packet if any enumerated forbidden string survives. The packet inspected here contains no hypothesis, comparison model, preferred result, or consequence mapping. C4 does not pretend the filesystem enforces non-access: it expressly says the copied directory and printed list are procedural evidence and cannot prove that a seat avoided an absolute path or lacked prior exposure. That limitation is honest.

## 5. The seal

SOUND. Receipt P binds the protocol hash and commit id with an external custodian before limb A begins. Receipt T binds the one tally commit and its named constituent files after computation but before the protocol is opened. Independent re-hashing checks both receipts and all four values; missing or mismatched custody evidence files `CENSUS_AUDIT_FAILED` and leaves interpretation `NOT_RUN`. The document correctly limits the claim to existence of the two records in their stated forms before comparison, not the state of the author's mind.

## 6. Fairness

SOUND. The negative arithmetic outcome mandates “unreproduced from the stated inputs,” not “error.” No operative class name or clause asserts misconduct or more than its predicate. `REPRO_WITHIN_STATED_PRECISION` accurately names its tolerance rule, and `CENSUS_PARTIAL` is explicitly labeled inconclusive rather than adverse.

Rename check: `grep -n REPRO_EXACT R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md` returned only the §3 rename notice and §10 history. There is no governing old-token reference.

## 7. Stall / executability

### Q4

The operative workflow is executable as specified, with one delivered-artifact repair required.

Checked without opening files beyond the permitted packet, master, and attempted rename audit:

- Master and packet: readable; hashes are reported above.
- `/usr/bin/python3`: exists and is executable; live commands returned Python 3.9.6, SymPy 1.14.0, and interpreter sha256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`.
- Existence checks passed for `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_lane_tools.py`, `r3c2_timeout.py`, `r3c2_build_seat_packet.py`, `R3C2_SEAT_PACKET.sha256`, `r3c2_seat_packet/SEAT_BRIEF.md`, the seat packet, `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, and `R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md`.
- The literal harness commands were executed successfully. The builder, ledger, lane, and timeout commands were checked for named-program/path availability but not executed, because doing so would open additional prohibited files or require future-run ledger operands. Their placeholders are all assigned to run-produced files, and the master specifies the working directory, resolved command, outputs, stderr, and exit handling.
- Future artefacts such as candidate ledgers, merged ledgers, receipts, `C6_AUDIT.json`, and the dated check sheet are outputs, not falsely claimed present inputs.

Finding 7.1 — missing delivered rename audit (COSMETIC to the preregistration design, but a required packet repair).

Quoted promise from the gate instruction: “A lane audit of every surviving old-token line is at R3C2_V22_RENAME_AUDIT_20260905.md in the same directory; you may open it AFTER the master to verify the rename.”

Defect: both `shasum` and `sed` returned “No such file or directory.” The described artefact does not exist as delivered. This changes no filed class, control passability, outcome reachability, conclusion, or tested value; the direct grep independently establishes that all old-token hits are confined to the permitted rename notice and §10 history.

Exact replacement: “The rename is verified directly by `grep -n REPRO_EXACT R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md`; every hit must lie in the §3 rename notice or under a `## 10` heading.” Alternatively, deliver `R3C2_V22_RENAME_AUDIT_20260905.md` at the promised path before dispatch.

R3C2_V22_GATE_COMPLETE
