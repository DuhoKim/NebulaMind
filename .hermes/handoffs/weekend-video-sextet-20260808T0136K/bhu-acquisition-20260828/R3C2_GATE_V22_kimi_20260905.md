ACCESS_SHA=5cd4e6da543d2c0dd4b301a9ebdbed17336257cd6ed731ae615d97230f14832e
PACKET_SHA=a87af75a2d7d0e4853bf3a0136a16c95d5dae3f92a975a8e5616b9a850626c3d
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=packet alone let me infer the domain (cosmology corpus on the Planck 2018 baseline, from the closed STANDARD table), that REPRO_FAILED is an anticipated reportable outcome (the fairness wording exists only because failures will be phrased publicly), that provenance is heavily policed (double-classified origin, tie-break order, audit re-classification, >10% dispute stop), and a conservative inconclusive-bias (CENSUS_PARTIAL takes precedence over CENSUS_COMPLETE) — no directional expectation, comparison model, or preferred outcome inferable; all of this sits at or below the accepted floor ruled in C4
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE — the packet shows only handling mechanics ("Every input's `origin` is classified independently by both seats", C3; "the auditor re-classifies the record from the pinned sources", C3; a >10% disagreement stops the census under CENSUS_ORIGIN_DISPUTED, §4.7); no sentence states any tally, subgroup, comparison, or conclusion computed from `origin`, and §3 states "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" — what the recorded origin is FOR cannot be stated from the packet

Seat: kimi. Date: 2026-09-05. Master gated: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V22). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md.
Ordering: ACCESS_SHA printed first; packet read and Q5/Q6/Q3 answered from the packet alone BEFORE the master was opened; master read second; tooling checks (Q4) last. §10 treated as history, binding nothing; none of its narrated findings is reported here as mine.
Scope disclosure: per Q4's direct order ("every named script, every file the document says exists... name each thing you checked and how") I opened, beyond the two named files, only the four pinned scripts (read + executed on synthetic fixtures in /tmp), the pin file R3C2_SEAT_PACKET.sha256, and existence/digest checks on the other named artefacts. No gate file, no corpus source, no interpretation-protocol content, no SEAT_BRIEF content, and no §10 narrative was read. Packet-first answers were complete before any of this.
Dispatch-brief discrepancy (process note, not a master defect): the brief pointed me to R3C2_V22_RENAME_AUDIT_20260905.md; that file does not exist. The master itself never names it (its §10.16 history names R3C2_V21_RENAME_AUDIT_20260905.md, which exists). The rename condition was therefore verified directly: `grep -n REPRO_EXACT` on the master returns the line-118 rename notice ("*(named `REPRO_EXACT` until V21; renamed by the principal's ruling)*") and hits at lines 516–987, every one under a "## 10" heading. Governing-region hits for all retired tokens (REPRO_EXACT, REPRO_AFTER_CHOICE, NOT_ATTEMPTED, R3C_NO_CLASS) are exactly the line-118 rename notice and three historical SEAT-REDACT notes (lines 126, 170, 205). No governing reference to any old token. The rename is clean.

══════════════════════════════════════════════
1. OUTCOME CLASSES — and Q1
══════════════════════════════════════════════
Exhaustive and mutually exclusive: YES, as written under option (c).

Per-claim partition. Every included claim walks §2: no equation/procedure stated → REPRO_NO_DERIVATION_STATED (the "named but not specified" sentence closes the loophole of a prose provenance); an input traced to a named source that is unpinned or unmatched → REPRO_BLOCKED; an input neither printed nor traced → REPRO_INPUT_ABSENT; the attempt hits the cap or missing machinery → REPRO_NOT_EVALUABLE (SYMBOLIC_TIMEOUT / MACHINERY_UNAVAILABLE, point reached printed); the attempt completes → exactly one arithmetic-group outcome via the stated-precision rule (stated uncertainty: |reproduced − printed| ≤ stated uncertainty once, asymmetric handled by side; none stated: round at printed precision, half away from zero). I could not construct a real claim fitting two per-claim outcomes — every co-occurrence I built is resolved by the total precedence (REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then arithmetic group): blocked+absent → REPRO_BLOCKED; no-derivation+absent → REPRO_NO_DERIVATION_STATED (there is nothing to attempt, so no input list exists); a blocked input "ends that claim's attempt" (§2), so it precedes any timeout. Nor one fitting none: the six classes partition the attempt tree.

Study-level partition. Eight classes with a stated total order (R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT, CENSUS_DENOMINATOR_DISPUTED, CENSUS_OUTCOME_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL, CENSUS_COMPLETE — chronological, controls before enumeration before origin before audit). A tally fitting two is filed by the order (audit failure during a partial census → CENSUS_AUDIT_FAILED). A tally fitting none: not constructible — with a denominator and an audit disposition, at least one of PARTIAL/COMPLETE/AUDIT_FAILED/DISPUTED always holds; zero denominator is explicitly routed to CENSUS_PARTIAL ("no census is complete over nothing").

INCONCLUSIVE is genuinely reachable: one ABSENT input anywhere in the corpus files REPRO_INPUT_ABSENT on its claim → at least one non-arithmetic outcome → CENSUS_PARTIAL. No definitional heroics needed.

Q1 — does §3 under option (c) make every declared outcome reachable AND keep the reproduction question decidable? YES. The tension that failed four rounds is removed structurally: the outcome path carries no admissibility judgement at all — arithmetic consumes by STATUS alone (PRINTED or STANDARD), and provenance is recorded separately and computed lane-side after both seats exit. Hardest cases traced:
 (a) printed-but-chosen input — "we adopt n_s = 0.96" (not verbatim on the closed list): §2.3 routes PRINTED (the STANDARD/PRINTED rule keeps both seats on one route, outcome-identical); origin CHOSEN via ORIG_CHOICE_STATED; consumed; exactly one arithmetic-group outcome by the stated-precision rule; lane-side rests_on = USES_CHOSEN (verified live, T12 alt-branch).
 (b) a fitted parameter the paper states — "The fit gives n_s = 0.96": PRINTED/FITTED via ORIG_FIT_STATED; consumed; one arithmetic outcome; rests_on = USES_FITTED (verified live, T11 claim c2 root set {FITTED, UNDECLARED} → USES_UNDECLARED by the severity order — the FITTED root is correctly present in root_origins).
 (c) an imported value — "H0 = 67.4 ... from Planck (2018)": machine-match at the cited line of a pinned enumerable text → PRINTED/IMPORTED, consumed, one arithmetic outcome, rests_on USES_IMPORTED (T11 claim c1); no match or unpinned source → REPRO_BLOCKED, recorded BLOCKED/IMPORTED with no value, never consumed (validate enforces exactly this shape, T4–T5 vs source).
One residual considered and discharged: two seats could split on the FACT of whether a source is named (REPRO_BLOCKED vs REPRO_INPUT_ABSENT), where the §2.5/§4.6 reconciliation standard ("against the printed numeral and the stated-precision rule") is tailored to arithmetic splits. The failure mode is not a wrong tally — a surviving split files the declared stop class CENSUS_OUTCOME_DISPUTED with both outcomes, both number pairs, and the step each reached, and the underlying origin split is carried as origin_alt and counted toward CENSUS_ORIGIN_DISPUTED. Fail-closed; not a defect.

══════════════════════════════════════════════
2. CONTROLS — and Q2
══════════════════════════════════════════════
No control is passable by assertion. Each of C0–C6 names an exact code with PASS|FAIL|NOT_RUN, and each binds PASS to a printed artefact: C0 to the independently authored and independently verified exhibition table (lane owner checks coverage only); C1/C2/C3 to printed script runs with exit 0 — "a token asserted without the printed run is FAIL" (C3); C4/C5b to the printed path list, openly labelled self-reported ("makes no claim that the list is complete"); C5 to live command output ("a transcribed value in place of live output is FAIL"); C6 to the printed C6_AUDIT.json — "a token without the artefact is FAIL". Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes" plus §4's "Once a stop class applies, later limbs are unreached and their controls are NOT_RUN." Handled.

Q2 — is the provenance field checkable rather than asserted? YES, with the one gap disclosed in the document itself and backstopped by two independent readers. Live test (T8): I wrote a ledger record for the paper line "We take H0 = 67.4 km/s/Mpc from Planck (2018) and use G = 6.67430e-11." filed as:
 {"claim_id":"c1","input_id":"i1","symbol":"H0","status":"PRINTED","origin":"CHOSEN",
  "origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"paper.md","source_line":1,"verbatim":"from Planck (2018)"},
  "derived_from":[],"value":"67.4","source_file":"paper.md","source_line":1}
This origin is wrong but plausible: the sentence names an external source, so the C3 precedence (ORIG_CITATION first — "a sentence that names an external source for the value is a citation whatever else it says") requires IMPORTED. Result: `validate` PASSES it (exit 0) — the machine verifies the quotation is a real substring of the cited line, the value machine-matches, and the reason_code→origin pair is consistent; it does not enforce the cross-code precedence. The catch, as written and verified live: (i) the second seat's independent classification — merging the wrong ledger against a correct one carries "origin_alt": "CHOSEN" (T10) and counts the disagreement; above 10% of included claims the study stops under CENSUS_ORIGIN_DISPUTED with both classifications and both quotations listed; (ii) the C6 auditor re-classifies every audited input's origin from the pinned sources and prints MATCH/MISMATCH — a MISMATCH means C6_AUDIT_SAMPLE≠PASS → CENSUS_AUDIT_FAILED; at or below threshold the claim's rests_on is computed under both classifications and marked DISPUTED (T12: rests_on=['USES_IMPORTED','USES_CHOSEN'], DISPUTED). The document states this floor verbatim in C3: a precedence-misapplied code "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." The mechanism as written catches the record everywhere except identical misclassification by all readers — and says so. ORIG_SILENT is likewise checkable: the printed origin_search {query, files, matches} is the adequacy evidence, "a search of one query with no variants is not adequate", and validate hard-requires the field (T7 FAIL).

══════════════════════════════════════════════
3. CIRCULARITY
══════════════════════════════════════════════
The lane's expectation cannot reach the evidence through any of the four channels. Inclusion: the §1 rule seats apply ("a passage printing a numeral the paper asserts as a result of its own", with five enumerated excluded kinds) contains no pattern content — verified directly on the packet (no forbidden string survives; see §4 below), and inclusion is double-keyed with tolerance zero and a stop class. Exclusion: kinds are enumerated, not tasted; every exclusion is ledgered with file/line/numeral/kind, machine-reconciled against candidates (T1–T3: included-AND-excluded and excluded-without-row are hard FAILs), and the auditor re-audits the FULL candidate and exclusion ledgers against every pinned source before any sampling — a contrary claim cannot be quietly excluded without a printed, audited row. Origin classification: evidence-cited with a fixed specificity tie-break, double-keyed, disputes reported never reconciled ("reconciling it would destroy it" — in the master only), >10% stops the census. Interpretation: lives in R3C2_INTERPRETATION_PROTOCOL_20260904.md, which no seat may open, receipted (P) before limb A, so the mapping is frozen before evidence exists; the tally is receipted (T) before the protocol is opened, so evidence is frozen before the mapping is applied to it. The packet leaks only domain and conservatism (LEAK line, at the accepted floor); it never maps any outcome to any conclusion.

══════════════════════════════════════════════
4. THE BLIND
══════════════════════════════════════════════
Machine-asserted as claimed — verified live, end to end (T18). The pinned builder (sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3, matches C4), run in a scratch copy, printed C4_PACKET_REDACTED=PASS as its first line, dropped exactly sections [0., 7., 8., 10.] (all 10.x included), stripped 50 inline SEAT-REDACT spans, and reproduced the committed packet BYTE-IDENTICALLY: built packet sha256 a87af75a2d7d0e4853bf3a0136a16c95d5dae3f92a975a8e5616b9a850626c3d = the pin in R3C2_SEAT_PACKET.sha256 = my independently computed PACKET_SHA; its printed master sha256 equals ACCESS_SHA. The builder fails closed: unclosed/stray redact markers → not written; any REQUIRED seat-facing string missing (all control codes, all six outcome tokens, origin_evidence/derived_from/origin_search, the path-printing instruction) → not written; any FORBIDDEN string surviving in packet OR brief → C4_PACKET_REDACTED=FAIL, not written; "a dispatch built by any other bytes files C4_PACKET_REDACTED=FAIL". My independent grep of the delivered packet confirms: no SEAT-REDACT markers, no forbidden word (pattern, ΛCDM/LCDM, hypothesis, refute, stake, prior, expectation, engine names, custody names, gate history — none). The forbidden list covers content (pattern name/topic, comparison model, gate history, engines, custody chain) AND consequence words AND the comparison vocabulary (rests_on, root_origins, two tallies, USES_, severity) — the packet therefore reveals that provenance is carefully recorded and independently re-checked, but not that it is combined with the tally, what the combination is called, or how the interpretation reads it; that floor is the principal's ruled acceptance and is stated in C4. The C4 mechanism prevents furnishing the pattern record in the dispatched copy and machine-guarantees the packet/brief text; its limits are stated honestly and exactly: the copy discipline "is procedural, not enforced by the filesystem", the path list is self-reported and possibly incomplete, and "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The document claims exactly what the mechanism delivers and labels the rest.

══════════════════════════════════════════════
5. THE SEAL (§7)
══════════════════════════════════════════════
The receipted relay binds what it claims, before the step it must precede. (P) the interpretation protocol's hash and commit id go to Blanc (outside the lane, reporting to Duho) BEFORE limb A — "limb A does not begin without receipt P" — so the mapping is frozen before any evidence exists. (T) one commit containing the merged candidate file, the exclusion ledger, and the merged input ledger with its compute output is hashed and sent after the tally commit and BEFORE the protocol is opened; the receipt names every file in the commit; "the interpretation protocol is not opened without both receipts." Ordering is correct on both sides. After opening, Blanc independently re-hashes tally and protocol and verifies all four values against the two receipts; the interpretation report must print both receipts and the four verified values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves interpretation NOT_RUN, and voids the comparison — and §4.3's definition names this case, as §7 claims ("§4, whose definition now names this case" — verified: §4.3 reads "**or the receipt verification of the seal fails**"). Custody is not overclaimed: "a local commit in Tori's own custody can be amended or reset, so it is tamper evidence, not an external custodian"; "A send with no recorded receipt is an assertion of custody, not custody"; and the stated limit — "It does not prove Tori had not already guessed the answer ... It bounds the record, not the mind" — is the honest ceiling.

══════════════════════════════════════════════
6. FAIRNESS
══════════════════════════════════════════════
Every negative outcome is worded against the attempt, never against the paper: REPRO_FAILED carries "**Wording: "unreproduced from the stated inputs," not "error."**" in master and packet alike. Class names assert no more than their definitions: REPRO_WITHIN_STATED_PRECISION names exactly its test (the rename repaired the old name's overclaim); REPRO_BLOCKED and REPRO_INPUT_ABSENT name input conditions; REPRO_NO_DERIVATION_STATED names the absence of a procedure; REPRO_NOT_EVALUABLE names the lane's limits, not the claim. CENSUS_PARTIAL is labelled "INCONCLUSIVE" in its definition. The lane-side rests_on vocabulary (DERIVED_ONLY, USES_*) names consumption, is computed by script, never reaches seats, and is gated by the receipted protocol before any interpretation touches it. §11: "No tier, warrant token, standing or stamp moves." No clause asserts more than its definition.

══════════════════════════════════════════════
7. STALL / EXECUTABILITY — and Q4
══════════════════════════════════════════════
Stall guards: the 120.0 s monotonic wrapper with SYMBOLIC_TIMEOUT and exit 124 (T16 live: deadline hit at 0.504 s, exit 124; T15 live: fast child passes through with command, streams, exit, elapsed printed); MACHINERY_UNAVAILABLE; one repeat permitted (meaningful only for REPRO_NOT_EVALUABLE), consistent with C1's attempts ∈ {0,1,2}; a declared stop class at every disagreement point; NOT_RUN discipline; third-seat dispatch declared a lane-side administrative action "not claimed executable from the packet" — honest.

Q4 — can anything described NOT be executed as delivered? NO. Each thing checked, and how (all live on synthetic fixtures in /tmp unless stated; no corpus file opened):
 - /usr/bin/python3 — exists; `--version` prints Python 3.9.6; sympy imports and prints 1.14.0 (run via a script file — the literal `-c` form is policy-blocked in this referee session; the interpreter and library C5 pins are present); interpreter hashes (b8763cf2…).
 - r3c2_ledger_tools.py — exists; sha256 f9b7d3c818647e6e3e10e19013db3a042247fc141ee01282b8928de06372c9ac matches §3, the packet, and the pin file. Source + runs: exposes exactly `census [final]` and `validate` (usage error exit 2); has NO merge/compute, as the master states. T1: valid pre-final census PASS exit 0, declared vs recomputed counts printed. T2: PENDING in `final` → FAIL exit 1, named. T3: wrong declared_attempt_count → FAIL exit 1, named. T4/T5: valid ledgers PASS. T6: seat ledger carrying rests_on → FAIL "carries a field outside the schema" (the C3 lane-side clause works). T7: ORIG_SILENT without origin_search → FAIL. T8: citation sentence filed CHOSEN → PASS (the disclosed machine floor, §2 above). Source-verified: PRINTED value machine-match at cited line, verbatim substring check, STANDARD closed-list string compare (list matches the C3 table verbatim, e.g. H0="67.36", age_Gyr="13.797"), derived_from existence/acyclicity/DERIVED-has-parents, ABSENT/BLOCKED carry no value, BLOCKED must be IMPORTED/ORIG_CITATION.
 - r3c2_lane_tools.py — exists; sha256 2aa1ea7dc6f425aad13e7fffe5cdc5815865a74165fbc380f3ba81b3f8fa8a94 matches §5/§9. T9: merge of identical ledgers exit 0. T10: origin split carried as origin_alt. T11: compute prints rests_on and root_origins; severity order USES_UNDECLARED > USES_IMPORTED > USES_FITTED > USES_CHOSEN and DERIVED_ONLY behave as written. T12: disputed origin → pair marked DISPUTED. T13: ledger arriving with root_origins/rests_on set → REJECT exit 2. T14: input_id set mismatch → exit 1 (→ the one reconciliation, then CENSUS_DENOMINATOR_DISPUTED). Fields the master says the scripts compute (root_origins, rests_on, origin_alt, origin_evidence_alt, derived_from_alt, PARENTS_DISPUTED, DISPUTED) all observed in output. The NOT_COMPUTED row for claims with no ledger record is a tally-reporting step downstream of compute (compute builds claims from records); consistent as written, not separately executable — noted, not a defect.
 - r3c2_timeout.py — exists; sha256 fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 matches §9; behavior per T15/T16 exactly as §9 describes.
 - r3c2_build_seat_packet.py — exists; sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3 matches C4; T18 reproduces the pinned packet byte-identically with PASS first line (§4 above).
 - R3C2_CORPUS_MANIFEST.md — exists; sha256 300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24 matches §2 (existence+digest only; contents not opened).
 - R3C2_SEAT_PACKET.sha256 — exists; pins the packet (a87af75a…, = PACKET_SHA), SEAT_BRIEF.md, wrapper, ledger tools, builder.
 - r3c2_seat_packet/SEAT_BRIEF.md — exists, pinned, and asserted clean by the builder (T18's PASS covers it).
 - r3c2_seat_packet/R3C2_SEAT_PACKET.md — exists at the committed path C4 names.
 - R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists. §7 states receipt P as a precondition of limb A; the header states NOT FROZEN/NOT RUN, so nothing claims the receipt exists yet — consistent.
 - R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md — exists (C4 reference).
 - C6 sampling — `random.Random(int(seed_hex,16)).sample(sorted(set(included)−set(arithmetic)), min(max(1,ceil(0.20×N)),R))` with R=0 handled; standard library, executable as written; the missing-seed route files CENSUS_AUDIT_FAILED with the missing seed named.
 - Seat paths — every component C4 lists for the redacted copy directory exists and is digest-pinned; the copy directory itself is a dispatch-time artefact whose pre-launch listing is the lane owner's duty, correctly not claimed to pre-exist.
 Nothing described as delivered failed to execute.

FINDINGS (two, both COSMETIC — each changes no class filed, no control's passability, no reachability, no conclusion, and no tested value):

F1 (COSMETIC) — §4.2 parenthetical attachment.
 Quote: "**at least one included claim carries a non-arithmetic outcome, or the denominator is zero** (`REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`)."
 Defect: the parenthetical defines the first limb's "non-arithmetic outcome" but is attached to the zero-denominator limb, where it cannot literally apply (a zero denominator admits no claims to carry any outcome). A strict reader must reattach it; no filing changes (the four listed tokens are exactly the non-arithmetic set, and §4.1 already routes zero denominator to PARTIAL with the empty enumeration named). Same text rides in the packet §4.
 Exact replacement (master and packet): "**at least one included claim carries a non-arithmetic outcome (`REPRO_NO_DERIVATION_STATED`, `REPRO_INPUT_ABSENT`, `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`), or the denominator is zero**."

F2 (COSMETIC) — r3c2_lane_tools.py cmd_merge duplicated final line.
 Quote (source, end of cmd_merge): the line `pathlib.Path(out).write_text(json.dumps({"records":out_recs},indent=1)); print(f"merged {len(out_recs)} records; origin disagreements={ndis}")` appears twice in immediate succession (the first preceded by a PARENTS_DISPUTED print), so the merged file is written twice with identical bytes and the "merged …" line prints twice (observed in T9/T10 stdout).
 Defect: harmless redundancy in a digest-pinned tool; the printed artefact carries one duplicated line. No behaviour, file content, exit status, class, or tested value changes; the master's description of merge is accurate as written. Repairing it changes the pinned sha256, so it is a re-pin decision for the lane, not a gate blocker.
 Exact replacement: delete the duplicated second occurrence, keeping the single `write_text` + both prints once.

No substantive defect found. The design is internally consistent, every outcome reachable and decidable, every control artefact-bound, the blind machine-asserted and byte-verified, the seal correctly ordered, the wording fair, and everything described executes as delivered.

R3C2_V22_GATE_COMPLETE
