ACCESS_SHA=55b466fadf8ca75f4894f97721da86584057ddc4d15c15ca1739c67ff6333c6f
PACKET_SHA=a7353e9199e14c2e711ad20a32dd100d6d62f126d182eeb30f33ff91e41ce10b
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=The packet says origin is independently re-classified from pinned sources and compared against the sealed record; disagreements above 10% produce CENSUS_ORIGIN_DISPUTED.

# 1. Outcome classes

The six per-claim outcomes are exhaustive and mutually exclusive after the explicit precedence rule. The arithmetic group is binary once a recipe exists, its stated inputs are available, and the attempt is executable: the reproduced value either satisfies the stated-precision test or it does not. The four earlier terminal conditions cover no stated procedure, named-source blockage, an input neither printed nor sourced, and timeout/unavailable machinery. Their stated order resolves co-occurrence.

Hard overlap test: a claim has a named but unspecified procedure, one input cited to an unpinned source, and another input absent. It initially fits three predicates, but the sentence “Where more than one terminal condition holds, file the first in this order: `REPRO_NO_DERIVATION_STATED`, `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, then the arithmetic group” assigns exactly `REPRO_NO_DERIVATION_STATED`.

No per-claim gap found. A real-form counterexample candidate such as “we obtain 4.2” with no equation or executable operations is included under §1 and files `REPRO_NO_DERIVATION_STATED`; it does not fall through merely because its inputs cannot be listed.

The eight study-level classes are also total under their precedence. A tally with a failed early control and a later audit mismatch files `R3C2_NO_CLASS`; a one-seat pass/one-seat fail files `CENSUS_CONTROL_SPLIT`; enumeration/input-list disagreement precedes outcome disagreement; origin disagreement above threshold precedes audit failure; and only after those stop/failure classes can `CENSUS_PARTIAL` or `CENSUS_COMPLETE` apply. A zero denominator is assigned to `CENSUS_PARTIAL`, preventing vacuous completeness.

`INCONCLUSIVE` is genuinely reachable: one included claim with an executable recipe but an input neither printed nor traced to a named source files `REPRO_INPUT_ABSENT`; therefore the tally meets `CENSUS_PARTIAL`, explicitly labelled “INCONCLUSIVE.” It is also reachable at denominator zero.

## Q1. Does §3 under option (c) make every outcome reachable and keep reproduction decidable?

Yes. The hardest provenance cases remain decidable because consumption depends on `status`, while provenance is a separate recorded/computed tally:

- Printed-but-chosen input: `status=PRINTED`, `origin=CHOSEN`, with `ORIG_CHOICE_STATED` quotation. Arithmetic consumes it. The claim files exactly `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`; `compute` follows the input to root origin `CHOSEN` and assigns `rests_on=USES_CHOSEN`.
- A fitted parameter stated by the paper: `status=PRINTED`, `origin=FITTED`, with `ORIG_FIT_STATED`. It is consumed and reaches exactly one arithmetic outcome; its root produces `rests_on=USES_FITTED`.
- An imported value found at the cited line of a pinned enumerable text: `status=PRINTED`, `origin=IMPORTED`, `ORIG_CITATION`, source file/line and machine-matched value. It is consumed and reaches one arithmetic outcome; its root produces `rests_on=USES_IMPORTED`.
- An imported value not machine-matched at the cited line, or sourced outside the enumerable manifest: `status=BLOCKED`, `origin=IMPORTED`, no value. It is not consumed and files exactly `REPRO_BLOCKED` by precedence; `compute` can still derive the provenance tally from the record.

Thus a chosen or fitted value does not make reproduction undecidable and is not silently converted into a negative reproduction result. Every declared per-claim outcome and study class has a concrete path.

# 2. Controls

Every control has an exact `PASS|FAIL|NOT_RUN` code: `C0_REACHABILITY`, `C1_DENOMINATOR_PRINTED`, `C2_INPUT_LEDGER`, `C3_NO_SUBSTITUTION`, `C4_SEAT_ISOLATION`, `C4_PACKET_REDACTED`, `C5_HARNESS_PINNED`, `C5B_NO_CROSS_LANE`, and `C6_AUDIT_SAMPLE`. The document states: “Controls in an unreached limb are `NOT_RUN`, never passes.”

C0 requires a printed exhibition table and independent verification. C1 requires printed declared counts and validator runs. C2/C3 require the resolved command and complete output/status. C5 requires live command output. C6 requires the printed audit JSON. C4/C5b are necessarily self-reported path lists, but their PASS claims are deliberately narrow: they certify only the printed list and do not claim completeness or actual non-access. Therefore they do not pass a stronger claim by bare assertion.

## Q2. Is provenance checkable rather than asserted?

Yes, with an honestly stated human-audit floor. Test record: the source line says “We adopt H0 = 67.4 from Planck (2018),” but a seat records `origin=CHOSEN`, `reason_code=ORIG_CHOICE_STATED`, and quotes that line. This is plausible because “adopt” signals choice, and the validator can verify the quotation and mapping. It is nevertheless wrong under the explicit tie-break: a named external source makes `ORIG_CITATION`/`IMPORTED` win.

The validator alone may not catch that semantic misclassification. The second seat independently classifies the record; a disagreement is retained rather than reconciled. C6 then “re-classif[ies] every input's `origin` from the pinned sources” and records `MATCH` or `MISMATCH` against the sealed record. That independent reclassification catches the planted error and can yield `CENSUS_ORIGIN_DISPUTED` or `CENSUS_AUDIT_FAILED` as defined. If all human readers make the identical semantic mistake, it can stand; the master states that limit rather than calling the classification fully machine-proved. `root_origins` and `rests_on` are script-computed, so seats cannot assert those downstream fields.

# 3. Circularity

No operative route lets the lane's hoped-for pattern affect reproduction evidence. The packet contains no hypothesis, comparison model, preferred result, magnitude/shape screen, interpretation mapping, `rests_on` rule, or severity order. Inclusion is governed by a content-neutral numeral/result definition, all exclusions are ledgered, and disagreement stops under `CENSUS_DENOMINATOR_DISPUTED`; a contrary included claim cannot be quietly dropped without appearing in the candidate/exclusion audit.

Origin classification can affect the separate provenance tally and origin-dispute class, but not whether a `PRINTED`/`STANDARD` input is consumed or whether arithmetic reproduces the number. Section 7 fixes the interpretation protocol before limb A and delays opening it until the tally is receipted. No circularity defect found.

# 4. The blind

Packet-first Q5: no sentence says any outcome is more consequential than another. The packet exposes procedural stopping consequences but no empirical stake or preferred tally.

Packet-first Q6: the purpose that can be stated is integrity checking. The packet says the auditor re-classifies origin from pinned sources and compares it with the sealed record; disagreements above 10% can file `CENSUS_ORIGIN_DISPUTED`. It does not disclose the `rests_on` computation, the severity order, or how interpretation uses provenance.

Packet-first Q3: no hypothesis, comparison model, preferred outcome, or expected pattern can be inferred. The question is neutral and no packet sentence maps any result to a scientific consequence.

The C4 redaction is machine-asserted only to its claimed extent: the pinned builder strips specified sections/spans and rejects enumerated forbidden strings. The master explicitly says that this list “does not establish that every consequence-bearing word is gone.” It also states that filesystem access is not denied, path reporting is incomplete, actual non-access is not proved, and prior exposure cannot be excluded. Thus C4 does not prevent all possible leakage, but it does not claim to; the packet test confirms the intended hypothesis/comparison/stake content is absent. No defect found.

# 5. The seal

Receipt P binds the interpretation protocol's hash and commit id before limb A begins. Receipt T binds the single tally commit, its hash, and every named tally file after computation but before the protocol is opened. The external custodian acknowledges and timestamps both; later re-hashing verifies four values, and any missing receipt or mismatch files `CENSUS_AUDIT_FAILED` with interpretation `NOT_RUN`. This binds what is claimed at the required times. The text correctly limits the seal to proving existence of the two artefacts in stated forms, not the lane owner's mental ignorance. No defect found.

# 6. Fairness

The negative arithmetic outcome is expressly worded “unreproduced from the stated inputs,” not “error.” `REPRO_BLOCKED`, `REPRO_NOT_EVALUABLE`, `REPRO_NO_DERIVATION_STATED`, and `REPRO_INPUT_ABSENT` likewise describe limits of the delivered recipe/input/machinery rather than misconduct. `REPRO_WITHIN_STATED_PRECISION` now matches its test. The old token `REPRO_EXACT` has no governing use: grep found one operative rename notice at line 118 and all other hits under §10 history; the permitted rename audit likewise reports zero governing references.

One repair is required.

Quoted defect: “`DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`”.

Defect: the category name asserts derivation only, while its definition also includes standard and measured roots. This overstates what the class means. It affects the name filed in the second tally, so it is not marked COSMETIC under the supplied test, although it does not alter membership, any reproduction outcome, or any study-level outcome.

Exact replacement: replace `DERIVED_ONLY` with `DERIVED_STANDARD_OR_MEASURED_ONLY` everywhere it is a live output token, including the lane-side script and interpretation protocol, leaving the membership rule exactly “every root origin is `DERIVED`, `STANDARD` or `MEASURED`.” Add a historical rename notice only in §10.

# 7. Stall / executability

## Q4. Can anything described not be executed as delivered?

No missing delivered artefact was found within the permitted checks. I checked existence, without opening their contents, for:

- `R3C2_CORPUS_MANIFEST.md`
- `r3c2_ledger_tools.py`
- `r3c2_lane_tools.py`
- `r3c2_timeout.py`
- `r3c2_build_seat_packet.py`
- `R3C2_SEAT_PACKET.sha256`
- `r3c2_seat_packet/R3C2_SEAT_PACKET.md`
- `r3c2_seat_packet/SEAT_BRIEF.md`
- `R3C2_INTERPRETATION_PROTOCOL_20260904.md`

All exist. `C6_AUDIT.json`, candidate/exclusion ledgers, seat ledgers, merged/output ledgers, dispatch record, receipts, and the dated check sheet are run products rather than claimed pre-existing inputs; their absence before execution is not a delivery defect.

I executed the three literal C5 commands. They succeeded and printed Python `3.9.6`, SymPy `1.14.0`, and `/usr/bin/python3` sha256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`.

I checked every `REPRO_EXACT` hit in the master with the prescribed grep and read the expressly permitted `R3C2_V21_RENAME_AUDIT_20260905.md`; no governing old-token reference remains.

The ledger `census`, `validate`, lane `merge`/`compute`, timeout-wrapper, and builder commands are literal and have identified interpreters/scripts. Their data-path placeholders are intentionally resolved to run-produced seat files; running them now without those preregistered products would test fabricated inputs, not delivery. The master specifies each script-produced field (`origin_alt`, `origin_evidence_alt`, `root_origins`, `rests_on`), output path, exit behavior, and printed artefact. The auditor-authored `C6_AUDIT.json` has a complete required schema and deterministic sampling expression. No unexecutable described action or missing delivered artefact was found.

R3C2_V23_GATE_COMPLETE
