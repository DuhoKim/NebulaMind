ACCESS_SHA=55b466fadf8ca75f4894f97721da86584057ddc4d15c15ca1739c67ff6333c6f
PACKET_SHA=a7353e9199e14c2e711ad20a32dd100d6d62f126d182eeb30f33ff91e41ce10b
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE

Seat: kimi. Date: 2026-09-06. Gated artifact: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V23), design only.
Packet answered before the master was read (Q5, Q6, Q3 below, in that order, from the packet alone).
Section 10 of the master was treated as history binding nothing; no finding below is taken from it.
Files opened: the packet, the master, and — after the master, as permitted — R3C2_V21_RENAME_AUDIT_20260905.md.
Additionally, for Q4 only: digest prints (shasum) of named artifacts, an ls, a read of the pin file
R3C2_SEAT_PACKET.sha256, execution of the delivered tools on my own synthetic fixtures in /tmp, and
existence-only checks (no content) for R3C2_INTERPRETATION_PROTOCOL_20260904.md and
R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md. The manifest's content was not opened; its digest binds it.

=====================================================================
1. OUTCOME CLASSES (Q1 answered here)
=====================================================================

Per-claim (§3): six outcomes. Exhaustive — the case analysis is total: no equation/procedure stated
(incl. named-but-unspecified) -> REPRO_NO_DERIVATION_STATED; equation stated, an input neither printed
nor traced -> REPRO_INPUT_ABSENT; an input traced to a named source that is not an enumerable manifest
text or does not machine-match at the cited line -> REPRO_BLOCKED; inputs available, attempt exceeds
the cap or machinery -> REPRO_NOT_EVALUABLE; attempt completes -> REPRO_WITHIN_STATED_PRECISION or
REPRO_FAILED. The named-source test keeps BLOCKED and INPUT_ABSENT disjoint. Mutually exclusive as
filed, via the stated total precedence: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT,
REPRO_NOT_EVALUABLE, then the arithmetic group.

A real claim fitting TWO per-claim outcomes (constructed): a paper states an equation for its printed
result; input A is not printed but names a source outside the manifest (BLOCKED condition); input B is
neither printed nor traced to any source (ABSENT condition). Both terminal conditions genuinely hold.
The precedence files REPRO_BLOCKED, exactly one outcome; "Encountering one ends that claim's attempt"
makes the same answer mechanically. A real claim fitting NONE: not constructible — every branch of the
case analysis lands in a declared class, and exclusions are ledgered separately, never silently.

Study-level (§4): eight classes with a stated total precedence. A real tally fitting TWO classes
(constructed): one included claim files REPRO_BLOCKED (CENSUS_PARTIAL condition) AND the C6 audit
returns a MISMATCH (CENSUS_AUDIT_FAILED condition). The precedence files CENSUS_AUDIT_FAILED, exactly
one class. INCONCLUSIVE is genuinely reachable: any single non-arithmetic outcome files CENSUS_PARTIAL
("INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE"), and a zero denominator files
CENSUS_PARTIAL with the empty enumeration named — a vacuous CENSUS_COMPLETE is expressly barred.

Q1 — DOES §3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP
THE REPRODUCTION QUESTION DECIDABLE? YES. The hardest cases, each traced to exactly one outcome and one
rests_on value:
  (a) Printed-but-chosen input: the paper prints "we choose beta = 1/929.25" and its equation uses it.
      Status PRINTED (given in the paper); origin CHOSEN via ORIG_CHOICE_STATED with a machine-matched
      quotation. §3's blockquote admits by status, so the arithmetic consumes it; the claim files
      exactly one of REPRO_WITHIN_STATED_PRECISION / REPRO_FAILED (or REPRO_NOT_EVALUABLE if the cap
      bites). Lane-side, compute derives root origin CHOSEN -> rests_on USES_CHOSEN. One outcome, one
      rests_on. The old unreachable class for this case is retired; its content is the rests_on field.
  (b) A fitted parameter the paper states: "our fit gives sigma8 = 0.811". PRINTED, origin FITTED via
      ORIG_FIT_STATED; consumed; arithmetic outcome; rests_on USES_FITTED. One outcome, one rests_on.
  (c) An imported value: "H0 = 67.4 (Planck 2018)". If Planck is an enumerable manifest text and the
      value machine-matches at its cited line: PRINTED from that source, origin IMPORTED, consumed;
      arithmetic outcome; rests_on USES_IMPORTED. If the value does not machine-match at the cited
      line, or the named source is not an enumerable manifest text: status BLOCKED, no value, never
      consumed, the claim files exactly REPRO_BLOCKED (precedence places BLOCKED ahead of
      INPUT_ABSENT and of the arithmetic group); rests_on is computed from the claim's ledger records
      (the BLOCKED record's IMPORTED origin is a root -> USES_IMPORTED). One outcome, one rests_on.
  Reachability no longer fights decidability because provenance was moved off the verdict: admission
  is by status (machine-checked), the verdict rule is mechanical (no stated precision -> round to the
  printed numeral at the printed precision, half away from zero; stated uncertainty -> |reproduced -
  printed| <= stated uncertainty, taken once; asymmetric -> the half-width on the side the reproduced
  value falls), and origin is recorded and cited, never gated on. The remaining human boundary
  (specified procedure vs named-but-unspecified) is double-read with a dispute class; that is a
  stated, bounded judgement, not an undecidable definition.

=====================================================================
2. CONTROLS (Q2 answered here)
=====================================================================

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED,
C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED,
C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. Unreached controls are NOT_RUN twice over: §4 ("Once a stop class
applies, later limbs are unreached and their controls are NOT_RUN") and §5 ("Controls in an unreached
limb are NOT_RUN, never passes").

Pass-by-assertion is closed where a machine check is claimed: C1 PASS only on exit 0 with the five
declared counts printed and the script's declared-vs-recomputed comparison; C3 "PASS only on exit 0
from every printed run in the artefact; a token asserted without the printed run is FAIL"; C5 requires
three live commands with full stdout, "a transcribed value in place of live output is FAIL"; C6
requires the printed C6_AUDIT.json artefact, "a token without the artefact is FAIL"; C4_PACKET_REDACTED
is asserted by a builder pinned by sha256, not by hand-checking ("the study does not proceed on a
hand-checked copy"). The two self-reported controls (C4_SEAT_ISOLATION, C5B_NO_CROSS_LANE) are
assertion-based by design and are labelled so in their own text: PASS certifies the printed list's
contents and "makes no claim that the list is complete". That is the honest statement of their
strength, not a hidden assertion path.

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? YES at the record layer, with the
classification layer's floor stated in the document. TEST IT, run live: my synthetic paper line
"We adopt H0 = 67.4 km/s/Mpc from Planck (2018)." I filed a ledger record with origin CHOSEN,
reason_code ORIG_CHOICE_STATED, verbatim "We adopt H0 = 67.4", value "67.4" cited to that line — a
wrong-but-plausible origin (the sentence names an external source; the reason-code precedence says
ORIG_CITATION files first, "a sentence that names an external source for the value is a citation
whatever else it says"). Result: /usr/bin/python3 r3c2_ledger_tools.py validate -> C3_NO_SUBSTITUTION=PASS,
exit 0. The machine does NOT catch it: the quotation is a genuine substring and the value genuinely
matches, and validate does not adjudicate which reason code should have won. What catches it, as
written: (i) the second seat's independent origin classification — a disagreement files ORIGIN_DISPUTED,
is reported with both seats' classifications and both quotations, is never reconciled, and above 10%
of included claims stops the census under CENSUS_ORIGIN_DISPUTED; (ii) the C6 auditor, who re-classifies
every input's origin from the pinned sources without sight of earlier work — a disagreement is a
MISMATCH and files CENSUS_AUDIT_FAILED. What the machine DOES catch (all verified live, section 7):
a value not at the cited line, an extra field, an ABSENT/BLOCKED record carrying a value, a DERIVED
record with no parents, a derived_from cycle, an ORIG_SILENT record with no origin_search, a STANDARD
value off the closed list. The residual floor — every reader misclassifying identically — is stated in
the master's own C3 note ("caught only by the second seat's independent classification and the C6
re-classification, never by the machine; if every reader misclassifies identically, the record stands —
that floor is stated here rather than implied away"). The mechanism as written catches the plausible
wrong origin at the two-human layer and says exactly that. No defect.

=====================================================================
3. CIRCULARITY
=====================================================================

The lane's expectation has no open channel to the evidence:
  - Inclusion: the §1 rule ("a passage printing a numeral the paper asserts as a result of its own",
    with five enumerated excluded kinds) contains no pattern content; two blind seats apply it
    independently; disagreement surviving two reconciliations stops the census
    (CENSUS_DENOMINATOR_DISPUTED) rather than being steered.
  - Exclusion: an exclusion must cite one of five enumerated kinds, is recorded with file, line and
    numeral, is reported alongside the denominator, and the C6 auditor "first audits the full candidate
    and exclusion ledgers against every pinned source — completeness, not just outcomes". A contrary
    claim quietly excluded — or quietly never enumerated — is an incompleteness disposition and files
    CENSUS_AUDIT_FAILED. Exclusion cannot hide anything.
  - Origin classification: origin never gates the arithmetic ("Arithmetic consumes records according
    to status PRINTED or STANDARD"); the reason-code precedence is fixed and evidenced; the seat-facing
    lists are alphabetical ("the list carries no order of its own"); disputes are reported, never
    reconciled. A provenance reading cannot be smoothed toward an expectation.
  - Interpretation: the mapping lives in a seat-invisible protocol, committed and receipted (receipt P)
    before limb A begins; rests_on is computed by the lane tool only after both seats have exited, and
    compute REJECTS (exit 2) a ledger arriving with root_origins or rests_on already set, so no seat —
    and no early lane hand — can pre-write the field the interpretation reads.
  Residuals, both stated in the document: the identical-misclassification floor (section 2 above) and
  the impossibility of proving a seat has no prior exposure (C4: "nothing available here can"). No
  unstated channel found.

=====================================================================
4. THE BLIND (Q5, Q6, Q3 answered here — from the packet ALONE, before the master was read)
=====================================================================

Q5 (answered first) — CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN
ANY OTHER? NO. No sentence does. The packet's only consequence-touching sentences are procedural and
attach to process states, not to the weight of any census outcome for anyone: "disagreement on any
candidate that survives two reconciliation attempts stops the study under CENSUS_DENOMINATOR_DISPUTED",
"The census does not proceed", "No tally is filed". The one sentence that speaks to consequence at all
removes it from every outcome alike — §11: "No tier, warrant token, standing or stamp moves." Nothing
ranks REPRO_FAILED against REPRO_WITHIN_STATED_PRECISION, or CENSUS_PARTIAL against CENSUS_COMPLETE,
for the lane or for anyone.

Q6 (answered second) — CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED origin OF AN INPUT IS
FOR? CANNOT_STATE for any analytic purpose. What the packet lets me state is integrity mechanics only:
"Every input's origin is classified independently by both seats"; disagreements file ORIGIN_DISPUTED
and above 10% of included claims stop the census (CENSUS_ORIGIN_DISPUTED); the C6 auditor re-classifies
"every input's origin from the pinned sources". No sentence states what the study does with origin
after those checks — whether any tally, comparison or conclusion ever reads it. The packet expressly
closes the one use I might have inferred: "Arithmetic consumes records according to status PRINTED or
STANDARD" and "origin is one recorded attribute of a ledger record, beside status, value, source_file
and source_line; a seat records it and writes no field outside the schema". The arithmetic does not
read origin; nothing in the packet says what does. CANNOT_STATE.

Q3 (answered third) — WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?
Nothing that reaches a hypothesis, a comparison model, a preferred outcome, or an expected "pattern".
Every inference I could draw, with its leaking sentence:
  (i) The corpus is cosmology-adjacent — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing
      baseline." That is content the seat needs to do the classification at all, and it cannot steer
      anything: the STANDARD/PRINTED routing is declared "outcome-identical", so expecting Planck-2018
      constants changes no outcome.
  (ii) Provenance is recorded with unusual care — seven origins, machine-matched quotations, two
      independent readers, disputes carried not reconciled (C3). I can infer provenance MATTERS to
      someone; I cannot infer what for (Q6). This is the ruled accepted floor ("hide the comparison,
      keep the taxonomy" — a gate finding that a seat can tell provenance matters is the accepted
      floor, not a defect), and on my own reading it is exactly where the packet lands.
  (iii) The lane anticipates that any outcome may occur — C0 requires exhibiting a concrete input for
      every declared outcome and condition before any freeze. That is reachability discipline, not an
      expectation about the tally.
  Adversarially trying to reconstruct the stake: someone wants to know whether the corpus's printed
  numbers follow from their own stated inputs, and wants provenance recorded carefully beside that.
  Which tally would please the lane, what origin will be compared against, what pattern is hoped for
  or feared — the packet gives me nothing. LEAK=NONE.

The blind's machinery, judged as written and verified live: C4 states the redaction is machine-asserted
by a pinned builder (sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3, matching
the file on disk) that drops §0/§7/§8/§10 whole, strips every SEAT-REDACT span, and asserts the forbidden
list absent — "If any survives, the packet is not written and C4_PACKET_REDACTED=FAIL; the study does not
proceed on a hand-checked copy." I ran the delivered builder in a /tmp sandbox on a copy of the master:
first line "C4_PACKET_REDACTED=PASS", sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'], dropped
['0.','7.','8.','10.'], 50 inline spans redacted, and the rebuilt packet is byte-identical to the
committed, pinned packet (sha256 a7353e9199e14c2e711ad20a32dd100d6d62f126d182eeb30f33ff91e41ce10b).
Independently of the builder, my own word-boundary case-insensitive grep of the packet for the
consequence words (expectation, refute, consequential, invisible, favourable, unfavourable, stake,
prior, tempting, warn, hypothesis, pattern), the custody names, the engine names, and the topic strings
returned zero hits; "rests_on", "root_origins", "DERIVED_ONLY", "severity", "comparison" and the old
class token are all absent from the packet. The mechanism prevents what it claims to prevent — no
pattern content, no consequence words, no gate history, no names — and its limits are stated honestly
in the document: the forbidden list "blocks the enumerated strings and does not establish that every
consequence-bearing word is gone; procedural consequences of stop outcomes remain visible";
C4_SEAT_ISOLATION "is a self-reported control with a structural aid, and is labelled so"; and "It cannot
prove a seat has no prior exposure from training or an earlier session — nothing available here can."
The accepted floor is recorded with its ruling. Sound.

=====================================================================
5. THE SEAL (§7)
=====================================================================

Two receipts, each binding its object before the step it must precede:
  (P) The interpretation protocol's hash and commit id go to the external custodian BEFORE limb A;
      "limb A does not begin without receipt P." The protocol's bytes are therefore fixed before any
      evidence exists — it cannot be written to suit the tally, and the text now rests the claim on
      the receipt, not on the commit alone.
  (T) The tally — one commit containing the merged candidate file, the exclusion ledger, and the merged
      input ledger with its compute output — is hashed and receipted AFTER the tally and BEFORE the
      protocol is opened; "the receipt names every file in that commit; the interpretation protocol is
      not opened without both receipts." The tally's bytes are therefore fixed before interpretation.
  After opening, the custodian independently re-hashes tally and protocol and verifies all four values
  against the two receipts; the interpretation report must print both receipts and the four verified
  values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (whose §4 definition names the
  seal-receipt case). The audit seed is supplied by the custodian only after the tally digests are
  receipted and is recorded with the receipt, so the sampling frame cannot be predicted or shaped.
  The overclaim check passes: "a local commit in Tori's own custody can be amended or reset, so it is
  tamper evidence, not an external custodian"; "It does not prove Tori had not already guessed the
  answer — nothing available here could prove that. It bounds the record, not the mind." Sound.

=====================================================================
6. FAIRNESS
=====================================================================

Every negative outcome is worded "unreproduced from the stated inputs," never "error" — the rule is
mandatory and sits inside REPRO_FAILED's own definition. Class names match their definitions:
REPRO_WITHIN_STATED_PRECISION asserts exactly the stated-precision test (the old name asserted more;
the rename is the fairness fix, verified below); REPRO_FAILED is bounded by the mandated wording;
REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NO_DERIVATION_STATED, REPRO_NOT_EVALUABLE name states of the
record, not faults of the paper; CENSUS_PARTIAL's "INCONCLUSIVE" matches its definition (a
non-arithmetic outcome or a zero denominator) and carries no blame. No clause asserts more than its
definition. One label asserts LESS than its set — F2 below, cosmetic, master-only.

=====================================================================
7. STALL / EXECUTABILITY (Q4 answered here)
=====================================================================

Stall: the 120-second cap is enforced by a committed wrapper and has a per-claim outcome to file into
(REPRO_NOT_EVALUABLE, one repeat permitted, "meaningful only for REPRO_NOT_EVALUABLE"); enumeration
gets two reconciliation attempts, outcomes one, input lists one; every dispute lands in a named stop
class; unreached controls are NOT_RUN. Live wrapper check: a fast command exited 0 with the wrapper
command, child stdout and status printed; a 1.0-second cap over `sleep 5` printed SYMBOLIC_TIMEOUT at
1.004 s and exited 124, exactly as §9 describes.

Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO. Each thing checked, and how:
  1. r3c2_ledger_tools.py — exists (ls); sha256 matches the §3/C3 pin
     f9b7d3c818647e6e3e10e19013db3a042247fc141ee01282b8928de06372c9ac; usage run shows census and
     validate only — the document's "The seat tool r3c2_ledger_tools.py has no compute and no merge"
     holds as delivered. Exercised on my own synthetic fixtures in /tmp:
     - validate, 9 runs: a conforming ledger PASSED (exit 0, prints C3_NO_SUBSTITUTION=PASS); each
       attack FAILED with a named failure line and exit 1: a value not at its cited line; a field
       outside the schema (rests_on in a seat-authored ledger); an ABSENT record carrying a value; a
       DERIVED record with no derived_from; a derived_from cycle (both nodes named); an ORIG_SILENT
       record with no origin_search; a STANDARD value not equal to the closed-list string ("validate
       compares strings" confirmed); the Q2 wrong-origin attack PASSED, confirming the stated floor.
     - census, 6 runs: pre-tally consistent files PASS (exit 0) printing declared AND recomputed
       counts; a declared-vs-recomputed count mismatch FAILS naming it; `final` with a PENDING outcome
       FAILS; `final` with a non-§3 outcome token FAILS naming the token; a declared_attempt_count
       mismatch FAILS; a complete final file PASSES. The C1 description (five declared counts,
       disposition and exclusion coverage checks, PENDING rejection, printed/reproduced value
       requirement on arithmetic outcomes) is accurate as delivered.
  2. r3c2_lane_tools.py — exists; sha256 matches the C3 lane-side pin
     8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848; usage shows merge and compute
     with the described semantics (origin_alt/origin_evidence_alt on origin disagreement,
     PARENTS_DISPUTED on derived_from disagreement, rests_on severity order, DISPUTED pairs, and
     REJECT exit 2 for a ledger arriving with root_origins or rests_on already set). Not run on real
     ledgers — none exist pre-dispatch; the described interface exists as delivered.
  3. r3c2_build_seat_packet.py — exists; sha256 matches the C4 pin
     4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3; executed in a /tmp sandbox on a
     copy of the master: first line C4_PACKET_REDACTED=PASS, then the master digest (55b466fa…, equal
     to ACCESS_SHA) and the packet digest (a7353e91…, equal to PACKET_SHA); rebuilt packet
     byte-identical to the committed pinned packet. The documented build command runs as written.
  4. r3c2_timeout.py — exists; sha256 matches the §9 pin
     fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331; live runs above.
  5. R3C2_CORPUS_MANIFEST.md — exists; sha256 matches the §2 pin
     300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24. Content not opened (outside the
     two named files); the digest binds it.
  6. R3C2_SEAT_PACKET.sha256 — exists; lists the packet, SEAT_BRIEF.md, r3c2_timeout.py,
     r3c2_ledger_tools.py and the builder; all five digests match the files on disk.
  7. r3c2_seat_packet/R3C2_SEAT_PACKET.md — sha256 equals PACKET_SHA and the pin; its header embeds the
     master digest, equal to my independently computed ACCESS_SHA.
  8. r3c2_seat_packet/SEAT_BRIEF.md — exists; digest matches the pin.
  9. R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists (existence-only check; seat-invisible, not
     opened). Receipt P is a run-time step; nothing pre-run is missing.
 10. R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md — exists (existence-only; referenced by C4's floor clause).
 11. R3C2_V21_RENAME_AUDIT_20260905.md — opened after the master, as permitted (rename verification
     below).
 12. /usr/bin/python3 — C5's three commands all exit 0 on this machine: Python 3.9.6; sympy 1.14.0;
     interpreter digest printable.
 13. Paths a seat is told to use: `.` as the sole sources_dir for validate (consistent with the
     dispatch copy's layout: packet, brief, pin file, manifest, ledger tool, wrapper, and the pinned
     sources — the dispatch copy itself is a run-time artefact, listed by the lane owner before launch,
     and correctly does not exist yet).
 Every literal command, named script, pinned file, computed field and seat path that the document
 describes exists and behaves as delivered. No executability finding.

RENAME VERIFICATION (per the STEP 2 note, my own check on the V23 master):
grep -n REPRO_EXACT on the master: 16 hits. Line 118 is the §3 rename notice itself ("*(named
REPRO_EXACT until V21; renamed by the principal's ruling)*", inside a SEAT-REDACT span — stripped from
the packet, confirmed). The other 15 hits (lines 533–988) all lie under "## 10" headings — the first
"## 10" heading is line 497, and every hit is inside §10/§10.4–§10.15; none is in §11 (lines 585–588)
or in any other operative section. Zero governing references to the old token. The packet carries zero
occurrences of REPRO_EXACT and zero of rests_on/root_origins. The audit file's LIVE/HISTORICAL rule is
consistent with this; my count is on the current master, not on the audit's claim.

=====================================================================
FINDINGS
=====================================================================

F1 (COSMETIC — changes no class filed, no control's passability, no reachability, no conclusion and no
tested value). The sealed candidate and exclusion artefacts have no field-selection rule where the two
seats agree on every dispositive field.
  Verbatim (§2 step 5): "the sealed reproduction tally is the merged candidate file on which the two
  seats' `outcome` fields agree, claim by claim, after one reconciliation against the printed numeral
  and the stated-precision rule of §3".
  Defect: the sentence gives the assembly rule for the dispositive fields only. Where the two seats'
  outcome fields agree claim-by-claim but non-outcome fields differ (e.g., reproduced_value formatting),
  no rule selects the merged file's value, and no named command produces the merged candidate file —
  by contrast with the input-ledger merge, which is the named, pinned `r3c2_lane_tools.py merge`. The
  same gap covers §3's exclusion ledger: where both seats exclude the same candidate under different
  kinds, the sealed row's kind has no selection rule (the census check requires exactly one exclusion
  row per excluded candidate, so both rows cannot be carried). Consequence is nil: the dispositive
  fields are agreed, C6 re-adjudicates outcomes and exclusions against the pinned sources, and receipt
  T names every file regardless — hence COSMETIC.
  Exact replacement (append to §2 step 5 after the CENSUS_OUTCOME_DISPUTED clause):
  "**Where the two seats agree on every claim's outcome and on every candidate's disposition, the
  sealed candidate and exclusion files carry, for every other field, the value on which the two seats
  agree; a non-dispositive field on which they still differ after the same reconciliation — including
  an excluded candidate's kind — is carried at its seat-A value in the sealed file and is reported
  beside it with both values; the difference changes no outcome, no disposition and no count.**"

F2 (COSMETIC — noted; the rename is recorded as reserved to the principal, so this is an observation
with a replacement, not a demand). A lane-side label asserts a proper subset of its defined set.
  Verbatim (§3, master-only SEAT-REDACT span): "`DERIVED_ONLY` when every root origin is `DERIVED`,
  `STANDARD` or `MEASURED`".
  Defect: the label DERIVED_ONLY denotes {DERIVED, STANDARD, MEASURED}-only root sets — it asserts
  less than its definition, the safe direction, and it sits in master-only text the seats never read.
  No class filed, no control, no conclusion and no tested value is affected: COSMETIC.
  Exact replacement: in that sentence and in the lane tool's compute output, replace the token
  `DERIVED_ONLY` with `ROOTS_DERIVED_STANDARD_OR_MEASURED`. (The document's own record already reserves
  this rename to the principal; I independently locate the same label and agree it is label-only.)

No other defect found. Sections 1–7, 9 and 11 are sound as written; the two items above are cosmetic.

R3C2_V23_GATE_COMPLETE
