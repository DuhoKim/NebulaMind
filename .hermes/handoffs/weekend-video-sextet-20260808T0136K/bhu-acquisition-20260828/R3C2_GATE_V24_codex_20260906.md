ACCESS_SHA=bcc16e32b6dfd112e4276b61616293bc03bbc28ee3c360b5b9fd7ec65f3645b3
PACKET_SHA=b4bd7e37e01f33e5ff65329c5e37be624310d92f1ec53eadb475b76849aaedb5
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=CENSUS_AUDIT_FAILED suppresses tally filing: “No tally is filed; report which.” This is a procedural consequence, not a disclosed empirical stake.
ORIGIN_PURPOSE=Independent audit checking: “the re-classified `origin` of each of its inputs, and `MATCH` or `MISMATCH` against the sealed record.” No interpretation purpose is disclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES — C4 expressly permits the runtime paths and all three C5 commands succeeded here; the separately named brief was not opened, so its consistency is unverified.

This is a design judgment, not a census result. Only the master and packet were opened as study documents. I read the packet and answered Q5, Q6 and Q3 before reading the master. The master was subsequently read in full; §10 supplied no evidence for my verdict. No gate files were sought or opened.

Packet-only answers, in the required order

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

There is no sentence disclosing an empirically preferred outcome or a differential consequence for the lane's hypothesis. However, the literal word “any” includes procedural consequences: under CENSUS_AUDIT_FAILED, “No tally is filed; report which.” COMPLETE instead requires reporting a tally. The packet also says, “No tier, warrant token, standing or stamp moves.” Thus procedural consequences are visible; a scientific stake is not.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?

Yes, for checking: C6 requires “the re-classified `origin` of each of its inputs, and `MATCH` or `MISMATCH` against the sealed record.” Independent classifications are compared, disagreements are retained, and disagreements affecting more than 10% of included claims stop the study. I cannot state any subsequent interpretive use from the packet.

Scope correction to my initial packet-only answers: I initially used “consequential” to mean empirical stakes and “purpose” to mean interpretation. Those narrower answers omitted the procedural consequences and checking purpose that the questions also encompass. The sentences supporting this correction were already in the packet; it does not depend on the master.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

No directional hypothesis, comparison-model preference, desired outcome, or particular pattern is inferable. The strongest supported inferences are:

- Reproduction and provenance are separately important: “The reproduction verdict and the provenance fields are recorded separately.”
- Provenance receives unusually explicit scrutiny: “Every input's `origin` is classified independently by both seats.” Its dispute threshold reinforces that inference.
- Cosmology is relevant: “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.” This identifies subject matter and a constants source, not a preferred comparison result.
- Printed choices may reproduce: “Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`.” This reveals the methodological boundary, not which side the actual corpus is expected to occupy.

I tried to infer that the lane hopes to distinguish successful arithmetic from dependence on chosen inputs. The packet establishes that such information is collected, but does not establish that hope, a preferred direction, or a mapping from provenance to a conclusion. LEAK=NONE concerns hypothesis reachability, not absence of methodological information.

1. OUTCOME CLASSES — Q1

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

All six per-claim outcomes and all eight study-level classes have concrete witnesses. Option (c) resolves the chosen-input tension: a stated choice is consumed mechanically and remains visible in provenance. However, I cannot answer an unconditional YES to the combined operational question: the imported-value evidence rule conflicts with C3 for an ordinary supplying-source sentence, preventing a compliant outcome-plus-provenance record. Defect D1 below concerns that conflict, not the principal's settled option.

Concrete numerical witnesses below are constructed physical claims, not claimed observations from an unopened corpus.

| Printed claim and source situation | Unique per-claim filing | rests_on |
|---|---|---|
| “We choose a = 2. For x = 3 m measured with the described ruler, y = ax = 6 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe and inputs, but the asserted result is 7 m, with integer printed precision and no uncertainty | REPRO_FAILED; unreproduced from the stated inputs | USES_CHOSEN |
| “The fitted coefficient is a = 2”; x = 3 m and y = ax = 6 m | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| a is cited only to an unpinned source; x = 3 m and y = ax = 6 m | REPRO_BLOCKED | USES_IMPORTED, assuming the other root is MEASURED |
| a is neither printed nor attributed; x = 3 m and y = ax = 6 m | REPRO_INPUT_ABSENT | USES_UNDECLARED, with an adequate ORIG_SILENT search |
| A fully specified computation on stated measured inputs exceeds the wrapper's 120-second deadline | REPRO_NOT_EVALUABLE | DERIVED_ONLY under the defined membership rule |
| “Our resulting length is 6 m,” with no equation or specified procedure and no ledger records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

A claim needing both an externally cited unavailable coefficient and a wholly absent offset meets BLOCKED and INPUT_ABSENT conditions. The explicit precedence files BLOCKED alone. A complete numerical result that rounds to the printed numeral is in the precision class; FAILED is read against that same precision criterion, not exact digit identity.

Study-level witnesses: a positive denominator consisting entirely of arithmetic outcomes with a passing audit gives COMPLETE; one missing-input claim with a passing audit gives PARTIAL; an empty enumeration with a passing completeness audit also gives PARTIAL; an audit mismatch or missing receipt gives AUDIT_FAILED; both attempting seats failing C5 twice gives NO_CLASS; a persistent inclusion or input-list disagreement gives DENOMINATOR_DISPUTED; an outcome disagreement after its reconciliation gives OUTCOME_DISPUTED; two origin-disputed claims out of ten gives ORIGIN_DISPUTED; one seat passing C5 and another failing it twice gives CONTROL_SPLIT.

A tally with nine arithmetic claims and one blocked claim, followed by an audit mismatch, satisfies both PARTIAL and AUDIT_FAILED conditions. The precedence files AUDIT_FAILED alone. An early control failure does not additionally acquire an audit failure merely because the audit is unreached: later controls are NOT_RUN. I found no unresolved double filing among these study-level conditions.

INCONCLUSIVE is genuinely reachable through PARTIAL, including zero denominator. It is the interpretation of that class, not an additional filing token. The 120-second and unavailable-machinery outcomes make this a bounded procedure; they do not promise to decide the truth of arbitrary computations.

D1 — SUBSTANTIVE: imported-value evidence is assigned to the wrong source.

Verbatim §2 sentence:

> A value the paper does not print but traces to a named source that is itself an enumerable text in `R3C2_CORPUS_MANIFEST.md` is
> classified `PRINTED` from that source, with `origin` `IMPORTED`, `origin_evidence` `ORIG_CITATION` cited to the named
> source's file and line, and the value machine-matched there — **only when such a match exists; a cited value that does
> not machine-match at the named source's cited line, or whose named source is not an enumerable text of the manifest,
> files `REPRO_BLOCKED` under §3.**

Hardest case: the claiming paper says “Use a from Smith (2020)” and prints y = ax = 6 m with measured x = 3 m. Smith's pinned value line says “We choose a = 2.” The number machine-matches. Section 2 therefore requires PRINTED/IMPORTED and ORIG_CITATION at Smith's line. But Smith's line supplies ORIG_CHOICE_STATED evidence, not a citation. C3's reason-code rule cannot substantiate the required IMPORTED classification from that sentence. Using CHOSEN violates §2 and changes rests_on to USES_CHOSEN; using the claiming paper's citation line violates §2's specified evidence location.

The intended arithmetic outcome is unambiguously REPRO_WITHIN_STATED_PRECISION and the intended provenance is USES_IMPORTED. As written, no evidence-compliant record gives that pair in this case. Machine substring matching alone could accept the mislabeled quotation; that would not resolve the semantic contradiction.

EXACT replacement for the quoted sentence:

> A value the claiming paper does not print but traces to a named source that is an enumerable text in `R3C2_CORPUS_MANIFEST.md` is classified `PRINTED` with `origin` `IMPORTED` only when its value machine-matches a cited line in that source. Record the value's `source_file` and `source_line` at that supplying source. Record `origin_evidence` with reason code `ORIG_CITATION`, its `source_file` and `source_line` at the claiming paper's sentence naming the supplying source, and `verbatim` equal to the quotation establishing that attribution. These evidence locations may differ. If the supplying source is not enumerable in the manifest, or the value does not machine-match at its cited line, file `REPRO_BLOCKED` under §3.

2. CONTROLS — Q2

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

It is checkable by source-reading humans and partly checked mechanically. It is not mechanically certified as semantically correct. The master explicitly acknowledges that identical reader misclassification can survive.

Concrete wrong-but-plausible ledger record, against a hypothetical pinned paper.txt line 42 reading “We adopt a = 2 from Smith (2020).”:

```json
{
  "claim_id": "q1",
  "input_id": "q1.a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "paper.txt",
    "source_line": 42,
    "verbatim": "We adopt a = 2 from Smith (2020)."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "paper.txt",
  "source_line": 42
}
```

The declared validator checks can accept the value, quotation substring, schema, and CHOSEN/reason-code pairing. They do not recognize the sentence's attribution semantics. C3's precedence requires ORIG_CITATION/IMPORTED. The independently classifying second seat can expose the error; C6 reclassification must expose it for an audited claim if performed correctly. Every arithmetic-group claim is audited. An unsampled non-arithmetic record has no guaranteed third-seat reclassification. If all readers make the same mistake, the mechanism cannot catch it; the master says so.

This attack differs from D1: here the proper citation quotation exists at the required location, so the rule can be correctly applied. D1 makes the designated evidence location itself unsuitable.

Control inventory:

| Control code | Required evidence / limitation |
|---|---|
| C0_REACHABILITY | Concrete exhibition table, independent author and verifier; no bare PASS |
| C1_DENOMINATOR_PRINTED | Candidate/exclusion files, five declared counts, printed census runs and exit 0, including final mode |
| C2_INPUT_LEDGER | Printed resolved validation command, stdout, stderr and exit status |
| C3_NO_SUBSTITUTION | Both seats' validation evidence and lane merge/compute runs; script-generated provenance fields |
| C4_PACKET_REDACTED | Pinned builder invocation and output, matching packet pin, dispatch inventory |
| C4_SEAT_ISOLATION | Printed working directory and path list; self-reported list, explicitly not complete |
| C5_HARNESS_PINNED | Three live command outputs, all exits 0 |
| C5B_NO_CROSS_LANE | Printed scope-labeled path list; same self-report limitation |
| C6_AUDIT_SAMPLE | Printed C6_AUDIT.json with required sampling, completeness and comparison fields, no mismatch |

Every named control declares PASS/FAIL/NOT_RUN. Unreached controls are explicitly NOT_RUN. C4/C5b can pass on an inaccurate but printed self-report; their definitions honestly restrict what that PASS certifies. The record is not an independent access trace. Script implementation and actual output-schema enforcement remain unverified under the two-document restriction.

3. CIRCULARITY

The declared inclusion rule does not invoke the lane's hypothesis. Excluded candidates remain in a ledger, both seats classify independently, and C6 checks the entire candidate/exclusion frame against every pinned source. A contrary included claim cannot be quietly removed under a compliant execution. A denominator dispute stops the study and reports the disputed frame.

Residual judgment remains in what counts as the paper's own result, input completeness and origin evidence. Agreement and audit constrain those judgments; they do not make them mechanical. Corpus selection is fixed by a manifest, but I could not inspect that manifest or establish how representative its selection is. I therefore endorse only a census of the declared corpus, not an unbiased sample of literature.

The separate pre-receipted interpretation protocol limits post-tally remapping. Its actual mapping was not read, so its scientific validity is not certified. D1 can alter a provenance conclusion through conflicting evidence rules, and D2 below supplies an avoidable access channel into the blind.

4. THE BLIND

My independent text check selected operative sections 1–6, 9 and 11 from the master, removed SEAT-REDACT spans, and compared that result to the packet's operative body. They matched after whitespace normalization. The packet's embedded master hash equals the access hash.

A separate literal scan found no packet occurrence of rests_on, root_origins, DERIVED_ONLY, USES_CHOSEN, ΛCDM, Tori, Blanc, Duho, pattern, expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting or warn. This is evidence about these bytes, not proof that the named builder performed its assertion or that its entire forbidden list is correct. The builder was not opened or run.

C4 accurately states the basic blind's limit: a redacted dispatch directory and self-reported paths do not enforce filesystem denial, establish actual non-access, or rule out prior exposure.

D2 — SUBSTANTIVE: the runtime exception is unrestricted in what imported code it licenses.

Verbatim C4 sentence:

> The system binaries C5 names — `/usr/bin/python3`, `/usr/bin/shasum` — and whatever they load while executing the mandated
> commands are IN SCOPE and are not "outside paths" under this control; every other path outside the working directory is.

Verbatim C5b sentence:

> **`/usr/bin/python3`, `/usr/bin/shasum` and what they load are `IN_SCOPE` (C4).**

“Whatever they load” is broader than system/runtime dependencies. For example, /tmp/r3c2_unapproved/sitecustomize.py could be loaded through Python startup configuration while the exact mandated command runs. An unapproved sympy module could similarly be selected through import-path configuration. Those are illustrative attack paths, not files I found or created. Such startup code could read the lane's R3C2_INTERPRETATION_PROTOCOL_20260904.md, which §7 expressly withholds. The exception does not distinguish that loading from a legitimate dependency, and C5b's echo drops even the mandated-command qualification.

This is a scope-policy opening, not a claim that it occurred. Merely trusting the two executable paths does not bound Python's startup/import environment.

EXACT replacement for the C4 sentence:

> For the mandated C5, ledger and timeout commands only, `/usr/bin/python3`, `/usr/bin/shasum`, and runtime dependencies on an external-runtime allowlist recorded before dispatch are IN_SCOPE. The dispatch record identifies the actual interpreter, hashing interpreter, modules, libraries and runtime configuration paths needed by those commands. The launch environment and module search paths must select only that runtime allowlist and the pinned seat files; unlisted startup hooks and module overrides are disabled. This exception does not authorize reading any lane study document or other unlisted data file, including a read initiated by imported code. If a required runtime path is missing from the allowlist, stop and report it before extending the list.

EXACT replacement for the C5b sentence:

> Apply C4's recorded runtime allowlist and command restriction when assigning IN_SCOPE; an unlisted outside path is OUT_OF_SCOPE even when a permitted executable or imported module opens it.

Apply the same scope language to the brief and rebuild/re-pin the packet. This changes permitted access and control passability, so it is not COSMETIC.

The packet opening “Do not open any other path; print every path you open” is general; I read the specific C4 runtime exception as qualifying it. I do not count that ordinary general/specific relationship as a separate defect.

5. THE SEAL

The timing is adequate as specified: receipt P binds the protocol hash and commit before limb A; receipt T binds the named tally commit and its constituent files before protocol opening. The external custodian independently verifies both hashes and both commit identifiers; the interpretation report prints the receipts and four values. Missing or mismatching receipts prevent interpretation and file AUDIT_FAILED.

The unpredictable audit seed arrives after the tally receipt, blocking selection of a favorable sample by editing tally serialization before receipt. The text does not claim mind-reading or physical immutability. Its guarantee is a detectable, externally bound record. Actual receipts and protocol contents were not inspected and are not assumed to exist for this unrun design.

6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” BLOCKED, INPUT_ABSENT, NOT_EVALUABLE and NO_DERIVATION_STATED describe limitations of the reproduction attempt; none declares the paper wrong. COMPLETE means arithmetic coverage with a passing audit and can include numerical failures. WITHIN_STATED_PRECISION correctly avoids claiming exact equality.

The old-token scan found REPRO_EXACT only in the rename notice at master line 118 and under §10 headings. No governing old-token reference was found. I did not open the optional rename audit.

D3 — COSMETIC: the DERIVED_ONLY display label is broader than ordinary “derived only.”

Verbatim defining passage:

> `DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or
> `MEASURED`; otherwise the most severe root origin present, in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` >
> `USES_FITTED` > `USES_CHOSEN`.

Measured-only roots legitimately receive this token, so an unexplained display could overstate derivation. Its explicit membership definition is nevertheless precise.

EXACT replacement for that passage:

> `DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`; report this token with the display explanation “only DERIVED, STANDARD or MEASURED roots,” not as a claim that every input was derived. Otherwise use the most severe root origin present, in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` > `USES_FITTED` > `USES_CHOSEN`.

This retains the token, all membership rules, all tested values, every class and every conclusion. It is a display clarification, not an unauthorized taxonomy rename.

7. STALL / EXECUTABILITY — Q4 AND Q7

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

D1 prevents a valid provenance record for the imported-value case exhibited above. D2 leaves the permitted runtime insufficiently bounded. The live C5 commands themselves execute successfully here. Complete script-level and dispatch-level executability cannot be certified without opening the additional files prohibited by this review request.

Live checks, exact commands and full stdout:

```text
$ /usr/bin/python3 --version
Python 3.9.6
exit=0

$ /usr/bin/python3 -c "import sympy; print(sympy.__version__)"
1.14.0
exit=0

$ shasum -a 256 /usr/bin/python3
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3
exit=0

$ command -v shasum
/usr/bin/shasum
exit=0
```

These tests used the provided workspace environment. They are not a reproduction run or evidence about a future sandbox. Python's successful import does not inventory every runtime path.

Existence checks used filesystem metadata only, without reading these files:

| Named deliverable | Result |
|---|---|
| R3C2_CORPUS_MANIFEST.md | Regular file, 21,182 bytes |
| r3c2_ledger_tools.py | Regular file, 11,782 bytes |
| r3c2_lane_tools.py | Regular file, 5,058 bytes |
| r3c2_timeout.py | Regular file, 1,760 bytes |
| r3c2_build_seat_packet.py | Regular file, 7,517 bytes |
| r3c2_seat_packet/SEAT_BRIEF.md | Regular file, 2,309 bytes |
| R3C2_SEAT_PACKET.sha256 beside the master | Regular file, 461 bytes |
| R3C2_INTERPRETATION_PROTOCOL_20260904.md | Regular file, 3,454 bytes |
| R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md | Regular file, 8,523 bytes |

The manifest, pin file, ledger tool and timeout wrapper are absent at r3c2_seat_packet/<their respective basenames>. Thus the repository packet subdirectory by itself is not an executable flat seat workspace. C4 explicitly requires the lane owner to assemble and inventory a separate dispatch copy before launch, which addresses that layout. I do not misreport these absent copies as missing root deliverables or claim that a dispatch copy was verified.

Command and field coverage:

- Both `census <candidates.json> <exclusions.json>` and its `final` form: read and checked for specified arguments, counts, dispositions, attempts and outcome fields. Not executed: doing so opens the forbidden script and needs generated ledgers.
- `validate <ledger.json> .`: the sole source directory is explicitly resolved to the printed seat working directory. Schema, value/quotation matching, closed STANDARD membership and parent-reference checks are described. Actual enforcement not inspected.
- Lane `merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>`: paths and reconciliation rule are specified; origin_alt, origin_evidence_alt and parent-list disputes are described. Actual merging of differences not inspected.
- Lane `compute <merged.json> <out.json>`: root_origins and rests_on, their disputed alternatives, rejection of prefilled computed fields, cycles and missing parents are described. Actual implementation not inspected.
- `/usr/bin/python3 r3c2_build_seat_packet.py`: script exists; its pin and assertions were not verified by executing it, which would read additional files and rewrite delivery artifacts. The independent two-text redaction comparison passed.
- `/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>`: wrapper exists; monotonic deadline, child output and exit 124 are specified. Enforcement and process cleanup were not tested.
- C6's `random.Random(seed_int).sample(remaining_ids, k)`: the declared sorted frame, hexadecimal seed conversion and k bound are executable Python expressions; the bound is zero when R=0 and never exceeds R.
- C6_AUDIT.json, candidate/exclusion/input ledgers, compute outputs, receipts, dispatch/provider logs and R3C2_CHECK_SHEET_<date>.md are run-produced artifacts, not claimed completed census results. Their current absence was neither established nor treated as a defect.
- Every individual pinned source, its digest and byte count remain unverified because that inventory is inside the unopened manifest. Third-seat dispatch is expressly administrative, not a packet-executable command.

Q7(a). Can a seat obeying C4 and the brief execute every C5 command?

YES under the explicit C4 scope exception in the inspected master and packet, in this environment. C5 says:

> Execute and print `/usr/bin/python3 --version`,
> `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 /usr/bin/python3` — the interpreter
> every ledger command runs under.

C4's quoted exception in D2 includes both binaries and their loads; shasum resolves to /usr/bin/shasum here. All three commands exited 0. I cannot quote or independently certify the separate brief's amendment: it was not one of the two permitted documents. The YES flag is therefore scoped to the inspected rules and tested environment, not an attestation about the unopened brief.

Q7(b). Does the clause open an improper path or leave a needed path closed?

It opens unlisted startup/import paths such as /tmp/r3c2_unapproved/sitecustomize.py, as explained in D2. No dependency needed by the tested C5 commands remains closed under the broad exception. The unqualified shasum command is environment-dependent; it resolved correctly here. When adopting D2, use `/usr/bin/shasum -a 256 /usr/bin/python3` and verify the runtime allowlist in the actual dispatch environment.

Q7(c). Is anything else different from signed V23?

UNVERIFIED. Neither the signed V23 document nor its brief was permitted to be opened, and no baseline diff was supplied. I cannot truthfully state “nothing else,” or an exhaustive difference list, from V24 alone. Section 10's assertion is not independent evidence and binds this review in no way. This is a verification limitation, not a newly discovered change to V23.

The gate is UNSOUND because D1 blocks a compliant imported-input record and D2 changes the blind's allowed-access boundary in an uncontrolled way. D3 is cosmetic. The settled one-pass/two-tally design, outcome precedence and receipt timing do not require replacement.

R3C2_V24_GATE_COMPLETE
