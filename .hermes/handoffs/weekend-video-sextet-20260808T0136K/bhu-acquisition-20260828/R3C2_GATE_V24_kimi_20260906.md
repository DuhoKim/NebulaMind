ACCESS_SHA=bcc16e32b6dfd112e4276b61616293bc03bbc28ee3c360b5b9fd7ec65f3645b3
PACKET_SHA=b4bd7e37e01f33e5ff65329c5e37be624310d92f1ec53eadb475b76849aaedb5
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE — from the packet alone I could infer only that non-arithmetic outcomes are anticipated and pre-framed conservatively ("unreproduced from the stated inputs, not 'error'"; "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE"); no hypothesis, comparison model, preferred outcome, or "pattern" is reachable, and packet §11 states "No tier, warrant token, standing or stamp moves."
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=origin is a cross-reader integrity and audit field the arithmetic never consumes: both seats classify it independently and a >10% dispute stops the census (packet §4: "CENSUS_ORIGIN_DISPUTED — the two seats' independent `origin` classifications disagree on inputs affecting more than 10% of included claims"), the C6 auditor re-derives "re-classifying every input's `origin` from the pinned sources" (packet §5 C6), while packet §3 states "Arithmetic consumes records according to status `PRINTED` or `STANDARD`."
C5_EXECUTABLE_UNDER_SCOPE=YES — every path in the three C5 commands is named in scope by C4 ("The system binaries C5 names — `/usr/bin/python3`, `/usr/bin/shasum` — and whatever they load while executing the mandated commands are IN SCOPE and are not 'outside paths' under this control; every other path outside the working directory is."), echoed by C5b ("`/usr/bin/python3`, `/usr/bin/shasum` and what they load are `IN_SCOPE` (C4)"), and the seat brief names the same two binaries in scope (count-verified), so a letter-exact seat can run all three commands.

SEAT: kimi. Master read AFTER the packet answers below were fixed. Section 10 of the master was read as history only; nothing below rests on its narrated findings. Files opened in full: the master and the seat packet only, plus R3C2_V21_RENAME_AUDIT_20260905.md as expressly permitted. Named artefacts were statted and sha256-hashed (metadata, not content); SEAT_BRIEF.md was checked by line-count greps only (booleans/counts, no prose read), because my brief confines full reads to the two named files plus the audit.

PACKET PHASE (answered from the packet alone, before the master was opened, in the order Q5, Q6, Q3)

Q5 — consequence. No. No sentence in the packet tells me any outcome is more consequential than any other, to the lane or to anyone. The only sentence that speaks to consequence asserts uniform inconsequentiality: §11, "No tier, warrant token, standing or stamp moves." The precedence language ("INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`") is a filing rule for co-occurring conditions, not a consequence statement.

Q6 — origin purpose. Yes, statable. `origin` is recorded for cross-seat comparison and audit, not arithmetic: packet §4 class 7 ("the two seats' independent `origin` classifications disagree on inputs affecting more than 10% of included claims. The census does not proceed"), packet §5 C6 (the auditor re-derives "re-classifying every input's `origin` from the pinned sources"), and packet §3 ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`" — origin is never consumed). What I cannot see from the packet: what the recorded provenance is later compared against or combined with.

Q3 — hypothesis reachability. Almost nothing is reachable, and I tried to reconstruct the stake. The corpus is hidden behind a digest (packet §2 names only "R3C2_CORPUS_MANIFEST.md (sha256 300d4da1…)"). The only inferable items: (a) the lane anticipates non-arithmetic filings and pre-constrains their framing — §3, "Wording: 'unreproduced from the stated inputs,' not 'error.'"; (b) a conservative design preference — §4, "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" and "no census is complete over nothing"; (c) the taxonomy itself — seven quoted origin values with two independent readers tells me provenance is measured with care, which is the accepted floor, and nothing in the packet tells me what it is compared against. No hypothesis, comparison model, numeric pattern, or preferred science outcome is reachable; §11 states no stake moves for anyone. The two STANDARD-list cosmology rows tell me the corpus touches cosmology — content the seat must have to do the classification, not a stake. Independent machine check: grep of the packet for the forbidden-list vocabulary I can infer from the master (ΛCDM/LCDM, pattern, the custody-chain names, both engine names, gate, expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting, warn, rests_on, DERIVED_ONLY, R3D) returned zero hits.

SECTION 1 — OUTCOME CLASSES, and Q1

Per-claim (§3): six outcomes, exactly one filed per claim under a stated total precedence (REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group). Exhaustive: for any included claim either the paper states a producing procedure (else NO_DERIVATION_STATED, with the named-but-unspecified case folded in) or it does; each needed input is then PRINTED/STANDARD (attempt proceeds), BLOCKED (cited source not enumerable-pinned or no machine-match at the cited line), or ABSENT (no print, no named source); a started attempt either finishes (WITHIN or FAILED under the total precision test) or cannot (NOT_EVALUABLE, both limbs named). Mutually exclusive by the filing order; co-occurrence is real and is resolved by rule, e.g. a claim with one BLOCKED and one ABSENT input files REPRO_BLOCKED, first in the order. A real claim fitting two conditions exists (that one); a real claim fitting none does not — the precision test is total (stated uncertainty: |reproduced − printed| ≤ stated uncertainty, once, asymmetric handled as the half-width on the side the value falls; none stated: round to the printed numeral at printed precision, half away from zero).

Study-level (§4): eight classes, exactly one filed under a stated total order (R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT, CENSUS_DENOMINATOR_DISPUTED, CENSUS_OUTCOME_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL, CENSUS_COMPLETE). A real tally fitting two exists and is ruled: every claim arithmetic + a missing seed satisfies CENSUS_COMPLETE's C6 limb failure and CENSUS_AUDIT_FAILED — AUDIT_FAILED files, earlier in the order. A tally fitting none does not exist: controls all fail in every seat → R3C2_NO_CLASS; split → CENSUS_CONTROL_SPLIT; enumerations or input lists split → DENOMINATOR_DISPUTED; outcomes split → OUTCOME_DISPUTED; origins split >10% → ORIGIN_DISPUTED; audit fails or seal receipt fails → AUDIT_FAILED; any non-arithmetic outcome or zero denominator → CENSUS_PARTIAL; else CENSUS_COMPLETE. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is labelled INCONCLUSIVE and is forced by a single blocked or absent input anywhere in the corpus — and it takes precedence over CENSUS_COMPLETE, so it cannot be papered over.

Q1 (does §3 under option (c) make every declared outcome reachable AND keep the reproduction question decidable): YES. Hardest cases traced:
- Printed-but-chosen input ("we take β = 1/929.25", used in the recipe): status PRINTED (not verbatim on the closed list), origin CHOSEN via ORIG_CHOICE_STATED with a machine-matched quotation; the arithmetic consumes it (consumption is by status); the claim files exactly one of REPRO_WITHIN_STATED_PRECISION / REPRO_FAILED; lane-side `compute` derives rests_on = USES_CHOSEN. One outcome, one rests_on. Decidable.
- Fitted parameter the paper states ("our fit gives n_s = 0.9649", printed): PRINTED, origin FITTED via ORIG_FIT_STATED; consumed; WITHIN/FAILED; rests_on USES_FITTED. A fit described but with no printed value folds into the recipe: the seat lists the leaves the stated sub-procedure needs (intermediates computed inside the attempt are not ledger inputs); a DERIVED origin attaches to values the paper both prints and derives, with derived_from walked to root origins by script.
- Imported value ("H₀ = 67.4 (Planck 2018)" with Planck pinned and enumerable): PRINTED from that source, origin IMPORTED, ORIG_CITATION to the named source's file and line, machine-matched there; consumed; WITHIN/FAILED; rests_on USES_IMPORTED. No match at the cited line, a non-enumerable or RAW manifest source, or an unpinned source: REPRO_BLOCKED, status BLOCKED, never consumed. No source at all: REPRO_INPUT_ABSENT, origin UNDECLARED with a printed origin_search.
- Closed-list value printed verbatim: STANDARD by the deterministic either/or rule ("the two routes are outcome-identical, and this rule keeps both seats on the same one") — seats cannot split on the boundary.
Every §3 outcome is exhibitable (that is also C0's own job pre-freeze) and the reproduction question stays decidable because admissibility no longer turns on provenance: arithmetic consumes status PRINTED/STANDARD only, and provenance rides beside the verdict as a script-computed field.

SECTION 2 — CONTROLS, and Q2

Every control names an exact code with PASS/FAIL/NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. None can be passed by bare assertion: C1/C2/C3 pass only on exit 0 of printed runs of the pinned script ("a token asserted without the printed run is FAIL"); C4 requires the printed path list and PASS "makes no claim that the list is complete"; C4_PACKET_REDACTED is machine-asserted by the pinned builder (I re-ran it: PASS, byte-identical packet); C5 requires all three commands' live stdout ("a transcribed value in place of live output is FAIL"); C6 requires the printed C6_AUDIT.json with no MISMATCH and no incompleteness ("a token without the artefact is FAIL"). Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes," and §4's stop-class sentence carries the same.

Q2 (is the provenance field checkable rather than asserted): YES, with the floor stated. Planted record test: input printed as "We adopt H₀ = 67.36 km s⁻¹ Mpc⁻¹" in the paper; I file origin DERIVED with ORIG_EQUATION and a verbatim quotation that genuinely machine-matches the cited line. `validate` passes it — the machine checks that the quotation is a substring of the cited line and the value machine-matches, not that the reason code is the right one. What catches it: the mandated second classification ("Every input's `origin` is classified independently by both seats") under the stated tie-break (ORIG_CITATION > ORIG_FIT_STATED > ORIG_CHOICE_STATED > ORIG_MEASURED > ORIG_EQUATION > ORIG_CONSTANT > ORIG_SILENT — "we adopt" is CHOSEN, not DERIVED), so the merged record carries origin_alt/origin_evidence_alt and rests_on is computed under both and marked DISPUTED; and the C6 auditor re-classifies every origin from the pinned sources without sight of earlier work, a MISMATCH failing the audit. The machine alone does not catch it, and the document says exactly that: a precedence-misapplied code "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." Checkable by construction, with the honest floor declared.

SECTION 3 — CIRCULARITY

The inclusion rule (§1) is pattern-free on its face: a numeral the paper asserts as its own result, minus five enumerated excluded kinds; two seats classify independently with tolerance zero and a dispute stop. Exclusion cannot hide a contrary claim: every excluded candidate lands in the exclusion ledger with file, line, numeral and kind, the denominator counts included claims only, and C6's first pass "audits the full candidate and exclusion ledgers against every pinned source — completeness, not just outcomes" by a third seat without sight of earlier work; a quiet exclusion is a completeness disposition against the auditor's own enumeration and files CENSUS_AUDIT_FAILED. Origin classification cannot be steered silently: quotations are machine-matched substrings of cited lines, both seats classify, disputes are carried not reconciled, and the auditor re-classifies. The interpretation cannot reach the evidence: it reads the sealed tallies afterwards under a protocol no seat may open, receipted before limb A (section 5). The residual judgement surface — whether a numeral is "asserted as a result of its own" — is duplicated, zero-tolerance, and audited; that is as closed as a judgement boundary gets.

SECTION 4 — THE BLIND

Machine-asserted as claimed, and I verified the assertion rather than taking it: the pinned builder (sha256 4ed52d4b…, digest matches the on-disk file) re-run in a scratch directory printed C4_PACKET_REDACTED=PASS, reported master sha256 bcc16e32… (my ACCESS_SHA) and produced a packet whose sha256 is b4bd7e37… — byte-identical to the delivered packet and to the pin in R3C2_SEAT_PACKET.sha256. The builder dropped §0/§7/§8/§10 whole and redacted 50 inline spans. My own grep of the packet for the inferable forbidden vocabulary returned zero hits. C4-as-mechanism prevents the pattern record being furnished in the working directory and makes the seat's printed path list the detection; it does not and cannot prove non-access, and the document says so: "This is procedural, not enforced by the filesystem," and "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The accepted floor (the seat can tell provenance is measured with care) is stated with its ruling. The claim matches the delivered mechanism; the limits are stated, not implied away.

SECTION 5 — THE SEAL (§7)

Binds what it claims, before the step it must precede. Receipt P: the interpretation protocol's hash and commit id go to the outside custodian and "limb A does not begin without receipt P" — the protocol is fixed before any enumeration exists to suit. Receipt T: one named tally commit (merged candidate file, exclusion ledger, merged input ledger with compute output) is hashed and receipted, "the receipt names every file in that commit," and "the interpretation protocol is not opened without both receipts." After opening, the custodian independently re-hashes and verifies all four values; the interpretation report must print both receipts and the four verified values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The overclaim guard is present: a local commit is "tamper evidence, not an external custodian," and the limit is stated plainly — "It does not prove Tori had not already guessed the answer — nothing available here could prove that." The relay binds existence and form of both artefacts before comparison; nothing more is claimed.

SECTION 6 — FAIRNESS

Every negative per-claim outcome is worded as a property of the inputs, not the paper: REPRO_FAILED carries "Wording: 'unreproduced from the stated inputs,' not 'error.'" Grep of the master for "error" finds it only in that forbidding rule (line 131) and in §10 history; the packet carries only the rule. No class name asserts more than its definition: REPRO_WITHIN_STATED_PRECISION names exactly its test; REPRO_BLOCKED/REPRO_INPUT_ABSENT are distinguished by the named-source test and neither implies fault; CENSUS_COMPLETE is guarded by "no census is complete over nothing" and is subordinated to CENSUS_PARTIAL ("INCONCLUSIVE, and it takes precedence"). Origin values carry an explicit disclaimer: the reason-code order "is a tie-break by the specificity of the evidence, not a ranking of the values," and the origin list "carries no order of its own."

SECTION 7 — STALL / EXECUTABILITY, and Q4

Q4 (can anything the document describes not be executed as delivered): nothing found undeliverable; each item checked and how:
- Master and packet: sha256 computed (ACCESS_SHA, PACKET_SHA); packet header cites the same master digest; R3C2_SEAT_PACKET.sha256 pins the packet at exactly PACKET_SHA and also pins SEAT_BRIEF.md, r3c2_timeout.py, r3c2_ledger_tools.py, r3c2_build_seat_packet.py — all five pin digests match the on-disk files (shasum run).
- r3c2_ledger_tools.py: exists; digest equals the §3/C4 pin f9b7d3c8…; executed with no args — usage prints the `census` and `validate` subcommands with the C1 schema, the {0,1,2} attempts rule, and the `census … final` behavior (rejects PENDING, requires printed_value/reproduced_value on arithmetic outcomes), matching C1/C2/C3 as written.
- r3c2_lane_tools.py: exists; digest equals the C3 pin 8e990c7a…; usage prints `merge` and `compute` and the rests_on rule (DERIVED_ONLY when every root origin is DERIVED/STANDARD/MEASURED, else the most severe root) matching the master-only span; not seat-given, as designed.
- r3c2_timeout.py: exists; digest equals the §9 pin fbb9bef7…; usage matches §9's literal invocation `/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>`.
- r3c2_build_seat_packet.py: exists; digest equals the C4 pin 4ed52d4b…; executed in a scratch copy — first line C4_PACKET_REDACTED=PASS, then the master and packet digests, exactly as C4 predicts; rebuilt packet digest equals the delivered packet's.
- R3C2_CORPUS_MANIFEST.md: exists; digest equals the §2 pin 300d4da1… (contents are the seats' read set, not the gate's — not read).
- R3C2_INTERPRETATION_PROTOCOL_20260904.md: exists (3,454 bytes); not opened — no seat may, and the gate need not.
- C5 harness commands: `/usr/bin/python3 --version` executed → "Python 3.9.6", exit 0; `shasum -a 256 /usr/bin/python3` executed → exit 0; `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"` could not be executed by me (my sandbox blocks the -c flag) — the sympy package is present in this interpreter's user site (~/Library/Python/3.9/lib/python/site-packages/sympy, stat-verified), and C5 is a LIVE control whose own PASS predicate tests this at dispatch. `which shasum` → /usr/bin/shasum; `which python3` → /usr/bin/python3 on this harness.
- Fields scripts are said to compute: recomputed-vs-declared counts (census, usage-confirmed); origin_alt/origin_evidence_alt and PARENTS_DISPUTED (merge, usage-confirmed); root_origins and per-claim rests_on (compute, usage-confirmed). `validate`'s per-assertion behavior (machine-match, closed-list membership, schema-only) was not exercised on a constructed ledger by me; the tool is digest-pinned and its usage matches the document.
- Rename (per the gate note): `grep -n REPRO_EXACT` on the master gives 15 hits — line 118 is the §3 rename notice inside a SEAT-REDACT span ("named `REPRO_EXACT` until V21; renamed by the principal's ruling"), and lines 534–990 all sit under `## 10` headings; zero governing references. The permitted audit file R3C2_V21_RENAME_AUDIT_20260905.md classifies the same lines under the same rule and records the seat tool and builder carrying the new token only — consistent with the delivered digests. No defect.
- Observation, not a finding (outside the gated text): the builder's stdout labels character counts as "bytes" ("25457 bytes" vs the 25,671-byte file; multibyte UTF-8). The digests — the values that bind — match exactly.

FINDINGS (both COSMETIC — neither changes any class filed, any control's passability on the delivered harness, any reachability, any conclusion, or any tested value)

F1 (COSMETIC). Verbatim: §10.18, "The frozen brief receives the same sentence." Defect: describe-versus-deliver mismatch. The delivered SEAT_BRIEF.md carries a sentence naming both system binaries as in scope (count-only greps: "system binaries" ×1, "in scope" ×1, "usr/bin/python3" ×2, "shasum" ×2) but no "whatever they load" limb (×0), so it is not the same sentence as C4's clause. (My mandate limited me to count greps on the brief; I did not read its prose. §10 binds nothing; C4/C5b in the master and packet carry the full clause; every path in the three C5 commands is named in scope by the brief's own sentence, and interpreter/loader internals are not paths a seat opens, so no control's passability changes.) Exact replacement: make the brief's sentence identical to C4's — "The system binaries C5 names — /usr/bin/python3, /usr/bin/shasum — and whatever they load while executing the mandated commands are in scope and are not 'outside paths'; every other path outside the working directory is." — or reword §10.18 to "the brief receives the same rule in the brief's own words."

F2 (COSMETIC). Verbatim: C5, "Execute and print … `shasum -a 256 /usr/bin/python3`" against C4's "The system binaries C5 names — `/usr/bin/python3`, `/usr/bin/shasum` …". Defect: the command's bare `shasum` is PATH-resolved while the clause names the absolute /usr/bin/shasum; on the delivered harness they coincide (`which shasum` → /usr/bin/shasum), but a PATH resolving `shasum` elsewhere would execute a binary the clause does not name — the same letter-exact trap V24 exists to close, one level down. Exact replacement: C5's third command reads `/usr/bin/shasum -a 256 /usr/bin/python3` (or the clause reads "…`shasum` as resolved on the harness PATH (on the delivered harness, /usr/bin/shasum)…").

Q7 (V24-only)
(a) YES. C4 (master and packet): "The system binaries C5 names — `/usr/bin/python3`, `/usr/bin/shasum` — and whatever they load while executing the mandated commands are IN SCOPE and are not 'outside paths' under this control; every other path outside the working directory is." C5b: "`/usr/bin/python3`, `/usr/bin/shasum` and what they load are `IN_SCOPE` (C4)." The brief names the same two binaries in scope (count-verified). All three C5 commands' own paths are in scope under both texts; I executed two of the three on this harness (exit 0) and stat-verified sympy for the third.
(b) No unintended path opened, and none needed left closed. "Whatever they load" is bounded to the mandated commands (C5's three; the validate/census runs; the wrapper); the stretch readings — environment-influenced loads (PYTHONPATH/PERL5LIB/DYLD_…) or a symlinked working-dir file — require the seat to construct an outside path, which C4 already forbids ("Do not construct a path outside it") and which the printed path list and pre-launch dispatch inventory would show; that is the self-report floor the document declares, not a new hole. Needed paths are closed nowhere: the wrapper and both scripts live in the working directory and are pinned; `/usr/bin/python3` (executed and read) and `/usr/bin/shasum` (executed) are named; sympy is present in the 3.9 user site. Residual wording notes are F1 and F2, both cosmetic.
(c) Within my permitted read set, the V24-identifying changes are exactly: the header's V24/§10.18 references, the V24 row in the §10 table, §10.18 itself, and the two operative clauses (the C4 sentence and its C5b echo), which the rebuilt packet also carries (verified by rebuilding: digest-identical). "No class, control code, threshold, precedence, taxonomy or rule of interpretation changes" is §10.18's assertion; I could not independently diff against the signed V23 file because my mandate confines full reads to the master, the packet, and the rename audit. Nothing else different is visible within the files I may read.

R3C2_V24_GATE_COMPLETE
