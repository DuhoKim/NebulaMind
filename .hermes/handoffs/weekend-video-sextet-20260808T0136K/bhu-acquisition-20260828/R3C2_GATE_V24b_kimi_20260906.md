ACCESS_SHA=972e01d122149fac1dc9d702130a20c1fdda9e73cebed338e6435beea0916115
PACKET_SHA=7521fa5da2b2eb46de3aed0e310433ec166e597f30437ede9749027ac8b2d9b7
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level residue at the accepted floor: the corpus is cosmology (the Planck-2018 closed STANDARD list), failures are anticipated as possible (negative wording pre-bound), and provenance is visibly classified with care — no hypothesis, comparison model, or preferred outcome is inferable
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=statable only as an integrity check: "Every input's `origin` is classified independently by both seats" (packet §5/C3), "the auditor re-classifies the record from the pinned sources" (packet §5/C3), and disagreement ">10% of included claims" files CENSUS_ORIGIN_DISPUTED (packet §4) — what the recorded origin is compared or weighed against is not stated
C5_EXECUTABLE_UNDER_SCOPE=NO — /usr/bin/python3 --version and /usr/bin/shasum -a 256 /usr/bin/python3 ran clean here (exit 0), but on this stock macOS host the mandated `import sympy` resolves from the user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages, outside the five licensed roots, so a seat obeying C4 and the brief to the letter cannot pass C4/C5b while running it; the text mandates the command without stating the provisioning it now requires.

STEP 1 — PACKET-ONLY ANSWERS (given before the master was read)

Q5 (before Q3): No sentence in the packet tells me any outcome is more consequential than any other, to the lane or to anyone. The only consequence-bearing sentence is scope §11: "No tier, warrant token, standing or stamp moves." — which asserts the opposite, that nothing moves for anyone on any outcome. §4's filing order ("file the first in this order") is precedence, not consequence; "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" labels information value, not stake. Closest approach: the fairness clause "Wording: 'unreproduced from the stated inputs,' not 'error.'" shows someone pre-bound how a negative may be written — it constrains wording, not weight. No sentence does more.

Q6 (before Q3): Statable only as a check. The packet says what the study DOES with `origin` after recording: "Every input's `origin` is classified independently by both seats" (§5/C3); "that printed `origin_search` (query, files, matches) is the mechanism by which the second seat and the auditor judge the search adequate ... and the auditor re-classifies the record from the pinned sources" (§5/C3); and §4 files CENSUS_ORIGIN_DISPUTED when the two seats' classifications "disagree on inputs affecting more than 10% of included claims". Also §1: "The reproduction verdict and the provenance fields are recorded separately." No sentence states any further use — what the record is combined with, compared against, or weighed into. Purpose beyond the cross-reader integrity check: cannot state.

Q3 (hypothesis-reachability, packet alone): Every inference I can draw, with the leaking sentence:
(a) The corpus is cosmology. The closed STANDARD table prints the Planck 2018 baseline verbatim ("H₀ 67.36 ± 0.54, Ω_m 0.3153, Ω_Λ 0.6847, σ₈ 0.8111, τ 0.0544, ln(10¹⁰ A_s) 3.044, age 13.797 Gyr") and the sentence "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." A census whose admissible constants are cosmological parameters enumerates cosmology texts.
(b) Failed reproduction is a live, anticipated outcome whose reception was considered. "Wording: 'unreproduced from the stated inputs,' not 'error.'" — the clause defends the framing of REPRO_FAILED before any failure exists. Any census implies failures are possible; this says no direction.
(c) Provenance is the axis the designers expect to be contested: seven origin values, machine-matched quotations, "Every input's `origin` is classified independently by both seats", a tie-break order "by the specificity of the evidence", disagreements "reported, never reconciled", and a study-stop above 10%. That is effort spent where disputes are expected — but it reveals care, not a preferred answer.
(d) The dispute machinery (two seats, zero-tolerance enumeration reconciliation, external post-receipt seed, 100% audit of the arithmetic group "without sight of earlier work") tells me the tally is expected to be challenged by someone. That is an inference about the social setting, not about the numbers.
What I cannot infer: any hypothesis, any comparison model, any preferred outcome, any expected pattern of per-claim results, and what the recorded provenance is ever weighed into. The stake I can reconstruct is epistemic/reputational only — "No tier, warrant token, standing or stamp moves." This is the hypothesis-reachability test answered: the packet gives content (cosmology, a careful provenance taxonomy) and procedural contestation signals; it gives no stake in any outcome.

STEP 2 — THE MASTER, JUDGED AS WRITTEN (option (c) settled; §10 treated as history binding nothing)

1. OUTCOME CLASSES — SOUND. Q1: YES.

Exhaustive and mutually exclusive as filed. The six §3 conditions genuinely co-occur (my constructions below), and the declared total order — "Where more than one terminal condition holds, file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group" — assigns exactly one filed outcome in every case I could build:
- A claim stating no equation whose equation would also need an unprinted, untraced input satisfies REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT; the order files the first. One outcome.
- A claim with one input traced to a non-manifest source and another traced to no source satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT; the order files REPRO_BLOCKED. One outcome.
- A claim whose 120-second attempt would also have filed REPRO_FAILED had it finished: the order files REPRO_NOT_EVALUABLE only when the arithmetic "could not be completed"; a completed attempt is in the arithmetic group. No overlap survives.
Fitting none: I could not construct one. Every §2 path terminates — recipe stated or not (REPRO_NO_DERIVATION_STATED, with "a procedure named but not specified" explicitly routed there), each input PRINTED/STANDARD/BLOCKED/ABSENT by the named-source test, and a completed attempt decided by the stated-precision rule (round half away from zero at the printed precision; |reproduced − printed| ≤ stated uncertainty taken once; asymmetric uncertainty = the half-width on the side the reproduced value falls).

Two study-level classes at once: an audit MISMATCH over a tally that also contains a blocked claim satisfies CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the §4 order (R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT, CENSUS_DENOMINATOR_DISPUTED, CENSUS_OUTCOME_DISPUTED, CENSUS_ORIGIN_DISPUTED, CENSUS_AUDIT_FAILED, CENSUS_PARTIAL, CENSUS_COMPLETE) files the audit failure first, which is also the chronological order of detection. A control split with any downstream condition files CENSUS_CONTROL_SPLIT. None: none found — every terminal state of the described procedure lands in one of the eight, including a zero denominator ("A denominator of zero files CENSUS_PARTIAL with the empty enumeration named; no census is complete over nothing.") and a never-supplied audit seed ("the study files CENSUS_AUDIT_FAILED with the missing seed named").

INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is labelled "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" and is reached by any single non-arithmetic per-claim outcome (one unobtainable cited source suffices) or by an empty corpus.

Q1 (asked directly): Does §3's definition as written under option (c) make every declared outcome reachable and keep the reproduction question decidable? YES. The four earlier rounds failed because admissibility gated the attempt; under option (c) admissibility is gone from the verdict — "Arithmetic consumes records according to status PRINTED or STANDARD" whatever the origin — and provenance is a computed field, so nothing is decided twice. My hardest cases, each traced to exactly one outcome and one rests_on value:
- Printed-but-chosen input (the case that broke the old design): the paper prints "we take β = 1/929.25" and its recipe. §2 step 3: the value appears in the paper → status PRINTED (it is not on the closed list verbatim); C3: origin CHOSEN via ORIG_CHOICE_STATED with the machine-matched quotation. §2 step 4 consumes it ("every ledger record with status PRINTED or STANDARD", "whatever its origin"). The arithmetic completes → exactly one of REPRO_WITHIN_STATED_PRECISION / REPRO_FAILED by the stated-precision rule. rests_on: the root origin is CHOSEN → USES_CHOSEN (master-only fixed severity order, computed by the pinned lane script). One outcome, one rests_on.
- A fitted parameter the paper states ("our fit yields Ω_m = 0.315", not the closed-list string): status PRINTED, origin FITTED via ORIG_FIT_STATED; consumed; arithmetic group; rests_on USES_FITTED. The §2 tie rule ("Where a value the paper prints is on the closed list verbatim, file STANDARD; otherwise PRINTED — the two routes are outcome-identical") keeps both seats on one status; the outcome is identical either way.
- An imported value: the paper prints no value but names a source that IS an enumerable text of the manifest, and the value machine-matches at the named source's cited line → "classified PRINTED from that source, with origin IMPORTED, origin_evidence ORIG_CITATION cited to the named source's file and line" (§2); consumed; arithmetic group; rests_on USES_IMPORTED. No machine-match at the cited line, or a source not in the manifest → REPRO_BLOCKED, status BLOCKED, origin IMPORTED with ORIG_CITATION to the claiming paper's naming sentence, no value, "the arithmetic never consumes it". Mechanical both ways.
- The imported-choice seam (codex's pending D1): the named source's own line reads "we choose a = 2". §2's rule for this route is unconditional — origin IMPORTED, evidence ORIG_CITATION at the named source's line — and C3's tie-break files ORIG_CITATION first in any case; validate passes (value machine-matches, quotation is a substring). One record, rests_on USES_IMPORTED, no seat split for a seat obeying the document. Determinate as written; the tie-break's rationale sentence does not describe this evidence shape — recorded as COSMETIC finding F3 (already escalated to the principal in §10.18 as a rule-change question).
- Asymmetric uncertainty: reproduced value falls on the low side; the test uses the low-side half-width, once. Decidable.
Reachability: no declared class has an empty exclusive domain as written — REPRO_BLOCKED vs REPRO_INPUT_ABSENT are separated by the named-source test; REPRO_NO_DERIVATION_STATED covers the named-but-unspecified procedure; REPRO_NOT_EVALUABLE has two named limbs (SYMBOLIC_TIMEOUT, MACHINERY_UNAVAILABLE); the two-member arithmetic group is decidable mechanically; all eight §4 classes have concrete triggers (enumerated above). C0 exists precisely to exhibit each of these with two agreeing independent seats before any freeze, and the text does not freeze without it.

2. CONTROLS — SOUND, with the stated self-report floors. Q2: YES, checkable, floors printed.

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE.
Passed by assertion? C1 requires the printed script run ("Print its command, stdout and exit status", "PASS only on exit 0"). C2/C3: "C3_NO_SUBSTITUTION=PASS only on exit 0 from every printed run in the artefact; a token asserted without the printed run is FAIL." C5: "any non-zero exit, missing output, or a transcribed value in place of live output is FAIL." C6: "C6_AUDIT_SAMPLE=PASS only if that artefact exists, is printed, and carries no MISMATCH and no incompleteness; a token without the artefact is FAIL." C4_PACKET_REDACTED is machine-asserted by the pinned builder ("If any survives, the packet is not written and C4_PACKET_REDACTED=FAIL"). The two controls that rest on the seat's own printed path list — C4_SEAT_ISOLATION and C5B_NO_CROSS_LANE — can be passed by an incomplete list; the document says so in terms ("PASS ... makes no claim that the list is complete", "C4_SEAT_ISOLATION is a self-reported control with a structural aid, and is labelled so"). That is a disclosed floor, not a hidden assertion channel. C0's artefact is the exhibition table, authored by one independent seat and verified by a second; the lane owner "checks only that every declared outcome and condition has a row and does not judge reachability".
Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes." plus "Once a stop class applies, later limbs are unreached and their controls are NOT_RUN." Handled.

Q2 (asked directly): Is the provenance field checkable rather than asserted? YES — machine-checked where machine-decidable, independently re-derived where not, and the boundary between the two is printed in the document. My wrong-but-plausible records:
(i) The paper prints "We adopt H₀ = 67.4 from Planck (2018)"; a seat files origin CHOSEN with ORIG_CHOICE_STATED quoting "We adopt H₀ = 67.4". The quotation machine-matches, so validate does NOT catch it. What catches it: first, the document's own tie-break makes the record mis-authored — "Where more than one reason code matches the cited sentence, file the first in this order: ORIG_CITATION, ORIG_FIT_STATED, ORIG_CHOICE_STATED, ... — a sentence that names an external source for the value is a citation whatever else it says" — so a seat obeying the text files IMPORTED; second, the second seat's independent classification (a surviving disagreement is carried as ORIGIN_DISPUTED, above 10% of included claims it stops the study); third, "the auditor re-classifies the record from the pinned sources" and a MISMATCH files CENSUS_AUDIT_FAILED. The residual is printed in the master: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away."
(ii) An ORIG_SILENT/UNDECLARED record over a value whose origin is in fact stated: caught by the printed origin_search rule — "a search of one query with no variants is not adequate, and the auditor re-classifies the record from the pinned sources".
(iii) A STANDARD record with value "67.4": machine-caught — "A STANDARD record carries symbol = the ledger key and value = the exact string printed in the value column; validate compares strings."
(iv) A PRINTED record whose value does not appear at its cited line: machine-caught — "each PRINTED value machine-matches the text at its cited source line and each verbatim quotation is a substring of that line".
(v) A DERIVED record whose quoted equation does not in fact produce the value: not machine-decidable; caught by the second seat and C6 — "completeness of a claim's input list against the paper's equation is seat-authored and audited under C6, not machine-checked". Stated, not implied away.
root_origins and rests_on are computed by the byte-pinned lane script (digest verified below), which REJECTS a ledger arriving with either field already set (exit 2) and FAILS dangling parents, cycles, and parentless DERIVED records (exit 1) — so the lane cannot hand-write the fields and the seats cannot smuggle them ("a rests_on value present in a seat-authored input ledger fails this control").

3. CIRCULARITY — SOUND. The lane's expectation has no operative channel to the evidence:
- Inclusion: the §1 rule is enumeration, not judgement of content — "a passage in a pinned source that prints a numeral the paper asserts as a result of its own", with excluded kinds named; two independent seats enumerate from the packet alone; any surviving disagreement stops the study (CENSUS_DENOMINATOR_DISPUTED) rather than being resolved by the lane.
- Exclusion: every exclusion is a ledger row with file, line, numeral and a closed kind vocabulary; the C6 auditor "first audits the full candidate and exclusion ledgers against every pinned source — completeness, not just outcomes", and any incompleteness files CENSUS_AUDIT_FAILED. A contrary claim cannot be quietly excluded: it would have to be missed identically by two independent seats AND then survive a completeness audit against the sources.
- Origin classification: seat-authored with machine-matched quotations, independently duplicated, disputes carried not reconciled, re-classified by the auditor; the lane's own scripts only merge and compute on what the seats filed, and the compute step refuses pre-written provenance fields.
- The interpretation step: the protocol is fixed and receipted (receipt P) BEFORE limb A begins — before any tally exists to suit — and the tally is receipted (T) before the protocol is opened; the interpretation reads the sealed artefacts, not the lane's hopes (see 5).
The two soft spots are disclosed in the text, not hidden: the path list is self-reported (a seat could in principle read an absolute path into the lane undetected — stated at C4), and reason-code choice has a human floor (stated at C3). No channel runs from the hypothesis to inclusion, exclusion, origin, or interpretation as written.

4. THE BLIND — SOUND, empirically corroborated on the delivered packet.
Machine-asserted as claimed? The builder is byte-pinned in C4 (sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3 — verified against the delivered file), it "drops §0, §7, §8 and §10 whole, strips every span marked SEAT-REDACT", and asserts the forbidden list before writing. I probed the delivered packet (read-only) for the enumerated list — old tokens (REPRO_EXACT, REPRO_AFTER_CHOICE, NOT_ATTEMPTED), computed-field names (rests_on, root_origins, DERIVED_ONLY, USES_), the pattern word, the comparison model, consequence words (expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting, warn), engine names, custody-chain names, R3D, "gate", "comparison": ZERO hits. The packet's own header ("Built from master sha256 972e01d1...") matches my ACCESS_SHA of the gated master, so the packet the seats get was built from the bytes I gated. The packet visibly lacks §0/§7/§8/§10 and every redacted span I saw in the master (the rests_on machinery, the severity order, the custody names).
Does C4 prevent what it claims? It claims to certify the dispatch copy and the printed list — "the dispatch record proves that the seat was not furnished the pattern record in its working directory" — and that much the mechanism does (redacted copy outside the lane, inventory listed before launch). It does NOT claim to prevent a seat opening an absolute path into the lane, and says so: "This is procedural, not enforced by the filesystem ... C4_SEAT_ISOLATION is a self-reported control with a structural aid, and is labelled so."
Are its limits stated honestly? Three explicit ones: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed"; the path list's completeness is not claimed; and "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The accepted leak floor (the principal's 18:52 ruling: "a gate finding that a seat can tell provenance matters is the accepted floor") is recorded in C4 itself. My packet-only inferences (STEP 1) sit at or below that floor.

5. THE SEAL (§7) — SOUND. It binds what it claims, before the steps it must precede:
- Receipt P precedes the evidence: "Before limb A begins, Tori commits the interpretation protocol and sends its hash and commit id to Blanc ... limb A does not begin without receipt P." The protocol is therefore fixed before any tally exists to suit — the earlier "committed, so unwritable" overclaim is gone; the text now rests unwritability on the receipt: "the receipt, not the commit alone, is what makes it unwritable to suit the tally."
- Receipt T precedes the interpretation: "After the tally — one commit containing the merged candidate file, the exclusion ledger, and the merged input ledger with its compute output — is committed and before the protocol is opened, Tori sends the tally hash and tally commit id; Blanc receipts them as T, and the receipt names every file in that commit; the interpretation protocol is not opened without both receipts. The relay is complete only when it is receipted."
- Verification after opening: "Blanc independently re-hashes the tally and the protocol and verifies all four values against the two receipts, and the interpretation report must print both receipts and the four verified values"; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4's definition names this case) and "voids the comparison".
- Why a receipt and not a commit: "a local commit in Tori's own custody can be amended or reset, so it is tamper evidence, not an external custodian".
- Limit stated, not implied away: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind, and the record should say so rather than imply more." A shadow pre-computation before receipt P remains logically possible and the text says exactly that.
The seal binds the protocol before the evidence and the tally before the interpretation, with an external custodian and a named failure class. Sound as written.

6. FAIRNESS — SOUND. Every negative per-claim outcome is worded to the stated inputs, never to the paper's correctness:
- REPRO_FAILED: "Wording: 'unreproduced from the stated inputs,' not 'error.'"
- REPRO_BLOCKED blames the census's own scope, not the source: "whether that source is obtainable elsewhere is irrelevant, because the census may not open or consume it".
- REPRO_INPUT_ABSENT and REPRO_NO_DERIVATION_STATED are factual ("neither printed nor traced to any named source"; "states no equation or computational procedure that could produce it"); REPRO_NOT_EVALUABLE names the lane's own limits (a timeout, missing machinery).
No class name asserts more than its definition. The one name that did — REPRO_EXACT — is renamed precisely for that reason and survives only as the redacted rename notice and §10 history (verified by grep: 16 hits, line 118 the notice, the other 15 all under ## 10 headings; zero governing references). "error" appears in operative text only inside the fairness clause's own quotation. The study-level names describe filing states, not verdicts on any paper; "No study-level outcome is a verdict about the pattern" (master-only, and nothing seat-visible contradicts it).

7. STALL / EXECUTABILITY — ONE SUBSTANTIVE FINDING (F2), ONE MINOR (F1), ONE COSMETIC (F3). Q4 and Q7 answered here.

Q4 (asked directly): Can anything the document describes not be executed as delivered? I checked each named thing as follows:
- r3c2_ledger_tools.py — exists; sha256 f9b7d3c818647e6e3e10e19013db3a042247fc141ee01282b8928de06372c9ac matches the §3 pin byte-exactly; executed with no arguments: prints usage naming exactly the census and validate subcommands (no compute, no merge — as §3/C3 claim), the C1 schema and count checks, the validate assertion set (schema-only fields, status and origin vocabularies, reason-code/origin pairs, no value on ABSENT/BLOCKED, BLOCKED carries IMPORTED/ORIG_CITATION, quotation-substring, closed-list STANDARD strings, derived_from existence/acyclicity/parents, origin_search on ORIG_SILENT), exit 2 on usage. Consistent with every check the document attributes to it.
- r3c2_timeout.py — exists; sha256 fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 matches the §9 pin; executed with no arguments: usage confirms the `/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>` form, monotonic deadline, prints wrapper command/child stdout/stderr/exit status, SYMBOLIC_TIMEOUT with exit 124 on the deadline. As §9 describes.
- r3c2_lane_tools.py — exists; sha256 8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848 matches the C3/§9 pin. Lane-side; not executed by this seat (its merge/compute behaviour is attested by the pin and the printed-run requirement).
- r3c2_build_seat_packet.py — exists; sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3 matches the C4 pin. NOT executed: running it writes the packet, a side effect reserved to the lane ("Tori re-runs the builder and re-checks the pin before any dispatch"). Its delivered output was verified read-only instead (zero forbidden-list hits; packet header matches the gated master).
- R3C2_CORPUS_MANIFEST.md — exists; sha256 300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24 matches the §2 pin. Contents not opened (outside my permitted reads).
- R3C2_SEAT_PACKET.sha256 — exists (test -f). Contents not opened (outside my permitted reads); the packet-to-pin match is therefore not checked by this seat — noted, not a defect: the master states the lane owner re-checks the pin before dispatch.
- r3c2_seat_packet/R3C2_SEAT_PACKET.md and SEAT_BRIEF.md — exist; read in full (the two permitted seat-side reads).
- R3C2_INTERPRETATION_PROTOCOL_20260904.md — exists (test -f). Sealed by design; not opened; §7 pins it by receipt P, not by an in-document digest, so there is nothing further for a gate to check pre-run.
- r3c2_seat_sandbox.sb — exists (test -f). Named only in §10.18 (history); contents not opened.
- The C5 harness binaries — /usr/bin/python3 (Python 3.9.6, Mach-O universal, exit 0) and /usr/bin/shasum (used for both digests in this report; digest of the interpreter b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9, exit 0) — run on this host (macOS 26.6.2, arm64).
- Every literal command in the operative text names files that exist with matching pins; every path a seat is told to use (the working directory, `.` as sole sources_dir, r3c2_seat_packet/…) exists. Fields a script is said to compute (origin_alt, PARENTS_DISPUTED, root_origins, rests_on) are computed by the byte-pinned lane tool; no census has been run and none was run by me, so the computation itself is attested by pin and by the mandated printed runs, not re-derived here — that is the delivered mechanism.
- The one thing that cannot be executed as delivered on a stock-provisioned host is C5's second command under the bounded scope clause — finding F2 below.

Q7 (this version): (a) Can a seat that obeys C4 and the brief to the letter now execute every C5 command? Textually the clause licenses exactly what the three commands need on a correctly provisioned host — C4: "The two system binaries C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — and the files they load from the system runtime locations only (`/usr`, `/System`, `/Library`, `/private/var/folders`, `/dev`) while executing the exact mandated commands are IN SCOPE and are not 'outside paths' under this control; a startup, configuration or import file loaded from any other location — the working directory's parents, the lane, `/tmp`, or any user-writable path — is an outside path and `FAIL`; every other path outside the working directory is `FAIL`." C5b: "`/usr/bin/python3`, `/usr/bin/shasum` and the files they load from the system runtime locations C4 lists are `IN_SCOPE`; anything loaded from elsewhere is `OUT_OF_SCOPE` (C4)." Brief: "The two system binaries the packet's C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — and the files they load from the system runtime locations only (`/usr`, `/System`, `/Library`, `/private/var/folders`, `/dev`) while executing the exact mandated commands are in scope; anything loaded from elsewhere is an outside path." C5 mandates: "Execute and print `/usr/bin/python3 --version`, `/usr/bin/python3 -c \"import sympy; print(sympy.__version__)\"`, and `/usr/bin/shasum -a 256 /usr/bin/python3`". The 10:17 dilemma (the harness command itself naming an outside path) is cured: the binaries and their licensed loads are in scope. But on the only host I can test, the second command's dependency resolves OUTSIDE the licensed roots — so under the letter, a seat here still cannot run all three and keep C4/C5b clean. Empirical answer: NO as this host is provisioned (finding F2); YES on a host whose sympy resolves inside the five roots.
(b) Paths the clause opens or closes: it opens the two binaries and their loads from the five system roots during mandated commands — nothing else; /tmp, the lane, the working directory's parents and every other user-writable path stay closed. That is the correct shape, with one wording seam (F1) and one provisioning gap the text does not name (F2).
(c) Anything else different from V23: the document declares its change set in §10.18 — the C4 clause, the C5b echo, the brief clause, C5's third command path-qualified (`/usr/bin/shasum -a 256 /usr/bin/python3`, present in both master and packet), and the §10.18 record itself — and I verified each of those texts as present and consistent in the master, the packet and the brief. Nothing else in the operative sections differs from the V23 state described in §10.15–§10.17 (class set, precedence, thresholds, taxonomy, controls' logic, seal). A byte-level diff against the signed V23 file is outside my permitted reads; within that limit, nothing else.

Q7 addendum (round 2) — the bound (/usr, /System, /Library, /private/var/folders, /dev):
(i) Sufficient to close the startup/import-path hole? YES as a written rule: a startup, configuration or import file loaded from /tmp, the lane, the working directory's parents, or any user-writable path outside the five roots is now an outside path and FAIL — the unbounded "whatever they load" reading is gone. Two residuals, both stated or structural rather than hidden: /private/var/folders contains the user-writable per-user temporary directories, so a planted file there would be nominally licensed (the prefix is needed — stock dyld/python touch it — and it is the same prefix the kernel sandbox profile permits); and the control remains self-reported, so a load omitted from the printed list is undetected — the accepted floor, unchanged by the bound.
(ii) Too tight for a stock macOS python3 + sympy? On this host, YES — it excludes the one path the mandated import actually needs: /Users/duhokim/Library/Python/3.9/lib/python/site-packages. Evidence: /usr/bin/python3 is the stock CLT interpreter (3.9.6); `pip`'s default non-root target for it is the user site (ENABLE_USER_SITE: True, USER_SITE exists and precedes the framework site-packages in sys.path); sympy and mpmath exist ONLY there (nothing in /Library/Python — which does not exist — and nothing in the CLT framework site-packages, which holds only pip/setuptools/wheel and four small libs); `/usr/bin/python3 -m sympy` locates and imports the package (erroring only on __main__), proving the resolution. So on a default-provisioned stock Mac the mandated import loads import files from a user-writable path outside the five roots: an honest C5b list FAILs, C4 FAILs — and under the kernel sandbox profile that permits only the five prefixes, the import cannot even resolve. (My own execution of the exact `-c` command was blocked by my sandbox's approval gate; the evidence above is on-disk resolution plus the `-m` import probe.) The bound itself is NOT too tight for a correctly provisioned host: every stock load of all three commands — the CLT framework under /Library/Developer/CommandLineTools/…, the dyld cache under /System, the runtime roots — sits inside the five prefixes. What is missing is the sentence saying sympy must be provisioned there.
(iii) Consistent between C4, C5b and the seat brief? The five locations are identical in all three copies, and the brief carries the same qualifier as C4 ("while executing the exact mandated commands"); C5b omits the qualifier but defers with "(C4)", so the qualifier governs everywhere. Not interchangeable-wording but convergent — except that the qualifier's antecedent is unstated (F1).

FINDINGS (quote — defect — exact replacement)

F1 (minor; not cosmetic — it can decide whether C2/C3 run to PASS for a letter-literal seat, and this lane has already produced one):
Quote (C4, and equivalently the brief): "…the files they load from the system runtime locations only (`/usr`, `/System`, `/Library`, `/private/var/folders`, `/dev`) while executing the exact mandated commands are IN SCOPE…" — versus C5b's copy, which drops the qualifier: "…and the files they load from the system runtime locations C4 lists are `IN_SCOPE`…".
Defect: "the exact mandated commands" has no explicit antecedent. Read naturally it covers every command the document mandates a seat to run (the C1 census runs, the C2/C3 validate runs, C5's three, the §9 wrapper) and all three copies agree; read at its narrowest — the reading a hyper-literal seat applied to the signed V23 at 10:17 — it licenses loads only during C5's three commands, and the validate/census runs' interpreter loads from /System and /Library are unlicensed again, re-creating the abort at C2. The three copies are convergent but not interchangeable.
Exact replacement: in C4 and in the brief, replace "while executing the exact mandated commands" with "while executing the commands this document mandates the seat to run (the C1 census runs, the C2/C3 validate runs, the C5 harness commands, and the §9 wrapper invocations)"; in C5b, after "the files they load from the system runtime locations C4 lists", append "while executing those same mandated commands".

F2 (substantive; not cosmetic — on a stock-provisioned host it decides whether the mandated harness can pass at all):
Quotes: C4/C5b/brief bound the two binaries' loads to "the system runtime locations only (`/usr`, `/System`, `/Library`, `/private/var/folders`, `/dev`)", with "a startup, configuration or import file loaded from any other location — … or any user-writable path — is an outside path and `FAIL`"; C5 mandates "`/usr/bin/python3 -c \"import sympy; print(sympy.__version__)\"`".
Defect: the document mandates a command whose dependency's default install location the new bound excludes. On this stock host (CLT python 3.9.6) sympy and mpmath exist only in the user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages — pip's default non-root target — which is outside the five roots and user-writable; the import resolves from there (verified on disk and by import probe), while no sympy exists in any licensed root. As delivered to such a host, C5's second command cannot be run without an honest seat printing OUT_OF_SCOPE rows (C4/C5b FAIL) — or, under the kernel sandbox profile permitting only the five prefixes, without the import failing outright (C5 FAIL). The bound is right; the text never states the provisioning it now requires, so C5's passability silently depends on an undocumented host property.
Exact replacement (one sentence appended to C5 after the command list): "The dispatch host's sympy must resolve from within the system runtime locations C4 lists (stock CLT location: /Library/Developer/CommandLineTools/Library/Frameworks/Python3.framework/Versions/3.9/lib/python3.9/site-packages); the lane owner prints the resolved sympy path in the dispatch record before launch, and a host whose sympy resolves elsewhere is not dispatched."
(Equally acceptable operationally: provisioning the seat hosts accordingly with no text change — but then this report's token stands as the record of why.)

F3 (COSMETIC — changes no class filed, no control's passability, no reachability, no conclusion and no tested value):
Quote (C3 tie-break rationale): "— a sentence that names an external source for the value is a citation whatever else it says —".
Defect: for an input §2 routes through a pinned enumerable source, the cited evidence sentence is the named source's OWN line (e.g. "we choose a = 2"), which names no external source, so the rationale does not describe the case it governs. The ORDER still files ORIG_CITATION first and §2's rule fixes origin IMPORTED/ORIG_CITATION unconditionally, so the filed record is unchanged either way. (Already escalated to the principal as a rule-change question in §10.18 — codex D1 — so no lane action is expected; recorded here for completeness.)
Exact replacement: append to the rationale sentence: "— and for an input §2 routes through a pinned enumerable source, the named source's own line is the citation".

OBSERVATIONS (no change asked)
O1: C6 as written re-derives outcomes and re-classifies origins but does not recompute rests_on. By construction the blind auditor cannot hold the severity rule (master-only; moving it seat-ward was the V11 leak repair). Every input to rests_on is audited, and the mapping is a fixed public rule applied by a byte-pinned script whose runs are printed. Deliberate and consistent; noted so it is chosen, not overlooked.
O2: The packet's pin file (R3C2_SEAT_PACKET.sha256) exists but was not opened (outside my permitted reads); packet-to-pin match unverified by this seat. The packet header's embedded master digest equals my ACCESS_SHA.
O3: My execution of C5's exact second command was blocked by my own sandbox (approval gate on `python3 -c`); evidence for F2 is on-disk resolution and an import probe (`/usr/bin/python3 -m sympy` located and imported the package), not the verbatim command. The other two C5 commands were executed and printed above.
O4: The builder was not executed (running it writes the packet — a lane action with side effects). Its pin matches the C4 pin and its delivered output passed the read-only forbidden-string probe described in section 4.
O5: Rename verification (required note): `grep -n REPRO_EXACT` on the master returns 16 hits — line 118 is the redacted rename notice ("(named `REPRO_EXACT` until V21; renamed by the principal's ruling)") and the other 15 (lines 537, 702, 758, 803, 826, 852, 870, 898, 927, 949, 957, 968, 982, 991, 993) all lie under ## 10 headings. Governing references to the old token: 0. Consistent with R3C2_V21_RENAME_AUDIT_20260905.md, whose line-3 header hit no longer exists under the V24 header and whose tool/builder digest claims still match the delivered bytes. Old-token sweep: REPRO_AFTER_CHOICE and NOT_ATTEMPTED appear only in redacted retirement notes (lines 126, 170) and ## 10; C4_PATTERN_BLIND / "pattern-blind" only in ## 10; "error" in operative text only inside the fairness clause's own quotation (line 131).

VERDICT
The design as written is sound on classes, controls, circularity, the blind, the seal and fairness; the V24 scope clause cures the signed text's 10:17 defect in the right shape. Two repairs are required before signature — F1 (state the qualifier's antecedent in all three copies) and F2 (state the sympy provisioning the bounded scope now requires, one sentence) — plus one cosmetic (F3, already the principal's pending item). Until F2 is applied or the hosts are provisioned, the harness remains not-executable-under-scope on a stock Mac, which is why line 7 reads NO.

R3C2_V24B_GATE_COMPLETE
