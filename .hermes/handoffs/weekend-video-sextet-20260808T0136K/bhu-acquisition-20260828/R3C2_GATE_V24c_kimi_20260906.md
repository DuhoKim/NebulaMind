ACCESS_SHA=4609d97973bb28f9740ac1d6a48f9443c33d08de3673c131ec806451a9f1ec6b
PACKET_SHA=4043cd72d1e84f5cfaa0e479e1311df3799c2861545e9748f78476ed548d3cee
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=NONE — no hypothesis, comparison model, preferred outcome or "pattern" inferable; only content-level residue: the corpus is cosmology (the Planck-2018 STANDARD table) and provenance is visibly handled as mattering (seven origins, quotations, two readers)
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader integrity and audit attribute only: recorded, classified independently by both seats, disputes reported and stopping the census above 10% of included claims, re-classified by the C6 audit; the arithmetic never consumes it — from "origin is one recorded attribute of a ledger record, beside status, value, source_file and source_line", "Arithmetic consumes records according to status PRINTED or STANDARD", §4 class 7 and C6's "re-classifying every input's origin from the pinned sources"; no sentence shows any use beyond that
C5_EXECUTABLE_UNDER_SCOPE=NO — the five commands all run and exit 0 on this host (verified live), but C4's own sentence "A startup, configuration or import file loaded from any other location — /usr/local, /tmp, the working directory's parents, the lane, or any other user-writable path — is an outside path and FAIL" condemns loads this host performs on every mandated python3 run (two user-site startup hooks) and on the sympy import (the mpmath dependency tree), none of which the admission clause covers, so C5B_NO_CROSS_LANE cannot PASS under the letter.

Seat: kimi. Date: 2026-09-06. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V24c, living draft).
Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged the DESIGN only; no derivation run by me.
Disclosure of method: my own tool sandbox blocks `python3 -c` and heredocs, so C5 commands 2 and 4
were run as `/usr/bin/python3 /tmp/r3c2_sympy_probe.py` (same import resolution: user site enabled,
same sys.path semantics for module loading) and `/usr/bin/python3 /tmp/r3c2_sympy_modules_probe.py`.
Commands 1, 3 and 5 were run literally. All exits 0.

=====================================================================
STEP 1 — ANSWERED FROM THE PACKET ALONE, BEFORE THE MASTER WAS READ
=====================================================================

Q5 (answered first, before Q3). Can I tell, from the packet alone, that any outcome is more
consequential than any other? NO. No sentence ranks any outcome by weight. The only
consequence-adjacent sentence is §11: "No tier, warrant token, standing or stamp moves." — and it
denies consequence to every outcome equally. The stop classes say "The census does not proceed" and
§4 says "No tally is filed"; those are procedural and attach symmetrically to every stop class.
REPRO_WITHIN_STATED_PRECISION and REPRO_FAILED are reported with the same machinery ("Report both
numbers" on each). No sentence tells a seat which filing helps or hurts anyone.

Q6 (answered second, before Q3). Can I state what the recorded `origin` is FOR? Yes, but only up to
a hard boundary. The packet says what the study does with it: "`origin` is one recorded attribute of
a ledger record, beside `status`, `value`, `source_file` and `source_line`"; "Arithmetic consumes
records according to status `PRINTED` or `STANDARD`" — so the arithmetic never reads it; "Every
input's `origin` is classified independently by both seats"; §4 class 7 stops the census when the two
classifications disagree on inputs affecting "more than 10% of included claims"; C6 re-derives
"re-classifying every input's `origin` from the pinned sources". So: it is a cross-reader integrity
check and an audit target. What I CANNOT state from the packet: any use beyond that — no tally, no
comparison, no interpretive step is visible. Prior seats answered CANNOT_STATE; on this packet the
integrity-check purpose is statable (sentences above), and nothing further is.

Q3 (answered third). What can I infer about what the lane expects or hopes the census will show?
Every inference I could draw, with the leaking sentence:
  (a) The corpus is cosmology. "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing
      baseline" and the STANDARD table (H0, Omega_m, sigma8, ...). Content, not consequence: the
      seat must read these sources to do the work at all.
  (b) The lane anticipates filing negative outcomes and pre-defends their wording: "Wording:
      "unreproduced from the stated inputs," not "error."" This tells me the drafter expects
      REPRO_FAILED to be filed on real papers and expects that filing to be contested. It constrains
      how a seat writes a negative, not whether it finds one; any reproduction census implies
      failures are possible. It is an inference about anticipated scrutiny, not about a preferred
      tally.
  (c) The lane anticipates attacks on exclusion and on quiet disagreement: "the exclusion ledger is
      reported alongside it and audited under C6, so nothing is hidden by being excluded", and the
      two-seat/denominator-dispute machinery. Again: defensive posture, not a stake.
  (d) Provenance is measured with unusual care (seven origin values, machine-matched quotations, two
      independent readers, disputes carried and counted) — so provenance matters to the lane. What
      it matters FOR is not stated anywhere: no sentence combines origin with the reproduction
      verdict, names a summary of it, or says what any distribution of origins would show.
  What I could NOT reconstruct, adversarially, trying: a hypothesis (no sentence says what result
  would support or weaken anything), a comparison model (none named anywhere in the packet), a
  preferred outcome (the classes are symmetric; §11 denies any consequence), a "pattern" (no
  threshold, shape, or success criterion appears). The stake is not reconstructable from the packet.
  LEAK=NONE at the hypothesis/consequence level; the residue at (a) and (d) is content-level.

=====================================================================
STEP 2 — THE MASTER, JUDGED AS WRITTEN (sections 1,2,3,4,5,6,7,9,11)
=====================================================================

Rename verification (performed first, as instructed — audit R3C2_V21_RENAME_AUDIT_20260905.md opened
after the master): `grep -n REPRO_EXACT` on the master returns 16 hits. Line 118 is the rename notice
itself ("named `REPRO_EXACT` until V21; renamed by the principal's ruling", inside a SEAT-REDACT
span). Every other hit (lines 541,706,762,807,830,856,874,902,931,953,961,972,986,995,997) lies
under a `## 10` heading. Zero governing references to the old token; the audit's rule checks out
against the current file. The seat tool, lane tool and builder each carry 0 occurrences of the old
token (grep); the seat tool's OUTCOMES set carries REPRO_WITHIN_STATED_PRECISION.

---------------------------------------------------------------------
SECTION 1 — OUTCOME CLASSES, and Q1
---------------------------------------------------------------------
Per-claim: six outcomes with a total precedence order ("file the first in this order:
REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the
arithmetic group"). Exhaustive: every path through §2 terminates — extraction always possible; each
input lands in exactly one of four statuses; the attempt either completes (arithmetic group), stops
at an ABSENT input (REPRO_INPUT_ABSENT), stops at a BLOCKED input (REPRO_BLOCKED), exceeds the cap or
lacks machinery (REPRO_NOT_EVALUABLE), or there was no procedure to attempt
(REPRO_NO_DERIVATION_STATED). Mutually exclusive by the declared total order — a claim with no stated
derivation AND an absent input satisfies two conditions and files NO_DERIVATION_STATED; the order, not
the conditions, carries exclusivity, and it is total. A claim fitting NONE: I could not construct one.
Study-level: eight classes plus a total precedence; overlapping conditions (a zero-denominator census
with a failed control) file the first in the order (R3C2_NO_CLASS). A tally fitting two classes:
constructible only as overlapping conditions, and the order resolves them deterministically. A tally
fitting none: not constructible — the last two limbs (CENSUS_PARTIAL, CENSUS_COMPLETE) cover every
tally that reaches them. INCONCLUSIVE genuinely reachable: CENSUS_PARTIAL is declared "INCONCLUSIVE,
and it takes precedence over CENSUS_COMPLETE" and is reachable by any single non-arithmetic outcome
or a zero denominator; the three dispute classes are likewise non-verdict stops.

Q1 — DOES §3 UNDER OPTION (c) MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION
DECIDABLE? YES, with one boundary case named below. The tension is resolved because admissibility no
longer gates the arithmetic: "Arithmetic consumes records according to status PRINTED or STANDARD",
whatever the origin. Hardest cases, each traced to exactly one outcome and one rests_on value:
  (i) Printed-but-chosen input (paper: "we take β = 1/929.25"). Status PRINTED (given in the paper;
      not verbatim on the closed STANDARD list), origin CHOSEN via ORIG_CHOICE_STATED with a
      machine-matched quotation. Consumed. Outcome: exactly one of REPRO_WITHIN_STATED_PRECISION /
      REPRO_FAILED by the rounding/uncertainty rules. rests_on: root CHOSEN → USES_CHOSEN (computed
      by the lane script; seat never writes it — validate rejects it, verified in code).
  (ii) A fitted parameter the paper states ("our fit gives a = 2.7"). PRINTED/FITTED via
      ORIG_FIT_STATED. Consumed. One arithmetic outcome; rests_on USES_FITTED.
  (iii) An imported value. Cited to a pinned enumerable text, machine-match at the cited line:
      PRINTED from that source, origin IMPORTED, consumed → one arithmetic outcome, rests_on
      USES_IMPORTED. Cited but no machine-match at the cited line, or source not enumerable:
      REPRO_BLOCKED; the record is BLOCKED/IMPORTED/ORIG_CITATION with no value, never consumed;
      rests_on computed from the ledger including the BLOCKED leaf → USES_IMPORTED. Exactly one
      outcome each way; the BLOCKED/ABSENT boundary is the named-source test, and the precedence
      order puts BLOCKED before ABSENT. Decidable.
  (iv) The named boundary (the imported-choice seam): the citing paper names a pinned source whose
      cited line itself states a choice ("we choose a = 2"). §2 mandates "classified PRINTED from
      that source, with origin IMPORTED, origin_evidence ORIG_CITATION cited to the named source's
      file and line" — while the source's own line names no external source, so the tie-break's
      citation rule does not naturally fire there. As written the seat files IMPORTED/ORIG_CITATION
      per §2's mandate; the machine checks pass (verbatim and value machine-match at the cited line —
      I demonstrated the machine-passes-wrong-origin property live, §2/Q2 below); the verbatim
      quotation preserves the choice statement for the second seat, the C6 re-classification and the
      interpretation. The filing is deterministic; the fidelity caveat is real. The master records
      this seam (§10.18, codex D1 / kimi F3) as pending the principal because a repair changes the
      reason-code pairing — a rule change. I concur it is a rule matter, not a drafting patch, and I
      do not count it a new defect; I name it because Q1 demands the hardest cases.
Reachability: with REPRO_AFTER_CHOICE retired by the ruling, every remaining §3 outcome has a
constructive path (traced above), and C0 makes two independent seats exhibit each one before any
freeze — the design self-tests reachability rather than asserting it. Decidability: the reproduction
question never references provenance; stated-precision rounding (half away from zero), single-shot
uncertainty, and the asymmetric half-width rule are mechanical. Q1 = YES.

---------------------------------------------------------------------
SECTION 2 — CONTROLS, and Q2
---------------------------------------------------------------------
Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED,
C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED,
C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. Unreached controls: "Controls in an unreached limb are NOT_RUN,
never passes" and §4's stop rule. Pass-by-assertion: blocked everywhere it matters — C1/C2/C3 PASS
only on a printed script run with exit 0 ("a token asserted without the printed run is FAIL"); C5
PASS requires live output ("a transcribed value in place of live output is FAIL"); C6 PASS requires
the printed C6_AUDIT.json artefact; C0 PASS requires two independent seats; C4_SEAT_ISOLATION and
C5B are self-reported lists and SAY SO ("makes no claim that the list is complete"). No control is
passable by bare assertion without a printed artefact.

Q2 — IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? YES — with the floor the master itself
states, which I verified by execution. Test, as demanded: I wrote a ledger record whose origin is
wrong but plausible. Source line (crafted): "We adopt beta = 0.5 from Ref. [12] for the amplitude
fit." Record: status PRINTED, origin CHOSEN, ORIG_CHOICE_STATED, verbatim "We adopt beta = 0.5 from
Ref. [12]", value "0.5", correct file/line. The tie-break makes this misclassified: "a sentence that
names an external source for the value is a citation whatever else it says" — the correct filing is
ORIG_CITATION → IMPORTED. I ran the delivered tool: `/usr/bin/python3 r3c2_ledger_tools.py validate
/tmp/q2/ledger.json /tmp/q2src` → "C3_NO_SUBSTITUTION=PASS", exit 0. The machine does NOT catch it:
CODE_PRECEDENCE exists in the tool (line 29) but cmd_validate never applies it — validate enforces
the schema, the reason-code→origin pair map, machine-matched quotation and value, STANDARD string
equality against the printed 14-row list (verified identical in code), no-value-on-ABSENT/BLOCKED,
derived_from existence/acyclicity/no-orphan-DERIVED, and origin_search on ORIG_SILENT. What catches
the wrong origin: (1) the second seat's independent classification — "Every input's origin is
classified independently by both seats" — after which the lane merge carries origin_alt and compute
emits the DISPUTED pair; disagreements on inputs affecting more than 10% of included claims file
CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor, who re-classifies "every input's origin from the pinned
sources" and files MISMATCH → CENSUS_AUDIT_FAILED on an audited claim; (3) for ORIG_SILENT, the
printed origin_search {query, files, matches}, with "a search of one query with no variants is not
adequate". The floor — "if every reader misclassifies identically, the record stands" — is stated in
the master rather than implied away. Checkable, not asserted: YES.

---------------------------------------------------------------------
SECTION 3 — CIRCULARITY
---------------------------------------------------------------------
Inclusion cannot carry an expectation: the §1 rule is operational (a numeral asserted as the paper's
own result, five enumerated exclusion kinds), applied by two independent seats, with
CENSUS_DENOMINATOR_DISPUTED on surviving disagreement. A contrary claim cannot be quietly excluded:
both seats would have to exclude it with matching kinds, the exclusion ledger is printed before any
tally, and C6 "first audits the full candidate and exclusion ledgers against every pinned source —
completeness, not just outcomes". Origin classification cannot reach the verdict: the arithmetic
consumes by status "whatever its origin", and the severity order, the rests_on tally and the
interpretation mapping are seat-invisible (verified stripped from the delivered packet — Section 4
below). The interpretation step is post-seal and receipt-bound (Section 5). The one visible residue —
a seat can tell provenance is measured carefully — is the floor the principal ruled accepted
("hide the comparison, keep the taxonomy", recorded in C4); it does not tell the seat what any
classification implies. No channel from the lane's expectation to the evidence survives scrutiny.

---------------------------------------------------------------------
SECTION 4 — THE BLIND
---------------------------------------------------------------------
Machine-asserted as claimed, and independently re-verified by me against the delivered artefacts:
the delivered builder (digest 4ed52d4b…, matches the C4 pin) drops §0/§7/§8/§10 (DROP_SECTIONS =
["0.","7.","8.","10."]), strips every SEAT-REDACT span, refuses the build on unclosed spans, asserts
a REQUIRED list present and a FORBIDDEN list absent from both packet and brief, and prints
C4_PACKET_REDACTED=PASS only then. I ran the forbidden list (all 50+ entries, builder semantics:
case-insensitive substring) against the delivered packet and brief: zero hits; no redact markers
remain. The pin file R3C2_SEAT_PACKET.sha256 records the packet digest 4043cd72… (identical to my
PACKET_SHA) and the brief digest be72223b… (verified against the file), and the packet header embeds
master digest 4609d979… — identical to my ACCESS_SHA, so the delivered packet corresponds to this
master. What C4 prevents: furnishing the pattern record, the comparison vocabulary, gate history,
engine and custody-chain names to the seat's working directory — mechanically checked at build time.
What it does not prevent, stated in the document's own words: it is "procedural, not enforced by the
filesystem"; the printed path list "is the detection" and "makes no claim that the list is complete";
"It cannot prove a seat has no prior exposure from training or an earlier session — nothing available
here can." The forbidden list "blocks the enumerated strings and does not establish that every
consequence-bearing word is gone". Limits stated honestly at exactly the strength the mechanism
supports. Sound.

---------------------------------------------------------------------
SECTION 5 — THE SEAL (§7)
---------------------------------------------------------------------
Two receipts, each preceding the step it must bind. (P) the interpretation protocol's hash and commit
id go to Blanc (outside the lane) "before limb A begins; limb A does not begin without receipt P" —
so the protocol is custodied before any tally exists and cannot be written to suit it. (T) after the
tally commit (merged candidate file, exclusion ledger, merged input ledger with compute output; the
receipt names every file) and "before the protocol is opened", Blanc receipts the tally hash and
commit id; "the interpretation protocol is not opened without both receipts." After opening, Blanc
re-hashes both artefacts and verifies all four values against the receipts; the interpretation report
must print both receipts and the four verified values; any missing receipt or mismatch files
CENSUS_AUDIT_FAILED (the §4 definition names this case), leaves interpretation NOT_RUN, voids the
comparison. The relay binds existence and exact form of both artefacts before the comparison is
filed. The overclaim is refused in text: "It does not prove Tori had not already guessed the answer —
nothing available here could prove that. It bounds the record, not the mind." Sound.

---------------------------------------------------------------------
SECTION 6 — FAIRNESS
---------------------------------------------------------------------
Every negative outcome is worded against the inputs, not the paper: REPRO_FAILED carries "Wording:
"unreproduced from the stated inputs," not "error.""; the brief repeats it. No class name asserts
more than its definition: REPRO_WITHIN_STATED_PRECISION claims only rounding to the paper's own
stated precision; REPRO_BLOCKED / REPRO_INPUT_ABSENT / REPRO_NO_DERIVATION_STATED /
REPRO_NOT_EVALUABLE each name an evidence state (named source, absent input, missing procedure,
timeout/machinery), none a fault. The uncertainty rule is single-shot and symmetric with a declared
asymmetric case. Discrimination against any model is explicitly out of scope (§8). Study-level names
describe process states. I found no clause that asserts more than its definition.

---------------------------------------------------------------------
SECTION 7 — STALL / EXECUTABILITY, and Q4
---------------------------------------------------------------------
Q4 — CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Each thing checked, and how:
  - r3c2_ledger_tools.py: present; sha256 f9b7d3c8… == the master/pin digest; `census` and
    `validate` subcommands exist; NO merge/compute (as the master claims); OUTCOMES == the six §3
    tokens; ARITH == the arithmetic group; STANDARD_LIST == the printed 14-row table verbatim
    (14/14 strings compared); validate run live on a crafted ledger (PASS, exit 0).
  - r3c2_lane_tools.py: present; sha256 8e990c7a… == the master digest; merge + compute exist;
    severity order USES_UNDECLARED > USES_IMPORTED > USES_FITTED > USES_CHOSEN, DERIVED_ONLY rule,
    DISPUTED pair, PARENTS_DISPUTED, exit codes — all present in code (grep/read).
  - r3c2_timeout.py: present; sha256 fbb9bef7… == master; monotonic deadline, SYMBOLIC_TIMEOUT,
    exit 124 in code.
  - r3c2_build_seat_packet.py: present; sha256 4ed52d4b… == C4's pin; REQUIRED/FORBIDDEN assertion,
    DROP_SECTIONS, PASS-first-line behaviour in code; delivered packet independently verified clean.
  - R3C2_CORPUS_MANIFEST.md: present; sha256 300d4da1… == master/packet text.
  - R3C2_SEAT_PACKET.sha256: present; all five recorded digests verified against the files on disk.
  - r3c2_seat_packet/R3C2_SEAT_PACKET.md and SEAT_BRIEF.md: present; digests match the pin file.
  - R3C2_INTERPRETATION_PROTOCOL_20260904.md: present (digest-only check, content not read). Its
    current digest differs from the V21 rename-audit's recorded value — later edits; §7 binds it by
    receipt P at run time, which has not run. Informational; no claim made against it.
  - r3c2_seat_sandbox.sb: present (named in §10.18).
  - C5's five commands: run on this host; all exit 0. Printed values: Python 3.9.6; sympy 1.14.0;
    sha256(/usr/bin/python3) = b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9;
    sympy.__file__ = /Users/duhokim/Library/Python/3.9/lib/python/site-packages/sympy/__init__.py;
    sha256 of that file = 4e9476348ba105feab28d82f5bcf6cdba2e3e84de6e059bbfe7a13728c0a4ab0.
    (Commands 2 and 4 run via an equivalent probe file — my sandbox blocks `-c`; disclosed above.)

  FINDING F1 (SUBSTANTIVE — changes a control's passability on the delivery host):
  Verbatim sentence (C4): "A startup, configuration or import file loaded from any other location —
  `/usr/local`, `/tmp`, the working directory's parents, the lane, or any other user-writable path —
  is an outside path and `FAIL`." And the admission: "(b) the SymPy package at the single path C5
  prints, pinned by the digest C5 prints and recorded in the dispatch record before launch".
  Defect: on this host, EVERY mandated `/usr/bin/python3` run loads two startup files from the user
  site — demonstrated at bare startup with no import: `_distutils_hack/__init__.py` and
  `__editable___astro_agent_0_1_0_finder.py`, both under
  /Users/duhokim/Library/Python/3.9/lib/python/site-packages — a user-writable path the admission
  does not cover. C5's sympy commands (2 and 4) additionally load the mpmath package (60+ files —
  mpmath is a hard dependency of sympy, imported at sympy import time; demonstrated live) and 418
  sympy files beyond `__init__.py`. mpmath is not "the SymPy package"; the startup hooks are not
  covered by (a) or (b); the digest pin binds only `__init__.py`. A seat obeying C4/C5b to the letter
  must mark these OUT_OF_SCOPE; "any OUT_OF_SCOPE row fails the control" — C5B_NO_CROSS_LANE=FAIL in
  the seat, and after two attempts in every seat, R3C2_NO_CLASS. The study stops at dispatch, the
  same abort family as the 10:17 run, one level of imports deeper. This is the third round in which
  naming paths has missed the next load; naming packages cannot keep up with what the interpreter
  actually loads.
  Exact replacement: pin the loaded set instead of naming packages. Replace limb (b) with:
  "(b) every file the interpreter loads while executing the mandated commands from the site-packages
  directory that contains the pinned SymPy package, the complete set pinned by a manifest C5 prints
  and the dispatch record records before launch — C5 gains a sixth printed command,
  `/usr/bin/python3 -c "import sys, sympy, hashlib, pathlib; fs = sorted({m.__file__ for m in
  sys.modules.values() if getattr(m, '__file__', None)}); [print(hashlib.sha256(
  pathlib.Path(f).read_bytes()).hexdigest(), f) for f in fs]"`, whose stdout (path, digest rows) is
  the manifest; a load outside the manifest, or a digest mismatch against the dispatch record, is
  FAIL" — and C5's PASS predicate extends to the sixth command. (The alternative — provisioning
  SymPy into a system root — is recorded in §10.18 as an install the principal has not authorised;
  the manifest repair needs no install. Extending C5 to six commands is a rule change; flagged for
  the principal, as with the imported-choice seam.)

  COSMETIC notes (change no class, passability beyond F1, reachability, conclusion or tested value):
  C-note 1: the seat brief carries the clause as "the SymPy package at the single path C5 prints,
  while executing the commands the packet mandates you to run" without the digest-pin half of the
  admission that C4/C5 carry. The brief defers ("where a rule and this brief differ, the packet
  governs"), so passability is unchanged. Suggested: append "pinned by the digest C5 prints and
  recorded in the dispatch record" to the brief's clause.
  C-note 2: Q7's premise says "V24 changes ONE thing"; the diff shows C5 itself also changed (three
  commands → five, the pin sentence, the FAIL-on-mismatch predicate). Same correction, but named
  exactly: C4 + C5b + brief clause AND the C5 command list. No action; stated for the record.

No other stall found: every literal command exists as delivered; every named script runs; every field
a script is said to compute is computed by the named script; every path a seat is told to use exists.

---------------------------------------------------------------------
Q7 (V24 vs the signed V23)
---------------------------------------------------------------------
(a) Can a letter-exact seat now execute every C5 command? The commands execute — all five exit 0 on
this host (run by me; values above). The clause names the binaries in scope: "The two system
binaries C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — are IN SCOPE, together with (a) the
files they load from the system runtime locations `/usr` (excluding `/usr/local`), `/System`,
`/Library`, `/private/var/folders` and `/dev`, and (b) the SymPy package at the single path C5
prints, pinned by the digest C5 prints and recorded in the dispatch record before launch — while
executing the commands this document mandates the seat to run". But executability-under-scope fails
at the next layer: per F1, the mandated runs load user-site files (startup hooks at every launch;
mpmath during the sympy import) that the admission does not cover and that C4's own final sentence
condemns: "A startup, configuration or import file loaded from any other location — … or any other
user-writable path — is an outside path and `FAIL`." So the commands run and the scope control they
operate under cannot PASS. NO.
(b) Paths opened that should not be / needed paths left closed. Opened correctly: /usr/local is
excluded by name; /tmp, the working directory's parents, the lane and other user-writable paths are
condemned by name. Under a generous reading of "the SymPy package at the single path C5 prints" (the
whole tree rooted there), the clause would admit 418 unpinned files from a user-writable tree — a
path it should not open without the manifest pin; named in F1. Needed paths left closed on this
host: the mpmath package directory (sympy's required dependency, same site-packages root) and the
two user-site startup files (`_distutils_hack/__init__.py`,
`__editable___astro_agent_0_1_0_finder.py`).
(c) Anything else different from V23 (diffed against R3C2_V23_DESIGN_OF_RECORD_55b466fa.md): the
header preamble (V23 → V24 living-draft statement); the C4 in-scope clause; the C5 command list
(three → five commands, the dispatch-record pin sentence, the FAIL-on-mismatch predicate); the C5b
echo clause; §10 (V23 row marked SIGNED, V24 row added, §10.18 added). The brief carries the clause
in its own wording (pinned at be72223b… in R3C2_SEAT_PACKET.sha256). NOTHING else — no class,
control code, threshold, precedence, taxonomy or rule of interpretation differs.

Q7 addendum (round 2) — the runtime-locations bound: (i) NOT sufficient on this host — it excludes
the user site from which /usr/bin/python3 actually resolves sympy (verified at round 2 by the lane,
re-verified by me: sympy.__file__ is the user-site path); (ii) as written it WAS too tight — it
excluded the one path stock /usr/bin/python3 + sympy needs here (the user-site package dir); the
round-3 single-path admission is the attempted fix and F1 shows its residual; (iii) C4, C5b and the
brief carry the same five locations, the same /usr/local exclusion and the same single-path
admission; consistent (the brief omits the pin half — C-note 1, cosmetic).

Q7 addendum (round 3) — executable as written on this host? The five commands run (verified: all
exit 0; the printed path is the user-site sympy/__init__.py and its digest prints). Under C4/C5b as
written: NO — per F1, the interpreter's own startup loads two files from the user site on every
mandated run, and `import sympy` loads mpmath (60+ files) plus 418 sympy files beyond the pinned
`__init__.py`; none of these is admitted, and C4's startup-file sentence names the first kind
explicitly. Does the pin close the user-writable-path objection or merely name it? It binds exactly
one file (`__init__.py`) of a 419-file package plus its 60+-file dependency, in a user-writable
tree; the remaining ~478 files load admitted-or-not by reading, unpinned, with integrity resting on
the kernel sandbox's read-only mount — a deployment fact recorded in §10.18, not an operative clause
of C4/C5b. As written, the pin names the path more than it closes the objection; the manifest
replacement in F1 closes it. Anything else changed versus round 2: per the §10.18 record and the
text as delivered — /usr/local excluded by name in C4 and the brief; the SymPy admission narrowed to
the single printed path with the digest pin and pre-launch dispatch-record requirement; C5 extended
to five commands with the FAIL-on-mismatch predicate; the mandated-commands antecedent spelled out
(the C1 census runs, the C2/C3 validate runs, the C5 harness commands, and the §9 wrapper
invocations); C5b and the brief echo the clause. Nothing else.

R3C2_V24C_GATE_COMPLETE
