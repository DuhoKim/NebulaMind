ACCESS_SHA=944300dc68257c705be1da503660559242256ced061b3ef1503ce44c2e4030cc
PACKET_SHA=0bc411ef1ed353fb209949642a7678e8bba6a0e6540a9795154b98ed763ee3d0
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=Independent classification, audit comparison and a dispute stop: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.”
C5_EXECUTABLE_UNDER_SCOPE=NO — All five commands ran successfully, but command 5 invokes find, sort and xargs, which the literal two-binary exception does not admit.

Packet-first answers, recorded before reading the master:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

No sentence identifies a greater empirical or external stake for any outcome. “No tier, warrant token, standing or stamp moves.” Procedural consequences are visible: some outcomes stop the study or prevent a tally. Thus NO above concerns comparative stakes, not an assertion that all administrative consequences are identical.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?

Yes for a procedural purpose; no for a substantive interpretation. “Every input's `origin` is classified independently by both seats.” “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” C6 also requires reclassification and comparison against the sealed record. The packet does not say how provenance contributes to a substantive conclusion.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

“The reproduction verdict and the provenance fields are recorded separately” and “Provenance is transitive” disclose interest in provenance independent of arithmetic. “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” suggests a cosmological subject. Auditing every arithmetic-group claim indicates particular verification effort there, but both numerical success and numerical failure belong to that group. None supplies a directional hypothesis, preferred result, comparison-model preference, or identifiable pattern. These content and procedural inferences do not reconstruct a specific stake.

Only the authorized packet, master, brief and V23 reference were opened as study documents. Runtime access required by the requested C5 tests was also performed. No gate file was sought or opened. Section 10 was read as part of the master, but its verdicts and narrated tests supply no evidence for this verdict. Examples below are fully specified adversarial design fixtures, not claims attributed to an unread corpus.

## 1. Outcome classes — Q1

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

The mechanical definition itself permits all six outcomes and preserves a bounded reproduction decision. The complete design does not consistently carry every admissible imported input through a valid provenance ledger, so the unqualified answer is NO as delivered. This is an evidence-location contradiction, not a request to reverse option (c).

Concrete paths, assuming other controls pass:

| Fixture | Per-claim outcome | rests_on |
|---|---|---|
| “We choose a = 2; y = 3a = 6.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| “Fitting the observations gives a = 2; y = 3a = 6.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| Same chosen input and equation, printed result 7 without uncertainty | REPRO_FAILED | USES_CHOSEN |
| “Use a from Smith”; Smith is outside the enumerable corpus | REPRO_BLOCKED | USES_IMPORTED |
| y = 3a = 6, but a is neither given nor sourced | REPRO_INPUT_ABSENT | USES_UNDECLARED, with the required search |
| Stated chosen inputs and a computation exceeding 120 seconds | REPRO_NOT_EVALUABLE | USES_CHOSEN |
| “Our result is 6”, with no procedure or input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

A concrete overlap attempt is y = a + b = 6, with a cited only to an unpinned source and b absent. Both terminal conditions hold, but precedence files REPRO_BLOCKED only. A claim with no usable procedure and missing inputs files REPRO_NO_DERIVATION_STATED first. I find no double filing after applying precedence. Arithmetic success uses the uncertainty/rounding test, and failure is its numerical complement.

Study-level witnesses are: one successfully audited arithmetic claim → CENSUS_COMPLETE; one successfully audited no-procedure claim → CENSUS_PARTIAL; an audit mismatch or missing seed → CENSUS_AUDIT_FAILED; repeated C3 failure in all attempting seats → R3C2_NO_CLASS; unresolved inclusion/input-list disagreement → CENSUS_DENOMINATOR_DISPUTED; unresolved outcome disagreement → CENSUS_OUTCOME_DISPUTED; origin disagreements affecting two of ten claims → CENSUS_ORIGIN_DISPUTED; C3 failing in one seat and passing in the other after retries → CENSUS_CONTROL_SPLIT. Zero included claims also reaches CENSUS_PARTIAL.

An all-arithmetic tally with failed audit does not file COMPLETE because C6=PASS is required. A partial tally with a missing seed files AUDIT_FAILED by precedence. These establish genuine INCONCLUSIVE reachability and no double study-level filing in completed/stopped executions; an execution still awaiting work is not a final tally.

D1 — Imported-value evidence cannot always satisfy both rules. SUBSTANTIVE.

Verbatim sentence from §2:

“A value the paper does not print but traces to a named source that is itself an enumerable text in `R3C2_CORPUS_MANIFEST.md` is
classified `PRINTED` from that source, with `origin` `IMPORTED`, `origin_evidence` `ORIG_CITATION` cited to the named
source's file and line, and the value machine-matched there — **only when such a match exists; a cited value that does
not machine-match at the named source's cited line, or whose named source is not an enumerable text of the manifest,
files `REPRO_BLOCKED` under §3.**”

Construct claiming paper P: “Use a from Q, line 12; y = 3a = 6.” Q line 12 says only “We choose a = 2.” Both are pinned enumerable texts. Arithmetic must use 2 and reproduces 6; §2 requires IMPORTED/ORIG_CITATION with evidence at Q:12. But Q:12 supports CHOSEN/ORIG_CHOICE_STATED, not a citation. C3's sentence-based classification therefore conflicts with §2. Moving the citation evidence to P satisfies C3's meaning but violates §2's mandated evidence location. No record satisfies both requirements. The intended unique result is REPRO_WITHIN_STATED_PRECISION with USES_IMPORTED.

Exact replacement for the quoted sentence:

“A value the claiming paper does not print but traces to a named enumerable source in `R3C2_CORPUS_MANIFEST.md` is classified `PRINTED` with `origin` `IMPORTED` only when its value machine-matches a cited line in that source. The record's `source_file` and `source_line` identify that value-bearing line; `origin_evidence` uses `ORIG_CITATION` and quotes the claiming paper's sentence naming the source, with that sentence's file and line. If the source is not enumerable or the value does not machine-match there, file `REPRO_BLOCKED` under §3.”

D2 — The exclusion vocabulary does not cover every non-result numeral. SUBSTANTIVE.

Verbatim sentence:

“**The exclusion ledger's
`kind` is one of `EQUATION_NUMBER`, `REFERENCE_NUMBER`, `PAGE_OR_LINE_NUMBER`, `DATE`, `ATTRIBUTED_NOT_DERIVED`.**”

A passage “For this calculation we set the grid width to 256 cells” can be an author-specified input rather than an asserted result. It is neither an included claim under §1 nor any listed excluded kind. The rule requiring every candidate failing §1 to enter the exclusion ledger therefore has an encoding gap. Restricting “candidate” silently to avoid this numeral would make completeness depend on an unstated screening rule.

Exact replacement:

“The exclusion ledger's `kind` is one of `EQUATION_NUMBER`, `REFERENCE_NUMBER`, `PAGE_OR_LINE_NUMBER`, `DATE`, `ATTRIBUTED_NOT_DERIVED`, or `NOT_ASSERTED_AS_OWN_RESULT`; the last kind records a numeral that fails §1 because it is an input, setting, example or other non-result, with the passage quoted. Every numeral-bearing passage is entered in the candidate ledger before this disposition is assigned.”

The validator's permitted kinds must implement that replacement and its new bytes must be pinned. This changes the auditable enumeration, so it is not cosmetic.

## 2. Controls — Q2

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

Yes, with a human semantic check; no, not as a machine-established fact. The quotation matcher establishes that quoted words exist, not that the selected reason code correctly describes them. Computed root_origins and rests_on cannot repair an incorrectly classified root.

Concrete wrong-but-plausible record, for hypothetical source P.txt line 12 containing exactly the quoted sentence:

```json
{
  "claim_id": "c1",
  "input_id": "a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "P.txt",
    "source_line": 12,
    "verbatim": "We choose a = 2 from Smith (2020)."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "P.txt",
  "source_line": 12
}
```

The value, quotation and code-origin pairing satisfy the described structural checks. The citation-first rule makes the correct classification IMPORTED. An independently correct second seat catches this by reading “from Smith (2020)”; the disagreement is carried, with the stated threshold stop. C6 catches it on an audited claim by reclassifying from the source and reporting mismatch. Every arithmetic-group claim is audited; only a sample of remaining claims is. Shared human misclassification, or an unsampled non-arithmetic claim misclassified by both initial seats, can survive. The master expressly acknowledges the common-reader failure floor.

I did not execute this fixture against the delivered validator: doing so would open a script outside the permitted document list. The result above tests the written mechanism, not undocumented implementation behavior.

C0, C1, C2, C3, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5, C5B and C6 have exact PASS/FAIL/NOT_RUN codes. Their required artifacts are respectively the exhibition/verification, candidate and exclusion counts with census runs, validator run, validator plus lane merge/compute runs, builder output/pins, printed paths and dispatch inventory, live harness, marked paths, and C6_AUDIT.json. Unreached controls are explicitly NOT_RUN. C4/C5B can certify an incomplete self-reported list, but their definitions openly limit PASS to that list.

D3 — C5's pipeline can conceal an upstream failure. SUBSTANTIVE.

Verbatim sentence:

“**PASS requires all five commands to exit 0, their full stdout printed, the path of (4) and the digest of
(5) equal to the dispatch record's; a mismatch, a non-zero exit, missing output, or a transcribed value in place of live output is
FAIL.**”

Without pipefail, command 5 returns the final shasum's status. find, sort, xargs or an earlier shasum can fail while the final shasum emits a digest and exits 0. Repeating the same incomplete traversal at dispatch and at C5 can even satisfy the digest comparison. Successful execution here does not repair that control predicate.

Exact replacement:

“PASS requires all five commands and every stage of command 5's pipeline to exit 0, complete stdout and stderr to be printed, and the path from (4) and digest from (5) to equal the dispatch record. Any stage failure, mismatch, missing output or transcribed substitute is FAIL.”

Exact replacement for command 5:

```sh
cd <the directory (4) printed> && (set -o pipefail; /usr/bin/find . -type f -print0 | /usr/bin/sort -z | /usr/bin/xargs -0 /usr/bin/shasum -a 256 | /usr/bin/shasum -a 256)
```

Use this same command to generate the dispatch pin. D5 below supplies the corresponding scope repair.

D4 — A named inherited control lacks a result code. SUBSTANTIVE to the requested control contract.

Verbatim sentence:

“Lane-side procedure, not the seat's: the no-fallback control is the provider log showing
no fallback line for the seat's session, checked by the lane owner; a one-page check sheet `R3C2_CHECK_SHEET_<date>.md`
in plain words with source lines is written by the lane owner after the tally; the lane owner runs `r3c2_lane_tools.py` (sha256 `8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848`; merge, then compute) after both seats exit and re-runs every script; a
critic note precedes any ruling.”

The no-fallback control names evidence but no exact result token, missing-log disposition or study-level route.

Exact replacement:

“Lane-side procedure, not the seat's: the lane owner prints the provider log for each seat session and records `C5_NO_FALLBACK=PASS|FAIL|NOT_RUN` as a component of C5. PASS requires a complete session log containing no fallback event; a fallback event or unavailable/incomplete log is FAIL; an unreached check is NOT_RUN. C5 cannot pass unless both its harness and no-fallback components pass, and §4's C5 failure/split rules apply. After the tally the lane owner writes `R3C2_CHECK_SHEET_<date>.md` with source lines, runs the pinned `r3c2_lane_tools.py` merge then compute after both seats exit, and re-runs every script. A critic note precedes any ruling.”

## 3. Circularity

The operative inclusion criterion does not mention a lane hypothesis or comparison model. Independent enumeration, visible exclusions, zero-tolerance reconciliation and full-source completeness audit make a contrary claim difficult to exclude quietly if the specified controls are actually performed. D2 leaves a denominator boundary that needs explicit treatment.

Origin is an interpretive input, but its classification is source-constrained and independently checked; D1 is a real contradictory constraint at that boundary. The protocol is externally receipted before enumeration. Its contents were not opened, so this gate cannot certify that its actual mapping is fair or hypothesis-independent. The seal prevents unnoticed replacement of a receipted mapping; it does not establish that mapping's scientific validity.

## 4. The blind

I independently extracted master sections 1–6, 9 and 11, removed the SEAT-REDACT spans, and compared the resulting body with the delivered packet: exact match. Redaction delimiters balance, and the packet's embedded master digest matches the access proof.

That verifies the delivered transformation. It does not verify the builder's actual forbidden-list implementation, digest, failure behavior or brief assertion: opening/running that builder was outside the permitted file set.

C4 honestly says the dispatch-copy arrangement and path lists do not prove actual non-access or lack of prior exposure. The user supplies a kernel-sandbox/read-only premise for this host; I did not inspect its profile and do not infer enforcement from the document's self-reported controls.

D5 — The new fifth C5 command needs executables outside the literal exception. SUBSTANTIVE.

Verbatim sentence containing the exception:

“**The two system binaries C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — are IN SCOPE, together with (a) the files they load
from the system runtime locations `/usr` (excluding `/usr/local`), `/System`, `/Library`, `/private/var/folders` and `/dev`, and (b) the
interpreter's user site-packages directory at the single path C5 prints, taken WHOLE — every file under it, including SymPy, its
dependency mpmath, and any startup hook installed there — pinned by the manifest digest C5 prints (the sha256 of the null-separated,
sorted sha256 list of every file under that directory, exactly as C5's fifth command computes it) and recorded in the dispatch record before launch — while executing the commands
this document mandates the seat to run (the C1 census runs, the C2/C3 validate runs, the C5 harness commands, and the §9 wrapper
invocations), every one of which invokes the interpreter with `-E` so that no `PYTHON*` environment variable can redirect a load.”

On this host the extra utilities resolve to /usr/bin/find, /usr/bin/sort and /usr/bin/xargs. They are commands launched by the shell, not files loaded by Python or shasum. The exception admits files in /usr only in that latter capacity; it does not admit arbitrary executables there. A seat obeying it literally cannot launch all of command 5. Bare utility names also leave command resolution dependent on PATH.

Exact replacement for that sentence:

“The mandated commands may execute `/usr/bin/python3`, `/usr/bin/shasum`, `/usr/bin/find`, `/usr/bin/sort`, and `/usr/bin/xargs`, using these absolute paths through the seat's dispatch-recorded system shell. Files loaded by that shell and these executables are IN_SCOPE only in `/usr` excluding `/usr/local`, `/System`, `/Library`, `/private/var/folders`, `/dev`, the seat working directory, or the entire user site-packages directory printed by C5 and pinned in the dispatch record by C5's fifth command. These permissions apply only to the mandated census, validate, harness, source-digest and wrapper commands. Every Python invocation, including a Python child of the wrapper, must use `/usr/bin/python3 -E`.”

Replace C5b's exception and the brief's exception with this same sentence, and use the path-qualified command in D3. Keep the existing outside-load FAIL rule and whole-user-site admission.

D6 — The claimed -E coverage is contradicted by literal commands. SUBSTANTIVE.

The quoted C4 sentence in D5 says “every one of which invokes the interpreter with `-E`”. But C1 literally mandates:

```sh
/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json>
/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json> final
```

C2/C3 likewise omit -E in both validate forms, and §9 omits it from the wrapper invocation. The brief repeats the final census command without -E. Thus obeying the printed commands and obeying the -E requirement are incompatible.

Exact replacements, at every corresponding seat-facing occurrence:

```sh
/usr/bin/python3 -E r3c2_ledger_tools.py census <candidates.json> <exclusions.json>
/usr/bin/python3 -E r3c2_ledger_tools.py census <candidates.json> <exclusions.json> final
/usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> .
/usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir>
/usr/bin/python3 -E r3c2_timeout.py 120.0 -- <command>
```

Retain the rule resolving sources_dir to “.”. Add the exact sentence: “If `<command>` invokes Python, it begins `/usr/bin/python3 -E`; wrapper flags do not propagate to its child.”

D7 — The audit sees the prior answers before its supposedly unseen re-derivation. SUBSTANTIVE.

Verbatim sentence:

“**C6 — audit, with a frozen sampling frame.** A third independent seat **first audits the full candidate and
exclusion ledgers against every pinned source** — completeness, not just outcomes — then re-derives, **without sight of earlier work and re-classifying every input's `origin` from the pinned sources**: **(i) every claim in the arithmetic
group** — no sampling discount — and **(ii) a sample of `min(max(1, ceil(0.20 × N)), R)` of the remaining included
claims**, `N` being the sealed denominator and `R` the number of remaining claims (when `R` is zero the sample is empty
and every included claim is already audited under (i)), drawn by `random.Random(seed_int).sample(remaining_ids, k)`
where **`remaining_ids = sorted(set(included_ids) − set(arithmetic_group_ids))` and `seed_int = int(seed_hex, 16)`, 
the custodian's seed being 64 lowercase hexadecimal characters**.”

C1 requires the full candidate ledger to contain outcomes and, for arithmetic claims, both numbers. Reading it first reveals exactly the earlier work C6 promises to withhold.

Exact replacement:

“The third independent seat first enumerates the pinned sources without receiving any earlier ledger. Its enumeration is preserved before comparison. It then audits completeness using a projection containing only candidate identifiers, source locations, numerals, inclusion dispositions and exclusion kinds, with prior outcomes, reproduced values and input ledgers withheld. An external custodian supplies audit targets comprising every arithmetic-group claim and a sample of `min(max(1, ceil(0.20 × N)), R)` remaining claims, drawn by `random.Random(int(seed_hex, 16)).sample(sorted(set(included_ids) - set(arithmetic_group_ids)), k)`, with the existing sealed denominator and seed rules. The auditor independently reproduces those targets and reclassifies their inputs, preserving those results before receiving earlier outcomes, numbers and ledgers for MATCH/MISMATCH comparison. Target selection may reveal arithmetic-group membership; it does not reveal the earlier outcome within that group or the earlier calculation.”

The later C6 artifact and failure requirements remain applicable.

## 5. The seal

The timing and custody design is adequate as written: receipt P binds the protocol hash and commit before limb A; receipt T binds the tally commit, its named files and computed provenance before the protocol is opened; external verification checks the four receipted values and the interpretation prints them. Missing receipts or mismatches void the comparison and file CENSUS_AUDIT_FAILED.

A send alone cannot satisfy it. A mutable local commit alone cannot satisfy it. The document states the limit concerning prior guesses. Receipt artifacts are future run outputs, not evidence furnished for this design gate; none was inspected. No seal repair is required by the operative text.

## 6. Fairness

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The brief extends that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NOT_EVALUABLE and NO_DERIVATION_STATED describe limitations of this reproduction procedure, not scientific falsity. REPRO_WITHIN_STATED_PRECISION accurately names the uncertainty/rounding test.

The requested grep found 16 REPRO_EXACT hits: the rename notice at master line 118 and 15 hits within section-10 history. No governing old-token reference survives.

D8 — DERIVED_ONLY has a misleading ordinary-language reading. COSMETIC if repaired solely as a display clarification.

Verbatim sentence:

“*(Master only — the rule the script implements: `DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or
`MEASURED`; otherwise the most severe root origin present, in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` >
`USES_FITTED` > `USES_CHOSEN`.”

A claim resting solely on the paper's own measurement receives DERIVED_ONLY. The explicit definition permits that, but the bare label suggests derivation alone.

Exact replacement:

“Master only — the script returns `DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`; every report displays this token as `DERIVED_ONLY (derived, standard or measured roots)`, which makes no claim of derivation alone. Otherwise the script returns the first present root category in the fixed order `USES_UNDECLARED`, `USES_IMPORTED`, `USES_FITTED`, `USES_CHOSEN`.”

This clarification retains every machine token, membership rule, filed class, control predicate and tested value. It is not a proposed reversal of the settled taxonomy.

## 7. Stall / executability — Q4 and Q7

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

Yes: D1 prevents a fully conforming imported-choice ledger; D2 leaves some candidate dispositions unencodable; D5/D6 prevent literal compliance with all execution instructions; D7 prevents the stated unseen audit sequence. D3 leaves the manifest control weaker than its stated pass predicate.

Verification inventory:

- Master and packet: live SHA-256; master read in full through bounded reads; packet read before master; independent section/redaction extraction matched exactly.
- SEAT_BRIEF.md: read for scope consistency and literal commands. It admits the same runtime roots and entire pinned user site, and repeats the -E contradiction.
- V23 design of record: direct diff against the master. Operative master changes are confined to C4, C5 and C5b. Additional differences are the version header, V23/V24 history-table entries, and appended §10.18. C5 itself changed from three commands to five, added -E, path-qualified shasum, and added path/digest matching requirements. Therefore “only C4 and C5b changed” is not literally true, although these are all part of the runtime amendment.
- Metadata-only existence checks succeeded for r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_timeout.py, r3c2_build_seat_packet.py, root R3C2_SEAT_PACKET.sha256, R3C2_CORPUS_MANIFEST.md, R3C2_INTERPRETATION_PROTOCOL_20260904.md, the packet and brief.
- The pin file is absent inside the source packet subdirectory but present beside the master. This is not a missing-delivery finding: C4 specifies a separately assembled flat dispatch copy, including the root pin file. No such dispatch inventory was inspected.
- census, census final, both validate forms, merge, compute, builder and timeout invocation: checked against their literal textual contracts. Their script bytes, pinned hashes, schemas, claimed root_origins/rests_on computation, negative-test behavior and runtime execution were NOT verified because those files may not be opened under this gate's access restriction.
- Corpus member existence/digests could not be checked without opening the forbidden manifest. The corpus is not reported missing. Candidate/input/output JSON, C0 exhibition, receipts, audit JSON, provider log, check sheet and SEAT_REPORT are run-generated artifacts; their absence before a run is not a defect.
- Administrative third-seat dispatch is expressly not claimed executable from the packet. The wrapper's 120-second enforcement is specified but its implementation was not tested.

Q7(a), including round 4: all five C5 commands were run in order from the working directory:
`/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828`.

Exact commands and complete stdout:

1. `/usr/bin/python3 -E --version`
   Exit 0; stdout: `Python 3.9.6`

2. `/usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"`
   Exit 0; stdout: `1.14.0`

3. `/usr/bin/shasum -a 256 /usr/bin/python3`
   Exit 0; stdout:
   `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3`

4. `/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"`
   Exit 0; stdout:
   `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`

5. `cd /Users/duhokim/Library/Python/3.9/lib/python/site-packages && find . -type f -print0 | sort -z | xargs -0 /usr/bin/shasum -a 256 | /usr/bin/shasum -a 256`
   Exit 0; stdout:
   `5809f89a1093a5d4ff17ae0c5d7eca72f1281df2ba1294ee793ff76bc9131af5  -`

All five returned without reported stderr. The manifest command completes on this host; its spaces-in-filenames issue does not recur. These are host executions, not a certification of a dispatched seat's sandbox. No dispatch record was supplied for the path/digest comparison, so C5_HARNESS_PINNED=PASS is not established.

C5_EXECUTABLE_UNDER_SCOPE=NO under the literal two-binary exception, for D5's three utility executables. Python and SymPy themselves now run within the expressly admitted locations. The brief says “each interpreter call with `-E`”; C5b says “while executing those same mandated commands (each with `-E`)”. Those agree with C4's policy, but disagree with the literal ledger/wrapper commands identified in D6.

Q7(b): A separate live diagnostic imported SymPy and inspected resolved __file__ paths of loaded Python modules. No retained module path fell outside the admitted working-directory/system/user-site set. The user-module root was exactly the directory command 4 printed. This is positive evidence for the current import, not a complete file-open trace: it cannot establish that transient startup reads, subprocess loads or every future mandated command stay inside the set. No needed SymPy/mpmath module path was observed to remain excluded.

The system-root-only bound would exclude this host's user site; whole-directory admission now includes that needed path and its startup hooks. Excluding /usr/local closes that particular textual allowance. -E prevents PYTHON* environment redirection in calls that actually use it; it does not disable admitted startup hooks or validate paths those hooks choose. Outside loads remain forbidden, and the text alone does not enforce that ban. The current omitted -E flags and unadmitted pipeline utilities are the concrete remaining inconsistencies.

The pin binds the manifest measured at dispatch and checks it again at C5. It does not make the host directory immutable, attest the intent of startup hooks, or prevent another writer changing files after C5. With the user-supplied read-only-to-seat premise, it addresses seat writes and observable dispatch-to-C5 changes. It does not eliminate user-writability by other host processes. Admitting the entire directory is treated as the principal's decision, not as a defect.

Q7(c): A byte-for-byte comparison with round 2 or round 3 cannot be made from the permitted reference: it is V23, not either intermediate V24 snapshot. I therefore cannot certify “nothing else changed” against those rounds. Relative to the round-3 specification supplied in the question, the visible amendment replaces the individual-package pin with the whole user-site manifest, admits dependencies/hooks there, adds the -E requirement, and uses a null-safe filename pipeline. The actual V23 diff is enumerated above; section 10's account is not substituted for missing intermediate bytes.

D9 — The digest description misstates which stream is null-separated. COSMETIC.

The C4 sentence quoted in D5 calls it “the sha256 of the null-separated, sorted sha256 list of every file under that directory”. Command 5 null-separates the sorted input filenames; shasum emits textual hash records into the final hash, not a null-separated hash list.

Exact replacement for that parenthetical:

“(the sha256 of the textual hash-record stream produced by C5's fifth command, whose filename traversal and sorting use null delimiters)”

This does not change the command or any tested digest.

The verdict rests on the operative contradictions and control gaps above, not on prior gates, the wisdom of the admitted runtime, or an inference about unrun scientific results.
R3C2_V24E_GATE_COMPLETE
