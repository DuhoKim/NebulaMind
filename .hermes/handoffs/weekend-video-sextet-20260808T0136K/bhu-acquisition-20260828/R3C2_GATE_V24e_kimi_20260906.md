ACCESS_SHA=944300dc68257c705be1da503660559242256ced061b3ef1503ce44c2e4030cc
PACKET_SHA=0bc411ef1ed353fb209949642a7678e8bba6a0e6540a9795154b98ed763ee3d0
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology (C3's closed STANDARD list is "the Planck 2018 TT,TE,EE+lowE+lensing baseline"), the lane anticipates failed reproductions and pre-legislates their framing ("unreproduced from the stated inputs," not "error"), and §11 reveals the project has tiers/warrants/standings/stamps the study disclaims moving; no directional hypothesis, comparison model, preferred outcome or pattern is inferable from any packet sentence.
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE
C5_EXECUTABLE_UNDER_SCOPE=YES — a seat obeying C4 and the brief to the letter can run all five C5 commands on this host (I ran them: exit codes 0,0,0,0,0; the path of (4) is the admitted user site and the null-safe manifest command of (5) completes over its 20637 files, digest 542480bbb33099023858da7ff804dca1db28174e06bfe7b5d4c4655dcedb72a3), subject to the one repair in section 7: the clause's universal "-E" claim is contradicted by the printed C1/C2/C3/§9 commands, which carry no -E.

Seat: kimi. Date: 2026-09-06. Master gated: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V24e, living draft). Packet answered BEFORE the master was read, per the brief's STEP 1.

=====================================================================
STEP 1 — PACKET-ALONE ANSWERS (given before the master was opened)
=====================================================================

Q5 (answered before Q3): NO. From the packet alone I cannot tell that any outcome is more consequential than any other, to the lane or to anyone. The only consequence-touching sentence is §11: "No tier, warrant token, standing or stamp moves." — it denies consequence uniformly rather than ranking outcomes. Filing-precedence language ("INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE", §4.2) says which class gets filed, not what any class costs anyone. No sentence tells me one outcome is worse, better, safer, or more dangerous than another.

Q6 (answered before Q3): CANNOT_STATE — strictly, the packet never says what the recorded `origin` is FOR. What it does state is mechanical handling only: "The reproduction verdict and the provenance fields are recorded separately." (§1); "`origin` is one recorded attribute of a ledger record, beside `status`, `value`, `source_file` and `source_line`; a seat records it and writes no field outside the schema" (§3); both seats classify it independently (C3), disagreements are filed `ORIGIN_DISPUTED` and escalate past 10% of included claims (`CENSUS_ORIGIN_DISPUTED`, §4.7), and the C6 auditor "re-classif[ies] every input's `origin` from the pinned sources". But no packet sentence says what the study does with origin after that — no tally, stratification, summary or conclusion consumes it, and the arithmetic explicitly ignores it: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3). So: what is DONE with it (recorded, cited, cross-checked, audited) — statable; what it is FOR — not statable from the packet.

Q3: WHAT I CAN INFER ABOUT WHAT THE LANE EXPECTS OR HOPES — from the packet alone:
  (a) DOMAIN. The corpus is cosmology/quantitative-gravity papers whose inputs include Planck-baseline parameters: "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3 table note). A 14-entry closed constant list (G, c, ħ, k_B, H₀, Ω_m, Ω_Λ, Ω_b h², Ω_c h², n_s, σ₈, τ, ln(10¹⁰ A_s), age) only exists because the lane expects claims consuming exactly these.
  (b) ANTICIPATED FAILURE, FRAMING-SENSITIVE. "`REPRO_FAILED` — ... Wording: \"unreproduced from the stated inputs,\" not \"error.\"" (§3). A lane does not legislate the wording of an outcome it considers unreachable or toothless; it expects some claims may not reproduce and wants that outcome read as a property of the stated inputs, not of the papers.
  (c) AN INTER-CITING CORPUS. The IMPORTED rule and the whole REPRO_BLOCKED class ("a value the paper does not print but traces to a named source that is itself an enumerable text in `R3C2_CORPUS_MANIFEST.md`...") only matter if the pinned texts cite each other's values.
  (d) RAW MATERIAL AND A MULTI-LANE PROJECT. "Files listed there as RAW are not enumerable" (§2) and "Published sources only; nothing from another lane." (§11).
  (e) THE STAKE THAT IS DISCLAIMED. "No tier, warrant token, standing or stamp moves." (§11) tells me the surrounding project possesses tiers, warrant tokens, standings and stamps that a census result could in principle bear on — the packet pre-emptively neutralizes that reading. This is a disclosed stake-boundary, not a hidden one.
  WHAT I CANNOT INFER: any directional expectation (which §3 class will dominate), any comparison model, any preferred outcome, any "pattern". No packet sentence states or hints one; the outcome taxonomy is symmetric between reproduction success and failure, and every dispute class stops the study rather than steering it. Adversarially, the strongest reconstruction of the stake I can manage is (b)+(e): "the lane expects some published numbers may not reproduce from their stated inputs, and has fenced off the consequences." That is a content-level leak about the study's setting, not hypothesis reachability — I cannot name the hypothesis, its direction, or what would confirm it.

=====================================================================
STEP 2 — THE MASTER, JUDGED (operative sections 1,2,3,4,5,6,7,9,11)
=====================================================================

---------------------------------------------------------------------
1. OUTCOME CLASSES — and Q1
---------------------------------------------------------------------
Per-claim (§3): six outcomes with a total precedence for co-occurring terminal conditions (`REPRO_NO_DERIVATION_STATED`, `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, then the arithmetic group). Exhaustive: every included claim either has no stated procedure (NO_DERIVATION), or each input lands PRINTED/STANDARD (consumed), BLOCKED (named source unpinned or unmatched), or ABSENT (untraced); the first BLOCKED else the first ABSENT else the attempt decides, and the attempt terminates in the arithmetic group or NOT_EVALUABLE (120 s cap / missing machinery). Two-class construction: an input the paper neither prints nor traces to an obtainable pinned value but names an unpinned source satisfies both REPRO_BLOCKED and REPRO_INPUT_ABSENT — the precedence files REPRO_BLOCKED, exactly one outcome. No-class construction: none found; the stated-precision rule makes the arithmetic group total — "the reproduced value must round to the printed numeral at that precision, rounding half away from zero", and "|reproduced − printed| ≤ the stated uncertainty, taken once... Where the stated uncertainty is asymmetric, the stated uncertainty is the half-width on the side the reproduced value falls" — an inclusive, decidable boundary in every case.
Study-level (§4): eight classes, total precedence stated (`R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`), so a tally fitting two classes files exactly one. CENSUS_COMPLETE and CENSUS_PARTIAL are exclusive by construction (arithmetic-group-on-every-claim vs at-least-one-non-arithmetic-or-zero-denominator). INCONCLUSIVE is genuinely reachable: it is §4.2's own label for CENSUS_PARTIAL ("INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE"), reached by a single blocked or absent input anywhere in the corpus, or by denominator zero — the document itself names the zero case ("no census is complete over nothing").

Q1 (direct): YES, as written under option (c) — every declared outcome is reachable and the reproduction question stays decidable; the old tension is dissolved because provenance never gates the arithmetic. Hardest cases traced:
  - PRINTED-BUT-CHOSEN input ("we take τ = 0.05" printed in the paper): status PRINTED (or STANDARD if verbatim on C3's list), consumed by the arithmetic; verdict REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical test; origin CHOSEN recorded; lane-side `rests_on` computes USES_CHOSEN. One outcome, one rests_on.
  - FITTED parameter the paper states: same path, origin FITTED via ORIG_FIT_STATED, rests_on USES_FITTED. The choice/fit does not block reproduction because the value is printed — decidability survives.
  - IMPORTED value: pinned enumerable source with a machine-match at the cited line → PRINTED from that source, consumed; no match or unpinned source → status BLOCKED, outcome REPRO_BLOCKED, and the BLOCKED record still carries origin IMPORTED so rests_on computes USES_IMPORTED. One outcome, one rests_on.
  - SILENT input: ABSENT/UNDECLARED → REPRO_INPUT_ABSENT, rests_on USES_UNDECLARED.
  - Claim with no ledger records: §3's master-side rule files `rests_on` `NOT_COMPUTED` with its own tally row — no gap.
  Empirically exercised on the pinned tools: the DISPUTED-pair path computes exactly as the text says (my T13: merge of two seats disagreeing CHOSEN-vs-IMPORTED on one input → compute prints `rests_on=['USES_IMPORTED', 'USES_CHOSEN'] ... DISPUTED`, exit 0). The retired REPRO_AFTER_CHOICE's content now rides as a computed field, so no declared outcome depends on a judgement the seats cannot make mechanically.

---------------------------------------------------------------------
2. CONTROLS — and Q2
---------------------------------------------------------------------
Every control names an exact code with PASS|FAIL|NOT_RUN (C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE). None is passable by bare assertion: C3 — "a token asserted without the printed run is FAIL"; C5 — "a transcribed value in place of live output is FAIL"; C6 — "a token without the artefact is FAIL"; C1 — PASS only on exit 0 of the printed census run with declared and recomputed counts; C4_PACKET_REDACTED is asserted by the builder, not by a person. Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes", plus §4's "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`."

Q2 (direct): IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? — Checkable for fabricated evidence; asserted-floor for precedence-misuse with a genuine quotation; the document states that floor itself. TESTED by writing the record:
  My ledger record (in /tmp/r3c2_gate_lab/ledger_q2.json): source line 1 of srcA.txt is "We adopt H0 = 67.4 from Planck (2018)."; the record files origin CHOSEN with reason_code ORIG_CHOICE_STATED and verbatim "We adopt H0 = 67.4" — a genuine substring of a sentence that is, by the document's own tie-break, a citation ("a sentence that names an external source for the value is a citation whatever else it says" → ORIG_CITATION files first). Result of `validate`: C3_NO_SUBSTITUTION=PASS, exit 0 — the machine does NOT catch it. Which mechanism catches it, as written: not the machine; only "the second seat's independent classification and the C6 re-classification" — the master's C3 note says exactly this: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." A disagreement is reported, never reconciled, and stops the census above 10% of included claims.
  What the machine DOES catch (all verified live against the pinned tool, digest matching the §3 pin): a field outside the schema (my `rests_on` record → "seat-authored ledger carries a field outside the schema", exit 1 — which is also the master's "A `rests_on` value present in a seat-authored input ledger fails this control"); an ABSENT record carrying a value (exit 1); a PRINTED value absent from its cited line (exit 1); a STANDARD value off the closed list (exit 1); and by code inspection the verbatim-substring, BLOCKED-must-be-IMPORTED/ORIG_CITATION, derived_from existence/acyclicity/DERIVED-names-parents, and ORIG_SILENT-requires-origin_search checks. The tool's CODE_PRECEDENCE table exists but is never consulted by validate — consistent with the stated human floor.

---------------------------------------------------------------------
3. CIRCULARITY
---------------------------------------------------------------------
The lane's expectation has no open channel to the evidence:
  - INCLUSION: the §1 rule (a numeral the paper asserts as its own result; five enumerated excluded kinds) is applied by two independent blind seats; disagreement surviving two reconciliations stops the study under CENSUS_DENOMINATOR_DISPUTED — shaping the denominator is visible and fatal, not quiet.
  - EXCLUSION: every exclusion is ledgered with kind, and the C6 auditor "first audits the full candidate and exclusion ledgers against every pinned source — completeness, not just outcomes". A contrary claim quietly excluded is ledger incompleteness → CENSUS_AUDIT_FAILED. The channel is closed.
  - ORIGIN CLASSIFICATION: quoted evidence, two independent seats, disputes reported and never reconciled, >10% stops the census; the auditor re-classifies from the sources.
  - INTERPRETATION: removed to a sealed protocol no seat may open, committed and receipted (P) before limb A; the tally is receipted (T) before the protocol is opened; the master states the interpretation READS the two tallies but not what it concludes. Verified empirically that the seats' copy carries none of it: the built packet contains 0 hits on the forbidden vocabulary (pattern, ΛCDM/LCDM, hypothesis, rests_on, root_origins, tallies, severity, USES_, ruling, principal, stake, prior, refute, expectation, favourable, consequential, invisible, tempting, warn, and every custody/engines name), and the pinned builder reproduces that packet byte-for-byte from this master (section 4 below).
  Residual, disclosed: seats see a careful seven-value provenance taxonomy; the master records this as the principal's accepted floor ("hide the comparison, keep the taxonomy") and states the argument lives in the leak-floor judgement. Sound.

---------------------------------------------------------------------
4. THE BLIND
---------------------------------------------------------------------
Machine-asserted as claimed, verified by rebuilding: I ran the pinned builder (sha256 4ed52d4b…, matching the C4 pin) in a scratch copy; output first line `C4_PACKET_REDACTED=PASS`, sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] with §0/§7/§8/§10 dropped, 50 inline spans redacted, and the built packet's sha256 is 0bc411ef1ed353fb209949642a7678e8bba6a0e6540a9795154b98ed763ee3d0 — identical to the pin in R3C2_SEAT_PACKET.sha256 and to the packet on disk, whose header embeds master sha 944300dc… (= ACCESS_SHA). The builder also asserts the forbidden list over SEAT_BRIEF.md and REFUSES to write the packet on any survival (exit 1), and asserts REQUIRED seat-facing content survives (so a control's instruction cannot be redacted away leaving a bare token).
  Does C4's mechanism prevent what it claims? It claims a procedural blind with a structural aid and says so: "This is procedural, not enforced by the filesystem: nothing here denies a seat an absolute path into the lane, so the seat's printed path list is the detection, and `C4_SEAT_ISOLATION` is a self-reported control with a structural aid, and is labelled so." The dispatch record (directory listing + digests before launch) proves what the seat was furnished; the printed path list is detection, not proof of non-access. The limit is stated honestly and completely: "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can. The record states that limit rather than implying a stronger blind." Sound.

---------------------------------------------------------------------
5. THE SEAL (§7)
---------------------------------------------------------------------
Binds what it claims, before the step it must precede. Receipt P: the interpretation protocol's hash and commit id go to the external custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the interpretive mapping is fixed before any evidence exists. Receipt T: after the single tally commit (merged candidate file, exclusion ledger, merged input ledger with compute output) and before the protocol is opened, the tally hash and commit id are receipted, "and the receipt names every file in that commit; the interpretation protocol is not opened without both receipts. The relay is complete only when it is receipted." After opening, the custodian independently re-hashes and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, which §4.3's definition expressly names. The C6 audit seed is supplied by the external custodian only after the tally digests are receipted, so the audit frame cannot be shaped by the tally's producer. The overclaim guard is present: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." Sound.

---------------------------------------------------------------------
6. FAIRNESS
---------------------------------------------------------------------
The negative outcome carries the mandated wording: "`REPRO_FAILED` — ... Wording: \"unreproduced from the stated inputs,\" not \"error.\"" No per-claim class name asserts more than its definition: REPRO_WITHIN_STATED_PRECISION names exactly the mechanical test it defines; REPRO_FAILED is immediately bounded by the wording rule; the four non-arithmetic classes name their stopping conditions. One noted item, COSMETIC and already declared pending in the document: the lane-side tally label `DERIVED_ONLY` fires when every root origin is DERIVED, STANDARD **or MEASURED** — the name reads narrower than its printed definition; the definition is printed literally beside the label, the label decides nothing (it is a tally row, not a verdict), and the document records the rename as escalated to the principal and NOT done. No class filed, no control's passability, no reachability, no conclusion and no tested value changes: COSMETIC. Not a fresh finding.

---------------------------------------------------------------------
7. STALL / EXECUTABILITY — and Q4, then Q7
---------------------------------------------------------------------
Q4 (direct): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Everything I checked executed as delivered, with ONE internal inconsistency in the new C4 clause (the defect below). Checked and how:
  - FILES THE DOCUMENT SAYS EXIST: master, packet, brief, r3c2_ledger_tools.py, r3c2_timeout.py, r3c2_lane_tools.py, r3c2_build_seat_packet.py, R3C2_CORPUS_MANIFEST.md, R3C2_SEAT_PACKET.sha256 — all present. Every embedded digest pin matches the bytes on disk: ledger tools f9b7d3c8… (§3 pin), wrapper fbb9bef7… (§9 pin), lane tools 8e990c7a… (C3 lane-side pin), builder 4ed52d4b… (C4 pin), corpus manifest 300d4da1… (§2 pin), packet 0bc411ef… (pin file; the pin file also pins brief and both tools — all match).
  - LITERAL COMMANDS — C5's five: (1) `/usr/bin/python3 -E --version` → Python 3.9.6, exit 0. (2) sympy import under -E → 1.14.0, exit 0 (run as a byte-identical script file because this gate shell blocks inline `-c`; same interpreter, same -E, same import resolution). (3) `/usr/bin/shasum -a 256 /usr/bin/python3` → b8763cf2…, exit 0. (4) user site path → /Users/duhokim/Library/Python/3.9/lib/python/site-packages, exit 0. (5) `cd <(4)> && find . -type f -print0 | sort -z | xargs -0 /usr/bin/shasum -a 256 | /usr/bin/shasum -a 256` → exit 0, digest 542480bbb33099023858da7ff804dca1db28174e06bfe7b5d4c4655dcedb72a3 over the whole 20637-file directory; the null-safe form completes without error on this host.
  - NAMED SUBCOMMANDS: `census` and `validate` exist and behave as C1/C3 describe — consistent tiny ledgers PASS (exit 0); tampered declared count FAILs ("declared_candidate_count=3 but recomputed 2", exit 1); `final` rejects a PENDING outcome (exit 1) and passes a resolved arithmetic outcome carrying printed/reproduced strings (exit 0). Lane-side `merge` exits 1 on differing input_id sets, carries origin_alt on disagreement (exit 0); `compute` exits 2 rejecting a ledger arriving with root_origins/rests_on set, exits 0 printing the DISPUTED pair exactly as the clause describes.
  - THE WRAPPER: success path prints WRAPPER_COMMAND, child stdout/stderr, WRAPPER_EXIT=0, WRAPPER_ELAPSED; deadline path (0.3 s against a 5 s sleeper) prints SYMBOLIC_TIMEOUT and exits 124 — as §9 states.
  - THE BUILDER: executable; reproduces the pinned packet byte-for-byte (section 4).
  - FIELDS A SCRIPT IS SAID TO COMPUTE: the five recomputed counts (census, verified), root_origins/rests_on/DISPUTED pairs (compute, verified).
  - PATHS A SEAT IS TOLD TO USE: the seat working-directory layout the dispatch list names (packet, brief, pin, manifest, ledger tools, wrapper, sources) exists as described; the user-site path C5 prints is readable.
  - RENAME CHECK (per the brief's note): `grep -n REPRO_EXACT` on the master → 16 hits. Line 118 is the rename notice itself ("(named `REPRO_EXACT` until V21; renamed by the principal's ruling)"), inside a SEAT-REDACT span; every other hit lies under a `## 10` heading (the first is at line 510; the hits are at 546, 711, 767, 812, 835, 861, 879, 907, 936, 958, 966, 977, 991, 1000, 1002). Governing references to the old token: 0 — consistent with the V21 rename audit's rule and conclusion (the audit numbered V21's lines; the same classification holds on V24e's bytes).

  DEFECT 1 (the only one; minor, exactly repairable, NOT cosmetic under the round-4 charge of "executable and internally consistent"):
  QUOTE (C4): "...while executing the commands this document mandates the seat to run (the C1 census runs, the C2/C3 validate runs, the C5 harness commands, and the §9 wrapper invocations), every one of which invokes the interpreter with `-E` so that no `PYTHON*` environment variable can redirect a load."
  DEFECT: the universal "-E" claim is false for three of the four named command sets AS PRINTED. The printed commands are: C1 — "`/usr/bin/python3 r3c2_ledger_tools.py census <candidates.json> <exclusions.json>`" (and its `final` form); C2 — "`/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> .`"; C3 — "`/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> <sources_dir>`"; §9 — "`/usr/bin/python3 r3c2_timeout.py 120.0 -- <command>`". None carries -E; only the C5 harness commands do. So during a census, validate or wrapper run, a PYTHON* variable (e.g. PYTHONPATH) could redirect a load to an outside path — the precise event the clause says cannot happen — and a load so redirected would be an outside-path FAIL under the clause's own rule. The clause's stated guarantee and the mandated commands contradict each other in the very text V24 adds.
  EXACT REPLACEMENT: insert -E into the four printed templates (master and packet copies alike): "`/usr/bin/python3 -E r3c2_ledger_tools.py census <candidates.json> <exclusions.json>`", "`/usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> .`" / "`<sources_dir>`", "`/usr/bin/python3 -E r3c2_timeout.py 120.0 -- <command>`" — and mirror it in the brief's step-5 wrapper reference; alternatively narrow the clause's parenthetical to the C5 harness, but adding -E is the better repair because it closes the path instead of re-admitting it. (Lane-side merge/compute may take -E for uniformity, though the clause's antecedent does not name them.) Impact: no class filed, no control's passability, no reachability, no conclusion and no tested value changes; the commands run identically (all my T-runs above pass with or without it). The defect is that the text asserts a guarantee its own commands do not deliver — an internal inconsistency in the scope clause, which is what this version exists to get right.

Q7 (this version) — full answer:
  (a) YES — a seat obeying C4 and the brief to the letter can now execute every C5 command, verified by running all five on this host (exit codes above; sympy 1.14.0 resolves from the admitted user site). Governing sentences: C4 — "The two system binaries C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — are IN SCOPE, together with (a) the files they load from the system runtime locations `/usr` (excluding `/usr/local`), `/System`, `/Library`, `/private/var/folders` and `/dev`, and (b) the interpreter's user site-packages directory at the single path C5 prints, taken WHOLE — every file under it, including SymPy, its dependency mpmath, and any startup hook installed there — pinned by the manifest digest C5 prints ... while executing the commands this document mandates the seat to run ..., every one of which invokes the interpreter with `-E` ..." (the last clause subject to Defect 1); brief — "The two system binaries the packet's C5 names — `/usr/bin/python3` and `/usr/bin/shasum` — are in scope, with the files they load from the system runtime locations `/usr` (not `/usr/local`), `/System`, `/Library`, `/private/var/folders` and `/dev`, and the interpreter's user site-packages directory at the single path C5 prints, taken whole and pinned by the manifest digest C5 prints, while executing the commands the packet mandates you to run (census, validate, the C5 harness, the wrapper), each interpreter call with `-E`; anything loaded from elsewhere is an outside path." C5b carries the same set as `IN_SCOPE`.
  (b) PATHS: the admitted set opens one path that needs watching and names it honestly — the user site is user-writable on the host; the clause states the two mitigations verbatim ("the seat's confinement makes it read-only to the seat, and the manifest digest binds its bytes at dispatch; a change by another writer between the dispatch record and C5 is what the C5 comparison detects") — detection, not prevention, and it says so. The one path it should not open but leaves ajar is Defect 1's PYTHON* redirection during the non-harness mandated runs. No needed path is closed: a -v import trace of the heaviest mandated interpreter use (the sympy import) shows 564 code files loaded, ALL inside the admitted set (472 in the user site taken whole, 92 under /Library; nothing from /usr/local, /tmp, the working directory's parents, or the lane).
  Round-2 addendum: (i) the runtime-locations bound is sufficient for the startup/import-path hole only together with (b)'s whole-directory admission and -E — with Defect 1 as the caveat; (ii) not too tight: stock macOS python3 + sympy + mpmath run within it on this host (verified live, exit 0); (iii) C4, C5b and the brief are mutually consistent (all three texts compared line by line; the brief's "not /usr/local" matches C4's "excluding /usr/local"; same mandated-command sets; same -E claim — including sharing Defect 1's overreach, since the brief's "each interpreter call with -E" is likewise contradicted by the packet's printed census/validate/wrapper commands).
  Round-3 addendum: C5 executable on this host as written — I ran the five commands (exit codes 0,0,0,0,0; the manifest command completes). The pin closes the user-writable-path objection to the extent a digest pin can: it detects any post-dispatch change at C5 time and the clause says exactly that — it names the detection honestly rather than claiming prevention. Versus round 2: whole-directory admission replaced SymPy-only admission, the manifest digest replaced the single-file digest, -E was added to the mandated calls, /usr/local was excluded by name, and the mandated-commands antecedent was spelled out (per §10.18's round-3 record, checked against the current texts).
  Round-4 addendum: all five C5 commands run by me from a working directory — exit codes 0, 0, 0, 0, 0 — and the manifest (fifth) command completes without error on this host (digest 542480bbb33099023858da7ff804dca1db28174e06bfe7b5d4c4655dcedb72a3 over 20637 files; the null-safe `-print0 | sort -z | xargs -0` form handles the space-containing filenames). C5 executable under C4/C5b/brief as written: YES, with Defect 1 as the single internal-consistency repair. Nothing the mandated commands load falls outside the admitted set (564-file trace above, including the two startup hooks the clause expressly admits — the editable-install finder and _distutils_hack — both inside the taken-whole user site). Different from round 3: only the null-safe form of C5's fifth command, per the V24d→V24e note; the diff-permitted files are otherwise unchanged. Judged only for executability and internal consistency, per the round-4 charge; the wisdom of admitting the whole user site is the principal's recorded decision and is not judged here.
  (c) ANYTHING ELSE DIFFERENT FROM V23: NOTHING ELSE. The full diff of V23 (design of record) against this master shows exactly: the header version line; the C4 scope clause; C5 (three commands → five, path-qualified shasum, -E, plus the site path and manifest digest and their dispatch-record pin); the C5b echo sentence; the V23 row gaining its signed hash and the added V24 row; and §10.18 appended. Every operative section outside C4/C5/C5b is byte-identical.

RENAME AUDIT VERIFICATION (per the brief's note): opened R3C2_V21_RENAME_AUDIT_20260905.md after the master; applied its LIVE/HISTORICAL rule to V24e's bytes via `grep -n REPRO_EXACT`; every hit lies under a `## 10` heading except line 118, which is the rename notice itself and sits inside a SEAT-REDACT span (invisible to seats). Governing references to the old token: 0. No defect.

VERDICT: PREREG_SOUND_WITH_REPAIRS — the design is sound: outcome classes exhaustive, exclusive, reachable and decidable under option (c) (Q1 YES); controls artefact-bound with named codes and honest NOT_RUN handling; provenance machine-checkable for fabrication with its human floor stated verbatim (Q2, tested); no circularity channel open; the blind machine-asserted and its limit stated; the seal binding and receipted before the steps it must precede; fairness wording exact; everything described executes as delivered (Q4) with one exception — Defect 1, the universal "-E" claim contradicted by the printed C1/C2/C3/§9 commands, repaired by adding -E to those four templates (master, packet, brief). C5_EXECUTABLE_UNDER_SCOPE=YES.

R3C2_V24E_GATE_COMPLETE
