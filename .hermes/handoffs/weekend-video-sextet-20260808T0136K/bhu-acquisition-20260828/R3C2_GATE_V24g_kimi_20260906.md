ACCESS_SHA=c232b4ceb32be109e73cfb247ed911a6080f893a787eb28a233ad0a84104deb3
PACKET_SHA=1119e0ee25b89efe6fd63580992685c200239647555516ed43d70d5496ccb3a5
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only, at/below the ruled floor: from the packet alone the corpus is cosmology (the STANDARD table is the Planck 2018 baseline, printed literally) and provenance is measured with care (seven origins, dual classification, an origin-dispute stop class, C6 re-classification); no hypothesis, comparison model, preferred tally, or stake direction is inferable
CONSEQUENCE_VISIBLE=REPRO_FAILED only: it is the one outcome whose wording is policed ("Wording: "unreproduced from the stated inputs," not "error."") and §11 reveals a status system outcomes are barred from moving ("No tier, warrant token, standing or stamp moves"); which tally benefits the lane is not visible
ORIGIN_PURPOSE=audit-only attribute, statable mechanically: "The reproduction verdict and the provenance fields are recorded separately." (§1); arithmetic consumes records by status "whatever its `origin`" (§3); "Every input's `origin` is classified independently by both seats" (C3); the auditor re-classifies it (C6); >10% disagreement stops the study (§4 CENSUS_ORIGIN_DISPUTED); what conclusion it feeds: not stated in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES — all five C5 commands ran from a working directory containing r3c2_manifest.py and exited 0; every open they make falls under C4 (ii) "read the pinned environment — the interpreter `/usr/bin/python3` and the site-packages directory whose path and manifest digest the dispatch record fixes before launch and C5 prints live" or (iii) "execute the commands this document prints verbatim — ... the five C5 harness commands ... — together with whatever those commands themselves invoke or load"
NO_MASKED_STAGE=YES

# R3C2 GATE V24g — seat kimi — 2026-09-06

Gate of the living draft R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 24) against the seat packet
r3c2_seat_packet/R3C2_SEAT_PACKET.md. Design judged as written; no derivation run; section 10 treated as history
binding nothing. Section 3's core definition judged as settled under option (c) per the brief.

## Packet answers (given from the packet ALONE, before the master was read)

Q5 (consequence, answered first): PARTIAL. One outcome is visibly singled out for special handling: the
REPRO_FAILED definition carries the sentence "Wording: "unreproduced from the stated inputs," not "error."" — a
framing constraint no other outcome receives, so a negative filing is visibly the sensitive act. And §11's "No
tier, warrant token, standing or stamp moves" tells me a status system exists adjacent to the census and that all
outcomes are equally barred from moving it. What I could NOT tell: that any outcome is more consequential than
another to any hypothesis, model, or person — no sentence ranks outcomes by benefit.

Q6 (origin purpose, answered second): STATABLE MECHANICALLY, NOT ANALYTICALLY. The packet tells me what happens to
a recorded origin: "The reproduction verdict and the provenance fields are recorded separately." (§1); "Arithmetic
consumes records according to status `PRINTED` or `STANDARD`" with inputs consumed "whatever its `origin`" (§3
boxed text); "Every input's `origin` is classified independently by both seats" (C3); the auditor re-classifies
"every input's `origin` from the pinned sources" (C6); disagreement on inputs affecting more than 10% of included
claims files CENSUS_ORIGIN_DISPUTED and the census does not proceed (§4). So: it is recorded, double-classified,
audit-re-classified, and carries a dispute tripwire — and the arithmetic never consumes it. What the study
concludes from it, what it is compared against, or what summary it feeds: no packet sentence says. (Also noted:
the packet's sweep shows no `rests_on`, no severity order, no tally of origins — those are not in the seat's copy.)

Q3 (hypothesis reachability, answered third): what the packet alone let me infer, with the leaking sentences:
  (i) The corpus is cosmology-theory: the STANDARD table prints only G, c, ħ, k_B and "the Planck 2018
      TT,TE,EE+lowE+lensing baseline" (C3 table + "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing
      baseline."). Content a seat must have; not a stake.
  (ii) Provenance is measured with care and a provenance-dispute is a study-stopping event: seven origin values,
      machine-matched quotations, "Every input's `origin` is classified independently by both seats", and §4's
      CENSUS_ORIGIN_DISPUTED. A census that polices provenance this hard intends to report on it. This is exactly
      the floor C4 records the principal as having accepted; nothing shows what the provenance record is compared
      against or what the comparison is called.
  (iii) Failure outcomes are anticipated and pre-armored: the "not "error."" wording rule, "INCONCLUSIVE, and it
      takes precedence over `CENSUS_COMPLETE`", and "no census is complete over nothing" show the lane expects
      non-arithmetic outcomes and has prioritized not-overclaiming over completion. Any census implies failures
      are possible; this leaks no direction.
  NOT inferable: which papers, which numbers, what fraction, any comparison model, any preferred tally direction,
  any hypothesis sentence. There is none to quote.

## 1. Outcome classes (Q1 placed here)

Per-claim: six outcomes (REPRO_WITHIN_STATED_PRECISION, REPRO_FAILED, REPRO_BLOCKED, REPRO_NOT_EVALUABLE,
REPRO_NO_DERIVATION_STATED, REPRO_INPUT_ABSENT) with a total precedence ("Exactly one outcome is filed per claim.
Where more than one terminal condition holds, file the first in this order: ..."), plus a recorded exclusion
ledger that is explicitly not an outcome. I could construct no real claim fitting two classes after the
precedence is applied, and none fitting zero: printed+derivable claims land in the arithmetic group; no-procedure
claims in NO_DERIVATION_STATED (including "a procedure named but not specified"); named-source failures in
BLOCKED; untraced inputs in INPUT_ABSENT; timeout/machinery in NOT_EVALUABLE.
Study-level: eight classes with a total chronological precedence; a tally must land in exactly one. INCONCLUSIVE
is genuinely reachable: CENSUS_PARTIAL files on any single non-arithmetic claim or a zero denominator, and "it
takes precedence over `CENSUS_COMPLETE`".

Q1 (direct): YES — with one recorded seam that is already the principal's pending item. Under option (c) every
declared outcome is reachable and the reproduction question stays decidable. Hardest cases traced:
  (a) printed-but-chosen input ("we take β = 1/929.25", printed): status PRINTED, origin CHOSEN, consumed by the
      arithmetic ("whatever its `origin`") → exactly one arithmetic-group outcome; rests_on = USES_CHOSEN computed
      by script. One outcome, one rests_on.
  (b) fitted parameter the paper states ("our fit gives a = 2.3"): PRINTED/FITTED → attempted → WITHIN or FAILED;
      rests_on = USES_FITTED. Decidable.
  (c) imported value (cited, not printed): machine-match at the named pinned source's cited line → PRINTED/
      IMPORTED, attempted; no match or non-enumerable source → REPRO_BLOCKED, recorded BLOCKED/IMPORTED/
      ORIG_CITATION, never consumed. Each limb lands in exactly one outcome.
  (d) stated-precision edge: no stated precision → round at the printed precision, half away from zero; stated
      uncertainty → |reproduced − printed| ≤ stated uncertainty, once; asymmetric → the half-width on the side
      the reproduced value falls. Mechanical; "13.8 Gyr" vs 13.797 files WITHIN, not FAILED.
  (e) THE SEAM (recorded, authority: the principal; pending item D1 in §10.18 — NOT a new finding of this seat):
      §2 mandates for the imported value "with `origin` `IMPORTED`, `origin_evidence` `ORIG_CITATION` cited to
      the named source's file and line", while C3's tie-break applies reason codes to "the cited sentence" and
      holds only that "a sentence that names an external source for the value is a citation whatever else it
      says". Where the named source's cited line itself states a choice ("we choose a = 2") and names no external
      source, ORIG_CITATION does not match that line and ORIG_CHOICE_STATED does — the two rules can file
      different origins for the same input, and the seats can split. This fails CLOSED (ORIGIN_DISPUTED, reported
      never reconciled; above 10% the study stops under CENSUS_ORIGIN_DISPUTED); it cannot move any per-claim
      outcome (status PRINTED either way) nor any rests_on computation (the pair is computed and marked DISPUTED).
      Candidate exact replacement (for the principal; a reason-code rule change is his): append to §2's IMPORTED
      sentence — "the record files `ORIG_CITATION` whatever else the named source's cited line states; the
      reason-code tie-break of C3 governs the claiming paper's own sentences and does not reclassify an input
      admitted under this rule."

## 2. Controls (Q2 placed here)

Every control C0–C6 and C4_PACKET_REDACTED names an exact code with PASS|FAIL|NOT_RUN; unreached controls are
NOT_RUN ("Controls in an unreached limb are `NOT_RUN`, never passes" and §4's "later limbs are unreached and
their controls are `NOT_RUN`"). Pass-by-assertion is closed where it matters: C1 PASS only on exit 0 of the
printed census run; C3 "a token asserted without the printed run is FAIL"; C5 requires all five exits 0, full
stdout, and equality with the dispatch record, with "a transcribed value in place of live output is FAIL"; C6 "a
token without the artefact is FAIL". Two controls ARE self-reported by design — C4_SEAT_ISOLATION ("a
self-reported control with a structural aid, and is labelled so") and C5B_NO_CROSS_LANE ("makes no claim that the
list is complete") — and the document says so at full strength rather than hiding it.

Q2 (direct): CHECKABLE, WITH A STATED HUMAN FLOOR — demonstrated live. Attack record written and run through the
pinned validator: a PRINTED input with origin MEASURED whose ORIG_MEASURED quotation is genuine but whose cited
sentence ("The age of the universe is 13.8 Gyr.") reports no measurement — wrong but plausible. Result:
`C3_NO_SUBSTITUTION=PASS`, exit 0. The machine limb verifies evidence FORM (quotation substring of the cited
line, value machine-match, closed-list membership, schema closure, acyclic derived_from, origin_search for
ORIG_SILENT), not the fit of the reason code to the sentence — exactly as the document states: "A reason code
that matches its quotation but misapplies the precedence is caught only by the second seat's independent
classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the
record stands — that floor is stated here rather than implied away." The catch mechanisms are: the second seat's
independent classification (disagreement → ORIGIN_DISPUTED, reported, never reconciled; >10% of included claims →
CENSUS_ORIGIN_DISPUTED stops the study) and C6's re-classification from the sources (MISMATCH →
CENSUS_AUDIT_FAILED). The floor is real and is stated; provenance is checkable, not asserted.

## 3. Circularity

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule contains no pattern
content (verified independently: 28-token sweep of the packet — pattern, comparison model, hypothesis and
consequence words, engine and custody names, rests_on, severity, "two tallies", interpretation — zero hits);
exclusions are by enumerated kinds only, both seats must agree with tolerance zero, and C6 audits "a completeness
disposition for every candidate and every exclusion" against the pinned sources — a contrary claim cannot be
quietly excluded without both seats and the auditor misclassifying identically. Origin classification: seats
classify independently; disputes are reported, never reconciled; the severity order and the rests_on field live
only in master-side redacted spans (zero hits in the packet) and are computed by a script that REJECTS (exit 2,
verified) a ledger arriving with rests_on set. Interpretation: lives in a protocol no seat may open, receipted
before limb A (receipt P), reading the tally only after the tally itself is receipted (receipt T). The
hypothesis cannot reach the evidence through inclusion, exclusion, origin classification, or the interpretation
step; the residual (a seat can tell provenance is measured) is the principal's ruled and recorded floor.

## 4. The blind

Machine-asserted as claimed — verified three ways, not taken on the document's word:
  (a) positive: the pinned builder (sha256 4ed52d4b…, matches disk) run from an isolated copy printed
      `C4_PACKET_REDACTED=PASS` as its first line, then master sha256 c232b4ce… (= ACCESS_SHA) and packet sha256
      1119e0ee… (= PACKET_SHA = the pin file's row); "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.']
      dropped ['0.','7.','8.','10.'] inline spans redacted 50" — exactly C4's description; rebuilt packet
      byte-identical to the dispatched one.
  (b) negative probe: one forbidden word ("refute") planted into §1's visible text of a copy →
      `FAIL: C4_PACKET_REDACTED=FAIL — forbidden content survived redaction` naming the line, "Packet NOT
      written.", exit 1. The assertion fails closed.
  (c) independent sweep: 28 forbidden-class tokens grep'd against the packet: zero hits.
What C4 prevents: furnishing hypothesis content via the packet and dispatching an unlisted copy (the dispatch
record lists contents+digests before launch; "a copy missing any of them is not dispatched"). Its limits are
stated honestly and at true strength: "This is procedural, not enforced by the filesystem"; the printed path list
"is the detection"; "It cannot prove a seat has no prior exposure from training or an earlier session"; the
forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is
gone". The mechanism does what it claims and does not claim more.
Observation (not a defect): the §9 wrapper's `<command>` placeholder is seat-chosen content inside a printed
command; the common case (seat-authored script inside the working directory, imports from the pinned directory)
is covered by C4 (i)+(ii), and any abuse of the placeholder is inside the design's already-stated self-report
floor (the printed path list is the detection). No new hole.

## 5. The seal (section 7)

Binds what it claims, before the steps it must precede. Receipt P: the interpretation protocol is committed and
its hash and commit id sent to an external custodian, "limb A does not begin without receipt P" — the protocol is
fixed before any evidence exists. Receipt T: the tally (one named commit: merged candidate file, exclusion
ledger, merged input ledger with compute output) is hashed and receipted, and "the interpretation protocol is not
opened without both receipts" — the tally is fixed before the interpretation is read. After opening, the
custodian independently re-hashes and verifies all four values; "the interpretation report must print both
receipts and the four verified values"; any missing receipt or mismatch files CENSUS_AUDIT_FAILED — and §4's
definition of that class does name the seal case ("or the receipt verification of the seal fails"). Limits
stated, not implied away: "a local commit in Tori's own custody can be amended or reset, so it is tamper
evidence, not an external custodian"; "It does not prove Tori had not already guessed the answer". Sound.

## 6. Fairness

Every negative outcome is worded "unreproduced from the stated inputs," never "error": the REPRO_FAILED
definition carries the rule; the only operative "error" occurrences are that rule itself and the manifest
script's machine diagnostic `ERROR=<path>` (a file-read failure token — not a judgement on any paper; noted so no
future reader mistakes it for a verdict word). Class names vs definitions: REPRO_WITHIN_STATED_PRECISION asserts
exactly what its rounding/uncertainty rules define; CENSUS_PARTIAL's INCONCLUSIVE label fits its definition; the
arithmetic group is "exactly `REPRO_WITHIN_STATED_PRECISION` and `REPRO_FAILED`".
One name asserts more than its definition (recorded, authority: the principal; pending item D8 — NOT new): the
master-side rule reads "`DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`" — a claim
resting only on standard constants or measurements files DERIVED_ONLY. It lives in a redacted span (zero hits in
the packet), so it reaches no seat and no blind is harmed; the rename is escalated and is the principal's. No
replacement drafted by this seat.

## 7. Stall / executability (Q4 placed here)

Q4 (direct): everything the document describes that I exercised executed as delivered; nothing described was
found non-executable. Checked, each named with how:
  - Files and pins: R3C2_CORPUS_MANIFEST.md (300d4da1…), r3c2_ledger_tools.py (f9b7d3c8…), r3c2_lane_tools.py
    (8e990c7a…), r3c2_manifest.py (19a8ce47…), r3c2_timeout.py (fbb9bef7…), r3c2_build_seat_packet.py (4ed52d4b…)
    — all exist beside the master and every digest matches its printed pin. R3C2_SEAT_PACKET.sha256 contents
    cross-checked: packet 1119e0ee… (= PACKET_SHA), brief 379d1014…, wrapper, seat tool, manifest script,
    builder — all match disk. R3C2_INTERPRETATION_PROTOCOL_20260904.md, R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md,
    R3C2_INHERITED_ITEMS_FOR_DUHO_20260906.md exist (stat only — the protocol is "no seat may open"; not read).
  - The five C5 commands, run myself from a working directory containing r3c2_manifest.py copied from this lane
    (_tmp_gate_v24g_kimi_c5/): (1) Python 3.9.6, exit 0; (2) sympy 1.14.0, exit 0; (3) /usr/bin/python3 sha256
    b8763cf2…, exit 0; (4) /Users/duhokim/Library/Python/3.9/lib/python/site-packages, exit 0; (5) FILES=20637,
    MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0. Disclosure: this
    gate's sandbox blocked the literal `-c` form, so commands (2) and (4) were run as `/usr/bin/python3 -E
    <file>.py` carrying the identical one-line body — same interpreter, same -E, same semantics; exit codes are
    genuine.
  - Manifest script negative control: directory with one chmod-000 file → `ERROR=locked.txt: Permission denied`,
    `MANIFEST_SHA256=INCOMPLETE (1 unreadable)`, exit 1 — fails loudly, nothing skipped silently.
  - Builder: positive and negative runs as in §4 above.
  - Wrapper r3c2_timeout.py: fast child exit 0; failing child status carried (exit 1); /bin/sleep 3 under a 1.0s
    cap → SYMBOLIC_TIMEOUT printed, exit 124, elapsed 1.004s.
  - census: PASS on a limb-A fixture (PENDING) and on a `final` fixture (arithmetic outcome with both values),
    printing declared and recomputed counts; wrong declared_included_count → `FAIL: declared_included_count=2 but
    recomputed 1`, exit 1.
  - validate: PASS on a schema-valid four-record ledger (including the Q2 attack record); FAIL with named lines
    and exit 1 on each of six negative fixtures (STANDARD value off the closed list; field outside the schema;
    derived_from cycle; PRINTED value not at the cited line; ABSENT record carrying a value; ORIG_SILENT without
    origin_search).
  - lane merge/compute: merge exit 0 with "origin disagreements=1", merged record carrying origin_alt and
    origin_evidence_alt; compute prints the claim's rests_on as the pair ['USES_CHOSEN','USES_IMPORTED'] marked
    DISPUTED, root_origins derived transitively (a DERIVED record's root resolved through its parent); compute on
    a ledger arriving with rests_on set → "REJECT: i1 arrives with rests_on set", exit 2.
  - Seat tool split: r3c2_ledger_tools.py prints usage naming census and validate only and exits 2 on `compute`
    or `merge` — "has no `compute` and no `merge`" confirmed.
  - Fields a script is said to compute: recomputed counts (census), root_origins and rests_on (compute),
    origin_alt/origin_evidence_alt (merge), FILES/MANIFEST_SHA256/ERROR (manifest script) — all observed live.
  - Paths a seat is told to use: the working directory, `.` as the sole sources_dir, the C5 paths — all
    consistent with the C4 principle; no told path is forbidden, no forbidden path is told (see Q7(c)).
  - Stall: the only long command (C5(5)) completed in seconds over 20,637 files; every symbolic operation is
    capped at 120s with a reportable outcome; every dispute and failure mode has a stop class; a missing audit
    seed files CENSUS_AUDIT_FAILED with the missing seed named.

FINDINGS (each: verbatim quote, defect, exact replacement):

F1 (REPAIR, low; cosmetic by the brief's test but falsifies a stated invariant at the point that guards the
blind). Quote: "Build command, run from this directory: `/usr/bin/python3 r3c2_build_seat_packet.py`; expected
first line of output `C4_PACKET_REDACTED=PASS`, then the master and packet digests." Defect: this is the only
printed interpreter command in the document without `-E`; V24g's own stated invariant ("every printed interpreter
command carries -E", and the gate brief's preamble repeats it) is therefore not true by the text, and the -E
rationale — no `PYTHON*` variable can redirect a load — applies most of all to the builder, the blind's one
machine assertion. Exact replacement: "Build command, run from this directory: `/usr/bin/python3 -E
r3c2_build_seat_packet.py`; expected first line of output `C4_PACKET_REDACTED=PASS`, then the master and packet
digests." Classification: COSMETIC per the brief's definition (changes no class filed, no control's passability,
no reachability, no conclusion, no tested value); filed because a stated invariant is literally false without it.

F2 (COSMETIC). The seat tool's usage text prints its two subcommands without -E
("/usr/bin/python3 r3c2_ledger_tools.py census   <candidates.json> <exclusions.json>" and
"/usr/bin/python3 r3c2_ledger_tools.py validate <ledger.json> <sources_dir>"), while every mandated form in the
document carries -E. Not itself a mandated command; a seat could copy the usage form. Exact replacement: usage
lines read "/usr/bin/python3 -E r3c2_ledger_tools.py census ..." and "/usr/bin/python3 -E r3c2_ledger_tools.py
validate ..." (tool re-pin follows). Classification: COSMETIC.

F3 (RECORDED, principal's pending item D1; not new; candidate repair drafted in §1(e) above for his ruling only).
F4 (RECORDED, principal's pending item D8; the DERIVED_ONLY label; §6 above).

## Q7 (V24g) — the scope principle

Run evidence: the five C5 commands above, run from a working directory containing r3c2_manifest.py copied from
this lane; exit codes printed there.
(a) C5_EXECUTABLE_UNDER_SCOPE=YES. Governing sentences: C4 "(ii) read the pinned environment — the interpreter
    `/usr/bin/python3` and the site-packages directory whose path and manifest digest the dispatch record fixes
    before launch and C5 prints live; (iii) execute the commands this document prints verbatim — the C1 `census`
    runs, the C2/C3 `validate` runs, the five C5 harness commands, and the §9 wrapper invocations — together with
    whatever those commands themselves invoke or load, because executing a printed command is the instruction,
    not a scope choice." The brief echoes it: "read the pinned environment (the interpreter and the site-packages
    directory the packet's C5 prints); and execute the commands the packet prints verbatim, with whatever they
    themselves load". Every open the five commands make — the interpreter binary (command 3 reads it), the pinned
    directory's 20,637 files (command 5 reads every one), the interpreter's own loads — falls under (ii) or
    (iii). A seat obeying C4 and the brief to the letter can execute all five.
(b) D3 check: every mandated command in the document is a single process whose exit status is the control's.
    Enumerated: C1 census (×2), C2/C3 validate, the lane merge and compute, the builder, the five C5 commands,
    the §9 wrapper. No shell pipeline exists anywhere in the document (grep-verified; the only "|" rows are the
    STANDARD table and a §10 history line); the wrapper is one process that carries the child's status or prints
    SYMBOLIC_TIMEOUT and exits 124; C5's digest equality is a comparison against the dispatch record performed on
    printed live values, not a masked stage. NO_MASKED_STAGE=YES.
(c) The principle leaves no path the seat is TOLD to run but the text forbids (checked against every printed
    command and the brief's order of work), and no path it may CHOOSE that the text should forbid beyond the
    already-stated self-report floor: "Any path the seat CHOOSES to open that is not (i) or (ii) — anything not
    opened by a printed command under (iii) — is an outside path and `FAIL`", with the printed path list as the
    stated detection.
(d) Changed versus the signed V23 (diff of the operative text, §0–§9+§11, ten regions, nothing else): the header
    version line; `-E` added to the C1 census runs (×2), the C2 validate, the C3 validate and lane merge/compute
    commands, and the §9 wrapper invocation; C4 gains the scope principle "(V24)" with its stated residual; the
    dispatch-copy list gains `r3c2_manifest.py`; C5 is rewritten from three commands to five (adding the
    site-packages path print and the r3c2_manifest.py walk, the dispatch-record path/digest equality predicate,
    and the single-process/no-pipeline sentence); C5b gains the IN_SCOPE/OUT_OF_SCOPE definitions tied to C4's
    (i)–(iii). Plus the new delivered artefact r3c2_manifest.py (pinned 19a8ce47…, verified), its row in
    R3C2_SEAT_PACKET.sha256, the rebuilt packet (byte-identical to the pin, verified), the brief's matching scope
    sentence, and §10 history. NOTHING else in the operative text differs.
Round-2 addendum mapped onto V24g: (i) the startup/import-path hole is closed as tamper-evidence — the manifest
digest covers every file under the pinned directory (verified: FILES=20637, one digest), -E kills `PYTHON*`
redirects, and the environment is read-only to the seat; what remains (a non-minimal environment already present
at pin time) is the residual the document states verbatim: "it does not prove the environment is minimal or free
of startup code". (ii) Not too tight: on this host stock /usr/bin/python3 + sympy 1.14.0 run with every needed
load inside (ii)/(iii); no needed path is excluded. (iii) C4, C5b and the brief are consistent — principle,
IN_SCOPE definition, and brief scope sentence align word-for-word in substance.
Round-3 addendum mapped onto V24g: the pin closes the detection limb of the user-writable-path objection (any
post-pin change flips MANIFEST_SHA256 and C5 FAILs on the mismatch) and names rather than closes the trust limb —
which the document says; `/usr/local` is no longer admitted by any reading (it is neither (i), (ii), nor (iii)).

Rename verification (brief's note for STEP 2): `grep -n REPRO_EXACT` on the master returns 16 hits — 15 under
`## 10` headings (version table and change records, history) and 1 at §3 line 118, which is the rename notice
itself ("*(named `REPRO_EXACT` until V21; renamed by the principal's ruling)*"). Governing references to the old
token: 0. Consistent with R3C2_V21_RENAME_AUDIT_20260905.md (opened after the master, as permitted).

Run artefacts of this gate (scratch, in-lane): _tmp_gate_v24g_kimi_c5/ (C5 outputs, fixtures, builder test
copies). Nothing outside the report file was written outside that directory; the packet, master, pins, and tools
on disk are unchanged (packet re-verified byte-identical after the builder run).

R3C2_V24G_GATE_COMPLETE
