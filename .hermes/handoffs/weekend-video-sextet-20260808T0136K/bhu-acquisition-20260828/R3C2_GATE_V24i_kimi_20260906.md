ACCESS_SHA=9c4b873d4281419a93b8aac9208e5a5de22481217fdd124b1dcfa6a7ae4d88be
PACKET_SHA=24da8b8e5dcee0196b18eb9541f3bb5fc1086d23ac608b02d3ecdfb3a739d529
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=at the ruled floor only: the corpus is cosmology (the packet's STANDARD table is the Planck 2018 baseline) and provenance is classified with conspicuous care (seven origins, quotations, two readers, disputes carried, auditor re-classifies), so provenance matters; no hypothesis, comparison model, or preferred tally is inferable from the packet
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=statable from the packet as a cross-reader and audit integrity check the arithmetic never consumes: "Arithmetic consumes records according to status PRINTED or STANDARD", both seats classify origin independently, disagreements file ORIGIN_DISPUTED and above 10% CENSUS_ORIGIN_DISPUTED, and the auditor re-classifies every origin from the pinned sources; what origin is compared against or combined with cannot be stated from the packet
C5_EXECUTABLE_UNDER_SCOPE=YES — I ran all five C5 commands verbatim on this host from a working directory containing the pinned r3c2_manifest.py (sha256 19a8ce4750bb47655868ef15b55f2b168833147b460c03ad56f84dd3c9bc56f2, verified): every one exited 0, and C4's (ii) and (iii) admit every path they touch
NO_MASKED_STAGE=YES

Seat: kimi. Date: 2026-09-06. Master gated: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (V24i living draft).
Files opened: the master; the seat packet r3c2_seat_packet/R3C2_SEAT_PACKET.md; then, as the brief permits after the master: R3C2_V21_RENAME_AUDIT_20260905.md, r3c2_seat_packet/SEAT_BRIEF.md, R3C2_V23_DESIGN_OF_RECORD_55b466fa.md, and a copy of r3c2_manifest.py taken to /tmp for the C5 run. Existence (not contents) of the other named artefacts was verified by digest/listing only. No gate file was read; section 10 was read as history and binds nothing below.

=====================================================================
STEP 1 — ANSWERS FROM THE PACKET ALONE (given before the master was read)
=====================================================================

Q5 (answered first): NO outcome is more consequential than any other, and the packet says so itself. The only consequence-bearing sentence is §11: "No tier, warrant token, standing or stamp moves." That sentence denies any consequence differential between outcomes rather than creating one. The §4 stop classes carry procedural consequences ("The census does not proceed"), but those are identical in kind for every stop and attach to no particular tally. No sentence tells the seat that any per-claim outcome or any study-level class is weightier than another.

Q6 (answered second): I CAN state what the recorded origin is for, up to a point, from the packet alone. The sentences: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`."; "`origin` is one recorded attribute of a ledger record, beside `status`, `value`, `source_file` and `source_line`; a seat records it and writes no field outside the schema; `validate` fails a ledger that carries one."; "Every input's `origin` is classified independently by both seats."; C6's auditor works "without sight of earlier work and re-classifying every input's `origin` from the pinned sources"; §4: "CENSUS_ORIGIN_DISPUTED — the two seats' independent `origin` classifications disagree on inputs affecting more than 10% of included claims." So origin is a provenance attribute that (a) the arithmetic never consumes, (b) is double-classified and audited as an integrity cross-check, and (c) can stop the study on disagreement. What the packet does NOT say: what origin is compared against, combined with, or weighed into. Its analytic use beyond the cross-check is not statable from the packet.

Q3 (answered third): what I can infer about what the lane expects or hopes, with the leaking sentences:
1. The corpus is cosmology, and specifically ΛCDM-parameter cosmology. The STANDARD closed list prints H₀ 67.36, Ω_m 0.3153, Ω_Λ 0.6847, Ω_b h², Ω_c h², n_s, σ₈, τ, ln(10¹⁰ A_s), age 13.797 Gyr, and states: "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." A lane censusing arbitrary physics does not need σ₈ and τ on a closed list; the lane expects papers that consume the Planck 2018 baseline.
2. Provenance matters to the lane — the chosen/fitted/imported/undeclared distinction is measured with machinery far beyond what a pure reproduction verdict needs: seven origin values, machine-matched verbatim quotations, independent classification by both seats, disputes "reported, never reconciled", an auditor who re-classifies from the sources. Sentences: the C3 evidence rules and "the auditor re-classifies the record from the pinned sources." I infer the study's second purpose is to learn what the corpus's numbers rest on. I cannot infer what conclusion rides on it: the packet carries no severity order (the origin list is explicitly alphabetical, "the list carries no order of its own"), no comparison, and no mapping from any provenance pattern to any consequence.
3. The printed-but-chosen case is anticipated and pre-answered: "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED." The lane expects chosen inputs in the corpus and has fixed in advance that they do not block reproduction.
4. Non-reproduction is anticipated and must be worded neutrally: 'Wording: "unreproduced from the stated inputs," not "error."' Any reproduction census implies failures are possible; the packet additionally shows the lane cares that a failure not read as an accusation.
5. A tier/warrant/standing system exists somewhere and this study is fenced from it ("No tier, warrant token, standing or stamp moves") — but the fence is symmetric: nothing moves whatever the tally.
6. What I tried and failed to reconstruct (the adversarial half): a hypothesis, a comparison model, a preferred tally, or any direction of hoped-for result. The outcome classes are symmetric; the stop consequences are procedural and equal; no sentence weights one tally over another. The stake I can reconstruct is exactly the ruled floor: the lane is running a reproduction census over cosmology papers and is measuring what the numbers rest on. Why that measurement matters, and against what it will be compared, the packet does not leak.

=====================================================================
SECTION 1 — OUTCOME CLASSES (with Q1)
=====================================================================

Per-claim: six outcomes, a total precedence ("file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group"), and a closed arithmetic group of exactly two. Exhaustive: every included claim either states no procedure (NO_DERIVATION_STATED), or has an input neither printed nor traced (INPUT_ABSENT), or has an input traced to a named source that is not enumerable-pinned or does not machine-match (BLOCKED), or its attempt times out / lacks machinery (NOT_EVALUABLE), or the arithmetic completes (WITHIN / FAILED by the stated-precision rule). Mutually exclusive by the precedence; I could not construct a claim landing in two after the precedence is applied, nor one landing in none. Exclusions are not outcomes; the exclusion ledger's five kinds are closed and audited.

Study-level: eight classes plus a total precedence that matches chronology. A tally fitting two (e.g., zero denominator plus an audit failure) files the first (CENSUS_AUDIT_FAILED before CENSUS_PARTIAL). A tally fitting none: none exists — after controls pass and enumeration/outcomes/origins agree, the claims are either all arithmetic-group (COMPLETE) or not (PARTIAL, zero denominator included). INCONCLUSIVE is genuinely reachable and named: CENSUS_PARTIAL, "INCONCLUSIVE, and it takes precedence over CENSUS_COMPLETE" — reachable via any single BLOCKED or ABSENT input anywhere in the corpus, which §10's own C0 history confirms is easy.

Q1: YES. Under option (c) §3 as written makes every declared outcome reachable and keeps the reproduction question decidable. Hardest cases, each traced to exactly one outcome and one rests_on:
- Printed-but-chosen input ("we take β = 1/929.25", no source): status PRINTED, origin CHOSEN via ORIG_CHOICE_STATED; the arithmetic consumes it ("every ledger record with status PRINTED or STANDARD"); outcome ∈ {REPRO_WITHIN_STATED_PRECISION, REPRO_FAILED}; rests_on USES_CHOSEN (lane-side compute). One outcome, one rests_on.
- Fitted parameter the paper states ("our fit yields σ₈ = 0.811"): PRINTED + FITTED/ORIG_FIT_STATED; consumed; arithmetic group; rests_on USES_FITTED.
- Imported value ("H₀ = 67.36 [Planck 2018]", the source pinned and enumerable, value machine-matching at the cited line): PRINTED from that source, origin IMPORTED, ORIG_CITATION; consumed; arithmetic group; rests_on USES_IMPORTED. The same citation with no machine-match at the cited line, or a source that is RAW or unpinned: REPRO_BLOCKED, the input recorded BLOCKED with no value, never consumed.
- An input the paper neither prints nor traces: REPRO_INPUT_ABSENT, input named.
- No procedure stated, or a procedure named but not specified: REPRO_NO_DERIVATION_STATED, passage named — and the precedence files it ahead of any input-based class.
- A 120.0-second overrun: SYMBOLIC_TIMEOUT via the wrapper (exit 124), REPRO_NOT_EVALUABLE; machinery missing: MACHINERY_UNAVAILABLE.
- The one genuine seam is already one of the pending five (D1): an imported value whose named source's own line states a choice ("we choose a = 2") matches ORIG_CHOICE_STATED on that line while §2's PRINTED-from-source rule requires IMPORTED/ORIG_CITATION. The verdict is unaffected (the value is PRINTED and consumed either way; the claim still files exactly one arithmetic-group outcome); only the rests_on pole (USES_CHOSEN vs USES_IMPORTED) is at stake, and the item is escalated. It does not break decidability and it is not new.
Reachability versus decidability no longer pull against each other: admissibility is by status (mechanical), provenance is a recorded field (computed), and no class gates on a judgement the method cannot produce.

=====================================================================
SECTION 2 — CONTROLS (with Q2)
=====================================================================

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. None can be passed by bare assertion: C1 PASS "only on exit 0" of the printed census run; C3 "PASS only on exit 0 from every printed run in the artefact; a token asserted without the printed run is FAIL"; C5 "a transcribed value in place of live output is FAIL"; C6 "a token without the artefact is FAIL". Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes", and §4's precedence makes later limbs NOT_RUN after a stop. C4_SEAT_ISOLATION and C5B are self-report and say so at their true strength ("means only that the list contains no outside path; it makes no claim that the list is complete").

Q2: the provenance field is checkable, not asserted. Test record, wrong but plausible: {claim c17, input i3, symbol H0, status PRINTED, origin CHOSEN, origin_evidence {ORIG_CHOICE_STATED, paper.tex, line 812, verbatim "We adopt H₀ = 67.4 km s⁻¹ Mpc⁻¹ from Planck (2018)"}}. The quotation is a true substring of the cited line, so validate's machine checks pass it; but the sentence names an external source, so ORIG_CITATION matches and the tie-break ("file the first in this order: ORIG_CITATION, ORIG_FIT_STATED, ORIG_CHOICE_STATED, …") makes IMPORTED the only correct classification. What catches it: (1) the second seat's independent origin classification — a disagreement is carried as origin_alt, filed ORIGIN_DISPUTED, reported with both quotations, never reconciled, and above 10% of included claims stops the study as CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor, who re-classifies every origin from the pinned sources and files MISMATCH → CENSUS_AUDIT_FAILED. What does NOT catch it: the machine. The document states exactly this floor rather than implying it away: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." Harder variants are machine-caught: a PRINTED value that does not match the cited line fails validate's machine-match; a BLOCKED or ABSENT record carrying a value fails the script's assertion; a DERIVED record with no derived_from, a dangling parent id, or a cycle fails validate. ORIG_SILENT is held to an adequacy standard ("a search of one query with no variants is not adequate") judged by the second seat and auditor via the printed origin_search.

=====================================================================
SECTION 3 — CIRCULARITY
=====================================================================

The lane's expectation cannot reach the evidence on any of the four channels:
- Inclusion: the §1 rule is operational ("prints a numeral the paper asserts as a result of its own") with five closed exclusion kinds; both seats enumerate independently; disagreement stops the study (CENSUS_DENOMINATOR_DISPUTED). There is no category judgement through which a preference could be smuggled.
- Exclusion: exclusions are recorded in a separate ledger with file, line, numeral and kind, "audited under C6, so nothing is hidden by being excluded". A contrary claim cannot be quietly excluded: the auditor checks the full candidate and exclusion ledgers against every pinned source for completeness before any re-derivation.
- Origin classification: cited quotations machine-matched, both seats independent, auditor re-classifies; the tie-break is by evidence specificity with citation first, so a citation cannot be down-ranked to a choice without the second reader or the auditor catching it (floor stated, §2 above).
- Interpretation: the mapping lives in R3C2_INTERPRETATION_PROTOCOL_20260904.md, which "no seat may open", receipted (P) before limb A and opened only after the tally is receipted (T); the verdict is computed before the interpretation is read. "The hypothesis cannot reach the evidence."
The residual is stated honestly and is the correct strength: the lane chose and pinned the corpus before any of this, and §7 concedes "It does not prove Tori had not already guessed the answer — nothing available here could prove that." Within what a pre-registration can bind, the channel is closed.

=====================================================================
SECTION 4 — THE BLIND
=====================================================================

Machine-asserted as claimed. The builder r3c2_build_seat_packet.py is pinned (sha256 4ed52d4b638562f72a604e42c8073d2ea77ff834eb5a6b07e2a46ca32694fbe3 — verified on disk), drops §0/§7/§8/§10 and every SEAT-REDACT span, and asserts a forbidden list: "If any survives, the packet is not written and C4_PACKET_REDACTED=FAIL; the study does not proceed on a hand-checked copy." I verified the delivered packet directly: zero occurrences of pattern, ΛCDM, shape, magnitude, codex, kimi, Tori, Duho, Blanc, Hwao, expectation, refute, consequential, invisible, favourable, unfavourable, stake, prior, tempting, warn, principal, ruling, rests_on, DERIVED_ONLY, comparison, gate, R3D. The packet on disk matches its pin (R3C2_SEAT_PACKET.sha256 pins 24da8b8e…d529, my computed digest), and the packet header embeds the master's digest (9c4b873d…88be, the file gated here), so the packet was built from this master. The claim's strength is stated correctly: "the list blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; the accepted floor (a seat can tell provenance matters) is stated with its ruling; the V24i confinement paragraph makes the kernel sandbox profile (r3c2_seat_sandbox.sb, digest 6978d590…1fa6 verified on disk) the mechanism and C4/C5b the self-reported account; the prior-exposure limit is stated: "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The C4 mechanism prevents a seat being furnished lane content in its working directory and bounds what it can read at dispatch; it does not claim more.

=====================================================================
SECTION 5 — THE SEAL (§7)
=====================================================================

Two receipts, each preceding the step it must precede. (P) the interpretation protocol's hash and commit id are receipted by Blanc (outside the lane, reports to Duho) before limb A begins; "limb A does not begin without receipt P" — so the mapping is custodied before any enumeration exists to suit. (T) the tally commit (merged candidate file, exclusion ledger, merged input ledger with compute output) is receipted, every file named, before the protocol is opened; "the interpretation protocol is not opened without both receipts." After opening, Blanc independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The relay binds what it claims: both artefacts existed in the stated form before the comparison was filed. Its limit is stated: a local commit is "tamper evidence, not an external custodian", and the seal "bounds the record, not the mind". Sound.

=====================================================================
SECTION 6 — FAIRNESS
=====================================================================

Every negative outcome is worded against the inputs, not the authors: REPRO_FAILED carries 'Wording: "unreproduced from the stated inputs," not "error."', and the seat brief repeats it. I grepped the operative text (master §0–§9/§11 and the packet): the only occurrences of "error" are that fairness rule itself and the manifest script's ERROR=<path> file-read failure token, which names an unreadable file, not a claim. No class name asserts more than its definition: REPRO_WITHIN_STATED_PRECISION is defined by an explicit rounding/uncertainty test (round half away from zero; |reproduced − printed| ≤ stated uncertainty taken once; asymmetric uncertainty = the half-width on the side the value falls); CENSUS_COMPLETE requires C6_AUDIT_SAMPLE=PASS and a non-zero denominator; "No study-level outcome is a verdict about the pattern." Fair.

=====================================================================
SECTION 7 — STALL / EXECUTABILITY (with Q4 and Q7)
=====================================================================

Q4 — everything the document describes was checked for executability as delivered:
- Pinned artefacts, existence and digest verified on disk against the digests the document prints: r3c2_ledger_tools.py (ea63fda5…8054 ✓), r3c2_lane_tools.py (8e990c7a…a848 ✓), r3c2_manifest.py (19a8ce47…56f2 ✓), r3c2_timeout.py (fbb9bef7…f331 ✓), r3c2_build_seat_packet.py (4ed52d4b…fbe3 ✓), R3C2_CORPUS_MANIFEST.md (300d4da1…9b24 ✓), r3c2_seat_sandbox.sb (6978d590…1fa6 ✓), R3C2_SEAT_PACKET.sha256 (pins 24da8b8e…d529 = the packet ✓, plus the brief and all three scripts at their correct digests).
- Tool structure verified against the text: r3c2_ledger_tools.py dispatches exactly "validate" and "census" (no merge, no compute — as C3 requires); r3c2_lane_tools.py dispatches exactly "merge" and "compute"; r3c2_timeout.py enforces a monotonic deadline, prints WRAPPER_COMMAND/stdout/stderr/WRAPPER_EXIT, SYMBOLIC_TIMEOUT and exit 124, and returns the child's status; r3c2_manifest.py is one stdlib process that walks, reads and hashes every regular file, prints FILES= and MANIFEST_SHA256=, and exits 1 with ERROR= on any unreadable file.
- Literal commands: every printed interpreter command carries -E; every command is path-qualified where the text requires it; "." is the sole sources_dir in C2 and matches C3's <sources_dir> form. The five C5 commands were executed (below). R3C2_INTERPRETATION_PROTOCOL_20260904.md exists on disk (existence only checked; it is seat-invisible and receipt P is a run-time event, so its unopened presence is by design). No described artefact is missing. One tool-text cosmetic: r3c2_timeout.py's own usage line omits -E ("/usr/bin/python3 r3c2_timeout.py …") while §9 mandates "/usr/bin/python3 -E r3c2_timeout.py 120.0 -- <command>"; the governing §9 text is correct — worth folding into the next tool re-pin.

Q7 (V24i):
(a) C5_EXECUTABLE_UNDER_SCOPE=YES. Governing sentences: C4 "(ii) read the pinned environment — the interpreter /usr/bin/python3 and the site-packages directory whose path and manifest digest the dispatch record fixes before launch and C5 prints live; (iii) execute the commands this document prints verbatim — the C1 census runs, the C2/C3 validate runs, the five C5 harness commands, and the §9 wrapper invocations — together with whatever those commands themselves invoke or load, because executing a printed command is the instruction, not a scope choice." A seat obeying C4 and the brief to the letter can execute every C5 command: the five commands are printed verbatim and admitted by (iii); /usr/bin/python3 and the printed site-packages directory are admitted by (ii); /usr/bin/shasum rides (iii) as one of the five. My own run from /tmp/r3c2_gate_kimi_c5 containing the pinned r3c2_manifest.py: (1) "Python 3.9.6", exit 0; (2) "1.14.0", exit 0; (3) "b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3", exit 0; (4) "/Users/duhokim/Library/Python/3.9/lib/python/site-packages", exit 0; (5) "FILES=20637 / MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed", exit 0. (Run outside the sandbox profile, which is a dispatch-time artefact; the profile file itself was verified by digest.)
(b) D3 check: every mandated command is a single process whose exit status is the control's. I read each one: the C1 census runs, the C2/C3 validate runs, the five C5 commands, the §9 wrapper invocation, and the lane-side merge/compute — no pipeline anywhere (the only "|" in operative text is the STANDARD table's markdown); C5 command 5's placeholder is filled from command 4's live print, which is substitution, not a pipeline; the wrapper is one invocation that prints and returns its child's status (verified in its source). The document's own sentence is true: "no mandated command is a shell pipeline, because a pipeline's exit status is its last stage's and an upstream failure can be masked." No masked stage remains. NO_MASKED_STAGE=YES.
(c) No path the seat is TOLD to run is forbidden, and no path it may CHOOSE is left open that should be closed. Told paths resolve under (ii) or (iii); every chosen path — placeholder, wrapped <command>, import, data path — is expressly made a seat choice subject to (i)–(ii): "Executing a printed command does not authorise arbitrary data access: every placeholder the seat resolves, every child command or <command> the seat supplies to the §9 wrapper, every import and every data path the seat selects is a seat CHOICE and is subject to (i)–(ii)." One wording seam remains (COSMETIC-1 below): (iii)'s parenthetical enumeration does not name the digest prints the brief's step 1 and §9's ACCESS_SHA discipline mandate; the told/chose principle covers them on the natural reading, but the 10:17 abort was caused by exactly this species of literalism, so the enumeration should say so.
(d) Versus the signed V23 (operative text diffed, §10 excluded both sides), the changes are exactly the V24 chain and nothing else: the C4 flat prohibition "Any path outside the working directory is FAIL" and the sentence "This is procedural, not enforced by the filesystem" are replaced by the (i)/(ii)/(iii) principle, the sandbox-profile confinement paragraph, and the rewritten residual; C5 grows from three commands (unqualified shasum, no -E) to five (path-qualified, -E throughout, commands 4–5 printing the site-packages path and manifest digest, the stronger PASS predicate, the no-pipeline statement); C5b's IN_SCOPE/OUT_OF_SCOPE are redefined on the told/chose principle; the dispatch-copy list gains r3c2_manifest.py; -E is added to every printed interpreter command (census ×2, validate ×2, lane merge/compute, builder, wrapper); the seat tool is re-pinned (f9b7d3c8… → ea63fda5…, the -E usage text); the brief is rewritten to match and says "all three pinned scripts". No class, control code, threshold, precedence, taxonomy, seal, or rule of interpretation changed.
(e) YES — the confinement statements agree with each other and with the residual. C4's confinement paragraph: "That profile is the mechanism; C4_SEAT_ISOLATION and C5B_NO_CROSS_LANE remain the seat's self-reported account of what it opened, and PASS on them means what the printed list shows, no more." The residual: "matching manifest digests establish matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them and not the absence of startup code; no inference about trusted content follows from the pin alone. What bounds such code is the confinement recorded in the dispatch record (the kernel profile and its printed probes), not this clause; C4 and C5b are self-report controls and do not themselves bound automatic code access." The blind-limit paragraph: "this design does not prove more than the recorded confinement profile and its probes establish about what the seat could read." Three statements, one claim: the profile is the mechanism, the digests are snapshots, the controls are self-report. Consistent.
(f) NOTHING BEYOND THE PENDING FIVE at substantive weight. The five (D1 imported-choice evidence, D2 exclusion kind for author-specified inputs, D4 no result code on the lane-side no-fallback control, D7 C6 auditor sees outcomes before re-deriving, D8 DERIVED_ONLY label) are the only substantive open items, and each is expressly parked for the principal. My own findings are the two cosmetics below, neither of which is substantive.

=====================================================================
DEFECTS (both COSMETIC — neither changes any class filed, any control's
passability, any reachability, any conclusion, or any tested value)
=====================================================================

COSMETIC-1 — C4's (iii) enumeration omits the mandated digest prints.
Verbatim: "execute the commands this document prints verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, and the §9 wrapper invocations — together with whatever those commands themselves invoke or load"
Defect: the enumeration reads as closed, but the seat is also told to run digest prints — the brief's step 1 ("Print `shasum -a 256` of the packet, this brief, all three pinned scripts and every source text you read") and §9's ACCESS_SHA discipline — which appear in no enumerated kind. The told/chose principle already licenses them ("executing a printed command is the instruction, not a scope choice"), and every file they read is inside the working directory under (i), so nothing is unexecutable and no control's passability changes; but the 10:17 abort was a seat obeying this document to the letter, and a letter-reader should not have to choose between the principle and the enumeration.
Exact replacement: "execute the commands this document prints verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, the §9 wrapper invocations, and the `shasum` digest prints the brief's step 1 and §9's ACCESS_SHA discipline mandate — together with whatever those commands themselves invoke or load" (with the matching half-sentence in C5b's IN_SCOPE definition and the brief's scope paragraph).

COSMETIC-2 — C5's PASS predicate does not compare command (3)'s interpreter digest.
Verbatim: "PASS requires all five commands to exit 0, their full stdout printed, the path of (4) and the `MANIFEST_SHA256` of (5) equal to the dispatch record's"
Defect: command (3) prints the interpreter's digest live (a transcribed value is FAIL, so it cannot be asserted), but the text never requires that digest to equal anything, so an interpreter swapped between the dispatch record and C5 would not fail C5 on its own terms. The sandbox profile and the live print bound the interpreter evidentiarily, so this is a missing comparison, not a hole in passability.
Exact replacement: "PASS requires all five commands to exit 0, their full stdout printed, the digest of (3), the path of (4) and the `MANIFEST_SHA256` of (5) equal to the dispatch record's; a mismatch, a non-zero exit, an `ERROR=` line, missing output, or a transcribed value in place of live output is FAIL." (Requires the dispatch record to pin the interpreter digest before launch, one added line there.)

Tool-text note (not a document defect): r3c2_timeout.py's usage line should gain -E at its next re-pin.

Rename verification (per the STEP 2 note): `grep -n REPRO_EXACT` on the master returns 15 lines; line 118 is the §3 rename notice itself (inside a SEAT-REDACT span: "named `REPRO_EXACT` until V21; renamed by the principal's ruling") and every other hit lies under a "## 10" heading (the version table and §10.4–§10.15 change records). R3C2_V21_RENAME_AUDIT_20260905.md classifies the same pattern for V21. Governing references to the old token: 0. REPRO_AFTER_CHOICE survives only as the §3 retirement notice (SEAT-REDACT) and §10 history. No defect.

R3C2_V24I_GATE_COMPLETE
