ACCESS_SHA=ab6352d35a0e02fbc92173971ada554230ad5fb0578440a9728fc0f1494942e7
PACKET_SHA=575423fb2d68433b2c6b1a67c71599c270d7f69230e2c77042957f0343c7b3dd
GATE=PREREG_UNSOUND
LEAK=The packet reveals a cosmology context and special attention to provenance, including chosen inputs; no directional hypothesis, comparison preference, or preferred empirical result is reconstructable.
CONSEQUENCE_VISIBLE=Procedural only: disputed outcomes stop the study (“The census does not proceed”); no sentence identifies a preferred empirical outcome or its consequence to the lane.
ORIGIN_PURPOSE=Independent comparison and audit: “Every input's `origin` is classified independently by both seats”; disagreements above 10% of included claims stop the census, and C6 re-classifies origin against the sealed record.
C5_EXECUTABLE_UNDER_SCOPE=YES — C4 explicitly permits the five C5 commands and their automatic loads; all five exited 0 on this host.
NO_MASKED_STAGE=YES

## 1. Outcome classes

This is a design verdict, not a reproduction result. I read the packet before the master and answered Q5, Q6, then Q3 before opening the master. I did not open a previous gate or the interpretation protocol. Section 10 supplies no evidence for this verdict. The findings below follow from operative clauses, constructed counterexamples, and the reported live tests.

**Packet-only answers, preserved with their scope clarified:**

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?** My initial answer was no sentence identifies a consequential empirical outcome; the packet says, “No tier, warrant token, standing or stamp moves.” On the question's literal, broader wording, procedural consequences ARE visible: `CENSUS_OUTCOME_DISPUTED` says, “The census does not proceed”; C6 audits “every claim in the arithmetic group” but only a sample of remaining claims. These disclose stopping and workload consequences, not which empirical result the lane wants. The header records this distinction rather than converting “no revealed stake” into “no consequences whatsoever.” This clarification uses only the packet.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR?** Yes for its procedural use; no for a later interpretation. My initial CANNOT_STATE answer referred to that later use and was too broad for the literal question. The packet says, “Every input's `origin` is classified independently by both seats.” C6 requires “re-classifying every input's `origin` from the pinned sources” and records `MATCH` or `MISMATCH` against the sealed record. Section 4 stops on disagreements affecting more than 10% of included claims. Thus recording, comparing, auditing, and potentially stopping are stated uses. Neither a provenance-summary rule nor an empirical interpretation is recoverable from the packet. This correction also uses only packet text.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?** I can infer methodological concern about chosen inputs from: “Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`.” I can infer cosmological context from: “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.” The detailed origin taxonomy makes provenance an intended object of scrutiny. Neither sentence identifies a hypothesis, an empirical shape/magnitude pattern, a comparison preference, or which tally would support the lane. Reconstructing any of those would require speculation beyond the packet. I do not treat the master’s later disclosures as packet inferences.

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?** The six outcome definitions have reachable witnesses and an explicit precedence. Option (c) itself succeeds: chosen and fitted printed inputs remain consumable. But the complete operative procedure does not give every such claim a consistent, valid ledger: the imported-input evidence conflict below prevents an unconditional YES.

Concrete scientific claim constructions, not assertions about unread corpus passages:

| Claim and stated inputs | Unique per-claim disposition | `rests_on` |
|---|---|---|
| “We choose a = 2; y = 2a; our result is y = 4.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_CHOSEN` |
| Same recipe and input, but claimed y = 5 with integer precision | `REPRO_FAILED` | `USES_CHOSEN` |
| “The fitted slope is b = 3”; x = 2 is chosen; y = bx; claimed y = 6 | `REPRO_WITHIN_STATED_PRECISION` | `USES_FITTED` |
| y = 2a; a comes from a named source outside the enumerable manifest | `REPRO_BLOCKED` | `USES_IMPORTED` |
| y = 2a; a is neither printed nor attributed anywhere | `REPRO_INPUT_ABSENT` | `USES_UNDECLARED`, with an adequate documented silent-origin search |
| Claimed y = 4 without any equation or specified procedure | `REPRO_NO_DERIVATION_STATED` | `NOT_COMPUTED` if there are no ledger records |
| Fully specified numerical calculation with chosen inputs, exceeding the enforced cap | `REPRO_NOT_EVALUABLE` with `SYMBOLIC_TIMEOUT` | `USES_CHOSEN` |

A missing required facility independently reaches `REPRO_NOT_EVALUABLE` with `MACHINERY_UNAVAILABLE`. A claim needing both an externally blocked input and an entirely absent input satisfies two raw conditions, but the precedence files exactly `REPRO_BLOCKED`. It is not a double filing. A missing procedure takes precedence over either input condition. I found no double filing among the six outcomes once their precedence is applied.

Study-level reachability is also concrete: one audited arithmetic claim reaches COMPLETE; one agreed absent-input claim with a passing audit reaches PARTIAL; a zero denominator independently reaches PARTIAL; an audit mismatch or missing receipt reaches AUDIT_FAILED; the same C5 failure twice in every attempting seat reaches NO_CLASS; a passing and failing seat on that control reaches CONTROL_SPLIT; surviving inclusion/input-list disagreement reaches DENOMINATOR_DISPUTED; surviving outcome disagreement reaches OUTCOME_DISPUTED; two origin-disputed claims among ten included claims reach ORIGIN_DISPUTED. These examples assume earlier-precedence stop conditions are absent. A tally with one absent-input claim and a failed audit satisfies the raw PARTIAL and AUDIT_FAILED conditions but files only AUDIT_FAILED. INCONCLUSIVE is genuinely reachable through PARTIAL; it does not require an arithmetic failure.

**D1 — substantive: an imported value can have no evidence record satisfying both §2 and C3.**

Verbatim governing sentence from §2:

> A value the paper does not print but traces to a named source that is itself an enumerable text in `R3C2_CORPUS_MANIFEST.md` is classified `PRINTED` from that source, with `origin` `IMPORTED`, `origin_evidence` `ORIG_CITATION` cited to the named source's file and line, and the value machine-matched there — **only when such a match exists; a cited value that does not machine-match at the named source's cited line, or whose named source is not an enumerable text of the manifest, files `REPRO_BLOCKED` under §3.**

Hardest case: claiming paper A states “Use a from B, line 1; y = 2a; our result is y = 4.” Enumerable B, line 1 says “We choose a = 2.” Arithmetic must consume 2 and reproduce 4. The intended result is `REPRO_WITHIN_STATED_PRECISION` plus `USES_IMPORTED`. However, §2 demands ORIG_CITATION evidence at B's line, which contains a choice and no citation. C3's sentence-based reason-code rule supports ORIG_CHOICE_STATED there. Citing A fixes the evidence but violates §2's location requirement; calling the origin CHOSEN violates §2's IMPORTED requirement; BLOCKED is false because B is enumerable and the value matches. Mechanical substring validation can accept the semantically false citation tag, but the source audit should reject it. This is a control-validity conflict, not a reason to reopen option (c).

Exact replacement for the quoted sentence:

> A value the claiming paper does not print but traces to a named source is classified `PRINTED` with `origin` `IMPORTED` only when that source is an enumerable text in `R3C2_CORPUS_MANIFEST.md` and the value machine-matches its cited source line. Record `source_file` and `source_line` at that external value line; record `origin_evidence` with reason code `ORIG_CITATION` and a machine-matched quotation at the claiming paper's sentence directing the reader to that source. The origin records the claiming paper's import, regardless of how the external source obtained the value. If the named source is not enumerable or the value does not match there, file `REPRO_BLOCKED` under §3.

**Q7(g), AUTHOR_SPECIFIED_INPUT hardest case.** “For this calculation we set a = 2, and report a = 2 as our result” must be included: the explicit result assertion defeats the new kind's conjunct “does not assert as a result of its own.” If no producing procedure is stated, it files `REPRO_NO_DERIVATION_STATED`; if y = 2a is specified and y = 4 is asserted, the latter result is attempted using the chosen input. Where a setup passage merely sets a = 2 and a distinct passage derives and asserts a = 2, enumerate both passages: exclude the setup-only passage as AUTHOR_SPECIFIED_INPUT and include the derived-result passage. Equality of numerals does not merge passage candidates. Two seats cannot validly exclude an explicit result merely because it was also chosen. They can disagree about an ambiguous passage's assertion; §1 routes a surviving disagreement to DENOMINATOR_DISPUTED. C1 checks row disposition and retained fields, not semantic correctness of that judgment. The added kind closes the missing setup-input category without removing stated chosen inputs from arithmetic.

## 2. Controls

C0 requires an exhibition and independent verification; C1 requires declared/recomputed ledgers and printed runs; C2/C3 require validator evidence; C4_PACKET_REDACTED requires the pinned builder; C4_SEAT_ISOLATION and C5B_NO_CROSS_LANE require printed path lists but explicitly remain self-report; C5 requires live outputs and dispatch-pin comparison; C6 requires a printed audit artifact. All these named controls have PASS/FAIL/NOT_RUN codes. An unreached control is NOT_RUN. A bare PASS assertion satisfies none of their stated artifact predicates. This does not make a self-reported path list complete or a semantically wrong ledger automatically detectable.

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?** Yes as a cited, independently reviewable field; no as a machine-certified semantic classification. I executed this deliberately wrong but plausible synthetic record:

```json
{
  "claim_id": "q1",
  "input_id": "a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "synthetic.txt",
    "source_line": 1,
    "verbatim": "We adopt a = 2 from Source B."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "synthetic.txt",
  "source_line": 1
}
```

The synthetic file contained exactly that quotation. `validate wrong_origin.json .` printed `C3_NO_SUBSTITUTION=PASS`, exit 0. Merging two identical copies exited 0 with zero origin disagreements. `compute` exited 0 and printed:

```text
q1	rests_on=USES_CHOSEN	root_origins=['CHOSEN']
```

The code correctly propagates the supplied classification; it does not correct it. C3's citation-first rule requires IMPORTED because the quotation explicitly attributes the value to Source B. The second independent classifier catches it if that reader applies the rule; C6's independent re-classification catches it on an audited claim if the auditor applies the rule. An arithmetic claim is necessarily audited. A non-arithmetic claim can escape the input re-classification sample. Identical mistakes by all relevant readers survive, a limitation the operative master expressly admits. The machine-matched quotation is evidence to examine, not proof that the reason code is true. D1 additionally creates a case where the specified evidence location itself is wrong; D2 below compromises the promised independence of the audit.

**D3 — substantive: §9's no-fallback control lacks a result code and a filing rule.**

Verbatim sentence:

> Lane-side procedure, not the seat's: the no-fallback control is the provider log showing no fallback line for the seat's session, checked by the lane owner; a one-page check sheet `R3C2_CHECK_SHEET_<date>.md` in plain words with source lines is written by the lane owner after the tally; the lane owner runs `r3c2_lane_tools.py` (sha256 `8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848`; merge, then compute) after both seats exit and re-runs every script; a critic note precedes any ruling.

A log is named, but missing logs, an observed fallback, and an unperformed check do not receive an exact token or a study-level disposition. This can change whether a census is filed, so it is not cosmetic.

Exact replacement:

> Lane-side procedure, not the seat's: before accepting each seat attempt, the lane owner prints the complete provider log for that session, its session identifier, and `C9_NO_FALLBACK=PASS|FAIL|NOT_RUN`. PASS requires the complete session log to be available, printed, and to contain no fallback event; a missing or incomplete log or any fallback event is FAIL; an unreached check is NOT_RUN. Apply the two-attempt control rules of §4 to C9 exactly as to C0 through C5b. A one-page check sheet `R3C2_CHECK_SHEET_<date>.md` in plain words with source lines is written by the lane owner after the tally; the lane owner runs `r3c2_lane_tools.py` (sha256 `8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848`; merge, then compute) after both seats exit and re-runs every script; a critic note precedes any ruling.

Corresponding exact replacement of §4's NO_CLASS definition:

> **`R3C2_NO_CLASS`** — a control among C0 through C5b, or C9_NO_FALLBACK, fails in every seat that attempted it after two attempts; a packet or seat-isolation failure before dispatch files this class. A C6 audit failure or a seal-receipt failure files `CENSUS_AUDIT_FAILED`, not this class.

## 3. Circularity

The inclusion rule contains no model preference. Every candidate disposition is retained, and C6 audits candidate/exclusion completeness against every pinned source. A contrary, explicitly asserted result cannot properly be excluded as AUTHOR_SPECIFIED_INPUT. Concealment remains possible through reader error or misconduct, but the rule does not authorize it. Origin judgment is still a human channel, bounded by independent classification, quotations, dispute reporting, and audit. Interpretation is placed after the receipts and is not provided to the seats.

**D2 — substantive: C6 exposes earlier outcomes before its promised independent re-derivation.**

Verbatim sentence:

> A third independent seat **first audits the full candidate and exclusion ledgers against every pinned source** — completeness, not just outcomes — then re-derives, **without sight of earlier work and re-classifying every input's `origin` from the pinned sources**: **(i) every claim in the arithmetic group** — no sampling discount — and **(ii) a sample of `min(max(1, ceil(0.20 × N)), R)` of the remaining included claims**, `N` being the sealed denominator and `R` the number of remaining claims (when `R` is zero the sample is empty and every included claim is already audited under (i)), drawn by `random.Random(seed_int).sample(remaining_ids, k)` where **`remaining_ids = sorted(set(included_ids) − set(arithmetic_group_ids))` and `seed_int = int(seed_hex, 16)`, the custodian's seed being 64 lowercase hexadecimal characters**.

C1 makes the full candidate ledger carry `outcome` and, for arithmetic claims, `printed_value` and `reproduced_value`. Reading it first reveals exactly the earlier result the auditor is supposed to re-derive without seeing. No inference from history is needed: these two schema/order requirements directly conflict. This does not prove lane-expectation contamination occurred; it defeats a specified barrier against correlated classifications and outcomes.

Exact replacement:

> A third independent seat first enumerates and classifies candidate passages independently from every pinned source, without access to either seat's candidate, exclusion, input, or outcome ledgers. After receipt T and supply of the external seed, the custodian computes the audit selection: every arithmetic-group claim plus `k = min(max(1, ceil(0.20 × N)), R)` remaining claims, using `random.Random(seed_int).sample(remaining_ids, k)`, where `remaining_ids = sorted(set(included_ids) − set(arithmetic_group_ids))` and `seed_int = int(seed_hex, 16)` for the custodian's 64-lowercase-hexadecimal seed. The auditor receives only the selected claim identifiers and source locators, without earlier outcomes, number pairs, origin classifications, or group labels, and independently re-derives those claims and re-classifies their inputs from the pinned sources. The auditor's enumeration and re-derivations are committed and receipted before the earlier ledgers are disclosed. Only then does the auditor compare the full candidate and exclusion ledgers against its independent enumeration and every pinned source, compare the selected re-derivations and input classifications against the sealed records, and produce `C6_AUDIT.json` with the existing MATCH/MISMATCH and completeness predicates.

## 4. The blind

**Machine redaction verified by execution.** I copied the named builder inputs into an isolated temporary directory and ran `/usr/bin/python3 -E r3c2_build_seat_packet.py`. It exited 0, printed `C4_PACKET_REDACTED=PASS`, retained sections 1, 2, 3, 4, 5, 6, 9, 11, and reported 51 redacted inline spans. Its generated packet hash exactly equaled the PACKET_SHA above. A second run with the word `expectation` inserted into the copied §1 exited 1 and printed:

```text
FAIL: C4_PACKET_REDACTED=FAIL — forbidden content survived redaction:
  line 13: 'expectation' in: expectation
Packet NOT written.
```

The original master and packet were not edited. This checks reproducibility and a real negative assertion, not every possible semantic disclosure. The text honestly states that a forbidden-string assertion cannot prove all consequence-bearing language absent. Its stated limits on prior exposure, self-report, snapshot matching, and startup code are appropriate. The kernel-profile digest matches the delivered file, but I did not execute a full seat dispatch or inspect a dispatch record; I do not certify actual confinement from prose or a matching hash.

**C1 — COSMETIC overstatement within an otherwise qualified scope rule.**

Verbatim sentence:

> Why the line is drawn here: what the seat is told to run cannot leak the study's content, because the packet's own text is reviewed and blinded before dispatch; what the seat decides to open can.

Reviewing command text does not establish that automatically loaded code cannot disclose content. The very next residual correctly assigns that boundary to confinement, not the pin or command text. Exact replacement:

> The distinction separates mandated execution from seat-selected access; protection against disclosure through automatically loaded code depends on the recorded confinement profile and its probes, not on review of command text alone.

COSMETIC because the following residual already supplies the governing limit; this removes an inconsistent explanation without changing any pass predicate, path permission, class, or tested value.

## 5. The seal

Section 7's two receipted relays bind the proposed objects at the needed times. Receipt P fixes the protocol hash and commit identifier before limb A. Receipt T fixes the tally commit containing candidates, exclusions, merged inputs and compute output before opening the protocol; its file list prevents an unnamed tally component from being silently substituted. Both receipts and four independently verified values must appear in the interpretation report. Missing receipts or mismatches void the comparison through AUDIT_FAILED and interpretation NOT_RUN.

This is a sound prospective custody design. I verified only that the named protocol file exists, without opening it. I did not verify any actual receipt, commitment, timestamp, custodian action, or the protocol's mapping. Those are future execution evidence, not facts established by this gate. The clause correctly disclaims proof that the lane owner had not guessed an answer beforehand. No seal repair is required by the inspected operative text.

## 6. Fairness

`REPRO_FAILED` explicitly requires “unreproduced from the stated inputs,” not “error”; the brief applies that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NOT_EVALUABLE and NO_DERIVATION_STATED identify reasons an attempt cannot supply an arithmetic verdict. COMPLETE is coverage plus audit success, not a claim that every paper reproduced successfully. PARTIAL is explicitly inconclusive. No operative outcome calls an author's result an error.

**C2 — COSMETIC label overreach under an explicit definition.**

Verbatim rule:

> `DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`; otherwise the most severe root origin present, in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` > `USES_FITTED` > `USES_CHOSEN`.

A measured-only root set receives DERIVED_ONLY, so the label by itself says more than its definition. Exact replacement:

> `DERIVED_ONLY` is the fixed code for a root-origin set contained in `{DERIVED, STANDARD, MEASURED}`; it does not mean that every input was derived. Otherwise use the first present root-origin category in the fixed order `USES_UNDECLARED` > `USES_IMPORTED` > `USES_FITTED` > `USES_CHOSEN`.

This clarification preserves the code, its membership, every filed class, and every computed value. It is COSMETIC under the requested criterion. I have not silently renamed a principal-controlled token.

**Rename check:** I searched all master headings and every `REPRO_EXACT` occurrence. The sole occurrence outside a §10 heading is the explicit rename notice at line 118. All others are under §10 or a §10.x heading, including those after the physically interposed §11. There is no governing old-token reference. I did not use a narrated rename audit as evidence.

## 7. Stall / executability

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?** Yes: D1 prevents a semantically valid C3 record for its imported-choice case, and D2 cannot be executed both in the required order and without seeing earlier outcomes. D3 leaves an explicitly named control without a complete filing procedure. These are design execution defects even though the requested C5 commands and exclusion fixtures run.

**Q7(a): C5 executable under C4/C5b/brief — YES.** Governing C4 text permits:

> execute the commands this document prints verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, and the §9 wrapper invocations — together with whatever those commands themselves invoke or load, because executing a printed command is the instruction, not a scope choice.

C5b defines IN_SCOPE by C4's (i), (ii), and printed-command execution under (iii). The brief agrees: “execute the commands the packet prints verbatim, with whatever they themselves load — executing a printed command is the instruction, not a scope choice.” Its final sentence makes the packet govern any difference. The specific exceptions govern the preceding general directory instruction.

I copied the delivered, hash-matching `r3c2_manifest.py` into this working directory and ran all five commands there, in order:

```text
/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v25_codex_c5_0kj579kz
```

Full C5 stdout and exit codes; stderr was empty throughout:

```text
1. /usr/bin/python3 -E --version
Python 3.9.6
EXIT_1=0

2. /usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"
1.14.0
EXIT_2=0

3. /usr/bin/shasum -a 256 /usr/bin/python3
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3
EXIT_3=0

4. /usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"
/Users/duhokim/Library/Python/3.9/lib/python/site-packages
EXIT_4=0

5. /usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
EXIT_5=0
```

An earlier run from the lane, which already contains the manifest script, produced the same outputs. These executions establish host executability, not `C5_HARNESS_PINNED=PASS` for a census dispatch: no pre-launch dispatch record was inspected and no sandboxed census seat was launched.

**Q7(b), D3 check: NO_MASKED_STAGE=YES.** I checked the operative C1 census and census-final templates, C2/C3 validate templates, lane merge and compute templates, builder command, all five C5 commands, §9 wrapper template, and the brief's hash/final-census instructions. They are individual invocations, not mandated shell pipelines. Every printed Python interpreter command in these governing instructions has `-E`. The wrapper necessarily launches a child, so “single process” is literally inappropriate for it; the document correctly qualifies this as one invocation with a reported child status. A live child deliberately exiting 7 produced `WRAPPER_EXIT=7` and wrapper exit 7. No masked stage was observed. Historical shell examples in §10 are not mandated operations.

**Q7(c): boundary and startup/import issue.** No required C5 path remains forbidden by the governing principle. `/usr/bin/shasum`, the interpreter's own runtime loads, and SymPy's automatic dependencies are authorized as parts of mandated execution. The exact site-packages directory is admitted under (ii); a seat-selected `/usr/local` path, lane-master path outside its copy, or other outside data path remains FAIL. Wrapping such a selection in a permitted command does not exempt it. C4, C5b, and the brief agree on this rule.

The manifest pin does not make host-user-writable code immutable or trusted; it compares snapshots. `-E` does not itself suppress every startup mechanism. The current text explicitly says those things and assigns automatic-code confinement to the recorded kernel profile and probes. Thus the pin identifies and detects some changes to the surface; it does not independently close the user-writable-path objection. The live run shows the current bound is not too tight for this host's Python and SymPy. I do not infer that every platform or dispatch profile will work from this one run. The earlier prefix-list questions are superseded by the current principle, not additional simultaneous rules.

**Q7(d): actual changes versus signed V23.** I directly diffed the authorized V23 file, SHA256 `55b466fadf8ca75f4894f97721da86584057ddc4d15c15ca1739c67ff6333c6f`, against this master. “Nothing else” would be false. The complete operative differences are: the header/version/signing description; the seat-tool digest; AUTHOR_SPECIFIED_INPUT, alphabetic exclusion-kind ordering, retained exclusion fields, mismatch rejection and the separate count; `-E` on census, census-final, validate, merge, compute, builder and timeout invocations; the C4 permission principle, placeholder/import/child-path restrictions and startup/snapshot residual; addition of the manifest script to the dispatch inventory; replacement of the procedural-only confinement account with a pinned kernel profile and required probes; the revised blind-limit wording; replacement of three C5 commands with five, including absolute shasum, live site-directory and manifest outputs and dispatch equality checks; and C5b's corresponding scope definitions. No other operative section differs in the master comparison. Outside those sections, the history table changes V23's status and adds V24, §10.11's tool digest changes, and §§10.18–10.19 are appended. Those historical changes are a diff result, not findings adopted from their narrative. I checked the current brief's consistency; no separate signed V23 brief was supplied for a byte-level brief diff. No V24i or round-2 snapshot was opened, so I do not certify an exact diff against either.

**Q7(e–f): delivery and pin evidence.** Every following lane-side file exists and its live SHA256 matches the corresponding master pin: `r3c2_ledger_tools.py`, `r3c2_lane_tools.py`, `r3c2_manifest.py`, `r3c2_timeout.py`, `r3c2_build_seat_packet.py`, `r3c2_seat_sandbox.sb`, and `R3C2_CORPUS_MANIFEST.md`. The root `R3C2_SEAT_PACKET.sha256` exists. The authorized packet and brief exist; the isolated builder reproduced the original packet digest. The working-directory template has resolvable relative command names when the prescribed flat dispatch copy is assembled. The scripts and pin file are not inside the lane's `r3c2_seat_packet/` subdirectory; that is not a missing-delivery defect because C4 explicitly requires assembling a separate flat copy before dispatch, and the corresponding lane-root artifacts exist.

I exercised `validate`, `merge`, and `compute` using synthetic inputs; `root_origins` and `rests_on` appeared in live compute output. I exercised builder success/failure and wrapper child-failure propagation. I exercised census with the three explicitly named exclusion fixtures below. I did not execute census-final on a real study ledger, every graph-validation failure mode, a timeout-duration probe, an unreadable-file manifest probe, or the actual dispatch sandbox. I did not open the corpus manifest as text or enumerate its source files, inspect custody receipts, inspect provider logs, or open the interpretation protocol. The narrow file-reading instruction does not permit claiming a complete source-delivery or custody audit; uninspected artifacts are unverified, not presumed absent. `C6_AUDIT.json`, reconciled census ledgers, dispatch records, receipts and the dated check sheet are execution outputs or prerequisites to future steps, not artifacts this unrun design claims have already been produced. Third-seat dispatch is expressly administrative, not a packet-delivered executable. No missing named root script was found.

**Q7(h): live AUTHOR_SPECIFIED_INPUT controls.** Commands ran from the lane directory with the delivered pinned seat tool. Positive run:

```text
/usr/bin/python3 -E r3c2_ledger_tools.py census _tmp_r3c2_ledger_ctl/cand_asi.json _tmp_r3c2_ledger_ctl/excl_asi.json
declared: candidates=4 included=1 excluded=3 attempts=1 exclusions=3
outcomes: PENDING=1
author_specified_input=1
recomputed: candidates=4 included=1 excluded=3 attempts=1 reconciled=YES
C1_DENOMINATOR_PRINTED=PASS
EXIT=0
```

Wrong-numeral negative run:

```text
/usr/bin/python3 -E r3c2_ledger_tools.py census _tmp_r3c2_ledger_ctl/cand_asi.json _tmp_r3c2_ledger_ctl/excl_asi_wrongnumeral.json
FAIL: exclusion ASI1: numeral differs from the candidate row
declared: candidates=4 included=1 excluded=3 attempts=1 exclusions=3
outcomes: PENDING=1
author_specified_input=1
recomputed: candidates=4 included=1 excluded=3 attempts=1 reconciled=NO
C1_DENOMINATOR_PRINTED=FAIL
EXIT=1
```

Missing-numeral negative run:

```text
/usr/bin/python3 -E r3c2_ledger_tools.py census _tmp_r3c2_ledger_ctl/cand_asi.json _tmp_r3c2_ledger_ctl/excl_asi_nonumeral.json
FAIL: exclusion ASI1: missing numeral (the excluded numeral and its line are retained in the exclusion ledger)
declared: candidates=4 included=1 excluded=3 attempts=1 exclusions=3
outcomes: PENDING=1
author_specified_input=1
recomputed: candidates=4 included=1 excluded=3 attempts=1 reconciled=NO
C1_DENOMINATOR_PRINTED=FAIL
EXIT=1
```

The positive PENDING output is appropriate for this pre-limb-B fixture, not proof of a finished census. These controls verify the new retained-numeral behavior; they do not decide whether a real passage is an author's result. The gate remains UNSOUND because D1 and D2 prevent the declared procedure from being carried out consistently, with D3 additionally leaving a control unclassified. The C5 scope repair and new exclusion controls pass the specific tests reported here.

R3C2_V25_GATE_COMPLETE
