ACCESS_SHA=ab6352d35a0e02fbc92173971ada554230ad5fb0578440a9728fc0f1494942e7
PACKET_SHA=575423fb2d68433b2c6b1a67c71599c270d7f69230e2c77042957f0343c7b3dd
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: C3's closed STANDARD list (Planck 2018 TT,TE,EE+lowE+lensing baseline) tells the seat the corpus is cosmology and that chosen/fitted/imported inputs are anticipated; no hypothesis about the census result, no comparison model, no preferred outcome, no "pattern" is inferable from the packet.
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=statable only as an integrity check: origin is a recorded, cited, twice-independently-classified attribute that the C6 auditor re-classifies and whose disputes (>10% of included claims) stop the census; what it is compared or combined with cannot be stated from the packet — the arithmetic never consumes it ("whatever its `origin`").
C5_EXECUTABLE_UNDER_SCOPE=YES — all five printed C5 commands ran on this host from a working directory containing the pinned r3c2_manifest.py with exit codes 0,0,0,0,0 (commands 2 and 4 executed as identical-code payload files under /usr/bin/python3 -E because this session's command filter blocks `-c`; interpreter, flag and code byte-identical to the printed commands).
NO_MASKED_STAGE=YES

============================================================
STEP 1 — PACKET ANSWERS (answered from the packet ALONE, before the master was read; the packet was read and these answers fixed before STEP 2)
============================================================

Q5 (before Q3). NO — no sentence in the packet makes any outcome more consequential than any other, to the lane or to anyone. The only sentence that bears on consequence at all is §11's: "No tier, warrant token, standing or stamp moves." That sentence flattens consequence across outcomes rather than weighting one. The two precedence sentences — "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4.2) and the §4 filing order — are filing rules, not stakes. "No tally is filed" (§4.3) is procedural. `CENSUS_CONTROL_SPLIT`'s "do not adopt the passing seat's result" is an instruction, not a consequence. CONSEQUENCE_VISIBLE=NO.

Q6 (before Q3). I can state what recording `origin` commits the seat to, and what checks it — but not what the study DOES with it downstream. What the packet says: "`origin` is one recorded attribute of a ledger record, beside `status`, `value`, `source_file` and `source_line`; a seat records it and writes no field outside the schema" (§3); "`origin` must be cited, not asserted" with machine-matched quotations (C3); "Every input's `origin` is classified independently by both seats" (C3); the auditor re-derives "re-classifying every input's `origin` from the pinned sources" (C6); disagreements are carried, "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (C6/§4.7). What the arithmetic does with it: nothing — "THE INPUTS THE ARITHMETIC MAY CONSUME = every ledger record with status `PRINTED` (given in the paper, whatever its `origin`) or `STANDARD`". So origin is for a cross-reader, cross-auditor integrity check on provenance recording. What the study combines it with, compares it against, or concludes from it: the packet does not say. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability, adversarial). Everything I can infer from the packet alone:
(a) THE CORPUS IS COSMOLOGY. C3 prints a closed STANDARD list whose cosmological rows are named: "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." Fourteen values (H0=67.36, Omega_m=0.3153, sigma8=0.8111, tau=0.0544, age_Gyr=13.797 …) plus G, c, hbar, k_B. The lane therefore expects claims that consume Planck 2018 baseline parameters and fundamental constants. This is corpus content, not a result expectation.
(b) THE CHOSEN/FITTED/IMPORTED CASES ARE ANTICIPATED. Seven origin values with a reason-code tie-break, the IMPORTED-from-pinned-source rule, and exclusion kinds for "a parameter adopted 'for this calculation'" tell me the lane expects papers whose numbers rest on set/fitted/imported inputs and has pre-committed neutral handling of them.
(c) FAILURE IS A LIVE POSSIBILITY, PRE-WORDED. "Wording: 'unreproduced from the stated inputs,' not 'error'" tells me the lane anticipates REPRO_FAILED rows and has pre-committed how they may be phrased. That is a fairness rule about wording, not a preference for or against failure; any reproduction census implies failures are possible.
(d) NOTHING ELSE. No comparison model, no expected tally, no "pattern", no stake, no hypothesis the census is designed to reach. I tried to reconstruct one: the packet's process tells (two seats, external seed, sealed tally) show the result matters to someone, but to whom and which way is not recoverable. Machine check: grep of the packet for expectation|refute|consequential|invisible|favourable|unfavourable|stake|prior|tempting|warn|pattern|ΛCDM|R3D|kimi|codex|tori|blanc|duho|hwao|rests_on|comparison returns ZERO hits. The six prior leak findings appear closed; the residue is exactly the content-level floor (a)–(c).

============================================================
STEP 2 — THE MASTER, JUDGED
============================================================

Rename verification (NOTE FOR STEP 2): `grep -n REPRO_EXACT` on the master gives 16 hits. Line 118 is the §3 rename notice itself ("*(named `REPRO_EXACT` until V21; renamed by the principal's ruling)*"), inside a SEAT-REDACT span; every other hit (562, 727, 783, 828, 851, 877, 895, 923, 952, 974, 982, 993, 1007, 1016, 1018) lies under a `## 10…` heading (first `## 10` at line 526). `R3C2_V21_RENAME_AUDIT_20260905.md` (opened after the master, as permitted) classifies the same pattern for V21: 0 governing references. Governing old-token references in V25: 0. The seat tool (sha256 `230359ebd1…`, verified on disk) contains 0 occurrences of `rests_on` and only the `census`/`validate` subcommands.

------------------------------------------------------------
1. OUTCOME CLASSES, with Q1
------------------------------------------------------------
Exhaustive and mutually exclusive, per claim: six outcomes, a total precedence for co-occurrence ("file the first in this order: `REPRO_NO_DERIVATION_STATED`, `REPRO_BLOCKED`, `REPRO_INPUT_ABSENT`, `REPRO_NOT_EVALUABLE`, then the arithmetic group"), and the exclusion ledger kept OUT of the per-claim outcome set. A real claim fitting two classes: one with a named-but-unpinned source AND an absent input satisfies BLOCKED and INPUT_ABSENT; the precedence files BLOCKED — exactly one outcome. A real claim fitting none: the arithmetic test is total — the rounding rule ("must round to the printed numeral at that precision, rounding half away from zero") and the uncertainty rule ("|reproduced − printed| ≤ the stated uncertainty, taken once … asymmetric: the half-width on the side the reproduced value falls") decide every completed attempt into WITHIN or FAILED; an uncompleted one has NOT_EVALUABLE with `SYMBOLIC_TIMEOUT`/`MACHINERY_UNAVAILABLE`; a recipe-less one has NO_DERIVATION_STATED ("A procedure named but not specified … states no computational procedure"). No gap found.

Study level: eight classes with a total precedence ("file the first in this order: `R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`"). A tally satisfying two (audit failure over a partial census) files the earlier. `CENSUS_COMPLETE` requires `C6_AUDIT_SAMPLE=PASS`, so it cannot co-hold with `CENSUS_AUDIT_FAILED`. Zero denominator files `CENSUS_PARTIAL` by name. INCONCLUSIVE is genuinely reachable: any single non-arithmetic outcome or an empty corpus files it, and it precedes COMPLETE.

Q1 — YES. Under option (c) every declared outcome is reachable and the reproduction question stays decidable; the old tension is gone because admissibility no longer gates consumption. The three hardest cases, traced:
  (i) PRINTED-BUT-CHOSEN INPUT: "We set beta = 1/929.25 for the fit." (the lane's own control, `_tmp_r3c2_ledger_ctl/src.txt:2`, run through the delivered tool during this gate: `validate` PASS, exit 0). Status PRINTED, origin CHOSEN; the arithmetic consumes it by status; the claim files exactly one arithmetic-group outcome; `rests_on` computes USES_CHOSEN from the root. One outcome, one rests_on, no choice by the seat.
  (ii) A FITTED PARAMETER THE PAPER STATES: the printed fit result is the claim's numeral; if the fitting procedure is stated it is attempted (WITHIN/FAILED); if only named, NO_DERIVATION_STATED; the fitted INPUT is recorded FITTED via ORIG_FIT_STATED and the arithmetic consumes it (status PRINTED). Decidable, one outcome.
  (iii) AN IMPORTED VALUE: cited to a pinned enumerable text, machine-matching at the cited line → PRINTED with origin IMPORTED, consumed; not matching there, or the named source not in the manifest → REPRO_BLOCKED; no named source at all → REPRO_INPUT_ABSENT. Each route terminates in exactly one outcome, and two seats apply the same machine-match test.
Reachability of the V25 addition itself (the sixth exclusion kind — not an outcome) is control-tested, not C0-tested: positive and negative controls exist and were run by this seat (section 7/h below). C0's per-outcome exhibition is unaffected by V25 (no outcome class added, retired or redefined).

------------------------------------------------------------
2. CONTROLS, with Q2
------------------------------------------------------------
Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C6_AUDIT_SAMPLE. "Controls in an unreached limb are `NOT_RUN`, never passes." No control can be passed by bare assertion: C1 PASS "only on exit 0" of the printed run; C3 "a token asserted without the printed run is FAIL"; C5 "a transcribed value in place of live output is FAIL"; C6 "a token without the artefact is FAIL"; C0 requires the exhibited table, authored and verified by two independent seats. C4/C5b are self-report BY DESIGN and say so at the right strength ("PASS … means only that the list contains no outside path; it makes no claim that the list is complete"), with the kernel sandbox profile and its probes named as the actual confinement mechanism.

Q2 — THE PROVENANCE FIELD IS CHECKABLE, AND THE MACHINE'S FLOOR IS STATED IN THE DOCUMENT. Test, executed: I wrote a ledger whose origin is wrong but plausible — the paper line is "For this calculation we take sigma8 = 0.811." and the record files origin `DERIVED` with `ORIG_EQUATION` quoted verbatim ("we take sigma8 = 0.811") and `derived_from: ["G"]` (G a STANDARD record). The delivered tools ran: `validate` → `C3_NO_SUBSTITUTION=PASS`, exit 0; `merge` → exit 0; `compute` → `C1 rests_on=DERIVED_ONLY root_origins=['STANDARD']`, exit 0. A value the author SET is laundered to the clean pole and no machine check sees it. The first attempt also confirmed the machine DOES enforce: quotation substring-of-cited-line (a quote not at the record's line FAILs), schema completeness (missing keys FAIL), code→origin mapping, closed-list STANDARD membership, ORIG_SILENT→origin_search, derived_from referential integrity/acyclicity/DERIVED-needs-parents (the five-failure negative control prints exactly these). What catches the plausible-wrong origin, as written: (1) the second seat's independent classification — the merge carries `origin_alt`/`origin_evidence_alt` on disagreement and above 10% of included claims the study stops at `CENSUS_ORIGIN_DISPUTED`; (2) the C6 auditor, who re-classifies every input's origin from the pinned sources and files `CENSUS_AUDIT_FAILED` on a MISMATCH; (3) for ORIG_SILENT, the printed `origin_search` adequacy rule. What never catches it: the machine — and the master says so itself in C3: "A reason code that matches its quotation but misapplies the precedence is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." The mechanism as written catches it through the readers; the identical-misread residue is disclosed, not implied away. Not a defect.

------------------------------------------------------------
3. CIRCULARITY
------------------------------------------------------------
The lane's expectation cannot reach the evidence through any of the four channels:
- INCLUSION: the §1 rule ("a numeral the paper asserts as a result of its own" plus enumerated excluded kinds) contains no pattern vocabulary (verified: the packet has zero hits for the forbidden list). Two seats apply it independently; disagreement after two reconciliations stops the census with both ledgers reported — a contrary claim cannot be dropped unilaterally.
- EXCLUSION: the sixth kind is the one new judgement at the denominator. It cannot quietly swallow a claim: rows are retained with numeral and line ("excluded from judgement, retained in the record, never discarded"), the tool FAILS a row lacking or mismatching them (ran it), the kind's count prints beside the denominator, both seats must agree, and C6 "first audits the full candidate and exclusion ledgers against every pinned source — completeness, not just outcomes". A contrary claim excluded as AUTHOR_SPECIFIED_INPUT would have to be excluded identically by two blind seats and then survive the auditor; and if any passage asserts the numeral as a result the kind does not apply at all (see (g) below).
- ORIGIN CLASSIFICATION: the tie-break is a stated evidence-specificity rule, applied by two readers, disputes carried not reconciled; the severity order that could encode a preference lives only in SEAT-REDACT spans and the lane-side tool the seats never receive (verified: the seat tool has no `rests_on`, no merge/compute, usage shows only census/validate).
- INTERPRETATION: the mapping lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which exists on disk (sha256 962383bd…; not opened — no seat may open it), is committed and receipted (receipt P) BEFORE limb A, and reads the two sealed tallies after receipt T. The verdict the seats produce is pattern-free; the pattern touches the record once, afterwards, in a seat-invisible document sealed before enumeration begins.

------------------------------------------------------------
4. THE BLIND
------------------------------------------------------------
Machine-asserted as claimed — verified by execution: `/usr/bin/python3 -E r3c2_build_seat_packet.py` ran from the lane directory, first line `C4_PACKET_REDACTED=PASS` exactly as C4 predicts, then the master and packet digests; the master digest it printed equals ACCESS_SHA; the rebuilt packet is byte-identical to the delivered one (sha256 575423fb…, matching the pin file `R3C2_SEAT_PACKET.sha256`, which also pins the brief b734243f… — verified — and the three scripts). The builder drops §0/§7/§8/§10 whole and redacts 51 inline spans; the packet's own header attests it was built from master ab6352d3…. The C4 mechanism as described does what it claims within its stated limits: redacted copy outside the lane, contents and digests listed in the dispatch record before launch, kernel sandbox with printed positive/negative probes, self-report controls capped at "what the printed list shows". Limits stated honestly: the list's incompleteness, no proof against prior exposure from training, manifests as snapshots not immutability, startup code bounded by the recorded confinement "not this clause". One overclaim about the profile found — F2 below (COSMETIC).

------------------------------------------------------------
5. THE SEAL (§7)
------------------------------------------------------------
Binds what it claims, before the step it must precede. Receipt P: protocol hash and commit id sent to Blanc and receipted BEFORE limb A ("limb A does not begin without receipt P") — the interpretation is fixed before any claim is enumerated, so it cannot be written to suit the tally. Receipt T: one named commit (merged candidate file, exclusion ledger, merged input ledger with `compute` output), hash and commit id receipted after the tally and "the interpretation protocol is not opened without both receipts"; "The relay is complete only when it is receipted." After opening, Blanc independently re-hashes and verifies all four values; a missing receipt or mismatch files `CENSUS_AUDIT_FAILED`, leaves interpretation NOT_RUN, voids the comparison. The custody gap the V10 seats found (protocol in the lane's own custody while the tally was computed) is closed by P's timing. The limit is stated, not overclaimed: "this proves the tally and the protocol both existed, in the stated form, before the comparison was filed. It does not prove Tori had not already guessed the answer."

------------------------------------------------------------
6. FAIRNESS
------------------------------------------------------------
Every negative outcome is worded as required: "Wording: 'unreproduced from the stated inputs,' not 'error.'" (§3 REPRO_FAILED), repeated in the packet and in the brief ("Negative outcomes are worded 'unreproduced from the stated inputs'"). No class name asserts more than its definition: REPRO_WITHIN_STATED_PRECISION asserts exactly the rounding/uncertainty test it defines; REPRO_FAILED is defined as arithmetic-not-matching from stated inputs and carries the wording rule; CENSUS_PARTIAL is labelled INCONCLUSIVE, not failure of the corpus. The `DERIVED_ONLY` label (which also covers STANDARD- and MEASURED-only roots) is a named open item pending for the principal (§10.15, §10.18 D8), lane-side only, seats never see it — recorded as pending, not a defect of this version. The exclusion kind name AUTHOR_SPECIFIED_INPUT matches its definition ("sets rather than derives and does not assert as a result"). Nothing found.

------------------------------------------------------------
7. STALL / EXECUTABILITY, with Q4
------------------------------------------------------------
Q4 — checked, each item named:
- ACCESS_SHA of the master (run as mandated): ab6352d3… — the packet header cites exactly this digest as its build source. MATCH.
- PACKET_SHA 575423fb… equals the pin in `R3C2_SEAT_PACKET.sha256` line 1; the pin file also carries the brief (b734243f…, verified), `r3c2_timeout.py` (fbb9bef7…, verified), `r3c2_ledger_tools.py` (230359eb…, verified), `r3c2_manifest.py` (19a8ce47…, verified), `r3c2_build_seat_packet.py` (4ed52d4b…, verified). All pinned digests match the files on disk.
- `R3C2_CORPUS_MANIFEST.md` exists, sha256 300d4da1…, matches §2's pin.
- `r3c2_lane_tools.py` 8e990c7a…, `r3c2_seat_sandbox.sb` 6978d590…, `R3C2_V23_DESIGN_OF_RECORD_55b466fa.md` 55b466fadf8ca75f4894f97721da86584057ddc4d15c15ca1739c67ff6333c6f (matches the version-table prefix), `R3C2_INTERPRETATION_PROTOCOL_20260904.md` exists (962383bd…; not opened).
- C1 census: positive control `cand_asi.json`+`excl_asi.json` exit 0 PASS; two negative controls exit 1 FAIL with named lines (tokens in (h) below). The `final` form is printed in C1; the seat brief step 5 carries it.
- C2/C3 validate: positive ledger exit 0 PASS; negative ledger exit 1 with exactly five failure lines (matching §10.13's "the negative control's exact failure set is now five"); the Q2 attack ledgers behaved as section 2 records.
- Lane tools: `merge` and `compute` ran (exit 0 each) on the Q2 attack ledger pair; output prints `root_origins` and `rests_on` as described.
- Builder: runs as printed, PASS first line, reproduces the packet byte-identically.
- §9 wrapper: fast child → prints WRAPPER_COMMAND/stdout/stderr/exit/elapsed, exit 0; `1.0`-second cap over `/bin/sleep 5` → `SYMBOLIC_TIMEOUT`, elapsed 1.004s, exit 124 — as §9 declares.
- C5 five commands: all exit 0 on this host (details in Q7(a)).
- The seat tool's usage text shows `-E` and only `census`/`validate`; zero `rests_on` occurrences.
- r3c2_manifest.py read in full: stdlib only, one process, walks and reads every regular file itself, no subprocess, `ERROR=<path>` + exit 1 on any unreadable file, exit 2 on usage — the "ONE process … nothing is skipped silently" claim is true.
Nothing the document describes failed to exist or failed to run as delivered. Three wording seams found (F1, F3, F4 below); none defeats executability.

DEFECTS (each: verbatim quote, defect, exact replacement):

F1 — §1's excluded-kinds prose was not updated for the sixth kind (consistency; small). Master §1: "Excluded, by definition and not by taste: numerals that are equation numbers, reference numbers, page or line numbers, dates, or values the paper attributes to another work without deriving." The same sentence stands in the packet's §1. But §3's kind list — in master and packet — has six kinds including `AUTHOR_SPECIFIED_INPUT`, the tool enforces six, and §1's own rule is what the seats apply ("Inclusion is assigned independently by the two independent seats from the §1 rule alone"). A seat reasoning from §1's list alone has no kind for an author-set numeral; the two live copies of the exclusion inventory now disagree — the exact family this lane's own §6 note names ("the second live copy of a repaired rule is how the last defect got in"). The §1 predicate still routes such numerals out of inclusion, so no class, passability, reachability, conclusion or tested value changes; I mark it COSMETIC by the gate's test, but it should be repaired before signature because it is the load-bearing kind of stale copy. EXACT REPLACEMENT: "Excluded, by definition and not by taste: numerals that are equation numbers, reference numbers, page or line numbers, dates, values the paper attributes to another work without deriving, or numerals the paper sets as inputs to its own calculation rather than asserts as results of its own (`AUTHOR_SPECIFIED_INPUT`, §3)."

F2 — the master's description of the sandbox profile overclaims it (COSMETIC). Master C4: "each seat is dispatched inside a kernel sandbox profile (`r3c2_seat_sandbox.sb`, sha256 `6978d590…`) that denies reads outside the working directory and the pinned environment and denies all writes outside the working directory". The pinned profile on disk permits reads well beyond that set — the system runtime locations plus `/opt/homebrew`, `~/.local`, `~/.codex`, `~/.nvm`, `~/.hermes`, `/private/etc`, `/var/folders` — and permits writes to `/private/var/folders`, `/private/tmp`, `/dev`, `~/.codex`, `~/.hermes`. What matters for the blind holds: the lane directory is NOT permitted (deny default), the pinned site-packages directory is read-only to the seat (not in the write list), `/usr/local` and `/private/tmp` reads are denied. But the sentence as written misdescribes the mechanism the design now leans on. EXACT REPLACEMENT: "each seat is dispatched inside a kernel sandbox profile (`r3c2_seat_sandbox.sb`, sha256 `6978d590…`) that permits reads of the working directory, the pinned site-packages directory (read-only), the system runtime locations and the seat harness's own runtime directories, denies reads of `/usr/local` and `/private/tmp` and — by `deny default` — of the lane, and permits writes only inside the working directory and the harness's own directories; the profile's digest and its live positive and negative probes are printed in the dispatch record before launch."

F3 — the brief's step 1 mandates a command C4's (iii) does not enumerate (wording seam; small). Brief step 1: "Print `shasum -a 256` of the packet, this brief, all three pinned scripts and every source text you read". C4 (iii) licenses "the commands this document prints verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, and the §9 wrapper invocations". The `shasum` binary is outside (i)–(ii), and the brief's digest step is not in (iii)'s list; the brief's own scope sentence likewise names only "the commands the packet prints verbatim". There is no contradiction of the 10:17 kind (nothing forbids the step), and a reasonable seat folds the brief into the instruction set — the packet's first line does ("Work only from this file, the seat brief, and the pinned sources") — but by the letter of (iii) two seats could split on whether the step-1 binary is IN_SCOPE under C5b. EXACT REPLACEMENT (C4 (iii), C5b, and the brief's scope sentence alike): "execute the commands this document and the seat brief print verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, the §9 wrapper invocations, and the brief's digest step —".

F4 — C5's "the one site-packages directory it loads from" is loose (COSMETIC). C5: "The five commands establish the interpreter every ledger command runs under and the one site-packages directory it loads from". On this host the interpreter's sys.path under `-E` also carries the CLT framework site-packages (`/Library/Developer/CommandLineTools/…/lib/python3.9/site-packages`), which is non-empty and executes startup code (`distutils-precedence.pth` → `_distutils_hack`); that directory is not the pinned one. The sentence's material claim is TRUE — the mandated imports resolve solely from the pinned directory (verified live: `sympy.__file__` and `mpmath.__file__` both under `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`; the digest covers everything they can load from it) — and C4's residual states the true limit ("not the absence of startup code … What bounds such code is the confinement recorded in the dispatch record … not this clause"). EXACT REPLACEMENT: "the one site-packages directory the mandated imports load from".

------------------------------------------------------------
Q7 (V25)
------------------------------------------------------------
(a) C5_EXECUTABLE_UNDER_SCOPE=YES. Governing sentence: "(iii) execute the commands this document prints verbatim — the C1 `census` runs, the C2/C3 `validate` runs, the five C5 harness commands, and the §9 wrapper invocations — together with whatever those commands themselves invoke or load, because executing a printed command is the instruction, not a scope choice." I ran all five from `/tmp/r3c2_c5_kimi_20260906` containing the pinned `r3c2_manifest.py` (copied from this lane; digest verified 19a8ce47…):
  (1) `/usr/bin/python3 -E --version` → `Python 3.9.6`, exit 0.
  (2) `import sympy; print(sympy.__version__)` under `/usr/bin/python3 -E` → `1.14.0`, exit 0 (via payload file; this session's filter blocks `-c` — disclosed on line 7).
  (3) `/usr/bin/shasum -a 256 /usr/bin/python3` → `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`, exit 0.
  (4) `site.getusersitepackages()` under `-E` → `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`, exit 0.
  (5) `/usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages` → `FILES=20637`, `MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed`, exit 0.
(b) D3 check — NO_MASKED_STAGE=YES. Every mandated command in the document is one invocation whose exit status is the control's: C5(1)–(5) are single processes; `r3c2_manifest.py` is verified one process (stdlib only, walks/reads every file itself, ERROR=path + exit 1 on any unreadable file, no subprocess); the census/validate runs are single processes; the §9 wrapper is one invocation that prints its child's exit status (demonstrated: exit 0 and exit 124 cases). No mandated command is a shell pipeline — I checked every printed command in C1, C2, C3, C5, §9 and the builder command; none pipes.
(c) Told-vs-chose: one told-but-unenumerated path — F3 (the brief's digest step); no contradiction, repair offered. Chose-but-should-forbid: within (ii) the seat may read (and import) anything in the 20,637-file user site — read-only to the seat under the profile, digest-pinned, blind content; the document states this surface itself (§10.18 V24e note) and `MACHINERY_UNAVAILABLE`/`CENSUS_CONTROL_SPLIT` bound the machinery-divergence risk. No lane-content path is open to a seat's choice: the lane is outside (i)–(ii) and profile-denied. The principle leaves no told path forbidden except F3, and no chosen path open that carries study content.
(d) Changed versus the signed V23 (full diff inspected, 315 lines): the header; the seat-tool re-pin `f9b7d3c8…`→`230359ebd1…` (§3 and §10.11); the sixth exclusion kind with retention rules, the census count line and the adoption note (§3); `-E` on every printed interpreter invocation (C1 ×2, C2, C3 validate, lane `merge`/`compute`, the builder command, the §9 wrapper); the C4 scope principle (i)–(iii) with the told-vs-chose clause and the stated residual; the C4 confinement paragraph (kernel sandbox + probes) replacing "procedural, not enforced by the filesystem"; the blind-limit sentence reworded; C5 three→five commands with `-E`, path-qualified `/usr/bin/shasum`, the one-process manifest, the no-pipeline sentence and the expanded PASS predicate; C5b's `IN_SCOPE`/`OUT_OF_SCOPE` definitions tied to the principle; the dispatch-copy list gains `r3c2_manifest.py`; the version table marks V23 signed and adds the V24 row; §10.18 and §10.19 appended. Nothing else.
Round-2/3 addendum consistency: C4, C5b and the seat brief now carry the same principle (all three texts compared); the digest pin closes the user-writable-path objection to the extent the residual states — matching manifests prove matching snapshots at pinning and at C5, not immutability between, and the profile (not the clause) makes the directory read-only to the seat, which the write-list confirms.

------------------------------------------------------------
V25 DELTA versus V24i — (a)–(f), (g), (h)
------------------------------------------------------------
(a) Does the delta do what it claims: YES — the kind is in §3's list (alphabetical, six kinds) in master and packet; every exclusion row must carry the candidate's `source_file`, `source_line` and `numeral`; `census` FAILs a row lacking them or differing from its candidate row; the kind's count prints beside the denominator; the tool is re-pinned and the pin verifies; positive and negative controls exist in `_tmp_r3c2_ledger_ctl/` and behave (see (h)).
(b) New paths opened: none that leak — the kind list is neutral and seat-visible; an exclusion cannot hide a claim because the row is retained, both seats enumerate independently, disputes stop the census, the count prints beside the denominator, and C6 audits the full exclusion ledger for completeness.
(c) Exhaustiveness/exclusivity impact: the six kinds still partition by the numeral's nature; the inclusion predicate is unchanged; the one stale copy is F1 (§1's prose list).
(d) Executability of the new tool behaviour: demonstrated — three census runs in (h); the tool digest matches the pin.
(e) Consistency across live copies: master §3 = packet §3 = tool KINDS set (all six, alphabetical) ✓; §1 prose in master and packet names five ✗ (F1); the brief names no kinds and defers to the packet ✓.
(f) Anything else changed: no — within the delta, §3's kind sentence with its retention/count clauses, the SEAT-REDACT adoption note, the tool re-pin, and §10.19 are the whole of it (verified against the full V23→V25 diff).
(g) Hardest case for the new kind. Single passage both setting and asserting: "Setting Ω_m = 0.3153, our model yields Ω_m = 0.3153." The kind requires "does not assert as a result of its own"; this passage DOES assert, so §1/C1 file INCLUDE — the chosen-ness is recorded, not excluded: origin CHOSEN on the input record, `rests_on` carries it, and C1's one-disposition rule forbids double-filing. Set in one place, derived in another: dispositions attach per passage — the setting passage files AUTHOR_SPECIFIED_INPUT (numeral and line retained), the asserting passage is a claim and is attempted; the two rows are distinct candidates. Can two seats split? Only on the pre-existing §1 judgement — whether a passage asserts the numeral as a result — which the design already treats as the two-reader question: two reconciliation attempts, then `CENSUS_DENOMINATOR_DISPUTED` with both ledgers reported; the row is retained and audited under C6 either way. The kind adds no new silent-exclusion route.
(h) Control runs (executed by this seat, from the lane directory):
  `/usr/bin/python3 -E r3c2_ledger_tools.py census _tmp_r3c2_ledger_ctl/cand_asi.json _tmp_r3c2_ledger_ctl/excl_asi.json` →
    declared: candidates=4 included=1 excluded=3 attempts=1 exclusions=3
    outcomes: PENDING=1
    author_specified_input=1
    recomputed: candidates=4 included=1 excluded=3 attempts=1 reconciled=YES
    C1_DENOMINATOR_PRINTED=PASS    exit 0
  `/usr/bin/python3 -E r3c2_ledger_tools.py census _tmp_r3c2_ledger_ctl/cand_asi.json _tmp_r3c2_ledger_ctl/excl_asi_wrongnumeral.json` →
    FAIL: exclusion ASI1: numeral differs from the candidate row
    … reconciled=NO
    C1_DENOMINATOR_PRINTED=FAIL    exit 1
  (additional negative, named in §10.19) `… excl_asi_nonumeral.json` →
    FAIL: exclusion ASI1: missing numeral (the excluded numeral and its line are retained in the exclusion ledger)
    C1_DENOMINATOR_PRINTED=FAIL    exit 1

VERDICT: PREREG_SOUND_WITH_REPAIRS. The design is executable as delivered (every pinned artefact exists at its pinned digest; every printed command ran), the reproduction question is decidable with every declared outcome reachable, the blind is machine-asserted and verified from both sides, the seal precedes what it must bind, and the packet leaks nothing beyond the ruled content-level floor. Repairs: F1 (§1 kind-list consistency) and F3 (brief/(iii) wording seam) — small wording repairs; F2 and F4 are COSMETIC. No defect touches a class filed, a control's passability in practice, reachability, a conclusion, or a tested value.

R3C2_V25_GATE_COMPLETE
