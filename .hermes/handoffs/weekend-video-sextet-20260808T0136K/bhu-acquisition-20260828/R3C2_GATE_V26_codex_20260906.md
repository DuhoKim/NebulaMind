ACCESS_SHA=22f01220a33f8695d18599176234c466fab36ff134b69af6c7d2cecdd978cb84
PACKET_SHA=3b5cb636fe7b5b082dd903778810d033bb09ff39d3ab4edb75e046c39c1242bb
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=Procedural: CENSUS_AUDIT_FAILED says “No tally is filed; report which.” No preferred empirical result is disclosed.
ORIGIN_PURPOSE=Independent comparison and audit: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” No interpretive mapping is disclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO

Packet-first answers, before the design assessment:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

Procedurally, yes: under CENSUS_AUDIT_FAILED, “No tally is filed; report which.” Other outcomes permit a tally. No sentence identifies a preferred empirical result or its stakes for anyone. My initial packet-stage answer used NO for substantive lane stakes while acknowledging different stopping conditions; that was too narrow for the literal phrase “to anyone.” This correction uses only sentences already read in the packet. “No tier, warrant token, standing or stamp moves” limits institutional consequences but does not erase procedural ones.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED origin OF AN INPUT IS FOR?

Partly: independent classifications are compared and audited, and disagreement can stop the census. The packet says: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” It also says the auditor “re-classifies each of its inputs' `origin` from the pinned sources”. I cannot state any downstream substantive comparison or interpretation from the packet alone.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

No directional hypothesis, comparison model, preferred outcome, or predicted pattern is reconstructible. “The reproduction verdict and the provenance fields are recorded separately” reveals that provenance receives independent attention. “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” reveals a cosmological context. The chosen/imported-input rules reveal anticipated classification difficulties. None states which provenance or reproduction result is hoped for. LEAK=NONE means no reconstructed hypothesis or preference, not absence of subject-matter or procedural information.

This is a design gate, not a census or adoption. I read the packet before the master; section 10 supplied no verdict or binding finding. Its twelve-item list is checked below solely because Q7 requests it. I opened no gate report or interpretation protocol. Q7's explicit testing instructions were treated as authorization for the named tools, brief, manifests and staged kit; the optional integrated candidate was opened only after the master. All derivations below are synthetic fixtures, not corpus results.

1. OUTCOME CLASSES

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

YES for the settled classification rule and its bounded operational question. NO would be warranted for the delivered workflow as a whole: the execution and control failures below prevent treating formal reachability as executable readiness. These are distinct conclusions; I am not reopening option (c).

Concrete claim constructions, with all unmentioned controls passing:

| Printed claim and recipe | Unique per-claim outcome | rests_on |
|---|---|---|
| “We choose a = 3; y = 2a = 6.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same inputs and recipe, but printed y = 7 | REPRO_FAILED | USES_CHOSEN |
| “We fit f = 4”; y = 2f = 8 | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 3 from X”; y = 2a = 6, all printed locally | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Claiming paper names pinned external X; X's first symbol/value line prints a = 3; y = 2a = 6 | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Same import, but X is unpinned and the value is not locally printed | REPRO_BLOCKED | USES_IMPORTED |
| y = 2a = 6, a neither printed nor traced to a source; its provenance is silent | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| All required values printed; specified computation exceeds the wrapper cap | REPRO_NOT_EVALUABLE | Determined independently by its input roots |
| “Our result is 6”, with no producing procedure and no input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |
| Printed measured m = 3; y = 2m = 6 | REPRO_WITHIN_STATED_PRECISION | DERIVED_ONLY, by the stated membership rule |

The hardest combined case is y = a + f + b = 12, with a = 3 explicitly chosen, f = 4 explicitly fitted, and b = 5 imported from a verified pinned text. All three are consumed mechanically. The result files REPRO_WITHIN_STATED_PRECISION and USES_IMPORTED, because IMPORTED outranks FITTED and CHOSEN in the declared summary. Changing the printed result to 13 changes only the reproduction outcome to REPRO_FAILED. If the claiming paper prints b itself, its import remains usable without opening X. No choice by the reproducer is needed.

A claim needing both an unprinted cited a and an entirely absent b satisfies the raw BLOCKED and INPUT_ABSENT predicates; the precedence files only REPRO_BLOCKED. An unspecified procedure needing unavailable machinery files NO_DERIVATION_STATED first. I found no double filing after applying precedence. The cap makes this an operational decision procedure; it is not a promise to decide arbitrary mathematics.

All eight study classes have witnesses: one arithmetic claim and audit PASS gives COMPLETE; one non-arithmetic claim successfully audited gives PARTIAL/INCONCLUSIVE; an audit mismatch gives AUDIT_FAILED; repeated common control failure gives NO_CLASS; persistent inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; persistent arithmetic-verdict disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 included claims gives ORIGIN_DISPUTED; a surviving fail/pass control split gives CONTROL_SPLIT. A mixed non-arithmetic tally plus audit failure satisfies two raw predicates but files only AUDIT_FAILED. A truly empty enumeration on both sides can audit PASS and file PARTIAL. A zero included denominator with excluded passages instead triggers the stated audit failure. Thus INCONCLUSIVE is genuinely reachable, but the mere presence of non-arithmetic outcomes does not bypass earlier controls.

COSMETIC C1 — local-import wording conflicts with the later “machine floor” summary. Verbatim sentence:

“For an import it then checks: the citing file is the claiming file; the two files differ; the source is an exact manifest row with verified bytes; the quotation is non-empty and present at the cited claiming line; the value is a numeric token at the cited source line; some line of the source carries both symbol and numeral, and the cited line is the first such line.”

Exact replacement:

“For an external import, whose value line is outside the claiming file, validate checks that the evidence file is the claiming file, the external source is an exact manifest row with verified bytes, the non-empty quotation occurs at the cited claiming line, and the cited source line is the first line carrying both the symbol and the numeric value token. A locally printed import instead follows the local-import branch above.”

The specific preceding branch and delivered tool already resolve the case; this replacement changes no tested value or class.

2. CONTROLS

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

Partly, but not to the machine-checking strength promised. Reason-code/origin pairing and source quotations support human review. They do not establish semantic correctness. Both roots and rests_on are computed, but the dependency graph supplied to that computation is not independently compared by the delivered audit.

My wrong-but-plausible record was:

```json
{"claim_id":"p.txt#c","input_id":"p.txt#a","symbol":"a","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"p.txt","source_line":1,"verbatim":"We adopt a = 3 from q.txt."},"derived_from":[],"value":"3","source_file":"p.txt","source_line":1}
```

The source really contains that sentence. validate returned C3_NO_SUBSTITUTION=PASS, exit 0. The citation-precedence rule requires IMPORTED. An independently correct second seat detects an origin dispute; an auditor classifying this selected record as IMPORTED detects a mismatch against a merged CHOSEN record. If both census seats and the auditor share the misreading, no mechanism catches it. The master honestly acknowledges that semantic floor. The following failures go beyond it.

F1 — NON-COSMETIC: the promised every-status quotation check is incomplete.

Verbatim governing text:

“Every record carries `origin_evidence` with a reason code —”

and its operative requirement:

“for EVERY record whatever its status: `validate` requires a non-empty quotation and matches it at `origin_evidence.source_file`/`source_line`; for `STANDARD` it additionally requires the value as a numeric token at the record's own cited line and closed-list membership; for `BLOCKED` it checks the claiming paper's naming quotation while requiring an empty value.”

Observed counterexamples, all exit 0/C3_NO_SUBSTITUTION=PASS: an ABSENT/FITTED record with a fabricated quotation at line 999; an ordinary PRINTED/CHOSEN record with an empty quotation; and an ordinary PRINTED record with a real quotation but origin_evidence pointing to nonexistent.txt:999. The ordinary PRINTED branch tests against the value line, not the declared evidence coordinates. ABSENT bypasses quotation checking entirely. The requested fabricated STANDARD quotation correctly fails, so that single test does not prove “every status”.

Exact replacement for the quoted operative requirement:

“Before any status-specific branch, validate requires every non-ORIG_SILENT record to carry a non-empty quotation occurring at the positive, one-based origin_evidence.source_line in origin_evidence.source_file. This applies equally to PRINTED, STANDARD, ABSENT and BLOCKED. Value-line checks are separate and never substitute for this evidence check. STANDARD additionally requires a cited numeric value token and closed-list membership; BLOCKED additionally requires empty value and naming evidence in the claiming file.”

Implement that shared check before branches that continue, then re-pin. The existing sentence already intends this strength; wording alone cannot repair the tool.

F2 — NON-COSMETIC: C6 does not check the dependency graph that determines the interpretive provenance summary.

Verbatim sentence:

“Provenance is transitive.”

Verbatim audit instruction:

“the auditor re-derives each assigned claim and re-classifies each of its inputs' `origin` from the pinned sources, and the custodian seals those re-derivations by digest (`audit seal-rederivation`) BEFORE any sealed ledger is released.”

The rederivation interface records input IDs mapped to origins. compare checks those origins, outcomes and arithmetic value strings; it does not compare derived_from, status, value or evidence coordinates. In my two complete synthetic audit sequences, changing only a valid dependency edge from a CHOSEN parent to a STANDARD parent changed p.txt#c from USES_CHOSEN to DERIVED_ONLY. Both variants returned C6_AUDIT_SAMPLE=PASS. This is not a hash collision or post-seal mutation: either wrong graph can be committed before T and pass the same audit predicates.

Exact replacement for the audit instruction:

“The auditor independently reconstructs each assigned claim's complete input records and dependency closure from the pinned sources, including cross-claim dependencies, and records status, value, source coordinates, origin evidence and derived_from for each input. The custodian seals this reconstruction before releasing any census ledger. audit compare compares those reconstructed fields and dependency edges against the sealed ledger, carrying declared origin alternatives explicitly; a missing input, differing dependency edge or unsupported record is MISMATCH and files CENSUS_AUDIT_FAILED. Recompute root_origins and rests_on from both reconstructions and print the comparison.”

The audit schema and compare implementation must change with this replacement. Independent origin labels alone cannot audit transitive provenance.

Control contracts otherwise exist: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE have PASS/FAIL/NOT_RUN rules. C2 shares C3's printed run. C0 requires an exhibition and independent verification, not a bare token. C4/C5b require printed path lists but expressly do not prove list completeness. C5C requires session-identified provider logs. Unreached controls are NOT_RUN. These predicates are assessable; F1/F2 show that some passing artefacts establish less than the operative prose promises.

3. CIRCULARITY

The inclusion rule does not mention the lane's hypothesis or comparison. Chosen inputs are excluded as standalone results while remaining available as inputs to included claims. A contrary numerical result asserted as the paper's own result must be listed and attempted; calling it an input contrary to the source is a classification error, not an authorized exclusion.

Two independent enumerations, retained exclusions, zero-tolerance reconciliation and the auditor's full independent enumeration make a unilateral quiet exclusion detectable. They cannot exclude shared reading error. C6 also expressly tolerates a reported inclusion-dispute rate up to 10%; that is visible disagreement, not guaranteed perfect completeness.

The packet did not give me a directional expectation. The seal prevents retrospectively replacing the interpretation protocol unnoticed. However, F2 permits an unsupported graph to alter the provenance summary while the audit passes, and F1 weakens evidence checks on origin classification. Those are concrete routes by which judgment can affect the evidence without the advertised checks detecting it. I find no basis for a stronger assertion that the lane's expectation cannot reach the evidence.

4. THE BLIND

The packet is reproducible: the delivered builder returned C4_PACKET_REDACTED=PASS and reproduced PACKET_SHA exactly, both with explicit paths into my fixture directory and with the literal no-argument command from the lane working directory. It kept sections 1, 2, 3, 4, 5, 6, 9 and 11 and stripped 51 inline spans. The forbidden-string assertions cover the output packet and brief; they are not a proof against all semantic disclosures or all content a runtime may load.

The delivered sandbox hash matches C4:
6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6.

Its source allows network access and broad runtime/configuration exceptions. The revised confinement paragraph states those limits honestly and requires dispatch-specific probes for the master and withheld interpretation material. C4/C5b remain self-report checks, as stated. I did not create a real seat dispatch or certify its future probes.

COSMETIC C2 — the builder-arguments claim is inaccurate but the literal command works through defaults. Verbatim parenthetical:

“(it accepts explicit `--master`, `--out` and `--brief` arguments; the printed build command names them)”

Exact replacement of the printed build command:

`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out r3c2_seat_packet/R3C2_SEAT_PACKET.md --brief r3c2_seat_packet/SEAT_BRIEF.md`

The explicit and default invocations produce identical packet bytes here. This changes no class, passability, reachability, conclusion or tested value.

5. THE SEAL

Section 7's two-receipt design binds the stated artefacts at the right times: receipt P fixes the protocol before limb A; T fixes the tally commit and named files before opening the protocol and before the external sampling seed. The later independent rehash and receipt comparison detect substitution. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

The claim is appropriately limited to custody and chronology, not ignorance of likely results. The protocol's path exists, checked by metadata only; I did not open it. No real P or T was requested, produced or certified. The synthetic audit seals test the audit API, not external custodial receipt timing.

The new batch seals have a separate execution defect, F4 below. A sound P/T rule does not fix a chain that seals the wrong limb files.

6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The brief applies that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NOT_EVALUABLE and NO_DERIVATION_STATED report limitations of the stated-input reproduction attempt; they do not prove the paper wrong. The stated uncertainty and rounding rules constrain the positive outcome without asserting exact identity.

Q7(e): DERIVED_ONLY denotes the explicitly defined set whose roots are DERIVED, STANDARD or MEASURED. The name overstates derivation if read without its definition, but consistently renaming that token and its consumers leaves membership, controls and reachability unchanged. I found no class or threshold using a different meaning of the name.

COSMETIC C3 — verbatim definition:

“`DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`”

Exact optional replacement:

“`DERIVED_STANDARD_OR_MEASURED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`”

This is a recommendation only; the principal's reserved naming choice is not adopted. F2 concerns changing actual membership through a wrong graph, and is independent of this cosmetic name.

7. STALL / EXECUTABILITY

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

YES. The scripts launch, but several literal instructions either omit required output, seal the wrong files, or cannot pass for an allowed cross-batch dependency.

F3 — NON-COSMETIC: C3 retains an obsolete lane-tool pin and a compute invocation that omits candidate coverage.

Verbatim sentence:

“Lane side, after both seats have exited: the lane owner runs the lane-side script `r3c2_lane_tools.py`, committed beside this document, sha256 `8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848`: `/usr/bin/python3 -E r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>` and then `/usr/bin/python3 -E r3c2_lane_tools.py compute <merged.json> <out.json>`, printing for each the working directory, the resolved command, complete stdout and stderr, and the exit status.”

The delivered tool and §9 instead have hash da39212e26f9cb5733c9e580d37d1499167c078d5911013dfb7c9a1ddb44e862. Both compute arities are accepted. With an empty ledger and one included candidate, C3's literal arity exits 0 and writes claims={}; the three-argument form emits that claim's NOT_COMPUTED row. Thus a successful printed run can omit the declared denominator.

Exact replacement:

“Lane side, after both seats have exited, run `/usr/bin/python3 -E r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>`, then `/usr/bin/python3 -E r3c2_lane_tools.py compute <merged.json> <out.json> <candidates.json>`, using r3c2_lane_tools.py sha256 da39212e26f9cb5733c9e580d37d1499167c078d5911013dfb7c9a1ddb44e862. Print the working directory, resolved commands, complete stdout and stderr, and exit statuses. The candidate argument is mandatory; compute without it exits 2 and writes no output.”

Remove the permissive old CLI branch and update its usage text when implementing the repair.

F4 — NON-COSMETIC: the limb-B brief's filenames do not match seal/join.

Verbatim master sentence:

“Limb B resumes the same ownership batches in separate sessions, writing final outcomes into separate limb-B copies of the four files, sealed in a separate ordered LIMB-B chain bound to the agreed limb-A seals; a sealed limb-A file is never overwritten; `join` of the limb-B chain verifies that binding.”

Verbatim brief instruction:

“the same directory holds the agreed `candidates_b<k>.json`; write final outcomes into a COPY `candidates_b<k>_limbB.json` (never modify the sealed limb-A file)”

The tool's ART(k) uses candidates_bk.json, exclusions_bk.json, ledger_bk.json and SEAT_REPORT_bk.md for both chains. My exact brief layout produced successful chain-B seals and JOIN=PASS while joining unchanged PENDING limb-A candidates. Final census then failed on both claims. The suffixed final candidates and report were not bound by chain B.

Exact replacement for the master sentence and the brief's limb-B paragraph:

“Limb B uses a separate directory from limb A. The custodian copies the agreed limb-A batch artefacts into that directory under the canonical names candidates_b<k>.json, exclusions_b<k>.json, ledger_b<k>.json and SEAT_REPORT_b<k>.md. The seat updates only these limb-B copies, records final outcomes, and runs `/usr/bin/python3 -E r3c2_ledger_tools.py census candidates_b<k>.json exclusions_b<k>.json final`. The custodian seals and joins that directory with chain B bound to the agreed limb-A seals file. The original limb-A directory and seals remain unchanged; no _limbB filename suffix is used.”

I tested that exact separate-directory layout: chain-B join and final census PASS; rejoining the untouched chain A also PASS.

F5 — NON-COSMETIC: batch-local validation cannot resolve permitted cross-batch dependency IDs.

Verbatim sentence:

“Every `DERIVED` record lists its `derived_from` ids; `validate` fails a `derived_from` id that names no record, a cycle, and a `DERIVED` record with no `derived_from`.”

C2 directs validation against the per-session batch file, and brief step 4 mandates it before sealing. A p.txt claim in batch 1 depending on q.txt#b in batch 2 fails that run with “derived_from q.txt#b absent”, even though the dependency resolves in the joined seat ledger. Having every source text available does not provide every other batch's ledger record. Copying that record into batch 1 would violate join's ownership rule.

Exact replacement for brief step 4 and C2's per-session validation instruction:

“Each batch session writes its owned input records and globally qualified derived_from IDs, then seals them without running validate; C2_INPUT_LEDGER and C3_NO_SUBSTITUTION are NOT_RUN at this stage. After all limb-A batches are sealed, join resolves the complete dependency graph. Each seat then runs `/usr/bin/python3 -E r3c2_ledger_tools.py validate <joined_ledger.json> . <joined_candidates.json>` in its integration working directory holding all pinned texts, and prints the complete run. Both seats' joined validation must PASS before limb-A agreement or arithmetic.”

This preserves ownership and performs the full graph check when its inputs actually exist.

F6 — NON-COSMETIC: C5 pins different manifest-tool bytes from those delivered.

Verbatim pin clause:

“`r3c2_manifest.py` is committed beside this document, sha256 `19a8ce4750bb47655868ef15b55f2b168833147b460c03ad56f84dd3c9bc56f2`, delivered in the seat's working directory and pinned in `R3C2_SEAT_PACKET.sha256`”

The verified pin sheet and delivered file instead identify 974458f3ec3d530ebaeb1159ce0489b53a9d8e4cf82591b7639538322a5a5a19. This discrepancy is also in the built packet. The correct script executes, but it cannot satisfy both byte identities.

Exact replacement of that clause:

“`r3c2_manifest.py` is committed beside this document, sha256 `974458f3ec3d530ebaeb1159ce0489b53a9d8e4cf82591b7639538322a5a5a19`, delivered in the seat's working directory and pinned in `R3C2_SEAT_PACKET.sha256`”

Rebuild/re-pin the packet after correcting the master. If the script changes for F7, replace this digest with the newly computed digest everywhere instead.

F7 — NON-COSMETIC boundary omission: manifest refuses nested symlinks but follows a symlink supplied as the root.

Verbatim requirement:

“the manifest command fails with `ERROR=<path>` and exit 1 on any symlink or non-regular entry, including a symlinked directory, rather than skipping it”

My nested-directory symlink probe returned ERROR and exit 1. Supplying a directory symlink as the manifest root instead returned FILES=1, a digest, and exit 0. The advertised refusal does not cover its own root argument.

Exact replacement:

“The manifest command first rejects a root path that is a symlink or is not a directory, printing ERROR=<path> and exiting 1. It then rejects every symlink or non-regular entry encountered below that root, including symlinked directories; it never follows or silently skips them.”

Add the root check before os.walk and re-pin. This changes a tested PASS to FAIL.

Q7(a): verification and independently executed fixtures

Both pin sheets verify from the lane working directory: R3C2_SEAT_PACKET.sha256 has 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 has 10/10 OK. The staged sheet is inside r3c2_staged_d1d7; its entries are lane-relative. The root-level shorthand R3C2_STAGED_D1D7.sha256 does not exist. Both sheets were checked again after testing and still verify.

I ran `/usr/bin/python3 -E r3c2_staged_tests.py` with working directory r3c2_staged_d1d7. Exit 0; last three lines:

```text
deletion_probes=45
controls=131 passed=131 failed=0
STAGED_TESTS=PASS
```

The independently authored fixture outputs, using the delivered pinned names, were:

```text
local_import: C3_NO_SUBSTITUTION=PASS; exit 0
external_import: C3_NO_SUBSTITUTION=PASS; exit 0
fabricated_STANDARD: C3_NO_SUBSTITUTION=FAIL; exit 1
empty_ledger: C3_NO_SUBSTITUTION=PASS; exit 0
compute_with_candidates: p.txt#c rests_on=NOT_COMPUTED; exit 0
cross_claim_merge: PARENTS_DISPUTED=0; origin disagreements=1; exit 0
cross_claim_compute: p.txt#c DISPUTED; rests_on=['USES_CHOSEN', 'USES_FITTED']; exit 0
cross_claim_compute: q.txt#c DISPUTED; rests_on=['USES_CHOSEN', 'USES_FITTED']; exit 0
audit seal-enumeration: exit 0
audit select: N=1 R=0 k=0 audited=1; exit 0
audit handout: 1 claim; claim_id/source_file/source_line only; exit 0
audit seal-rederivation: exit 0
audit compare: C6_AUDIT_SAMPLE=PASS; exit 0
partition: 2 texts into 2 batches; exit 0
seal chain A: batches 1 and 2 sealed; both exit 0
join chain A: JOIN=PASS; exit 0
coverage: C1B_BATCH_COVERAGE=PASS; exit 0
seal chain B bound to A, brief suffix layout: both exit 0
join chain B, brief suffix layout: JOIN=PASS; exit 0
final census of that join: C1_DENOMINATOR_PRINTED=FAIL; both outcomes still PENDING; exit 1
seal/join chain B, separate-directory repair: JOIN=PASS; exit 0
final census of repaired layout: C1_DENOMINATOR_PRINTED=PASS; exit 0
recheck original chain A: JOIN=PASS; exit 0
```

The full first synthetic audit seals were:

```text
AUDITOR_CANDIDATES_SHA256=aa00f5a97b69087d6ca2022e429eee8d7b20752ff4505de223e6cca5eb44bba6
AUDITOR_EXCLUSIONS_SHA256=19f4e2630f65b83a68e8c575126bd448fe31a931e4e8fb9b1a89308fdfc14b9e
AUDITOR_CENSUS_STDOUT_SHA256=c5521b74ab6adcd6f63079af190292697baf27bd60d5c791f0019b117e566a18
AUDITOR_REDERIVATIONS_SHA256=4b9bffb9fb373df596bea09be43c500b5848839752c21ad58f49729f1d8c7fc1
```

Fixture files and the full resolved commands, working directories, stdout, stderr and exit statuses are preserved at:
`/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v26_referee_5jt0z74y/test_log.json`.
The independent drivers are `/tmp/r3c2_v26_referee_tests.py` and `/tmp/r3c2_v26_audit_graph_test.py`. These are temporary referee evidence, not study deliverables or adopted tools.

Q7(b): C5_EXECUTABLE_UNDER_SCOPE=YES; NO_MASKED_STAGE=YES.

All five literal C5 commands execute under the stated permission principle. Live outputs: Python 3.9.6; SymPy 1.14.0; interpreter SHA b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`; FILES=20637; MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed. Each command exited 0. These establish execution, not equality to a future dispatch record or resolution of F6's contradictory script pin.

No operative mandated command is a shell pipeline. The wrapper returned 0 for a successful child, propagated exit 7 for a failing child, and returned SYMBOLIC_TIMEOUT/124 at 0.104 seconds for a deliberately shortened 0.1-second probe. The two ordinary wrapper tests used the printed 120.0 limit. I did not claim that a 0.1-second probe was a 120-second census attempt. NO_MASKED_STAGE concerns command exit propagation, not the separate false-PASS defects above.

Q7(c): twelve routine items, checked against operative text and delivered implementation

| Item listed in §10.21 | Closure finding |
|---|---|
| 1. validate third argument | CLOSED: C2/C3 and brief print it; own imports validate with candidates. |
| 2. Batch brief and A/B chains | NOT CLOSED: suffixed limb-B files are ignored; batch-local dependency validation also stalls. F4/F5. |
| 3. Locally printed citation | CLOSED functionally: own local-import fixture PASS. The general summary needs cosmetic C1. |
| 4. Empty ledger / NOT_COMPUTED | NOT CLOSED end to end: repaired form works; C3 retains a successful form that omits rows and an obsolete pin. F3. |
| 5. Evidence for every status | NOT CLOSED: STANDARD repair works, but ABSENT and ordinary PRINTED still bypass promised checks. F1. |
| 6. Cross-claim dispute propagation | CLOSED for the declared computed flag and pair: independently tested through merge and compute. F2 is a separate audit-of-edges defect. |
| 7. JOIN/C1B/C5C contracts | CLOSED as result contracts: tokens, failure filing and unreached handling are explicit; JOIN and coverage execute. C5C is a printed-log procedure, not a missing script. |
| 8. Confinement paragraph | CLOSED as a truthful statement of the delivered profile's limited guarantees; live dispatch probes remain future obligations. |
| 9. Builder arguments and pin sheet | Functional closure: both argument forms work and the eight pins verify. The statement that the printed command names the arguments is still false, cosmetic C2. |
| 10. Manifest symlink refusal | NOT CLOSED fully: nested symlink refusal works; the root bypass and stale operative pin remain. F6/F7. |
| 11. §3 six-kind parenthetical | CLOSED: text lists six kinds and census implements the six-token set. |
| 12. Diff sentence | The operative diff matches the listed areas of change. Any assertion that every copy is repaired is false for F1/F3/F4/F6; this is not independent evidence of closure. |

The optional integrated candidate's verified hash was f4edb5145b5c150c43a6550b30f0beca1d08bf4675305383e7860c3657f951fd. I computed a diff restricted to operative sections after reading the master. The delivered ledger tool differs from its staged counterpart only in its introductory docstring, so the staged tests do exercise the same relevant implementation, but their PASS does not cover my counterexamples.

Q7(d): NO, the rewritten brief does not support every allowed batch session as delivered. F4/F5 give direct executions. Chain B stores and verifies the digest of its supplied chain-A seals file; that binding works. It does not by itself select the brief's final files. The separate-directory replacement preserves both chains and passes the final census without overwriting a limb-A artefact.

Other delivery checks and boundaries: the corpus manifest exists and hashes to 300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24. Its 89 enumerable files all exist at its declared source location, `../bhu-reading-20260823/sources/`, checked by metadata only; I did not read their contents or claim corpus-byte verification. They are not flat files in the lane root, which is not itself the future seat copy. The staged 12-batch partition exists and its pin verifies. The packet, brief, all named ledger/lane/batch/builder/manifest/timeout tools and sandbox exist and were hashed or pin-verified; each relevant executable interface was run as described above. The actual dispatch directory, OWNERSHIP_b<k>.txt files, seat reports, joined census outputs, provider logs, receipts and dated check sheet are run-time deliverables, not pre-existing evidence I certify. C4 requires their appropriate preparation before dispatch. I did not search for history artefacts or gate files to supply them.

The decisive gate failures are machine evidence checks that falsely pass, an unaudited dependency graph capable of changing rests_on, and incompatible instructions for producing the final joined tally. They require implemented and re-pinned repairs before this delivered design is sound. Nothing in this report adopts or signs a method, authorizes a dispatch, or runs the reproduction census.

R3C2_V26_GATE_COMPLETE
