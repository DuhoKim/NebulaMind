ACCESS_SHA=22f01220a33f8695d18599176234c466fab36ff134b69af6c7d2cecdd978cb84
PACKET_SHA=3b5cb636fe7b5b082dd903778810d033bb09ff39d3ab4edb75e046c39c1242bb
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=structural only: the corpus is cosmology (C3's closed list is the Planck 2018 baseline); the lane anticipates REPRO_FAILED/ABSENT/BLOCKED outcomes (the "unreproduced from the stated inputs," not "error" wording rule; the "a seat may not supply a value" armor) and records provenance separately as if it may later be examined — no directional hypothesis, comparison model, or preferred outcome is inferable
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO (10 of 12 fully closed; residuals: §10.21 item 5 every-status evidence — ABSENT records with non-SILENT reason codes escape the check, fixture T11; item 2 brief/chains — the brief's limb-B file names are not the names the chain-B seal binds, fixture T10)

Seat: kimi. Date: 2026-09-06. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 26, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) per the principal's ruling and judged as written. §10 treated as history binding nothing; no prior verdict read or inferred; no gate files opened. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (given before the master was opened)

Q5 (asked and answered before Q3): No sentence in the packet tells me any outcome is more consequential than any other — to the lane, to anyone. The only consequence-adjacent sentence is §11: "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane." It asserts uniform non-consequence on the named axes; it ranks nothing. §4 labels CENSUS_PARTIAL "INCONCLUSIVE" and CENSUS_COMPLETE is the only non-stop terminal class, but no sentence states what any outcome does for anyone. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3): I cannot state what the recorded origin is FOR. §1: "The reproduction verdict and the provenance fields are recorded separately" — that states separation, not purpose. §3: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" — origin explicitly does not feed the arithmetic. What the packet shows being done WITH origin after recording are checks ON the recording: two-seat comparison ("the two seats' independent `origin` classifications disagree on inputs affecting more than 10% of included claims. The census does not proceed", §4) and C6 audit re-classification. No sentence states what conclusion, tally, comparison or decision the recorded origin serves. ORIGIN_PURPOSE=CANNOT_STATE.

Q3 (hypothesis-reachability): No directional hypothesis, comparison model or preferred outcome leaks. What is inferable is structural, each with its leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline" (C3); (ii) failure outcomes are anticipated and pre-framed non-accusatorily — 'Wording: "unreproduced from the stated inputs," not "error."' (§3, REPRO_FAILED); (iii) the rescue temptation is anticipated and armored — "A seat may not supply a value for an `ABSENT` or `BLOCKED` input." (§2); (iv) provenance is recorded with dispute machinery (§4 CENSUS_ORIGIN_DISPUTED) as if reproduction-versus-provenance structure may later be examined — but §1's "recorded separately" withholds any such model. Reconstructing a stake beyond this (e.g., "imports are expected to explain failures") exceeds what any sentence supports. Independent confirmation beyond the builder's assertion: my own grep of the packet for pattern/comparison-model/person/engine/consequence vocabulary returned only benign hits ("attempt", "stops the study").

1. OUTCOME CLASSES
Exhaustive and mutually exclusive — by construction plus a stated total precedence, and I could not break either.
Per-claim: I constructed real collisions — a claim stating no procedure whose input is also absent satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT; a claim blocked on an unobtainable named source whose arithmetic would also fail satisfies both REPRO_BLOCKED and REPRO_FAILED. The stated order ("file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group") files each to exactly one outcome. A claim fitting none: inputs all PRINTED/STANDARD ⇒ arithmetic group; input neither printed nor sourced ⇒ ABSENT; sourced but not machine-matchable ⇒ BLOCKED; no procedure ⇒ NO_DERIVATION; machinery/time ⇒ NOT_EVALUABLE. Total. Study-level: an audit failure over a blocked claim files CENSUS_AUDIT_FAILED (precedence over CENSUS_PARTIAL); a control split over an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name. The eight classes partition the reachable state space; the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim or an empty enumeration.
Q1 (asked directly): YES. Under option (c) every declared outcome is reachable and the reproduction question stays decidable. Hardest cases, each traced to exactly one outcome and one rests_on: (a) printed-but-chosen input — "we choose β = 0.5" is an AUTHOR_SPECIFIED_INPUT exclusion as a candidate and a PRINTED/CHOSEN record as an input; arithmetic consumes it; the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED; rests_on USES_CHOSEN; (b) a fitted parameter the paper states — as an input it is PRINTED/FITTED and consumed; as a claim its recipe is the fit, filing an arithmetic outcome if mechanically stated, else REPRO_NO_DERIVATION_STATED or REPRO_NOT_EVALUABLE; (c) an imported value — fixture T3: PRINTED/IMPORTED consumed when the named source is an enumerable manifest text with a numeric-token match at the first symbol-and-numeral line; REPRO_BLOCKED when the source is not enumerable or does not match (precedence puts BLOCKED before ABSENT); REPRO_INPUT_ABSENT only when no source is named; (d) the locally printed import ("we adopt a = 3 from X" printed by the claiming paper) — fixture T2: PRINTED/IMPORTED at its own line, exactly one outcome. The four earlier rounds' tension (reachability pulling against decidability) is closed here precisely because option (c) consumes by status and records provenance beside the verdict rather than gating admissibility on it.

2. CONTROLS
Every control names an exact code with PASS|FAIL|NOT_RUN (C0, C1, C1B_BATCH_COVERAGE, JOIN, C2, C3, C4_SEAT_ISOLATION, C4_PACKET_REDACTED, C5, C5B, C5C, C6). Assertion versus artefact: C3 PASS exists only on the printed validate runs ("a token asserted without the printed run is FAIL"); C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts; C5 FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals and the recomputed selection. The two controls that CAN be passed by assertion — C4 and C5b self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete." Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes" and §4's stop-class sentence assigns NOT_RUN to later limbs.
Q2 (asked directly): YES, checkable rather than asserted — with the floor the document itself states. Fixture: I wrote a ledger record whose origin is wrong but plausible — paperA prints "Using H0 = 67.36 km/s/Mpc from Planck 2018 we compute the age." and I filed it CHOSEN with ORIG_CHOICE_STATED and that true sentence as the quotation (T12): validate PASSES it (C3_NO_SUBSTITUTION=PASS), because the machine checks that the quotation is present, not that the reason code is the right one for the sentence. The mechanisms that catch it, as written: the second seat's independent origin classification (a disagreement is filed ORIGIN_DISPUTED, reported with both quotations, never reconciled; above 10% of included claims the study files CENSUS_ORIGIN_DISPUTED) and the C6 auditor's re-classification from the pinned sources — demonstrated (T8g): an auditor re-classifying that input differently makes audit compare print MISMATCH and C6_AUDIT_SAMPLE=FAIL, filing CENSUS_AUDIT_FAILED. The machine floor that IS enforced is demonstrated (T12b): a value line lying in another file but not filed IMPORTED/ORIG_CITATION fails validate ("such a record must be IMPORTED with ORIG_CITATION"). The document states the residual floor honestly in C3's lane-side span: a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands." That is the answer, and it is stated rather than implied away.

3. CIRCULARITY
No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule contains no pattern content (verified by my own packet grep, beyond the builder's forbidden-list assertion). Exclusion: every excluded candidate is retained in the exclusion ledger with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator (verified in tool output, T1); C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes any quietly excluded contrary claim an OMISSION that files CENSUS_AUDIT_FAILED. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are reported and never reconciled, the reason-code tie-break is stated as evidence specificity, and the severity order exists only lane-side in redacted spans. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. The one judgement-bearing exclusion kind (AUTHOR_SPECIFIED_INPUT) is counted, printed and audited; a mis-sort during a run is declared "a finding to bring back, not a widening."

4. THE BLIND
Machine-asserted as claimed — verified end to end: I re-ran the pinned builder (/usr/bin/python3 -E r3c2_build_seat_packet.py --master … --out … --brief …), first line of output C4_PACKET_REDACTED=PASS, sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'], 51 inline spans redacted, and the rebuilt packet is byte-identical to the pinned packet (sha256 3b5cb636… = PACKET_SHA). The C4 mechanism does what it states: a redacted out-of-lane copy, a kernel sandbox profile (r3c2_seat_sandbox.sb, digest 6978d590… verified on disk) whose permitted surface — runtime and temporary-directory access, configuration-directory exceptions, network access — is printed in the text, plus self-report lists. Its limits are stated honestly and without overclaim: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; PASS on C4/C5b "makes no claim that the list is complete"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." The accepted floor (a seat can tell provenance is classified with care) is the principal's ruled floor and is recorded as such.

5. THE SEAL (§7)
The receipted relay binds what it claims, before the step it must precede. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P." Receipt T: one commit containing the merged candidate file, the exclusion ledger and the merged input ledger with its compute output is sent and receipted BEFORE the protocol is opened, "the receipt names every file in that commit". After opening, the custodian independently re-hashes the tally and the protocol and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that."

6. FAIRNESS
Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the brief repeats it). My sweep of the packet's "error" vocabulary found only audit-internal uses ("may detect an error", "shared reader error"), never an outcome label. No class name or clause asserts more than its definition, with one held exception: DERIVED_ONLY is defined as "every root origin is DERIVED, STANDARD or MEASURED", so the name under-describes its membership — the item §10.21 holds for the principal (see Q7(e)). R3C2_NO_CLASS is bounded to C0–C5b with C6/seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases. REPRO_WITHIN_STATED_PRECISION's name matches its mechanical tests (rounding half away from zero; the one-sided half-width rule for asymmetric uncertainties).

7. STALL / EXECUTABILITY
Q4 (asked directly): With four exceptions named below as findings, everything the document describes executed as delivered. Each thing checked, and how: /usr/bin/python3 (runs, 3.9.6); sympy (imports, 1.14.0); /usr/bin/shasum (runs); r3c2_ledger_tools.py (exists, digest 16ae91a2… verified against R3C2_SEAT_PACKET.sha256; census T1/T13, validate T2–T5/T11–T14, all five audit subcommands T8); r3c2_lane_tools.py (exists, §9 digest da39212e… verified; merge/compute T6b/T7); r3c2_batch_tools.py (exists, digest eeafc482… verified; partition/seal/join/coverage T9); r3c2_manifest.py (exists; C5-5 run live over 20,637 files, exit 0; symlink refusal T15); r3c2_timeout.py (digest fbb9bef7… verified; wrapper run T16, prints command/stdout/exit, trivial child exit 0); r3c2_build_seat_packet.py (digest b1f2cce1… verified; --master/--out/--brief present; rebuild byte-identical); r3c2_seat_sandbox.sb (digest 6978d590… verified); R3C2_CORPUS_MANIFEST.md (digest 300d4da1… verified; 89 enumerable rows counted); R3C2_SEAT_PACKET.sha256 (8/8 OK); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 (10/10 OK); the kit (cd r3c2_staged_d1d7 && /usr/bin/python3 -E r3c2_staged_tests.py) — last three lines: "deletion_probes=45", "controls=131 passed=131 failed=0", "STAGED_TESTS=PASS"; partition_12_of_89.json (15b69217… matches C1's cited prefix); script-computed fields (rests_on, root_origins, NOT_COMPUTED, DISPUTED pair, declared/recomputed counts, JOIN, C1B, C6 tokens — all observed in fixtures). The four exceptions are findings F1–F4 below; F1 and F3 are described artefacts whose printed digests do not match the delivered bytes; F2 is a brief-versus-tool naming seam; F4 is a text-versus-tool corner.

Q7 (V26) ANSWERS
(a) Verifications, all from the working directory: both pin sheets verify (R3C2_SEAT_PACKET.sha256 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 10/10 OK). The kit runs: last three lines "deletion_probes=45 / controls=131 passed=131 failed=0 / STAGED_TESTS=PASS". Own fixtures (all tokens printed above): one locally printed import through validate with the candidate file — PASS (T2); one external import — PASS (T3), and the first-line rule enforced (T4 FAIL); a STANDARD record with a fabricated quotation — FAIL (T5a), the true quotation PASS (T5b); an empty ledger — PASS (T6a) and compute with the candidate file emits the NOT_COMPUTED row (T6b); a dependent claim whose parent lives in another claim with a disputed origin — DISPUTED with the pair [USES_CHOSEN, USES_UNDECLARED] on both claims (T7); a full audit sequence — seal-enumeration (first-write), select refuses without the stage-1 seal, select with seed (N=1 R=0 k=0), handout (ids+file+line only), seal-rederivation, compare C6_AUDIT_SAMPLE=PASS (T8a–f), and a tampered re-classification FAILs compare (T8g); partition → seal chain A (b1, b2) → join JOIN=PASS → coverage C1B_BATCH_COVERAGE=PASS (T9a–e), then a chain-B seal bound to chain A and its join JOIN=PASS (T9f–h), an unbound chain-B first seal refused (T9i). Tokens printed: C1_DENOMINATOR_PRINTED, C3_NO_SUBSTITUTION, C6_AUDIT_SAMPLE, JOIN, C1B_BATCH_COVERAGE.
(b) C5_EXECUTABLE_UNDER_SCOPE=YES — all five printed C5 commands executed on this host under the printed scope, each exit 0: Python 3.9.6; sympy 1.14.0; interpreter digest printed; user site path printed; r3c2_manifest.py over that path printed FILES=20637 and MANIFEST_SHA256=1b5463c1…, exit 0. NO_MASKED_STAGE=YES — every mandated command is one invocation (the five C5 commands, census, validate, compute, the audit subcommands, the batch subcommands, the §9 wrapper as one invocation printing its child's status); no mandated command is a shell pipeline (r3c2_manifest.py is ONE process — the old pipeline is gone); the kit's 45 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative.
(c) PRIOR_FINDINGS_CLOSED=NO. Item by item against §10.21's twelve: (1) validate's third argument — REPAIRED in text (C2/C3 carry <candidates.json>) and tool (T14: a PRINTED record without the candidate file FAILs); (2) batch-session brief and limb-A/B chains — PARTIALLY: brief rewritten, chains work in tool (T9), but residual seam F2 (T10); (3) locally printed cited value — REPAIRED (§2 wording; T2); (4) empty ledgers and NOT_COMPUTED — REPAIRED (§3 lane-side sentence; T6a/T6b); (5) evidence for every status — PARTIALLY: text states it, tool enforces it for PRINTED/STANDARD/BLOCKED (T5a) but not for ABSENT with a non-SILENT code — residual F4 (T11); (6) dispute propagation across claims — REPAIRED (C3 lane-side sentence; T7); (7) JOIN/C1B/C5C result contracts — REPAIRED (C1B states JOIN=PASS|FAIL|NOT_RUN and C1B_BATCH_COVERAGE=PASS|FAIL|NOT_RUN with the §4 filing rules; §9 states C5C_NO_FALLBACK=PASS|FAIL|NOT_RUN; tools print the tokens, T9); (8) the confinement paragraph — REPAIRED (C4 states what the profile permits, including that it does not deny all outside reads or writes); (9) builder arguments and the candidate pin sheet — REPAIRED (CLI verified, C4 names the arguments; R3C2_STAGED_D1D7.sha256 verifies 10/10); (10) the manifest's symlink refusal — REPAIRED (C5 sentence; T15: ERROR=link.txt … exit 1); (11) §3's parenthetical — REPAIRED (six kinds named, matching the tool's KINDS set); (12) the diff sentence — REPAIRED (§10.20's "What changed versus V25 (operative)"; spot-diff confirms the candidate f4edb514… lacks the repaired clauses the master carries, consistent with "repaired here").
(d) The brief as rewritten lets a seat execute every printed command in a limb-A batch session as delivered (census two-argument T1; validate three-argument T2; the five C5 commands live; digest prints). For limb B, NO as delivered: the brief directs final outcomes into `candidates_b<k>_limbB.json` and `SEAT_REPORT_b<k>_limbB.md`, while the chain-B seal binds exactly the four ART names (`candidates_b<k>.json`, `exclusions_b<k>.json`, `ledger_b<k>.json`, `SEAT_REPORT_b<k>.md`) — F2. The limb-A seals are preserved (first-write, order-enforced, never overwritten — separate sessions; an unbound chain-B seal is refused, T9i), but under the brief's naming the limb-B outcome artefacts are not what the chain-B seal binds, so the lifecycle does not preserve every seal as delivered. One printed sentence repairs it.
(e) The DERIVED_ONLY name: confirmed it changes no class, passability or reachability. The token is emitted only by the lane tool's rests_on() and consumed only by the seat-invisible interpretation protocol; no §3 outcome, §4 class, or control predicate references it; the membership rule (every root origin DERIVED, STANDARD or MEASURED) is unchanged by a rename. The lockstep consumers on a rename are the lane tool's emitted string, the §3 lane-side parenthetical, and the interpretation protocol — all lane-side. Label-only, as §10.21 holds.

FINDINGS (verbatim quote, defect, exact replacement)

F1 (non-cosmetic, lane-side text): stale second live copy of the lane-tool pin and the compute signature.
Quote (C3, lane-side span): "the lane owner runs the lane-side script `r3c2_lane_tools.py`, committed beside this document, sha256 `8e990c7a22fb4b093d5e74218e9bfcee4b108c52bbc2df615ed3b6b2aaefa848`: `/usr/bin/python3 -E r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>` and then `/usr/bin/python3 -E r3c2_lane_tools.py compute <merged.json> <out.json>`"
Defect: the delivered r3c2_lane_tools.py is da39212e26f9cb5733c9e580d37d1499167c078d5911013dfb7c9a1ddb44e862 (verified on disk and in R3C2_SEAT_PACKET.sha256), and §9's operative form is `compute <merged.json> <out.json> <candidates.json>`. The printed pin fails verification against the delivered file, and the two-argument form omits the candidate file — skipping the NOT_COMPUTED rows and the rests_on-rows-equal-denominator check that §10.21 item 4 exists to guarantee. This is the second-live-copy failure mode this document itself names elsewhere.
Exact replacement: "…sha256 `da39212e26f9cb5733c9e580d37d1499167c078d5911013dfb7c9a1ddb44e862`: `/usr/bin/python3 -E r3c2_lane_tools.py merge <ledger_seatA.json> <ledger_seatB.json> <merged.json>` and then `/usr/bin/python3 -E r3c2_lane_tools.py compute <merged.json> <out.json> <candidates.json>`" — or delete the duplicated literals and cite §9.

F2 (non-cosmetic, brief-versus-seal seam): the brief's limb-B file names are not the names the chain-B seal binds.
Quote (SEAT_BRIEF.md, limb-B order of work): "the same directory holds the agreed `candidates_b<k>.json`; write final outcomes into a COPY `candidates_b<k>_limbB.json` (never modify the sealed limb-A file)"
Defect: r3c2_batch_tools.py seal binds exactly `candidates_b<k>.json`, `exclusions_b<k>.json`, `ledger_b<k>.json`, `SEAT_REPORT_b<k>.md`. Demonstrated (T10): a limb-B directory holding only the brief-named files fails the chain-B seal with "FAIL: batch 1 artefact missing: candidates_b1.json"; and where the agreed limb-A `candidates_b<k>.json` is present, the chain-B seal binds the PENDING limb-A bytes while the limb-B outcomes sit unbound in the `_limbB` files — the chain C1 describes ("writing final outcomes into separate limb-B copies of the four files, sealed in a separate ordered LIMB-B chain") does not bind the outcomes as delivered.
Exact replacement (brief limb-B paragraph): "the limb-B session directory is separate from the sealed limb-A session directory; write the final outcomes into this session's `candidates_b<k>.json` (the sealed limb-A file lives in the limb-A session directory and is never modified), keep `exclusions_b<k>.json` and `ledger_b<k>.json` as agreed, and write `SEAT_REPORT_b<k>.md`; this session's four files are sealed in the LIMB-B chain bound to the agreed limb-A seals." (Equally acceptable: keep the `_limbB` names and print in C1 a custodian step that assembles the chain-B sealing directory under the four ART names from them — but one of the two must be printed.)

F3 (COSMETIC under the five tests; a false literal in a control sentence): C5's printed digest for r3c2_manifest.py is not the delivered file.
Quote (C5; the packet carries the same sentence): "`r3c2_manifest.py` is committed beside this document, sha256 `19a8ce4750bb47655868ef15b55f2b168833147b460c03ad56f84dd3c9bc56f2`, delivered in the seat's working directory and pinned in `R3C2_SEAT_PACKET.sha256`"
Defect: the delivered file's digest is 974458f3ec3d530ebaeb1159ce0489b53a9d8e4cf82591b7639538322a5a5a19 (verified on disk; correctly carried by R3C2_SEAT_PACKET.sha256 and by §10.20). The clause "pinned in `R3C2_SEAT_PACKET.sha256`" is true; the printed digest is false. No class filed, no control's passability (C5's PASS predicate compares the dispatch record, not this sentence), no reachability, no conclusion and no tested value changes — COSMETIC — but it is a wrong literal in a control seats execute.
Exact replacement: "sha256 `974458f3ec3d530ebaeb1159ce0489b53a9d8e4cf82591b7639538322a5a5a19`" (edit the master sentence, rebuild the packet, re-pin; the pin sheet already carries the true digest).

F4 (minor corner; the every-status sentence overclaims the machine check): ABSENT records with non-SILENT reason codes escape the evidence check.
Quote (C3): "and, except for `ORIG_SILENT`, a **verbatim quotation machine-matched to the cited line** — for EVERY record whatever its status: `validate` requires a non-empty quotation and matches it at `origin_evidence.source_file`/`source_line`"
Defect: demonstrated (T11) — {status ABSENT, origin MEASURED, reason_code ORIG_MEASURED, empty verbatim} PASSES validate (exit 0, C3_NO_SUBSTITUTION=PASS); the tool applies the every-status evidence check to STANDARD and BLOCKED records (and to PRINTED in its own branches) but not to ABSENT. The corner is coherent — a measurement described but never printed is ABSENT with origin MEASURED — and its origin feeds rests_on, so the unchecked corner reaches a reported tally; the catching mechanisms are the second seat and C6, not validate as the sentence states.
Exact replacement (text-bounding): "for EVERY record with status PRINTED, STANDARD or BLOCKED: `validate` requires a non-empty quotation and matches it at `origin_evidence.source_file`/`source_line`; an `ABSENT` record carries `UNDECLARED` with `ORIG_SILENT` and its printed `origin_search`" — or extend the tool's evidence check to ABSENT records with non-SILENT codes and re-pin (if the ABSENT-implies-UNDECLARED pairing is intended, having validate assert it closes the corner mechanically).

COSMETIC NOTES (no repair required; recorded for completeness)
N1: the delivered lane, batch and manifest tool docstrings still read "STAGED, UNADOPTED copy" (and the delivered ledger tool's body text carries "STAGED (D1, UNADOPTED)" / "STAGED (D7, UNADOPTED)"), while §10.20 records them as adopted under their pinned names. Behavior unchanged.
N2: the delivered lane tool's usage text shows the two-argument compute while §9 mandates the three-argument form; the code accepts both and the three-argument form works (T6b).
N3: the brief names `OWNERSHIP_b<k>.txt`, which no printed command produces; it is a custodian extract of the pinned partition_12_of_89.json. The dispatch record's contents listing covers it; one sentence in C1 would make its provenance explicit.

Why SOUND_WITH_REPAIRS and not UNSOUND: no finding changes a class filed, a precedence, a reachability verdict, the blind, the seal, or any control's passability as gated; Q1 and Q2 answer YES with fixtures; the blind rebuilds byte-identically under machine assertion; the two non-cosmetic items (F1, F2) are literal/artefact repairs in lane-side and brief text with one-sentence fixes, and F3/F4 are a false literal and a bounded corner. Why not SOUND: Q4 and Q7(d) each found a real as-delivered discrepancy (F1–F4).

R3C2_V26_GATE_COMPLETE
