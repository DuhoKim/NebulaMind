ACCESS_SHA=12daf4f5aa9eb89d5411089c1d7120d153caa4f81cb3bf6549c3805bda06f62a
PACKET_SHA=6da7de231b531cc7e033ccd3623c33209f71e0bb1633760c97ea18f1ee793387
GATE=PREREG_UNSOUND
LEAK=NONE
CONSEQUENCE_VISIBLE=Procedural: CENSUS_AUDIT_FAILED says “No tally is filed; report which.” No preferred empirical result is disclosed.
ORIGIN_PURPOSE=Independent checking and dispute handling: “Every input's `origin` is classified independently by both seats.” The packet also requires audit comparison; no interpretive mapping is disclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO

Packet-first answers, recorded before the master assessment:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

Procedurally, yes. “No tally is filed; report which.” makes CENSUS_AUDIT_FAILED different from outcomes that permit reporting a tally. No sentence identifies a substantively preferred reproduction result or says which result benefits anyone. My initial packet-stage answer distinguished substantive stakes from procedural consequences; the header uses the latter because the question says “any outcome” and “to anyone.”

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED origin OF AN INPUT IS FOR?

I can state its checking function: “Every input's `origin` is classified independently by both seats.” The packet further says the audit “compares those reconstructed fields and dependency edges against the sealed ledger, carrying a declared origin alternative explicitly”. Disagreement can stop the census. I cannot state its downstream interpretive purpose. My initial CANNOT_STATE answer concerned that downstream purpose; it was too narrow for the literal question about any use after recording.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

No directional hypothesis, comparison model, preferred outcome or predicted pattern is reconstructible. These are the inferences I could draw from the packet alone:

- “The reproduction verdict and the provenance fields are recorded separately.” Provenance receives independent attention; a later joint analysis is plausible, but neither its existence nor its mapping follows from this sentence.
- “Distinct from a claim whose inputs the paper DOES state, chosen or not” highlights anticipated difficulties involving chosen inputs. It does not tell me whether their presence is hoped for.
- “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.” The task has cosmological subject matter. This does not establish Planck as the lane's comparison model.
- The detailed fitted/imported-input rules anticipate those cases. Their procedural treatment supplies no desired frequency or result.

LEAK=NONE means no reconstructed hypothesis or preference, not absence of subject matter or process information.

This is a design assessment. The packet was read first. The master was then read, with section 10 treated as nonbinding history. Only the two explicitly authorized V26 reports were consulted for the requested closure assessment. Q7's testing instructions were treated as authorization to inspect the brief, named tools, manifests, sandbox and staged kit. No interpretation-protocol content or other gate report was opened. All calculations below concern independently authored synthetic fixtures, not corpus reproduction attempts.

## 1. OUTCOME CLASSES

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

YES for the settled classification definition and its bounded operational question. The delivered controls are not sound enough to certify that classification in execution, as sections 2 and 7 demonstrate. This does not reopen option (c).

Concrete, physically meaningful claim constructions:

| Claim and stated recipe | Unique outcome | rests_on |
|---|---|---|
| Choose acceleration a = 3 m/s²; v = a × 2 s = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe and inputs, but claim v = 7 m/s | REPRO_FAILED | USES_CHOSEN |
| Fit a = 3 m/s²; state it; v = a × 2 s = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| Print “we adopt a = 3 from X”; same velocity recipe | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Cite a in enumerable, byte-verified X, with the first symbol/numeric-token line printing 3 | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Cite a in unpinned X without printing its value locally | REPRO_BLOCKED | USES_IMPORTED |
| Need a, neither print it nor name its source, with provenance silent | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| Fully specified numerical procedure exceeds 120 seconds, using printed measured inputs | REPRO_NOT_EVALUABLE | DERIVED_STANDARD_OR_MEASURED_ONLY |
| “Our result is 6 m/s”, with no producing procedure and no input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

Hardest combined case: v = a t + v0 = 17 m/s, with a = 3 m/s² explicitly chosen, t = 4 s explicitly fitted and stated, and v0 = 5 m/s imported under the verified-source rule. All three inputs are mechanically consumed. Exactly REPRO_WITHIN_STATED_PRECISION and USES_IMPORTED result. Printing 18 m/s instead changes only the reproduction outcome to REPRO_FAILED. If v0 is printed locally with its citation, no external opening is needed. The reproducer chooses no missing value.

A claim requiring both a blocked cited input and an entirely absent input satisfies two raw predicates, but precedence files only REPRO_BLOCKED. A named-but-unspecified procedure takes NO_DERIVATION_STATED before machinery limitations. I found no double filing after precedence. The question is operationally decidable through explicit non-evaluable outcomes; it is not a claim that arbitrary mathematics is decidable.

Each study class has a witness, with other controls passing where reached:

| Witness | Unique study class |
|---|---|
| One arithmetic claim, audit PASS | CENSUS_COMPLETE |
| One non-arithmetic claim, successfully audited | CENSUS_PARTIAL / INCONCLUSIVE |
| Audit mismatch, missing seed or failed receipt verification | CENSUS_AUDIT_FAILED |
| Repeated common prerequisite-control failure | R3C2_NO_CLASS |
| Persistent candidate-inclusion or input-list disagreement | CENSUS_DENOMINATOR_DISPUTED |
| Persistent per-claim outcome disagreement | CENSUS_OUTCOME_DISPUTED |
| Origin disagreement affecting 2 of 10 included claims | CENSUS_ORIGIN_DISPUTED |
| Surviving fail/pass prerequisite-control split | CENSUS_CONTROL_SPLIT |

A non-arithmetic tally with an audit failure satisfies two raw conditions but files only CENSUS_AUDIT_FAILED. A genuinely empty enumeration on both sides can file PARTIAL; zero included claims with excluded passages instead meets C6's explicit zero-denominator failure rule. INCONCLUSIVE is genuinely reachable, including an audited, recordless NO_DERIVATION_STATED claim. No unclassified completed tally was found under the precedence rules.

## 2. CONTROLS

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

It is checkable by cited evidence and independent readers, but the delivered machine checks still establish less than promised.

My wrong-but-plausible record was:

```json
{"claim_id":"p.txt#c","input_id":"p.txt#a","symbol":"a","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"p.txt","source_line":5,"verbatim":"We adopt a = 3 from q.txt."},"derived_from":[],"value":"3","source_file":"p.txt","source_line":5}
```

That sentence really occurs at the cited line. validate returned C3_NO_SUBSTITUTION=PASS, exit 0. Citation precedence requires IMPORTED. A correctly classifying second seat catches the disagreement: my merge/compute run reported one origin disagreement and rests_on=['USES_CHOSEN', 'USES_IMPORTED'], DISPUTED. An independently reconstructed IMPORTED record against an undisputed CHOSEN sealed record returned C6_AUDIT_SAMPLE=FAIL, exit 1. If every reader shares the wrong classification, no mechanism catches it. The master explicitly acknowledges that semantic floor.

The following defects are additional implementation failures, not that acknowledged floor.

**F1 — NON-COSMETIC: C6 accepts incomplete reconstructions and does not compare every promised field.**

Verbatim governing sentence:

> **Second**, after
> receipt T and the supply of the external seed, the custodian computes the selection (`audit select`, which refuses without the stage-1
> seal) — every arithmetic-group claim plus `k = min(max(1, ceil(0.20 × N)), R)` of the remaining included claims, `N` being the sealed denominator and `R` the number of remaining claims (when `R` is zero the
> sample is empty), drawn by `random.Random(seed_int).sample(remaining_ids, k)` where
> `remaining_ids = sorted(set(included_ids) − set(arithmetic_group_ids))` and `seed_int = int(seed_hex, 16)`, the custodian's seed being
> 64 lowercase hexadecimal characters — and hands the auditor claim identifiers with source file and line ONLY (`audit handout`); the auditor independently reconstructs each assigned claim's complete input records and dependency closure from the pinned sources, including cross-claim dependencies, recording status, value, source coordinates, origin evidence and `derived_from` for each input, and the custodian seals this reconstruction by digest (`audit seal-rederivation`) BEFORE any sealed ledger is released; `audit compare` compares those reconstructed fields and dependency edges against the sealed ledger, carrying a declared origin alternative explicitly, and a missing input, a differing dependency edge or an unsupported record is `MISMATCH`; it recomputes each input's chain of origins to its roots from both reconstructions and prints the comparison.

The required full-record graph test works: changing only d's parent from a to b, both CHOSEN roots, returns C6_AUDIT_SAMPLE=FAIL with a derived_from mismatch. Equal root sets do not hide an explicitly supplied differing edge.

But three separately sealed counterexamples return C6_AUDIT_SAMPLE=PASS, exit 0:

1. Reconstruction records omit status, value, source coordinates and evidence. Only origin and the derived record's parent list remain.
2. A complete reconstruction changes only origin_evidence.verbatim to “fabricated”.
3. An assigned claim reconstructs d but omits its cross-claim parent a. Root computation silently obtains a from the sealed ledger.

The code compares selected fields only under `if fld in ar`; it never compares origin_evidence, origin_search, symbol or claim_id. Its root computation uses `roots(dict(full_by,**a_by),i)`, supplying missing auditor dependencies from the sealed side. Thus the auditor can assert a label while omitting the evidence and independent graph reconstruction the control promises.

Exact replacement for the quoted sentence, preserving the sampling rule:

> Second, after receipt T and the external seed, audit select requires the stage-1 seal and selects every arithmetic-group claim plus k = min(max(1, ceil(0.20 × N)), R) remaining included claims, using random.Random(int(seed_hex, 16)).sample(sorted(set(included_ids) − set(arithmetic_group_ids)), k), where seed_hex has 64 lowercase hexadecimal characters. audit handout supplies only claim identifiers and source file and line. The auditor reconstructs every assigned claim's full C3 input records and complete cross-claim dependency closure. Before any census ledger is released, audit seal-rederivation validates the reconstruction schema and seals its digest. audit compare requires every C3 field, including claim_id, input_id, symbol, status, value, source coordinates, origin, origin_evidence, derived_from and origin_search where required; compares every field and dependency edge; and reports any missing field, missing dependency or unsupported difference as MISMATCH. Origin evidence is compared with the corresponding sealed classification's evidence, with any differing valid citation explicitly reviewed and recorded before it can be accepted. Auditor roots are computed solely from reconstructed records, never by borrowing records from the sealed ledger. Print a per-input root comparison and the corresponding per-claim summaries for both sides. Any failed predicate files C6_AUDIT_SAMPLE=FAIL and CENSUS_AUDIT_FAILED.

Implement and re-pin this contract; editing prose alone cannot fix the false PASS.

**F2 — NON-COSMETIC: the declared alternative is accepted locally and rejected again by root comparison.**

The F1 governing sentence explicitly promises “carrying a declared origin alternative explicitly”. Section 3 also states:

> A claim with a disputed root carries the pair computed under both classifications and
> is marked `DISPUTED`.

My sealed input has origin CHOSEN and origin_alt FITTED. The complete auditor reconstruction selects FITTED. compare removes the origin mismatch because it matches origin_alt, but then compares audit roots ['FITTED'] only against primary sealed roots ['CHOSEN']. It files C6_AUDIT_SAMPLE=FAIL. This is a below-threshold disputed input's promised alternative being defeated by a later check. It can occur in a larger census where one disputed claim is at or below 10%; it is not limited to my one-claim API fixture.

Exact replacement for the quoted section-3 sentence:

> A claim with a disputed input or parent list carries both script-computed classifications and is marked DISPUTED. During C6 comparison, bind an auditor classification to its matching declared sealed origin, evidence and parent-list branch; recompute roots under that branch and compare like with like. Report both sealed branches and the auditor's matching branch. Matching an explicitly declared alternative alone is not MISMATCH; an unsupported classification or graph difference remains MISMATCH.

Apply the same rule to audit compare. Do not remove dispute reporting or reconcile the two classifications.

**F3 — NON-COSMETIC: STANDARD and BLOCKED still bypass status-specific evidence obligations.**

Verbatim STANDARD sentence:

> **A `STANDARD` record carries `symbol` = the ledger key and `value` = the exact
> string printed in the value column; `validate` compares strings.**

The additional operative C3 requirement is verbatim:

> `STANDARD` additionally requires the value as a numeric token at the record's own cited line and closed-list membership; `BLOCKED` additionally requires an empty value and naming evidence in the claiming file.

Verbatim BLOCKED sentence:

> An input that files `REPRO_BLOCKED` under §3 is recorded with status `BLOCKED`,
> `origin` `IMPORTED`, `ORIG_CITATION` evidence cited to the claiming paper's naming sentence, and no value; the arithmetic
> never consumes it.

All these fixtures incorrectly return C3_NO_SUBSTITUTION=PASS, exit 0:

- STANDARD H0=67.36, ORIG_SILENT/UNDECLARED with an origin_search, pointing its value to missing.txt:999.
- STANDARD H0=67.36 with valid MEASURED evidence, but empty value source_file and no source_line.
- BLOCKED/IMPORTED quoting q.txt although its candidate's claiming file is p.txt.

The first two skip STANDARD's conditional value-line check. BLOCKED checks empty value and origin/code pairing, but never binds its naming-evidence file to the candidate's claiming file. Whether a sentence really names the value remains reader judgment; which file contains it is machine-checkable and promised.

Exact replacement for the additional operative C3 requirement quoted above:

> Every STANDARD record, including ORIG_SILENT, requires non-empty source_file, a positive one-based source_line, closed-list membership and the value as a numeric token at that line; absence of origin evidence never waives value evidence. Every BLOCKED record requires an empty value, IMPORTED/ORIG_CITATION and a candidate-file binding proving that its naming-evidence file is the claiming file. Missing coordinates or missing candidate binding fails validate.

Implement these checks unconditionally in the relevant status branches and re-pin.

**F4 — NON-COSMETIC: an ordinary PRINTED record cannot cite valid origin evidence on a different line from its value.**

Verbatim governing sentence:

> A
> script asserts that no `ABSENT` or `BLOCKED` record carries a value, that **each `PRINTED` value machine-matches the
> text at its cited source line and each verbatim quotation is a substring of that line**, and that **each `STANDARD`
> value is one of a closed list PRINTED LITERALLY BELOW** — so "standard" cannot become a selectable family;
> **completeness of a claim's input list against the paper's equation is seat-authored and audited under C6, not
> machine-checked**:

This retains a same-line restriction despite the new independent origin_evidence coordinates and the instruction that value-line checks are separate. Fixture: p.txt:6 says “We choose a for the calculation.”; p.txt:7 says “a = 3.” The record correctly cites origin evidence at 6 and value at 7. The shared evidence check succeeds, then the ordinary PRINTED branch fails with “verbatim not found at p.txt:7”. Both seats can follow the schema correctly and fail C3 twice, stopping a reproducible claim under NO_CLASS.

Exact replacement:

> validate asserts that ABSENT and BLOCKED records carry no value, each PRINTED value machine-matches its source_file/source_line, and each non-ORIG_SILENT quotation occurs at its own origin_evidence.source_file/source_line. The evidence line may differ from the value line; no second quotation check is applied to the value line. Every STANDARD value additionally satisfies the closed list and its own value-line check. Completeness of a claim's input list against the equation remains seat-authored and audited under C6.

Remove the redundant ordinary-PRINTED quotation-at-value-line check and re-pin.

Control contracts otherwise exist: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE each have PASS/FAIL/NOT_RUN rules. C0 requires an exhibition and independent verification. C2 shares the printed validator artefact. C4/C5b require path lists but expressly do not prove completeness. C5C requires session-identified provider logs. Unreached controls are NOT_RUN, including per-session validation before joining. Bare PASS assertions do not meet these contracts; F1 and F3 show that printed passing artefacts can nevertheless omit promised checks.

## 3. CIRCULARITY

The inclusion rule contains no directional hypothesis. A contrary number asserted as the paper's own result must be enumerated. An author-specified input is excluded as a standalone result but retained in the exclusion ledger and usable by an included claim's recipe.

Independent enumerations, retained exclusion coordinates, zero-tolerance seat reconciliation and the auditor's full enumeration make unilateral quiet exclusions detectable. They do not eliminate shared reader error. An omitted passage produces an OMISSION; a listed passage classified differently produces a reported inclusion dispute, tolerated only within the declared threshold. It would be wrong to claim that every mis-exclusion necessarily fails C6.

The packet did not reveal a desired answer, and receipt P prevents unnoticed retrospective protocol replacement. Nevertheless, F1 permits unsupported provenance evidence and incomplete independent dependency reconstruction to survive audit PASS; F3 allows unverified inputs through validation. These leave concrete judgment channels less checked than advertised. No inference of intentional manipulation is made.

## 4. THE BLIND

The packet's redaction is machine-asserted at the stated lexical strength. I executed the pinned builder with explicit --master, --out and --brief arguments, directing output to my fixture directory. It returned C4_PACKET_REDACTED=PASS and reproduced the delivered packet byte for byte. The embedded master digest equals ACCESS_SHA. The builder checks required content and forbidden strings in the packet and forbidden strings in the brief; it is not a semantic proof about all delivered code or runtime content.

The sandbox file exists and hashes to the operative pin. Its actual exceptions include network, runtime, temporary and configuration-directory access. C4 states these exceptions and requires dispatch-specific denied-read probes for the master and interpretation material. C4/C5b are explicitly self-report controls; neither establishes a complete access log or absence of prior exposure. These limits are honestly stated. I did not launch a census seat or certify future dispatch probes.

## 5. THE SEAL

Section 7 binds the promised artefacts at the required times: receipt P fixes protocol hash and commit before limb A; receipt T fixes the tally commit and named files before protocol opening and external-seed sampling. Independent rehashing against both receipts detects replacement; missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

The claim is limited to artefact custody and chronology, not the lane owner's inability to anticipate results. The protocol path exists, checked by metadata only. No actual P or T was generated, inspected or certified.

The separate batch-chain test also succeeds: chain B binds the supplied agreed chain-A seals digest; final candidates are sealed under canonical names; both chains remain verifiable. This establishes the fixture lifecycle, not an external custodian's future chronology.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The brief applies that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NOT_EVALUABLE and NO_DERIVATION_STATED describe limitations of this reproduction attempt rather than proving the paper wrong. The positive class's name matches its uncertainty/rounding definition. Audit references to reader error are not paper-outcome labels.

DERIVED_STANDARD_OR_MEASURED_ONLY now accurately names its declared membership. Its rename is label-only under the requested five tests.

**COSMETIC C1 — the operative historical parenthesis names the wrong old token.**

Verbatim text:

> `DERIVED_STANDARD_OR_MEASURED_ONLY` (the token `DERIVED_STANDARD_OR_MEASURED_ONLY` of V10–V26, identical membership)

Exact replacement:

> `DERIVED_STANDARD_OR_MEASURED_ONLY` (formerly `DERIVED_ONLY`, with identical membership)

Change only this operative parenthesis, not section 10's history. No class, control passability, reachability, conclusion or tested value changes.

## 7. STALL / EXECUTABILITY

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

YES. The named scripts launch, and the requested batch lifecycle now works, but F1–F4 show advertised checks that do not execute as specified and a valid input record that cannot pass. Execution readiness is therefore not established by the staged kit's PASS.

Q7(a): both pin sheets verified from the working directory, before and after testing: R3C2_SEAT_PACKET.sha256, 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256, 10/10 OK. The staged sheet's entries are lane-relative. Packet line 9's master digest equals ACCESS_SHA.

I ran `/usr/bin/python3 -E r3c2_staged_tests.py` in r3c2_staged_d1d7. Exit 0; last three lines:

```text
deletion_probes=49
controls=141 passed=141 failed=0
STAGED_TESTS=PASS
```

Independently authored fixture results:

```text
audit full-record match: C6_AUDIT_SAMPLE=PASS; exit 0
audit equal-root-set dependency-edge change: C6_AUDIT_SAMPLE=FAIL; exit 1
ABSENT/MEASURED empty quotation: C3_NO_SUBSTITUTION=FAIL; exit 1
ABSENT/MEASURED describing sentence quoted: C3_NO_SUBSTITUTION=PASS; exit 0
PRINTED empty quotation: C3_NO_SUBSTITUTION=FAIL; exit 1
PRINTED nonexistent evidence coordinates: C3_NO_SUBSTITUTION=FAIL; exit 1
partition two texts/two batches: exit 0
seal chain A batches 1 and 2: exit 0 each
join chain A: JOIN=PASS; exit 0
coverage: C1B_BATCH_COVERAGE=PASS; exit 0
joined cross-batch validate: C3_NO_SUBSTITUTION=PASS; exit 0
limb B separate directory, canonical names, per-batch final census: C1_DENOMINATOR_PRINTED=PASS; exit 0 each
seal chain B bound to chain A: exit 0 each
join chain B: JOIN=PASS; exit 0
joined limb-B census final: C1_DENOMINATOR_PRINTED=PASS; exit 0
LIMB_A_UNCHANGED=PASS
attempt to reseal chain-A batch 1: refused; exit 1
compute with two arguments: usage refusal; exit 2; no output file
compute with three arguments: p.txt#c rests_on=NOT_COMPUTED; exit 0
manifest on symlinked root: ERROR=<root>: root is a symlink or not a directory (refused); exit 1
builder: C4_PACKET_REDACTED=PASS; exit 0
PACKET_BYTE_IDENTICAL=PASS
```

Additional adversarial tokens establishing F1–F4:

```text
incomplete audit records: C6_AUDIT_SAMPLE=PASS; exit 0
changed audit origin_evidence: C6_AUDIT_SAMPLE=PASS; exit 0
missing cross-claim auditor dependency closure: C6_AUDIT_SAMPLE=PASS; exit 0
auditor matches declared origin_alt: C6_AUDIT_SAMPLE=FAIL; exit 1
STANDARD/ORIG_SILENT missing value line: C3_NO_SUBSTITUTION=PASS; exit 0
STANDARD empty value coordinates: C3_NO_SUBSTITUTION=PASS; exit 0
BLOCKED naming evidence in wrong claiming file: C3_NO_SUBSTITUTION=PASS; exit 0
valid separate origin/value lines: C3_NO_SUBSTITUTION=FAIL; exit 1
```

Q7(b): C5_EXECUTABLE_UNDER_SCOPE=YES and NO_MASKED_STAGE=YES for this text. All five C5 commands executed live under C4's permission principle:

```text
/usr/bin/python3 -E --version
Python 3.9.6

/usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"
1.14.0

/usr/bin/shasum -a 256 /usr/bin/python3
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3

/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"
/Users/duhokim/Library/Python/3.9/lib/python/site-packages

/usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
```

All exited 0. This does not certify equality to a future dispatch record. No operative mandated command is a shell pipeline. The wrapper returned 0 for a successful child and propagated exit 7 under the printed 120.0 limit; a separately labelled 0.1-second timeout probe returned SYMBOLIC_TIMEOUT/124. A passing stage can still have insufficient predicates, as F1 shows; NO_MASKED_STAGE addresses exit propagation, not that separate problem.

Q7(c): V26 closure, checked against the authorized reports and current evidence, not adopted from section 10:

| Prior item | Status and evidence |
|---|---|
| codex F1, every-status quotation checks | CLOSED for the reported counterexamples: ABSENT empty evidence and ordinary PRINTED empty/bad-coordinate evidence now fail. F3/F4 are separate remaining status/value and redundant-line defects. |
| codex F2, independent input/graph audit | PARTLY: full supplied edge changes fail even with equal roots; missing fields, ignored evidence and borrowed cross-claim closure still pass. F1; alternative handling also fails, F2. |
| codex F3, lane pin and compute arity | CLOSED: C3 and §9 pins match; two arguments exit 2 with no output; three emit NOT_COMPUTED. |
| codex F4, limb-B canonical artefacts | CLOSED: separate-directory B sealing/join/final census passes; A bytes and seals unchanged. |
| codex F5, cross-batch validation lifecycle | CLOSED: brief and C2 defer per-session validation; joined cross-batch fixture passes before arithmetic. |
| codex F6, manifest pin | CLOSED: operative digest and both verified pin sheets match delivered bytes. |
| codex F7, manifest root symlink | CLOSED: own root-symlink fixture refuses with ERROR and exit 1. |
| codex C1, local-import summary | CLOSED: external/local branches are distinguished in the text; own locally printed import validates. |
| codex C2, printed builder arguments | CLOSED: all three arguments appear; explicit invocation reproduces packet bytes. |
| codex C3, provenance label | CLOSED as label-only: implemented token matches membership. Cosmetic C1 above concerns its historical parenthesis. |
| kimi F1, lane pin/compute signature | CLOSED: same evidence as codex F3. |
| kimi F2, limb-B filenames | CLOSED: same evidence as codex F4. |
| kimi F3, manifest pin | CLOSED: same evidence as codex F6. |
| kimi F4, ABSENT evidence | CLOSED: required empty/quoted ABSENT fixtures fail/pass respectively. |
| kimi N1, tool docstrings | NOT: delivered lane, batch and manifest docstrings still say STAGED, UNADOPTED; the seat tool retains staged comments. Cosmetic C2 below. |
| kimi N2, compute usage | CLOSED: usage now names mandatory candidates and CLI rejects the old arity. |
| kimi N3, ownership-list provenance | CLOSED: C1 explicitly assigns the extract to the custodian and pins its digest in dispatch. |

**COSMETIC C2 — delivered tool provenance/usage text is still stale.**

Verbatim batch-tool opening sentence:

> r3c2_batch_tools_STAGED.py — STAGED, UNADOPTED (batch-reading candidate, repaired 2026-09-06 after two independent reviews). LANE-SIDE.

Exact replacement:

> r3c2_batch_tools.py — the V27 lane-side ownership partition, batch seal, join and coverage tool.

Verbatim lane-tool opening sentence:

> r3c2_lane_tools_STAGED.py — STAGED, UNADOPTED copy (V26cand gate F2/F4 repairs) of the LANE-SIDE tool, never given to a seat: merge two validated seat ledgers; compute root_origins and
> per-claim rests_on from the merged ledger.

Exact replacement:

> r3c2_lane_tools.py — the V27 lane-side tool, never given to a seat: merge validated seat ledgers and compute root_origins and per-claim rests_on.

Verbatim manifest-tool opening sentence:

> r3c2_manifest_STAGED.py <directory> — STAGED, UNADOPTED copy (V26cand gate F9: symlinks and non-regular entries fail closed) — the pinned-environment manifest as ONE process (codex V24e D3: a shell pipeline can mask an
> upstream failure).

Exact replacement:

> r3c2_manifest.py <directory> — the V27 pinned-environment manifest, executed as one process and refusing symlinks and non-regular entries.

The seat-tool usage also still prints `/usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir>`; replace that exact line with `/usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir> <candidates.json>`. Remove stale STAGED/UNADOPTED editorial status labels in current delivered tool comments, retaining historical records elsewhere. These documentation changes alter none of the five substantive tests; re-pin changed bytes consistently. They do not repair F1–F4.

Q7(d): YES for the repaired two-limb batch lifecycle and preservation of seals; NO for an unconditional claim that every valid batch session can complete its controls as delivered. The brief now uses canonical names in a separate B directory, defers validate until join and permits all printed harness commands. My full two-batch fixture, including a cross-batch dependency, passes through B final census without overwriting A. F4 still prevents some valid joined ledgers from passing, and F1–F3 leave advertised predicates unimplemented.

Delivery inventory and how checked:

- Packet, brief, ledger/lane/batch/builder/manifest/timeout tools: existence and exact digests verified by the eight-file pin sheet. Relevant census, validate, merge, compute, all five audit subcommands, partition/seal/join/coverage, builder, manifest and wrapper interfaces executed as recorded above. Full reconstruction/root and candidate-coverage outputs were inspected.
- Staged tools, test driver, README, partition JSON/text and counterexample exhibit: ten-file sheet verified; staged tests executed. The partition digest is 15b69217a35dbc6fb8f47057cefbb827e3ed0710ecb04f2c71d3e68444b97b06, matching C1's prefix.
- R3C2_CORPUS_MANIFEST.md: digest 300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24 verified. Its 89 enumerable source paths all exist under ../bhu-reading-20260823/sources/, checked by metadata only. Corpus contents were not opened or rehashed; this is not a corpus audit.
- r3c2_seat_sandbox.sb: read and hashed to 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6. Interpretation protocol: path existence only, no content opened.
- OWNERSHIP_b<k>.txt, dispatch inventories/probes, provider logs, batch and joined ledgers/reports, C0 exhibition, C6_AUDIT.json, receipts P/T and the dated check sheet are explicitly produced at their respective stages. Their future absence is not asserted as a missing delivered script. The actual working directory is the lane root, not a prepared census-seat copy; dispatch preparation remains required by C4.

Full resolved fixture commands, working directories, stdout, stderr and exit statuses are preserved in `/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v27_own_29the5yf/test_log.json`. Independent drivers are `/tmp/r3c2_v27_own_tests.py` and `/tmp/r3c2_v27_extra_tests.py`; the final additional origin/merge/audit probe is recorded in the same log. These are referee test evidence, not study deliverables.

The decisive defects are false audit passes on incomplete reconstructions, failed handling of a declared alternative, remaining status-specific validation bypasses and rejection of valid separate evidence/value lines. They require implemented and re-pinned repairs. This report adopts and signs nothing and runs no reproduction census.

R3C2_V27_GATE_COMPLETE
