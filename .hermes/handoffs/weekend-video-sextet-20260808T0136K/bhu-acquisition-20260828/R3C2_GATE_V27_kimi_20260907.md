ACCESS_SHA=12daf4f5aa9eb89d5411089c1d7120d153caa4f81cb3bf6549c3805bda06f62a
PACKET_SHA=6da7de231b531cc7e033ccd3623c33209f71e0bb1633760c97ea18f1ee793387
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology (C3's "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline"); failure outcomes are anticipated and pre-worded (§3's "unreproduced from the stated inputs," not "error"); §11's "No tier, warrant token, standing or stamp moves" shows a stake exists and is fenced off — no hypothesis, comparison model, or preferred outcome inferable
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO (19 of 20 closed with evidence below; kimi N1 NOT closed — the delivered pinned tools still self-describe as "STAGED, UNADOPTED"; cosmetic, behavior unchanged)

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 27, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) per the principal's ruling and judged as written. §10 treated as history binding nothing; its narrated findings are not reported here as mine. Files opened: the packet, the master, the two V26 gate reports (after the master, as the brief permits), the delivered pinned tools (r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py, r3c2_manifest.py, r3c2_build_seat_packet.py) to execute Q7's mandated fixtures, and the seat brief (a named artefact Q7(d) asks about). The interpretation protocol was checked by metadata only, never opened. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened)

Q5 (asked and answered before Q3): No sentence in the packet tells me any outcome is more consequential than any other — to the lane, to anyone. The only consequence-adjacent sentence is §11: "No tier, warrant token, standing or stamp moves." It denies consequence transfer on the named axes; it ranks nothing. §4's stop classes have procedural effects ("The census does not proceed", "No tally is filed") and §4 labels CENSUS_PARTIAL "INCONCLUSIVE", but no sentence states what any outcome does for anyone or makes one weightier than another. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3): I cannot state what the recorded origin is FOR. The packet shows checking mechanics done WITH it after recording — "Every input's `origin` is classified independently by both seats" (C3), a >10% disagreement files CENSUS_ORIGIN_DISPUTED and "The census does not proceed" (§4), and the C6 auditor "re-classifies the record from the pinned sources" (C3) — but no sentence states any analytic use: no tally, outcome, comparison or conclusion is computed from origin anywhere in the packet. §1 states the separation, not a purpose: "The reproduction verdict and the provenance fields are recorded separately." ORIGIN_PURPOSE=CANNOT_STATE.

Q3 (hypothesis-reachability): No directional hypothesis, comparison model or preferred outcome is reconstructible. Every inference I can draw, with its leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline" (C3); that is corpus content a seat must have, not a stake. (ii) An operational preference for completion — CENSUS_PARTIAL is declared "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4); only a complete census concludes anything, but REPRO_FAILED is an arithmetic outcome, so a census full of reproduction failures still concludes — no direction leaks. (iii) Failure is anticipated and pre-framed non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3, REPRO_FAILED). (iv) The rescue temptation is anticipated and armored — "A seat may not supply a value for an `ABSENT` or `BLOCKED` input." (§2). (v) A stake exists and is fenced off — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the census could otherwise be read as bearing on some tier/warrant system, but the direction of any preference is not stated. (vi) The audit audits every arithmetic-group claim and samples the rest (C6) — that shows where the lane sees risk, not what result it wants. Adversarially reconstructed stake: provenance is recorded with unusual care (seven origins, quotations, two readers, disputes carried) as if reproduction-versus-provenance structure may later be examined — but §1's "recorded separately" withholds any such model. Beyond this, nothing. Independent confirmation: my own grep of the built packet for rests_on / USES_ / severity / interpretation / "two tallies" / pattern vocabulary returned no hits (only the benign exclusion-kind and origin-taxonomy lines).

1. OUTCOME CLASSES
Exhaustive and mutually exclusive — by construction plus a stated total precedence, and I could not break either.
Per-claim: I constructed real collisions. A claim stating no procedure whose input is also absent satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT; a claim blocked on an unobtainable named source whose other input is absent satisfies both REPRO_BLOCKED and REPRO_INPUT_ABSENT. The stated order ("file the first in this order: REPRO_NO_DERIVATION_STATED, REPRO_BLOCKED, REPRO_INPUT_ABSENT, REPRO_NOT_EVALUABLE, then the arithmetic group") files each to exactly one outcome. A claim fitting none: inputs all PRINTED/STANDARD ⇒ arithmetic group; any input neither printed nor sourced ⇒ INPUT_ABSENT; sourced but not machine-matchable ⇒ BLOCKED; no procedure ⇒ NO_DERIVATION_STATED; cap/machinery ⇒ NOT_EVALUABLE. Total. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split over an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name. The eight classes partition the reachable state space; the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim (with the audit at PASS) or by an empty enumeration ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named").
Q1 (asked directly): YES. Under option (c) every declared outcome is reachable and the reproduction question stays decidable. Hardest cases, each traced to exactly one outcome and one rests_on: (a) printed-but-chosen input — "we choose β = 0.5" is an AUTHOR_SPECIFIED_INPUT exclusion as a candidate and a PRINTED/CHOSEN record as an input; arithmetic consumes it by status; the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED; rests_on USES_CHOSEN. (b) A fitted parameter the paper states — PRINTED/FITTED as an input, consumed; as a claim its recipe is the fit: an arithmetic outcome if the fit is mechanically stated, else REPRO_NO_DERIVATION_STATED or REPRO_NOT_EVALUABLE; rests_on USES_FITTED when consumed. (c) An imported value — PRINTED/IMPORTED, consumed, when the named source is an enumerable manifest text whose bytes verify and the value machine-matches as a numeric token at the first line carrying symbol and numeral (external branch), or at its own line when the claiming paper itself prints it (local branch); REPRO_BLOCKED when the source is not enumerable or does not match (precedence puts BLOCKED before ABSENT); REPRO_INPUT_ABSENT only when no source is named; rests_on follows the record's origin. (d) An unprinted measurement — ABSENT/MEASURED with the describing sentence quoted (my fixture: validate PASS); the attempt stops (a seat may not supply a value), filing REPRO_INPUT_ABSENT; the record's roots are {MEASURED}, rests_on DERIVED_STANDARD_OR_MEASURED_ONLY. One outcome, one rests_on, no reproducer choice anywhere. The four earlier rounds' tension (reachability pulling against decidability) is closed here precisely because option (c) consumes by status and records provenance beside the verdict instead of gating admissibility on it. Every §4 class also has a concrete witness: COMPLETE (all-arithmetic tally + audit PASS), PARTIAL (one non-arithmetic claim), AUDIT_FAILED (any audit mismatch, a seal-receipt failure, or a missing seed), NO_CLASS (one control failing twice in every seat), CONTROL_SPLIT (a surviving fail/pass split), DENOMINATOR_DISPUTED (enumeration or input-list disagreement surviving reconciliation — the merge exit-1 route), OUTCOME_DISPUTED (a surviving per-claim split), ORIGIN_DISPUTED (origin disagreements on inputs affecting >10% of included claims).

2. CONTROLS
Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C3 PASS exists only on the printed validate runs ("a token asserted without the printed run is FAIL"); C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate ≤10%, and no MISMATCH. The two controls that CAN be passed by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete," and the kernel-sandbox confinement paragraph states at its true strength what the profile does and does not deny. Unreached controls: "Controls in an unreached limb are NOT_RUN, never passes," and §4's stop sentence assigns NOT_RUN to later limbs.
Q2 (asked directly): YES — checkable rather than asserted, with the floor the document itself states. My wrong-but-plausible record: the paper prints "Using H0 = 67.36 km/s/Mpc from Planck 2018 we compute the age" and I file the input CHOSEN with ORIG_CHOICE_STATED and that true sentence as the quotation. validate PASSES it: the machine checks that the quotation is present at the cited line and that reason_code maps to origin, not that the code is the right one for the sentence. The mechanisms that catch it, as written: (1) the second seat's independent origin classification — a disagreement is filed ORIGIN_DISPUTED, reported with both quotations, never reconciled, and above 10% of included claims files CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification from the pinned sources — a differing origin is a per-input MISMATCH making C6_AUDIT_SAMPLE=FAIL (the kit's "D7 neg: re-classified origin differs from the sealed ledger" control demonstrates the FAIL; my own fixtures demonstrate the per-field comparison machinery firing). The machine floor that IS enforced is real: a record whose value line lies in another file but is not filed IMPORTED/ORIG_CITATION fails validate ("such a record must be IMPORTED with ORIG_CITATION"), and the every-status shared evidence check now fires before any status branch (demonstrated: ABSENT/MEASURED with an empty quotation FAILS). The stated residual floor (C3's lane-side span): a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." That is the answer, and it is stated rather than implied away. One observation, cosmetic at most: C6 says compare "compares those reconstructed fields" after listing origin evidence among what the auditor records; the tool compares origin, status, value, source coordinates and derived_from per input — not the evidence coordinates themselves. An origin MATCH with divergent evidence coordinates changes no class, no tally and no tested value (rests_on reads origins, not evidence), and wrong origins are caught as above — noted, not filed.

3. CIRCULARITY
No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule contains no pattern content (my grep of the built packet beyond the builder's assertion). Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator (observed in tool output: "author_specified_input=0"); C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded contrary claim an OMISSION that files CENSUS_AUDIT_FAILED (kit probe: a required passage omitted by both seats and retained by the auditor → FAIL, listed). The one judgement-bearing kind (AUTHOR_SPECIFIED_INPUT) is counted, printed and audited, and a mis-sort during a run is declared "a finding to bring back, not a widening"; a contrary claim asserted as the paper's own result cannot be quietly re-sorted into it — both seats must agree, and the auditor's independent inclusion of it surfaces as AUDIT_INCLUSION_DISPUTED, counted and reported (above 10% the audit fails). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the reason-code tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans. rests_on is computed by script, not seats: the lane tool REJECTS (exit 2) a ledger arriving with root_origins or rests_on set, and the audit now compares dependency edges and recomputes root chains from both sides — my graph-bypass fixture shows a single altered edge fails the audit even with identical root sets. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, and the judgement floor on reason-code choice — all stated in the document rather than implied away.

4. THE BLIND
Machine-asserted as claimed, verified end to end on this master: I re-ran the pinned builder with the printed explicit-argument command; output first line C4_PACKET_REDACTED=PASS, sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'], dropped ['0.','7.','8.','10.'], 51 inline spans redacted, and the rebuilt packet is byte-identical to the pinned packet (sha256 6da7de23… = PACKET_SHA). The builder refuses to write on any surviving forbidden string and asserts the brief too. The C4 mechanism does what it states: a redacted out-of-lane copy directory, a kernel sandbox profile (r3c2_seat_sandbox.sb, digest 6978d590… verified on disk) whose permitted surface — runtime and temporary-directory access, configuration-directory exceptions, network access — is printed in the text, plus self-report lists with the completeness disclaimer. Its limits are stated honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots … not continuous immutability". The accepted floor (a seat can tell provenance is classified with care) is the principal's ruled floor and is recorded as such in C4.

5. THE SEAL (§7)
The receipted relay binds what it claims, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one commit containing the merged candidate file, the exclusion ledger and the merged input ledger with its compute output is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and audit select refuses to run without the stage-1 seal (demonstrated in the kit). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."

6. FAIRNESS
Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the brief repeats it). My sweep of the packet's "error" vocabulary found only audit-internal uses ("may detect an error", "shared reader error"), never an outcome label. The positive outcome's name matches its mechanical tests (rounding half away from zero; |reproduced − printed| ≤ stated uncertainty, once; the asymmetric half-width rule). No class name or clause asserts more than its definition: the V26-held label is now DERIVED_STANDARD_OR_MEASURED_ONLY, which matches its membership exactly (every root origin DERIVED, STANDARD or MEASURED — verified in the lane tool's emitted output). R3C2_NO_CLASS is bounded to C0–C5b with C6/seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases. Exclusion-kind names match their definitions and the excluded numeral is retained, not discarded. One cosmetic scar on the rename's own parenthetical history — finding F1 below.

7. STALL / EXECUTABILITY
Q4 (asked directly): With two COSMETIC exceptions filed as findings, everything the document describes executed as delivered. Each thing checked, and how: /usr/bin/python3 (runs, 3.9.6); /usr/bin/shasum (runs); sympy (imports, 1.14.0); the user site path prints and r3c2_manifest.py ran live over it (FILES=20637, MANIFEST_SHA256=1b5463c1…, exit 0) — the two `-c` one-liners of C5 were run as equivalent one-line script files because my referee shell blocks `-c`, a restriction on me, not on the text; r3c2_ledger_tools.py (digest 481629a4… verified against R3C2_SEAT_PACKET.sha256 = the §3 pin; census, validate and all five audit subcommands executed in my fixtures); r3c2_lane_tools.py (digest fd91ce95… verified = the C3 and §9 pin; merge and compute executed — compute with two arguments exits 2 and writes nothing, with three emits the NOT_COMPUTED row); r3c2_batch_tools.py (digest eeafc482… verified = the C1 pin; partition, seal, join, coverage executed, including the chain-B binding and the unbound-chain refusal); r3c2_manifest.py (digest afa2dee7… verified = the C5 pin; symlinked root refused, symlink below root refused, real directory PASS, live 20,637-file run); r3c2_timeout.py (digest fbb9bef7… verified = the §9 pin; wrapper run — prints the command, child stdout/stderr and status; child exit 7 carried); r3c2_build_seat_packet.py (digest b1f2cce1… verified = the C4 pin; rebuild byte-identical to the pinned packet); r3c2_seat_sandbox.sb (digest 6978d590… verified = the C4 pin); R3C2_CORPUS_MANIFEST.md (digest 300d4da1… verified = the §2 pin); partition_12_of_89.json (digest 15b69217… matches C1's cited prefix, verified in the kit sheet); R3C2_SEAT_PACKET.sha256 (8/8 OK); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 (10/10 OK); the kit (last three lines: "deletion_probes=49", "controls=141 passed=141 failed=0", "STAGED_TESTS=PASS"); script-computed fields observed in my fixtures (rests_on, root_origins, NOT_COMPUTED, DERIVED_STANDARD_OR_MEASURED_ONLY, declared/recomputed counts, JOIN, C1B, C6 tokens, PARENTS_DISPUTED counter). R3C2_INTERPRETATION_PROTOCOL_20260904.md exists (3947 bytes, mtime 2026-09-06 23:54 — metadata only, never opened). OWNERSHIP_b<k>.txt is a custodian extract of the pinned partition, now stated in C1 (kimi N3); the dispatch record, seat reports, receipts and check sheet are run-time deliverables, not pre-existing artefacts. The two exceptions: F1 (a false historical literal in §3's parenthetical) and F2 (the delivered tools' docstrings still read "STAGED, UNADOPTED") — both COSMETIC.

FINDINGS (verbatim quote, defect, exact replacement)

F1 (COSMETIC — changes no class filed, no control's passability, no reachability, no conclusion and no tested value): §3's lane-side span mis-states the historical token.
Quote (§3): "`DERIVED_STANDARD_OR_MEASURED_ONLY` (the token `DERIVED_STANDARD_OR_MEASURED_ONLY` of V10–V26, identical membership) when every root origin is `DERIVED`, `STANDARD` or `MEASURED`"
Defect: the token of V10–V26 was `DERIVED_ONLY` — §10.22 itself says "`DERIVED_ONLY` is renamed `DERIVED_STANDARD_OR_MEASURED_ONLY` in this version", and the V26 gates quote the old definition ("`DERIVED_ONLY` when every root origin is `DERIVED`, `STANDARD` or `MEASURED`"). The parenthetical rewrites the history it cites. Membership, the emitted token and every consumer are correct; only the historical note is false.
Exact replacement: "`DERIVED_STANDARD_OR_MEASURED_ONLY` (the token `DERIVED_ONLY` of V10–V26, identical membership) when every root origin is `DERIVED`, `STANDARD` or `MEASURED`"

F2 (COSMETIC — same five tests): the delivered, pinned tools still self-describe as unadopted staging copies, so kimi's V26 N1 is not delivered in the pinned bytes although §10.22 lists "tool docstrings (kimi N1)" among the repairs.
Quotes (verified against the delivered files whose digests match both pin sheets): r3c2_lane_tools.py line 2 — "r3c2_lane_tools_STAGED.py — STAGED, UNADOPTED copy (V26cand gate F2/F4 repairs) of the LANE-SIDE tool, never given to a seat"; r3c2_batch_tools.py line 2 — "r3c2_batch_tools_STAGED.py — STAGED, UNADOPTED (batch-reading candidate, repaired 2026-09-06 after two independent reviews). LANE-SIDE."; r3c2_manifest.py line 2 — "r3c2_manifest_STAGED.py <directory> — STAGED, UNADOPTED copy (V26cand gate F9: symlinks and non-regular entries fail closed)"; r3c2_ledger_tools.py carries five internal "STAGED D7" docstrings and one "# STAGED D1" comment.
Defect: §10.20 records these tools as adopted under their pinned names and §10.22 records the docstrings as repaired; the bytes say otherwise. Behavior is unchanged — every fixture in this report ran these exact bytes and all pins verify — so this is a wrong self-description, not a functional defect, and it makes the §10.22 repair claim partly undelivered.
Exact replacement: strip the staging markers, e.g. lane tool line 2 → "r3c2_lane_tools.py — the LANE-SIDE tool, never given to a seat: merge two validated seat ledgers; compute root_origins and per-claim rests_on from the merged ledger." (and likewise for the batch and manifest tools and the ledger tool's internal "STAGED D1/D7" labels); re-pin the four tools in the master, R3C2_SEAT_PACKET.sha256 and the kit sheet; re-run the kit.

Why SOUND_WITH_REPAIRS and not UNSOUND: no finding changes a class filed, a control's passability, a reachability verdict, the blind, the seal, or any tested value; Q1 and Q2 answer YES, with my own fixtures demonstrating the V27-repaired properties (the graph bypass caught, the every-status evidence check firing on ABSENT records, the two-limb lifecycle passing end to end). Why not SOUND: Q4 and Q7(c) each found a real as-delivered discrepancy (F1, F2), both cosmetic with one-sentence fixes.

Q7 (V27) ANSWERS
(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative entries inside r3c2_staged_d1d7). The packet's line 9, "Built from master sha256 `12daf4f5aa9eb89d5411089c1d7120d153caa4f81cb3bf6549c3805bda06f62a`", equals ACCESS_SHA. The kit runs: last three lines "deletion_probes=49", "controls=141 passed=141 failed=0", "STAGED_TESTS=PASS".
Own fixtures, all against the delivered pinned tools (full commands, exits and outputs executed this session; tokens printed): GRAPH BYPASS — a sealed ledger (a←b←m) and an auditor reconstruction (a←x←m) differing ONLY in one dependency edge, with b and x both DERIVED from the same MEASURED root so the recomputed root sets are equal ({MEASURED} on both sides, printed in C6_AUDIT.json): `audit compare` exits 1, prints "FAIL: AUDIT t1.txt#c1: MISMATCH (input t1.txt#a: derived_from ['t1.txt#x'] vs sealed ['t1.txt#b'])" and "C6_AUDIT_SAMPLE=FAIL" with "study_files": "CENSUS_AUDIT_FAILED" — the edge comparison, not the roots, caught it; the identical-edge control runs the full stage sequence (seal-enumeration → select N=1 R=0 k=0 audited=1 → handout ids+file+line only → seal-rederivation → compare) to C6_AUDIT_SAMPLE=PASS. ABSENT/MEASURED — empty quotation: validate exits 1, "FAIL: t1.txt#m: empty quotation (every non-ORIG_SILENT record quotes its evidence line)", C3_NO_SUBSTITUTION=FAIL; same record with its describing sentence quoted at the cited line: C3_NO_SUBSTITUTION=PASS, exit 0. LIFECYCLE — partition (2 texts, 2 batches) → seal chain A (b1, b2 in order) → coverage C1B_BATCH_COVERAGE=PASS → join JOIN=PASS → a limb-B directory copied under the canonical names, outcomes recorded, sealed as chain B bound to the agreed limb-A seals → join (binding verified) JOIN=PASS → `census … final` on the joined limb-B files: C1_DENOMINATOR_PRINTED=PASS, exit 0; re-joining the untouched chain A afterward: JOIN=PASS (no seal overwritten); an unbound chain-B first seal is refused ("FAIL: a limb-B chain must be bound to the agreed limb-A seals file at its first seal"). COMPUTE — two arguments: exit 2, usage line, no output file written; three arguments: exit 0, "t1.txt#c1 rests_on=NOT_COMPUTED root_origins=[]" for the recordless included claim; merge + compute over a MEASURED record emits "rests_on=DERIVED_STANDARD_OR_MEASURED_ONLY" and never the historical token. MANIFEST — a symlinked root: "ERROR=…: root is a symlink or not a directory (refused)", exit 1; a symlink below the root: "ERROR=slink.txt: symlink or non-regular entry …", exit 1; the real directory: FILES=1, MANIFEST_SHA256 printed, exit 0. BUILDER — the printed explicit-argument build command: C4_PACKET_REDACTED=PASS, rebuilt packet byte-identical to the pinned packet. WRAPPER — child-ok exit 0; child exit 7 carried as exit 7. Tokens printed this session: C3_NO_SUBSTITUTION (PASS and FAIL), C6_AUDIT_SAMPLE (PASS and FAIL), C1_DENOMINATOR_PRINTED=PASS, C1B_BATCH_COVERAGE=PASS, JOIN=PASS, C4_PACKET_REDACTED=PASS, STAGED_TESTS=PASS.
(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0: Python 3.9.6; sympy 1.14.0; interpreter digest b8763cf2… printed; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages printed; r3c2_manifest.py over that path: FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0. (The two `-c` one-liners were run as equivalent one-line files; my referee shell blocks `-c` — a restriction on this referee, not on the text's executability.) NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the manifest is ONE process, run live over 20,637 files); the §9 wrapper is one invocation that prints and carries its child's exit status (demonstrated: 0 and 7); the kit's 49 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative.
(c) PRIOR_FINDINGS_CLOSED=NO — item by item over the V26 gate's findings:
codex F1 (every-status evidence check) — CLOSED: master C3 carries the shared pre-branch check for every non-ORIG_SILENT record, "PRINTED, STANDARD, ABSENT and BLOCKED alike", with "an ABSENT input keeps whatever origin its evidence supports"; the tool implements it at lines 120–128 (PROBE:EV_EMPTY_ALL / EV_LINE_POS / EV_LINE_ALL / EV_VERBATIM_ALL); my ABSENT/MEASURED fixtures FAIL on empty quotation and PASS on the quoted sentence.
codex F2 (graph bypass) — CLOSED: master C6 now records full reconstructed input records (status, value, source coordinates, origin evidence, derived_from), compares "those reconstructed fields and dependency edges", "carrying a declared origin alternative explicitly", and "recomputes each input's chain of origins to its roots from both reconstructions"; the tool compares derived_from per input (PROBE:C6_EDGES) and recomputes roots from both sides; my edge-only fixture with equal root sets FAILS the audit.
codex F3 (stale lane-tool pin + two-argument compute) — CLOSED: master C3's lane-side span pins fd91ce950b27101b… (= the delivered bytes, verified) and mandates "compute <merged.json> <out.json> <candidates.json>" with "(the candidate argument is mandatory; compute without it exits 2 and writes no output)"; §9 matches; the tool's two-argument form exits 2 with a usage line and writes nothing (my fixture), the permissive branch gone.
codex F4 (limb-B filenames versus seal) — CLOSED: master C1 puts limb B in a separate directory under the canonical names, chain B "bound to the agreed limb-A seals file", "no `_limbB` filename suffix is used", "the limb-A directory and seals remain unchanged", "`join` of the limb-B chain verifies the binding"; the brief's limb-B paragraph says the same; my end-to-end fixture passes and the unbound chain-B seal is refused.
codex F5 (batch-local validation cannot resolve cross-batch dependencies) — CLOSED: master C2 — "(per-session validation is `NOT_RUN`)" and "both seats' joined validation must PASS before limb-A agreement or arithmetic", with C1's lifecycle matching; the brief step 4 instructs "Do NOT run `validate` in this session: C2/C3 are NOT_RUN per session, because a cross-batch `derived_from` resolves only in the joined ledger"; the kit's batch controls show the joined ledger validating over the full corpus where the batch-local view cannot.
codex F6 (C5 pinned different manifest bytes) — CLOSED: master C5 pins afa2dee798a8802ff…, equal to the delivered r3c2_manifest.py on disk and to both pin sheets.
codex F7 (manifest followed a symlinked root) — CLOSED: master C5 — "the manifest command first rejects a root path that is a symlink or is not a directory, printing `ERROR=<path>` and exiting 1, then rejects every symlink or non-regular entry below that root"; my fixtures: symlinked root refused, nested symlink refused, real directory PASS.
codex C1 (local-import summary sentence) — CLOSED: master §2's machine-floor sentence now reads "For an external import, whose value line is outside the claiming file, validate checks that the evidence file is the claiming file, the external source is an exact manifest row with verified bytes, the non-empty quotation occurs at the cited claiming line, and the cited source line is the first line carrying both the symbol and the numeric value token. A locally printed import instead follows the local-import branch above." — codex's replacement; my local-import understanding is consistent with the tool's branch (gate F1 controls in the kit).
codex C2 (printed build command names the arguments) — CLOSED: master C4 prints "/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out r3c2_seat_packet/R3C2_SEAT_PACKET.md --brief r3c2_seat_packet/SEAT_BRIEF.md"; I ran its explicit-argument form: PASS, byte-identical packet.
codex C3 (DERIVED_ONLY label) — CLOSED: the token is renamed DERIVED_STANDARD_OR_MEASURED_ONLY in the master §3, the lane tool emits it (my compute fixture), the kit asserts "compute emits DERIVED_STANDARD_OR_MEASURED_ONLY and never the historical token DERIVED_ONLY", and §10.22 records the interpretation protocol updated (protocol V5; the protocol file exists — metadata only, not opened); membership unchanged, as both V26 reviewers found. The rename's own parenthetical mis-states the historical token — new cosmetic finding F1, independent of closure.
kimi F1 — CLOSED (same item and evidence as codex F3).
kimi F2 — CLOSED (same item and evidence as codex F4, brief verified).
kimi F3 — CLOSED (same item and evidence as codex F6).
kimi F4 (ABSENT records with non-SILENT codes escaped the evidence check) — CLOSED: master C3 extends the shared check to ABSENT and adds "an unprinted measurement is `MEASURED` with its describing sentence quoted — and is not forced into `UNDECLARED` to close the corner"; my two fixtures demonstrate FAIL and PASS.
kimi N1 (tool docstrings "STAGED, UNADOPTED") — NOT CLOSED: the delivered pinned tools still carry the markers verbatim (quotes in finding F2); §10.22's "tool docstrings" repair is not delivered in the pinned bytes. Cosmetic; behavior unchanged.
kimi N2 (lane tool usage showed two-argument compute) — CLOSED: the delivered usage shows "compute <merged.json> <out.json> <candidates.json>   (candidate file mandatory)"; the two-argument invocation exits 2 naming the candidate argument.
kimi N3 (ownership list's provenance) — CLOSED: master C1 — "The ownership list `OWNERSHIP_b<k>.txt` a session reads is the custodian's extract of the pinned partition, listed with its digest in the dispatch record."
(d) YES for THIS text. The brief lets a batch session execute every printed command as delivered in both limbs without a seal being overwritten: limb A — digest prints, the five C5 commands (all executed live under scope), per-batch enumeration with the two-argument census (schema-valid per batch file), ledger construction with validate expressly NOT_RUN per session and the joined three-argument validate printed for the integration directory; limb B — a separate directory under the canonical names, outcomes recorded into the copies, `census … final` printed per batch and required by C1 over the joined files. Executed end to end in my lifecycle fixture: chain A sealed in order, coverage PASS, join PASS, chain B sealed bound to the agreed limb-A seals, join PASS, final census PASS, chain A re-joining PASS afterward (seals are first-write and order-enforced; a repeated seal and an unbound chain-B seal are both refused). The only artefact a session reads that no printed command produces is OWNERSHIP_b<k>.txt, whose custodian provenance C1 now states (kimi N3).

Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence, not corpus data and not a census.

R3C2_V27_GATE_COMPLETE
