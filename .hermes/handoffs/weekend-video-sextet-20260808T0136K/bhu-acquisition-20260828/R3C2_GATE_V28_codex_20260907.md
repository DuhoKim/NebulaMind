ACCESS_SHA=c9a43610be7451aacee491d1ab3e16d4ba658dc368663f884a6da657625de206
PACKET_SHA=fc2696fa295518117961d62c336bcbcfc0d93d0c2ab8f5e5f78c7e9a16c59343
GATE=PREREG_UNSOUND
LEAK=Content-level only: cosmology and careful transitive provenance analysis; no directional hypothesis, comparison-model preference or preferred empirical result reconstructed.
CONSEQUENCE_VISIBLE=Procedural: excessive origin disagreement stops the census — “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent comparison, audit and dispute reporting — “An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled.”
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO

Design-only assessment of the unsigned V28 draft. No corpus derivation, adoption, signature, dispatch or interpretation was performed. Section 10 was read as nonbinding history; its narrated results are not my evidence. The two V27 reports were opened only after the master, for Q7. Q7's explicit execution and delivery checks were treated as permission to inspect the named kit, tools, brief and manifests beyond the initial two-file restriction. No other gate report or interpretation-protocol content was opened.

Packet-first answers, recorded before reading the master:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

Yes, procedurally. “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” Excessive origin disagreement prevents continuation. This does not identify a preferred scientific result. “No tier, warrant token, standing or stamp moves” explicitly limits institutional consequences.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?

Yes, as a checking and reporting function: “An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled.” The packet also describes independent reclassification and recomputation of origin chains. I cannot state the downstream interpretive mapping or ultimate provenance-tally rule from the packet.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

The packet makes the following limited inferences possible:

- “The reproduction verdict and the provenance fields are recorded separately.” Provenance is an independently important analytical dimension; later consideration alongside reproduction is plausible, but no particular mapping follows.
- “**Provenance is transitive.** Every `DERIVED` record lists its `derived_from` ids”. The design is interested in the ancestry of inputs, including chosen, fitted, imported, measured and derived inputs. This does not reveal a desired distribution.
- “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.” The subject includes cosmology. It does not establish Planck as a comparison model for the lane's hypothesis.
- “Distinct from a claim whose inputs the paper DOES state, chosen or not” anticipates chosen-input cases. It gives their procedural treatment, not their hoped-for frequency.
- “No tier, warrant token, standing or stamp moves.” There is an institutional vocabulary of consequences, explicitly fenced off here; no beneficiary or preferred empirical result is identified.

I could not reconstruct a directional hypothesis, comparison-model preference or predicted pattern. These packet-only answers have not been retrospectively replaced by knowledge from the master.

## 1. OUTCOME CLASSES

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

YES for the settled outcome definition and bounded operational question. NO claim is made that arbitrary mathematics becomes decidable. The explicit incomplete-attempt outcomes make the census fileable without supplying missing inputs. The executable controls still have defects described below; those do not require reopening option (c).

Concrete physical claim constructions, not purported findings about an unopened corpus paper:

| Stated claim and recipe | Unique outcome | Unique rests_on |
|---|---|---|
| Choose a = 3 m/s²; state t = 2 s as chosen; v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same inputs and recipe, but print v = 7 m/s | REPRO_FAILED | USES_CHOSEN |
| State fitted a = 3 m/s² and measured t = 2 s; v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| Locally print “we adopt a = 3 from X”; measured t = 2 s; v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Instead obtain a from the first symbol/value line in a verified enumerable named source | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Name an unpinned source for a without printing its value | REPRO_BLOCKED | USES_IMPORTED |
| Need a but neither print it nor name its source; origin is silent | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| Fully specified computation over printed measured inputs exceeds the cap, or requires unavailable machinery | REPRO_NOT_EVALUABLE | DERIVED_STANDARD_OR_MEASURED_ONLY |
| “Our result is 6 m/s,” with no producing procedure and no input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

Hardest combined case: v = at + v0 = 17 m/s, with printed chosen a = 3 m/s², printed fitted t = 4 s and imported v0 = 5 m/s satisfying the import rule. Mechanical consumption gives exactly REPRO_WITHIN_STATED_PRECISION and USES_IMPORTED. Printing 18 instead gives REPRO_FAILED and the same rests_on. A chosen input's own numeral can be excluded as AUTHOR_SPECIFIED_INPUT while remaining an input to an included result. No reproducer choice is needed.

A claim needing both a blocked input and an absent input satisfies two raw conditions, but precedence files only REPRO_BLOCKED. A named but unspecified procedure files NO_DERIVATION_STATED before machinery limitations. I found no double filing after the stated precedence and no uncovered claim among these hard cases.

Study-level witnesses, with other reached controls passing:

| Concrete state | Unique filed class |
|---|---|
| One arithmetic claim, successful audit | CENSUS_COMPLETE |
| One successfully audited NO_DERIVATION_STATED claim | CENSUS_PARTIAL / INCONCLUSIVE |
| Audit mismatch, absent external seed or receipt-verification failure | CENSUS_AUDIT_FAILED |
| Repeated common prerequisite-control failure | R3C2_NO_CLASS |
| Persistent inclusion or input-list disagreement | CENSUS_DENOMINATOR_DISPUTED |
| Persistent outcome disagreement | CENSUS_OUTCOME_DISPUTED |
| Origin disagreements affecting 2 of 10 claims | CENSUS_ORIGIN_DISPUTED |
| A prerequisite control still fails in one seat and passes in the other | CENSUS_CONTROL_SPLIT |

A non-arithmetic tally whose audit fails satisfies PARTIAL and AUDIT_FAILED predicates, but files only AUDIT_FAILED. An entirely empty enumeration on both sides can file PARTIAL; a zero included denominator with candidate passages instead triggers C6's explicit zero-denominator failure. INCONCLUSIVE is genuinely reachable. I found no unclassified completed tally under the stated precedence.

## 2. CONTROLS

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

It is evidence-checkable, with an explicitly acknowledged semantic judgment floor. It is not mechanically guaranteed correct. My wrong-but-plausible record was:

```json
{"claim_id":"p.txt#c","input_id":"p.txt#a","symbol":"a","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"p.txt","source_line":3,"verbatim":"We adopt a = 3 from X."},"derived_from":[],"value":"3","source_file":"p.txt","source_line":3}
```

The quoted sentence exists in my fixture. `validate` returns C3_NO_SUBSTITUTION=PASS, exit 0: it checks quotation occurrence and code/origin pairing, not the meaning of “from X.” Citation precedence requires IMPORTED. Independently correcting the second seat to IMPORTED produces `origin disagreements=1`, and merge/compute prints `rests_on=['USES_CHOSEN', 'USES_IMPORTED']`, DISPUTED. An auditor's IMPORTED reconstruction against an undisputed CHOSEN seal returns MISMATCH and C6_AUDIT_SAMPLE=FAIL, exit 1. If all readers make the same semantic mistake, no mechanism catches it; the text honestly acknowledges that floor.

Every control has a named three-valued contract: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. C0 requires an exhibition and independent verification; C1–C3 require printed script artefacts; C5 requires live outputs; C5C requires session-identified provider logs; C6 requires its printed JSON and seals. Bare PASS tokens do not satisfy those contracts. C4/C5b can be satisfied by an asserted path list, and expressly do not establish its completeness. Unreached controls are NOT_RUN, including validation before batch integration.

The following are additional executable defects, not the acknowledged reader-error floor.

**F1 — NON-COSMETIC: reconstructed dependency-closure records outside the selected claim set are not compared.**

Verbatim sentence, §3:

> During the C6 comparison an auditor classification is bound to the matching declared sealed origin, evidence and parent-list branch; roots are recomputed under that branch and compared like with like; both sealed branches and the auditor's matching branch are reported.

The governing C6 requirement also says, verbatim:

> compares every field, every evidence coordinate and quotation, and every dependency edge against the sealed ledger

Counterexample: selected claim p.txt#c consumes derived d whose parent a belongs to p.txt#other. The other claim is included but not selected by the recomputed seed. The auditor reconstructs a under that claim's own key, so it is present in the auditor's graph. Change only a's evidence quotation to “Fabricated dependency evidence.” All schema fields and graph edges remain present. Seal exits 0; compare returns C6_AUDIT_SAMPLE=PASS, exit 0. Changing that dependency record's value to 999 also returns PASS. The fixture has 32 included claims; p.txt#other is demonstrably absent from `audited_ids`.

The code builds `a_graph` from every reconstruction, but performs record comparisons only inside the loop over `audited_ids`. Thus it uses an independently supplied closure record without checking its fields against the seal. Missing dependencies are now caught; present but altered off-sample dependencies are not.

Exact replacement for the quoted §3 sentence:

> During C6 comparison, first validate and compare every reconstructed input in the complete dependency closure of each selected claim, including records assigned to claims outside audited_ids. Bind each record to its sealed input_id and claim_id, compare every required field, evidence coordinate, quotation and dependency edge, and report any unsupported difference as MISMATCH on every selected claim that depends on it. Perform branch matching over this full closure before recomputing roots. Report both sealed branches and the auditor's matching branch.

Implement this contract in `audit compare` and re-pin. Wording alone cannot repair the false PASS.

**F2 — NON-COSMETIC: declared alternatives are handled only for a local origin change, not for parent-only or inherited disputes.**

Verbatim sentences, §3:

> A claim with a disputed input or parent list carries both script-computed classifications and is marked `DISPUTED`.

> Matching an explicitly declared alternative is not `MISMATCH`; an unsupported classification or graph difference remains `MISMATCH`.

Two independently authored counterexamples:

1. Both seats classify d as DERIVED. The sealed record has `derived_from=[a]`, `derived_from_alt=[b]`, `PARENTS_DISPUTED=true`. The auditor reconstructs the declared alternative `[b]`. Compare returns MISMATCH: `derived_from ['p.txt#b'] vs sealed ['p.txt#a']`. The branch flag is set only when `origin` differs and matches `origin_alt`, so an origin-identical parent-list alternative cannot be accepted.
2. Claim c depends on a record in claim other. That record has primary CHOSEN and declared alternative FITTED; the auditor exactly matches FITTED and its sealed alternative evidence. Comparison accepts the record in other but reports c as MISMATCH: `root_origins differ: audit ['FITTED'] vs sealed ['CHOSEN']`. The matched branch map is reset for each claim instead of being applied through its dependency closure.

These fixtures include 30 additional non-arithmetic claims. One affected claim out of 31, or two out of 32, is below the 10% stop threshold. The failure cannot be dismissed as an already-stopped origin dispute. The requested simple local CHOSEN→FITTED alternative does PASS; the broader contract does not.

Exact replacement for the second quoted sentence:

> Matching an explicitly declared alternative is not MISMATCH. Match the auditor's origin, origin evidence and parent list against the complete declared primary or alternative record, including an alternative parent list when origin is unchanged. Construct one consistent matched-branch graph over the selected claims and their complete dependency closure, and use that graph for every dependent claim's sealed-root comparison. An unsupported record, evidence, parent list or inconsistent branch remains MISMATCH; report both sealed branches and the matching branch without reconciling the original dispute.

Implement and re-pin; retain dispute reporting.

**F3 — NON-COSMETIC: STANDARD can bypass the external-source import requirements.**

Verbatim sentence, §2:

> **`STANDARD` applies only when the value appears in the claiming paper (or in a pinned enumerable text under the `IMPORTED` rule below): a value the paper does not print is classified by the named-source rule alone and is never `STANDARD`.

My record has `status=STANDARD`, `symbol=H0`, `value=67.36`, `origin=UNDECLARED`, valid ORIG_SILENT search fields, and value coordinates q.txt:2. Its candidate belongs to p.txt. The value occurs at q.txt:2, but q.txt has no verified manifest row and the record carries no import citation. `validate` returns C3_NO_SUBSTITUTION=PASS, exit 0. STANDARD checks coordinates and numeric membership, but only the PRINTED branch binds the value file to the claiming file and enforces the external import conditions. This is a machine-checkable file/provenance bypass, independent of whether a quotation semantically supports a classification.

Exact replacement:

> STANDARD applies to a closed-list value printed in the claiming paper. For every STANDARD record, validate binds claim_id to the claiming file through the candidate file and checks its own positive value coordinates and numeric token. A value outside the claiming file follows the named-source rule and must be recorded as PRINTED/IMPORTED with ORIG_CITATION, a verified enumerable source and the required first symbol/value line, even when its value belongs to the closed list. Missing candidate binding or a STANDARD value line outside the claiming file fails validate.

Implement the same rule in §2 and the validator, then re-pin. This changes control passability; it is not cosmetic. It leaves mechanical consumption of admissible closed-list values intact.

## 3. CIRCULARITY

The operational inclusion rule supplies no directional hypothesis. A contrary numeral asserted as the paper's own result must enter the candidate ledger. An author-specified input is excluded as a standalone result, retained with its numeral and coordinates, and still available to an included claim's recipe.

Independent seat enumeration, retained exclusions, zero-tolerance inclusion reconciliation and the auditor's complete independent enumeration make unilateral quiet exclusion detectable. They do not exclude shared reader error. An omitted passage and a differently classified listed passage have different audit treatment; disputes at or below the declared threshold are reported, not necessarily fatal. It would overstate the design to say every mis-exclusion is guaranteed to fail C6.

Receipt P fixes the interpretation protocol before limb A, so the intended evidence-to-interpretation separation has a custody mechanism. F1 and F3 nevertheless let unsupported evidence or inputs survive printed PASS artefacts. They weaken the claimed barrier between classification judgments and accepted evidence. No intentional manipulation is inferred.

## 4. THE BLIND

The builder was read and executed with the current master, a separate output path and the delivered brief. It returned C4_PACKET_REDACTED=PASS, removed 51 marked spans, retained §§1–6, 9 and 11, and produced a byte-identical packet. The packet's “Built from master” digest equals ACCESS_SHA. The forbidden-list assertion is real and checks packet and brief; it is a lexical assertion, not proof that all inferable stakes disappear.

The packet-only inferences above stay within the operative accepted taxonomy/procedural floor. No directional preference was reconstructed. C4 describes an outside-lane dispatch directory, recorded inventory, pinned sandbox and live positive/negative probes. I read and hashed the sandbox profile; it permits runtime/configuration access and network access, as the current text acknowledges. C4/C5b themselves remain self-report. Neither their PASS nor the manifest proves a complete access history, continuous immutability or absence of prior exposure. No actual dispatch confinement or denied-read probe is certified by this review.

## 5. THE SEAL

Section 7 binds the intended artefacts at the required times: receipt P fixes the protocol hash and commit before limb A; receipt T fixes the tally commit and named files before protocol opening. The external seed follows receipt of the tally. Independent rehashing and verification against both receipts detects substitution; missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

This is a binding of recorded artefacts and chronology, not proof that the lane owner could not anticipate the answer. The interpretation-protocol path exists, checked only by metadata: 3,947 bytes. Its contents and any actual P/T receipts were not opened or certified. The batch chains provide additional tamper evidence, not a substitute for external receipts. No new seal-design defect was established.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The delivered brief applies that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NO_DERIVATION_STATED and NOT_EVALUABLE describe this reproduction attempt's limitations, not proof that a paper is wrong. The positive outcome name accurately states its precision test. References to shared reader error are not negative labels for papers.

DERIVED_STANDARD_OR_MEASURED_ONLY accurately names its membership. The operative historical parenthesis now reads “formerly `DERIVED_ONLY`, with identical membership”; the V27 parenthesis finding is closed. No additional class-name overclaim was found.

## 7. STALL / EXECUTABILITY

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

YES: the full audit comparison and declared-alternative behavior cannot be obtained as promised (F1–F2), and the full input-source validation contract is not enforced (F3). The requested ordinary fixtures and harness run, but those results do not establish the stronger universal contracts. No missing pre-existing operative artefact was established by the inventory checks below. Future ownership extracts, reports, receipts, provider logs and dispatch records are deliverables required before their respective stages, not files claimed to have already been produced.

Q7(a), pin verification from the working directory:

```text
shasum -a 256 -c R3C2_SEAT_PACKET.sha256
8 entries: OK; exit 0
shasum -a 256 -c r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256
10 entries: OK; exit 0
PACKET_BUILT_FROM_ACCESS_SHA=YES
REBUILT_PACKET_BYTE_IDENTICAL=YES
```

The kit was executed with `/usr/bin/python3 -E r3c2_staged_tests.py` from r3c2_staged_d1d7. Exit 0. Last three lines:

```text
deletion_probes=54
controls=156 passed=156 failed=0
STAGED_TESTS=PASS
```

My own fixtures use the delivered tool, not a modified implementation. Reproduction: `/usr/bin/python3 -E _v28_codex_fixtures/followup.py`. Complete command arrays, working directories, stdout, stderr and exit codes are retained in `_v28_codex_fixtures/transcript.json`; fixtures and comparison JSON remain beside it. `tokens.txt` contains the concise output.

| Own fixture | Actual token / exit |
|---|---|
| Complete baseline | C6_AUDIT_SAMPLE=PASS / 0 |
| Missing symbol at reconstruction seal | Refused, exit 1; no seal written |
| Same incomplete reconstruction at compare | MISMATCH; C6_AUDIT_SAMPLE=FAIL / 1 |
| Complete selected-input reconstruction with fabricated evidence quotation | MISMATCH; C6_AUDIT_SAMPLE=FAIL / 1 |
| Cross-claim dependency not reconstructed | MISMATCH; C6_AUDIT_SAMPLE=FAIL / 1 |
| Local auditor classification exactly equal to sealed origin alternative | C6_AUDIT_SAMPLE=PASS / 0 |
| STANDARD + ORIG_SILENT without value coordinates | C3_NO_SUBSTITUTION=FAIL / 1 |
| BLOCKED naming quotation from another file | C3_NO_SUBSTITUTION=FAIL / 1 |
| Ordinary PRINTED evidence on line n, value on n+1 | C3_NO_SUBSTITUTION=PASS / 0 |
| Declared parent-only alternative | MISMATCH; C6_AUDIT_SAMPLE=FAIL / 1 — F2 |
| Accepted alternative inherited across claims | MISMATCH; C6_AUDIT_SAMPLE=FAIL / 1 — F2 |
| Reconstructed off-sample dependency with fabricated quotation | C6_AUDIT_SAMPLE=PASS / 0 — F1 |
| Reconstructed off-sample dependency with changed value | C6_AUDIT_SAMPLE=PASS / 0 — F1 |
| STANDARD value in an unverified external file without import classification | C3_NO_SUBSTITUTION=PASS / 0 — F3 |

For the incomplete reconstruction's direct compare test only, I supplied a deliberately constructed matching digest seal after the seal command refused. This tests compare's independent rejection; it does not claim the bad reconstruction can pass the legitimate sealing workflow. The missing dependency can pass schema sealing, but compare correctly refuses it. Schema completeness and dependency completeness are different checks.

C5 live outputs:

```text
/usr/bin/python3 -E --version
Python 3.9.6
/usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"
1.14.0
/usr/bin/shasum -a 256 /usr/bin/python3
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3
/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"
/Users/duhokim/Library/Python/3.9/lib/python/site-packages
/usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
```

The harness completed successfully. C4's mandated-command and pinned-environment clauses license these accesses. C5_EXECUTABLE_UNDER_SCOPE=YES concerns this text's executability, not a completed study-control PASS against a future dispatch record. No mandated shell pipeline was found. A child exiting 7 under the §9 wrapper printed WRAPPER_EXIT=7 and returned 7; a 0.05-second test deadline printed SYMBOLIC_TIMEOUT and returned 124 at 0.054 seconds. The short deadline tests the wrapper, not an amended study cap. NO_MASKED_STAGE=YES.

Delivery and interface checks:

| Artefact / interface | Check performed and limit |
|---|---|
| Packet, brief, ledger/lane/batch/manifest/builder/timeout tools | Eight-entry pin verification; relevant source inspection; rebuilt packet identical. |
| Staged tools, test driver, README, partition JSON/text, counterexample exhibit | Ten-entry pin verification; kit executed. All five staged Python implementation files are byte-identical to their delivered counterparts. |
| census and census final | Executed by own fixtures and kit; declared/recomputed counts printed; own final census PASS. |
| validate, with candidate file and resolved sources directory | Own positive/negative fixtures; printed tokens above. |
| merge and compute with mandatory candidates | Own disagreement fixture produced a DISPUTED pair and root_origins; old compute arity exited 2. Kit also exercises coverage and NOT_COMPUTED. |
| audit seal-enumeration, select, handout, seal-rederivation, compare | All five interfaces executed in own fixtures; selected ids and comparison/root output inspected. |
| batch partition, seal, join, coverage; A/B-chain binding | Exercised by the passing delivered kit, whose batch tool is byte-identical to the delivered tool; interface source inspected. This is synthetic execution, not certification of future batch custody. |
| manifest and timeout | Live C5 directory walk; kit manifest refusal tests; own wrapper child-failure and timeout tests. |
| builder with explicit master/out/brief | Executed to a separate review output; C4_PACKET_REDACTED=PASS and byte identity established. |
| R3C2_CORPUS_MANIFEST.md | Hash verified: 300d4da144d96ae9f1390c9018e919ae1ba6cf00be9f45ad36fdccfdcfbf9b24. All 89 enumerable paths exist in its named source directory, checked by metadata only; corpus contents were not audited. |
| partition_12_of_89.json | Hash verified: 15b69217a35dbc6fb8f47057cefbb827e3ed0710ecb04f2c71d3e68444b97b06, matching C1's prefix. |
| r3c2_seat_sandbox.sb | Read and hash-verified: 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6. No live study dispatch certified. |
| Interpretation protocol | Existence/size only; content not opened. |
| OWNERSHIP_b<k>.txt, canonical batch JSON/report paths, integration directory, receipts and check sheet | Their generation and custody are assigned by the operative text/brief. No placeholder was treated as an already-existing literal filename. |

**C1 — COSMETIC: V27's tool-documentation repair is only partly delivered.**

The lane and batch opening descriptions now name V27 tools, as requested. The delivered manifest's opening sentence still reads verbatim:

> r3c2_manifest_STAGED.py <directory> — STAGED, UNADOPTED copy (V26cand gate F9: symlinks and non-regular entries fail closed) — the pinned-environment manifest as ONE process (codex V24e D3: a shell pipeline can mask an
> upstream failure).

Exact replacement:

> r3c2_manifest.py <directory> — the V28 pinned-environment manifest, executed as one process and refusing symlinks and non-regular entries.

The seat-tool usage still contains this exact line:

>   /usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir>

Exact replacement:

>   /usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir> <candidates.json>

The internal audit docstring still says, verbatim:

> STAGED D7 stage 1 (custodian): census-gated, first-write seal of the auditor's OWN enumeration, before any sealed ledger is revealed.

Exact replacement:

> C6 stage 1 (custodian): census-gated, first-write seal of the auditor's OWN enumeration, before any sealed ledger is revealed.

Apply the same exact textual substitution `STAGED D7` → `C6` to the other audit docstrings and `# STAGED D1` → `# Imported-value validation` to the remaining comment; re-pin changed files. These documentation corrections change no filed class, control passability, reachability, conclusion or tested value. The operative master/brief already give the correct validate command. This cosmetic item does not cause the UNSOUND verdict.

Q7(c), closure of the actual V27 findings:

| V27 finding | Status and evidence |
|---|---|
| codex F1: reconstruction completeness, evidence and missing dependency | PARTLY. Missing-field seal refusal, compare rejection, selected-record fabricated quotation and absent dependency tests now work. Present off-sample dependency records still bypass comparison: current F1. |
| codex F2: declared alternative | PARTLY. The requested local origin alternative PASSes. Parent-only alternatives and inherited alternatives still fail: current F2. |
| codex F3: STANDARD coordinates and BLOCKED claiming-file binding | CLOSED for both cited failures: own fixtures FAIL correctly. Current F3 is an additional external-STANDARD bypass. |
| codex F4: separate PRINTED evidence/value lines | CLOSED: own split-line fixture PASSes. |
| codex C1: historical parenthesis | CLOSED: operative §3 now says formerly DERIVED_ONLY. |
| codex C2: tool descriptions and usage | PARTLY: lane/batch descriptions fixed; manifest staging label, ledger usage and internal staging labels remain. |
| kimi F1: historical parenthesis | CLOSED: same operative-text evidence. |
| kimi F2: delivered-tool staging descriptions | PARTLY: lane/batch openings fixed; manifest and ledger remnants remain. |

Q7(d): the new executable counterexamples are F1's present-but-altered off-sample closure records, F2's parent-only and cross-claim alternative paths, and F3's external STANDARD bypass. Each changes control passability; F1 and F3 permit false PASS, and F2 permits false FAIL below the dispute threshold. These are sufficient for PREREG_UNSOUND despite the passing kit and requested ordinary fixtures. The master and packet hashes were rechecked unchanged at the end of testing.

R3C2_V28_GATE_COMPLETE
