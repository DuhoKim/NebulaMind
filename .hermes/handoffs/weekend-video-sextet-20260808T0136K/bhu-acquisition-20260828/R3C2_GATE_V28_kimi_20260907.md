ACCESS_SHA=c9a43610be7451aacee491d1ab3e16d4ba658dc368663f884a6da657625de206
PACKET_SHA=fc2696fa295518117961d62c336bcbcfc0d93d0c2ab8f5e5f78c7e9a16c59343
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology (C3's "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline"); failure outcomes are anticipated and pre-worded (§3's "unreproduced from the stated inputs," not "error"); the author-specified-input count is a reported output ("census prints the AUTHOR_SPECIFIED_INPUT count as its own line beside the denominator"); §11's "No tier, warrant token, standing or stamp moves" shows a stake exists and is fenced off — no hypothesis, comparison model, preferred outcome or pattern inferable
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE (checking mechanics done WITH it are visible — "Every input's `origin` is classified independently by both seats", the auditor "re-classifies the record from the pinned sources", ">10% of included claims, CENSUS_ORIGIN_DISPUTED" — but no packet sentence states any analytic use: no tally, comparison or conclusion is computed from origin; §1 states the separation, not a purpose: "The reproduction verdict and the provenance fields are recorded separately.")
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO (6 of 8 CLOSED with my own fixtures; codex C2 and kimi F2 PARTLY — the lane and batch tools now self-describe as V27 tools as ordered, but the manifest tool docstring and the seat tool's STAGED labels/usage line remain; cosmetic, evidence below)

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 28, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Files opened: the packet (first, answered before the master), the master, the two V27 gate reports (after the master, as the brief permits), the delivered pinned tools (r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py, r3c2_manifest.py, r3c2_build_seat_packet.py), the kit r3c2_staged_d1d7/, both pin sheets, the corpus manifest, and the seat brief (a pinned, named artefact Q7(d) asks about). The interpretation protocol was checked by metadata only, never opened; corpus texts were byte-verified against the manifest by hash, never read. All fixtures below are synthetic referee evidence under _tmp_gate_v28_kimi/ (driver _tmp_gate_v28_kimi_driver.py), not corpus data and not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed 2026-09-07 before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER? No. No sentence does. The consequence-adjacent sentences are: §11 "No tier, warrant token, standing or stamp moves." — it denies consequence transfer on the named axes and ranks nothing; §4 "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`." — a filing precedence, not a weight; the stop classes' "The census does not proceed" and "No tally is filed" — procedural effects attached to failure states, not to any preferred result. No sentence states what any outcome does for the lane or for anyone. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR? I cannot state what it is FOR. What the study DOES with it after recording is visible only as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); "the auditor re-classifies the record from the pinned sources" (C3) and compare reports MATCH/MISMATCH "per re-classified input origin" (C6); "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4). But no sentence states any analytic use — no tally, outcome, comparison or conclusion is computed from `origin` anywhere in the packet. §1's "The reproduction verdict and the provenance fields are recorded separately." states a separation, not a purpose. ORIGIN_PURPOSE=CANNOT_STATE.

Q3 (hypothesis-reachability). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? No directional hypothesis, comparison model, preferred outcome or "pattern" is reconstructible. Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); corpus content a seat must have, not a stake. (ii) Reproduction failures are anticipated and pre-worded non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3, REPRO_FAILED). (iii) The chosen/set-versus-derived composition of the corpus is a wanted, reported output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); you do not pre-commit a printed statistic you do not intend to use. (iv) The chosen-input case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3) and "A seat may not supply a value for an `ABSENT` or `BLOCKED` input." (§2). (v) The reason-code tie-break disclaims a reading before any reader makes it — "the order is a tie-break by the specificity of the evidence, not a ranking of the values" (C3); someone cared that CHOSEN not read as worse than DERIVED. (vi) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the census could otherwise be read as bearing on some tier/warrant system, but the direction of any preference is not stated. (vii) Completion matters for concluding anything — CENSUS_PARTIAL is declared "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4) — but REPRO_FAILED is an arithmetic outcome, so a census full of failures still concludes; no direction leaks. Adversarial reconstruction of the stake, stated as far as the packet supports and no further: provenance is recorded with unusual care (seven origins, machine-matched quotations, two independent readers, disputes carried and never reconciled, an audit that re-classifies from the sources) as if a reproduction-versus-provenance structure of the corpus may later be examined — but §1's "recorded separately" withholds any such model, and I cannot say which provenance composition the lane would rather find. Beyond this, nothing: no expected percentages, no comparison model, no preferred verdict. This is the content-level floor the principal's ruling accepts; LEAK is reported at exactly that strength.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose 120-second cap is exceeded while an input is also absent files REPRO_INPUT_ABSENT first (the attempt never reaches the cap, but even taken as co-occurring the order decides). A real claim fitting none: none exists — a claim either states a procedure or not; its inputs are printed/standard (arithmetic group), named-but-unmachineable (BLOCKED), or untraced (ABSENT); the attempt completes or files NOT_EVALUABLE. Total. Study-level collisions: an audit failure over a blocked claim satisfies CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."). The eight classes partition the reachable state space; the stated order ("`R3C2_NO_CLASS`, `CENSUS_CONTROL_SPLIT`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_OUTCOME_DISPUTED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_AUDIT_FAILED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`") makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration.

Q1 (asked directly): YES — under option (c) §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on, several executed in my fixtures: (a) a printed-but-chosen input — as a candidate the numeral is an AUTHOR_SPECIFIED_INPUT exclusion; as an input it is a PRINTED/CHOSEN record; the arithmetic consumes it by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"); the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED; rests_on USES_CHOSEN. (b) a fitted parameter the paper states — PRINTED/FITTED, consumed; rests_on USES_FITTED. (c) an imported value — my q1 fixture: an external import (value line in another manifest text) validates PASS under the external branch (bytes verified against the manifest row, numeric token, first line carrying symbol and numeral); a locally printed citation validates PASS under the local branch; consumed; rests_on USES_IMPORTED; a named source that is unpinned or non-matching files REPRO_BLOCKED (recorded BLOCKED, never consumed — my F6 fixtures show the binding checks firing); an input traced to no source files REPRO_INPUT_ABSENT. (d) an unprinted measurement — ABSENT/MEASURED with its describing sentence quoted; the attempt stops ("A seat may not supply a value for an `ABSENT` or `BLOCKED` input"), filing REPRO_INPUT_ABSENT; rests_on is computed from the remaining records. (e) the combined case — my lane fixture: chosen + fitted + imported (external) + standard + a DERIVED record over chosen and standard parents: validate PASS for both seats, merge exit 0, compute exit 0 printing root_origins=['CHOSEN','FITTED','IMPORTED','STANDARD'] and rests_on=USES_IMPORTED for the claim, NOT_COMPUTED for a recordless included claim, and DERIVED_STANDARD_OR_MEASURED_ONLY on a clean ledger. One outcome, one rests_on, no reproducer choice anywhere: the seats never write rests_on (compute REJECTS a ledger arriving with it — exit 2, demonstrated), the script computes it. The four earlier rounds' tension is closed precisely because option (c) consumes by status and records provenance beside the verdict instead of gating admissibility on it. Every §4 class also has a concrete witness (COMPLETE: all-arithmetic tally + audit PASS; PARTIAL: one non-arithmetic claim; AUDIT_FAILED: any MISMATCH, missing seed, or receipt failure; NO_CLASS: a control failing in every seat after two attempts; DENOMINATOR/OUTCOME/ORIGIN_DISPUTED: the three carried disagreements past their reconciliations; CONTROL_SPLIT: a surviving fail/pass split).

## 2. CONTROLS

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts (observed: "recomputed: ... reconciled=YES", "C1_DENOMINATOR_PRINTED=PASS"); C3 PASS only on printed validate runs — "a token asserted without the printed run is FAIL"; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate at or below 10%, and no MISMATCH. The two controls that can be passed by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete," and the confinement paragraph states at its true strength what the sandbox profile does and does not deny. Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes," plus §4's stop sentence and C2's per-session NOT_RUN.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with the floor the document itself states. My wrong-but-plausible record: q1.txt line 5 prints "We take the bias b = 1.7 from Q (2021)." and seat B files the input CHOSEN with ORIG_CHOICE_STATED and that true sentence as its quotation. `validate` returns C3_NO_SUBSTITUTION=PASS, exit 0 — the machine checks that the quotation occurs at the cited line and that reason_code maps to origin, not that the code is the right one for the sentence. The mechanisms that catch it, as written, each demonstrated this session: (1) the second seat's independent classification — seat A files IMPORTED for the same record; `merge` prints "merged 6 records; origin disagreements=1", the merged record carries origin_alt, and `compute` prints the claim "rests_on=['USES_IMPORTED', 'USES_FITTED'] ... DISPUTED"; above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification — my F2 fixture shows a one-word change in origin_evidence.verbatim is MISMATCH at compare, and F4-neg shows an auditor origin matching neither sealed branch is MISMATCH. The machine floor that IS enforced is also real: my first seat-B variant — the same citation misfiled CHOSEN with its value line in another file — FAILS validate ("such a record must be IMPORTED with ORIG_CITATION"). The stated residual floor (C3's lane-side span): a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." That is the answer, and it is stated rather than implied away.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) contains no pattern content; I read the built packet in full and found none either. Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator (observed in census output: "author_specified_input=0"); a contrary claim asserted as the paper's own result cannot be quietly re-sorted — both seats must agree, and C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded passage an OMISSION that files CENSUS_AUDIT_FAILED (the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt emits exactly these cases: both seats omit a required passage; its reverse; a passage the auditor excluded that the seats never enumerated; disputes at 10% and 15%). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans. rests_on is computed by script, not seats: the lane tool REJECTS (exit 2) a ledger arriving with root_origins or rests_on set (demonstrated), and the audit compares every field, evidence coordinate, quotation and dependency edge and recomputes roots from the auditor's own graph (my F3: a missing cross-claim parent is MISMATCH, never borrowed). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — all stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master: I re-ran the pinned builder with the printed command (`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out r3c2_seat_packet/R3C2_SEAT_PACKET.md --brief r3c2_seat_packet/SEAT_BRIEF.md`); first line of output C4_PACKET_REDACTED=PASS, then "master sha256 c9a43610…" (= ACCESS_SHA) and "seat packet sha256 fc2696fa…" (= PACKET_SHA, byte-identical to the pinned packet), "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The builder refuses to write on any surviving forbidden string and asserts the brief too. The C4 mechanism does what it states: a redacted out-of-lane copy directory, a kernel sandbox profile (r3c2_seat_sandbox.sb, digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface is printed in the text, plus self-report lists with the completeness disclaimer. Its limits are stated honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is the principal's ruled floor and is recorded as such in C4. The packet's line 9 names the master it was built from; that digest equals ACCESS_SHA (grep-verified).

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one commit containing the merged candidate file, the exclusion ledger and the merged input ledger with its compute output is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (the tool's PROBE:C6_SELECT_NEEDS_SEAL; my selections ran only with the seal present). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind."

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the brief's line 43 repeats it). The packet's "error" vocabulary is audit-internal only ("may detect an error", "shared reader error"), never an outcome label. The positive outcome's name matches its mechanical tests (rounding half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY matches its membership exactly — observed emitted by the lane tool on a ledger whose only root is STANDARD ("rests_on=DERIVED_STANDARD_OR_MEASURED_ONLY root_origins=['STANDARD']"), and the kit asserts the historical token is never emitted. R3C2_NO_CLASS is bounded to C0–C5b with C6/seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases. The exclusion-kind names match their definitions and the excluded numeral is retained, not discarded.

## 7. STALL / EXECUTABILITY

Q4 (asked directly): with the five COSMETIC exceptions filed as findings N1–N5 below, everything the document describes executed as delivered. Each thing checked, and how: /usr/bin/python3 (runs, 3.9.6); /usr/bin/shasum (runs); sympy (imports, 1.14.0); the user site path prints and r3c2_manifest.py ran live over it (FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0) — C5's two `-c` one-liners were run as equivalent one-line script files because my referee shell blocks `-c`, a restriction on me, not on the text; r3c2_ledger_tools.py (digest f19303d1… verified = the §3 pin and both pin sheets; census two-argument and final, validate three-argument, and all five audit subcommands executed in my fixtures); r3c2_lane_tools.py (digest 853d5576… verified = the C3 and §9 pins; merge and compute executed — merge carries origin disagreements, compute with two arguments exits 2 with a usage line and writes nothing, with three emits the NOT_COMPUTED row, a planted rests_on is REJECTED at exit 2); r3c2_batch_tools.py (digest 3822f12c… verified = the C1 pin; `partition R3C2_CORPUS_MANIFEST.md 12` reproduces the pinned partition byte-identically, digest 15b69217a35dbc6fb8f47057cefbb827e3ed0710ecb04f2c71d3e68444b97b06 = C1's cited prefix and the kit pin; the kit's 156 controls exercise seal/join/coverage including the negative and deletion paths); r3c2_manifest.py (digest afa2dee7… verified = the C5 pin; the live 20,637-file run above); r3c2_timeout.py (digest fbb9bef7… verified = the §9 pin; wrapper prints WRAPPER_COMMAND/stdout/stderr/WRAPPER_EXIT and carries a child's exit 0; a 0.5-second probe on a 3-second child printed SYMBOLIC_TIMEOUT and exited 124 at 0.504s); r3c2_build_seat_packet.py (digest b1f2cce1… verified = the C4 pin; rebuild byte-identical to the pinned packet, section 4 above); r3c2_seat_sandbox.sb (digest 6978d590… verified = the C4 pin); R3C2_CORPUS_MANIFEST.md (digest 300d4da1… verified = the §2 pin; all 89 enumerable rows byte-verified — 0 missing files, 0 digest mismatches, 0 byte-count mismatches under ../bhu-reading-20260823/sources/; the RAW listing is present and marked non-enumerable); partition_12_of_89.json (12 batches, 89 files exactly once, rows 1..89 in manifest order, its manifest_sha256 field equals the manifest digest); R3C2_SEAT_PACKET.sha256 (8/8 OK) and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 (10/10 OK) re-verified after all runs; R3C2_INTERPRETATION_PROTOCOL_20260904.md (exists — metadata only, never opened; the blind's own discipline); R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md and R3C2_V23_DESIGN_OF_RECORD_55b466fa.md (exist). Script-computed fields observed in my runs: rests_on (including the DISPUTED pair), root_origins, NOT_COMPUTED, DERIVED_STANDARD_OR_MEASURED_ONLY, declared/recomputed counts, author_specified_input, the recomputed selection, the per-input comparison rows. Dispatch-time artefacts (OWNERSHIP_b<k>.txt, dispatch records and probes, provider logs, batch ledgers and reports, C0 exhibitions, C6_AUDIT.json in a real audit, receipts P and T, the check sheet) are produced at their stages; their future absence is not asserted as a missing delivered script. The kit (last three lines of output): "deletion_probes=54", "controls=156 passed=156 failed=0", "STAGED_TESTS=PASS".

FINDINGS (verbatim quote, defect, exact replacement). All five are COSMETIC — each changes no class filed, no control's passability, no reachability, no conclusion and no tested value.

N1 (COSMETIC): the builder's summary line prints character counts labelled "bytes".
Quote (builder output, this session): "written       r3c2_seat_packet/R3C2_SEAT_PACKET.md  (39654 bytes, master 140083 bytes)"
Defect: the packet is 39654 characters but 39938 bytes on disk (stat-verified), and the master is 140083 characters but 141122 bytes; the figures are character counts. The digests the same output prints are correct, and the pin sheet verifies before and after — only the label is wrong.
Exact replacement: in r3c2_build_seat_packet.py's summary line, print len(text.encode("utf-8")) for both files (or relabel the figures as characters); re-pin the builder in the master (C4), R3C2_SEAT_PACKET.sha256 and the kit sheet; re-run the kit.

N2 (COSMETIC): the corpus manifest's "non-blank lines" column is wrong for 9 of 89 rows on the pinned bytes.
Quote (manifest row 20-area, verbatim row for the largest delta): "| 20 | `2006.12611_clean.txt` | `…` | … | 5266 |" — the row's own format is "| # | file | sha256 | bytes | non-blank lines |".
Defect: the pinned bytes of 2006.12611_clean.txt contain 6520 non-blank lines, not 5266; eight more rows disagree under a direct count and under every counting convention I tried (lines with any strip() content; any alphanumeric; any letter; ≥2 alphanumerics; any non-punctuation): 2006.12611 (5266→6520), 2007.10999 (310→312), 2101.01735 (707→709), 2104.00521 (267→268), 2104.00880 (708→752), 2207.05124 (204→208), 2503.14738 (1765→1949), 2512.05099 (229→233), astro-ph_0212469 (999→1004). Every row's sha256 and byte count — the fields every tool consumes — verify exactly (batch seal/coverage check the digest only; the partition only prints the column's sums; §10.5's scale note is informational). No machine consumer exists.
Exact replacement: recompute the column from the pinned bytes under one stated convention (or strike the column); re-pin the manifest in §2 and regenerate/re-pin the partition.

N3 (COSMETIC): the kit README describes an older suite and an older seat-tool pin.
Quote (r3c2_staged_d1d7/README_STAGED_UNADOPTED.md): "`r3c2_staged_tests.py` — 111 controls: positives, negatives asserting the EXACT failure set, and 38 deletion probes" and "The pinned seat tool remains `r3c2_ledger_tools.py` (sha256 230359eb…)".
Defect: the delivered suite prints "controls=156 passed=156 failed=0" and "deletion_probes=54", and the pinned seat tool is now f19303d1…. The README is descriptive text in the kit; the pin sheet and the suite itself are correct.
Exact replacement: update the README's counts and pin reference; re-pin it in R3C2_STAGED_D1D7.sha256.

N4 (COSMETIC): the codex-C2/kimi-F2 remainder — two delivered tools still carry staging labels, and the seat tool's usage synopsis omits the mandatory third argument.
Quotes (verified against the delivered, pin-verified bytes): r3c2_manifest.py line 2 — "`r3c2_manifest_STAGED.py <directory>` — STAGED, UNADOPTED copy (V26cand gate F9: symlinks and non-regular entries fail closed)"; r3c2_ledger_tools.py — five internal "STAGED D7" docstrings and one "# STAGED D1" comment (lines 162, 260, 274, 281, 305, 313), and usage line 21: "  /usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir>".
Defect: §10.23 records the delivered tool docstrings as repaired, and the lane and batch tools now correctly self-describe as "the V27 lane-side tool" (verified); the manifest tool's docstring, the seat tool's STAGED labels, and the two-argument validate synopsis (the tool requires <candidates.json> as the third argument — §3 states it and every fixture ran it that way) were not updated. Behavior is unchanged — every fixture in this report ran these exact bytes and all pins verify.
Exact replacement: manifest tool line 2 → "r3c2_manifest.py <directory> — the pinned-environment manifest as ONE process; symlinks and non-regular entries fail closed."; strip the seat tool's "STAGED D1/D7" labels; replace the synopsis line with "  /usr/bin/python3 -E r3c2_ledger_tools.py validate <ledger.json> <sources_dir> <candidates.json>"; re-pin in the master, R3C2_SEAT_PACKET.sha256 and the kit sheet; re-run the kit.

N5 (COSMETIC): `merge` is silent when the two seats disagree on an input's value, status or coordinates (as opposed to its origin or parents).
Quote (master C3, lane-side span): "`merge` exits 1 if the two `input_id` sets differ — … ; where the two `origin` classifications differ the merged record carries `origin_alt` and `origin_evidence_alt`; where the two `derived_from` lists differ the merged record carries both parent lists marked `PARENTS_DISPUTED`, and `compute` derives `root_origins` under both, printed as a pair, as for a disputed origin."
Defect: my counterexample — seat A records value "0.5" at q1.txt:2 and seat B records "0.6" at q1.txt:3 for the same input id with the same origin: `merge` exits 0 printing "merged 1 records; origin disagreements=0", and the merged record carries A's "0.5" at line 2 with B's value nowhere. The text neither states this rule nor states the omission. Bound, verified against the compare code and my F2 fixture: every arithmetic-group claim is audited, and the audit compares value, source coordinates, evidence and edges field-by-field, so no per-claim outcome can be silently moved; a value in a non-arithmetic claim moves neither its outcome nor its rests_on; sampled ledgers are compared field-by-field. Consequence: nil for outcomes and tallies; the gap is documentary.
Exact replacement: append to the quoted merge sentence: "where the two seats' value, status or source coordinates differ on the same input id, the merged record carries both and the disagreement is reported like an origin disagreement" — or state explicitly that merge takes seat A's record for these fields and that C6's per-field comparison is the only check on them.

## Q7 (V28) ANSWERS

(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative entries), re-verified after every run below; the packet's line 9, "Built from master sha256 `c9a43610be7451aacee491d1ab3e16d4ba658dc368663f884a6da657625de206` …", equals ACCESS_SHA. The kit runs (`/usr/bin/python3 -E r3c2_staged_tests.py` in r3c2_staged_d1d7): last three lines "deletion_probes=54", "controls=156 passed=156 failed=0", "STAGED_TESTS=PASS"; the kit pin sheet's last three pinned entries (partition_12_of_89.json, partition_12_of_89.txt, C6_COUNTEREXAMPLE_EXHIBIT.txt — the exhibit deterministically rewritten by the run) re-verify OK afterward.

Own fixtures, all against the delivered pinned tools (full pipeline each: seal-enumeration → select → handout → seal-rederivation → compare; tokens verbatim from stdout):
- RECONSTRUCTION MISSING A FIELD (derived_from omitted): `audit seal-rederivation` exit 1 — "FAIL: pB.txt#c1/pB.txt#c1#a: reconstruction missing ['derived_from']", "re-derivation NOT sealed: the reconstruction is incomplete" (REFUSED AT THE SEAL); forced past the seal with a hand-written seal2 naming its digest, `audit compare` exit 1 — "FAIL: C6_RECONSTRUCTION: pB.txt#c1/pB.txt#c1#a: reconstruction missing ['derived_from']", C6_AUDIT_SAMPLE=FAIL (MISMATCH AT COMPARE).
- COMPLETE RECONSTRUCTION, ONE FABRICATED EVIDENCE QUOTATION: seal-rederivation exit 0; compare exit 1 — "FAIL: AUDIT pB.txt#c1: MISMATCH (input pB.txt#c1#a: origin_evidence.verbatim This quotation is fabricated. vs sealed a choice we make here.)".
- CROSS-CLAIM DEPENDENCY THE AUDITOR DID NOT RECONSTRUCT (claim tB's input derived from a parent in non-audited claim tA, seed 0…03 chosen so the sample excludes tA): compare exit 1 — "FAIL: AUDIT tB.txt#c1: MISMATCH (dependency not reconstructed by the auditor or cyclic: tB.txt#c1#d: derived_from tA.txt#c1#m not in ledger)" — MISMATCH, NOT BORROWED (the roots recomputation runs on the auditor's own graph only). Companion: the auditor reconstructs the closure including the cross-claim parent — C6_AUDIT_SAMPLE=PASS, exit 0.
- AUDITOR CLASSIFICATION EQUAL TO THE SEALED ALTERNATIVE (sealed origin CHOSEN with origin_alt IMPORTED; auditor files IMPORTED with the alternative's evidence): compare exit 0, C6_AUDIT_SAMPLE=PASS; C6_AUDIT.json records the branch note "sealed alternative classification (ORIGIN_DISPUTED carried, compared like with like)" and roots {"audit": ["IMPORTED"], "sealed_primary": ["CHOSEN"], "sealed_under_matching_branch": ["IMPORTED"]} — like compared with like. Companion: an auditor classification matching NEITHER sealed branch (FITTED) — exit 1, "MISMATCH (input pB.txt#c1#a: origin FITTED vs sealed CHOSEN; root_origins differ: audit ['FITTED'] vs sealed ['CHOSEN'])".
- STANDARD RECORD WITH ORIG_SILENT AND NO COORDINATES: validate exit 1 — "FAIL: pB.txt#c1#h: STANDARD record needs its own source_file and a positive source_line (absence of origin evidence never waives value evidence)", C3_NO_SUBSTITUTION=FAIL. Companion with coordinates carrying the token: PASS, exit 0.
- BLOCKED RECORD QUOTING ANOTHER FILE (evidence in qX.txt, claiming file pB.txt bound through the candidate file): validate exit 1 — "FAIL: pB.txt#c1#z: BLOCKED naming evidence is in qX.txt, but claim pB.txt#c1 belongs to pB.txt", C3_NO_SUBSTITUTION=FAIL. Companion with the naming sentence in the claiming file: PASS, exit 0.
- PRINTED RECORD WITH EVIDENCE ON LINE n AND VALUE ON LINE n+1 (evidence pB.txt:3, value token at pB.txt:4): validate exit 0, C3_NO_SUBSTITUTION=PASS. Companion with the value absent from its own line: exit 1, "FAIL: pB.txt#c1#e: value 0.95 is not a numeric token at pB.txt:4".

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0: Python 3.9.6; sympy 1.14.0; interpreter digest b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9 printed; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages printed; r3c2_manifest.py over that path: FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0. (The two `-c` one-liners were run as equivalent one-line files; my referee shell blocks `-c` — a restriction on this referee, not on the text's executability.) NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the manifest is ONE process, run live over 20,637 files); the §9 wrapper is one invocation that prints and carries its child's exit status (demonstrated: 0 carried, and SYMBOLIC_TIMEOUT/124 at the deadline); the kit's 54 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative.

(c) PRIOR_FINDINGS_CLOSED=NO — item by item over the V27 gate's findings, each checked against the current master text and the delivered pinned tools with my own fixtures:
- codex F1 (C6 accepted incomplete reconstructions; evidence never compared; missing cross-claim dependencies silently borrowed) — CLOSED: master C6 now has `audit seal-rederivation` validating the reconstruction schema ("every input carries symbol, status, value, source coordinates, origin, origin evidence and `derived_from` (and `origin_search` when silent)") and refusing an incomplete one, and `audit compare` requiring every field, comparing "every field, every evidence coordinate and quotation, and every dependency edge" and never borrowing a dependency; my fixtures: missing field refused at the seal AND MISMATCH at compare; fabricated quotation MISMATCH; unreconstructed cross-claim parent MISMATCH via the auditor's own graph; complete closure PASS.
- codex F2 (a declared alternative accepted locally then rejected again by root comparison) — CLOSED: master §3's disputed-input sentence and C6 bind "an auditor classification that matches a declared sealed alternative to that branch" and recompute roots like with like; my F4 fixture PASSes with the branch note and equal branch roots printed, and the unsupported classification still MISMATCHes.
- codex F3 (STANDARD and BLOCKED bypassed status-specific evidence obligations) — CLOSED: master C3 now requires every STANDARD record's coordinates unconditionally ("absence of origin evidence never waives value evidence") and every BLOCKED record's "candidate-file binding proving that its naming-evidence file is the claiming file"; my F5 (ORIG_SILENT, no coordinates) FAILs with the exact message, F5-pos PASSes; my F6 (evidence in another file) FAILs with the exact message, F6-pos PASSes.
- codex F4 (an ordinary PRINTED record could not cite evidence on a different line from its value) — CLOSED: master C3 — "the evidence line may differ from the value line, and no second quotation check is applied to the value line"; my F7 (evidence line 3, value line 4) PASSes; the value-line check still fires (F7-neg FAIL).
- codex C1 (the historical parenthesis named the wrong old token) — CLOSED: master §3 now reads "`DERIVED_STANDARD_OR_MEASURED_ONLY` (formerly `DERIVED_ONLY`, with identical membership)" (line 117, verified).
- codex C2 (delivered tool provenance/usage text stale) — PARTLY: the delivered lane and batch tools now self-describe as "the V27 lane-side tool" (both docstrings verified, and the tools' digests match both pin sheets) — exactly the V28 change Q7 names. Not done: the manifest tool's docstring still reads "r3c2_manifest_STAGED.py … STAGED, UNADOPTED copy" (its digest afa2dee7… is unchanged since V26), and the seat tool retains its "STAGED D1/D7" labels and the two-argument validate synopsis. Cosmetic; remainder filed as N4.
- kimi F1 (same parenthesis) — CLOSED: same evidence as codex C1.
- kimi F2 (delivered pinned tools still self-described as STAGED, UNADOPTED) — PARTLY: same evidence as codex C2; the lane and batch tools are repaired, the manifest tool and the seat tool's internal labels are not. Cosmetic; remainder filed as N4.

(d) NEW executable defects: none above COSMETIC. The five cosmetic findings N1–N5 in section 7 (builder's byte-label; the manifest's non-blank-lines column on 9 rows; the kit README's stale counts/pin; the docstring/synopsis remainder; the merge silence on value/status/coordinate disagreement, with its counterexample and its bound) are the complete list. Each has a verbatim quote, the defect, and an exact replacement; none changes a class filed, a control's passability, a reachability verdict, a conclusion or a tested value.

Why SOUND_WITH_REPAIRS and not UNSOUND: every finding is cosmetic under the five tests; Q1 and Q2 answer YES with my own fixtures; the blind, the seal, the fairness wording, the outcome classes and the executability of every described artefact all verified as written, including every V28-repaired property the two V27 seats found wanting. Why not SOUND: five cosmetic as-delivered discrepancies (N1–N5) and two prior cosmetic items only PARTLY closed (codex C2 / kimi F2), each with a one-sentence or one-column repair.

Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under _tmp_gate_v28_kimi/ (driver _tmp_gate_v28_kimi_driver.py), not corpus data and not a census. The master, both pin sheets, the packet and the brief were re-verified unchanged after all runs (master digest c9a43610… = ACCESS_SHA; both sheets OK).

R3C2_V28_GATE_COMPLETE
