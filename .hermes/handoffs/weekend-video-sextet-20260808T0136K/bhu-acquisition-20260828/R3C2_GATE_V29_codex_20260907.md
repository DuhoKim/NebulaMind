ACCESS_SHA=37ba58918ec6f6b09775808041acc370aff3dd3dfd561e63407829255080e64d
PACKET_SHA=b7ea4cd5df4a7b9fb8a44a7f3ed6a2af1b2f3213abe04ae209aeb49c60fe8c8e
GATE=PREREG_UNSOUND
LEAK=Content-level interest in reproduction versus input provenance, including chosen inputs; cosmology; no directional hypothesis, preferred comparison model or desired empirical distribution reconstructed.
CONSEQUENCE_VISIBLE=Procedural stops only: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent classification, comparison and audit: “Every input's `origin` is classified independently by both seats”; “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” No downstream interpretive purpose reconstructed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO

Design-only review of the unsigned V29 draft. No corpus derivation, adoption, signature, dispatch or interpretation was performed. Section 10 supplies no verdict evidence. The packet was read and its answers recorded before reading the master. Only the two expressly authorized V28 reports were subsequently consulted. Q7's execution instructions authorize inspection of the kit, delivered tools, brief and manifests; corpus contents were consumed only for SHA-256 and byte-count verification. Synthetic fixtures and complete command logs are in `_tmp_gate_v29_codex/`.

Packet-first answers, in the required order:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

No substantive reproduction result is identified as more consequential to the lane. “No tier, warrant token, standing or stamp moves” limits institutional consequences. Procedural consequences are visible: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” Thus, for the question's literal “to anyone,” yes: excessive origin disagreement stops participants' work. This qualifies the initial packet-only answer's distinction between procedural stops and substantive stakes; it does not introduce knowledge from the master.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?

Yes, operationally: independent classification, comparison, audit and dispute reporting. “Every input's `origin` is classified independently by both seats.” “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” I cannot state the downstream scientific interpretation or its mapping from the packet.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

“The reproduction verdict and the provenance fields are recorded separately” suggests an interest in whether successful arithmetic depends on particular kinds of inputs. “Provenance is transitive” indicates interest in ancestry beyond immediate inputs. “Distinct from a claim whose inputs the paper DOES state, chosen or not” anticipates the printed-but-chosen case. “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” identifies cosmological content, not a preferred comparison model. The author-specified-input exclusion anticipates ambiguity between setting and deriving numbers. None of these sentences identifies an expected distribution, a preferred substantive result, a directional hypothesis or a particular pattern. Any stronger reconstruction would be speculation.

## 1. OUTCOME CLASSES

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

YES for the settled definition and bounded operational question. This does not claim decidability of arbitrary mathematics: incomplete-attempt outcomes provide terminal filings. The executable audit defects below do not require reopening option (c).

Concrete physical claim constructions, not findings about corpus papers:

| Stated claim and inputs | Unique outcome | Unique rests_on |
|---|---|---|
| Choose a = 3 m/s² and t = 2 s; state v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe and inputs, but print v = 7 m/s | REPRO_FAILED | USES_CHOSEN |
| State fitted a = 3 m/s² and measured t = 2 s; v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| Locally print “we adopt a = 3 from X”; measured t = 2 s; v = at = 6 m/s | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Obtain a instead from the first symbol/value line of a verified enumerable named source | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Name an unpinned source for unprinted a | REPRO_BLOCKED | USES_IMPORTED |
| Require a but neither print it nor name its source; origin silent | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| A fully specified calculation over measured inputs exceeds the cap or requires unavailable machinery | REPRO_NOT_EVALUABLE | DERIVED_STANDARD_OR_MEASURED_ONLY |
| Assert “our result is 6 m/s,” with no producing procedure and no ledger records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

Hardest combined case: v = at + v0 = 17 m/s, with printed chosen a = 3 m/s², printed fitted t = 4 s, and imported v0 = 5 m/s satisfying §2. It files exactly REPRO_WITHIN_STATED_PRECISION and USES_IMPORTED. Printing 18 instead files REPRO_FAILED with the same rests_on. The chosen input's numeral can be excluded as a standalone result while remaining available to the included claim's arithmetic.

A claim needing both a blocked input and an absent input fits two raw conditions, but precedence files only REPRO_BLOCKED. A named but unspecified procedure files REPRO_NO_DERIVATION_STATED before machinery limitations. I found no double filing after precedence in these hard cases.

Study-level witnesses, with other reached prerequisites passing:

| State | Unique filed class |
|---|---|
| One arithmetic claim and successful audit | CENSUS_COMPLETE |
| One successfully audited NO_DERIVATION_STATED claim | CENSUS_PARTIAL / INCONCLUSIVE |
| Audit mismatch, missing seed or receipt failure | CENSUS_AUDIT_FAILED |
| Repeated common prerequisite-control failure | R3C2_NO_CLASS |
| Persistent inclusion or input-list disagreement | CENSUS_DENOMINATOR_DISPUTED |
| Persistent outcome disagreement | CENSUS_OUTCOME_DISPUTED |
| Origin disagreement affecting 2 of 10 claims | CENSUS_ORIGIN_DISPUTED |
| Persistent prerequisite-control fail/pass split | CENSUS_CONTROL_SPLIT |

A non-arithmetic tally with an audit failure satisfies PARTIAL and AUDIT_FAILED predicates but files only AUDIT_FAILED. A completely empty enumeration on both sides can file PARTIAL; zero included claims with candidate passages instead triggers C6's zero-denominator failure. INCONCLUSIVE is genuinely reachable. No uncovered completed tally was demonstrated.

## 2. CONTROLS

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

Evidence-checkable, yes; mechanically guaranteed correct, no. My wrong-but-plausible record was:

```json
{"claim_id":"p.txt#main","input_id":"p.txt#a","symbol":"a","status":"PRINTED","origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"p.txt","source_line":4,"verbatim":"We adopt a = 3 from X."},"derived_from":[],"value":"3","source_file":"p.txt","source_line":4}
```

The sentence exists in my synthetic source. `validate` returns `C3_NO_SUBSTITUTION=PASS`, exit 0. Citation precedence requires IMPORTED, but the machine checks occurrence and code/origin pairing, not the quotation's meaning. An independently correct second seat would create a reported origin dispute; an independently correct auditor would mismatch an undisputed CHOSEN seal. If both seats and the auditor share the mistake, it survives. The operative text acknowledges this semantic floor. Computed root_origins and rests_on cannot correct a wrong accepted origin.

Every control has a named PASS/FAIL/NOT_RUN contract: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Required artefacts include exhibitions, printed script runs, path lists, live harness output, provider logs and audit JSON/seals. Bare tokens do not suffice. C4/C5b can pass on an asserted path list and expressly do not prove completeness. Unreached controls are NOT_RUN, including per-session validation before integration.

Two executable findings remain beyond that acknowledged semantic floor.

**F1 — NON-COSMETIC: reconstruction identity is not validated before flattening, allowing an incorrect claim binding and a fabricated record to survive PASS.**

Verbatim sentence, §3:

> During the C6 comparison, first every reconstructed input in the complete dependency closure of each selected claim — including records assigned to claims outside `audited_ids` — is validated and compared: each record is bound to its sealed `input_id` and claim, every required field, evidence coordinate, quotation and dependency edge is compared, and any unsupported difference is `MISMATCH` on every selected claim that depends on it; branch matching is performed over this full closure before roots are recomputed; both sealed branches and the auditor's matching branch are reported.

Counterexamples, each through seal-enumeration, select, handout, seal-rederivation and compare:

- Selected p.txt#main consumes d derived from a, whose sealed claim is p.txt#other. The recomputed sample excludes p.txt#other. Change the reconstructed a's explicit claim_id to p.txt#f0. Seal and compare both exit 0; dependent main is MATCH; C6_AUDIT_SAMPLE=PASS. Neither schema validation nor compare checks that identity field against the enclosing claim and sealed record.
- In that same reconstruction, give a under p.txt#other the quotation “Fabricated shadowed quote.” Add a second occurrence of input ID p.txt#a under p.txt#f99, carrying the correct original record. Seal and compare again exit 0 with main MATCH and C6_AUDIT_SAMPLE=PASS. The global a_graph dictionary silently overwrites the first occurrence. The fabricated occurrence is sealed but never compared. Removing the duplicate exposes the fabricated quotation and yields MISMATCH, as the requested ordinary off-sample fixture confirms.

The second case is not shared reader error: the supplied reconstruction visibly contains conflicting records, and the promised machine comparison discards one. The first case independently violates the expressly claimed identity binding.

Exact replacement for the quoted sentence:

> Before sealing or comparing a reconstruction, require each input_id to occur exactly once across the entire reconstruction. Bind its enclosing claim key, and any explicit claim_id or input_id fields, to that record's sealed identity at comparison; reject conflicting identities and never overwrite duplicate occurrences. Validate and compare every record in each selected claim's complete dependency closure, including records assigned to unselected claims. Report any duplicate, identity conflict, missing record or unsupported field, evidence or edge difference as MISMATCH on every selected claim that depends on it. Perform complete branch matching over this closure before recomputing roots and report the sealed and matched branches.

Implement uniqueness checks before constructing a_graph, identity checks at compare, and re-pin. The seal can check internal identity consistency and uniqueness before it has access to the sealed ledger; sealed-identity binding belongs at compare.

**F2 — NON-COSMETIC: the alternative matcher accepts a hybrid record that matches neither complete declared branch.**

Verbatim sentence, §3:

> Matching an explicitly declared alternative is not `MISMATCH`: the auditor's origin, origin evidence and parent list are matched against the complete declared primary or alternative record, including an alternative parent list when the origin is unchanged; one consistent matched-branch graph is constructed over the selected claims and their complete dependency closure and used for every dependent claim's sealed-root comparison; an unsupported record, evidence, parent list or inconsistent branch remains `MISMATCH`; both sealed branches and the matching branch are reported without reconciling the original dispute.

Counterexample: primary d is DERIVED with its equation evidence and parents [a]. Its declared alternative is CHOSEN with choice evidence and parents [b]. Both a and b have CHOSEN roots. The auditor supplies DERIVED with the primary evidence but parents [b]. This is neither declared record. Nevertheless, compare files C6_AUDIT_SAMPLE=PASS, exit 0, and main MATCH. The condition `same_origin and ev_ok and par_alt_ok` accepts the mixed record as the alternative even when the alternative also changes origin. Root comparison fails to expose it because both graphs yield CHOSEN roots.

I separately validated both synthetic seat ledgers: both returned C3_NO_SUBSTITUTION=PASS. Real merge returned exit 0, PARENTS_DISPUTED=1, FIELD_DISAGREEMENTS=0 and origin disagreements=1; its merged records equal the sealed fixture's records. Only one of 32 included claims is affected, below the origin-dispute stopping threshold. This is a reachable false PASS, not merely malformed hand-written merge data.

Exact replacement for the quoted sentence:

> Matching a declared alternative is not MISMATCH only when the auditor's origin, origin evidence and parent list jointly equal one complete declared branch. Construct the primary branch from the primary fields and the alternative branch by applying all declared alternative fields together. Accept an unchanged-origin parent alternative only when the alternative branch's origin and evidence are also unchanged. If neither complete branch matches, report MISMATCH even when the root-origin sets are equal. Construct one consistent graph from the matched complete records over all selected claims and their dependency closures, compare roots using that graph, and report the original disputes and matched branches.

Implement exhaustive complete-record branch comparison with an unconditional mismatch when neither branch matches, then re-pin. Equal root summaries cannot substitute for record equality.

## 3. CIRCULARITY

The inclusion rule supplies no directional hypothesis. A contrary numeral asserted as the paper's own result must be enumerated. Author-specified inputs are excluded as standalone results, retained with coordinates and numerals, and remain available to included recipes.

Independent enumeration, retained exclusions, reconciliation and the auditor's full independent enumeration make unilateral quiet exclusion detectable. Shared reader error remains possible. Differences classified as audit inclusion disputes at or below 10% are reported but need not fail the study; the design does not guarantee every mistaken exclusion is fatal.

Receipt P fixes the interpretation protocol before limb A. That separates prospective interpretation from observed tallies. F1 and F2 nevertheless allow unsupported reconstructed evidence or graph structure to acquire printed audit PASSes. They weaken evidence integrity without demonstrating intentional manipulation.

## 4. THE BLIND

The builder's redaction is a real executable assertion. With the delivered brief and current master, a separate output build returned C4_PACKET_REDACTED=PASS, removed 51 spans, retained §§1–6, 9 and 11, and reproduced the packet byte-for-byte. Its embedded master digest equals ACCESS_SHA. Required-content and forbidden-string checks are present; the brief is checked too.

The packet-only inferences above reveal content and procedural importance, not a directional hypothesis. C4 explicitly limits the lexical assertion and acknowledges prior exposure, self-report and confinement limitations. The inspected, hash-matching sandbox profile permits additional runtime/configuration accesses and networking, as the current operative text states. This review certifies no actual dispatch or denied-read probe. The dispatch inventory, confinement profile and probes must still be printed at launch.

## 5. THE SEAL

The specified relay binds what it claims: receipt P precedes limb A and identifies the interpretation protocol hash and commit; receipt T fixes the named tally commit and its files before protocol opening. Both receipts and independently verified values must be printed; a missing receipt or mismatch prevents interpretation and files CENSUS_AUDIT_FAILED.

Commits alone are not custody. The external receipts supply the temporal commitment. The text correctly limits this to committed records, not the lane owner's prior guesses. No receipt's existence or protocol content was assumed in this design review; the protocol's existence was checked by metadata only.

## 6. FAIRNESS

REPRO_FAILED expressly requires “unreproduced from the stated inputs,” not “error.” The delivered brief applies that wording to negative outcomes generally. The other negative classes identify missing procedures, unavailable inputs or bounded execution limits; they do not assert that a paper is erroneous.

REPRO_WITHIN_STATED_PRECISION matches its rounding and uncertainty rules. DERIVED_STANDARD_OR_MEASURED_ONLY matches its defined membership; imported provenance describes the claiming paper's import, not whether the external source itself chose or derived the value. No additional outcome-label overclaim was demonstrated. F1 and F2 concern actual control passability, not cosmetic wording.

## 7. STALL / EXECUTABILITY

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

The supplied commands launch and the named tools are present. However, the promised complete identity and branch comparisons are not implemented faithfully: F1 and F2 are executable counterexamples. Therefore the answer is YES for the described behavior, despite C5_EXECUTABLE_UNDER_SCOPE=YES.

Checks performed:

- Master and packet: read; SHA-256 computed; packet-first ordering preserved; embedded digest checked; master remained unchanged.
- R3C2_SEAT_PACKET.sha256: all eight entries verified from the lane working directory, before and after testing. Kit pin sheet: all ten lane-relative entries verified before and after its run.
- Corpus manifest: digest `2d8f29656dc86bbcea86ba649388f2c6d37e03655985fdc23a849e1c9e162c2f`, equal to §2. All 89 source rows under `../bhu-reading-20260823/sources/` matched their SHA-256 and byte counts; zero missing files or mismatches. No source text was displayed or interpreted.
- Partition: `partition R3C2_CORPUS_MANIFEST.md 12` reproduced the pinned JSON byte-for-byte; 89 texts in 12 ownership batches. The kit exercises seal, coverage and join, including limb-B binding, missing artefacts, ownership violations, cycles and seal mismatches.
- Seat tool: read and pin-verified; census and census final executed, declared/recomputed counts and outcome fields observed; three-argument validate executed with positive and negative fixtures. All five audit subcommands executed on independently authored fixtures.
- Lane tool: read and pin-verified; merge and three-argument compute executed; root_origins and rests_on observed, including USES_CHOSEN. Kit controls cover missing mandatory candidates, planted computed fields, NOT_COMPUTED and dispute propagation.
- Builder and brief: read, pin-verified, rebuilt packet identical. Builder correctly printed packet 40267 bytes and master 144272 bytes.
- Timeout wrapper: read and pin-verified; the literal 120.0-second invocation carried child exit 0 and exit 7. A shortened 0.2-second negative probe printed SYMBOLIC_TIMEOUT, exited 124 and reported WRAPPER_ELAPSED=0.204. No corpus arithmetic was attempted.
- C5 commands executed live: Python 3.9.6; SymPy 1.14.0; interpreter SHA-256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`; user site `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`; manifest FILES=20637 and MANIFEST_SHA256=`1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed`. All five exited 0. Actual dispatch-pin equality remains a dispatch check.
- Sandbox: read; SHA-256 `6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6`, equal to C4. Interpretation protocol, leak-floor judgment and signed-design reference exist, checked by metadata only; their contents were not opened.
- Dispatch inventories, OWNERSHIP_b<k>.txt, provider logs, batch ledgers/reports, real audit seals, receipts and the later check sheet are stage-generated artefacts. Their absence before an authorized study is not a missing delivered-tool finding. Historical paths in §10 were not treated as operative deliverables or searched.

C5_EXECUTABLE_UNDER_SCOPE=YES: C4 authorizes the five printed invocations and the pinned environment, while chosen placeholders remain bounded. NO_MASKED_STAGE=YES: mandated commands contain no shell pipeline; the manifest is one process and the wrapper carries child failure/timeout. These tokens do not certify the semantic correctness of all checks.

Q7(a), kit's last three lines, verbatim:

```text
deletion_probes=57
controls=165 passed=165 failed=0
STAGED_TESTS=PASS
```

Own requested fixture results:

```text
baseline: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
offsample_fabrication: EXIT=1 C6_AUDIT_SAMPLE=FAIL main=MISMATCH
parent_only_alternative: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
inherited_alternative: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
standard_external: EXIT=1 C3_NO_SUBSTITUTION=FAIL
merge_disagreement: EXIT=1 OUTPUT_EXISTS=False FIELD_DISAGREEMENTS=1 MERGE=FAIL
```

The off-sample mismatch names the fabricated quotation on p.txt#a in the selected dependent claim. The parent-only fixture changes only the declared parent list. The inherited fixture matches a FITTED alternative through another claim's record. The STANDARD fixture puts H0=67.36 in q.txt while its claim belongs to p.txt. The merge fixture disagrees on value 3 versus 4, prints the disagreement and writes no output.

Q7(c), V28 findings, checked against operative text, delivered code and current execution:

| Prior finding | Status | Evidence |
|---|---|---|
| codex F1: off-sample closure comparison | PARTLY | Ordinary fabricated closure evidence now mismatches; F1 above demonstrates duplicate shadowing and absent claim binding. |
| codex F2: parent-only/inherited alternatives | PARTLY | Both requested positives pass; F2 above demonstrates an unsupported mixed branch also passing. |
| codex F3: external STANDARD bypass | CLOSED | §2 binds claiming file; external STANDARD fixture fails validate. |
| codex C2: staging/usage remnants | CLOSED | Delivered seat, lane, batch and manifest tools contain no STAGED/UNADOPTED labels; validate synopsis includes candidates.json. |
| kimi N1: builder byte counts | CLOSED | UTF-8 byte lengths printed correctly: 40267 and 144272. |
| kimi N2: manifest line-count convention | CLOSED | Manifest now explicitly defines newline-delimited nonblank counting; manifest re-pinned and partition reproduced. Source hashes/bytes independently verified; source line counts were not recomputed under the requested metadata/digest-only scope. |
| kimi N3: stale kit README | CLOSED | README names 165 controls, 57 probes and current seat-tool digest; live suite agrees. |
| kimi N4: tool labels and synopsis | CLOSED | Same delivered-code evidence as codex C2. |
| kimi N5: silent merge disagreement | CLOSED | Value disagreement prints FIELD_DISAGREEMENTS=1, exits 1 and creates no merged file. |

Q7(d), new executable defects: F1 and F2 above. All five requested regression fixtures pass their expectations, but the additional fixtures show the broader repair contracts are incomplete:

```text
unsupported_hybrid: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
wrong_claim_binding: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
duplicate_shadow: EXIT=0 C6_AUDIT_SAMPLE=PASS main=MATCH
```

**C1 — COSMETIC: the manifest incorrectly says its informational line-count column is consumed by no tool.**

Verbatim column text:

> non-blank lines (convention, stated V29: the pinned bytes split on the newline character `\n` only — a form feed or other Unicode line separator does not start a line — counting lines with any non-whitespace character; informational, consumed by no tool)

`r3c2_batch_tools.py` parses that column, sums it and writes nonblank_lines into the partition. Integrity predicates do not depend on those counts. Exact replacement for the column text:

> non-blank lines (convention, stated V29: the pinned bytes split on the newline character `\n` only — a form feed or other Unicode line separator does not start a line — counting lines with any non-whitespace character; informational, summed and reported by the partition tool, not used as an integrity predicate)

This clarification changes no class, control passability, reachability, conclusion or tested value. Re-pin the manifest and regenerated partition if applied.

The UNSOUND verdict rests on F1 and F2's demonstrated false PASSes, not the cosmetic item, historical verdicts or a challenge to option (c). Nothing here adopts, signs or runs the census.

R3C2_V29_GATE_COMPLETE
