ACCESS_SHA=37ba58918ec6f6b09775808041acc370aff3dd3dfd561e63407829255080e64d
PACKET_SHA=b7ea4cd5df4a7b9fb8a44a7f3ed6a2af1b2f3213abe04ae209aeb49c60fe8c8e
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline", C3); failures are anticipated and pre-worded ("unreproduced from the stated inputs," not "error", §3); the author-specified-input count is a wanted printed statistic ("census prints the AUTHOR_SPECIFIED_INPUT count as its own line beside the denominator", §3); provenance is visibly recorded with care (the accepted floor of the 18:52 ruling); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=CANNOT_STATE (checking mechanics are visible — "Every input's `origin` is classified independently by both seats" (C3), the auditor "re-classifies the record from the pinned sources" (C3), "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (§4) — but no packet sentence states any analytic use: no tally, comparison or conclusion is computed from `origin`; §1 states a separation, not a purpose: "The reproduction verdict and the provenance fields are recorded separately.")
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO (8 of 9 CLOSED with my own fixtures: codex F1, F2, F3, C2; kimi N1, N3, N4, N5; kimi N2 PARTLY — the line-count convention is now stated and the manifest re-pinned with the partition regenerated, but the column's numbers still disagree with the stated convention on the same 9 rows; cosmetic, evidence below)

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 29, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Files opened: the packet (first, answered before the master), the master, the two V28 gate reports (after the master, as the brief permits), the delivered pinned tools (r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py, r3c2_manifest.py, r3c2_build_seat_packet.py, r3c2_timeout.py), the kit r3c2_staged_d1d7/, both pin sheets, the corpus manifest and its declared source directory (digests and byte counts only, never content). The interpretation protocol was checked by metadata only (3,947 bytes), never opened. All fixtures are synthetic referee evidence under _tmp_gate_v29_kimi/ (drivers: driver.py, extras.py, extras2.py), not corpus data and not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The consequence-adjacent sentences and why none of them ranks an outcome: §11 "No tier, warrant token, standing or stamp moves." — it denies consequence transfer on the named axes and ranks nothing; §4 "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`." — a filing precedence, not a weight; the stop classes' "The census does not proceed" and "No tally is filed" — procedural effects attached to failure states, not to any preferred result; §3's filing order and the reason-code tie-break are mechanical, and the tie-break expressly "is a tie-break by the specificity of the evidence, not a ranking of the values". No sentence states what any outcome does for the lane or for anyone.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? I cannot state what it is FOR. What the study DOES with it is visible only as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are "filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4/C6); "the auditor re-classifies the record from the pinned sources" (C3) and the audit reports MATCH/MISMATCH "per re-classified input origin" (C6). But no sentence states any analytic use — no tally, outcome, comparison or conclusion is computed from `origin` anywhere in the packet. §1's "The reproduction verdict and the provenance fields are recorded separately." states a separation, not a purpose. ORIGIN_PURPOSE=CANNOT_STATE.

Q3 (hypothesis-reachability). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3) — content a seat must have to file STANDARD at all, not a stake. (ii) Reproduction failures are anticipated and pre-worded non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3). (iii) The set-versus-derived composition of the corpus is a wanted, reported output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a printed statistic is not pre-committed to remain unused. (iv) The chosen-input case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3). (v) The tie-break disclaims a ranking before any reader makes one — "the order is a tie-break by the specificity of the evidence, not a ranking of the values." (C3); someone cared that CHOSEN not read as worse than DERIVED. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried, audit re-classification; that is the ruled accepted floor, and nothing in the packet says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, stated as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their stated inputs, and how much of the corpus rests on chosen/fitted/imported/undeclared inputs rather than derived/standard/measured ones — with failures pre-framed as "unreproduced", not "error". No directional hypothesis, comparison model, preferred outcome or "pattern" is reconstructible; I could not name what the lane hopes the tally shows.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt times out while an input is absent files REPRO_INPUT_ABSENT (the attempt stops at the absent input, and even taken as co-occurring the order decides). A real claim fitting none: none exists — a claim either states a procedure or not; each input is printed/standard (arithmetic group), named-but-unmachineable (BLOCKED), or untraced (ABSENT); the attempt completes, times out, or lacks machinery (NOT_EVALUABLE). Total. Study-level collisions: an audit failure over a blocked claim satisfies CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."). The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS — my FA/FC candidate sets are exactly such tallies (1 arithmetic + 4 non-arithmetic, audit executable at PASS) — or by an empty enumeration.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on, several executed in my fixtures this session:
(a) A printed-but-chosen input. As a candidate the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (the census run prints "author_specified_input=<n>" beside the denominator — observed in my census runs); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN; the arithmetic consumes it by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"); the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED. Executed: my FB ledger (a CHOSEN "a=2", a MEASURED "b=3", a DERIVED d over them) validates PASS for both seats, merges exit 0, and the claim's roots compute to {CHOSEN, MEASURED} under both sealed branches (C6_AUDIT.json "_roots" printed below) — rests_on USES_CHOSEN.
(b) A fitted parameter the paper states. PRINTED/FITTED, consumed, rests_on USES_FITTED. Executed: my FC seat-B ledger (r FITTED with "we fit r = 1.5 to the data") validates PASS; in FC-main the auditor's FITTED reconstruction is the sealed alternative and the roots compare equal under the matched branch: audit ['FITTED'] vs sealed_under_matching_branch ['FITTED'] (sealed_primary ['CHOSEN'] printed beside it).
(c) An imported value. Executed: my QI fixture — an external import (value line in another manifest text) validates PASS under the external branch (verified bytes against the manifest row, numeric token, first line carrying symbol and numeral), is consumed, and computes rests_on=USES_IMPORTED (printed: "pI.txt#c1 rests_on=USES_IMPORTED root_origins=['IMPORTED']"); a named source that is unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record that validates PASS and is never consumed; an input traced to no source files REPRO_INPUT_ABSENT. A locally printed citation ("we adopt a = 3 from X" printed by the claiming paper) validates PASS under the local-import branch (my Q2 record).
(d) The combined case — printed chosen + fitted + imported + standard + derived — consumes every PRINTED/STANDARD record, files one arithmetic outcome, and computes the most severe root present; where the two seats disagree the design carries the pair marked DISPUTED rather than a silent choice (executed: "pQ.txt#c1 rests_on=['USES_CHOSEN', 'USES_IMPORTED'] root_origins=['CHOSEN'] DISPUTED"; the dispute propagates across claims: tC.txt#cB marked DISPUTED through tC.txt#cA's disputed record).
One outcome, one rests_on, no reproducer choice anywhere: the seats never write rests_on (compute REJECTS a ledger arriving with rests_on or root_origins set, exit 2 — the kit asserts it), and the reproduction question never asks why an input is what it is, only whether the paper's number follows from the paper's stated values.

## 2. CONTROLS

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts (observed in my runs: the "declared:" and "recomputed: ... reconciled=YES" lines, then C1_DENOMINATOR_PRINTED=PASS); C3 PASS only on printed validate runs — "a token asserted without the printed run is FAIL"; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate ≤10%, and no MISMATCH. The two controls passable by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete". Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes," plus §4's stop sentence and C2's per-session NOT_RUN.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with the floor the document itself states. My wrong-but-plausible record, executed this session: pQ.txt line 2 prints "We take the bias b = 1.7 from Q (2021)." and seat A files the input CHOSEN with ORIG_CHOICE_STATED and that true sentence as its quotation. `validate` returns C3_NO_SUBSTITUTION=PASS, exit 0 — the machine checks that the quotation occurs at the cited line and that reason_code maps to origin, not that the code is the right one for the sentence. The mechanisms that catch it, as written, each demonstrated: (1) the second seat's independent classification — seat B files IMPORTED for the same record; `merge` prints "merged 1 records; origin disagreements=1", the merged record carries origin_alt, and `compute` prints "rests_on=['USES_CHOSEN', 'USES_IMPORTED'] root_origins=['CHOSEN'] DISPUTED"; above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification — my FC-neg fixture shows an auditor origin matching neither sealed branch is MISMATCH at compare ("origin UNDECLARED vs sealed CHOSEN; ... root_origins differ"), and FC-main shows a classification matching a declared sealed alternative is bound to that branch and compared like with like. The machine floor that IS enforced is also real: the same citation misfiled with its value line in another file FAILs validate ("such a record must be IMPORTED with ORIG_CITATION" — the D1 branch, exercised by the kit and both V28 seats). The stated residual floor (C3's lane-side span): a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." That is the answer, and it is stated rather than implied away.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) contains no pattern content; I read the built packet in full and found none either (spot checks below). Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator (observed in my census output); a contrary claim asserted as the paper's own result cannot be quietly re-sorted — both seats must agree, and C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded passage an OMISSION that files CENSUS_AUDIT_FAILED (the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt, deterministically regenerated in my run, emits exactly these cases: both seats omit a required passage; its reverse; a passage the auditor excluded that the seats never enumerated; disputes at 10% and 15%). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans. rests_on is computed by script, not seats: compute REJECTS (exit 2) a ledger arriving with root_origins or rests_on set, and the audit compares every field, evidence coordinate, quotation and dependency edge and recomputes roots from the auditor's own graph — my FA-neg2 shows a missing cross-claim parent is MISMATCH ("dependency not reconstructed by the auditor or cyclic"), never borrowed. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — all stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master: I re-ran the pinned builder with the printed command form (`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out <scratch> --brief r3c2_seat_packet/SEAT_BRIEF.md`); output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 37ba5891…" (= ACCESS_SHA) and "seat packet sha256 b7ea4cd5…" (= PACKET_SHA), "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51", "written … (40267 bytes, master 144272 bytes)". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (cmp); the printed figures equal the on-disk byte sizes (stat: 40267 and 144272), so the builder now prints true byte counts (kimi's V28 N1 repaired). Spot checks on the packet: zero SEAT-REDACT markers; no hit for pattern/ΛCDM/rests_on/DERIVED_ONLY/the custody names/the engine names/the consequence words. The C4 mechanism does what it states: a redacted out-of-lane copy directory, a kernel sandbox profile (r3c2_seat_sandbox.sb digest 6978d590… verified on disk, equal to the C4 pin) whose permitted surface the text prints, plus self-report lists with the completeness disclaimer. Its limits are stated honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is the principal's ruled floor and is recorded as such in C4. The packet's line 9 names the master it was built from; that digest equals ACCESS_SHA (verified programmatically, not by eye).

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one commit containing the merged candidate file, the exclusion ledger and the merged input ledger with its compute output is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal — executed: with no seal present, exit 1, "FAIL: no stage-1 seal of the auditor's enumeration; selection refused". The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes, metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it). The packet's "error" vocabulary is audit-internal only (line 58 "may detect an error", line 308 "shared reader error") — never an outcome label; verified by grep. The positive outcome's name matches its mechanical tests (rounding half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY matches its membership exactly — the lane tool emits it only for a root set inside {DERIVED, STANDARD, MEASURED} (my FC compute emitted USES_CHOSEN/USES_FITTED pairs and NOT_COMPUTED rows, never the clean label on a disputed or chosen ledger), and the code comment records the historical token as historical. R3C2_NO_CLASS is bounded to C0–C5b, with C6/seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases. The exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — the kit asserts it).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): with the two COSMETIC exceptions filed as N1–N2 below, everything the document describes executed as delivered. Each thing checked, and how: /usr/bin/python3 (runs, 3.9.6); /usr/bin/shasum (runs); sympy (imports, 1.14.0); the user site path prints and r3c2_manifest.py ran live over it (FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0) — the two `-c` one-liners were run as equivalent one-line script files because my referee shell blocks `-c`, a restriction on me, not on the text; r3c2_ledger_tools.py (digest a68ad2b2… verified = the §3 pin and both pin sheets; census two-argument and final, validate three-argument, and all five audit subcommands executed in my fixtures); r3c2_lane_tools.py (digest 1da02f3b… verified = the C3 and §9 pins; merge and compute executed — merge carries origin disagreements and FIELD_DISAGREEMENTS, compute emits the DISPUTED pair and NOT_COMPUTED rows); r3c2_batch_tools.py (digest 3822f12c… verified = the C1 pin; `partition R3C2_CORPUS_MANIFEST.md 12` reproduces the pinned partition BYTE-IDENTICALLY, digest 1a4c30909f17f3b7a24867bb7c71b06de0aa2c2aef9211c05b5b12d7b70042fd = C1's cited prefix and the kit pin); r3c2_manifest.py (digest 6b6e4bce… verified = the C5 pin; the live 20,637-file run above); r3c2_timeout.py (digest fbb9bef7… verified = the §9 pin; wrapper prints WRAPPER_COMMAND/stdout/stderr/WRAPPER_EXIT and carries a child's exit 0; a 0.5-second deadline on a 3-second child printed SYMBOLIC_TIMEOUT and exited 124 at 0.504s); r3c2_build_seat_packet.py (digest 56ac7d1c… verified = the C4 pin; rebuild byte-identical, section 4); r3c2_seat_sandbox.sb (digest 6978d590… verified = the C4 pin); R3C2_CORPUS_MANIFEST.md (digest 2d8f2965… verified = the §2 pin; all 89 enumerable rows' sha256 and byte counts equal the files at its declared source location ../bhu-reading-20260823/sources/, checked by digest and metadata only, contents never read); the seat tool carries no compute and no merge subcommands and no REPRO_EXACT remnant (grep). No mandated command is a shell pipeline; every mandated command is one invocation whose exit status is the control's.

FINDINGS (verbatim quote, defect, exact replacement). Both are COSMETIC — each changes no class filed, no control's passability, no reachability, no conclusion and no tested value.

N1 (COSMETIC): the corpus manifest's informational line-count column still disagrees with its own stated convention on 9 rows (kimi's V28 N2, repaired only in part).
Quote (header, master §2's pinned manifest): "| # | file | sha256 | bytes | non-blank lines (convention, stated V29: the pinned bytes split on the newline character `\n` only — a form feed or other Unicode line separator does not start a line — counting lines with any non-whitespace character; informational, consumed by no tool) |" — and row 22: "| 22 | `2006.12611_clean.txt` | `b3503ae6…` | 341817 | 5266 |".
Defect: under the stated convention (split on \n only, count lines with any non-whitespace character) the pinned bytes of 2006.12611_clean.txt contain 6520 such lines, not 5266; eight more rows disagree identically to V28: 2007.10999 (310 vs 312), 2101.01735 (707 vs 709), 2104.00521 (267 vs 268), 2104.00880 (708 vs 752), 2207.05124 (204 vs 208), 2503.14738 (1765 vs 1949), 2512.05099 (229 vs 233), astro-ph_0212469 (999 vs 1004) (manifest value first, my count second — recomputed this session from the pinned bytes). The convention is now stated (the V28 ambiguity is gone) and every row's sha256 and byte count — the fields every tool consumes — verify exactly on all 89 rows; the column is marked informational and has no machine consumer.
Exact replacement: recompute the column from the pinned bytes under the stated convention (or strike the column); re-pin the manifest in §2 and regenerate/re-pin the partition.

N2 (COSMETIC): a duplicated comment line in the delivered seat tool.
Quote (r3c2_ledger_tools.py, lines 38–40, the pinned bytes):
"# when more than one reason code matches the cited sentence, the first applicable in this order is filed (a sentence
# naming an external source for the value is a citation whatever else it says):
# external source for the value is a citation whatever else it says):"
Defect: line 40 is a stray duplicate fragment of the sentence completed at line 39; a comment scar only. Behavior is unchanged — every fixture in this report ran these exact bytes and all pins verify; no docstring, usage line or label is affected (the V28 docstring/label items are repaired — grep finds no "STAGED" in any delivered tool).
Exact replacement: delete line 40; re-pin the seat tool in the master (§3), R3C2_SEAT_PACKET.sha256 and the kit sheet; re-run the kit.

## Q7 (V29) ANSWERS

(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK (packet b7ea4cd5…, brief, timeout fbb9bef7…, ledger a68ad2b2…, manifest 6b6e4bce…, builder 56ac7d1c…, batch 3822f12c…, lane 1da02f3b… — each equal to the master's named pins); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative entries; the five staged tools byte-identical to the delivered ones); both re-verified exit 0 after every run. The packet's line 9, "Built from master sha256 `37ba58918ec6f6b09775808041acc370aff3dd3dfd561e63407829255080e64d` …", equals ACCESS_SHA (compared programmatically): PACKET_BUILT_FROM_ACCESS_SHA=YES. The corpus manifest's digest 2d8f29656dc86bbcea86ba649388f2c6d37e03655985fdc23a849e1c9e162c2f equals §2's pin; all 89 enumerable rows' sha256 and byte counts equal the files at the declared source location (mismatches: NONE). The kit runs (/usr/bin/python3 -E r3c2_staged_tests.py in r3c2_staged_d1d7): last three lines "deletion_probes=57", "controls=165 passed=165 failed=0", "STAGED_TESTS=PASS"; the kit pin sheet re-verifies OK afterward (the exhibit is deterministically rewritten). The V29 partition regenerates byte-identically (1a4c3090… = C1's prefix).

Own fixtures, all against the delivered pinned tools (full pipeline: seal-enumeration → select → handout → seal-rederivation → compare; tokens verbatim from stdout):
- OFF-SAMPLE DEPENDENCY RECORD WITH A FABRICATED QUOTATION (the claim tB.txt#c1 selected as arithmetic; the dependency's owner tA.txt#c1 demonstrably outside audited_ids under the chosen seed): compare exit 1 — "FAIL: AUDIT tB.txt#c1: MISMATCH (input tA.txt#c1#m: origin_evidence.verbatim This quotation is fabricated. vs sealed we measured m = 4 with the balance described)", C6_AUDIT_SAMPLE=FAIL. Companion with the TRUE quotation: C6_AUDIT_SAMPLE=PASS, exit 0. Companion with the dependency NOT reconstructed: exit 1 — "MISMATCH (dependency not reconstructed by the auditor or cyclic: tB.txt#c1#d: derived_from tA.txt#c1#m not in ledger)" — never borrowed. Companion with the off-sample dependency's VALUE changed to 999: exit 1 — "MISMATCH (input tA.txt#c1#m: value 999 vs sealed 4)", C6_AUDIT_SAMPLE=FAIL.
- PARENT-ONLY DECLARED ALTERNATIVE MATCHED BY THE AUDITOR (sealed d: derived_from [a], derived_from_alt [b], PARENTS_DISPUTED=true, origin unchanged; auditor files [b] with the primary origin and evidence): compare exit 0, C6_AUDIT_SAMPLE=PASS; C6_AUDIT.json records d on branch "alt" with roots audit ['CHOSEN','MEASURED'] = sealed_under_matching_branch ['CHOSEN','MEASURED'] (sealed_primary identical here because a is itself a sealed input of the claim). Companion with an UNSUPPORTED parent list [q]: exit 1 — "MISMATCH (input pB.txt#c1#d: derived_from ['pB.txt#c1#q'] vs sealed ['pB.txt#c1#a'] (alternative ['pB.txt#c1#b']); dependency not reconstructed …)", C6_AUDIT_SAMPLE=FAIL.
- INHERITED ALTERNATIVE THROUGH ANOTHER CLAIM'S RECORD (selected claim tC.txt#cB depends on tC.txt#cA#r, whose sealed record carries primary CHOSEN with origin_alt FITTED and alternative evidence; the auditor exactly matches FITTED and its sealed alternative evidence): compare exit 0, C6_AUDIT_SAMPLE=PASS; C6_AUDIT.json records r on branch "alt" with roots audit ['FITTED'], sealed_under_matching_branch ['FITTED'], sealed_primary ['CHOSEN'] — like compared with like across the whole closure. Companion with the auditor origin matching NEITHER branch (UNDECLARED/ORIG_SILENT): exit 1 — "MISMATCH (input tC.txt#cA#r: origin UNDECLARED vs sealed CHOSEN; origin_search differs; root_origins differ: audit ['UNDECLARED'] vs sealed ['CHOSEN'])", C6_AUDIT_SAMPLE=FAIL.
- A STANDARD VALUE LINE IN ANOTHER FILE (status STANDARD, symbol H0, value 67.36, coordinates qD.txt:2 while the candidate binds the claim to pD.txt): validate exit 1 — "FAIL: pD.txt#c1#h: STANDARD value line is in qD.txt, outside the claiming file pD.txt: a value outside the claiming paper follows the named-source rule (PRINTED/IMPORTED with ORIG_CITATION), even when it is on the closed list", C3_NO_SUBSTITUTION=FAIL. Companion with the value line in the claiming file: C3_NO_SUBSTITUTION=PASS, exit 0.
- TWO SEAT LEDGERS DISAGREEING ON A VALUE (both individually validate PASS: A files "0.5" at pE.txt:2, B files "0.6" at the same line, whose text carries both tokens): merge exit 1 — "FAIL: pE.txt#c1#e: seats disagree on value ('0.5' vs '0.6') — a value/status/coordinate disagreement is not silently resolved", "PARENTS_DISPUTED=0", "FIELD_DISAGREEMENTS=1", "MERGE=FAIL"; the merged file is NOT written (verified absent). Companion with agreeing ledgers: exit 0, "merged 1 records; origin disagreements=0".

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0: Python 3.9.6; sympy 1.14.0; interpreter digest b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9 printed; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages printed; r3c2_manifest.py over that path: FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0. (The two `-c` one-liners were run as equivalent one-line files; my referee shell blocks `-c` — a restriction on this referee, not on the text's executability.) NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the manifest is ONE process, run live over 20,637 files); the §9 wrapper is one invocation that prints and carries its child's exit status (demonstrated: 0 carried; SYMBOLIC_TIMEOUT/124 at 0.504s on a 0.5s deadline); the kit's 57 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative.

(c) PRIOR_FINDINGS_CLOSED=NO — item by item over the V28 gate's findings, each checked against the current master text and the delivered pinned tools with my own fixtures:
- codex F1 (reconstructed dependency-closure records outside the selected claim set not compared) — CLOSED: master §3's disputed-input sentence and C6 now require validating and comparing "every reconstructed input in the complete dependency closure of each selected claim — including records assigned to claims outside `audited_ids`", with any unsupported difference MISMATCH "on every selected claim that depends on it"; my fixtures: fabricated quotation MISMATCH on the dependent selected claim, changed value MISMATCH, unreconstructed MISMATCH, true quotation PASS.
- codex F2 (declared alternatives handled only for a local origin change) — CLOSED: master §3 now matches "an alternative parent list when the origin is unchanged" and builds "one consistent matched-branch graph … over the selected claims and their complete dependency closure … used for every dependent claim's sealed-root comparison"; my fixtures: parent-only alternative PASS (branch "alt", roots equal), inherited alternative through another claim's record PASS (audit FITTED vs sealed-under-matching-branch FITTED, sealed_primary CHOSEN printed), unsupported variants still MISMATCH.
- codex F3 (STANDARD could bypass the external-source import requirements) — CLOSED: §2's new sentence ("`STANDARD` applies to a closed-list value printed in the claiming paper … a missing candidate binding or a `STANDARD` value line outside the claiming file fails `validate`"); my fixture FAILs with the exact V29 message; the in-file companion PASSes.
- codex C2 (tool documentation repair only partly delivered) — CLOSED: grep finds no "STAGED" string in any delivered tool; the manifest tool's docstring now reads "r3c2_manifest.py <directory> — V28: symlinks and non-regular entries fail closed — the pinned-environment manifest as ONE process…"; the seat tool's usage synopsis names the third argument ("validate <ledger.json> <sources_dir> <candidates.json>"); the internal audit labels read "V28 D1/D7". All pins re-verify.
- kimi N1 (builder printed character counts labelled "bytes") — CLOSED: the builder now prints len(text.encode("utf-8")) (line 147); observed "(40267 bytes, master 144272 bytes)" equals the on-disk byte sizes (stat-verified).
- kimi N2 (manifest line-count column wrong for 9 of 89 rows) — PARTLY: the convention is now stated in the column header and the manifest is re-pinned (2d8f2965… = §2, verified) with the partition regenerated (byte-identical, 1a4c3090… = C1's prefix); but the column's NUMBERS still disagree with the stated convention on the same 9 rows (evidence at N1 above). The offered repair — recompute under one stated convention, or strike the column — was done only halfway. Cosmetic; remainder filed as N1.
- kimi N3 (kit README described an older suite and an older pin) — CLOSED: the README now reads "165 controls … 57 deletion probes" and pins the seat tool at "a68ad2b259719d76…", matching the suite's printed output and the pin sheet.
- kimi N4 (docstring/synopsis remainder) — CLOSED: same evidence as codex C2.
- kimi N5 (merge silent on value/status/coordinate disagreement) — CLOSED: master C3's merge sentence now carries the FIELD_DISAGREEMENTS clause; the tool prints every per-field disagreement, reports FIELD_DISAGREEMENTS=<n>, prints MERGE=FAIL, exits 1 and writes no merged file — executed on my value-disagreement fixture, with the agreeing companion at exit 0.

(d) NEW executable defects: none above COSMETIC. The two cosmetic findings N1 (the manifest column remnant) and N2 (the duplicated comment line) in section 7 are the complete list; each has a verbatim quote, the defect, and an exact replacement; neither changes a class filed, a control's passability, a reachability verdict, a conclusion or a tested value.

Why SOUND_WITH_REPAIRS and not UNSOUND: every finding is cosmetic under the five tests; Q1 and Q2 answer YES with my own fixtures; the blind, the seal, the fairness wording, the outcome classes and the executability of every described artefact all verified as written, and every V28 substantive repair (closure comparison, declared alternatives, the STANDARD file binding, merge's open failure) executed as repaired on my own fixtures. Why not SOUND: two cosmetic as-delivered discrepancies (N1–N2) and one prior cosmetic item (kimi N2) only PARTLY closed.

Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under _tmp_gate_v29_kimi/ (drivers driver.py, extras.py, extras2.py), not corpus data and not a census. The master, the packet and both pin sheets were re-verified unchanged after all runs (master 37ba5891… = ACCESS_SHA; packet b7ea4cd5… = PACKET_SHA; both sheets exit 0).

R3C2_V29_GATE_COMPLETE
