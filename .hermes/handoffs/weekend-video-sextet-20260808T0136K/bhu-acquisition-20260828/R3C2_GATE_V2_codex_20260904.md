ACCESS_SHA=bf404b621679263fb68f3aceb09d93272d859a1f05a4cd27be8cdf27d52ee7ba
GATE=PREREG_UNSOUND

1. QUOTE: “**`NOT_ATTEMPTED`** — the claim states no number to reproduce.”
   DEFECT: Section 1 defines a quantitative claim by the presence of a printed numeral asserted as the paper's result. An included quantitative claim therefore cannot satisfy this outcome. If this outcome instead applies to excluded candidate passages, those passages are not claims, and counting them in the census silently changes the denominator from quantitative claims to candidate passages. Thus `NOT_ATTEMPTED` is neither a coherent per-claim outcome nor a safe route out of the census. The other five classes can cover every included claim: a number either follows without a disqualifying choice, follows only after one, fails with sufficient inputs, is blocked by an unobtainable source, or lacks an input.
   EXACT REPLACEMENT: Replace the entire `NOT_ATTEMPTED` bullet with: “**Candidate exclusions are not per-claim outcomes.** Every enumerated candidate passage that fails the §1 definition is recorded in a separate exclusion ledger with file, line, and the failed definitional element; it is excluded from the quantitative-claim denominator. Every passage included as a quantitative claim must receive exactly one of the five outcomes above. Report both the candidate count and the quantitative-claim denominator.” Replace C1's first sentence with: “**C1 — denominator.** The complete candidate ledger, the exclusion ledger, and the resulting quantitative-claim denominator are printed before any arithmetic or tally.”

2. QUOTE: “If the enumeration is itself contested between the two seats beyond a stated tolerance, stop and report — a census whose denominator is disputed cannot proceed.”
   DEFECT: This is a reachable stop with no named study-level outcome. `CENSUS_PARTIAL` concerns unresolved claims, `CENSUS_AUDIT_FAILED` concerns the §6 sample, and `R3C2_NO_CLASS` concerns a control failing in every seat; none necessarily applies to an enumeration dispute. Section 4 also gives no precedence when, for example, an audit failure and a control failure coexist. The inconclusive route is reachable, but the study-level scheme is not exhaustive or mutually decisive.
   EXACT REPLACEMENT: Replace the quoted sentence with: “If the two seats disagree on any candidate's inclusion after two reconciliation attempts (tolerance zero), stop with **`CENSUS_DENOMINATOR_DISPUTED`**; list every disputed candidate and report **INCONCLUSIVE**.” Add to §4: “Apply study-level outcomes in this precedence order: `CENSUS_AUDIT_FAILED`, `R3C2_NO_CLASS`, `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`; file the first condition met.”

3. QUOTE: “The path list is kept as a second check, not the only one; either a forbidden path in the list or a packet that contains a forbidden file fails the run.”
   DEFECT: A redacted directory and working-directory setting do not prevent a process from reading parent, absolute, cached, network, or previously supplied paths. The path list remains self-reported detection after access, not construction-level prevention. Nor does the design exclude a seat with prior exposure to the pattern. C4 therefore cannot support its claimed mechanical blindness, although no textual route in the scoring rule itself exposes the pattern.
   EXACT REPLACEMENT: Replace the C4 paragraph with: “**C4 — pattern blindness.** Use a fresh seat with no inherited conversation and a recorded declaration of no prior pattern exposure. Run it in an enforced read allowlist containing only this preregistration, its seat brief, and pinned sources; deny all other filesystem reads and all network access. Before work begins, Tori records the allowlist and verifies that no allowed file contains the pattern, synthesis, interpretation protocol, or any `R3C*`/`R3D*` file other than this preregistration. Any denied-read attempt or eligibility failure voids the seat. `C4_PATTERN_BLIND=PASS`.”

4. QUOTE: “Git is the external custodian; the seal is not Tori's word.”
   DEFECT: Git in the same writer's custody is tamper evidence, not an external custodian. A local commit can be amended, reset, replaced, or paired with a rewritten interpretation-protocol commit, after which the result can quote the new commit id without revealing the rewrite. The tally is therefore not irreversibly sealed against the actor who performs §7.
   EXACT REPLACEMENT: Replace the quoted sentence with: “Before the interpretation protocol is opened, Tori sends the tally hash, tally commit id, interpretation-protocol hash, and protocol commit id to an append-only custodian outside Tori's write authority; the custodian's timestamped receipt is quoted in the result. Any mismatch voids §7 and files `CENSUS_AUDIT_FAILED`.”

5. QUOTE: “The arithmetic may consume only values present in that ledger with status `PRINTED` or `STANDARD`, and a script asserts that every value used appears there and that no record with status `ABSENT` carries a value.”
   DEFECT: C3 mechanically checks consumption but not the truth of the classifications. A seat can put an unstated value in the ledger as `PRINTED`, or treat an open-ended “STANDARD” such as “Planck 2018 parameters” as a selectable family. The ledger also has no field distinguishing a paper-stated chosen/fitted/imported value from an independently determined input, even though that distinction assigns `REPRO_AFTER_CHOICE`. This permits inconsistent outcome assignment, though pattern-blind seats still cannot make it pattern-directed.
   EXACT REPLACEMENT: Replace the quoted sentence with: “The ledger additionally records `origin: DERIVED|CHOSEN|FITTED|IMPORTED|UNDECLARED`; each `PRINTED` value must be machine-matched to the cited source line, and each `STANDARD` value must match a preregistered finite whitelist giving constant name, numerical value, uncertainty, version, and source. Arithmetic may consume only validated `PRINTED` or whitelisted `STANDARD` values. Any `CHOSEN`, `FITTED`, or undeclared `IMPORTED` dependency that is supplied routes to `REPRO_AFTER_CHOICE`; an unsupplied required value routes to `REPRO_INPUT_ABSENT`.”

6. QUOTE: “A third seat, also pattern-blind, re-derives a **random 20%** of per-claim outcomes without seeing the first seats' work.”
   DEFECT: “Random 20%” supplies no frozen sampling frame, seed, rounding rule, or minimum sample, so the sample can be selected after the tally or become zero in a small census. C6 also audits outcomes but not the completeness of the denominator, the point at which selection could occur.
   EXACT REPLACEMENT: Replace the quoted sentence with: “A third pattern-blind seat first audits the full candidate and exclusion ledgers against every pinned source, then re-derives without seeing prior work a sample of `max(1, ceil(0.20*N))` quantitative claims selected from sorted frozen claim ids by a preregistered PRNG and seed committed before enumeration.”

7. QUOTE: “**Wording: "unreproduced from the stated inputs," not "error."**”
   DEFECT: None. The standing wording is explicit and consistent throughout the design; no replacement is warranted.
   EXACT REPLACEMENT: None.

Answers to 1, 2, and 9:

1. The original circularity is actually gone from the substantive criterion: the lane's pattern cannot influence enumeration, input classification, per-claim assignment, or the sealed tally through any instruction in this document, because seats are not given the pattern and §7 occurs afterward. `NOT_ATTEMPTED` can distort the denominator, but it cannot do so *in the pattern's direction* unless C4 blindness fails. The residual path is operational: current C4 does not truly prevent a seat from learning the pattern, and current Git sealing lets the §7 custodian rewrite the evidentiary state. After the exact C4 and seal repairs above, I find no remaining path for the hypothesis to reach its evidence.

2. The criterion is external. Reproducing a published number from its stated inputs is ordinary, pattern-independent verification and predates this lane's shape/magnitude hypothesis. It is not that hypothesis restated: it can fail for algebra, missing inputs, inaccessible sources, or undeclared imports regardless of whether a construction fixes a shape or a magnitude.

9. Worth: Yes—once repaired, a corpus-wide reproducibility census is worth roughly 3–4 seat-days because it yields a reusable claim/input ledger and tests a basic evidentiary property independent of the lane's hypothesis. Better: Yes—it is materially better than the replaced design because claim eligibility no longer asks whether evidence matches a lane-authored pattern.

R3C2_GATE_COMPLETE
