ACCESS_SHA=57de1252d5f9d4592181517e70d0bca1c715830ccbb595f4bc9622a18e4779fd
PACKET_SHA=e195a57ae8d20e80f58f27cd2ea1b683a57371bc7f21bac8443f8c6e7a260aac
GATE=PREREG_UNSOUND
LEAK=Interest in reproduction and transitive input provenance, especially chosen inputs; cosmology is identifiable; no directional hypothesis, preferred model or empirical payoff reconstructed.
CONSEQUENCE_VISIBLE=Procedural only: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Origin disagreement can stop the census; no substantive result is identified as more consequential.
ORIGIN_PURPOSE=Independent provenance checking: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” No interpretive mapping reconstructed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO

Design-only review of the unsigned V30 draft. The verdict rests on a new synthetic false PASS, not §10's history. No corpus derivation, census dispatch, adoption, signature or interpretation was performed. Q7's specific execution requirements were treated as authorization to inspect the kit, tools, brief and pin sheets and mechanically verify corpus metadata. Only the two authorized V29 reports were consulted after the master. The interpretation protocol was checked for existence by metadata only, never opened.

The packet was read and these answers recorded before the master, in this order:

Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?

No sentence identifies a substantive reproduction outcome as weightier to the lane or anyone. The literal question does admit procedural consequences: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” This disagreement can stop work; it does not disclose a preferred empirical outcome.

Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR?

Yes, as a downstream integrity check: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” Origins also support reported disagreements. I cannot reconstruct the subsequent interpretive mapping from this packet.

Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?

The packet invites a content-level inference that dependence on adopted or chosen inputs matters to the investigation: “**Provenance is transitive.**” Also, “`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator.” The sentence “**The reproduction verdict and the provenance fields are recorded separately.**” reveals two separately recorded dimensions. The sentence “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” identifies subject matter. None establishes a direction, a preferred comparison model, a shape/magnitude hypothesis, an expected distribution or a payoff. Careful provenance classification is inferable; the hidden ranking and interpretation are not.

## 1. OUTCOME CLASSES

Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?

YES at the definition level. Status controls consumption; origin records provenance. A finite operational classification remains available when arithmetic cannot finish. This does not guarantee evaluation of every mathematical recipe.

Concrete claim constructions below are synthetic paper passages, not findings about the unopened corpus contents:

| Printed claim and recipe | Unique per-claim outcome | Unique `rests_on`, absent reader disagreement |
|---|---|---|
| “We choose a = 3; y = 2a = 6.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_CHOSEN` |
| “Our fit gives a = 3; y = 2a = 6.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_FITTED` |
| “We adopt a = 3 from X; y = 2a = 6.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_IMPORTED` |
| “Use a from X; y = 2a = 6”; verified enumerable X prints a = 3. | `REPRO_WITHIN_STATED_PRECISION` | `USES_IMPORTED`, regardless of X's own provenance |
| Same cited-input claim, X outside the enumerable manifest. | `REPRO_BLOCKED` | `USES_IMPORTED` for the BLOCKED record |
| “We choose a = 3; y = 2a = 7.” | `REPRO_FAILED` | `USES_CHOSEN` |
| “y = 2a = 6”, with a neither printed nor sourced. | `REPRO_INPUT_ABSENT` | `USES_UNDECLARED`, with the required silent-search record |
| “Our result is y = 6”, without a producing procedure. | `REPRO_NO_DERIVATION_STATED` | `NOT_COMPUTED` for an empty ledger |
| A specified recipe with supplied inputs exceeds the cap or requires unavailable machinery. | `REPRO_NOT_EVALUABLE` | Determined independently by those inputs |

The chosen a = 3 itself belongs in the exclusion ledger if asserted only as an input; the derived y remains included. A printed fitted value is consumed, not silently refitted. An external named value must satisfy the pinned-source rule; it is not substituted from general knowledge.

An overlap attempt is y = a + b with a cited to an unavailable source and b absent. Both terminal conditions hold, but §3's precedence files only `REPRO_BLOCKED`. No claim fitting two filed outcomes or none was established under the stated operational rules.

Study-level witnesses: one agreed arithmetic claim and a passing audit gives `CENSUS_COMPLETE`; an agreed missing-input claim with a passing audit gives `CENSUS_PARTIAL`; an audit mismatch or missing seed gives `CENSUS_AUDIT_FAILED`; a twice-failed pre-tally control in every attempting seat gives `R3C2_NO_CLASS`; a surviving fail/pass split gives `CENSUS_CONTROL_SPLIT`; surviving inclusion/input-list disagreement gives `CENSUS_DENOMINATOR_DISPUTED`; surviving outcome disagreement gives `CENSUS_OUTCOME_DISPUTED`; origin disagreement affecting 2 of 10 claims gives `CENSUS_ORIGIN_DISPUTED`.

A partial tally whose audit fails satisfies both descriptive conditions, but the explicit precedence files only `CENSUS_AUDIT_FAILED`. INCONCLUSIVE is genuinely reachable: N=1, one agreed `REPRO_INPUT_ABSENT`, the auditor independently confirms it and the enumeration, all earlier controls and receipts pass, and the study files `CENSUS_PARTIAL`. A zero-denominator census cannot become complete; zero with candidate passages can instead fail the audit as expressly specified.

## 2. CONTROLS

Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?

YES as cited, independently reviewable evidence; NO as guaranteed machine-correct semantic classification. My plausible wrong record is:

```json
{"claim_id":"p.txt#owner","input_id":"p.txt#a","symbol":"a","status":"PRINTED","value":"1","source_file":"p.txt","source_line":37,"origin":"CHOSEN","origin_evidence":{"reason_code":"ORIG_CHOICE_STATED","source_file":"p.txt","source_line":37,"verbatim":"We adopt a = 1 from X."},"derived_from":[]}
```

The synthetic source actually contains that sentence. Live `validate` returned `C3_NO_SUBSTITUTION=PASS`, exit 0. The quotation and token match, and the submitted reason code maps to CHOSEN. Semantically, citation precedence requires IMPORTED/ORIG_CITATION. The second seat can catch that by independently applying the precedence; the C6 auditor can catch it by reconstructing the source classification. An auditor's corrected classification differs from an undisputed sealed CHOSEN record and should mismatch. Neither reader is guaranteed to notice, and an unselected claim's inputs need not be reclassified unless reached through a selected dependency. This is the stated shared-reader-error floor, not a newly discovered guarantee violation.

`root_origins` and `rests_on` are script-computed. Live merge and compute produced a CHOSEN root, the disputed pair `[USES_CHOSEN, USES_CHOSEN]` for the branch fixture, and `NOT_COMPUTED` for included claims without records. A dispute remains a dispute even when the two summaries coincide.

The named contracts are C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE, each with PASS/FAIL/NOT_RUN. They require exhibitions, printed executions, seals, audit JSON, path lists or provider logs. A bare PASS token is insufficient. C4/C5b can pass on an asserted path list and explicitly do not establish its completeness. Unreached controls are NOT_RUN; per-session validation waits for the joined ledger.

Two implementation defects remain.

**F1 — NON-COSMETIC: wrong ownership fails globally but is not propagated into dependent MISMATCH rows.**

Verbatim §3 sentence:

> Before sealing or comparing a reconstruction, every `input_id` must occur exactly once across the entire reconstruction, and an explicit `claim_id` or `input_id` field must agree with its enclosing keys; at comparison each reconstructed record is bound to its sealed identity — its enclosing claim key must be the sealed record's claim — and a duplicate, an identity conflict, a missing record or an unsupported field, evidence or edge difference is `MISMATCH` on every selected claim that depends on it; every record in each selected claim's complete dependency closure is validated and compared, records assigned to unselected claims included; complete branch matching is performed over this closure before roots are recomputed, and the sealed and matched branches are reported.

Counterexample: selected `p.txt#main` consumes d, derived from a. Sealed a belongs to the unselected `p.txt#owner`. Reconstruct a under `p.txt#extra`, with explicit claim_id also set to extra. Internal sealing correctly succeeds; compare prints `C6_IDENTITY`, exits 1 and returns `C6_AUDIT_SAMPLE=FAIL`. However, `audited[p.txt#main].result` and its a input row are both `MATCH`. Identity checking appends only a global failure; record comparison never receives it. The prior false overall PASS is repaired, but the stated per-dependent result is not. This changes a tested result field, so it is not COSMETIC.

Exact replacement for that sentence:

> Before sealing, reject duplicate input_ids and explicit identity fields inconsistent with their enclosing keys. At comparison, record each enclosing-claim versus sealed-claim identity conflict as a MISMATCH for that input, propagate it to every selected claim whose dependency closure contains the input, and fail the overall audit. Compare every required field, evidence item and dependency edge throughout each selected claim's complete dependency closure, including records assigned to unselected claims. Match complete branches before recomputing roots and report the sealed and matched branches.

Implementation required: retain identity failures keyed by input_id and include them in `compare_record`'s differences before constructing input and claim result rows. Keep the global audit failure. Re-pin after repair.

**F2 — NON-COSMETIC, verdict-determining: the reverse hybrid still receives a false PASS.**

Verbatim §3 sentence:

> Matching a declared alternative is not `MISMATCH` only when the auditor's origin, origin evidence and parent list jointly equal one COMPLETE declared branch: the primary branch is the primary fields, the alternative branch is every declared alternative field applied together with the undeclared fields staying primary; an unchanged-origin parent alternative is accepted only when the alternative branch's origin and evidence are also unchanged; a record matching neither complete branch is `MISMATCH` even when the root-origin sets agree; one consistent graph is constructed from the matched complete records over all selected claims and their dependency closures and used for every dependent claim's sealed-root comparison; the original disputes and the matched branches are reported without reconciling the dispute.

My two synthetic seat ledgers both pass `validate`. Real `merge` returns exit 0, `PARENTS_DISPUTED=1`, `FIELD_DISAGREEMENTS=0`, and one origin disagreement among 32 included claims. The merged d branches are:

| Branch | Origin and evidence | Parents |
|---|---|---|
| Primary | DERIVED; “d = a = 1.” | [a] |
| Alternative | CHOSEN; “We choose d = 1.” | [b] |
| New unsupported reconstruction | CHOSEN; alternative evidence | [a] |

Both a and b have CHOSEN roots. The third row matches neither complete branch. Nevertheless, seal succeeds and compare returns exit 0, `C6_AUDIT_SAMPLE=PASS`, main MATCH, with d labelled primary. This is the reverse of the requested hybrid, which correctly fails.

Cause: `matches_primary` and `matches_alt` are both false, but the diagnostic fallback appends no difference: the origin matches the alternative, its evidence matches the alternative, and the parents match the primary. An empty differences list becomes MATCH. Equal roots do not expose it.

Exact replacement for the quoted sentence:

> An input matches only if its origin, origin evidence and parent list jointly equal the complete primary branch or the complete alternative branch, with every declared alternative field applied together and undeclared fields left primary. If neither complete branch matches, append an unconditional record-level MISMATCH before any field-specific diagnostics, propagate it to every dependent selected claim, and fail the audit even when root-origin sets agree. Construct the comparison graph only from matched branches and report the original disputes and matched branches without reconciliation.

Exact code insertion at the start of `compare_record`'s `else` following `elif matches_alt`:

```python
            diffs.append("record matches neither complete declared branch")
```

Retain the existing detailed diagnostics. Add both hybrid directions as negative controls and re-pin. The prose already promises rejection; an implementation change is necessary.

## 3. CIRCULARITY

Inclusion is tied to printed own-result numerals, with retained exclusions, two independent enumerations and an independent full-corpus audit enumeration. A contrary claim cannot be quietly removed by a permitted exclusion operation: its passage and disposition remain recorded. Shared omission or shared misclassification remains possible. At or below the declared audit-dispute threshold, a disagreement may survive, but it is reported.

The packet reveals careful provenance work without revealing the interpretation ranking or preferred model. Receipt P prevents choosing a new recorded interpretation after limb A. Those mechanisms reduce the lane expectation's access to evidence; they do not make semantic classification mechanical. F2 independently weakens the audit barrier by accepting a reconstruction supported by neither sealed branch. No intentional manipulation is inferred.

## 4. THE BLIND

The builder was inspected and run into referee scratch using the delivered master and brief. It returned `C4_PACKET_REDACTED=PASS`; the result is byte-identical to the pinned packet. Whole-section removal, marked-span stripping, case-insensitive forbidden-string checks, required-content checks and brief checks are implemented. The builder removed 51 inline spans and retained sections 1, 2, 3, 4, 5, 6, 9 and 11.

The forbidden list proves absence of listed strings, not absence of all inferable interests. Packet-only inferences above fall within the operative “hide the comparison, keep the taxonomy” floor. C4/C5b explicitly limit PASS to self-reported paths. The separate sandbox profile is present and matches its pin; the design requires actual dispatch probes and an inventory. Those future probes were not performed or claimed here. The text acknowledges extra runtime/configuration/network allowances and prior-exposure limits. No stronger blindness has been verified.

## 5. THE SEAL

Section 7 is prospectively adequate for its stated custody claim: receipt P acknowledges the interpretation hash and commit before limb A; receipt T acknowledges the tally hash and commit, naming its files, before opening the protocol. Independent rehashing checks both hashes and commit identifiers, and the report must print both receipts and four verified values. Missing receipts or mismatches leave interpretation NOT_RUN and file `CENSUS_AUDIT_FAILED`.

This binds recorded bytes and sequence, not the owner's prior guesses. No receipt, commit, dispatch or protocol content was inspected to certify an actual run. The protocol file exists by metadata; its contents remain unopened.

## 6. FAIRNESS

The operative failure wording is “unreproduced from the stated inputs,” not an accusation of error. The brief applies that wording to negative outcomes generally. BLOCKED, INPUT_ABSENT, NO_DERIVATION_STATED and NOT_EVALUABLE describe limits of the stated-input attempt. WITHIN_STATED_PRECISION accurately describes the rounding/uncertainty rule. DERIVED_STANDARD_OR_MEASURED_ONLY names its defined membership. Imported provenance concerns the claiming paper's import, not a claim that the external source lacked a derivation.

No additional class-label overclaim was demonstrated. F1 and F2 concern actual tested audit results.

## 7. STALL / EXECUTABILITY

Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?

YES: the promised unconditional complete-branch rejection and per-dependent identity MISMATCH reporting are not executed faithfully, as F2 and F1 demonstrate. The named commands launch, the required tools exist, and C5 is executable under the stated scope. These are distinct findings.

Q7(a), delivered artefacts and execution checks:

- From the working directory, `shasum -a 256 -c R3C2_SEAT_PACKET.sha256` verifies 8/8 entries; `shasum -a 256 -c r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256` verifies 10/10. Both exit 0 before and after testing. The staged tools' digests match their delivered counterparts.
- Packet line 9's Built-from digest equals ACCESS_SHA. A fresh builder run produces exactly the delivered 40,267 packet bytes from the 146,303-byte master.
- Corpus manifest SHA-256 is `7f97591b5fd4a138971cb2a6737ee500908ba551c04d08678abf3307a16b4d77`, equal to §2. All 89 enumerable rows match SHA-256 and byte count at `../bhu-reading-20260823/sources/`. All 89 informational line counts also match a mechanical recount under the exact U+000A/U+0020/U+0009/U+000D convention. No source passage was interpreted or printed. Partition regeneration is byte-identical to `partition_12_of_89.json`.
- The kit was run with `/usr/bin/python3 -E r3c2_staged_tests.py` from its directory. Its final three lines were:

```text
deletion_probes=61
controls=174 passed=174 failed=0
STAGED_TESTS=PASS
```

- The kit exercises census, validation, audit stages, batch seals/joins/coverage, manifest refusal paths and mutation probes. Independent fixtures additionally ran both seat validations, merge, compute, audit seal-enumeration, select, handout, seal-rederivation and compare. The generated handout contains identifiers and source coordinates only.
- C5's five commands ran live in order, each exit 0: Python 3.9.6; SymPy 1.14.0; interpreter SHA-256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9`; user site `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`; manifest `FILES=20637`, `MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed`. No future dispatch-environment match is claimed.
- The §9 wrapper ran at the literal 120.0-second limit with success and an exit-7 child; it returned 0 and 7 respectively. A shortened 0.1-second boundary probe returned `SYMBOLIC_TIMEOUT`, exit 124, at 0.104 seconds. The timeout implementation uses the monotonic clock through subprocess timing and prints the child outputs/status. No mandated shell pipeline masks an upstream exit.
- Packet, brief, pin sheets, manifest, ledger/lane/batch/manifest/builder/timeout scripts, partition and sandbox profile were verified as delivered. Sandbox SHA-256 is `6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6`. The interpretation protocol exists, checked by stat only. OWNERSHIP files, candidate/exclusion/input ledgers, batch reports, joined outputs, dispatch/provider records, receipts, C6 JSON and check sheets are described as generated at their respective stages; their absence before a census is not a missing-deliverable finding. Canonical limb-B names and the separate-directory lifecycle are stated in both master and brief.

Independent fixture results, including the requested tokens:

| Fixture | Seal exit | Compare exit | Audit token | Selected main result |
|---|---:|---:|---|---|
| Primary branch, positive control | 0 | 0 | `C6_AUDIT_SAMPLE=PASS` | MATCH |
| Same input ID under two claims | 1 | NOT_RUN | Refused: input_id already reconstructed | NOT_RUN |
| Explicit claim_id conflicts with enclosing key | 1 | NOT_RUN | Refused: explicit claim_id conflicts | NOT_RUN |
| Explicit input_id conflicts with its key | 1 | NOT_RUN | Refused: explicit input_id conflicts | NOT_RUN |
| Dependency assigned to a different sealed owner | 0 | 1 | `C6_AUDIT_SAMPLE=FAIL`; `C6_IDENTITY` | MATCH — F1 |
| Primary origin/evidence plus alternative parents | 0 | 1 | `C6_AUDIT_SAMPLE=FAIL` | MISMATCH |
| Complete alternative branch | 0 | 0 | `C6_AUDIT_SAMPLE=PASS` | MATCH |
| Alternative origin/evidence plus primary parents | 0 | 0 | `C6_AUDIT_SAMPLE=PASS` | MATCH — false PASS, F2 |

The dependency owner is unselected in every case. The seed is 64 zeroes; N=32, R=31, k=7, eight claims audited. Both genuine branch reconstructions pass. The two origin/parent variants came from independently validating synthetic ledgers merged by the real lane tool; they were not fabricated alternative fields outside the merge contract.

Q7(b): `C5_EXECUTABLE_UNDER_SCOPE=YES`; `NO_MASKED_STAGE=YES`. These answers concern this text's scope and exit propagation, not a claim that every audit predicate is correct.

Q7(c), V29 findings independently checked:

| V29 finding | Status | Evidence |
|---|---|---|
| codex F1, reconstruction identity | PARTLY | Duplicate and explicit-key cases are refused at sealing; wrong owner fails globally, but its dependent claim/input still says MATCH. |
| codex F2, complete alternative matching | PARTLY | Requested hybrid fails and complete alternative passes; reverse hybrid still falsely passes. |
| codex C1, “consumed by no tool” | NOT | Exact wording survives; partition code still sums the column into nonblank_lines. |
| kimi N1, line-count convention and rows | CLOSED | Character-level convention explicit; all 89 row counts independently recomputed and agree; manifest pin and regenerated partition verify. |
| kimi N2, duplicated comment fragment | CLOSED | Duplicate fragment absent from delivered seat tool; pin verifies and kit passes. |

**C1 — COSMETIC: deferred manifest consumer wording remains false.**

Verbatim column heading:

> non-blank lines (convention, stated V30: the pinned bytes decoded as UTF-8 and split on the newline character U+000A only; a line is non-blank if it contains any character other than U+0020 space, U+0009 tab and U+000D carriage return — a non-breaking space or other Unicode space counts as content; informational, consumed by no tool)

The partition function sums column 5 into `nonblank_lines`. It does not use that count as an integrity predicate. Exact replacement:

> non-blank lines (convention, stated V30: the pinned bytes decoded as UTF-8 and split on the newline character U+000A only; a line is non-blank if it contains any character other than U+0020 space, U+0009 tab and U+000D carriage return — a non-breaking space or other Unicode space counts as content; informational, summed and reported by the partition tool, not used as an integrity predicate)

Re-pin the manifest and dependent partition after applying. This wording change changes no class filed, control passability, reachability, conclusion or tested value. It is the disclosed deferred item, not a new executable defect.

Q7(d): F2 is a new executable counterexample to the broader V29 repair; F1 is its separate remaining identity-reporting gap. The UNSOUND verdict is determined by F2's false PASS. Passing the kit and requested fixtures does not establish the full branch-matching property.

Reproducible referee evidence is in `_tmp_gate_v30_codex/`: `probes.py`, `extras.py`, `commands.log`, `results.txt`, `extras.txt`, `q2.txt`, the JSON fixtures and each comparison's `C6.json`. Source contents were accessed only for mechanical digest/size/line-count checks. Master and packet hashes and both pin sheets were rechecked unchanged after testing. This report changes no preregistration or delivered tool and authorizes no census run.

R3C2_V30_GATE_COMPLETE
