ACCESS_SHA=57de1252d5f9d4592181517e70d0bca1c715830ccbb595f4bc9622a18e4779fd
PACKET_SHA=e195a57ae8d20e80f58f27cd2ea1b683a57371bc7f21bac8443f8c6e7a260aac
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only: the corpus is cosmology ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline", C3); provenance is visibly recorded with care (seven origins, quotations, two readers, disputes carried — the ruled floor); failures are anticipated and pre-worded ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("census prints the AUTHOR_SPECIFIED_INPUT count as its own line beside the denominator", §3); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO (the only consequence-addressing sentence — §11 "No tier, warrant token, standing or stamp moves." — equalizes all outcomes; procedural stops such as "The census does not proceed" attach to failure states, not to any preferred result; no sentence ranks any outcome)
ORIGIN_PURPOSE=CANNOT_STATE (checking mechanics are visible — "Every input's `origin` is classified independently by both seats" (C3); disagreements "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (§4); the auditor "re-classifies the record from the pinned sources" (C3) — and §3 states the arithmetic "consumes records according to status `PRINTED` or `STANDARD`", so origin never gates consumption; but no packet sentence states any analytic use: no tally, comparison or conclusion is computed from `origin`; §1's "The reproduction verdict and the provenance fields are recorded separately." states a separation, not a purpose)
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=NO (4 of 5 CLOSED with my own fixtures — codex F1, F2; kimi N1, N2; codex C1 NOT, the declared deferral, verified still present and cosmetic)

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 30, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Order kept: the ACCESS digest was printed first; the packet was read and Q5/Q6/Q3 answered before the master was opened; the two V29 gate reports were opened only after the master, as the brief permits. Files opened: the packet, the master, the two V29 gate reports, the delivered pinned tools (r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py, r3c2_manifest.py, r3c2_build_seat_packet.py, r3c2_timeout.py), the kit r3c2_staged_d1d7/, both pin sheets, the corpus manifest and its declared source directory (digests, byte counts and line-count recomputation only, never content), the sandbox profile (digest only). The interpretation protocol was checked by metadata only (3,947 bytes), never opened. All fixtures are synthetic referee evidence under /tmp/r3c2_v30_fx/, not corpus data and not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The one consequence-addressing sentence equalizes every outcome: §11 "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane." It denies consequence transfer on the named axes for any outcome and ranks nothing. The remaining consequence-adjacent sentences are procedural and attach to failure states, not to any result: §4's stop classes' "The census does not proceed" and "No tally is filed"; §4.2's "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" — a filing precedence, not a weight. §3's filing order and C3's reason-code tie-break are mechanical, and the tie-break says of itself it "is a tie-break by the specificity of the evidence, not a ranking of the values." No sentence states what any outcome does for the lane or for anyone.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? I cannot state what it is FOR. What the study DOES with it is visible only as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed and "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4 item 7); "the auditor re-classifies the record from the pinned sources" (C3) and the audit reports MATCH/MISMATCH "per re-classified input origin" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) — origin never gates the arithmetic, and §1 states "The reproduction verdict and the provenance fields are recorded separately." But no sentence states any analytic use — no tally, outcome, comparison or conclusion computed from `origin`. ORIGIN_PURPOSE=CANNOT_STATE.

Q3 (hypothesis-reachability). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Reproduction failures are anticipated as live possibilities and pre-worded non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3). (iii) The set-versus-derived composition of the corpus is a wanted, reported output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3). (iv) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3). (v) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried not reconciled, audit re-classification; nothing says what it is weighed against. (vi) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. (vii) Process history leaks via "(V24)" in C4 — heavy prior iteration on exactly this leak surface. Adversarial reconstruction of the stake, stated as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their stated inputs, and how much of the corpus rests on chosen/fitted/imported/undeclared inputs rather than derived/standard/measured ones — with failures possible and reputationally delicate (hence the pre-committed neutral wording). No directional expectation, no comparison model, no preferred tally, no "pattern" is reachable: no sentence states what the lane hopes the census shows.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT (the attempt stops at the absent input; even taken as co-occurring, the order decides). A real claim fitting none: none exists — a claim either states a procedure or not; each input is printed/standard (arithmetic group), named-but-unmachineable (BLOCKED), or untraced (ABSENT); the attempt completes, times out, or lacks machinery (NOT_EVALUABLE). Total. A real claim fitting two per-claim outcomes after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on: (a) a printed-but-chosen input — as a candidate the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (the census prints "author_specified_input=<n>" beside the denominator — observed in my census runs); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), and the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED — executed: my seat ledgers validate PASS, merge exit 0, and compute prints "fa.txt#1 rests_on=['USES_CHOSEN', 'USES_FITTED'] root_origins=['CHOSEN'] DISPUTED" on a chosen+fitted disagreement; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same executed run); (c) an imported value — the external-import branch (verified manifest row, numeric token, first symbol-and-value line) and the local-import branch are both enforced by validate (kit controls; code lines 151-181), consumed; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT. One outcome, one rests_on, no reproducer choice anywhere: seats never write rests_on (compute REJECTS a ledger arriving with root_origins or rests_on set, exit 2 — the kit asserts it; my own validate run rejects a seat-authored rests_on field: "seat-authored ledger carries a field outside the schema: ['rests_on']", exit 1), and the reproduction question never asks why an input is what it is.

## 2. CONTROLS

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts (observed: the "declared:" and "recomputed: ... reconciled=YES" lines, then C1_DENOMINATOR_PRINTED=PASS, including the final run); C3 PASS only on printed validate runs — "a token asserted without the printed run is FAIL"; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate ≤10%, and no MISMATCH. The two controls passable by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete". Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes," plus §4's stop sentence and C2's per-session NOT_RUN.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with the floor the document itself states. The wrong-but-plausible record: a paper line printing "We adopt a = 3 from X" filed CHOSEN with ORIG_CHOICE_STATED and that true sentence as its quotation passes validate — the machine checks that the quotation occurs at the cited line and that reason_code maps to origin (and, since V30, that every non-silent record of every status carries that quotation), not that the code is the right one for the sentence; the citation precedence (ORIG_CITATION first) is a rule seats apply, not a machine test. Which mechanisms catch it, as written: (1) the second seat's independent classification — a disagreement is carried, never reconciled: executed this session, merge prints "merged 3 records; origin disagreements=1", PARENTS_DISPUTED=1, exit 0, and compute prints the pair "rests_on=['USES_CHOSEN', 'USES_FITTED'] root_origins=['CHOSEN'] DISPUTED"; above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification — an auditor classification matching neither complete declared branch is MISMATCH at compare (my fixtures below), and one matching a declared alternative is bound to that branch and compared like with like. The machine floor that IS enforced is real: the same citation misfiled with its value line in another file FAILs validate ("such a record must be IMPORTED with ORIG_CITATION" — the kit exercises both import branches). The stated residual floor (C3's lane-side span): a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away." That is the answer, and it is stated rather than implied away.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) contains no pattern content; I read the built packet in full and found none either. Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator (observed in my census output); a contrary claim asserted as the paper's own result cannot be quietly re-sorted — both seats must agree, and C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded passage an OMISSION that files CENSUS_AUDIT_FAILED (the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt, deterministically regenerated in my run, emits exactly these cases: both seats omit a required passage; its reverse; a passage the auditor excluded that the seats never enumerated; disputes at 10% and 15%). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans. rests_on is computed by script, not seats. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — all stated in the document rather than implied away. One robustness note, not a finding: the V30 identity binding (a reconstructed record's enclosing claim key must be its sealed owner) is enforced mechanically but stated only in the lane-side §3 span; an auditor filing a cross-claim dependency under the consuming selected claim gets a named C6_IDENTITY MISMATCH even with perfect content. That fails safe — never a false PASS — resolves in the open, and the correct filing is discoverable from the `<owned file>#` id form and the sources; I do not file it as a defect.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master: I re-ran the pinned builder with the printed command form to a scratch output (`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out /tmp/r3c2_v30_fx/pkt/R3C2_SEAT_PACKET.md --brief r3c2_seat_packet/SEAT_BRIEF.md`): first line "C4_PACKET_REDACTED=PASS", then "master sha256 57de1252…" (= ACCESS_SHA) and "seat packet sha256 e195a57a…" (= PACKET_SHA), "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (same digest; pin sheet row 1 re-verifies). The packet's line 9 names the master it was built from; that digest equals ACCESS_SHA (compared programmatically). A missing brief fails closed (exit 1, "SEAT_BRIEF.md missing beside the packet", nothing written — exercised). The C4 mechanism does what it states: a redacted out-of-lane copy, a kernel sandbox profile (r3c2_seat_sandbox.sb digest 6978d590… verified on disk, equal to the C4 pin) whose permitted surface the text prints, plus self-report lists with the completeness disclaimer. Its limits are stated honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible"; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is the principal's ruled floor and is recorded as such in C4. My own packet-first answers (above) are the empirical check on this seat: consequence equalized, origin purpose not statable analytically, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (merged candidate file, exclusion ledger, merged input ledger with its compute output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (code line 277; refusal exercised by the V29 seats and re-implemented unchanged in these bytes). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes, metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it). The packet's "error" vocabulary is audit-internal only ("may detect an error", "shared reader error") — never an outcome label; verified by grep. The positive outcome's name matches its mechanical tests (rounding half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY matches its membership exactly — my compute run on a chosen+fitted ledger emitted the USES_CHOSEN/USES_FITTED pair marked DISPUTED, never the clean label; R3C2_NO_CLASS is bounded to C0–C5b with C6/seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases. The exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — the kit asserts it).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): with ONE known cosmetic exception (the manifest header clause, codex V29 C1, declared-deferred — below), everything the document describes executed as delivered. Each thing checked, and how: /usr/bin/python3 (runs, "Python 3.9.6", exit 0); /usr/bin/shasum (runs; interpreter digest b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9); sympy (imports under -E, 1.14.0); the user site path prints (/Users/duhokim/Library/Python/3.9/lib/python/site-packages) and the pinned r3c2_manifest.py ran live over it (FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0) — the two `-c` one-liners were run as equivalent one-line script files because my referee shell blocks `-c`, a restriction on me, not on the text; r3c2_ledger_tools.py (digest 73000335… verified = the §3 pin and both pin sheets; census two-argument and final and validate three-argument executed PASS on my fixtures; all five audit subcommands executed on my fixtures); r3c2_lane_tools.py (digest 1da02f3b… verified = the C3 and §9 pins; merge exit 0 with PARENTS_DISPUTED=1 and "origin disagreements=1", compute exit 0 printing the DISPUTED pair); r3c2_batch_tools.py (digest 3822f12c… verified = the C1 pin; `partition R3C2_CORPUS_MANIFEST.md 12` reproduces the pinned partition_12_of_89.json BYTE-IDENTICALLY, digest 778de7a2… = the kit pin and C1's cited prefix); r3c2_manifest.py (digest 6b6e4bce… verified = the C5 pin; the live 20,637-file run above); r3c2_timeout.py (digest fbb9bef7… verified = the §9 pin; the wrapper prints WRAPPER_COMMAND/stdout/stderr/WRAPPER_EXIT and carries child exit 0 and exit 2; a 0.5-second deadline on a 3-second child printed SYMBOLIC_TIMEOUT and exited 124 at WRAPPER_ELAPSED=0.504); r3c2_build_seat_packet.py (digest 56ac7d1c… verified = the C4 pin; rebuild byte-identical, section 4); r3c2_seat_sandbox.sb (digest 6978d590… verified = the C4 pin); R3C2_CORPUS_MANIFEST.md (digest 7f97591b… verified = the §2 pin; all 89 enumerable rows' sha256, byte counts AND non-blank line counts under the stated V30 character-level convention recomputed from the pinned bytes — all equal, 0 mismatches, total 108,206 non-blank lines; all 79 RAW rows' sha256 and byte counts equal; declared source location ../bhu-reading-20260823/sources/); the two pin sheets (R3C2_SEAT_PACKET.sha256 8/8 OK; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 10/10 OK, lane-relative, re-verified after the kit run); the kit (run: last three lines "deletion_probes=61", "controls=174 passed=174 failed=0", "STAGED_TESTS=PASS"); R3C2_V23_DESIGN_OF_RECORD_55b466fa.md (exists, metadata only). Stage-generated artefacts (dispatch inventories, OWNERSHIP lists, seals, receipts, provider logs, check sheets) are produced by the stages themselves; their absence before an authorized run is not a missing delivered-tool finding. Stall handling: the 120-second cap is enforced by the committed wrapper (executed); REPRO_NOT_EVALUABLE carries SYMBOLIC_TIMEOUT / MACHINERY_UNAVAILABLE; one repeat permitted, meaningful only for NOT_EVALUABLE; stop classes route later limbs' controls to NOT_RUN.

FINDINGS (verbatim quote, defect, exact replacement). One COSMETIC item, already filed by the V29 codex seat (its C1) and declared-deferred by the lane; re-verified present in this version. It changes no class filed, no control's passability, no reachability, no conclusion and no tested value.

C1 (COSMETIC, carried, declared-deferred): the corpus manifest's informational line-count column still says it is "consumed by no tool" while the partition tool consumes it.
Quote (manifest line 6, pinned by §2): "| # | file | sha256 | bytes | non-blank lines (convention, stated V30: the pinned bytes decoded as UTF-8 and split on the newline character U+000A only; a line is non-blank if it contains any character other than U+0020 space, U+0009 tab and U+000D carriage return — a non-breaking space or other Unicode space counts as content; informational, consumed by no tool) |".
Defect: r3c2_batch_tools.py line 31 parses that column and sums it into the partition ("nonblank_lines":sum(r[4] for r in sl)); the clause "consumed by no tool" is therefore not true of the delivered partition tool. Verified this session: the column's numbers themselves are now correct on all 89 rows under the stated convention (my recomputation), and no integrity predicate consumes the sums (grep of seal/join/coverage: no reference) — the defect is the header clause only.
Exact replacement for the column text: "non-blank lines (convention, stated V30: the pinned bytes decoded as UTF-8 and split on the newline character U+000A only; a line is non-blank if it contains any character other than U+0020 space, U+0009 tab and U+000D carriage return — a non-breaking space or other Unicode space counts as content; informational, summed and reported by the partition tool, not used as an integrity predicate)". Re-pin the manifest in §2 and regenerate/re-pin the partition if applied — as the lane's own deferral note states, the manifest is pinned in this master, so the wording change belongs to the next version.

## Q7 (V30) ANSWERS

(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK (packet e195a57a…, brief ae445233…, timeout fbb9bef7…, ledger 73000335…, manifest 6b6e4bce…, builder 56ac7d1c…, batch 3822f12c…, lane 1da02f3b… — each equal to the master's named operative pins); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative; the five staged tools byte-identical to the delivered ones); both re-verified exit 0 after every run. The packet's line 9, "Built from master sha256 `57de1252d5f9d4592181517e70d0bca1c715830ccbb595f4bc9622a18e4779fd` …", equals ACCESS_SHA (compared programmatically): PACKET_BUILT_FROM_ACCESS_SHA=YES. The corpus manifest's digest 7f97591b5fd4a138971cb2a6737ee500908ba551c04d08678abf3307a16b4d77 equals §2's pin; all 89 enumerable rows' sha256, byte counts and V30-convention non-blank line counts recomputed from the files at the declared source location (mismatches: NONE; duplicates: none); all 79 RAW rows verified likewise. The kit runs (/usr/bin/python3 -E r3c2_staged_tests.py in r3c2_staged_d1d7): last three lines verbatim — "deletion_probes=61", "controls=174 passed=174 failed=0", "STAGED_TESTS=PASS"; the kit pin sheet re-verifies OK afterward (the exhibit is deterministically rewritten; confirmed). The V30 partition regenerates byte-identically (778de7a2adf1f03eaaeae0040589214f29558cf78809c49a7dbc9780ad0b9192 = C1's cited prefix and the kit pin).

Own fixtures, all against the delivered pinned tools (full pipeline where applicable: seal-enumeration → select → handout → seal-rederivation → compare; tokens verbatim from stdout; seeds 64 lowercase hex):
- ONE INPUT ID UNDER TWO CLAIMS (refused at the seal): audit seal-rederivation exit 1 — "FAIL: fb.txt#1/fa.txt#i1: input_id already reconstructed under fa.txt#1 — every input_id occurs exactly once across the reconstruction", "re-derivation NOT sealed: the reconstruction is incomplete"; no seal file written.
- EXPLICIT IDENTITY FIELDS CONFLICTING WITH THEIR KEYS (refused at the seal): exit 1 — "FAIL: fa.txt#1/fa.txt#i1: explicit claim_id 'fb.txt#1' conflicts with its enclosing claim fa.txt#1" and "FAIL: fa.txt#1/fa.txt#i2: explicit input_id 'fa.txt#iZ' conflicts with its key"; not sealed.
- A DEPENDENCY RECONSTRUCTED UNDER A CLAIM THAT IS NOT ITS SEALED OWNER (MISMATCH at compare): compare exit 1 — "FAIL: C6_IDENTITY: fa.txt#i1 reconstructed under claim fb.txt#1, but the sealed record belongs to claim fa.txt#1"; C6_AUDIT_SAMPLE=FAIL. The record's content was otherwise identical and the root sets agreed — without the identity binding this fixture would PASS.
- A HYBRID RECORD — primary origin and evidence with the alternative's parent list — WITH AGREEING ROOT SETS (MISMATCH): compare exit 1 — "FAIL: AUDIT fa.txt#1: MISMATCH (input fa.txt#i2: derived_from ['fa.txt#i3'] vs sealed ['fa.txt#i1'] (alternative ['fa.txt#i3']))"; C6_AUDIT_SAMPLE=FAIL; C6_AUDIT.json roots row: audit ['CHOSEN'] = sealed_under_matching_branch ['CHOSEN'] — the hybrid is MISMATCH even though the root sets agree.
- THE COMPLETE ALTERNATIVE BRANCH (PASS): the auditor files the alternative origin, alternative evidence and alternative parents together; compare exit 0, C6_AUDIT_SAMPLE=PASS.
- THE UNCHANGED-ORIGIN PARENT ALTERNATIVE, BOTH LIMBS: origin and evidence unchanged with the alternative parents → compare exit 0, C6_AUDIT_SAMPLE=PASS; the same alternative parents with the evidence also changed → exit 1, "MISMATCH (… origin_evidence.source_line 6 vs sealed 5; origin_evidence.verbatim y from z instead vs sealed y = x + 1; derived_from ['fa.txt#i3'] vs sealed ['fa.txt#i1'] (alternative ['fa.txt#i3']))", C6_AUDIT_SAMPLE=FAIL.
- Lane-side executions: merge on disagreeing seat ledgers exit 0 ("PARENTS_DISPUTED=1", "FIELD_DISAGREEMENTS=0", "merged 3 records; origin disagreements=1"); compute exit 0 ("fa.txt#1 rests_on=['USES_CHOSEN', 'USES_FITTED'] root_origins=['CHOSEN'] DISPUTED"); validate rejects a seat-authored rests_on (exit 1); census and census final PASS with declared = recomputed.

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 (the two `-c` one-liners as equivalent one-line files; my referee shell blocks `-c` — a restriction on this referee, not on the text's executability). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the master's printed spans are the STANDARD table's markdown cell separators; the manifest is ONE process, run live over 20,637 files); the §9 wrapper is one invocation that prints and carries its child's exit status (0 and 2 carried; SYMBOLIC_TIMEOUT/124 at 0.504s on a 0.5s deadline); the kit's 61 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative. Where exit codes matter in this report they were captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=NO — item by item over the V29 gate's findings, each checked against the current master text and the delivered pinned tools with my own fixtures:
- codex F1 (reconstruction identity not validated: wrong claim binding and duplicate shadowing survive PASS) — CLOSED: master §3 now requires uniqueness of every input_id across the whole reconstruction and key agreement of explicit identity fields before sealing, and binds each reconstructed record to its sealed claim at comparison; code implements all three (recon_schema_fails lines 291-308; C6_IDENTITY lines 356-359); my fixtures: duplicate id refused at seal, explicit claim_id/input_id conflicts refused at seal, a perfect-content dependency under the wrong claim is MISMATCH via C6_IDENTITY at compare.
- codex F2 (the alternative matcher accepts a hybrid matching neither complete declared branch) — CLOSED: master §3 now matches a declared alternative "only as a COMPLETE branch" with "an unchanged-origin parent alternative only when the alternative's origin and evidence are unchanged", a hybrid being "MISMATCH even when the root sets agree"; code implements the complete-branch test (lines 380-396); my fixtures: hybrid with AGREEING root sets MISMATCH, complete alternative branch PASS, unchanged-origin parent alternative PASS, alternative-parents-plus-changed-evidence MISMATCH.
- codex C1 (manifest header says the line column is "consumed by no tool" while the partition tool sums it) — NOT (declared-deferred): verified still present exactly as described — manifest line 6 "informational, consumed by no tool" versus r3c2_batch_tools.py line 31 summing the column into every batch's nonblank_lines; the lane's deferral statement (wording change belongs to the next version because the manifest is pinned in this master) is accurate; cosmetic; carried as C1 above with an exact replacement.
- kimi N1 (the manifest's line-count column disagreed with its stated convention on 9 of 89 rows) — CLOSED: the V30 header states the convention at the character level (U+000A split; blank = only U+0020/U+0009/U+000D; a non-breaking space counts as content) and my recomputation of all 89 rows from the pinned bytes under that convention matches every row (sha256, bytes and line counts; 0 mismatches); the manifest is re-pinned (7f97591b… = §2, verified) and the partition regenerated (byte-identical regeneration this session).
- kimi N2 (a duplicated comment line in the delivered seat tool) — CLOSED: the duplicate fragment is absent from the current bytes (lines 38-40 now read as one coherent comment plus the CODE_PRECEDENCE assignment); the new pin 73000335… verifies in the master, both pin sheets and the kit; the kit passes on these exact bytes.

(d) NEW executable defects: NONE found. Every required fixture behaved as the V30 text claims, the parent-alternative limbs were exercised in both directions, the builder, manifest, partition, merge, compute, wrapper and all five audit subcommands executed as printed, and the one open wording item (C1) is the lane's own declared deferral, cosmetic, with its exact replacement carried above. Why SOUND_WITH_REPAIRS and not SOUND: one cosmetic as-delivered discrepancy (C1) remains open by deliberate deferral. Why not UNSOUND: every substantive V29 repair executed as repaired on my own fixtures; Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds before the steps it must precede; fairness wording holds; and every described artefact executed as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 57de1252… = ACCESS_SHA; packet e195a57a… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v30_fx/, not corpus data and not a census.

R3C2_V30_GATE_COMPLETE
