ACCESS_SHA=7c661ac0c6157bff51062bf1a578a972e65986de7c3d9434e9a8419c0621aa35
PACKET_SHA=0c876a17949eae383196b78693fd79132590a92becdfdfeab963416c7302ad4d
GATE=PREREG_UNSOUND
LEAK=Careful provenance classification and special attention to author-specified inputs; cosmology identifiable; no directional hypothesis, preferred comparison model or empirical payoff reconstructed.
CONSEQUENCE_VISIBLE=Procedural only: “The census does not proceed” for CENSUS_ORIGIN_DISPUTED; no substantive reproduction outcome given greater external consequences.
ORIGIN_PURPOSE=Independent audit comparison: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” Subsequent interpretation cannot be stated.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only review of the unsigned V31 draft. The requested V30 counterexamples are repaired, but two new synthetic counterexamples change the audit verdict without changing the substantive reconstruction. These determine UNSOUND. No corpus derivation, census dispatch, adoption, signature or interpretation was performed. Section 10 supplies no evidence for this verdict.

The master was hashed first. The packet was then hashed and read before the master. Q7's specific execution requirements were treated as authorization to inspect the named kit, delivered tools, brief and pin sheets, and to verify corpus metadata and digests. Only the two expressly permitted V30 reports were consulted after the master; no other gate files were sought. The interpretation protocol was never opened.

Packet-first answers, in the requested order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

No sentence supplies a greater external consequence for a substantive reproduction result. “No tier, warrant token, standing or stamp moves.” Procedural consequences are explicit: the origin-dispute class says, “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” My initial packet-first answer distinguished these procedural outcomes from an external stake; the header makes that distinction explicit rather than claiming that all procedural consequences are invisible.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR?**

For the eventual interpretation, I cannot. For the literal question about any subsequent use, yes: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” The packet also says, “The reproduction verdict and the provenance fields are recorded separately.” My initial CANNOT_STATE answer concerned eventual study use and was too broad if read as denying this explicit audit use. This clarification uses only packet sentences, not a mapping learned from the master.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

The packet reveals interest in provenance and author choices: “Provenance is transitive,” and “`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator.” Its seven origin labels and independent classification requirements reinforce that inference. “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” identifies subject matter. I cannot infer an expected distribution, a directional hypothesis, a preferred comparison model, a shape/magnitude pattern or an empirical payoff. The packet supplies neither a ranking of origins nor the interpretation mapping. The provenance interest is the explicitly accepted taxonomy floor, not a newly reconstructed stake.

## 1. OUTCOME CLASSES

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES for the outcome definitions. Provenance does not prohibit consuming a stated value. The procedure produces either an arithmetic decision or a named bounded non-arithmetic result; it does not promise to decide arbitrary mathematics within 120 seconds. The executable audit defects below are separate from this definition.

Concrete physical-claim examples, constructed for this review rather than attributed to an unread corpus paper:

| Claim and stated recipe | Unique filed outcome | rests_on |
|---|---|---|
| “We choose a = 3 m; L = 2a = 6 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same inputs and recipe, printed result 7 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 3 m; L = 2a = 6 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 3 m from X; L = 2a = 6 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a not printed locally, but named X has its first symbol/value line in verified enumerable text | REPRO_WITHIN_STATED_PRECISION if the arithmetic agrees | USES_IMPORTED |
| a traced to X, which is outside the enumerable manifest | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, but a neither printed nor traced to a source | REPRO_INPUT_ABSENT | USES_UNDECLARED for a silent ABSENT record |
| All inputs available; stated calculation exceeds the cap or needs unavailable machinery | REPRO_NOT_EVALUABLE | Computed independently from its recorded origins |
| “Our result is L = 6 m,” with no producing operations | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED if there are no input records |

The chosen input numeral is separately excluded as AUTHOR_SPECIFIED_INPUT; the asserted derived result remains included. A fitted parameter asserted as an own result is itself eligible for enumeration; its downstream use is still permitted.

A claim with both an unpinned cited input and an uncited missing input satisfies two raw terminal conditions, but the explicit precedence files only REPRO_BLOCKED. A claim with no producing procedure cannot also be filed as arithmetic failure. No double-filed or unfileable claim was demonstrated after applying precedence.

Study-level witnesses, assuming earlier controls pass unless specified: one arithmetic claim and C6 PASS gives CENSUS_COMPLETE; one absent-input claim and a matching audit gives CENSUS_PARTIAL; absent external seed gives CENSUS_AUDIT_FAILED; both seats failing C3 twice gives R3C2_NO_CLASS; surviving candidate/input-list disagreement gives CENSUS_DENOMINATOR_DISPUTED; surviving outcome disagreement gives CENSUS_OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives CENSUS_ORIGIN_DISPUTED; one seat passing and one failing a control after the allowed attempts gives CENSUS_CONTROL_SPLIT.

An absent-input tally plus missing audit seed satisfies PARTIAL and AUDIT_FAILED conditions, but precedence files AUDIT_FAILED only. An entirely empty enumeration on both sides can audit successfully and then file PARTIAL; zero included claims with candidate passages instead fail the audit as specified. INCONCLUSIVE is genuinely reachable through a nonempty PARTIAL census. I found no unreachable study class. These are referee witnesses, not a substitute for the required independent two-seat C0 exhibition.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

Yes as cited, independently reviewable evidence; no as a guaranteed machine determination of provenance semantics. A quotation match establishes occurrence, not correct interpretation or reason-code precedence.

My wrong-but-plausible ledger record was:

```json
{
  "claim_id": "fixture.txt#q0",
  "input_id": "fixture.txt#wrong",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "fixture.txt",
    "source_line": 2,
    "verbatim": "We adopt a = 3 from Reference."
  },
  "derived_from": [],
  "value": "3",
  "source_file": "fixture.txt",
  "source_line": 2
}
```

The delivered validator returned exit 0, C3_NO_SUBSTITUTION=PASS. The correct origin is IMPORTED under the citation-first rule. The second independent seat can identify that citation and preserve the disagreement; C6 can independently classify it as IMPORTED and mismatch an unsupported CHOSEN-only sealed record. Neither the numeric-token check nor the quotation check catches this semantic mistake. All readers can share it, and C6 reclassifies only selected claims. The master states this limitation honestly.

Every control has an exact three-valued contract: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their artefacts are exhibitions, executions, seals, audit JSON, path lists or provider logs. C4/C5b can pass on an asserted path list and explicitly disclaim its completeness. A naked PASS does not satisfy the printed-run controls. Unreached controls are NOT_RUN; per-session validation is NOT_RUN until integration.

**F1 — NON-COSMETIC: the merge loses the alternative branch's required origin_search, making a supported alternative fail audit.**

Verbatim §3 sentence:

> An input matches only if its origin, origin evidence and parent list jointly equal the complete primary branch or the complete alternative branch, every declared alternative field applied together and undeclared fields left primary.

Verbatim C3 lane-side description:

> *(Lane side: `r3c2_lane_tools.py merge` adds `origin_alt` and `origin_evidence_alt`; `compute` adds `root_origins` and per-claim `rests_on`.)*

Counterexample: ten included claims; only q0 has a disputed input, so the disagreement affects exactly 10%, not the stopping threshold of more than 10%. The source says “a = 3 for the calculation.” Seat A files CHOSEN with that quotation; seat B files UNDECLARED/ORIG_SILENT with a multi-term origin_search. These alternative readings need not be reconciled. Both ledgers pass validate. Merge and compute exit 0 and compute reports q0 DISPUTED with [USES_CHOSEN, USES_UNDECLARED]. The other nine toy claims have input-free stated recipes.

The auditor reconstructs seat B's complete record, including exactly B's search. Both audit seals succeed. Compare identifies branch alt and equal UNDECLARED root sets, but returns q0 MISMATCH, “origin_search differs,” C6_AUDIT_SAMPLE=FAIL, exit 1. Merge retained neither origin_search nor origin_search_alt from B, and compare checks the reconstruction against the primary record's absent search.

Swap A and B while retaining the same auditor reconstruction: merge exits 0 and compare returns C6_AUDIT_SAMPLE=PASS, exit 0. Thus arbitrary seat ordering changes the audit verdict. This is an execution defect, not a disagreement about which reader's classification is correct.

Exact replacement for the §3 sentence:

> An input matches only when its origin, origin evidence, parent list and any required origin_search jointly match one complete declared branch. Merge preserves the primary branch's origin_search and the alternative branch's origin_search_alt whenever that branch uses ORIG_SILENT; audit compare checks the search belonging to the matched branch. Undeclared alternative fields retain their primary values except that origin_search is required only for a branch using ORIG_SILENT. A reconstruction matching neither complete branch is an unconditional record-level MISMATCH propagated to every dependent selected claim.

Exact replacement for the quoted C3 parenthetical:

> *(Lane side: `r3c2_lane_tools.py merge` preserves branch-specific provenance evidence, adding `origin_alt`, `origin_evidence_alt` and, when the alternative uses ORIG_SILENT, `origin_search_alt`; it also preserves disputed parent lists. `compute` adds `root_origins` and per-claim `rests_on`.)*

Implementation must preserve B's search, compare it against the selected branch, retain the unconditional branch guard, and re-pin changed deliverables. Exact-branch positives and changed-search negatives must work in either seat order.

**F2 — NON-COSMETIC: JSON object key order changes an audit result.**

Verbatim executable sentence in r3c2_ledger_tools.py:

```python
if aev.get("reason_code")=="ORIG_SILENT" and str(ar.get("origin_search"))!=str(sr.get("origin_search")): diffs.append("origin_search differs")
```

On F1's passing reversed-seat case, I reversed only the insertion order of the query/files/matches keys in the auditor's origin_search object. The complete parsed reconstruction remained equal to the original Python JSON object; it was independently re-sealed. Compare changed from PASS to FAIL, again reporting “origin_search differs.” Python dictionary display strings are order-sensitive; JSON object membership is not.

Exact replacement, coordinated with F1:

```python
expected_search = sr.get("origin_search_alt") if branch == "alt" and sr.get("origin_alt") else sr.get("origin_search")
if aev.get("reason_code") == "ORIG_SILENT" and ar.get("origin_search") != expected_search:
    diffs.append("origin_search differs")
```

This compares JSON structure while retaining differences in actual search contents. Re-pin after implementation. This changes control passability and a tested verdict, so it is not COSMETIC.

## 3. CIRCULARITY

The inclusion rule contains no model preference; every candidate and exclusion must remain visible. Independent enumeration, zero-tolerance seat reconciliation and C6's independent whole-corpus enumeration obstruct quiet exclusion. An auditor-only passage is an omission even when the auditor excludes it. Disposition disputes up to 10% remain reportable rather than hidden; common omissions by all readers remain possible.

Provenance classification retains human judgement, but seats do not receive the interpretation ranking. The interpretation protocol is receipted before limb A. These prevent a disclosed directional expectation from becoming the operative reproduction criterion; they do not prove absence of prior exposure or common reader error.

F1 introduces an unintended channel: which seat is made primary changes whether an otherwise supported provenance reading survives audit. That is a mechanical source of asymmetry, not evidence of intentional manipulation. No contrary claim can be legitimately discarded without a ledger trace under the written method.

## 4. THE BLIND

The pinned builder was executed with explicit master, output and brief arguments, writing a scratch packet. It printed C4_PACKET_REDACTED=PASS, kept sections 1–6, 9 and 11, dropped 0, 7, 8 and 10, and removed 51 inline spans. The 40,267-byte result is byte-identical to the pinned packet; its built-from digest equals ACCESS_SHA.

The code checks required content and forbidden strings in the packet and brief. This supports exactly the finite-list redaction claim. It does not prove semantic non-disclosure. The packet-only inference above stays within the acknowledged taxonomy floor.

The sandbox profile exists and its digest matches the master. I inspected its actual exceptions: runtime/configuration directories, the working directory and user site are readable; network access is permitted. This agrees with the operative disclosure that it does not deny all outside access. C4/C5b remain self-report controls. Future dispatch must print effective exceptions and live probes; this review did not launch a census seat or attest a dispatch. The stated limitations on startup code, prior exposure, snapshot pinning and incomplete path lists are material and honestly stated.

## 5. THE SEAL

Section 7 has the required ordering: receipt P binds the protocol hash and commit before limb A; receipt T binds the named tally files and commit before protocol opening. Both must be preserved, and the custodian independently verifies the hashes and commit identifiers after opening. A missing receipt or mismatch files CENSUS_AUDIT_FAILED and leaves interpretation NOT_RUN.

This binds the committed record before the relevant steps, conditional on carrying out the custody procedure. It does not establish what the lane owner previously guessed or read. The protocol's existence was checked by metadata only: 3,947 bytes. Its contents and any actual receipts were not inspected; no execution-stage custody is certified here.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error”; the brief applies that wording to negative outcomes generally. Other non-arithmetic outcomes describe missing inputs, unavailable sources, unspecified procedures or computational limits without alleging a paper error. Uses of “error” about readers or audit mechanics do not label a scientific result.

REPRO_WITHIN_STATED_PRECISION matches its rounding/uncertainty definition. DERIVED_STANDARD_OR_MEASURED_ONLY names the allowed root set without claiming a purely derived result. CENSUS_COMPLETE means completion of reproduction attempts, not universal success. No additional outcome-label overclaim was demonstrated. F1 and F2 concern audit verdicts, not cosmetic labels.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES: supported silent alternative branches and structurally equal reordered search objects are not compared as promised, demonstrated by F1 and F2. The commands themselves launch, the required delivered tools exist, and C5 is executable under the text's scope. Those facts do not cure the audit defects.

Checks performed:

| Item | Verification and result |
|---|---|
| Master and packet | Independently hashed; packet read first; master subsequently read in full; both hashes unchanged at end |
| R3C2_SEAT_PACKET.sha256 | Verified from working directory: 8/8 OK, exit 0, before and after execution |
| r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 | Verified lane-relative: 10/10 OK, exit 0, before and after execution |
| Corpus manifest | SHA256 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 equals §2 |
| All 89 enumerable source rows | At ../bhu-reading-20260823/sources/: all files exist and all byte counts and SHA256 values match; metadata/digest only, no source-content interpretation |
| Partition | Re-generated from manifest into scratch; byte-identical to partition_12_of_89.json; SHA256 39b8d9bf199265ab678ab584c7cc2cff48327aac68f6134fb9266ec6f507a420 |
| r3c2_ledger_tools.py | Pin verified; census, census final, validate and all five audit subcommands executed on own fixtures |
| r3c2_lane_tools.py | Both operative pins verified; merge and mandatory three-argument compute executed; DISPUTED, root_origins, rests_on and NOT_COMPUTED observed |
| r3c2_batch_tools.py | Pin verified; partition executed independently; seal/join/coverage and separate limb-chain cases exercised by the delivered kit |
| r3c2_build_seat_packet.py and SEAT_BRIEF.md | Pins verified; explicit-argument rebuild byte-identical; brief's ownership, canonical filenames and integration command inspected |
| r3c2_manifest.py | Pin verified; live C5 invocation completed over the printed user-site directory |
| r3c2_timeout.py | Pin verified; 120.0-second invocation with fast child returns 0; failing child returns 7; short-cap probe prints SYMBOLIC_TIMEOUT and returns 124 at 0.104 seconds |
| r3c2_seat_sandbox.sb | Existence, full digest and effective profile text inspected; no new dispatch probes claimed |
| Interpretation protocol and named signed design of record | Exist, checked by metadata only; respectively 3,947 and 100,275 bytes; neither opened |
| Future artefacts and paths | OWNERSHIP_b<k>.txt, batch JSON/report files, integration files, seals, dispatch inventory, provider logs, receipts and dated check sheet are stage-produced; their absence before a run is not a missing delivered-tool finding |

An exploratory lookup of kit README.md failed because I guessed that filename; the pin sheet identifies README_STAGED_UNADOPTED.md, which exists and was read. This is not a missing artefact promised by the design.

C5 commands were executed literally, individually, with their own captured exits:

```text
/usr/bin/python3 -E --version
Python 3.9.6
EXIT=0
/usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"
1.14.0
EXIT=0
/usr/bin/shasum -a 256 /usr/bin/python3
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3
EXIT=0
/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"
/Users/duhokim/Library/Python/3.9/lib/python/site-packages
EXIT=0
/usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
EXIT=0
```

C5_EXECUTABLE_UNDER_SCOPE=YES: mandated commands and their loads are permitted by C4(iii); the resolved manifest directory is the expressly permitted pinned-environment directory. This is executability, not certification against an uninspected future dispatch record.

NO_MASKED_STAGE=YES: the operative mandated commands are individual invocations, not shell pipelines. The manifest walker fails directly; the wrapper carries child failure and timeout exits. No output-filtering stage supplied the reported exit statuses. This token does not claim that audit predicates are correct.

Q7(a), kit run: `/usr/bin/python3 -E r3c2_staged_tests.py`, working directory r3c2_staged_d1d7, exit 0. Last three lines:

```text
deletion_probes=61
controls=176 passed=176 failed=0
STAGED_TESTS=PASS
```

Own full audit pipelines used fresh synthetic fixtures, independently of the kit's fixtures:

| Fixture | Compare exit | Token | Dependent claim/input result |
|---|---:|---|---|
| Complete primary branch | 0 | C6_AUDIT_SAMPLE=PASS | MATCH |
| Primary origin/evidence with alternative parents | 1 | C6_AUDIT_SAMPLE=FAIL | MISMATCH: record matches neither complete declared branch |
| Alternative origin/evidence with primary parents | 1 | C6_AUDIT_SAMPLE=FAIL | MISMATCH: record matches neither complete declared branch |
| Complete alternative branch | 0 | C6_AUDIT_SAMPLE=PASS | MATCH, branch alt |
| Dependency assigned to a claim other than its sealed owner | 1 | C6_AUDIT_SAMPLE=FAIL | Consuming selected claim and dependency row both MISMATCH, with identity reason |
| Complete UNDECLARED alternative after actual validate/merge/compute | 1 | C6_AUDIT_SAMPLE=FAIL | MISMATCH: origin_search differs — F1 |
| Same two seats swapped; identical auditor reconstruction | 0 | C6_AUDIT_SAMPLE=PASS | MATCH — F1 |
| Passing case with only origin_search object-key order reversed | 1 | C6_AUDIT_SAMPLE=FAIL | MISMATCH despite equal parsed JSON — F2 |

Both hybrid fixtures have equal CHOSEN root sets on the reconstructed and sealed sides; root agreement does not mask the repaired full-record predicate. Wrong-owner identity likewise propagates even when the consuming claim's root sets agree. Every successful reconstruction seal precedes the comparison in these synthetic pipelines.

Q7(c), closure of the specifically requested V30 findings:

| Prior finding | Status | Fresh evidence |
|---|---|---|
| codex F1: identity conflict missing from dependent rows | CLOSED | wrong_owner/audit.json: fixture.txt#result and its fixture.txt#x dependency both MISMATCH with the identity reason |
| codex F2: reverse hybrid false PASS | CLOSED | Both hybrid directions fail unconditionally with equal roots; complete alternative passes |
| codex C1: manifest says column is consumed by no tool | CLOSED | Header now explicitly says summed and reported for per-batch totals, not an integrity predicate; partition code and byte-identical regeneration agree |
| kimi C1: same manifest-header discrepancy | CLOSED | Same independently verified header and partition evidence |

PRIOR_FINDINGS_CLOSED=YES refers to those concrete V30 findings. It does not mean that every possible alternative-origin case is repaired; F1 here is a new required-field counterexample.

**C1 — COSMETIC: kit README overstates the deletion-probe contract.**

Verbatim README text:

> `r3c2_staged_tests.py` — 176 controls: positives, negatives asserting the EXACT failure set, and 61 deletion probes (the check on a marked line
>   is neutralised in a copy of the tool; the matching negative must then PASS).

The current evidence/edge diagnostic probes intentionally keep the authoritative guard and expect exit 1. They do not require the whole negative to become PASS. Exact replacement:

> `r3c2_staged_tests.py` — 176 controls: positives, negatives asserting the EXACT failure set, and 61 deletion probes. Each probe neutralises a marked check in a copy and tests its declared expected result; probes of redundant diagnostics retain the authoritative branch guard and expect the audit still to FAIL.

This documentation repair changes no filed class, control passability, reachability, conclusion or tested value. Re-pin the README if changed.

Q7(d): NEW executable defects are F1 and F2. Their scripts, exact inputs, commands, full stdout/stderr, exits and audit JSON are preserved under /tmp/r3c2_v31_codex_review/, especially silent_clean.py, order_probe.py and silent_clean/. The requested branch and identity fixtures are in sibling directories there. All 89 source metadata/digest results are in source_metadata.json. No proposed repair was applied to the preregistration or tools.

R3C2_V31_GATE_COMPLETE
