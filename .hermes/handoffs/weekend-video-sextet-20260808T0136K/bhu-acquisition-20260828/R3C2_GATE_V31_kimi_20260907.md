ACCESS_SHA=7c661ac0c6157bff51062bf1a578a972e65986de7c3d9434e9a8419c0621aa35
PACKET_SHA=0c876a17949eae383196b78693fd79132590a92becdfdfeab963416c7302ad4d
GATE=PREREG_SOUND
LEAK=content-level only: the corpus is cosmology ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline", C3); failures are anticipated and pre-worded ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator", §3); provenance visibly matters (seven origins, quotations, two readers, disputes carried — the ruled floor); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11); a process tell survives ("closed boundary (V24)", C4) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader and audit integrity check, never an arithmetic input: "Every input's `origin` is classified independently by both seats" (C3), disagreements filed and "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (§4 item 7), the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch" (C6), and "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" (§3) — no analytic use (no tally, comparison or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 31, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Order kept: the ACCESS digest was printed first and only then the packet was opened and Q5/Q6/Q3 answered; the master was read after those answers were fixed; the two V30 gate reports were opened only after the master, as the brief permits. Files opened: the packet, the master, the two V30 gate reports, both pin sheets, the corpus manifest and its declared source directory (digests, byte counts and line-count recomputation only, never content), the kit r3c2_staged_d1d7/ (run, fixtures inspected as code), the delivered pinned tools (executed), the sandbox profile (digest only). The interpretation protocol was checked by metadata only (3,947 bytes), never opened. All fixtures are synthetic referee evidence under /tmp/r3c2_gate_v31_kimi_*, not corpus data and not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The one consequence-addressing sentence equalizes every outcome: §11 "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane." It denies consequence transfer on the named axes for any outcome and ranks nothing. The remaining consequence-adjacent sentences are procedural and attach to failure states, not to any result: the stop classes' "The census does not proceed" and "No tally is filed" (§4), and "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2) — a filing precedence, not a weight. §3's filing order and C3's reason-code tie-break are mechanical, and the tie-break says of itself it "is a tie-break by the specificity of the evidence, not a ranking of the values." CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Partly. What the study DOES with it is visible as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled, and "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4 item 7); "the auditor re-classifies the record from the pinned sources" (C3) and the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) — origin never gates the arithmetic — and §1 states "The reproduction verdict and the provenance fields are recorded separately." What I cannot state from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Reproduction failures are anticipated as live possibilities and pre-worded non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3). (iii) The set-versus-derived composition of the corpus is a wanted, reported output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3). (iv) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3). (v) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried not reconciled, audit re-classification; nothing says what it is weighed against. (vi) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. (vii) A process tell — "Scope, as a principle with a closed boundary (V24)." (C4): the seat can see the scope rule has a long iteration history on exactly this leak surface; content-free about the study's question. Adversarial reconstruction of the stake, stated as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their stated inputs, and it cares how much of the corpus rests on chosen/fitted/imported/undeclared inputs — with failures possible and reputationally delicate (hence the pre-committed neutral wording and the fencing sentence). No directional expectation, no comparison model, no preferred tally, no "pattern" is reachable: no sentence states what would confirm or disconfirm anything, and the dispute machinery punishes both directions symmetrically.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT (the attempt stops at the absent input; even taken as co-occurring, the order decides). A real claim fitting none: none exists — a claim either states a procedure or not; each input is printed/standard (arithmetic group), named-but-unmachineable (BLOCKED), or untraced (ABSENT); the attempt completes, times out, or lacks machinery (NOT_EVALUABLE). A real claim fitting two per-claim outcomes after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on: (a) a printed-but-chosen input — as a candidate the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator — observed in my census runs through the kit); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), and the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED with rests_on USES_CHOSEN — executed: my two synthetic seat ledgers (CHOSEN vs FITTED on the same value line) both validate PASS, merge exits 0 carrying "origin disagreements=1", and compute prints "fa.txt#1 rests_on=['USES_CHOSEN', 'USES_FITTED'] root_origins=['CHOSEN'] DISPUTED" — one outcome per seat, one script-computed rests_on, the disagreement printed as a marked pair, never reconciled; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same executed run); (c) an imported value — the external-import branch (verified manifest row, numeric token, first symbol-and-value line) and the local-import branch are both enforced by validate (kit controls; my own refiled-import negative below), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT with rests_on USES_UNDECLARED. Seats never write rests_on: compute REJECTS a ledger arriving with root_origins or rests_on set (exit 2, asserted by the kit), and validate fails a seat-authored ledger carrying a field outside the schema. The reproduction question never asks why an input is what it is; decidability and reachability no longer pull against each other because provenance left the verdict.

## 2. CONTROLS

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts; C3 PASS only on printed validate runs — "a token asserted without the printed run is FAIL"; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate ≤10%, and no MISMATCH. The two controls passable by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete". Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes," plus §4's stop sentence and C2's per-session NOT_RUN.

Q2 (asked directly): YES — checkable rather than asserted, with exactly the floor the document states. My wrong-but-plausible record (own fixture, own sources): fa.txt line 5 prints "We adopt b = 5 from Z (2021)." and I filed the input CHOSEN with ORIG_CHOICE_STATED, that true sentence as the quotation, value "5" at the same line. `validate` returned C3_NO_SUBSTITUTION=PASS, exit 0 — the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the right one for the sentence; the citation precedence (ORIG_CITATION first) is a rule seats apply, not a machine test. Which mechanisms catch it, as written: (1) the second seat's independent classification — a disagreement is carried, never reconciled (executed: merge prints "merged 1 records; origin disagreements=1", compute prints the DISPUTED pair), and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification — an auditor classification matching neither complete declared branch is MISMATCH at compare (my fixtures below), and one matching a declared alternative is bound to that branch and compared like with like. The machine floor that IS enforced is real: the same citation misfiled with its value line in another file FAILs validate — "FAIL: fa.txt#1.b: value line is in fb.txt but claim fa.txt#1 belongs to fa.txt: such a record must be IMPORTED with ORIG_CITATION (submitted CHOSEN/ORIG_CHOICE_STATED)", exit 1 (my fixture). The stated residual floor: a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away" (C3 lane-side note). That is the answer, and it is stated rather than implied away.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) contains no pattern content; I read the built packet in full and found none either (grep of the packet for the forbidden-list vocabulary — pattern, ΛCDM, the lane members' names, the engine names, the consequence words — returns zero; only "comparison" survives twice, both in C6's own mechanical sense of comparing ledgers). Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator; a contrary claim asserted as the paper's own result cannot be quietly re-sorted — both seats must agree, and C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded passage an OMISSION that files CENSUS_AUDIT_FAILED (the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt, deterministically regenerated in my run, emits exactly these cases: both seats omit a required passage; its reverse; a passage the auditor excluded that the seats never enumerated; disputes at 10% and 15%). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans. rests_on is computed by script, not seats. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — all stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master. I re-ran the pinned builder with the printed command form to a scratch output (`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out /tmp/r3c2_kimi_rebuilt_packet.md --brief r3c2_seat_packet/SEAT_BRIEF.md`): first line "C4_PACKET_REDACTED=PASS", then "master sha256 7c661ac0…" (= ACCESS_SHA) and "seat packet sha256 0c876a17…" (= PACKET_SHA), "sections kept ['1.', '2.', '3.', '4.', '5.', '6.', '9.', '11.'] dropped ['0.', '7.', '8.', '10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (same digest; pin sheet row 1 re-verifies afterward). The packet's line 9 names the master it was built from; that digest equals ACCESS_SHA (compared character for character). The C4 mechanism as described does what it states and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible"; the seat runs from a redacted copy outside the lane under a kernel sandbox profile (r3c2_seat_sandbox.sb digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface the text prints; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is the principal's ruled floor and is recorded as such in C4 with the ruling cited. My packet-first answers above are the empirical check on this seat: consequence equalized, origin purpose statable only as a check, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (the kit asserts the refusal; deletion probe turns it into PASS only with the check removed). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes, metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the packet's "error" vocabulary is audit-internal only — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute run on a chosen+fitted ledger emitted the USES_CHOSEN/USES_FITTED pair marked DISPUTED, never the clean label; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — the kit asserts it).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: /usr/bin/python3 -E --version → "Python 3.9.6", exit 0; sympy imports under -E → 1.14.0; /usr/bin/shasum -a 256 /usr/bin/python3 → b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; the user site path prints → /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py ran live over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, exit 0 (the two `-c` one-liners were run as an equivalent one-line script file because my referee harness blocks `-c` — a restriction on me, not on the text's executability); r3c2_timeout.py ran as `/usr/bin/python3 -E r3c2_timeout.py 120.0 -- /usr/bin/python3 -E --version` → prints WRAPPER_COMMAND, child stdout/stderr, WRAPPER_EXIT=0, WRAPPER_ELAPSED — one invocation carrying the child's status; r3c2_ledger_tools.py (digest 65b570be… = §3's pin and both pin sheets): census, census final, validate three-argument, and all five audit subcommands executed on my fixtures; r3c2_lane_tools.py (1da02f3b… = the C3 and §9 pins): merge exit 0 ("origin disagreements=1"), merge exit 1 with FIELD_DISAGREEMENTS=1 on a coordinate disagreement (fails in the open, nothing written), compute exit 0 printing the DISPUTED pair, compute's NOT_COMPUTED row on an empty ledger asserted by the kit; r3c2_batch_tools.py (3822f12c… = the C1 pin): `partition R3C2_CORPUS_MANIFEST.md 12` regenerated the pinned partition BYTE-IDENTICALLY (digest 39b8d9bf199265ab678ab584c7cc2cff48327aac68f6134fb9266ec6f507a420 = the kit pin and C1's cited `39b8d9bf199265ab…`), printing the per-batch line sums the manifest header now describes; r3c2_manifest.py (6b6e4bce… = the C5 pin): the live 20,637-file run above; r3c2_build_seat_packet.py (56ac7d1c… = the C4 pin): byte-identical rebuild, section 4; r3c2_seat_sandbox.sb exists with digest 6978d590… = the C4 pin; R3C2_CORPUS_MANIFEST.md exists with digest 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 = the §2 pin; R3C2_SEAT_PACKET.sha256 verifies 8/8; the kit pin sheet verifies 10/10; the interpretation protocol, the leak-floor judgement and the V23 design of record all exist (metadata only). Fields the scripts are said to compute — root_origins, rests_on, NOT_COMPUTED, the DISPUTED pair, origin_alt/PARENTS_DISPUTED — all observed live in my own runs. Paths a seat is told to use — `.` as the sole sources_dir, the working directory, the pinned interpreter and the printed site-packages directory — are consistent across §2, C2, C4 and C5. No described artefact is missing as delivered. NEW executable defects: NONE found.

## Q7 (V31) ANSWERS

(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK (packet 0c876a17…, brief 00004dc9…, timeout fbb9bef7…, ledger 65b570be…, manifest 6b6e4bce…, builder 56ac7d1c…, batch 3822f12c…, lane 1da02f3b… — each equal to the master's named operative pins); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative); both re-verified exit 0 after every run. The packet's line 9, "Built from master sha256 `7c661ac0c6157bff51062bf1a578a972e65986de7c3d9434e9a8419c0621aa35` …", equals ACCESS_SHA: PACKET_BUILT_FROM_ACCESS_SHA=YES. The corpus manifest's digest 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 equals §2's pin; all 89 enumerable rows' sha256, byte counts and stated-convention non-blank line counts recomputed from the files at the declared source location `../bhu-reading-20260823/sources/` (mismatches: NONE; duplicates: none); all 79 RAW rows verified likewise (digest and byte count). The kit runs (`/usr/bin/python3 -E r3c2_staged_tests.py` from its directory); its last three lines verbatim:

  deletion_probes=61
  controls=176 passed=176 failed=0
  STAGED_TESTS=PASS

and the kit pin sheet re-verifies OK afterward (the exhibit is deterministically rewritten; confirmed). The V31 partition regenerates byte-identically (39b8d9bf… = the kit pin and C1's cited prefix).

Own fixtures, all against the delivered pinned tools, full pipeline (seal-enumeration → select → handout → seal-rederivation → compare; tokens verbatim from stdout; seed 64 lowercase hex "0123456789abcdef"×4; my own claim ids k1–k4, my own sources and verbatim strings):

- BASELINE (primary branch, positive control): compare exit 0, C6_AUDIT_SAMPLE=PASS.
- HYBRID, FORWARD — primary origin+evidence (DERIVED/ORIG_EQUATION) with the ALTERNATIVE's parents ['i3']: compare exit 1 — "FAIL: AUDIT k1: MISMATCH (input i1: record matches neither complete declared branch; derived_from ['i3'] vs sealed ['i2'] (alternative ['i3']))"; C6_AUDIT_SAMPLE=FAIL.
- HYBRID, REVERSE — alternative origin+evidence (CHOSEN/ORIG_CHOICE_STATED) with the PRIMARY parents ['i2']: compare exit 1 — "FAIL: AUDIT k1: MISMATCH (input i1: record matches neither complete declared branch; root_origins differ: audit ['CHOSEN', 'STANDARD'] vs sealed ['STANDARD'])"; C6_AUDIT_SAMPLE=FAIL.
- REVERSE HYBRID WITH AGREEING ROOT SETS (my sharpest construction — declared alternative same origin DERIVED, alternative evidence, alternative parents; auditor files the alternative evidence with the PRIMARY parents): compare exit 1 — "FAIL: AUDIT k1: MISMATCH (input i1: record matches neither complete declared branch; origin_evidence.source_line 6 vs sealed 4; origin_evidence.verbatim …)"; C6_AUDIT.json roots row: audit ['STANDARD'] = sealed_primary ['STANDARD'] = sealed_under_matching_branch ['STANDARD'] — the full-record predicate decides the verdict even when the root sets agree, exactly as §3 states.
- COMPLETE ALTERNATIVE BRANCH (alternative origin + alternative evidence + alternative parents together): compare exit 0, C6_AUDIT_SAMPLE=PASS.
- DEPENDENCY RECONSTRUCTED UNDER A CLAIM THAT IS NOT ITS SEALED OWNER (i9's sealed owner is the off-sample claim k2; the auditor files it under kZ): compare exit 1 — "FAIL: C6_IDENTITY: i9 reconstructed under claim kZ, but the sealed record belongs to claim k2" AND "FAIL: AUDIT k1: MISMATCH (input i9: identity: reconstructed under claim kZ, but the sealed record belongs to claim k2)"; C6_AUDIT.json: the dependent selected claim k1's row result is MISMATCH and its i9 input row is MISMATCH with "identity: …" in why — not only a global failure.
- TRANSITIVE PROPAGATION (same mis-owned i9 two edges in: k1.i1 → k2.i8 → k2.i9): compare exit 1 with the same two FAIL lines; k1 result MISMATCH; rows i1 and i8 MATCH, row i9 MISMATCH — the conflict is carried through the whole closure.
- Lane-side executions: validate on both seats' ledgers PASS; merge exit 0 ("merged 1 records; origin disagreements=1"); compute exit 0 ("fa.txt#1 rests_on=['USES_CHOSEN', 'USES_FITTED'] root_origins=['CHOSEN'] DISPUTED"); merge on a value-line coordinate disagreement exit 1 ("seats disagree on source_line (2 vs 3) — a value/status/coordinate disagreement is not silently resolved", FIELD_DISAGREEMENTS=1, MERGE=FAIL, no merged file written); the Q2 wrong-but-plausible record above.

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 (the two `-c` one-liners as an equivalent one-line file; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the document's printed spans are the STANDARD table's markdown cell separators); the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status; the kit's 61 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative. Where exit codes matter in this report they were captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES — item by item over the V30 gate's findings, each checked against this master text and the delivered pinned tools with my own fixtures:
- codex F1 (wrong-owner identity failed globally but the dependent selected claim's rows still read MATCH) — CLOSED: master §3 now records "each enclosing-claim versus sealed-claim identity conflict … as a `MISMATCH` of that input, propagated to every selected claim whose dependency closure contains the input, and fails the overall audit"; my fixtures: the global C6_IDENTITY line AND the dependent selected claim's own row MISMATCH (direct dependency, and transitively two edges in), with faithful rows still MATCH.
- codex F2 (the reverse hybrid — alternative origin+evidence with the primary parents — falsely PASSED) — CLOSED: master §3 now makes the full-record branch predicate authoritative ("If neither complete branch matches, an unconditional record-level `MISMATCH` is appended before any field-specific diagnostic … the audit fails even when the root-origin sets agree — the full-record predicate decides the verdict and the diagnostics only explain it"); my fixtures: the reverse hybrid MISMATCHES both when the roots differ and, in the sharpest construction, when the root sets agree exactly; the forward hybrid MISMATCHES; the complete alternative branch PASSES.
- codex C1 (the manifest's header said the line column is "consumed by no tool" while the partition tool sums it) — CLOSED: the manifest header now reads "informational: summed and reported by the partition tool for per-batch totals, not used as an integrity predicate" — codex's exact replacement in substance, with the per-batch use named; the manifest is re-pinned (26dabe7c… = §2, verified) and the partition regenerated (my regeneration byte-identical, 39b8d9bf… = the kit pin and C1's cited prefix); all 89 rows re-verified on digest, bytes and line counts; the partition tool's printed per-batch line sums observed live.
- kimi C1 (the same manifest-header item, carried declared-deferred) — CLOSED by the same evidence: the deferred wording is applied verbatim in substance, the re-pin and regeneration verify, and the declared-deferral note (the change belongs to the version that re-pins the manifest) is satisfied by this version.

(d) NEW executable defects, with counterexample: NONE found. Every required fixture behaved as the V31 text claims — both hybrid directions fail (whatever the root sets), the complete alternative branch passes, the wrong-owner dependency is a MISMATCH on the dependent selected claim's row and not only globally, and the identity conflict propagates transitively through the closure; the builder, manifest, partition, merge, compute, wrapper, census, validate and all five audit subcommands executed as printed; both pin sheets, the manifest and its 89+79 rows, the partition and the sandbox profile verify as delivered. Robustness note, not a finding: the seat-visible marker "(V24)" in C4's scope principle is a process trace (a long iteration history on the leak surface); it reveals no hypothesis, model, stake direction or outcome consequence and sits at or below the accepted floor — recorded here, no repair requested. Why SOUND and not SOUND_WITH_REPAIRS: no defect at any level, cosmetic included, remains open against this text; every prior finding is closed with my own fixtures. Why not UNSOUND: Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds before the steps it must precede; fairness wording holds; and every described artefact executed as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 7c661ac0… = ACCESS_SHA; packet 0c876a17… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_gate_v31_kimi_*, not corpus data and not a census.

R3C2_V31_GATE_COMPLETE
