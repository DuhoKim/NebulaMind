ACCESS_SHA=849483185cfbac11d1dd548204cc3d70396ca417d80aa3837fe3f2bdf17f7da6
PACKET_SHA=5637ed791508adf0e74b617c4ce0e1da1c287afbee822d369ffbf24ebe643e92
GATE=PREREG_UNSOUND
LEAK=Provenance matters independently of arithmetic; no directional hypothesis, comparison preference or predicted pattern reconstructed.
CONSEQUENCE_VISIBLE=Procedural: CENSUS_ORIGIN_DISPUTED stops the census — “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent classification and audit comparison — “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.”
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only review of the unsigned V32 draft. The specifically requested V31 regressions are repaired. One new executable counterexample remains: identical origin classifications with different searches produce an audit verdict dependent on seat order. That changes control passability and the study outcome, and determines UNSOUND.

The master was hashed first; the packet was read and Q5, Q6 and Q3 answered before reading the master. Q7's specific verification instructions authorize inspection of the delivered kit, tools, brief and pins, corpus metadata/digests, and the two named V31 reports after the master. No other gate report was opened. Section 10 is history, not evidence for this verdict. The interpretation protocol's contents were not opened. Executions below are synthetic design tests, not corpus derivations, adoption, signature, dispatch or a census run.

Packet-only answers, recorded before the master:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Yes, procedurally. The origin-dispute class says: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” That outcome prevents proceeding. No packet sentence identifies a preferred substantive reproduction result or an external empirical payoff. This answers the literal question without treating procedural consequences as a reconstructed hypothesis.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED origin OF AN INPUT IS FOR?**

Yes, for independent agreement and audit: “Every input's `origin` is classified independently by both seats.” C6 says the audit “recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” The packet does not disclose a subsequent substantive interpretation of provenance.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

The sentence “The reproduction verdict and the provenance fields are recorded separately” and the transitive-origin audit imply that provenance matters independently of arithmetic. I could not reconstruct a directional hypothesis, comparison model preference, preferred origin category, predicted distribution or pattern. All stated values are consumed regardless of origin, and the packet supplies no substantive success criterion. The visible taxonomy permits speculation about a provenance-related stake, but does not select one. These are the packet-first inferences; no interpretation learned from the master is substituted into them.

## 1. OUTCOME CLASSES

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES at the definition level. The bounded procedure decides whether the stated recipe reproduces the numeral or files a specified non-arithmetic outcome. It does not promise to decide arbitrary mathematics within 120 seconds. Origin does not prevent consuming a printed chosen, fitted or imported value.

Concrete physical-claim witnesses below are synthetic, not quotations attributed to corpus papers whose contents this review was forbidden to inspect:

| Claim and stated inputs | Unique filed outcome | rests_on |
|---|---|---|
| “We choose a = 4 m; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe and input, but result printed as 9 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 4 m; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 4 m from X; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted locally, but X is verified enumerable text with its first symbol/value line stating a = 4 m | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted and traced to X outside the enumerable manifest | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, with a neither printed nor traced to a source | REPRO_INPUT_ABSENT | USES_UNDECLARED for an ABSENT/ORIG_SILENT record |
| Fully stated computation exceeds the cap or needs unavailable machinery | REPRO_NOT_EVALUABLE | Computed separately from the recorded origins |
| “Our result is L = 8 m,” with no producing equation or procedure | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED if the ledger is empty |

The numeral set as an input is excluded as AUTHOR_SPECIFIED_INPUT; the downstream result remains included. A fitted parameter asserted as the paper's own result is itself eligible for enumeration, while its use in another claim remains permitted.

Hard collision: L = a + b, where a is unprinted and cites an unpinned source while b is unprinted and uncited. BLOCKED and INPUT_ABSENT conditions both hold, but precedence files only REPRO_BLOCKED. No double filing or gap was demonstrated after applying the order.

All eight study classes have witnesses: a nonzero all-arithmetic tally with C6 PASS gives COMPLETE; an absent-input claim with a matching audit gives PARTIAL; a missing seed gives AUDIT_FAILED; C3 failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-set disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives ORIGIN_DISPUTED; a surviving fail/pass control split gives CONTROL_SPLIT.

An absent-input tally with a missing seed meets PARTIAL and AUDIT_FAILED conditions, but files AUDIT_FAILED by precedence. An entirely empty enumeration on both sides can pass the audit and file PARTIAL; zero included claims with candidate passages fails the audit as specified. INCONCLUSIVE is genuinely reachable through a nonempty PARTIAL census. These witnesses do not replace the required two-seat C0.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

Yes as cited evidence subject to independent classification; not as an infallible machine classification of meaning. I executed this wrong-but-plausible record against a synthetic source whose second line is the quoted sentence:

```json
{
  "claim_id": "fixture.txt#c0",
  "input_id": "fixture.txt#b",
  "symbol": "b",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "fixture.txt",
    "source_line": 2,
    "verbatim": "We adopt b = 5 from Reference."
  },
  "derived_from": [],
  "value": "5",
  "source_file": "fixture.txt",
  "source_line": 2
}
```

Observed: exit 0, C3_NO_SUBSTITUTION=PASS. The citation-first rule instead requires IMPORTED. The quotation and numeric-token checks establish occurrence, not correct reason-code precedence. A second seat applying that precedence can preserve the disagreement. An independent auditor classifying IMPORTED can mismatch an unsupported CHOSEN-only sealed record. Neither machine check catches the semantic mistake; shared reader error can survive. The master expressly acknowledges this floor.

Every control names a three-valued contract: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Required artefacts include exhibitions, printed executions, seals, audit JSON, path lists and provider logs. C4/C5b can pass on a self-reported path list and explicitly disclaim completeness. Naked PASS assertions do not satisfy the printed-run controls. Unreached controls are NOT_RUN, including per-session validation before integration.

**F1 — NEW, NON-COSMETIC: merge silently discards a second search when the seats agree on origin, leaving audit verdicts dependent on seat order.**

Verbatim operative sentence from §3:

> An input matches only when its origin, origin evidence, parent list and any required `origin_search` jointly match one complete declared branch: `merge` preserves the primary branch's `origin_search` and the alternative branch's `origin_search_alt` whenever that branch uses `ORIG_SILENT`; `audit compare` checks the search belonging to the matched branch, as JSON structure (member equality, not key order); undeclared alternative fields keep their primary values, except that `origin_search` is required only for a branch using `ORIG_SILENT`; the verdict does not depend on which seat is A and which is B.

The delivered merge creates an alternative only on unequal origin labels:

```python
if Bm[k]["origin"]!=A[k]["origin"]:
```

Counterexample: ten included arithmetic claims; c0 has one PRINTED input a = 4. Both seats file UNDECLARED/ORIG_SILENT, identical evidence coordinates, empty quotation and no parents. Their searches are:

```json
A: {"query":["a","choose","adopt","fit"],"files":["fixture.txt"],"matches":["fixture.txt:1"]}
B: {"query":["a","choose","adopt","fit","estimate"],"files":["fixture.txt"],"matches":["fixture.txt:1"]}
```

Both complete ledgers validate with exit 0. Neither origin nor parent classification is disputed. Both merge and compute runs exit 0. Merge(A,B) retains only A's search; merge(B,A) retains only B's. Neither merged record has origin_search_alt.

The auditor independently supplies exactly B's complete record. Both enumeration/reconstruction seals succeed. Full audit execution produces:

```text
SAME_ORIGIN_AB: C6_AUDIT_SAMPLE=FAIL EXIT=1
SAME_ORIGIN_BA: C6_AUDIT_SAMPLE=PASS EXIT=0
```

The failing input reports “record matches neither complete declared branch” and “origin_search differs (structural comparison against the matched branch's search)”. In BOTH orders, audited, sealed-primary and matched-branch root sets are exactly ["UNDECLARED"]. Thus this is neither a changed origin conclusion nor a changed reconstruction. A secondary search supplied by a validated seat was silently lost. Arbitrary seat ordering decides whether the audit files CENSUS_AUDIT_FAILED.

Exact replacement for the quoted §3 sentence:

> An input matches only when its origin, origin evidence, parent list and any required origin_search jointly match one complete seat-authored branch. Merge compares both seats' complete provenance branches as JSON structures, with parent lists compared as sets. Whenever origin, origin evidence, a required origin_search or the parent set differs, it preserves the second seat's origin and origin evidence as origin_alt and origin_evidence_alt, its origin_search as origin_search_alt when that branch uses ORIG_SILENT, and its differing parent list as derived_from_alt. This preservation also applies when the origin labels agree. Evidence-only or search-only alternatives do not count as origin-classification disagreements. Audit compare accepts a complete match to either preserved branch, compares search objects structurally, and rejects hybrids and matches to neither branch. Reversing seat A and seat B cannot change the audit verdict.

Implementation must broaden branch preservation beyond the unequal-origin conditional, retaining the existing origin-disagreement count and full-branch guard. Re-pin affected tools and regenerate the packet after repair. Tests must cover both same-origin search alternatives in both orders, unchanged-origin evidence alternatives, unsupported search content and both hybrids.

This is not COSMETIC: it changes C6 passability and the filed study class. No separate cosmetic repair is requested.

## 3. CIRCULARITY

The inclusion rule supplies no comparison preference. Both seats enumerate independently, exclusions remain visible, and C6 independently enumerates the entire corpus before receiving sealed ledgers. An auditor-only passage is an omission even if the auditor excludes it. A contrary claim cannot legitimately be discarded without a record; common omissions by all readers remain possible.

Origin classification retains human judgement, with the interpretation ranking withheld. Receipt P fixes interpretation before limb A. These measures constrain the route from expectation into evidence; they do not prove absence of prior exposure or shared misclassification.

F1 supplies a mechanical route by which primary-seat selection affects the audit outcome without changing arithmetic or provenance labels. This is an unintended asymmetry demonstrated by execution, not an allegation of intentional manipulation.

## 4. THE BLIND

The delivered builder was executed with explicit master, output and brief arguments, writing only a scratch packet. It printed C4_PACKET_REDACTED=PASS, kept sections 1–6, 9 and 11, dropped 0, 7, 8 and 10, and stripped 51 inline spans. The 40,267-byte result is byte-identical to the pinned packet. Its embedded master digest equals ACCESS_SHA.

The builder checks the finite forbidden list and required content; it does not establish universal semantic redaction. Procedural consequences and careful provenance classification remain visible, consistent with the operative C4 disclosure and accepted taxonomy floor. No directional hypothesis was reconstructed in the packet-first test.

The sandbox profile exists, matches its stated digest, and its inspected exceptions include runtime/configuration access and network access. This agrees with C4's explicit statement that it does not deny all outside access. C4/C5b remain self-report controls. A future dispatch must supply its effective profile and live probes; this review did not dispatch a seat or certify those future probes. The limits concerning prior exposure, startup code, snapshot pinning and incomplete path lists are stated honestly.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before protocol opening. The custodian must independently verify the four values and the report must print both receipts. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

That ordering binds the records before their required steps, conditional on actually performing the custody procedure. It does not prove what the lane owner had already guessed. The protocol exists as a 3,947-byte file, checked by metadata only. No actual receipt or interpretation content was inspected, so this is a judgement of the seal design, not certification that custody has occurred.

## 6. FAIRNESS

REPRO_FAILED requires “unreproduced from the stated inputs,” not “error”; the delivered brief applies that wording to negative outcomes generally. Non-arithmetic classes describe missing sources, missing inputs, unspecified procedures or computational limits without alleging an erroneous scientific result.

REPRO_WITHIN_STATED_PRECISION matches the rounding/uncertainty rule. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means all included claims received arithmetic outcomes with an audit PASS, not that every claim reproduced successfully. No additional outcome-label overclaim was demonstrated. F1 changes an audit result, not wording alone.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES: the promised seat-order-independent complete-branch audit fails on F1. The commands launch and the C5 scope is executable; that does not cure a wrong audit verdict.

Checks performed, with their limits:

| Named item | Method and observed result |
|---|---|
| Master and packet | Hashed independently; packet read first, master subsequently read in full; rebuilt packet byte-identical |
| R3C2_SEAT_PACKET.sha256 | Verified from the working directory, 8/8 OK, exit 0, before and after tests |
| r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 | Verified lane-relative, 10/10 OK, exit 0, before and after tests |
| R3C2_CORPUS_MANIFEST.md | SHA256 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 equals §2 |
| Its 89 enumerable source rows | All files at ../bhu-reading-20260823/sources/ exist and match byte counts and digests; metadata/digest only |
| partition_12_of_89.json | Regenerated with the delivered partition command; byte-identical; SHA256 39b8d9bf199265ab678ab584c7cc2cff48327aac68f6134fb9266ec6f507a420 |
| r3c2_ledger_tools.py | Pin checked; census, census final, validate and all five audit subcommands executed on own fixtures |
| r3c2_lane_tools.py | Both operative pins agree with delivered bytes; merge in both orders and mandatory-candidate compute executed; root_origins, rests_on, NOT_COMPUTED, alternatives and disputed parent lists observed |
| r3c2_batch_tools.py | Pin checked; partition executed independently; seal, join, coverage and limb-chain checks exercised by the kit |
| r3c2_build_seat_packet.py and SEAT_BRIEF.md | Pins checked; builder executed against the delivered brief; C4_PACKET_REDACTED=PASS |
| r3c2_manifest.py | Pin checked; live C5 directory manifest executed, exit 0 |
| r3c2_timeout.py | Pin checked; 120-second wrapper invocation propagated child exits 0 and 7; separate 0.1-second synthetic timeout probe printed SYMBOLIC_TIMEOUT and exited 124 |
| r3c2_seat_sandbox.sb | Exists; inspected; SHA256 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 matches C4 |
| Interpretation protocol and referenced leak-floor judgement | Existence/size checked only; contents not opened |
| Seat paths and generated artefacts | Brief and packet agree on working-directory paths, canonical batch names, custodian-generated ownership lists, separate limbs and joined validation using sources_dir “.” |
| Receipts, provider logs, reports, batch outputs and dispatch records | Future execution artefacts, not represented here as already produced; no live census or custody certification |

The initial guessed kit pathname README.md does not exist; the delivered README is README_STAGED_UNADOPTED.md, named in its verified pin sheet. This was a lookup correction, not a missing promised artefact.

**Q7(a): delivered verification and own fixtures.**

Both pin sheets verify as above. PACKET_BUILT_FROM_ACCESS_SHA=YES. The corpus manifest and all 89 required rows verify at the declared source location.

The kit was run as /usr/bin/python3 -E r3c2_staged_tests.py from r3c2_staged_d1d7. Its process exited 0; its last three stdout lines are:

```text
deletion_probes=63
controls=182 passed=182 failed=0
STAGED_TESTS=PASS
```

Own fixtures used the delivered tools, ten synthetic included claims, both independently validated ledgers, merge, compute, census, census final, seal-enumeration, select, handout, seal-rederivation and compare. The fixed synthetic seed was "1234567890abcdef" repeated four times. No fixture was copied from a prior reviewer.

For the requested case, seat A files CHOSEN for a = 4, and seat B files UNDECLARED/ORIG_SILENT with its own search. The auditor matches B completely, with search object keys reordered from query/files/matches to matches/files/query:

```text
SILENT_AB_REORDER: C6_AUDIT_SAMPLE=PASS EXIT=0
SILENT_BA_REORDER: C6_AUDIT_SAMPLE=PASS EXIT=0
SILENT_AB_CHANGED: C6_AUDIT_SAMPLE=FAIL EXIT=1
SILENT_BA_CHANGED: C6_AUDIT_SAMPLE=FAIL EXIT=1
HYBRID_FORWARD: C6_AUDIT_SAMPLE=FAIL EXIT=1
HYBRID_REVERSE: C6_AUDIT_SAMPLE=FAIL EXIT=1
HYBRID_FULL_B: C6_AUDIT_SAMPLE=PASS EXIT=0
```

Changed-content tests add "estimate" to the auditor's query list while retaining the original seat searches. Hybrid tests give A and B different parent lists, then combine A's origin/evidence with B's parents and vice versa. Both fail the authoritative complete-branch predicate. The complete B branch passes.

Own fixtures and complete resolved commands/stdout/stderr/exit logs are retained at:
/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v32_own_1apvmczb/
The kit's captured log is at:
/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v32_codex_q_g43vx1/kit.log

**Q7(b): C5_EXECUTABLE_UNDER_SCOPE=YES; NO_MASKED_STAGE=YES.**

All five literal C5 commands ran, with the two -c commands executed directly. Each exited 0:

```text
Python 3.9.6
sympy=1.14.0
/usr/bin/python3 SHA256=b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
USER_SITE=/Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
```

C4 authorizes the printed commands and pinned environment while constraining seat-selected paths. Actual C5_HARNESS_PINNED certification still requires matching a future dispatch record.

The operative mandated commands are individual invocations, not shell pipelines. The manifest is one process. The wrapper propagates failure and timeout. Kit and fixture process exit codes were captured before display filtering. NO_MASKED_STAGE does not mean every semantic predicate is correct; F1 demonstrates the distinction.

**Q7(c): PRIOR_FINDINGS_CLOSED=YES for the specified V31 findings.**

| V31 finding | Status | Independent evidence |
|---|---|---|
| codex F1: CHOSEN primary loses ORIG_SILENT alternative search | CLOSED | Required A/B and B/A complete-B tests both PASS; merge preserves origin_search_alt when origin labels differ |
| codex F2: search object key order changes the verdict | CLOSED | Reordered keys PASS in both orders; changed content FAILS in both orders; code compares JSON structures |
| codex C1: README claims every diagnostic deletion must turn the whole negative into PASS | CLOSED, COSMETIC | Delivered README explicitly permits the authoritative guard to retain FAIL while the neutralised diagnostic disappears; kit passes |
| kimi: no V31 findings | CLOSED / no items | The expressly permitted V31 report has no required repair |

These statuses close the actual V31 counterexamples, not every possible generalization. F1 in this report is the new same-origin case.

**Q7(d): NEW executable defect.**

F1 above is demonstrated end to end: same origin, different complete searches, identical auditor record, different audit verdict solely from reversing seat order. No delivered tool was repaired during this review. Both pin sheets remained valid after testing.

R3C2_V32_GATE_COMPLETE
