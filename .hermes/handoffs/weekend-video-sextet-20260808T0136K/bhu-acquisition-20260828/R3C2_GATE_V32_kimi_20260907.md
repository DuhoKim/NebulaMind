ACCESS_SHA=849483185cfbac11d1dd548204cc3d70396ca417d80aa3837fe3f2bdf17f7da6
PACKET_SHA=5637ed791508adf0e74b617c4ce0e1da1c287afbee822d369ffbf24ebe643e92
GATE=PREREG_SOUND
LEAK=content-level only: the corpus is cosmology ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline", C3); reproduction failures anticipated as live and pre-worded ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator", §3); provenance visibly matters (seven origins, quotations, two readers, disputes carried — the ruled floor); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11); a process tell survives ("closed boundary (V24)", C4) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader and audit integrity check, never an arithmetic input: "Every input's `origin` is classified independently by both seats" (C3), disagreements filed, never reconciled, "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (§4 item 7), "the auditor re-classifies the record from the pinned sources" (C3) and "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6); while "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1) — no analytic use (no tally, comparison or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 32, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged: §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history binding nothing; none of its narrated findings is reported here as mine. Order kept: the ACCESS digest was printed first; the packet was then opened and Q5/Q6/Q3 answered from it alone; only then was the master read in full; the two V31 gate reports were opened only after the master, as the brief permits. Files opened: the packet, the master, R3C2_GATE_V31_codex_20260907.md, R3C2_GATE_V31_kimi_20260907.md, both pin sheets, the corpus manifest and its declared source directory (digests, byte counts and line-count recomputation only, never content), the kit r3c2_staged_d1d7/ (run; README read), the delivered pinned tools (executed), the sandbox profile (digest only). The interpretation protocol was checked by metadata only (3,947 bytes), never opened. All fixtures are synthetic referee evidence under /tmp/r3c2_v32_kimi_gate and /tmp/r3c2_v32_kimi_*.py, not corpus data and not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The one consequence-addressing sentence equalizes every outcome: §11 "No tier, warrant token, standing or stamp moves. Published sources only; nothing from another lane." It denies consequence transfer on the named axes for every outcome and ranks nothing. The remaining consequence-adjacent sentences are procedural and attach to stop states, not to results: "The census does not proceed" (§4 items 5, 6, 7), "No tally is filed" (§4 item 3), and "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2) — a filing precedence, not a weight. §3's per-claim filing order and C3's reason-code tie-break are mechanical, and the tie-break says of itself it "is a tie-break by the specificity of the evidence, not a ranking of the values." CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Partly. What the study DOES with it is visible as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled — "An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (C6/§4); "the auditor re-classifies the record from the pinned sources" (C3) and the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) — origin never gates the arithmetic — and §1 states "The reproduction verdict and the provenance fields are recorded separately." What I cannot state from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Reproduction failures are anticipated as live possibilities and pre-worded non-accusatorially — '**Wording: "unreproduced from the stated inputs," not "error."**' (§3). (iii) The set-versus-derived composition of the corpus is a wanted, reported output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3). (iv) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3). (v) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried not reconciled, audit re-classification; nothing says what it is weighed against. (vi) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); the need to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. (vii) A process tell — "Scope, as a principle with a closed boundary (V24)." (C4): the seat can see the scope rule has an iteration history on exactly this surface; content-free about the study's question. Adversarial reconstruction of the stake, stated as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their stated inputs, and it cares how much of the corpus rests on chosen/fitted/imported/undeclared inputs — with failures possible and reputationally delicate (hence the pre-committed neutral wording and the fencing sentence). No directional expectation, no comparison model, no preferred outcome, no "pattern" is reconstructible. Why the packet gives nothing more: §1 asks one neutral question ("does the paper's own number follow from the paper's own recipe applied to the inputs it states"); the outcome classes are symmetric in direction; the forbidden vocabulary is absent from the packet (my grep for the pattern word, the comparison model's name, every engine name, every custody-chain name and the consequence words returns zero hits; "comparison" survives only twice, both in C6's mechanical sense of comparing ledgers).

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT (the attempt stops at the absent input; even taken as co-occurring, the order decides). A real claim fitting none: none exists — a claim either states a procedure or not; each input is printed/standard (arithmetic group), named-but-unmachineable (BLOCKED), or untraced (ABSENT); the attempt completes, times out, or lacks machinery (NOT_EVALUABLE). A real claim fitting two per-claim outcomes after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on: (a) a printed-but-chosen input — as a candidate the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator — observed in my own census run: "author_specified_input=0" printed as its own line); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), and the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED with rests_on USES_CHOSEN — executed: my two synthetic seat ledgers (CHOSEN vs UNDECLARED/ORIG_SILENT on the same value line) both validate PASS (C3_NO_SUBSTITUTION=PASS, exit 0), merge exits 0 in both orders ("merged 2 records; origin disagreements=1", PARENTS_DISPUTED=1), and compute prints the pair "rests_on=['USES_CHOSEN', 'USES_UNDECLARED'] … DISPUTED" (order A,B) and "rests_on=['USES_UNDECLARED', 'USES_CHOSEN'] … DISPUTED" (order B,A) — one outcome per seat, one script-computed rests_on, the disagreement printed as a marked pair, never reconciled; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same mechanics); (c) an imported value — the external-import branch (verified manifest row, numeric token, first symbol-and-value line) and the local-import branch are both enforced by validate (the kit asserts each; my own reading of the validate code confirms the branches), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT with rests_on USES_UNDECLARED. Seats never write rests_on: compute REJECTS a ledger arriving with root_origins or rests_on set (exit 2, asserted by the kit), and validate fails a seat-authored ledger carrying a field outside the schema. The reproduction question never asks why an input is what it is; decidability and reachability no longer pull against each other because provenance moved from the verdict to a computed field. Each declared outcome has a concrete reaching input (the C0 exhibition by two seats precedes the freeze).

## 2. CONTROLS

Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. Assertion versus artefact: C1 PASS only on exit 0 of the printed census run with declared-versus-recomputed counts (my run prints both blocks); C3 PASS only on printed validate runs — "a token asserted without the printed run is FAIL"; C5 requires live output and FAILs "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission, dispute rate ≤10%, and no MISMATCH. The two controls passable by assertion — C4's and C5b's self-reported path lists — say so themselves: PASS "means only that the list contains no outside path; it makes no claim that the list is complete". Unreached controls: "Controls in an unreached limb are `NOT_RUN`, never passes," plus §4's stop sentence and C2's per-session NOT_RUN.

Q2 (asked directly): YES — checkable rather than asserted, with exactly the floor the document states. My wrong-but-plausible record (own fixture, own source): krefa.txt line 1 prints "Alpha run: we take beta = 11 here." and I could file the input CHOSEN with ORIG_CHOICE_STATED, that true sentence as the quotation, value "11" at the same line — `validate` returned C3_NO_SUBSTITUTION=PASS, exit 0. Had the sentence instead been "we take beta = 11 from Reference Z" filed CHOSEN, the machine would still PASS it: it checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the right one for the sentence; the citation precedence (ORIG_CITATION first) is a rule seats apply, not a machine test. Which mechanisms catch it, as written: (1) the second seat's independent classification — a disagreement is carried, never reconciled (executed: merge prints "merged 2 records; origin disagreements=1", compute prints the DISPUTED pair), and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (2) the C6 auditor's re-classification — an auditor classification matching neither complete declared branch is an unconditional MISMATCH at compare (my two hybrid fixtures FAIL in both seat orders), and one matching a declared alternative is bound to that branch and compared like with like (my B-complete fixture PASSES in both orders). The machine floor that IS enforced is real: a citation misfiled with its value line in another file FAILs validate ("value line is in <f> but claim <c> belongs to <f2>: such a record must be IMPORTED with ORIG_CITATION" — the kit asserts this exact line). The stated residual floor: a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away" (C3 lane-side note). That is the answer, and it is stated rather than implied away.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) contains no pattern content; I read the built packet in full and my grep of it for the forbidden vocabulary (the pattern word, the comparison model's name, every engine name, every custody-chain name, the consequence words) returns zero hits; "comparison" survives only in C6's own mechanical sense. Exclusion: every excluded candidate is retained with file, line, numeral and kind; the AUTHOR_SPECIFIED_INPUT count is printed beside the denominator; a contrary claim asserted as the paper's own result cannot be quietly re-sorted — both seats must agree on inclusion with tolerance zero, and C6's independent enumeration of all 89 enumerable texts with UNION completeness rows makes a quietly excluded passage an OMISSION that files CENSUS_AUDIT_FAILED, on whichever side the omission falls (the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt, deterministically regenerated in my run, emits exactly these cases: both seats omit a required passage; its reverse; a passage the auditor excluded that the seats never enumerated; disputes at 10% and 15%). Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried and never reconciled, the tie-break is stated as evidence specificity, and the severity order lives only lane-side in redacted spans; rests_on is computed by script, not seats (compute REJECTS a ledger arriving with it set). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — all stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master. I re-ran the pinned builder with the printed command form to a scratch output (`/usr/bin/python3 -E r3c2_build_seat_packet.py --master R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md --out /tmp/r3c2_kimi_rebuilt_packet.md --brief r3c2_seat_packet/SEAT_BRIEF.md`): first line "C4_PACKET_REDACTED=PASS", then "master sha256 84948318…" (= ACCESS_SHA) and "seat packet sha256 5637ed79…" (= PACKET_SHA), "sections kept ['1.', '2.', '3.', '4.', '5.', '6.', '9.', '11.'] dropped ['0.', '7.', '8.', '10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (same digest; pin sheet row 1 re-verifies afterward). The packet's line 9 names the master it was built from; that digest equals ACCESS_SHA (compared character for character). The C4 mechanism as described does what it states and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible"; the seat runs from a redacted copy outside the lane under a kernel sandbox profile (r3c2_seat_sandbox.sb digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface the text prints; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is the principal's ruled floor and is recorded as such in C4 with the ruling cited. My packet-first answers above are the empirical check on this seat: consequence equalized, origin purpose statable only as a check, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values against the two receipts; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (executed in my pipelines: selection ran only after the seal existed; the kit asserts the refusal and its deletion probe). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes, metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the packet's "error" vocabulary is audit-internal only — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs on a chosen/undeclared ledger emitted the USES_CHOSEN/USES_UNDECLARED pair marked DISPUTED, never the clean label; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — the kit asserts it).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first (ACCESS_SHA above) and re-hashed unchanged after all runs; the packet hashed (PACKET_SHA above) and re-hashed unchanged; R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative), both before and after every run; the packet's line 9 built-from digest equals ACCESS_SHA. C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; /usr/bin/shasum of the interpreter b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; the user site path /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text's executability; kimi V31 did the same). r3c2_timeout.py: a fast child returns 0 with WRAPPER_COMMAND, child stdout/stderr, WRAPPER_EXIT and WRAPPER_ELAPSED printed; a failing child's exit 7 is carried as the wrapper's own exit 7 — one invocation per §9. r3c2_ledger_tools.py (eb479feb… = §3's pin and both pin sheets): census PASS with declared-versus-recomputed counts and the author_specified_input line; validate three-argument PASS on both my seat ledgers; all five audit subcommands executed in stage order across my eight pipelines. r3c2_lane_tools.py (146ff299… = the C3 and §9 pins): merge exit 0 in both orders ("origin disagreements=1", PARENTS_DISPUTED=1); merge exit 1 on my value-disagreement corner — "seats disagree on value ('11' vs '12')", FIELD_DISAGREEMENTS=1, MERGE=FAIL, no merged file written — fails in the open as the text states; compute exit 0 printing the DISPUTED pair in both orders. r3c2_batch_tools.py (3822f12c… = the C1 pin): `partition` regenerated the pinned partition BYTE-IDENTICALLY (digest 39b8d9bf199265ab678ab584c7cc2cff48327aac68f6134fb9266ec6f507a420 = the kit pin and C1's cited prefix), printing the per-batch line sums the manifest header describes. r3c2_manifest.py (6b6e4bce… = the C5 pin): the live 20,637-file run above. r3c2_build_seat_packet.py (56ac7d1c… = the C4 pin): the byte-identical rebuild of section 4. r3c2_seat_sandbox.sb exists on disk with digest 6978d590… equal to the C4 pin. R3C2_CORPUS_MANIFEST.md: digest 26dabe7c… equals §2; all 89 enumerable rows verified against the files at the declared source location ../bhu-reading-20260823/sources/ (digests and byte counts; 0 failures) and all 79 RAW rows verified likewise. R3C2_INTERPRETATION_PROTOCOL_20260904.md: exists, 3,947 bytes, metadata only, never opened. Stage-produced artefacts (dispatch records, OWNERSHIP lists, batch files, seals, receipts, the check sheet) do not exist yet by design — they are produced at run time; their absence is not a missing delivered artefact. No described command, script, file, field or path failed to exist or to execute as delivered.

## Q7 (V32) ANSWERS

(a) Verifications, all from the working directory: R3C2_SEAT_PACKET.sha256 verifies 8/8 OK (packet 5637ed79…, brief 80203295…, timeout fbb9bef7…, ledger eb479feb…, manifest 6b6e4bce…, builder 56ac7d1c…, batch 3822f12c…, lane 146ff299… — each equal to the master's named operative pins); r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 10/10 OK (lane-relative); both re-verified exit 0 after every run. The packet's line 9, "Built from master sha256 `849483185cfbac11d1dd548204cc3d70396ca417d80aa3837fe3f2bdf17f7da6` …", equals ACCESS_SHA character for character: PACKET_BUILT_FROM_ACCESS_SHA=YES. The corpus manifest's digest 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 equals §2's pin; all 89 enumerable rows' sha256 and byte counts recomputed from the files at the declared source location ../bhu-reading-20260823/sources/ (mismatches: NONE) and all 79 RAW rows verified likewise (metadata and digest only, never content). The kit runs (`/usr/bin/python3 -E r3c2_staged_tests.py` from its directory, exit 0); its last three lines verbatim:

  deletion_probes=63
  controls=182 passed=182 failed=0
  STAGED_TESTS=PASS

and the kit pin sheet re-verifies OK afterward (the exhibit is deterministically rewritten; confirmed). The partition regenerates byte-identically (39b8d9bf… = the kit pin and C1's cited prefix).

Own fixtures (all synthetic, my own ids/sources/values, against the delivered pinned tools; seed "5e"×32): two seat ledgers over one claim (krefa.txt#k1) where seat A files beta CHOSEN (ORIG_CHOICE_STATED, quotation at the value line, derived_from [krefa.txt#b0]) and seat B files it UNDECLARED with ORIG_SILENT and origin_search {query:"beta", files:["krefa.txt"], matches:3}, derived_from [] — both ledgers validate PASS (C3_NO_SUBSTITUTION=PASS). Merge in BOTH orders: exit 0 each; order A,B carries origin_alt=UNDECLARED with origin_search_alt equal to B's search and PARENTS_DISPUTED; order B,A carries the search on the primary record and origin_alt=CHOSEN — "the verdict does not depend on which seat is A and which is B" holds in what follows. Compute on both: exit 0, the DISPUTED pair printed. Full audit pipelines (seal-enumeration → select → handout → seal-rederivation → compare; handout carries claim_id/source_file/source_line only) over each merged ledger:
- B-COMPLETE, SEARCH KEYS REORDERED (auditor's search {"matches":3,"files":["krefa.txt"],"query":"beta"} — same structure, different key order): compare exit 0, C6_AUDIT_SAMPLE=PASS — BOTH orders.
- CHANGED SEARCH CONTENT (matches:99): compare exit 1 — "record matches neither complete declared branch; … origin_search differs (structural comparison against the matched branch's search)"; C6_AUDIT_SAMPLE=FAIL — BOTH orders.
- HYBRID FORWARD (primary origin CHOSEN + its evidence, with the ALTERNATIVE's parents []): compare exit 1 — "record matches neither complete declared branch; derived_from [] vs sealed ['krefa.txt#b0'] (alternative [])" — and in order A,B the root sets AGREE (audit ['CHOSEN','MEASURED'] = sealed), so the full-record predicate alone decides; C6_AUDIT_SAMPLE=FAIL — BOTH orders.
- HYBRID REVERSE (alternative origin UNDECLARED + ORIG_SILENT evidence + B's search, with the PRIMARY parents [krefa.txt#b0]): compare exit 1 — "record matches neither complete declared branch; …"; C6_AUDIT_SAMPLE=FAIL — BOTH orders.
Tokens printed verbatim above; every expectation met (ALL_FIXTURE_EXPECTATIONS_MET).

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the document's printed spans are the STANDARD table's markdown cell separators); the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed: 0 and 7); the kit's 63 deletion probes each turn a negative into PASS only when its check is removed, so no check is decorative. Where exit codes matter in this report they were captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES — item by item over the V31 gate's findings (codex F1, F2, C1; kimi: none), each checked against this master text and the delivered pinned tools with my own fixtures:
- codex F1 (merge dropped the alternative branch's origin_search; compare checked the primary's absent search; the verdict depended on seat order) — CLOSED: master §3 now states "`merge` preserves the primary branch's `origin_search` and the alternative branch's `origin_search_alt` whenever that branch uses `ORIG_SILENT`; `audit compare` checks the search belonging to the matched branch … the verdict does not depend on which seat is A and which is B", and the C3 lane-side parenthetical now names origin_search_alt and the preserved disputed parent lists; my fixtures: merge preserves origin_search_alt in order A,B and carries the search on the primary in order B,A; the auditor matching B's complete record PASSES in BOTH orders (V31 failed order A,B); the two hybrid directions FAIL in both orders.
- codex F2 (origin_search compared via str(), so JSON key order changed the verdict) — CLOSED: master §3 now states the search is compared "as JSON structure (member equality, not key order)"; the code compares parsed objects (== on dicts, line 386/402 of the delivered tool, marked PROBE:C6_SEARCH_BRANCH); my fixture with the same search and keys in a different order PASSES in both orders, while a changed search CONTENT FAILS in both orders.
- codex C1 (kit README overstated the deletion-probe contract as "the matching negative must then PASS") — CLOSED: the README now states the contract as it is: "the matching negative must then either PASS or, where an authoritative guard overlaps the neutralised diagnostic, still FAIL on that guard with the diagnostic line absent — a guard is never weakened to make a probe killable" (README line 9, pinned digest 0d77bd6b… verified); the kit runs 182 controls / 63 probes PASS.
- kimi V31: no findings — nothing to close.

(d) NEW executable defects, with counterexample: NONE found. Every required fixture behaved as the V32 text claims — merge preserves the silent alternative's search in both seat orders, the complete silent alternative passes with its search in any key order, changed search content fails, both hybrid directions fail unconditionally (in one construction with the root sets exactly equal), and the verdict never moved with seat order. Beyond the required fixtures I executed: the merge FIELD_DISAGREEMENTS path (exit 1, every disagreement printed, MERGE=FAIL, no file written); the wrapper's child-status carrying (exit 7); the partition's byte-identical regeneration; the builder's byte-identical rebuild; both pin sheets before and after. Robustness notes, not findings: (i) where both seats file the SAME origin with DIFFERENT origin_search content, merge keeps the primary record's search without marking a dispute — no origin disagreement exists to carry, the classification itself is agreed, and the audit compares the sealed record strictly (an unsupported search difference is MISMATCH and files CENSUS_AUDIT_FAILED, the cause named) — the stated strictness of comparing "every field", working as designed; (ii) compute prints the DISPUTED pair and root_origins in an order that follows the merge's seat order (['USES_CHOSEN','USES_UNDECLARED'] vs reversed) — the pair is an unordered set, no predicate consumes its order (the audit recomputes roots as sorted sets and my pipelines PASS/FAIL identically in both orders), and the document nowhere promises an ordering of the printed pair. Neither changes any class filed, any control's passability, any reachability, any conclusion or any tested value. Why SOUND and not SOUND_WITH_REPAIRS: no defect at any level, cosmetic included, remains open against this text; every V31 finding is closed with my own fixtures. Why not UNSOUND: Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds before the steps it must precede; fairness wording holds; and every described artefact executed as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 84948318… = ACCESS_SHA; packet 5637ed79… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v32_kimi_gate, not corpus data and not a census.

R3C2_V32_GATE_COMPLETE
