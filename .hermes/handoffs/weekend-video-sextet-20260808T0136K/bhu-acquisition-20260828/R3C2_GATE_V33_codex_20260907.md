ACCESS_SHA=70a02733917c4626b9d60e03feaa41c8002b5209f17d72f4791730ae3cb085b8
PACKET_SHA=1d91909731ee2d96809aea2fd3c8f170187160c5fcaf685f02c39d19e6821f7b
GATE=PREREG_UNSOUND
LEAK=Careful transitive provenance classification suggests provenance matters; no directional hypothesis, comparison preference or hoped-for pattern reconstructed.
CONSEQUENCE_VISIBLE=Procedural only: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” No substantive lane stake identified.
ORIGIN_PURPOSE=Independent classification and audit: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Downstream analytical purpose cannot be stated from the packet.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only gate of the delivered unsigned V33 bytes. The prompt's Version 32 description is stale; the actual header says Version 33. Section 10 supplies no inherited verdict. The specifically named V32 finding is closed, but the stronger SPI contract is not fully implemented or exhibited. That mandatory contract determines this gate. I found no new reproduction-verdict flip: the new counterexample changes merged bytes and the full audit artefact's ledger digest while both audits PASS.

Access order: master hashed without reading its contents; packet hashed and read; Q5, Q6 and Q3 answered before the master; master subsequently read in full. Q7's later instruction to execute the tools and kit was treated as permission to inspect those deliverables, their pins, brief, environment, corpus metadata/digests and the two explicitly permitted V32 reports. No other gate report or interpretation-protocol contents were opened. Tests used synthetic fixtures; no corpus derivation, census, dispatch, adoption or signature occurred.

Packet-only answers, in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

No sentence identifies a greater substantive payoff or harm to the lane or anyone. The packet says, “No tier, warrant token, standing or stamp moves.” Procedural differences are visible: the origin-dispute class says, “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” Thus the literal procedural answer is yes; the empirical-stake answer is no. This makes explicit the distinction in my packet-first answer, which said no substantive consequence and noted the stop consequences.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

For independent classification, audit and disagreement handling, yes: “Every input's `origin` is classified independently by both seats”; “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Its downstream analytical purpose cannot be stated. No packet sentence tells me about a separate provenance tally or its interpretation.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

“Provenance is transitive” and “The reproduction verdict and the provenance fields are recorded separately” suggest an interest in dependencies beyond arithmetic agreement. The chosen/fitted/imported distinctions anticipate those cases. The constants table identifies cosmological subject matter. None identifies a directional hypothesis, preferred provenance class, comparison-model preference, predicted distribution or hoped-for pattern. Speculation that the lane values provenance is possible; reconstructing which result would support its stake is not. These are packet-only inferences, not knowledge subsequently obtained from the master.

## 1. OUTCOME CLASSES

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES at the definition level: the bounded procedure returns arithmetic agreement/disagreement or a declared non-arithmetic result. It does not promise to decide arbitrary mathematics within 120 seconds. Origin is not an admissibility veto.

These are concrete synthetic physical-claim witnesses, not claims attributed to unopened corpus papers:

| Claim and inputs | Unique filed outcome | rests_on |
|---|---|---|
| “We choose a = 4 m; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same stated recipe/input, but L printed as 9 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 4 m; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 4 m from X; L = 2a = 8 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted locally, present at the first symbol/value line of verified enumerable X | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted, traced to non-enumerable X | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, a neither printed nor traced to a source | REPRO_INPUT_ABSENT | USES_UNDECLARED with an ABSENT/ORIG_SILENT record |
| Fully stated recipe exceeds the cap | REPRO_NOT_EVALUABLE | Computed independently from its recorded inputs |
| “Our result is L = 8 m,” with no producing procedure | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED with an empty ledger |

The input numeral set by the author is excluded as AUTHOR_SPECIFIED_INPUT; the downstream result remains included. A fitted parameter asserted as the paper's own result is itself a candidate; using its printed value in a later calculation remains permitted. Own compute executions emitted USES_CHOSEN, USES_FITTED and USES_IMPORTED exactly, with the corresponding single root origin. The kit separately exercised empty-ledger NOT_COMPUTED, imports, absent/blocked records and dependency propagation.

Hard collision: L = a + b, where a is unprinted and cites an unpinned source, while b is unprinted and uncited. BLOCKED and INPUT_ABSENT conditions coexist, but the declared precedence files REPRO_BLOCKED only. I found no double filing after applying precedence, nor a gap in these cases.

All eight study classes have witnesses: nonzero all-arithmetic tally plus audit PASS gives COMPLETE; an absent-input claim plus matching audit gives PARTIAL; missing seed gives AUDIT_FAILED; C3 failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives ORIGIN_DISPUTED; a surviving fail/pass control split gives CONTROL_SPLIT.

An absent-input tally with a missing seed satisfies both PARTIAL and AUDIT_FAILED conditions, but files AUDIT_FAILED by precedence. INCONCLUSIVE is genuinely reachable through PARTIAL with a successful audit. An entirely empty enumeration on both sides can also reach PARTIAL; zero included claims with candidate passages triggers the stated audit failure. These witnesses do not substitute for the required independent two-seat C0.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

YES as cited evidence plus independent semantic classification; NO as an automatic guarantee that a reason code correctly interprets its quotation. My wrong-but-plausible record was:

```json
{
  "claim_id": "fixture.txt#c0",
  "input_id": "fixture.txt#a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "fixture.txt",
    "source_line": 1,
    "verbatim": "We adopt a = 4 from Reference."
  },
  "derived_from": [],
  "value": "4",
  "source_file": "fixture.txt",
  "source_line": 1
}
```

The synthetic source actually contains that sentence. Delivered validate returned exit 0 and C3_NO_SUBSTITUTION=PASS. Citation-first precedence requires IMPORTED. The machine catches nonexistent quotations and mismatched values, not this semantic misclassification. An independent second seat can preserve an IMPORTED alternative. Against a CHOSEN-only sealed ledger, my independently IMPORTED auditor record produced exit 1 and C6_AUDIT_SAMPLE=FAIL. Shared misclassification by all readers can survive; the master expressly states that floor. root_origins and rests_on are script outputs, not accepted seat assertions.

The controls name PASS/FAIL/NOT_RUN contracts: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their artefacts are exhibitions, executions, seals, audit JSON, path lists and provider logs. C4/C5b accept self-reported lists and explicitly disclaim completeness. Bare PASS assertions do not meet the printed-run controls. Unreached controls are NOT_RUN, including per-session validation before the joined ledger exists.

**F1 — NON-COSMETIC: merge does not enforce the promised byte-level SPI for every validated pair.**

Verbatim §3 sentence:

> SPI is exhibited by `r3c2_spi_exhibition.py` in the kit: every reviewer construction is merged in both seat orders and the merged bytes and the entire audit outcome must be equal; the same exhibition FAILS against pinned byte copies of the V32 tools.

Counterexample: use the Q2 record corrected to IMPORTED/ORIG_CITATION as A. Construct B by reversing the insertion order of its top-level JSON members and its origin_evidence members, changing no values. A == B as JSON objects. Both delivered validations PASS. The canonical SHA key is equal because canon_key sorts keys. The <= tie chooses whichever record was supplied first; merge then serializes its original member order without sort_keys.

Both merge and compute executions exit 0. The identical complete auditor record passes both full audit pipelines. Observed tokens:

```text
KEY_ORDER MERGED_BYTES_EQUAL=False WHOLE_OUTCOME_EQUAL=False VERDICTS=[0, 0]
KEY_ORDER0 MERGED_SHA=1d71ddfda7beb4168212c904c8c43b9e08b0587593815e094f2dc539fbbbeede
KEY_ORDER1 MERGED_SHA=7f72dd4af779ddfd7a4f13c72ebbd9687ccb0bc225589b1de65b2b0fb1b59596
KEY_ORDER DIFFERING_ARTIFACT_FIELDS=['sealed_ledger_sha256']
```

Scope of the finding: no reproduction verdict or filed study class changed. The prescribed batch join already writes sorted JSON keys, so this particular difference is normalized away in that upstream path. It nonetheless defeats Q7's explicit any-pair test and §3's claim that merge alone enforces merged-byte and whole-outcome equality. The tested digest changes, so this is not COSMETIC under the requested definition. It is not evidence of a new semantic audit-verdict flip.

Exact replacement for the quoted sentence:

> SPI is exhibited by r3c2_spi_exhibition.py in the kit over each reviewer construction and over structurally equal records serialized with different top-level and nested member orders. Merge serializes its output recursively with sorted object keys, so equality of canonical record keys cannot expose the first seat's original member order. Each construction uses one fixed auditor reconstruction in both seat orders and asserts equal merged bytes, equal exit codes, equal ordered failure lines and equal complete audit artefacts, including sealed_ledger_sha256. The exhibition fails against the pinned V32 tools.

Exact minimal implementation replacement in cmd_merge:

```python
# Replace:
pathlib.Path(out).write_text(json.dumps({"records":out_recs},indent=1))
# With:
pathlib.Path(out).write_text(json.dumps({"records":out_recs},indent=1,sort_keys=True))
```

This replacement was tested only in a scratch copy: SCRATCH_REPAIR_MERGED_BYTES_EQUAL=True. Delivered tools were not edited. Add the unsorted-input fixture without normalizing it in the fixture writer, then re-pin the amended tool and kit and rebuild the packet.

**F2 — NON-COSMETIC: the SPI exhibition does not contain one advertised hybrid construction.**

Verbatim exhibition row label:

> hybrid direction 1: primary origin+evidence with the alternative's parents

The actual pair is DERIVED with parents [i2] versus DERIVED with parents [i3], using identical origin and evidence. Its auditor record has the second record's parents and the same evidence: it is a complete B branch, not a hybrid. Execution returns [0, 0]. The equality assertion passes without testing the claimed unsupported combination.

Exact replacement label:

> complete parent-only alternative: identical origin and evidence, auditor matches the second parent set

Retain that legitimate positive case and add the missing construction: A has DERIVED, equation evidence E_A and parents [i2]; B has CHOSEN, choice evidence E_B and parents [i3]; auditor has A's origin/evidence with B's parents. Require audit exits [1, 1], the unconditional “record matches neither complete declared branch” diagnostic in both orders, equal merged bytes and equal complete outcomes. Also test the reciprocal hybrid with B's origin/evidence and A's parents. Define A/B here as fixed source records, independently of which branch becomes canonical primary.

Exact replacement for the exhibition's test contract:

> Every listed fixture asserts its expected PASS or FAIL as well as equality under both seat orders. The suite includes both unsupported hybrid directions, a complete parent-only alternative, both complete provenance alternatives, and structurally equal records with different JSON member orders. It compares the complete audit artefact without deleting sealed_ledger_sha256 and compares failure lines in emitted order.

This changes tested inputs and predicates, not scientific classes. It is not COSMETIC. The present exhibition's removal of sealed_ledger_sha256 is redundant when its separate byte-equality assertion holds, but retaining that field directly tests the stated whole-artefact contract. Its current sorted-key fixture writer explains why F1 was absent from coverage.

## 3. CIRCULARITY

The inclusion rule names no comparison-model preference. Both seats enumerate independently; exclusions retain passages and numerals. C6 independently enumerates every pinned source before receiving the sealed ledgers. An auditor-only passage is an omission even if the auditor excludes it. A contrary claim cannot legitimately be quietly discarded without risking this check; common omissions or shared classification mistakes remain possible.

Origin classification involves judgement, but the interpretation ranking is withheld. Receipt P fixes interpretation before limb A. These constrain expectation reaching inclusion, exclusion, classification and interpretation; they do not prove the lane owner or readers lack prior beliefs. F1 is a serialization dependence, not evidence of intentional selection or a demonstrated path changing a scientific conclusion.

## 4. THE BLIND

The pinned builder was executed with explicit master, scratch output and delivered brief arguments. It returned C4_PACKET_REDACTED=PASS, stripped 51 spans, kept sections 1–6, 9 and 11, and produced 40,267 bytes identical to the delivered packet. The embedded master hash equals ACCESS_SHA. Both packet and brief are checked against the finite forbidden list; required instruction strings are checked too.

This is machine-asserted redaction of enumerated strings, not a proof that all semantic stakes are inaccessible. Procedural consequences and careful provenance classification remain visible, as C4 acknowledges. The packet-first test reconstructed no directional stake.

The sandbox profile exists and matches its stated hash. Its inspected exceptions include runtime/configuration access and network access; C4 honestly says it does not deny all outside reads or writes. C4/C5b remain self-report controls. A future dispatch must print its effective profile and live probes; this review does not certify a dispatch that has not occurred. Limits concerning prior exposure, startup code and snapshot pinning are stated.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before opening the protocol. Independent re-hashing and verification of the four values, with both receipts printed, are required. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

The ordering binds the records before the relevant steps, conditional on performing the stated custody procedure. It does not prove the lane owner had not guessed the result. The protocol exists, checked by file metadata only; its contents and actual receipts were not inspected. This is a design judgement, not certification of completed custody.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The delivered brief applies that wording to negative outcomes generally. Missing-input, blocked-source, absent-procedure and computational-limit classes do not assert an erroneous scientific result.

REPRO_WITHIN_STATED_PRECISION matches the rounding/uncertainty rule. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means every included claim received an arithmetic outcome and the audit passed; it does not imply every result reproduced. No additional outcome-label overclaim was demonstrated.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES: the universal merge-byte SPI guarantee fails on F1, and the advertised first hybrid is not exhibited by its named fixture (F2). The commands launch; the C5 scope is executable. No missing currently promised deliverable was found in the inspected operative inventory. Future dispatch records, receipts and seat outputs are prerequisites to future steps, not artefacts certified as already produced here.

| Item checked | Method and result |
|---|---|
| Master and packet | Independent hashes; packet first; full master read; scratch rebuild byte-identical |
| R3C2_SEAT_PACKET.sha256 | 8/8 entries OK before and after testing |
| Kit pin sheet | 13/13 entries OK before and after testing, including V32 byte copies |
| Corpus manifest and enumerable sources | Manifest pin verified; all 89 source files at its declared source location match byte counts and SHA256; contents not interpreted |
| partition_12_of_89.json | Recreated by delivered partition command; byte-identical |
| r3c2_ledger_tools.py | census, census final, validate and all five audit subcommands executed on own fixtures; additional negative paths exercised by kit |
| r3c2_lane_tools.py | Both pins verified; merge both orders; mandatory-candidate compute; roots, rests_on and NOT_COMPUTED observed |
| r3c2_batch_tools.py | Pin verified; partition run independently; seal, join, coverage and separate limb chains exercised by kit; join serialization inspected |
| Builder and SEAT_BRIEF.md | Pins verified; explicit-argument builder run; byte-identical packet; scope and batch filenames inspected |
| r3c2_manifest.py | Pin verified; live fifth C5 command exit 0; kit tests symlink/root refusal |
| r3c2_timeout.py | Pin verified; child success exit 0, child failure exit 7, synthetic 0.1-second cap exit 124 with SYMBOLIC_TIMEOUT |
| r3c2_seat_sandbox.sb | Inspected; hash 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 matches |
| Interpretation protocol and named leak-floor judgement | Existence/size only; contents not opened |
| OWNERSHIP_b<k>.txt, canonical batch files and integration paths | Packet/brief agree: custodian supplies ownership extracts; separate limb directories; joined validation uses sources_dir “.” |
| Receipts, provider logs, reports and dispatch records | Future procedural artefacts; no claim that this review creates or verifies them |

**Q7(a): executable results on these bytes.**

The delivered kit exited 0:

```text
deletion_probes=63
controls=184 passed=184 failed=0
STAGED_TESTS=PASS
```

The two SPI controls call the exhibition against the staged tools, whose bytes equal the delivered tools, and against the pinned V32 copies. The first requires exit 0 plus SPI=PASS. The second requires exit 1, SPI=FAIL and a verdict(AB,BA)=[1, 0] occurrence. I separately ran both exhibitions: live SPI=PASS; V32 SPI=FAIL, including the requested same-origin search verdict flip. These are observed results, not adopted historical narration. F1/F2 explain the gap between those passing controls and the claimed class coverage.

Own fixed-auditor same-origin/different-search test:

```text
SAME_ORIGIN_SEARCH MERGED_BYTES_EQUAL=True WHOLE_OUTCOME_EQUAL=True VERDICTS=[0, 0]
SAME_ORIGIN_SEARCH DIFFERING_ARTIFACT_FIELDS=[]
WRONG_PLAUSIBLE_ORIGIN EXIT=0 C3_NO_SUBSTITUTION=PASS
RECLASSIFY_AUDIT EXIT=1 C6_AUDIT_SAMPLE=FAIL
CENSUS EXIT=0 C1_DENOMINATOR_PRINTED=PASS
CENSUS_FINAL EXIT=0 C1_DENOMINATOR_PRINTED=PASS
```

Source inspection found one seat-primary selection, cmd_merge's canonical-key conditional. Downstream audit preference for a matching primary branch is branch matching against the merged record; it does not reselect which census seat is primary. The observable seat-order dependence is the equal-key serialization tie identified in F1.

Q1 is supported by the declared witness paths, own compute results and kit executions. Q2 includes an actually executed misclassification and audit rejection. Q3/Q5/Q6 are packet-first reading tests, not executable semantic proofs; the executable redaction check returned C4_PACKET_REDACTED=PASS. No machine token is presented as proving the absence of all inference.

**Q7(b): C5 and failure propagation.**

All five live C5 commands exited 0. Exact observed outputs:

```text
Python 3.9.6
1.14.0
b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3
/Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
```

C4 authorizes the mandated commands and the resolved pinned-environment directory. C5_EXECUTABLE_UNDER_SCOPE=YES does not certify matching a future dispatch pin. NO_MASKED_STAGE=YES concerns the operative literal commands: no shell pipeline hides an upstream exit; the manifest is one process and the wrapper propagated child failure and timeout. It is not an assertion that every internal exhibition stage checks its return code.

**Q7(c): prior findings.**

V32 codex F1: CLOSED as the specifically reported instance. Equal origins with different complete searches now retain alternatives; the same auditor passes both orders with equal merged bytes and equal complete outcomes. V32 kimi: no findings to close. Therefore PRIOR_FINDINGS_CLOSED=YES. This does not imply the broader V33 SPI claim is established; F1 above is a new boundary counterexample and F2 concerns the claimed exhibition coverage.

**Q7(d): new executable defects.**

F1 and F2 above are the complete new finding list. Neither is COSMETIC under the requested tested-value criterion. Evidence and the runnable own-fixture script are in /tmp/r3c2_v33_codex/check.py and its adjacent JSON/log files; the scratch-only proposed serialization repair is lane_sorted_output.py there. The master, packet and all pinned deliverables remained unchanged. Nothing in this report adopts, signs or runs the census.

R3C2_V33_GATE_COMPLETE
