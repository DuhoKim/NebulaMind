ACCESS_SHA=70a02733917c4626b9d60e03feaa41c8002b5209f17d72f4791730ae3cb085b8
PACKET_SHA=1d91909731ee2d96809aea2fd3c8f170187160c5fcaf685f02c39d19e6821f7b
GATE=PREREG_SOUND
LEAK=content-level only: the corpus is cosmology on the Planck 2018 baseline ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.", C3) across 89 enumerable texts; failures are anticipated and pre-worded ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator", §3); provenance visibly matters (seven origins, machine-matched quotations, two readers, disputes carried — the ruled floor); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader and audit integrity check, never an arithmetic input: "Every input's `origin` is classified independently by both seats." (C3); disagreements filed, never reconciled, "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`" (§4 item 7); "the auditor re-classifies the record from the pinned sources" (C3) and the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6); while "Arithmetic consumes records according to status `PRINTED` or `STANDARD`" (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1) — no analytic use (no tally, comparison, weighting or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 33, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine; no V33 gate file was sought or opened. Order kept: the master was hashed first (ACCESS_SHA, line 1); the packet was then opened and Q5, Q6, Q3 answered from it alone, in that order; only then was the master read in full; the two named V32 gate reports were opened only after the master, as the brief permits. Files opened: the packet, the master, R3C2_GATE_V32_codex_20260907.md, R3C2_GATE_V32_kimi_20260907.md, both pin sheets, the delivered tools (read and executed), the kit r3c2_staged_d1d7/ (run; README, the exhibition and the two SPI controls read), the corpus manifest (metadata only: digests and byte counts, never content), the batch tool (read and executed), the builder (executed), the sandbox profile (digest only), the interpretation protocol (metadata only: exists, 3,947 bytes; never opened). No other file. Every fixture below is synthetic referee evidence under /tmp/r3c2_kimi_v33_* with my own sources, ids and values; no corpus content; not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalizes every outcome: "No tier, warrant token, standing or stamp moves." (§11) — it denies consequence transfer on the named axes for every outcome and ranks nothing. The remaining consequence-adjacent sentences are procedural and attach to stop states, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), and "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2) — a filing precedence, not a weight. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Partly. What the study DOES with it is visible as checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled — "An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`."; "the auditor re-classifies the record from the pinned sources" (C3) and the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What I cannot state from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), partitioned into 12 batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "**Wording: \"unreproduced from the stated inputs,\" not \"error.\"**" (§3); that is a reporting rule, not an expectation of any rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, audit re-classification — but nothing says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their own stated inputs, and it cares how much of the corpus rests on chosen/fitted/imported/undeclared inputs — with failures possible and reputationally delicate (hence the pre-committed neutral wording and the fencing sentence). No directional expectation, no comparison model, no preferred outcome, no predicted distribution or "pattern" is stated or implied. LEAK as on line 4.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2; and even taken as co-occurring, the order decides). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration (a zero denominator with no passage on either side can pass the audit; "A sealed denominator of zero with any passage on either side fails", C6).

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on: (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), and the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; asymmetric = the half-width on the side the reproduced value falls) with rests_on USES_CHOSEN — executed in my K2-family fixtures: both seat ledgers validate exit 0, merge exits 0 in both orders, compute derives the root set from the recorded origins; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same mechanics; the kit exhibition's CHOSEN-vs-FITTED construction exercises the branch); (c) an imported value — the local-import branch (source_file equal to the claiming file: evidence and value checks at the claiming paper) and the external-import branch (verified manifest row, numeric token, first symbol-and-value line) are both enforced by validate (delivered r3c2_ledger_tools.py lines 151–181; the kit asserts each exact failure line), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT with roots {UNDECLARED}. Seats never write rests_on: compute REJECTS a ledger arriving with root_origins or rests_on set (exit 2; lane tool lines 59–61, kit-asserted). The four earlier rounds' tension is gone because provenance no longer gates admissibility: consumption is by status alone, the verdict test is mechanical, every non-decidable path files a named non-arithmetic outcome, and provenance is a script-computed field beside the verdict.

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed and printed (my runs print both blocks and the author_specified_input line); C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate ≤ 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. My wrong-but-plausible records (own fixtures, own source myA.txt): (i) value "7" at myA.txt:5 filed CHOSEN/ORIG_CHOICE_STATED with the quotation "We choose k = 7 for this run." — a true substring of the cited line — validate returned C3_NO_SUBSTITUTION=PASS, exit 0: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (ii) The machine floor that IS enforced is real: a record whose value line lies in another file must be IMPORTED with ORIG_CITATION whatever code was submitted (validate lines 148–150, the kit asserts the exact line); an empty quotation on any non-silent record fails; a STANDARD value line outside the claiming file fails; a BLOCKED record without the claiming-file binding fails. (iii) The mechanisms that catch a precedence-violating but quotation-true origin, as written: the second seat's independent classification — a disagreement is carried, never reconciled (merge preserves origin_alt; above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED) — and the C6 auditor's re-classification — an auditor classification matching neither complete preserved branch is an unconditional MISMATCH (my K2b and K3b fixtures FAIL in both seat orders; roots are recomputed under the matched branch only). The stated residual floor: a reason code that matches its quotation but misapplies the precedence "is caught only by the second seat's independent classification and the C6 re-classification, never by the machine; if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away" (C3 lane-side note). origin_search adequacy is judged from the printed search ("a search of one query with no variants is not adequate") by the second seat and the auditor, who re-classifies from the pinned sources. Checkable, not infallible, and the document says so itself.

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no pattern content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below), and my packet-first Q3 found no hypothesis. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind; both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any sealed-side omission (included or excluded) files CENSUS_AUDIT_FAILED — the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt (regenerated deterministically in my kit run) emits exactly these cases in both directions. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script — compute REJECTS a ledger arriving with it set. The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — each stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 70a02733…" (= ACCESS_SHA, character for character) and "seat packet sha256 1d919097…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (digest equality checked byte-for-byte; the pin sheet's packet row re-verifies OK afterward). The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed"; the seat runs from a redacted copy outside the lane under the kernel sandbox profile r3c2_seat_sandbox.sb (digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface the text prints; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited. My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison — and §4's CENSUS_AUDIT_FAILED definition names the seal-receipt case. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (my pipelines ran the audit strictly in stage order; the kit asserts the refusal and its deletion probe). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary is audit-internal — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs on disputed ledgers emitted the pair marked DISPUTED, never the clean label; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — kit-asserted).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first (ACCESS_SHA, line 1) and read in full; the packet hashed (PACKET_SHA, line 2); R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 13/13 OK from the lane directory, both re-verified exit 0 after the kit run; the packet's line 9 built-from digest equals ACCESS_SHA. Every named script exists beside the document and matches the digest the document prints for it: r3c2_ledger_tools.py bd1f4a237357f43976d0776ec178eac78b1a648f8c5aa43e16a085f78a62a3e3 (§3), r3c2_lane_tools.py 42c1577e30ed1349dbc697f7c021feb98cb62d224779938409c841b2755a2f5f (C3, §9), r3c2_batch_tools.py 3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e (C1), r3c2_manifest.py 6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a (C5), r3c2_timeout.py fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 (§9), r3c2_build_seat_packet.py 56ac7d1c78196c4d246244126b43666d804f093f8fbe4e2c2289ab885def728e (C4), r3c2_seat_sandbox.sb 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 (C4), R3C2_CORPUS_MANIFEST.md 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 (§2). C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text's executability). r3c2_timeout.py: a fast child exits 0 with WRAPPER_EXIT=0 printed; a failing child's exit 7 is carried as the wrapper's own exit 7; a 5-second child under a 0.3-second cap printed SYMBOLIC_TIMEOUT and exited 124. r3c2_ledger_tools.py: census prints declared-versus-recomputed counts and the author_specified_input line; validate three-argument exit 0 on all 18 of my seat ledgers; all five audit subcommands executed in stage order across my 18 audit pipelines. r3c2_lane_tools.py: merge exit 0 in both orders on every construction; merge exit 1 on my value-disagreement corner with identical stdout in both orders and no merged file written ("seats disagree on value ('7' vs '12')", FIELD_DISAGREEMENTS=1, MERGE=FAIL) — fails in the open as the text states; compute exit 0 printing single rests_on values, DISPUTED pairs and a NOT_COMPUTED row as applicable, and exit 2 with no output when the candidate argument is omitted (usage line) — both as the text states. r3c2_batch_tools.py partition regenerates the pinned partition_12_of_89.json byte-identically and its stdout matches the pinned .txt. The builder rebuilds the packet byte-identically (section 4). The corpus manifest's 89 enumerable rows and 79 RAW rows all verify at ../bhu-reading-20260823/sources/ by digest and byte count (metadata only). The interpretation protocol exists (3,947 bytes; metadata only). Fields the scripts are said to compute were observed computed: root_origins and rests_on (single, pair-DISPUTED, NOT_COMPUTED), the merged alt fields (origin_alt, origin_evidence_alt, origin_search_alt, derived_from_alt, PARENTS_DISPUTED), the selection (audited_ids covering every arithmetic-group claim plus the sampled remainder), the C6_AUDIT.json artefact with completeness rows and per-claim MATCH/MISMATCH. Paths a seat is told to use (the working-directory layout, `.` as the sole sources_dir, the pinned scripts) are consistent between the packet and the delivered artefacts. No described artefact was found missing or non-executable as delivered.

## Q7 (V33) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=13/13 OK (before and after the kit run)
  TOOL_PINS_MATCH_MASTER=YES (ledger bd1f4a23…, lane 42c1577e…, batch 3822f12c…, manifest 6b6e4bce…, timeout fbb9bef7…, builder 56ac7d1c…, sandbox 6978d590…, corpus 26dabe7c…)
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only)
  KIT_RUN: deletion_probes=63 / controls=184 passed=184 failed=0 / STAGED_TESTS=PASS (exit 0)
  SPI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; all 9 reviewer constructions: merged-bytes-equal=True outcome-equal=True; the codex V32 F1 construction verdict(AB,BA)=[0, 0])
  SPI_EXHIBITION_V32_BYTECOPY=SPI=FAIL (exit 1; the codex construction flips exactly: verdict(AB,BA)=[1, 0]; the byte copies hash to the V32 pins 146ff299…/eb479feb… recorded in both V32 gate reports)
  OWN_FIXTURES_SPI=PASS (exit 0; 9 constructions of my own design — equal-label different searches (auditor=B, auditor=A, auditor=third search), equal-label different evidence (auditor=B, auditor=fabricated quotation), MEASURED-vs-IMPORTED with different parents (auditor=B complete, auditor=hybrid), DERIVED with different parent sets (auditor=B, auditor=neither) — every seat ledger validates exit 0 (18/18), merged bytes equal in both orders in all 9, compute stdout equal, the whole audit outcome (exit code, every FAIL line, the artefact minus the sealed-ledger digest) equal; compare exits (0,0) for complete-branch auditors and (1,1) for hybrids and neither-branch auditors, exactly as the text requires)
  CANONICAL_PRIMARY_IS_CONTENT_KEYED=YES (the primary was seat A in my K1/K2/K4 and seat B in K3 — identical across invocation orders, so the key, not argv order, decides)
  MERGE_FIELD_DISAGREEMENT=exit 1 both orders, identical stdout, no merged file
  C5_LIVE: Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1… (exit 0 each)
  WRAPPER: exit 0 (fast) / exit 7 carried (failing child) / exit 124 with SYMBOLIC_TIMEOUT (0.3 s cap on a 5 s child)
  BUILDER_REBUILD: C4_PACKET_REDACTED=PASS, rebuilt packet byte-identical to the pinned packet
  PARTITION_REGENERATED=byte-identical, stdout matches the pinned .txt
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the document's printed spans are the STANDARD table's markdown cell separators); the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the kit's 63 deletion probes each turn a negative into PASS only when its check is neutralised (or show the diagnostic absent while an overlapping guard still fails), so no check is decorative; every exit code cited in this report was captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES over the V32 gate's findings (codex F1; kimi none), each with my own evidence:
  - codex F1 (with EQUAL origin labels and different complete origin_search objects, merge kept only seat A's search, so the identical auditor record failed in A/B order and passed in B/A order) — CLOSED. Text: §3 now states SEAT-PERMUTATION INVARIANCE once and by name — "the audit verdict, and the whole audit outcome, are invariant under permutation of the census seats" — enforced at ONE place: merge "orders the two seats' records by a seat-blind canonical key (the smaller sha256 of each record's canonical JSON is the PRIMARY branch) and preserves the other seat's complete provenance branch … whenever origin, origin evidence, a required search or the parent set differs, equal origin labels included". Code: delivered r3c2_lane_tools.py line 104 is the only seat-order-sensitive read anywhere in the delivered tools (the canonical-key comparison, marked PROBE:SPI_CANONICAL); everything downstream of it reads P/Q; the branch-preservation block (lines 108–116) preserves origin_alt, origin_evidence_alt, origin_search_alt (when the secondary branch uses ORIG_SILENT) and derived_from_alt on any provenance-bearing difference, equal labels included. Execution: the exhibition's codex construction now returns verdict(AB,BA)=[0, 0] with equal merged bytes on the delivered tools, and flips exactly [1, 0] on the pinned V32 byte copies (fail-first, as §10.28(4) claims); my own K1a/K1b fixtures (the F1 shape with my own values, auditor = each seat's complete record) PASS in both orders with equal merged bytes and equal whole outcome, and K1c (a third search matching neither branch) FAILS in both orders.
  - kimi V32: no findings — nothing to close.
  The class, not the instance, was tested: I built nine constructions spanning same-origin different searches (both auditor directions and a neither search), equal-label evidence-only alternatives, origin-plus-parents disputes with a hybrid, and parents-only disputes with a neither parent set, plus a fabricated-quotation auditor and a fabricated third search; every merged byte string and every whole audit outcome is invariant across seat orders. No second predicate anywhere re-decides which seat is primary: grep-level inspection of both delivered tools finds no seat/A/B reference outside merge's canonical key; compare (r3c2_ledger_tools.py lines 368–403) and compute operate on the merged ledger only; branch matching is over complete preserved branches (matches_primary / matches_alt, with the neither-branch guard authoritative), the search compared is the matched branch's own (structural equality on parsed JSON), and sealed_view plus root recomputation use only the matched branch. The kit's two SPI controls assert exactly what §10.28 says they assert: control "SPI: the audit outcome is identical under both seat orders for every reviewer construction (delivered tools)" requires the exhibition's SPI=PASS over all nine reviewer constructions in both orders (§10.28(3)); control "SPI fail-first: the same exhibition FAILS against byte copies of the V32 tools" requires exit 1, SPI=FAIL and the exact flip string "verdict(AB,BA)=[1, 0]" — the verdict flipping on the codex construction exactly (§10.28(4)) — and the byte copies' digests equal the V32-era pins recorded in both V32 gate reports.

(d) NEW executable defects, with counterexample: NONE. Every construction behaved as the V33 text claims. Tested beyond the required set: the merge FIELD_DISAGREEMENTS path (exit 1 in both orders, identical stdout, no file written); the compute rejection paths (exit 2 without candidates; REJECT of a ledger arriving with rests_on/root_origins set — code read, kit-asserted); the wrapper's three behaviours; the builder's byte-identical rebuild; the partition's byte-identical regeneration; both pin sheets before and after. Observation, NOT a finding: a stray origin_search attached to a non-ORIG_SILENT record is preserved only with the primary branch and is not compared by the audit — this matches the text as written ("parent list and any required `origin_search`", §3; the schema requires the search only "when reason_code is ORIG_SILENT"), the field feeds no root, verdict, count or class, and merged bytes remain order-invariant; it changes no class filed, no control's passability, no reachability, no conclusion and no tested value, so it is not even a COSMETIC repair request.

Why SOUND and not SOUND_WITH_REPAIRS: no defect at any level, cosmetic included, remains open against this text; the one V32 finding is closed above with my own fixtures and the class it instanced is stated once, enforced in one place, and exhibited over every reviewer construction in both seat orders with a fail-first record against the V32 bytes. Why not UNSOUND: Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds each artefact before the step it must precede; every negative outcome is worded within its definition; and nothing the document describes failed to execute as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 70a02733… = ACCESS_SHA; packet 1d919097… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_kimi_v33_*, not corpus data and not a census.

R3C2_V33_GATE_COMPLETE
