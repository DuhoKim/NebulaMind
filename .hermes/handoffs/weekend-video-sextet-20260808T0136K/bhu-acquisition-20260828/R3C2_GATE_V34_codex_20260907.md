ACCESS_SHA=6e18bc39bbe35fa02a2aca4cdac9980b1790156c1915c1ef2a4e9a6e679d273c
PACKET_SHA=1dcbe84dd100541e472dea79ea6c8dbd6ac7561b59ac8e7cf6476486f983b05c
GATE=PREREG_UNSOUND
LEAK=Careful transitive provenance classification reveals a research focus; no directional hypothesis, comparison preference or hoped-for pattern reconstructed.
CONSEQUENCE_VISIBLE=Procedural only: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” No substantive payoff to the lane identified.
ORIGIN_PURPOSE=Independent classification, audit and dispute handling: “Every input's `origin` is classified independently by both seats.” Its downstream analytical use cannot be stated from the packet.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only review of the delivered unsigned **Version 34**, as its header actually states. Both V33 findings are closed. A new executable counterexample remains: independently canonicalising each input can splice two valid dependency graphs into a cyclic primary graph. Both seat orders then fail identically, including when the auditor reconstructs either complete, supported original graph. Permutation equality holds; the promised substantive behaviour does not.

Access order: master hashed first without reading its contents; packet hashed and read; Q5, Q6 and Q3 answered before reading the master; master subsequently read in full. Section 10 supplies no inherited verdict. Q7's later executable instructions were treated as authorisation to inspect the named tools, kit and necessary dependencies, pins, brief, corpus metadata and the two explicitly permitted V33 reports. No other gate report or interpretation-protocol contents were opened. Corpus files were hashed, not interpreted. All executable work used synthetic control fixtures; no census, corpus derivation, dispatch, adoption or signature occurred.

Packet-only answers, preserved in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Procedurally, yes: “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Disagreement can stop the census. No sentence identifies a greater substantive payoff or harm from a particular reproduction or provenance result. “No tier, warrant token, standing or stamp moves.” My packet-first answer distinguished these procedural consequences from an undisclosed empirical stake.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

For independent classification, audit and dispute handling, yes. The packet says: “An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled.” Its downstream analytical use cannot be stated. No packet sentence explains a separate provenance tally or its interpretation.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

“The reproduction verdict and the provenance fields are recorded separately” reveals interest beyond numerical agreement. “Every `DERIVED` record lists its `derived_from` ids” suggests interest in ultimate dependencies, including chosen, fitted and imported inputs. The constants table identifies cosmological subject matter. These permit a research-focus inference, but no predicted distribution, comparison-model preference, preferred outcome or directional pattern. Inferring that provenance matters does not establish which result would serve the lane's stake. These answers preceded the master.

## 1. OUTCOME CLASSES

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

**YES at the per-claim definition and class-reachability level; NO as an unqualified end-to-end executable guarantee**, because F1 below can prevent the separate provenance computation on valid inputs. Option (c) itself does not reintroduce provenance as an admissibility veto. Bounded classification remains decidable without solving arbitrary mathematics: timeout and unavailable machinery have explicit outcomes.

Concrete physical-claim witnesses below are synthetic, not findings attributed to unopened corpus papers:

| Claim and inputs | Unique outcome | `rests_on` |
|---|---|---|
| “We choose a = 2 m; L = 2a = 4 m.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_CHOSEN` |
| Same recipe/input, but L printed as 5 m | `REPRO_FAILED` | `USES_CHOSEN` |
| “Fitting gives a = 2 m; L = 2a = 4 m.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_FITTED` |
| “We adopt a = 2 m from X; L = 2a = 4 m.” | `REPRO_WITHIN_STATED_PRECISION` | `USES_IMPORTED` |
| a unprinted locally but token-matched in verified enumerable X | `REPRO_WITHIN_STATED_PRECISION` | `USES_IMPORTED` |
| a unprinted and traced to non-enumerable X | `REPRO_BLOCKED` | `USES_IMPORTED` |
| L = 2a, with a neither printed nor traced to a source | `REPRO_INPUT_ABSENT` | `USES_UNDECLARED`, with the appropriate absent/silent record |
| Fully stated recipe exceeds the cap | `REPRO_NOT_EVALUABLE` | Computed independently from the recorded inputs |
| Own result L = 4 m, with no producing procedure | `REPRO_NO_DERIVATION_STATED` | `NOT_COMPUTED` for an empty ledger |

My delivered-tool executions emitted `USES_CHOSEN`, `USES_FITTED` and `USES_IMPORTED` with their respective root origins. The kit exercised the other encoding boundaries, empty ledgers, imports and dependency propagation. These checks do not replace the required independent two-seat C0.

The author-set input numeral is excluded as `AUTHOR_SPECIFIED_INPUT`; its downstream result remains included. A fitted parameter asserted as an own result is itself a candidate, while its stated value remains consumable in a later claim.

Hard collision: L = a + b, with a unprinted and cited to an unpinned source, and b unprinted and uncited. Both terminal conditions hold, but precedence files `REPRO_BLOCKED` only. No double filing or unclassified per-claim state was demonstrated after applying precedence.

All eight study classes have witnesses: nonzero all-arithmetic tally and audit PASS gives COMPLETE; a matching absent-input claim and successful audit gives PARTIAL; missing seed gives AUDIT_FAILED; C3 failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives ORIGIN_DISPUTED; surviving fail/pass control split gives CONTROL_SPLIT.

A partial tally with a missing seed satisfies two conditions but files AUDIT_FAILED by precedence. INCONCLUSIVE is genuinely reachable through PARTIAL with a successful audit. An entirely empty enumeration on both sides can also reach PARTIAL; a zero included denominator with candidate passages instead meets the stated audit-failure condition.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

YES as cited evidence plus independent semantic classification; NO as a machine guarantee of correct interpretation. My wrong-but-plausible record was:

```json
{
  "claim_id": "p.txt#c",
  "input_id": "p.txt#a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "p.txt",
    "source_line": 3,
    "verbatim": "We adopt a = 2 from Reference."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "p.txt",
  "source_line": 3
}
```

The synthetic source contains that exact sentence. Both delivered validations returned exit 0 and `C3_NO_SUBSTITUTION=PASS`. Citation-first precedence requires IMPORTED. The machine checks quotation existence and value evidence, not this semantic error. An independent second seat can supply the correct classification; my independently IMPORTED auditor against the CHOSEN-only seal produced exit 1 and `C6_AUDIT_SAMPLE=FAIL` in both orders. Identical misclassification by all readers can survive, as the master explicitly acknowledges. Computed provenance fields cannot legitimately be supplied by a census seat.

The named controls have PASS/FAIL/NOT_RUN contracts: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their artefacts are exhibitions, printed executions, seals, audit JSON, path lists and provider logs. A bare PASS assertion does not satisfy a printed-run control. C4/C5b can pass on an incomplete self-reported list; their stated pass predicates expressly acknowledge that limitation. Unreached controls are NOT_RUN, including per-session validation before integration.

**F1 — NON-COSMETIC: permutation-invariant branch mixing creates a graph neither seat supplied, and a diagnostic graph can overturn a correct audit.**

Verbatim §3 sentence:

> A claim with a disputed input or parent list carries both script-computed classifications and is marked `DISPUTED`.

Verbatim §3 sentence:

> The comparison graph is constructed only from matched branches; the original disputes and the matched branches are reported without reconciliation.

Counterexample source `p.txt`, with one included claim `p.txt#c0` at line 7:

```text
We measure r = 2.
x = y = 2.
x = r = 2.
y = r = 2.
y = x = 2.
Definitions.
The stated recipe L = x + y gives L = 4.
```

All records have status PRINTED, value string `2`, source_file `p.txt`, and claim_id `p.txt#c0`. IDs are `p.txt#x`, `p.txt#y`, `p.txt#r`; symbols are x, y, r. Value lines are respectively 2, 4 and 1 in BOTH ledgers. Equation evidence uses ORIG_EQUATION/DERIVED and the exact indicated source line; measurement evidence uses ORIG_MEASURED/MEASURED at line 1.

| Record | Seat A parents / evidence line | Seat B parents / evidence line |
|---|---|---|
| x | [y] / 2 | [r] / 3 |
| y | [r] / 4 | [x] / 5 |
| r | [] / 1 | [] / 1 |

Each original graph is acyclic: A is x → y → r; B is y → x → r. Both have only MEASURED roots. Both validate, and each computes successfully on its own. There are **zero origin-label disagreements**; parent alternatives are explicitly supported by the design.

The canonical record keys select A's x and B's y as PRIMARY. The merged PRIMARY graph is therefore x ↔ y. That graph was supplied by neither seat. Running `compute` on the merged ledger exits 1 instead of emitting the promised DISPUTED pair. Reversing seat order changes nothing.

Use one fixed auditor reconstruction equal to complete A in both orders, then repeat with complete B. Both reconstructions match preserved branches and have acyclic measured-root graphs. Nevertheless, delivered `audit compare` fails both: its report additionally evaluates `roots(full_by, ...)` for `sealed_primary`. The exception from that unmatched cyclic graph is added to the audit's failure reasons.

Observed tokens:

```text
CYCLE_SEAT_VALIDATION=[0,0]
CYCLE_ORIGINAL_GRAPH_COMPUTE=[0,0]
CYCLE_ORIGINAL_ROOTS=['MEASURED']
CYCLE_MERGED_BYTES_EQUAL=True
CYCLE_WHOLE_OUTCOME_EQUAL=True
CYCLE_MERGED_SHA256=517165c735169a648068ebca4ac5ac80d1464e30c74f16a89c39101dd9c4bc62
CYCLE_MERGED_COMPUTE=[1,1]
CYCLE_COMPUTE_STDOUT=FAIL: cycle at p.txt#x
CYCLE_AUDITOR_EQUALS_A=[1,1]
CYCLE_AUDITOR_EQUALS_B=[1,1]
CYCLE_AUDIT_FAILURE=FAIL: AUDIT p.txt#c0: MISMATCH (dependency not reconstructed by the auditor or cyclic: cycle at p.txt#x)
```

This is not another permutation counterexample: equality holds. It is a false failure on supported complete branches and a failure to produce the promised provenance pair. The interpretation step cannot receive the expected two facts through the mandated successful-compute path.

Exact replacement for the first quoted sentence:

> A claim with a disputed input or parent list is marked `DISPUTED`. In addition to its canonical per-input alternatives, `merge` preserves the two complete validated provenance graphs in `provenance_graphs`, ordered by the SHA256 of canonical JSON over their records sorted by `input_id`; equal graphs are retained once. `compute` obtains the claim's reported classification pair from those complete graphs, retaining equal classifications as a pair when the claim is disputed. It never treats an independently mixed PRIMARY or ALT graph as a graph supplied by a seat. The merged output, including these graphs, is serialised recursively with sorted object keys.

Exact replacement for the second quoted sentence:

> The comparison graph used to decide the audit is constructed only from complete matched input branches. A cycle or missing dependency in that matched graph fails the audit. Unmatched PRIMARY or ALT graphs are diagnostic views only: if their roots cannot be computed, the report records that fact without adding a mismatch or changing the audit verdict. The original disputes and the matched branches are reported without reconciliation.

These replacements require implementation, re-pinning and regression controls, not a prose-only repair. Keep seat-blind ordering at `merge`; preserve coherent graphs there instead of adding downstream seat-order selection. Add both auditor directions above, requiring successful graph computation, DISPUTED output and audit PASS in both orders, alongside the existing hybrid negatives.

Causal probe: in a scratch copy only, I replaced the audit's `sealed_primary` root computation with a diagnostic placeholder. Both previously failing complete auditor reconstructions then returned exit 0. No branch-match predicate was weakened. This isolates the false audit failure; it is not a complete repair of `compute`. Delivered files were not edited.

**C1 — COSMETIC: the exhibition docstring still describes the old comparison.**

Verbatim sentence from `r3c2_spi_exhibition.py`:

> For each reviewer construction: build two seat ledgers A and B; merge in BOTH orders; assert the merged ledgers are byte-identical; run the
> full audit (seal-enumeration → select → handout → seal-rederivation → compare) against each merged ledger with the SAME auditor
> reconstruction; assert the WHOLE outcome (exit code, every FAIL line, and the complete C6_AUDIT.json minus the sealed-ledger digest
> field, which is identical anyway when the bytes are) is equal across permutations.

Exact replacement:

> For each reviewer construction, merge both seat orders and require identical merged bytes; run seal-enumeration, select, seal-rederivation and compare with one fixed auditor reconstruction; require identical exit codes, failure lines in emitted order and complete C6_AUDIT.json artefacts including sealed_ledger_sha256; also assert the construction's expected verdict and required diagnostic.

The implementation retains the digest and does not call handout in this exhibition helper; my independent pipelines did call handout. This docstring correction changes no tested value, control predicate, class or conclusion.

## 3. CIRCULARITY

The inclusion rule names no comparison-model preference. Both seats enumerate independently; excluded numerals and passages remain recorded. C6 independently enumerates all pinned texts before receiving sealed ledgers, including candidates the census seats might omit. An auditor-only passage is an omission even when the auditor excludes it. A contrary claim cannot legitimately be quietly discarded without risking that check. Shared omissions and shared classification mistakes remain possible.

Origin classification requires judgement, but the interpretation ranking is withheld. Receipt P fixes interpretation before limb A. These constrain expectation reaching inclusion, exclusion, origin classification and interpretation without claiming readers lack prior beliefs. F1 supplies a mechanism for false failure, not evidence of intentional selection.

## 4. THE BLIND

The delivered builder ran against this master, the delivered brief and a scratch output. It returned `C4_PACKET_REDACTED=PASS`, stripped 51 spans, kept sections 1–6, 9 and 11, and produced 40,267 bytes identical to the delivered packet. The embedded master hash equals ACCESS_SHA. Tool and pin hashes match.

The builder asserts a finite forbidden list and required content. That is not proof of semantic blindness to every possible stake. The packet-first answers found procedural consequences and a provenance focus, but no directional hypothesis. C4 states that floor explicitly.

The sandbox profile exists and matches its stated digest. Its inspected exceptions include runtime/configuration directories and network access; the text honestly says it does not deny all outside access. C4/C5b remain self-report controls. A future dispatch must supply its actual inventory, profile and positive/negative probes. No unperformed dispatch is certified here. Prior exposure, startup-code access and snapshot-pinning limits are stated.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before protocol opening. The report must print both receipts and four independently verified values. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

This ordering binds the records before the relevant steps, conditional on executing the custody procedure. It does not establish an unguessable interpretation or an unexposed mind. The protocol's existence was checked by metadata only; its contents and actual receipts were not inspected. This is a design judgement, not certification of completed custody.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error”; the brief applies that wording to negative outcomes generally. Missing-input, blocked-source, absent-procedure and computational-limit classes do not claim a scientific error. REPRO_WITHIN_STATED_PRECISION matches the rounding/uncertainty test. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means complete arithmetic classification with audit PASS, not universal successful reproduction.

F1's false audit failure is material despite these fair labels. No additional outcome-name repair is required by this review.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

**YES: F1 prevents the promised provenance pair and makes a complete supported reconstruction fail.** No missing currently promised deliverable was found in the inspected operative inventory. Future dispatch records, receipts, ownership files and seat outputs are future prerequisites, not claimed completed artefacts.

| Item checked | Method and result |
|---|---|
| Master, packet, brief and builder | Hashes, full readings, explicit-argument scratch rebuild; packet byte-identical |
| Seat and kit pins | 8/8 and 15/15 entries verified |
| Corpus manifest and files | Manifest hash verified; 89/89 enumerable and 79/79 RAW files match bytes and SHA256; no scientific reading |
| `partition_12_of_89.json` | Delivered partition command regenerated it byte-identically |
| `r3c2_ledger_tools.py` | Own census, census final, validate and all five audit subcommands executed; 30 own seat ledgers validated |
| `r3c2_lane_tools.py` | Own merge in both orders and mandatory-candidate compute; roots, classifications and F1 failures recorded; additional refusal paths exercised by kit |
| `r3c2_batch_tools.py` | Partition independently run; seal, join, coverage and separate limb chains exercised by kit |
| `r3c2_manifest.py` | Live fifth C5 command succeeded; kit exercised root/symlink refusal |
| `r3c2_timeout.py` | Child success 0; child failure 7 propagated; 0.1-second synthetic cap gave SYMBOLIC_TIMEOUT and exit 124 at 0.105 seconds |
| `r3c2_seat_sandbox.sb` | Contents inspected and digest verified; future live dispatch probes not certified |
| Protocol and named leak-floor judgement | Existence/size only; contents not opened |
| Seat paths and filenames | Packet/brief agree on ownership extracts, canonical batch names, separate limbs, integration directory and `.` as sources_dir |
| SPI exhibition and historical byte copies | Delivered and both fail-first executions run independently; pin verification and three kit predicates checked |

C5 live commands, each as its rendered Markdown code span, were:

```text
/usr/bin/python3 -E --version
  exit 0; Python 3.9.6
/usr/bin/python3 -E -c "import sympy; print(sympy.__version__)"
  exit 0; 1.14.0
/usr/bin/shasum -a 256 /usr/bin/python3
  exit 0; b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"
  exit 0; /Users/duhokim/Library/Python/3.9/lib/python/site-packages
/usr/bin/python3 -E r3c2_manifest.py /Users/duhokim/Library/Python/3.9/lib/python/site-packages
  exit 0; FILES=20637
  MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
```

Literal-source distinction disclosed: executing C5(2) with the raw Markdown source's newline and two-space indentation preserved inside the quoted Python program returned exit 1, `IndentationError: unexpected indent`. Markdown inline-code rendering replaces that source line ending with a space; the resulting one-line command above passes. C5_EXECUTABLE_UNDER_SCOPE=YES concerns that printed command and the C4 permissions, not a claim that the raw multiline extraction succeeded. No C5_HARNESS_PINNED=PASS is certified without a dispatch pin comparison.

NO_MASKED_STAGE=YES: the operative mandated commands are individual invocations, not shell pipelines. Child exit status and timeout propagation were tested. Output redirection in this referee's log capture did not replace the child exit status.

**Q7(a), current-text executable tokens:**

```text
PACKET_BUILT_FROM_ACCESS_SHA=YES
PACKET_REBUILD_BYTE_IDENTICAL=YES
C4_PACKET_REDACTED=PASS
SEAT_PIN_SHEET=8/8_OK
KIT_PIN_SHEET=15/15_OK
TOOL_PINS_MATCH_MASTER=YES
CORPUS_ENUMERABLE=89/89_OK
CORPUS_RAW=79/79_OK
PARTITION_REGENERATED=BYTE_IDENTICAL
KIT_CONTROLS=185/185_PASS
KIT_DELETION_PROBES=63
STAGED_TESTS=PASS
SPI_DELIVERED=PASS
SPI_V33_BYTECOPY=FAIL_MEMBER_ORDER_ROW_ONLY
SPI_V32_BYTECOPY=FAIL
SPI_V32_SAME_ORIGIN_SEARCH_AUDITOR_B=[1,0]
OWN_CONSTRUCTIONS=15
OWN_VALIDATED_SEAT_LEDGERS=30/30_PASS
OWN_MERGED_BYTES_EQUAL=15/15
OWN_WHOLE_OUTCOMES_EQUAL=15/15
OWN_EXPECTED_VERDICTS=13/15
OWN_UNEXPECTED_VERDICTS=cycle_A,cycle_B
Q1_BASIC_ORIGIN_COMPUTE=CHOSEN,FITTED,IMPORTED_AS_SPECIFIED
Q2_WRONG_PLAUSIBLE_ORIGIN_VALIDATE=PASS
Q2_CORRECTED_AUDITOR_VS_WRONG_SEAL=FAIL_BOTH_ORDERS
Q3_DIRECTIONAL_HYPOTHESIS_RECONSTRUCTED=NO
Q5_SUBSTANTIVE_CONSEQUENCE_RECONSTRUCTED=NO
Q6_DOWNSTREAM_ANALYTICAL_PURPOSE_RECONSTRUCTED=NO
C5_RENDERED_COMMAND_EXITS=[0,0,0,0,0]
C5_RAW_MULTILINE_SYMPY_COPY_EXIT=1
WRAPPER_EXITS=[0,7,124]
```

Own constructions include equal records with reversed top-level/nested member order; chosen, fitted and imported roots; semantic origin correction; both complete provenance branches; both unsupported hybrid directions; four independently varied same-origin search alternatives; and both complete auditor reconstructions of the graph counterexample. Whole-outcome comparison retains sealed_ledger_sha256, ordered failures and exit codes. All permutation comparisons passed. No finite suite proves an unrestricted universal property; the code inspection supports the canonical ordering/serialization repair, while F1 demonstrates that invariant outputs can still be wrong.

**Q7(b):** C5_EXECUTABLE_UNDER_SCOPE=YES and NO_MASKED_STAGE=YES, with the exact raw-source distinction above.

**Q7(c): PRIOR_FINDINGS_CLOSED=YES.**

- **V33 codex F1 — CLOSED.** `merge` now writes JSON with recursive `sort_keys=True`. My reversed-member fixture validates in both seats and yields equal merged bytes and complete audit outcomes, with exits [0,0]. The delivered 11-construction exhibition passes. Pinned V33 tools fail only the member-order row, with unequal bytes/artifacts and unchanged verdicts [0,0].
- **V33 codex F2 — CLOSED.** The complete parent-only alternative is relabelled and retained as positive. Both true hybrids are present, require exits [1,1] and the unconditional “record matches neither complete declared branch” diagnostic. Both executed accordingly. Every fixture asserts its expected verdict; the writer preserves input member order; complete artefacts and emitted failure order are compared.
- **V33 kimi — no findings.** Nothing to close.

The three SPI kit controls assert the promised conditions: delivered exhibition exit 0 and SPI=PASS; V33 exit 1 and SPI=FAIL with exactly one BAD row identifying member order; V32 exit 1 and SPI=FAIL with the explicit [1,0] verdict flip. All three passed when rerun. The copied kit initially lacked two parent-directory fixture dependencies in my scratch layout; after copying those delivered dependencies, its unchanged runner completed 185/185. That scratch setup error is not a missing-deliverable finding.

**Q7(d): NEW executable defect — F1**, with the source, complete graph construction, output tokens and exact replacement text above. C1 is cosmetic. The gate remains UNSOUND because F1 changes computation availability and a supported auditor's verdict, despite closure of the prior findings.

Reproducibility evidence is under `/tmp/r3c2_v34_codex.mPIvB3/`: `own_checks.py`, `own/commands.json`, `own/results.json`, `own/cycle_A/`, `own/cycle_B/`, `kit.log`, and `spi_live.log`, `spi_v33.log`, `spi_v32.log`. The cycle source's final line was clarified to print L = x + y = 4 after the initial run; both ledgers were revalidated successfully against the clarified source, and all ledger/candidate/audit bytes determining the reported counterexample remain unchanged. The delivered master, packet and tools remain unchanged. This report proposes repairs; it adopts, signs and starts nothing.

R3C2_V34_GATE_COMPLETE
