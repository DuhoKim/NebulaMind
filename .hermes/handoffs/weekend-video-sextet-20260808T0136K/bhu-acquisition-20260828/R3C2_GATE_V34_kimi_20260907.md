ACCESS_SHA=6e18bc39bbe35fa02a2aca4cdac9980b1790156c1915c1ef2a4e9a6e679d273c
PACKET_SHA=1dcbe84dd100541e472dea79ea6c8dbd6ac7561b59ac8e7cf6476486f983b05c
GATE=PREREG_SOUND
LEAK=content-level only: the corpus is cosmology on the Planck 2018 baseline ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.", C3) across "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6); failures are contemplated and pre-worded non-accusatorially ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator", §3); the printed-but-chosen case is anticipated and armored ("Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`", §3); provenance visibly matters (seven origins, machine-matched quotations, two readers, disputes carried never reconciled — the ruled floor); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader and audit integrity check, never an arithmetic input: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled — "An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`."; "the auditor re-classifies the record from the pinned sources" (C3) and the audit "recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison" (C6); while "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1) — no analytic use (no tally, comparison, weighting or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 34, living draft, unsigned — the header says Version 34; the brief's "Version 32" description is stale). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine. Order kept: the master was hashed first (ACCESS_SHA, line 1); the packet was then opened and Q5, Q6, Q3 answered from it alone, in that order, before the master was read; the master was then read in full; the two named V33 gate reports were opened only after the master, as the brief permits. Files opened: the packet; the master; R3C2_GATE_V33_codex_20260907.md; R3C2_GATE_V33_kimi_20260907.md; R3C2_SEAT_PACKET.sha256 and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256; the delivered tools r3c2_ledger_tools.py and r3c2_lane_tools.py (read and executed); r3c2_batch_tools.py (usage read; partition executed); r3c2_build_seat_packet.py, r3c2_manifest.py, r3c2_timeout.py (executed); r3c2_seat_sandbox.sb (digest only); the kit r3c2_staged_d1d7/ (README, r3c2_spi_exhibition.py, and the three SPI-control lines of r3c2_staged_tests.py read; the kit and the exhibition executed; _v32_bytecopy/ and _v33_bytecopy/ checked by digest and by grep of the merge write lines); R3C2_CORPUS_MANIFEST.md (metadata only: header, table formats, row digests and byte counts — never content); the corpus directory listing (names only); R3C2_INTERPRETATION_PROTOCOL_20260904.md (metadata only: exists, 3,947 bytes; never opened). No other file; no other version's gate file was sought or opened. Every fixture below is synthetic referee evidence under /tmp/r3c2_v34_kimi* with my own sources, ids and values; no corpus content; not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalizes every outcome: "No tier, warrant token, standing or stamp moves." (§11) — it denies consequence transfer on the named axes for every outcome and ranks nothing. The remaining consequence-adjacent sentences are procedural and attach to stop states, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), and "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2) — a filing precedence, not a weight. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Yes for the checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled ("Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`", §4 item 7); the auditor re-classifies ("the auditor re-classifies the record from the pinned sources", C3) and recomputes the chains of origins to their roots under the matching branch (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What cannot be stated from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), partitioned into 12 batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "**Wording: \"unreproduced from the stated inputs,\" not \"error.\"**" (§3); a reporting rule, not an expectation of any rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, audit re-classification — but nothing says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' numbers follow from their own stated inputs, and it cares how much of the corpus rests on chosen/fitted/imported/undeclared inputs — with failures possible and reputationally delicate (hence the wording rule). Nothing in the packet says which tally would help or hurt any hypothesis; §11 affirms nothing moves. That is the ruled floor, and this seat's answer sits at it.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions I constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2; and even taken as co-occurring, the order decides). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable or non-enumerable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration (a zero denominator with no passage on either side can pass the audit; "A sealed denominator of zero with any passage on either side fails", C6).

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on (my own fixtures, delivered tools): (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; asymmetric = the half-width on the side the reproduced value falls), rests_on USES_CHOSEN — executed in my M3–M6 family: both seat ledgers validate exit 0 (24/24), merge exits 0 in both orders, compute derives the root set from the recorded origins and emitted the disputed pair rests_on=['USES_CHOSEN', 'DERIVED_STANDARD_OR_MEASURED_ONLY'] marked DISPUTED for the disputed merged record; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same mechanics; the lane exhibition's CHOSEN-vs-FITTED construction exercises the branch); (c) an imported value — the local-import branch (source_file equal to the claiming file) and the external-import branch (verified manifest row, numeric token, first symbol-and-value line) are both enforced by validate (delivered r3c2_ledger_tools.py lines 151–181; my IMPORTED record at myB.txt:5 — the first line carrying both symbol and numeral — validates exit 0; my Q2 RIGHT_EXT record likewise), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT with roots {UNDECLARED}. Seats never write rests_on: compute REJECTS a ledger arriving with root_origins or rests_on set (executed: "REJECT: i1 arrives with root_origins set", exit 2); an empty ledger yields rests_on NOT_COMPUTED rows for every included claim (executed, exit 0). The four earlier rounds' tension is gone because provenance no longer gates admissibility: every stated input is consumed by status (the mechanical test stays decidable), and the provenance question is a computed field beside the verdict, not a verdict — so every declared outcome has a path and no outcome requires a judgement the method forbids.

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed and printed (my runs print both blocks and the author_specified_input line); C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate ≤ 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. Executed with my own fixtures (/tmp/r3c2_v34_kimi_q2, own sources myA.txt/myB.txt): (1) THE WRONG-BUT-PLAUSIBLE RECORD: value "2" printed at myA.txt:3 in the sentence "We adopt a = 2 from myB (2020)." — filed CHOSEN/ORIG_CHOICE_STATED with that true quotation at its true line — validate returned exit 0, C3_NO_SUBSTITUTION=PASS: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (2) THE MECHANISMS THAT CATCH IT, AS WRITTEN: (i) the second seat's independent classification — filing IMPORTED/ORIG_CITATION (local branch, same coordinates) merged exit 0 with origin=IMPORTED origin_alt=CHOSEN — the dispute is carried, never silently resolved, and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; filing IMPORTED with the external value line instead made merge exit 1 ("seats disagree on source_file ('myA.txt' vs 'myB.txt') — a value/status/coordinate disagreement is not silently resolved", FIELD_DISAGREEMENTS=2, no merged file) — the disagreement surfaces in the open before the tally; (ii) the C6 re-classification — against a CHOSEN-only sealed ledger, my IMPORTED auditor record produced compare exit 1, C6_AUDIT_SAMPLE=FAIL, first diagnostic "record matches neither complete declared branch" (the roots are recomputed under the matched branch only); when the second seat's IMPORTED branch was preserved, the same auditor record PASSED (exit 0). (3) THE STATED FLOOR: when my auditor repeated the same wrong code, compare returned exit 0, C6_AUDIT_SAMPLE=PASS — "if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away" (C3 lane-side note). The machine floor that IS enforced is real and was exercised: a record whose value line lies in another file must be IMPORTED with ORIG_CITATION whatever code was submitted (validate lines 148–150); an empty quotation on any non-silent record fails; a STANDARD value line outside the claiming file fails; a BLOCKED record without the claiming-file binding fails; an ORIG_SILENT record without origin_search fails. origin_search adequacy is judged from the printed search ("a search of one query with no variants is not adequate"); root_origins and rests_on are script outputs — compute REJECTS a ledger arriving with them set (executed, exit 2).

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no pattern content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below: rebuilt byte-identical, C4_PACKET_REDACTED=PASS), and my packet-first Q3 found no hypothesis. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind (census fails a row that lacks or alters those fields — kit-asserted); both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any sealed-side omission (included or excluded, either direction) files CENSUS_AUDIT_FAILED — the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt (regenerated deterministically in my kit run) emits exactly these cases. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script — compute REJECTS a ledger arriving with it set (executed). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — each stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 6e18bc39…" (= ACCESS_SHA, character for character) and "seat packet sha256 1dcbe84d…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (both 1dcbe84dd100541e472dea79ea6c8dbd6ac7561b59ac8e7cf6476486f983b05c), and the packet's line 9 built-from digest equals ACCESS_SHA. The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed"; the seat runs from a redacted copy outside the lane under the kernel sandbox profile r3c2_seat_sandbox.sb (digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface the text prints; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited. My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED, leaves the interpretation NOT_RUN and voids the comparison — and §4's CENSUS_AUDIT_FAILED definition names the seal-receipt case. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (my pipelines ran the audit strictly in stage order; the kit asserts the refusal and its deletion probe). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened).

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary is audit-internal — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs emitted it only inside a disputed pair marked DISPUTED, never as a clean label over a disputed graph; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — kit-asserted).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first (ACCESS_SHA, line 1) and read in full; the packet hashed (PACKET_SHA, line 2); R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 15/15 OK from the lane directory, both re-verified exit 0 after all runs; the packet's line 9 built-from digest equals ACCESS_SHA. Every named script exists beside the document and matches the digest the document prints for it: r3c2_ledger_tools.py bd1f4a237357f43976d0776ec178eac78b1a648f8c5aa43e16a085f78a62a3e3 (§3), r3c2_lane_tools.py 5ded15ec9685d9faa05991aab83c62dd9f4872b5e03828e743d84404a0baacff (C3, §9), r3c2_batch_tools.py 3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e (C1), r3c2_manifest.py 6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a (C5), r3c2_timeout.py fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 (§9), r3c2_build_seat_packet.py 56ac7d1c78196c4d246244126b43666d804f093f8fbe4e2c2289ab885def728e (C4), r3c2_seat_sandbox.sb 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 (C4), R3C2_CORPUS_MANIFEST.md 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 (§2). C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text's executability). r3c2_timeout.py: a fast child exits 0 with WRAPPER_EXIT=0 printed; a failing child's exit 7 is carried as the wrapper's; a 0.3 s cap on a 5 s child printed SYMBOLIC_TIMEOUT and exited 124. r3c2_lane_tools.py: merge both orders across 12 constructions; compute derives roots and rests_on (USES_CHOSEN and the disputed pair observed), NOT_COMPUTED rows on an empty ledger, exit 2 without the candidate argument (usage, no output written), exit 2 REJECT on a ledger arriving with root_origins set; merge FIELD_DISAGREEMENTS path exits 1 in both orders with identical stdout and no merged file. r3c2_ledger_tools.py: validate on 24/24 seat ledgers exit 0 plus the Q2 negative paths; census and census final exit 0 with C1_DENOMINATOR_PRINTED=PASS, both count blocks and the author_specified_input line printed; the five audit subcommands run in stage order across all constructions. r3c2_batch_tools.py: partition regenerated byte-identical to the pinned 39b8d9bf199265ab… json. The builder rebuilt the packet byte-identical with C4_PACKET_REDACTED=PASS. The corpus manifest's rows verify at their declared source location: enumerable 89/89, RAW 79/79 (digests and byte counts only; contents never read). The kit: 185 controls, 63 deletion probes, STAGED_TESTS=PASS, exit 0. Nothing the document describes failed to execute as delivered. Future receipts, provider logs, dispatch records and seat outputs are procedural artefacts of a future run, not deliverables this design text claims exist; the interpretation protocol exists (3,947 bytes; metadata only).

## Q7 (V34) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=15/15 OK (before and after the kit run)
  TOOL_PINS_MATCH_MASTER=YES (ledger bd1f4a23…, lane 5ded15ec…, batch 3822f12c…, manifest 6b6e4bce…, timeout fbb9bef7…, builder 56ac7d1c…, sandbox 6978d590…, corpus 26dabe7c…)
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only, at the declared ../bhu-reading-20260823/sources/)
  KIT_RUN: deletion_probes=63 / controls=185 passed=185 failed=0 / STAGED_TESTS=PASS (exit 0)
  SPI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; all 11 reviewer constructions ok: merged-bytes-equal=True outcome-equal=True; hybrid directions 1 and 2 verdict(AB,BA)=[1, 1] with the "record matches neither complete declared branch" diagnostic; the member-order construction verdict(AB,BA)=[0, 0])
  SPI_EXHIBITION_V33_BYTECOPY=SPI=FAIL (exit 1; exactly ONE BAD line — the member-order construction: merged-bytes-equal=False outcome-equal=False verdict(AB,BA)=[0, 0]; the V33 bytecopy lane tool hashes 42c1577e…, the V33-era delivered pin recorded in both V33 gate reports)
  SPI_EXHIBITION_V32_BYTECOPY=SPI=FAIL (exit 1; the codex V32 construction flips exactly: verdict(AB,BA)=[1, 0]; the byte copies hash 146ff299…/eb479feb… as recorded in the kit pin sheet)
  OWN_FIXTURES_SPI=PASS (exit 0; 12 constructions of my own design — M1 structurally equal IMPORTED records member-scrambled at every level; M2 structurally equal ORIG_SILENT records with origin_search scrambled inside; M3/M4 my own true hybrids (MEASURED origin+evidence with CHOSEN parents, and the reciprocal) both FAIL in both orders with the NEITHER diagnostic; M5/M6 complete branches PASS; M7 equal labels with different searches AND different parents, auditor = A-search + B-parents, FAILS both orders with NEITHER (the joint-match predicate); M8a/b/c the V32-F1 shape (PASS, PASS, FAIL-NEITHER); M9 parent-only alternative PASS; M10 fabricated quotation FAIL-NEITHER — every seat ledger validates exit 0 (24/24), merged bytes equal in both orders in all 12 on the DELIVERED tools, and the whole audit outcome (exit code, ordered FAIL lines, the complete C6_AUDIT.json including sealed_ledger_sha256) is equal)
  OWN_FIXTURES_V33_BYTECOPY=SPI=FAIL (exit 1; 9 of 12 BAD — every construction containing a structurally-equal record pair scrambled across the files (M1, M2, and the STD helpers in M3–M7, M9, M10) shows merged-bytes-equal=False, whole-outcome-equal=False with VERDICTS UNCHANGED (expected exit in both orders, diagnostic present): codex F1's exact signature — equal canonical keys, tie broken by supply order, different sealed_ledger_sha256, same verdict; M8a/b/c, with no structurally-equal record across seats, remain ok: the leak needs a canon tie)
  OWN_FIXTURES_V32_BYTECOPY=SPI=FAIL (exit 1; 11 of 12 BAD, including VERDICT FLIPS: M7 [0,1], M8a [0,1], M8b [1,0] — the V32 class is bigger: seat-A-primary swaps both bytes and verdicts)
  MERGE_FIELD_DISAGREEMENT=exit 1 both orders, identical stdout, no merged file
  COMPUTE: roots/rests_on on my merged ledgers (USES_CHOSEN; disputed pair ['USES_CHOSEN','DERIVED_STANDARD_OR_MEASURED_ONLY'] marked DISPUTED) / exit 2 without candidates / exit 2 REJECT with root_origins set / NOT_COMPUTED rows on an empty ledger
  CENSUS=exit 0 C1_DENOMINATOR_PRINTED=PASS / CENSUS_FINAL=exit 0 C1_DENOMINATOR_PRINTED=PASS
  Q2_MECHANISM: WRONG_CHOSEN_ON_CITATION validate exit 0 / second seat IMPORTED(local) merge exit 0 origin_alt preserved / second seat IMPORTED(external) merge exit 1 FIELD_DISAGREEMENTS=2 no file / auditor IMPORTED vs CHOSEN-only sealed exit 1 C6_AUDIT_SAMPLE=FAIL / vs preserved alt exit 0 PASS / auditor repeats CHOSEN exit 0 PASS (the stated floor)
  C5_LIVE: Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1… (exit 0 each)
  WRAPPER: exit 0 (fast) / exit 7 carried (failing child) / exit 124 with SYMBOLIC_TIMEOUT (0.3 s cap on a 5 s child)
  BUILDER_REBUILD: C4_PACKET_REDACTED=PASS, rebuilt packet byte-identical to the pinned packet (51 spans; sections kept/dropped as stated)
  PARTITION_REGENERATED=byte-identical (39b8d9bf199265ab…)
  NO_SECOND_PREDICATE=YES (grep-level sweep: delivered r3c2_lane_tools.py line 104, the canonical-key comparison marked PROBE:SPI_CANONICAL, is the only seat-order-sensitive read in either delivered tool; the seat tool contains no A/B-seat reference; compare and compute operate on the merged ledger only)
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the document's printed spans are the STANDARD table's markdown cell separators); the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the kit's 63 deletion probes each turn a negative into PASS only when its check is neutralised (or show the diagnostic absent while an overlapping guard still fails), so no check is decorative; every exit code cited in this report was captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES over the V33 gate's findings (codex F1, F2; kimi none), each with my own evidence:
  - codex F1 (`merge` wrote its output without sorted keys, so two structurally EQUAL seat records supplied in different JSON member order — equal canonical keys, tie broken by supply order — merged to different bytes and a different sealed_ledger_sha256 with the same verdict) — CLOSED. Text: §3 now carries the SPI sentence with the sorted-keys clause: "`merge` serialises its output recursively with sorted object keys, so equality of canonical record keys cannot expose the first seat's original member order" — codex's replacement in substance (his text read "serializes"; the document's spelling is "serialises"; his minimal implementation patch is adopted verbatim in code and verified below). Code: delivered r3c2_lane_tools.py line 121 writes `json.dumps({"records":out_recs},indent=1,sort_keys=True)` — recursive sorted-key serialisation, marked PROBE:SPI_SORTED_BYTES (V34); line 104 remains the ONE seat-order-sensitive read (the seat-blind canonical key); no second predicate added anywhere (my sweep). Execution: my M1/M2 constructions — structurally equal records member-scrambled at every level, including nested origin_evidence and origin_search — merge to EQUAL bytes and an EQUAL whole audit outcome on the delivered tools (verdicts [0, 0], sealed_ledger_sha256 equal); the same constructions on the pinned V33 bytecopy leak exactly (merged-bytes-equal=False, the artefact's sealed_ledger_sha256 differs, verdicts unchanged [0, 0]); every multi-record construction of mine (M3–M10) carries scrambled structurally-equal helper records and shows the same live-vs-V33 split; the lane exhibition FAILS against the V33 bytecopy on the member-order row only (my run: exactly one BAD line) and against the V32 bytecopy with the codex construction's verdict flipping [1, 0] exactly. The fail-first claim of §10.29 is reproduced, not narrated.
  - codex F2 (the exhibition row labelled "hybrid direction 1" was a complete parent-only alternative, so no true hybrid in that direction had been constructed) — CLOSED. The row is relabelled "complete parent-only alternative: identical origin and evidence, auditor matches the second parent set (was mislabelled a hybrid — codex V33 F2)" and kept as a positive case (expected exit 0); the true hybrid — "hybrid direction 1: A's origin+evidence (DERIVED, equation) with B's parents (B = CHOSEN, choice evidence, parents [i3])" — and its reciprocal "hybrid direction 2" are added, both required to FAIL in both orders with the unconditional "record matches neither complete declared branch" diagnostic (exhibition rows, and observed in my live run: both verdict(AB,BA)=[1, 1], diagnostic present in both orders); every construction asserts its expected PASS or FAIL as well as permutation equality (the exhibition's exp column, asserted in its line 69); the fixture writer no longer normalises member order (line 12, no sort_keys); the complete artefact is compared with sealed_ledger_sha256 retained (the artefact tuple element) and failure lines in emitted order (line 57). The class, not the instance: my own M3/M4 are a different hybrid family (MEASURED origin+measurement evidence with CHOSEN parents, and the reciprocal), and both FAIL in both seat orders with the same unconditional diagnostic on the delivered tools; my M7 (equal origin labels, different searches AND different parents; auditor = A's search with B's parents) FAILS in both orders — the joint-match predicate ("origin, origin evidence, parent list and any required `origin_search` jointly match one complete preserved branch") holds.
  - kimi V33: no findings — nothing to close.
  The kit's three SPI controls assert what §10.29 says: control "SPI: the audit outcome is identical under both seat orders for every reviewer construction (delivered tools)" requires the exhibition's exit 0 plus SPI=PASS over the staged tools, whose bytes equal the delivered tools (§10.29: "PASSES against the delivered tools"); control "SPI fail-first (V34): the exhibition FAILS against byte copies of the V33 tools on the member-order construction only (merge wrote unsorted keys; codex V33 F1)" requires exit 1, SPI=FAIL, exactly one BAD line, and that line naming the member-order construction (§10.29: "FAILS against pinned byte copies of the V33 tools on the member-order row only"); control "SPI fail-first: the same exhibition FAILS against byte copies of the V32 tools (seat A was always primary; same-origin searches dropped)" requires exit 1, SPI=FAIL and the exact flip string "verdict(AB,BA)=[1, 0]" (§10.29: "and against the V32 copies as before"; §10.28(4): the verdict flipping on the codex construction exactly). All three ran ok inside my kit run (185/185, 63 probes, STAGED_TESTS=PASS), and I reproduced all three exhibition runs independently with the tokens above.

(d) NEW executable defects, with counterexample: NONE. Every construction behaved as the V34 text claims. Tested beyond the required set: the merge FIELD_DISAGREEMENTS path (exit 1 in both orders, identical stdout, no file written); both compute rejection paths (exit 2 without candidates; REJECT of a ledger arriving with root_origins set); the empty-ledger NOT_COMPUTED rows; census and census final; the wrapper's three behaviours; the builder's byte-identical rebuild; the partition's byte-identical regeneration; both pin sheets before and after; the corpus rows (89/89 enumerable, 79/79 RAW, metadata only); the Q2 wrong-origin mechanism chain end to end. Observations, NOT findings: (i) a stray origin_search attached to a non-ORIG_SILENT record is preserved only with the primary branch and is not compared by the audit — this matches the text as written ("parent list and any required `origin_search`", §3; the schema requires the search only "when reason_code is ORIG_SILENT"), the field feeds no root, verdict, count or class, and merged bytes remain order-invariant; it changes no class filed, no control's passability, no reachability, no conclusion and no tested value, so it is not even a COSMETIC repair request. (ii) COSMETIC note, not a repair request: §3's SPI sentence carries codex's V33 F1 replacement with the document's spelling ("serialises" for his "serializes") and §10.29 narrates it as "codex's exact text"; the adopted behaviour is his exact minimal implementation patch, verified at line 121 and by every construction above — the wording difference changes no class filed, no control's passability, no reachability, no conclusion and no tested value, and §10 binds nothing.

Why SOUND and not SOUND_WITH_REPAIRS: no defect at any level, cosmetic included, remains open against the operative text; both V33 findings are closed above with my own fixtures, and the class each instanced is stated once (§3's SPI paragraph), enforced in one place (merge's canonical key plus recursive sorted-key serialisation), and exhibited over every reviewer construction and my own twelve in both seat orders, with fail-first reproduced against two pinned generations of byte copies. Why not UNSOUND: Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds each artefact before the step it must precede; every negative outcome is worded within its definition; and nothing the document describes failed to execute as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 6e18bc39… = ACCESS_SHA; packet 1dcbe84d… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v34_kimi*, not corpus data and not a census.

R3C2_V34_GATE_COMPLETE
