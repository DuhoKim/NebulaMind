ACCESS_SHA=b2e2073c49936328a533c5926465354ea01db0a63aaec7cca3626acc07e39872
PACKET_SHA=a3314cb5605a647bf0401269145b72078b1246baac3aa7326e7f3cea6033f0d8
GATE=PREREG_UNSOUND
LEAK=Provenance is a research focus and the constants identify cosmological subject matter; no directional hypothesis, comparison preference or hoped-for result reconstructed.
CONSEQUENCE_VISIBLE=Procedural: CENSUS_ORIGIN_DISPUTED stops continuation — “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent checking, audit and dispute handling — “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Its downstream analytical use is not disclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only review of the delivered unsigned Version 35. The V34 mixed-primary-graph defect and exhibition docstring finding are closed. Two new executable defects remain: a matched dependency cycle crossing a CHOSEN record passes the audit; an all-DERIVED matched cycle produces nondeterministic diagnostics and unequal complete audit outcomes despite identical merged bytes. The first changes control passability; the second changes an explicitly tested value.

Access order: the exact requested master hash command ran first; the packet was hashed and read, and Q5, Q6 and Q3 were answered before reading the master. The master was then read in full. Section 10 supplies no inherited verdict. Q7's later executable instructions were treated as authorization to inspect the required tools, kit, dependencies, pins, brief and corpus metadata, and the two specifically permitted V34 reports. No other gate report or interpretation-protocol contents were opened. Corpus files were verified by hashes and byte counts, without scientific interpretation. All executions used synthetic control fixtures; no corpus derivation, census, dispatch, adoption or signature occurred.

Packet-only answers, preserved in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Yes, procedurally. For CENSUS_ORIGIN_DISPUTED: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” This outcome stops continuation. No sentence identifies an externally preferred scientific result. “No tier, warrant token, standing or stamp moves” expressly limits external consequences. Procedural consequence is visible; an empirical stake is not reconstructed.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

Yes, partly: “An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Origin is used for independent agreement checks, audit and a stopping threshold. The packet does not disclose its downstream substantive classification or interpretation mapping.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

I cannot reconstruct a directional scientific hypothesis, preferred result or comparison-model preference. “The reproduction verdict and the provenance fields are recorded separately” reveals methodological interest in provenance beyond arithmetic. The origin-disagreement threshold makes provenance operationally important. “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline” identifies subject matter and reference values, but not a comparison hypothesis. These limited inferences do not identify whether reproduction, failure or any particular provenance distribution would serve the lane's expectation. These answers were committed before the master was read.

## 1. OUTCOME CLASSES

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

**YES at the definitional and bounded per-claim filing level.** Provenance does not veto consumption of a PRINTED or STANDARD input. The separate executable audit guarantees are defective as described in section 2 below; that does not require reopening option (c).

Concrete physical-claim witnesses are synthetic design examples, not findings attributed to unopened corpus papers:

| Claim and inputs | Unique outcome | `rests_on` |
|---|---|---|
| “We choose a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same stated input and recipe, but L printed as 5 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 2 m from X; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted locally, but found at the required line of verified enumerable X | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted and cited to non-enumerable X | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, with a neither printed nor traced to a source | REPRO_INPUT_ABSENT | USES_UNDECLARED for an appropriately evidenced silent record |
| Fully specified computation exceeds the cap | REPRO_NOT_EVALUABLE | Computed separately from its input records |
| Own numerical result with no producing procedure and no input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

The author-set input numeral is excluded as AUTHOR_SPECIFIED_INPUT; the downstream numerical result remains included. A fitted parameter asserted as an own result is itself a candidate, and its printed value remains consumable in another claim. My delivered-tool fixtures emitted the stated CHOSEN, FITTED and IMPORTED classifications. Empty-ledger computation emitted NOT_COMPUTED.

Hard overlap: L = a + b, where a is unprinted and cited to an unpinned source, while b is unprinted and uncited. Both terminal predicates hold, but the explicit precedence files REPRO_BLOCKED only. A stated input whose computation times out takes REPRO_NOT_EVALUABLE before the arithmetic group. I found no double filing or missing per-claim class after applying the precedence. This is bounded classification, not a promise to decide arbitrary mathematics within 120 seconds.

All eight study classes have witnesses: a nonempty all-arithmetic tally and audit PASS gives COMPLETE; a matching absent-input claim and successful audit gives PARTIAL; missing seed gives AUDIT_FAILED; C3 failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 included claims gives ORIGIN_DISPUTED; a surviving fail/pass control split gives CONTROL_SPLIT.

A partial tally with no audit seed satisfies two conditions but files AUDIT_FAILED by precedence. INCONCLUSIVE is genuinely reachable through PARTIAL with a successful audit. An entirely empty enumeration on both sides can also reach PARTIAL; a zero included denominator with candidate passages instead triggers the stated audit-failure predicate. No unclassified terminal study state was exhibited. These witnesses do not substitute for the required independent two-seat C0 before freeze.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

YES as cited evidence plus independent semantic classification; NO as a machine guarantee that the quoted sentence has been interpreted correctly. My wrong-but-plausible record was:

```json
{
  "claim_id": "p.txt#c",
  "input_id": "p.txt#a",
  "symbol": "a",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "p.txt",
    "source_line": 1,
    "verbatim": "We adopt a = 2 from Reference."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "p.txt",
  "source_line": 1
}
```

The synthetic source contains that exact sentence at line 1. Both seat validations returned exit 0. The citation-first rule requires IMPORTED, but quotation matching cannot determine that semantic precedence. An independently correct second classification exposes a dispute. My IMPORTED auditor against the CHOSEN-only seal returned exit 1 and C6_AUDIT_SAMPLE=FAIL in both orders, including “record matches neither complete declared branch” and differing root origins. Shared misclassification by all readers can survive, as the master acknowledges. This is a stated limitation, not a new defect.

The controls name PASS/FAIL/NOT_RUN contracts: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their required artefacts include exhibitions, command output, seals, audit JSON, path lists and provider logs. A bare asserted PASS cannot satisfy a printed-run control. C4/C5b can pass with an incomplete self-reported path list; their predicates expressly disclose that limitation. Unreached checks are NOT_RUN, including per-session validation before integration.

**F1 — NON-COSMETIC: a matched cycle crossing a non-DERIVED record can pass C6.**

Verbatim operative sentence, §3:

> A cycle or missing dependency in that matched graph fails the audit.

Counterexample: both census-seat graphs validate and are acyclic. All input records belong to p.txt#c, have IDs p.txt#x, p.txt#y and p.txt#r, status PRINTED, value string `2`, and source_file p.txt. The synthetic evidence source is:

```text
1  We choose x = 2 using y.
2  y is calculated as 2 from r.
3  We measure r = 2 using a ruler.
4  We choose x = 2 using r.
5  y is calculated as 2 from x.
6  The recipe gives L = 4.
7  The other result is 2.
```

The line numbers shown are labels, not bytes in the source. The graph-control candidate is p.txt#c at line 6, with an agreed arithmetic outcome and printed/reproduced strings `4`. These are synthetic ledger/audit fixtures, not a complete census of this miniature text.

| Record | Origin / reason code | Value line | Seat A parents / evidence line | Seat B parents / evidence line |
|---|---|---:|---|---|
| x | CHOSEN / ORIG_CHOICE_STATED | 1 | [y] / 1 | [r] / 4 |
| y | DERIVED / ORIG_EQUATION | 2 | [r] / 2 | [x] / 5 |
| r | MEASURED / ORIG_MEASURED | 3 | [] / 3 | [] / 3 |

Every evidence quotation is the complete corresponding source line. A is x → y → r; B is y → x → r. There are zero origin-label disagreements. Parent alternatives are explicitly supported; the schema does not prohibit CHOSEN records with parents, and the delivered exhibition itself uses such records.

The fixed auditor takes A's complete x record, B's complete y record and the common r record. Each input jointly matches a complete preserved branch: there is no within-record hybrid. The resulting matched graph is x → y → x. It is cyclic.

Observed on the delivered tools, in both seat orders:

```text
F1_SEAT_VALIDATE=[0,0]
F1_MERGED_BYTES_EQUAL=True
F1_COMPUTE=[0,0]
F1_RESTS_ON=['USES_CHOSEN','USES_CHOSEN'], DISPUTED=True
F1_AUDIT=[0,0]
F1_C6_AUDIT_SAMPLE=PASS
F1_WHOLE_OUTCOME_EQUAL=True
```

The cause is that `roots()` returns immediately at any origin other than DERIVED. The audit relies on that traversal to detect cycles. Its closure walker follows every parent edge but suppresses revisits without diagnosing cycles. Consequently the CHOSEN vertex terminates root traversal before the cycle can be detected. This is a false PASS under the quoted graph-integrity requirement, not merely a different substantive provenance interpretation.

Exact replacement for the quoted sentence:

> Before root-origin classification, `audit compare` checks every `derived_from` edge in each selected claim's auditor-reconstructed dependency closure and its matched sealed closure for missing records and cycles, independently of `origin`. A missing dependency or cycle makes that claim `MISMATCH` and fails the audit, including when the cycle passes through a CHOSEN, FITTED, IMPORTED, MEASURED, STANDARD or UNDECLARED record. Root-origin traversal retains its stated origin rule; stopping at a non-DERIVED origin never substitutes for graph-integrity validation. Unmatched diagnostic views do not participate in this decision.

Implement and re-pin this check; a prose-only replacement is insufficient. Add the counterexample as an expected FAIL, with complete A and complete B retained as expected PASS controls. A scratch causal probe adding an origin-independent graph check changed the cyclic auditor to exit 1 while both complete original reconstructions remained exit 0. Delivered tools were not edited.

**F2 — NON-COSMETIC: randomized set traversal changes the complete audit outcome.**

Verbatim operative sentence, §3:

> **SEAT-PERMUTATION INVARIANCE ("SPI"): the audit verdict, and the whole audit outcome, are invariant under permutation of the census seats.**

Use the same topology as F1, but make x DERIVED with equation evidence. The complete source is:

```text
1  x is calculated as 2 from y.
2  y is calculated as 2 from r.
3  We measure r = 2 using a ruler.
4  x is calculated as 2 from r.
5  y is calculated as 2 from x.
6  The recipe gives L = 4.
7  The other result is 2.
```

Origins are DERIVED for x and y, MEASURED for r. Value lines are 1, 2 and 3. A's evidence lines are 1, 2 and 3; B's are 4, 5 and 3. Parent lists are unchanged from F1. Both seat graphs validate. The fixed auditor again takes A.x, B.y and common r, so its matched graph contains an all-DERIVED cycle.

Both orders correctly return audit exit 1 and compute exit 0, with identical merged bytes. However, one audit reports `cycle at p.txt#x` and the other `cycle at p.txt#y`. The difference appears in the ordered FAIL output, the per-claim `why` array and `inputs._roots.error` of C6_AUDIT.json. The independent paired checks exhibited this twice. Twelve repeats of the identical compare command also produced both diagnostics.

Cause: `own` is a Python set, and root recomputation iterates it without sorting. The first cycle encountered therefore depends on process hash randomization. This is not evidence of a preferred seat: identical inputs can vary without any seat permutation. It nevertheless defeats the specified equality of whole outcomes across the separate processes used for the two seat orders.

Exact replacement for the quoted sentence:

> **SEAT-PERMUTATION INVARIANCE ("SPI"): with one fixed auditor reconstruction, permutation of the census seats produces identical merged bytes, exit codes, ordered failure lines and complete audit artefacts. Audit traversals and diagnostic selection are deterministic: starting input IDs and dependency IDs are visited in sorted order, and no reported error or output order depends on set iteration or process hash randomization. Canonical branch selection remains solely at `merge`; downstream deterministic traversal does not select a census seat. The exhibition also repeats identical cyclic and missing-dependency comparisons in fresh processes and requires byte-identical audit artefacts.**

Implement deterministic traversal, re-pin and add fresh-process failure-path controls. A sorted graph-check probe produced the same x-cycle diagnostic in six repeats. The current defect does not flip PASS/FAIL, but it changes values the SPI contract explicitly tests, so it is not COSMETIC under the requested criterion.

## 3. CIRCULARITY

The inclusion rule names no hypothesis or comparison preference. Two seats enumerate independently; every exclusion retains the numeral and source coordinates. C6 independently enumerates every pinned text before sealed ledgers are released. An auditor-only passage is an omission even if the auditor excludes it. A contrary claim cannot legitimately be quietly discarded without risking these checks; shared omissions and shared classification errors remain possible.

The origin taxonomy requires judgement, while its interpretation ranking is withheld. Receipt P fixes interpretation before limb A. These mechanisms constrain expectation reaching inclusion, exclusion, provenance and interpretation; they do not establish minds without prior beliefs. F1 is an integrity-check bypass, not evidence of intentional selection.

## 4. THE BLIND

The delivered builder ran on this master with the delivered brief and a scratch output. It returned C4_PACKET_REDACTED=PASS, removed 51 spans, kept sections 1–6, 9 and 11, and produced 40,267 bytes identical to the delivered packet. The packet embeds the correct ACCESS_SHA. Injecting forbidden `hypothesis` into a seat-visible section caused exit 1 and no output packet. Removing required C2_INPUT_LEDGER content likewise caused exit 1 and no packet.

This verifies the finite forbidden-list and required-content assertions, not universal semantic blindness. The packet-first answers reveal procedural consequences and provenance interest, but no directional scientific stake. C4 states that accepted floor and its limits.

The sandbox profile matches its pin. Its runtime, configuration and network exceptions agree with the text's limited confinement claim. A live probe could hash my in-scope synthetic source but received “Operation not permitted” for the lane master. Sandboxed SymPy import succeeded from the permitted working directory. My initial import probe from the denied lane working directory failed; correcting the probe's cwd resolved it, as the dispatch design requires. This setup mistake is not a deliverable defect.

No future dispatch inventory or complete confinement record is certified here. C4/C5b remain self-report controls; prior exposure, startup-code access and snapshot-pinning limits are explicitly disclosed.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before protocol opening. Independent verification must print both receipts and the four verified values; missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

The ordering binds the records before the relevant steps, conditional on executing the custody procedure. It does not prove the lane owner had not guessed the result. The protocol exists, 3,947 bytes, checked by metadata only; its contents and actual receipts were not inspected. This is a design judgement, not certification of completed custody.

## 6. FAIRNESS

REPRO_FAILED expressly requires “unreproduced from the stated inputs,” not “error.” The delivered brief applies that wording to negative outcomes generally. The missing-input, blocked-source, absent-procedure and computational-limit labels do not assert a scientific error.

REPRO_WITHIN_STATED_PRECISION matches the rounding/uncertainty predicate. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means complete arithmetic classification with audit PASS, including arithmetic failures; it does not assert that every claim reproduced. No additional outcome-label repair is required. F1 remains material despite fair outcome wording because it can make the audit certify a graph it promises to reject.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

**YES: the unconditional matched-graph rejection and whole-audit equality guarantees fail on F1 and F2.** No missing currently promised deliverable was found in the operative inventory checked below. Future dispatch records, ownership handouts, receipts, provider logs and seat outputs are prerequisites of a future run, not artefacts claimed already completed.

| Item checked | Method and result |
|---|---|
| Master and packet | Required hashes; packet-first reading; master read in full; final hashes unchanged |
| Brief, builder, packet pin | Read, hashed, explicit-argument scratch rebuild; byte-identical packet; two negative build probes refused |
| Seat and kit pin sheets | 8/8 and 17/17 entries verified |
| Corpus manifest and source paths | Manifest digest matched; 89/89 enumerable and 79/79 RAW files matched hashes and byte counts at the declared source location |
| Partition | Delivered partition command regenerated partition_12_of_89.json byte-identically |
| Ledger tool | Own validate, census, census final, seal-enumeration, select, handout, seal-rederivation and compare executions; 60 seat validations passed |
| Lane tool | 30 pairs of merge/compute executions; 60 computations exited 0; complete-graph root oracle agreed in 30/30 cases |
| Computed fields and refusal paths | Roots/rests_on checked; alternatives without provenance_graphs refused with exit 1; missing candidates and injected root_origins refused with exit 2; empty ledger produced NOT_COMPUTED |
| Batch tool and batch lifecycle | Delivered-byte kit exercised partition, coverage, seal and join, ownership, cross-batch references, both limb chains and their negative controls |
| C5 interpreter, SymPy and site tree | All five rendered commands ran live with exit 0; versions, interpreter digest and full-tree manifest printed |
| Manifest tool | Live full-directory hash; kit checked absent/symlink roots, symlink entries and associated refusal controls |
| Timeout wrapper | Fast child exit 0; failing child exit 7 preserved; shorter-cap control emitted SYMBOLIC_TIMEOUT and exit 124 |
| Sandbox | Digest matched; live source-read positive and lane-master negative; SymPy import positive from permitted cwd |
| Interpretation protocol | Existence and size only; contents withheld |
| SPI exhibition and old tool copies | Read assertions; direct runs against delivered tools and pinned V34, V33 and V32 copies; results below |
| Full kit | Unchanged runner in an isolated copy with its delivered fixture dependencies: 186/186 controls, 63 deletion probes, exit 0 |

The source pin file is beside the master; the future dispatch copies it into the seat working directory. No existing completed dispatch directory is presumed. The kit's historical integrated-candidate file was consumed only as its builder-test dependency, not as governing design or a source of verdicts.

**Q7(a): executable tokens for THIS text.**

```text
PACKET_REBUILD=C4_PACKET_REDACTED=PASS
PACKET_BYTE_IDENTICAL=True
PACKET_FORBIDDEN_INJECTION=REFUSED
PACKET_REQUIRED_CONTENT_DELETION=REFUSED
SEAT_PINS=8/8
KIT_PINS=17/17
CORPUS_ENUMERABLE_VERIFIED=89/89
CORPUS_RAW_VERIFIED=79/79
PARTITION_BYTE_IDENTICAL=True
KIT_EXIT=0
KIT_CONTROLS=186/186
KIT_DELETION_PROBES=63
STAGED_TESTS=PASS
SPI_LIVE=PASS, exit 0, 13 constructions
SPI_V34=FAIL, exit 1, exactly two mixed-cycle rows
SPI_V34_CYCLE_AUDIT=[1,1]
SPI_V34_CYCLE_COMPUTE=[1,1]
SPI_V33=FAIL, exit 1, exactly member-order plus two mixed-cycle rows
SPI_V32=FAIL, exit 1, same-origin-search auditor-B verdict [1,0]
OWN_CONSTRUCTIONS=30
OWN_VALIDATED_SEAT_LEDGERS=60/60
OWN_MERGED_BYTES_EQUAL=30/30
OWN_COMPUTE_EXITS_ZERO=60/60
OWN_COMPLETE_GRAPH_ROOT_ORACLE=30/30
OWN_WHOLE_AUDIT_OUTCOMES_EQUAL=28/30
F1_MATCHED_CHOSEN_CYCLE_AUDIT=[0,0], expected [1,1]
F2_MATCHED_DERIVED_CYCLE_AUDIT=[1,1], unequal diagnostics/artefacts
F2_IDENTICAL_COMMAND_REPEATS=12, two distinct cycle diagnostics
Q1_CHOSEN_FITTED_IMPORTED_CLASSIFICATIONS=AS_SPECIFIED
Q2_WRONG_PLAUSIBLE_ORIGIN_VALIDATE=[0,0]
Q2_CORRECTED_AUDITOR_VS_WRONG_SEAL=[1,1]
Q3_DIRECTIONAL_HYPOTHESIS_RECONSTRUCTED=NO
Q5_PROCEDURAL_CONSEQUENCE_VISIBLE=YES
Q6_DOWNSTREAM_ANALYTICAL_PURPOSE_RECONSTRUCTED=NO
C5_COMMAND_EXITS=[0,0,0,0,0]
PYTHON_VERSION=3.9.6
SYMPY_VERSION=1.14.0
INTERPRETER_SHA256=b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
USER_SITE=/Users/duhokim/Library/Python/3.9/lib/python/site-packages
FILES=20637
MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
WRAPPER_EXITS=[0,7,124]
```

Own constructions include chosen, fitted, imported, measured and undeclared roots; eight deterministic pairs of varied acyclic graphs with each complete auditor branch; complete mixed-cycle alternatives; cyclic matched combinations; and semantic origin correction. Both input graphs were validated before each pair of executions. Whole-outcome comparison retained compute JSON, compute stdout/exit, audit exit, ordered failure lines and the entire audit JSON including sealed_ledger_sha256. Thirty successful computations are evidence over those constructions, not proof of an unrestricted universal property.

**Q7(b): C5_EXECUTABLE_UNDER_SCOPE=YES; NO_MASKED_STAGE=YES.** C4 expressly permits the five mandated commands and their loads, and bounds seat-chosen paths separately. C5's rendered Python code spans were executed as one-line commands. Each command's own exit status was captured before display filtering. There is no mandated shell pipeline. The manifest is one process, and the wrapper preserved failure and timeout statuses. These tokens describe invocation/scope behaviour, not the correctness of every internal audit predicate.

**Q7(c): PRIOR_FINDINGS_CLOSED=YES over the V34 findings.**

- **V34 codex F1 — CLOSED.** The two complete graphs are preserved and canonically ordered; equal graphs are retained once. Computation on every tested merged output succeeded and matched the complete-graph oracle, including equal classifications retained as DISPUTED pairs. The deterministic exhibition's tag was 0. Complete A and complete B auditors passed on V35 in both orders; pinned V34 tools failed precisely those two rows with audit [1,1] and compute [1,1]. Diagnostic mixed-primary cycles no longer add a mismatch. New F1 above concerns a cycle in the matched graph, not the retired diagnostic-view false failure.
- **V34 codex C1 — CLOSED.** The exhibition docstring now names seal-enumeration, select, seal-rederivation and compare, retains sealed_ledger_sha256 and ordered failure lines, and states the expected-verdict assertion. Its added V35 sentence describes compute exit/stdout participation. No cosmetic repair remains requested for that finding.
- **V34 kimi — no findings.** Nothing to close.

The SPI kit controls assert the disclosed expectations: delivered exhibition exit 0/PASS; V34 exit 1/FAIL with exactly the two cycle rows and audit/compute [1,1]; V33 exit 1/FAIL with exactly the member-order row plus two cycle rows; V32 exit 1/FAIL with the [1,0] verdict flip. All four controls passed, and direct exhibition runs independently reproduced their outputs.

Both disclosed conflict resolutions are visible in the delivered test code. The disputed-ledger fixture is now built by merge from two seat ledgers before the existing label, inherited-dispute and propagation-probe assertions. Those assertions remain present and pass; no weakened expectation was observed. The V33 fail-first control explicitly requires three BAD rows with the named composition, preserving the member-order failure while including the two older graph defects. The current lane tool has one dispute-reachability resolver and one call adding disputed claims; the removed redundant predicate is not present. This confirms the delivered state, without adopting section 10's historical narrative as independently established edit history.

**Q7(d): NEW executable defects are F1 and F2 above.** The kit passing does not close them: its cycle rows use complete acyclic auditors, so they test the repaired diagnostic-view failure. They do not test a cyclic matched composition passing through a CHOSEN record, or repeat a cyclic failing auditor to expose process-dependent diagnostics.

Reproducibility evidence is in `/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v35_codex_xr3qinpu/`: `own_checks.py`, `supplement.py`, `own/commands.json`, `own/supplement_commands.json`, both result files, `own/F1_cycle/`, `own/F2_cycle/`, `nondeterminism.json`, `causal_probe.json`, `harness.json`, `kit.log` and the four `spi_*.log` files. The master and packet hashes were rechecked unchanged. Delivered tools were not repaired or re-pinned by this review. This report proposes repairs; it adopts, signs and starts nothing.

R3C2_V35_GATE_COMPLETE
