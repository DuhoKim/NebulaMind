ACCESS_SHA=b2e2073c49936328a533c5926465354ea01db0a63aaec7cca3626acc07e39872
PACKET_SHA=a3314cb5605a647bf0401269145b72078b1246baac3aa7326e7f3cea6033f0d8
GATE=PREREG_SOUND
LEAK=content-level only: the corpus is cosmology on the Planck 2018 baseline ("The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.", C3) across "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6); failures are contemplated and pre-worded non-accusatorially ("unreproduced from the stated inputs," not "error", §3); the set-versus-derived count is a wanted printed statistic ("`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator", §3); the printed-but-chosen case is anticipated and armored ("Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`", §3); provenance visibly matters (seven origins, machine-matched quotations, two readers, disputes carried never reconciled — the ruled floor); a stake exists and is fenced ("No tier, warrant token, standing or stamp moves.", §11) — no hypothesis, comparison model, preferred outcome or "pattern" reconstructible
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=a cross-reader and audit integrity check, never an arithmetic input: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled — "An input on which the two classifications disagree is filed `ORIGIN_DISPUTED` and reported with both seats' classification and both quotations; it is **not** reconciled. Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`."; "the auditor re-classifies the record from the pinned sources" (C3); while "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1) — no analytic use (no tally, comparison, weighting or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 35, living draft, unsigned — the header says Version 35; the brief's "Version 32" description is stale). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine. Order kept: the master was hashed first (ACCESS_SHA, line 1); the packet was then hashed and read, and Q5, Q6, Q3 answered from it alone, in that order, before the master was read; the master was then read in full (the two over-long lines 117 and 316 recovered in full by shell text extraction after the file reader truncated them); the two named V34 gate reports were opened only after the master, as the brief permits. Files opened: the packet; the master; R3C2_GATE_V34_codex_20260907.md; R3C2_GATE_V34_kimi_20260907.md; R3C2_SEAT_PACKET.sha256 and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256; the delivered tools r3c2_ledger_tools.py and r3c2_lane_tools.py (read and executed); r3c2_batch_tools.py (partition executed); r3c2_build_seat_packet.py, r3c2_manifest.py, r3c2_timeout.py (executed); r3c2_seat_sandbox.sb (digest only); the kit r3c2_staged_d1d7/ (README_STAGED_UNADOPTED.md, r3c2_spi_exhibition.py and r3c2_staged_tests.py read; the kit and the exhibition executed against the delivered tools and the _v34/_v33/_v32 bytecopies); R3C2_CORPUS_MANIFEST.md (metadata only: header, table formats, row digests and byte counts — never content); the corpus files (hashed and byte-counted only, never content); R3C2_INTERPRETATION_PROTOCOL_20260904.md (metadata only: exists, 3,947 bytes; never opened). No other file; no other version's gate file was sought or opened. Every fixture below is synthetic referee evidence under /tmp/r3c2_v35_kimi with my own sources, ids and values; no corpus content; not a census. Nothing here adopts, signs or runs anything.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalizes every outcome: "No tier, warrant token, standing or stamp moves." (§11) — it denies consequence transfer on the named axes for every outcome and ranks nothing; that the lane needed to say it tells me a standing system exists that outcomes could in principle bear on, but not which outcome would move it or how. The remaining consequence-adjacent sentences are procedural and attach to stop states, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2 — a filing precedence, not a weight), "do not adopt the passing seat's result" (§4 item 8). CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Yes for the checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled, and above 10% of included claims the census stops ("`CENSUS_ORIGIN_DISPUTED`", §4 item 7); the auditor re-classifies ("the auditor re-classifies the record from the pinned sources", C3) and compares "per re-classified input origin" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What cannot be stated from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), partitioned into 12 batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "**Wording: \"unreproduced from the stated inputs,\" not \"error.\"**" (§3); a reporting rule, not an expectation of any rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, an adequacy rule on `origin_search`, audit re-classification — but nothing says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' printed numbers follow from their own stated inputs, and it cares — with unusual care — how much of the corpus rests on chosen/fitted/imported/undeclared inputs, with failures expected to be reputationally delicate (hence the not-"error" wording and the no-movement clause). What I cannot reconstruct: any hypothesis with a direction (that the lane hopes most claims reproduce, or hopes they fail), any comparison model, any preferred outcome, any predicted distribution or "pattern". The packet's stops and pre-wordings are symmetric between reproduction success and failure. This is the hypothesis-reachability test passed at the ruled floor: the stake's existence is reachable; its direction is not.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2; and even taken as co-occurring, the order decides). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable or non-enumerable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name ("A denominator of zero files `CENSUS_PARTIAL` with the empty enumeration named; no census is complete over nothing."); a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration (a zero denominator with no passage on either side can pass the audit; "A sealed denominator of zero with any passage on either side fails", C6).

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on (my own fixtures, delivered tools): (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`"), the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; asymmetric = the half-width on the side the reproduced value falls), rests_on USES_CHOSEN — executed in my ORIGDIS family: both seat ledgers validate exit 0, merge exits 0 in both orders with equal merged bytes, compute derives the pair from the two complete graphs and emits rests_on=['USES_UNDECLARED','USES_CHOSEN'] marked DISPUTED; (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (same mechanics; the kit's CHOSEN-vs-FITTED complete-branch construction exercises it in both orders); (c) an imported value — the local-import branch (source_file equal to the claiming file) and the external-import branch (verified manifest row, numeric token, first symbol-and-value line) are both enforced by validate (my Q2 RIGHT_LOCAL and RIGHT_EXTERNAL records validate exit 0), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT. The V34 boundary case — a printed input whose two seats' provenance graphs are each valid yet whose per-input mix composes a cycle — now also traces to exactly one outcome: compute exits 0 with the equal classification pair retained as DISPUTED from the two complete graphs (my CYCLE family), and the mixed view is never classified (refused when presented without graphs, exit 1). Decidability is preserved: the 120-second cap files SYMBOLIC_TIMEOUT/REPRO_NOT_EVALUABLE and missing machinery MACHINERY_UNAVAILABLE, so no claim requires solving arbitrary mathematics.

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed and printed (the kit and my drivers print both blocks and the author_specified_input line); C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate ≤ 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. Executed with my own fixtures (/tmp/r3c2_v35_kimi/d2, own sources myA.txt/myB.txt): (1) THE WRONG-BUT-PLAUSIBLE RECORD: value "2" printed at myA.txt:3 in the sentence "We adopt a = 2 from myB (2020)." — filed CHOSEN/ORIG_CHOICE_STATED with that true quotation at its true line — validate returned exit 0: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (2) THE MECHANISMS THAT CATCH IT, AS WRITTEN: (i) the second seat's independent classification — filing IMPORTED/ORIG_CITATION (local branch, same coordinates) merged exit 0 with origin=CHOSEN origin_alt=IMPORTED preserved — the dispute is carried, never silently resolved, and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; filing IMPORTED with the external value line instead made merge exit 1 (a value/status/coordinate disagreement, FIELD_DISAGREEMENTS, no merged file written) — the disagreement surfaces in the open before the tally; (ii) the C6 re-classification — against a CHOSEN-only sealed ledger, my IMPORTED auditor record produced compare exit 1, C6_AUDIT_SAMPLE=FAIL, first diagnostic "record matches neither complete declared branch" (the roots are recomputed under the matched branch only); when the second seat's IMPORTED branch was preserved, the same auditor record PASSED (exit 0). (3) THE STATED FLOOR: when my auditor repeated the same wrong code, compare returned exit 0, C6_AUDIT_SAMPLE=PASS — "if every reader misclassifies identically, the record stands — that floor is stated here rather than implied away" (C3 lane-side note). The machine floor that IS enforced is real and was exercised: quotation machine-matched at the cited line for every non-ORIG_SILENT record of every status; value as a numeric token at its own line; external import bound to a verified manifest row at the first symbol-and-value line; STANDARD closed-list string comparison; BLOCKED bound to the claiming file with an empty value; computed fields (root_origins, rests_on) REJECTED at compute (exit 2, executed) and un-writable by a seat (schema fail).

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no pattern content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below: rebuilt byte-identical, C4_PACKET_REDACTED=PASS), and my packet-first Q3 found no hypothesis. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind (census fails a row that lacks or alters those fields — kit-asserted); both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any sealed-side omission (included or excluded, either direction) files CENSUS_AUDIT_FAILED — the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt (regenerated deterministically in my kit run) emits exactly these cases. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script from the complete graphs merge preserved — compute REJECTS a ledger arriving with it set (executed, exit 2) and REFUSES a mixed PRIMARY/ALT view presented without the complete graphs (executed, exit 1, no file written). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — each stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on this master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 b2e2073c…" (= ACCESS_SHA, character for character) and "seat packet sha256 a3314cb5…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (both a3314cb5605a647bf0401269145b72078b1246baac3aa7326e7f3cea6033f0d8), and the packet's line 9 built-from digest equals ACCESS_SHA. The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone; procedural consequences of stop outcomes remain visible while hypothesis mappings, comparison-model preferences and empirical stakes are removed"; the seat runs from a redacted copy outside the lane under the kernel sandbox profile r3c2_seat_sandbox.sb (digest 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 verified on disk, equal to the C4 pin) whose permitted surface the text prints; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited. My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4's definition names the seal-receipt case), leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (kit control and its deletion probe both ok in my run). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened). This is a judgement of the binding order in the design; no custody has been executed and none is certified here.

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary is audit-internal — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs emitted it for the undisputed measured-root claims and inside the equal pair marked DISPUTED for the disputed ones, never as a clean label over a disputed graph; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED, whose definition names those cases; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — kit-asserted).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first (ACCESS_SHA, line 1) and read in full; the packet hashed (PACKET_SHA, line 2) and read first; R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 17/17 OK from the lane directory, both re-verified exit 0 after all runs (the kit's deterministic rewrite of C6_COUNTEREXAMPLE_EXHIBIT.txt reproduced the pinned bytes); the packet's line 9 built-from digest equals ACCESS_SHA. Every named script exists beside the document and matches the digest the document prints for it: r3c2_ledger_tools.py 0c11e019b0a438dee6353e3924000b14cd0ff4cf77b3a3cc797c34f30a8a8041 (§3), r3c2_lane_tools.py 20796ac462bca3daeda1839505f638fcbb803b4da3165b28e0dd0e3dd609f156 (C3, §9), r3c2_batch_tools.py 3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e (C1), r3c2_manifest.py 6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a (C5), r3c2_timeout.py fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 (§9), r3c2_build_seat_packet.py 56ac7d1c78196c4d246244126b43666d804f093f8fbe4e2c2289ab885def728e (C4), r3c2_seat_sandbox.sb 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 (C4), R3C2_CORPUS_MANIFEST.md 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 (§2); the kit's _STAGED copies are byte-identical to the delivered tools. C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text's executability). r3c2_timeout.py: a fast child exits 0 with WRAPPER_EXIT=0 printed; a failing child's exit 7 is carried as WRAPPER_EXIT=7; a 5-second child under a 0.3-second cap printed SYMBOLIC_TIMEOUT with WRAPPER_EXIT=124 at 0.304 s. The builder rebuilt the packet byte-identically with C4_PACKET_REDACTED=PASS. r3c2_batch_tools.py partition regenerated partition_12_of_89.json byte-identically (39b8d9bf199265ab…). The corpus rows verify 89/89 enumerable and 79/79 RAW by digest and byte count at the declared ../bhu-reading-20260823/sources/ (metadata only; no content read). r3c2_ledger_tools.py: census, census final, validate and all five audit subcommands executed; ten of my own seat ledgers validated exit 0. r3c2_lane_tools.py: merge in both orders on three own constructions; compute on every merged output; every refusal and rejection path exercised. The interpretation protocol exists (3,947 bytes; metadata only). The sandbox profile exists with the pinned digest (not live-probed; no dispatch occurred). No described artefact failed to execute as delivered; future dispatch records, receipts, ownership extracts and seat outputs are future prerequisites, not claimed existing artefacts.

## Q7 (V35) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=17/17 OK (before and after the kit run)
  TOOL_PINS_MATCH_MASTER=YES (ledger 0c11e019…, lane 20796ac4…, batch 3822f12c…, manifest 6b6e4bce…, timeout fbb9bef7…, builder 56ac7d1c…, sandbox 6978d590…, corpus 26dabe7c…)
  STAGED_EQ_DELIVERED=5/5 byte-identical
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only)
  KIT_RUN: deletion_probes=63 / controls=186 passed=186 failed=0 / STAGED_TESTS=PASS (exit 0)
  SPI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; all 13 constructions ok; the two mixed-cycle rows verdict(AB,BA)=[0, 0], compute=[0, 0], DISPUTED pair printed; mixed-cycle fixture tag=0 printed)
  SPI_EXHIBITION_V34_BYTECOPY=SPI=FAIL (exit 1; exactly TWO BAD rows — the two mixed-cycle rows, each verdict(AB,BA)=[1, 1] and compute=[1, 1] in both orders, codex's exact V34 observation)
  SPI_EXHIBITION_V33_BYTECOPY=SPI=FAIL (exit 1; exactly THREE BAD rows — the member-order row with merged-bytes-equal=False and unchanged verdicts [0, 0], plus the two mixed-cycle rows [1, 1]/compute [1, 1])
  SPI_EXHIBITION_V32_BYTECOPY=SPI=FAIL (exit 1; the codex V32 construction flips exactly verdict(AB,BA)=[1, 0]; byte leaks across constructions)
  OWN_CONSTRUCTIONS=3 + 1 negative (mixed-cycle codex class, my own sources and tag; structurally equal records member-scrambled at every level; origin disagreement + parent difference + ORIG_SILENT search + record/member-order scramble; the matched-cyclic auditor negative)
  OWN_VALIDATED_SEAT_LEDGERS=10/10 exit 0
  OWN_MERGED_BYTES_EQUAL=3/3 in both orders
  OWN_COMPUTE_ON_MERGE=3/3 exit 0 with the §3 classification pair (equal classifications retained as a pair marked DISPUTED; origin pair ['USES_UNDECLARED','USES_CHOSEN']; pair order stable across seat orders)
  OWN_WHOLE_OUTCOME_EQUAL=YES for every construction (audit exit code, ordered FAIL lines, the complete C6_AUDIT.json including sealed_ledger_sha256, and compute's exit code and stdout)
  OWN_AUDIT_COMPLETE_A_AND_B=[0,0] in both orders for both auditors (cycle family and origin family)
  OWN_MATCHED_CYCLIC_AUDITOR=[1,1] in both orders with "cycle at p.txt#x" — the MATCHED graph still decides
  DIAGNOSTIC_VIEW_NEVER_CHANGES_VERDICT=YES (artefact records sealed_primary=None with sealed_primary_diagnostic_error='cycle at p.txt#x' while C6_AUDIT_SAMPLE=PASS)
  COMPUTE_REFUSALS=[alternatives-without-graphs exit 1 with message and NO file; three graphs exit 1; a graph not covering the input set exit 1]
  COMPUTE_REJECTS=[root_origins preset exit 2; missing candidates exit 2 with NO file]
  NO_ALT_LEDGER_IS_ONE_COMPLETE_GRAPH=exit 0
  Q2_MECHANISM: WRONG_CHOSEN_ON_CITATION validate exit 0 / second seat IMPORTED(local) merge exit 0 origin_alt preserved / second seat IMPORTED(external) merge exit 1 FIELD_DISAGREEMENTS no file / auditor IMPORTED vs CHOSEN-only sealed exit 1 C6_AUDIT_SAMPLE=FAIL / vs preserved alt exit 0 PASS / auditor repeats CHOSEN exit 0 PASS (the stated floor)
  C5_LIVE=[0,0,0,0,0] Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1…
  WRAPPER=[0,7,124] (SYMBOLIC_TIMEOUT at 0.304 s under a 0.3 s cap)
  BUILDER_REBUILD=C4_PACKET_REDACTED=PASS, rebuilt packet byte-identical to the pinned packet (51 spans; sections kept/dropped as stated)
  PARTITION_REGENERATED=byte-identical (39b8d9bf…)
  ONE_RESOLVER=YES (grep-level sweep: disputed_reach, PROBE:DISPUTE_PROPAGATES, is the only dispute predicate in the delivered lane tool; the kit's propagation probe kills on its deletion)
  NO_SECOND_SEAT_ORDER_READ=YES (PROBE:SPI_CANONICAL is the only seat-order-sensitive read; provenance graphs are ordered by the seat-blind graph_key)
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline (the only pipe characters in the document's printed spans are the STANDARD table's markdown cell separators); the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the kit's 63 deletion probes each turn a negative into PASS only when its check is neutralised (or show the diagnostic absent while an overlapping guard still fails), so no check is decorative; every exit code cited in this report was captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES over the V34 gate's findings (codex F1, C1; kimi none), each with my own evidence:

  - codex F1 (the per-input canonical choice could COMPOSE a primary graph no seat supplied — two acyclic seat graphs A x→y→r, B y→x→r merging to the cyclic view x↔y; compute exited 1 instead of reporting the DISPUTED pair; audit compare's diagnostic root computation over that mixed view added a mismatch even when the auditor equalled complete A or complete B) — CLOSED. Text: §3 carries codex's two exact replacement sentences — the provenance_graphs sentence ("In addition to its canonical per-input alternatives, `merge` preserves the two complete validated provenance graphs in `provenance_graphs`, ordered by the SHA256 of canonical JSON over their records sorted by `input_id`; equal graphs are retained once. `compute` obtains the claim's reported classification pair from those complete graphs, retaining equal classifications as a pair when the claim is disputed. It never treats an independently mixed PRIMARY or ALT graph as a graph supplied by a seat. …") and the diagnostic-view sentence ("The comparison graph used to decide the audit is constructed only from complete matched input branches. A cycle or missing dependency in that matched graph fails the audit. Unmatched PRIMARY or ALT graphs are diagnostic views only: if their roots cannot be computed, the report records that fact without adding a mismatch or changing the audit verdict. …"). Code, verified by reading: merge preserves the two complete seat graphs stripped of alternative fields, ordered by the seat-blind graph_key, equal graphs retained once (lane tool lines 139–146); compute obtains classifications only from those graphs, REFUSES a ledger carrying alternatives but no graphs (exit 1 with the named message, no file written — executed), accepts an alternative-free ledger as itself one complete graph (executed), refuses more-than-two and non-covering graphs (executed); audit compare's sealed_primary computation is wrapped in its own try/except and recorded with its diagnostic error without joining the failure reasons, while the matched graph's roots still decide (ledger tool lines 444–453 — executed). Execution, my own construction of the same class (tag 0; canonical mix x-primary=A, y-primary=B; zero origin-label disagreements; both seat ledgers validate exit 0; each original graph computes exit 0 with MEASURED roots): merged bytes equal in both seat orders; compute exit [0,0] with rests_on=['DERIVED_STANDARD_OR_MEASURED_ONLY','DERIVED_STANDARD_OR_MEASURED_ONLY'] marked DISPUTED — the promised pair; audit exit [0,0] with the auditor equal to complete A and to complete B in both orders, the whole outcome equal across orders; the matched-cyclic auditor (A's x with B's y) FAILS [1,1] with "cycle at p.txt#x" in both orders — a cycle in the MATCHED graph still fails while the unmatched view is diagnostic only (sealed_primary=None, diagnostic error recorded, verdict PASS). The lane exhibition's two cycle rows assert exactly this and PASS on the delivered tools; the fail-first is reproduced, not narrated: the V34 byte copies FAIL on the two cycle rows only with audit [1,1] and compute [1,1] exactly as codex observed; the V33 copies FAIL on the member-order row plus the same two cycle rows; the V32 copies FAIL with the [1,0] verdict flip on codex's construction.
  - codex C1 (the exhibition docstring described the old comparison) — CLOSED. The exhibition docstring now carries codex's exact replacement sentence verbatim ("For each reviewer construction, merge both seat orders and require identical merged bytes; run seal-enumeration, select, seal-rederivation and compare with one fixed auditor reconstruction; require identical exit codes, failure lines in emitted order and complete C6_AUDIT.json artefacts including sealed_ledger_sha256; also assert the construction's expected verdict and required diagnostic.") plus the V35 sentence joining lane compute's exit code and stdout to every construction's compared outcome (asserted in the runner; the compute=[…] column observed on every row of all four of my exhibition runs).
  - kimi V34: no findings — nothing to close.
  The kit's SPI controls assert what §10.30 says: the delivered-tools control requires the exhibition's exit 0 and SPI=PASS over the staged tools (whose bytes equal the delivered tools); the V35 fail-first control requires exit 1, SPI=FAIL, exactly two BAD rows each carrying verdict(AB,BA)=[1, 1] and compute=[1, 1]; the V33 fail-first control requires exit 1, SPI=FAIL, exactly three BAD rows — one naming the member-order construction and two the cycle rows, no other row; the V32 fail-first control requires exit 1 and the exact flip string "verdict(AB,BA)=[1, 0]". All four ran ok inside my kit run (186/186, 63 probes, STAGED_TESTS=PASS), and I reproduced all four exhibition runs independently with the tokens above. The two disclosed conflict resolutions check out: (i) the kit's disputed-ledger fixture (the V27-label and gate-F4 controls) is now PRODUCED BY THE DELIVERED merge from two seat ledgers (staged tests lines 87–94) with the assertions unchanged — the label control, the cross-claim propagation control and its deletion probe all ok in my kit run; (ii) the V34 fail-first control's "only" clause now names the exact failure set at V33 (member-order plus the two cycle rows, nothing else) and ran ok. The disclosed removal also checks out: the delivered lane tool carries exactly one dispute resolver (disputed_reach), and no second graph-difference predicate exists anywhere in compute (grep-level sweep; the propagation probe still kills on deletion of the one resolver).

(d) NEW executable defects, with counterexample: NONE. Every construction behaved as the V35 text claims. Tested beyond the required set: all three refusal paths and both rejection paths of compute; the no-alternative ledger path; the matched-cyclic auditor in both orders; structurally equal records member-scrambled at every level (merged bytes equal, one graph retained); an origin-label disagreement with an ORIG_SILENT branch, a parent difference and record/member-order scrambling (merged bytes equal, DISPUTED pair stable across orders, audits PASS with the auditor equal to each complete graph); the lane exhibition's full suite and all three fail-first generations; the kit end to end. Observations, NOT findings: (i) `roots_alt` in the delivered lane tool is now an unused helper — it is called nowhere, computes nothing that reaches any output, and changes no class filed, no control's passability, no reachability, no conclusion and no tested value; not even a COSMETIC repair request. (ii) compute's refusal of alternatives-without-graphs exits 1 (FAIL) while its preset-field rejection exits 2 (REJECT); the master says "REFUSED" without assigning a code, both are non-zero with no output file written, and the split is consistent with §3's exit-1-FAIL / exit-2-REJECT convention — no defect.

Why SOUND and not SOUND_WITH_REPAIRS: no defect at any level, cosmetic included, remains open against the operative text; both V34 findings are closed above with my own fixtures; and the class the V34 finding instanced is stated once (§3's complete-graphs and diagnostic-view sentences), enforced at the one place seat order could enter (merge's canonical key, with the graphs ordered seat-blind), and exhibited over every reviewer construction and my own four in both seat orders, with fail-first reproduced against three pinned generations of byte copies. Why not UNSOUND: Q1 and Q2 answer YES; the blind is machine-asserted and reproduced byte-for-byte; the seal binds each artefact before the step it must precede; every negative outcome is worded within its definition; and nothing the document describes failed to execute as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master b2e2073c… = ACCESS_SHA; packet a3314cb5… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v35_kimi, not corpus data and not a census.

R3C2_V35_GATE_COMPLETE
