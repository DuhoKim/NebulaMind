ACCESS_SHA=34c90d0b4062f3443ff099d7af33326bc2b79080f28901618c9fe3f46a0c171c
PACKET_SHA=3def6c39ed09e87209d98e0fb629226c3077fe12ab88b8c81f8552e945f80272
GATE=PREREG_UNSOUND
LEAK=Provenance is a methodological focus; no directional hypothesis, comparison preference or hoped-for result reconstructed.
CONSEQUENCE_VISIBLE=Procedural: origin disagreement above 10% stops the census — “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent comparison and root-origin auditing — “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” The substantive interpretation remains undisclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Design-only review of the delivered unsigned Version 36, identified by the hashes above. Both V35 findings are closed; three new executable findings remain. Section 10 supplies no inherited verdict. The requested access command ran first, and packet-only answers were recorded before the master was read in full. Q7's later executable instructions were treated as expanding access to the required tools, pins, brief, corpus metadata/bytes, kit dependencies and specifically permitted V35 reports. No other gate report or interpretation-protocol contents were opened. All computations below are synthetic referee checks, not corpus derivations, census execution, dispatch, adoption or signature.

## 1. OUTCOME CLASSES

Packet-only answers, preserved in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Yes, procedurally. “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” Origin disagreement above 10% stops continuation. No sentence identifies a preferred substantive finding or a consequence for standing; “No tier, warrant token, standing or stamp moves.”

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

Partly: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” Origin is compared between readers and audited; disagreements can stop the study. The packet does not disclose a downstream substantive interpretation of those roots.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

“The reproduction verdict and the provenance fields are recorded separately” reveals methodological interest in provenance beyond arithmetic. Transitive auditing reinforces that inference. The Planck table identifies cosmological subject matter and reference values. None establishes a preferred origin, comparison model, expected pattern or hoped-for result. All origins remain consumable according to status. I could not reconstruct the substantive stake.

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES at the definitional, bounded-filing level. The executable integrity defects below do not require reopening option (c). Concrete physical-claim witnesses are synthetic examples, not attributed findings from corpus papers:

| Claim and inputs | Unique outcome | `rests_on` |
|---|---|---|
| “We choose a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe and input, L printed as 5 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 2 m from X; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a found externally at the first qualifying line of verified enumerable X | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| a unprinted and cited to non-enumerable X | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, with a unprinted and uncited | REPRO_INPUT_ABSENT | USES_UNDECLARED with a properly documented silent search |
| Specified computation exceeds the cap | REPRO_NOT_EVALUABLE | Computed independently from its records |
| Own numerical result, no producing procedure or input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

A fitted parameter asserted as an own result is itself a candidate; its printed value remains consumable downstream. An author-set input numeral is excluded as AUTHOR_SPECIFIED_INPUT while the downstream result remains included. The delivered compute command emitted USES_CHOSEN, USES_FITTED, USES_IMPORTED and NOT_COMPUTED on my fixtures. Final census validation accepted all six per-claim tokens.

Hard overlap: L = a + b, with a unprinted but cited to an unpinned source and b unprinted and uncited, satisfies two terminal predicates. Precedence files REPRO_BLOCKED only. No remaining double filing or gap was exhibited after applying precedence. “Decidable” here means a bounded reproduction classification, including inability to evaluate; it does not promise to decide arbitrary mathematics.

All eight study classes have witnesses: a nonempty all-arithmetic tally with audit PASS gives COMPLETE; a matching absent-input claim with audit PASS gives PARTIAL; missing audit seed gives AUDIT_FAILED; C3 failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives ORIGIN_DISPUTED; a surviving fail/pass control split gives CONTROL_SPLIT.

A partial tally with a missing seed satisfies two conditions but files AUDIT_FAILED by precedence. INCONCLUSIVE is genuinely reachable as PARTIAL with successful audit. Empty enumerations on both sides can also reach PARTIAL; a zero included denominator with candidate passages triggers the stated audit-failure predicate. These witnesses do not substitute for the independent two-seat C0 prerequisite.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

YES as evidence-backed human classification; NO as automatic semantic verification. My wrong-but-plausible record was:

```json
{
  "claim_id": "p.txt#c",
  "input_id": "p.txt#x",
  "symbol": "x",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "p.txt",
    "source_line": 5,
    "verbatim": "We adopt x = 2 from Reference."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "p.txt",
  "source_line": 5
}
```

That exact sentence occurs at the synthetic source's line 5. `validate` returned 0 and C3_NO_SUBSTITUTION=PASS: quotation matching cannot enforce citation-first semantic precedence. A correct second reader supplies IMPORTED and exposes disagreement. My IMPORTED auditor against the CHOSEN-only sealed record returned 1, including “record matches neither complete declared branch.” Shared misclassification can survive, as explicitly acknowledged. This limitation is not a new defect.

The named three-valued controls are C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Required artefacts include exhibitions, live command output, seals, audit JSON, path lists and session-identified provider logs. A bare asserted PASS cannot satisfy a printed-run predicate. C4/C5b can pass on an incomplete self-reported list, and explicitly say so. Unreached checks are NOT_RUN, including batch-session validation before integration.

**F1 — NON-COSMETIC: dependency-list order violates MOI.**

Verbatim §3 sentence:

> **MEANINGLESS-ORDER INVARIANCE ("MOI", formerly SPI): the audit verdict and the whole audit outcome — verdict, the full ordered finding set with each finding's code and reason, the merged ledger bytes and the ledger digest — are invariant under every ordering that carries no meaning.**

Counterexample: three records belonging to p.txt#c, all PRINTED with value string `2`. x is DERIVED from measured y and z. Their evidence/value lines are respectively “x = 2 from y and z.”, “We measure y = 2.” and “We measure z = 2.” The candidate's agreed arithmetic outcome has printed/reproduced strings `4` at line 4. These are graph fixtures, not a census of this miniature text.

Supply both seats with x.derived_from = [p.txt#y, p.txt#z]. In the second construction reverse that list in both seats, changing nothing else. The dependency set is identical; merge's own parent-comparison predicate sorts these lists, confirming that their order has no meaning.

Both ledgers validate. Across 24 fresh runs — both dependency orders × both record-arrival orders × both seat orders × seeds 0,1,2 — every audit passes, but the two merged hashes are:

```text
3ff7ad63ba107618d930e5c0c492d8c787394657c983c080b911362f99bf7e87
abbfd27f8722d3defceed4af76f1dbc2de2d52dba969fd71aae2fa4f263f4d23
```

The audit artefacts differ in sealed_ledger_sha256; compute output bytes also differ. Canonical JSON sorts object keys, not dependency arrays. `canon_key`, `graph_key`, retained records and preserved graphs retain this irrelevant ordering. This changes expressly tested values, even though no verdict flips.

Exact replacement for the quoted sentence:

> **MEANINGLESS-ORDER INVARIANCE ("MOI", formerly SPI): the audit verdict and the whole audit outcome — verdict, the full ordered finding set with each finding's code and reason, the merged ledger bytes and the ledger digest — are invariant under every ordering that carries no meaning, including dependency-list order. Before computing canonical record keys or graph digests, selecting branches, deduplicating graphs or serialising merged output, `merge` normalises every `derived_from` list into sorted input-id order in both complete seat graphs and every retained primary or alternative record. This does not reorder a paper's computational operations: `derived_from` records dependency edges, not an execution sequence.**

Implement, re-pin, and add this construction to the combined property. A prose-only repair is insufficient.

**F2 — NON-COSMETIC: C3 validation and lane computation still accept non-DERIVED cycles.**

Verbatim C3 sentence:

> Every `DERIVED` record lists its `derived_from` ids; `validate` fails a `derived_from` id that names no record, a cycle, and a `DERIVED` record with no `derived_from`.

Counterexample: one PRINTED/CHOSEN record x with derived_from=[x], value `2`, and matching value/evidence coordinates at the sentence “We choose x = 2 using x.” The candidate binding is valid. Actual results:

```text
EXPLICIT_CHOSEN_SELF_CYCLE_VALIDATE_EXIT=0
C3_NO_SUBSTITUTION=PASS
EXPLICIT_CHOSEN_SELF_CYCLE_COMPUTE_EXIT=0
rests_on=USES_CHOSEN
```

Both functions rely on root traversal, which terminates at CHOSEN. The new origin-independent function is invoked by C6, not by validate or compute. Direct graph fixtures through FITTED, IMPORTED, MEASURED, STANDARD and UNDECLARED likewise passed those two functions; DERIVED cycles failed. The explicit CHOSEN witness alone establishes the defect without relying on those other fixtures' semantic labels.

C6 correctly rejects these cycles, and batch join also contains an origin-independent walk. Thus this is a false PASS of the promised C3 boundary, not a demonstrated end-to-end census escape or a reopening of V35 F1.

Exact replacement:

> Every `DERIVED` record lists its `derived_from` ids. Before root classification, `validate` checks every dependency edge of the complete input graph independently of origin and returns FAIL for any missing dependency or cycle; it also rejects a `DERIVED` record with no parents. Lane-side `compute` applies the same origin-independent integrity check to every complete provenance graph it classifies and fails before writing output if any graph is invalid. Stopping root traversal at a non-DERIVED origin never substitutes for either integrity check.

This also makes true C3's existing compute promise that it fails on missing dependencies and cycles. Implement and re-pin both boundaries; retain C6's independent check.

**F3 — NON-COSMETIC: per-subcase fail-first tests accept unrelated failures.**

Verbatim §3 sentence:

> Fail-first is PER SUBCASE, each its own kit test method, against pinned byte copies of the V35 tools (and of the V32–V34 tools for the earlier subcases).

The delivered kit's `moi` helper actually decides:

```python
rc,out=run(*args); got_pass=(rc==0 and "SPI=PASS" in out); ok=(got_pass==expect_pass)
```

For each negative subcase, expect_pass=False. Therefore a launch error, traceback, missing fixture or unrelated exhibition failure is counted as a successful fail-first test. Executing the unmodified extracted helper with its child runner returning `(2, "unrelated launch failure\n")` printed `ok  adversarial unrelated crash` and recorded True. It does not assert F1's false PASS, F2's cross-seed difference, or the missing diagnostic claimed for the other subcases.

My independent runs reproduced all four claimed V35 failures; the historical evidence is real. The defect is that the executable regression predicate cannot distinguish that evidence from an unrelated failure. The kit's 192/192 result does not close this gap.

Exact replacement:

> Fail-first is tested independently for every subcase against pinned predecessor bytes. Each test requires successful fixture setup, the exact expected exhibition exit code and completion token, exactly the selected construction rows, and that subcase's expected verdicts, ordered diagnostics and equality results. F1 requires a false audit PASS with the graph-integrity diagnostic absent; F2 requires audit FAIL with unequal complete outcomes across seeds 0 and 1; the missing-dependency and multi-diagnostic subcases require audit FAIL with the specified graph-integrity diagnostics absent. A launch error, traceback, missing artefact, empty construction selection or unrelated failure fails the kit test. Rows expected to pass on predecessor bytes remain explicitly labelled controls.

Implement structured per-subcase assertions and a launch-failure negative control; do not weaken the production guard to make a probe succeed.

## 3. CIRCULARITY

The inclusion rule contains no hypothesis or comparison preference. Independent enumerations, retained exclusion rows and the auditor's full-corpus enumeration constrain selective omission. An auditor-only passage is an omission even when the auditor excludes it. A contrary claim cannot legitimately be quietly discarded; shared omissions or reader errors remain possible.

The packet reveals that provenance matters operationally, but withholds its substantive ranking and interpretation mapping. Receipt P fixes interpretation before limb A. These mechanisms constrain expectation reaching evidence; they do not prove absence of prior beliefs. I found no additional directional leak or expectation-dependent exclusion rule.

## 4. THE BLIND

The delivered builder, using this master and the delivered brief with scratch output, returned C4_PACKET_REDACTED=PASS and reproduced the packet byte-for-byte: 40,267 bytes, 51 redacted spans, sections 1–6, 9 and 11 retained. Injecting `hypothesis` into a visible section failed with exit 1 and no packet; removing required C2_INPUT_LEDGER content did likewise.

This establishes the finite forbidden-list and required-content checks, not universal semantic blindness. The packet-first answers fall within the operative text's accepted provenance/procedural floor.

The sandbox profile matches its pin. Live probes permitted a synthetic in-scope source read and SymPy import, while reading the lane master failed with “Operation not permitted.” Runtime/configuration/network exceptions remain, as the text admits. C4/C5b are self-report controls; prior exposure and continuous immutability are not proven. No future dispatch inventory or custody record is certified here.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before protocol opening. Independent re-hashing must print both receipts and the four verified values. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

The ordering binds the records before the steps they must precede, conditional on executing the procedure. It does not prove the owner had not guessed the answer. The protocol exists at its named path, 3,947 bytes, checked by metadata only; its contents and actual receipts were not inspected. No design repair to §7 is required.

## 6. FAIRNESS

REPRO_FAILED expressly requires “unreproduced from the stated inputs,” never “error.” The delivered brief applies that wording to negative outcomes generally. Missing-input, blocked-source, missing-procedure and computational-limit classes do not assert a scientific error.

REPRO_WITHIN_STATED_PRECISION matches its rounding/uncertainty rule. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means complete arithmetic classification with audit PASS, including arithmetic failures; it does not mean every claim reproduced. No substantive label repair is required.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES: the universal MOI guarantee, unconditional C3 cycle-rejection promises and fail-first assurance fail as F1–F3 demonstrate. No currently promised operative deliverable was found missing. Future ownership extracts, session reports, ledgers, seals, receipts, provider logs and the dated check sheet are production prerequisites/outputs, not already-completed artefacts.

Checks performed:

- Packet and brief read; both pin sheets verified: 8/8 and 19/19 entries. Five staged production scripts are byte-identical to their delivered counterparts. Sandbox and corpus pins match the master.
- `r3c2_ledger_tools.py`: read validate, census, selection, handout, seal and comparison implementations; executed synthetic census/final, validate and every audit subcommand. Complete reconstructions, dependency closures, roots and branch diagnostics were inspected. All-origin cycle/missing-edge C6 probes failed as intended.
- `r3c2_lane_tools.py`: read merge/compute, canonical keys, preserved graphs, root classification and dispute propagation; executed the 24-run order construction and provenance/empty-ledger fixtures. F1 and F2 identify the remaining failures.
- `r3c2_batch_tools.py`: read partition/seal/join/coverage; regenerated the 12-batch partition byte-identically; the full kit exercised both limb chains, coverage, ownership, digest, graph and binding failures.
- `R3C2_CORPUS_MANIFEST.md`: verified all 89 enumerable and 79 RAW files by bytes and digest only, with no scientific interpretation. The partition paths and totals are delivered.
- `r3c2_build_seat_packet.py`: rebuilt this packet and ran forbidden-content/required-content negatives. The kit's historical integrated-candidate file was copied solely as its required build-test fixture, not used as the design under review.
- `r3c2_manifest.py`: ran C5's live directory manifest and a fresh-process reversed-filesystem-listing probe. `r3c2_timeout.py`: fast child returned 0, failing child returned 7, and a two-second sleep under a 0.2-second test cap returned 124 with SYMBOLIC_TIMEOUT at 0.205 seconds. This tests the deadline mechanism without running a derivation.
- `r3c2_spi_exhibition.py` and `r3c2_staged_tests.py`: read their construction/ordering/acceptance logic, ran the full delivered exhibition, full isolated kit and four separate V35 fail-first subcases. The first isolated kit launch lacked a copied fixture dependency; after supplying the delivered dependency, the unchanged kit completed. That setup error is not a deliverable finding.

The audit's graph-integrity, root and closure walks sort starts/dependencies; diagnostics use sorted closure IDs. Some comprehensions temporarily iterate sets, such as the starts passed into graph_integrity, but the callee sorts before diagnosis. File-ordered maps also remain in setup, while subsequent comparison/output boundaries sort them. I found no surviving hash-seed-dependent C6 diagnostic in these tests. This bounded inspection does not establish the universal invariant, which F1 disproves at merge.

**C1 — COSMETIC: the filesystem-source inventory is false.**

Verbatim §3 clause:

> (e) filesystem listing order — no delivered tool walks a directory, so this source has no instance today.

`r3c2_manifest.py` calls os.walk. It already sorts directories, filenames and final digest rows. Reversing supplied listings in fresh processes produced identical stdout and digest. This correction changes no class, passability, reachability, conclusion or tested value.

Exact replacement:

> (e) filesystem listing order — `r3c2_manifest.py` walks a directory and sorts subdirectory names, filenames and final digest rows; this source has a delivered instance whose ordering is already normalised.

Q7(a–b), executable tokens for THIS text:

```text
PACKET_REBUILD=BYTE_IDENTICAL
BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT
SEAT_PIN_SHEET=8/8_OK
KIT_PIN_SHEET=19/19_OK
STAGED_EQUALS_DELIVERED=5/5
CORPUS_BYTES_VERIFIED=89/89_ENUMERABLE_AND_79/79_RAW
PARTITION_REBUILD=BYTE_IDENTICAL
SIX_PER_CLAIM_TOKENS_FINAL_CENSUS=PASS
Q1_COMPUTE=USES_CHOSEN,USES_FITTED,USES_IMPORTED,NOT_COMPUTED
Q2_WRONG_ORIGIN_VALIDATE_EXIT=0
Q2_CORRECT_AUDITOR_AGAINST_WRONG_SEAL_EXIT=1
C5_LIVE_EXIT_CODES=0,0,0,0,0
PYTHON=3.9.6
SYMPY=1.14.0
INTERPRETER_SHA256=b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
C5_FILES=20637
C5_MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
WRAPPER_EXIT_CODES=0,7,124
STAGED_TESTS=PASS
KIT_CONTROLS=192/192
KIT_DELETION_PROBES=63
DELIVERED_EXHIBITION=SPI_PASS_19/19
EXHIBITION_RUNS_PER_CONSTRUCTION=8
OWN_PARENT_ORDER_RUNS=24
OWN_PARENT_ORDER_AUDIT_EXITS=ALL_0
OWN_PARENT_ORDER_WHOLE_OUTCOME_EQUAL=NO
OWN_C6_CYCLES_REJECTED=7/7_ORIGINS
OWN_C6_MISSING_DEPENDENCIES_REJECTED=7/7_ORIGINS
KIT_ACCEPTS_UNRELATED_LAUNCH_FAILURE=YES
FILESYSTEM_LISTING_ORDER_EQUAL=YES
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
```

C5's five literal commands ran in order. The printed user-site path was `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`; C4 admits that pinned environment and mandated command loads. No mandated operative command is a shell pipeline, and the manifest/wrapper failures propagate through their own exit statuses. These YES tokens concern executability and stage propagation, not certification of a future dispatch pin or the kit's separate F3 acceptance predicate.

The supplied exhibition executes both seat orders × three seeds, plus one reversed-arrival run and one reversed-rederivation-member-order run: eight runs per construction, as operative §3 says. It is not the full Cartesian product suggested by Q7's multiplication shorthand. Its comparison retains the ledger digest and permits only the stated rederivation-seal exception. My additional construction varies the previously unenumerated dependency-list ordering source.

Q7(c), prior findings:

- **V35 codex F1 — CLOSED.** The delivered C6 graph-integrity check runs before roots and independently of origin. The old mixed CHOSEN cycle now gives audit FAIL with graph-integrity diagnostics; complete A and B remain PASS controls. My additional self-cycle and missing-edge comparisons failed through all seven origins. The V35 byte copy reproduces the false PASS: verdicts=[0], expected=1, diagnostic-present=False.
- **V35 codex F2 — CLOSED.** The all-DERIVED cycle produces identical complete outcomes under both seat orders and seeds 0,1,2 in the delivered exhibition. On V35 bytes, seed 1 differs from seed 0 in both seat orders; the verdict remains FAIL but whole-outcome-equal=False. This closes that diagnostic defect, not every possible MOI violation.
- **V35 kimi — no findings to close.**

Independent V35 missing-dependency and multi-diagnostic runs both returned SPI=FAIL with audit verdicts=[1] and diagnostic-present=False. The kit's disclosed widened gate-3 failure set includes exactly the new “graph integrity (auditor closure): missing dependency i2 of i1” diagnostic, and that check passed. The separate subcase invocations exist, but their helper's inadequate assertions remain F3.

Q7(d): new executable defects are F1–F3 above. Evidence scripts, fixtures and logs are retained under `/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v36_codex_gpmn92fc/`, especially `own_checks.py`, `own.log`, `exhibition.log`, `old_f1.log`, `old_f2.log`, `old_missing.log`, `old_multi.log` and `kit.log`. The master, packet and delivered tools were not edited. Nothing here adopts, signs or authorises a census run.

R3C2_V36_GATE_COMPLETE
