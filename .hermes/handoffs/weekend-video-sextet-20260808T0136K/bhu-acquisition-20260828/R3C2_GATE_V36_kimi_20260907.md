ACCESS_SHA=34c90d0b4062f3443ff099d7af33326bc2b79080f28901618c9fe3f46a0c171c
PACKET_SHA=3def6c39ed09e87209d98e0fb629226c3077fe12ab88b8c81f8552e945f80272
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only (the ruled floor): the corpus is cosmology on the Planck 2018 baseline and provenance is visibly measured with unusual care; no hypothesis, comparison model, preferred outcome or "pattern" reconstructible from the packet alone
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=independent cross-reader classification with disputes filed never reconciled (>10% of included claims stops the census as CENSUS_ORIGIN_DISPUTED) and audit re-classification, never an arithmetic input ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`."); no analytic use (tally, comparison, weighting or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 36, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine. Order kept: the master was hashed first (ACCESS_SHA, line 1); the packet was then hashed and read, and Q5, Q6, Q3 answered from it alone, in that order, before the master was opened; the master was then read in full (the two over-long lines 117 and 316 recovered in full by shell text extraction after the file reader truncated them); the two named V35 gate reports were opened only after the master, as the brief permits. Files opened: the packet; the master; R3C2_GATE_V35_codex_20260907.md; R3C2_GATE_V35_kimi_20260907.md; R3C2_SEAT_PACKET.sha256; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256; r3c2_staged_d1d7/README_STAGED_UNADOPTED.md; r3c2_staged_d1d7/r3c2_spi_exhibition.py and r3c2_staged_d1d7/r3c2_staged_tests.py (read; both executed); the delivered tools r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_manifest.py, r3c2_timeout.py, r3c2_build_seat_packet.py (read and executed), r3c2_batch_tools.py (partition executed; seal/join/coverage exercised by the kit), r3c2_seat_sandbox.sb (digest only); R3C2_CORPUS_MANIFEST.md (metadata only — header, row digests, byte counts; never content); the corpus files (hashed and byte-counted only, never content); R3C2_INTERPRETATION_PROTOCOL_20260904.md (metadata only: exists, 3,947 bytes; never opened); R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md (metadata only: exists, 8,523 bytes; never opened). No other gate file, no other version's report, no interpretation contents. Every fixture below is synthetic referee evidence under /tmp/r3c2_v36_kimi with my own sources, ids and values; no corpus content; not a census.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalizes every outcome: "No tier, warrant token, standing or stamp moves." (§11) — it denies consequence transfer on the named axes for every outcome and ranks nothing; that the lane needed to say it tells me a standing system exists that outcomes could in principle bear on, but not which outcome would move it or how. The remaining consequence-adjacent sentences are procedural and attach to stop states, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2 — a filing precedence, not a weight), "do not adopt the passing seat's result" (§4 item 8). CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Yes for the checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled, and above 10% of included claims the census stops ("`CENSUS_ORIGIN_DISPUTED`", §4 item 7); the auditor re-classifies ("the auditor re-classifies the record from the pinned sources", C3) and compares "per re-classified input origin" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What cannot be stated from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), partitioned into 12 batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "**Wording: \"unreproduced from the stated inputs,\" not \"error.\"**" (§3); a reporting rule, not an expectation of any rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, an adequacy rule on `origin_search`, audit re-classification — but nothing says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' printed numbers follow from their own stated inputs, and it cares — with unusual care — how much of the corpus rests on chosen/fitted/imported/undeclared inputs. What it HOPES the census will show — a preferred rate, direction, comparison or pattern — is not reconstructible: no sentence maps any tally to any conclusion, names a comparison model, or ranks any outcome. That is (vi)–(vii), the floor the principal's 18:52 ruling accepted, and nothing beyond it.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either. Per-claim collisions constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2; and even taken as co-occurring, the order decides). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable or non-enumerable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name; a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration (a zero denominator with no passage on either side can pass the audit; "A sealed denominator of zero with any passage on either side fails", C6).

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on (my own fixtures, delivered tools): (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status, the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test (round half away from zero at the printed precision; |reproduced − printed| ≤ the stated uncertainty, taken once; asymmetric = the half-width on the side the reproduced value falls), rests_on USES_CHOSEN. (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (my G_CYCLE_FITTED fixture: both seat ledgers validate exit 0). (c) an imported value — the local-import branch (source_file equal to the claiming file) and the external-import branch (verified manifest row, numeric token, first symbol-and-value line) are both enforced by validate (my Q2 RIGHT record validates exit 0; the kit's D1 positives agree), consumed, rests_on USES_IMPORTED; a named source unpinned or non-matching files REPRO_BLOCKED with a BLOCKED record never consumed; an input traced to no source files REPRO_INPUT_ABSENT. The V36 addition traces cleanly too: a claim whose matched dependency graph is cyclic or incomplete now files the audit failure path — my G family shows such a claim MISMATCHed with the graph-integrity diagnostic in both seat orders, so the audit fails and the study files CENSUS_AUDIT_FAILED; the complete-A/complete-B auditors PASS, so the same claim with a sound graph remains fileable. Decidability is bounded mechanical classification, not a promise to decide arbitrary mathematics in 120 seconds; the cap's own outcome (REPRO_NOT_EVALUABLE / SYMBOLIC_TIMEOUT) is reachable and was exhibited (wrapper exit 124, observed).

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed and printed (executed: my census and census-final runs both printed both blocks, the author_specified_input line, exit 0); C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate ≤ 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. Executed on THIS text with my own fixtures (/tmp/r3c2_v36_kimi/own, sources myA.txt/myB.txt): (1) THE WRONG-BUT-PLAUSIBLE RECORD: value "2" printed at myA.txt:3 in the sentence "We adopt a = 2 from myB (2020)." — filed CHOSEN/ORIG_CHOICE_STATED with that true quotation at its true line — validate returned exit 0: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (2) THE MECHANISMS THAT CATCH IT, AS WRITTEN, each executed: (i) the second seat's independent classification — filing IMPORTED/ORIG_CITATION (same coordinates) merged exit 0 with origin=CHOSEN origin_alt=IMPORTED preserved — the dispute is carried, never silently resolved, and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (ii) the C6 re-classification — against a CHOSEN-only sealed ledger, my IMPORTED auditor record produced compare exit 1 (C6_AUDIT_SAMPLE=FAIL); when the second seat's IMPORTED branch was preserved in the merged ledger, the same auditor record PASSED (exit 0). (3) THE STATED FLOOR: when my auditor repeated the same wrong code, compare returned exit 0, C6_AUDIT_SAMPLE=PASS — the document states this floor itself rather than implying it away (C3 lane-side note). The machine floor that IS enforced is real and was exercised: quotation machine-matched at the cited line for every non-ORIG_SILENT record of every status, value machine-matched as a numeric token at its line, closed-list membership for STANDARD, candidate binding for every PRINTED record, and the machine floor that a record whose value line lies in another file must be IMPORTED/ORIG_CITATION whatever reason code was submitted (observed firing on my own first O-family fixture: a PRINTED record with an external value line filed UNDECLARED was refused, validate exit 1).

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no pattern content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below: rebuilt byte-identical, C4_PACKET_REDACTED=PASS), and my packet-first Q3 found no hypothesis. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind (census fails a row that lacks or alters those fields — kit-asserted); both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any sealed-side omission (included or excluded, either direction) files CENSUS_AUDIT_FAILED — the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt (regenerated deterministically in my kit run, pin re-verified) emits exactly these cases. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script from the complete graphs merge preserves — compute REJECTS a ledger arriving with it set and REFUSES a mixed PRIMARY/ALT view presented without the complete graphs (both paths kit-asserted). The V36 graph-integrity check introduces no channel: it is origin-independent by construction (a cycle through any origin fails), it runs before root classification, and unmatched diagnostic views cannot change a verdict (my G family: complete-A and complete-B auditors PASS on the same merged ledger whose mixed auditor FAILS). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error, prior exposure, the reason-code judgement floor — each stated in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on THIS master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 34c90d0b…" (= ACCESS_SHA, character for character) and "seat packet sha256 3def6c39…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (both 3def6c39ed09e87209d98e0fb629226c3077fe12ab88b8c81f8552e945f80272, 40,267 bytes), and the packet's line 9 built-from digest equals ACCESS_SHA. Negative probes on scratch copies: injecting the forbidden string "hypothesis" into seat-visible §1 produced exit 1, "C4_PACKET_REDACTED=FAIL — forbidden content survived redaction", no packet written; deleting the required token C2_INPUT_LEDGER produced exit 1, "required seat-facing content did not survive redaction", no packet written. The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited; the judgement file it cites exists (8,523 bytes; metadata only). The sandbox profile's digest equals the C4 pin (6978d590…). My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4's definition names the seal-receipt case), leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (kit control and its deletion probe both ok in my run). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened). This is a judgement of the binding order in the design; no custody has been executed and none is certified here.

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary is audit-internal — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs emitted the DISPUTED pair (['USES_UNDECLARED', 'USES_IMPORTED'] and the reverse under seat permutation) marked DISPUTED over disputed graphs, never a clean label over one; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (census fails a row that lacks or alters those fields — kit-asserted; my census run printed the author_specified_input line).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first and read in full; the packet hashed and read first; R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 19/19 OK from the lane directory, both re-verified exit 0 after all runs (the kit's deterministic rewrite of C6_COUNTEREXAMPLE_EXHIBIT.txt reproduced the pinned bytes); the packet's line 9 built-from digest equals ACCESS_SHA. Every named script exists beside the document and matches the digest the document prints for it: r3c2_ledger_tools.py 6c938c3f6b8af37e96996b963ea5ab8daebc52b13561e8af379035ba39622a3a (§3), r3c2_lane_tools.py 363713006f3e7c78eb03d10f0cba29685d663485f56948a46599959f1b7423d3 (C3, §9), r3c2_batch_tools.py 3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e (C1), r3c2_manifest.py 6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a (C5), r3c2_timeout.py fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 (§9), r3c2_build_seat_packet.py 56ac7d1c78196c4d246244126b43666d804f093f8fbe4e2c2289ab885def728e (C4), r3c2_seat_sandbox.sb 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 (C4), R3C2_CORPUS_MANIFEST.md 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 (§2); the kit's _STAGED copies are byte-identical to the delivered tools. C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text). The §9 wrapper: fast child exit 0, failing child exit 7 carried, 5-second child under a 1.0-second cap printed SYMBOLIC_TIMEOUT and exited 124 at 1.004 s. `audit handout` exit 0, fields claim_id/source_file/source_line only. `census` and `census … final` both exit 0 with C1_DENOMINATOR_PRINTED=PASS over my fixture. `audit seal-enumeration | select | seal-rederivation | compare` all executed (every construction below). The builder build command executed to a scratch output (section 4). `r3c2_batch_tools.py partition R3C2_CORPUS_MANIFEST.md 12 …` regenerated partition_12_of_89.json BYTE-IDENTICAL to the pinned partition (39b8d9bf…); seal/join/coverage were exercised by the kit (batch chain controls, both limb chains, the order probe, the ACCESS_SHA report probe — all ok). Corpus: 89/89 enumerable and 79/79 RAW files verified against the manifest by digest and byte count at the declared source location (metadata only). Sandbox: digest matched the pin (no live probe run by this seat; the text's confinement claim is judged as written, and C4/C5b's self-report limits are stated in the document). Future dispatch records, ownership handouts, receipts, provider logs and seat outputs are prerequisites of a future run, not artefacts the document claims already exist.

## Q7 (V36) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=19/19 OK (before and after the kit run)
  TOOL_PINS_MATCH_MASTER=YES (ledger 6c938c3f…, lane 36371300…, batch 3822f12c…, manifest 6b6e4bce…, timeout fbb9bef7…, builder 56ac7d1c…, sandbox 6978d590…, corpus 26dabe7c…)
  STAGED_EQ_DELIVERED=5/5 byte-identical
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only)
  PARTITION_REGENERATED=byte-identical (39b8d9bf…)
  KIT_RUN: deletion_probes=63 / controls=192 passed=192 failed=0 / STAGED_TESTS=PASS (exit 0)
  MOI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; all 18 constructions ok: 2 seat orders x PYTHONHASHSEED 0,1,2 + reversed record arrival + reversed rederivation member order each; the two V34 mixed-cycle rows verdict(AB,BA)=[0,0], compute=[0,0], DISPUTED pair printed; mixed-cycle fixture tag=0)
  V35_BYTECOPY_F1=SPI=FAIL (exit 1; verdicts=[0] where 1 required — the false PASS reproduced on the V35 bytes)
  V35_BYTECOPY_F2=SPI=FAIL (exit 1; whole outcomes differ across seeds — DIFFERING_RUNS at seeds 1 and 2 against the seed-0 base, so seeds 0 and 1 reproduce the diagnostic variation exactly as §10.31 states)
  V35_BYTECOPY_MISSING_DEP=SPI=FAIL (exit 1; no graph-integrity diagnostic on the V35 bytes)
  V35_BYTECOPY_MULTI_DIAG=SPI=FAIL (exit 1; no graph-integrity diagnostic on the V35 bytes)
  V35_BYTECOPY_MEMBER_ORDER=SPI=PASS (control, closed at V34)
  V35_BYTECOPY_V34_MIXED_CYCLE=SPI=PASS (control, closed at V35)
  V35_BYTECOPY_V32_SEARCH=SPI=PASS (control, closed at V32 — my independent run; the kit retains the other two V35-bytes controls)
  V34_BYTECOPY_FULL=SPI=FAIL (exit 1; the two mixed-cycle rows each verdict(AB,BA)=[1,1] with compute-ok=False — codex's exact V34 observation)
  V33_BYTECOPY_FULL=SPI=FAIL (exit 1; the member-order row merged-bytes-equal=False with unchanged verdicts [0,0])
  V32_BYTECOPY_FULL=SPI=FAIL (exit 1; the codex V32 construction flips exactly verdicts=[0,1] across seat orders)
  OWN_G_CYCLE_ORIGINS=CHOSEN,FITTED,IMPORTED,MEASURED,STANDARD,UNDECLARED: each seat ledger validates [0,0]; the matched cyclic auditor FAILS [1,1] in both orders with the graph-integrity diagnostic on BOTH sides (gi_auditor=['cycle at x'], gi_sealed=['cycle at x']); the complete-A auditor PASSES (0) on the same merged ledger; merged bytes equal across seat orders
  OWN_G_SELFLOOP: validate=1 (validate itself fails a cycle), compute=1, audit=1 with the graph-integrity diagnostic
  OWN_G_SEALED_SIDE: a faithful auditor against a sealed ledger with a dangling parent → audit=1, gi_auditor=gi_sealed=['missing dependency ghost of d']; against a sealed self-cycle → audit=1, gi_auditor=gi_sealed=['cycle at d'] (defence in depth: validate and compute fail these first; the audit's matched-sealed check is the last line and it fires)
  OWN_O2_ORDERING (both seat ledgers validate [0,0]; origin dispute IMPORTED vs UNDECLARED with an ORIG_SILENT search, plus a parent dispute): 8 runs — AB/BA x PYTHONHASHSEED 0,1,2 + reversed record arrival + reversed rederivation member order + reversed INNER record member order + LC_ALL=C — whole_outcome_equal=True for every run (merged bytes, compute exit/stdout with the DISPUTED pair ['USES_UNDECLARED','USES_IMPORTED'], audit exit 0, ordered FAIL lines, the complete artefact; only the rederivation seal differs in the reversed-rederivation run, the one identity field §3 allows)
  OWN_SEALED_ARRAY_SHUFFLE (NOT an enumerated source — the delivered sealed ledger's records-array order): audit_rc=0, the whole outcome identical except sealed_ledger_sha256, the digest of what was delivered (an identity, not an ordering)
  OWN_M_DETERMINISM: my own multi-diagnostic case (two missing dependencies + two value mismatches + a self-loop) in 12 fresh processes under random PYTHONHASHSEEDs — audit_rc=1, all 12 whole outcomes byte-identical (ordered FAIL line: inputs in sorted order, graph-integrity lines after record lines, auditor closure before matched sealed closure)
  Q2_MECHANISM: WRONG_CHOSEN_ON_CITATION validate exit 0 / RIGHT_IMPORTED validate exit 0 / merge exit 0 origin_alt=IMPORTED preserved / auditor corrected vs CHOSEN-only sealed exit 1 FAIL / vs alt-preserved exit 0 PASS / auditor repeats the wrong code exit 0 PASS (the stated floor)
  C5_LIVE=[0,0,0,0,0] Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1…
  WRAPPER=[0,7,124] (SYMBOLIC_TIMEOUT at 1.004 s under a 1.0 s cap)
  HANDOUT=exit 0, claim_id/source_file/source_line only
  CENSUS=PASS exit 0; CENSUS_FINAL=PASS exit 0
  NO_UNORDERED_TRAVERSAL=YES (read-level sweep: every set, dict-from-set and set-difference iteration in the delivered audit, census, selection, merge and compute is sorted at its production boundary; every JSON write uses sorted keys; the only directory walk in any delivered tool is r3c2_manifest.py's os.walk, which sorts dirnames and filenames at every level and sorts the digest rows — see the one cosmetic finding below)
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only
  LEAK_FLOOR_JUDGEMENT=exists, 8,523 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline; the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the kit's 63 deletion probes each turn a negative into PASS only when its check is neutralised (or show the diagnostic absent while an overlapping guard still fails), so no check is decorative; every exit code cited in this report was captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES over the V35 gate's findings (codex F1, F2; kimi none), each with my own evidence:

  - codex F1 (a matched graph cycle passing through a CHOSEN record with parents escaped C6 — root traversal stops at any non-DERIVED origin; the closure walker suppressed revisits without diagnosing them — a FALSE PASS) — CLOSED. Text: §3 carries codex's exact replacement sentences ("Before root-origin classification, `audit compare` checks every `derived_from` edge in each selected claim's auditor-reconstructed dependency closure and its matched sealed closure for missing records and cycles, independently of `origin`. A missing dependency or cycle makes that claim `MISMATCH` and fails the audit, including when the cycle passes through a CHOSEN, FITTED, IMPORTED, MEASURED, STANDARD or UNDECLARED record. Root-origin traversal retains its stated origin rule; stopping at a non-DERIVED origin never substitutes for graph-integrity validation. Unmatched diagnostic views do not participate in this decision."). Code, verified by reading: r3c2_ledger_tools.py lines 86–100 (graph_integrity: origin-independent, sorted traversal, sorted problems) and lines 460–465 (both closures checked BEFORE root classification; the diagnostics join the claim's why). Executed: my G family builds the codex F1 topology with the cycle routed through EACH of the six non-DERIVED origins in turn — every one FAILS in both seat orders with the graph-integrity diagnostic on both the auditor and the matched sealed side, while the complete-A auditor PASSES on the same merged ledger; a self-loop, a sealed-side missing dependency and a sealed-side cycle are likewise caught (validate and compute fail them first; the audit's matched-sealed check fires as the last line). Fail-first: on the pinned V35 bytes the same construction is a FALSE PASS (verdicts=[0] where 1 is required) — my own run, exit 1. I could not build a cycle or missing dependency through any origin that still passes C6.
  - codex F2 (the cycle diagnostic varied between processes — cycle at x / cycle at y — because root recomputation iterated a set) — CLOSED. Code: roots() now iterates sorted derived_from (seat tool line 109, marked V36); graph_integrity visits starts and parents sorted and returns sorted problems; every other traversal in compare is sorted (V36 canon markers). Executed: the exhibition's F2 construction on the delivered tools gives identical whole outcomes across PYTHONHASHSEED 0,1,2 in both seat orders; my own multi-diagnostic construction gives byte-identical whole outcomes in 12 fresh processes under random seeds; on the pinned V35 bytes the same construction's outcome differs across seeds (DIFFERING_RUNS at seeds 1 and 2 against the seed-0 base) — the variation reproduced exactly where §10.31 says it lived. The class repair is structural, not per-site: the exhibition is ONE combined property over seat orders x seeds x reversed arrival x reversed rederivation member order, and my O2 family adds reversed inner record member order, LC_ALL, and the sealed ledger's array order with the whole outcome invariant.
  - kimi V35: no findings — nothing to close.
  The kit's per-subcase methods assert what §10.31 says: the MOI property control on the delivered tools (ok), the four fail-first V35 subcases F1 / F2 / missing-dependency / multi-diagnostic (each its own test, each SPI=FAIL on the V35 bytes — reproduced independently), the two retained V35-bytes controls labelled as controls (member order, V34 mixed-cycle; the third named row, V32 same-origin-search, passes on the V35 bytes in my independent run), and the fail-first V34 / V33 / V32 subcases (each SPI=FAIL on its own bytes with exactly the named rows — reproduced independently). The ONE disclosed pre-existing control conflict checks out: the gate-3 F1 "omitted dependency" control now asserts the widened exact failure set including "graph integrity (auditor closure): missing dependency i2 of i1" (test file line 212; ok in my kit run).

(d) NEW executable defects, with counterexample: NONE at the executable level. Every construction behaved as the V36 text claims, including constructions derived from ordering sources NOT in the enumerated list (sealed-ledger records-array order; inner JSON member order of rederivation records; LC_ALL) and graph-integrity attacks through every origin. Two observations, neither a repair request beyond the one cosmetic below: (i) §10.31's sentence "the member-order, V34 mixed-cycle and V32 same-origin-search subcases PASS on the V35 bytes … the rows are retained as controls, stated as such" — the kit retains two of the three named rows as V35-bytes control tests; the third (V32 same-origin-search on the V35 bytes) has no kit row and passes in my independent run (SPI=PASS, exit 0). The substantive claim is true as verified; §10 is history that binds nothing. (ii) the lane tool's roots_alt helper is defined and never called — carried from the V35 kimi gate's observation; it computes nothing that reaches any output and changes no class filed, no control's passability, no reachability, no conclusion and no tested value.

ONE FINDING — COSMETIC (changes no class filed, no control's passability, no reachability, no conclusion and no tested value).

Verbatim sentence, §3 (lane-side MOI paragraph):
> (e) filesystem listing order — no delivered tool walks a directory, so this source has no instance today.

Defect: the stated reason is false as written. `r3c2_manifest.py` — a delivered tool, pinned in C5 and delivered in the seat's working directory — walks the pinned-environment directory with os.walk (line 12). The conclusion ("no instance today") stands only because that tool enforces the invariant structurally: it sorts `dirnames` and `filenames` at every level and sorts the digest rows before hashing (code-verified; its docstring specifies the sorted digest; two live runs on this host printed the same MANIFEST_SHA256=1b5463c1… over 20,637 files). A reader trusting "no delivered tool walks a directory" could unsort that walk in a future edit without re-examining MOI. The walk's output feeds the C5 harness digest, not the audit outcome; no class filed, no control's passability, no reachability, no conclusion and no tested value changes — COSMETIC.

Exact replacement:
> (e) filesystem listing order — one delivered tool walks a directory (`r3c2_manifest.py`, over the pinned environment); it sorts `dirnames` and `filenames` at every level and sorts the digest rows before hashing, so the digest it prints carries no listing order and this source has no instance with an observable effect today.

Why SOUND_WITH_REPAIRS and not SOUND: the one cosmetic inaccuracy above is a repair request, however small. Why not UNSOUND: Q1 and Q2 answer YES on executed evidence; the blind is machine-asserted and reproduced byte-for-byte on this master; the seal binds each artefact before the step it must precede; every negative outcome is worded within its definition; both V35 findings are closed above with my own fixtures (cycles through every origin fail; diagnostics are process-invariant), the class and the principle hold under every ordering source I could construct, enumerated or not; and nothing the document describes failed to execute as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 34c90d0b… = ACCESS_SHA; packet 3def6c39… = PACKET_SHA; both sheets exit 0). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v36_kimi, not corpus data and not a census.

R3C2_V36_GATE_COMPLETE
