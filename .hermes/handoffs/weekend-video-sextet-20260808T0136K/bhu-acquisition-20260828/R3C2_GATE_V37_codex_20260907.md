ACCESS_SHA=5d83997018367a591be3fe25bf48c30d0a3d4de2c207705ad68d16ab8b8f8c9a
PACKET_SHA=007fb3ce9119d70fc6e6b4be2f115fbc4bf3fbf41ebfddaeff93c4d07560d6d4
GATE=PREREG_UNSOUND
LEAK=Methodological interest in reproduction and transitive provenance; no directional hypothesis, comparison preference or hoped-for result reconstructed.
CONSEQUENCE_VISIBLE=Procedural: origin disagreement above 10% stops continuation — “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.”
ORIGIN_PURPOSE=Independent provenance comparison and audit — “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.”
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=NO
PRIOR_FINDINGS_CLOSED=NO

Design-only review of the delivered, unsigned Version 37. The access command ran first. Packet-only answers were recorded before reading the master; the master was then read in full, followed by the two expressly permitted V36 reports. Section 10 supplies no inherited verdict. Q7's later explicit executable instructions were treated as authorizing necessary kit/tool/fixture access. No other gate reports or interpretation-protocol contents were opened. All executable work was synthetic referee verification, not a census or corpus derivation.

The gate fails on the incomplete exact-failure repair and a new order-dependent audit verdict. The dependency-list repair and origin-independent graph checks pass the tests below. NO_MASKED_STAGE=NO concerns the kit accepting a crashed stage; the five C5 commands and the timeout wrapper themselves propagate their statuses correctly.

## 1. OUTCOME CLASSES

Packet-only answers, preserved in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Yes, procedurally. “Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`.” Also, the audit-failure class says: “No tally is filed; report which.” These affect continuation and reporting. No sentence establishes a preferred substantive result. “No tier, warrant token, standing or stamp moves” denies movement on those axes.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

Yes for checking: “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” Origins are independently classified, compared, and audited; disagreements can stop the study. The packet does not disclose a substantive interpretation mapping or preferred provenance category.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

The question asks whether “the paper's own number follow[s] from the paper's own recipe applied to the inputs it states.” “Provenance is transitive” and the detailed origin-evidence/audit rules reveal methodological interest beyond arithmetic. The Planck table reveals cosmological subject matter. The explicit author-specified-input count reveals interest in distinguishing stipulated inputs from asserted results. The standing disclaimer suggests a broader evaluative framework. None supplies a directional hypothesis, a comparison-model preference, a desired rate of reproduction, a preferred origin, or a particular hoped-for pattern. These are content/procedure inferences, not a reconstructed substantive stake.

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES at the level of bounded filing rules. The executable defects below do not require reopening option (c). The following are concrete synthetic physical claims, not attributed findings from corpus papers:

| Claim or condition | Unique per-claim outcome | `rests_on` |
|---|---|---|
| “We choose a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same recipe/input, L printed as 5 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 2 m from X; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Unprinted a is found at the first qualifying line of verified enumerable X | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Unprinted a is cited to an unpinned source | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a; a is unprinted and uncited, with an adequate silent search | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| A specified computation exceeds the cap | REPRO_NOT_EVALUABLE | Computed independently from its input records |
| Own numerical result without a producing procedure or input records | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

The author-set input numeral itself is excluded as AUTHOR_SPECIFIED_INPUT; the downstream result is included and attempted. A fitted parameter asserted as the paper's own result is itself a candidate: if its fitting procedure is merely named, that candidate can file NO_DERIVATION_STATED, while its printed value remains consumable by a downstream claim. Origin does not prohibit arithmetic consumption.

Delivered `compute` emitted USES_CHOSEN, USES_FITTED, USES_IMPORTED, USES_UNDECLARED, DERIVED_STANDARD_OR_MEASURED_ONLY and NOT_COMPUTED on my fixtures. Final `census` validation accepted all six per-claim tokens. This verifies encoding, not the substantive correctness of any corpus arithmetic.

Hard overlap: L = a + b, where a is unprinted but cited to an unpinned source and b is unprinted and uncited. BLOCKED and INPUT_ABSENT predicates co-occur; the explicit precedence files BLOCKED only. I found no remaining double filing after precedence. Decidability means a bounded reproduction classification, including inability to evaluate, rather than a promise to decide arbitrary mathematics.

Study-level witnesses: a nonempty all-arithmetic tally with audit PASS gives COMPLETE; one absent-input claim with audit PASS gives PARTIAL; a missing seed gives AUDIT_FAILED; a pre-tally control failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 included claims gives ORIGIN_DISPUTED; a surviving control fail/pass split gives CONTROL_SPLIT.

A partial tally with a missing seed satisfies two predicates but files AUDIT_FAILED by precedence. INCONCLUSIVE is genuinely reachable as PARTIAL with successful audit. Empty enumerations on both sides can also reach PARTIAL; zero included claims with candidate passages instead triggers the stated audit-failure rule. These witnesses do not replace the required independent two-seat C0.

## 2. CONTROLS

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

Yes as evidence-backed human classification; not as automatic semantic verification. My wrong-but-plausible record was:

```json
{
  "claim_id": "p.txt#c",
  "input_id": "p.txt#x",
  "symbol": "x",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "p.txt",
    "source_line": 3,
    "verbatim": "We adopt x = 2 from X."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "p.txt",
  "source_line": 3
}
```

The quotation actually appears at the synthetic source's line 3. `validate` returned 0: matching the quotation and reason-code/origin pair does not enforce citation-first semantic precedence. A correct second reader files IMPORTED and exposes the dispute. A corrected IMPORTED auditor against this CHOSEN-only sealed record returned 1; an auditor repeating the wrong classification returned 0. Thus the human comparison can catch it, but identical reader error survives. The document explicitly acknowledges that floor; it is not a new defect.

The named three-valued controls are C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their artefacts are exhibitions, printed runs, ledgers, seals, audit JSON, path lists or session-identified provider logs. A bare asserted token cannot satisfy a printed-run predicate. C4/C5b can pass on an incomplete self-reported path list and honestly state that limit. Unreached controls are NOT_RUN, including validation before batch integration.

**F3, continued — NON-COSMETIC: the exact-failure rule is not applied throughout the kit.**

Verbatim operative sentences:

> A NEGATIVE CONTROL MUST ASSERT THE EXACT EXPECTED FAILURE, NEVER THE BARE POLARITY.

> The same rule governs every other control in the kit that judges an outcome, the deletion probes included: each asserts its own diagnostic and exit code, and no traceback or launch failure can satisfy one.

The central rule appears once in the operative text and once at the kit's head. But the general `check` helper, at kit lines 12–15, does not call `setup_ok`. Its completion token is optional, diagnostic matching uses substrings, and diagnostic order is ignored. The new safeguards primarily protect `moi_judge`, not every judge.

Executable counterexample: I extracted the delivered `check` function unchanged and supplied the output of a real subprocess containing:

```python
print('FAIL: provenance graph 0: cycle at p.txt#x', flush=True)
raise RuntimeError('injected infrastructure failure')
```

The subprocess exited 1 and printed a traceback. The exact invocation used by the kit's V37 compute-cycle negative nevertheless accepted it:

```text
GENERAL_CHECK_ACCEPTS_REAL_TRACEBACK=True
CRASH_EXIT=1
```

The same checker also accepted a changed diagnostic suffix and reversed diagnostic order. These are acceptance tests of the delivered predicate, not claims that the normal production command crashes.

The deletion-probe judge has a setup guard, but does not require the supposedly deleted diagnostic to be absent. I moved only the `PROBE:C6_EDGES` annotation onto a standalone comment in a scratch tool copy, leaving every production guard and diagnostic active. The delivered `probe` helper neutralized the comment and accepted the unchanged hybrid failure because its expected guard substring and exit 1 remained:

```text
PROBE_ACCEPTS_UNDELETED_DIAGNOSTIC=True
STILL_EMITTED=["input i1: record matches neither complete declared branch; derived_from ['i3'] vs sealed ['i2'] (alternative ['i3'])"]
```

Thus a probe can claim a successful deletion test when the diagnostic was never deleted. This directly contradicts the kit's advertised diagnostic-absence contract.

Exact replacement for the second quoted sentence:

> Every outcome-judging helper in the kit uses one common exact-result evaluator: it rejects empty output and setup, launch, import or traceback failures; requires the subcommand's completion token and exact exit code; and compares the complete ordered diagnostic rows and subcase evidence with their declared expectations, not substrings. A subcommand without a completion token must gain one before serving as control evidence. Deletion probes additionally require the targeted diagnostic to be absent; when an overlapping guard remains, they require its exact retained failure result. Relocating a deletion marker without disabling its diagnostic must fail the probe. Meta-controls exercise every helper with a real crash, an unrelated subcase failure, a wrong diagnostic and an undeleted diagnostic. Production guards are never weakened to satisfy a probe.

Implement this evaluator and its consumers, then re-pin and re-run. Merely repeating the existing principle does not close F3. The full unmodified kit passed 201/201 controls and 65 probes while these acceptance defects remained.

## 3. CIRCULARITY

The inclusion rule contains no hypothesis or comparison preference. Two independent enumerations, retained exclusion rows and the auditor's independent full-corpus enumeration constrain selective omission. A contrary claim cannot legitimately be quietly discarded; an auditor-only passage is an omission even when the auditor excludes it. Shared omission or classification error remains possible.

The packet reveals provenance's operational importance but withholds its substantive ranking and interpretation mapping. Receipt P precedes limb A. Those mechanisms constrain expectation reaching evidence; they do not establish absence of prior beliefs. I found no additional directional leak or expectation-dependent exclusion rule.

The new order defect in section 7 can reject equivalent evidence, but does not itself establish a directional bias toward any substantive result.

## 4. THE BLIND

The pinned builder rebuilt this master with the delivered brief to a scratch output:

```text
C4_PACKET_REDACTED=PASS
PACKET_REBUILD_BYTE_IDENTICAL=True
PACKET_BYTES=40648
REDACTED_SPANS=51
```

Sections 1–6, 9 and 11 survived. Injecting `hypothesis` into visible section 1 failed with exit 1 and no packet; removing required C2_INPUT_LEDGER content also failed with exit 1 and no packet. The packet's built-from digest equals ACCESS_SHA.

This verifies the finite forbidden-list/required-content mechanism, not universal semantic blindness. Procedural consequences and provenance-checking purpose remain visible, as the operative floor admits.

The sandbox profile matches its pin. A live in-scope source read succeeded; a lane-master read was refused; SymPy imported successfully when launched from the actual scratch working directory. A preliminary probe launched from the denied lane directory failed during import-path lookup; correcting the probe's working directory resolved that setup error. No confinement guarantee beyond the recorded profile/probes is inferred. Runtime/configuration/network exceptions, self-report incompleteness and possible prior exposure are expressly acknowledged.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before the protocol is opened. Independent re-hashing must verify the four values and print both receipts. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

This is adequate record binding at the specified stages, conditional on executing the procedure. It does not prove that the owner had not guessed the answer or previously knew the protocol. The protocol exists at the named path, 3,947 bytes, checked by metadata only. Its contents and actual receipts were not inspected. No custody event is certified here.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” never “error.” The delivered brief applies that wording to negative outcomes generally. Missing-input, blocked-source, missing-procedure and computational-limit classes do not assert a scientific error.

REPRO_WITHIN_STATED_PRECISION matches the stated rounding/uncertainty test. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE means complete arithmetic classification with audit PASS, including arithmetic failures; it does not mean every claim reproduced. No substantive label repair is required.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES: the universal exact-failure assurance fails as F3 demonstrates, and the universal meaningless-order assurance fails on the new construction below. No currently promised operative deliverable was found missing. The exhibition is delivered inside `r3c2_staged_d1d7/`, not beside the master; the kit references it there. Future ownership extracts, session reports, ledgers, receipts, seals, provider logs and the dated check sheet are prerequisites/outputs, not already-completed artefacts.

**N1 — NON-COSMETIC: order inside `origin_search` changes merged bytes and can flip C6's verdict.**

Verbatim operative sentence:

> MEANINGLESS-ORDER INVARIANCE ("MOI", formerly SPI): the audit verdict and the whole audit outcome — verdict, the full ordered finding set with each finding's code and reason, the merged ledger bytes and the ledger digest — are invariant under every ordering that carries no meaning, including dependency-list order.

My additional ordering sources are the enumeration order of searched files and returned search matches. Neither changes which searches were performed or what evidence was found. The query list and every substantive field remain unchanged.

Clean synthetic source: p.txt line 6 prints `x = 2.` and line 8 asserts `We find L = 4 m.`; neither p.txt nor q.txt states x's origin. q.txt line 1 mentions x without giving provenance. A PRINTED/UNDECLARED input records:

```json
"origin_search": {
  "query": ["x", "choose", "adopt", "fit"],
  "files": ["p.txt", "q.txt"],
  "matches": ["p.txt:6", "q.txt:1"]
}
```

Both seats provide the same record. Reversing only `files`, or only `matches`, preserves the evidence inventory. All variants validate and, when the auditor uses the corresponding order, audit PASS. Nevertheless their merged digests differ:

```text
BASE=b8b87c7b012226ef9634112f953e116ec86da06735e30f117f29ace187533706
FILES_REVERSED=cc7f5e3ea96846442879333200a880e625b9383b262f1d9e3f88d25578f01dc8
MATCHES_REVERSED=cc9f559b4f1be11d0daa5c1b220014a2689705fb1d70f565f9d3afdd23c085ed
```

More consequentially, hold the original merged ledger fixed and reverse only the auditor's file list or match list. After properly sealing that reconstruction, C6 changes from exit 0 to exit 1:

```text
AUDITOR_ONLY_FILES_EXIT=1
AUDITOR_ONLY_MATCHES_EXIT=1
input p.txt#x: record matches neither complete declared branch; origin_search differs (structural comparison against the matched branch's search)
```

This is not merely an identity-digest difference: equivalent evidence receives a different audit verdict. `merge` hashes/preserves these arrays in supplied order, and C6 uses order-sensitive Python list equality inside the search object. Dependency-list normalization does not cover them.

Exact replacement for the quoted sentence:

> MEANINGLESS-ORDER INVARIANCE ("MOI", formerly SPI): the audit verdict and the whole audit outcome — verdict, the full ordered finding set with each finding's code and reason, the merged ledger bytes and the ledger digest — are invariant under every ordering that carries no meaning, including dependency-list order and the enumeration order of `origin_search.files` and `origin_search.matches`. The latter two fields record unordered evidence inventories. A shared canonical-search function sorts their entries by canonical JSON without dropping entries or changing their content; it preserves the query field. `merge` applies it before canonical keys, graph digests, branch selection and serialization, and C6 applies it to both reconstructed and sealed search evidence before branch matching and comparison. Reordering an inventory alone must preserve the complete outcome; adding, removing or changing an entry must remain detectable. These cases join the combined exhibition, including auditor-only permutations.

Implement and re-pin. This finding does not reopen the repaired dependency-list instance; it disproves the wider claimed invariant through another evidence field.

**C1 — COSMETIC: the exhibition's run description is stale.**

Verbatim operative sentence:

> EXHIBITION, one combined property in `r3c2_spi_exhibition.py` (the file and its PASS token keep the historical name): every construction is run under both seat orders and PYTHONHASHSEED 0, 1 and 2 (0 and 1 reproduce the V35 diagnostic variation on the V35 bytes), plus one run with both seats' record lists reversed and one with the auditor's rederivation file in reversed member order, and the whole outcome must be identical across all of them — the only field allowed to differ in the reversed-rederivation run is the seal of the rederivation bytes, which is the digest of what was delivered, an identity and not an ordering; every construction also asserts its expected verdict and diagnostic.

That sentence enumerates eight runs. The delivered code adds three dependency-order variants and actually runs eleven, not the nine narrated in §10.32/Q7: six seat-order/seed runs plus five additional runs. I counted eleven compute outputs for the dependency construction. The code also applies its seal exception to the auditor-dependency permutation.

Exact replacement:

> EXHIBITION, one combined property in `r3c2_spi_exhibition.py`: each construction has eleven runs — both seat orders at each of PYTHONHASHSEED 0, 1 and 2; one reversed-record-arrival run; one reversed-rederivation-member-order run; reversed dependency lists in both seat ledgers under each seat order; and one reversed-auditor-dependency-list run. Every run asserts the construction's expected verdict and diagnostic and equality of the complete outcome. Only the rederivation-byte seal may differ in the two variants that reorder the delivered auditor reconstruction; every other compared value remains invariant.

This corrects the description of existing behavior without changing any executed test, class, passability, reachability or conclusion: COSMETIC. Record the historical nine-versus-eleven correction in a new history entry; do not rewrite §10.32.

Q4 verification inventory and method:

- Master and packet hashed and read; brief read; both pin sheets independently verified, 8/8 and 21/21 entries. The five staged production tools match their delivered counterparts. The master and packet hashes remained unchanged after verification.
- `r3c2_ledger_tools.py`: inspected validation, census, graph traversal, selection, seals, reconstruction schema, branch comparison and audit output; ran census/final, validate and every audit subcommand through my fixtures and the kit. Verified root-origin comparisons, corrected-origin rejection, graph-integrity failures, complete positive controls and the search-order counterexample.
- `r3c2_lane_tools.py`: read the complete file; ran merge and compute on provenance, empty-ledger, graph and ordering fixtures. Compared V36/V37 production bytes: changes add dependency normalization and integrity checks and remove dead `roots_alt`; no production guard was weakened. The authoritative C6 full-record mismatch remains unchanged.
- `r3c2_batch_tools.py`: read partition/seal/join/coverage; regenerated the 12-batch partition byte-identically. The isolated kit exercised both limb chains, coverage, ownership, digest, graph and binding failures.
- `R3C2_CORPUS_MANIFEST.md`: read metadata and verified bytes/digests for all 89 enumerable texts and 79 RAW files. No scientific interpretation of their contents was performed. The partition files exist and match their pins.
- `r3c2_build_seat_packet.py`: read and executed this-master rebuild and two negative mutations. The historical integrated-candidate file was copied solely as the kit's required build fixture, not treated as the reviewed design.
- `r3c2_manifest.py`: read; executed C5's live directory manifest and a reversed-filesystem-listing experiment. Identical files yielded identical stdout/digest under reversed listings.
- `r3c2_timeout.py`: read; executed a successful child, exit-7 child and deadline child; observed exits 0, 7 and 124, with SYMBOLIC_TIMEOUT at 0.204 seconds under a 0.2-second synthetic test cap.
- `r3c2_spi_exhibition.py` and `r3c2_staged_tests.py`: inspected construction, permutation and judging logic; ran the complete isolated kit, including the delivered 20-construction exhibition, eight predecessor negative subcases, three retained V35 positive controls and all three meta-controls. Scratch isolation prevents the kit's deletion/rewrite operations from touching delivered files.
- `r3c2_seat_sandbox.sb`: read, pin checked and live access probes executed. Interpretation protocol and leak-floor memorandum were checked for existence by metadata only. No future dispatch inventory, provider log or receipt is certified.

Q7(a–b), tokens from THIS version:

```text
PACKET_REBUILD=BYTE_IDENTICAL
BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT
SEAT_PIN_SHEET=8/8_OK
KIT_PIN_SHEET=21/21_OK
STAGED_EQUALS_DELIVERED=5/5
CORPUS_BYTES_VERIFIED=89/89_ENUMERABLE_AND_79/79_RAW
PARTITION_REBUILD=BYTE_IDENTICAL
SIX_PER_CLAIM_TOKENS_FINAL_CENSUS=PASS
Q1_COMPUTE=USES_CHOSEN,USES_FITTED,USES_IMPORTED,USES_UNDECLARED,DERIVED_STANDARD_OR_MEASURED_ONLY,NOT_COMPUTED
Q2_WRONG_ORIGIN_VALIDATE_EXIT=0
Q2_CORRECTED_AUDITOR_EXIT=1
Q2_SHARED_WRONG_AUDITOR_EXIT=0
ALL_ORIGIN_CYCLE_REJECTIONS=21/21_AT_VALIDATE_COMPUTE_C6
ALL_ORIGIN_MISSING_DEPENDENCY_REJECTIONS=21/21_AT_VALIDATE_COMPUTE_C6
ALL_ORIGIN_VALID_CONTROLS=21/21_AT_VALIDATE_COMPUTE_C6
INVALID_GRAPH_COMPUTE_OUTPUTS_WRITTEN=0
STAGED_TESTS=PASS
KIT_CONTROLS=201/201
KIT_DELETION_PROBES=65
DELIVERED_EXHIBITION=20_CONSTRUCTIONS_11_RUNS_EACH
MOI_META_CONTROLS=3/3_PASS
GENERAL_CHECK_ACCEPTS_REAL_TRACEBACK=YES
PROBE_ACCEPTS_UNDELETED_DIAGNOSTIC=YES
SEARCH_FILES_ORDER_WHOLE_OUTCOME_EQUAL=NO
SEARCH_MATCHES_ORDER_WHOLE_OUTCOME_EQUAL=NO
AUDITOR_ONLY_SEARCH_ORDER_FLIPS_VERDICT=YES
FILESYSTEM_LISTING_ORDER_EQUAL=YES
C5_LIVE_EXIT_CODES=0,0,0,0,0
PYTHON=3.9.6
SYMPY=1.14.0
INTERPRETER_SHA256=b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
C5_FILES=20637
C5_MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
WRAPPER_EXIT_CODES=0,7,124
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=NO
PRIOR_FINDINGS_CLOSED=NO
```

C5's commands ran in order using the Markdown code span's rendered one-line SymPy invocation. A preliminary extraction that preserved the source's indented physical line break produced IndentationError; that failed attempt is not counted as a pass. The printed user-site path was `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`, admitted by C4's pinned-environment rule. No mandated operative command is a shell pipeline. The kit's acceptance of a crashed subprocess is why the broader NO_MASKED_STAGE token is NO.

Q7(c), V36 findings individually:

| V36 item | Closure | Evidence on V37 |
|---|---|---|
| codex F3: fail-first accepts unrelated failure | PARTLY | `moi_judge` rejects the empty-tools launch, wrong subcase and wrong-evidence meta-cases; general `check` still accepts a real traceback, and deletion probes accept an undeleted diagnostic. |
| codex F1: dependency-list order | CLOSED | Normalization occurs at the head of merge before keys/graphs/serialization; delivered dependency-order construction passes all eleven variants, while its V36 predecessor subcase fails as expected. N1 is a distinct surviving source of order dependence. |
| codex F2: non-DERIVED graph integrity at C3 | CLOSED | Cycles and missing dependencies rejected through all seven origins at validate, compute and C6; valid controls pass; invalid compute writes no output. Both new guard probes execute. |
| codex C1: false filesystem inventory | CLOSED | Operative source (e) names the manifest walk and its sorting; reversed-listing experiment preserves the result. |
| kimi cosmetic: same filesystem sentence | CLOSED | Same evidence as codex C1. |
| kimi observation (i): third V35 control missing | CLOSED | The same-origin-search V35 control row is present and passed in the isolated kit. |
| kimi observation (ii): dead roots_alt | CLOSED | V36/V37 diff removes the unused helper; current lane tool contains no such definition. |

Q7(d): the new executable counterexample is N1. F3's two surviving judge paths show why the broader repair remains partial. C1 is documentation-only. I do not adopt §10.32's reported predicate measurement as my own; my evidence is the independently executed cases above.

Evidence scripts, fixtures and logs are retained at `/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v37_codex_aquujaxd/`: `own_checks.py`, `own.log`, `own/commands.json`, `search_order_clean.py`, `search_clean.log`, `search_clean/commands.json`, `probe_attack.py`, `support_checks.py`, `support.log`, `support/commands.json`, and `kit.log`. The master, packet and delivered production tools were not edited. Nothing here adopts, signs or authorizes a census run.

R3C2_V37_GATE_COMPLETE
