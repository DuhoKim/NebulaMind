ACCESS_SHA=5d83997018367a591be3fe25bf48c30d0a3d4de2c207705ad68d16ab8b8f8c9a
PACKET_SHA=007fb3ce9119d70fc6e6b4be2f115fbc4bf3fbf41ebfddaeff93c4d07560d6d4
GATE=PREREG_SOUND_WITH_REPAIRS
LEAK=content-level only (the ruled floor): the corpus is cosmology on the Planck 2018 baseline and provenance is visibly classified with unusual care; no hypothesis, comparison model, preferred outcome or "pattern" reconstructible from the packet alone
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=independent cross-reader classification with disputes filed never reconciled (>10% of included claims stops the census as CENSUS_ORIGIN_DISPUTED) and audit re-classification from the pinned sources, never an arithmetic input ("Arithmetic consumes records according to status `PRINTED` or `STANDARD`."); no analytic use (tally, comparison, weighting or conclusion computed from `origin`) is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 37, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine; its two named subsections (§10.31, §10.32) were read only as the repair claims Q7 orders me to test. Order kept: the master was hashed first (ACCESS_SHA, line 1, before any content was read); the packet was then hashed and read, and Q5, Q6, Q3 answered from it alone, in that order, before the master was opened; the master was then read in full (the over-long lines 117 and 315 recovered in full by shell text extraction after the file reader truncated them); the two named V36 gate reports were opened only after the master, as the brief permits. Files opened: the packet; the master; R3C2_GATE_V36_codex_20260907.md; R3C2_GATE_V36_kimi_20260907.md; R3C2_SEAT_PACKET.sha256; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256; r3c2_staged_d1d7/README_STAGED_UNADOPTED.md; r3c2_staged_d1d7/r3c2_staged_tests.py and r3c2_staged_d1d7/r3c2_spi_exhibition.py (read; both executed); the delivered tools r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py, r3c2_manifest.py, r3c2_timeout.py, r3c2_build_seat_packet.py (read and executed), r3c2_seat_sandbox.sb (digest only); the V36 bytecopy tools (diffed against delivered); R3C2_CORPUS_MANIFEST.md (metadata only); the corpus files (hashed and byte-counted only, never content); R3C2_INTERPRETATION_PROTOCOL_20260904.md (metadata only: exists, 3,947 bytes; never opened); R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md (metadata only: exists, 8,523 bytes; never opened). No other gate file, no other version's report, no interpretation contents. Every fixture below is synthetic referee evidence under /tmp/r3c2_v37_kimi with my own sources, ids and values; no corpus content; not a census.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalizes every outcome: "No tier, warrant token, standing or stamp moves." (packet §11) — it denies consequence transfer on the named axes for every outcome and ranks nothing; that the lane needed to say it tells me a standing system exists that outcomes could in principle bear on, but not which outcome would move it or how. The remaining consequence-adjacent sentences are procedural and attach to stop states or filing order, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2 — a filing precedence, not a weight), "do not adopt the passing seat's result" (§4 item 8). The wording clause of §3 ("Wording: \"unreproduced from the stated inputs,\" not \"error.\"") restrains what REPRO_FAILED asserts rather than stating a consequence for it. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Only the checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled, and above 10% of included claims the census stops ("`CENSUS_ORIGIN_DISPUTED`", §4 item 7); the auditor re-classifies ("the auditor re-classifies the record from the pinned sources", C3) and compares "per re-classified input origin" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What cannot be stated from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), partitioned into 12 batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" (§3); a reporting rule, not an expectation of any rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armored — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, an adequacy rule on `origin_search`, audit re-classification — but nothing says what it is weighed against. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' printed numbers follow from their own stated inputs, and it cares — with unusual care — how much of the corpus rests on chosen/fitted/imported/undeclared inputs. That is the ruled floor (C4's accepted floor: a seat can tell provenance is classified with care). No hypothesis, no comparison model, no preferred outcome, no "pattern" is reachable. LEAK as on line 4.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders, and I could not break either on THIS text. Per-claim collisions constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable or non-enumerable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name; a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by an empty enumeration.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on (my own fixtures, delivered tools): (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status, the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test, rests_on USES_CHOSEN (executed: my compute run emitted USES_CHOSEN). (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (executed). (c) an imported value — the local-import branch and the external-import branch are both enforced by validate (the kit's D1 positives on THIS text; my Q2 RIGHT record validates exit 0), consumed, rests_on USES_IMPORTED (executed). Also executed: DERIVED_STANDARD_OR_MEASURED_ONLY, USES_UNDECLARED, NOT_COMPUTED from compute; all six per-claim tokens through census final (exit 0, C1_DENOMINATOR_PRINTED=PASS). The V37 addition traces cleanly too: a claim whose dependency graph is cyclic or has a missing dependency now fails validate and compute before any classification (my seven-origin sweeps below), and at C6 the matched-closure graph-integrity check fails the audit — so a broken graph cannot be smuggled into the arithmetic group, and the study files CENSUS_AUDIT_FAILED rather than a tally. Decidability is bounded mechanical classification, not a promise to decide arbitrary mathematics in 120 seconds; the cap's own outcome (REPRO_NOT_EVALUABLE / SYMBOLIC_TIMEOUT) is declared.

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed; C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate ≤ 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. Executed on THIS text with my own fixtures (/tmp/r3c2_v37_kimi/own, sources p.txt/q.txt): (1) THE WRONG-BUT-PLAUSIBLE RECORD: value "2" printed at p.txt:6 in the sentence "We adopt v = 2 from q (2020)." — filed CHOSEN/ORIG_CHOICE_STATED with that true quotation at its true line — validate returned exit 0, C3_NO_SUBSTITUTION=PASS: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (2) THE MECHANISMS THAT CATCH IT, AS WRITTEN, each executed: (i) the second seat's independent classification — filing IMPORTED/ORIG_CITATION (same coordinates) merged exit 0 with origin=CHOSEN origin_alt=IMPORTED preserved — the dispute is carried, never silently resolved, and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (ii) the C6 re-classification — against the CHOSEN-only sealed ledger, my IMPORTED auditor record produced compare exit 1, C6_AUDIT_SAMPLE=FAIL; when the second seat's IMPORTED branch was preserved in the sealed ledger, the same auditor record PASSED (exit 0). (3) THE STATED FLOOR: when my auditor repeated the same wrong code, compare returned exit 0 — the document states this floor itself rather than implying it away. The machine floor that IS enforced is real: quotation machine-matched at the cited line for every non-ORIG_SILENT record of every status, value machine-matched as a numeric token at its line, closed-list membership for STANDARD, candidate binding for every PRINTED record, and the machine floor that a record whose value line lies in another file must be IMPORTED/ORIG_CITATION whatever reason code was submitted (my WRONG record deliberately used a local value line, so that floor does not fire — the two reader-side mechanisms above are the catch, as the document says).

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no hypothesis content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below: rebuilt byte-identical, C4_PACKET_REDACTED=PASS), and my packet-first Q3 found no hypothesis. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind (census fails a row that lacks or alters those fields — kit-asserted); both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any sealed-side omission (included or excluded, either direction) files CENSUS_AUDIT_FAILED — the kit's C6_COUNTEREXAMPLE_EXHIBIT.txt (regenerated deterministically in my kit run, pin re-verified) emits exactly these cases. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script from the complete graphs merge preserves — compute REJECTS a ledger arriving with it set (executed: exit 2). The V37 graph-integrity additions introduce no channel: they are origin-independent by construction (a cycle through any origin fails), they run before root classification, and unmatched diagnostic views cannot change a verdict (the kit's complete-A/complete-B control rows PASS on the same merged ledger whose mixed auditor FAILS). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error and prior exposure, both recorded in the document rather than implied away.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on THIS master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 5d839970…" (= ACCESS_SHA, character for character) and "seat packet sha256 007fb3ce…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (both 007fb3ce9119d70fc6e6b4be2f115fbc4bf3fbf41ebfddaeff93c4d07560d6d4, 40,648 bytes), and the packet's line 9 built-from digest equals ACCESS_SHA. Negative probes on scratch copies: injecting the forbidden string "hypothesis" into seat-visible §1 produced exit 1, "C4_PACKET_REDACTED=FAIL — forbidden content survived redaction", no packet written; removing the required token C2_INPUT_LEDGER produced exit 1, "required seat-facing content did not survive redaction", no packet written. The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited; the judgement file it cites exists (8,523 bytes; metadata only). The sandbox profile's digest equals the C4 pin (6978d590…). My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4's definition names the seal-receipt case), leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (kit control and its deletion probe both ok in my run). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened). This is a judgement of the binding order in the design; no custody has been executed and none is certified here.

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary is audit-internal — "may detect an error", "shared reader error" — never an outcome label). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs emitted exactly that label over an all-DERIVED/MEASURED graph and the USES_* labels otherwise, never a clean label over a disputed graph; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (kit-asserted).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? NO — everything I checked executed as delivered. Each thing checked, and how: the master hashed first and read in full; the packet hashed and read first; R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 21/21 OK from the lane directory, both re-verified exit 0 after all runs (the kit's deterministic rewrite of C6_COUNTEREXAMPLE_EXHIBIT.txt reproduced the pinned bytes). Every named script exists beside the document and matches the digest the document prints for it: r3c2_ledger_tools.py 5424fd3b3f1c9591317243cf3bae7d862dc212e84e62eef330dbac55fa04f3f3 (§3), r3c2_lane_tools.py cb89046f014f2f249538a9c62e00fa7ffeb8d6f9d4dec9d9d7a23e8b923af828 (C3, §9), r3c2_batch_tools.py 3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e (C1), r3c2_manifest.py 6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a (C5), r3c2_timeout.py fbb9bef7d6622a17b4dc2e856791e3166b60394c187286ea5581b2f39003f331 (§9), r3c2_build_seat_packet.py 56ac7d1c78196c4d246244126b43666d804f093f8fbe4e2c2289ab885def728e (C4), r3c2_seat_sandbox.sb 6978d590bf2acc519f00f38e8bc71b0e9ef476b95eeb6afa38443dbaef731fa6 (C4), R3C2_CORPUS_MANIFEST.md 26dabe7cd94d3f2a1501c347ec1193b83528325905dc417c85e28a4a2fe84096 (§2); the kit's _STAGED copies are byte-identical to the delivered tools. C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text). The §9 wrapper: a fast child returned 0, a failing child returned 7, a two-second sleep under a 0.2-second cap returned 124 with SYMBOLIC_TIMEOUT printed. The corpus: all 89 enumerable and 79 RAW files verified by digest and byte count, metadata only. The partition regenerated byte-identical (39b8d9bf…). compute without the candidate argument exits 2 and writes no output (executed); compute rejects a ledger arriving with rests_on preset (exit 2, executed). The interpretation protocol and the leak-floor judgement exist at their named paths (metadata only). One description inaccuracy — not an executability failure — is the cosmetic finding below.

## Q7 (V37) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  PACKET_REBUILD=BYTE_IDENTICAL (40,648 bytes, 51 spans, C4_PACKET_REDACTED=PASS)
  BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT (forbidden string; required token removed)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=21/21 OK (before and after the kit run)
  TOOL_PINS_MATCH_MASTER=YES (ledger 5424fd3b…, lane cb89046f…, batch 3822f12c…, manifest 6b6e4bce…, timeout fbb9bef7…, builder 56ac7d1c…, sandbox 6978d590…, corpus 26dabe7c…)
  STAGED_EQ_DELIVERED=5/5 byte-identical
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only)
  PARTITION_REGENERATED=byte-identical (39b8d9bf…)
  KIT_RUN: deletion_probes=65 / controls=201 passed=201 failed=0 / STAGED_TESTS=PASS (exit 0)
  MOI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; 20 constructions; printed runs per construction: 2 seat orders x seeds 0,1,2 + reversed arrival + reversed rederivation order + reversed dependency order (both seat orders) + reversed auditor dependency order — 11 runs)
  Q1_COMPUTE=DERIVED_STANDARD_OR_MEASURED_ONLY,USES_CHOSEN,USES_FITTED,USES_IMPORTED,USES_UNDECLARED,NOT_COMPUTED (each rc=0)
  SIX_PER_CLAIM_TOKENS_CENSUS_FINAL=PASS (exit 0)
  Q2_MECHANISM: WRONG_CHOSEN_ON_CITATION validate exit 0 PASS / RIGHT_IMPORTED validate exit 0 / merge exit 0 origin=CHOSEN origin_alt=IMPORTED preserved / auditor corrected vs CHOSEN-only sealed exit 1 FAIL / vs alt-preserved exit 0 PASS / auditor repeats the wrong code exit 0 PASS (the stated floor)
  F1_DEPENDENCY_ORDER_INVARIANT=YES (reversed derived_from in both seats: merged bytes, ledger digest, compute exit+stdout and complete audit artefact all equal across AB / AB-reversed / BA-reversed; audit rc=0)
  F2_CYCLES_VALIDATE_RC1=7/7 origins (CHOSEN,FITTED,IMPORTED,MEASURED,STANDARD,UNDECLARED,DERIVED; each with the "graph integrity: cycle at …" diagnostic)
  F2_CYCLES_COMPUTE_RC1=7/7 origins (each with "FAIL: provenance graph 0: cycle at …"; no output written)
  F2_MISSING_VALIDATE_RC1=7/7 origins
  F2_MISSING_COMPUTE_RC1=7/7 origins
  F2_COMPUTE_REJECTS_PRESET_RESTSON=exit 2; COMPUTE_NO_CANDIDATES=exit 2, no output
  C6_CYCLES_AUDIT_RC1=7/7 origins, graph-integrity diagnostic on BOTH the auditor and the matched sealed closure
  C6_MISSING_AUDIT_RC1=7/7 origins, graph-integrity diagnostic on BOTH closures
  OWN_ORDERING_SOURCES_BEYOND_(a)-(f): LC_ALL=C — invariant; TZ=Pacific/Auckland — invariant; sealed ledger records-array order — artefact identical except sealed_ledger_sha256 (the digest of what was delivered, an identity); candidate-array order — census stdout identical, audit artefact identical except sealed_candidates_sha256/stage1_seal (the same identity kind); inner member order of auditor rederivation records — identical except rederivation_seal (the one exception §3 names); 8 random PYTHONHASHSEEDs — merged bytes and compute byte-identical
  C5_LIVE=[0,0,0,0,0] Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1…
  WRAPPER=[0,7,124] (SYMBOLIC_TIMEOUT printed under a 0.2 s cap)
  MANIFEST_WALK_ORDER_INVARIANT=YES
  ROOTS_ALT_IN_DELIVERED_LANE_TOOLS=0 occurrences
  EXACT_FAILURE_RULE_OCCURRENCES: master=1 (§3), kit=1 (the head block; referenced by name thereafter, never restated)
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only
  LEAK_FLOOR_JUDGEMENT=exists, 8,523 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline; the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the kit's 65 deletion probes each turn a negative into PASS only when its marked check is neutralised (or still FAIL on an overlapping authoritative guard), so no check is decorative; every exit code cited in this report was captured from the tool itself before any display filtering.

(c) PRIOR_FINDINGS_CLOSED=YES over the V36 gate's findings, each with my own evidence:

  - codex F1 (reversing a `derived_from` list changed merged bytes, the ledger digest, compute output and the audit artefact while every verdict PASSED) — CLOSED. Text: §3's MOI clause now names dependency-list order and orders canonicalisation at ONE place ("Before computing canonical record keys or graph digests, selecting branches, deduplicating graphs or serialising merged output, `merge` normalises every `derived_from` list into sorted input-id order in both complete seat graphs and every retained primary or alternative record"), and source (f) is enumerated. Code, diff-verified against the V36 bytecopy: merge sorts every derived_from and derived_from_alt list at its head, before keys/digests/branch selection/dedup/serialisation — additive change, no guard touched. Executed: reversed dependency lists in both seat ledgers under both seat orders give identical merged bytes, digest, compute output and complete audit artefact on the delivered tools; the kit's V36-bytecopy fail-first row shows the same construction DID differ on the V36 bytes (merged-bytes-equal=False, the differing runs being the dependency-order runs) — reproduced in my kit run.
  - codex F2 (a CHOSEN record whose `derived_from` named itself passed `validate` and was classified USES_CHOSEN by `compute`) — CLOSED. Text: C3 now runs origin-independent graph integrity at BOTH boundaries before root classification / before compute writes output, with "Stopping root traversal at a non-DERIVED origin never substitutes for either integrity check." Code, diff-verified: validate calls graph_integrity over the complete input graph (cycles), with the per-record missing-dependency check itself origin-independent; compute checks every complete provenance graph and fails before writing output — both additive. Executed: a self-cycle and a missing dependency through EACH of the seven origins fail validate (exit 1, "graph integrity: cycle at …" for cycles) and compute (exit 1, "provenance graph 0: …", no output written); the kit's two deletion probes demonstrate each guard is load-bearing (deleting the validate guard lets the self-cycle PASS; deleting the compute guard classifies USES_CHOSEN — both probes ok). C6's independent check is retained and was exercised on THIS text: matched cycles and missing dependencies through every origin fail the audit with the diagnostic on both closures.
  - codex F3 (the kit's fail-first helper decided a negative case by bare polarity, crediting ANY failure) — CLOSED, tested as the principle, not the instance, in both directions. The rule is stated ONCE in the operative text (§3, one occurrence) and ONCE at the head of the kit (one occurrence; referenced by name thereafter). It is applied wherever a control judges an outcome: the kit's check() requires the exact failure set (count and text, each substring exactly once) plus exit code and token; the deletion-probe judge carries the same setup guard; moi_judge requires setup, completion line, no MERGE_FAIL, exit code, token, exactly the expected rows, and each subcase's own verdicts/diagnostics/equality results. Three meta-controls hold the predicate itself (unrelated launch failure rejected; a REAL failure of a DIFFERENT subcase rejected; the right subcase failing for the WRONG stated reason rejected) — all ok in my kit run. My independent subversions, each judged by the kit's own moi_judge exec'd from its delivered source: S1 emptied tools directory — the retired polarity predicate credits it (True), the delivered judge rejects it ("a construction's merge did not run (MERGE_FAIL)"); S2 a corrupted lane tool in a copied bytecopy — retired credits, delivered rejects (MERGE_FAIL); S3 the V35-F1 spec pointed at the WRONG bytes (the V36 bytecopy, where the defect is fixed — the construction PASSES) — delivered rejects ("exit 0 != expected 1"); S4 an empty construction selection (--only matches nothing; SPI=PASS, exit 0, zero rows — the retired predicate would credit a POSITIVE control vacuously) — delivered rejects (exit-code and empty-selection guards). No control passed under any subversion. S5 (a probed tool crashing with a SyntaxError) exposed the one literal gap recorded as finding 2 below — with no passability effect, the token/exit-code conjuncts reject the case. No production guard was weakened to make a probe pass: the V36→V37 diffs of both tools are purely additive plus the dead-code removal, and the probe mechanism neutralises checks only in scratch copies under the kit's work directory, never in the delivered tools (pin sheets re-verified after the kit run).
  - codex C1 and kimi's cosmetic (the same false filesystem sentence) — CLOSED. §3 source (e) now reads "filesystem listing order — `r3c2_manifest.py` walks a directory and sorts subdirectory names, filenames and final digest rows; this source has a delivered instance whose ordering is already normalised". Verified against the delivered tool (os.walk with sorted dirnames/filenames; the digest rows sorted before hashing) and by my walk-order probe (two builds with reversed creation order print the same MANIFEST_SHA256).
  - kimi observation (i) (§10.31 named three V35-bytes control rows; the kit carried two) — CLOSED. The kit now carries the third (the same-origin-search control row on the V35 bytes), ok in my kit run; the correction is recorded in §10.32 rather than by rewriting §10.31, as stated.
  - kimi observation (ii) (the lane tool's dead `roots_alt` helper) — CLOSED. `roots_alt` occurs 0 times in the delivered lane tool.

(d) NEW executable defects: NONE at the non-cosmetic level. Every construction behaved as the V37 text claims, including constructions derived from ordering sources NOT in the enumerated list (LC_ALL; TZ; the sealed ledger's records-array order; the candidate file's array order; inner JSON member order of rederivation records; random PYTHONHASHSEEDs) and graph-integrity attacks through every origin at every boundary. Two items, both COSMETIC (each changes no class filed, no control's passability, no reachability, no conclusion and no tested value):

FINDING 1 — COSMETIC. Verbatim sentence, §3 (lane-side MOI paragraph):
> every construction is run under both seat orders and PYTHONHASHSEED 0, 1 and 2 (0 and 1 reproduce the V35 diagnostic variation on the V35 bytes), plus one run with both seats' record lists reversed and one with the auditor's rederivation file in reversed member order, and the whole outcome must be identical across all of them

Defect: the enumeration describes 8 runs per construction, but the delivered combined property runs 11 — it adds the two runs with every `derived_from` list reversed in both seat ledgers (one per seat order) and the one run with every `derived_from` list reversed in the auditor's reconstruction, the three runs V37 added and that source (f) in the same paragraph promises is "exercised in the combined property". The sentence as written is true of the runs it names but omits the dependency-order runs from the property it describes; a reader rebuilding the exhibition from the text would construct 8 runs, not the 11 the kit executes and prints. The delivered property is a strict superset, so nothing becomes passable and no tested value weakens — COSMETIC.
Exact replacement:
> every construction is run under both seat orders and PYTHONHASHSEED 0, 1 and 2 (0 and 1 reproduce the V35 diagnostic variation on the V35 bytes), plus one run with both seats' record lists reversed, one with the auditor's rederivation file in reversed member order, two with every `derived_from` list reversed in both seat ledgers (one per seat order) and one with every `derived_from` list reversed in the auditor's reconstruction, and the whole outcome must be identical across all of them

FINDING 2 — COSMETIC. Verbatim sentence, §3 (the exact-failure rule):
> Each test requires successful fixture setup, the exact expected exhibition exit code and completion token, exactly the selected construction rows, and that subcase's expected verdicts, ordered diagnostics and equality results. … A launch error, traceback, missing artefact, empty construction selection, a construction whose merge did not run, or an unrelated failure fails the kit test.

Defect: the delivered setup guard (the kit's SETUP_ERRORS literal list) catches a traceback, module/import errors, missing files, permission errors, can't-open-file and usage banners, and requires non-empty output — but a SyntaxError crash of a probed tool prints none of those literals and passes the guard (executed: a syntax-broken copy of the ledger tool exits 1 printing "SyntaxError: invalid syntax"; the kit's own setup_ok returns "" over that output). The document's "launch error" phrase is broader than the delivered literal list. The same case is still rejected by the remaining conjuncts — the expected completion token is absent and the exit code mismatches — so no control's passability, no class, no reachability, no conclusion and no tested value changes: COSMETIC.
Exact replacement (one word in the kit's SETUP_ERRORS list; the document text needs no change):
> SETUP_ERRORS=("Traceback (most recent call last)","ModuleNotFoundError","ImportError:","SyntaxError","No such file or directory","command not found","Permission denied","can\'t open file","usage: ")

Why SOUND_WITH_REPAIRS and not SOUND: the two cosmetic items above are repair requests, however small. Why not UNSOUND: Q1 and Q2 answer YES on executed evidence; the blind is machine-asserted and reproduced byte-for-byte on this master; the seal binds each artefact before the step it must precede; every negative outcome is worded within its definition; all four V36 findings, the one shared cosmetic and both kimi observations are closed above with my own fixtures (dependency-order invariance holds on the delivered tools and fails on the pinned V36 bytes; cycles and missing dependencies through every origin fail at validate, compute and C6; the exact-failure predicate rejects every subversion I constructed, enumerated or not); and nothing the document describes failed to execute as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 5d839970… = ACCESS_SHA; packet 007fb3ce… = PACKET_SHA; seat sheet 8/8 OK; kit sheet 21/21 OK). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v37_kimi, not corpus data and not a census. Evidence scripts retained: own_checks.py, own_checks2.py, own_checks3.py, own_checks4.py, own_checks5.py, subvert.py, misc_checks.py, misc_checks2.py, diffart.py.

R3C2_V37_GATE_COMPLETE
