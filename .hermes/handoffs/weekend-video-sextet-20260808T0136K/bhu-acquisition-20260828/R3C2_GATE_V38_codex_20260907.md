ACCESS_SHA=8095c18c8f29901b6a4ebe83a07b78dcab5955d440c25b4f19a9f0d713ba8ac7
PACKET_SHA=d18589006489339559945b77762cbcc8dc9937f6772d0b1ed48df63a8548b498
GATE=PREREG_UNSOUND
LEAK=Provenance and reproducibility matter operationally; no directional hypothesis, comparison-model preference or hoped-for empirical pattern reconstructed.
CONSEQUENCE_VISIBLE=Procedural: CENSUS_ORIGIN_DISPUTED stops the study — “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.”
ORIGIN_PURPOSE=Independent classification and audit — “Every input's `origin` is classified independently by both seats.” Disagreement can stop the study; no substantive interpretation mapping is disclosed.
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=NO
PRIOR_FINDINGS_CLOSED=NO

Design-only gate of the delivered Version 38, unsigned. The mandatory master hash command ran first. Packet-only answers were recorded before reading the master; the master was then read in full, followed by the two expressly permitted V37 reports. Section 10 supplies no inherited verdict. Q7's later executable instructions were treated as the narrow authorization for necessary kit, tool and synthetic-fixture access. No other gate report or interpretation-protocol content was opened. No census, adoption, signature or custody event occurred.

Four non-cosmetic findings remain: alternative-branch search order flips C6; JSON type differences escape search comparison; the promised common exact-result judge is not implemented across judges; and two operative tool hashes disagree with delivered bytes. The unmodified kit passed 206/206 controls and 65 deletion probes despite these counterexamples.

## 1. OUTCOME CLASSES

Packet-only answers, preserved in the required order:

**Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone?**

Yes, procedurally. Under CENSUS_ORIGIN_DISPUTED: “The census does not proceed; every disputed input is listed with both seats' classification and both quotations.” Audit failure also says “No tally is filed; report which.” These affect continuation and reporting. No sentence establishes a preferred scientific result or an outcome that changes the lane's standing. “No tier, warrant token, standing or stamp moves” denies movement on those named axes.

**Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it?**

Yes for checking. “Every input's `origin` is classified independently by both seats.” C6 further says “it recomputes each input's chain of origins to its roots from the auditor's own graph and from the sealed ledger under the matching branch and prints the comparison.” Origins are compared and audited; disagreement can stop the study. I cannot state a downstream scientific interpretation, preferred category or conclusion mapping from the packet.

**Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW?**

The packet supports these limited inferences:

- Reproducibility is the central measurement: “does the paper's own number follow from the paper's own recipe applied to the inputs it states.” No expected success rate is supplied.
- Provenance receives separate scrutiny: “The reproduction verdict and the provenance fields are recorded separately.” Its detailed taxonomy and audit reveal interest, without revealing a favored origin.
- The subject includes cosmology: “The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline.” This identifies subject matter, not a comparison-model preference.
- Author-set inputs are a separately wanted statistic: “`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator.” No desired count is supplied.
- A broader standing system is contemplated by “No tier, warrant token, standing or stamp moves.” This does not identify a directional stake.

The quoted material supplies no hypothesis, preferred empirical outcome, comparison model to defeat, or specific hoped-for pattern. The operational importance of provenance is visible; its substantive ranking is not.

**Q1. DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE AND KEEP THE REPRODUCTION QUESTION DECIDABLE?**

YES as bounded filing rules. This is not a promise to decide arbitrary mathematics within 120 seconds. Option (c) keeps status-based arithmetic separate from provenance, and explicitly files inability to evaluate. The implementation findings below do not require reopening that definition.

Concrete physical-claim witnesses follow. These are synthetic referee constructions, not attributed corpus findings:

| Claim/condition | Unique per-claim outcome | `rests_on` |
|---|---|---|
| “We choose a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_CHOSEN |
| Same input/recipe, but L printed as 5 m | REPRO_FAILED | USES_CHOSEN |
| “Fitting gives a = 2 m; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_FITTED |
| “We adopt a = 2 m from X; L = 2a = 4 m.” | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Unprinted a found at the first qualifying line of verified enumerable X | REPRO_WITHIN_STATED_PRECISION | USES_IMPORTED |
| Unprinted a cited only to an unpinned source | REPRO_BLOCKED | USES_IMPORTED |
| L = 2a, with a unprinted and uncited; adequate silent search recorded | REPRO_INPUT_ABSENT | USES_UNDECLARED |
| Specified computation exceeds the cap with a measured input | REPRO_NOT_EVALUABLE | DERIVED_STANDARD_OR_MEASURED_ONLY |
| Own numerical result with no producing procedure and no ledger record | REPRO_NO_DERIVATION_STATED | NOT_COMPUTED |

The chosen input numeral is itself an AUTHOR_SPECIFIED_INPUT exclusion, but its downstream result is included and attempted. A fitted parameter asserted as the paper's own result is itself a candidate; a merely named fitting procedure can give that candidate NO_DERIVATION_STATED while its printed value remains consumable downstream. An imported value's provenance is the claiming paper's import, regardless of how the external source obtained it.

Executable checks accepted all six outcome tokens through `census … final`, and `compute` emitted the five substantive labels above plus NOT_COMPUTED. These check encoding and classification, not scientific correctness of a corpus calculation.

Hard overlap: L = a + b, where a is unprinted but cited to an unpinned source and b is unprinted and uncited. BLOCKED and INPUT_ABSENT both apply before precedence; the declared order files BLOCKED only. I constructed no double filing after precedence.

Study-level witnesses: nonempty all-arithmetic tally plus audit PASS gives COMPLETE; one absent-input claim plus audit PASS gives PARTIAL; missing seed gives AUDIT_FAILED; a control failing twice in every attempting seat gives NO_CLASS; surviving inclusion/input-list disagreement gives DENOMINATOR_DISPUTED; surviving outcome disagreement gives OUTCOME_DISPUTED; origin disagreement affecting 2 of 10 claims gives ORIGIN_DISPUTED; surviving fail/pass control split gives CONTROL_SPLIT. A partial tally with a missing seed files AUDIT_FAILED by precedence. INCONCLUSIVE is reachable through PARTIAL with a successful audit. Entirely empty enumerations can also reach PARTIAL; zero included claims with candidate passages instead triggers the explicit audit-failure rule. These witnesses do not replace independent two-seat C0.

## 2. CONTROLS

The named three-valued controls are C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK and C6_AUDIT_SAMPLE. Their declared artefacts are exhibitions, printed runs, ledgers, seals, audit JSON, path lists and provider logs. Bare tokens do not satisfy printed-run predicates. C4/C5b deliberately accept self-reported path lists without certifying completeness. Unreached controls are NOT_RUN, including validation before batch integration.

**Q2. IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED?**

Yes as evidence-backed human classification, not automatic semantic verification. My wrong-but-plausible source-backed record was:

```json
{
  "claim_id": "c1",
  "input_id": "i1",
  "symbol": "x",
  "status": "PRINTED",
  "origin": "CHOSEN",
  "origin_evidence": {
    "reason_code": "ORIG_CHOICE_STATED",
    "source_file": "p.txt",
    "source_line": 2,
    "verbatim": "We adopt x = 2 from X."
  },
  "derived_from": [],
  "value": "2",
  "source_file": "p.txt",
  "source_line": 1
}
```

Synthetic p.txt line 1 prints `x = 2.` and line 2 contains exactly that quotation. The candidate file binds c1 to p.txt. `validate` returned 0: a true quotation and correctly paired code/origin do not establish semantic correctness of CHOSEN. Citation precedence requires IMPORTED. A correct second reader exposes the dispute; C6 with a corrected IMPORTED auditor against the CHOSEN-only sealed record returned 1. Repeating the wrong origin in the auditor returned 0. Shared reader error therefore survives, exactly as the operative text acknowledges. This is a demonstrated limitation, not a new design defect.

**F3-continued — NON-COSMETIC: the common exact-result evaluator still does not govern every judge, and its completion test is not exact.**

Verbatim operative sentences:

> Every outcome-judging helper in the kit uses one common exact-result evaluator: it rejects empty output and setup, launch, import or traceback failures; requires the subcommand's completion token and exact exit code; and compares the complete ordered diagnostic rows and subcase evidence with their declared expectations, not substrings.

> Deletion probes additionally require the targeted diagnostic to be absent; when an overlapping guard remains, they require its exact retained failure result.

Inspection and execution contradict both assurances:

1. `check` delegates to `judge`, but `probe` and `moi_judge` implement separate predicates. Neither calls `judge`. Several direct result assertions also bypass it.
2. `judge` accepts any line matching its generic TOKEN_LINE regex. For an actual pinned graph-cycle control, the exact pinned failure row followed only by `UNRELATED=PASS` passed. Replacing that line with `INVOCATION=REJECTED` also passed. A subcommand-specific completion token is not required when the optional token argument is absent. A separate `ERROR=unrelated runtime failure` row was ignored because only `FAIL:` rows enter the pinned comparison.
3. `moi_judge` still uses substrings for its token and row evidence. It accepted `BAD case verdicts=[0] unrelated diagnostics` followed by `runs per construction: aborted; SPI=FAIL`: no exact SPI completion line, and no complete diagnostic-row equality.
4. The deletion judge checks that pinned old diagnostics disappear, but does not compare the complete remaining result to pinned expectations. I used the actual named gate-2 F2 edge probe and its unchanged pinned vanished rows. A real subprocess emitted those rows unprobed; after deletion it emitted `FAIL: unrelated launch condition; record matches neither complete declared branch is NOT the cause`, followed by `UNRELATED=FAIL`, exiting 1. The delivered probe accepted it because the requested substring remained, the exit code matched, and the old rows vanished. This is acceptance of the wrong stated reason by a named delivered probe predicate, using a synthetic tool fixture, not a claim that the normal production tool emits that text.

Observed tokens:

```text
GENERAL_UNRELATED_TOKEN=True
GENERAL_REJECTED_INVOCATION=True
GENERAL_ERROR_EXTRA=True
MOI_SUBSTRING_COMPLETION=True
NAMED_EDGE_PROBE_WRONG_REASON=True
```

The original real-traceback attack is now rejected. Strict `judge` rejects a changed FAIL-row suffix and an extra FAIL row. The relocated-marker meta-control also passes. Those repairs are real but do not establish the universal claim.

Exact replacement for the two quoted sentences:

> Every outcome-judging helper, including check, probe and moi_judge, calls one exact-result evaluator with a mandatory control specification: expected exit code, exact subcommand completion line, complete ordered diagnostics and required artefact evidence. A generic token, an embedded token substring, INVOCATION=REJECTED outside an explicitly specified argument-rejection control, an unexpected ERROR row, or an unrelated failure cannot satisfy that specification. A deletion probe evaluates both its unprobed and probed results through this evaluator, requires its pinned target diagnostics or artefact evidence to disappear, and requires the complete remaining result to equal its separately pinned post-deletion expectation.

Implement the shared call path and specifications; changing prose alone cannot close this finding.

**Pinned-table recapture attack, part of F3.** The kit README says, verbatim:

> Regenerate deliberately with `NM_KIT_CAPTURE=1`, never to make a failing control pass.

This plainly prohibits the misuse, but does not prevent it. In capture mode `judge` skips exact-row comparison and records observed rows after substring checks. I took a named pinned cycle control, appended `WRONG_REASON` to its diagnostic, and observed strict rejection, capture acceptance, then strict acceptance after using the captured rows. No tool repair occurred. The table is pinned externally, so re-pinning is detectable; this is not an undetectable cryptographic bypass. Nevertheless the evaluator allows recapture to redefine a failing expectation and can still present a passing result.

Exact replacement:

> Expectation capture is a non-certifying maintenance operation: it writes a proposed table to a separate path, never replaces the pinned table, and never emits STAGED_TESTS=PASS. Every proposed expectation change requires an explicit old/new diagnostic diff and a reason tied to the declared control predicate; a strict run against the separately reviewed and newly pinned table is the only certifying mode.

This is non-cosmetic because it changes what may count as passing control evidence. Preserve the guard that each declared expectation must appear, but do not treat that substring as sufficient justification for a new full row.

## 3. CIRCULARITY

The inclusion rule contains no hypothesis or comparison preference. Independent seat enumeration, retained exclusions and a third independent full-corpus enumeration constrain selective omission. An auditor-only passage is an omission whether the auditor includes or excludes it. A contrary claim cannot legitimately disappear quietly, although shared omission or shared classification error remains possible. Differences within C6's stated dispute tolerance are reported, not hidden.

The packet withholds the provenance severity ordering and interpretation mapping. Receipt P precedes limb A. These constrain expectation reaching evidence; they do not prove absence of prior beliefs. I found no additional directional leak. The counterexamples below demonstrate inconsistent or insufficient evidence comparison, not a demonstrated directional scientific bias.

## 4. THE BLIND

The delivered builder rebuilt THIS master byte-identically: packet 40,648 bytes; 51 redacted spans; sections 1–6, 9 and 11 retained. A visible `hypothesis` injection and removal of required C2_INPUT_LEDGER content each returned 1 and wrote no packet. The packet's embedded master hash matches ACCESS_SHA.

```text
C4_PACKET_REDACTED=PASS
PACKET_REBUILD_BYTE_IDENTICAL=True
BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT
```

This verifies the finite forbidden/required-string mechanism, not universal semantic blindness. The procedural consequences and checking purpose identified before reading the master remain visible, as the operative text admits.

The sandbox profile matches its pin. Live probes from the temporary working directory allowed a packet read and SymPy import, and refused the lane-master read with “Operation not permitted.” The profile's runtime/configuration/network exceptions, incomplete self-report and possible prior exposure are acknowledged. I certify only those tested accesses, not universal confinement or an actual dispatch.

## 5. THE SEAL

Receipt P binds the protocol hash and commit before limb A. Receipt T binds the named tally files and commit before the protocol is opened. Independent re-hashing must verify the four values and print both receipts. Missing receipts or mismatches file CENSUS_AUDIT_FAILED and leave interpretation NOT_RUN.

This is adequate record binding at the specified stages, conditional on executing the procedure. It cannot establish that the owner had not guessed the answer or previously knew the protocol. The protocol exists at the named path, 3,947 bytes, checked by metadata only. Its contents and actual receipts were not inspected. No custody event is certified.

## 6. FAIRNESS

REPRO_FAILED explicitly requires “unreproduced from the stated inputs,” not “error.” The delivered brief applies that wording to negative outcomes generally. Missing-input, blocked-source, missing-procedure and computational-limit classes do not assert a scientific error.

REPRO_WITHIN_STATED_PRECISION matches its rounding/uncertainty test. DERIVED_STANDARD_OR_MEASURED_ONLY describes its membership. CENSUS_COMPLETE denotes complete arithmetic classification with audit PASS, including arithmetic failures; it does not mean every claim reproduced. No outcome-label repair is required.

## 7. STALL / EXECUTABILITY

**Q4. CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED?**

YES. F3's universal exact-result assurance is false. N1/N2 below break the promised search-comparison contract, and N3 makes the operative pins inconsistent with delivered tools. I found no missing current operative deliverable among those checked. Future ownership extracts, session artefacts, provider logs and receipts are prerequisites/outputs, not claimed completed work.

**N1 — NON-COSMETIC: auditor-only search permutation still flips C6 on an alternative branch.**

Verbatim operative sentence:

> `merge` applies it before canonical keys, graph digests, branch selection and serialisation, and C6 applies it to both reconstructed and sealed search evidence before branch matching and comparison.

`merge` normalizes both searches before keys and serialization. However, `compare_record` uses `canon_search` for the primary branch and raw Python equality for the alternative branch (`r3c2_ledger_tools.py`, line 417). The later canonicalized diagnostic comparison cannot repair a failed authoritative branch match.

Construction: one seat supplies CHOSEN, the other UNDECLARED with the search below. The canonical merged primary is CHOSEN and its alternative is UNDECLARED. The auditor independently supplies the complete silent alternative:

```json
{
  "query": ["x", "choose", "adopt", "fit"],
  "files": ["p.txt", "q.txt"],
  "matches": ["p.txt:6", "q.txt:1"]
}
```

C6 passes. Reverse only the auditor's `files` and `matches`, leaving query and content unchanged, and seal that reconstruction correctly: C6 fails.

```text
ALT_SEARCH_BASE_EXIT=0
ALT_SEARCH_REVERSED_EXIT=1
FAIL: AUDIT c1: MISMATCH (input i1: record matches neither complete declared branch; origin_search differs (structural comparison against the matched branch's search); root_origins differ: audit ['UNDECLARED'] vs sealed ['CHOSEN'])
```

This tests the C6 boundary directly. The fixture is not a whole-study filing; additional undisputed claims can place the one provenance dispute within the study's 10% allowance. The delivered exhibition's silent-alternative case has singleton inventories, so reversing them cannot expose this failure. Its multi-entry cases use the primary branch.

Exact replacement:

> Before either primary or alternative branch matching, C6 computes a canonical search representation for the auditor and for each candidate sealed branch and uses that same representation for branch selection and diagnostic comparison. A multi-entry silent alternative must pass under auditor-only file and match permutations, with identical complete audit outcomes apart from the permitted reconstruction-byte seal.

Implement it in the alternative predicate and add the combined branch-plus-order construction. The old primary-only N1 witness is repaired; the class of failure is not closed.

**N2 — NON-COSMETIC: a genuine search-entry type difference compares equal.**

Verbatim operative sentence:

> Reordering an inventory alone must preserve the complete outcome; adding, removing or changing an entry must still fail, and changed-entry, added-entry and deleted-entry negatives are carried in the kit to prove that canonicalisation erases no evidence difference.

Both source-backed searches below pass `validate`'s schema. With the first sealed and the second reconstructed, C6 returns 0:

```json
{"query":["x","choose","adopt","fit"],"files":["p.txt","q.txt"],"matches":[{"line":1}]}
{"query":["x","choose","adopt","fit"],"files":["p.txt","q.txt"],"matches":[{"line":true}]}
```

Nested `{"hit":{"line":1}}` versus `{"hit":{"line":true}}` also passes. Python container equality equates `1` and `True`. Sorting retains their different JSON representations; the comparison, not sorting, erases the difference. The same untyped equality is used for merge's search-difference decision.

Direct attack results:

| Difference | C6 exit | Assessment |
|---|---:|---|
| Number 1 → boolean true | 0 | False match |
| Nested number 1 → boolean true | 0 | False match |
| Add a duplicate entry | 1 | Correct rejection; multiplicity retained |
| Reverse an inner list | 1 | Correct rejection; inner sequence retained |
| Add whitespace inside a string | 1 | Correct rejection; string content retained |
| Change, add, or delete an ordinary string entry | 1 each | Correct rejection |

I found no different valid JSON inventories that produce identical canonical serialized sequences merely by sorting; object-key ordering is intentionally immaterial. The demonstrated defect is that different canonical sequences nevertheless compare equal under Python's cross-type equality.

Exact replacement:

> Search equality is equality of a type-preserving canonical JSON serialization: preserve query and every entry's content, nesting and multiplicity, sort only the top-level files and matches inventories by canonical JSON, and compare the resulting serialized representations at merge and at both C6 branch predicates. Boolean and numeric values are distinct. Non-JSON values are rejected. Tests include nested boolean-versus-number differences, duplicates, inner-list order and string whitespace, as well as ordinary changed, added and deleted entries.

Implement the representation comparison consistently; retaining sorted objects and comparing them with Python `==` does not satisfy it.

**N3 — NON-COSMETIC: operative batch and manifest hashes are stale.**

Verbatim C1 sentence:

> Each session writes `candidates_b<k>.json`, `exclusions_b<k>.json`, `ledger_b<k>.json` and `SEAT_REPORT_b<k>.md`, sealed by the custodian in batch order before the next batch is dispatched, in a LIMB-A chain; the lane's pinned `join` (pure function, no renaming; `r3c2_batch_tools.py`, sha256 `3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e`; partition `39b8d9bf199265ab…`) produces the seat's one candidate file, one exclusion file and one ledger, over which `census` and `validate` run, and §6's two-seat agreement on limb A is obtained before any arithmetic.

Verbatim C5 sentence:

> Execute and print, in order: (1) `/usr/bin/python3 -E --version`; (2) `/usr/bin/python3 -E -c "import sympy;
>   print(sympy.__version__)"`; (3) `/usr/bin/shasum -a 256 /usr/bin/python3`; (4) `/usr/bin/python3 -E -c "import site; print(site.getusersitepackages())"`;
>   (5) `/usr/bin/python3 -E r3c2_manifest.py <the directory (4) printed>` — `r3c2_manifest.py` is committed beside this document, sha256 `6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a`, delivered in the seat's working directory and pinned in `R3C2_SEAT_PACKET.sha256`; it is ONE process that walks
>   the directory itself, reads every file, prints `FILES=<n>` and `MANIFEST_SHA256=<digest>`, and on any unreadable file prints
>   `ERROR=<path>` and exits 1 — nothing is skipped silently.

Actual delivered hashes, agreeing with both current pin sheets:

```text
r3c2_batch_tools.py=e078e05cb0d4b2c055f2087d79a5280a08e5c7b17494bcab8b067e0fc17c0b74
r3c2_manifest.py=f3854f8b1a4be978ec7a0cfb764ed73c2803747808ab5cda6fddc140b6e04ec0
```

The packet repeats the stale operative hashes. A seat cannot verify delivered bytes against both representations of the pin. Successful execution of current files does not validate the other hashes. This changes a tested integrity value and is not COSMETIC.

Exact replacements: in the quoted C1 sentence replace only `3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e` with `e078e05cb0d4b2c055f2087d79a5280a08e5c7b17494bcab8b067e0fc17c0b74`; in the quoted C5 sentence replace only `6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a` with `f3854f8b1a4be978ec7a0cfb764ed73c2803747808ab5cda6fddc140b6e04ec0`. Rebuild and re-pin the packet after the master amendment; preserve historical execution records.

Q4 verification inventory and method:

- Master and packet: hashed, fully read, packet-first order preserved; current packet rebuilt byte-identically, two builder negatives executed.
- Seat brief and pin sheets: read; seat sheet 8/8 and kit sheet 24/24 verified. Five staged production tools match delivered counterparts. Direct master-pin comparison exposed N3.
- `r3c2_ledger_tools.py`: inspected validation, schema, graph checks, branch comparison and dispatch; executed census/final, validate, and all five audit subcommands through isolated kit and own fixtures. All six outcome encodings accepted; wrong-origin and corrected-auditor paths tested.
- `r3c2_lane_tools.py`: inspected normalization, graph preservation, merge/compute and dispatch; executed all provenance labels, empty-ledger behavior, both search counterexamples and all-origin integrity tests. Invalid graph computations wrote no output.
- `r3c2_batch_tools.py`: inspected partition/seal/join/coverage; regenerated the real 12-batch partition byte-identically. Isolated kit exercised limb-A/B chains, binding, coverage, ownership, digest and graph negatives.
- Corpus manifest: read its metadata; all 89 enumerable texts verified by raw-byte digest and byte count from its declared source directory. No scientific census or derivation performed. RAW files are outside enumeration and were not independently verified.
- `r3c2_build_seat_packet.py`: read and executed this-master rebuild and negative mutations. The older integrated candidate was copied only as the kit's explicitly required build fixture, not judged as the current design.
- `r3c2_manifest.py`: read and ran C5 live over the printed user-site directory; kit exercised root/symlink refusals. Its code sorts directory names, filenames and final rows.
- `r3c2_timeout.py`: read and executed success, exit-7 and timeout children, yielding 0, 7 and 124 respectively.
- `r3c2_spi_exhibition.py`, `r3c2_staged_tests.py`, `r3c2_exact_rows.json`, README and pinned V32–V37 byte copies: inspected relevant construction/judge logic and executed the full isolated kit. The delivered property ran 26 constructions with twelve runs each. Its predecessor and eight meta-controls passed; independent counterexamples still broke the universal guarantees.
- `r3c2_seat_sandbox.sb`: read, hash checked and live source/master/SymPy probes run. Interpretation protocol and leak-floor memorandum checked for existence/size only. Actual dispatch inventories, provider logs and receipts are not certified.

An initial support-script corpus lookup used the lane root instead of the manifest's declared source directory and failed. I corrected that referee script and reran it; the failed lookup is not a missing-deliverable finding or passing evidence.

Q7(a–b), tokens from THIS package:

```text
PACKET_REBUILD=BYTE_IDENTICAL
BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT
SEAT_PIN_SHEET=8/8_OK
KIT_PIN_SHEET=24/24_OK
OPERATIVE_TOOL_PIN_MISMATCHES=2
CORPUS_ENUMERABLE_BYTES=89/89_VERIFIED
PARTITION_REBUILD=BYTE_IDENTICAL
SIX_PER_CLAIM_TOKENS_FINAL_CENSUS=PASS
Q1_COMPUTE=USES_CHOSEN,USES_FITTED,USES_IMPORTED,USES_UNDECLARED,DERIVED_STANDARD_OR_MEASURED_ONLY,NOT_COMPUTED
Q2_WRONG_ORIGIN_VALIDATE_EXIT=0
Q2_SHARED_WRONG_AUDITOR_EXIT=0
Q2_CORRECTED_AUDITOR_EXIT=1
ALL_ORIGIN_CYCLE_REJECTIONS=21/21_AT_VALIDATE_COMPUTE_C6
ALL_ORIGIN_MISSING_DEPENDENCY_REJECTIONS=21/21_AT_VALIDATE_COMPUTE_C6
ALL_ORIGIN_VALID_CONTROLS=21/21_AT_VALIDATE_COMPUTE_C6
INVALID_GRAPH_COMPUTE_OUTPUTS_WRITTEN=0
STAGED_TESTS=PASS
KIT_CONTROLS=206/206
KIT_DELETION_PROBES=65
DELIVERED_EXHIBITION=26_CONSTRUCTIONS_12_RUNS_EACH
ALT_SEARCH_BASE_EXIT=0
ALT_SEARCH_REVERSED_EXIT=1
SEARCH_BOOL_NUMBER_EXIT=0
SEARCH_NESTED_BOOL_NUMBER_EXIT=0
SEARCH_DUPLICATE_ADDITION_EXIT=1
SEARCH_INNER_LIST_REVERSAL_EXIT=1
SEARCH_STRING_WHITESPACE_EXIT=1
SEARCH_CHANGED_ADDED_DELETED_EXIT=1,1,1
GENERAL_REAL_TRACEBACK_ACCEPTED=NO
GENERAL_UNRELATED_COMPLETION_ACCEPTED=YES
GENERAL_INVOCATION_REJECTED_ACCEPTED=YES
GENERAL_UNEXPECTED_ERROR_ROW_ACCEPTED=YES
NAMED_EDGE_PROBE_WRONG_REASON_ACCEPTED=YES
MOI_EMBEDDED_COMPLETION_ACCEPTED=YES
CHANGED_ROW_STRICT_ACCEPTED=NO
CHANGED_ROW_CAPTURE_ACCEPTED=YES
CHANGED_ROW_RECAPTURED_STRICT_ACCEPTED=YES
C5_LIVE_EXIT_CODES=0,0,0,0,0
PYTHON=3.9.6
SYMPY=1.14.0
INTERPRETER_SHA256=b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9
C5_FILES=20637
C5_MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed
WRAPPER_EXIT_CODES=0,7,124
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=NO
PRIOR_FINDINGS_CLOSED=NO
```

C5's rendered inline commands ran in order. Its printed user-site path was `/Users/duhokim/Library/Python/3.9/lib/python/site-packages`, allowed by C4's pinned-environment rule. YES concerns scope and actual execution; N3 separately prevents certifying the conflicting textual hashes. No mandated operative command is a shell pipeline. NO_MASKED_STAGE=NO concerns kit judges accepting incomplete or wrong-stage evidence, not C5 or wrapper status propagation.

Q7(c), V37 findings individually:

| Prior finding | Closure | Evidence on V38 |
|---|---|---|
| codex N1: search-inventory order | PARTLY | Merge and primary-branch normalization work; delivered multi-entry primary cases pass. N1 above still flips the alternative-branch verdict. N2 exposes a type-sensitive comparison gap. |
| codex F3-continued: exact-result rule incomplete | PARTLY | Real traceback, missing-all-tokens, altered strict FAIL rows and relocated inert marker are rejected. Unrelated tokens, separate judges, wrong retained probe failure and recapture remain acceptance paths. |
| codex C1: exhibition run description | CLOSED | Operative sentence now lists twelve runs, matching the delivered loop and generated outputs: six seat/seed plus six additional runs. |
| kimi cosmetic 1: omitted dependency-order variants | CLOSED | Same twelve-run sentence includes both seat dependency reversals and auditor dependency reversal. |
| kimi cosmetic 2: SyntaxError missing from recognizer | CLOSED | SETUP_ERRORS now explicitly includes SyntaxError; the shared setup guard rejects that text. |

Q7(d): N2 and N3 are new executable/integrity defects. N1 and F3 demonstrate incomplete closure of earlier failure classes. No finding above is COSMETIC: each changes an audit verdict, control acceptance or tested integrity value. The two prior documentation cosmetics are closed without changing execution records.

Evidence is retained under `/var/folders/64/71dstw0j1gd_n58lsxnhl3p80000gn/T/r3c2_v38_codex_2iwsh4tl/`: isolated kit and `kit.log`; `r3c2_v38_attack.py` and its log; `r3c2_v38_support.py` and its log; `support/commands.json`; and all synthetic ledgers, seals and audit JSON under `own/`. Additional named-probe/schema checks are in `r3c2_v38_extra.py` and its log. The master, packet and delivered tools were not edited. Only the requested gate report is overwritten in the lane.

R3C2_V38_GATE_COMPLETE
