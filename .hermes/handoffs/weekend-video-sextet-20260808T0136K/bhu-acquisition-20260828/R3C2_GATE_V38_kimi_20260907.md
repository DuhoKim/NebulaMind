ACCESS_SHA=8095c18c8f29901b6a4ebe83a07b78dcab5955d440c25b4f19a9f0d713ba8ac7
PACKET_SHA=d18589006489339559945b77762cbcc8dc9937f6772d0b1ed48df63a8548b498
GATE=PREREG_UNSOUND
LEAK=content-level only (the ruled floor): the corpus is cosmology on the Planck 2018 baseline and provenance is visibly classified with unusual care while every stated consumer of it is an integrity check; no hypothesis, comparison model, preferred outcome or "pattern" reconstructible from the packet alone
CONSEQUENCE_VISIBLE=NO
ORIGIN_PURPOSE=checking mechanics only, never an arithmetic input — "Every input's `origin` is classified independently by both seats." (C3), disagreements filed never reconciled with "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4), audit re-classification ("the auditor re-classifies the record from the pinned sources", C3); "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3); no tally, comparison, weighting or conclusion computed from `origin` is stated anywhere in the packet
C5_EXECUTABLE_UNDER_SCOPE=YES
NO_MASKED_STAGE=YES
PRIOR_FINDINGS_CLOSED=YES

Seat: kimi. Date: 2026-09-07. Master: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 38, living draft, unsigned). Packet: r3c2_seat_packet/R3C2_SEAT_PACKET.md. Judged §§1,2,3,4,5,6,7,9,11 as written; §3's core definition taken as settled under option (c) by the principal's ruling and judged as written. §10 treated as history that BINDS NOTHING; none of its narrated findings is reported here as mine; §10.33 was read only as the repair claims Q7 orders me to test. Order kept: the master was hashed first (ACCESS_SHA, line 1, before any content was read); the packet was then hashed and read, and Q5, Q6, Q3 answered from it alone, in that order, before the master was opened; the master was then read in full (the over-long lines 117 and 315 recovered in full by shell text extraction after the file reader byte-truncated them); the two named V37 gate reports were opened only after the master, as the brief permits. Files opened: the packet; the master; R3C2_GATE_V37_codex_20260907.md; R3C2_GATE_V37_kimi_20260907.md; R3C2_SEAT_PACKET.sha256; r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256; r3c2_staged_d1d7/README_STAGED_UNADOPTED.md; r3c2_staged_d1d7/r3c2_staged_tests.py and r3c2_spi_exhibition.py (read; both executed from a scratch copy); the delivered tools r3c2_ledger_tools.py, r3c2_lane_tools.py, r3c2_batch_tools.py (read in the regions quoted; executed), r3c2_manifest.py, r3c2_timeout.py, r3c2_build_seat_packet.py (executed), r3c2_seat_sandbox.sb (digest only); the V37 bytecopy tools (diffed against delivered, not run); R3C2_CORPUS_MANIFEST.md (metadata and row digests only); the 89 enumerable texts and 79 RAW files (hashed and byte-counted only, never content); r3c2_exact_rows.json (digest plus row samples through the delivered judge); R3C2_INTERPRETATION_PROTOCOL_20260904.md (metadata only: exists, 3,947 bytes; never opened); R3C2_LEAK_FLOOR_JUDGEMENT_20260905.md (metadata only: exists, 8,523 bytes; never opened); R3C2_V26_INTEGRATED_CANDIDATE_UNADOPTED_20260906.md (copied solely as the kit's required build fixture; not read). No other gate file, no other version's report, no interpretation contents. Every fixture below is synthetic referee evidence under /tmp/r3c2_v38_kimi with my own sources, ids and values; nothing here is corpus data, a census, an adoption, a signature, or a run.

PACKET-FIRST ANSWERS (committed before the master was opened; the packet and only the packet had been read)

Q5 (asked and answered before Q3). CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to anyone? No. No sentence does. The only consequence-addressing sentence equalises every outcome: "No tier, warrant token, standing or stamp moves." (packet §11). The remaining consequence-adjacent sentences are procedural and attach to stop states or filing order, not to results: "The census does not proceed" (§4 items 5–7), "No tally is filed" (§4 item 3), "INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`" (§4 item 2 — a filing precedence), "do not adopt the passing seat's result" (§4 item 8). The §3 wording clause ("unreproduced from the stated inputs," not "error") restrains what REPRO_FAILED asserts, not what it costs. CONSEQUENCE_VISIBLE=NO.

Q6 (asked and answered before Q3). CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with it after you record it? Only the checking mechanics, each with its sentence: "Every input's `origin` is classified independently by both seats." (C3); disagreements are filed, never reconciled, and "Above 10% of included claims, `CENSUS_ORIGIN_DISPUTED`." (§4 item 7); "the auditor re-classifies the record from the pinned sources" (C3) and compares "per re-classified input origin" (C6). What it is NOT for is also stated: "Arithmetic consumes records according to status `PRINTED` or `STANDARD`." (§3) and "The reproduction verdict and the provenance fields are recorded separately." (§1). What cannot be stated from the packet is any analytic use: no sentence says a tally, comparison, weighting or conclusion is computed from `origin`. ORIGIN_PURPOSE as on line 6.

Q3 (hypothesis-reachability; adversarial). WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? Every inference I could draw, with the leaking sentence: (i) the corpus is cosmology on the Planck 2018 baseline — "The cosmological rows are the Planck 2018 TT,TE,EE+lowE+lensing baseline." (C3); content a seat must have to file STANDARD at all. (ii) Scale: "all 89 enumerable texts of `R3C2_CORPUS_MANIFEST.md`" (C6), 12 ownership batches (C1). (iii) Reproduction failures are contemplated as live and pre-worded non-accusatorially — "Wording: \"unreproduced from the stated inputs,\" not \"error.\"" (§3); a reporting rule, not an expected rate. (iv) The set-versus-derived composition of the corpus is a wanted, printed output — "`census` prints the `AUTHOR_SPECIFIED_INPUT` count as its own line beside the denominator." (§3); a wanted statistic, not a preferred value of one. (v) The printed-but-chosen case is anticipated and armoured — "Distinct from a claim whose inputs the paper DOES state, chosen or not — that claim is attempted and files `REPRO_WITHIN_STATED_PRECISION` or `REPRO_FAILED`." (§3); it tells the seat how to file, not what to find. (vi) Provenance visibly matters — seven origin values, machine-matched quotations, two independent readers, disputes carried never reconciled, an adequacy rule on `origin_search`, audit re-classification — while every stated consumer of provenance is an integrity check; the machinery's care implies an unstated downstream use, but nothing says what it is weighed against or which pattern would be preferred. (vii) A stake exists and is fenced — "No tier, warrant token, standing or stamp moves." (§11); needing to say so implies the tally could otherwise be read as bearing on some standing system, but the direction of any preference is not stated. Adversarial reconstruction of the stake, as far as the packet supports and no further: the lane wants to know, for a cosmology corpus, whether papers' printed numbers follow from their own stated inputs, and it cares — with unusual care — how much of the corpus rests on chosen/fitted/imported/undeclared inputs versus derived/standard/measured ones; no sentence states which answer it hopes for, no comparison model is named, and no outcome is made weightier than another. That is the ruled floor, no higher.

## 1. OUTCOME CLASSES

Exhaustive and mutually exclusive — by construction plus two stated total orders; I could not break either on THIS text. Per-claim collisions constructed: a claim stating no procedure whose unstated equation would also need an absent input satisfies both REPRO_NO_DERIVATION_STATED and REPRO_INPUT_ABSENT — the order files NO_DERIVATION_STATED; a claim blocked on an unpinned named source with a second input neither printed nor sourced satisfies REPRO_BLOCKED and REPRO_INPUT_ABSENT — the order files BLOCKED; a claim whose attempt would time out while an input is absent files REPRO_INPUT_ABSENT ("Encountering one ends that claim's attempt", §2). A real claim fitting NONE: none exists — a claim either states a procedure or not; each needed input is printed/standard (consumed), named-but-not-machine-matchable or non-enumerable (BLOCKED), or untraced (ABSENT); the attempt completes (arithmetic group), times out or lacks machinery (NOT_EVALUABLE). A real claim fitting TWO after the order: none constructed. Study-level: an audit failure over a blocked claim satisfies both CENSUS_AUDIT_FAILED and CENSUS_PARTIAL — the order files AUDIT_FAILED; a control split co-occurring with an outcome dispute files CENSUS_CONTROL_SPLIT; a zero denominator files CENSUS_PARTIAL by name; a seal-receipt failure is routed to CENSUS_AUDIT_FAILED and away from R3C2_NO_CLASS by name. The eight classes partition the reachable state space and the stated order makes filing unique. INCONCLUSIVE is genuinely reachable: CENSUS_PARTIAL is declared INCONCLUSIVE and is produced by any single non-arithmetic claim with the audit at PASS, or by a zero denominator with the empty enumeration named.

Q1 (asked directly): YES — under option (c), §3's definition as written makes every declared outcome reachable and keeps the reproduction question decidable. The hardest cases, each traced to exactly one outcome and one rests_on (my own fixtures, delivered tools): (a) a printed-but-chosen input — as a candidate passage the numeral is an AUTHOR_SPECIFIED_INPUT exclusion (retained with file, line and numeral; its count printed beside the denominator); as an INPUT to an included claim it is a PRINTED record with origin CHOSEN, consumed by status, the claim files REPRO_WITHIN_STATED_PRECISION or REPRO_FAILED by the mechanical precision test, rests_on USES_CHOSEN (executed: my compute run emitted USES_CHOSEN, rc 0). (b) a fitted parameter the paper states — PRINTED/FITTED, consumed, rests_on USES_FITTED (executed, rc 0). (c) an imported value — the local-import branch and the external-import branch are both enforced by validate (my RIGHT record validates exit 0), consumed, rests_on USES_IMPORTED (executed, rc 0). Also executed: DERIVED_STANDARD_OR_MEASURED_ONLY over an all-DERIVED/MEASURED graph, USES_UNDECLARED over an ORIG_SILENT record, NOT_COMPUTED over an empty ledger (each rc 0); all six per-claim tokens through census final (exit 0, C1_DENOMINATOR_PRINTED=PASS, SEAT_CENSUS=PASS). A claim whose dependency graph is cyclic or has a missing dependency now fails validate (7/7 origins each, exit 1) and compute (7/7 each, exit 1, no output written) before any classification, and at C6 the matched-closure graph-integrity check fails the audit (7/7 cycles and 7/7 missing dependencies, exit 1, the diagnostic on BOTH the auditor and the matched sealed closure) — so a broken graph cannot be smuggled into the arithmetic group, and the study files CENSUS_AUDIT_FAILED rather than a tally. Decidability is bounded mechanical classification, not a promise to decide arbitrary mathematics in 120 seconds; the cap's own outcome (REPRO_NOT_EVALUABLE / SYMBOLIC_TIMEOUT) is declared, and the wrapper that enforces it was executed (exits 0, 7, 124; SYMBOLIC_TIMEOUT printed at 0.204 s under a 0.2 s cap).

## 2. CONTROLS

No control is passable by bare assertion except the two that say so themselves. Every control names an exact code with PASS|FAIL|NOT_RUN: C0_REACHABILITY, C1_DENOMINATOR_PRINTED, C1B_BATCH_COVERAGE, JOIN, C2_INPUT_LEDGER, C3_NO_SUBSTITUTION, C4_PACKET_REDACTED, C4_SEAT_ISOLATION, C5_HARNESS_PINNED, C5B_NO_CROSS_LANE, C5C_NO_FALLBACK (§9, lane-side), C6_AUDIT_SAMPLE. C1 PASS is only exit 0 of the printed census run with the five declared counts recomputed; C3 PASS is only exit 0 of every printed validate run — "a token asserted without the printed run is FAIL"; C5 requires all five commands' live output and fails "a transcribed value in place of live output"; C6 PASS requires the printed C6_AUDIT.json with both seals matching, the recomputed selection matching, no omission row, dispute rate at or below 10% and no MISMATCH. The two self-report controls state their own limit: PASS "means only that the list contains no outside path; it makes no claim that the list is complete" (C4; C5b likewise). Unreached controls are handled: "Once a stop class applies, later limbs are unreached and their controls are `NOT_RUN`", "Controls in an unreached limb are `NOT_RUN`, never passes", and C2's per-session validation is NOT_RUN until the joined ledger exists.

Q2 (asked directly): YES — the provenance field is checkable rather than asserted, with exactly the floor the document states. Executed on THIS text with my own fixtures (/tmp/r3c2_v38_kimi/own, sources p.txt/q.txt): (1) THE WRONG-BUT-PLAUSIBLE RECORD: value "2" printed at p.txt:5 in a paper whose line 2 says "We adopt v = 2 from q (2020)." — filed CHOSEN/ORIG_CHOICE_STATED with that true quotation at its true line and the value at its own local line — validate returned exit 0, C3_NO_SUBSTITUTION=PASS: the machine checks that the quotation occurs at the cited line and that the reason code maps to the origin, not that the code is the apt one for the sentence; the citation-first precedence is a rule seats apply, not a machine test. (2) THE MECHANISMS THAT CATCH IT, AS WRITTEN, each executed: (i) the second seat's independent classification — filing IMPORTED/ORIG_CITATION (same coordinates) merged exit 0 with origin=IMPORTED origin_alt=CHOSEN preserved (canonical branch order) — the dispute is carried, never silently resolved, and above 10% of included claims the study stops with CENSUS_ORIGIN_DISPUTED; (ii) the C6 re-classification — against the CHOSEN-only sealed ledger, my IMPORTED auditor record produced compare exit 1, C6_AUDIT_SAMPLE=FAIL. (3) THE STATED FLOOR: when my auditor repeated the same wrong code, compare returned exit 0 — the document states this floor itself ("Whether the quotation cites THAT value is seat judgement; the second seat and C6 may detect an error, but can share it", §2) rather than implying it away. The machine floor that IS enforced is real: quotation machine-matched at the cited line for every non-ORIG_SILENT record of every status, value machine-matched as a numeric token at its line, closed-list membership for STANDARD, candidate binding for every PRINTED record, and the floor that a record whose value line lies in another file must be IMPORTED/ORIG_CITATION whatever reason code was submitted (my WRONG record deliberately used a local value line, so that floor does not fire — the two reader-side mechanisms above carry it).

## 3. CIRCULARITY

No path found for the lane's expectation to reach the evidence. Inclusion: the §1 rule ("prints a numeral the paper asserts as a result of its own", with the enumerated excluded kinds) carries no hypothesis content; the packet the seats receive is machine-asserted against the forbidden list (section 4 below: rebuilt byte-identical, C4_PACKET_REDACTED=PASS; two negative mutations rejected without output), and my packet-first Q3 found no directional stake. Exclusion: a contrary claim cannot be quietly dropped — every excluded candidate is retained with file, line, numeral and kind; `census` fails a row that lacks or alters those fields; both seats must agree with tolerance zero; C6 independently enumerates all 89 enumerable texts before any sealed ledger is released, and any omission in either direction (sealed-included, audit-included, or audit-excluded passages) files CENSUS_AUDIT_FAILED. Origin classification cannot be tuned toward a preferred rests_on: two seats classify independently, disagreements are carried never reconciled, the reason-code tie-break is stated as evidence specificity ("not a ranking of the values"), the severity order lives only in redacted lane-side spans, and rests_on is computed by script from the complete graphs merge preserves — executed: validate rejects a seat-authored rests_on or root_origins (exit 1, "field outside the schema"), and compute REJECTS a ledger arriving with rests_on set (exit 2, "REJECT: i1 arrives with rests_on set", no output written). The interpretation step is receipted (P) before limb A begins and reads fields the seats never see; §7 states the two-receipt binding. Residual channels are the stated floors: shared reader error and prior exposure, both recorded in the document rather than implied away. The finding in section 7 below (query order) is a spurious-FAIL defect in the audit layer — it cannot manufacture a PASS, hide a difference, or steer any tally, and opens no channel from expectation to evidence.

## 4. THE BLIND

Machine-asserted as claimed, verified end to end on THIS master. I re-ran the pinned builder with the printed command form to a scratch output: first line "C4_PACKET_REDACTED=PASS", then "master sha256 8095c18c…" (= ACCESS_SHA, character for character) and "seat packet sha256 d1858900…"; "sections kept ['1.','2.','3.','4.','5.','6.','9.','11.'] dropped ['0.','7.','8.','10.'] inline spans redacted 51". The rebuilt packet is BYTE-IDENTICAL to the pinned packet (both d18589006489339559945b77762cbcc8dc9937f6772d0b1ed48df63a8548b498, 40,648 bytes), and the packet's line-9 built-from digest equals ACCESS_SHA. Negative probes on scratch copies: injecting the forbidden string "hypothesis" into seat-visible §1 produced exit 1, "C4_PACKET_REDACTED=FAIL — forbidden content survived redaction", no packet written; removing the required token C2_INPUT_LEDGER produced exit 1, "required seat-facing content did not survive redaction", no packet written. The C4 mechanism as described does what it claims and states its limits honestly: the forbidden list "blocks the enumerated strings and does not establish that every consequence-bearing word is gone"; C4/C5b PASS means only what the printed self-report list shows; "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can"; matching manifest digests establish "matching snapshots of the pinned directory at pinning and at C5, not continuous immutability between them". The accepted floor — a seat can tell provenance is classified with care — is recorded in C4 with the principal's ruling cited; the judgement file it cites exists (8,523 bytes; metadata only). The sandbox profile's digest equals the C4 pin (6978d590…). My packet-first answers are the empirical check on this seat: no consequence ranking visible, origin purpose statable only as checking mechanics, no directional stake reconstructible.

## 5. THE SEAL (§7)

The receipted relay binds what it claims to bind, before the step it must precede — in both directions. Receipt P: the interpretation protocol is committed and its hash and commit id sent to the outside custodian BEFORE limb A begins — "limb A does not begin without receipt P" — so the protocol cannot be written to suit a tally that does not yet exist. Receipt T: one named commit (the merged candidate file, the exclusion ledger, and the merged input ledger with its `compute` output) is sent and receipted BEFORE the protocol is opened, and "the receipt names every file in that commit" — so the tally cannot be reshaped after the protocol is read. "The relay is complete only when it is receipted." After opening, the custodian independently re-hashes both and verifies all four values; any missing receipt or mismatch files CENSUS_AUDIT_FAILED (§4's definition names the seal-receipt case), leaves the interpretation NOT_RUN and voids the comparison. The external seed is supplied only after the tally digests are receipted, and `audit select` refuses without the stage-1 seal (kit control and its deletion probe both ok in my kit run). The limit is stated plainly: "It does not prove Tori had not already guessed the answer — nothing available here could prove that. It bounds the record, not the mind." The protocol file exists (3,947 bytes; metadata only, never opened). This is a judgement of the binding order in the design; no custody has been executed and none is certified here.

## 6. FAIRNESS

Every negative outcome is worded "unreproduced from the stated inputs," never "error" (§3 REPRO_FAILED; the packet repeats it; the surviving "error" vocabulary in the operative sections is audit-internal — "may detect an error", "shared reader error", "launch error", "argument errors" — never an outcome label; swept by grep). The positive outcome's name matches its mechanical tests (stated precision with round half away from zero; the uncertainty test taken once; the asymmetric half-width rule). No class name or clause asserts more than its definition: DERIVED_STANDARD_OR_MEASURED_ONLY names its membership exactly — my compute runs emitted exactly that label over an all-DERIVED/MEASURED graph and the USES_* labels otherwise; CENSUS_COMPLETE requires every included claim in the arithmetic group with C6_AUDIT_SAMPLE=PASS, and the zero-denominator clause blocks a vacuous complete; R3C2_NO_CLASS is bounded to C0–C5b with C6 and seal failures routed to CENSUS_AUDIT_FAILED; the exclusion-kind names match their definitions and the excluded numeral is retained, not discarded (kit-asserted).

## 7. STALL / EXECUTABILITY

Q4 (asked directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? YES — one class of described artefact does not exist as delivered: two in-text pins name digests the delivered files do not have (finding 2 below). Everything else I checked executed as delivered. Each thing checked, and how: the master hashed first and read in full; the packet hashed and read first; R3C2_SEAT_PACKET.sha256 verifies 8/8 OK and r3c2_staged_d1d7/R3C2_STAGED_D1D7.sha256 verifies 24/24 OK from the lane directory, re-verified after all runs (the kit's deterministic rewrite of C6_COUNTEREXAMPLE_EXHIBIT.txt reproduced the pinned bytes, 289fb920…). Named scripts beside the document, digest against the document's printed pin: r3c2_ledger_tools.py 1ad74791… OK; r3c2_lane_tools.py c08b7c22… OK; r3c2_timeout.py fbb9bef7… OK; r3c2_build_seat_packet.py 56ac7d1c… OK; r3c2_seat_sandbox.sb 6978d590… OK; R3C2_CORPUS_MANIFEST.md 26dabe7c… OK; r3c2_batch_tools.py MISMATCH (printed 3822f12c…, delivered e078e05c…); r3c2_manifest.py MISMATCH (printed 6b6e4bce…, delivered f3854f8b…). The kit's _STAGED copies are byte-identical to the delivered tools for all five; the seat pin sheet pins the DELIVERED digests for both mismatched tools, so the sheets are right and the in-text pins are wrong. The 12-batch partition regenerated byte-identical (39b8d9bf…, matching the master's pinned prefix; BATCH_PARTITION=PASS). Corpus: 89/89 enumerable texts and 79/79 RAW files verified by digest and byte count (never content). C5's five printed commands on this host, each exit 0 with live output: Python 3.9.6; sympy 1.14.0; interpreter sha256 b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9; user site /Users/duhokim/Library/Python/3.9/lib/python/site-packages; the pinned r3c2_manifest.py over it → FILES=20637, MANIFEST_SHA256=1b5463c1072994b2c458d8cbe73a931d23c4b9b10cee3dfd789989d59d9f03ed, MANIFEST=PASS (the two `-c` one-liners were run as equivalent one-line script files because my referee harness blocks `-c` — a restriction on this referee, not on the text). The §9 wrapper: fast child 0, exit-7 child 7, deadline child 124 with SYMBOLIC_TIMEOUT. Argument errors: ledger, lane and batch tools each print usage plus INVOCATION=REJECTED, exit 2 (executed); the no-arg manifest and timeout print usage, exit 2; completion tokens observed on successful runs (SEAT_CENSUS=PASS, C1_DENOMINATOR_PRINTED=PASS, COMPUTE=PASS, MANIFEST=PASS, BATCH_PARTITION=PASS). Fields the scripts are said to compute: root_origins and rests_on (executed — the six labels and NOT_COMPUTED); origin_alt/origin_search_alt (executed — merge carried origin_alt=CHOSEN and, in the query-differing case, origin_search_alt); the five declared counts recomputed (kit); the recomputed selection (kit). Files the document says exist: all present (protocol 3,947 bytes and the leak-floor judgement 8,523 bytes, metadata only; r3c2_exact_rows.json present and pinned 95a229f3…; the sandbox profile present, digest matching). Future dispatch inventories, session reports, seals, receipts, provider logs and the dated check sheet are prerequisites/outputs, not already-completed artefacts; none is certified here.

FINDING 1 — NON-COSMETIC (Q7(d); the sixth instance of the ordering disease, in the sibling field N1's repair preserved). Verbatim sentence, §3 (the lane-side MOI paragraph):
> A shared canonical-search function sorts their entries by canonical JSON without dropping entries or changing their content; it preserves the `query` field.

Defect: `files` and `matches` are canonicalised, but the ORDER of the `query` list is canonicalised nowhere — not at `merge`, not on either side of the C6 comparison — and no stated rule gives query order any meaning: the packet's adequacy rule judges query CONTENT and variants ("a search of one query with no variants is not adequate"), and the audit compares searches BETWEEN different agents (seat versus auditor), a comparison in which the order the queries were run can carry no meaning. MOI's own clause claims the outcome "invariant under every ordering that carries no meaning… The list is of KNOWN sources; the invariant governs any other that is found." Query order is such an ordering, it is not enumerated in (a)–(g), and it flips the verdict. Executed (synthetic fixtures, delivered tools): both seats file the identical ORIG_SILENT record with query ["x","choose","adopt","fit"]; the auditor reconstructs the identical record with only the query list reversed → C6 exit 1 ("origin_search differs"; C6_AUDIT_SAMPLE=FAIL) where the unpermuted auditor passes (exit 0) — equivalent evidence, different verdict, exactly the N1 shape. With the two seats in two different query orders, merge preserves origin_search_alt (executed), the auditor matching one preserved order passes (exit 0), and the auditor with a third permutation fails (exit 1). This is not exotic: the adequacy rule REQUIRES multiple query variants, nothing prescribes their order, and the auditor reconstructs before any sealed ledger is released, so it cannot match orders deliberately — a spurious CENSUS_AUDIT_FAILED is the modal outcome whenever an ORIG_SILENT record sits inside a sampled claim. Safe direction (it cannot manufacture a PASS), but it is a verdict determined by an ordering that carries no meaning, contradicting the clause as written. Exact replacement:
> A shared canonical-search function sorts the entries of `query`, `files` and `matches` by canonical JSON without dropping entries or changing their content.

— applied at the same two boundaries as now (merge head; both sides of the C6 comparison), with source (g) widened to name query-list order, an auditor-only query-order permutation added to the combined property (thirteen runs), and the changed/added/deleted-entry guards extended to the query list. (If the lane instead intends query order to be meaningful, the meaning must be stated in the text; nothing stated today gives it one.)

FINDING 2 — NON-COSMETIC by Q4's own definition ("a described artefact that does not exist as delivered is a finding"); no class, control passability, reachability, conclusion or tested value changes. Verbatim sentences:
> the lane's pinned `join` (pure function, no renaming; `r3c2_batch_tools.py`, sha256 `3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e`; partition `39b8d9bf199265ab…`)  (§5, C1)
> `r3c2_manifest.py` is committed beside this document, sha256 `6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a`, delivered in the seat's working directory and pinned in `R3C2_SEAT_PACKET.sha256`  (§5, C5)

Defect: the delivered r3c2_batch_tools.py hashes e078e05cb0d4b2c055f2087d79a5280a08e5c7b17494bcab8b067e0fc17c0b74 and the delivered r3c2_manifest.py hashes f3854f8b1a4be978ec7a0cfb764ed73c2803747808ab5cda6fddc140b6e04ec0 (both gained their V38 completion tokens; both pin SHEETS — seat 8/8 and kit 24/24 — pin the delivered bytes and verify OK). The in-text pins were not re-pinned, and because the packet is built from this master, the seat-facing packet asserts the same two false digests for files in the seat's own working directory. A seat that applies the lane's own ACCESS discipline to the printed pins finds a mismatch on a correct dispatch; the design's custody model is pins, and §10.8 records exactly this class as a finding when the direction was reversed. The control predicates do not read these pins, so nothing operative changes — stated plainly. Exact replacement: in C1 replace `3822f12c282aaf4b5c850bef08fbfbe3bce81ac09d5f9e3c578459239439f79e` with `e078e05cb0d4b2c055f2087d79a5280a08e5c7b17494bcab8b067e0fc17c0b74`; in C5 replace `6b6e4bce1136448b2cb223225a8710f493c469aef82a0379b3f47e066260019a` with `f3854f8b1a4be978ec7a0cfb764ed73c2803747808ab5cda6fddc140b6e04ec0`; then re-run the builder and re-check the packet pin before dispatch, as C4 already requires.

RESIDUE NOTE — COSMETIC (changes nothing; no repair requested beyond tidiness): the kit directory carries an unpinned leftover `_spi_live_tools/` holding V33-era tool bytes (bd1f4a23…, 42c1577e…). Nothing in the kit or the document references it; pointing the exhibition at it fails closed (MERGE_FAIL, SPI=FAIL), never passes. The kit's own live run builds fresh copies of the delivered tools and passes (SPI=PASS, executed).

## Q7 (V38) ANSWERS

(a) Executable checks of Q1–Q6 rerun on THIS text, tokens:

  PACKET_BUILT_FROM_ACCESS_SHA=YES (packet line 9 equals ACCESS_SHA character for character)
  PACKET_REBUILD=BYTE_IDENTICAL (40,648 bytes, 51 spans, C4_PACKET_REDACTED=PASS)
  BUILDER_NEGATIVES=2/2_REJECTED_WITHOUT_OUTPUT (forbidden string injected; required token removed)
  SEAT_PIN_SHEET=8/8 OK (before and after all runs)
  KIT_PIN_SHEET=24/24 OK (before and after all runs)
  TOOL_PINS_MATCH_MASTER=ledger 1ad74791 OK, lane c08b7c22 OK, timeout fbb9bef7 OK, builder 56ac7d1c OK, sandbox 6978d590 OK, corpus 26dabe7c OK; batch 3822f12c STALE (delivered e078e05c), manifest 6b6e4bce STALE (delivered f3854f8b) — finding 2
  STAGED_EQ_DELIVERED=5/5 byte-identical
  CORPUS_ROWS_VERIFIED=enumerable 89/89, RAW 79/79 (digests and byte counts only)
  PARTITION_REGENERATED=byte-identical (39b8d9bf…)
  KIT_RUN_STRICT: deletion_probes=65 / controls=206 passed=206 failed=0 / STAGED_TESTS=PASS (exit 0)
  KIT_RUN_CAPTURE: controls=206 passed=206 / CAPTURED 175 expectations / exact_rows digest UNCHANGED 95a229f3… (capture is a fixed point on the delivered tools)
  MOI_EXHIBITION_DELIVERED=SPI=PASS (exit 0; 26 constructions; 12 runs each: 2 seat orders x seeds 0,1,2 + reversed arrival + reversed rederivation order + reversed dependency order (both seat orders) + reversed auditor dependency order + reversed auditor search-inventory order)
  Q1_COMPUTE=USES_CHOSEN,USES_FITTED,USES_IMPORTED,USES_UNDECLARED,DERIVED_STANDARD_OR_MEASURED_ONLY,NOT_COMPUTED (each rc 0)
  SIX_PER_CLAIM_TOKENS_CENSUS_FINAL=PASS (exit 0; C1_DENOMINATOR_PRINTED=PASS; SEAT_CENSUS=PASS)
  Q2_MECHANISM: WRONG_CHOSEN_ON_CITATION validate exit 0 / RIGHT_IMPORTED validate exit 0 / merge exit 0 origin=IMPORTED origin_alt=CHOSEN preserved / auditor corrected vs CHOSEN-only sealed exit 1 / auditor repeats wrong exit 0 (the stated floor)
  VALIDATE_REJECTS_PRESET=rests_on exit 1, root_origins exit 1 (field outside the schema)
  COMPUTE_REJECTS_PRESET_RESTSON=exit 2, no output / COMPUTE_NO_CANDIDATES=exit 2 INVOCATION=REJECTED, no output
  CYCLES_VALIDATE_RC1=7/7 origins (CHOSEN,FITTED,IMPORTED,MEASURED,STANDARD,UNDECLARED,DERIVED)
  CYCLES_COMPUTE_RC1=7/7 (no output written)
  MISSING_VALIDATE_RC1=7/7 / MISSING_COMPUTE_RC1=7/7
  C6_CYCLES_AUDIT_RC1=7/7 origins, graph-integrity diagnostic on BOTH closures / C6_MISSING_AUDIT_RC1=7/7, BOTH closures
  N1_MERGE_BYTES_EQUAL_UNDER_FILES_OR_MATCHES_REVERSAL=YES (including seat swap)
  N1_AUDITOR_ONLY_FILES_OR_MATCHES_PERMUTATION_PASSES=YES (exit 0; the V37 flip is closed)
  N1_NEGATIVES_STILL_FAIL=changed-entry, added-entry, deleted-entry, duplicate-entry, whitespace-entry, changed-match — each exit 1
  N1_NESTED_MEMBER_ORDER_PASSES=YES (same JSON object; intended, source (d))
  MY_NEW_QUERY_ORDER_PROBE: auditor-only reversed query list exit 1 / two seats' two orders + auditor's third order exit 1 / auditor's matched order exit 0 — finding 1
  JUDGE_ATTACKS_ALL_REJECTED: real traceback (V37 codex's case); missing completion token; padded row; reordered rows; dropped row; unpinned control (fail-closed); SyntaxError recognised by the setup guard; relocated inert probe marker ("NO diagnostic vanished"); probed tool exiting right code for the wrong reason (baseline traceback caught)
  INVOCATION_REJECTED=ledger, lane, batch on argument errors (executed); manifest/timeout usage exit 2
  V37_TO_V38_DIFFS=additive only (canon_search in both tools; _emit completion tokens; INVOCATION=REJECTED); no production guard touched
  C5_LIVE=[0,0,0,0,0] Python 3.9.6 / sympy 1.14.0 / interpreter b8763cf2… / user site printed / FILES=20637 MANIFEST_SHA256=1b5463c1… MANIFEST=PASS
  WRAPPER=[0,7,124] (SYMBOLIC_TIMEOUT at 0.204 s under a 0.2 s cap)
  INTERPRETATION_PROTOCOL=exists, 3,947 bytes, metadata only / LEAK_FLOOR_JUDGEMENT=exists, 8,523 bytes, metadata only

(b) C5_EXECUTABLE_UNDER_SCOPE=YES for THIS text — all five printed C5 commands executed on this host under the printed scope, each exit 0 with live output (the two `-c` one-liners as equivalent one-line files; my referee harness blocks `-c` — a restriction on this referee, not on the text). NO_MASKED_STAGE=YES for THIS text — every mandated command is one invocation whose exit status is the control's; no mandated command is a shell pipeline; the manifest is ONE process, run live over 20,637 files with FILES= and MANIFEST_SHA256= printed; the §9 wrapper is one invocation that prints and carries its child's exit status (observed 0 and 7) and prints SYMBOLIC_TIMEOUT with exit 124 on the deadline; the V37 defect that grounded NO_MASKED_STAGE=NO (the kit's general `check` crediting a real traceback, and probes accepting an undeleted diagnostic) is closed: one exact-result evaluator now judges every control — setup guard (traceback/import/launch/SyntaxError/empty), exact exit code, mandatory completion token, complete ordered diagnostic rows against the pinned table, and a vanished-diagnostic requirement on probes — and every subversion I executed against the delivered functions was rejected, with the kit's eight meta-controls passing in the kit run.

(c) PRIOR_FINDINGS_CLOSED=YES over the V37 gate's findings, each with my own evidence:
  - codex N1 (origin_search.files/.matches order changed merged bytes; an auditor-only permutation flipped C6 PASS→FAIL) — CLOSED as filed. Diff-verified: canon_search added identically to both tools and applied at the merge head before keys/digests/branch selection/serialisation (lane tool, additive) and on both sides of the C6 comparison (ledger tool, additive); the V37 replacement text itself preserved `query`, and V38 implements that text. Executed: merge bytes equal under files/matches reversal including seat swap; auditor-only reversals PASS (exit 0); changed/added/deleted/duplicate/whitespace entries all FAIL (exit 1); nested member-order PASS. The sibling field N1's repair deliberately preserved (`query`) is a new instance of the same class and is filed as finding 1, not as N1 unclosed.
  - codex F3-continued (the general `check` omitted the setup guard, treated the token as optional and matched by substring — a real traceback was credited; probes accepted an undeleted diagnostic when only the marker moved) — CLOSED. Source-verified: ONE exact-result evaluator serves check/probe/moi_judge; the pinned table r3c2_exact_rows.json is required for every judged control (an unpinned control is rejected fail-closed — executed); deletion probes run the tool unprobed and probed and require the pinned targeted diagnostic to have vanished (a relocated inert marker is rejected — executed); SyntaxError is in SETUP_ERRORS (executed); completion tokens on every subcommand through one dispatcher, INVOCATION=REJECTED on argument errors (executed on ledger/lane/batch); eight meta-controls pass in my kit run; V37→V38 diffs are additive, no guard weakened.
  - codex C1 (the exhibition description enumerated 8 runs; the delivered code ran 11) — CLOSED. §3's exhibition sentence now states the delivered TWELVE runs, including "one reversed-auditor-search-inventory run"; the delivered exhibition executes exactly those twelve (source lines 148–156) and passes (SPI=PASS, 26 constructions, executed).
  - kimi cosmetic 1 (the same stale exhibition sentence) — CLOSED on the same evidence.
  - kimi cosmetic 2 (a SyntaxError crash passed the kit's setup guard, which lacked the literal) — CLOSED. SETUP_ERRORS now includes "SyntaxError"; executed: a syntax-crash output is rejected by the setup guard itself.

(d) NEW executable defects: finding 1 (the query-order MOI residual; counterexample executed three ways above) and finding 2 (two stale in-text pins; the delivered bytes hash elsewhere than the text asserts). Under (1) of the Q7 attack order I also tried: crashing a fixture into the evaluator, an empty tools directory (kit meta-control), a relocated probe marker, a wrong-bytes subcase (kit meta-control), a probed tool exiting with the right code for the wrong reason, and re-capturing the pinned table — no control passed under any subversion; capture mode on the delivered tools is a byte-identical fixed point, and any expectation-changing re-capture breaks the kit pin sheet, with the README stating plainly "Regenerate deliberately with `NM_KIT_CAPTURE=1`, never to make a failing control pass." Under (3), ordering sources probed beyond (a)–(g): query-list order (finding 1 — the one live residual); nested JSON member order inside inventory entries (invariant, intended); duplicate/whitespace/type-distinct entries (still FAIL, as they must). Under (4), cycles and missing dependencies fail through every origin at validate, compute and C6 (7/7 each, both-closure diagnostics at C6).

Why UNSOUND and not SOUND_WITH_REPAIRS: finding 1 is the N1 class — an ordering that carries no meaning decides a verdict on equivalent evidence, executed — and it contradicts the MOI clause as written in the common ORIG_SILENT case; finding 2 is a false custody statement in seat-facing operative text. Both carry exact replacements and neither touches §3's settled definition. Q1 and Q2 answer YES on executed evidence; the blind is machine-asserted and reproduced byte-for-byte on this master; the seal binds each artefact before the step it must precede; fairness is clean; every V37 finding is closed with my own evidence; everything else the document describes executed as delivered.

The master, the packet and both pin sheets were re-verified unchanged after all runs (master 8095c18c… = ACCESS_SHA; packet d1858900… = PACKET_SHA; seat sheet 8/8 OK; kit sheet 24/24 OK). Nothing in this report adopts, signs or runs anything; every fixture above is synthetic referee evidence under /tmp/r3c2_v38_kimi (own/, n1/, c6gi/, kit/ scratch copy, exhibit_live/), not corpus data and not a census. Evidence scripts retained: n1_attack.py, own_checks.py, c6_gi_sweep.py, judge_attacks.py, judge_attacks2.py, preset_checks.py, corpus_verify.py, and the kit/exhibition logs.

R3C2_V38_GATE_COMPLETE
