ACCESS_SHA=c945c22e110bf0306f92b3bebcc66050078bf562ee99cd5253ccccea5a03611b
GATE=PREREG_UNSOUND

1. QUOTE: “Claims enumerated, attempted, and `NOT_ATTEMPTED`, printed before any tally.”
   DEFECT: The only route out of the census was expressly abolished in §3, but C1 still requires and therefore revives it. That makes the denominator rule internally inconsistent and permits an included claim to be parked outside the exhaustive §3 outcomes. This affects questions 1, 3, 5 and 8.
   EXACT REPLACEMENT: “Every candidate passage and its inclusion or exclusion decision, and every included claim with exactly one §3 outcome, are printed before any tally. `NOT_ATTEMPTED` is not an allowed ledger value or outcome. `C1_DENOMINATOR_PRINTED=PASS`.”

2. QUOTE: “`REPRO_EXACT` — the printed number follows, within its own stated precision, from printed + standard inputs only.”
   DEFECT: `PRINTED` is a location/status, not a provenance. Under C3 a printed input may have origin `CHOSEN`, `FITTED` or `IMPORTED`; a number produced from such an input satisfies the literal `REPRO_EXACT` rule and can also satisfy `REPRO_AFTER_CHOICE`. Conversely, an undeclared imported value must be `ABSENT` and cannot be supplied, despite `REPRO_AFTER_CHOICE` saying an imported quantity was supplied. The classes are not mutually exclusive, and C3 does not machine-enforce outcome precedence. This affects questions 1, 3, 5 and 8.
   EXACT REPLACEMENT: “`REPRO_EXACT` — the printed number follows, within its own stated precision, using only consumed inputs whose origin is `DERIVED` or whose status is `STANDARD`; this class is forbidden if any consumed input has origin `CHOSEN`, `FITTED`, `IMPORTED` or `UNDECLARED`. `REPRO_AFTER_CHOICE` — the printed number follows and at least one consumed, paper-printed input has origin `CHOSEN`, `FITTED` or `IMPORTED`; name every such quantity, and this class takes precedence over `REPRO_EXACT`. An undeclared or unprinted needed value is `ABSENT`, may not be consumed, and yields `REPRO_INPUT_ABSENT`. C3 machine-checks these allowed status/origin combinations and the precedence rule.”

3. QUOTE: “So C4 is three things together — a fresh seat with no inherited conversation, a recorded declaration of no prior pattern exposure printed by the seat itself, and a read allowlist naming only this preregistration, the seat brief and the pinned sources.”
   DEFECT: This names an allowlist but never says that the execution environment enforces it. A declaration is self-report, and a path list detects only disclosed file reads; neither prevents shell access outside the list, network retrieval, repository search, cached content, or communication that reveals the pattern. Thus a pattern-exposed seat can still influence enumeration, origin classification and outcomes. C4 is not mechanical, so the central anti-circularity claim is not yet secured. This affects questions 1 and 5.
   EXACT REPLACEMENT: “C4 passes only if each seat is launched with no inherited conversation in a fresh sandbox that (a) exposes the preregistration, seat brief and pinned sources as the only readable bind mounts, (b) denies all other filesystem reads at the operating-system boundary, (c) disables network and inter-process communication, and (d) records the sandbox policy and denied-read log. Before launch the seat declares no prior pattern exposure; any prior exposure, missing declaration, sandbox-policy mismatch, forbidden successful read, or communication outside the packet voids that seat and requires a fresh replacement. The path list is supplementary detection only. `C4_PATTERN_BLIND=PASS`.”

4. QUOTE: “`R3C2_NO_CLASS` — a control fails in every seat that attempted it after two attempts.”
   DEFECT: A control that fails in one seat and passes in another satisfies neither this class nor any other study-level class. `CENSUS_COMPLETE` is defined only by per-claim coverage and could therefore be filed despite a failed control. The study-level outcomes are not exhaustive and the stall remains. This affects questions 4, 5 and 8.
   EXACT REPLACEMENT: “`R3C2_NO_CLASS` — after one rerun of the failing control in each reached seat, any reached control lacks an all-seat `PASS`, including a split between `PASS` and failure. This class takes precedence over `CENSUS_COMPLETE`; report the control, each attempt and each seat result. Controls in an unreached limb remain `NOT RUN` and do not trigger this class.”

5. QUOTE: “Live harness; `ACCESS_SHA` proof for any pinned source audited, verified by Tori after the run and not on the seat's claim; path lists; 120-second cap on symbolic operations with `SYMBOLIC_TIMEOUT` as a reportable outcome; unreached controls `NOT RUN`.”
   DEFECT: `SYMBOLIC_TIMEOUT` is called an outcome but is absent from the exhaustive §3 list. A timed-out real claim can therefore carry no per-claim class. `CENSUS_PARTIAL` does not cure this because it requires an undefined “unresolved after two attempts” without assigning the claim an allowed outcome. This affects questions 3, 4 and 8.
   EXACT REPLACEMENT: “Live harness; `ACCESS_SHA` proof for any pinned source audited, verified by Tori after the run and not on the seat's claim; path lists; a 120-second cap on each symbolic operation. After two timed-out attempts, assign that claim `REPRO_BLOCKED`, name `SYMBOLIC_TIMEOUT` as the reason, and file `CENSUS_PARTIAL` (INCONCLUSIVE); unreached controls are `NOT RUN`.”

6. QUOTE: “Before the interpretation protocol is opened, Tori commits the tally, then sends four digests — tally hash, tally commit id, protocol hash, protocol commit id — to Blanc, who is outside this lane and reports to Duho.”
   DEFECT: Sending is not a completed custody handshake, and the preregistration does not require Blanc to acknowledge and timestamp receipt before opening, nor require the filed tally and protocol to be rehashed and matched to Blanc’s retained values before comparison is accepted. Tori can silently compare a replacement artifact unless that final verification is mandatory. The claimed ordering and sealing are therefore not yet demonstrated. This affects question 6.
   EXACT REPLACEMENT: “Before the interpretation protocol is opened, Tori commits the tally and sends Blanc the tally hash, tally commit id, protocol hash and protocol commit id. Blanc must retain and return a timestamped acknowledgement containing all four values; no one may open the protocol until that acknowledgement is filed. After comparison, Blanc independently hashes the exact filed tally and protocol and verifies both hashes and both commit ids against the acknowledged values. A missing pre-opening acknowledgement or any mismatch voids the comparison and files `R3C2_NO_CLASS`; no outcome or tally may be altered after acknowledgement.”

7. QUOTE: “If the enumeration is itself contested between the two seats beyond a stated tolerance, stop and report — a census whose denominator is disputed cannot proceed.”
   DEFECT: The operative text states no tolerance. The version-history claim that tolerance was set to zero does not define the running rule, so the denominator stop is discretionary. This affects questions 1, 4 and 8.
   EXACT REPLACEMENT: “The enumeration tolerance is zero: if the two seats disagree on any candidate’s presence, inclusion or exclusion after two documented reconciliation attempts, file `CENSUS_DENOMINATOR_DISPUTED`, list every disputed candidate, and do not begin Limb B.”

Answers

1. IS THE CIRCULARITY ACTUALLY GONE? At the level of the scientific criterion, yes: the lane’s shape/magnitude pattern is absent from enumeration, reproduction and outcome definitions, and §7 forbids changing the sealed tally. I find no semantic route by which the pattern itself selects claims or assigns outcomes. At the level of the executable design, no: C4 is presently an instruction plus self-report rather than a mechanically enforced information boundary, and the residual `NOT_ATTEMPTED` language and provenance overlap give a pattern-aware seat discretionary routes to alter the denominator or tally. The redesign removes the conceptual circularity but has not yet secured that removal operationally.

2. IS THE CRITERION EXTERNAL? Yes. Reproducing a printed number from the source’s stated inputs is an ordinary, pre-existing reproducibility criterion. It neither asks whether a construction fixes shape rather than magnitude nor encodes the lane’s pattern in other words. Its result may later bear on that pattern, but that does not make the criterion endogenous.

3–8. The actual per-claim set is nonexclusive until finding 2 is repaired and nonexhaustive for timeout until finding 5 is repaired. The study-level set has a reachable inconclusive route (`CENSUS_PARTIAL`) but is nonexhaustive for split control failures until finding 4 is repaired. C3’s value/source checks are strong, but its status/origin semantics and precedence need finding 2; C6’s full-ledger audit and nonempty deterministic sample are materially adequate once C4 is genuinely enforced. Section 7’s prohibition on outcome changes is correctly stated, but the custody handshake needs finding 6. The standing wording is consistently “unreproduced from the stated inputs,” not “error”; I found no fairness slip. The unrepaired `NOT_ATTEMPTED`, timeout, mixed-control and denominator-tolerance paths can stall without a valid class.

9. WORTH: Yes—if repaired before dispatch, a corpus-wide reproduction ledger is worth approximately 3–4 seat-days because it yields auditable claim-level evidence rather than another interpretation of the pattern. BETTER: Yes—the redesign is substantially better than its predecessor because an external reproduction criterion replaces hypothesis-authored screening, even though the operational blindness and outcome partitions are not yet safe enough to run.

R3C2_GATE_COMPLETE
