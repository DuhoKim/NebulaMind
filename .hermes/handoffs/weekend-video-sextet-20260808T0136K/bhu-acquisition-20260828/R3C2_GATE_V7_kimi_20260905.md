ACCESS_SHA=19a075c66696c4a0793c2fb13a45247f9e174f4b2194ec3a9f540ed001597000
GATE=PREREG_SOUND_WITH_REPAIRS

Findings, keyed to the seven questions. Two defects are major (1a, 2a), three moderate (1b, 2b, 3b), three minor (2c, 7a, 7b). Findings 2a and 3a interact with the held clause; the dependence is stated there explicitly and is not counted as a fresh report of the held clause itself.

1. CAN THE HYPOTHESIS STILL REACH THE COUNT OR THE OUTCOMES? Mostly no; two residual channels.

   Closed, verified: the denominator is double-enumerated at tolerance zero with a named stop class; exclusions are by enumerated kinds into an audited ledger; origins must be machine-matched quotations with a computed transitive root_origins; classification is blind-double with disputes reported-never-reconciled and a >10% stop; the reproduced class is fully re-derived by a blind third seat; the tally is sealed before interpretation, which is excised from every seat.

   1a (MAJOR, see also 5). QUOTE: "The interpretation step lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which **no seat may open** and which is itself fixed and committed before limb A begins, so it cannot be written to suit the tally."
   DEFECT: The "so" is an overclaim by this document's own standard. The only custody binding on the protocol is the §7 seal, which runs AFTER Tori has computed the tally. Between tally computation and Blanc's receipt, the protocol exists solely as a local commit in this lane's own custody — which §7 itself classifies as amendable ("a local commit in Tori's own custody can be amended or reset, so it is tamper evidence, not an external custodian"). In that window the interpretation mapping — the exact place the pattern's stake lives — can be rewritten to suit the known tally, then sealed; every §7 verification still passes. The seal proves both artefacts existed before the comparison was FILED; it does not prove the protocol predates knowledge of the tally.
   EXACT REPLACEMENT: "The interpretation step lives in `R3C2_INTERPRETATION_PROTOCOL_20260904.md`, which **no seat may open** and which is **bound before limb A begins by the same custody relay as the tally: its hash and commit id are sent to Blanc and receipted before any enumeration starts, and the §7 seal re-verifies those two values against that earlier receipt**. A local commit before limb A would not establish this — it is amendable in this lane's custody, the same objection this section makes about the tally — so without the early receipt the protocol could still be written to suit the tally."

   1b (MODERATE, see also 3). QUOTE: "**The exhibitions are authored by a seat and only verified by Tori** — deciding what counts as reachable is where an author's prior would enter, so the author does not decide it."
   DEFECT: Internally contradictory. Tori authored this document and holds the lane whose prior C0 exists to guard against; C0 explicitly covers "any condition whose failure would refute this lane's own expectation". Verification — judging whether an exhibited input actually traverses the document to the claimed verdict — is exactly "deciding what counts as reachable", and the sentence assigns it to the author while asserting the author does not decide it.
   EXACT REPLACEMENT: "**The exhibitions are authored by one pattern-blind seat and verified by a second pattern-blind seat, which re-derives each exhibited path from this document; the two seats must agree on every row, and Tori's role is limited to checking that every §3 outcome and every §4 class appears in the table** — deciding what counts as reachable is where an author's prior would enter, so the author does not decide it."

2. EXHAUSTIVE AND MUTUALLY EXCLUSIVE? No — three defects, one major. HELD-CLAUSE DEPENDENCE stated where it applies.

   2a (MAJOR; interacts with the held clause, dependence stated below). QUOTES: (i) §2: "4. **Attempt the arithmetic with ADMISSIBLE inputs only** (§3's definition: origin `DERIVED` or `STANDARD`). A printed value whose origin is `CHOSEN` or `FITTED` is **not** admissible." (ii) §3: "**`REPRO_FAILED`** — admissible inputs are sufficient but the arithmetic does not give the paper's number. Report both numbers."
   DEFECT, two limbs of one hole. Limb 1: the only arithmetic attempt §2 orders is admissible-only, so no stated procedure ever establishes "it follows only once an inadmissible input is used" — REPRO_AFTER_CHOICE, the class §3 itself says is what this census exists to detect, is unreachable from the method as written. Limb 2: a claim whose equation needs a printed-but-inadmissible input, where the number follows NEITHER from admissible inputs (they are insufficient) NOR from the printed inadmissible value, satisfies no §3 class: not EXACT, not AFTER_CHOICE ("it follows only once…" — it does not follow), not FAILED as literally conditioned ("admissible inputs are sufficient" — they are not), not INPUT_ABSENT (the paper stated it), not BLOCKED, not NOT_EVALUABLE, not NO_DERIVATION_STATED. This is a reachable per-claim state with no fileable outcome (see 7).
   HELD-CLAUSE DEPENDENCE: if Duho's ruling collapses the arithmetic group (e.g., abolishes AFTER_CHOICE as distinct from a single non-reproduction outcome), Limb 1 disappears and Limb 2 closes without a choice-test; the repair below is required iff the ruling keeps the three-member arithmetic group that the standing (b) wording declares. The repair's final wording must land in the same amendment as the ruling.
   EXACT REPLACEMENT (standing wording): insert into §2 as step 4b: "**If the admissible-only attempt does not reproduce the number and every remaining input has a value printed in the paper, attempt the arithmetic once more using the printed inadmissible value(s), naming each inadmissible input used; this second attempt decides between `REPRO_AFTER_CHOICE` and `REPRO_FAILED`.**" And replace REPRO_FAILED's definition with: "**`REPRO_FAILED`** — every input the equation needs has a value (admissible, or printed with an inadmissible origin), the arithmetic completes, and it does not give the paper's number. Report both numbers and, if an inadmissible input was used, name it. **Wording: "unreproduced from the stated inputs," not "error."**"

   2b (MODERATE). QUOTE: "**INCONCLUSIVE, and it takes precedence over `CENSUS_COMPLETE`.**"
   DEFECT: This is the only filing precedence §4 states. CENSUS_AUDIT_FAILED (void), R3C2_NO_CLASS, CENSUS_CONTROL_SPLIT and CENSUS_ORIGIN_DISPUTED can each co-occur with CENSUS_PARTIAL or with each other (e.g., a blocked claim plus an irreproducible audited outcome; a split control plus blocked claims), with no rule choosing the filed class.
   EXACT REPLACEMENT: append after class 7: "**Exactly one study-level class is filed. Where more than one holds, file the first in this order and report every other applicable class alongside it:** `CENSUS_DENOMINATOR_DISPUTED`, `CENSUS_CONTROL_SPLIT`, `R3C2_NO_CLASS`, `CENSUS_AUDIT_FAILED`, `CENSUS_ORIGIN_DISPUTED`, `CENSUS_PARTIAL`, `CENSUS_COMPLETE`."

   2c (MINOR). QUOTE: "**If any survives, the packet is not written and `C4_PACKET_REDACTED=FAIL`**; the study does not proceed on a hand-checked copy."
   DEFECT: A mandated pre-dispatch stop that names no §4 class. R3C2_NO_CLASS covers "a control fails in every seat that attempted it after two attempts" — a builder failure before any seat is dispatched is not a seat attempt.
   EXACT REPLACEMENT (class 4): "4. **`R3C2_NO_CLASS`** — a control fails **in every seat that attempted it** after two attempts, **or a pre-dispatch control (the builder assertion `C4_PACKET_REDACTED`, the harness `C5`) cannot be brought to PASS before any seat is dispatched.**"

   Sound within §3 otherwise: the four non-arithmetic classes plus precedence order are exclusive for their stated co-occurrences; exclusions are correctly outside the per-claim outcomes. The arithmetic group's membership is fixed by the held clause, and the document's device of referring to it by name so §4 need not reopen is good design; CENSUS_COMPLETE's soundness therefore rides on the ruling, exactly as the document says.

3. CONTROLS MECHANICAL? Mostly; two exceptions (1b above and 3b here), plus one held-dependent note.

   Mechanical and verified as written: C5's three literal commands (`python3 --version`; `python3 -c "import sympy; print(sympy.__version__)"`; `shasum -a 256 $(command -v python3)`) are executable as written on this platform; C6's sampling frame `random.Random(seed).sample(sorted(claim_ids), k)` with `k = max(1, ceil(0.20 × N))` is executable and externally seeded post-receipt; every control names an exact code with =PASS, C4 names =FAIL, and "Controls in an unreached limb are `NOT RUN`, never passes" covers NOT_RUN. C4's seat declaration and C5b's path list ARE assertion-based, but the document itself demotes them to "secondary detection" behind the structural packet control — honest, not a defect.

   3b (MODERATE). QUOTE: "and that **each `STANDARD` value is one of a closed, named list fixed here** — `G`, `c`, `ħ`, `k_B`, and the Planck 2018 TT,TE,EE+lowE+lensing values quoted with their published uncertainties** — so "standard" cannot become a selectable family."
   DEFECT: The list is not "fixed here". The document prints four constant symbols and a citation; the Planck 2018 TT,TE,EE+lowE+lensing results comprise dozens of base, derived and nuisance values across tables, and no member, value or uncertainty is printed in the document. A machine membership assertion cannot run against a citation; the boundary of "standard" lives wherever the script author put it, unauditable from this prereg, and a seat cannot fill a STANDARD record's value from this document at all. The control built to stop "standard" becoming a selectable family leaves the family's boundary selectable.
   EXACT REPLACEMENT: "and that **each `STANDARD` value is one of a closed, named list fixed here** — the measured constants `G`, `c`, `ħ`, `k_B`, and the Planck 2018 TT,TE,EE+lowE+lensing baseline parameters — **each member printed literally in this section with its value and published uncertainty, the arithmetic script asserting membership against that printed list, and a value not printed here not being `STANDARD`** — so "standard" cannot become a selectable family."

   HELD-DEPENDENT NOTE (no finding): whether the C3 clause "the arithmetic may consume only records with status `PRINTED` or `STANDARD`" correctly expresses admissibility depends on the ruling, since the admissibility consequence is explicitly deferred in §10.1. Likewise C6's fully-audited class "(i) every claim whose filed outcome asserts that the arithmetic reproduced the number" has membership fixed by which outcomes the ruling counts as asserting reproduction. Both are correctly flagged by the document as held; nothing further reported.

4. BLINDING CLAIM HONEST? Sound; no replacement. The document states the wrapper "grants directories; it does not restrict them", calls the seat declaration "self-report", demotes it to secondary detection, and states the residual limit plainly: "It cannot prove a seat has no prior exposure from training or an earlier session — nothing available here can." Nothing overclaimed on the blinding side.

5. SEAL HONESTLY DESCRIBED? The mechanism is sound and its own limits are honestly stated ("It does not prove Tori had not already guessed the answer… It bounds the record, not the mind"); the four-digest receipt relay, the post-opening independent re-hash, and the mismatch class are neither useless nor overclaimed. The single defect is the ¶1 overclaim repaired in 1a: the protocol's pre-tally fixity is asserted through a mechanism the document itself classes as non-custodial. With repair 1a applied, this section is sound.

6. FAIRNESS WORDING HELD? Sound; no replacement. REPRO_FAILED carries "unreproduced from the stated inputs," not "error", and no sentence characterises any paper's number as wrong. The two "error" occurrences ("the class in which an error is both consequential and invisible"; audit-failure discussion) refer to this lane's own arithmetic and audit, not to any paper — correct usage.

7. CAN THE RUN TERMINATE WITH NO FILEABLE CLASS? Yes, two paths, both repaired above: (i) via 2a — the printed-inadmissible-input claim whose number follows neither way carries no §3 outcome, which makes CENSUS_COMPLETE impossible while CENSUS_PARTIAL's condition ("at least one included claim carries a non-arithmetic outcome") is also unmet, since the claim carries no outcome at all; (ii) via 2c — a persistent pre-dispatch builder failure stops the study with no named class. All other mandated stops I traced land in exactly one named class: ABSENT input → REPRO_INPUT_ABSENT; enumeration disagreement → CENSUS_DENOMINATOR_DISPUTED; origin disagreement >10% → CENSUS_ORIGIN_DISPUTED; split control → CENSUS_CONTROL_SPLIT; all-seat control failure → R3C2_NO_CLASS; irreproducible audit or ledger incompleteness → CENSUS_AUDIT_FAILED; seal mismatch → CENSUS_AUDIT_FAILED with §7 NOT RUN; symbolic timeout → REPRO_NOT_EVALUABLE; C0 unreachability correctly blocks the freeze pre-run rather than a study class.

Additional minor defects, not fitting any single question:

   7a (MINOR, version discipline). QUOTES: (i) header: "**Tori, 2026-09-04 21:32 KST. Version 1. ORDERED by Duho, "redesign r3c", 2026-09-04 21:30 KST.**" (ii) final line: "R3C2_PREREG_V6_NONDEFINITIONAL_READY".
   DEFECT: Both are stale on a document §10 identifies as V7 — the same live-marker drift the document itself names as how the V4 row came to carry a hash no seat gated. A dispatcher keying on the trailing token would bind the V6 identity to a V7 file.
   EXACT REPLACEMENTS: (i) "**Tori, 2026-09-04 21:32 KST. Version 7. ORDERED by Duho, "redesign r3c", 2026-09-04 21:30 KST; amendment history in §10.**" (ii) "R3C2_PREREG_V7_NONDEFINITIONAL_READY".

   7b (MINOR, gate record). QUOTES: (i) "**Nine dispatched gate runs across R3C and R3C2, no `SOUND` without repairs.**" (ii) header/§0: "which failed four gate rounds across two engines" / "Four gates, two engines, all `UNSOUND`".
   DEFECT: "Nine" is not derivable from this document. §10's own tables name eight gate reports (two R3C, six R3C2); §0's "four" for R3C alone would make ten. A section whose stated virtue is checkability ("Every row is checkable") carries a summary count its tables do not support.
   EXACT REPLACEMENT: "**Every dispatched gate run across R3C and R3C2 is a row in one of the two tables above; the count is the row count of those tables, and no `SOUND` without repairs appears among them. Any run not tabulated — including the predecessor rounds §0 summarises as four — is named explicitly or the summary count is struck.**"

Summary: the anti-circularity architecture (excised interpretation, mechanical redaction with asserted packet, provenance-based ledger with computed roots, blind-double classification, external seed and custody) is fundamentally sound, and the held §3 clause is not counted here. The repairs in 1a, 1b, 2a, 2b, 2c, 3b, 7a, 7b are each local and specified exactly; 2a must land in the same amendment as Duho's ruling on the held clause. With those applied, this design freezes.

R3C2_V7_KIMI_COMPLETE
