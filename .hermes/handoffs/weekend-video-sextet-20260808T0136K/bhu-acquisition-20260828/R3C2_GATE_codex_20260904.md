ACCESS_SHA=977cc127d7bc161d7956d2d684bd391c00d9ff21013843ee02f354d13f226799
GATE=PREREG_UNSOUND

1. QUOTE: “A tally containing **one or more `REPRO_EXACT`** is a counterexample to the pattern and the pattern record must be amended.” DEFECT: §7 discloses the hypothesis-to-outcome mapping in the same preregistration used by the enumerating and classifying seats. A seat need not open the named pattern record to know which outcomes support or break it, so the pattern can still influence enumeration, `NOT_ATTEMPTED`, input classification, and borderline arithmetic rulings. This defeats the claimed pattern blindness and leaves a path for the hypothesis to reach its evidence. EXACT REPLACEMENT: “The seat-visible protocol contains no statement mapping any census outcome to the pattern. Before dispatch, an independent custodian receives and hashes a separate comparison rule; neither seats nor Tori may access that rule until the custodian has received and acknowledged the canonical sealed-tally hash. Only then may Tori receive the rule and perform a comparison that cannot change the census artifact.” Delete the two sentences in §7 that state which tallies support or counterexample the pattern from every seat-visible packet.

2. QUOTE: “Each seat prints every path it opened; the presence of `SHAPE_MAGNITUDE_PATTERN_RECORD_20260904.md`, `BHU_CORPUS_SYNTHESIS_20260902.md`, or any `R3C*`/`R3D*` file in that list **fails the run**.” DEFECT: C4 is a filename audit, not mechanical pattern blindness. It cannot detect prior exposure, a relayed description, copied content under another name, messages, or the mapping disclosed by §7 itself; read literally, it also fails a seat for opening this `R3C2*` preregistration. EXACT REPLACEMENT: “C4 — pattern blindness. Seats receive only an allowlisted, content-hashed packet that omits §7 and every statement of the pattern or its outcome mapping; they run in an isolated workspace with no messaging or network access, print every opened path, and attest before dispatch that they have had no prior access to the pattern or prior R3C/R3D work. Any prior exposure, non-allowlisted read, communication, or failure of isolation yields `C4_PATTERN_BLIND=FAIL` and voids that seat. Opening the hashed seat protocol itself is permitted.”

3. QUOTE: “For every quantitative claim in the corpus, **does the paper's own printed number follow from the paper's own stated inputs with no quantity chosen, fitted or imported undeclared?**” DEFECT: “quantitative claim” has no operational enumeration rule, while `NOT_ATTEMPTED` lets a seat remove any item it says has no number. Thus a pattern-aware or merely inconsistent seat can change the denominator before the tally. The route is also internally confused: an item with no number cannot be a claim whose “own printed number” is being tested. EXACT REPLACEMENT: “The census population is every author-asserted numerical value printed in the frozen corpus’s title, abstract, body, display equations, tables, or figure captions, excluding only bibliographic metadata, dates, page/line/equation labels, and numbers appearing solely inside quotations from other works. Limb A records every occurrence before deduplication; identical repetitions are merged only when paper, quantity, value, units, and stated derivation are identical, with all merged locations retained. Every retained item must receive a §3 reproduction outcome; `NOT_ATTEMPTED` is deleted.”

4. QUOTE: “**A seat may not supply a value for an `ABSENT` input.** Encountering one ends that claim's attempt.” DEFECT: no §3 outcome covers this mandated stop. `REPRO_AFTER_CHOICE` says the number follows only after a value is supplied, but supplying an `ABSENT` value is forbidden; `REPRO_BLOCKED` applies only to an unobtainable source. The input classes also overlap because a value may be both printed and “standard,” and “Planck 2018 parameters” is neither a measured constant nor a single frozen value set. Per-claim outcomes are therefore not exhaustive and input classification is not reproducible. EXACT REPLACEMENT: “`REPRO_MISSING_INPUT` — at least one required input is neither printed nor declared by a specific obtainable citation; name each missing input and do not supply a value.” Replace the input rule with: “Classify each input by precedence as `PRINTED` if its value is printed in the paper, `CITED` if the paper identifies a specific source, `STANDARD` only if it is a frozen mathematical definition or CODATA constant listed with value, units, edition, and hash in an appendix, otherwise `ABSENT`. Observational parameter sets, including Planck 2018, are never `STANDARD`. An unobtainable `CITED` source yields `REPRO_BLOCKED`; an `ABSENT` input yields `REPRO_MISSING_INPUT`.”

5. QUOTE: “If the enumeration is itself contested between the two seats beyond a stated tolerance, stop and report — a census whose denominator is disputed cannot proceed.” DEFECT: the “stated tolerance” is not stated, and this required stop has no study-level outcome. Section 4 classes can also overlap (`CENSUS_PARTIAL` with `R3C2_NO_CLASS`, and an all-seat C6 failure with `CENSUS_AUDIT_FAILED`), with no precedence. Separately, §9 calls `SYMBOLIC_TIMEOUT` a reportable outcome although §3 does not declare it. The design can therefore stall with no class or with two classes. EXACT REPLACEMENT: “`CENSUS_DENOMINATOR_DISPUTED` — the two frozen enumerations differ on any included item after one reconciliation attempt; report the symmetric difference and stop. INCONCLUSIVE.” Add “`REPRO_TIMEOUT` — the declared 120-second cap is reached; record the operation and harness.” Then replace §4’s decision rule with: “Assign exactly the first applicable class in this order: `R3C2_NO_CLASS` for any non-audit control that fails in every valid seat that attempted it after two attempts; `CENSUS_DENOMINATOR_DISPUTED`; `CENSUS_AUDIT_FAILED`; `CENSUS_PARTIAL` if any retained item lacks a final per-claim outcome after two attempts; otherwise `CENSUS_COMPLETE`. Audit disagreement is governed only by `CENSUS_AUDIT_FAILED`.”

6. QUOTE: “A script asserts no `ABSENT` input was given a value.” DEFECT: C3 names an assertion but does not specify the machine-readable ledger, the fields inspected, or whether arithmetic can consume values outside that ledger, so the asserted control does not mechanically establish no substitution. C6’s “random 20%” likewise lacks a population, rounding rule, selection time, and seed. EXACT REPLACEMENT: “C3 passes only if every arithmetic script reads inputs exclusively from the frozen per-claim ledger; the validator rejects any consumed input without a ledger row, rejects any consumed `ABSENT` value, and records script and ledger hashes.” Replace the C6 sampling phrase with: “After the complete ordered claim list and first-seat outcomes are frozen, select `ceil(0.20 × N)` retained claims without replacement using SHA-256 counter-mode draws seeded by the predeclared future public seed [name the source and draw], and publish the seed, algorithm, ordered population hash, and selected indices before the auditor begins.”

7. QUOTE: “Only once the tally is sealed and hashed does Tori compare it with `SHAPE_MAGNITUDE_PATTERN_RECORD_20260904.md`.” DEFECT: hashing is only self-consistent tamper evidence unless the pre-access hash is committed outside Tori’s control. “Recorded in the result” can be silently rewritten together with the tally and result, and no canonical serialization is defined. The seal therefore is not real against the actor performing §7. EXACT REPLACEMENT: “Serialize the tally as canonical UTF-8 JSON with sorted keys, fixed field order, and LF endings; an independent custodian timestamps and acknowledges its SHA-256 before Tori receives or opens the comparison rule or pattern record. The result records the artifact path, byte count, pre-access hash, custodian acknowledgment, comparison-access time, and post-comparison hash; any mismatch yields `CENSUS_AUDIT_FAILED`, and no replacement tally is admissible under the same version.”

8. QUOTE: “**Wording: "unreproduced from the stated inputs," not "error."**” DEFECT: none. The fairness wording is exact and is not contradicted elsewhere in the preregistration. EXACT REPLACEMENT: NONE.

Answers to 1, 2 and 9:

1. No. The old category criterion is gone, but circular influence is not actually gone: §7 reveals the pattern’s outcome mapping before the tally, enumeration is undefined, `NOT_ATTEMPTED` can alter the denominator, input classification has discretion, and C4 cannot detect knowledge obtained without opening the listed filenames. The tally itself cannot be altered legitimately after sealing, but the current seal can be replaced silently because it lacks an external pre-access commitment.

2. Yes, the core criterion “reproduce the printed number from stated inputs” is ordinary reproduction and is external to, and not a restatement of, the shape/magnitude pattern. However, §7’s claim that any `REPRO_EXACT` is a counterexample and that a tally of `REPRO_AFTER_CHOICE` and `REPRO_FAILED` supports the pattern is a separate, undefended construct-validity bridge; exact reproduction using an observational input does not by itself show that the construction fixes a magnitude. That bridge must be independently preregistered and gated after the census design is fixed.

9. Worth ~3–4 seat-days: No, not as written; the missing outcome classes, denominator stop, non-blind packet, and non-custodied seal can waste the full run without an interpretable result. Better than the replaced design: Yes in concept, because plain reproduction is genuinely independent of the lane-authored pattern, but it is not yet better in executable form until the leakage and exhaustiveness defects above are repaired.

R3C2_GATE_COMPLETE
