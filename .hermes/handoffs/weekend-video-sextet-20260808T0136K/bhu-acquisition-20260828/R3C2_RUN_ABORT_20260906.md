# R3C2 RUN — ABORT filed 2026-09-06 09:42:21 KST (abort condition A3 of the run plan; census STOPPED)

**Observed state.** Seat A (codex) exited at 09:40:5x KST, 2 minutes after launch, with ACCESS_SHA equal to the packet digest, a
complete report (`R3C2_SEAT_COMPLETE`), no ledgers, and this result in its own words:
> "none of the 89 enumerable source texts listed in `R3C2_CORPUS_MANIFEST.md` exists in the seat working directory. Every attempted
> source open returned `No such file or directory`. The packet prohibits opening any path outside this directory … Seat disposition:
> `R3C2_NO_CLASS` (seat-isolation/corpus provisioning failure before a valid census could be dispatched)."
Its path list shows every source attempted as a child of the working directory, `IN_SCOPE, MISSING`; no OUT_OF_SCOPE row.

**Cause, as the evidence shows it (a fact about my step 3, not a judgement about the design).** I built each seat's tree as
`<seat>/lane/` (working directory) plus `<seat>/bhu-reading-20260823/sources/` so that the manifest's stated relative path
`../bhu-reading-20260823/sources/` would resolve. The dispatcher confines the seat to the working directory (`--add-dir` = lane/),
and my brief told the seat it "may open nothing outside it". So the sources were present in the tree and in the dispatch inventory,
but unreachable from where the seat was allowed to look. The frozen design's C4 says the copy directory itself contains the pinned
sources; my layout put them one level up.

**Which abort.** A3 in substance: a copy directory the seat cannot use. Filed as A3. The seat did not fail (A4) and no control
split occurred; the seat correctly reported what it saw.

**What is filed.** This record; seat A's artefacts left in place at `/Users/duhokim/HermesOps/r3c2_run_20260906/A/lane` (report, stdout, stderr); the run log entry below.
Seat B (kimi) was still running at 2026-09-06 09:42:21 KST under the same layout; it was NOT stopped by me, because stopping it is an action and
the rule is file-and-stop, not judge. Its output, whatever it is, is void under this abort and will be archived unread.

**What the plan says next (for Blanc and Duho, not for me to decide).** A3: "Rebuild the copy; resume at step 3." The fix is
lane-side only: place the 89 sources INSIDE each seat's working directory at a path the manifest names (e.g. a `sources/`
subdirectory, with the seat brief's path line and the dispatch inventory pointing there), re-inventory, re-dispatch both seats from
fresh copies. No frozen byte is touched; the packet's manifest states the freeze-time location, not the copy's, so no design change
is implied — but that reading is mine and is offered, not applied.

**Cost so far:** 2 minutes of seat A; seat B's run to whenever it exits or is stopped.

R3C2_RUN_ABORT_COMPLETE
