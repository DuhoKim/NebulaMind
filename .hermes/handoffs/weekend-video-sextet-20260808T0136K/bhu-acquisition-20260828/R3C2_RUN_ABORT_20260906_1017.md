# R3C2 RUN — second ABORT, filed 2026-09-06 10:18:40 KST — the frozen design's scope rule and its harness contradict each other; census STOPPED

**Observed state.** Seat A (codex-cli 0.153.4, gpt-6-astra, sandboxed) exited at 10:17:06 KST, 2 minutes after launch, with
ACCESS_SHA equal to the packet digest, a complete report, no ledgers, every control `NOT_RUN` except C4/C5b PASS, and:
> "The run stopped at the harness prerequisite, before corpus enumeration. The user's instruction permits opening nothing outside
> the working directory. Packet C4 also says: 'Do not construct a path outside it; if you believe you need one, stop and report
> that instead of opening it.' However, C5 requires `shasum -a 256 /usr/bin/python3` … C5 commands were not executed."
> "Study-level outcome: R3C2_NO_CLASS."
Its path list shows six in-scope files and nothing else; it did not touch a source.

**Cause, quoted from the FROZEN text (V23, 55b466fa…), not from my wrapper.** Packet C4: "Work **only** from the files in your
working directory … Do not construct a path outside it; if you believe you need one, stop and report that instead of opening
it. … Any path outside the working directory is `FAIL`." Frozen brief: "Work only from the files in this directory … Do not
open any other path." Packet C5: "Execute and print `/usr/bin/python3 --version`, `/usr/bin/python3 -c "import sympy; …"`, and
`shasum -a 256 /usr/bin/python3`." `/usr/bin/python3` is a path outside the working directory. A seat that obeys C4 and the
brief to the letter cannot execute C5; a seat that executes C5 has, by C4's wording, constructed an outside path. The packet
nowhere says that the interpreter, `shasum`, or what they load are in scope. R3D's run brief (2026-09-05) said exactly that
in words; R3C2's frozen text does not.

**My contribution, stated so it is not hidden:** the access-proof wrapper I wrote around the frozen brief added "you may open
nothing outside it", which the seat quoted first. That sentence is mine and unsigned; removing it does not remove the
contradiction in the signed text, and a strict seat could stop on the frozen wording alone.

**What this is.** Not A4 (the seat completed correctly) and not A3 (the copy was reachable). It is the case the diagnosis of
09:2x was asked to rule out and, on the then-evidence, ruled out: **the signed V23 describes a control (C5) that a seat cannot
execute without breaking another control's rule (C4/brief).** The 09:2x diagnosis was about the lane's step-0 harness and
was right about that; it did not test whether the SEAT's harness step is executable under the seat's own scope rule, because
no seat had yet run V23's C5 under a live "nothing outside" instruction. The gates (V17–V23) executed no seat step; C0
exhibited C5 as reachable by reading, not by running.

**What fixing it touches.** Frozen bytes. The smallest repair is one sentence in C4 (or C5b) of the design: "the system
binaries C5 names — `/usr/bin/python3`, `/usr/bin/shasum` — and what they load are in scope and are not 'outside paths'",
plus the same sentence in the frozen brief, then a rebuilt packet, new pins, a §10 amendment (V24), a C0 and a gate, and a
new signature. That is Duho's decision, not mine and not Blanc's. Dropping my wrapper sentence is lane-side and is done in any
case, but it is not sufficient.

**Seat B** (kimi-k3, sandboxed, same wrapper) was still running at 2026-09-06 10:18:40 KST with 2 new files; it was NOT stopped by me. Its output is
void under this abort whatever it contains and will be archived unread.

**Not done:** no fix, no re-dispatch, no edit to V23, no claim read by a seat.

R3C2_RUN_ABORT_1017_COMPLETE
