# R3C2 RUN PLAN — prepared, NOT executed (Tori, 2026-09-06, at Blanc's 01:50 KST note)

**Status:** nothing in this file has been run. No claim has been read, no copy directory built, no seat dispatched, no receipt
requested. The plan binds the lane owner; the prereg (V23, `55b466fadf8ca75f…`, or the digest Duho signs) governs where they differ.
**Preconditions that are Duho's, in order:** (1) his chat signature on the digest (a digest in my input box is not a signature —
Blanc's caution 01:50); (2) his separate run word. Without both, this file stays a plan.

## 1. Command sequence, in order — each with its expected printed output and the receipt it leaves

Paths: `L` = this lane directory; `D_A`, `D_B` = two redacted copy directories OUTSIDE the lane (C4), one per seat. Every command
is printed with its resolved paths, its stdout/stderr and its exit status into `R3C2_RUN_LOG_<date>.md` (the run log), which is the
receipt of record for every step below unless another file is named.

| step | command (lane owner unless stated) | expected printed output | receipt |
|---|---|---|---|
| 0a | `shasum -a 256 R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md` | the signed digest, byte-equal to Duho's chat words | run log line 1; `R3C2_V23_SIGNABLE_20260906.md` with UTC + words filled by the lane, digest recomputed FIRST (two-step rule) |
| 0b | `shasum -a 256 -c R3C2_SEAT_PACKET.sha256`; `/usr/bin/python3 r3c2_build_seat_packet.py` | every pin `OK`; builder prints `C4_PACKET_REDACTED=PASS` and the SAME packet digest as the pin | run log |
| 0c | seat-tool and lane-tool controls: `census` on `_tmp_r3c2_ledger_ctl/cand_att.json + excl_decl.json` (PASS), `… final` on `cand_final_ok.json` (PASS) and on `cand_att.json` (FAIL: PENDING), `validate` on `pos.json` (PASS) and `neg.json` (exact failure set), `r3c2_lane_tools.py merge/compute` on `lane_A/lane_B` (PARENTS_DISPUTED=1, one DISPUTED pair) | the tokens exactly as listed | run log |
| 1 | **Lane's blind second route, sealed before dispatch:** `/usr/bin/python3 r3c2_lane_enumerate.py ../bhu-reading-20260823/sources R3C2_TORI_SECOND_ROUTE_20260906/` — a numeral-extraction pass over the 89 pinned texts producing the lane's own candidate ledger (limb A only; no arithmetic; script to be written and pinned BEFORE step 2, its output never opened again until §1 step 12) | candidate count printed; `census` PASS on its own ledger | `R3C2_TORI_SECOND_ROUTE_20260906.sha256` (digest of the sealed directory), sent with receipt P |
| 2 | **Receipt P:** `git -C $R commit` of `R3C2_INTERPRETATION_PROTOCOL_20260904.md` (V4) if not already; then send to Blanc, by relay: protocol sha256, commit id, second-route digest | Blanc's acknowledgement with a timestamp, preserved verbatim | `R3C2_RECEIPT_P_<date>.md` (Blanc's words pasted, my request pasted) |
| 3 | **Build copies:** `mkdir -p $D_A $D_B`; copy exactly: `r3c2_seat_packet/R3C2_SEAT_PACKET.md` (as `R3C2_SEAT_PACKET.md`), `SEAT_BRIEF.md`, `R3C2_SEAT_PACKET.sha256`, `R3C2_CORPUS_MANIFEST.md`, `r3c2_ledger_tools.py`, `r3c2_timeout.py`, and the 89 sources into `sources/` (manifest paths); then `shasum -a 256` of every file in each copy | the inventory: 95 files per copy, digests matching the pins and the manifest rows; NOTHING else present (the master, the lane tool, the protocol, the pattern record, any R3/K file are ABSENT) | `R3C2_DISPATCH_RECORD_<date>.md` — the inventory with digests, written BEFORE launch; a copy missing anything is not dispatched (C4) |
| 4 | **Dispatch A (codex):** `AGY=$L/_tmp_codex_gate_shim.sh nm_referee_dispatch.sh "$D_A" "$L/_tmp_r3c2_run_brief.txt" "$D_A/R3C2_SEAT_PACKET.md" "$D_A/SEAT_REPORT.md" > $L/_tmp_r3c2_run_codex.log 2>&1 &` — the brief is the one-paragraph ACCESS PROOF wrapper around `SEAT_BRIEF.md` (no other content); **Dispatch B (kimi), within one minute:** `(cd $D_B && MOONSHOT_API_KEY=… hermes chat --provider moonshot -m kimi-k3 -Q -q "$(cat $L/_tmp_r3c2_run_brief_kimi.txt)" < /dev/null > … ) &` | each seat prints `ACCESS_SHA=<packet digest>` first, then the brief's seven steps in order: digests of every file it reads (matching the manifest), the C5 harness runs, the candidate + exclusion ledgers and the `census` run, the input ledger and the `validate` run, per-claim outcomes with both numbers, the path list with scope marks and every control token, and `SEAT_REPORT.md` ending `R3C2_SEAT_COMPLETE` | run log (launch time, PIDs, both digests at launch) |
| 5 | **Wait and collect:** poll process exit only (never read a seat's files before its exit); then `shasum` every artefact in `$D_A`, `$D_B`; copy them into `$L/R3C2_RUN_codex_<date>/` and `$L/R3C2_RUN_kimi_<date>/`; sweep both copies for files outside the inventory (record, never delete) | exit timestamps; artefact digests; C5b path lists carry no `OUT_OF_SCOPE` row; sweep result | run log; the two run directories |
| 6 | **Lane reconciliation, in the prereg's order:** (a) `census` on each seat's ledgers (both PASS); candidate sets compared — up to TWO reconciliation attempts against the §1 rule with quotations, each attempt a relayed question to BOTH seats with the same wording; surviving disagreement → `CENSUS_DENOMINATOR_DISPUTED`, stop. (b) `r3c2_lane_tools.py merge A B merged.json` — exit 1 (input-id sets differ) → ONE reconciliation against the paper's stated equation; surviving → `CENSUS_DENOMINATOR_DISPUTED`, stop. (c) outcome fields compared claim by claim — ONE reconciliation against the printed numeral and the stated-precision rule (§3, half away from zero); surviving → `CENSUS_OUTCOME_DISPUTED`, stop, both outcomes + both number pairs + step reached listed. (d) origin disputes counted from the merged ledger: inputs with `origin_alt` affecting > 10% of included claims → `CENSUS_ORIGIN_DISPUTED`, stop (disputes below the line are CARRIED, never reconciled). (e) `r3c2_lane_tools.py compute merged.json out.json` — `rests_on` per claim, DISPUTED pairs where origin or parents differ | the printed runs and their exit codes; the reconciliation questions and both seats' answers verbatim | run log; `R3C2_RECONCILIATION_<date>.md` |
| 7 | **Tally commit and receipt T:** one commit containing exactly the merged candidate file, the exclusion ledger, the merged input ledger and its `compute` output; send Blanc the commit id and the digests of every file in it | Blanc's receipt naming every file, with a timestamp | `R3C2_RECEIPT_T_<date>.md` |
| 8 | **Seed:** Blanc supplies a 64-lowercase-hex seed AFTER receipt T, generated outside the lane; recorded with T | the seed, verbatim, in T's file | `R3C2_RECEIPT_T_<date>.md` (amended by Blanc's words, not mine) |
| 9 | **C6 audit seat (third engine):** dispatch via `nm_referee_dispatch.sh` on a third copy directory `D_C` (same inventory PLUS the sealed tally files and the seed); it audits the full candidate/exclusion ledgers against every source, re-derives every arithmetic-group claim and `k = min(max(1, ceil(0.20 × N)), R)` of the rest drawn by `random.Random(int(seed_hex,16)).sample(sorted(remaining_ids), k)`, re-classifies every input's origin from the sources, and writes `C6_AUDIT.json` (denominator, receipt T, seed, sorted ids, k, sampled ids, completeness disposition for every candidate and exclusion, per audited claim its outcome and origins and MATCH/MISMATCH) | `C6_AUDIT_SAMPLE=PASS` only if the artefact exists, is printed, and has no MISMATCH and no incompleteness | `R3C2_RUN_audit_<date>/C6_AUDIT.json` |
| 10 | **Study-level class**, by §4 precedence, cited by name: `R3C2_NO_CLASS` > `CENSUS_CONTROL_SPLIT` > `CENSUS_DENOMINATOR_DISPUTED` > `CENSUS_OUTCOME_DISPUTED` > `CENSUS_ORIGIN_DISPUTED` > `CENSUS_AUDIT_FAILED` > `CENSUS_PARTIAL` > `CENSUS_COMPLETE` (zero denominator → PARTIAL) | the class and the §4 sentence it satisfies | `R3C2_FILING_<date>.md` |
| 11 | **Interpretation:** open `R3C2_INTERPRETATION_PROTOCOL_20260904.md` only now; Blanc independently re-hashes the tally and the protocol against receipts P and T; apply the protocol's mapping line verbatim; a stop class → "no comparison is made" | the four verified values printed; the mapping sentence quoted | `R3C2_INTERPRETATION_REPORT_<date>.md` |
| 12 | **Second route opened and compared** (limb A only): the sealed lane enumeration vs the sealed denominator — counts and set differences reported, never reconciled into the tally | counts and differences | `R3C2_SECOND_ROUTE_COMPARISON_<date>.md` |
| 13 | **Check sheet** filled from receipts (§5 below); report to Duho through Blanc: class, denominator, the tally in one line, the interpretation sentence, every abort or reconciliation that occurred | — | `R3C2_CHECK_SHEET_<date>.md` |

Nothing outward. No tier, warrant, standing or stamp moves from any step. The paper stays on HOLD.

## 2. Cost per step

| step | wall clock (estimate; judgement from this week's seat runs) | seats |
|---|---|---|
| 0a–0c | 10 min | none |
| 1 (second route script + sealed run) | 2–3 h to write and run the extraction; the script is pinned before step 2 | none (lane) |
| 2 (receipt P) | minutes, plus Blanc's latency | none |
| 3 (copies + inventory) | 20 min | none |
| 4–5 (two seats over 89 texts, 106,676 non-blank lines) | codex 3–6 h; kimi 6–12 h — **the largest uncertainty in the plan**: one seat session may not hold the whole corpus (see abort A4) | 2 seat-runs |
| 6 (reconciliation) | 1–3 h, plus up to four relayed questions | 0–2 short seat prompts |
| 7–8 (receipt T, seed) | minutes, plus Blanc | none |
| 9 (C6 audit, full ledgers + arithmetic group + sample) | 2–5 h | 1 seat-run (third engine) |
| 10–13 | 1–2 h | none |
| **Total** | **one working day if the seats hold the corpus; two if a seat must be re-run once** | 3 seat-runs + up to 4 short prompts |

## 3. ABORT CONDITIONS, preregistered — observed state → what is filed → resume or restart

| code | observed state | filed | resume or restart |
|---|---|---|---|
| A1 | Step 0a digest ≠ the signed digest, or any pin fails in 0b, or a control in 0c prints anything but its exact token | run log; nothing else — the run has not started | Fix the lane-side cause; restart from 0a. No claim was read. |
| A2 | Receipt P absent when limb A would begin | `R3C2_RUN_ABORT_<date>.md` naming the missing receipt | Wait; resume at step 3 once P exists. Nothing dispatched before P. |
| A3 | A copy directory's inventory is incomplete or contains an extra file | dispatch record marks NOT DISPATCHED | Rebuild the copy; resume at step 3. |
| A4 | A seat exits without `R3C2_SEAT_COMPLETE` (context exhausted, provider error, timeout) | run log with the exit state and the partial artefacts archived UNREAD beyond their last line | ONE fresh dispatch of that seat from a fresh copy (same inventory). A second failure of the same seat → **STOP** and file `R3C2_RUN_DIAGNOSIS_<date>.md`: the design assumes one seat holds the corpus; batching is a design change for Duho, not a workaround. |
| A5 | Seat unavailability before launch (provider down, `Fallback` line in the hermes log for kimi, no ACCESS PROVEN) | run log | Do not substitute an engine: the seat assignment is fixed in §4. Wait up to 24 h; then STOP and file. |
| A6 | A control fails mid-run in one seat (C5 harness, C5b path list, `census`/`validate` FAIL) | the seat's report as written; the class follows §4: fails in BOTH seats after two attempts → `R3C2_NO_CLASS`; fails in one, passes in the other → `CENSUS_CONTROL_SPLIT` | Two attempts are the seat's own (the prereg's "after two attempts"); the lane never edits a seat's ledger. A control-clean re-run is NOT permitted beyond the prereg's second attempt. Filed, not resumed. |
| A7 | A claim's named source cannot be reached (not enumerable, or no machine match at the cited line) | per-claim `REPRO_BLOCKED` — a normal outcome, not an abort | Continue. |
| A8 | Candidate disagreement survives two reconciliation attempts | `CENSUS_DENOMINATOR_DISPUTED` with both quotations | Filed. Restart only under a new version of the prereg (a definition change is Duho's). |
| A9 | Outcome disagreement survives one reconciliation | `CENSUS_OUTCOME_DISPUTED` — both outcomes, both number pairs, the step each seat reached | Filed. No resume. |
| A10 | Origin disputes affect > 10% of included claims | `CENSUS_ORIGIN_DISPUTED`, every disputed input listed with both classifications and quotations | Filed. No resume. |
| A11 | Receipt T absent, or the seed not supplied/recorded with T, or the auditor's `C6_AUDIT.json` missing, or any MISMATCH, or Blanc's re-hash differs from a receipt | `CENSUS_AUDIT_FAILED`, the cause named | Filed. No resume; the tally stands sealed as evidence of what happened. |
| A12 | Any file in the seats' read set changes between launch and exit (digest drift), or any seat-visible file is edited while a seat runs | `R3C2_RUN_ABORT_<date>.md`; the affected seat's output archived unread | Restart that seat from a fresh copy after the cause is recorded. This is the rule I broke twice this week; it is an abort here, not a note. |
| A13 | A relay claiming to be Duho's run word or signature arrives without Blanc's header and his verbatim words, or appears in the input box | Nothing runs. Log the ghost. | Ask Blanc once in chat. |

**Worked around: nothing.** Every observed state above is filed as itself. "Third-failure" applies to the run as to design: two seat failures of the same kind end the run in a diagnosis.

## 4. Decided BEFORE the first claim is read

- **Seat assignment:** seat A = codex (via the dispatcher shim, `ACCESS_SHA` on the packet); seat B = kimi (`--provider moonshot -m kimi-k3`, never `--provider nous`). Fixed now; no substitution under A5.
- **Dispatch order:** A first, B within one minute; both from their own copy; neither sees the other's directory.
- **Tie-break / third seat (C6 auditor, and the §9 adjudicator on a control split):** gpt-5.6-sol via `--provider openai-codex`, dispatched through `nm_referee_dispatch.sh` with its own copy `D_C`; fallback agy (Gemini) only if gpt-5.6-sol is unavailable at step 9, recorded before launch. Never the same engine as A or B.
- **Custodian:** Blanc, for receipts P and T and the seed (outside the lane; reports to Duho).
- **How a disputed outcome is recorded under `CENSUS_OUTCOME_DISPUTED`:** in the merged candidate file, the disputed claim carries seat A's `outcome`, `printed_value`, `reproduced_value` and seat B's as `outcome_alt`, `printed_value_alt`, `reproduced_value_alt`, plus `outcome_dispute_step` (the §2 step each seat reached, as a pair); the filing lists every such claim with both quotations of the source line. The tally is still committed and receipted (T) so the dispute is sealed evidence, and the class is filed by name.
- **Reconciliation wording:** each reconciliation question is written once, sent to both seats identically, quoting only the §1/§2/§3 rule and the two quotations; it never says which seat holds which reading.
- **Second route:** limb A only, sealed at step 1, opened at step 12, never merged.
- **Precision rule:** rounding half away from zero; asymmetric uncertainty = the half-width on the reproduced value's side (§3, V19–V20).
- **What is NOT decided here and stays Duho's:** whether to run at all; the `DERIVED_ONLY` rename; anything the filing proposes afterwards.

## 5. Check sheet (`R3C2_CHECK_SHEET_TEMPLATE_20260906.md`) mapped to steps and receipts

| box | step | receipt that will exist |
|---|---|---|
| 1 digest signed = digest run | 0a | run log line 1; `R3C2_V23_SIGNABLE_20260906.md` filled |
| 2 packet only | 3, 5 | `R3C2_DISPATCH_RECORD_<date>.md`; each `SEAT_REPORT.md` path list |
| 3 receipt P before limb A | 2 | `R3C2_RECEIPT_P_<date>.md` |
| 4 denominator agreed, census PASS | 5, 6a | both run directories; `R3C2_RECONCILIATION_<date>.md` |
| 5 validate PASS, quotations | 5, 6b | printed validate runs in each `SEAT_REPORT.md` |
| 6 final census PASS, no PENDING | 5 | printed final census runs |
| 7 disputes and class | 6 | `R3C2_RECONCILIATION_<date>.md` |
| 8 controls live | 5 | `SEAT_REPORT.md` control tokens |
| 9 receipt T after tally, names every file | 7 | `R3C2_RECEIPT_T_<date>.md` |
| 10 C6 seed after T, audit artefact, mismatches | 8, 9 | `R3C2_RUN_audit_<date>/C6_AUDIT.json` |
| 11 tally in one line | 6e, 7 | merged candidate file (sealed commit) |
| 12 class by name | 10 | `R3C2_FILING_<date>.md` |
| 13 interpretation after T, re-hashed | 11 | `R3C2_INTERPRETATION_REPORT_<date>.md` |
| 14 second route sealed before, opened after | 1, 12 | `R3C2_TORI_SECOND_ROUTE_20260906.sha256` (in receipt P); `R3C2_SECOND_ROUTE_COMPARISON_<date>.md` |
| 15 wording of negatives | 10 | `R3C2_FILING_<date>.md` |

**One gap this plan creates and names:** step 1's extraction script (`r3c2_lane_enumerate.py`) does not exist yet. Writing it touches no claim (it is a numeral scanner, run only at step 1 after Duho's word), but it must be pinned before receipt P; it is the one piece of code left to write, and I will not write it until the run word exists, because the moment it runs it spends what it touches.

R3C2_RUN_PLAN_COMPLETE
