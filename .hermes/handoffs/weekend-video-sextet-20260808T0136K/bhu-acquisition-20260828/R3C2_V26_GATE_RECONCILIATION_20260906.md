# R3C2 — two-seat gate on V26 (`22f01220…`), reconciled BY TOPIC (2026-09-06 23:53 KST)

Reports: `R3C2_GATE_V26_codex_20260906.md` (PREREG_UNSOUND; written 23:26, wrapper absent at the 23:34:32 check), `R3C2_GATE_V26_kimi_20260906.md`
(PREREG_SOUND_WITH_REPAIRS; written 23:36, count 0 observed 23:38:50). Access lines verified. Blind intact (codex LEAK=NONE). C5 YES, no masked stage, both.
Blanc's orders 23:44 / 23:45: prioritise the graph bypass and the cross-batch lifecycle; extend the evidence check without bending ABSENT into UNDECLARED; the DERIVED_ONLY rename is label-only (both reviewers) and is implemented as routine.

| topic | codex | kimi | disposition in V27 `d2df83bfa9c1ba50…` |
|---|---|---|---|
| audit compares only origin labels, not dependency edges (a wrong graph changed rests_on and PASSED) — the graph bypass | F2 | — | tool: reconstructed records, edges, root recomputation; text; control + probe |
| limb-B files under suffixed names are not what the chain binds; per-session validate cannot resolve cross-batch derived_from — the lifecycle | F4, F5 | F2 | text + brief: separate limb-B directory, canonical names; per-session validate NOT_RUN, joined validation before agreement |
| evidence check for every non-silent record before any branch | F1 | F4 | tool + text: extended to every status; ABSENT keeps the origin its evidence supports; controls + probes |
| C3's second lane-tool pin and two-argument compute | F3 | F1, N2 | text; compute without candidates exits 2; control |
| C5 manifest pin stale | F6 | F3 | text |
| manifest follows a symlinked root | F7 | — | tool + text; control + probe |
| local-import summary sentence; build command; docstrings; ownership-list provenance | C1, C2 | N1, N3 | applied |
| DERIVED_ONLY name | C3 | (earlier note) | label-only by both; renamed DERIVED_STANDARD_OR_MEASURED_ONLY in master, lane tool, protocol V5, kit; historical token preserved (§10, V23 DoR, protocol V1–V4) |

Kit: 141 controls, 49 deletion probes, PASS; both pin sheets verify. C0 and the gate run on V27 next.
