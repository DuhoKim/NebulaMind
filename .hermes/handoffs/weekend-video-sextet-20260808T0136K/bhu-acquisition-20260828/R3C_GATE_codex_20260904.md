ACCESS_SHA=c5e9462000f57bf8bb160734fa78859235c39e936e09a545fd44193003572381
GATE=PREREG_UNSOUND
1. QUOTED (confirmed present in the hashed file at lines 126–129): “If a seat cannot evaluate (b) because removing the BHU element leaves no defined model, condition 5 is recorded as `NOT APPLICABLE` for that candidate and conditions 1–4 decide; that outcome is reported, never silently treated as a pass.” DEFECT: This makes breaker condition 5 non-decidable for a reachable candidate and contradicts the stated all-five conjunction. If conditions 1–4 pass and comparator (b) is undefined, the candidate neither satisfies all five nor fails at a named condition, so it fits neither `CENSUS_COUNTEREXAMPLE` nor `CENSUS_PATTERN_HOLDS`. This is a real stop with no class. EXACT REPLACEMENT: “If removing the BHU-specific element leaves no defined model, record `CONDITION_5_FAIL_COMPARATOR_UNDEFINED` for that candidate. The candidate does not satisfy condition 5 and is counted in `CENSUS_PATTERN_HOLDS`; report why the stripped comparator is undefined.”
2. QUOTED (confirmed present in the hashed file at lines 48–49): “CENSUS_CLASSIFICATION_ARTEFACT — the outcome depends on how \"observable magnitude\" is drawn. INCONCLUSIVE; state the boundary cases exactly. This self-critical class must be reachable and must not be quietly avoided.” DEFECT: The classes are not mutually exclusive because no precedence is stated. A boundary case can yield a counterexample under one defensible drawing and an exclusion under another, satisfying both this class and `CENSUS_COUNTEREXAMPLE`; an empty shortlist under one drawing and a nonempty shortlist under another can satisfy both this class and `CENSUS_NO_CANDIDATE`. EXACT REPLACEMENT: “CENSUS_CLASSIFICATION_ARTEFACT — if any row’s limb-A inclusion changes under two defensible applications of ‘observable magnitude’, this class takes precedence over `CENSUS_COUNTEREXAMPLE`, `CENSUS_PATTERN_HOLDS`, and `CENSUS_NO_CANDIDATE`. INCONCLUSIVE; state each boundary case and both resulting classifications exactly.”
3. QUOTED (confirmed present in the hashed file at lines 59–60): “C2 — condition-1 screen is mechanical. Each exclusion cites the row’s claim cell and names which of shape / scale / frequency / sign / ratio / form it is. `C2_SCREEN_MECHANICAL=PASS`.” DEFECT: This control still names the lane-authored claim cell as its evidence, contrary to §5’s source-paper requirement. Its named code can therefore be asserted beside the wrong artefact, and the control text itself preserves the circular route the later section tries to close. EXACT REPLACEMENT: “C2 — condition-1 screen is mechanical. For every exclusion, preserve a machine-readable record containing the row ID, the classification from {shape, scale, frequency, sign, ratio, functional form}, the pinned source-paper path, line number, and exact establishing quotation. Preserve and hash the verifier’s stdout showing quotation-at-line and forbidden-reference checks for every record. `C2_SCREEN_MECHANICAL=PASS` may be printed only when that artefact and verifier output are present; otherwise print `C2_SCREEN_MECHANICAL=FAIL`.”
4. QUOTED (confirmed present in the hashed file at lines 66–69): “C6 — adversarial re-screen. A second seat re-screens the excluded rows looking for a wrongly-excluded candidate, printing an independent list of exclusions for automated diffing against the first seat’s list; any row on one list and not the other is reported, not reconciled silently. A census that only checks its survivors is not a census. `C6_EXCLUSIONS_RECHECKED=PASS`.” DEFECT: C6 requires lists but does not require preservation of either list, the diff invocation, or its output. A seat can print the PASS assertion and say a diff occurred without supplying an auditable diff artefact. EXACT REPLACEMENT: “C6 — adversarial re-screen. The second seat preserves its independently produced machine-readable exclusion list before seeing seat 1’s list. Preserve and hash both lists, the exact automated-diff command, and complete diff output. Every mismatch is reported without silent reconciliation. `C6_EXCLUSIONS_RECHECKED=PASS` may be printed only with those artefacts; otherwise print `C6_EXCLUSIONS_RECHECKED=FAIL`.”
5. QUOTED (confirmed present in the hashed file at line 53): “R3C_NO_CLASS — a control fails in both seats after two attempts.” DEFECT: The stop rule is not exhaustive. A control can fail twice in one seat and pass in the other, or C6 itself can fail; neither event satisfies “fails in both seats,” yet the design supplies no further attempt, adjudication, or outcome class. EXACT REPLACEMENT: “R3C_NO_CLASS — after each seat has at most two attempts, any reached control that is not `PASS` in every seat required to run it, including C6, ends the study in this class. List each seat’s final control state. Controls in unreached limbs remain `NOT RUN` and do not trigger this class.”
6. QUOTED (confirmed present in the hashed file at lines 37–38): “Limb B (2–3 seat-days): apply conditions 2–5 to each survivor, in order, stopping each candidate at its first failure. Report the failure condition per candidate.” DEFECT: As written, this repeats already closed condition-specific work for the R3A coefficient row and R3B rigidity row, although those results are expressly context for this design. A census may incorporate them, but re-running closed tests is unnecessary and creates an undeclared conflict path if the rerun differs. EXACT REPLACEMENT: “Limb B (2–3 seat-days): apply conditions 2–5 to each survivor in order, stopping at its first failure. For a condition already closed by R3A or R3B, import the frozen result only after verifying the cited preregistration, result, and pinned-source hashes; do not re-run that closed condition. Run every unclosed condition, including condition 5, and report the failure condition per candidate. Any hash mismatch is `R3C_NO_CLASS`.”
7. DESIGN JUDGMENTS: Outcome classes are not exhaustive or mutually exclusive until repairs 1, 2, and 5; the inconclusive route is reachable. C1, C3, C4, C5, and C5b name exact codes, and the document correctly requires unreached-limb controls to be `NOT RUN`. C2 and C6 remain assertion-satisfiable until repairs 3 and 4. The circularity prohibition is substantively aimed at the right source: §5 requires independent pinned-source text, bans the pattern record as evidence, automatically checks quotation occurrence and forbidden references, diffs two screens, and uses a blind third-seat sample; I found no compliant route to exclude a row without independent source evidence once C2 is made controlling and its artefacts are preserved. Of the five breaker conditions, condition 5 is not decidable on the document’s expressly reachable undefined-comparator branch; conditions 1–4 have specified seat operations. The design repeats the closed R3A and R3B condition tests unless repair 6 is applied. The required fairness wording is exactly “unreproduced from the stated inputs,” not “error”; there is no slip. The design can stall without a class in the concrete cases stated in findings 1 and 5.
R3C_CODEX_GATE_COMPLETE
