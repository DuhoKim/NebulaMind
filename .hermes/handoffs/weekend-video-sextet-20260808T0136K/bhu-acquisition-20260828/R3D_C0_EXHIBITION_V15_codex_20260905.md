ACCESS_SHA=4e12ef211eea02808b36f1d9c0c756f7f5986459bb333aee8aa7f1cddcb4d708
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — R3D V15

Scope: this exhibition uses only `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md` and the numbers printed in it. The constructions below are machinery-test inputs, not claims about the Dymnikova sources or judgments of their physics. `DYM_FLOOR_COMPLETION_DEPENDENT` is retired and is not tested.

The document declares six classes: four scientific classes (classes 1–4) and two non-scientific terminal states (classes 5–6). The request's later phrase “five scientific classes” is inconsistent with §4's operative heading, table, and enumerated list; there is no fifth declared scientific class in V15.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `DYM_FLOOR_DERIVED` | Stated census/ledger configuration: a reproduced manifest relation gives the non-empty allowed set `M ∈ [10,∞) kg`; the exhaustive admissible-reading inventory contains only the completion-free reading. Thus `P={10 kg}`, `Z=∅`, `I=∅`. All reached controls pass. | §2 defines the completion-free admissible reading and the census → §3 limb B because a mass bound is reproduced → §4 positive-floor definition gives infimum `10 kg` → §4 class-1 row: `P` non-empty, all `P` agree, `Z=I=∅` → §4.1 files `DYM_FLOOR_DERIVED` and engages C6 under §5 C6. | yes |
| `DYM_FLOOR_UNDERDETERMINED` | The completion-free reading gives `M ∈ [10,∞) kg`; one named, exactly-one-assumption admissible completion gives `M ∈ [20,∞) kg`. Thus `P` contains floors 10 kg and 20 kg, while `Z=I=∅`. All reached controls pass. | §2 admits the completion-free reading and an exactly-one-assumption consistent completion → §3 limb B → §4 puts both readings in `P` → §4 class-2 row fires because `P` holds two different floors → §4.2 files `DYM_FLOOR_UNDERDETERMINED`, reports both readings, and engages C6 under §5 C6. | yes |
| `DYM_NO_SIZE_MASS_RELATION` | A complete §2c census reproduces no relation that binds size to mass and no relation that bounds mass at all; all reached controls pass. | §2c exhaustive census → §3 limb-A condition (“If none is reproduced”) → §3 files `DYM_NO_SIZE_MASS_RELATION`, records C3, C4 and C6 `NOT_RUN` → §4 class-3 row and §4.3 confirm the same filing. | yes |
| `DYM_NO_POSITIVE_FLOOR` | A reproduced mass relation permits exactly `M ∈ (0,∞) kg`; it bounds/relates mass enough to reach limb B but permits masses approaching zero. The completion-free reading is the sole admissible reading, so `P=∅`, `Z={that reading}`, `I=∅`. | §2/§2c reproduce the relation → §3 limb B because a mass relation/bound was reproduced → §4 assigns the consistent reading to `Z` → §4 class-4 row (`P` empty and `Z` non-empty) → §4.4 files `DYM_NO_POSITIVE_FLOOR`; §5 C6 says `NOT_RUN`. | yes |
| `DYM_SOURCE_BLOCKED` | Required manifest source entry 18 cannot be read (alternatively, its computed digest differs from §2a). | §2a: inability to read or identity mismatch files `DYM_SOURCE_BLOCKED` → §4.5 identifies it as the non-scientific waiting state → C6 is `NOT_RUN` if filed before evaluation under §5 C6. | yes |
| `R3D_NO_CLASS` | All sources are readable, identities match, and nothing is unresolved, so `DYM_SOURCE_BLOCKED` is ruled out. In seat A, a reached required C5 harness control fails on attempt 1 and again on attempt 2; under the seat-split procedure the third seat re-runs that failed control and it fails again. | §4.6 first rules out `DYM_SOURCE_BLOCKED` → §4.6 persistent required-control failure after two attempts triggers the no-class route → §4.6 third-seat re-run also fails → file `R3D_NO_CLASS`. If filed before C6 evaluation, §5 C6 gives `NOT_RUN`. | yes |
| C6 condition 1 — PASS | File the derived floor as the degenerate interval `[10,10] kg`. | §5 C6 evaluates the quantity actually filed → condition-1 bounded procedure classifies dimension → kg is mass, satisfying its pass criterion. | yes |
| C6 condition 1 — FAIL (`ENTAILED` integrity case) | Malformed filing: enter the dimensionless ratio `0.5` as the “floor.” | §5 C0 expressly allows an entailed condition to be proved and tested with a malformed filing → §5a proves entailment for every correctly filed mass floor and retains the malformed-filing check → §5 C6 says a non-mass filing fails condition 1 → dimensionless `0.5` fails. This is not a valid scientific-class member; it is the document's expressly licensed integrity-test input. | yes |
| C6 condition 2 — PASS | Derived floor `M_f=sqrt(ħc/G)=2.176434e-8 kg`, with constants only `ħ`, `c`, and `G`. | §2b lists all three constants and prints the derived Planck mass → condition-2 scope tests constants of the floor → each terminates in §2b → every row passes the provenance criterion. | yes |
| C6 condition 2 — FAIL | Derived floor `M_f=A sqrt(ħc/G)` with `A=2` introduced by the sole named assumption “we choose `A=2`,” with no manifest derivation for `A`. | §5 condition 2 traces each floor constant → `ħ,c,G` terminate in §2b, but `A` terminates in a chosen coefficient rather than a manifest equation or §2b → condition 2 fails. The reading can be reported under §4.2 alongside the completion-free reading, so C6 is engaged. | yes |
| C6 condition 3 — PASS | `M_f=sqrt(ħc/G)=2.176434e-8 kg`; replace every non-§2b parameter: there are none, and the simplified expression has an empty non-§2b free-symbol set. | §5 condition-3 bounded SymPy procedure → simplified expression contains no non-§2b symbol → PASS exactly under its criterion. | yes |
| C6 condition 3 — FAIL | `M_f=λ sqrt(ħc/G)`, where `λ>0` remains free; the free-symbol run prints `[λ]`. | A positive-floor family exists for each positive `λ`, routing the surviving freedom to §4.2 and engaging C6 → §5 condition 3 replaces non-§2b parameters by independent symbols → simplified expression still contains `λ` → FAIL. | yes |
| C6 condition 4 — PASS | `M_f=sqrt(ħc/G)` with no quantity held constant except the §2b measured constants; the fixity table lists `ħ`, `c`, and `G` with §2b as their deriving clause. | §5 condition-4 procedure lists every held-constant quantity → every row has the document's deriving passage (§2b) → PASS. | yes |
| C6 condition 4 — FAIL | `M_f=ρ_0 V_0=10 kg`, with `ρ_0` held fixed by choice and no reproduced manifest passage deriving its constancy. | The positive numeric floor engages C6 through §4.1 or §4.2 as its reading inventory dictates → §5 condition 4 lists `ρ_0` as held constant → no manifest derivation line exists for its constancy → FAIL. | yes |
| C6 condition 5 — PASS | Filed floor interval `[1.000e15,1.000e15] kg`. It overlaps none of `[2.176434e-8,2.176434e-8]`, `[1.729e11,5.190e11]`, or `[4.375e30,5.768e30]` kg; ΛCDM has no interval. | §5 condition-5 procedure compares the filed interval with the complete printed comparator set → all three numerical interval intersections are empty and ΛCDM can never match → the iff rule says condition 5 PASSES. | yes |
| C6 condition 5 — FAIL | Filed floor interval `[2.000e11,2.000e11] kg`. It overlaps comparator 2, Hawking evaporation `[1.729e11,5.190e11] kg`; it does not overlap comparator 1 `[2.176434e-8,2.176434e-8]` or comparator 3 `[4.375e30,5.768e30]`, and ΛCDM has no interval. | §5 condition-5 complete comparison table → `1.729e11 ≤ 2.000e11 ≤ 5.190e11`, hence a non-empty overlap with comparator 2 → the iff decision rule says condition 5 FAILS and requires that comparator to be named. | yes |
| `C6_BREAKER_TEST=PASS` | A class-1 floor `[1.000e15,1.000e15] kg` is printed by a manifest equation; every constant terminates in that manifest equation/§2b, there are no non-§2b free symbols, no underived held-fixed quantity, the dimensional analysis is kg, and the complete four-row comparison table shows no overlap. | §4.1 reaches a positive-floor class → §5 C6 applies → conditions 1–5 each pass their decision rules (including condition 5's no-overlap rule) and the table is complete → §5's three-outcome clause emits `C6_BREAKER_TEST=PASS`. | yes |
| `C6_BREAKER_TEST=FAIL` | A class-1 floor `[2.000e11,2.000e11] kg`, otherwise fully sourced and fixed, with a complete comparator table. Condition 5 fails by overlap with Hawking comparator 2 `[1.729e11,5.190e11] kg`. | §4.1 reaches a positive-floor class → §5 C6 applies → condition 5 fails its iff rule → §5's three-outcome clause emits `C6_BREAKER_TEST=FAIL`; §4.1 still files `DYM_FLOOR_DERIVED`. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | Use the class-3 construction above: the complete census reproduces neither a size–mass relation nor any mass bound. | §3 limb A files `DYM_NO_SIZE_MASS_RELATION` and expressly records C6 as `NOT_RUN` → §5 C6 says C6 was never engaged on that class → `C6_BREAKER_TEST=NOT_RUN`. | yes |

## Partition and overlap findings

All six declared classes have a reachable input. V15 declares four, not five, scientific classes; each of those four is exhibited above. The two non-scientific terminal states are also exhibited. The retired completion-dependent state is reachable as a physical/configuration state but is intentionally routed to class 2: for example, completion-free `M ∈ (0,∞) kg` plus a completion giving `[10,∞) kg` yields `P≠∅` and `Z≠∅`, hence `DYM_FLOOR_UNDERDETERMINED`.

No admissible-reading case constructed here fits two scientific classes or no scientific class. The §4 decision procedure separates them as follows: absence of any binding/bounding relation goes to class 3 before the `P/Z/I` split; once limb B is reached, `P=∅` with `Z∪I≠∅` goes only to class 4, `P≠∅` with any disagreement goes only to class 2, and `P≠∅` with unanimous floor and `Z=I=∅` goes only to class 1. The terminal examples also do not double-file: §4.6 makes `R3D_NO_CLASS` available only after `DYM_SOURCE_BLOCKED` is ruled out.

C6 can return `FAIL` on **2 of the 6 outcome classes**: `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`. Every breaker condition has both required directions exhibited; condition 1's substantive FAIL is entailed away for a correctly filed floor, but its malformed-filing FAIL is expressly required and licensed by §5 C0 and §5a.

## Unreachable verdicts and blocking clauses

None. Therefore there is no blocking clause to quote.

R3D_C0_EXHIBITION_COMPLETE
