ACCESS_SHA=74f8e0c35cf3bdff4d84be848c1bd60d7ba61d10bbb6e348c16a0335994fc501
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — R3D V18

Scope: this is a machinery test of `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md` only. The constructions below are synthetic stated configurations over the document's printed decision rules. No Dymnikova source was read and no physics judgement is made.

## Reachability table

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| **A1 — `DYM_FLOOR_DERIVED`** | All required sources are readable and identity-matched; all reached controls pass. The exhaustive census prints a source relation whose allowed mass set is `M ∈ [M_☉,∞)`, with `M_☉ = 1.98892e30 kg`; no candidate added completion is admissible. Thus the completion-free reading is the only admissible reading and has floor `M_☉`. | §2 defines completion-free derivation as an admissible reading for §4 counting → §3 reproduces a printed mass bound and enters limb B → §4 gives `P={M_☉}`, `Z=∅`, `I=∅` → §4 class 1 rule (“`P` non-empty, all of `P` agree on one floor, `Z` AND `I` both empty”) → `DYM_FLOOR_DERIVED`. | **YES** |
| **A2 — `DYM_FLOOR_UNDERDETERMINED`** | Sources and controls pass. A printed relation allows `M ∈ [10,∞) kg`. The completion-free reading has floor `10 kg`; one named admissible completion operates on that printed relation by additionally restricting the allowed set to `M ∈ [20,∞) kg`, consistently with it, and has floor `20 kg`. | §2 admissibility: exactly one named assumption, consistent with the printed relation, and operating on it → §3 printed mass bound sends the case to limb B → §4 gives `P={10 kg,20 kg}`, `Z=∅`, `I=∅` → `P` holds two different floors → class 2. Per §4 class 2, C6 is evaluated separately on both floors and no floor is chosen. | **YES** |
| **A3 — `DYM_NO_SIZE_MASS_RELATION`** | Sources and controls needed through the census pass. The complete census reproduces no printed relation binding size to mass and no printed mass bound. In particular, there is no printed relation on which a completion capable of supplying a mass bound could operate. | §2c complete census → §3 limb A finds neither kind of relation → §3 mandates `DYM_NO_SIZE_MASS_RELATION`, records C3/C4/C6 `NOT_RUN`, and stops → §4 says class 3 is reached **ONLY** from limb A. | **YES** |
| **A4 — `DYM_NO_POSITIVE_FLOOR`** | Sources and controls pass. The census prints `M ∈ (0,100] kg`. This bounds mass, so limb B is entered. The sole completion-free admissible reading is consistent, non-empty, and permits masses approaching zero; no candidate added completion is admissible. | §3 reproduces a printed mass bound → limb B → §4 has `P=∅`, `Z={completion-free reading}`, `I=∅` → limb-B class 4 rule (`P` empty and `Z` or `I` non-empty) → `DYM_NO_POSITIVE_FLOOR`. | **YES** |
| **A5 — `DYM_SOURCE_BLOCKED`** | One required pinned source cannot be read; no C6 evaluation has begun. | §2a makes the source required → §4 class 5 expressly covers a required pinned source that cannot be read → study waits and files `DYM_SOURCE_BLOCKED`; §9 records later, unengaged controls `NOT_RUN`; §5 C6 is `NOT_RUN` because the block preceded C6. | **YES** |
| **A6 — `R3D_NO_CLASS`** | All required evidence is readable/resolved, excluding `DYM_SOURCE_BLOCKED`. A required reached control (for example C5: the live harness command returns nonzero) fails on its first execution and again on the mandated second attempt in one seat. | §4 class 6 first rules out `DYM_SOURCE_BLOCKED` → §9 seat-split/control-clean procedure supplies the second attempt → the required control still fails after two attempts → §4 class 6 files `R3D_NO_CLASS`. | **YES** |
| **B1 PASS — C6 condition 1** | Filed floor `M_☉ = 1.98892e30 kg`. | §5 C6 applies to the positive-floor filing → condition 1 bounded procedure classifies dimensions → kg is mass → PASS criterion is met. | **YES** |
| **B1 FAIL / `ENTAILED` rejection exhibition — C6 condition 1** | Malformed filing: a seat writes the dimensionless ratio `0.50` as its “floor” and engages C6. | §5 says conditions act on the quantity actually filed and expressly says a non-mass fails condition 1 → dimensional analysis gives dimensionless, not kg or `M_☉` → FAIL. Under C0 this is the prescribed malformed-filing exhibition; it is not valid membership in a positive-floor class. | **YES** (malformed filing, as expressly required by the `ENTAILED` route) |
| **B2 PASS — C6 condition 2** | Floor `m_P = sqrt(ħc/G) = 2.176434e-8 kg`; its only constants are `ħ`, `c`, and `G`. | §2b lists all three measured constants and prints the derived Planck mass → condition 2 scope is constants of the derived floor → every provenance row terminates in §2b → PASS. | **YES** |
| **B2 FAIL — C6 condition 2** | Floor `A·m_P` where `A=7` is introduced only as “we choose 7” and has no manifest equation or §2b provenance. | Positive floor engages C6 → condition 2 traces `A` → terminus is the chosen coefficient, neither a manifest-source equation nor §2b → condition 2 FAIL. | **YES** |
| **B3 PASS — C6 condition 3** | Floor `m_P = sqrt(ħc/G)` with no non-§2b parameter. | Replace every non-§2b parameter by an independent symbol (there are none) → one simplification → expression contains no forbidden free symbol → condition 3 PASS. | **YES** |
| **B3 FAIL — C6 condition 3** | Floor `α m_P`, with unconstrained non-§2b normalization `α`. | Replace `α` by an algebraically independent symbol → simplification leaves free-symbol set `{α}` → condition 3's exact criterion fails. | **YES** |
| **B4 PASS — C6 condition 4** | Floor `m_P`, with only §2b measured constants used and no derivation-specific quantity held constant by choice. The fixity table has no underived held-constant row. | Condition 4 lists every held-constant quantity → every listed constant is covered by §2b/no chosen fixity is present → every applicable row has the required derivation basis → PASS. | **YES** |
| **B4 FAIL — C6 condition 4** | Floor `sqrt(ρ₀/ρ_ref) m_P`, with `ρ₀` held fixed by choice and no manifest passage deriving its constancy (take `ρ_ref=ρ₀/4` so the filed floor is the positive mass `2m_P`). | Positive floor engages C6 → fixity table lists held-constant `ρ₀` → no manifest derivation line exists → condition 4's pass criterion is violated → FAIL. | **YES** |
| **B5 PASS — C6 condition 5** | Point floor `[1.98892e30,1.98892e30] kg` (`M_☉`). | Compare against all printed rows: it is disjoint from Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, and TOV `[4.375e30,5.768e30]`; ΛCDM has no interval and can never match → no overlap → condition 5 PASS. | **YES** |
| **B5 FAIL — Planck row** | Point floor `[2.176434e-8,2.176434e-8] kg`. Computed intersection with the Planck interval is `[2.176434e-8,2.176434e-8] kg`. It overlaps **Planck only**. | Condition 5 represents the point as a degenerate interval → it overlaps printed Planck `[2.176433e-8,2.176435e-8]` → the iff overlap rule gives FAIL and names Planck. | **YES** |
| **B5 FAIL — Hawking row** | Point floor `[2.000e11,2.000e11] kg`. Computed intersection with the Hawking interval is `[2.000e11,2.000e11] kg`. It overlaps **Hawking only**. | Degenerate floor interval overlaps printed Hawking `[1.729e11,5.190e11]` and is disjoint from the other finite rows → condition 5 FAIL, Hawking named. | **YES** |
| **B5 FAIL — TOV row** | Point floor `[5.000e30,5.000e30] kg`. Computed intersection with the TOV interval is `[5.000e30,5.000e30] kg`. It overlaps **TOV only**. | Degenerate floor interval overlaps printed TOV `[4.375e30,5.768e30]` and is disjoint from the other finite rows → condition 5 FAIL, TOV named. | **YES** |
| **C — `C6_BREAKER_TEST=PASS`** | A class-1 floor `M_☉ = 1.98892e30 kg`, with a complete four-row comparison table, provenance terminating in §2b, no non-§2b free symbol, and no underived held fixity. | §4 class 1 engages C6 → condition 1 mass PASS; condition 2 provenance PASS; condition 3 free-symbol PASS; condition 4 fixity PASS; condition 5 has no comparator overlap PASS; complete table → §5 C6 definition: every condition satisfies its rule → `C6_BREAKER_TEST=PASS`. | **YES** |
| **C — `C6_BREAKER_TEST=FAIL`** | A class-1 floor `m_P = 2.176434e-8 kg`, with complete artefacts and conditions 1–4 passing as above. | §4 class 1 engages C6 → condition 5 overlaps the Planck interval and FAILS → §5 says any condition failure gives `C6_BREAKER_TEST=FAIL`; class 1 remains filed. | **YES** |
| **C — `C6_BREAKER_TEST=NOT_RUN`** | The A3 configuration: complete census, but no printed size–mass relation and no printed mass bound. | §3 limb A files class 3 and explicitly records C6 `NOT_RUN` → §5 says C6 was never engaged on `DYM_NO_SIZE_MASS_RELATION` → `C6_BREAKER_TEST=NOT_RUN`. | **YES** |

## Partition stress test and overlap findings

The document declares **six classes total: four scientific classes and two non-scientific terminal states**. (The prompt's later phrase “five scientific classes” does not match §4; the retired `DYM_FLOOR_COMPLETION_DEPENDENT` was not tested.) Every one of the six declared classes has a reachable construction above.

For the specifically requested configuration with **no printed size–mass relation and no printed mass bound**, the single filing is class 3, `DYM_NO_SIZE_MASS_RELATION`. It reaches §3's limb-A exit and stops. Although the semantic consequence is that the completion-free reading permits masses approaching zero, it cannot also file class 4 because §4 restricts class 4 to **“LIMB B ONLY”** and later states: **“Class 3 is reached ONLY from limb A; classes 1, 2 and 4 ONLY from limb B.”** Thus the limb separation works even though §4 itself correctly observes that class 3 entails class 4 by meaning.

Among the valid complete cases constructed here, **none fits two declared classes and none fits no class**. Limb A uniquely files class 3. Within limb B, the mutually exclusive `P/Z/I` alternatives uniquely file class 1, 2, or 4. Source/control terminal rules route the two non-scientific configurations to classes 5 or 6. The only deliberately invalid item is condition 1's dimensionless malformed filing, which C0 expressly requires as the rejection exhibition for the `ENTAILED` condition; it is not claimed to be a scientific-class member.

## How many of the six terminal classes can carry C6 FAIL?

**4 of 6.** C6 is initially engaged from 2 classes, `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`, and can fail while either remains the terminal class. Once engaged, its FAIL also travels to `DYM_SOURCE_BLOCKED` when condition 3 remains `UNDECIDED` after the fallback, and to `R3D_NO_CLASS` when a persistent reached-control failure follows engagement. It is `NOT_RUN` on class 3 or class 4 because neither yields a positive floor. This is the engagement/terminal-filing distinction stated in §5a.

## Unreachable verdicts and blocking clauses

None. Therefore there is no blocking clause to quote for an unreachable verdict.

R3D_C0_EXHIBITION_COMPLETE
