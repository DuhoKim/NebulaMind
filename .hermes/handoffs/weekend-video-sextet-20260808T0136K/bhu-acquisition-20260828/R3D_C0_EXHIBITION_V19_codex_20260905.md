ACCESS_SHA=28a97c1a0522d6476cee6897470d272f8f7cd92ccfae332bc0132caa2fd02b54
C0_REACHABILITY=PASS

Scope note: §4 declares four scientific classes and two non-scientific terminal states, not five scientific classes. The retired `DYM_FLOOR_COMPLETION_DEPENDENT` label is not tested. The completion-dependent state is tested under class 2 as §4 requires. All constructions below are machinery tests using only the preregistration's printed definitions and numbers; they are not claims about any Dymnikova source or about physics.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| 1 `DYM_FLOOR_DERIVED` | Stated configuration: the completed §2 census reproduces a printed relation whose non-empty allowed mass set is `M ∈ [10,∞) kg`; the completion-free reading and every admissible one-completion reading have that same set. Thus `P` is non-empty with the sole floor 10 kg and `Z=I=∅`. | §2 admissible-reading rules → §3 limb A reproduces a mass bound, so limb B → §4 decision procedure: `P≠∅`, all `P` agree, `Z=I=∅` → class 1. C6 is engaged by §5. | yes |
| 2 `DYM_FLOOR_UNDERDETERMINED` | Printed relation gives the completion-free set `[10,∞) kg`; one named admissible completion, operating on that relation and consistent with it, narrows the set to `[20,∞) kg`. The two readings give floors 10 kg and 20 kg. | §2 completion-free reading plus exactly one admissible completion → §3 printed mass bound enters limb B → §4 has `P` containing two different floors → class 2; §4 requires C6 separately on both floors. | yes |
| 2 `DYM_FLOOR_UNDERDETERMINED` (completion-dependent state) | Printed relation gives `(0,∞) kg`; one admissible completion operating on it gives `[10,∞) kg`. Completion-free reading is in `Z`, completed reading is in `P`. | §2 admissible readings → §3 limb B because a mass relation is printed → §4 `P≠∅` and `Z≠∅` → class 2. This expressly follows §4's routing of the retired class-5 state. | yes |
| 3 `DYM_NO_SIZE_MASS_RELATION` | Completed, reconciled §2 census reproduces no relation binding size to mass and no relation bounding mass. | §2 complete census → §3 limb A finds neither kind of relation → mandatory limb-A stop → §4 class 3, with C3/C4/C6 `NOT_RUN`. Although the completion-free reading consequently permits masses approaching zero, §4 says class 3 is reached **only** from limb A and class 4 **only** from limb B; limb B was never entered, so this input cannot also file class 4. | yes |
| 4 `DYM_NO_POSITIVE_FLOOR` | A printed limb-B relation yields the non-empty allowed set `M ∈ (0,100] kg`; no admissible completion exists. Hence `P=∅`, `Z` contains the completion-free reading, and `I=∅`. | §2 census reproduces a mass relation → §3 limb B → §4 `P` empty and `Z` non-empty → class 4. | yes |
| 5 `DYM_SOURCE_BLOCKED` | Stated configuration: required manifest entry 18 cannot be read before any C6 evaluation. | §2a requires every pinned source and says an unread required source files `DYM_SOURCE_BLOCKED` → §4 class 5; later controls, including C6, are `NOT_RUN` under §9. | yes |
| 6 `R3D_NO_CLASS` | All required sources are readable and resolved; C6 is engaged on the 10 kg class-1 candidate; afterward C5's three-command harness fails in one seat on its first execution and on the prescribed third-seat re-run. | §4 first rules out `DYM_SOURCE_BLOCKED` → §4 class-6 rule: a required control fails after two attempts in a seat → `R3D_NO_CLASS`. Because C6 was already engaged, its actual result travels with the terminal filing. | yes |
| C6 condition 1 PASS | Filed floor `10 kg`. Dimensional analysis: `[M]=kg`. | §5 C6 applies to the positive-floor class → condition 1 bounded procedure classifies the filed quantity → mass dimension meets the pass criterion. | yes |
| C6 condition 1 FAIL integrity example (`ENTAILED` substantively) | Malformed filing: dimensionless ratio `M/M_☉=2` is entered in the floor field. Dimensional analysis gives `[M/M_☉]=1`. | §5 C0's `ENTAILED` exception applies: valid positive-floor membership entails a mass floor; §5 C6 nevertheless evaluates the quantity actually filed → condition 1 rejects dimensionless quantity. This is the required malformed-filing rejection, not a reachable substantive failure by a correctly filed class. | yes |
| C6 condition 2 PASS | Floor `2 m_P = 4.352868e-8 kg`, with `m_P=sqrt(ħc/G)`; all constants are `2`, `ħ`, `c`, and `G`. | Positive-floor class → C6 → provenance rows terminate in the construction's equation or §2b (`ħ,c,G`; numeric 2 is in the equation) → every constant traces. | yes |
| C6 condition 2 FAIL | A reproduced manifest passage gives floor `M=K=10 kg` but defines `K` only as “following [reference]”; the cited reference lies outside §2a. | Positive-floor class → C6 condition 2 follows `K` only within the manifest → outside-manifest terminus fails exactly as the decision rule states. | yes |
| C6 condition 3 PASS | Floor `2 m_P`; after replacing all non-§2b parameters, the simplified expression is `2*sqrt(ħ*c/G)` and the printed non-§2b free-symbol set is `{}`. | Positive-floor class → C6 condition 3 one SymPy simplification → no non-§2b symbol → PASS. | yes |
| C6 condition 3 FAIL | Floor expression `a m_P`, with `a` a non-§2b free normalization; the displayed numerical floor 10 kg is recovered only after choosing `a=10 kg/m_P`. | Positive-floor class → replace `a` by an algebraically independent symbol → simplified expression retains `{a}` → condition 3 FAIL. | yes |
| C6 condition 4 PASS | Floor `2 m_P`, with no quantity held constant by choice; the fixity table has no held-choice rows. | Positive-floor class → condition 4 enumerates held constants → none lack a deriving passage → PASS. | yes |
| C6 condition 4 FAIL | Floor construction holds a core density `ρ₀` constant by choice, and the C2 reproduced passages contain no passage deriving that constancy. | Positive-floor class → condition 4 fixity table row `ρ₀` has no manifest deriving line → FAIL. | yes |
| C6 condition 5 PASS | Filed floor interval `[10,10] kg`. It overlaps none of Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, or TOV `[4.375e30,5.768e30]`; ΛCDM has no interval. | Positive-floor class → execute all four comparator rows → no finite comparator overlap → condition 5 PASS. | yes |
| C6 condition 5 FAIL — Planck row | Filed floor interval `[2.176434e-8,2.176434e-8] kg`. Intersection with Planck interval is the same point, `[2.176434e-8,2.176434e-8] kg`; no Hawking or TOV overlap. | Positive-floor class → full comparator table → overlap disjunction is true for the Planck comparator → FAIL. | yes |
| C6 condition 5 FAIL — Hawking row | Filed floor interval `[2.000e11,2.000e11] kg`. Intersection with Hawking interval is `[2.000e11,2.000e11] kg`; no Planck or TOV overlap. | Positive-floor class → full comparator table → overlap disjunction is true for the Hawking comparator → FAIL. | yes |
| C6 condition 5 FAIL — TOV row | Filed floor interval `[5.000e30,5.000e30] kg`. Intersection with TOV interval is `[5.000e30,5.000e30] kg`; no Planck or Hawking overlap. | Positive-floor class → full comparator table → overlap disjunction is true for the TOV comparator → FAIL. | yes |
| `C6_BREAKER_TEST=PASS` | Class-1 construction with floor `[10,10] kg`; source-derived expression uses only §2b constants, contains no non-§2b free symbol, assumes no unsupported fixity, and supplies the complete four-row comparison table with no overlaps. | §4 class 1 → §5 C6 engaged → conditions 1–5 all PASS and table complete → C6 PASS. | yes |
| `C6_BREAKER_TEST=FAIL` | Class-1 construction with floor `[2.176434e-8,2.176434e-8] kg`; conditions 1–4 pass, complete table shows Planck overlap. | §4 class 1 remains filed despite a breaker failure → §5 condition 5 FAIL → C6 FAIL. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 no-relation/no-bound construction above. | §3 limb-A stop expressly records C6 `NOT_RUN` → §5 says C6 was never engaged on class 3 → NOT_RUN. | yes |

Partition findings: every one of the six declared §4 classes has a reachable input. Among the scientific decision-procedure cases constructed above, none fits two filed classes and none fits no filed class. In particular, the no-relation/no-bound case semantically satisfies class 4's state predicate, as §4 itself acknowledges, but it files only class 3 because the normative routing is by limb: class 3 only from limb A, class 4 only from limb B. The constructed run stops in limb A and therefore cannot file class 4.

C6 can return `FAIL` in **4 of the 6 terminal outcome classes**: it is initially engaged in classes 1 and 2, and an already-engaged failure can travel to class 5 when condition 3 is `UNDECIDED` after fallback, or to class 6 after a persistent later control failure. Initial engagement occurs on 2 of 6 classes. This distinction follows §5a and the explicit C6 rule that an engaged run records its actual result rather than `NOT_RUN`.

Unreachable verdicts: none. Therefore there are no blocking clauses to quote.

R3D_C0_EXHIBITION_COMPLETE
