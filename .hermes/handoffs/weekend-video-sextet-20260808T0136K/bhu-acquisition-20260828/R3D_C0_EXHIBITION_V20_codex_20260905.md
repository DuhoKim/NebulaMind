ACCESS_SHA=301cba9b498eb7d202b5667b396aa1711f443bcfe9c7164fda2bb6e0f42d2a7c
C0_REACHABILITY=PASS

This exhibition tests only the decision machinery and numerical intervals printed in `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md`. Its inputs are counterfactual census/ledger/run configurations, not claims about the manifest sources.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| §4 class 1 `DYM_FLOOR_DERIVED` | A completed, control-clean census reproduces a printed relation `M >= 10 kg`; the completion-free allowed set is `[10 kg, infinity)`. Every other admissible reading, if any, has that same allowed set. Thus `P` is non-empty with the sole floor 10 kg and `Z=I=empty`. | §2 admissible-reading rules -> §3 limb B because a mass bound is reproduced -> §4 definition of positive floor -> §4 class-1 row (`P` non-empty; all `P` agree; `Z` and `I` empty) -> class 1. C6 is engaged by §5 C6. | yes |
| §4 class 2 `DYM_FLOOR_UNDERDETERMINED` | The completion-free reading has allowed set `[10 kg, infinity)` while one admissible one-assumption completion, operating on the printed relation, has allowed set `[20 kg, infinity)`. Hence `P` contains distinct floors 10 kg and 20 kg. | §2 admissibility -> §3 limb B -> §4 `P` definition -> §4 class-2 disagreement row (`P` holds two different floors) -> class 2; under class 2, C6 is evaluated separately on both floors. | yes |
| §4 class 3 `DYM_NO_SIZE_MASS_RELATION` | The complete §2 census is control-clean but reproduces neither a size-mass relation nor any mass bound. | §2 complete census -> §3 limb A test finds neither kind of relation -> mandatory limb-A exit and stop -> §4 limb-A-only row -> class 3; C3, C4 and C6 are `NOT_RUN`. | yes |
| §4 class 4 `DYM_NO_POSITIVE_FLOOR` | The census reproduces a mass-containing relation, so limb B is entered; take the completion-free allowed set to be `(0, infinity) kg`, and every admissible one-assumption completion also permits masses approaching zero. Thus `P=empty`, `Z` is non-empty, and `I=empty`. | §3 limb B (a mass relation is printed) -> §4 reading classification puts all readings in `Z` -> §4 class-4 row (`P` empty and `Z` or `I` non-empty) -> class 4; C6 is not engaged. | yes |
| §4 class 5 `DYM_SOURCE_BLOCKED` | The computed identity of required manifest entry 18 is any value unequal to its frozen digest, for example 64 zeroes, before C6 is engaged. | §2a requires the frozen identity -> C1 says a mismatch files `DYM_SOURCE_BLOCKED` -> §4 class 5; later controls are `NOT_RUN` under §9. | yes |
| §4 class 6 `R3D_NO_CLASS` | All sources are readable and matched and no proposition is unresolved, but a reached required control (for example C5) fails on its first execution and again on the seat-split re-run. | Rule out class 5 under §4 class 6 -> §9 seat-split re-runs the failed control -> required control fails twice -> §4 class 6 `R3D_NO_CLASS`. | yes |
| C6 condition 1 PASS | File `M_min=10 kg`. | C6 evaluates the quantity actually filed -> decision rule 1 dimensional analysis gives `[M]` and kg -> observable mass magnitude -> PASS. | yes |
| C6 condition 1 FAIL (malformed-filing integrity case) | Malformed filing: file the dimensionless ratio `M/M_sun=2` as “the floor.” | C6 explicitly evaluates the actually filed quantity -> decision rule 1 finds dimensionless ratio, not kg or solar-mass dimension -> FAIL. Valid positive-floor membership entails PASS, so this is the malformed filing that C0's `ENTAILED` exception asks the check to reject. | yes (malformed filing); substantive failure is entailed impossible |
| C6 condition 2 PASS | A derived floor `M_min=10 sqrt(hbar*c/G)` kg-equivalent, with the coefficient 10 and the formula printed in a manifest-source passage reproduced in C2; `hbar`, `c`, and `G` terminate in §2b. | Condition-2 scope -> provenance row for 10 terminates in reproduced manifest equation; rows for `hbar,c,G` terminate in §2b -> every constant traces -> PASS. | yes |
| C6 condition 2 FAIL | A filed floor `M_min=A sqrt(hbar*c/G)` with `A=10` introduced only by “we choose A=10,” with no deriving manifest passage. | Condition-2 provenance traversal -> `hbar,c,G` trace to §2b but `A` terminates in an unsupported choice -> stated pass criterion is not met -> FAIL. | yes |
| C6 condition 3 PASS | `M_min=10 sqrt(hbar*c/G)`, with 10 source-derived and no non-§2b parameter. | Replace every non-§2b parameter by independent symbols -> there are none in the simplified final expression -> printed free-symbol set is empty -> PASS. | yes |
| C6 condition 3 FAIL | `M_min=alpha sqrt(hbar*c/G)` with free non-§2b normalisation `alpha`. | Replace `alpha` by an independent symbol -> one simplification leaves `{alpha}` -> the printed number requires choosing `alpha` -> FAIL. | yes |
| C6 condition 4 PASS | A derivation holds `rho0` constant and the C2 artefact reproduces a manifest passage explicitly deriving `rho0=3c^2/(8 pi G r0^2)` and its constancy for the construction. | List `rho0` in the fixity table -> identify that reproduced deriving passage -> every held-constant row has a passage -> PASS. | yes |
| C6 condition 4 FAIL | The same configuration holds `rho0` constant, but the complete C2 artefact contains no manifest passage deriving its constancy. | Fixity table lists `rho0` -> no manifest derivation can be cited -> condition-4 pass criterion fails -> FAIL. | yes |
| C6 condition 5 PASS | Filed floor interval `[1,1] kg`. | Compare with Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, and TOV `[4.375e30,5.768e30]`: all intersections are empty; LambdaCDM has no interval and cannot match -> no overlap -> PASS. | yes |
| C6 condition 5 FAIL — Planck row | Filed floor interval `[2.176434e-8,2.176434e-8] kg`. | Its intersection with the Planck interval is the same point, `2.176434e-8 kg`; intersections with Hawking and TOV are empty -> overlaps Planck -> FAIL. | yes |
| C6 condition 5 FAIL — Hawking row | Filed floor interval `[2.000e11,2.000e11] kg`. | `1.729e11 <= 2.000e11 <= 5.190e11`, so the intersection with the Hawking interval is `[2.000e11,2.000e11] kg`; Planck and TOV intersections are empty -> overlaps Hawking -> FAIL. | yes |
| C6 condition 5 FAIL — TOV row | Filed floor interval `[5.000e30,5.000e30] kg`. | `4.375e30 <= 5.000e30 <= 5.768e30`, so the intersection with the TOV interval is `[5.000e30,5.000e30] kg`; Planck and Hawking intersections are empty -> overlaps TOV -> FAIL. | yes |
| `C6_BREAKER_TEST=PASS` | Control-clean class-1 input with `M_min=10 sqrt(hbar*c/G)=2.176434e-7 kg`; 10 is supplied by a reproduced source equation, no free symbol or assumed fixity survives, and the point overlaps none of the three comparator intervals. Print the complete four-row comparison table. | §4 class 1 -> §5 engages C6 -> conditions 1–4 PASS by mass dimension, provenance, empty free-symbol set, and derived fixity -> condition 5 PASS because `2.176434e-7` is outside every interval -> complete artefacts -> C6 PASS. | yes |
| `C6_BREAKER_TEST=FAIL` | Control-clean class-1 input `M_min=2.176434e-8 kg`, fully source-derived with no free normalisation or assumed fixity, and a complete comparison table. | §4 class 1 -> C6 engaged -> conditions 1–4 PASS -> condition 5 overlaps Planck `[2.176433e-8,2.176435e-8]` -> any failed condition makes C6 FAIL; class 1 is still filed. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 no-relation/no-bound census configuration above. | §3 limb A exit -> §4 class 3 -> §5 expressly says C6 was never engaged and is `NOT_RUN`. | yes |

Partition findings:

- The four scientific classes in the document (classes 1–4) and the two non-scientific terminal states (classes 5–6) each have a reachable input. The request's phrase “five scientific classes” conflicts with §4's explicit “four scientific outcomes”; the retired `DYM_FLOOR_COMPLETION_DEPENDENT` is not tested. Its former state is nevertheless reachable and files class 2: completion-free `Z` plus completion floor 10 kg gives non-empty `P` and `Z`, hence disagreement.
- No constructed valid case fits two filed classes or none. In particular, the case with no printed size-mass relation and no printed mass bound has the semantic consequence `P=empty, Z non-empty`, but it files only class 3: §3 orders the limb-A stop, and §4 says class 4 is “LIMB B ONLY” and that class 3 is reached only from limb A. It therefore cannot also file class 4 under the stated procedure.
- C6 can return FAIL in 4 of the 6 declared terminal classes: directly in classes 1 and 2, in class 5 if engaged condition 3 remains `UNDECIDED` after fallback, and in class 6 if an engaged C6 is followed by the persistent control failure specified there. It cannot return FAIL in classes 3 or 4 because no positive floor engages C6. Initial engagement occurs on 2 of 6 classes.
- All five breaker conditions have both requested polarities exhibited. Condition 1's FAIL is deliberately the malformed filing licensed by C0's `ENTAILED` route; a correctly filed positive mass floor cannot substantively fail condition 1.

UNREACHABLE verdicts: none. Therefore there is no blocking-clause quotation to supply.

R3D_C0_EXHIBITION_COMPLETE
