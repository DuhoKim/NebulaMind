ACCESS_SHA=4939d193aeeec0ee492333f5864b3c10e3f566d19a80a002f411cf931361e9ae
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — V23

Scope: this exhibition uses only `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md` and the numbers printed in it. It does not read or assess any Dymnikova source. “Printed relation” below is a concrete hypothetical census result supplied to exercise the document's routing machinery, not a claim about the sources.

Section 4 actually declares **four scientific classes and two non-scientific terminal states**, not five scientific classes. The six declared classes are all exhibited below. The retired `DYM_FLOOR_COMPLETION_DEPENDENT` is not tested.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| `DYM_FLOOR_DERIVED` (class 1) | A control-clean census reproduces `M >= 10 kg`; the completion-free reading and every admissible one-completion reading have the same non-empty allowed set `[10 kg, infinity)`. Thus `P` is non-empty with the sole floor 10 kg, while `Z=I=empty`. | §3: a mass-bounding relation is reproduced, so Limb B; §4 admissible-reading partition: all readings enter `P` and agree; §4 class-1 rule: `P` non-empty, all agree, `Z` and `I` empty; file class 1. C6 is engaged by §5 C6. | yes |
| `DYM_FLOOR_UNDERDETERMINED` (class 2) | The completion-free reading gives `M >= 10 kg`; one admissible completion operating on that printed relation gives `M >= 20 kg`. Both allowed sets are non-empty, so `P` contains distinct floors 10 kg and 20 kg. | §3 Limb B; §4 puts both consistent positive-floor readings in `P`; §4 class-2 rule applies because `P` holds two different floors; §4(2) reports both and chooses none; C6 runs separately on both floors. | yes |
| `DYM_NO_SIZE_MASS_RELATION` (class 3) | The completed census reproduces **no printed size–mass relation and no printed mass bound**. | §3 Limb A: neither disjunct (“binding size to mass” or “bounding the mass”) is reproduced; §3 orders class 3 and stop; §4 says class 3 is reached only from Limb A. C3, C4 and C6 are `NOT_RUN`. | yes |
| `DYM_NO_POSITIVE_FLOOR` (class 4) | The census reproduces the mass relation `M > 0 kg`, whose non-empty allowed set is `(0,infinity) kg`. The completion-free reading permits masses approaching zero; take every admissible completion to do likewise. Hence `P=empty`, `Z` non-empty, `I=empty`. | §3: a mass bound is reproduced, so Limb B; §4 sends the readings to `Z`; §4 class-4 rule (`P` empty and `Z` or `I` non-empty) files class 4. C6 is `NOT_RUN` because there is no positive floor. | yes |
| `DYM_SOURCE_BLOCKED` (class 5) | Required manifest source entry 18 is unreadable on both the initial read and the §9 split re-execution; no source-dependent scientific proposition is used. | §2a: required pinned source cannot be read; §4(5): unread required pinned source files `DYM_SOURCE_BLOCKED`; §9 records later unengaged controls `NOT_RUN`. | yes |
| `R3D_NO_CLASS` (class 6) | All sources are readable and matched and no proposition is unresolved. Two seats otherwise agree on class 1 and 10 kg, but one seat's reached `C5_HARNESS_PINNED` fails; the third seat re-runs that failed control once and it fails again. | §4(6): first rule out `DYM_SOURCE_BLOCKED`; same-scientific-class report not control-clean triggers third-seat re-run; persistent second failure in a seat files `R3D_NO_CLASS`. | yes |
| C6 condition 1 PASS | Filed floor `M_min=10 kg`. Dimensional analysis: `[M_min]=kg`, a mass. | §5 C6 condition 1 bounded procedure; dimension is mass, satisfying its pass criterion. | yes |
| C6 condition 1 FAIL (`ENTAILED` integrity case) | Malformed filing declares the dimensionless ratio `M/M_sun=2` to be “the floor.” Dimensional analysis gives `[M/M_sun]=1`, not kg or solar mass. | §5 C0 expressly permits `ENTAILED`: every valid positive-floor class concerns an allowed **mass** set, so a correctly filed floor is a mass; §5 C6 nevertheless evaluates the quantity actually filed and says a non-mass fails. This malformed filing is the required rejected case, not a valid scientific-class member. | yes |
| C6 condition 2 PASS | `M_min=sqrt(hbar*c/G)=2.176434e-8 kg`; all constants are exactly `hbar`, `c`, and `G` from §2b. | §5 C6 condition 2 scope and bounded provenance procedure; every constant terminates in the §2b list. | yes |
| C6 condition 2 FAIL | `M_min=a*sqrt(hbar*c/G)` with fixed `a=7` introduced only by “we choose a=7,” with no manifest equation deriving it. | §5 C6 condition 2 traces `a`; its terminus is an un-derived “we choose” coefficient, which the pass criterion expressly rejects. | yes |
| C6 condition 3 PASS | `M_min=sqrt(hbar*c/G)`. Replacing every non-§2b parameter introduces none; the simplified expression has no non-§2b free symbol. | §5 C6 condition 3 free-symbol procedure; printed sorted non-§2b free-symbol set is `[]`; PASS. | yes |
| C6 condition 3 FAIL | `M_min=a*sqrt(hbar*c/G)` with algebraically independent non-§2b symbol `a`. | §5 C6 condition 3; one simplification leaves free-symbol set `[a]`, so a free normalisation survives; FAIL. | yes |
| C6 condition 4 PASS | `M_min=sqrt(hbar*c/G)` and the fixity table contains no construction-specific held-constant quantity; `hbar,c,G` are §2b measured constants. | §5 C6 condition 4 bounded procedure; every held-constant row has a permitted derivation (vacuously for the empty construction-specific list); PASS. | yes |
| C6 condition 4 FAIL | `M_min=r0*c^2/(2G)` with `r0=1 m` held constant solely by choice and no verbatim C2 manifest passage deriving that constancy. | §5 C6 condition 4; the fixity row for `r0` lacks a manifest deriving passage, so FAIL. | yes |
| C6 condition 5 PASS | Filed floor interval `[1,1] kg`. | §5 C6 condition 5: it overlaps neither Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, nor TOV `[4.375e30,5.768e30]`; ΛCDM has no interval and can never match; therefore PASS. | yes |
| C6 condition 5 FAIL — Planck row | Filed floor interval `[2.176434e-8,2.176434e-8] kg`. Computed intersection with Planck comparator is `[2.176434e-8,2.176434e-8] kg`. | §5 condition-5 iff rule: non-empty overlap with Planck `[2.176433e-8,2.176435e-8]`; FAIL, comparator named Planck. | yes |
| C6 condition 5 FAIL — Hawking row | Filed floor interval `[2.000e11,2.000e11] kg`. Computed intersection with Hawking comparator is `[2.000e11,2.000e11] kg`. | §5 condition-5 iff rule: non-empty overlap with Hawking `[1.729e11,5.190e11]`; FAIL, comparator named Hawking. | yes |
| C6 condition 5 FAIL — TOV row | Filed floor interval `[5.000e30,5.000e30] kg`. Computed intersection with TOV comparator is `[5.000e30,5.000e30] kg`. | §5 condition-5 iff rule: non-empty overlap with TOV `[4.375e30,5.768e30]`; FAIL, comparator named TOV. | yes |
| `C6_BREAKER_TEST=PASS` | A class-1 floor `M_min=10*sqrt(hbar*c/G)=2.176434e-7 kg`, with the dimensionless 10 printed by a manifest equation; no non-§2b free parameter, no underived held-fixed quantity, and a complete four-row comparison table. It overlaps none of the three finite comparators. | §5: positive-floor class engages C6; conditions 1–4 pass their respective dimensional, provenance, free-symbol and fixity rules; condition 5 passes by no overlap; table complete; §5 C6 outcome rule gives PASS. | yes |
| `C6_BREAKER_TEST=FAIL` | A class-1 floor interval equal to the Planck point `[2.176434e-8,2.176434e-8] kg`, with complete artefacts and conditions 1–4 passing. | §5 engages C6 on class 1; condition 5 overlaps the Planck interval and fails; §5 C6 outcome rule says any failed condition yields FAIL while §4(1) still files the floor class. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 no-relation/no-bound census input above. | §3 expressly stops after Limb A and records C6 `NOT_RUN`; §5 C6 says it is never engaged on `DYM_NO_SIZE_MASS_RELATION`; §5 outcome rule yields NOT_RUN. | yes |

## Partition and limb findings

The requested hard case—no printed size–mass relation and no printed mass bound—files **only class 3**. Although its completion-free semantics would permit masses approaching zero and thus satisfy class 4's state predicate in isolation, §3 stops in Limb A and §4 says class 4 is **Limb B only**. It therefore cannot also file class 4 under the document's decision procedure. This is separation by filing limb, exactly as §4 specifies, not separation of the underlying predicates.

No constructed valid case files two classes. No constructed valid case fits no class. For scientific routing, Limb A files class 3; in Limb B the completion-free reading guarantees at least one admissible reading, every reading is assigned exactly one of `P`, `Z`, or `I`, and the rules then exhaustively select class 1, 2, or 4. The two terminal states are selected by their control/source rules. Thus **each of the six declared classes has a reachable input**.

C6 is initially engaged on 2 of the 6 classes (classes 1 and 2). As §5a distinguishes, after engagement `C6_BREAKER_TEST=FAIL` can accompany **4 of the 6 terminal classes**: `DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED`, `DYM_SOURCE_BLOCKED` after condition 3 remains undecided following fallback, and `R3D_NO_CLASS` after a subsequent persistent control failure. Therefore the requested number of six outcome classes on which C6 can return FAIL is **4**.

## Unreachable verdicts

None. Every requested declared class, each breaker-condition direction (with condition 1's document-authorized `ENTAILED` malformed-filing rejection), all three comparator-specific condition-5 failures, and all three C6 outcomes have concrete exhibitions. Consequently there is no blocking clause to quote.

R3D_C0_EXHIBITION_COMPLETE
