ACCESS_SHA=6d60bb0a20519a64cb7fc83cae780822e17b830fa15d090da119ed3980e5132d
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — R3D V26

Scope: this exhibition uses only `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md` and the numbers printed in it. The Dymnikova source files were not read. The operative §4 declares six classes: four scientific classes (1–4) and two non-scientific terminal classes (5–6). The prompt's later phrase “five scientific classes” is inconsistent with that operative enumeration; no fifth scientific class is invented. `DYM_FLOOR_COMPLETION_DEPENDENT` is retired and is not tested.

## Reachability table

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| A1 — `DYM_FLOOR_DERIVED` | Stated census result: printed relations include `M >= 10 kg` and ancillary relations on four objects left unbound, so all four named one-assumption completions are applicable and operate on a printed relation without changing the mass set. Explicit finite reading set: completion-free `R0`, Euclidean-volume `RE`, uniform-interior `RU`, coefficient-set-to-1 `RK`, and GR-exterior `RG`. Each has allowed masses `[10 kg, infinity)`, so each is in `P` with floor 10 kg. Thus `P={R0,RE,RU,RK,RG}`, all of `P` agree, and `Z=I=empty`. | §2: exactly four completion kinds and the completion-free reading; §3: a printed mass bound enters limb B; §4 definition: 10 kg is a strictly positive greatest lower bound; §4 decision row: `P` non-empty, all agree, `Z` and `I` empty; §4 class 1. C6 is then engaged by §5 C6. | YES |
| A2 — `DYM_FLOOR_UNDERDETERMINED` | Stated census result: a completion-free printed bound gives allowed masses `[10 kg, infinity)`. A printed density/volume relation leaves geometry unbound; the admissible Euclidean-volume completion operates on it and adds a stronger bound `[20 kg, infinity)`. Other completion kinds are inapplicable. Reading set: `R0` has floor 10 kg and `RE` has floor 20 kg. Hence `P` contains two different floors, `Z=I=empty`. | §2 admissibility and exact-four-kind finite set; §3 printed mass bound -> limb B; §4 `P` definition; §4 class-2 row (“`P` holds two different floors”); §4 class 2 requires reporting both and choosing none; §5 C6 evaluates 10 kg and 20 kg separately. | YES |
| A3 — `DYM_NO_SIZE_MASS_RELATION` | Completed frozen census reproduces no relation binding size to mass and no relation bounding mass. In particular, there is no printed relation on which a completion could operate to supply a mass bound. | §2c complete census; §3 limb A condition is met and explicitly files class 3, records C3/C4/C6 `NOT_RUN`, and stops; §4 LIMB A EXIT row; §4 “Class 3 is reached ONLY from limb A.” | YES |
| A4 — `DYM_NO_POSITIVE_FLOOR` | Completed census reproduces the printed mass relation `M >= 0 kg`, so limb B is entered, but its sole completion-free allowed set is `[0 kg, infinity)`; none of the four completions is applicable. Thus `P=empty`, `Z={R0}`, `I=empty`. | §3: a mass-bounding relation is reproduced -> limb B; §4 positive-floor definition excludes zero; §4 class-4 row: limb B, `P` empty, `Z` non-empty; §4 class 4; §4 records C6 `NOT_RUN`. | YES |
| A5 — `DYM_SOURCE_BLOCKED` | At C1, one required pinned source is unreadable (equivalently, its computed digest differs from its §2a manifest digest). | §2a read/identity requirement; C1 says unreadable or mismatch files `DYM_SOURCE_BLOCKED`; §4 class 5 definition; §9 later unengaged controls are `NOT_RUN`. | YES |
| A6 — `R3D_NO_CLASS` | All evidence is readable and resolved, ruling out class 5. Two seats agree on class 1 and its floor, but seat A has `C4_GR_BENCHMARK=FAIL`; the third seat reruns that failed control and it fails again. | §4 class 6 first rules out `DYM_SOURCE_BLOCKED`; §4 control-clean rule orders one rerun per failed control; persistent second failure files `R3D_NO_CLASS`; §9 preserves the already-engaged C6 result and marks only later unengaged controls `NOT_RUN`. | YES |
| B1 PASS — condition 1 | Filed floor `10 kg`. Dimensional analysis: `[M_floor]=kg`, a mass magnitude. | §5 C6 applies on class 1 or 2; condition-1 bounded procedure classifies dimension; `kg` meets its pass criterion. | YES |
| B1 FAIL integrity case — condition 1 | Malformed filing states the “floor” as dimensionless ratio `M/M_sun = 2`. Dimensional analysis gives 1, not kg or `M_sun`. | §5 says conditions act on the quantity actually filed and explicitly says a non-mass fails condition 1; condition-1 decision row fails. This is the malformed filing C0 requires as the rejection exhibition. | YES (`ENTAILED` for valid filings; malformed rejection reachable) |
| B2 PASS — condition 2 | Floor `3*sqrt(hbar*c/G) = 6.529302e-8 kg`; provenance rows terminate `3` in the construction's printed equation and `hbar,c,G` in §2b. | §5 condition 2 scopes only constants of the derived floor; every constant terminates in a manifest equation or §2b -> PASS. | YES |
| B2 FAIL — condition 2 | Floor `7 kg`, where the ledger says coefficient 7 entered only as “we choose 7” and has no manifest equation or §2b terminus. | §5 condition-2 chain follows the constant through the C2 passages; a `we choose`/outside-manifest terminus fails. | YES |
| B3 PASS — condition 3 | Floor `3*sqrt(hbar*c/G)`; replacing every non-§2b parameter leaves no such symbol because `hbar,c,G` are all §2b constants. | §5 condition-3 SymPy/free-symbol rule; simplified expression has no non-§2b free symbol -> PASS. | YES |
| B3 FAIL — condition 3 | Filed underdetermined floor family `lambda*sqrt(hbar*c/G) kg` with `lambda>0` not in §2b. The printed sorted non-§2b free-symbol set is `[lambda]`. | §4 class 2 permits surviving freedom and runs C6 on each floor in `P`; §5 condition 3 substitutes only §2b constants; `lambda` survives -> FAIL. | YES |
| B4 PASS — condition 4 | Floor `3*sqrt(hbar*c/G)` with no quantity held fixed by choice; the fixity table is empty (or every listed fixed quantity has its manifest deriving passage). | §5 condition-4 procedure; every row has a C2 manifest passage, vacuously when no rows exist -> PASS. | YES |
| B4 FAIL — condition 4 | Floor `rho0*V0 = 10 kg`, with `rho0` held constant solely by choice and no manifest passage deriving its constancy. | §5 condition-4 fixity table contains `rho0` with no source line; its pass criterion is violated -> FAIL. | YES |
| B5 PASS — condition 5 | Point floor `[1.000e15,1.000e15] kg`. It overlaps none of Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, or TOV `[4.375e30,5.768e30]`; ΛCDM has no interval. | §5 condition-5 single-disjunction rule: no comparator interval overlaps -> PASS, assuming the required four-row artefact is printed. | YES |
| B5 FAIL — Planck row | Point floor `[2.176434e-8,2.176434e-8] kg`. Intersection with Planck row is `[2.176434e-8,2.176434e-8] kg`; no overlap with Hawking or TOV. | §5 comparator row 1; point lies between `2.176433e-8` and `2.176435e-8`; named overlap makes condition 5 FAIL. | YES |
| B5 FAIL — Hawking row | Point floor `[2.000e11,2.000e11] kg`. Intersection with Hawking row is `[2.000e11,2.000e11] kg`; no overlap with Planck or TOV. | §5 comparator row 2; `1.729e11 <= 2.000e11 <= 5.190e11`; named overlap makes condition 5 FAIL. | YES |
| B5 FAIL — TOV row | Point floor `[5.000e30,5.000e30] kg`. Intersection with TOV row is `[5.000e30,5.000e30] kg`; no overlap with Planck or Hawking. | §5 comparator row 3; `4.375e30 <= 5.000e30 <= 5.768e30`; named overlap makes condition 5 FAIL. | YES |
| C — `C6_BREAKER_TEST=PASS` | A manifest-derived point floor `1.000e15 kg`; dimension is kg; its numeric constant terminates in the construction's printed source equation; no non-§2b free symbol; no assumed fixity; complete four-row table shows no overlap. | §4 class 1 engages C6; §5 C6 outcomes: complete table and conditions 1–5 all satisfy their decision rules -> PASS. | YES |
| C — `C6_BREAKER_TEST=FAIL` | Manifest-derived point floor `2.000e11 kg`; conditions 1–4 pass, but the completed comparison table shows overlap with Hawking `[1.729e11,5.190e11]`. | §4 class 1 engages C6; §5 condition 5 fails on named overlap; §5 C6 outcomes says any condition failure -> FAIL while §4 still files class 1. | YES |
| C — `C6_BREAKER_TEST=NOT_RUN` | The A3 limb-A configuration: complete census reproduces neither a size–mass relation nor a mass bound, so class 3 is filed before C6 engages. | §3 files class 3 and records C6 `NOT_RUN`; §5 limits `NOT_RUN` to cases where C6 was never engaged and names class 3. | YES |

## Partition stress test

The special no-relation construction (A3) has neither a printed size–mass relation nor a printed mass bound. It files **only class 3** because §3 stops in limb A and §4 says class 3 is reached only from limb A, while classes 1, 2 and 4 are reached only from limb B. Although the completion-free state would semantically permit zero and therefore resembles class 4's `P=empty, Z non-empty` predicate, that predicate is never evaluated on this path. It cannot also file class 4 because limb B is never entered. Thus the limb separation, not a predicate distinction, prevents double filing.

The class-1 finite reading set is explicitly `R0, RE, RU, RK, RG`; all five lie in `P` and all have the same 10 kg floor. The class-2 finite reading set is explicitly `R0, RE`; both lie in `P`, but their floors are 10 kg and 20 kg. These exercise agreement and disagreement under §2's exact four-kind completion bound.

Each of the four scientific cases constructed above fits exactly one scientific class. None fits two classes and none fits no class. The two control/terminal configurations each fit exactly one of classes 5 and 6. Therefore **all six declared classes have a reachable input**, with no constructed case fitting two declared classes or none.

## Breaker-path count

C6 is initially engaged on **2 of the 6** classes: `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`. Once engaged, `C6_BREAKER_TEST=FAIL` can be recorded in **4 of the 6 terminal classes**: those two, plus `DYM_SOURCE_BLOCKED` when condition 3 is `UNDECIDED` after fallback, and `R3D_NO_CLASS` when a later control persistently fails. Therefore the requested number of the six outcome classes on which C6 can return/retain FAIL at terminal filing is **4**. It is `NOT_RUN` on the two no-positive-floor scientific routes; early class-5/class-6 filings also have `NOT_RUN` only when C6 was never engaged.

## Unreachable verdicts and blocking clauses

None. Condition 1's substantive PASS is entailed by valid positive-floor membership; C0 expressly licenses the `ENTAILED` proof plus a malformed filing that its dimensional check rejects. Every other requested direction and outcome has a valid concrete exhibition above.

R3D_C0_EXHIBITION_COMPLETE
