ACCESS_SHA=3e5b6979930535e5cb09c8eb6d3f6f3c7fefd47c6df8697cf2d384124c753234
C0_REACHABILITY=PASS

This is a reachability exhibition of Version 27's machinery, not a physics finding. All relations, reading sets, failures, and source/control states below are stipulated test inputs. No Dymnikova source was read. The document declares six classes: four scientific classes (1–4) and two non-scientific terminal states (5–6). The retired `DYM_FLOOR_COMPLETION_DEPENDENT` is not tested.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| §4 class 1 `DYM_FLOOR_DERIVED` | Stipulate a complete, control-clean census whose printed relations directly give the non-empty allowed mass set `M >= 10 kg`. The census leaves one object for each of the four §2 completion kinds unbound, so the finite reading set is exactly: completion-free; Euclidean-volume completion; uniform-interior completion; order-unity coefficient set to 1; GR-exterior completion. Each completion operates on a printed relation, is consistent, and leaves the mass set `M >= 10 kg`. Thus `P={CF:10 kg, EV:10 kg, UI:10 kg, OU:10 kg, GR:10 kg}`, `Z=I=empty`. | §2: exactly four completion kinds, each instantiated at most once when its object is UNBOUND, plus the completion-free reading -> §3 limb A reproduces a mass bound, so limb B -> §4: `P` non-empty, all `P` agree, `Z` and `I` empty -> class 1. C3 is stipulated PASS and reached controls pass. | yes |
| §4 class 2 `DYM_FLOOR_UNDERDETERMINED` | Stipulate printed relations `M=rho V`, `rho=1 kg/m^3`, and `r>=1 m`, with `V` unbound and all other completion-kind objects bound. Reading set: completion-free permits `V>0` and hence masses approaching zero (`Z`); the one applicable Euclidean-volume reading adds `V=(4 pi/3)r^3` and yields floor `4pi/3 kg` (`P`). Thus `P={EV:4pi/3 kg}`, `Z={CF}`, `I=empty`. | §2 admissibility: the Euclidean-volume assumption operates on printed `M=rho V` and is not itself the sole source of the bound because printed `r>=1 m` participates -> §3 reproduces a size–mass binding and enters limb B -> §4: `P` non-empty and `Z` non-empty means readings disagree -> class 2. The retired completion-dependent state expressly routes here. | yes |
| §4 class 3 `DYM_NO_SIZE_MASS_RELATION` | Stipulate a complete, control-clean census containing no printed relation binding size to mass and no printed mass bound. | §2 census completes -> §3 limb A cannot reproduce either kind of relation -> mandatory limb-A exit and class 3; C3, C4, C6 are `NOT_RUN`. It cannot also file class 4: §4 says class 3 is reached ONLY from limb A and classes 1/2/4 ONLY from limb B; limb B is never entered. Although the hypothetical completion-free reading would semantically be in `Z`, the filing separation is by limb, not predicate. | yes |
| §4 class 4 `DYM_NO_POSITIVE_FLOOR` | Stipulate a printed binding `M=(1 kg/m)r` with allowed `r>0`, and stipulate every completion-kind object BOUND, so the only reading is completion-free. Its non-empty allowed masses approach zero: `P=empty`, `Z={CF}`, `I=empty`. | §2 finite reading set -> §3 reproduces a size–mass binding and enters limb B -> §4 limb-B rule `P` empty and `Z` non-empty -> class 4. C3 and C6 are expressly `NOT_RUN`. | yes |
| §4 class 5 `DYM_SOURCE_BLOCKED` | Configure required manifest entry 18 as unreadable (permission/read error) before C6 is engaged. | §2a requires the pinned source -> §4.5: a required pinned source cannot be read -> `DYM_SOURCE_BLOCKED`; §9 records later, unengaged controls `NOT_RUN`. | yes |
| §4 class 6 `R3D_NO_CLASS` | All evidence is readable/resolved. A seat's reached `C4_GR_BENCHMARK` lacks the required exterior-limit algebra and fails; the third-seat re-run again lacks it and fails, while no `DYM_SOURCE_BLOCKED` predicate is true. | §4.6 first rules out `DYM_SOURCE_BLOCKED` -> same required control fails on the original attempt and its one authorized re-run -> persistent failure -> `R3D_NO_CLASS`. | yes |
| C6 condition 1 PASS | Filed floor `M_floor=10 kg`; dimensional analysis gives `[M_floor]=kg`. | §5 C6 condition 1 bounded procedure classifies dimension -> mass meets pass criterion. Valid positive-floor membership also entails this result. | yes |
| C6 condition 1 FAIL (filing-integrity case) | Malformed filing puts the dimensionless ratio `r_h/r_0=2` in the floor field. | §5 says conditions inspect the quantity actually FILED -> dimensional analysis gives dimension 1, not mass -> condition 1 FAIL. This is the malformed filing C0's `ENTAILED` route requires; no correctly filed positive-floor input can substantively fail condition 1 because §4's positive floor is a mass. | yes (`ENTAILED` for valid filings; malformed filing rejected) |
| C6 condition 2 PASS | Derived floor `M_floor=3 sqrt(hbar c/G)`; `3` is printed by the stipulated construction equation and `hbar,c,G` are in §2b. | Condition 2 scopes constants of the derived floor -> provenance rows terminate in the construction's manifest equation or §2b -> all rows pass. | yes |
| C6 condition 2 FAIL | Derived floor `M_floor=alpha sqrt(hbar c/G)` with `alpha=3` introduced only as “we choose 3,” with no manifest derivation. | Condition 2 traces `alpha` through the frozen C2 passages -> terminus is a choice outside the manifest and not §2b -> condition 2 FAIL. | yes |
| C6 condition 3 PASS | Derived floor `M_floor=sqrt(hbar c/G)` with no non-§2b symbol. | Replace every non-§2b parameter (there are none) and simplify -> printed free-symbol set outside §2b is empty -> condition 3 PASS. | yes |
| C6 condition 3 FAIL | Derived floor `M_floor=A sqrt(hbar c/G)` with free non-§2b normalization `A`. | Replace `A` by an algebraically independent symbol and simplify once -> free-symbol output `{A}` -> condition 3 FAIL. | yes |
| C6 condition 4 PASS | The derivation holds `G`, `c`, and `hbar` fixed; the fixity table cites §2b for each and the stipulated manifest construction equation derives every other fixed quantity. | Condition 4 lists every held-constant quantity -> each row has a permitted deriving passage/§2b basis -> condition 4 PASS. | yes |
| C6 condition 4 FAIL | The derivation holds core density `rho_0=7 kg/m^3` constant by choice, and the frozen C2 passages contain no passage deriving that constancy. | Condition 4 fixity table has a `rho_0` row without a manifest deriving passage -> condition 4 FAIL. | yes |
| C6 condition 5 PASS | Filed floor interval `[1.000e15,1.000e15] kg`. Computed intersections: with Planck `[2.176433e-8,2.176435e-8]`, empty; Hawking `[1.729e11,5.190e11]`, empty; TOV `[4.375e30,5.768e30]`, empty. ΛCDM has no interval. | Complete all four comparator rows -> §5 condition 5 fails iff an overlap exists -> no overlap -> PASS. | yes |
| C6 condition 5 FAIL — Planck row | Filed floor interval `[2.176434e-8,2.176434e-8] kg`. Its overlap with Planck `[2.176433e-8,2.176435e-8]` is exactly `[2.176434e-8,2.176434e-8] kg`; Hawking and TOV intersections are empty. | Complete comparison table -> named Planck overlap -> condition 5 FAIL by the iff rule. | yes |
| C6 condition 5 FAIL — Hawking row | Filed floor interval `[2.000e11,2.000e11] kg`. Its overlap with Hawking `[1.729e11,5.190e11]` is exactly `[2.000e11,2.000e11] kg`; Planck and TOV intersections are empty. | Complete comparison table -> named Hawking overlap -> condition 5 FAIL by the iff rule. | yes |
| C6 condition 5 FAIL — TOV row | Filed floor interval `[5.000e30,5.000e30] kg`. Its overlap with TOV `[4.375e30,5.768e30]` is exactly `[5.000e30,5.000e30] kg`; Planck and Hawking intersections are empty. | Complete comparison table -> named TOV overlap -> condition 5 FAIL by the iff rule. | yes |
| `C6_BREAKER_TEST=PASS` | A class-1 floor `[1.000e15,1.000e15] kg`; all its constants terminate in stipulated manifest equations or §2b, it has no non-§2b free symbol, every held-fixed quantity has a deriving passage, and the complete four-row comparison table shows no overlap. | §4 class 1 engages C6 -> conditions 1, 2, 3, 4, and 5 each PASS -> complete artefacts -> §5 C6 outcome rule -> `PASS`. | yes |
| `C6_BREAKER_TEST=FAIL` | A class-1 floor `[2.000e11,2.000e11] kg`, with conditions 1–4 passing and a complete table showing Hawking overlap. | §4 class 1 engages C6 -> condition 5 FAIL -> §5 says any failed condition yields `C6_BREAKER_TEST=FAIL`; class 1 is still filed. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 no-relation/no-bound census input above. | §3 limb-A exit -> §4 class 3 -> §5 says C6 was never engaged on this no-positive-floor-producing path -> `NOT_RUN`. | yes |

Partition stress result: all six declared classes have a reachable input. The class-1 exhibition uses the complete finite set of five readings (completion-free plus all four applicable completion kinds), and all of `P` agree. The class-2 exhibition uses the complete finite set applicable to that census and its readings disagree. Among the cases constructed here, no case files two classes and no case files none. In particular, the no-relation/no-bound case files class 3 alone: class 4's state predicate is semantically implied, as §4 itself acknowledges, but the operative limb-A stop prevents entry to class 4.

C6 path count: C6 is initially engaged on 2 of the 6 classes (`DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED`). Once engaged, `C6_BREAKER_TEST=FAIL` can be returned and carried into 4 of the 6 terminal classes: those two, `DYM_SOURCE_BLOCKED` after condition 3 remains `UNDECIDED` following fallback, and `R3D_NO_CLASS` after a later persistent control failure. Thus the requested number of six outcome classes on which C6 can return FAIL is **4**.

UNREACHABLE verdicts: none. Therefore there are no blocking clauses to quote.

R3D_C0_EXHIBITION_COMPLETE
