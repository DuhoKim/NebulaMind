ACCESS_SHA=ff1e5011f3e2fce5393b9647b9f2540d8055f5b90179955049dbfb1d1e60f430
C0_REACHABILITY=PASS

# C0 reachability exhibition — Version 29

Scope: this exhibition uses only `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md` and the numbers printed in it. The source manifest was not opened. Constructions below are reachability inputs to the document's decision machinery, not claims about Dymnikova physics.

The prompt calls for “five scientific classes,” but §4 expressly declares **four** scientific classes and two non-scientific terminal states. The table exhibits all six declared classes. The retired `DYM_FLOOR_COMPLETION_DEPENDENT` is not tested.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| Class 1 — `DYM_FLOOR_DERIVED` | Census contains a mass-binding relation but leaves an object unbound for each of the four tested kinds, so the finite reading set is exactly: completion-free; Euclidean-volume completion; uniform-interior completion; coefficient-set-to-1 completion; GR-exterior completion. Stipulate that each is consistent and has allowed masses `[10 kg,∞)`. Thus every reading is in `P`, all floors are 10 kg, and `Z=I=∅`. | §2: completion-free plus exactly the applicable members of the four named kinds; §3: a mass-binding relation is reproduced, so limb B; §4 decision table: `P` nonempty, all `P` agree, `Z=I=∅`; §4.1 files class 1. | yes |
| Class 2 — `DYM_FLOOR_UNDERDETERMINED` | Use the same explicit five-reading set, but allowed masses are: completion-free `[10 kg,∞)`; Euclidean `[20 kg,∞)`; uniform `[10 kg,∞)`; coefficient=1 `[10 kg,∞)`; GR exterior `[10 kg,∞)`. All are consistent and in `P`, but `P` contains floors 10 and 20 kg. | §2 finite reading rule; §3 limb B; §4 decision table: `P` nonempty and its readings disagree; §4.2 files class 2, reports every reading, and chooses no floor. C6 evaluates 10 and 20 kg separately. | yes |
| Class 3 — `DYM_NO_SIZE_MASS_RELATION` | A complete, control-clean census reproduces **no printed size–mass relation and no printed mass bound**. | §2c complete census; §3 limb A condition is met and commands class 3, C3/C4/C6 `NOT_RUN`, and stop; §4 says class 3 is reached only from limb A. Although the completion-free state would semantically permit masses approaching zero, limb B is never entered, so §4's class-4 filing rule cannot also file it. | yes |
| Class 4 — `DYM_NO_POSITIVE_FLOOR` | Census reproduces the printed mass relation `M > 0 kg`. It bounds mass “at all,” so limb B is entered, but its allowed set `(0,∞) kg` has infimum 0 and permits masses approaching zero. Take every applicable reading to retain that set. Hence `P=I=∅`, `Z` nonempty. (Independent inconsistency construction: printed `M≥1 kg` and `M≤0 kg`, giving `I` nonempty and `P=∅`, also files class 4.) | §3 enters limb B because a mass bound was reproduced; §4 puts each nonempty `(0,∞)` reading in `Z`; `P` empty and `Z` nonempty files class 4; §4.4 records C6 `NOT_RUN`. | yes |
| Class 5 — `DYM_SOURCE_BLOCKED` | The required pinned entry-18 file cannot be read (alternatively, its computed digest mismatches the §2a pin). | §2a requires the manifest source; C1 says an unread source or digest mismatch files `DYM_SOURCE_BLOCKED`; §4.5 defines this non-scientific terminal state and the study waits. | yes |
| Class 6 — `R3D_NO_CLASS` | All evidence is readable, digest-matched, and resolved. Both seats otherwise agree on class 1, but seat A's reached C5 fails; the third seat reruns that failed C5 once and it fails again. | §4.6 first rules out `DYM_SOURCE_BLOCKED`; its control-clean rule orders one rerun per failed control; the repeated C5 failure is the persistent failure and files `R3D_NO_CLASS`. | yes |
| C6 condition 1 PASS | Filed floor is the point interval `[10,10] kg`; dimensional analysis gives mass dimension. | §5 C6 condition 1 bounded procedure → dimension is kg → PASS. | yes |
| C6 condition 1 FAIL / malformed filing rejected | A seat improperly files the dimensionless ratio `M/M_☉=1` as its “floor.” | §5 says evaluation is on the quantity actually filed; condition 1 rule says dimensionless/ratio is not mass → FAIL. This is the expressly required malformed-filing exhibition. For a correctly filed positive mass floor, PASS is entailed by the class entry criterion, so no substantive valid-member failure exists or is required by C0's `ENTAILED` exception. | yes |
| C6 condition 2 PASS | Floor `3 sqrt(ħc/G)=6.529302e-8 kg`; provenance rows terminate `3` in a construction equation and `ħ,c,G` in §2b. | §5 condition 2 scope/provenance procedure; every constant terminates in a manifest-source equation or §2b → PASS. | yes |
| C6 condition 2 FAIL | Floor `α sqrt(ħc/G)` with `α=2` introduced only by “we choose α=2,” with no manifest derivation. | §5 condition 2 follows `α` to an assumed terminus outside the permitted provenance → FAIL. | yes |
| C6 condition 3 PASS | Floor `3 sqrt(ħc/G)`; replacing all non-§2b parameters leaves none, and simplification returns the printed number. | §5 condition 3 free-symbol procedure → no non-§2b symbol remains → PASS. | yes |
| C6 condition 3 FAIL | Floor `β sqrt(ħc/G)` with free non-§2b normalisation `β`; symbolic replacement leaves `β` in the simplified expression. | §5 condition 3 → a non-§2b symbol survives → FAIL. | yes |
| C6 condition 4 PASS | Floor `3 sqrt(ħc/G)` with no construction quantity held constant by choice; the fixity table is empty (or every listed constant has its cited manifest passage). | §5 condition 4 lists every held-constant quantity; every row has a deriving passage vacuously/on the stipulated table → PASS. | yes |
| C6 condition 4 FAIL | Floor depends on `ρ₀`, which is held constant in the frozen derivation, while the fixity table has no manifest passage deriving that constancy. | §5 condition 4 → held-constant quantity without a manifest derivation → FAIL. | yes |
| C6 condition 5 PASS | Filed point floor `[10,10] kg`. It overlaps none of the printed Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, or TOV `[4.375e30,5.768e30]` kg intervals; ΛCDM has no interval. | §5 condition 5 full four-row table → no overlap → PASS. | yes |
| C6 condition 5 FAIL — Planck row | Filed point floor `[2.176434e-8,2.176434e-8]` kg. Intersection with Planck interval is that same point, so it overlaps the **Planck** comparator; it overlaps neither Hawking nor TOV. | §5 condition 5: overlap with any enumerated interval iff FAIL → Planck overlap → FAIL. | yes |
| C6 condition 5 FAIL — Hawking row | Filed point floor `[2.000e11,2.000e11]` kg. Since `1.729e11 ≤ 2.000e11 ≤ 5.190e11`, the intersection is `[2.000e11,2.000e11]` kg: overlap with the **Hawking** comparator only. | §5 condition 5 overlap disjunction → Hawking overlap → FAIL. | yes |
| C6 condition 5 FAIL — TOV row | Filed point floor `[5.000e30,5.000e30]` kg. Since `4.375e30 ≤ 5.000e30 ≤ 5.768e30`, the intersection is `[5.000e30,5.000e30]` kg: overlap with the **TOV** comparator only. | §5 condition 5 overlap disjunction → TOV overlap → FAIL. | yes |
| `C6_BREAKER_TEST=PASS` | Class-1 floor `[10,10] kg`; dimension is mass; all floor constants trace; no free symbol survives; no underived fixity; complete four-row comparator table has no overlap. | §5 C6 applies to class 1; each condition 1–5 passes; table complete → C6 PASS. | yes |
| `C6_BREAKER_TEST=FAIL` | Class-1 floor `[2.176434e-8,2.176434e-8]` kg, with conditions 1–4 passing, complete table, and condition 5 failing by Planck overlap. | §5 C6 applies to class 1; any failed condition → C6 FAIL; §4.1 still files the derived-floor class. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 no-relation/no-bound construction above. | §3 limb A explicitly records C6 `NOT_RUN` and stops; §5 C6 says NOT_RUN when no positive-floor class is entered. | yes |

## Partition and path findings

All six declared classes have a reachable input. The four scientific filing rules are exhaustive on their permitted limbs: the class-3 construction files only from limb A; limb B is never entered, so it cannot also file class 4 even though the same unconstrained mass state satisfies class 4's state-level consequence. Classes 1, 2 and 4 are mutually exclusive on limb B because respectively `(P nonempty, no disagreement)`, `(P nonempty, disagreement)`, and `(P empty)` cannot coincide. None of the constructions fits two filed classes, and none fits no class. The terminal examples are also separated by §4.6's instruction to rule out `DYM_SOURCE_BLOCKED` before filing `R3D_NO_CLASS`.

C6 can return FAIL in **4 of the 6 declared terminal outcome classes**: it is initially engaged in classes 1 and 2, and an already-engaged FAIL can travel into class 5 (`DYM_SOURCE_BLOCKED`, via condition 3 remaining undecided) or class 6 (`R3D_NO_CLASS`, via a later persistent control failure). It is initially engaged on **2 of 6** classes. It is NOT_RUN on direct class-3 and class-4 paths and on class-5/class-6 paths filed before evaluation.

There are no UNREACHABLE verdicts, so no blocking-clause quotation is required.

R3D_C0_EXHIBITION_COMPLETE
