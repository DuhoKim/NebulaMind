ACCESS_SHA=a99aad15f168290fa5afaaf957d478cedd4cf57cb967e97dd3ea8d6b2a25840b
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — V30

Scope: this exhibition tests only the machinery and printed numbers of `R3D_DYMNIKOVA_FLOOR_PREREG_20260904.md`. It does not read or judge the Dymnikova sources. Configurations below are concrete hypothetical census/reading/control inputs to the document's decision procedure.

The document declares six classes: four scientific classes (1–4) and two non-scientific terminal states (5–6). The retired `DYM_FLOOR_COMPLETION_DEPENDENT` is not tested. Although the round instruction once says “five scientific classes,” §4 itself expressly says four scientific outcomes, and its six enumerated classes are all covered below.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| Class 1 `DYM_FLOOR_DERIVED` | A completed §2c census reproduces a printed relation whose allowed mass set is `M ∈ [10,∞) kg`. It also leaves objects for all four §2 completion kinds unbound, and each kind operates on a printed relation without changing that mass set. The finite reading set is: completion-free → floor 10 kg; Euclidean-volume reading → 10 kg; uniform-interior reading → 10 kg; coefficient-set-to-1 reading → 10 kg; GR-exterior reading → 10 kg. Thus `P={CF:10,E:10,U:10,C:10,GRE:10}`, `Z=∅`, `I=∅`. | §2: completion-free plus exactly the four named, individually applicable completions; §3: a mass-bounding relation is reproduced, so limb B; §4 partition: `P` nonempty, all `P` agree, `Z` and `I` empty; §4(1): file `DYM_FLOOR_DERIVED`. | yes |
| Class 2 `DYM_FLOOR_UNDERDETERMINED` | Census/reading set: completion-free allowed set `[10,∞) kg` (floor 10 kg); applicable Euclidean-volume reading `[20,∞) kg` (floor 20 kg); applicable uniform-interior reading `[10,∞) kg`; no coefficient or GR-exterior reading because those objects are bound. Thus `P={CF:10,E:20,U:10}`, `Z=∅`, `I=∅`. | §2 finite admissible set; §3 printed mass-bound relation → limb B; §4: `P` nonempty and `P` holds two different floors; §4(2): report every reading, choose none, file `DYM_FLOOR_UNDERDETERMINED`, and run C6 separately on 10 and 20 kg. | yes |
| Class 3 `DYM_NO_SIZE_MASS_RELATION` | Exhaustive §2c census contains no printed size–mass relation and no printed mass bound. | §2c census completes; §3 limb A: neither a size–mass binding nor any mass bound is reproduced; file `DYM_NO_SIZE_MASS_RELATION`, record C3/C4/C6 `NOT_RUN`, and stop; §4 says class 3 is reached only from limb A. It cannot also file class 4: even though the completion-free consequence permits masses approaching zero, class 4 is “LIMB B ONLY,” and limb B is never entered. | yes |
| Class 4 `DYM_NO_POSITIVE_FLOOR` | Census reproduces the printed relation `r = 2GM/c²` with allowed configurations `M>0, r>0`; hence it binds size to mass, but permits the sequence `M_n=1/n kg`, `r_n=2G(1/n kg)/c²`, so no positive lower bound follows. Completion-free and every applicable completion likewise permit zero: `P=∅`, `Z` nonempty, `I=∅`. | §3: a size–mass relation is reproduced → limb B; §4 limb-B partition: `P` empty and `Z` nonempty; §4(4): file `DYM_NO_POSITIVE_FLOOR`; C6 `NOT_RUN`. | yes |
| Class 5 `DYM_SOURCE_BLOCKED` | Required pinned source configuration: the entry-18 path cannot be read (permission/read error) before a scientific outcome is selected. | §2a requires the pinned source; §4(5): a required pinned source cannot be read → `DYM_SOURCE_BLOCKED`; the study waits. C6 was not engaged, so §5 C6 gives `NOT_RUN`. | yes |
| Class 6 `R3D_NO_CLASS` | Both required sources/evidence are readable and resolved. A seat's reached `C5_HARNESS_PINNED` fails because `/usr/bin/python3 --version` prints `Python 3.9.7` instead of frozen `3.9.6`; the third-seat rerun prints `3.9.7` again. No unread/unresolved evidence exists. | §4(6): first rule out `DYM_SOURCE_BLOCKED`; a reached required control fails, third seat reruns it once, and it fails again; persistent failure after two attempts → `R3D_NO_CLASS`. | yes |
| C6 condition 1 PASS | Filed floor `M_min=1 kg`; dimensional analysis gives `[M_min]=kg`. | §5 C6 condition 1 bounded procedure: classify dimension; kg is mass → PASS. | yes |
| C6 condition 1 FAIL (filing-integrity case) | Malformed filing enters the dimensionless ratio `M/M_☉=1` as “the floor.” | §5 C0's `ENTAILED` exception and §5a: valid positive-floor membership entails a mass, but the required malformed filing is tested; §5 C6 says a filed non-mass fails; dimensionless ratio → condition 1 FAIL. This is reachable only as rejection of a malformed filing, not as a substantive failure of a valid positive-floor class. | yes (`ENTAILED` for valid filings) |
| C6 condition 2 PASS | `M_min=2 sqrt(ħc/G)=4.352868e-8 kg`; constants are exactly `2`, `ħ`, `c`, `G`; `ħ,c,G` terminate in §2b and 2 is exact arithmetic. | §5 C6 condition 2: trace constants of the derived floor using the C2 passages/§2b; every constant terminates in the allowed provenance → PASS. | yes |
| C6 condition 2 FAIL | `M_min=α sqrt(ħc/G)` with `α=7` introduced only as “we choose α=7,” with no manifest derivation. | Condition 2 provenance table reaches an introduced choice rather than a manifest equation or §2b constant → FAIL. | yes |
| C6 condition 3 PASS | `M_min=2 sqrt(ħc/G)`. There are no non-§2b parameters; SymPy simplification contains no replacement symbols. | Condition 3 free-symbol procedure → no non-§2b symbol survives → PASS. | yes |
| C6 condition 3 FAIL | `M_min=λ sqrt(ħc/G)` with algebraically independent, non-§2b `λ`; the printed number can be obtained only by choosing `λ=2`. | Replace non-§2b parameters by independent symbols; simplified result still contains `λ` → FAIL. | yes |
| C6 condition 4 PASS | `M_min=2 sqrt(ħc/G)`; fixity table lists `ħ,c,G` and cites their fixed measured values in §2b; no other quantity is held constant. | Condition 4: every held-constant row has a deriving allowed passage/value → PASS. | yes |
| C6 condition 4 FAIL | `M_min=sqrt(3c²/(8πGρ_0))` while `ρ_0=1 kg m⁻³` is held fixed by choice and the C2 artefact has no manifest passage deriving that constancy. | Condition 4 fixity table has a held-constant `ρ_0` row with no manifest derivation → FAIL. | yes |
| C6 condition 5 PASS | Point floor `[1,1] kg`. Computed intersections: with Planck `[2.176433e-8,2.176435e-8]` is empty; Hawking `[1.729e11,5.190e11]` empty; TOV `[4.375e30,5.768e30]` empty. ΛCDM has no interval. | Complete four-row comparison table; no comparator overlaps; condition 5 iff rule → PASS. | yes |
| C6 condition 5 FAIL — Planck row | Point floor `[2.176434e-8,2.176434e-8] kg`. Intersection with Planck interval is exactly `[2.176434e-8,2.176434e-8] kg`; intersections with Hawking and TOV are empty. | Complete comparison table; the point overlaps comparator row 1 `[2.176433e-8,2.176435e-8]`; condition 5 iff rule → FAIL, naming Planck. | yes |
| C6 condition 5 FAIL — Hawking row | Point floor `[2.000e11,2.000e11] kg`. Intersection with Hawking is `[2.000e11,2.000e11] kg`; intersections with Planck and TOV are empty. | Complete comparison table; the point overlaps comparator row 2 `[1.729e11,5.190e11]`; condition 5 iff rule → FAIL, naming Hawking evaporation. | yes |
| C6 condition 5 FAIL — TOV row | Point floor `[5.000e30,5.000e30] kg`. Intersection with TOV is `[5.000e30,5.000e30] kg`; intersections with Planck and Hawking are empty. | Complete comparison table; the point overlaps comparator row 3 `[4.375e30,5.768e30]`; condition 5 iff rule → FAIL, naming TOV / neutron-star maximum mass. | yes |
| `C6_BREAKER_TEST=PASS` | Class-1 configuration with `M_min=1 kg`, derived from a hypothetical printed mass equation using only a manifest-derived exact coefficient and §2b constants; no free normalization; all held constants have C2 passages; complete comparator table has no overlap. | §4(1) positive floor engages C6; conditions 1–4 meet their respective dimension/provenance/free-symbol/fixity rules; condition 5 comparison above passes; table complete; §5 C6 outcomes → PASS. | yes |
| `C6_BREAKER_TEST=FAIL` | Class-1 configuration with `M_min=2.176434e-8 kg`; conditions 1–4 pass as above, but the complete table shows overlap with the Planck interval. | §4(1) engages C6; condition 5 FAIL suffices under §5's disjunctive FAIL rule; file scientific class 1 unchanged and record `C6_BREAKER_TEST=FAIL`. | yes |
| `C6_BREAKER_TEST=NOT_RUN` | The class-3 input above: exhaustive census reproduces neither a size–mass relation nor any mass bound. | §3 limb-A exit explicitly records C6 `NOT_RUN`; §5 C6 says no positive-floor class was entered and C6 was never engaged → NOT_RUN. | yes |

## Partition stress test

All six declared classes have a reachable input. Among the scientific cases constructed above, no case files two classes and none falls outside the partition. In particular, the no-relation/no-bound input has the state-level consequence `P=∅, Z≠∅`, but it files only class 3: §3 stops in limb A and §4 expressly restricts class 4 to limb B. Thus the two predicates can describe cause and consequence, but the filing paths do not overlap.

The class-1 reading set is explicitly finite and contains the completion-free reading and all four applicable named completion kinds; all five members of `P` agree on 10 kg. The class-2 reading set is also explicit and finite; its completion-free and uniform-interior readings yield 10 kg while its Euclidean-volume reading yields 20 kg, so `P` disagrees and class 2 follows.

## C6 failure reach

C6 is initially engaged on 2 of the 6 classes: classes 1 and 2. Once engaged, `C6_BREAKER_TEST=FAIL` can be carried into 4 of the 6 terminal classes: classes 1 and 2 directly, class 5 if condition 3 is `UNDECIDED` after fallback, and class 6 if a later reached control fails persistently. Therefore, as a number, C6 can return FAIL in **4 of the six outcome classes**.

Every breaker condition has a PASS construction and a FAIL/rejection construction. Condition 1's valid-class PASS is entailed; its FAIL is deliberately the malformed filing required by C0's `ENTAILED` route. Conditions 2–5 have substantive failing inputs, and condition 5 has a separate explicit overlapping point for every numerical comparator row.

## Unreachable verdicts and blocking clauses

None. No blocking-clause quotation is required.

R3D_C0_EXHIBITION_COMPLETE
