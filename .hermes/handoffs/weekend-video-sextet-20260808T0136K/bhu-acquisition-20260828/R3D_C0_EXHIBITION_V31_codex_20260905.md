ACCESS_SHA=aa3b184192503205229ea2a7428f7145741757636fe6ccd7fd0d834ebb108e62
C0_REACHABILITY=PASS

# C0 REACHABILITY EXHIBITION — V31

This is a reachability exhibition of the document's machinery only. Every construction below is a synthetic census/reading/control configuration; it neither reads nor makes a claim about a Dymnikova source. `DYM_FLOOR_COMPLETION_DEPENDENT` is retired and is not tested.

| verdict | concrete input | clause path | reachable yes/no |
|---|---|---|---|
| A1 — `DYM_FLOOR_DERIVED` | The frozen census reproduces a printed direct constraint `M ∈ [10,∞) kg` and also printed relations on which all four §2 completion kinds operate, with volume, interior profile, an order-unity coefficient, and exterior left unbound. Reading set: `R0` completion-free → floor 10 kg; `R_E` Euclidean volume → 10 kg; `R_U` uniform interior → 10 kg; `R_O` coefficient set to 1 → 10 kg; `R_G` GR exterior → 10 kg. All allowed sets are non-empty. Thus `P={R0,R_E,R_U,R_O,R_G}`, every member has the same floor 10 kg, and `Z=I=∅`. | §2: exactly four completion kinds, each at most once, plus the completion-free reading; §3: a mass-bounding relation is reproduced, so limb B; §4 definition of `P/Z/I`; §4 class-1 row: “`P` non-empty, all of `P` agree on one floor, `Z` AND `I` both empty”; §4.1 files `DYM_FLOOR_DERIVED`. | yes |
| A2 — `DYM_FLOOR_UNDERDETERMINED` | The census reproduces a printed relation. The applicable finite reading set is `R0` completion-free → `M ∈ [10,∞) kg`, `R_E` Euclidean volume → `M ∈ [20,∞) kg`, `R_U` uniform interior → `M ∈ [10,∞) kg`; the coefficient and GR-exterior objects are census-bound, so those kinds are not instantiated. Thus `P` is non-empty but contains floors 10 kg and 20 kg; `Z=I=∅`. | §2 admissibility/applicability rule fixes exactly these readings; §3 enters limb B; §4 puts all three in `P`; §4 class-2 row fires because “`P` holds two different floors”; §4.2 files `DYM_FLOOR_UNDERDETERMINED`, reports every reading, and chooses none. | yes |
| A3 — `DYM_SIZE_MASS_RELATION_UNREPRODUCED` | A complete §2c census contains no printed size–mass relation and no printed mass bound. All reached controls are otherwise clean. | §2c complete census precedes outcome choice; §3 limb A: neither a size–mass binding nor any mass bound is reproduced; §3 commands this token and stops; §4 class-3 row is “LIMB A EXIT”; C3/C4/C6 are `NOT_RUN`. | yes |
| A4 — `DYM_POSITIVE_FLOOR_UNREPRODUCED` | The census reproduces the mass relation `M > 0 kg` but nothing stronger. Completion-free and every applicable completion retain allowed set `(0,∞) kg`; hence every reading permits masses approaching zero: `P=I=∅`, `Z≠∅`. | The printed inequality bounds mass at all, so §3 enters limb B; §4 assigns the consistent readings to `Z`; §4 class-4 row has `P` empty and `Z` non-empty; §4.4 files `DYM_POSITIVE_FLOOR_UNREPRODUCED`; C6 is `NOT_RUN`. | yes |
| A5 — `DYM_SOURCE_BLOCKED` | One required §2a manifest source is unreadable (for example, opening the pinned entry-18 path returns a permission/read error), before C6 is engaged. | §2a requires the source; §4.5 expressly files `DYM_SOURCE_BLOCKED` when a required pinned source cannot be read; §9 marks later, never-engaged controls `NOT_RUN`. | yes |
| A6 — `R3D_NO_CLASS` | All evidence is readable/resolved. Both seats otherwise agree on class 1 and a floor of 10 kg, but one seat's reached `C5_HARNESS_PINNED` fails; the third seat reruns that failed control once and it fails again, with no source block. | §4.6 first rules out `DYM_SOURCE_BLOCKED`; its control-clean rule orders one rerun per failed control; the second failure is a persistent required-control failure in a seat; §4.6 files `R3D_NO_CLASS`. | yes |
| B1 PASS — C6 condition 1 | File `M_min=10 kg`; dimensional analysis gives `[M_min]=kg`. | §5 C6 applies on a positive-floor class; condition-1 bounded procedure classifies dimension; “dimension is mass (kg or M☉)” passes. | yes |
| B1 FAIL — C6 condition 1 filing-integrity check | Malformed filing: enter the dimensionless ratio `r_h/r_0=2` as “the floor.” | C6 says conditions act on the quantity actually filed and expressly says a non-mass fails condition 1; dimensional analysis gives dimension 1, so condition 1 rejects it. This is the §5 C0 `ENTAILED` exception's required malformed filing, not a substantively valid positive-floor member. | yes |
| B2 PASS — C6 condition 2 | `M_min=sqrt(ħc/G)=2.176434e-8 kg`; the provenance table terminates `ħ`, `c`, and `G` in §2b. | C6 condition-2 scope is constants of the derived floor; every constant terminates in §2b, satisfying its pass criterion. | yes |
| B2 FAIL — C6 condition 2 | `M_min=a sqrt(ħc/G)` with `a=7` introduced as “we choose 7,” with no manifest derivation. | The provenance rows for `ħ,c,G` terminate in §2b, but `a` terminates at the choice and outside the manifest; condition 2 says such a terminus fails. | yes |
| B3 PASS — C6 condition 3 | `M_min=sqrt(ħc/G)`; after replacing non-§2b parameters there are none, and simplification's non-§2b free-symbol set is `{}`. | Condition-3 bounded procedure and fallback both pass exactly when no non-§2b symbol remains. | yes |
| B3 FAIL — C6 condition 3 | `M_min=α sqrt(ħc/G)` with `α` free; the printed free-symbol set is `{α}`. This can occur in the class-2 construction where one admissible reading retains a positive floor family and another differs. | §4 class 2 permits a positive-floor reading with surviving freedom and runs C6; condition 3 replaces `α` by an algebraically independent symbol; it remains, so the exact pass criterion is false and the condition fails. | yes |
| B4 PASS — C6 condition 4 | Derivation `M_min=sqrt(ħc/G)` holds no construction quantity constant; the fixity table has no held-constant rows (the measured constants are §2b inputs). | Condition 4 requires a complete list and a deriving passage for every row; the exhaustive empty held-constant list vacuously satisfies “every row has such a passage.” | yes |
| B4 FAIL — C6 condition 4 | `M_min=f(ρ_0)` is made rigid only by holding `ρ_0=10^18 kg m^-3` fixed, while the reproduced C2 passages contain no derivation of that constancy. | Condition-4 fixity table contains `ρ_0` with no manifest deriving passage; its pass criterion says every row must have one, and expressly says an underived held-constant quantity fails. | yes |
| B5 PASS — C6 condition 5 | Degenerate floor interval `[1.000e15,1.000e15] kg`. It overlaps none: Planck `[2.176433e-8,2.176435e-8]`, Hawking `[1.729e11,5.190e11]`, TOV `[4.375e30,5.768e30]`; ΛCDM has no interval. | Condition-5 comparator table is executed in full; the iff rule says no overlap passes. | yes |
| B5 FAIL — Planck row | Degenerate floor `[2.176434e-8,2.176434e-8] kg`. Intersection with Planck is the same point, `2.176434e-8 kg`; no overlap with Hawking or TOV. | The floor point lies inside Planck `[2.176433e-8,2.176435e-8]`; condition 5 fails iff the floor interval overlaps any named comparator. | yes |
| B5 FAIL — Hawking row | Degenerate floor `[2.000e11,2.000e11] kg`. Intersection with Hawking is `2.000e11 kg`; no overlap with Planck or TOV. | `1.729e11 ≤ 2.000e11 ≤ 5.190e11`; condition-5 iff-overlap rule files FAIL and names Hawking. | yes |
| B5 FAIL — TOV row | Degenerate floor `[5.000e30,5.000e30] kg`. Intersection with TOV is `5.000e30 kg`; no overlap with Planck or Hawking. | `4.375e30 ≤ 5.000e30 ≤ 5.768e30`; condition-5 iff-overlap rule files FAIL and names TOV. | yes |
| C — `C6_BREAKER_TEST=PASS` | A class-1 census-derived formula `M_min=10^23 sqrt(ħc/G)=2.176434e15 kg`, where the manifest-census equation itself supplies coefficient `10^23`; dimension is kg, all constants trace to that equation or §2b, no non-§2b symbol remains, no quantity is fixed by choice, and the completed four-row comparison table has no overlap. | §4 class 1 yields a positive floor, so §5 engages C6; conditions 1–5 all meet their rules and the table is complete; C6's outcome clause returns PASS. | yes |
| C — `C6_BREAKER_TEST=FAIL` | A valid class-1 floor `[2.176434e-8,2.176434e-8] kg`, with conditions 1–4 passing but condition 5 overlapping the Planck interval. | §4.1 still files `DYM_FLOOR_DERIVED`; §5 engages C6; condition 5 fails by overlap; C6 outcome clause returns FAIL while the scientific class remains filed. | yes |
| C — `C6_BREAKER_TEST=NOT_RUN` | Use A3: complete census reproduces neither a size–mass relation nor any mass bound. | §3 stops in limb A and expressly records C6 `NOT_RUN`; §5 says NOT_RUN when C6 was never engaged on class 3. | yes |

## Partition and overlap findings

All six declared classes have a reachable input. For the four scientific constructions, the decision procedure files exactly one class in each case; no constructed admissible case files two classes or none.

The deliberately hard case is A3: no printed size–mass relation and no printed mass bound implies the state-level consequence that no positive bound follows, but it still files only class 3. Section 3 stops the procedure in limb A, §4 says class 3 is reached “ONLY from limb A,” and class 4 is “LIMB B ONLY.” Therefore A3 cannot also file class 4. This is separation by limb, not separation of the underlying predicates.

A1 explicitly uses the complete finite reading set allowed by §2: completion-free plus all four applicable named completion kinds, with every member of `P` agreeing at 10 kg. A2 uses completion-free plus each applicable completion and makes `P` disagree at 10 kg versus 20 kg. No unlisted completion is counted.

Condition 1 is substantively entailed for every correctly filed positive-floor case, exactly as §5a discloses. Its FAIL row above is reachable only as the malformed-filing rejection that §5 C0 expressly requires for an `ENTAILED` condition. Conditions 2, 3, 4, and 5 each have substantive PASS and FAIL constructions.

`C6_BREAKER_TEST=FAIL` can be recorded in **4 of the 6 outcome classes**: initially in `DYM_FLOOR_DERIVED` or `DYM_FLOOR_UNDERDETERMINED`; after engagement it can travel into `DYM_SOURCE_BLOCKED` when condition 3 remains `UNDECIDED` after fallback, or into `R3D_NO_CLASS` when an engaged C6 is followed by a persistent control failure. C6 is initially engaged on **2 of 6** classes. Thus every breaker condition can fail on at least one licensed path (condition 1 only in its expressly retained malformed-filing role).

## Unreachable verdicts and blocking clauses

None. No blocking-clause quotation is required.

R3D_C0_EXHIBITION_COMPLETE
