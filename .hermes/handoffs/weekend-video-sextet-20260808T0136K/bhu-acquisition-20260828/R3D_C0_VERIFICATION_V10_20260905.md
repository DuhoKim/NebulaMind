# Tori's VERIFICATION of the SECOND C0 exhibition — `C0_REACHABILITY=PASS`

**Tori, 2026-09-05 00:15 KST.** Exhibition **authored by a seat**; this is verification only.
**No tier, token, standing or stamp moves. Paper HOLD. R3D is NOT run and NOT frozen.**

## Completion checked BEFORE reading, under the rule my last error produced

`_tmp_r3d_c0_v10.log` carries **`ACCESS PROVEN`**, and the codex process count was **zero** before the artefact was
opened. Both conditions, not just the presence of bytes. *(The previous verification read a file mid-write and
reported `PASS` where the finished file said `FAIL`.)*

## The exhibition

`R3D_C0_EXHIBITION_V10_codex_20260905.md`, codex, against **V10**.
**`ACCESS_SHA` re-verified by me against V10 on disk after the run: `a441c7c97213df66…`.**

**Verdict line, verbatim: `C0_REACHABILITY=PASS`.** Table: **19 reachable, 0 unreachable.**
**Token and table agree.**

## The two previously-unreachable verdicts, now reachable — and by the intended mechanisms

- **Condition 1 FAIL** — exhibited by a seat filing the dimensionless number `2.0` as "its floor". Routes through
  §4.2 → C6 runs → the **"quantity the seat actually FILED"** clause → condition 1 fails. **This is the mis-filing
  path V10 created, working as designed.**
- **Condition 3 FAIL** — exhibited by a completion floor `M_min = α × 1.000e15 kg` whose simplified expression
  retains `{α}` and yields the printed value only once `α = 2.0` is chosen. Routes through class 2 → C6 →
  condition 3. **This is the surviving-freedom path V10 created.**

**Neither was made reachable by deleting an outcome or widening a class.**

## The number Blanc asked for, independently confirmed

codex, unprompted by my note: *"C6 can return FAIL on **3 of the 7 outcome classes**: `DYM_FLOOR_DERIVED`,
`DYM_FLOOR_UNDERDETERMINED`, and `DYM_FLOOR_COMPLETION_DEPENDENT`."* **That matches repair note A's claim of
1 → 3 exactly**, arrived at by a seat reading only the document.

## Arithmetic re-checked by me

`2.000e11` lies inside the Hawking interval `[1.729e11, 5.190e11]` → condition 5 FAIL. `1.000e15` lies above
`5.190e11` and below `4.375e30` → overlaps nothing → condition 5 PASS. Both confirmed independently.

## A NEW defect the exhibition surfaced, which does NOT block C0

codex: *"The A5 construction is a routing overlap, not an unreachable verdict: it is accepted by both classes 4 and
5, and the document states no precedence between those two classes."*

**Correct, and it is a real defect** — `DYM_NO_POSITIVE_FLOOR` and `DYM_FLOOR_COMPLETION_DEPENDENT` can both accept
the same construction with nothing ordering them. It does not make any verdict unreachable, so C0 passes; it makes
two verdicts **ambiguously** reachable, which is the class-exclusivity family this study keeps producing.

**It belongs to repair note B, with the design-gate list, not to this note.** Recorded here so it is not lost
between the two.

## Status

**C0 has PASSED on R3D V10.** That satisfies the doctrine's precondition for freezing. **R3D is still not frozen
and not run**: the two-seat design gate is separate and still owed, kimi's V9 gate has not yet landed, and repair
note B has not been written.
