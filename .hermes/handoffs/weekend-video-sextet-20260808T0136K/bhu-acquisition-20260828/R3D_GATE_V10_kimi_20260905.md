ACCESS_SHA=a441c7c97213df66dc7f80ca346e398eff0ad8afcd492069b440410ea9b4f3eb
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — SOUND.
The seven classes are exhaustive and mutually exclusive over the reachable space. The class-3/class-5 seam (no printed relation at all, yet a lone completion yields a floor) is closed procedurally: §3 limb A files DYM_NO_SIZE_MASS_RELATION "and stop", so class 5's tests are only reachable after a relation exists. Classes 2/4 are disjoint on the "none permits masses approaching zero" clause, making the stated precedence redundant but harmless. Classes 1/2/5 are partitioned by the single completion-free discriminator, and the V10 floor definition (strictly positive g.l.b., attained or not) closes the M ∈ (1,2) kg divergence. The inconsistency case is explicitly routed to class 4. I could not construct a real result fitting two classes or none. INCONCLUSIVE is genuinely reachable: a digest mismatch, an UNRESOLVED census row, or a double symbolic timeout each lands on DYM_SOURCE_BLOCKED on a specified path. No replacement.

2. CONTROLS — SOUND.
No control is passable by bare assertion: C0 (exhibition table), C1 (printed computed digests, anti-transcription extended to C1 in §9), C2 (full printed ledger plus per-source line/disposition reconciliation count), C3 (printed captured output of the deleted state), C4 (printed algebra plus premise list), C5 (three executed commands), C5b (per-path IN_SCOPE/OUT_OF_SCOPE table), C6 (per-condition artefacts plus the four-row table). Unreached controls are handled by the global §9 rule ("controls recorded NOT_RUN, never as passes... every unreached control throughout this document"). Every literal command is executable as written: the three §9 commands are valid, and the sympy invocation is correctly inside python3 -c. One observation, not a defect: the §2c block provision ("a single disposition") reads against the reconciliation equality unless covered lines carry DUPLICATE rows — but the arithmetic makes a single-row block fail C2 loudly on the spot, forcing the correct reading, which is the failure mode this design deliberately prefers. No replacement.

3. CIRCULARITY — ONE DEFECT (REPAIR R1).
The prohibition itself is intact: pattern record and prior conclusions are barred from inclusion, exclusion and outcome selection; the census accounts for every non-blank line with reconciliation counts and a full equation cross-check, so a contrary relation cannot be quietly omitted without visible arithmetic; C6's conditions are pinned verbatim with a hash. But V10 widened C6 to three classes and left one clause at V9:

Verbatim (§2c): "the pattern enters this study at exactly one point — C6's breaker evaluation — and only after `DYM_FLOOR_DERIVED` has been reached on other grounds."

Defect: under V10, C6 is evaluated on DYM_FLOOR_UNDERDETERMINED and DYM_FLOOR_COMPLETION_DEPENDENT as well (§5 header; §4 classes 2 and 5 each say "C6 is RUN on this class"). The trailing clause misstates the pattern's single entry point and contradicts two operative clauses; a seat filing class 2 or 5 could read §2c as forbidding the very evaluation §4 and §5 order. Two obedient seats could diverge on whether C6 may run.

Exact replacement: "the pattern enters this study at exactly one point — C6's breaker evaluation — and only after a positive-floor outcome class — `DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED` or `DYM_FLOOR_COMPLETION_DEPENDENT` — has been reached on other grounds."

4. THE FALSIFIER — TWO DEFECTS (REPAIRS R2, R3).
Number, as the document counts paths in §8b: C6 can return a substantive FAIL on 3 of the 7 declared classes (DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, DYM_FLOOR_COMPLETION_DEPENDENT); it is NOT_RUN by design on classes 3 and 4. Note, feeding the defect below: as written, the code C6_BREAKER_TEST=FAIL also accompanies DYM_SOURCE_BLOCKED (condition-3 UNDECIDED route) and R3D_NO_CLASS (persistent missing-table control failure), i.e. the FAIL code can co-occur with paths terminating in 5 of the 7 classes.

Per condition:
- Condition 1: PASS — filed floor 1.0e15 kg on DYM_FLOOR_DERIVED; dimension kg. FAIL — seat files the printed ratio M_min/m_P = 4.6e22 (dimensionless) as its floor; §5 evaluates the filed quantity, so this is caught, not assumed away.
- Condition 2: PASS — floor m_P = sqrt(ħc/G), all constants in §2b, pure numbers terminating in manifest equations. FAIL — floor (1/2)·sqrt(ħc/G) with 1/2 entering as "the simplest form", or a coefficient whose chain terminates at "following [ref]" outside the manifest; both termini are named failures.
- Condition 3: PASS — with r₀→x, Λ→y as independent symbols, the expression simplifies once to sqrt(ħc/G), x and y cancelling through the printed relations; no non-§2b symbol survives. FAIL — the underdetermined family floor(x) = x·m_P on DYM_FLOOR_UNDERDETERMINED; x survives. This is exactly the path §8b names.
- Condition 4: PASS — every held-constant quantity (e.g. r₀) has its deriving passage reproduced in the C2 artefact. FAIL — the derivation holds the core density or r₀ constant with no deriving passage reproduced in C2; the fixity table row lacks a source line.
- Condition 5: PASS — floor 1.0e15 kg overlaps none of the three intervals. FAIL — 2.0e11 kg (overlaps Hawking), m_P (overlaps remnant point), or 5.0e30 kg (overlaps TOV); comparator named.

No condition is unable to fail: 5 of 5 can FAIL and 5 of 5 can PASS, each on a concrete input above.

REPAIR R2 — contradictory C6 codes on the UNDECIDED path.
Verbatim (§5, outcomes): "**`C6_BREAKER_TEST=NOT_RUN`** — no positive floor was produced at all, i.e. the filed class is `DYM_NO_SIZE_MASS_RELATION`, `DYM_NO_POSITIVE_FLOOR`, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS`."
Verbatim (§5, same block): "**If condition 3 is `UNDECIDED`, record `C6_BREAKER_TEST=FAIL`** with the failed primary and fallback artefacts, then file `DYM_SOURCE_BLOCKED`"
Verbatim (§5, C6 header): "and is `NOT_RUN`, never a pass, only where no positive floor is produced at all (`DYM_NO_SIZE_MASS_RELATION`, `DYM_NO_POSITIVE_FLOOR`, `DYM_SOURCE_BLOCKED`, `R3D_NO_CLASS`)."

Defect: on the condition-3 UNDECIDED path a positive floor was produced and C6 was evaluated, yet the filed class is DYM_SOURCE_BLOCKED. The second quote orders FAIL for that path; the first and third define filed-class-BLOCKED as NOT_RUN. Two clauses assign opposite codes to the same path, breaking the document's own claim that the three codes are mutually exclusive. The terminal class is unaffected, so no wrong verdict can escape, but two obedient seats would record different C6 codes on identical events.

Exact replacement for the NOT_RUN definition: "**`C6_BREAKER_TEST=NOT_RUN`** — C6 was never engaged: no positive-floor class was entered, i.e. the filed class is `DYM_NO_SIZE_MASS_RELATION` or `DYM_NO_POSITIVE_FLOOR`, or the run filed `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` before any C6 evaluation. A run that evaluated C6 and reached condition 3's `UNDECIDED` branch records `C6_BREAKER_TEST=FAIL` and then files `DYM_SOURCE_BLOCKED`; it is never `NOT_RUN`."
Exact replacement for the header parenthetical: "and is `NOT_RUN`, never a pass, only where C6 was never engaged — `DYM_NO_SIZE_MASS_RELATION`, `DYM_NO_POSITIVE_FLOOR`, or a `DYM_SOURCE_BLOCKED`/`R3D_NO_CLASS` filed before any C6 evaluation."

REPAIR R3 — no adjudication rule for a same-class C6 split.
Verbatim (§9): "**Seat split**: if the two blind seats return **different terminal classes of any kind** — scientific, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` — **the third seat adjudicates exactly that split from the printed artefacts**, **re-executing any blocked read once before ruling**; its class is filed **only if it agrees with one of the two**."

Defect: conditions 2 and 4 are judgement-bounded to "the verbatim source passages reproduced in the C2 artefact", and no rule requires the two seats' C2 ledgers to agree. Two control-clean seats can therefore file the SAME scientific class with opposite C6 outcomes — one PASS (counterexample, amend the record), one FAIL on condition 2 or 4 (no amendment) — and the seat-split rule, which triggers only on different terminal classes, convenes nobody. The study's pivotal determination is then decided by which seat's report is read. Conditions 1, 3 and 5 are mechanical and cannot produce this split; 2 and 4 can.

Exact replacement (trigger clause only; the remainder of the bullet stands): "**Seat split**: if the two blind seats return **different terminal classes of any kind** — scientific, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` — **or the same scientific class with different `C6_BREAKER_TEST` outcomes** — **the third seat adjudicates exactly that split from the printed artefacts**, **re-executing any blocked read once before ruling**; its class — and, where the split is over C6, its C6 outcome — is filed **only if it agrees with one of the two**. If all three differ, or the third seat cannot decide, file `DYM_SOURCE_BLOCKED`."

5. RE-RUN GUARD — SOUND.
§6 states the guard plainly and the design honours it: limb A/limb B let the branch speak first, DYM_FLOOR_DERIVED is a reachable exhibited class, and no clause presupposes K6's UNDERDETERMINED. C0 mechanises the reachability check with seat-authored, Tori-verified exhibitions. Noted as status, not defect: §8b declares C0's fresh re-exhibition outstanding ("R3D is NOT frozen and NOT run. C0 must return PASS from a fresh seat exhibition before either."), and since R1–R3 change the bytes, that re-exhibition must target the repaired document. No replacement.

6. FAIRNESS — SOUND.
The wording "unreproduced from the stated inputs", not "error" and not nonexistence, holds in every operative clause: §3 limb A ("a size–mass relation was unreproduced from the stated inputs"), class 3, class 4's inconsistency branch ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and §6's standing rule. §0's "the source never supplies" describes K6's already-filed verdict token about a different branch; it files nothing here and is not operative. Class 6 is expressly non-scientific. No replacement.

7. STALL — SOUND.
Every terminal path files exactly one declared class: unread/unresolved evidence → DYM_SOURCE_BLOCKED; persistent control failure after two attempts with evidence read → R3D_NO_CLASS; class splits → third seat, with all-differ or cannot-decide → DYM_SOURCE_BLOCKED; symbolic timeout → fallback → BLOCKED; condition-3 UNDECIDED → FAIL then BLOCKED; a substantive C6 failure files the floor class with the failed condition reported (the V10 repair that closed the one correctly-fileable-outcome gap). I could not construct a terminal state with no fileable class. No replacement.

SUMMARY. Three repairs: R1 (§2c stale single-class entry point for the pattern, contradicting V10's three-class C6), R2 (NOT_RUN definition contradicts the UNDECIDED→FAIL→BLOCKED route in both the outcomes block and the C6 header), R3 (seat-split rule blind to same-class C6-outcome splits, leaving the counterexample determination unadjudicated on a reachable path). Sections 1, 2, 5, 6, 7 are sound as written. Apply R1–R3, then run the fresh C0 seat exhibition against the repaired bytes; neither precondition is waivable.

R3D_V10_KIMI_COMPLETE
