Script execution is blocked in this mode, so I verified the comparator arithmetic by hand instead: m_P = sqrt(3.16152677e-26 / 6.67430e-11) = 2.176434e-8 kg; t_0 = 13.797e9 x 31,556,952 = 4.35391e17 s; Hawking lower = (3.70856e17 / 7.16573e-17)^(1/3) = (5.1754e33)^(1/3) = 1.7298e11 kg, upper = 5.1895e11; TOV 2.2 x 1.98892 = 4.375624, 2.9 x 1.98892 = 5.767868. All five printed values reproduce, outward rounding holds, and the V9 self-check paths (1.0e15 PASS, 2.0e11 FAIL, m_P FAIL, 3.0e30 PASS) are correct against the printed intervals.

ACCESS_SHA=bbcb4a894957de620f978357dd0a4c2099f8302925db878694348cd6a743ae79
GATE=PREREG_UNSOUND

1. OUTCOME CLASSES

Defect 1a (gating): a reachable result fits NO class — §3's limb-A entry condition and §4's class-3 condition route the same case to opposite places.

Quote (§3): "attempt to reproduce, from the stated inputs, a printed relation binding size to mass. If none is reproduced after the complete census of §2, report that **a size–mass relation was unreproduced from the stated inputs**, file `DYM_NO_SIZE_MASS_RELATION` and stop."
Quote (§4 table): "| no printed relation binds size to mass or bounds the mass at all | **3** `DYM_NO_SIZE_MASS_RELATION` |"
Quote (§4 class 3): "**DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed."

Defect: the three clauses name three different entry conditions. §3 stops and files class 3 whenever no SIZE-MASS relation is reproduced — even if a manifest source prints a direct mass bound (e.g. "M >= m_P" with no V(M)). §4's table and class-3 definition both require that NO mass bound of any kind was reproduced. A printed direct mass bound without a size-mass relation is entirely plausible in this corpus (regular-core papers routinely state a limiting mass in prose); on that input §3 orders the seat to file class 3 and stop, §4 forbids class 3, and limb B is never entered so P/Z/I are never evaluated and classes 1/2/4 cannot fire. One reachable case, three clauses, no valid class. The "or bounding the mass" widening was applied to §4 but never back-propagated to §3 — the same repair-scar class as kimi's V11 R1.

Exact replacement for §3's two limb sentences: "**Limb A (~1 seat-day):** attempt to reproduce, from the stated inputs, a printed relation binding size to mass or bounding the mass at all. If none is reproduced after the complete census of §2, report that **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs**, file `DYM_NO_SIZE_MASS_RELATION` and stop. **Limb B:** if one is reproduced, derive the floor and test it against the five breaker conditions."

Defect 1b (repair scar): the scientific-class count is stale.

Quote: "**How the five scientific classes partition the cases — a decision procedure, not five descriptions.**"
Quote: "**These five are mutually exclusive and cover every case in which limb B is reached, by construction — so NO precedence rule between them is needed, and none is stated.**"

Defect: post-retirement there are four scientific classes (1-4) and two non-scientific terminal states; the header of §4 itself says "four scientific outcomes". "Five" survives twice from the five-scientific-class era — precisely the defect class this document sanctions elsewhere.

Exact replacements: "How the four scientific classes partition the cases — a decision procedure, not four descriptions." and "These four are mutually exclusive and cover every case in which limb B is reached, by construction — so NO precedence rule between them is needed, and none is stated."

With 1a repaired, the P/Z/I decision procedure is exclusive and total (P empty forces class 4; P non-empty splits cleanly between classes 1 and 2), and INCONCLUSIVE is genuinely reachable via DYM_SOURCE_BLOCKED and R3D_NO_CLASS. No two-class fit exists among classes 1/2/4; class 5 vs 6 is correctly ordered by "only after ruling out DYM_SOURCE_BLOCKED".

2. CONTROLS

Defect 2a (gating): six controls still self-certify with a bare PASS literal — the exact form this document diagnosed and repaired at C0 one round ago.

Quote (C1): "The printed digests are the artefact; a claimed pass without them fails. `C1_SOURCE_IDENTITY=PASS`."
Identical terminal form at C2 ("`C2_COMPLETION_LEDGER=PASS`."), C3, C4, C5, C5b.
Quote (C0, the repair that was not propagated): "*(It previously ended with a bare `C0_REACHABILITY=PASS`, which reads as the document certifying its own pass — and did so while §8 recorded a failing exhibition. A control cannot certify itself in its own text.)*"

Defect: the document's own stated principle — a control cannot certify itself in its own text — is violated at the six sibling sites the repair did not reach. As written, C1-C5b each end with the document pre-printing its own pass token. This is the one-site-not-the-class fix this lane's own rules forbid.

Exact replacement (each of the six sites): delete the sentence-terminal "`Cx_...=PASS`." and substitute "This control emits `Cx_...=PASS|FAIL|NOT_RUN` from the run actually executed; the token is a result to be recorded, not a claim this document makes about itself." (mirroring C0's repaired clause verbatim).

Defect 2b: the header contradicts the body on the document's own control state.

Quote (header): "**Tori, 2026-09-04 21:02 KST. Version 14 (see §8). FROZEN pending the fresh referee gate."
Quote (§8f): "**C0 must be re-run on V14: the partition changed. R3D is NOT frozen and NOT run.**"

Defect: C0 is NOT_RUN on V14 (acknowledged; the trailer says READY_FOR_REEXHIBITION), so the document is not frozen by its own gating rule, yet the header asserts FROZEN. The header timestamp (2026-09-04 21:02 KST) also predates V13's order (2026-09-05 00:59 KST) — the same header-versus-body defect class the document itself flagged at V7 ("both seats gated the document without noticing its own version was wrong").

Exact replacement (header): "**Tori, 2026-09-05. Version 14 (see §8). NOT frozen and NOT run: C0 must return `C0_REACHABILITY=PASS` from a fresh seat exhibition on V14's bytes, followed by the fresh referee gate, before any freeze."

Otherwise sound: every control names an exact PASS|FAIL|NOT_RUN token (§9 vocabulary line), every control demands a printed artefact rather than an assertion (digests, ledger, captured deletion output, limit algebra + premise list, harness output, per-path table, comparison table), unreached controls are routed to NOT_RUN including the "never PASS" rule, and the three §9 commands (`python3 --version`; `python3 -c "import sympy; print(sympy.__version__)"`; `shasum -a 256 $(command -v python3)`) are executable as written on this platform.

3. CIRCULARITY — sound, no replacement.

The pattern is pinned (verbatim conditions + record hash, evaluation against any other text fails C6), forbidden for inclusion/exclusion/outcome, and admitted at exactly one point after a positive-floor class is reached on other grounds. Quiet omission is mechanised away: every non-blank line carries a disposition, per-source reconciliation counts must match, and every displayed equation is its own row. One channel exists and is disclosed rather than hidden: the four comparator conventions (5120pi, 3.0, [2.2, 2.9] M_sun, Gregorian year) are lane-asserted, and widening row 2 makes a condition-5 FAIL — the pattern-preserving direction — marginally more likely. It is physically motivated (more species raise the evaporating-today mass), printed, computed inside the document, outward-rounded, and inspectable by the second seat; I judge it an accepted, disclosed risk, not a defect.

4. THE FALSIFIER (C6)

The number, as declared: 2. Quote: "**C6 applies on 2 of the 6 outcome classes**" and "**Reachable: 2.**"
The number the document's own operative clauses produce: 3. §5 states: "If condition 3 is `UNDECIDED`, record `C6_BREAKER_TEST=FAIL` ... then file `DYM_SOURCE_BLOCKED`", and the NOT_RUN clause is scoped to "`DYM_SOURCE_BLOCKED` / `R3D_NO_CLASS` filed **before any C6 evaluation**". So a run that engages C6 and hits the condition-3 UNDECIDED path files DYM_SOURCE_BLOCKED WITH C6_BREAKER_TEST=FAIL. C6 can therefore return FAIL on runs filing 3 of the 6 declared classes: DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, and DYM_SOURCE_BLOCKED (post-engagement). (A 4th follows the same logic: C6-as-control failing repeatedly on a missing table terminates in R3D_NO_CLASS with FAIL recorded — also post-engagement.)

Defect (gating): §5a's flat summary contradicts the operative clauses.
Quote: "**It is `NOT_RUN` on the other 4, and that is correct rather than a gap:**"
Defect: false for DYM_SOURCE_BLOCKED filed after C6 engagement — §5's own parenthetical concedes that path records FAIL. This is the third recurrence of the declared-versus-actual mismatch family this document twice retired classes over, now sitting in the section that exists to report that very count.
Exact replacement for that sentence: "**It is `NOT_RUN` on the other 4 only where they are filed before any C6 evaluation. FAIL is reachable on runs filing 3 of the 6 classes: the two positive-floor classes, and `DYM_SOURCE_BLOCKED` filed after engagement via condition 3's `UNDECIDED` (and on an `R3D_NO_CLASS` filed after repeated C6 control failure). What is restricted to 2 classes is not FAIL but the counterexample-certifying PASS.**"

Per condition, with concrete inputs:
- Condition 1 (dimension is mass): CAN PASS — floor filed as 2.0e15 kg. CAN FAIL — only on a malformed filing: a seat files the dimensionless ratio M/m_P = 4.6e22 as its floor. It CANNOT fail on any valid (correctly-filed) path: every positive-floor class requires a mass, so the entailment the document discloses is real. The document handles this honestly (ENTAILED, counted as 4-of-5 substantive, retained as a filing-integrity check). This is the only condition that cannot fail on a valid path; no condition is unfailable on every path.
- Condition 2 (constants trace): CAN PASS — floor M_min = sqrt(ħc/G) * sqrt(3/8pi) with every constant terminating in §2b or a manifest equation reproduced in C2. CAN FAIL — floor contains a factor 4pi introduced as "the simplest form" with no manifest terminus.
- Condition 3 (no free normalisation): CAN PASS — floor expression built solely from §2b constants and manifest-pinned parameters; the simplified expression contains no independent symbol. CAN FAIL — floor = alpha * sqrt(ħc/G) with alpha surviving simplification (the DYM_FLOOR_UNDERDETERMINED surviving-freedom case). UNDECIDED->FAIL also reachable via primary timeout + fallback timeout/parse failure.
- Condition 4 (no fixity assumed): CAN PASS — every held-constant quantity (e.g. r_0 fixed by a printed r_0^2 = 3/Lambda passage reproduced in C2) has a deriving passage. CAN FAIL — seat holds the core scale or Lambda constant "by choice" with no passage in its own C2 artefact.
- Condition 5 (comparator overlap): CAN PASS — floor 1.0e15 kg (overlaps none of the three intervals). CAN FAIL — floor 2.0e11 kg (overlaps Hawking [1.729e11, 5.190e11], named) or floor = m_P (degenerate Planck interval). I recomputed all five comparator values by hand; they reproduce exactly, outward rounding holds, and the disjunctive decision rule is executable with nothing left to read.

5. RE-RUN GUARD — sound, no replacement. "K6's outcome may **not** be assumed to repeat" is operative: §0 frames the branch as "in principle able" and routes the decision to the census and C2 ledger; class 1's entry conditions form a consistent, exhibit-able set (C0 requires a concrete reaching input before freeze); nothing in the class structure requires the K6 underdetermination to recur.

6. FAIRNESS — sound, no replacement. Limb A's exit and class 3's definition both read "unreproduced from the stated inputs"; class 4's inconsistency branch reads "a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"; §6 states the rule; no operative clause asserts nonexistence or error about the branch. Class 4's table condition ("no consistent admissible reading yields a positive floor") is a statement about the admissible-reading set from stated inputs, not about the sources.

7. STALL — not sound as written, inheriting finding 1a.

The terminal machinery is otherwise total: UNRESOLVED and digest mismatch file DYM_SOURCE_BLOCKED; undecidable-after-fallback files DYM_SOURCE_BLOCKED; persistent control failure files R3D_NO_CLASS after BLOCKED is ruled out; the seat-split rule now covers non-scientific disagreement and same-class/opposite-C6 splits, and "Every terminal path files exactly one declared class" is true of every path EXCEPT the finding-1a case: a printed direct mass bound without a size-mass relation reaches §3's terminal instruction ("file DYM_NO_SIZE_MASS_RELATION and stop") while §4's own condition for that class excludes the case — the seat either misfiles a class whose stated condition is not met or has no class to file. Repair 1a closes it; no further stall defect found.

R3D_V14_KIMI_COMPLETE
