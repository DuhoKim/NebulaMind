Script execution is unavailable in my mode: both the Python kernel and the `python3 -c` shell form are blocked. Per the note, I verified all arithmetic by hand and print my intermediates below. The two §9 commands that need no `-c` flag I did execute verbatim; they returned `Python 3.9.6` and a valid shasum digest of `/usr/bin/python3`. The third command is assessed by inspection only.

ACCESS_SHA=4e12ef211eea02808b36f1d9c0c756f7f5986459bb333aee8aa7f1cddcb4d708
GATE=PREREG_SOUND_WITH_REPAIRS

EXECUTION HONESTY NOTE (before the findings)
  Script execution is blocked in this mode. All arithmetic below is hand-verified with
  intermediates printed. I executed two of §9's three harness commands verbatim
  (`python3 --version` → Python 3.9.6; `shasum -a 256 $(command -v python3)` →
  b8763cf2…0e9  /usr/bin/python3). The third, `python3 -c "import sympy;
  print(sympy.__version__)"`, I could not run here; by inspection it is a correctly
  wrapped, standard command (the V4 defect, `sympy.__version__` written as a bare shell
  command, is gone), and §8 records codex executing it on V7/V8 — which I cannot
  independently re-verify in this mode.

  Hand-verified comparator arithmetic (intermediates):
    Gregorian year: 365.2425 × 86400 = 31,556,952 s  (365×86400 = 31,536,000; 0.2425×86400 = 20,952)
    t0 = 13.797e9 × 31,556,952 = 4.3539127e17 s        doc 4.3539e17  OK
    ħc = 1.054571817e-34 × 2.99792458e8 = 3.1615268e-26   §8g hand value 3.16152677e-26  OK
    ħc/G = 3.1615268e-26 / 6.67430e-11 = 4.73687e-16
    m_P = sqrt(4.73687e-16) = 2.1764343e-8 kg   (2.1764343² = 4.73687)
        → codex's V8 machine value 2.1764343420511267e-8 confirmed to 8 sig figs
        → printed comparator-1 point 2.176434e-8 = 2.1764340e-8 < computed value.
          The degenerate interval does NOT contain its own computed value.  (Finding 4a.)
    c⁴ = (8.9875518e16)² = 8.0776087e33 ;  ħc⁴ = 0.8518418
    5120π = 16084.954 ;  G² = 4.454628e-21 ;  denominator = 7.16525e-17
    numerator = 4.3539127e17 × 0.8518418 = 3.70884e17
    ratio = 5.1762e33 ;  cube root: 1.7298³ = 5.17592, 1.72982³ ≈ 5.17610
        → Hawking lower ≈ 1.72982e11 kg; doc 1.7298e11 OK; codex 1.7298245132213753e11 OK
    3.0 × 1.7298245e11 = 5.1894735e11 ≤ printed 5.190e11  (outward OK)
    2.2 × 1.98892e30 = 4.375624e30 ≥ printed 4.375e30  OK ;  2.9 × 1.98892e30 = 5.767868e30
      ≤ printed 5.768e30  OK
    Condition-5 probes: floor 1.0e15 kg overlaps nothing (ratio to nearest comparator
      ≈ 1927, so no NEAR_MATCH) → PASS.  Floor 2.0e11 kg ∈ [1.729e11, 5.190e11] → FAIL,
      comparator 2.  Floor m_P (computed) ∉ [2.176434e-8, 2.176434e-8] → PASSES as
      printed — this is the defect in 4a, not a correct PASS.

1. OUTCOME CLASSES — NOT SOUND AS WRITTEN. Two defects.

   Defect 1a (substantive): the partition is not mutually exclusive; the study's most
   likely outcome matches two rows.
   Verbatim (table row 1): "| no printed relation binds size to mass or bounds the mass
   at all | **3** `DYM_NO_SIZE_MASS_RELATION` |"
   Verbatim (table row 2): "| **`P` is empty** and **`Z` or `I` is non-empty** — no
   consistent admissible reading yields a positive floor | **4** `DYM_NO_POSITIVE_FLOOR` |"
   Verbatim (the exclusivity claim): "**These five are mutually exclusive and cover every
   case in which limb B is reached, by construction — so NO precedence rule between them
   is needed, and none is stated.**"

   A real result fitting two classes, construction (i): the manifest sources print a
   metric with core scale r_0 and mass M, regularity conditions, and NO relation binding
   r_0 to M and no mass bound; no admissible completion yields a floor either. Limb A
   then files class 3 and stops. But the completion-free reading is consistent and
   permits masses approaching zero, so Z is non-empty and P is empty — row 2 (class 4) is
   satisfied verbatim. One physics, two classes. This is not an exotic corner: it is the
   K6-analogous negative result, the single most likely outcome of this study.
   Construction (ii): same sources, plus the admissible completion "r_0 = l_P" (exactly
   one named assumption, vacuously consistent with sources that print no r_0–M relation).
   The printed horizon equation then yields M ≥ M_min(l_P) > 0. P is non-empty (the
   completion's reading), Z is non-empty (the completion-free reading) — row 3 (class 2,
   "`P` non-empty and the readings disagree — ... or `Z` is non-empty") is satisfied
   verbatim, while row 1 (class 3) is also satisfied. Limb A's "file
   `DYM_NO_SIZE_MASS_RELATION` ... and stop" resolves this in practice, but that is a
   precedence the table explicitly disclaims, and a seat cross-checking its limb-A filing
   against the declared decision procedure finds two rows matching a case the document
   says can match only one.
   Exact replacement (one inserted sentence before the table): "**The first row is limb
   A's exit; rows two through four apply only when a printed relation binding size to
   mass or bounding the mass has been reproduced and limb B is entered. The first row and
   the lower three are complementary by that domain.**" Traced: consistent with §3's limb
   structure, with class 3's clause, and with the retired-class note (whose
   completion-dependent state then presupposes limb-B entry, matching its own example).

   Defect 1b (count scar, sixth appearance): "**These five are mutually exclusive and
   cover every case in which limb B is reached" — the table above it has FOUR rows
   (classes 1, 2, 3, 4), and one of the four (class 3) is precisely the case in which
   limb B is NOT reached. §8g records the V14 count scar repaired at the heading; this
   instance survived lower in the same section.
   Exact replacement: "**These four are mutually exclusive and, given the first row's
   domain restriction, cover every case the run can reach after the census, by
   construction — so NO precedence rule between them is needed, and none is stated.**"

   INCONCLUSIVE reachability: yes, genuinely reachable — `DYM_SOURCE_BLOCKED` (a digest
   mismatch or unreadable source) and `R3D_NO_CLASS` (a control failing after two
   attempts) are both constructible, and C0 must exhibit both before freeze.

2. CONTROLS — SOUND on the asked criteria.
   Assertion: no control is passable by assertion. Each names a printed artefact and
   voids a claimed pass without it: C1 printed digests (with the anti-transcription rule
   extended to C1), C2 the full ledger, C3 the captured deleted-state output, C4 the
   printed algebra plus premise list, C5 the printed command outputs, C5b the per-path
   IN_SCOPE/OUT_OF_SCOPE table, C0 the seat-authored exhibition table verified by Tori,
   C6 per-condition artefacts plus the four-row comparison table.
   Codes: all eight controls emit exact tokens with PASS|FAIL|NOT_RUN (§9 vocabulary);
   none prints a bare PASS.
   Unreached: handled — limb A's exit explicitly names C3, C4, C6 as NOT_RUN and C0, C1,
   C2, C5, C5b as reached; C6 names its own NOT_RUN domain; §9 permits NOT_RUN only where
   the document explicitly makes a control unreached, and every reachable path's
   dispositions follow from that default.
   Executability: every literal command is executable as written — two of §9's three
   commands executed by me verbatim (outputs above); the third assessed by inspection
   (mode block), form correct; `shasum -a 256` (§8a) and the ACCESS_SHA echo are standard.

3. CIRCULARITY — SOUND.
   The pattern cannot reach the evidence. Inclusion: the census accounts for every
   non-blank line with a printed reconciliation count per source, and every numbered or
   displayed equation is its own row, so a contrary relation cannot be silently omitted —
   omission appears as arithmetic. Exclusion: only under four predeclared codes,
   demonstrated from source text, with the excluded row's verbatim text printed, so a
   wrongful exclusion is visible to the second seat and the reviewer rather than hidden.
   Selection: the class is fixed by the P/Z/I decision procedure over admissible readings,
   not by judgement. The pattern record is pinned by hash, quoted verbatim, and admitted
   at exactly one point (C6) after a positive-floor class is reached on other grounds; §0
   and §8 are de-patterned; the §2c sentence now names both positive-floor classes,
   matching C6's gating. The eleven search terms are demoted to a printed convenience and
   may not justify any row.
   Non-blocking observation (no replacement demanded): class 2's "a surviving freedom is
   what condition 3 rejects, so it is expected to fail" is an expectancy, and it is
   miscalibrated for the subcase where the filed floor has no free symbols (condition 3
   then passes). It is non-operative — the outcome must be exhibited — but it is the
   lane's expectation sitting next to a decision rule, and the seats should not read it
   as one.

4. THE FALSIFIER — the number, the per-condition table, then three defects.

   NUMBER: C6 can return FAIL on 2 of the 6 declared outcome classes —
   `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`. The document's own §5a states the
   same ("C6 applies on 2 of the 6 outcome classes ... Declared and reachable are now the
   same number") — confirmed correct, declared equals reachable.

   Per condition (PASS/FAIL with concrete inputs):
   Condition 1 — CAN PASS: floor filed as 1.0e15 kg, dimension mass. CAN FAIL: only on a
     malformed filing — a seat files the dimensionless ratio r_+/r_g = 2.5 as its floor.
     THIS IS THE CONDITION THAT CANNOT FAIL ON ANY VALID PATH: every positive-floor class
     requires a mass, so a correctly filed floor passes dimensionally. The document
     discloses exactly this (`ENTAILED` in §5a, the entailment route in C0). Sound, and
     honestly labelled — 4 of 5 conditions substantively failable, as §5a says.
   Condition 2 — CAN PASS: floor = √3·m_P, constants ħ, c, G in §2b, the coefficient
     traced to a manifest equation. CAN FAIL: floor = c²r_0/2G where r_0's value
     terminates in no manifest equation and is not in §2b — or a coefficient introduced
     as "the simplest form".
   Condition 3 — CAN PASS: floor = 2·m_P; after substitution no non-§2b symbol survives.
     CAN FAIL: floor = c²r_0/(2G) with r_0 free; the r_0 symbol survives → FAIL.
     (UNDECIDED maps to FAIL + `DYM_SOURCE_BLOCKED`, so "does not pass" is never a fourth
     state — checked, consistent.)
   Condition 4 — CAN PASS: a held-constant Λ with a manifest deriving passage reproduced
     in the C2 artefact. CAN FAIL: a derivation holding the core density ρ_0 constant by
     choice, with no manifest passage deriving the constancy.
   Condition 5 — CAN PASS: floor 1.0e15 kg (no overlap, no NEAR_MATCH — verified by hand
     above). CAN FAIL: floor 2.0e11 kg (overlaps comparator 2); floor = m_P (overlaps
     comparator 1 — only after repair 4a below; as printed it escapes). An incomplete
     table fails C6 loudly as a control. The disjunctive polarity is correctly stated and
     there is no hidden precondition on the pass.

   Defect 4a (the serious one — comparator 1 does not contain its own computed value).
   Verbatim: "| 1 | semiclassical GR + QM: a Planck-scale remnant | `[2.176434e-8,
   2.176434e-8]` | `m_P = sqrt(ħc/G)`; a point value, so the interval is degenerate |"
   The computed value from §2b's own constants is m_P = 2.1764343e-8 kg (intermediates
   printed above), which is GREATER than 2.176434e-8. A degenerate interval cannot be
   rounded outward, so the outward-rounding convention — "**Comparator endpoints are
   rounded OUTWARD throughout the table.**" — silently fails at exactly this row, and the
   printed point truncates the true value. Consequence: a seat deriving a Planck-mass
   floor computes 2.1764343e-8 from §2b, finds NO overlap with comparator 1, and condition
   5 PASSES — so the canonical semiclassical-remnant case, the one input the comparator
   exists to catch, escapes, and C6 can certify a counterexample the pinned condition-5
   text ("the number is not shared with ... any standard model") exists to reject. This
   is the V6 inward-rounding bias recurring at the one site the outward rule could not
   reach, and it biases in the high-stakes direction (toward amending the pattern
   record). A C0 exhibition whose condition-5 FAIL inputs touch only the Hawking row
   would never see it.
   Exact replacement for the table cell: "| 1 | semiclassical GR + QM: a Planck-scale
   remnant | `[2.176433e-8, 2.176435e-8]` | `m_P = sqrt(ħc/G) = 2.1764343e-8` kg at the
   precision of §2b's constants; **a point cannot be rounded outward, so the printed
   point is widened one unit in the last printed digit on each side — the outward
   convention applied to a degenerate interval** |"
   And append to the convention sentence: "**For a degenerate interval, 'outward' widens
   the point one unit in the last printed digit on each side.**"
   And require in C0: condition 5's FAIL exhibitions must include one input per
   comparator row, Planck row included.

   Defect 4b (C6's evaluation target is undefined on a reachable class-2 subcase).
   Verbatim: "**The conditions are evaluated on the quantity the seat actually FILED as
   its floor, not on an idealised one.**" and class 2's "report the freedom and **choose
   none**."
   Reachable input: the completion-free reading yields floor 5 kg; one admissible
   completion yields floor 10 kg; no reading permits zero; no inconsistency. Then P holds
   two different floors, Z and I are empty — class 2 by the table's first disjunct. The
   seat files class 2 and chooses none, so "the quantity the seat actually FILED as its
   floor" has no unique referent. Two obedient seats can evaluate different floors (one
   overlaps a comparator, the other does not), split C6 within one class, and the §9
   third seat — adjudicating "from the printed artefacts" — finds no rule preferring
   either and files `DYM_SOURCE_BLOCKED` on a case that had a scientific answer.
   Exact replacement (append to class 2's clause): "**Where `P` holds more than one
   floor, C6 is evaluated separately on each floor in `P`; the per-floor artefacts are
   printed, and `C6_BREAKER_TEST=PASS` exactly when every per-floor evaluation satisfies
   all five decision rules.**"

   Defect 4c (minor wording). Verbatim (decision-table row): "| 5 | a measurement could
   falsify it | the comparator table below, executed in full | **no** comparator matches
   within tolerance | the completed comparison table |" — "tolerance" is undefined; the
   authoritative rule below is overlap, stated as a single disjunction ("**Condition 5
   FAILS if, and only if, that interval OVERLAPS**"), with the decade flag "reported,
   never decisive". The row's loose word is a foothold for a second reading.
   Exact replacement for the pass criterion: "**no** comparator interval is overlapped,
   per the single disjunction below".

5. RE-RUN GUARD — SOUND.
   §6 states K6's outcome may not be assumed to repeat, and the positive class is
   genuinely reachable: class 1 requires the completion-free reading in P, all of P
   agreeing, Z and I both empty — satisfied, for example, if the sources print a
   minimum-mass formula and every one-assumption completion agrees with it. Reachability
   is not assumed: C0 must exhibit a concrete input producing `DYM_FLOOR_DERIVED` from a
   fresh seat on this version's bytes before any freeze.

6. FAIRNESS — SOUND.
   "Unreproduced from the stated inputs," not "error," holds in every operative clause:
   limb A's filing sentence ("a relation binding size to mass, or bounding the mass, was
   unreproduced from the stated inputs"), class 3's clause (same words), class 4's
   inconsistency branch ("report that a consistent solution, and hence a positive lower
   bound, were unreproduced from the stated inputs"), §6's rule, and limb A's
   parenthetical naming what the class does not claim. Class 4's table gloss ("no
   consistent admissible reading yields a positive floor") is a claim about the frozen
   census's admissible readings, not about the branch — inside the fairness boundary as
   written. The V7 class-4 slip ("no positive lower bound exists") does not recur in any
   operative clause.

7. STALL — SOUND (once 1a's scoping repair is applied).
   Every terminal path files exactly one declared class: unreadable or mismatched source,
   an UNRESOLVED census row, or a procedure undecidable after its named fallback →
   `DYM_SOURCE_BLOCKED`; a required control failing after two attempts with no evidence
   unread → `R3D_NO_CLASS`; any terminal-class split, or same class with split
   `C6_BREAKER_TEST` → third seat, all-differ-or-undecidable → `DYM_SOURCE_BLOCKED`;
   condition-3 UNDECIDED → C6 FAIL plus `DYM_SOURCE_BLOCKED`; the three NOT_RUN domains
   are named; the seat-split rule's final sentence states the invariant outright. Without
   repair 1a the run still terminates (limb A's "file ... and stop" fires first), so 1a
   is a class-selection defect, not a stall — but the exclusivity claim the stall
   argument leans on is false until it is applied.

ADDITIONAL DEFECTS OUTSIDE THE SEVEN HEADINGS

   Defect S1 (title contradicts the document's own state — the §8g-claimed repair did not
   reach the title). Verbatim: "# FROZEN — ORDERED — R3-D pre-registration: does the
   Dymnikova regular-core branch fix a minimum black-hole mass?" versus "**Tori,
   2026-09-05. Version 15 (see §8). NOT FROZEN and NOT RUN: C0 must return
   `C0_REACHABILITY=PASS` from a fresh seat exhibition on this version's bytes ... before
   any freeze.**" §8g records kimi catching "the header still saying `FROZEN`" and the
   repair landed on the version line but not on the title's leading token. A document
   whose freeze is gated on a fresh C0 exhibition cannot wear FROZEN in its title.
   Exact replacement: "# ORDERED — R3-D pre-registration: does the Dymnikova regular-core
   branch fix a minimum black-hole mass?"

   Defect S2 (§2b residual overclaim, contradicted by §5's own disclosure). Verbatim:
   "**Every comparator value in C6 is likewise computed from this list inside this
   document**, which is what makes condition 5 executable without reading anything
   outside the manifest." This is false for the four comparator conventions: §5 itself
   states "**Those four are frozen COMPARATOR CONVENTIONS — not constants in the derived
   floor, and not derivations from §2b.**" (5120π, 3.0, [2.2, 2.9] M_☉, the Gregorian
   year). The V6 overclaim ("every comparator interval is derived from §2b") was gated as
   a defect and repaired in §5; this sentence is the same claim surviving in §2b.
   Exact replacement: "**Every comparator value in C6 is computed inside this document
   from this list together with the four frozen comparator conventions named in §5**,
   which is what makes condition 5 executable without reading anything outside the
   manifest."

VERDICT RATIONALE
   The load-bearing mechanics are intact and were checked, not assumed: the falsifier
   returns FAIL on both live classes (Hawking-overlap input verified by hand against the
   printed comparators), no control is passable by assertion, the census turns silent
   omission into arithmetic, the re-run guard and fairness wording hold in every
   operative clause, and every terminal path files a class. Seven defects are named, each
   with an exact one-site replacement traced against the rest of the document; none
   requires redesign. But the document must not freeze as-is: 1a double-routes the
   study's most likely outcome; 4a lets the canonical Planck floor escape its own
   comparator in the high-stakes direction; 4b gives the falsifier no defined input on a
   reachable class-2 subcase. Hence SOUND_WITH_REPAIRS, not SOUND — and not UNSOUND,
   since every repair is local and specified. After the seven repairs: C0 re-exhibition
   on the new bytes, with condition-5 FAIL exhibitions required to touch every comparator
   row, then the two-seat gate, before any freeze.

R3D_V15_KIMI_COMPLETE
