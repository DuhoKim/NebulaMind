ACCESS_SHA=498faefce38e23058db9403b6384709516d871ae9642e205e0b282fe7f5c6cba
GATE=PREREG_UNSOUND

1. OUTCOME CLASSES

The six terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Class 3 is selected only by the limb-A exit; classes 1, 2, and 4 partition limb B by the disjoint `P`/`Z`/`I` cases; class 5 has priority for unread, mismatched, unresolved, or bounded-undecidable evidence; and class 6 expressly applies only after class 5 is ruled out. A real inconclusive result is reachable: let the completion-free reading of a printed size–mass relation give `M >= 10 kg`, and let one admissible one-assumption completion operating on that relation give `M >= 20 kg`. Then `P` contains two floors and class 2, `DYM_FLOOR_UNDERDETERMINED`, files. I find no result fitting two classes or no class under the stated routing. This section is sound; no replacement.

2. CONTROLS

The reached controls cannot be passed by bare assertion: C0 requires an exhibition table; C1 computed digests and `repr()`; C2 the full census and ledger; C3 captured execution output; C4 algebra plus premise list; C5 command output; C5b a complete per-path table; and C6 its condition artefacts and completed comparison table. Each control has one exact `PASS|FAIL|NOT_RUN` code. The three literal shell commands in section 9 are executable exactly as written; I executed all three successfully.

Defect — early terminal blocking leaves later control statuses unspecified. The document permits `DYM_SOURCE_BLOCKED` as soon as C1 finds a mismatch, C2 finds `UNRESOLVED`, or a bounded procedure is undecidable, but explicitly assigns `NOT_RUN` only to selected limb-A controls and to C6. Its global rule says: **“`NOT_RUN` is permitted only where this document explicitly makes that control unreached.”** Thus, for example, a C1 digest mismatch files class 5 while giving no lawful status for C2, C3, C4, C5, or C5b if they have not run. This prevents the required complete control-status record.

Exact replacement:

**“`NOT_RUN` is permitted only where this document explicitly makes that control unreached. A control is explicitly unreached when an earlier control has already forced `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` and the later control has not been engaged; every such later control is recorded with its exact `=NOT_RUN` token. Controls already engaged retain their actual result.”**

3. CIRCULARITY

The lane pattern cannot select the evidence or outcome: every nonblank source line receives a printed disposition, every displayed equation receives its own row, exclusions use only four predeclared codes supported by verbatim source text, unresolved evidence blocks, and the pattern enters only after a positive-floor class has been selected. A contrary mass/size relation cannot be quietly omitted without breaking the per-source reconciliation or equation list, and it cannot validly be excluded under `NO_MASS_OR_SIZE_CONTENT`. C3 additionally tests injected relations after source-field-equation deletion. This section is sound; no replacement.

4. THE FALSIFIER

As the document is written, C6 can return `FAIL` with **4** of the 6 declared terminal outcome classes: classes 1 and 2 directly; class 5 when condition 3 becomes `UNDECIDED` after C6 has engaged; and class 6 when an engaged C6 fails as a required control (for example, a persistently incomplete comparison artefact) after `DYM_SOURCE_BLOCKED` has been ruled out. It is `NOT_RUN` only when classes 3 or 4 file, or when classes 5 or 6 file before C6 evaluation.

Defect — section 5a counts only positive scientific classes, not declared terminal classes on which an already-engaged C6 can return `FAIL`. It says: **“C6 applies on 2 of the 6 outcome classes — `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`, every class that yields a positive floor.”** That contradicts section 5's explicit post-engagement class-5 path and the control-failure route to class 6.

Exact replacement:

**“C6 is initially engaged on 2 of the 6 outcome classes — `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`. Once engaged, it can return `C6_BREAKER_TEST=FAIL` in 4 terminal classes: those two scientific classes, `DYM_SOURCE_BLOCKED` when condition 3 remains undecidable after fallback, and `R3D_NO_CLASS` when a non-source C6 control failure persists under the two-attempt rule. It is `NOT_RUN` on classes 3 and 4 and on classes 5 or 6 filed before engagement.”**

Per-condition reachability:

1. Condition 1 can PASS: file `M_min = 10 kg`. It cannot substantively FAIL on any valid positive-floor path because such a floor is, by class membership, a mass. It can reject only a malformed filing, for example filing the dimensionless ratio `M/M_sun = 2` as the floor. Condition 1 is the condition that cannot fail on any valid path.
2. Condition 2 can PASS: `M_min = sqrt(hbar*c/G)`, with every constant in section 2b. It can FAIL: `M_min = alpha*sqrt(hbar*c/G)` with `alpha = 2` introduced solely by “we choose” and unsupported by a manifest equation.
3. Condition 3 can PASS: `M_min = sqrt(hbar*c/G)`, whose substituted expression has no non-section-2b symbol. It can FAIL: `M_min = lambda*sqrt(hbar*c/G)` with free non-section-2b `lambda`; the free-symbol set contains `lambda`.
4. Condition 4 can PASS: a floor using only `G`, `hbar`, and `c`, with no independently held-fixed construction quantity. It can FAIL: derive `M_min = rho_0 r_0^3` while holding `rho_0` constant by choice and providing no manifest passage deriving that fixity.
5. Condition 5 can PASS: the point floor `[1.0e15, 1.0e15] kg`, which overlaps none of the three finite comparator intervals. It can FAIL: `[2.1764343e-8, 2.1764343e-8] kg`, which overlaps the Planck-remnant interval. Analogous declared FAIL witnesses are `2.0e11 kg` for the Hawking interval and `5.0e30 kg` for the stellar-collapse interval.

5. RE-RUN GUARD

The design expressly forbids assuming K6 repeats and keeps the positive class genuinely reachable. A completion-free manifest relation whose nonempty allowed mass set has unique infimum `10 kg`, with all admissible readings agreeing and no `Z` or `I` readings, reaches `DYM_FLOOR_DERIVED`. This section is sound; no replacement.

6. FAIRNESS

Defect — the fairness rule is stated but not maintained in every operative negative-finding clause. The limb-A and inconsistency clauses use the required “unreproduced from the stated inputs” language, but the limb-B class table asserts: **“`P` is empty and `Z` or `I` is non-empty: no consistent admissible reading yields a positive floor”**. That is direct nonexistence wording, not the required reproduction finding.

Exact replacement:

**“LIMB B ONLY — at least one relation IS printed. `P` is empty and `Z` or `I` is non-empty: a positive floor was unreproduced from the stated inputs.”**

7. STALL

The design has a fileable terminal class for unread/mismatched/unresolved evidence and for bounded procedures undecidable after fallback, a separate class for persistent non-source control failure, and a third-seat rule whose all-different or undecidable result files `DYM_SOURCE_BLOCKED`. Symbolic operations and fallbacks have two bounded 120-second caps. No terminal path with no fileable class remains. The missing later-control dispositions found in item 2 make a blocked report procedurally incomplete, but do not deprive it of class 5. This section is sound; no replacement.

R3D_V17_GATE_COMPLETE
