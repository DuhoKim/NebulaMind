Both execution paths are blocked in this mode (single-query approval policy: terminal returned BLOCKED for `python3 -c`, and execute_code is disallowed). I state that plainly: I performed NO script execution. The §9 harness commands are assessed statically below, not executed. All comparator arithmetic is done by hand with intermediates printed.

ACCESS_SHA=498faefce38e23058db9403b6384709516d871ae9642e205e0b282fe7f5c6cba
GATE=PREREG_SOUND_WITH_REPAIRS

FINDING 1 — OUTCOME CLASSES: SOUND.

The partition is now constructed two different ways that agree. By limb: class 3 is filed only from limb A's exit, classes 1/2/4 only from limb B, and limb A exit (no printed relation binding size to mass or bounding mass) is the exact complement of limb B entry — so class 3 cannot co-fire with 1/2/4. Within limb B: every admissible reading lies in exactly one of P (positive glb of non-empty allowed set), Z (no positive lower bound), I (empty allowed set = inconsistent). P empty forces Z or I non-empty (the completion-free reading always exists), so class 4 covers P-empty exhaustively; P non-empty splits into "all agree, Z and I empty" (class 1) versus everything else (class 2). No gaps, no overlaps.

Constructs I attempted and failed to make pathological:
(a) Two-class: printed relations mutually inconsistent -> completion-free reading in I; completions are all inadmissible (none can be "consistent with every printed relation"); P empty, I non-empty -> class 4 only. Single class.
(b) No-class: P empty AND Z empty AND I empty would require zero admissible readings; the completion-free derivation is always one reading. Unreachable.
(c) Completion-dependent floor (retired class 5 state): completion-free in Z, completions agree on 10 kg -> P non-empty and Z non-empty -> class 2, and the document names this routing explicitly. The state is not homeless.
(d) The one soft spot: a case with no printed binding/bounding relation but a completion operating on printed relations yielding a floor exits limb A as class 3 and never reaches class 2. This is defensible, not a defect: class 3's filing wording is "unreproduced from the stated inputs", and a completion is by definition not part of the stated inputs.

INCONCLUSIVE is genuinely reachable two ways: DYM_SOURCE_BLOCKED (unreadable source, digest mismatch, UNRESOLVED row, undecidable-after-fallback) and R3D_NO_CLASS (persistent control failure with no unread evidence). Both are terminal filings, not stalls.

FINDING 2 — CONTROLS: SOUND.

Assertion-passability: every control is artefact-bound with an explicit anti-assertion clause. C1: printed computed digests, and the anti-transcription rule is extended to C1 ("a digest copied from S2a rather than computed from the file's bytes in the seat's own run fails C1_SOURCE_IDENTITY"). C2: "a summary assertion fails". C3: "a claimed pass without that output fails". C4: "a claimed pass without them fails". C5b: "a claimed pass without it fails". C0: the exhibition table is the artefact. I found no control passable by bare assertion.

Codes: all eight controls carry exact tokens with PASS|FAIL|NOT_RUN in S9's vocabulary list, and "NOT_RUN is permitted only where this document explicitly makes that control unreached". Unreached controls: the limb-A exit names C3/C4/C6 NOT_RUN and C0/C1/C2/C5/C5b reached — that is the explicit authorisation S9 requires. C6's NOT_RUN is defined by its own clause. Class 4 (limb B, P empty) leaves C3 reached: it still runs and passes trivially when no floor survives on an injected relation; awkward but determinate.

Literal commands: I could NOT execute them (blocked in my mode, stated above). Static assessment only: `python3 --version` is well-formed; `python3 -c "import sympy; print(sympy.__version__)"` is correctly quoted (the V6 defect of `sympy.__version__` as a bare shell command is repaired); `shasum -a 256 $(command -v python3)` uses a standard macOS utility and a POSIX substitution. I claim no execution; a seat with execution must confirm.

FINDING 3 — CIRCULARITY: SOUND.

The pattern cannot reach the evidence through enumeration: the census is line-accounted (every non-blank line gets a disposition; reconciliation counts must match or C2 fails), equation-cross-checked (every numbered/displayed equation is its own row), and exclusion is only via four predeclared reason codes each demonstrated by printed source text — so omitting a contrary relation shows up as arithmetic, and a stretched exclusion shows up as inspectable verbatim text. The eleven keyword strings are explicitly demoted to a printed convenience and "no row may be justified by their absence" — the V4 blind spot (symbol-only relations like `r_0^2 = 3/L` producing zero hits) is closed.

The pattern cannot reach class selection: "The shape/magnitude pattern record and every prior lane conclusion are forbidden as evidence for inclusion, exclusion or outcome selection"; it enters only at C6, only after a positive-floor class is reached on other grounds, and C6's condition text is pinned by hash ("Evaluation against any condition text not pinned here fails C6"). S0's parenthetical records the prior expectancy leak and its removal; the current S0 text states only that the branch is "in principle able" and that the census and C2 ledger decide. One residual channel exists by design: a seat could nominate pattern-protecting completions to force class 2. It is mitigated, not eliminated — admissibility is constrained (one named assumption, consistent with all printed relations, must operate on a printed relation), every completion is printed in the C2 ledger, class 2 must name the completion a floor depends on, and the seat-split rule convenes a third seat on divergent filings. That is an inspectable surface, which is the most a prereg can achieve here.

FINDING 4 — THE FALSIFIER: ONE DEFECT.

The demanded number. On how many of the 6 declared outcome classes can C6 return FAIL:
  - As a substantive breaker verdict attached to the filed scientific class: 2 of 6 (DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED). The document's "2" is correct in this sense.
  - As a recorded C6 outcome on the filed run, counting every path: 4 of 6. Class 1 yes; class 2 yes; class 5 yes — the C6 section's own rule: "If condition 3 is UNDECIDED, record C6_BREAKER_TEST=FAIL ... then file DYM_SOURCE_BLOCKED", and its parenthetical confirms a run filing DYM_SOURCE_BLOCKED "records FAIL, not NOT_RUN"; class 6 yes — a C6 control failure (incomplete/missing table, explicitly "fails C6 as a control") failing after two attempts files R3D_NO_CLASS with FAIL recorded under the control-failure rule.

DEFECT (verbatim): "**It is `NOT_RUN` on the other 4, and that is correct rather than a gap:** `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR` produce **no number to test**, so there is no candidate counterexample to evaluate; while `DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` are **non-scientific terminal states** in which the study waits rather than concludes."
Defect stated: the sentence asserts C6 is NOT_RUN on classes 5 and 6 universally. That contradicts the C6 section two pages earlier, which records C6_BREAKER_TEST=FAIL on the condition-3-UNDECIDED path whose filed class is DYM_SOURCE_BLOCKED, and contradicts the control-failure route by which a missing C6 table in both seats files R3D_NO_CLASS with FAIL recorded. On classes 5 and 6, NOT_RUN holds only when the class was filed before any C6 evaluation. S5a exists precisely to make this accounting checkable in the text; as written it misstates it. Classes 3 and 4 are the only classes on which C6 is always NOT_RUN.
Exact replacement: "**On the other 4 classes: it is always `NOT_RUN` on `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR`** — those produce no number to test, so there is no candidate counterexample to evaluate. **`DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` carry `NOT_RUN` only when filed before any C6 evaluation; filed after C6 was engaged, they carry `FAIL`** — a condition-3 `UNDECIDED` records `C6_BREAKER_TEST=FAIL` and then files `DYM_SOURCE_BLOCKED`, and a C6 control failure (an incomplete or missing table) persisting after two attempts files `R3D_NO_CLASS` with `FAIL` recorded. **So C6 returns FAIL as a substantive breaker verdict on 2 of the 6 classes, and a filed run can carry `C6_BREAKER_TEST=FAIL` on 4 of the 6.** A breaker test run where there is nothing to test would report on nothing, which is why classes 3 and 4 are always `NOT_RUN`."

Per condition (inputs are design-level; the condition-5 ones are hand-verified below):
  1. Observable magnitude. CAN PASS: floor 10 kg (dimension mass). CAN FAIL: a seat files the dimensionless ratio r_core/r_horizon = 0.61 as its floor — caught because conditions evaluate the quantity actually filed. On every correctly filed positive-floor path it is ENTAILED-pass, disclosed in S5a.
  2. Constants trace. CAN PASS: floor m_P = sqrt(hbar*c/G) — hbar, c, G all terminate in S2b. CAN FAIL: floor 0.5*sqrt(hbar*c/G) with the 0.5 introduced as an order-unity choice — terminus is "we choose", outside the manifest and S2b.
  3. No free normalisation. CAN PASS: floor expression sqrt(hbar*c/G) contains no non-S2b parameter; free-symbol set is empty. CAN FAIL: printed number 1.0e-8 kg recoverable only as alpha*m_P; replacing alpha with symbol a leaves a in the simplified expression. (Also the UNDECIDED route: timeout plus fallback failure -> FAIL + DYM_SOURCE_BLOCKED.)
  4. No fixity assumed. CAN PASS: floor derived with every held-constant quantity (e.g. Lambda) carrying a deriving passage in the C2 artefact. CAN FAIL: floor derived holding r_0 = 1e-5 m constant by choice with no manifest passage.
  5. Measurement could falsify. CAN PASS: floor 1.0e15 kg (no overlap; 3.3 decades from the nearest comparator, no NEAR_MATCH). CAN FAIL per comparator row: 2.0e11 kg overlaps Hawking [1.729e11, 5.190e11]; m_P overlaps Planck [2.176433e-8, 2.176435e-8]; 5.0e30 kg overlaps TOV [4.375e30, 5.768e30].
  No condition is unable to fail on every path. Condition 1 is unable to fail on any correctly filed path — disclosed as ENTAILED in S5a, retained as a filing-integrity check; that is a property, not the defect, and it is already labelled.

Arithmetic, by hand, intermediates printed (execution blocked in my mode; no execution claimed):
  hbar*c = 1.054571817e-34 * 2.99792458e8 = 3.16152677e-26 (matches S8g's cited value).
  hbar*c/G = 3.16152677e-26 / 6.67430e-11 = 4.73687e-16; sqrt = 2.1764343e-8 kg. Printed m_P exact at 8 figures. In [2.176433e-8, 2.176435e-8]: yes — the V16 degenerate-row widening is correct.
  Gregorian year = 365.2425*86400 = 31,556,952 s. t_0 = 13.797e9 * 31,556,952 = 4.3539127e17 s (doc prints 4.3539e17; S8g's 4.353913e17 confirmed).
  c^2 = 8.98755179e16; c^4 = 8.07760872e33; hbar*c^4 = 8.51841858e-1.
  Hawking numerator = 4.3539127e17 * 8.51841858e-1 = 3.70884508e17. Denominator = 5120*pi*G^2 = 16084.9544 * 4.45462805e-21 = 7.16525e-17. M^3 = 5.17616e33; M = 1.72983e11 kg (doc 1.7298e11; S8h's executed 1.7298245e11 agrees to 5 figures). Lower rounded down to 1.729e11 contains the value; upper 3x = 5.18948e11 rounded up to 5.190e11 contains it. Outward rounding holds.
  TOV: 2.2*1.98892e30 = 4.375624e30 -> 4.375e30 down, exact; 2.9*1.98892e30 = 5.767868e30 -> 5.768e30 up, exact.
  Exhibitions: 1.0e15 -> PASS, no NEAR_MATCH; 3.0e30 -> PASS with NEAR_MATCH vs TOV (ratio 1.458, 0.164 decades, reported never decisive); 2.0e11 -> FAIL Hawking; m_P -> FAIL Planck; 5.0e30 -> FAIL TOV. All reproduce the document's self-checks.

FINDING 5 — RE-RUN GUARD: SOUND. S6 forbids assuming K6's outcome ("this is a different branch and the study must be able to return DYM_FLOOR_DERIVED"), and the positive class is genuinely reachable: limb B entry plus completion-free in P, all of P agreeing, Z and I empty, is a coherent configuration the census could produce, and C0 must exhibit a concrete input reaching it before freeze. Nothing in the class structure or controls pre-loads K6's UNDERDETERMINED result.

FINDING 6 — FAIRNESS: SOUND. Every operative negative clause carries the reproduction framing: limb A and class 3 file "a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs"; class 4's inconsistency branch files "a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"; class 4's main case is a statement about the stated inputs' readings, not about the branch's physics, and S6 states the rule ("unreproduced from the stated inputs," not "error"). The S8f-recorded slip ("no positive lower bound exists") is repaired in the current text. No operative clause asserts error or nonexistence.

FINDING 7 — STALL: SOUND. Every terminal path files exactly one declared class: unreadable/mismatched/UNRESOLVED/undecidable-after-fallback -> class 5; persistent control failure with no unread evidence -> class 6; seat splits (different classes, or same class with opposite C6 outcomes) convene the third seat, which re-executes a blocked read once, files only on agreement with one of the two, and otherwise files DYM_SOURCE_BLOCKED; condition-3 UNDECIDED maps to C6=FAIL plus class 5 rather than a fourth C6 state; symbolic timeouts route through the named fallback rows to a decision or to class 5. I found no reachable state without a fileable class.

Summary: six sections sound, one defect — S5a's falsifier accounting contradicts the C6 section it summarises (NOT_RUN claimed on classes 5 and 6 universally; FAIL is in fact recordable on both after C6 engagement). One exact replacement is supplied above. The substantive falsifier itself is live: PASS and FAIL both reachable on both positive-floor classes, all five conditions failable, comparator arithmetic exact, outward rounding verified by hand.

Execution honesty statement: script execution was blocked in my mode (both the terminal and the code-execution tool refused under single-query approval policy). I executed nothing. The three S9 harness commands were assessed statically and found well-formed for macOS, but executability is NOT verified by me and must be confirmed by a seat with execution. All numerical results above are hand arithmetic with intermediates shown.

R3D_V17_KIMI_COMPLETE
