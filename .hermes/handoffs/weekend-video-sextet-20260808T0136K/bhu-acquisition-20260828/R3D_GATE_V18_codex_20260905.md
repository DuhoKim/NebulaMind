ACCESS_SHA=74f8e0c35cf3bdff4d84be848c1bd60d7ba61d10bbb6e348c16a0335994fc501
GATE=PREREG_SOUND

1. OUTCOME CLASSES — SOUND.

The six declared terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Limb A alone files class 3. Limb B alone reaches the disjoint `P`/`Z`/`I` partition and files class 1, 2, or 4. Source/read/unresolved/undecidable blocking files class 5; after class 5 is ruled out, persistent control failure files class 6. I could construct neither a result fitting two operative classes nor a result fitting none.

An inconclusive scientific result is genuinely reachable. Concrete input: the completion-free reading gives `M_floor = 10 kg`, while one admissible one-assumption completion operating on a printed relation gives `M_floor = 20 kg`. Then `P` contains two different floors, so class 2, `DYM_FLOOR_UNDERDETERMINED`, is filed and no floor is chosen.

2. CONTROLS — SOUND.

No control can pass by bare assertion: C0 requires an exhibition table; C1 computed digests (and `repr()` for the extracted text); C2 the complete census and ledger; C3 executed deleted-state output; C4 printed limit algebra and premise list; C5 live command output; C5b a complete per-path scope table; and C6 the per-condition artefacts and completed comparator table. Every control has one exact `PASS|FAIL|NOT_RUN` code. Early terminal blocks explicitly assign actual results to already-engaged controls and `NOT_RUN` to later unengaged controls; limb A separately fixes C3, C4, and C6 as `NOT_RUN`. Persistent non-source control failure is fileable as `R3D_NO_CLASS`.

Every literal shell command directed to a seat is executable exactly as written. I executed the three §9 commands verbatim. They returned `Python 3.9.6`, SymPy `1.14.0`, and SHA-256 `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9` for `/usr/bin/python3`.

3. CIRCULARITY — SOUND.

The lane pattern is barred from inclusion, exclusion, and class selection and enters only in C6 after a positive-floor class has been reached independently. Enumeration is line-complete and equation-complete; each nonblank line has exactly one disposition, reconciliation counts must match, and every displayed or numbered equation receives its own row. Exclusion is restricted to four named codes and must be demonstrated with the reproduced source text. Thus a contrary relation cannot be quietly omitted: omission breaks reconciliation, while exclusion leaves an inspectable verbatim row and reason code. C3 additionally tests whether an injected relation alone carries a derived floor.

4. THE FALSIFIER — SOUND.

`C6_BREAKER_TEST=FAIL` can be recorded in **4** of the 6 declared terminal outcome classes: `DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED`, `DYM_SOURCE_BLOCKED` after condition 3 remains undecided following fallback, and `R3D_NO_CLASS` after an engaged C6 is followed by persistent control failure. C6 is initially engaged from the two positive-floor scientific classes; engagement and terminal filing are correctly distinguished.

Condition 1: PASS is possible with a filed floor of `10 kg`. A substantive FAIL is impossible on any valid positive-floor-class path because class membership requires a mass floor; the document correctly marks this `ENTAILED`. Its filing-integrity FAIL remains operationally possible with the concrete malformed filing `M_floor = 0.5` as a dimensionless ratio.

Condition 2: PASS is possible with `M_floor = sqrt(ħc/G)`, whose constants all terminate in §2b. FAIL is possible with `M_floor = A sqrt(ħc/G)` and the provenance row `A = 7, we choose`, which terminates neither in a manifest equation nor §2b.

Condition 3: PASS is possible with `M_floor = sqrt(ħc/G)`, whose simplified expression contains no non-§2b symbol. FAIL is possible with `M_floor = α sqrt(ħc/G)`, where the printed free-symbol set is `{α}`.

Condition 4: PASS is possible with `M_floor = sqrt(ħc/G)` and no construction quantity held fixed by choice. FAIL is possible where a derivation fixes `r₀ = 1 m` by choice and the fixity table has no manifest passage deriving that constancy.

Condition 5: PASS is possible with the point interval `[1.0e15, 1.0e15] kg`, which overlaps none of the three finite comparator intervals. FAIL is possible for every comparator row required by C0: `[2.1764343e-8, 2.1764343e-8] kg` overlaps the Planck interval; `[2.0e11, 2.0e11] kg` overlaps the Hawking interval; and `[5.0e30, 5.0e30] kg` overlaps the stellar-collapse interval. ΛCDM has no interval and is explicitly nonmatching.

The sole condition that cannot fail substantively on any valid class path is condition 1. No condition is unable to fail operationally, because condition 1 rejects a malformed filing as specified.

5. RE-RUN GUARD — SOUND.

The design expressly prohibits assuming K6 repeats and permits a completion-free consistent reading with one agreed positive mass infimum to reach `DYM_FLOOR_DERIVED`. The positive class and a C6 pass are both genuinely reachable; neither depends on an earlier study outcome.

6. FAIRNESS — SOUND.

Every operative negative scientific clause uses reproduction-bounded wording: class 3 says a binding or bounding relation “was unreproduced from the stated inputs”; class 4 says a positive floor “was unreproduced from the stated inputs”; and the inconsistent case says a consistent solution and positive lower bound “were unreproduced from the stated inputs.” No operative clause labels a negative finding an error.

7. STALL — SOUND.

Every symbolic operation has a 120-second cap and a second bounded fallback; an absent, timed-out, unparsable, or nondeciding fallback files `DYM_SOURCE_BLOCKED`. Seat disagreements invoke the third-seat rule, and all-three disagreement or third-seat inability also files `DYM_SOURCE_BLOCKED`. Persistent control failure files `R3D_NO_CLASS`. Therefore no terminal path lacks a fileable declared class.

No design defects found; no replacement text is required.

R3D_V18_GATE_COMPLETE
