ACCESS_SHA=74f8e0c35cf3bdff4d84be848c1bd60d7ba61d10bbb6e348c16a0335994fc501
GATE=PREREG_SOUND_WITH_REPAIRS

Execution note: script execution is partially available in my mode. `python3 --version` ran (Python 3.9.6) and `shasum -a 256 $(command -v python3)` ran (digest printed, b8763cf2...e9). The `python3 -c "import sympy; ..."` form is blocked by my mode's own approval filter, so I did not execute it; I verified sympy 1.14.0 is installed via `pip3 show sympy`, so the command as written is syntactically valid and would print. Arithmetic was executed via a script file with intermediates printed; no file in the target directory was read or sought. The document was judged as inlined.

FINDING 1 — OUTCOME CLASSES: SOUND.
Constructions attempted:
(a) Two-class fit, class 3 ∩ class 4: blocked. A run is in exactly one limb — limb A exits only when no relation binding size to mass or bounding the mass is reproduced; class 4's predicate is evaluated only in limb B. The meaning-level entailment (class 3 case satisfies class 4's predicate) is real but disclosed in the document's own finding box, and the limb separation prevents any case filing both.
(b) Two-class fit, class 1 ∩ class 2: blocked by complementary predicates — class 2 requires P to hold two floors, or Z non-empty, or I non-empty; class 1 requires P singleton-agreeing with Z and I both empty.
(c) No-class result within limb B: not constructible. The admissible-reading set is always non-empty (the completion-free reading always exists), P/Z/I is total and exclusive over it, and P empty → row 2, P non-empty → rows 3/4 cover the remainder.
(d) Cond-3 UNDECIDED fits class 2 and class 5 in meaning, but is explicitly routed ("record C6_BREAKER_TEST=FAIL ... then file DYM_SOURCE_BLOCKED") — filed exactly once.
INCONCLUSIVE is genuinely reachable: class 2 (e.g., completion-free reading in Z plus one admissible completion operating on a printed mass function yielding 10 kg → P = {10 kg}, Z non-empty), and the non-scientific terminals 5 and 6.

FINDING 2 — CONTROLS: SOUND.
No control passes on bare assertion: C0 requires the exhibition table; C1 the printed digests; C2 the full printed ledger; C3 the captured output of the executed deleted state; C4 the printed algebra plus premise list; C5 the printed command output; C5b the complete per-path IN_SCOPE/OUT_OF_SCOPE table; C6 the per-condition artefacts plus the completed four-row comparator table. Each control names an exact token with PASS|FAIL|NOT_RUN (§9's vocabulary list covers all eight). Unreached controls are handled: §3 names dispositions on the limb-A exit (C3, C4, C6 NOT_RUN; C0, C1, C2, C5, C5b reached with actual results), and §9 defines explicit unreach plus the engaged-before-block rule. Literal commands: two of three executed as written; the third is valid as written but unexecuted by me for the mode reason stated above. Residual note (not a repair): C1's anti-transcription rule is an honour rule — a copied digest is indistinguishable from a computed one — but the artefact requirement itself stands.

FINDING 3 — CIRCULARITY: SOUND.
The pattern is barred from inclusion, exclusion and outcome selection; its single entry is C6, after a positive-floor class is reached on other grounds. The census cannot quietly omit a contrary relation: the complete extracted text is printed with stable line numbers, every non-blank line carries a disposition, the per-source reconciliation count must equal the non-blank-line count or C2 fails, and every numbered/displayed equation is its own row. Exclusion only by predeclared code demonstrated from verbatim source text. Omission therefore shows as arithmetic or as an undispositioned line in a printed artefact. Residual judgement sites (WRONG_BRANCH calls; which completions get considered) are forced into print — census row with verbatim text, C2 ADDED_COMPLETION rows against the K6-named completion menu — and sit under blind-double plus full re-runs. No quiet channel found.

FINDING 4 — THE FALSIFIER (C6).
Number: 4. C6_BREAKER_TEST=FAIL can be returned in 4 of the 6 declared outcome classes: DYM_FLOOR_DERIVED (condition failure, floor stands), DYM_FLOOR_UNDERDETERMINED (per-floor evaluation, any failure), DYM_SOURCE_BLOCKED (cond-3 UNDECIDED after fallback), R3D_NO_CLASS (engaged C6 plus persistent control failure, including C6's own incomplete-table control failure). Classes 3 and 4 can never carry FAIL (NOT_RUN there). This matches §5a's corrected two-number statement.

Per condition (PASS/FAIL each, with concrete inputs):
1. Dimension is mass. CAN PASS: floor 1.0e15 kg. CAN FAIL: only on a malformed filing — seat files "M/m_P = 4.6e22", a dimensionless ratio, as its floor. This is the condition that cannot fail on any valid path: every positive-floor class requires a mass, so a correctly filed floor passes by entailment. The document discloses this (ENTAILED) rather than counting it.
2. Constants trace. CAN PASS: floor m_P = sqrt(ħc/G) — every constant terminates in §2b. CAN FAIL: floor 3·m_P where 3 enters as "the simplest form" — terminus outside manifest equations and §2b.
3. No free normalisation. CAN PASS: floor sqrt(ħc/G) — no non-§2b symbol survives one simplification. CAN FAIL: floor α·sqrt(ħc/G) with α a non-§2b parameter — α survives. (Plus the UNDECIDED→FAIL route on timeout with a non-deciding fallback.)
4. No fixity assumed. CAN PASS: Λ held fixed with its deriving passage (printed r₀² = 3/Λ) reproduced in the C2 artefact. CAN FAIL: floor rigid only because r₀ is held equal to l_P by choice, with no deriving passage in the C2 artefact.
5. No comparator overlap. CAN PASS: floor 1.0e15 kg — executed check: overlaps none of the three rows. CAN FAIL: floor 2.0e11 kg (overlaps row 2); m_P (overlaps row 1); 5.0e30 kg (overlaps row 3) — all three executed and confirmed.
Arithmetic verification (executed): t0 = 4.353913e17; ħc = 3.16152677e-26; m_P = 2.1764343421e-8 ∈ [2.176433e-8, 2.176435e-8] (the degenerate-row widening genuinely contains its own value); Hawking lower 1.7298262e11 with printed 1.729e11 rounded down and 3× = 5.1894786e11 ≤ 5.190e11 rounded up; TOV endpoints 4.375624e30 / 5.767868e30 exactly as printed, outward-contained. PASS polarity is a disjunction with the completeness requirement moved to a loud control failure; nothing unreachable remains on the pass side.

FINDING 5 — RE-RUN GUARD: SOUND.
§6 forbids assuming K6's outcome ("this is a different branch and the study must be able to return DYM_FLOOR_DERIVED"). The positive class is genuinely reachable: printed mass function plus regularity plus the de Sitter limit fixing M_min = k·m_P with k terminating in manifest equations, and no admissible completion altering it, gives P singleton with Z and I empty. C0 must exhibit this pre-freeze from a fresh seat.

FINDING 6 — FAIRNESS: ONE DEFECT.
Verbatim, §4's decision-procedure table, class-3 row: "LIMB A EXIT. No printed relation binds size to mass or bounds the mass at all, so limb B is never entered". Defect: a nonexistence claim about the branch in an operative clause — the identical form this same version repaired in class 4's row (§8j records that repair). Every other operative clause holds the wording (§3's exit, class 3's own entry, class 4's row and entry, §6's rule). Exact replacement: "LIMB A EXIT. After the frozen census of §2, a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs, so limb B is never entered".

FINDING 7 — STALL: THE STALL QUESTION IS CLEAN; ONE COVERAGE DEFECT IN THE TERMINAL PROCEDURE.
No terminal state lacks a fileable class: limb A → 3; limb B → 1/2/4; unread source / digest mismatch / UNRESOLVED / undecidable-after-fallback → 5; persistent control failure after ruling out 5 → 6; seat-split all-differ or indecisive → 5; symbolic timeout without a deciding fallback row → 5. Defect, verbatim, §9's split trigger: "if the two blind seats return **different terminal classes of any kind** — scientific, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` — **or the same scientific class with different `C6_BREAKER_TEST` outcomes**". Defect: two control-clean seats filing the same scientific class with the same C6 outcome but different reported floors (classes 1 and 2) or different families (class 4) convene nobody — reachable because the document itself states no rule forces the two C2 ledgers to agree (the same divergence model that motivated the V11 R3 repair this clause carries). The filed class would then be ambiguous on the study's central quantity. Exact replacement: "if the two blind seats return **different terminal classes of any kind** — scientific, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` — **or the same scientific class with different `C6_BREAKER_TEST` outcomes, or with different reported floor formulas or values (classes 1 and 2) or different reported families (class 4)**".

R3D_V18_KIMI_COMPLETE
