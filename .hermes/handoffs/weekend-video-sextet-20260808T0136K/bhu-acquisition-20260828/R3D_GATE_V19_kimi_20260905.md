ACCESS_SHA=28a97c1a0522d6476cee6897470d272f8f7cd92ccfae332bc0132caa2fd02b54
GATE=PREREG_SOUND_WITH_REPAIRS

EXECUTION DISCLOSURE (required by the brief): `python3 -c` is blocked in this mode; I executed the comparator arithmetic as a script written to /tmp/r3d_v19_arith.py and run with `python3 /tmp/r3d_v19_arith.py`. It reads no file in this directory; I read no file other than the inlined target. I did NOT execute `r3d_c3_deletion_probe.py` or the §9 harness commands; the document's claim that the probe is "committed" and control-tested is taken as a claim of the document, not verified by me. Executed results: Gregorian year = 31556952 s; t_0 = 4.35391266744e17 s (document: 4.3539e17, OK); ħc = 3.16152677e-26; ħc/G = 4.7368664e-16; m_P = 2.1764343421e-8 kg, inside the widened degenerate row [2.176433e-8, 2.176435e-8] (True); c⁴ = 8.0776087e33; numerator = 3.7088450e17; denominator = 7.1652489e-17; ratio = 5.1761566e33; Hawking lower = 1.7298262e11 kg (the §8-recorded 1.7298245e11 differs in the 5th significant digit because it uses the rounded t_0 = 4.3539e17; both round to the document's 1.7298e11 and the outward rounding absorbs the difference — immaterial); 3.0x = 5.1894786e11; row 2 outward-contains both (True, True); TOV endpoints 4.375624e30 / 5.767868e30, outward-contained by [4.375e30, 5.768e30] (True, True). All three comparator intervals contain their own computed values.

1. OUTCOME CLASSES — SOUND.
The limb-separated structure holds. Class 3 files only from the limb-A exit; classes 1, 2, 4 file only from limb B and partition it by the P/Z/I reading sets, which are disjoint and total by construction (every admissible reading is in exactly one of P, Z, I; the reading set is never empty because the completion-free reading always exists). Class 5 and class 6 are explicitly ordered ("only after ruling out DYM_SOURCE_BLOCKED ... otherwise file DYM_SOURCE_BLOCKED"). Attempted double-fits, constructed: (a) nothing printed plus a completion supplying M >= 10 kg — killed, because a completion must operate on at least one printed relation and cannot be the sole source of a mass bound, and because filing is determined by the limb actually taken, a single procedural bit with UNRESOLVED routed to class 5; (b) inconsistent printed relations plus an unread source — the census cannot complete with a source unread, so class 5 pre-empts class 4; (c) one reading yielding 10 kg and another permitting zero — P non-empty and Z non-empty, class 2 only; class 1 requires Z and I both empty. No real result fits two classes as filed, and none fits zero: P empty implies Z or I non-empty (class 4), P non-empty is class 1 or class 2 by agreement, limb-A exit is class 3, blockage is class 5, control failure after blockage ruled out is class 6. INCONCLUSIVE is genuinely reachable: class 5 (concrete input: a manifest digest mismatch), class 6 (concrete input: all evidence read, C4 fails after two attempts in any seat), and class 2 as scientific underdetermination (concrete input: completion-free reading yields 10 kg, one admissible completion operating on a printed relation yields 20 kg).

2. CONTROLS — SOUND WITH ONE REPAIR.
No control is passable by bare assertion: C0 requires the seat-authored exhibition table (with one condition-5 FAIL input per comparator row); C1 requires printed computed digests with the anti-transcription rule explicitly extended to C1 in §9; C2 requires the full printed ledger plus the §2c census with per-source non-blank-line/disposition reconciliation counts and the full equation list; C3 requires captured output of the deleted state from a named script with named exit codes; C4 requires printed algebra plus printed premise list; C5 requires live execution of the three §9 commands; C5b requires the per-path table; C6 requires the per-condition artefacts and the completed four-row table. Every control names exactly `PASS|FAIL|NOT_RUN`. Unreached controls are handled: §9 now states explicitly that a control unreached because an earlier control forced a terminal block is recorded NOT_RUN while engaged controls carry actual results, and the limb-A exit names its dispositions (C3, C4, C6 NOT_RUN; C0, C1, C2, C5, C5b reached). Literal commands: the three §9 harness commands are valid shell as written; C3's invocation `python3 r3d_c3_deletion_probe.py <relations.json>` is well-formed against a script the document declares committed — executability of that file not verified by me (above).

DEFECT 2a (V19-sharpened interaction: C5b's scope rule cannot classify paths the design itself requires seats to open).
Verbatim: "**Path list**: every seat prints every path it opened; this lane's own reading tree (`../bhu-reading-20260823/sources/`) is **in scope**, another lane's files are not."
Defect: C5b demands, per opened path, IN_SCOPE or OUT_OF_SCOPE "together with the exact §9 scope-rule clause applied", and any OUT_OF_SCOPE row fails the control. The rule's only positive clause names `../bhu-reading-20260823/sources/`. The design itself requires opening paths outside that tree which are also not "another lane's files": C5's own third command `shasum -a 256 $(command -v python3)` reads the python3 binary, and V19's newly wired C3 requires `python3 r3d_c3_deletion_probe.py <relations.json>`, opening the probe script and its JSON input — none of these is under the named tree. No clause classifies them: a literal seat prints OUT_OF_SCOPE and fails C5b on every conforming run, or asserts an in-scope clause whose text does not cover the path — passable only by assertion, the failure C5b exists to prevent. Before V19 the script path was unnamed; V19 made two of these paths mandatory and named.
Exact replacement: "- **Path list**: every seat prints every path it opened. **In scope:** this lane's own reading tree (`../bhu-reading-20260823/sources/`); this document; the committed script `r3d_c3_deletion_probe.py` and the `<relations.json>` the seat writes for it; the seat's own artefact files written this run; and the executables the three §9 harness commands open, `$(command -v python3)` included. **Another lane's files are not in scope, and no other path is.** That distinction is stated because getting it wrong once already produced a spurious `UNDETERMINED` (R3B §5)."

3. CIRCULARITY — SOUND.
The pattern record and every prior lane conclusion are forbidden as evidence for inclusion, exclusion or outcome selection, and enter at exactly one point (C6's breaker evaluation, pinned verbatim with the record's own hash, evaluation against any other text failing C6), only after a positive-floor class is reached on other grounds. §0's restated pattern verdict is removed and its removal documented. Enumeration cannot be steered: the census dispositions every non-blank line, exclusion only under predeclared reason codes demonstrated by source text, every numbered/displayed equation is its own row, and the per-source reconciliation count makes a quietly omitted contrary relation show up as arithmetic failing C2. UNRESOLVED may not be treated as absence and forces DYM_SOURCE_BLOCKED. Class selection is by the P/Z/I partition over admissible readings, with admissibility defined. The one surface the lane does control — the four asserted comparator conventions (5120π, 3.0, [2.2, 2.9] M_sun, Gregorian year) — is frozen in the document, named as asserted, printed in the condition-5 artefact, and scoped out of C2 and condition 2 by name; it is disclosed and identical for both seats, not a channel from the pattern to the evidence. No contrary-relation channel found.

4. THE FALSIFIER — ONE REPAIR (the document's own accounting of the answer contradicts itself inside §5a).
THE NUMBER, on the document's corrected and operative accounting: C6 can record `C6_BREAKER_TEST=FAIL` in 4 of the 6 declared classes — DYM_FLOOR_DERIVED (a condition fails its decision rule; the class still files), DYM_FLOOR_UNDERDETERMINED (per-floor evaluation; a surviving freedom fails condition 3), DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after fallback: C6 records FAIL, then class 5 files — explicit clause), and R3D_NO_CLASS (C6 engaged, then a persistent control failure; §9 carries the engaged control's actual result into the terminal class). It is NOT_RUN on classes 3 and 4 always, and on classes 5/6 only when filed before any C6 evaluation. This matches C6's outcome definitions, §5a's first paragraph, §8j and §8k.

DEFECT 4a (§5a contradicts itself: the superseded V13 accounting survived the V18 repair in the same section).
Verbatim (i): "**The document therefore claimed a falsifiability it does not have — three declared routes to a breaker failure, two that any case can actually travel.** The live pair is `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`."
Verbatim (ii): "**It is `NOT_RUN` on the other 4, and that is correct rather than a gap:** `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR` produce **no number to test**, so there is no candidate counterexample to evaluate; while `DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` are **non-scientific terminal states** in which the study waits rather than concludes. **A breaker test run where there is nothing to test would report on nothing.**"
Defect: three paragraphs after stating that FAIL "can be recorded in 4 terminal classes ... plus `DYM_SOURCE_BLOCKED` ... plus `R3D_NO_CLASS`", §5a states the falsifier can travel only 2 classes and that C6 "is `NOT_RUN` on the other 4", explicitly naming DYM_SOURCE_BLOCKED and R3D_NO_CLASS as NOT_RUN states. Both cannot hold: C6's own NOT_RUN definition assigns NOT_RUN on 5/6 only when filed before any C6 evaluation, and the condition-3-UNDECIDED clause records FAIL with a class-5 filing. Sentence (i) also misdescribes the present document — no operative clause declares three routes; only marked history does. A seat exhibiting C0 or recording C6 from §5a gets two contradictory answers (2 vs 4; NOT_RUN vs FAIL on post-engagement 5/6). Note also §8k's F4 disposition — "Already applied at V18 ... No change." — is inaccurate as written: the repair reached §5a's first paragraph but not its lower half, the one-site-not-the-class failure this document itself names elsewhere. (Cosmetic, no repair demanded: §5a opens with a doubled sentence — "Two numbers, because one of them was wrong ..." immediately followed by "Two different numbers, and conflating them was the defect both V17 seats found." — a merge scar, not a contradiction.)
Exact replacement for (i): "**V13 therefore claimed a falsifiability it did not then have — three declared routes to a breaker failure, two that any case could then travel.** The two initially-engaged classes remain `DYM_FLOOR_DERIVED` and `DYM_FLOOR_UNDERDETERMINED`; under the corrected accounting above, `C6_BREAKER_TEST=FAIL` is recordable in the 4 terminal classes named there."
Exact replacement for (ii): "**It is `NOT_RUN` on `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR`, and that is correct rather than a gap:** those classes produce **no number to test**, so there is no candidate counterexample to evaluate — **a breaker test run where there is nothing to test would report on nothing.** On `DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` it is `NOT_RUN` only when they were filed **before any C6 evaluation**; filed after engagement, C6 carries its actual result, which can be `FAIL`."

PER CONDITION, PASS and FAIL, with concrete inputs:
Condition 1 (observable magnitude; dimension is mass): CAN PASS — floor = 2.0e11 kg, dimension mass. CAN FAIL — only on a malformed filing: a seat files the dimensionless ratio M_min/m_P = 9.19e18 as its floor; the conditions are evaluated on the quantity actually filed, so the dimensional classification fails it. On every VALID path it cannot fail — every positive-floor class requires a mass — and the document says so itself (ENTAILED, retained as a filing-integrity check). This is the condition the brief asks me to name: condition 1 cannot fail on any valid path; the document discloses this rather than claiming 5 of 5.
Condition 2 (constants trace): CAN PASS — floor = sqrt(ħc/G); every constant (ħ, c, G) terminates in §2b. CAN FAIL — floor = (3/2)·sqrt(ħc/G) where the 3/2 enters as "we choose the simplest matching coefficient" with no deriving passage; terminus outside manifest and §2b fails.
Condition 3 (no free normalisation): CAN PASS — floor = sqrt(ħc/G); replacing non-§2b parameters (none) and simplifying leaves no free symbol. CAN FAIL — floor = α·sqrt(ħc/G) with α a non-§2b parameter; the symbol survives simplification (the class-2 case: a completion-supplied floor depending on a free r₀). The UNDECIDED-after-fallback branch also maps to FAIL and is exhibited by any expression the fallback tree cannot parse.
Condition 4 (no fixity assumed): CAN PASS — the derivation holds nothing constant, or holds r₀ fixed with a deriving passage reproduced in the C2 artefact (e.g., a printed r₀² = 3/Λ). CAN FAIL — the derivation holds the core density constant "for definiteness" with no deriving passage in the C2 artefact; that row fails.
Condition 5 (a measurement could falsify it): CAN PASS — floor = 1.0e15 kg; overlaps none of [2.176433e-8, 2.176435e-8], [1.729e11, 5.190e11], [4.375e30, 5.768e30]. CAN FAIL — per comparator row, as C0 now requires exhibited: 2.1764343e-8 kg (Planck row — verified above to lie inside the widened degenerate interval), 2.0e11 kg (Hawking row), 5.0e30 kg (TOV row). The disjunctive pass with the loudly-failing completeness artefact is internally consistent; the ΛCDM row can never match and says so.
The falsifier genuinely bites in both directions; the V16 Planck-row escape is closed and I verified the closure numerically.

5. RE-RUN GUARD — SOUND.
No clause assumes K6's `K6_FLOOR_UNDERDETERMINED` repeats: §0 states the outcome "is decided only by the census and the C2 ledger", and §6 states "K6's outcome may **not** be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`." The positive class is genuinely reachable: the partition admits it (P non-empty, all of P agreeing, Z and I empty — concrete input: the completion-free reading alone yields floor sqrt(ħc/G) and no admissible completion exists), and no condition is pre-disabled — condition 5 passes on 1.0e15 kg, the comparator intervals now contain their own computed values (verified by execution above), and conditions 1–4 each have exhibited passing inputs.

6. FAIRNESS — SOUND.
"Unreproduced from the stated inputs", not "error", is held in every operative clause: the limb-A exit ("a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs"), class 3's table row (same wording — kimi's F6 repair is visibly applied), class 3's body, class 4's table row ("a positive floor was unreproduced from the stated inputs" — the V18 codex repair), class 4's body ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and §6's rule. I found no residual nonexistence claim in any operative clause; the class-3 row's "at least one relation IS printed" in class 4's row is a limb precondition, not a finding wording.

7. STALL — SOUND.
Every terminal path files exactly one declared class (§9 states this explicitly). Source unreadable, digest mismatch, UNRESOLVED proposition, or a bounded procedure undecidable after its specified fallback: class 5. Persistent control failure after class 5 is ruled out, including the one-control-clean-report scenario with the third seat's re-run also failing: class 6. Condition-3 UNDECIDED: C6 records FAIL, then class 5 — never a fourth state. Seat splits of any kind (different terminal classes, same class with different C6 outcomes, floors, or families) convene the third seat; all-three-differ or undecidable: class 5. Symbolic timeout: named fallback row under a second cap; row absent, timed out, or non-deciding: class 5 with the affected control not passed. No classless terminal state found.

R3D_V19_KIMI_COMPLETE
