ACCESS_SHA=301cba9b498eb7d202b5667b396aa1711f443bcfe9c7164fda2bb6e0f42d2a7c
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

SOUND. The six declared terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Limb A alone files class 3. Limb B alone reaches the disjoint P/Z/I partition and files class 1, 2, or 4. Source/evidence blockage files class 5; a persistent control failure after blockage has been ruled out files class 6. A result cannot traverse both limbs.

I could construct neither a result fitting two classes nor a result fitting none. A genuinely inconclusive result is reachable: let the completion-free reading yield a floor of 10 kg while one admissible one-assumption completion operating on a printed relation yields 20 kg. Then P is nonempty with two distinct floors, so class 2, DYM_FLOOR_UNDERDETERMINED, files.

2. CONTROLS

SOUND. C0 through C6 each emit an exact PASS/FAIL/NOT_RUN code. C1, C2, C3, C4, C5, C5b, and C6 require printed artefacts rather than assertions. C0 requires an exhibition table. Later controls made unreachable by an earlier terminal block are explicitly NOT_RUN, while already-engaged controls retain actual results. Limb A explicitly assigns C3, C4, and C6 NOT_RUN and keeps C0, C1, C2, C5, and C5b reached.

Every literal shell command directed to a seat is syntactically executable as written: `python3 r3d_c3_deletion_probe.py relations.json`, `python3 --version`, `python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 $(command -v python3)`. The C3 command names a real argument rather than shell-placeholder syntax. Runtime failure remains a reportable control result, not an ambiguity in the command.

3. CIRCULARITY

SOUND. The pattern is expressly forbidden as evidence for census inclusion, exclusion, and outcome selection and enters only at C6 after a positive-floor class is reached independently. The census accounts for every nonblank source line, gives every displayed or numbered equation its own row, restricts exclusions to declared reason codes supported by source text, reconciles counts, and blocks on UNRESOLVED evidence. Thus a contrary relation cannot be quietly omitted while C2 passes. C3 additionally tests whether an injected relation alone retains a determinate floor after deletion of source-pinned field equations.

4. THE FALSIFIER

C6 can return FAIL in 4 of the 6 declared terminal outcome classes: DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, DYM_SOURCE_BLOCKED after condition 3 remains undecided following fallback, and R3D_NO_CLASS after an engaged C6 is followed by a persistent control failure.

- Condition 1 can PASS: file M_min = 10 kg. It can FAIL only as a filing-integrity failure: file the dimensionless ratio 1/2 as the alleged floor. It cannot fail on any valid positive-floor path, because valid membership already requires a mass; it is correctly marked ENTAILED.
- Condition 2 can PASS: M_min = sqrt(hbar*c/G), with all constants in section 2b. It can FAIL: M_min = alpha*sqrt(hbar*c/G), where alpha = 3 is introduced merely by choice and has no manifest derivation.
- Condition 3 can PASS: M_min = sqrt(hbar*c/G), which leaves no non-section-2b symbol. It can FAIL: M_min = lambda*sqrt(hbar*c/G), with free lambda surviving simplification.
- Condition 4 can PASS: hold only G, hbar, and c fixed, all justified by the frozen premise list. It can FAIL: derive M_min while holding an unfixed core density rho_0 constant without a manifest passage deriving that fixity.
- Condition 5 can PASS: the point floor 1.0e15 kg overlaps none of the three finite comparator intervals. It can FAIL: 2.1764343e-8 kg overlaps the Planck interval; 2.0e11 kg overlaps the Hawking interval; and 5.0e30 kg overlaps the stellar-collapse interval.

The only condition that cannot fail on any valid substantive path is condition 1.

5. RE-RUN GUARD

SOUND. Section 6 expressly forbids assuming K6 repeats and the partition admits a completion-free, unique positive floor, so DYM_FLOOR_DERIVED is genuinely reachable.

6. FAIRNESS

DEFECT. The operative prose consistently says “unreproduced from the stated inputs,” but two operative outcome codes still assert absence rather than non-reproduction.

Verbatim: `3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.`

Defect: `DYM_NO_SIZE_MASS_RELATION` categorically says there is no relation, contradicting the sentence’s expressly limited epistemic finding.

Exact replacement: `3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.` Replace every operative occurrence of the exact token `DYM_NO_SIZE_MASS_RELATION` with `DYM_SIZE_MASS_RELATION_UNREPRODUCED`.

Verbatim: `4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family.`

Defect: `DYM_NO_POSITIVE_FLOOR` categorically says no positive floor exists, although the class is explicitly a failure to reproduce one from stated inputs.

Exact replacement: `4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above. Report the family.` Replace every operative occurrence of the exact token `DYM_NO_POSITIVE_FLOOR` with `DYM_POSITIVE_FLOOR_UNREPRODUCED`.

7. STALL

SOUND. Every bounded symbolic timeout has a second bounded fallback; absent, timed-out, unparsable, or undeciding fallback results file DYM_SOURCE_BLOCKED. Source/evidence blockage and persistent control failure have distinct terminal classes, and the seat-split rule resolves disagreements or files DYM_SOURCE_BLOCKED. Every terminal path therefore has a fileable class.

R3D_V20_GATE_COMPLETE
