ACCESS_SHA=301cba9b498eb7d202b5667b396aa1711f443bcfe9c7164fda2bb6e0f42d2a7c
GATE=PREREG_SOUND_WITH_REPAIRS

EXECUTION STATEMENT, stated plainly as instructed: script execution is BLOCKED in my mode. I attempted the comparator arithmetic twice (terminal heredoc, then the Python kernel); both were refused by the single-query approval policy. All arithmetic below is done BY HAND with intermediates printed. I did not execute the three harness commands; my check of them is inspection, not execution, and I say so where it matters. I read no file other than the inlined document.

FINDING 1 — OUTCOME CLASSES: SOUND, with one stale cross-reference to repair (R1).

Exhaustiveness and exclusivity hold. In limb B every admissible reading lies in exactly one of P, Z, I; the completion-free reading always exists once limb B is entered, so P = Z = I = empty is impossible. Given P non-empty, class 1 ("all of P agree on one floor, Z AND I both empty") and class 2 ("P holds two different floors, or Z is non-empty, or I is non-empty") are negations of each other; P empty routes to class 4. Class 3 is reachable only from limb A, classes 1/2/4 only from limb B; the V17 limb separation holds operationally because limb A's exit and limb B's entry are complementary on the same predicate ("a relation binding size to mass, or bounding the mass, was unreproduced" — §3 and §4's class-3 row now agree verbatim).

Constructions I attempted and could not land: (a) allowed set (1,2) kg — GLB 1 is strictly positive, attained or not, so the reading is in P under the stated floor definition; files class 1 or 2, no ambiguity. (b) Nothing printed about mass — class 3 by limb; class 4's predicate is satisfied in meaning but class 4 is unreachable because limb B is never entered; single filing. (c) A printed direct mass bound with no size-mass relation — limb A does not exit ("or bounding the mass at all"), limb B entered, files 1/2/4; the V15 routing mismatch is closed in both clauses.

INCONCLUSIVE is genuinely reachable: printed r_0 = 2GM/c^2 alone (completion-free reading in Z) plus one admissible completion operating on it that yields a floor (P non-empty) files class 2 — a real middle verdict, not a stall.

R1 (repair required). Verbatim: "**Definition, so classes 1, 2 and 5 have a common discriminator:**"
Defect: stale cross-reference. That sentence dates from V10, when class 5 was DYM_FLOOR_COMPLETION_DEPENDENT, a floor-bearing class. Since V13's retirement, class 5 is DYM_SOURCE_BLOCKED, which has no floor content and no use for the discriminator. The definition serves the classes whose predicates quantify over positive floors: 1, 2 and 4. This is the scar family's ninth appearance: a sentence that reasons FROM the renumbering was not swept when the class was retired. Non-operative (no decision rule changes), but it is wrong text in the operative class section of a document whose own rule is to sweep dependents, not repetitions.
Exact replacement: "**Definition, so classes 1, 2 and 4 have a common discriminator:**"
Sibling noted for the sweep, no replacement needed: §5a's "DYM_FLOOR_COMPLETION_DEPENDENT IS A DEAD CLASS" is marked historical accounting and its numbers (2 engaged, 4 terminal) are current.

FINDING 2 — CONTROLS: SOUND.

No control is passable by bare assertion. Each names a printed artefact and states that a claimed pass without it fails: C0 the exhibition table, C1 the printed digests (with the anti-transcription rule explicitly extended to C1 in §9), C2 the full ledger plus census reconciliation counts, C3 the captured output of the executed deleted state with exit codes 0/1/2, C4 the printed limit algebra plus premise list, C5 the three command outputs, C5b the per-path IN_SCOPE/OUT_OF_SCOPE table, C6 the completed four-row comparison table. Every control emits exactly one named token PASS|FAIL|NOT_RUN from the run actually executed; none ends in a bare PASS literal.

Unreached controls are handled: §9 makes a control explicitly unreached when an earlier control has already forced DYM_SOURCE_BLOCKED or R3D_NO_CLASS and the later control was never engaged, with engaged controls carrying actual results; §3's limb-A exit names C3, C4, C6 as NOT_RUN and C0, C1, C2, C5, C5b as reached — consistent with that rule.

Literal commands: "python3 --version", "python3 -c \"import sympy; print(sympy.__version__)\"", "shasum -a 256 $(command -v python3)" are syntactically executable as written on this lane's macOS (shasum and command -v are present there); the C3 invocation "python3 r3d_c3_deletion_probe.py relations.json" is a literal invocation with no placeholder syntax (the V20 angle-bracket defect is repaired). Inspection only: I could not execute them in my mode, and I cannot verify the probe script's existence without reading the directory, which the brief forbids; I judge only that the command as written is executable in form given the committed file the document names.

FINDING 3 — CIRCULARITY: SOUND.

The pattern cannot reach the evidence through any of the three channels. Enumeration: the census is full-text line accounting — every non-blank line carries exactly one disposition, the per-source reconciliation line must make non-blank lines equal assigned dispositions or C2 fails, and every numbered or displayed equation is its own row; a quietly omitted contrary relation shows up as arithmetic, and the eleven terms are demoted to a printed convenience that may justify no row. Exclusion: only under the four predeclared codes, each demonstrated by verbatim source text, with UNRESOLVED forcing DYM_SOURCE_BLOCKED and forbidden from counting as absence; an abused reason code is inspectable by the second seat and the audit because every row carries its verbatim text. Class selection: mechanical over P/Z/I from the printed ledger, with the pattern record barred as evidence for inclusion, exclusion or outcome, entering only at C6 against verbatim-pinned condition text (hash 5232201a…c8d4647), only after a positive-floor class is reached on other grounds. §0's italic discloses that the pattern record once named this branch the likely breaker; that is disclosed history, the operative enumeration is mechanical, and any residual expectancy runs toward refutation — the direction the design's own artefacts must earn — not toward confirmation.

FINDING 4 — THE FALSIFIER: one defect (R2); per-condition analysis and the number follow.

THE NUMBER: 4. C6 is initially engaged on 2 of the 6 declared classes (DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED); once engaged, C6_BREAKER_TEST=FAIL can be recorded in 4 terminal classes — those two, plus DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after its fallback, which §5 explicitly records as FAIL) and R3D_NO_CLASS (an engaged C6 followed by a persistent control failure, e.g. a repeatedly incomplete comparison table failing C6 as a control). NOT_RUN correctly occupies classes 3 and 4 and any pre-engagement block. §5a's two numbers match the routes the text actually provides.

Per condition, with concrete inputs:
1. CAN PASS: filed floor 1.0e15 kg — dimension mass, passes the dimensional analysis. CAN FAIL: only a malformed filing — a seat files 0.63 (a ratio to M_sun) or 1.5e-10 m as its floor; §5 evaluates the quantity actually filed, so this is caught. It CANNOT fail on any VALID path: every positive-floor class requires a mass, so a correctly filed floor passes. This is the ENTAILED condition, and §5a discloses exactly that rather than counting it as substantively failable. Named as requested: condition 1 is the condition that cannot fail on any valid path; literally speaking no condition is unfailable on all paths, because condition 1 retains the malformed-filing failure.
2. CAN PASS: floor m_P derived from printed relations — ħ, c, G each terminate in §2b, chain stays inside the manifest. CAN FAIL: floor 3·sqrt(ħc/G) with the 3 introduced as "the simplest form" — terminus outside manifest and §2b, fails exactly as a we-assume terminus.
3. CAN PASS: floor m_P — replacing every non-§2b parameter by independent symbols and simplifying once leaves no surviving symbol. CAN FAIL: floor c^2 r_0 / 2G with r_0 a free non-§2b parameter — the symbol survives simplification, the number is recoverable only after choosing r_0; this is the expected class-2 failure.
4. CAN PASS: derivation holding nothing constant, or each held-constant quantity carrying a deriving passage reproduced in the C2 artefact (e.g. r_0^2 = 3/Lambda with Lambda derived in a pinned passage). CAN FAIL: core density held at the Planck density "by assumption" with no manifest passage — a row without a source line fails.
5. CAN PASS: floor 1.0e15 kg, or 3.0e30 kg — no overlap with any comparator interval. CAN FAIL, one per comparator row, Planck included: floor m_P = 2.1764343e-8 kg overlaps [2.176433e-8, 2.176435e-8]; floor 2.0e11 kg overlaps [1.729e11, 5.190e11]; floor 5.0e30 kg overlaps [4.375e30, 5.768e30].

Hand arithmetic (execution blocked; intermediates printed):
ħc = 1.054571817e-34 × 2.99792458e8 = 3.1615268e-26; ħc/G = 4.736866e-16; m_P = sqrt = 2.1764343e-8 kg — inside the widened degenerate row [2.176433e-8, 2.176435e-8]. OK.
Gregorian year = 365.2425 × 86400 = 31,556,952 s; t_0 = 13.797e9 × 31,556,952 = 4.353913e17 s. OK.
c^2 = 8.98755179e16, c^4 = 8.07760872e33, ħc^4 = 0.851842; numerator t_0·ħc^4 = 3.70885e17; 5120π = 16084.9544, G^2 = 4.45462805e-21, denominator = 7.16525e-17; M^3 = 5.17616e33; M = 1.7298265e11 kg — rounds DOWN to 1.729e11 (outward, contains the value); upper 3.0 × 1.7298265e11 = 5.1894795e11 — rounds UP to 5.190e11 (outward). OK.
TOV: 2.2 × 1.98892e30 = 4.375624e30 down to 4.375e30; 2.9 × 1.98892e30 = 5.767868e30 up to 5.768e30; both endpoints contained. OK.
Exhibition set {1.0e15 PASS, 3.0e30 PASS, 2.0e11 FAIL-Hawking, m_P FAIL-Planck, 5.0e30 FAIL-TOV}: all behave as printed. The V16 degenerate-row escape is closed at the stated precision.

R2 (repair required). Verbatim: "**C6 is RUN on this class** and its result reported: a surviving freedom is what condition 3 rejects, so it is expected to fail — but the failure must be **exhibited**, not assumed."
Defect: class 2 states no consequence for C6_BREAKER_TEST=PASS, and the state is reachable in principle from §2's own object list. Construction: the printed relations leave the matching surface unbound ("which surface" is §2's own object to bind). The completion-free reading's allowed mass set has infimum F1 = 1.0e15 kg — a positive floor, so the reading is in P. The admissible completion "the matching surface is the inner horizon" introduces exactly one named assumption, is consistent with every printed relation, operates on printed relations, adds NO constant and holds NO quantity constant; it yields F2 = 1.2e15 kg. P holds two floors, so the case files class 2. Evaluated per floor: both floors resolve to §2b constants (condition 2 passes), no free symbols survive (condition 3 passes), no quantity is held constant (condition 4's decision rule passes), neither overlaps a comparator (condition 5 passes), both are masses (condition 1 passes) — so C6_BREAKER_TEST=PASS on a class whose only text expects failure, while the document's sole amendment clause is confined to class 1. The breaker test can fire and oblige nothing; the conclusion about the lane's own pattern is left to silence in the pattern-preserving direction — the live danger this gate is told to assume. The V10 principle was that verdicts about the pattern be earned on the record, and here no verdict is even routed.
Exact replacement: "**C6 is RUN on this class** and its result reported: a surviving freedom is what condition 3 rejects, so it is expected to fail — but the failure must be **exhibited**, not assumed. **If `C6_BREAKER_TEST=PASS` results here — every per-floor evaluation satisfying all five decision rules while the readings disagree — the per-floor artefacts are printed, and any passing floor yielded by the completion-free reading is reported as a candidate counterexample: the pattern-record question is then decided on the exhibited artefacts, not by this document's silence. A passing floor that exists only under a completion does not by itself amend the record — the completion, not the construction, supplied the content — but that conclusion is stated from the printed artefacts, not assumed.**"

FINDING 5 — RE-RUN GUARD: SOUND.

§6 forbids assuming K6's outcome ("K6's outcome may not be assumed to repeat: this is a different branch and the study must be able to return DYM_FLOOR_DERIVED"), and no clause anywhere presumes an earlier study's result. The positive class is genuinely reachable: printed relations binding the core scale to the mass with everything resolving to §2b constants, completion-free reading in P with floor 1.0e15 kg, no admissible completion altering it, Z and I empty → class 1; the C6 PASS path on that floor is exhibited above (no comparator overlap, all five conditions satisfiable). The breaker is not pre-disabled in either direction.

FINDING 6 — FAIRNESS: SOUND.

Every operative negative clause holds "unreproduced from the stated inputs" and none asserts error or nonexistence about the branch: §3 limb A ("a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs"), §4 class 3's row (same wording — the V19 F6 repair is in place), §4 class 4's row ("a positive floor was unreproduced from the stated inputs" — the V18 repair is in place), and class 4's inconsistency gloss ("report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"). The preamble phrase "no positive lower bound follows from it" is a derivability claim about a reading, not a nonexistence claim about the branch, and class 3's own note states the distinction. I found no operative clause to repair.

FINDING 7 — STALL: no unfileable terminal state found; one terminal-routing inconsistency to repair (R3).

Every path I traced files exactly one declared class: scientific paths file 1–4; unreadable source, digest mismatch, UNRESOLVED proposition, or undecidable-after-fallback file DYM_SOURCE_BLOCKED; persistent control failure files R3D_NO_CLASS; seat splits convene the third seat and terminate in agreement-or-BLOCKED ("Every terminal path files exactly one declared class"); condition-3 UNDECIDED records C6=FAIL and files BLOCKED; early blocks leave later controls NOT_RUN under the explicit §9 rule.

R3 (repair required). Verbatim: "if the two seats return the same scientific class but exactly one report is control-clean, that class is filed **unless** the third seat re-runs the failed control and also fails it, in which case `R3D_NO_CLASS` is filed."
Defect: the rule covers exactly-one-clean only. Reachable zero-clean case: seat 1 returns class 1 with C4=FAIL; seat 2 returns class 1 with the same floor, the same C6 outcome, and C5b=FAIL. No split convenes — §9 requires a differing class, C6 outcome, floor or family, and none differs — and no single control has failed after two attempts (C4 failed once, C5b failed once), so R3D_NO_CLASS's condition is not met and class 6's "otherwise file DYM_SOURCE_BLOCKED" fires. That files a class whose own definition — a source that cannot be read, a digest mismatch, an UNRESOLVED proposition, an undecidable fallback — is false on every disjunct: a control-failure state routed into a source-blocked class whose stated conditions do not hold, and the study "waits" on sources that are fine. The run does not stall, but the terminal routing contradicts the class definition.
Exact replacement: "if the two seats return the same scientific class but **at least one** report is not control-clean, the third seat re-runs each failed control once; **if any re-run control fails again, that is the persistent failure named above and `R3D_NO_CLASS` is filed; if every re-run control passes, that class is filed from a control-clean report.**"

DISPOSITION: three repairs, all verbatim-targeted — R1 (stale class cross-reference in §4's definition line), R2 (no routed consequence for C6=PASS on DYM_FLOOR_UNDERDETERMINED), R3 (zero-control-clean agreement falls through to a DYM_SOURCE_BLOCKED whose definition is false). None disables the falsifier, breaks the partition, opens a circularity channel, or produces an unfileable terminal state. Outcome classes, controls, circularity, re-run guard and fairness are sound as written. PREREG_SOUND_WITH_REPAIRS; V20 does not inherit any prior verdict, and C0 must be re-run on the repaired bytes before freeze.

R3D_V20_KIMI_COMPLETE
