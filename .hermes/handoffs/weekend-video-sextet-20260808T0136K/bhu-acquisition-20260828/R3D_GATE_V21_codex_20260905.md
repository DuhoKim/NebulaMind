ACCESS_SHA=82e89d5cfb53d90c421070994e87fc3a6ada555b15fd898d4458d16f0dd61028
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — SOUND.

The six declared terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Class 3 is confined to the limb-A exit; classes 1, 2, and 4 are confined to limb B and partition it through the mutually exclusive `P`/`Z`/`I` cases; classes 5 and 6 cover source/procedure blockage and persistent non-source control failure in the stated order. A real inconclusive result is reachable: the completion-free reading yields a 10 kg floor while one admissible completion yields a 20 kg floor (or permits masses approaching zero); then `P` contains disagreeing floors (or `Z` is non-empty) and class 2, `DYM_FLOOR_UNDERDETERMINED`, files. I find no result fitting two operative classes or none. No replacement.

2. CONTROLS — SOUND.

No control can pass by bare assertion: C0 requires an exhibition table; C1 computed digests (and `repr()` where required); C2 the full census and ledger; C3 executed captured output; C4 printed algebra and premises; C5 printed command output; C5b a complete per-path scope table; and C6 its condition artefacts and completed comparator table. Each control has one exact `PASS|FAIL|NOT_RUN` code. Early terminal blocks explicitly give later, never-engaged controls `NOT_RUN`, while engaged controls retain actual results. The limb-A exit explicitly assigns C3, C4, and C6 `NOT_RUN` and requires actual results for C0, C1, C2, C5, and C5b. The literal commands are syntactically executable as written: `python3 r3d_c3_deletion_probe.py relations.json`, `python3 --version`, `python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 $(command -v python3)`. No replacement.

3. CIRCULARITY — SOUND.

The lane-authored pattern is expressly barred from inclusion, exclusion, and outcome selection and enters only at C6 after a positive-floor class has been selected. The census accounts for every non-blank source line and every displayed or numbered equation, requires verbatim locators, restricts exclusion to predeclared reason codes demonstrated from source text, and routes disagreement or missing evidence to `UNRESOLVED`/`DYM_SOURCE_BLOCKED`. A contrary relation therefore cannot be quietly omitted while C2 passes. C3 additionally tests whether an injected relation alone preserves a determinate floor after source equations are deleted. No replacement.

4. THE FALSIFIER — SOUND.

NUMBER: C6 can return `FAIL` in 4 of the 6 declared terminal outcome classes: `DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED`, an engaged run later filing `DYM_SOURCE_BLOCKED`, and an engaged run later filing `R3D_NO_CLASS`.

- Condition 1: PASS — file `M_min = 10 kg`. FAIL — file the dimensionless ratio `M/M_sun = 2` as the floor. The latter is a malformed filing, not a valid scientific-class path. This condition cannot fail on any valid path because positive-floor membership already requires a mass; it is correctly marked `ENTAILED`.
- Condition 2: PASS — `M_min = sqrt(hbar*c/G)`, with every constant in §2b. FAIL — `M_min = a*sqrt(hbar*c/G)` where `a` terminates in an unsupported “we choose a=2” premise.
- Condition 3: PASS — `M_min = sqrt(hbar*c/G)`, leaving no non-§2b symbol. FAIL — `M_min = alpha*sqrt(hbar*c/G)` with free `alpha` surviving.
- Condition 4: PASS — a derivation with no quantity held fixed by choice (for example `M_min = sqrt(hbar*c/G)`). FAIL — obtain the result only after holding `r_0` constant without a manifest passage deriving that constancy.
- Condition 5: PASS — the point floor `1.0e15 kg`, which overlaps none of the three numerical comparator intervals. FAIL — `2.1764343e-8 kg`, which overlaps the Planck-remnant interval (alternatively `2.0e11 kg` overlaps the Hawking interval, and `5.0e30 kg` overlaps the stellar interval).

Thus four conditions can substantively pass and fail; condition 1 passes on every valid path and fails only as a filing-integrity check. No replacement.

5. RE-RUN GUARD — SOUND.

The design expressly forbids assuming K6 repeats, treats this as a different branch, and makes `DYM_FLOOR_DERIVED` genuinely reachable. A concrete route is a consistent completion-free source reading whose non-empty allowed mass set has the unique infimum 10 kg, with all admissible readings agreeing and `Z=I=empty`. No replacement.

6. FAIRNESS — DEFECT.

The operative prose consistently uses “unreproduced from the stated inputs,” including the limb-A exit and classes 3 and 4. However, two operative outcome tokens themselves assert absence and will travel into reports without their qualifying definitions.

Quote: “3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Defect: the code `DYM_NO_SIZE_MASS_RELATION` says no relation exists, which is stronger than the required reproduction-limited finding.

Exact replacement: “3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Quote: “4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above.”

Defect: the code `DYM_NO_POSITIVE_FLOOR` says no positive floor exists, which is stronger than the required reproduction-limited finding.

Exact replacement: “4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above.”

Apply those token renames consistently to every operative reference in §§3–5 and §9. Historical quotations in §8 may remain historical if clearly identified as such. With these replacements, the fairness rule is held everywhere.

7. STALL — SOUND.

Every bounded symbolic operation has a second bounded fallback, and an absent, timed-out, unparsable, or nondeciding fallback files `DYM_SOURCE_BLOCKED`. Source/read/proposition failures also file that class; persistent non-source control failures file `R3D_NO_CLASS`; all seat splits have a third-seat rule and an all-three-differ/cannot-decide fallback. The run cannot reach a terminal state with no fileable class. No replacement.

R3D_V21_GATE_COMPLETE
