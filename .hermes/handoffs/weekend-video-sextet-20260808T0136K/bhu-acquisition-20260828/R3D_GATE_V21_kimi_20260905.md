Script execution is unavailable in my mode: execute_code, heredoc, and python3 -c are all blocked without an approver present. Per the brief, I did all arithmetic by hand; intermediates are printed under finding 4. I did not execute the harness commands or the C3 probe, and I read no file other than the inlined target.

ACCESS_SHA=82e89d5cfb53d90c421070994e87fc3a6ada555b15fd898d4458d16f0dd61028
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — SOUND AS A PARTITION, WITH ONE COVERAGE DEFECT IN THE TERMINAL MACHINERY (defect filed under finding 7, where it bites).

Limb B is partitioned totally and exclusively over admissible readings. Every reading is in exactly one of P, Z, I: a non-empty allowed mass set either has a positive lower bound (positive glb, attained or not) or it does not ("permits masses approaching zero, meaning no positive lower bound follows") — those are negations, so the dichotomy is total for non-empty sets; the empty set is I. Then: P empty forces class 4 (Z union I is automatically non-empty, since the completion-free reading always exists); P non-empty with Z and I both empty and all of P agreeing forces class 1; everything else in limb B is class 2. No overlap, no residue. Class 3 is filed only from limb A, classes 1/2/4 only from limb B. The entailment the document confesses — nothing printed implies the completion-free reading is in Z, so class 3's state satisfies class 4's predicate — is real, but the classes are now filed by limb, a procedural fact about the run, so no single run files both. I could not construct a result fitting two classes. DYM_SOURCE_BLOCKED and R3D_NO_CLASS are ordered ("only after ruling out DYM_SOURCE_BLOCKED"), so they are exclusive.

INCONCLUSIVE is genuinely reachable: completion-free reading yields no floor, one admissible completion operating on a printed relation yields 10 kg, none other yields anything — Z non-empty, P non-empty, class 2 files. Also reachable: class 1 (manifest prints r0^2 = 3/Lambda plus a mass function fixing one infimum, all readings agree, Z and I empty) and class 4 (printed relations mutually inconsistent; no completion can then be consistent with all of them, so only I is non-empty).

2. CONTROLS — SOUND.

Each of the eight controls emits a named three-valued token (C0_REACHABILITY, C1_SOURCE_IDENTITY, C2_COMPLETION_LEDGER, C3_DELETION_PROBE, C4_GR_BENCHMARK, C5_HARNESS_PINNED, C5B_PATH_LIST, C6_BREAKER_TEST, each PASS|FAIL|NOT_RUN), and each binds its pass to a printed artefact a summary assertion cannot substitute for: computed digests (C1), the full ledger with verbatim source text per SOURCE_DERIVED row (C2), captured output of the executed deleted state (C3), printed limit algebra plus its premise list (C4), printed command output (C5), the per-path IN_SCOPE/OUT_OF_SCOPE table with the clause applied (C5b), the completed four-row comparison table (C6), the exhibition table (C0). Assertion-passing is additionally blocked by the anti-transcription rule extended to C1 and by "Tori re-runs everything" (section 7). Unreached controls are handled exactly: the limb-A exit names C3, C4, C6 as NOT_RUN while C0, C1, C2, C5, C5b carry actual results, and section 9 states the general early-block rule ("every such later control is recorded NOT_RUN, and every control that WAS engaged before the block carries its actual result").

Literal commands the document tells a seat to execute: python3 --version; python3 -c "import sympy; print(sympy.__version__)"; shasum -a 256 $(command -v python3); python3 r3d_c3_deletion_probe.py relations.json. All four are syntactically executable as written — no placeholder metacharacters remain (the V20 repair removed the angle brackets). One caveat recorded, not counted as a defect: the probe script's existence and its positive/negative control behaviour are asserted in the document's prose, not demonstrated inside it; but C3's captured-output requirement means a missing or broken script fails loudly, it cannot pass by assertion. I could not execute anything (mode restriction, stated above), so executability beyond syntax is unverified by me.

3. CIRCULARITY — SOUND.

The pattern is barred as evidence for inclusion, exclusion, or outcome selection (section 2c) and enters only at C6, after a positive-floor class is reached on other grounds; section 0's expectancy was removed and the removal is disclosed. The enumeration key cannot be steered by the lane's expectations: every non-blank line carries a disposition, the per-source reconciliation line turns an omitted contrary relation into an arithmetic mismatch, and every numbered or displayed equation is its own row — a bare symbolic relation (r0^2 = 3/Lambda) cannot slip through a keyword net because there is no keyword net. Exclusion requires a predeclared code demonstrated from printed source text, UNRESOLVED may not be treated as absence and forces DYM_SOURCE_BLOCKED, and both the included and excluded rows print verbatim, so a mislabelled contrary relation is inspectable by the second blind seat and by Tori. A contrary relation therefore cannot be quietly omitted — only loudly misclassified, with its text attached. The admissibility rule (a completion must operate on at least one printed relation; one that is itself the sole source of a bound is inadmissible) blocks manufacturing the floor the pattern predicts absent, and C3's deletion probe tests whether an injected relation alone determines the floor. C6's conditions are pinned to a verbatim, hash-pinned copy of the pattern record, so evaluation cannot drift toward the lane's interest between seats. The residual — the four comparator conventions (5120pi, 3.0, [2.2, 2.9] M_sun, the Gregorian year) are lane-asserted — is disclosed, frozen, printed, and routed by name outside condition 2's provenance test; outward rounding errs against certifying a counterexample on a technicality, which is the safe direction for the high-stakes error.

4. THE FALSIFIER — C6 can return FAIL on 4 of the 6 declared outcome classes.

The number is 4: DYM_FLOOR_DERIVED and DYM_FLOOR_UNDERDETERMINED (engagement classes), plus DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after fallback — explicit: record FAIL, then file BLOCKED), plus R3D_NO_CLASS (C6 engaged with a substantive FAIL on record, then a different control fails persistently through the zero-clean re-run rule; section 9's "every control that WAS engaged before the block carries its actual result" puts the FAIL on that terminal record). On DYM_NO_SIZE_MASS_RELATION and DYM_NO_POSITIVE_FLOOR it is NOT_RUN, correctly, since those classes produce no number to test. The document's own section 5a count agrees.

Per condition (PASS input / FAIL input, concrete):
- Condition 1 (dimension is mass). CAN PASS: floor 1.0e15 kg — dimension mass, passes. CAN FAIL only via a malformed filing: a seat files the pure number 0.42 (or a length, r0 = 3e-12 m) as its floor; the rule evaluates the quantity actually FILED, so the dimensional check fails. On any valid path it cannot fail — every positive-floor class requires a mass — and the document discloses this as ENTAILED rather than claiming five substantive failures. This is the condition the brief asks me to name: condition 1 cannot fail on any valid path; no condition is unfailable on absolutely every path, since the malformed-filing path exists.
- Condition 2 (constants trace). PASS: floor m_P = sqrt(hbar c / G) — every constant terminates in section 2b. FAIL: floor 3 m_P where the coefficient 3 entered as "the simplest form" with no manifest derivation — a terminus outside the manifest and section 2b fails by the stated rule.
- Condition 3 (no free normalisation). PASS: floor sqrt(hbar c / G) has no non-2b parameter; after symbol substitution the simplified expression contains no free symbol, the printed number is recovered with nothing chosen. FAIL: floor printed as 1.0e15 kg but recoverable only by choosing r0 = 3.7e-12 m; replacing r0 with an algebraically independent symbol s leaves s in the simplified expression — a free normalisation survives. UNDECIDED (primary timeout, fallback absent/times out/unparseable) maps to FAIL plus DYM_SOURCE_BLOCKED, so "does not pass" is never a fourth state.
- Condition 4 (no fixity assumed). PASS: every quantity held constant (say Lambda) has its deriving passage in the frozen C2 artefact — every fixity-table row has a source line. FAIL: the derivation holds the core density rho0 = 1e-3 kg/m^3 constant with no manifest passage deriving that constancy — a row without a source line fails.
- Condition 5 (no comparator overlap). PASS: floor 1.0e15 kg (also 3.0e30 kg) overlaps no row. FAIL: 2.0e11 kg overlaps the Hawking row [1.729e11, 5.190e11]; m_P overlaps the Planck row; 5.0e30 kg overlaps the TOV row — one failing input per comparator row exists, as C0 requires.

Hand arithmetic (execution blocked in my mode; intermediates as required):
- Year: 365.2425 x 86400 = 31,556,952 s; t0 = 13.797e9 x 3.1556952e7 = 4.35391e17 s (Julian 365.25 x 86400 = 31,557,600 s gives 4.35398e17 ~ 4.3540e17). Matches section 2b.
- hbar c = 1.054571817e-34 x 2.99792458e8 = 3.1615268e-26; hbar c / G = 3.1615268e-26 / 6.67430e-11 = 4.73687e-16; m_P = sqrt(4.73687e-16) = 2.17643e-8 kg (2.17643^2 = 4.736847). Containment: 2.176433e-8 <= 2.1764343e-8 <= 2.176435e-8 — the widened degenerate row holds its own value.
- Hawking: c^2 = 8.9875518e16, c^4 = 8.0776087e33; hbar c^4 = 0.8518419; numerator t0 x hbar c^4 = 3.70885e17; G^2 = 4.4546280e-21; 5120 pi = 16084.954; denominator = 7.16525e-17; ratio = 5.1762e33; cube root = 1.72983e11 kg (1.7298^3 = 5.17592; target 5.17616). Upper end 3.0 x 1.7298245e11 = 5.18947e11. Containment: 1.729e11 <= 1.72982e11 and 5.18947e11 <= 5.190e11 — outward rounding holds.
- TOV: 2.2 x 1.98892e30 = 4.375624e30; 2.9 x 1.98892e30 = 5.767868e30. Containment: 4.375e30 <= 4.375624e30 and 5.767868e30 <= 5.768e30 — holds.
All three printed intervals contain their computed values; every comparator number is computed inside the document from section 2b; nothing is deferred and nothing needs to be read.

5. RE-RUN GUARD — SOUND.

Section 6 states the rule ("K6's outcome may not be assumed to repeat: this is a different branch and the study must be able to return DYM_FLOOR_DERIVED"), and the design enforces it rather than asserting it: C0 will not let the document freeze without a seat-exhibited input that reaches class 1, and nothing in the limb structure pre-filters toward underdetermination — limb B derives whatever the census supports, and the positive class files whenever P is non-empty, all of P agrees, and Z and I are both empty. A concrete reaching input: a manifest printing r0^2 = 3/Lambda and a mass function whose unique infimum is one mass, with no admissible completion disagreeing. The positive class is genuinely reachable.

6. FAIRNESS — ONE DEFECT, ALREADY ON THE DOCUMENT'S OWN RECORD AS ESCALATED; CONFIRMED ON THE MERITS.

Every operative clause holds the required wording: limb A ("a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs"), the class-3 table row and class-3 entry (same wording), the class-4 row ("a positive floor was unreproduced from the stated inputs"), the inconsistency clause ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and section 6's rule itself. No operative clause asserts nonexistence.

The class TOKENS do. Verbatim: "3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed." and "4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above." Defect: the names assert absence ("NO_SIZE_MASS_RELATION", "NO_POSITIVE_FLOOR") while their own definitions record a reproduction failure; a token travels into reports and summaries stripped of the definition that qualifies it, so the study's outcome vocabulary violates its own fairness rule. The document's section 8m concedes exactly this ("the tokens ... **assert absence** while their own definitions say *'unreproduced from the stated inputs'*"), classifies it substantive, and declines repair on authority grounds, not on merit. Exact replacement: rename DYM_NO_SIZE_MASS_RELATION to DYM_SIZE_MASS_RELATION_UNREPRODUCED and DYM_NO_POSITIVE_FLOOR to DYM_POSITIVE_FLOOR_UNREPRODUCED in every operative location (the class table, the numbered class entries, section 3's limb-A exit, C6's NOT_RUN lists, section 5a, and the control/token vocabulary); section 8's history stays as history.

7. STALL — ONE DEFECT: A REACHABLE TERMINAL STATE WITH NO CORRECTLY FILEABLE CLASS, IN THE BRANCH ADJACENT TO THE ONE V21 REPAIRED.

Verbatim: "if the two seats return the same scientific class but **at least one** report is not control-clean, **the third seat re-runs each failed control once. If any re-run control fails again, that is the persistent failure named above and `R3D_NO_CLASS` is filed. If every re-run control passes, that class is filed.**" And section 9: "the third seat adjudicates exactly that split from the printed artefacts, **re-executing any blocked read once before ruling**; its class is filed **only if it agrees with one of the two**."

Defect: the re-run machinery convenes only when the two blind seats return the SAME scientific class. Construct the reachable state: seat A files DYM_FLOOR_DERIVED with C4_GR_BENCHMARK=FAIL (a non-blocking control failure — claimed pass without the premise list); seat B files DYM_FLOOR_UNDERDETERMINED, control-clean. The split rule convenes the third seat (different terminal classes), which adjudicates from the printed artefacts and agrees with A's class. Now: "A scientific class may be filed only from a seat report in which every reached control passed" bars filing class 1 from A's report; no rule authorizes the third seat to re-run A's failed control (the split rule authorizes re-executing blocked READS only); class 6's R3D_NO_CLASS trigger requires a control failing "after two attempts" and only one attempt exists; the fall-through, "otherwise file DYM_SOURCE_BLOCKED", names a class whose definition — an unreadable source, a digest mismatch, an UNRESOLVED proposition, an undecidable-after-fallback procedure — is false of this state. This is the identical defect shape the document repaired for the same-class branch ("fell through to DYM_SOURCE_BLOCKED, whose definition would have been false of it"), left standing in the different-class branch. The state is reachable by obedient seats and terminates with either a misdescribed filing or none.

Exact replacement, extending the existing rule rather than adding a new mechanism — replace the sentence "if the two seats return the same scientific class but **at least one** report is not control-clean, **the third seat re-runs each failed control once." with: "if the two seats return the same scientific class but at least one report is not control-clean — or, after a different-class split, the third seat's adjudication agrees with a seat whose report is not control-clean — the third seat re-runs each failed control of that report once. If any re-run control fails again, that is the persistent failure named above and R3D_NO_CLASS is filed. If every re-run control passes, that class is filed."

Everything else about termination is covered: census failure modes route to DYM_SOURCE_BLOCKED; symbolic timeouts route through named fallbacks to BLOCKED; same-class splits on C6 outcome, floor, or family convene the third seat; all-three-differ or undecidable adjudications file BLOCKED; "Every terminal path files exactly one declared class" holds once the gap above is closed.

Disposition: two substantive defects, each with an exact replacement stated above — the fairness token rename (finding 6, document-acknowledged and escalated) and the adjudicated-to-non-clean-report routing gap (finding 7, fresh). With both applied, every section I was asked to judge stands: the classes partition, no control passes by assertion, the pattern cannot reach the evidence, the falsifier can fail on 4 of 6 classes with condition 1 honestly marked ENTAILED, the positive class is genuinely reachable, and every remaining terminal path files a class whose definition is true of it.

R3D_V21_KIMI_COMPLETE
