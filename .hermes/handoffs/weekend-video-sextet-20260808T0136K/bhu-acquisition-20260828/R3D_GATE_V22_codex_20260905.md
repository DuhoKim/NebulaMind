ACCESS_SHA=b76df473d8e0345dbf805ab7d4825fab9741dbe66e5a93106572a6830ee8eaca
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

SOUND. The six terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Limb A alone files class 3; limb B is partitioned by `P`, `Z`, and `I` into classes 1, 2, and 4; source/procedure blocking files class 5; persistent non-source control failure after the prescribed retry files class 6. A result cannot be filed in two terminal classes because the blocking and control-clean rules prevent filing the underlying scientific class. A fileable class is always available.

An inconclusive scientific result is genuinely reachable. Concrete example: the completion-free reading permits masses approaching zero (`Z` non-empty), while an admissible one-assumption completion yields a 10 kg positive floor (`P` non-empty). That files class 2, `DYM_FLOOR_UNDERDETERMINED`. A mutually inconsistent printed system also reaches class 4 through `I` when no reading is in `P`.

2. CONTROLS

DEFECT. Every control has an exact `PASS|FAIL|NOT_RUN` code, asserted passes are rejected in favor of printed artefacts, and early termination explicitly records never-engaged later controls as `NOT_RUN`. C0, C1, C2, C3, C4, C5, C5b, and C6 all have named outputs. The three C5 shell commands are executable as written. However, C3's mandated command uses a relative script path without fixing the seat's working directory; the document permits a seat's own working directory, so the literal command can fail despite the committed script being present.

Verbatim sentence: “The probe is the committed script `r3d_c3_deletion_probe.py`, invoked as `python3 r3d_c3_deletion_probe.py relations.json` — the JSON file `relations.json` carries `target`, `symbols`, the §2b `constants` list, and one record per relation with `id`, `origin` (`SOURCE_PINNED` or `INJECTED`) and `expr`.”

Exact replacement: “The probe is the committed script `/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py`; the seat writes `relations.json` in its current working directory and invokes `python3 /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py relations.json` — the JSON file carries `target`, `symbols`, the §2b `constants` list, and one record per relation with `id`, `origin` (`SOURCE_PINNED` or `INJECTED`) and `expr`.”

3. CIRCULARITY

SOUND. The lane-authored pattern is expressly barred from inclusion, exclusion, and outcome selection and enters only after a positive-floor class is independently reached. The census accounts for every nonblank line, separately enumerates every numbered or displayed equation, requires verbatim locators for included and excluded material, restricts exclusions to declared reason codes, and routes disagreement to `UNRESOLVED`/`DYM_SOURCE_BLOCKED`. Thus a contrary relation cannot be quietly omitted or excluded without a visible census/disposition defect. C3 additionally tests whether an injected relation alone determines the floor after source-pinned equations are deleted.

4. THE FALSIFIER

C6 can return `FAIL` in 4 of the 6 declared terminal classes: classes 1 and 2 directly, class 5 if condition 3 remains undecided after its fallback, and class 6 if an engaged C6 is followed by a persistent control failure.

- Condition 1 can PASS: file a derived floor of 10 kg. It can FAIL only as a filing-integrity violation: file the dimensionless ratio 2 as the “floor.” It cannot fail on any valid positive-floor-class path because membership entails a mass-dimensional floor; it is the sole condition with no substantive failing path.
- Condition 2 can PASS: `M_min = sqrt(ħc/G)`, with every constant terminating in §2b. It can FAIL: `M_min = α sqrt(ħc/G)` where `α` terminates in an unsupported choice rather than a manifest equation or §2b.
- Condition 3 can PASS: `M_min = sqrt(ħc/G)`, whose simplified expression has no non-§2b free symbol. It can FAIL: `M_min = α sqrt(ħc/G)` with free symbol `α` surviving.
- Condition 4 can PASS: a derivation holding no quantity constant by choice, or one whose every held-constant quantity has a manifest deriving passage. It can FAIL: hold core density `ρ0` fixed without a manifest passage deriving that fixity.
- Condition 5 can PASS: a point floor of `1.0e15 kg`, which overlaps none of the three comparator intervals. It can FAIL: `2.176434e-8 kg`, which overlaps the Planck-remnant interval; likewise `2.0e11 kg` overlaps the Hawking interval and `5.0e30 kg` overlaps the stellar-collapse interval.

No condition is unable to fail on every conceivable execution path because condition 1 rejects a malformed filing. Condition 1, however, cannot fail on any valid scientific path and therefore cannot fail substantively.

5. RE-RUN GUARD

SOUND. “K6's outcome may not be assumed to repeat” is explicit, the branch is independently censused, and class 1 `DYM_FLOOR_DERIVED` is genuinely reachable whenever the completion-free reading supplies the sole agreed positive floor with `Z` and `I` empty.

6. FAIRNESS

DEFECT. Operative explanatory clauses use “unreproduced from the stated inputs,” but the two negative outcome identifiers themselves assert absence. Those identifiers are mandatory report tokens and can travel without their qualifying definitions, so fairness is not held in every operative clause.

Verbatim sentence: “3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Exact replacement: “3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Verbatim sentence: “4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family.”

Exact replacement: “4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above. Report the family.”

Apply those two identifier replacements exactly and globally to every operative reference, including the limb-A instruction, the §4 partition table, C6 applicability/`NOT_RUN` clauses, C6 outcome definitions, and §5a. Historical quotations may remain unchanged when unmistakably marked as history.

7. STALL

SOUND. Every symbolic operation has a 120-second primary cap and a second capped, proposition-specific fallback. Missing, timed-out, unparsable, or nondecisive fallbacks route to `DYM_SOURCE_BLOCKED`; persistent non-source control failures route to `R3D_NO_CLASS`; seat disagreement has a third-seat rule and a terminal fallback. The run cannot reach a terminal state with no fileable class.

R3D_V22_GATE_COMPLETE
