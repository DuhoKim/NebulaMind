Script execution is blocked in my mode (both heredoc and `python3 -c` are flagged dangerous under single-query mode with no approver present). Per the brief, all arithmetic below is done by hand with intermediates printed, and I claim no execution. The §9 harness commands are assessed as-written only.

ACCESS_SHA=b76df473d8e0345dbf805ab7d4825fab9741dbe66e5a93106572a6830ee8eaca
GATE=PREREG_SOUND_WITH_REPAIRS

EXECUTION NOTE. No command was executed by this seat. All numeric checks in finding 4 are hand arithmetic with intermediates shown. The three §9 harness commands and the C3 probe invocation are judged for executability as written (syntax), not run. The existence of the "committed script r3d_c3_deletion_probe.py" is asserted by the document and is not verifiable from the document's own text; I was barred from reading the directory, so that existence is taken as an assumption, not confirmed.

FINDING 1 — OUTCOME CLASSES: SOUND.

Six declared: 1 DYM_FLOOR_DERIVED, 2 DYM_FLOOR_UNDERDETERMINED, 3 DYM_NO_SIZE_MASS_RELATION, 4 DYM_NO_POSITIVE_FLOOR (scientific); 5 DYM_SOURCE_BLOCKED, 6 R3D_NO_CLASS (non-scientific). Class 3 is limb-A-only; 1/2/4 are limb-B-only, so 3 is disjoint from the rest by limb. Within limb B the P/Z/I partition over admissible readings is total and exclusive: the completion-free derivation always exists and is an admissible reading, every reading lies in exactly one of P, Z, I, so P∪Z∪I is never empty. P empty ⇒ Z or I non-empty ⇒ class 4; P non-empty with full agreement and Z, I empty ⇒ class 1; P non-empty otherwise ⇒ class 2. No precedence rule is needed and none is stated.

Constructions attempted:
(a) A result fitting two classes. The only live candidate is the entailment the document itself names: a no-printed-relation world satisfies class 3's exit AND class 4's predicate (completion-free reading in Z, P empty). The limb separation assigns it to class 3 alone. I checked the residual escape: could P be non-empty in that world via a completion, making it genuinely class-2 and misfiled as class-3? §2 blocks it: "a completion that is itself the sole source of a mass bound is NOT admissible, because it supplies the content rather than completing it." Where nothing printed constrains the mass, any floor-yielding completion is the sole source of the bound and inadmissible, so P stays empty and the limb exit hides no class-2 state. I could not construct a two-class result.
(b) A result fitting no class. P empty with Z and I also empty is impossible (completion-free reading always lands in exactly one set). I could not construct a no-class result.
(c) INCONCLUSIVE reachability. Genuinely reachable, as class 2: completion-free reading permits zero, one admissible completion operating on a printed mass function yields floor 10 kg; P non-empty, Z non-empty, readings disagree → DYM_FLOOR_UNDERDETERMINED. Class 4 is also reachable (printed relations permit zero, no admissible completion rescues a floor).

No replacement.

FINDING 2 — CONTROLS: SOUND, with the execution-limit note above.

Every control names an exact code with PASS|FAIL|NOT_RUN (§9's vocabulary lists all eight: C0_REACHABILITY, C1_SOURCE_IDENTITY, C2_COMPLETION_LEDGER, C3_DELETION_PROBE, C4_GR_BENCHMARK, C5_HARNESS_PINNED, C5B_PATH_LIST, C6_BREAKER_TEST). No control is passable by bare assertion: each binds a printed artefact and states the anti-assertion clause — C1 "a claimed pass without them fails", C2 "a summary assertion fails", C3 "a claimed pass without that output fails", C4 "a claimed pass without them fails", C5b "a claimed pass without it fails", C6 per-condition artefacts with an incomplete table failing loudly, C0's exhibition table. Unreached controls are handled: §3's limb-A exit names C3/C4/C6 NOT_RUN with C0/C1/C2/C5/C5b reached and carrying actual results; §9 defines explicit unreach for early terminal blocks and distinguishes controls engaged before a block from those never engaged.

Literal commands, as written: `python3 --version` and `python3 -c "import sympy; print(sympy.__version__)"` and `shasum -a 256 $(command -v python3)` are each syntactically valid (the version print is inside a quoted -c string, so the old bare-word defect is closed). C3's `python3 r3d_c3_deletion_probe.py relations.json` contains no shell metacharacters; executable as written provided the committed script exists (assumption noted above). I executed none of these; no defect in the text.

No replacement.

FINDING 3 — CIRCULARITY: SOUND.

Channels checked: (i) Enumeration — every non-blank line of every manifest source gets a disposition with a per-source reconciliation count that must equal the non-blank line count, so an omitted line shows up as arithmetic; every numbered or displayed equation is additionally its own row; the eleven English terms are a printed convenience and may not justify any row. (ii) Exclusion — only under predeclared reason codes demonstrated by verbatim source text; UNRESOLVED may not be treated as absence and forces DYM_SOURCE_BLOCKED. (iii) Class selection — mechanical P/Z/I partition, blind double seat, split adjudication by a third seat from printed artefacts. (iv) The pattern enters only at C6, whose five conditions are copied verbatim and pinned to the record's sha256, with evaluation against any other text failing C6; C6 is engaged on every positive-floor outcome, and PASS amends the pattern record — the pattern is exposed to refutation, not protected. (v) The one place lane judgement touches the falsifier is the four asserted comparator conventions (5120π, 3.0, [2.2,2.9] M_☉, Gregorian year); these are disclosed as frozen scaffolding, printed in the condition-5 artefact, and C0 must exhibit a condition-5 FAIL per comparator row. Their width biases toward condition-5 FAIL, i.e. against the counterexample direction, which is conservative relative to the lane's interest and inspectable on the printed table.

A contrary printed relation cannot be quietly omitted: omission breaks the reconciliation count, and an equation-form relation must appear as its own row with verbatim text. The completion space is unbounded and seat-generated, but every considered completion must be printed with its named assumption, the completion-free reading is mandatory, and disagreement among a seat's own printed readings forces class 2 — concealment would require suppressing a printed artefact, which the second seat and the audit would see.

No replacement.

FINDING 4 — THE FALSIFIER (§5 C6): SOUND. Numbers and per-condition analysis as required.

THE NUMBER. C6 is initially engaged on 2 of the 6 declared classes (DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED). Once engaged, C6_BREAKER_TEST=FAIL can be recorded in 4 of the 6 declared classes: those two, plus DYM_SOURCE_BLOCKED (condition-3 UNDECIDED after fallback), plus R3D_NO_CLASS (persistent control failure after engagement). §5a's two-number statement is internally consistent with the C6 NOT_RUN clause (NOT_RUN = never engaged: classes 3, 4, or a 5/6 filed before any C6 evaluation).

Per condition, with concrete inputs:
- Condition 1 (dimension is mass). CAN PASS: floor = 10 kg. CANNOT FAIL on any valid path — this is the condition the brief asks me to name. §5a discloses it as ENTAILED: every positive-floor class requires a mass, so a correctly filed floor passes a dimensional test. It fails only on a malformed filing, e.g. a seat files the dimensionless ratio 0.61 as its floor; §5 catches it because the conditions are evaluated on the quantity actually filed.
- Condition 2 (constants trace). CAN PASS: floor m_P = sqrt(ħc/G), all constants terminating in §2b. CAN FAIL: floor = 7.3·m_P with 7.3 introduced as "the simplest form"; terminus outside the manifest/§2b fails.
- Condition 3 (no free normalisation). CAN PASS: floor sqrt(ħc/G) — no non-§2b parameter exists, the simplified expression contains none of the introduced symbols. CAN FAIL: floor (c²/2G)·r_0 with r_0 free — the number is recovered only after choosing r_0. UNDECIDED maps to C6 FAIL + DYM_SOURCE_BLOCKED, stated.
- Condition 4 (no fixity). CAN PASS: every held-constant quantity has a deriving passage in the C2 artefact. CAN FAIL: floor rigid only because r_0 = 1e-35 m is held constant by choice with no manifest row deriving the constancy.
- Condition 5 (falsifiability). CAN PASS: floor 1.0e15 kg — overlaps no comparator row. CAN FAIL per row: m_P (Planck row), 2.0e11 kg (Hawking row), 5.0e30 kg (TOV row). C0 requires a FAIL exhibition per row, Planck row included.

Hand arithmetic (no execution; intermediates printed):
- ħc = 1.054571817e-34 × 2.99792458e8 = 3.16152677e-26 (1.054571817×3 = 3.163715451, less 1.054571817×0.00207542 ≈ 0.00218868).
- m_P² = 3.16152677e-26 / 6.67430e-11 = 4.736867e-16; m_P = sqrt(4.736867)e-8 ≈ 2.1764343e-8 kg (2.1764² = 4.73671696; correction +2×2.1764×0.0000343 ≈ 0.000149 → 4.736866). Contained in [2.176433e-8, 2.176435e-8]: 2.176433 < 2.1764343 < 2.176435 ✓. The widened degenerate row contains its own computed value.
- Gregorian year = 365.2425 × 86400 = 31,556,952 s; t_0 = 13.797e9 × 31,556,952 = 4.35391267e17 ≈ 4.3539e17 ✓. Julian = 365.25 × 86400 = 31,557,600 s; t_0 = 4.3540e17 ✓.
- Hawking lower: c² = 8.9875518e16, c⁴ = 8.0776087e33, ħc⁴ ≈ 0.8518419; numerator t_0·ħc⁴ ≈ 4.35391267e17 × 0.8518419 ≈ 3.7088451e17. Denominator 5120π·G² = 16084.95439 × 4.454628049e-21 ≈ 7.1652489e-17. Ratio ≈ 5.17616e33. Cube root: 1.7298³ ≈ 5.17592, 1.73³ ≈ 5.17772; interpolating, ≈ 1.72983e11 kg ✓ printed 1.7298e11, rounded down to 1.729e11 ✓ (1.729 < 1.7298, interval contains the value).
- Upper = 3.0 × 1.7298245e11 ≈ 5.18947e11, rounded up to 5.190e11 ✓ contained.
- TOV: 2.2 × 1.98892e30 = 4.375624e30 → down 4.375e30 ✓; 2.9 × 1.98892e30 = 5.767868e30 → up 5.768e30 ✓.
- V9 exhibitions reproduce: 1.0e15 → no overlap → PASS; 2.0e11 ∈ [1.729e11, 5.190e11] → FAIL Hawking; m_P ∈ Planck row → FAIL; 3.0e30 < 4.375e30 → PASS.

Outward rounding confirmed at every row, including the degenerate Planck row. Condition 5's pass is a disjunction (overlap ⇒ FAIL, else PASS), with completeness enforced as a loud control failure rather than a silent unreachable pass. No replacement.

FINDING 5 — RE-RUN GUARD: SOUND.

§6: "K6's outcome may **not** be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`." Nothing in the design assumes K6_FLOOR_UNDERDETERMINED repeats. The positive class is genuinely reachable: a reading set where the printed mass function and de Sitter-limit condition jointly fix a unique positive infimum completion-free, and no admissible completion alters it — P non-empty, all agree, Z and I empty → class 1. C0 must exhibit it before any freeze. No replacement.

FINDING 6 — FAIRNESS: the wording is held in every operative clause I can find (§3 limb-A exit, §4 table rows 3 and 4, class bodies 3 and 4, §6 itself), but there are two defects, one carried and escalated, one new and minor.

Defect 6a (carried; both prior seats raised; escalated to Duho at §8m/§8n; I concur).
Quote, verbatim: "file `DYM_NO_SIZE_MASS_RELATION`" (§3) and the class tokens "`DYM_NO_SIZE_MASS_RELATION`" / "`DYM_NO_POSITIVE_FLOOR`" (§4).
Defect: the tokens assert absence while their own definitions say "unreproduced from the stated inputs". A class name travels into reports stripped of the qualifying definition, so the outcome vocabulary violates the study's own fairness rule. The document records this as a substantive, seat-found defect declined on authority grounds (renaming a class is Duho's ruling), not on merit. I concur on the merit.
Exact replacement (already drafted in §8m, pending Duho): rename `DYM_NO_SIZE_MASS_RELATION` → `DYM_SIZE_MASS_RELATION_UNREPRODUCED` and `DYM_NO_POSITIVE_FLOOR` → `DYM_POSITIVE_FLOOR_UNREPRODUCED` at every operative occurrence (§3, §4 table, §4 class bodies, §5a, §9 lists, C6 NOT_RUN clause); §8 history stays as written.

Defect 6b (new, minor; motivational paragraph, non-operative).
Quote, verbatim (§0): "the density ceiling cannot bound a mass without a size–mass relation `V(M)` the source never supplies."
Defect: "the source never supplies" is an absence claim about entry 51's source — the same violation form this lane repaired in class 4's row, class 3's row, and now escalates in the tokens. It sits in §0 (motivation, restating K6's finding), so it misroutes no procedure; hence minor.
Exact replacement: "the density ceiling cannot bound a mass without a size–mass relation `V(M)`, which K6 left unreproduced from the stated inputs."

FINDING 7 — STALL: no terminal state without a fileable class found; one minor truth-in-labelling defect.

Every terminal path files exactly one declared class: limb-A exit → 3; the P/Z/I partition → 1/2/4 with no residue; unreadable/mismatched/UNRESOLVED/undecidable evidence → 5; persistent control failure with no unread evidence → 6; condition-3 UNDECIDED → C6 FAIL + 5; seat splits are adjudicated with named fall-throughs. I could not construct a stalling run.

Defect 7a (minor).
Quotes, verbatim. §9: "If all three differ, or the third seat cannot decide, file `DYM_SOURCE_BLOCKED`." §4 class 5: "a required pinned source **cannot be read, or its computed identity does not match the frozen manifest of §2a, or a required source-dependent proposition is `UNRESOLVED`, or a required bounded procedure remains undecidable after its specified fallback.**"
Defect: a three-way split (or an undecidable adjudication) among control-clean reports is none of the four states class 5 enumerates, yet §9 files class 5 there. The filing is commanded, so the run does not stall — but the filed class's own definition is false of the state. This is the same defect shape the document repaired for the same-class branch at V21 and for the different-class non-clean branch at V22; the three-way branch is the remaining sibling.
Exact replacement (§4 class 5): append to the enumeration — "or a terminal seat split under §9 that no adjudication resolves to one declared class".

VERDICT JUSTIFICATION. Three open defects stand in the text: 6a (substantive per the document's own declared criterion, already escalated to Duho with the remedy drafted; it corrupts outcome vocabulary, not any procedure), 6b and 7a (minor wording completions of families the document itself repairs). The partition, controls, circularity guards, falsifier, re-run guard, and stall handling are sound; every comparator number hand-reproduces; the study is executable as designed. A clean PREREG_SOUND would falsely certify the document as defect-free; PREREG_UNSOUND would overstate, since nothing blocks a correct run and the one substantive defect already sits with its named decision-owner.

R3D_V22_KIMI_COMPLETE
