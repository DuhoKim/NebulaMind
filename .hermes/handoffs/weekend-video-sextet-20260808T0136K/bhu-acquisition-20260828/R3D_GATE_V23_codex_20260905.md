ACCESS_SHA=4939d193aeeec0ee492333f5864b3c10e3f566d19a80a002f411cf931361e9ae
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

The six declared classes are exhaustive and mutually exclusive under the stated routing. Class 3 is exclusively the limb-A exit; classes 1, 2, and 4 partition limb B through the disjoint P/Z/I cases; class 6 is available only after class 5 has been ruled out; and the seat-split and early-block rules route the remaining terminal states to class 5 or 6. I cannot construct a compliant result fitting two classes or none.

An inconclusive scientific result is genuinely reachable. Concrete input: the completion-free reading permits M approaching zero, while one admissible single-assumption completion yields a 10 kg floor. Then P and Z are both non-empty, so class 2, DYM_FLOOR_UNDERDETERMINED, is filed. A different inconclusive input is two admissible readings yielding distinct positive floors, 10 kg and 20 kg; again class 2 files.

This section is sound; no replacement is required.

2. CONTROLS

C0 through C6, including C5b, each name an exact PASS/FAIL/NOT_RUN code. C1, C2, C3, C4, C5, C5b, and C6 require printed artefacts rather than assertions; C0 requires an exhibition table. Early terminal blocks and unreached limbs explicitly receive NOT_RUN, while controls already engaged retain their actual result.

Defect — literal C3 command is not executable as written.

Verbatim sentence: “The probe is the committed script at the absolute path `.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py` relative to the repository root — a seat must resolve it there, not in its own working directory — and the seat writes `relations.json` in its own working directory, invoking `python3 <that absolute path> relations.json`”

Defect: `<that absolute path>` is prose inside a purported literal command. If executed literally by a shell, angle brackets are redirection syntax and the command does not name the probe. This violates the requirement that every command told to a seat be executable as written.

Exact replacement: “The probe is the committed script at `/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py`, and the seat writes `relations.json` in its own working directory, invoking `python3 /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py relations.json`.”

The three §9 harness commands are executable as written.

3. CIRCULARITY

The lane pattern is barred from inclusion, exclusion, and outcome selection and enters only after a positive-floor class is independently reached, through C6. The census accounts for every non-blank line, separately lists every displayed or numbered equation, requires verbatim locators, restricts exclusion to enumerated codes demonstrated from source text, and turns disagreement into UNRESOLVED/DYM_SOURCE_BLOCKED. Thus a contrary relation cannot be quietly omitted: omission breaks reconciliation or the equation list, while an exclusion remains visible and reviewable. C3 separately tests whether an injected relation alone preserves a determinate floor.

This section is sound; no replacement is required.

4. THE FALSIFIER

C6 can return FAIL in 4 declared terminal outcome classes: DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, DYM_SOURCE_BLOCKED after condition 3 remains UNDECIDED, and R3D_NO_CLASS after an engaged C6 is followed by persistent control failure.

Condition 1: PASS is possible with a filed floor of 1 kg. FAIL is possible only as a filing-integrity failure, for example filing the dimensionless quantity M/m_P = 1 as the “floor.” It cannot fail on any compliant scientific path because membership in either positive-floor class entails that the filed quantity is a mass.

Condition 2: PASS is possible for M_min = sqrt(ħc/G), whose constants all terminate in §2b. FAIL is possible for M_min = A sqrt(ħc/G) when A is introduced by “we choose A = 3” and has no manifest derivation.

Condition 3: PASS is possible for M_min = sqrt(ħc/G), which contains no non-§2b free symbol. FAIL is possible for M_min = λ sqrt(ħc/G), with λ surviving as a free symbol.

Condition 4: PASS is possible for M_min = sqrt(ħc/G) with no quantity held fixed by choice. FAIL is possible where the floor follows only after holding a core density ρ0 constant and no manifest passage derives that constancy.

Condition 5: PASS is possible for the point floor 1 kg, which overlaps none of the three finite comparator intervals. FAIL is possible for the point floor 2.1764343e-8 kg, which overlaps the Planck-remnant interval. Further valid FAIL examples are 2.0e11 kg for the Hawking interval and 5.0e30 kg for the stellar-collapse interval.

The only condition that cannot fail on a compliant path is condition 1; its FAIL input is necessarily a malformed filing, exactly as the document states.

This section is sound; no replacement is required.

5. RE-RUN GUARD

The design expressly forbids assuming K6 repeats and makes the positive class genuinely reachable. Concrete class-1 input: the completion-free printed relations yield the unique allowed-mass infimum M_min = sqrt(ħc/G), and every consistent admissible reading agrees with it, so P is non-empty while Z and I are empty. DYM_FLOOR_DERIVED files regardless of whether a later breaker condition passes.

This section is sound; no replacement is required.

6. FAIRNESS

The operative explanatory clauses use “unreproduced from the stated inputs,” including the limb-A exit and both negative scientific findings. However, the filed class tokens themselves remain operative claims of absence and therefore do not hold the mandated wording everywhere.

Defect — two operative outcome tokens overclaim absence.

Verbatim sentence: “3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Exact replacement: “3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Verbatim sentence: “4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family.”

Exact replacement: “4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above. Report the family.”

Apply those two token replacements consistently throughout the document, including the §4 table, C6 applicability/NOT_RUN clauses, §5a, and §9 routing language. The word “error” appears only in historical or explanatory discussion, not as the operative characterization of a negative study result.

7. STALL

The run can always reach a fileable terminal class. Unread, mismatched, unresolved, undecidable-after-fallback, and unresolved seat-split states route to DYM_SOURCE_BLOCKED. Persistent control failure after the prescribed rerun routes to R3D_NO_CLASS. Scientific paths route to exactly one of classes 1–4, and the early-block rule assigns later controls NOT_RUN without preventing filing.

This section is sound; no replacement is required.

R3D_V23_GATE_COMPLETE
