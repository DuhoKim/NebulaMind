Script execution is unavailable in my mode — `execute_code` is blocked and `python3` via terminal (heredoc and `-c`) is blocked by the single-query approval policy. Per the brief, I say so plainly and do the arithmetic by hand below, printing intermediates. I did not read any other file in the directory; the document was judged as inlined.

ACCESS_SHA=4939d193aeeec0ee492333f5864b3c10e3f566d19a80a002f411cf931361e9ae
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — SOUND.

Six declared: four scientific (1 DYM_FLOOR_DERIVED, 2 DYM_FLOOR_UNDERDETERMINED, 3 DYM_NO_SIZE_MASS_RELATION, 4 DYM_NO_POSITIVE_FLOOR) and two non-scientific (5 DYM_SOURCE_BLOCKED, 6 R3D_NO_CLASS).

The partition holds under attack. Every admissible reading exists (the completion-free derivation always exists) and lands in exactly one of P (consistent, non-empty allowed set, strictly positive glb), Z (consistent, "no positive lower bound follows"), I (inconsistent, empty allowed set) — total and exclusive, since a consistent non-empty mass set has glb >= 0 always: glb > 0 is P, glb = 0 is Z by the operative clause, inconsistency is I. In limb B: P empty forces Z or I non-empty (readings exist), so class 4 covers all of P-empty; within P non-empty, class 2's condition — two floors in P, or Z non-empty, or I non-empty — is the exact De Morgan complement of class 1's, so 1 and 2 are exclusive and total. Class 3 is severed from class 4 by limb, not predicate: limb A exit (no relation reproduced) versus limb B (at least one reproduced) are negations.

I tried to construct a two-class or no-class result and could not:
- Floor only under completions, all agreeing: completion-free in Z, completions in P, so P non-empty and Z non-empty — class 2 only.
- Printed relations mutually inconsistent, no floor: completion-free in I, P empty — class 4 only.
- One completion yields 10 kg, another permits zero: class 2 only.
- Floor derived, then condition 3 UNDECIDED: explicitly routed to class 5 with C6=FAIL, not class 1.
- Nothing printed: class 3 (limb A). The old entailment into class 4's predicate is cut by the limb separation, which the document states as a finding rather than a predicate patch.

An INCONCLUSIVE result is genuinely reachable, three ways: class 5 (digest mismatch, UNRESOLVED proposition, undecidable-after-fallback, unresolved split — "the study waits", explicitly not a verdict), class 6 (a control failing after two attempts), and class 2 as a scientific inconclusive. C0 must exhibit each before freeze.

2. CONTROLS — three defects. Everything else is strong: all eight controls emit exact PASS|FAIL|NOT_RUN tokens; C1, C2, C4, C5b each name a printed artefact and state that a claimed pass without it fails; C0's exhibition table is verified by Tori rather than self-certified; unreached controls are handled by section 9's explicit rule plus section 3's named limb-A dispositions; the three harness commands in section 9 ("python3 --version", "python3 -c \"import sympy; print(sympy.__version__)\"", "shasum -a 256 $(command -v python3)") are literal and executable as written.

DEFECT 2.1 (substantive) — the C3 probe is the one unpinned run dependency, and its invocation is not executable as written.
Verbatim: "**The probe is the committed script at the absolute path `.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py` relative to the repository root — a seat must resolve it there, not in its own working directory — and the seat writes `relations.json` in its own working directory, invoking `python3 <that absolute path> relations.json`**"
Three faults in one sentence. (i) No sha256 pins the probe. Everything else the run depends on is pinned: the sources by digest in section 2a, the pattern record by digest in section 5, the constants by value in section 2b, the python3 binary itself by printed digest in C5, and SymPy by cross-seat version match in condition 3. The script that DECIDES C3 is the single dependency with no identity check; "committed" names no commit, and the third seat via the wrapper may run later against moved bytes. (ii) ".hermes/..." is a relative path; calling it "the absolute path ... relative to the repository root" is self-contradictory. (iii) "python3 <that absolute path> relations.json" is placeholder notation, not executable as written — the identical form codex found at V20 ("the angle brackets are shell syntax"), repaired then, reintroduced by V23's path repair. The document holds C5 to the literal-execution standard; C3's invocation fails it.
Exact replacement: "**The probe is the committed script at the repository-root-relative path `.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py`, pinned by sha256 `<the lane prints the sha256 of the committed bytes here>` — a seat resolves it against the repository root, never against its own working directory, prints the computed digest of the file it executes, and a mismatch fails `C3_DELETION_PROBE`. The seat writes `relations.json` in its own working directory and executes and prints `python3 \"$(git rev-parse --show-toplevel)/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py\" relations.json`**"
(The digest value must be supplied by the lane: I was barred from reading other files in the directory, so I cannot compute it. The replacement is exact apart from that value.)

DEFECT 2.2 (substantive — the V15 codex defect's sibling, one class over) — class 4's filing names no control dispositions, and C3 is genuinely unreached there.
Verbatim: "4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family. If the printed relations are mutually inconsistent, **report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs**, file here, and **reproduce the contradiction.**"
Defect: on class 4 no admissible reading yields a floor, and an injected relation that alone yielded a floor is inadmissible under section 2 — so the C3 deletion probe has no candidate and cannot be meaningfully engaged, yet nothing says so. Section 9 permits NOT_RUN "only where this document explicitly makes that control unreached", and class 4's clauses never mention C3. Two obedient seats diverge: one records C3_DELETION_PROBE=NOT_RUN in breach of the explicitness rule, another attempts the probe with no floor candidate or omits the token. Section 3's limb-A exit was repaired for exactly this and names C3, C4, C6 NOT_RUN; class 4 is limb B's no-floor exit and was missed.
Exact replacement — append to class 4: "**Record `C3_DELETION_PROBE=NOT_RUN` on this class: no positive floor survives, so the deletion probe has no candidate; C0, C1, C2, C4, C5 and C5b remain reached and carry their actual results; C6 is `NOT_RUN` per §5.**"

DEFECT 2.3 (minor) — "that report" has no unique referent in the case the rule was written for.
Verbatim: "**the third seat re-runs each failed control of that report once."
Defect: the trigger covers "at least one report ... not control-clean", which includes both reports non-clean — the zero-clean case V21 added this rule for. "that report" is singular; re-running one report's failed control versus both can change the terminal class (class filed versus R3D_NO_CLASS).
Exact replacement: "**the third seat re-runs each failed control of each report that is not control-clean, once per failed control."

3. CIRCULARITY — SOUND.

The pattern record and every prior lane conclusion are forbidden as evidence for inclusion, exclusion, or outcome selection (section 2c); the pattern enters only at C6, after a positive-floor class is reached on other grounds. Section 0 was de-patterned with the removal recorded, and section 0 is redacted from seat packets (section 8o). The enumeration channel is closed: every non-blank line carries a disposition, exclusion requires one of four predeclared codes demonstrated from source text, DUPLICATE names its covering row, per-source reconciliation counts must be equal or C2 fails, and every numbered or displayed equation is its own row — an omitted contrary relation shows up as arithmetic, and the full text with line numbers is printed for review. The eleven search terms are demoted to a printed convenience and may not justify any row. The completion channel is closed by the admissibility definition: exactly one named assumption, consistent with every printed relation, operating on at least one printed relation, so a completion cannot be the sole source of a bound; every candidate is a printed C2 row with verbatim support. The one lane-authored scaffolding that does touch the falsifier — the four comparator conventions (5120pi, 3.0, [2.2, 2.9] M_sun, Gregorian year) — is disclosed as assertion, frozen in-document, printed in the artefact, and routed outside C2 and condition 2 by name. Residual seat discretion over which candidate completions to consider is visible in the printed ledger and reproduced by the second seat. I found no channel by which the lane's pattern reaches the evidence.

4. THE FALSIFIER — SOUND, with the entailment disclosed rather than hidden.

THE NUMBER: C6 can record C6_BREAKER_TEST=FAIL in 4 of the 6 declared outcome classes — class 1 (substantive condition failure; the floor still stands, not a counterexample), class 2 (evaluated per floor in P), class 5 (condition 3 UNDECIDED after its fallback records FAIL and then files DYM_SOURCE_BLOCKED), and class 6 (an engaged C6 carrying its actual result into a persistent-control-failure filing). NOT_RUN on classes 3 and 4 only, which is correct — those classes produce no number to test. The section 5a engagement/filing distinction (2 engaged, 4 recordable) is accurate.

Per condition — execution was blocked in my mode, so the arithmetic below is by hand with intermediates printed:

Condition 1 (dimension is mass). CAN PASS: floor 10 kg — dimension mass. CANNOT FAIL on any valid path: every positive-floor class requires a mass, so a correctly filed floor passes the dimensional test. It fails only on a malformed filing — a seat files 0.42, a dimensionless ratio, as its floor — which is exactly the filing-integrity check the document describes. This is the condition that cannot fail on any (valid) path; it is marked ENTAILED in section 5a and C0, the entailment is stated, and the 4-of-5 substantive count is printed. Disclosed, not hidden.

Condition 2 (constants trace). CAN PASS: floor = sqrt(hbar c/G) — hbar, c, G all terminate in the section 2b list. CAN FAIL: floor = 0.42 * m_P with 0.42 entered as "we choose" — terminates in no manifest equation and no section 2b entry; a terminus outside the manifest fails by the rule's own text.

Condition 3 (no free normalisation). CAN PASS: floor = sqrt(hbar c/G) — no non-2b parameters exist, so the free-symbol probe finds none. CAN FAIL: floor filed as M = c^2 r0 / 2G with r0 a free parameter — replacing r0 with an independent symbol s yields c^2 s / 2G after one simplification, s survives, FAIL. A third route to FAIL: timeout with an absent/timed-out/unparseable fallback gives UNDECIDED, which maps to C6=FAIL and class 5.

Condition 4 (no fixity assumed). CAN PASS: nothing is held constant by choice, or every held constant (e.g., Lambda fixed by the de Sitter-limit condition) has a deriving passage in the C2 artefact. CAN FAIL: the derivation sets r0 = l_P "for definiteness" with no manifest passage — the fixity table row has no source line, FAIL.

Condition 5 (no comparator overlap). CAN PASS: floor = 1.0e15 kg — no overlap with any row. CAN FAIL, one concrete input per comparator row: m_P = 2.1764343e-8 kg (Planck row), 2.0e11 kg (Hawking row), 5.0e30 kg (TOV row).

Hand verification of the comparators, intermediates printed:
- hbar*c = 1.054571817e-34 * 2.99792458e8 = 3.161526771e-26 (matches the V8g figure 3.16152677e-26).
- m_P^2 = 3.161526771e-26 / 6.67430e-11 = 4.736867e-16; m_P = 2.17643(4)e-8 kg — my long division gives 2.1764345e-8, the seats' executed 2.1764343e-8; agreement to 6 significant figures. Either way it lies inside the widened degenerate row [2.176433e-8, 2.176435e-8] with margin on both sides, and outside the old V16 point interval [2.176434e-8, 2.176434e-8] — the escape kimi found at V16 is genuinely closed.
- Gregorian year = 365.2425 * 86400 = 31,556,952 s; t0 = 13.797e9 * 31,556,952 = 4.353913e17 s (matches 4.3539e17 as printed and 4.353913e17 as recorded).
- Hawking lower: c^4 = 8.0776087e33; numerator t0 * hbar * c^4 = 4.353913e17 * 0.8518418 = 3.708845e17; denominator 5120pi * G^2 = 16084.9544 * 4.4546280e-21 = 7.1652488e-17; ratio = 5.176189e33; cube root = 1.72983e11 kg (seats executed 1.7298245e11; agreement to 5 significant figures). Printed interval [1.729e11, 5.190e11]: 1.729e11 <= 1.72982e11 (lower rounded down, outward) and 3.0 * 1.7298245e11 = 5.1894735e11 <= 5.190e11 (upper rounded up, outward). Correct.
- TOV: 2.2 * 1.98892e30 = 4.375624e30, printed 4.375e30 (down, outward); 2.9 * 1.98892e30 = 5.767868e30, printed 5.768e30 (up, outward). Correct.
- Probe checks: 1.0e15 kg overlaps nothing (PASS); 2.0e11 kg overlaps Hawking; m_P overlaps Planck; 5.0e30 kg overlaps TOV; 3.0e30 kg overlaps nothing (matches the document's own V9 self-check).

5. RE-RUN GUARD — SOUND.

Section 6 states it plainly: "K6's outcome may **not** be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`." The positive class is genuinely reachable — limb B entered, P non-empty with all readings agreeing, Z and I both empty — and C0 must exhibit an input producing it before any freeze. K6's K6_FLOOR_UNDERDETERMINED is not imported; section 0 poses the open question and bars the prior conclusion from inclusion, exclusion, or outcome. The gate record's V3 note that codex found this section sound is consistent with the text.

6. FAIRNESS — held in every operative clause; one carried token defect, disclosed and escalated, pending Duho.

Every operative clause holds the wording: section 3's limb-A exit and class 3's row and definition ("a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs"), class 4's table row ("a positive floor was unreproduced from the stated inputs") and definition ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and the section 6 rule itself.

The one breach is not new and is not mine to repair: the class TOKENS "DYM_NO_SIZE_MASS_RELATION" and "DYM_NO_POSITIVE_FLOOR" assert absence while their definitions say unreproduced. This was found independently by codex (V21), then by both engines (V22, V23), classified SUBSTANTIVE each time, and escalated on authority grounds — renaming a class is redefining one, which the document correctly reserves to Duho. I confirm the objection is correct on the merits: a token travels into reports stripped of its qualifying definition. It is disclosed in section 8m with options and costs, not hidden. I endorse the proposed "..._UNREPRODUCED" rename and record that the decision is the principal's, exactly as the document states. Section 0's "the source never supplies" was found by kimi (V23, 6b), classified COSMETIC, and knowingly DECLINED with the reason recorded (non-operative motivation, redacted from every seat packet) — that disposition is recorded rather than silent, and I accept it.

7. STALL — SOUND.

I could not construct a terminal state with no fileable class. Blocks at any point file class 5, with section 9's sweep assigning NOT_RUN to never-engaged later controls and actual results to engaged ones. A persistent control failure after class 5 is ruled out files class 6. Same-class splits on C6 outcome, floor formula or value, or family, and different-class splits of any kind, convene the third seat, which re-executes blocked reads once and re-runs failed controls of non-clean reports; all-three-differ or undecidable files class 5, whose enumeration now includes that state (the V23 repair makes the definition true of the routing). Condition 3's UNDECIDED is mapped to FAIL plus class 5, so "does not pass" is never a fourth state. Symbolic timeouts fall to named fallback rows, and an absent or undeciding fallback files class 5. The limb-A exit files class 3 with dispositions named. Section 9's closing sentence — "Every terminal path files exactly one declared class" — is borne out by the routing.

SUMMARY OF REPAIRS REQUIRED (all in section 2's findings; none touches the partition, the falsifier's reach, circularity, the re-run guard, or stall):
R1 (2.1): pin the C3 probe by sha256 and give a literal, executable invocation.
R2 (2.2): state C3's NOT_RUN disposition on class 4.
R3 (2.3): "that report" -> "each report that is not control-clean".
Carried, not mine: the two class-token renames await Duho's ruling (section 8m), endorsed here for a third time.

R3D_V23_KIMI_COMPLETE
