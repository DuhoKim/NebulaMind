Script execution is blocked in my mode (execute_code refused; terminal python3 -c refused as dangerous under single-query approvals). Per the brief, I say so plainly and do the arithmetic by hand below, printing intermediates. I read no other file in the directory; the probe digest and pattern-record digest are therefore unverified by me, which does not affect the design judgement.

ACCESS_SHA=4c8d0d3256134f8ac45bed4fdf5e222d1475ef01d8247cc1716723a379c95dde
GATE=PREREG_UNSOUND

EXECUTION NOTE. Script execution is unavailable in my mode, so all arithmetic below is done by hand with intermediates printed. I could not execute the three §9 harness commands, the C3 probe, or any exit-code check, and I claim no such execution. The harness commands read as valid POSIX/macOS shell as written; that is a reading, not an execution.

Hand verification of the comparator table (§2b values):
  m_P: ħc = 1.054571817e-34 × 2.99792458e8 = 3.1615268e-26 (document's §8g value 3.16152677e-26 confirmed). ħc/G = 3.1615268e-26 / 6.67430e-11 = 4.73687e-16. sqrt = 2.1764342e-8 kg. Lies inside the widened degenerate row [2.176433e-8, 2.176435e-8]. The V16 escape is closed.
  t_0: 365.2425 × 86400 = 31,556,952 s; × 13.797e9 = 4.3539127e17 s ≈ 4.3539e17. Confirmed.
  Hawking lower: c^4 = 8.0776087e33; ħc^4 = 0.8518419; G^2 = 4.4546280e-21; 5120π = 16084.954; denominator 5120πG^2 = 7.16525e-17; numerator t_0·ħc^4 = 3.7088450e17; ratio = 5.17616e33; cube root = 1.729826e11 kg. Rounded down to 1.729e11, outward. Upper = 3.0 × 1.729826e11 = 5.189478e11, rounded up to 5.190e11, outward. Interval contains both unrounded endpoints. Confirmed.
  TOV: 2.2 × 1.98892e30 = 4.375624e30, rounded down to 4.375e30; 2.9 × 1.98892e30 = 5.767868e30, rounded up to 5.768e30. Outward. Confirmed.
All five comparator numbers reproduce by hand; outward rounding holds at every row including the degenerate Planck row.

FINDING 1 — OUTCOME CLASSES: SOUND as a partition over a fixed reading set (but see Finding 3, which removes the fixity).
Exhaustive and mutually exclusive: class 3 is filed only from limb A, classes 1/2/4 only from limb B, and the limb-A exit (no relation reproduced) is the exact complement of limb-B entry. Within limb B the P/Z/I partition is total: the completion-free derivation is always an admissible reading and every admissible reading lies in exactly one of P, Z, I, so the reading set is never empty; therefore P empty forces Z or I non-empty, and class 4 covers every P-empty case. Classes 1 and 2 are disjoint on the agreement predicate; class 4 is disjoint on P empty. Classes 5 and 6 are ordered by "only after ruling out DYM_SOURCE_BLOCKED". I could not construct a real result fitting two classes or none, given a fixed reading set.
INCONCLUSIVE is genuinely reachable: class 2 (completion-free yields no floor while a completion yields 10 kg — P and Z both non-empty), class 4 (the only printed size–mass relation is r_g = 2GM/c^2, which binds size to mass and so enters limb B, but bounds no positive mass — P empty), and the two non-scientific terminals 5 and 6.
Caveat, stated in this lane's own terms: §2's own parenthetical counts "two obedient seats could file different classes for the same physics" as an outcome-class defect. That failure re-enters through the unfixed reading set; it is stated and repaired under Finding 3, not here.

FINDING 2 — CONTROLS: TWO DEFECTS. Otherwise sound: every control names an exact token with PASS|FAIL|NOT_RUN (§9's vocabulary); unreached controls are handled explicitly (the limb-A exit line, the class-4 disposition line, and the early-terminal-block rule); no control is passable by bare assertion — C1 requires printed digests with the anti-transcription rule extended to it, C2 the full printed ledger, C3 the captured deleted-state output, C4 the printed algebra and premise list, C5b the per-path table, C6 the per-condition artefacts, C0 the exhibition table; independent re-run (§7, "Tori re-runs everything") backs the transcription rules. The three §9 harness commands read as executable as written; per the execution note, that is a reading, not a run.

DEFECT 2a — the one literal command is not executable as instructed.
Verbatim: "A seat resolves it at that path, not in its own working directory, verifies the digest before use, and writes `relations.json` in its own working directory." followed by "**The literal command is:**  python3 .hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py relations.json".
Defect: the command carries two relative paths. Both resolve only when the working directory is the repository root AND relations.json sits in the repository root. The prose directs the seat to write relations.json in its OWN working directory and to resolve the script "not in its own working directory" — jointly satisfiable only if the seat's working directory is the repository root, which that clause denies. Two obedient seats diverge: one cd's to the repository root and writes relations.json there (runs); one keeps artefacts in its own working directory as told and the command cannot open the script or the input. Worse, CPython's standard behaviour on an unopenable script file is exit status 2 — the probe's own NOT_RUN code — so this failure can be silently recorded as a legitimate NOT_RUN (stated as language behaviour; I could not execute the check). This is the same C3-invocation defect family as V20 and V23/V24, and the V24 repair fixed the brackets and the pin without reconciling the working-directory instructions.
Exact replacement: "A seat resolves it at that path, verifies the digest before use, writes `relations.json` in the repository root, and runs the literal command from the repository root, so that both relative paths resolve as written."

DEFECT 2b — C3's disposition on class 2, and C3=FAIL's routing, are unstated.
Verbatim: §9 — "`NOT_RUN` is permitted only where this document explicitly makes that control unreached." C3 — "if a unique floor survives on an injected relation alone, that relation is circular and no derived-floor class may be filed." Class 2 names only C6 ("**C6 is RUN on this class** and its result reported") and never names C3.
Defect: on DYM_FLOOR_UNDERDETERMINED, C3 is neither explicitly RUN nor explicitly NOT_RUN, so §9's default forces it RUN — but the probe's JSON needs a `target`, and class 2 files no unique floor ("choose none"), so the target is undefined. And on ANY class, C3=FAIL bars "derived-floor class" filing with no successor named. The case is reachable: a completion such as "interior density equals the Planck density" (only §2b constants) operating on a printed mass-function relation that is not a field equation yields a determinate floor surviving deletion of the field equations — C3=FAIL on a filed floor, and the document says nothing about what files next. One obedient reading stalls with no fileable class; another files the barred class. Two seats diverge at the terminal step.
Exact replacement (two parts):
(a) Append to §4 class 2, after the C6 sentences: "**Record `C3_DELETION_PROBE=NOT_RUN` on this class: the probe's trigger is a unique floor surviving on an injected relation alone, and this class files no unique floor — the seat chooses none — so the probe has no target; every floor's dependence on an injected completion is already reported per reading, which is what the probe exists to expose.**"
(b) Append to C3, after "no derived-floor class may be filed.": "**When the probe files FAIL, the circular relation is removed from every reading that used it and the partition of §4 is recomputed on the remaining readings; the run files whichever class the recomputed partition selects, and the expelled relation is reported with the probe's captured output.**"

FINDING 3 — CIRCULARITY: DEFECT (load-bearing; this is what makes the gate UNSOUND).
The census machinery itself is sound: every non-blank line carries exactly one disposition with a printed reconciliation count per source, every numbered or displayed equation is its own row, exclusion is only by predeclared reason code demonstrated from source text, every row carries verbatim text, and UNRESOLVED forces DYM_SOURCE_BLOCKED — a contrary relation cannot be quietly omitted without breaking arithmetic. The pattern record is barred from inclusion, exclusion and outcome selection and enters only at C6, against a digest-pinned verbatim copy. §0's de-patterning is stated and its history disclosed. All of that holds.
Verbatim: "An **admissible reading** is either the **completion-free derivation** (the printed relations with nothing added) or **the printed relations plus exactly one admissible completion**." And: "**A completion is ADMISSIBLE if and only if it introduces exactly one named assumption, is consistent with every printed relation of the manifest sources, AND operates on at least one printed relation**".
Defect: the set of readings over which §4 partitions is never fixed. Infinitely many completions satisfy admissibility — any coefficient value, any matching-surface choice, any mass definition the sources leave unbound — and no clause says which are tested. Class 1 requires "P non-empty, all of P agree on one floor, Z AND I both empty". A completion that merely shrinks the allowed set is admissible, consistent with every printed relation, and operates on them, yet yields a different floor: printed relations giving M ≥ 10 kg, plus the single named assumption "core density does not exceed the Planck density" giving M ≥ 50 kg, puts two floors in P. So one obedient seat testing no completions files DYM_FLOOR_DERIVED and another testing that one completion files DYM_FLOOR_UNDERDETERMINED — identical physics, opposite classes, the exact failure this document cites as its own standard. And the channel points in the live direction: ANY class-1 candidate can be converted to class 2 by constructing one more admissible completion, so the lane's pattern can be protected at will through a step no control audits. C0 cannot see it — a seat exhibiting class 1 with a minimal completion set still produces a valid path. The seat-split rule cannot converge it — both blind seats are lane seats holding the same discretion, and the third seat holds it too. The pattern reaches the outcome not through which relations are enumerated (guarded) but through which READINGS are constructed (unguarded).
Exact replacement (append to §2, after the admissibility paragraph): "**The completions tested are exactly the four kinds named above — a Euclidean volume, a uniform interior, an order-unity coefficient set to 1, and the GR exterior — each instantiated only where the printed relations leave its object unbound, and no other completion may be introduced. The admissible readings over which §4 partitions are exactly the completion-free derivation plus one reading per such instantiation, so that the reading set is a function of the printed sources and not of the seat.**"

FINDING 4 — THE FALSIFIER: SOUND in design. The number is 4.
C6 can record FAIL in 4 of the 6 declared classes: DYM_FLOOR_DERIVED and DYM_FLOOR_UNDERDETERMINED (the engagement classes), DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after its fallback records C6_BREAKER_TEST=FAIL and then files 5), and R3D_NO_CLASS (an engaged C6 carries its actual result — FAIL included — into a persistent-control-failure filing under §9's "every control that WAS engaged before the block carries its actual result"). NOT_RUN on classes 3 and 4 is correct: no number to test. This matches §5a's corrected two-number statement (engaged on 2, FAIL recordable in 4); engagement and filing are now distinguished consistently, and the V18/V20 scar is closed.
Per condition, with concrete inputs:
  1 (dimension is mass): CAN PASS — floor 2.1764342e-8 kg. CAN FAIL — only on a malformed filing: a seat entering the ratio r_0/M as its floor. It CANNOT fail on any valid path: every positive-floor class requires a mass, so a correctly filed floor passes a dimensional test. The document marks it ENTAILED, states the entailment, and retains it as a filing-integrity check — disclosed, not hidden. Correct.
  2 (constants trace): CAN PASS — floor sqrt(ħc/G): ħ, c, G all terminate in §2b. CAN FAIL — floor (4π/3)·sqrt(ħc/G) with 4π introduced as "the simplest form": the chain terminates outside the manifest and §2b, which the rule says fails exactly as a "we assume" terminus fails.
  3 (no free normalisation): CAN PASS — floor sqrt(ħc/G): no non-§2b parameters exist, so the simplified expression contains no free symbols. CAN FAIL — floor λ·sqrt(ħc/G) with λ chosen to be 1 after the fact: the free-symbol run leaves λ in the expression, and §9's "free-symbol survival" fallback row fails it the same way. The UNDECIDED route (timeout, fallback absent/unparseable) maps to C6=FAIL plus class 5 — exhaustiveness holds.
  4 (no fixity assumed): CAN PASS — r_0 held constant with a printed deriving passage (r_0^2 = 3/Λ) reproduced in the C2 artefact. CAN FAIL — the core density held constant by choice with no deriving passage anywhere in the C2 artefact.
  5 (comparator overlap): CAN PASS — floor 1.0e15 kg: overlaps none of the three intervals, and the ΛCDM row can never match. CAN FAIL — floor 2.0e11 kg overlaps Hawking [1.729e11, 5.190e11]; floor m_P ≈ 2.1764342e-8 kg overlaps [2.176433e-8, 2.176435e-8]; floor 5.0e30 kg overlaps [4.375e30, 5.768e30]. One FAIL input per comparator row, as C0's per-row exhibition requires. The disjunctive pass ("no other precondition on the pass") with the complete table required as a loud artefact is a coherent polarity; the accepted cost (a lazy comparison passes) is stated, not hidden.
Condition that cannot fail on any path: none, if malformed filings count as paths. On valid paths only, condition 1 cannot fail — and it is labelled ENTAILED rather than counted, which is the honest treatment the V11 split was settled with.

FINDING 5 — RE-RUN GUARD: DEFECT by consequence of Finding 3; §6 itself is sound as far as it goes.
§6 does not assume K6's outcome repeats ("this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`"), and the partition formally admits class 1. But the positive class is genuinely reachable only under the discretionary reading of the reading set. Under the maximal reading — partition over ALL admissible completions — class 1's "all of P agree, Z empty" can never be established by any finite seat, since one more admissible completion is always constructible, so the positive class is unreachable in practice. Under the minimal reading — seat-chosen completions — class 1 is reachable but defeasible at will (Finding 3). Either way the guard's substance fails until the reading set is frozen. No edit to §6 is needed; Finding 3's replacement restores the guard by making the reading set a function of the printed sources.

FINDING 6 — FAIRNESS: DEFECT, and it is the one the document already knows about.
The operative definitions hold the wording. §3: "a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs". Class 3's row and class 4's row and text use the same form, and class 4's inconsistency clause says "a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs". No operative clause says "error" or asserts existence-level absence — except the filed vocabulary itself.
Verbatim: the class tokens "**DYM_NO_SIZE_MASS_RELATION**" and "**DYM_NO_POSITIVE_FLOOR**" — and §8m's own admission: "Cost of leaving: the study's own fairness rule is violated by its outcome vocabulary."
Defect: a class NAME travels into reports and summaries stripped of the definition that qualifies it, and these two names assert absence of a relation and of a floor in the branch — precisely the claim the lane forbids for a negative finding. Both seats found this independently four times (§8m–§8p); it is classified SUBSTANTIVE under the lane's own criterion (4) each time, and it stands unrepaired in the frozen candidate on authority grounds. A referee judges the document as it stands: the defect is present, substantive, and unfixed.
Exact replacement: rename throughout — DYM_NO_SIZE_MASS_RELATION → DYM_SIZE_MASS_RELATION_UNREPRODUCED, and DYM_NO_POSITIVE_FLOOR → DYM_POSITIVE_FLOOR_UNREPRODUCED — in §3, the §4 table and class list, §5a, C6's NOT_RUN enumeration, and §9. (The §0 line "the source never supplies" is non-operative motivation, redacted from seat packets, disclosed and knowingly declined at V23; I concur it is outside the operative clauses and demand no replacement for it.)

FINDING 7 — STALL: DEFECT, by Finding 2b.
Apart from the C3 gap, every terminal path files exactly one declared class: limb-A exit → 3; the limb-B partition → 1/2/4 with no hole (the reading set is provably non-empty); unreadable source, digest mismatch, UNRESOLVED row, undecidable-after-fallback, or unresolvable seat split → 5; persistent control failure after the seat-split rule → 6; condition-3 UNDECIDED → C6=FAIL then 5. But on the C3=FAIL path the document bars "derived-floor class" filing and names no successor — a terminal state with NO fileable class on one obedient reading, and an unaudited divergence on the other. Finding 2b's replacement (expel the circular relation, recompute the partition, file what it selects) closes it. With that repair and 2a's, the stall guard holds.

WHY UNSOUND rather than SOUND_WITH_REPAIRS. The spine is largely sound — the partition per fixed reading set, the census, the comparator arithmetic, the falsifier's polarity, the control vocabulary — and I verified every comparator number by hand. But Finding 3 is not a patch: it leaves the domain of the outcome-class partition (which readings exist) to seat discretion, reopens the same-physics-different-class failure this document itself treats as the standard, and does so in the live direction — any counterexample to the lane's pattern can be converted to underdetermination by constructing one more admissible completion. Finding 6 is a substantive fairness defect found four times by both engines and knowingly carried. Findings 2a/2b are local but real: the one literal command is not executable as instructed, and C3 has an unreachable-target class and a no-successor FAIL. Four substantive defects, one in the exact channel the brief flags as live, one admitted in the text. This document should not freeze as it stands.

R3D_V24_KIMI_COMPLETE
