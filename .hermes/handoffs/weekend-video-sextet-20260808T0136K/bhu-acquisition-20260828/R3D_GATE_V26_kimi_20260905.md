All execution checks are done. Hand arithmetic for the comparators (script execution for arithmetic was blocked in my mode, so I computed by hand with intermediates):

t0 = 13.797e9 × (365.2425 × 86400 = 31,556,952) = 4.3539127e17 s ✓ (document prints 4.3539e17)
ħc = 1.054571817e-34 × 2.99792458e8 = 3.16152677e-26; ħc/G = 4.736867e-16; m_P = sqrt = 2.1764343e-8 kg ✓, and 2.176433e-8 ≤ 2.1764343e-8 ≤ 2.176435e-8 — the widened degenerate row contains it ✓
c⁴ = 8.077609e33; t0·ħ·c⁴ = 3.708845e17; 5120πG² = 7.165250e-17; M³ = 5.17616e33; M = 1.72983e11 kg ✓ lower 1.729e11 rounds down, upper 3× = 5.18948e11 ≤ 5.190e11 rounds up ✓
TOV: 2.2 × 1.98892e30 = 4.375624e30 ≥ 4.375e30 ✓; 2.9 × 1.98892e30 = 5.767868e30 ≤ 5.768e30 ✓

GATE REPORT — R3-D pre-registration, V26, judged as it stands

Line 1: ACCESS_SHA=6d60bb0a20519a64cb7fc83cae780822e17b830fa15d090da119ed3980e5132d
Line 2: GATE=PREREG_SOUND_WITH_REPAIRS

FINDING 1 — OUTCOME CLASSES: SOUND WITH ONE REPAIR (shared with Finding 3)

Exhaustive and mutually exclusive as an operational decision procedure. Class 3 is filed only from the limb-A exit; classes 1, 2, 4 partition limb B by the disjoint P/Z/I cases; 5 and 6 are non-scientific terminal states with their own triggers. Every admissible reading is forced into exactly one of P (positive GLB of a non-empty allowed set), Z (no positive lower bound), I (empty allowed set), and the union is non-empty because the completion-free reading always exists, so the limb-B table is total: P empty gives class 4; P non-empty with agreement and Z=I=empty gives class 1; everything else in limb B gives class 2. I could not construct a real result fitting two classes or none. The V17 limb separation holds on this text: a limb-A case does satisfy class 4's predicate by meaning, but filing is by limb, and the table marks class 4 "LIMB B ONLY", so no case double-files. The class-3 entailment (nothing printed ⇒ a completion yielding a bound would be its sole source ⇒ inadmissible ⇒ P stays empty) is closed by the V16 "operates on at least one printed relation" rule, which is still present.

INCONCLUSIVE is genuinely reachable, twice over: class 4 (printed relation with a free parameter and no admissible completion rescuing a floor — e.g. m(r) = (4π/3)ρ_eff r³ with ρ_eff unbound and no printed fixing relation, P empty, Z non-empty) and class 2 (completion-free reading in Z while the GR-exterior completion yields a floor — P non-empty, Z non-empty).

The one defect here is not in the partition but in the construction of the set partitioned — stated and quoted under Finding 3, since it is also the circularity channel.

FINDING 2 — CONTROLS: SOUND WITH TWO REPAIRS (and the C3 execution report)

No control is passable by bare assertion: each of C0–C6/C5b names a printed artefact (exhibition table, digests, full ledger, captured probe output, printed algebra plus premise list, printed command output, per-path scope table, per-condition artefacts) and an exact token with PASS|FAIL|NOT_RUN. Unreached controls are handled: §9's vocabulary permits NOT_RUN only where the document explicitly says so, the early-block rule distinguishes engaged controls (actual result) from never-engaged ones (NOT_RUN), and classes 3 and 4 now name their dispositions.

THE C3 DELETION PROBE — EXECUTION REPORT, as required:
(a) Yes, I executed it — twice.
(b) Working directories: first run from /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828 (my session cwd, which is the lane directory itself); second run from /Users/duhokim ($HOME).
(c) Exact stdout, byte-identical in both runs:
    target            : M
    retained (all)    : ['eq_src', 'inj_ok']
    DELETED (pinned)  : ['eq_src']
    injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
    §2b constants     : ['hbar', 'c', 'G']
    with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    VERDICT: no unique floor without the source-pinned equations -> not circular
    C3_DELETION_PROBE=PASS
Exit code: 0, both runs.
Caveats stated plainly: the designated input file _c3_relations.json already existed and held the NEGATIVE control (injected r0 = 2GM/c²); I ran the literal command against it exactly as it stood and did not write, modify, or read it beyond what the command itself consumes. The exit-1 circularity path was therefore NOT exercised by me — §8q's transcripts assert it, and that is the document's own claim, not my run. I did compute the script's identity: sha256 = 7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874, matching the pin in C3. On this host /usr/bin/python3 is Python 3.9.6 and is the same file PATH resolves (both digests b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9), and the probe executed under it, so its SymPy dependency resolves under the pinned interpreter here.

Every other literal command the document seats to execute is executable as written: §9's three harness commands are well-formed (the sympy check is correctly inside a -c string), and C3's line contains no shell metacharacters — confirmed by execution, not inspection.

REPAIR 2a — the interpreter pin is PATH-dependent where C3 is absolute.
Verbatim: "**The interpreter is the one C5 pins by digest.**" against §9's "`shasum -a 256 $(command -v python3)`".
Defect: $(command -v python3) resolves through the seat's PATH and is nowhere constrained to be /usr/bin/python3, while C3's literal command executes /usr/bin/python3 unconditionally. On this host the two coincide (verified above), so the quoted claim happened to be true of my run — but the text does not enforce it. On a seat host with a venv or Homebrew interpreter first in PATH, C5 would pin one interpreter while C3 executes another, and condition 3's cross-seat SymPy check would compare versions of an interpreter the probe never used. Latent here; live in the text.
Exact replacement, in §9's harness bullet: replace "`python3 --version`, `python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 $(command -v python3)`" with "`/usr/bin/python3 --version`, `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 /usr/bin/python3`".

REPAIR 2b — probe exit 2 has no mapping where C3 is reached.
Verbatim: "It prints the retained ids, the deleted ids, the injected relations, and the solve result with and without the pinned equations, exiting **0 on PASS, 1 on FAIL, 2 on NOT_RUN**." against §9's "**`NOT_RUN` is permitted only where this document explicitly makes that control unreached.**"
Defect: on a class where C3 is reached (class 1, or any filing with a unique-floor candidate), an exit of 2 is outside the control vocabulary: NOT_RUN is not permitted there, PASS and FAIL are defined by exits 0 and 1, and no clause says what the seat files. A conforming seat can produce it — a malformed relations file, or an input the script declines — and then has no rule.
Exact replacement, inserted after the exit-code sentence in C3: "**On a class where C3 is reached, an exit of 2 is a control failure, not `NOT_RUN`: the seat corrects its relations file and re-runs once; a second exit of 2 is a persistently failing control and is routed by the §9 control-failure rule, with both captured outputs printed.**"

FINDING 3 — CIRCULARITY: ONE REPAIR (the bounded residue of the V24 defect)

The pattern cannot reach the evidence through the census: every non-blank line carries a printed disposition with verbatim text, exclusion needs a predeclared reason code demonstrated from the line itself, UNRESOLVED blocks, and the reconciliation count makes omission arithmetic rather than judgement. Class selection is mechanical once the reading set exists. But the construction of that set still contains one unmechanised judgement, and it is the channel through which a contrary relation can be quietly omitted.

Verbatim: "**THE COMPLETIONS TESTED ARE EXACTLY THE FOUR KINDS NAMED ABOVE — a Euclidean volume, a uniform interior, an order-unity coefficient set to 1, and the GR exterior — each instantiated ONLY where the printed relations leave its object unbound, and NO OTHER COMPLETION MAY BE TESTED OR COUNTED.** The admissible reading set is therefore finite: the completion-free derivation plus at most four one-completion readings."
Defect, two parts. (i) "leave its object unbound" names no decision procedure. Whether the core scale is left unbound by the printed relations is a seat judgement; a seat that judges it BOUND instantiates no completions, and if the completion-free reading yields a floor the partition trivially files class 1 — Z and I empty, P in trivial agreement. A seat that judges it UNBOUND tests completions that may yield different floors and files class 2. Same census, same physics, different class — and the direction that produces an uncontested class 1 is the high-stakes one. This is the V24 F3 defect one level down: the kinds are now bounded, but which instantiations exist still is not. (ii) "at most four one-completion readings" assumes each kind has at most one unbound object; if the printed relations leave two coefficients unbound, per-object instantiation yields five readings, and the document nowhere says whether a kind is instantiated per object or once for all its objects — two obedient seats can build different-sized reading sets.
Exact replacement, inserted immediately after the quoted passage: "**An object is UNBOUND unless the §2c census contains a printed relation that fixes it, cited by census row in the C2 ledger; otherwise it is BOUND and its kind is not instantiated. Each kind is instantiated AT MOST ONCE, in a single reading that sets every one of its unbound objects together — that joint setting is the kind's one named assumption. Two seats applying this rule to the same census construct the same reading set.**"

FINDING 4 — THE FALSIFIER: SOUND (numbers as required)

THE NUMBER: C6 can record FAIL in 4 of the 6 declared outcome classes — engaged initially on 2 (`DYM_FLOOR_DERIVED`, `DYM_FLOOR_UNDERDETERMINED`), travelling into `DYM_SOURCE_BLOCKED` when condition 3 remains UNDECIDED after its fallback, and into `R3D_NO_CLASS` when an engaged C6 meets a persistent control failure. It is NOT_RUN on classes 3 and 4 only. The document's §5a states exactly this; I verified the routing clauses agree.

Per condition, with concrete inputs:
1. Observable magnitude. CAN PASS: floor = 2.1764343e-8 kg, dimension mass. CAN FAIL only on a malformed filing: seat files the dimensionless 0.42 as its "floor". On any valid filing it cannot fail — this is the condition that cannot fail substantively, and the document already discloses it as ENTAILED. No condition is unfailable on every path; condition 1 is unfailable on every valid path.
2. Constants trace. CAN PASS: floor = sqrt(ħc/G) — ħ, c, G all terminate in §2b. CAN FAIL: floor = 3·sqrt(ħc/G) with the 3 introduced as "the simplest form" — terminates in no manifest equation and no §2b entry.
3. No free normalisation. CAN PASS: M = sqrt(ħc/G) — simplification leaves only §2b symbols. CAN FAIL: M = c²r₀/(2G) with r₀ a non-§2b symbol surviving simplification (this is exactly the probe's negative-control expression).
4. No fixity assumed. CAN PASS: every held-constant quantity has a deriving passage in the C2 artefact (r₀ derived from a reproduced printed relation). CAN FAIL: r₀ held at 1e-3 r_g by choice with no deriving passage in the artefact.
5. A measurement could falsify it. CAN PASS: floor 1.0e15 kg — no overlap with any comparator interval. CAN FAIL: 2.0e11 kg (overlaps Hawking [1.729e11, 5.190e11]); 2.1764343e-8 kg (overlaps the Planck row [2.176433e-8, 2.176435e-8]); 5.0e30 kg (overlaps TOV [4.375e30, 5.768e30]). All comparator arithmetic re-derived by hand above; the outward-rounded intervals contain their computed values, and the widened degenerate Planck row contains m_P = 2.1764343e-8 — the V16 escape is closed on the numbers, not just on the wording.

FINDING 5 — RE-RUN GUARD: SOUND

§6 states K6's outcome may not be assumed to repeat, and nothing in the limb structure, the partition, or the controls presupposes the K6 result. The positive class is genuinely reachable: printed relations fixing the core scale with the completion-free reading and every instantiated completion agreeing on one floor lands in class 1, and no clause routes that case elsewhere. No replacement.

FINDING 6 — FAIRNESS: ONE OPEN ITEM, PREVIOUSLY ESCALATED — I CONCUR AND DO NOT RE-ESCALATE

Every operative clause I traced holds the "unreproduced from the stated inputs" form: the limb-A exit, class 3's table row and definition, class 4's table row, and class 4's inconsistency clause ("report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"). §0's "the source never supplies" was adjudicated COSMETIC in §8o (non-operative, redacted from seat packets) and I take no different view.

The open item is the outcome vocabulary itself. Verbatim: "3. **DYM_NO_SIZE_MASS_RELATION**" and "4. **DYM_NO_POSITIVE_FLOOR**". The tokens assert absence while their definitions disclaim it; five independent seat-findings have said so, and the document escalates it each time as Duho's ruling. I concur on the merits and record the concurrence so this gate is not read as acquittal; the replacement remains the escalated …_UNREPRODUCED rename, which is not mine to order. No new finding.

FINDING 7 — STALL: SOUND, SUBJECT TO REPAIR 2b

Every terminal path files a declared class: limb-A exit → 3; limb-B partition → 1/2/4; unread/mismatched/UNRESOLVED/undecidable-after-fallback → 5; persistent control failure → 6; C3 FAIL → recomputed partition → 1/2/4 with the removed relation printed; seat splits convened on any terminal disagreement, with three-way or undecidable splits → 5 and the adjudicated-to-non-clean case routed through control re-runs → 6 or the agreed class. The one residual hole is Repair 2b's: a probe exit of 2 on a C3-reached class currently maps to nothing; with that replacement the stall analysis is closed. No additional replacement.

R3D_V26_KIMI_COMPLETE
