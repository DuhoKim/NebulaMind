All checks are in. Here is the gate report.

ACCESS_SHA=3e5b6979930535e5cb09c8eb6d3f6f3c7fefd47c6df8697cf2d384124c753234
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — one substantive defect: a constructed result that two clauses route to two different classes.

  The class predicates themselves are sound. Class 3 is filed only from limb A; classes 1, 2, 4 partition limb B by the disjoint P/Z/I sets (P empty => 4; P non-empty with disagreement or Z/I non-empty => 2; P non-empty, agreeing, Z and I empty => 1). That trichotomy is total and exclusive over a non-empty reading set, and the reading set is always non-empty in limb B (the completion-free derivation always exists). Classes 5 and 6 are gated behind "no scientific class is fileable", and class 6 is explicitly subordinated to class 5 ("only after ruling out DYM_SOURCE_BLOCKED"). An INCONCLUSIVE result is genuinely reachable three ways: class 2 (completion-free yields 10 kg, one admissible completion yields 20 kg), class 4 (P empty), and class 5 (the study waits; explicitly "not a scientific verdict"). I could not construct a result fitting NO class.

  I CAN construct a real result fitting TWO classes, and it is reachable:

  Verbatim: "**When the probe files `FAIL`, the circular relation is removed from every reading that used it, the partition of §4 is recomputed on the remaining readings, and the run files whichever class the recomputed partition selects — with the removed relation and the probe's captured output printed in the filing.**"

  Defect: this clause collides with class 6's rule, verbatim: "**A scientific class may be filed only from a seat report in which every reached control passed**". Construction: a seat's relations file contains a completion that alone fixes a determinate floor — the probe's own positive control, injected `M = 3*sqrt(hbar*c/G)`, which the seat wrongly admitted as an "order-unity coefficient" reading. The probe exits 1, `C3_DELETION_PROBE=FAIL`. Per the C3 clause the seat removes the relation, recomputes, and files the recomputed scientific class (say class 1: the remaining readings agree on one floor, Z and I empty). But the report now carries a reached control with token FAIL, so it is not control-clean; under class 6's rule, when both seats so agree, the third seat re-runs C3 once per report, the deterministic probe fails again, and `R3D_NO_CLASS` is filed. Two clauses, one reachable path, two different terminal classes — and no stated rule tells an adjudicator which governs. The trigger is precisely the seat-construction error the probe exists to catch (a §2-admissibility violation), so the ambiguity sits exactly where routing must be least ambiguous. The C6 carve-out in class 1 ("a breaker condition failing on its own decision rule is a substantive physics result, not a control failure") has no C3 parallel.

  Exact replacement: "**When the probe files `FAIL`, the circular relation is removed from every reading that used it, the partition of §4 is recomputed on the remaining readings, and the run files whichever class the recomputed partition selects — with the removed relation and the probe's captured output printed in the filing. A probe FAIL handled by this recomputation is a substantive circularity finding, not a control failure: it does not engage the control-clean rule of `R3D_NO_CLASS`, and the filing records `C3_DELETION_PROBE=FAIL` with the removed relation and the recomputation printed.**"

2. CONTROLS — one substantive defect; plus the required C3 execution report.

  Assertion vs artefact: no control is passable by bare assertion. C0 (exhibition table), C1 (printed digests), C2 (full printed ledger plus census reconciliation counts), C3 (captured probe output), C4 (printed algebra plus premise list), C5 (printed command outputs), C5b (complete per-path table), C6 (decision-rule artefacts per condition plus the four-row comparison table) each name a printed artefact and each states that a claimed pass without it fails. Each control names an exact token with PASS|FAIL|NOT_RUN (all eight listed in §9), and unreached controls are handled explicitly — per-class dispositions on classes 2, 3 and 4, and the early-terminal-block rule distinguishing controls engaged before a block from those never engaged.

  C3 deletion probe — did I run the literal command exactly as printed?
  (a) YES, I executed it, twice, verbatim, character-for-character as printed in the code block.
  (b) First run from working directory `/`, second run from working directory `/Users/duhokim` — neither is the lane directory, so the no-dependence-on-working-directory claim was exercised.
  (c) Exact stdout, both runs byte-identical:

      target            : M
      retained (all)    : ['eq_src', 'inj_ok']
      DELETED (pinned)  : ['eq_src']
      injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
      §2b constants     : ['hbar', 'c', 'G']
      with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
      pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
      VERDICT: no unique floor without the source-pinned equations -> not circular
      C3_DELETION_PROBE=PASS

      Exit code 0 on both runs. Caveat, stated plainly as this lane's own standard requires: the designated input `_c3_relations.json` already existed at the absolute path and already held the negative control (`inj_ok`); I ran the command against it as it stood and wrote nothing. What I verified is that the printed line executes, resolves both absolute paths from two unrelated working directories, produces the expected negative-control output, and exits 0. I did not verify a seat-authored relations file end-to-end.

  I also executed the probe digest check: `shasum -a 256 .../r3d_c3_deletion_probe.py` computed `7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874`, which matches the C3 pin exactly (match: yes). And two of the three C5 harness commands executed: `/usr/bin/python3 --version` printed `Python 3.9.6`; `shasum -a 256 /usr/bin/python3` computed and printed a digest without error. The third, `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"`, I could NOT execute: this session's approval layer blocks inline `-c` interpreter invocations, and I do not report a run I did not perform. That is a limit of my execution mode, not of the document — the same class of limit the V15 kimi seat reported, and the probe script itself (which does symbolic solving) ran cleanly under `/usr/bin/python3`.

  Defect — the scope rule fails a path C5 mandates. Verbatim: "**IN SCOPE:** this lane's own reading tree (`../bhu-reading-20260823/sources/`); **this preregistration**; **the committed script `r3d_c3_deletion_probe.py` and the JSON input the seat writes for it**; and **the seat's own working directory and artefacts**." combined with C5b's rule, verbatim: "Any `OUT_OF_SCOPE` row **fails** the control."

  Defect: C5 commands the seat to execute `shasum -a 256 /usr/bin/python3` and C3 invokes `/usr/bin/python3` absolutely; the interpreter's bytes are necessarily read by the digest command. No §9 scope clause names `/usr/bin/python3`, and the fallback clause is "any path not named above" is OUT OF SCOPE. A conforming seat applying the clauses mechanically — which is what C5b's per-path clause-citation requirement demands — must mark the interpreter OUT_OF_SCOPE and fail its own C5b, in both seats, deterministically, routing every run to `R3D_NO_CLASS` via the persistent-failure rule. This is the V20 defect shape one path over: a control that fails on a path its own design requires. Both V26 seats were steered to the C3 invocation and neither traced C5's mandated paths into C5b's table.

  Exact replacement: "**IN SCOPE:** this lane's own reading tree (`../bhu-reading-20260823/sources/`); **this preregistration**; **the committed script `r3d_c3_deletion_probe.py` and the JSON input the seat writes for it**; **the pinned interpreter `/usr/bin/python3`, executed and hashed under C5 and invoked by C3**; and **the seat's own working directory and artefacts**."

3. CIRCULARITY — sound, no replacement.

  The pattern is barred as evidence for inclusion, exclusion or outcome selection and enters only at C6, after a positive-floor class is reached on other grounds. The enumeration channel is closed mechanically: every non-blank line carries a disposition, the per-source reconciliation count must balance or C2 fails, and every numbered or displayed equation is its own row — so a contrary relation cannot be quietly omitted; omission shows up as arithmetic. Exclusion is only via predeclared reason codes demonstrated by source text, and UNRESOLVED forces `DYM_SOURCE_BLOCKED` rather than absence. Class selection is mechanical over P/Z/I, and the reading set is finite and rule-constructed (completion-free plus at most four one-kind readings), so the pattern cannot reach the class through an unbounded completion domain — the V26 repair holds. One residual judgement channel exists and is routed rather than silent: whether a printed relation "fixes" an object (BOUND vs UNBOUND) is a judgement two seats could split on (e.g. `r_0^2 = 3/Lambda` with Lambda itself not in §2b), but any such split produces different reading sets, different classes, and a convened third seat — it cannot resolve in the pattern's favour unexamined. Observation, not defect: class 2's stated reason for `C3_DELETION_PROBE=NOT_RUN` ("the probe's trigger is a unique floor ... this class files no unique floor") describes the filed quantity, while the probe tests a property of the relations file; the disposition nonetheless stands because §2's admissibility rule bars any sole-source completion and the C2 ledger prints every admitted completion with its operating relation, so a circular completion inside a multi-floor filing remains inspectable. No replacement.

4. THE FALSIFIER — sound, no replacement. The number is 4: `C6_BREAKER_TEST=FAIL` can be recorded in 4 of the 6 declared classes — `DYM_FLOOR_DERIVED` (substantive failure, floor still filed), `DYM_FLOOR_UNDERDETERMINED` (per-floor evaluation), `DYM_SOURCE_BLOCKED` (condition 3 UNDECIDED after its fallback, or any block after an engaged FAIL), and `R3D_NO_CLASS` (persistent control failure after engagement). On classes 3 and 4 C6 is never engaged and is `NOT_RUN`, because no positive floor exists to test. I traced each terminal class against the document's clauses and the count 4 is correct.

  Per condition, with a concrete input each way:

  Condition 1 (observable magnitude). CAN PASS: filed floor 5.0e-8 kg — dimension is mass, passes. CAN FAIL only on a malformed filing: a seat files `M_min/m_P = 0.5`, a dimensionless ratio, as its floor — dimensional classification fails. On every VALID path it cannot fail, because class membership requires a greatest lower bound of an allowed MASS set. The document discloses exactly this (`ENTAILED`) rather than claiming five substantive failures. So: named plainly, condition 1 is the condition that cannot fail on any valid path; counting malformed-filing paths, no condition is unfailable on literally every path. The disclosure is the correct handling.
  Condition 2 (constants trace). CAN PASS: floor `c^2 r_0 / (2G)` with `r_0^2 = 3/Lambda` a printed manifest relation and Lambda terminating in a manifest equation; G, c in §2b. CAN FAIL: floor `alpha * m_P` where alpha was introduced as "the simplest form" with no deriving passage and alpha not in §2b — the chain terminates in "we choose", FAIL.
  Condition 3 (no free normalisation). CAN PASS: floor `sqrt(hbar*c/G)` — free-symbol run recovers the number with only §2b symbols, PASS. CAN FAIL: floor `k * sqrt(hbar*c/G)` with k a free non-§2b parameter — the simplified expression contains k, FAIL (and the UNDECIDED-after-fallback branch also records FAIL and files class 5).
  Condition 4 (no fixity assumed). CAN PASS: `r_0` held constant with the deriving passage `r_0^2 = 3/Lambda` reproduced in the C2 artefact — every fixity row has a source line, PASS. CAN FAIL: `r_0` held constant "for definiteness" with no manifest derivation — FAIL.
  Condition 5 (measurement could falsify). CAN PASS: floor 1.0e15 kg — overlaps no comparator interval (my check: True), PASS. CAN FAIL on every numerical row: floor 2.0e11 kg overlaps Hawking `[1.729e11, 5.190e11]` (checked: True); floor m_P = 2.176434342051127e-8 kg overlaps Planck `[2.176433e-8, 2.176435e-8]` (checked: True — the widened degenerate row contains its own value); floor 5.0e30 kg overlaps TOV `[4.375e30, 5.768e30]` (checked: True).

  I recomputed every comparator number from §2b. Intermediates: `hbar*c = 3.161526771559562e-26`; Gregorian year `= 31556952 s`; `t_0 = 4.35391266744e17 s` (printed 4.3539e17); `m_P = sqrt(hbar*c/G) = 2.176434342051127e-8 kg`, inside the printed row. Hawking lower `(t_0 hbar c^4 / 5120 pi G^2)^(1/3) = 1.7298262e11 kg` at exact t_0, `1.7298245e11` at the printed t_0 — both round down to the printed `1.729e11`. Upper `3.0 x lower = 5.1894786e11`, rounds up to the printed `5.190e11`. TOV `2.2 x 1.98892e30 = 4.375624e30` (printed down `4.375e30`), `2.9 x 1.98892e30 = 5.767868e30` (printed up `5.768e30`). Every endpoint rounds outward as the convention states; no comparator excludes its own computed value.

5. RE-RUN GUARD — sound, no replacement. §6 forbids assuming K6's outcome, and the positive class is genuinely reachable by construction: if the printed relations fix every object (all BOUND, so no completion is instantiated) and the completion-free reading yields a unique positive floor, the reading set is a singleton in P with Z and I empty — class 1 fires. Nothing in the partition, the controls, or C6 presupposes the K6 result; C0 must exhibit the class-1 input before freeze.

6. FAIRNESS — sound, no replacement. Every operative clause holds "unreproduced from the stated inputs": limb A's exit text, class 3's table row and clause, class 4's table row and clause, and the inconsistency sub-clause ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"). I swept for absence-claims in operative text and found none. The `DYM_NO_*` tokens are class labels, not operative clauses; their overstatement is documented in §8m–§8s as an escalation reserved to Duho's authority (rename = redefinition), now raised by seven independent seat-findings. I concur with the escalation's merits and note it is correctly carried as open; it is not a new defect and I do not re-flag it as one.

7. STALL — sound, no replacement. Every terminal path has a fileable class: limb-A exit (3), the total P/Z/I partition (1/2/4), unreadable source or digest or probe-pin mismatch or UNRESOLVED or undecidable-after-fallback or unresolvable split (5), persistent control failure after ruling out 5 (6), and the seat-split machinery guaranteeing "every terminal path files exactly one declared class". Symbolic operations are double-capped with per-row fallbacks that name their proposition. The one routing pathology I found is the finding-1 case, which is an OVER-assignment (two clauses, two classes), not an under-assignment — a run on that path does not stall classless, it files ambiguously; the repair is recorded under finding 1.

Summary: the design's spine — census, partition, bounded completions, falsifier polarity, comparator arithmetic, control vocabulary, re-run guard, fairness, stall coverage — is sound, and every literal command I could execute, executed as printed (C3 verbatim from two unrelated directories, exit 0 both; probe digest matches its pin; C5's two runnable commands clean; the inline `-c` command is blocked by my mode, not by the document). Two substantive defects remain, both small and both repaired with exact text above: the C3-FAIL recomputation clause conflicts with class 6's control-clean rule on a reachable error path, and the C5b scope rule has no clause covering the interpreter C5 mandates hashing.

R3D_V27_KIMI_COMPLETE
