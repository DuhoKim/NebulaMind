ACCESS_SHA=bc79306d52e446706b7517d6a585c3daabea9878ed9a1d1b7f421c5509427ed6
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

SOUND. The operational decision procedure is exhaustive and mutually exclusive. Limb A alone files class 3. Limb B reaches a finite, non-empty reading set (the completion-free reading plus at most four bounded completion readings), and the disjoint P/Z/I partition files exactly one of classes 1, 2, or 4. Classes 5 and 6 are ordered by the explicit requirement that class 6 first rule out class 5, while the seat-split and persistent-control rules route terminal failures.

I could construct neither a result fitting two classes nor a result fitting none. In particular, the physical implication “no printed mass constraint implies zero remains permitted” does not create a filing overlap because class 3 is a limb-A procedural exit and class 4 is limb-B-only.

An inconclusive scientific result is genuinely reachable: for example, let the completion-free reading permit M approaching zero while one admissible completion, operating on a printed relation, yield inf M = 10 kg. Then P and Z are both non-empty and class 2, DYM_FLOOR_UNDERDETERMINED, files. Non-scientific inconclusiveness is also reachable through an unreadable or digest-mismatched pin, an UNRESOLVED proposition, an undecidable bounded procedure after fallback, or an unresolved terminal seat split, all routed to class 5.

2. CONTROLS

SOUND. C0, C1, C2, C3, C4, C5, C5b, and C6 each emit an exact PASS/FAIL/NOT_RUN code. Each claimed pass requires a printed artefact: C0’s exhibition table; C1’s computed digests and repr output; C2’s census and full ledger; C3’s captured execution; C4’s algebra and premise list; C5’s outputs, exit codes, and frozen-value comparisons; C5b’s per-path scope table; and C6’s per-condition artefacts and completed comparator table. Thus no control can pass by assertion alone.

Unreached controls are handled explicitly: later controls after an early terminal block are NOT_RUN; limb A specifies C3, C4, and C6 as NOT_RUN; class 2 specifies C3 as NOT_RUN; class 4 specifies C3 and C6 as NOT_RUN; and controls already engaged retain their actual status.

The literal commands that the document directs a seat to execute are syntactically executable as printed: the C3 invocation uses absolute interpreter, script, and input paths; its digest command is complete; and the three C5 commands are complete and use the same absolute interpreter.

C3 DELETION-PROBE EXECUTION

(a) Yes. I executed the literal C3 command exactly as printed:

    /usr/bin/python3 /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/r3d_c3_deletion_probe.py /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828/_c3_relations.json

(b) Working directory:

    /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828

(c) Exact stdout:

    target            : M
    retained (all)    : ['eq_src', 'inj_ok']
    DELETED (pinned)  : ['eq_src']
    injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
    §2b constants     : ['hbar', 'c', 'G']
    with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    VERDICT: no unique floor without the source-pinned equations -> not circular
    C3_DELETION_PROBE=PASS

Exit code: 0.

3. CIRCULARITY

SOUND. The lane-authored pattern cannot select which source relations enter the evidence, which are excluded, which completions are tested, or which scientific class files. The census accounts for every non-blank line and separately enumerates every numbered or displayed equation; exclusions require a predeclared code and supporting source text; disagreement becomes UNRESOLVED rather than absence; and the finite completion set is fixed before classification. A contrary relation therefore cannot be quietly omitted without breaking the reconciliation, equation list, or ledger artefact. The pattern enters only through the pinned C6 conditions after a positive-floor class has independently been reached. C3 additionally removes and exposes an injected relation that alone preserves a determinate floor, then recomputes the partition.

4. THE FALSIFIER

NUMBER: 4 declared outcome classes can terminally record C6_BREAKER_TEST=FAIL: DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, DYM_SOURCE_BLOCKED after condition 3 remains undecided, and R3D_NO_CLASS after an engaged C6 is followed by a persistent control failure. C6 is initially engaged from 2 classes.

Condition 1: PASS is possible with a filed floor M_min = 10 kg. A substantive FAIL is impossible on every valid path because positive-floor membership and the study question require the filed quantity to be a mass. It can reject the malformed input M_min = r0/r_g (dimensionless), but that input is not a valid member of a positive-floor class. Condition 1 is the one condition that cannot fail on any valid path; the document correctly marks it ENTAILED.

Condition 2: PASS is possible for M_min = 3 sqrt(hbar c/G) when 3 and the relation are printed in a manifest source and hbar, c, and G terminate in §2b. FAIL is possible for M_min = alpha sqrt(hbar c/G) when alpha terminates only in “we choose alpha = 3” and has no manifest derivation.

Condition 3: PASS is possible for M_min = 3 sqrt(hbar c/G), whose simplified expression has no non-§2b symbol. FAIL is possible for M_min = alpha sqrt(hbar c/G) with free alpha surviving.

Condition 4: PASS is possible for the same Planck-scale expression when every held-constant quantity has a deriving manifest passage (or when there are no additional held-constant quantities). FAIL is possible when r0 is fixed at 1 m by choice and no manifest passage derives that fixity.

Condition 5: PASS is possible for the point floor 1.0e15 kg, which overlaps none of the three numerical comparator intervals. FAIL is possible with 2.1764343e-8 kg (Planck comparator), 2.0e11 kg (Hawking comparator), or 5.0e30 kg (stellar-collapse comparator).

5. RE-RUN GUARD

SOUND. The design explicitly forbids assuming K6’s outcome repeats and leaves DYM_FLOOR_DERIVED genuinely reachable. For example, if every consistent admissible reading yields the same completion-free floor of 10 kg, with Z and I empty, class 1 files. Nothing in the routing forces a prior negative result.

6. FAIRNESS

DEFECT. The required “unreproduced from the stated inputs” wording is used in the operative prose, but it is not held in the operative outcome codes themselves. Those codes travel independently of their qualifying definitions and assert nonexistence.

Verbatim sentence:

“3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Defect: the token DYM_NO_SIZE_MASS_RELATION says “NO” even though the operative definition permits only the narrower reproduction finding.

Exact replacement:

“3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Verbatim sentence:

“4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above.”

Defect: the token DYM_NO_POSITIVE_FLOOR asserts nonexistence even though the class predicate and required report say only that a positive floor was unreproduced from the stated inputs.

Exact replacement:

“4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above.”

The two replacements must be applied consistently to every operative occurrence of the old tokens, including the §4 table, C6 applicability/NOT_RUN clauses, C6’s outcome definition, and the seat-split/control-routing text. Historical discussion in §8 may retain old tokens when clearly describing earlier text.

7. STALL

SOUND. Every symbolic operation has a 120-second cap and a second bounded fallback; absence, timeout, parse failure, or failure to decide routes to DYM_SOURCE_BLOCKED. Early blocks explicitly mark later unengaged controls NOT_RUN. Seat disagreement, three-way disagreement, persistent control failure, C3 exit 2, and C6 condition-3 undecidability all have terminal routing. I found no reachable state with no fileable class.

R3D_V28_GATE_COMPLETE
