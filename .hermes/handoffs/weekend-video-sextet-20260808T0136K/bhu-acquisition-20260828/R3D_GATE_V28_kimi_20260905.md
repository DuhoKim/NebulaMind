All execution checks are complete. Writing the adjudication.

ACCESS_SHA=bc79306d52e446706b7517d6a585c3daabea9878ed9a1d1b7f421c5509427ed6
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — SOUND, no replacement.

Exhaustive and mutually exclusive as an operational decision procedure. Class 3 is filed only from limb A; classes 1, 2, 4 only from limb B, where they partition the P/Z/I space of admissible readings. The partition is total: the reading set is never empty (the completion-free derivation is always a member), so P empty forces Z or I non-empty (class 4); P non-empty splits disjointly on the agreement predicate (all of P agree AND Z,I both empty → class 1; otherwise → class 2). P and Z are disjoint by construction: a strictly positive greatest lower bound contradicts "no positive lower bound follows." I tried to construct double-fits and orphans and could not make one stick:
  (a) P empty with Z and I both empty — impossible, the reading set is non-empty.
  (b) A reading in both P and Z — impossible, the two predicates are contradictory for a non-empty solution set.
  (c) The class-3 state (nothing printed) entails class 4's predicate (completion-free reading lies in Z), exactly as the document's own finding states — but the two are disjoint by limb, which the document says explicitly: "Class 3 is reached ONLY from limb A; classes 1, 2 and 4 ONLY from limb B. They are therefore disjoint by the limb that files them, not by any predicate." A procedural disjointness is a valid decision procedure; a seat either exits limb A or enters limb B, never both.
  (d) A consistent reading whose only solutions are negative-mass (empty physical mass set) — under the document's solution-set reading of "allowed mass set," no positive lower bound follows from it, so it lands in Z. Not an orphan.
  (e) The C3-FAIL recomputation path: the completion-free reading never used the removed injected relation, so it stays in P; the recomputed partition can select only class 1 or class 2, and "the run files whichever class the recomputed partition selects" covers both. Class 3 is unreachable from recomputation (limb-A-only), class 4 is unreachable (P stays non-empty). No orphan.
INCONCLUSIVE is genuinely reachable: DYM_SOURCE_BLOCKED (digest mismatch, UNRESOLVED row, undecidable-after-fallback, unresolved three-way split) and R3D_NO_CLASS (persistent control failure after two attempts) are both constructible terminal states, and the document bars reporting either as a scientific verdict.

2. CONTROLS — ONE DEFECT, replacement below. First, the required execution report.

C3 deletion-probe run, as required by this gate:
  (a) YES — I executed the literal command exactly as printed in the document, copied character-for-character (interpreter, script and argument all absolute; I added nothing and substituted nothing).
  (b) Working directory: /Users/duhokim — deliberately neither the lane directory nor the repository root, to exercise the no-dependence-on-working-directory claim.
  (c) Exact stdout and exit code:

    target            : M
    retained (all)    : ['eq_src', 'inj_ok']
    DELETED (pinned)  : ['eq_src']
    injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
    §2b constants     : ['hbar', 'c', 'G']
    with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    VERDICT: no unique floor without the source-pinned equations -> not circular
    C3_DELETION_PROBE=PASS

  exit code 0.

Scope of what this run proves: the relations file currently sitting at the pinned path holds the negative control (injected r0 = 2GM/c^2, M left free), so my run verifies executability, cwd-independence and the exit-code mapping — it is not a run against a fresh seat-written relations file. I executed the script without reading its contents or the JSON's contents, and I read no other file in the directory. I also executed the probe-digest check from C3: shasum -a 256 of the script returns 7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874 — equal to the pin. Match.

C5 harness, also executed: /usr/bin/python3 --version → "Python 3.9.6", exit 0 (equals the frozen value); shasum -a 256 /usr/bin/python3 → b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9, exit 0 (equals the frozen value). Disclosure: my own tooling blocks the literal "python3 -c" form, so the sympy check was run as an equivalent one-line temp script executed by the same pinned /usr/bin/python3 — it printed 1.14.0, equal to the frozen value. The other two C5 commands ran literally. All three frozen C5 values reproduce on this machine.

Assertion check: no control is passable by bare assertion. C1 requires printed digests (with the anti-transcription rule extended to C1 in §9); C2 requires the full printed ledger ("a summary assertion fails"); C3 requires the captured output of the deleted state; C4 requires printed algebra plus premise list; C5 requires captured outputs, exit codes and comparisons against frozen values; C5b requires the complete per-path table; C6 names an artefact per condition and makes an incomplete or missing four-row table a loud control FAIL; C0's exhibition table is the artefact and an unexhibited class blocks the freeze. Each control emits exactly one named token with PASS|FAIL|NOT_RUN, both in its own clause and in §9's vocabulary. Unreached controls are handled: NOT_RUN only where explicitly unreached, the early-block rule distinguishes controls engaged before a block (actual result) from those never engaged (NOT_RUN), and classes 3 and 4 enumerate their dispositions. One looseness noted, not a defect: C3's prose says "delete the source-pinned field equations" while the pinned probe deletes all SOURCE_PINNED records; the pinned schema governs and the probe's behavior is the stronger test.

DEFECT 2-A (controls). Verbatim, from class 2:
  "Record `C3_DELETION_PROBE=NOT_RUN` on this class: the probe's trigger is a unique floor surviving on an injected relation alone, and this class files no unique floor — the seat chooses none — so the probe has no target."
Verbatim, from C3:
  "the run files whichever class the recomputed partition selects — with the removed relation and the probe's captured output printed in the filing" and "the filing records `C3_DELETION_PROBE=FAIL` with the removed relation and the recomputation printed."
The defect: a reachable path on which two clauses command two different tokens for the same control in the same filing. Initial partition selects class 1 → the probe runs → FAIL (an injected relation alone determines the floor) → the relation is removed from every reading that used it → the recomputed partition selects class 2 (the completion-free reading remains in P with floor F; the stripped completion reading now permits zero, so Z is non-empty). On that filing, class 2's clause commands C3_DELETION_PROBE=NOT_RUN while C3's recomputation clause commands C3_DELETION_PROBE=FAIL. §9 requires every control to emit exactly one token, and permits NOT_RUN only where the document explicitly makes the control unreached — here C3 was reached and has an actual result, so NOT_RUN is not merely doubly commanded, it is false of the run. Two obedient seats can file the same class 2 carrying different C3 tokens, and §9's split rule — which convenes on class, C6 outcome, floors or families, not on control tokens — convenes nobody. This is the document's own scar family (V11 R2's FAIL/NOT_RUN double-assignment; V26 2b's C3-on-class-2; V28 F1's C3/class-6 collision) with one untraced corner remaining: the class-2 clause (V26) and the recomputation clause (V27/V28) were each repaired pairwise and never traced against each other. It does not change which class is filed; it corrupts the control-status record on a path the design itself treats as live.
EXACT REPLACEMENT for the class-2 sentence:
  "Record `C3_DELETION_PROBE=NOT_RUN` on this class when the run reaches it directly: the probe's trigger is a unique floor surviving on an injected relation alone, and a run reaching this class directly files no unique floor — the seat chooses none — so the probe has no target. If this class was instead selected by the recomputed partition after a probe FAIL, C3 was engaged and the filing records `C3_DELETION_PROBE=FAIL` with the removed relation and the recomputation printed, as C3's own clause commands; a control that ran is never recorded NOT_RUN."

3. CIRCULARITY — SOUND, no replacement.

The lane's pattern cannot reach the evidence through any of the three channels named in the brief. Enumeration: the census is line-mechanical (every non-blank line carries a disposition, per-source reconciliation counts must be equal, and every displayed equation is its own row), so a contrary relation cannot be quietly omitted — omission shows up as arithmetic, and the eleven English terms are explicitly demoted to a printed convenience that may not justify any row. Exclusion: only the four predeclared reason codes, each demonstrated from verbatim source text and printed with the row, so a misapplied code is visible to the second seat and to audit rather than silent. Selection: the class is chosen by the mechanical P/Z/I partition over a finite reading set (exactly the four named completion kinds, each instantiated at most once, only where a census row fails to fix its object), so the pattern cannot steer which class is filed. The pattern enters only as C6's pinned V2 condition text, and evaluation against any unpinned text fails C6. §0 and §8 carry the de-patterning and the prohibition is restated operatively in §2c.
Two observations, neither requiring repair. (i) The four comparator conventions (5120π, the 3.0 factor, the [2.2, 2.9] M☉ span, the Gregorian year) are lane-asserted and are the one place lane judgment touches the falsifier; they are frozen, printed in the condition-5 artefact, and the widening choices (3.0× upward, outward rounding) make a counterexample harder to certify, not easier — the conservative direction, and inspectable, which is the design's stated trade. (ii) Under the V16/V26 admissibility rule — a completion may not be the sole source of a mass bound — a probe FAIL is reachable only when an inadmissible sole-source completion was admitted to the JSON, i.e., through a seat's screening error. C3 therefore now functions as an integrity check on the seat rather than a plausible physics outcome; the "SUBSTANTIVE CIRCULARITY FINDING" framing slightly overstates its reach, but the machinery built for the path is coherent, and with defect 2-A repaired it is also consistent.

4. THE FALSIFIER — SOUND, no replacement. The document's numbers survive an independent count.

THE NUMBER: C6 can record FAIL in 4 of the 6 declared outcome classes — DYM_FLOOR_DERIVED and DYM_FLOOR_UNDERDETERMINED (where it is initially engaged), plus DYM_SOURCE_BLOCKED (engaged C6 followed by condition-3 UNDECIDED after fallback) and R3D_NO_CLASS (engaged C6 followed by a persistent control failure, including C6's own incomplete-table control failure). It is initially engaged on 2 of 6, and is NOT_RUN on DYM_NO_SIZE_MASS_RELATION and DYM_NO_POSITIVE_FLOOR, which produce no number to test. This matches §5a's two-number statement, and the two-number statement is the correct way to say it.

Per condition, with a concrete input in each direction:
  Condition 1 (observable magnitude, dimension mass). CAN PASS: filed floor 1.0e15 kg — dimension mass. CAN FAIL: only on a malformed filing — a seat files "0.62" (dimensionless) or files a length r0 as its floor; §5 catches it because the conditions are evaluated on the quantity actually filed. THIS IS THE CONDITION THAT CANNOT FAIL ON ANY VALID PATH: every positive-floor class requires a mass, so a correctly filed floor passes a dimensional test by entailment. The document discloses this — §5a marks it ENTAILED, counts 4 of 5 substantively failable, and C0 carries the ENTAILED route (prove the entailment, exhibit a malformed filing it rejects). A disclosed entailment is a property, not a defect.
  Condition 2 (constants trace). CAN PASS: a floor of form c^2·sqrt(3/Λ)/(2G) where Λ terminates in an equation of a §2a manifest source and c, G lie in §2b. CAN FAIL: a floor containing a coefficient terminating in "we choose / the simplest form" — e.g., a 4π introduced without derivation — or a terminus outside the manifest; the row makes an outside-manifest terminus fail exactly as a "we assume" terminus fails.
  Condition 3 (no free normalisation). CAN PASS: floor 3·sqrt(ħc/G) — substituting independent symbols for every non-§2b parameter and simplifying once leaves only §2b constants; the printed number is recovered with nothing chosen. CAN FAIL: floor c^2·r0/(2G) with r0 unbound by the census — the simplified expression still contains r0, so the number is recoverable only after choosing r0. The UNDECIDED branch (fallback absent/times out/cannot parse) records C6=FAIL and files DYM_SOURCE_BLOCKED; the double-assignment repaired at V11 is closed.
  Condition 4 (no fixity assumed). CAN PASS: every quantity held constant (e.g., r0) has a deriving passage reproduced in the frozen C2 artefact. CAN FAIL: the prediction is rigid only because r0 is held constant by the seat's choice with no manifest derivation — that row fails.
  Condition 5 (a measurement could falsify it). CAN PASS: floor 1.0e15 kg — I executed the comparison; it overlaps no comparator interval (also 3.0e30 kg → PASS with NEAR_MATCH to the TOV row, reported and never decisive). CAN FAIL: floor 2.0e11 kg → overlaps the Hawking row [1.729e11, 5.190e11] (executed); floor m_P → overlaps the Planck row (executed: m_P = 2.1764343421e-8 kg lies inside [2.176433e-8, 2.176435e-8]); floor 5.0e30 kg → overlaps the TOV row [4.375e30, 5.768e30] (executed). The disjunctive decision rule fires in both directions with no other precondition on the pass. I reproduced all comparator arithmetic from §2b with intermediates: Gregorian year 31,556,952 s, t0 = 4.353913e17 s (Julian 4.3540e17, matching the parenthetical), ħc = 3.16152677e-26, m_P = 2.1764343421e-8, Hawking lower (t0·ħ·c^4/(5120π·G^2))^(1/3) = 1.7298262e11, upper 3.0× = 5.1894786e11, TOV ends 4.375624e30 and 5.767868e30 — every printed interval contains its computed value under outward rounding, the degenerate-row widening holds, and the V16 Planck-row escape is closed.

5. RE-RUN GUARD — SOUND, no replacement.

§6 states the guard operatively: "K6's outcome may not be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`." §0 claims only that the branch is "in principle able" to fix a mass scale and subordinates the question to the census and the C2 ledger. The positive class is genuinely reachable as a design matter: a printed relation binding core scale to mass, with the completion-free reading and all four completion readings agreeing on one floor and Z, I empty, files class 1 by the mechanical partition — no clause presupposes K6's UNDERDETERMINED outcome, and the comparator conventions do not preclude any floor value from reaching class 1 (they act only afterward, at C6).

6. FAIRNESS — ONE DEFECT, replacement below.

Every operative clause holds the wording. Limb A: "report that a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs." Class 3's row and class 4's row use the same form; class 4's inconsistency clause reads "report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"; §6 states the rule itself. The V18 class-4-row and V19 class-3-row repairs are both in place.

DEFECT 6-A (the escalated token defect — I confirm it on the merits, eighth finding). Verbatim, §6:
  "The record's wording for any negative finding is **"unreproduced from the stated inputs," not "error."**"
Verbatim, §4's table: the same rows that carry those clauses name the classes `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR`.
The defect: the clauses say "unreproduced from the stated inputs"; the class NAMES assert absence. A token travels into reports, summaries and ledgers stripped of the definition that qualifies it, so the study's own outcome vocabulary can state the nonexistence finding its fairness rule forbids. The document's own record shows both engines reached this independently and classified it SUBSTANTIVE under stopping criterion (4) seven times (V21–V27); it stands unrepaired only because renaming a class is redefining one, which the lane correctly reserves to Duho. I concur with the seven prior findings on the merits — the defect is real, it is in the outcome vocabulary, and the authority it waits on is the same principal this gate reports to.
EXACT REPLACEMENT, subject to Duho's ruling: rename `DYM_NO_SIZE_MASS_RELATION` → `DYM_SIZE_MASS_RELATION_UNREPRODUCED` and `DYM_NO_POSITIVE_FLOOR` → `DYM_POSITIVE_FLOOR_UNREPRODUCED` at every occurrence (§3's limb-A exit, §4's table and class texts, §5's C6 NOT_RUN enumeration, §5a, §9, and any downstream record) — the form codex proposed.

7. STALL — SOUND, no replacement.

I could not construct a terminal state with no fileable class. The partition is total over limb B; limb A exits to class 3; unreadable/mismatched evidence (now including the pinned probe's digest), UNRESOLVED rows and undecidable-after-fallback procedures file class 5; a control failing after two attempts in any seat files class 6 after class 5 is ruled out; every seat-split shape has a route — different classes, same class with different C6 outcomes/floors/families, three-way or undecidable (class 5's enumeration now includes that state), and adjudication landing on a non-clean report (the third seat re-runs each failed control of each non-clean report, then files the class or class 6); condition-3 UNDECIDED records C6=FAIL and files class 5; a second probe exit-2 routes through the §9 control-failure rule to class 6. Including the recomputed-partition path of defect 2-A — which files class 1 or class 2 — every terminal path files exactly one declared class.

Disposition: two substantive repairs (2-A, the C3 token collision on the recomputed-class-2 path; 6-A, the class-token renames, which await Duho's ruling and have seven prior seat-findings). Neither touches the partition, the census, the falsifier's polarity, the comparator values, or any class routing; every other section is sound and the C3 probe, the probe pin, all three C5 frozen values and all comparator arithmetic verified by execution on this machine. Hence PREREG_SOUND_WITH_REPAIRS rather than SOUND (two live repairs) or UNSOUND (nothing blocks the study's ability to answer its question; the falsifier fires in both directions on executed inputs).

R3D_V28_KIMI_COMPLETE
