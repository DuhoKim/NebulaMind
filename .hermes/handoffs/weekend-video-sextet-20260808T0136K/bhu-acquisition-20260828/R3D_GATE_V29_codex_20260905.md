ACCESS_SHA=ff1e5011f3e2fce5393b9647b9f2540d8055f5b90179955049dbfb1d1e60f430
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

SOUND. The six declared terminal classes are exhaustive and mutually exclusive as an operational decision procedure. Limb A alone files class 3; limb B alone reaches the nonempty completion-free reading and partitions it, together with the finite completion readings, into the disjoint `P`, `Z`, and `I` cases that select classes 1, 2, or 4. `DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` are ordered explicitly, and scientific filing is restricted to control-clean reports except for the expressly substantive C3/C6 failures.

I could construct neither a result fitting two classes nor a result fitting none. A genuine inconclusive result is reachable: for example, let the completion-free reading yield `M_min = 10 kg` while the single admissible GR-exterior completion permits `M -> 0`. Then `P` and `Z` are both nonempty and class 2, `DYM_FLOOR_UNDERDETERMINED`, is selected. An inconsistent printed reading also reaches an inconclusive scientific outcome: `P` empty and `I` nonempty selects class 4.

2. CONTROLS

SOUND. No control can pass by assertion: C0 requires an exhibition table; C1 computed digests (and `repr()` where required); C2 the full census and ledger; C3 captured execution output; C4 printed algebra and premises; C5 captured command outputs, exit codes, and comparisons; C5b a complete per-path table; and C6 the condition artefacts and completed comparator table. Each control emits exactly one exact `PASS|FAIL|NOT_RUN` code. Early terminal blocks explicitly give later, never-engaged controls `NOT_RUN`, while engaged controls retain their actual results. The limb-A and direct class-2/class-4 clauses handle their specifically unreached controls, including the recomputation exceptions for a C3 that actually ran.

The operative literal commands are executable as written: the C3 digest command and probe invocation use absolute paths; the three C5 commands use the same absolute interpreter and valid shell quoting. Historical command strings in §8 are descriptions of superseded versions, not instructions to a seat.

C3 DELETION-PROBE EXECUTION: (a) Yes, I executed the literal command printed in §5 exactly as printed. (b) Working directory: `/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828`. (c) Exact stdout was:

```
target            : M
retained (all)    : ['eq_src', 'inj_ok']
DELETED (pinned)  : ['eq_src']
injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
§2b constants     : ['hbar', 'c', 'G']
with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
VERDICT: no unique floor without the source-pinned equations -> not circular
C3_DELETION_PROBE=PASS
```

Exit code: `0`.

3. CIRCULARITY

SOUND. The lane pattern cannot select the source relations, exclusions, reading set, or outcome: every nonblank manifest line receives a printed disposition; every displayed or numbered equation gets its own row; exclusions require one of four predeclared codes demonstrated from the independent source text; unresolved disagreements block; and the pattern is expressly forbidden until post-outcome C6 evaluation. The admissible completions are a closed four-kind list instantiated by a mechanical census-row test. A contrary relation cannot be quietly omitted: omission breaks line reconciliation or the independent equation list, while exclusion remains visible and reviewable in the full C2 artefact. C3 separately tests whether an injected relation alone carries a unique floor and removes it before repartitioning if so.

4. THE FALSIFIER

C6 can return `FAIL` in **4** of the six declared terminal outcome classes: initially in `DYM_FLOOR_DERIVED` or `DYM_FLOOR_UNDERDETERMINED`, and, after engagement, in `DYM_SOURCE_BLOCKED` (condition 3 remains undecided after fallback) or `R3D_NO_CLASS` (a later persistent control failure). It cannot be engaged from classes 3 or 4 because those produce no positive floor.

Condition 1 — PASS: file `M_min = 10 kg`; dimensional analysis returns mass. FAIL: file the dimensionless ratio `M_min/m_P = 3` as the alleged floor. That FAIL input is a malformed filing, not a valid member of a positive-floor class. Therefore condition 1 cannot fail substantively on any valid path; it is the condition that cannot fail on a valid path.

Condition 2 — PASS: `M_min = 3 sqrt(hbar c/G)` where `3` is fixed by a manifest equation and `hbar`, `c`, and `G` terminate in §2b. FAIL: `M_min = alpha sqrt(hbar c/G)` where `alpha` is introduced by “we choose alpha = 3” and has no manifest derivation.

Condition 3 — PASS: `M_min = 3 sqrt(hbar c/G)` with `3` source-fixed; replacing all non-§2b parameters leaves none. FAIL: `M_min = lambda sqrt(hbar c/G)` with free non-§2b `lambda`; the simplified expression retains `lambda`.

Condition 4 — PASS: a derivation in which every held-constant quantity, for example a core density `rho_0`, has a cited manifest passage deriving its constancy. FAIL: use the same formula while holding `rho_0` constant by choice and provide no manifest passage deriving that fixity.

Condition 5 — PASS: file the point interval `[1 kg, 1 kg]`; it overlaps none of the three numerical comparator intervals. FAIL: file `[2.1764343e-8 kg, 2.1764343e-8 kg]`; it overlaps the Planck-remnant interval `[2.176433e-8, 2.176435e-8] kg`. Hawking- and TOV-row FAILs are likewise reachable by filing point floors `2.0e11 kg` and `5.0e30 kg`, respectively.

5. RE-RUN GUARD

SOUND. The design expressly forbids assuming K6 repeats, studies a different branch, and makes the positive class genuinely reachable. A completion-free, consistent reading whose printed equations yield one positive floor, with every other admitted reading yielding that same floor and `Z = I = empty`, selects `DYM_FLOOR_DERIVED`.

6. FAIRNESS

DEFECT. The operative prose consistently uses “unreproduced from the stated inputs,” but the two operative class tokens assert absence when detached from their definitions.

Verbatim sentences:

> 3. **DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was
>    unreproduced from the stated inputs** after the frozen census of §2 was completed.

> 4. **DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family.

Defect: `DYM_NO_SIZE_MASS_RELATION` and `DYM_NO_POSITIVE_FLOOR` are themselves portable operative findings. Both say “NO,” not “unreproduced,” so reports or summaries that carry the token without its local gloss violate the document's fairness rule.

Exact replacement: replace every operative and historical reference to `DYM_NO_SIZE_MASS_RELATION` with `DYM_SIZE_MASS_RELATION_UNREPRODUCED`, and every operative and historical reference to `DYM_NO_POSITIVE_FLOOR` with `DYM_POSITIVE_FLOOR_UNREPRODUCED`. The two quoted operative sentences then become exactly:

> 3. **DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was
>    unreproduced from the stated inputs** after the frozen census of §2 was completed.

> 4. **DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above. Report the family.

This is a terminology repair, not a change to either class predicate or route.

7. STALL

SOUND. Every symbolic operation has a primary 120-second cap, a specified second capped fallback, and a terminal `DYM_SOURCE_BLOCKED` route if the fallback is absent, times out, cannot parse, or remains undecidable. Source, artefact, and adjudication failures also have declared terminal routes; persistent control failures route to `R3D_NO_CLASS`. The split rule covers differing terminal classes, C6 outcomes, floor formulas or values, and class-4 families. Every run can file exactly one terminal class; there is no terminal state with no fileable class.

R3D_V29_GATE_COMPLETE
