All execution checks are done and every printed value reproduced. Here is the gate.

ACCESS_SHA=ff1e5011f3e2fce5393b9647b9f2540d8055f5b90179955049dbfb1d1e60f430
GATE=PREREG_SOUND_WITH_REPAIRS

FINDING 1 — OUTCOME CLASSES: SOUND.
The partition mechanics hold. Every admissible reading lies in exactly one of P (consistent, non-empty allowed mass set, strictly positive greatest lower bound), Z (consistent, no positive lower bound follows), I (inconsistent, empty allowed set). The reading set is never empty — the completion-free derivation is always a member — so the limb-B rows are total: P empty implies Z or I non-empty, which is class 4; P non-empty with agreement and Z, I both empty is class 1; everything else in limb B is class 2. Class 3 is disjoint by limb, and the limb A/B boundary is the complementary reproduction question. Classes 5 and 6 are kept disjoint from 1-4 by the control-clean rule and from each other by class 6's "only after ruling out DYM_SOURCE_BLOCKED ... otherwise file DYM_SOURCE_BLOCKED".
Construction attempts, all failing to produce a two-class or no-class result:
  (i) Condition-3 UNDECIDED records C6_BREAKER_TEST=FAIL and files DYM_SOURCE_BLOCKED; class 1's carve-out covers only a condition that "FAILS under its stated decision rule", and UNDECIDED is a named distinct state — the two clauses do not both claim the path.
  (ii) C3 probe FAIL removes an injected relation and recomputes: the completion-free reading survives any injected-relation removal, so the recomputed reading set is non-empty and only classes 1/2/4 are selectable; class 3 is unreachable from limb B by its limb-only clause.
  (iii) Completion-dependent floor (completion-free reading in Z, all completions agree on 10 kg): files class 2 exactly (P non-empty, Z non-empty); no second class claims it.
  (iv) Digest mismatch on a run that is also a limb-A no-relation state: C1's mismatch clause files class 5 unconditionally, and the control-clean rule bars any scientific class.
INCONCLUSIVE is genuinely reachable: class 2 via the concrete input in (iii), class 4 when every reading permits zero, and the non-scientific terminal states 5/6.

FINDING 2 — CONTROLS: one substantive repair; execution report below.
Assertion sweep: every control names a printed artefact with "a claimed pass without it fails" or an equivalent mechanism; every control emits exactly one of PASS|FAIL|NOT_RUN per the §9 vocabulary; unreached controls are handled (§3's limb-A dispositions, the class-2/class-4 C3 clauses distinguishing direct reach from recomputed selection, §9's early-block rule distinguishing engaged from never-engaged controls). No control is passable by bare assertion. C0's exhibitions are seat-authored and Tori-verified, with the ENTAILED route for conditions that valid membership makes unfailable.
Literal commands: C5's three and C3's two (digest check plus invocation) contain no shell metacharacters and use absolute paths throughout. I executed all of them; one with a stated substitution, reported plainly below.

DEFECT (substantive): C5b's scope rule still fails paths the design's own mandated commands open.
  Quote (in-scope clause): "**the pinned interpreter `/usr/bin/python3`, executed and hashed under C5 and invoked by C3**"
  Quote (out-of-scope clause): "**OUT OF SCOPE:** another lane's files, and any path not named above."
  Quote (failure clause): "Any `OUT_OF_SCOPE` row **fails** the control."
  Defect: the clause names the interpreter binary but not the files the binary loads. C5's second command imports sympy; the pinned C3 probe imports sympy (its solve output is the required artefact); §9's fallback rows use mpmath. Executing these mandated commands opens the interpreter's standard-library and site-packages files, each "a path not named above", so a seat applying the rule as written prints OUT_OF_SCOPE rows and fails C5b on every conforming run — the V20/V28 scope defect one path down, against this document's own standard ("A control that fails on the paths its own design requires is not a scope rule"). I confirmed by execution that the mandated commands do load such files.
  EXACT REPLACEMENT for the in-scope sentence: "**the pinned interpreter `/usr/bin/python3`, executed and hashed under C5 and invoked by C3, together with every file that interpreter loads while executing the mandated commands — its standard library, the installed sympy and mpmath packages, and their dependent shared libraries — each printed and marked `IN_SCOPE` under this clause**"

C3 EXECUTION REPORT:
  (a) Yes — I executed the literal command exactly as printed, twice.
  (b) Working directories: `/` and then `/Users/duhokim` — neither the lane directory nor the repository root.
  (c) stdout from `/`, byte-identical from `/Users/duhokim` (cmp confirmed):
      target            : M
      retained (all)    : ['eq_src', 'inj_ok']
      DELETED (pinned)  : ['eq_src']
      injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
      §2b constants     : ['hbar', 'c', 'G']
      with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
      pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
      VERDICT: no unique floor without the source-pinned equations -> not circular
      C3_DELETION_PROBE=PASS
  Exit code 0 from both directories. Scope note on my run: the relations file at the designated absolute path already held the negative control (inj_ok: r0 = 2GM/c^2); I ran against it as it stood and wrote nothing. This confirms the command executes verbatim and is cwd-independent; it does not exercise the seat-written positive control.
  Pin check executed: shasum -a 256 of the probe returned 7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874 — matches the pin in C3.
  C5 on this machine: `Python 3.9.6` (exit 0), sympy `1.14.0` (exit 0), interpreter digest b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9 — all three equal the frozen values. One substitution, stated plainly: the literal `/usr/bin/python3 -c "..."` form is blocked in my execution mode, so I ran the same import through the same pinned interpreter via a scratch file; output identical. The `-c` form is an ordinary valid command — the block is my environment's, not the document's.
  Comparator arithmetic (executed with the pinned interpreter, intermediates printed): Gregorian year 31556952.0 s; t0 = 4.35391266744e17 s; ħc = 3.161526771559562e-26; m_P = 2.176434342051127e-8, inside [2.176433e-8, 2.176435e-8]; Hawking lower 1.7298261908304883e11 and upper x3.0 = 5.189478572491465e11, both inside [1.729e11, 5.190e11]; TOV 4.375624e30 and 5.767868e30 inside [4.375e30, 5.768e30]. Every outward-rounding claim reproduces.

FINDING 3 — CIRCULARITY: SOUND.
  Enumeration: the census key is every non-blank line plus every displayed equation, with a per-source line/disposition reconciliation count and a printed equation list; the eleven English terms may not justify any row. An omitted contrary relation shows up as arithmetic.
  Exclusion: only via predeclared codes demonstrated from source text; UNRESOLVED may not be treated as absence and forces DYM_SOURCE_BLOCKED; seat disagreement is UNRESOLVED, hence blocked.
  Addition: exactly four completion kinds, each instantiated at most once, only where a census-cited UNBOUND determination leaves its object unbound; a completion that is itself the sole source of a mass bound is inadmissible; the reading set is finite (completion-free plus at most four), and the class partition over it is mechanical. The pattern cannot add a kind, pick an object, or pick a class.
  Backstop: C3 mechanically tests whether a determinate floor follows from an injected relation alone; a FAIL removes the relation and recomputes — circularity is exhibited, not argued.
  The pattern enters only at C6, only after a positive-floor class is reached on other grounds; the five conditions are quoted verbatim and pinned by hash, and evaluation against any other text fails C6. A C6 PASS amends the record; nothing lets the record amend the evidence. Residual judgement points (DUPLICATE disposition, UNBOUND citation) are printed with locators and cross-checked by the second seat. I found no channel by which the lane's pattern reaches inclusion, exclusion, or outcome selection.

FINDING 4 — THE FALSIFIER.
  NUMBER: C6 can record FAIL in 4 of the 6 declared outcome classes: DYM_FLOOR_DERIVED (substantive FAIL, still filed), DYM_FLOOR_UNDERDETERMINED (per-floor evaluation), DYM_SOURCE_BLOCKED (condition-3 UNDECIDED after fallback), and R3D_NO_CLASS (concrete path: both seats file class 1 with C6=FAIL on condition 5, one report also fails C4, the third seat's re-run of C4 fails again — engaged C6, persistent control failure, FAIL carried into the class-6 filing). C6 is initially engaged on 2 classes and is NOT_RUN on classes 3 and 4, which produce no number to test. This matches §5a's two-number statement; I verified the class-6 leg by tracing the re-run rule.
  Per condition:
    Condition 1: PASS — floor 1.0e15 kg, dimension mass. FAIL — only on a malformed filing, e.g. a seat entering the dimensionless ratio 42 as its floor. CANNOT FAIL ON ANY VALID PATH — every positive-floor class requires a mass. The document discloses this as ENTAILED and counts 4 of 5 rather than claiming five; a disclosed entailment is a property, not the defect.
    Condition 2: PASS — floor sqrt(ħc/G), every constant in §2b. FAIL — floor c1*sqrt(ħc/G) with c1 introduced as "the simplest form", or any provenance terminus outside the manifest and §2b.
    Condition 3: PASS — floor sqrt(ħc/G): no non-§2b parameters exist, so the simplified expression contains none of the substituted symbols. FAIL — floor c^2 r0/(2G) with r0 a non-§2b symbol surviving simplification. UNDECIDED maps to C6=FAIL plus class 5, never a fourth state.
    Condition 4: PASS — a derivation holding nothing constant, or every held-constant quantity traced to a passage reproduced in the C2 artefact. FAIL — a floor derived with the core scale held constant by choice and no manifest derivation of the constancy.
    Condition 5: PASS — floor 1.0e15 kg (executed: no overlap). FAIL — executed one per comparator row: m_P = 2.1764343e-8 kg (Planck row), 2.0e11 kg (Hawking row), 5.0e30 kg (TOV row). The ΛCDM row has no interval and can never match. A lazy comparison yields PASS — the accepted, disclosed, inspectable cost of the V9 polarity.
  Condition that cannot fail on any path: condition 1 (valid paths only; failable on a malformed filing, and disclosed as such).

FINDING 5 — RE-RUN GUARD: SOUND.
§6 forbids assuming K6's outcome, and nothing in the census, the four completion kinds, the partition, or the comparator table imports K6's underdetermination as a premise — §0 names it as motivation only. The positive class is genuinely reachable: a printed relation fixing the core scale from §2b constants alone, with every admissible completion agreeing and Z, I empty, files DYM_FLOOR_DERIVED — and C0 must exhibit that input before any freeze.

FINDING 6 — FAIRNESS: held in every operative clause.
§3's limb-A exit, the §4 table rows for classes 3 and 4, class 4's numbered entry including its inconsistency branch ("report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and §6's rule all use "unreproduced from the stated inputs"; no operative clause asserts absence or says "error". The one standing exception is the two class TOKENS, whose names assert absence while their definitions say "unreproduced" — the item found by eight seat-findings and held as an OPEN RULING on this version with renaming authority reserved to Duho. I confirm the merits of that finding and record no new one.

FINDING 7 — STALL: SOUND.
Terminal-path walk: limb-A exit files class 3; limb B files 1/2/4, total by P/Z/I over a non-empty reading set; unreadable or mismatched source or probe, UNRESOLVED proposition, undecidable-after-fallback procedure, or unresolved split files class 5; persistent control failure with class 5 ruled out files class 6; condition-3 UNDECIDED files 5; a second C3 exit of 2 routes through the §9 control-failure rule to 6. Same-class splits (C6 outcome, floors, families) and different-class splits convene the third seat, which files only on agreement with one of the two, else class 5. Non-clean reports are re-run once per failed control per report — a pass files the class, a failure files 6; the zero-clean and adjudicated-to-non-clean branches are both closed. A single-seat control failure routes because a non-clean report still names its selected class — the zero-clean repair presupposes this — engaging the same-class trigger. A C0 failure prevents the freeze, so no run starts; that is not a fileless terminal state. I found no terminal state with no fileable class.

One substantive repair (Finding 2, the C5b scope clause); everything else sound, with all executability claims I could test verified by real execution above.

R3D_V29_KIMI_COMPLETE
