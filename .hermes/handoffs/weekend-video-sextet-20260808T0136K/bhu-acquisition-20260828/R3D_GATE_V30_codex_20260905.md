ACCESS_SHA=a99aad15f168290fa5afaaf957d478cedd4cf57cb967e97dd3ea8d6b2a25840b
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

The operative decision procedure is exhaustive and mutually exclusive. Limb A alone files class 3. Limb B partitions every admissible reading into exactly one of P (positive floor), Z (masses approach zero), or I (empty/inconsistent allowed set), and then files exactly one of classes 1, 2, or 4. Classes 5 and 6 cover non-scientific source/procedure/split blocks and persistent control failures, with class 6 expressly considered only after class 5 is ruled out. I could construct neither a result fitting two operative classes nor a result fitting none.

An inconclusive scientific result is genuinely reachable. Concrete example: the completion-free reading yields M_floor = 10 kg, while one admissible one-completion reading leaves M arbitrarily close to zero. Then P and Z are both non-empty and class 2, DYM_FLOOR_UNDERDETERMINED, files. Another reachable inconclusive case has two readings yielding 10 kg and 20 kg.

Section sound; no replacement.

2. CONTROLS

The controls require printed artefacts rather than bare assertions: C0 an exhibition table; C1 computed digests (and repr() where required); C2 the census and full ledger; C3 captured deleted-state output; C4 algebra plus premise list; C5 captured command outputs, exit codes, and comparisons; C5b a per-path scope table; and C6 the per-condition artefacts and completed comparator table. Every control has an exact PASS/FAIL/NOT_RUN token. Early terminal blocks and unreached limbs have explicit NOT_RUN handling, while controls already engaged retain their actual result. The operative literal C3 and C5 commands are executable as written.

C3 deletion-probe execution: (a) YES, I executed the literal command exactly as printed; (b) working directory: /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828; (c) exact stdout was:

target            : M
retained (all)    : ['eq_src', 'inj_ok']
DELETED (pinned)  : ['eq_src']
injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
§2b constants     : ['hbar', 'c', 'G']
with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
VERDICT: no unique floor without the source-pinned equations -> not circular
C3_DELETION_PROBE=PASS

Exit code: 0.

One operative routing phrase is not an exact declared code.

VERBATIM: “Two to four seat-days; sources believed in the lane — **limb A confirms that first and reports BLOCKED if not.**”

Defect: BLOCKED is neither a declared outcome token nor a control token. Although §§4 and 9 supply the intended source-block routing, this sentence independently tells the seat to report an undefined label.

EXACT REPLACEMENT: “Two to four seat-days; sources believed in the lane — **limb A confirms source readability and identity first and files `DYM_SOURCE_BLOCKED` if either check fails.**”

3. CIRCULARITY

The lane’s pattern cannot select the relations, exclusions, or scientific class under the operative design. The census accounts for every non-blank line and every displayed or numbered equation; exclusions require one of four predeclared reason codes demonstrated by source text; missing or disputed evidence becomes UNRESOLVED and blocks rather than absence. The reading set is finite and mechanically derived from census-bound objects. C6 sees the pattern only after a positive-floor class is selected independently. A contrary relation cannot be quietly omitted without breaking the line reconciliation/equation list and C2 artefacts.

Section sound; no replacement.

4. THE FALSIFIER

NUMBER: C6_BREAKER_TEST=FAIL can be recorded in 4 of the 6 declared terminal outcome classes: DYM_FLOOR_DERIVED, DYM_FLOOR_UNDERDETERMINED, DYM_SOURCE_BLOCKED after condition 3 remains undecided, and R3D_NO_CLASS after an engaged C6 is followed by a persistent control failure. C6 is initially engaged from 2 classes.

Condition 1 — PASS: yes; input M_floor = 10 kg. FAIL: only as a filing-integrity failure, e.g. the seat files the dimensionless ratio r0/r_g = 1 as its “floor.” It cannot fail on any valid positive-floor path because membership requires a mass floor. This is the condition that cannot fail substantively on any valid path.

Condition 2 — PASS: yes; input M_floor = sqrt(hbar*c/G), with every constant terminating in §2b. FAIL: yes; input M_floor = A*sqrt(hbar*c/G), where A = 3 is introduced by “we choose” and has no manifest or §2b terminus.

Condition 3 — PASS: yes; input M_floor = sqrt(hbar*c/G), whose simplified expression has no non-§2b symbol. FAIL: yes; input M_floor = alpha*sqrt(hbar*c/G), with free alpha surviving.

Condition 4 — PASS: yes; input M_floor = sqrt(hbar*c/G) with no independently chosen held-constant quantity. FAIL: yes; input M_floor = rho0*V0 where V0 is held fixed by choice and no manifest passage derives its constancy.

Condition 5 — PASS: yes; point floor 1.0e15 kg overlaps none of the three numerical comparator intervals. FAIL: yes; point floor 2.0e11 kg overlaps the Hawking interval. The other mandated comparator-row failure examples are 2.1764343e-8 kg (Planck interval) and 5.0e30 kg (stellar-collapse interval).

Section sound; no replacement.

5. RE-RUN GUARD

The design expressly forbids assuming K6 repeats and permits a completion-free, consistently determined positive mass floor to file DYM_FLOOR_DERIVED. The positive class is genuinely reachable; for example, a consistent completion-free allowed set M >= 10 kg puts its sole reading in P with Z and I empty.

Section sound; no replacement.

6. FAIRNESS

The operative explanatory clauses use “unreproduced from the stated inputs,” but the two operative outcome tokens themselves still assert absence and will travel without their qualifying definitions.

VERBATIM: “**3. DYM_NO_SIZE_MASS_RELATION** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.”

Defect: the token DYM_NO_SIZE_MASS_RELATION says no such relation exists, which is stronger than the operative finding that it was unreproduced from the stated inputs.

EXACT REPLACEMENT: “**3. DYM_SIZE_MASS_RELATION_UNREPRODUCED** — limb A's exit: **a relation binding size to mass, or bounding the mass, was unreproduced from the stated inputs** after the frozen census of §2 was completed.” Replace every operative occurrence of `DYM_NO_SIZE_MASS_RELATION` with `DYM_SIZE_MASS_RELATION_UNREPRODUCED`.

VERBATIM: “**4. DYM_NO_POSITIVE_FLOOR** — as partitioned above. Report the family.”

Defect: the token DYM_NO_POSITIVE_FLOOR says no positive floor exists, which is stronger than the class predicate’s finding that a positive floor was unreproduced from the stated inputs.

EXACT REPLACEMENT: “**4. DYM_POSITIVE_FLOOR_UNREPRODUCED** — as partitioned above. Report the family.” Replace every operative occurrence of `DYM_NO_POSITIVE_FLOOR` with `DYM_POSITIVE_FLOOR_UNREPRODUCED`.

7. STALL

The operative stall guard provides two bounded attempts and routes undecidable fallback results to DYM_SOURCE_BLOCKED. Seat splits, early blocks, persistent control failures, and probe recomputation all have terminal routing. Thus the run cannot reach a terminal state with no fileable declared class. The bare BLOCKED wording identified under finding 2 is ambiguous reporting terminology, but the controlling §§4 and 9 still route that state to DYM_SOURCE_BLOCKED.

Section otherwise sound; no additional replacement.

R3D_V30_GATE_COMPLETE
