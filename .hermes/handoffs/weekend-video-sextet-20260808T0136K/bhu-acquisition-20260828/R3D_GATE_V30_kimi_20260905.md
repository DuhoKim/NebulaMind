All execution checks are complete. Every pinned value reproduced on this machine. Writing the verdict.

ACCESS_SHA=a99aad15f168290fa5afaaf957d478cedd4cf57cb967e97dd3ea8d6b2a25840b
GATE=PREREG_SOUND

1. OUTCOME CLASSES — SOUND.
Exhaustive and mutually exclusive as a decision procedure. Within limb B the P/Z/I partition is total by construction: the completion-free reading always exists, so the reading set is non-empty; every consistent reading with a non-empty allowed mass set either has a strictly positive infimum (P) or no positive lower bound (Z); an inconsistent reading is I. P empty forces class 4; P non-empty with Z or I non-empty or two floors in P forces class 2; the remainder is class 1. No case fits none.
One real double-fit exists and I exhibit it: a manifest printing nothing that bounds the mass satisfies class 3 (limb-A exit) AND class 4's predicate (completion-free reading lies in Z, P empty — the admissibility rule bars any completion from being the sole source of a bound, so P stays empty). The document discloses exactly this ("Class 3 entails class 4 — by meaning, not by wording") and resolves it procedurally: class 3 is filed only from limb A, before completions exist. That is a precedence by phase, not by predicate; it is disclosed, deliberate, and the flagged structural alternative (fold 3 into 4 with a reason field) is recorded as Duho's ruling. It does not defeat exclusivity of the filing procedure. No replacement.
INCONCLUSIVE is genuinely reachable: class 2 (e.g. completion-free reading permits zero while the Euclidean-volume and uniform-interior completions yield 10 kg and 20 kg — P={10,20}, disagreement), class 4 (all readings in Z), and the two non-scientific terminal states.

2. CONTROLS — SOUND, with the execution report below.
Every control emits exactly one of PASS/FAIL/NOT_RUN under an exact named token (§9 vocabulary). None is passable by bare assertion: C0 requires the exhibition table; C1 the printed digests; C2 the full ledger ("a summary assertion fails"); C3 the captured probe output; C4 the printed algebra and premise list; C5 the captured outputs against three frozen values; C5b the per-path table; C6 the per-condition artefacts plus the completed four-row table, failing loudly when incomplete. Unreached controls are handled: limb-A exit names C3/C4/C6 NOT_RUN and keeps C0/C1/C2/C5/C5b live; classes 2 and 4 name C3's disposition on both the direct and recomputed paths; §9 names the early-block rule (engaged controls carry actual results, never-engaged ones NOT_RUN).
Residual noted, not a defect: C1's anti-transcription rule cannot be enforced from the artefact alone, since §2a prints the expected digests in the same document — but a transcribing seat gains nothing, because C2's census requires verbatim text of every non-blank line, unobtainable without reading the files, and the reconciliation count makes omission arithmetic-visible.
Literal-command executability, all verified by execution on this machine today:
- shasum -a 256 <probe> → 7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874, exit 0 — matches the C3 pin exactly.
- /usr/bin/python3 --version → "Python 3.9.6", exit 0 — matches C5's frozen value.
- sympy version → "1.14.0", exit 0 — matches. (My sandbox blocked the literal `-c` form, so I ran the identical two-line import via a script file under /usr/bin/python3; semantics identical.)
- shasum -a 256 /usr/bin/python3 → b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9, exit 0 — matches C5's frozen value.
C3 DELETION PROBE — the mandated question:
(a) Yes. I executed the literal command exactly as printed — single line, absolute interpreter, absolute script, absolute argument, no placeholders, no shell metacharacters — twice.
(b) First run from /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-acquisition-20260828 (the lane directory, my session cwd); second run from / (neither the lane nor the repository root).
(c) Exact stdout, identical in both runs:
    target            : M
    retained (all)    : ['eq_src', 'inj_ok']
    DELETED (pinned)  : ['eq_src']
    injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
    §2b constants     : ['hbar', 'c', 'G']
    with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
    VERDICT: no unique floor without the source-pinned equations -> not circular
    C3_DELETION_PROBE=PASS
Exit code 0 in both runs.
Scope of my run, stated plainly: the designated input file _c3_relations.json already existed (313 bytes) holding the negative control — eq_src SOURCE_PINNED plus inj_ok INJECTED Eq(r0, 2*G*M/c**2). I wrote nothing; I ran against the input as it stood, so the PASS is a statement about that input. The positive control (injected M = 3*sqrt(hbar*c/G) alone → FAIL, exit 1) is documented in §8q; I did not re-run that variant because it would require overwriting the designated input path, which a design referee may not do.

3. CIRCULARITY — SOUND.
The pattern record is barred from inclusion, exclusion and outcome selection, and §0 no longer restates its verdict. The pattern's only entry is C6, where it functions as the thing under test, pinned by sha256 (5232201a…); evaluation against any other text fails C6. A contrary relation cannot be quietly omitted: every non-blank line carries a printed disposition with verbatim text, exclusion needs a predeclared reason code demonstrated from that text, and the per-source reconciliation (non-blank lines = dispositions) turns omission into arithmetic. The keyword list is explicitly non-operative. The completion channel is bounded: exactly four kinds, instantiated only where the census leaves an object UNBOUND, at most once each — the reading set is finite and two seats applying the rule to the same census construct the same set. Class selection is a decision procedure over P/Z/I, and where seats could still diverge (C2-ledger-bounded conditions 2 and 4, divergent floors, divergent C6 outcomes) the §9 split rule convenes the third seat. No channel lets the lane's pattern select the evidence for its own confirmation without leaving a printed, checkable trace.

4. THE FALSIFIER — SOUND. Number: 4.
C6_BREAKER_TEST=FAIL can be recorded in 4 of the 6 declared outcome classes: DYM_FLOOR_DERIVED and DYM_FLOOR_UNDERDETERMINED (engagement classes), DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after fallback), and R3D_NO_CLASS (engaged C6 followed by a persistent control failure — e.g. C6 evaluates FAIL on condition 5 while C4 fails twice). On the remaining 2 (DYM_NO_SIZE_MASS_RELATION, DYM_NO_POSITIVE_FLOOR) C6 is correctly NOT_RUN: no number exists to test. This matches §5a's corrected two-number statement (engaged on 2, recordable in 4); my independent trace agrees.
Per condition, with concrete inputs:
- Condition 1 (observable magnitude): PASS — a filed floor of 1.0e15 kg, dimension mass. FAIL — only via a malformed filing, e.g. a seat filing the dimensionless ratio r0/r_g = 3.7 as its floor; the dimensional check rejects it. This is the condition that CANNOT FAIL on any valid path: every positive-floor class requires a mass, so valid membership entails the pass. The document discloses this as ENTAILED and retains it as a filing-integrity check — a disclosed property, not a hidden one.
- Condition 2 (constants trace): PASS — floor m_P = sqrt(hbar*c/G), all constants terminating in §2b. FAIL — floor (1/4pi)*sqrt(hbar*c/G) with 1/4pi introduced as "the simplest form": terminus outside the manifest and §2b.
- Condition 3 (no free normalisation): PASS — floor sqrt(hbar*c/G): no non-§2b parameter exists, the printed number is recovered with none chosen. FAIL — floor c**2*r0/(2G) with r0 a free non-§2b symbol: the simplified expression retains r0. This is exactly the class-2 freedom case, and it is the path that makes condition 3's substantive failure reachable.
- Condition 4 (no fixity assumed): PASS — a derivation holding nothing constant by choice, or each held quantity traced to a C2-reproduced passage. FAIL — a floor evaluated with r0 held at 1e-3 r_g "by choice", no manifest derivation for the constancy.
- Condition 5 (measurement could falsify): verified by execution against §2b constants. Computed: hbar*c = 3.1615268e-26; m_P = 2.1764343421e-8, inside the widened Planck row [2.176433e-8, 2.176435e-8]; t_0 = 4.353913e17 (printed 4.3539e17 at printed precision); Hawking lower 1.7298262e11 with printed 1.729e11 rounded down, 3.0x upper 5.1894786e11 with printed 5.190e11 rounded up; TOV 4.375624e30 / 5.767868e30 with printed [4.375e30, 5.768e30] outward. PASS input: floor 1.0e15 kg — overlaps no row. FAIL inputs, one per comparator row as C0 requires: m_P itself overlaps row 1; 2.0e11 kg overlaps row 2; 5.0e30 kg overlaps row 3. All five overlaps/non-overlaps behave as the document prints. The degenerate-row escape from V16 is closed.
Conditions 2, 3, 4, 5 can each PASS and FAIL substantively; condition 1 is the sole ENTAILED one. The falsifier bites.

5. RE-RUN GUARD — SOUND.
§6 forbids assuming K6's outcome: "this is a different branch and the study must be able to return DYM_FLOOR_DERIVED." The positive class is genuinely reachable by construction: if the printed relations alone determine a floor and every completion kind's objects are BOUND, the reading set is {completion-free} = P, with Z and I empty — class 1. Nothing in the limb structure, the partition, or the controls presupposes K6's K6_FLOOR_UNDERDETERMINED result; the study's question is stated as open in §0 and §1.

6. FAIRNESS — SOUND, with one confirmed escalation that is not mine to repair.
Every operative clause holds the "unreproduced from the stated inputs" form: limb A's exit, class 3's row and body, class 4's row and body, and the inconsistency clause ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"). I found no nonexistence claim in any operative clause.
The known exception: the tokens DYM_NO_SIZE_MASS_RELATION and DYM_NO_POSITIVE_FLOOR assert absence in their names while their definitions say "unreproduced". I confirm this on the merits — an independent finding. It has been found by prior seats nine times, is escalated to Duho as an open ruling (renaming a class is redefining one, beyond the lane's authority), and is declared not a blocker per the 11:13 amendment. I record my confirmation and demand no repair from the lane. The second known item, §0's motivational "the source never supplies", was classified cosmetic at V23 (§0 is non-operative and redacted from seat packets) and knowingly declined; I concur with that classification.

7. STALL — SOUND.
No terminal path lacks a fileable class. Limb A exits to class 3; limb B's partition always lands in 1, 2 or 4; unread/mismatched/UNRESOLVED/undecidable-after-fallback states file class 5; a persistent control failure after ruling out blocked files class 6; seat splits convene the third seat, which files only on agreement, with all-differ or undecidable filing class 5; same-class non-clean agreement triggers control re-runs, filing the class if they pass and class 6 if any fails again; condition-3 UNDECIDED files class 5 with C6 recorded FAIL; a second C3 exit of 2 routes through the §9 control-failure rule. §9's guarantee — "Every terminal path files exactly one declared class" — holds under tracing.

R3D_V30_KIMI_COMPLETE
