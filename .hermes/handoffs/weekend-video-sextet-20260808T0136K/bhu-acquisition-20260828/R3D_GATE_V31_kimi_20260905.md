The sympy check is blocked by my execution mode (single-query approval policy flags any `python3 -c`), not by the machine — I'll report that plainly. The C3 literal command ran cleanly from two unrelated working directories. Since `execute_code` is also blocked, the comparator arithmetic below is done by hand with intermediates printed, per the brief's fallback.

ACCESS_SHA=aa3b184192503205229ea2a7428f7145741757636fe6ccd7fd0d834ebb108e62
GATE=PREREG_SOUND

1. OUTCOME CLASSES — SOUND.

Exhaustive and mutually exclusive, by construction I verified rather than by the document's say-so. Per reading, the allowed mass set is a set of non-negative reals: empty (I), non-empty with infimum 0 (Z), or non-empty with infimum > 0 (P). That trichotomy is total and exclusive per reading; the reading set is non-empty (completion-free plus at most four one-completion readings), so at least one of P/Z/I is non-empty. Within limb B: P empty with Z or I non-empty → class 4; P non-empty with internal disagreement or Z/I non-empty → class 2; P non-empty, agreeing, Z and I empty → class 1. Those three predicates are disjoint and their union is all non-empty reading-set configurations. Class 3 is severed from class 4 by a single boolean (limb A's "at least one relation reproduced"), not by a predicate, so the entailment the document itself discloses ("class 3 entails class 4 — by meaning") cannot produce a double filing.

Attempts to break it, as instructed: (a) two classes — a completion-dependent agreed floor (old class-5 state) lands only in class 2, since I or Z is non-empty by construction; an inconsistent-completion-free reading with agreeing completions lands only in class 2 (I non-empty); P empty with only I non-empty lands only in class 4, whose own clause covers it ("report that a consistent solution … were unreproduced … and reproduce the contradiction"). I could not construct a real result fitting two classes. (b) none — every per-reading state is boxed; every limb-A state (relation reproduced / none / census blocked) routes. I could not construct a result fitting none. The one residual awkwardness is disclosed, not hidden: class 3 and class 4 remain the same physical state separated procedurally, with the structural alternative (retire class 3) recorded as Duho's open ruling #2 in §8u. That is a disclosed property with an owner, not a defect I can cite against this version.

INCONCLUSIVE is genuinely reachable, in two kinds: non-scientific — DYM_SOURCE_BLOCKED (e.g. one UNRESOLVED census row, or a digest mismatch) and R3D_NO_CLASS (e.g. a C5 frozen-value mismatch on a drifted machine, persisting across the mandated re-run); scientific — class 2, underdetermination with every reading reported and none chosen.

2. CONTROLS — SOUND, with the execution disclosures the brief requires.

Assertion vs artefact: no control is passable by bare assertion. Each of C0–C6 names its printed artefact and each carries an explicit "a claimed pass without <artefact> fails" clause; each emits exactly one of PASS|FAIL|NOT_RUN under the §9 vocabulary; NOT_RUN is permitted only where the document says so explicitly, and the early-block rule plus per-class disposition lists (class 3: C3/C4/C6 NOT_RUN; class 4 direct: C3/C6 NOT_RUN; class 2 direct: C3 NOT_RUN; recomputed classes: C3=FAIL with artefacts) leave no control's status unstated on any path I traced. The residual that all artefacts are self-reported is covered structurally — blind second seat plus "Tori re-runs everything" (§7) — and is the strongest form available to this design.

Literal-command executability, verified by execution on this machine today, from cwd=/ :
- C3 pin-check shasum line: executed; stdout `7db669313568d08dc9be7bb18d142a956db3cc3ad62a87bff9e4724c47527874  …/r3d_c3_deletion_probe.py`, exit 0 — matches the pin in C3 exactly.
- `/usr/bin/python3 --version`: executed; stdout `Python 3.9.6`, exit 0 — matches C5's frozen value.
- `shasum -a 256 /usr/bin/python3`: executed; stdout `b8763cf250e607a778bb4603cecb5b90338814d0a3dfcba0d57b1de242f610e9  /usr/bin/python3`, exit 0 — matches C5's frozen value.
- `/usr/bin/python3 -c "import sympy; print(sympy.__version__)"`: NOT executed. My execution mode blocks any interpreter `-c`/`-e` invocation outright (single-query approval policy); the block is a property of my mode, not of the command or the machine. I do not claim a run I did not perform. The matching interpreter version and interpreter digest above, and the fact that the C3 probe — which itself imports sympy — executed successfully under `/usr/bin/python3`, are consistent with the frozen `1.14.0` but are not a substitute for it; the V30 record states kimi executed all three on this machine with matching values.

THE C3 QUESTION, answered explicitly:
(a) Yes — I executed the literal command printed in C3, exactly as printed, character for character, twice. I wrote nothing; I read no file's content; the probe read the input path named in the command. Scope of the run, stated plainly as prior seats did: the designated input `_c3_relations.json` already held the negative control (injected `r0 = 2GM/c²` with `M` free), and I ran against it as it stood.
(b) Working directories: first run from `/`, second run from `/Users/duhokim` — deliberately neither the lane directory nor the repository root.
(c) Exact stdout (byte-identical on both runs):
```
target            : M
retained (all)    : ['eq_src', 'inj_ok']
DELETED (pinned)  : ['eq_src']
injected relations: [('inj_ok', 'Eq(r0, 2*G*M/c**2)')]
§2b constants     : ['hbar', 'c', 'G']
with everything   : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
pinned DELETED    : c**2*r0/(2*G)   (indeterminate: free in ['r0'])
VERDICT: no unique floor without the source-pinned equations -> not circular
C3_DELETION_PROBE=PASS
```
Exit code: 0, both runs. This matches §8q's Run 3 transcript and demonstrates the no-dependence-on-working-directory claim on the two directories I used.

3. CIRCULARITY — SOUND.

The pattern cannot reach the evidence through any of the three channels named. Enumeration: the census is keyed on every non-blank line with a per-source reconciliation count (non-blank lines = assigned dispositions, unequal counts fail C2) plus a cross-check requiring every numbered or displayed equation as its own row — a bare symbolic relation (`r_0² = 3/Λ`) becomes a row by the equation rule regardless of wording. Exclusion: only via four predeclared reason codes, each demonstrated by source text, with UNRESOLVED routed to DYM_SOURCE_BLOCKED and explicitly barred from being treated as absence. Class selection: the P/Z/I partition and limb gate decide; the pattern record is forbidden as evidence for inclusion, exclusion or outcome selection and enters only at C6, after a positive-floor class exists on other grounds — and C6's condition text is pinned verbatim to a sha256, so the lane-authored text cannot drift between seats. A contrary relation cannot be quietly omitted: omission is arithmetic (the reconciliation count), mis-disposition is auditable (verbatim text per row), and the completion set is closed to four kinds, each instantiated only where a census row leaves its object unbound, so the set §4 quantifies over is fixed and finite. Note also the structural asymmetry in the study's favour: the pattern record can only be amended (on C6 PASS) or left untouched — the study cannot strengthen it, so the self-confirmation channel the brief warns about is closed by construction. §0's prior leakage was de-patterned and §8's expectancy runs toward keeping the falsifier able to fire (against the lane's own interest), with seats' judgement calls boxed by the mechanical controls above.

4. THE FALSIFIER — the number is 4.

C6 can record FAIL in 4 of the 6 declared outcome classes: DYM_FLOOR_DERIVED (substantive failure, floor stands), DYM_FLOOR_UNDERDETERMINED (per-floor evaluation), DYM_SOURCE_BLOCKED (condition 3 UNDECIDED after fallback — explicit clause records C6_BREAKER_TEST=FAIL), and R3D_NO_CLASS (an engaged C6 failing as a control — e.g. a missing comparison table — persistently after the mandated re-runs). It is engaged on 2 classes and can never be other than NOT_RUN on classes 3 and 4. My independent count agrees with §5a's corrected two-number statement.

Per condition (PASS input / FAIL input):
1. Dimension is mass: PASS — floor `1.0e15 kg`. FAIL — only a malformed filing, e.g. a seat entering the dimensionless ratio `M_min/M_☉ = 0.75` as its floor. This is the condition the brief asks me to name: condition 1 cannot fail on any validly-filed path — every positive-floor class requires a mass — and the document discloses this as `ENTAILED` rather than counting it (4 of 5 substantively failable). It is failable only on the malformed-filing path, which C0's ENTAILED route exhibits. No condition is unfailable on every path; condition 1 is unfailable on every valid path, and that is labelled, not hidden.
2. Constants trace: PASS — floor built as `(3/2)·sqrt(ħc/G)`, every constant in §2b. FAIL — floor `4.7·sqrt(ħc/G)` with 4.7 introduced as "the simplest form" or via "following [ref]" where the reference never derives it: terminus outside the manifest and §2b fails, exactly as a `we assume` terminus.
3. No free normalisation: PASS — floor `3·sqrt(ħc/G)` (all symbols §2b; the simplified expression contains no non-§2b symbol — this is the probe's own positive-control relation). FAIL — floor `α·sqrt(ħc/G)` recoverable only after choosing α; the symbol survives the free-symbol probe. UNDECIDED routes to C6=FAIL plus DYM_SOURCE_BLOCKED, so "does not pass" is never a fourth state.
4. No fixity assumed: PASS — a held-constant Λ with a deriving passage reproduced in the C2 ledger. FAIL — r₀ held constant by choice with no manifest derivation row in the fixity table.
5. Comparator overlap: PASS — `1.0e15 kg` (overlaps nothing); also `3.0e30 kg` (NEAR_MATCH against TOV, reported, never decisive). FAIL — `2.0e11 kg` (Hawking row), `m_P = 2.1764343e-8 kg` (Planck row), `5.0e30 kg` (TOV row): one failing input per comparator row, as C0 requires.

Comparator arithmetic, recomputed by hand (script execution for arbitrary Python is blocked in my mode), intermediates printed:
- Gregorian year = 365.2425 × 86400 = 31,556,952 s; t_0 = 13.797e9 × 31,556,952 = 4.353913e17 s (document prints 4.3539e17 — consistent).
- ħc = 1.054571817e-34 × 2.99792458e8 = 3.1615268e-26; ħc/G = 4.73687e-16; m_P = sqrt = 2.17643e-8 kg. Printed degenerate interval [2.176433e-8, 2.176435e-8] contains it. The V16 escape is closed.
- c⁴ = 8.0776087e33; ħc⁴ = 0.85184; numerator t_0·ħc⁴ = 3.70885e17; denominator 5120πG² = 7.16525e-17; ratio = 5.1762e33; cube root = 1.72983e11 kg — the claimed 1.7298e11, printed down (outward) to 1.729e11, which contains the computed value. Upper = 3.0 × 1.72983e11 = 5.1895e11, printed up to 5.190e11, which contains it.
- 2.2 × 1.98892e30 = 4.375624e30 → printed down 4.375e30; 2.9 × 1.98892e30 = 5.767868e30 → printed up 5.768e30; both printed endpoints contain their computed values.
All four rows reproduce; outward rounding holds at every row including the degenerate one; the disjunctive decision rule has nothing left for a drafting error to disable.

5. RE-RUN GUARD — SOUND. §6 forbids assuming K6's outcome; nothing in the limb structure, the comparator set, or the partition presupposes `K6_FLOOR_UNDERDETERMINED`. The positive class is genuinely reachable: limb B entry plus completion-free in P, all of P agreeing, Z and I empty files DYM_FLOOR_DERIVED, C0's exhibition must demonstrate it pre-freeze, and C6 can pass on that path (verified in heading 4). The guard does not assume repetition in either direction.

6. FAIRNESS — SOUND. "Unreproduced from the stated inputs," not "error," holds in every operative clause I traced: limb A's exit wording, both renamed class tokens and their definitions, the §4 table rows, class 4's inconsistency clause ("report that a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"), and §6's standing rule. V31's rename removes the last operative violation — the tokens themselves — and §8x confirms zero old strings remain in §0–§7 and §9. The two known exceptions are non-operative and disclosed: §0's "the source never supplies" (classified cosmetic at §8o, knowingly declined, and §0 is redacted from seat packets) and the §8 version history, where the old tokens survive only as quotations of what earlier versions said — the record is not rewritten, which is correct.

7. STALL — SOUND. I traced the terminal routing for a state with no fileable class and found none: census/digest/UNRESOLVED/undecidable-after-fallback → DYM_SOURCE_BLOCKED; condition 3 UNDECIDED → C6=FAIL plus DYM_SOURCE_BLOCKED; persistent control failure after two attempts with no evidence unread → R3D_NO_CLASS, with the "otherwise" branch back to BLOCKED making the pair exhaustive; probe FAIL → remove the circular relation, recompute, file the recomputed class with artefacts; probe exit 2 twice → §9 control-failure rule; same-class splits (C6/floors/families) and different-class splits convene the third seat, with re-executed blocked reads and per-report control re-runs covering both the zero-clean and adjudicated-to-non-clean cases; three-way or undecidable split → DYM_SOURCE_BLOCKED, whose enumeration §4 now matches. §9's "every terminal path files exactly one declared class" holds on every path I could construct.

R3D_V31_KIMI_COMPLETE
