ACCESS_SHA=872d4978b73cabc6257328776fe9c7ffc7890b2268dffd160eb0284ee341080a
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES — TWO DEFECTS. INCONCLUSIVE is genuinely reachable (class 2 is a real scientific inconclusive; 5 and 6 are non-scientific terminals). But the classes are neither fully exclusive nor fully exhaustive.

DEFECT 1a — classes 3 and 4 overlap, and the document gives two contradictory rulings for the same physical state.
QUOTE: "If it does not, the K6 obstruction repeats and the answer follows without deep work — file `DYM_NO_SIZE_MASS_RELATION` and stop."
QUOTE: "**DYM_NO_POSITIVE_FLOOR** — the printed relations, alone or under **at least one** admissible completion, permit masses approaching zero. **This class takes precedence over classes 2 and 3.** Report the family."
Constructed result fitting two classes: the branch prints nothing binding size to mass (no size–mass relation, no mass bound). That state satisfies class 3 verbatim, and it also satisfies class 4 as written — with no relation at all, the printed relations (under the empty completion) impose no lower bound, so masses approaching zero are permitted. §3 orders class 3 filed; §4's precedence orders class 4 filed. Both rulings are textually supported, so two obedient seats file different classes and the third seat must choose between two correct readings of a contradictory document. Root cause: "permit" is undefined, so the vacuous case (no relations) falls inside class 4.
REPLACEMENT (for the class-4 sentence): "**DYM_NO_POSITIVE_FLOOR** — the printed relations include at least one relation binding size to mass or bounding the mass, and those relations, alone or under **at least one** admissible completion, permit masses approaching zero, where 'permit' means no positive lower bound on the mass follows; if the printed relations are mutually inconsistent so that no solution — and hence no positive lower bound — exists, file here and report the contradiction. **This class takes precedence over classes 2 and 3.** Report the family (or the contradiction)."
This makes 3 and 4 exclusive (3 = literally no size–mass relation or mass bound printed, which is exactly limb A's test; 4 = relations exist but yield no positive floor), resolves the §3/§4 contradiction in §3's favour, and sweeps the inconsistent-sources case into a fileable class.

DEFECT 1b — a real result fits no class (exhaustiveness hole).
QUOTE: "**DYM_FLOOR_DERIVED** — a unique positive floor follows from the printed relations with no added completion."
QUOTE: "**DYM_FLOOR_UNDERDETERMINED** — the printed relations admit **at least two positive but unequal floors** under admissible completions, **and no admissible completion permits masses approaching zero**."
Constructed result: the printed relations do not fix a floor alone, but under one named admissible completion (e.g. the GR-exterior matching §2 itself contemplates) a unique positive floor follows; every admissible completion yields that same floor; no completion permits masses approaching zero. Not class 1 (a completion was required). Not class 2 (not two unequal floors). Not class 3 (a size–mass relation exists under the completion). Not class 4 (no completion permits zero). Not 5 or 6 (nothing blocked, no control failed). The run reaches a terminal scientific result with no fileable class — the exact stall §9 claims to have closed.
REPLACEMENT (append to class 1's entry): "**DYM_FLOOR_COMPLETION_DEPENDENT** — a unique positive floor follows only under one or more named admissible completions, every admissible completion yields the same floor, and no admissible completion permits masses approaching zero. Report the formula, the completion(s) relied on, and the value; the breaker test is NOT_RUN and the pattern record is not amended, because the floor is not completion-free."

2. CONTROLS — TWO DEFECTS. Every control does name an exact code (C1_SOURCE_IDENTITY, C2_COMPLETION_LEDGER, C3_DELETION_PROBE, C4_GR_BENCHMARK, C5_HARNESS_PINNED, C5B_PATH_LIST, C6_BREAKER_TEST). Unreached controls are handled (explicit NOT_RUN for C5/C5b, conditional NOT_RUN for C6, and §9's blanket "Unreached limbs" rule). C2, C3, C5, C5b, C6 are artefact-bound. C1 and C4 are passable by assertion.

DEFECT 2a — C1 has no pinned reference and no printed-pass artefact.
QUOTE: "**C1 — source identity**, on raw bytes with `repr()` if the pinned text is PDF-extracted. `C1_SOURCE_IDENTITY=PASS`."
DEFECT: the document never lists the pinned sources as files with expected hashes — "entries 18–20, 55" and "the pinned Dymnikova sources" are never bound to a manifest of paths plus expected SHA-256s. §9's ACCESS_SHA rule makes a seat print the hash of whatever file it read, but nothing requires that hash to equal a pinned expected value. "Source identity" is therefore identity to nothing named in the frozen text, and a seat can print C1_SOURCE_IDENTITY=PASS having read a different extraction, edition, or truncation of the source — which is also a census-rigging channel (see finding 3): choosing which artefact counts as "the source" chooses which relations exist.
REPLACEMENT: "**C1 — source identity.** This section prints a manifest of the pinned Dymnikova sources — one row per source: entry number, absolute path under `../bhu-reading-20260823/sources/`, and expected SHA-256. The seat prints the computed SHA-256 of the raw bytes of each file it reads, with `repr()` of the extracted text if the pinned text is PDF-extracted; each printed digest must equal its manifest value; a mismatch, or any read outside the manifest, yields `DYM_SOURCE_BLOCKED`. The printed digests are the artefact; a claimed pass without them fails. `C1_SOURCE_IDENTITY=PASS`."

DEFECT 2b — C4 is pure assertion.
QUOTE: "**C4 — GR benchmark** as algebra only, supplying no interior premise. `C4_GR_BENCHMARK=PASS`."
DEFECT: no procedure, no printed artefact, no fail condition. A seat can write the code having done nothing; nothing distinguishes an executed benchmark from a claimed one. Contrast C2/C3, whose repairs explicitly make a summary assertion fail — C4 never got that repair.
REPLACEMENT: "**C4 — GR benchmark.** The seat prints, for every relation it uses, the stated-limit algebra showing equality with the Schwarzschild form in the exterior limit, and prints the premise list for that algebra showing no interior premise entered; the printed algebra and premise list are the artefact, and a claimed pass without them fails. `C4_GR_BENCHMARK=PASS`."

3. CIRCULARITY — TWO DEFECTS. The reason-code discipline (WRONG_BRANCH, DEFINITION_ONLY, NO_MASS_OR_SIZE_CONTENT, DUPLICATE, each demonstrated by printed source text) is genuinely good against relabelling listed rows. Two channels remain open.

DEFECT 3a — the census can be satisfied while a contrary row is quietly never born.
QUOTE: "**Before choosing a limb or an outcome, each seat constructs and prints an exhaustive census** of every equation or sentence in the pinned Dymnikova sources that mentions or relates **core scale, density, mass, mass function, radius, horizon, matching surface, regularity, or the de Sitter limit**."
DEFECT: the predeclared reason codes govern only rows that are listed. A row that is never surfaced leaves no trace — there is no enumeration key a reviewer can re-run to distinguish "relation absent" from "relation never looked for." Both blind seats are lane seats; a shared pattern-driven blind spot (the assumed-live danger) reproduces in both, and blind doubling does not catch it. The document's own parenthetical admits the motive ("a seat could report a relation absent, omit the contrary row") but the repair it installs (reason codes) covers only exclusion, not omission.
REPLACEMENT: "**Before choosing a limb or an outcome, each seat constructs and prints an exhaustive census** of every equation or sentence in the pinned Dymnikova sources that mentions or relates **core scale, density, mass, mass function, radius, horizon, matching surface, regularity, or the de Sitter limit**; for each source the seat also prints the complete output of a literal string search over that source's extracted text for each of the nine terms, every search hit appears either as its own census row or is cited inside a `DUPLICATE` row naming the covering row, and this search output is part of the C2 artefact — so a relation never listed is mechanically distinguishable from a relation excluded under a reason code."

DEFECT 3b — the pattern record's own conclusions are embedded in the frozen text, laundering the pattern into the seats' briefing.
QUOTE: "It is also, per the pattern record, one of the two places a **breaker** is most likely: a regular-core metric is explicit, so if any branch fixes a mass scale from its own geometry, this is it."
QUOTE: "The gate confirmed D does **not** assume K6's outcome and that `DYM_FLOOR_DERIVED` is genuinely reachable, and judged the branch \"the corpus's most likely candidate to break the pattern\"."
DEFECT: §2 forbids the pattern record "as evidence for inclusion, exclusion or outcome selection," but §0 and §8 restate the record's verdicts inside the frozen document itself. A seat citing §0 of the pre-reg is not, formally, citing the pattern record — the prohibition is bypassed through the document's own text. Expectancy in either direction (here, toward finding the predicted breaker) is the lane's pattern reaching the evidence path ahead of the census.
REPLACEMENT (§0): "A regular-core metric is explicit, so this branch is in principle able to fix a mass scale from its own geometry; whether it does is decided only by the census and the C2 ledger, and no prior lane conclusion — including any prediction about where a breaker is likely — is a reason for inclusion, exclusion or outcome anywhere in this document."
REPLACEMENT (§8): "The gate confirmed D does **not** assume K6's outcome and that `DYM_FLOOR_DERIVED` is genuinely reachable; the gate's expectation about the branch is not evidence and is not carried into this document."

4. THE FALSIFIER — TWO DEFECTS. Not every condition is decidable as specified, because the specification is not in the document.

DEFECT 4a — conditions 1–4 are never stated; their evaluation requires an unpinned lane-authored text.
QUOTE: "**C6 — breaker test.** If class 1 is reached, conditions 1–4 are evaluated by the stated calculation, a full citation-chain provenance table, a free-symbol probe, and a fixity derivation."
DEFECT: the five breaker conditions are nowhere in this frozen document; they live in the pattern record, which is referenced with no path and no hash. "The stated calculation" states nothing here; "free-symbol probe" and "fixity derivation" are named without bounded procedures or a pinned symbol set. Two seats can legitimately evaluate different condition texts (the record is lane-authored and may drift), and the referee cannot verify decidability of conditions the frozen artefact does not contain. The single point where the pattern is allowed to enter is exactly the point where it enters unpinned.
REPLACEMENT: "**C6 — breaker test.** The five breaker conditions are copied verbatim into this section from the pattern record, together with that record's path and SHA-256, each condition carrying its stated procedure, its bounded pass/fail rule, and any pinned comparator or symbol set it needs; evaluation against conditions not so pinned fails C6. If class 1 is reached, conditions 1–4 are evaluated by the stated calculation, a full citation-chain provenance table, a free-symbol probe, and a fixity derivation, each as pinned above."

DEFECT 4b — condition 5's frozen comparator set does not exist in the document that declares itself frozen.
QUOTE: "**So before the run is frozen, a finite named comparator set, the observable and tolerance that define \"shared\", and the permitted source corpus are fixed in writing**; every comparison is executed and printed."
DEFECT: the set, observable, tolerance and corpus are not listed anywhere in this document, yet the header declares the document FROZEN. The freezing instruction is satisfied nowhere; as frozen, condition 5 still has no bounded comparator set. The run-time fallback ("an incomplete frozen set yields DYM_SOURCE_BLOCKED") prevents a wrong pass but guarantees a block at exactly the moment class 1 is reached — the decisive test is pre-disabled by an incomplete freeze.
REPLACEMENT: "**The finite named comparator set, the observable and tolerance that define \"shared\", and the permitted source corpus are listed verbatim in this section as part of this frozen document**; every comparison is executed and printed."

5. RE-RUN GUARD — SOUND. "K6's outcome may **not** be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`" is explicit; class 1 is a genuinely reachable fileable class (nothing in the controls precludes it; C6 is NOT_RUN, not failed, if it is not reached); limb A's "the K6 obstruction repeats" is conditional labeling of outcome 3, not an evidentiary assumption; C3 borrows K6's *method*, not its outcome. No replacement.

6. FAIRNESS — SOUND. The mandated phrasing — "**unreproduced from the stated inputs,** not **error**" — is stated in §6 and held everywhere: class 2 ("Report the freedom; choose none"), class 3 ("the branch supplies no relation binding size to mass"), class 4 ("Report the family"), class 5 ("not a scientific verdict and must never be reported as one"), and §0's description of K6 ("a size–mass relation `V(M)` the source never supplies") are all absence-from-inputs claims; no sentence labels a source, a branch, or the prior study as in error. The occurrences of "defect"/"spurious" in §8/§9 describe this lane's own process history, not the science. No replacement.

7. STALL — TWO DEFECTS. The named paths do terminate in classes: double timeout → DYM_SOURCE_BLOCKED; UNRESOLVED census row → DYM_SOURCE_BLOCKED; unread comparator → DYM_SOURCE_BLOCKED; all-three-differ split → DYM_SOURCE_BLOCKED; control failed in both seats twice → R3D_NO_CLASS. Two terminal states still have no filing rule.

DEFECT 7a — a mixed split (one seat blocked, one scientific) is uncovered.
QUOTE: "if the two blind seats return different scientific classes, **the third seat adjudicates exactly that split from the printed artefacts**; its class is filed **only if it agrees with one of the two**."
DEFECT: the rule engages only when the seats return "different scientific classes." If seat one files DYM_FLOOR_UNDERDETERMINED and seat two files DYM_SOURCE_BLOCKED (its source read failed), the seats disagree terminally but not "scientifically" — no rule convenes the third seat, no rule files anything. Same hole for R3D_NO_CLASS versus any scientific class.
REPLACEMENT: "if the two blind seats return different terminal classes of any kind — scientific, `DYM_SOURCE_BLOCKED` or `R3D_NO_CLASS` — **the third seat adjudicates exactly that split from the printed artefacts**, re-executing any blocked read once before ruling; its class is filed **only if it agrees with one of the two**."

DEFECT 7b — a control failing in exactly one seat, with both seats returning the same scientific class, has no rule.
QUOTE: "**R3D_NO_CLASS** — a control fails in both seats after two attempts."
DEFECT: the text is silent on the one-seat failure. If both seats file class 2 but seat two's C4 failed twice, one reading files class 2 (seats agree), another refuses (class 2's only control-clean support is a single seat, violating blind-double). Two implementations of the document diverge at the terminal step.
REPLACEMENT: "**R3D_NO_CLASS** — a control fails in both seats after two attempts. A scientific class may be filed only from a seat report in which every reached control passed; if the two seats return the same scientific class but exactly one report is control-clean, that class is filed unless the third seat re-runs the failed control and also fails it, in which case `R3D_NO_CLASS` is filed."

R3D_V3_KIMI_COMPLETE
