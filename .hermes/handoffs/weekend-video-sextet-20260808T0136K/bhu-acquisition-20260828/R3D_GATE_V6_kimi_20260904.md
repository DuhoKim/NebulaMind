ACCESS_SHA=14db4dfbdc660fb3d6852f68dcf399adf8724ab8a4b3ef2a3a959760669e3f9e
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES. Seven classes are declared, not six (five scientific, two non-scientific). Two defects.

Defect 1a — a reachable construction files nowhere.
Verbatim (§2): "**A completion is ADMISSIBLE if and only if it introduces exactly one named assumption and is consistent with every printed relation of the manifest sources.**"
Verbatim (§4): "**DYM_FLOOR_UNDERDETERMINED** — the printed relations admit **at least two positive but unequal floors** under admissible completions, **and no admissible completion permits masses approaching zero**."
Defect: "exactly one named assumption" excludes the zero-assumption, completion-free derivation from being an admissible completion. Construct: printed relations fix a unique positive floor F with no completion; exactly one admissible completion yields a different positive floor G; nothing permits masses approaching zero. Class 1's guard ("no admissible completion yields a different floor") bars class 1; class 2 counts only floors "under admissible completions" — one, G — so it is not met; classes 3, 4, 5 each fail their conditions. No class files. The class-1 history note shows this construction was seen and was intended to land in class 2; the strict reading defeats the intent.
Exact replacement (class 2's sentence): "**DYM_FLOOR_UNDERDETERMINED** — the printed relations admit **at least two positive but unequal floors, counting the completion-free derivation, when one exists, as one of the admitted floors**, **and no admissible completion permits masses approaching zero**. Report the freedom; choose none."
With this repair I could construct no remaining overlap or gap (checked 1v4, 1v5, 2v5, 3v4, precedence of 4, inconsistency→4, empty case→3). INCONCLUSIVE is genuinely reachable, via DYM_SOURCE_BLOCKED / R3D_NO_CLASS / UNRESOLVED→BLOCKED; there is no scientific "undecided" — substantive symbolic indecision files BLOCKED, which is terminal and declared, though it misnames a math failure as a source failure (noted, no repair required; the class is explicitly the non-scientific wait bin).

Defect 1b — a substantive C6 failure has no correctly fileable class (also the finding for section 7).
Verbatim (class 1): "Report the formula and value, **and test it against all five breaker conditions**; if it passes them, this is a counterexample to the pattern and the pattern record must be amended."
Verbatim (class 7): "**A scientific class may be filed only from a seat report in which every reached control passed**"
Defect: C6 is one of the controls and is reached exactly when DYM_FLOOR_DERIVED is reached. A substantive C6 failure — the derived floor overlaps a comparator, a physics result, not a procedural fault — leaves the report not control-clean, so class 1 may not be filed from it; no other class fits a derived floor; filing R3D_NO_CLASS mislabels a substantive result as a control stall. The document says what passing C6 does and is silent on what failing C6 files. Two obedient seats diverge at the terminal step on the matching path.
Exact replacement (class 1's sentence): "Report the formula and value, **and test it against all five breaker conditions**; if it passes them, this is a counterexample to the pattern and the pattern record must be amended. **If any breaker condition fails under its stated decision rule, `DYM_FLOOR_DERIVED` is still filed and the failed condition is reported with its artefact: the floor stands, it is not a counterexample, and the pattern record is not amended. A breaker condition failing on its decision rule is a substantive result, not a control failure, and does not engage the control-clean rule of class 7.**"

2. CONTROLS. Sound; no replacement. Every control names an exact code (C1_SOURCE_IDENTITY, C2_COMPLETION_LEDGER, C3_DELETION_PROBE, C4_GR_BENCHMARK, C5_HARNESS_PINNED, C5B_PATH_LIST, C6_BREAKER_TEST); every pass requires its printed artefact, and bare assertion is explicitly failed for C1, C2, C3, C4. Unreached controls are handled: NOT_RUN, never PASS, with the exact-underscore-spelling rule applied document-wide; C6's NOT_RUN-if-class-1-not-reached is correct. Note, not a defect: the C1/C5 anti-transcription rules are enforceable only through §7's mandated re-runs, since a transcribed digest is byte-identical to a computed one — that is the strongest enforcement available to a document-gated lane and the re-runs are mandatory, so I accept it. I verified the §2a manifest against the live filesystem: all four files exist at the pinned relative paths with matching sha256 and byte counts (18: 18761 B, 2f3ca3e1…6b6f; 19: 34214 B, ded87358…af47; 20: 47876 B, 6616e311…5c92; 55: 194150 B, b34183bf…d8be). The expected digests a seat compares against are the true ones.

3. CIRCULARITY. One repair. The pattern record is barred from inclusion, exclusion and outcome selection; the census is no longer keyword-keyed; the eleven terms may not justify any row; exclusion requires a predeclared code demonstrated from source text; DUPLICATE names its covering row; every numbered or displayed equation is its own row. A contrary row can now be omitted only by printing an incomplete "complete extracted text" — and the artefact itself carries no mechanical completeness check; only the second seat's independent print would catch it.
Verbatim (§2c): "For each manifest source the seat **prints the complete extracted text with stable line numbers and assigns every non-blank line exactly one census disposition**"
Exact replacement: "For each manifest source the seat **prints the complete extracted text with stable line numbers and assigns every non-blank line exactly one census disposition, and prints a reconciliation line per source giving the count of non-blank lines and the count of assigned dispositions, which must be equal; a source whose counts differ fails C2**"
With this, an omitted relation (a line in the hashed bytes missing a disposition → count mismatch, caught within the artefact and by the re-run) is distinguishable from an excluded one (row plus demonstrated code), including a relation printed only as a bare displayed equation, which must additionally appear as its own equation row cited by page/line locator.

4. THE FALSIFIER. Conditions 1, 2, 4 and 5 are decidable as specified, with bounded procedures (dimensional classification; provenance confined to the C2 artefact with manifest/§2b termini; fixity table from the C2 artefact; comparator table — see additional checks). Condition 3's non-timeout branch is bounded with an exact PASS criterion. Its timeout branch is not executable as written.
Verbatim (C6 table, condition 3): "On timeout, run the §9 fallback row; if it does not decide, file the applicable non-scientific class"
Defect: §9's fallback table has three rows and none is named here; and no row's stated proposition ("the metric has ≥1 horizon for given (M, r₀)", "the core is regular", "a positive minimum mass exists") is condition 3's proposition (no free normalisation survives). On a real timeout the seat cannot execute "the §9 fallback row" as written, and every timeout path defaults to a non-scientific filing rather than deciding condition 3.
Exact replacement: "On timeout, run the §9 fallback row 'extremise the mass over the core scale' under its own 120-second cap: if that row accepts a positive floor from §2b constants with no parameter chosen, condition 3 PASSES; if the row is absent, times out, or does not accept, condition 3 is `UNDECIDED`, C6 does not pass, and the run files `DYM_SOURCE_BLOCKED`."
Wording note, same row: "the pinned SymPy of §9" pins nothing — §9 requires printing sympy.__version__ but fixes no version, and two seats on different SymPy versions can simplify differently. Replace "with the pinned SymPy of §9" with "with the seat's SymPy, whose printed version is part of the C5 artefact", or pin a version value in §9.

5. RE-RUN GUARD. Sound; no replacement. §6 forbids assuming K6's outcome; DYM_FLOOR_DERIVED is genuinely reachable, and with the comparator table computed in-document its C6 consequence is reachable too (traced below). The earlier branch's floor is nowhere used as an expectation.

6. FAIRNESS. Sound; no replacement. "Unreproduced from the stated inputs" is held in §3 limb A, class 3, and §6; limb A's parenthetical states why the wording matters; no "error" wording is applied to the sources anywhere.

7. STALL. Timeout → SYMBOLIC_TIMEOUT → fallback row → BLOCKED: terminal. Fallback absent/failed → BLOCKED and the control not passed: terminal. Seat split of any kind → third seat → all-differ or undecidable → BLOCKED: terminal. The one path that reaches a terminal state with no correctly fileable class is the substantive C6 failure — defect 1b, whose replacement closes it. No other no-class terminal path found.

ADDITIONAL ROUND-SPECIFIC CHECKS

A. Can condition 5 now PASS on a reachable path? Yes. I executed the comparison logic.
Non-matching case: floor 1.0e15 kg → no overlap with any of the three numerical intervals, ΛCDM row printed as never-matching → condition 5 PASSES; if conditions 1–4 pass, C6_BREAKER_TEST=PASS → DYM_FLOOR_DERIVED filed, counterexample declared, pattern amended. The twice-failed failure mode is closed: every value the test needs is inside the document; nothing is read.
Matching case: floor 2.0e11 kg → overlaps comparator 2 ([1.730e11, 5.189e11]) → condition 5 FAILS → C6 fails → as designed, filing breaks (defect 1b); under repair 1b it files DYM_FLOOR_DERIVED with C6 failed, not a counterexample, pattern not amended. A floor at exactly m_P overlaps the degenerate comparator 1 — also a fail. Both directions file a class once 1b is applied.

B. Number re-derivation (constants exactly as §2b; computed on this machine):
- Planck mass: sqrt(ħc/G) = 2.1764343e-8 kg. The printed 2.176434e-8 REPRODUCES (last-digit rounding).
- Hawking floor: (t_0 ħ c⁴ / 5120π G²)^(1/3) = 1.7298245e11 kg. The printed lower end 1.730e11 REPRODUCES as a rounded value.
- Hawking upper: 3.0 × the unrounded lower = 5.1894735e11 → printed 5.189e11 reproduces; but 3.0 × the printed lower (1.730e11) = 5.190e11 ≠ 5.189e11, and both printed endpoints are rounded INWARD: the interval [1.730e11, 5.189e11] does not contain the document's own computed value 1.7298245e11. Inward rounding narrows the comparator, making condition-5 failure marginally less likely — the bias favours the breaker, not the lane's pattern — but an interval that excludes its own derived point is wrong at stated precision.
Exact replacement (row 2's interval cell): "[1.729e11, 5.190e11]" and in its derivation cell: "lower end `M = (t_0 ħ c⁴ / 5120π G²)^(1/3) = 1.7298e11` kg, rounded down; upper end = 3.0 × lower = 5.1895e11 kg, rounded up. **Comparator endpoints are rounded outward throughout this table.**"
- TOV interval: 2.2 × 1.98892e30 = 4.375624e30 → 4.376e30 REPRODUCES; 2.9 × 1.98892e30 = 5.767868e30 → 5.768e30 REPRODUCES (same outward-rounding convention then applies).
- t_0: 4.3539e17 s reproduces from 13.797 Gyr only under the Gregorian year (365.2425 d = 31,556,952 s); the Julian year gives 4.3540e17. The year length is a value not in §2b and the convention is unstated — repaired in D below.

C. Omitted vs excluded, including a bare displayed equation: distinguishable, with repair 3 applied. A bare equation (r_0² = 3/Λ) is a non-blank line and additionally a displayed equation, so it must carry its own row with a page/line locator; exclusion requires a demonstrated predeclared code; omission shows as a count mismatch under repair 3 and as a cross-seat discrepancy regardless. Without repair 3 the distinction held only across seats, not within the artefact.

D. Assertion check. No control remains passable by bare assertion: each requires its printed artefact; C1/C5 transcription is explicitly failed and policed by the mandated re-runs; conditions 2 and 4 are confined to the C2 artefact; the comparator values are the frozen artefact itself, so a seat copying them into its table is the design, not a violation. One internal contradiction remains.
Verbatim (C6): "**Every comparator interval is derived from §2b inside this document.**"
Defect: false as stated. Row 2's upper end uses a factor 3.0 that the document itself says is "stated as a bound, not derived here"; row 3's [2.2, 2.9] span is asserted ("the span over which the TOV maximum is contested"); the 5120π lifetime coefficient and the Gregorian year length inside t_0 are likewise introduced. The values ARE supplied, not deferred, and are disclosed — but they are not derived from §2b, so the sentence breaks §2b's own rule ("**No other value may be introduced as "standard"**") and a strict seat could treat the comparator table as an added completion.
Exact replacement: "**Every comparator interval is computed in this document from §2b constants together with the stated dimensionless inputs — the single-species lifetime coefficient 5120π, the row-2 upper-end factor 3.0, the row-3 span [2.2, 2.9] M_☉, and the Gregorian year length 365.2425 d inside t_0 — which are asserted bounds, not derivations, are disclosed here so that nothing needs to be read, and are part of the frozen artefact, not added completions under C2.**"
Verbatim (§2b): "`G = 6.67430e-11` m³ kg⁻¹ s⁻², `c = 2.99792458e8` m s⁻¹, `ħ = 1.054571817e-34` J s, `k_B`, the solar mass `M_☉ = 1.98892e30` kg, and the age of the universe `t_0 = 13.797` Gyr `= 4.3539e17` s."
Defect: k_B is listed with no value; the t_0 equality's year convention is unstated.
Exact replacement: "`G = 6.67430e-11` m³ kg⁻¹ s⁻², `c = 2.99792458e8` m s⁻¹, `ħ = 1.054571817e-34` J s, `k_B = 1.380649e-23` J K⁻¹, the solar mass `M_☉ = 1.98892e30` kg, and the age of the universe `t_0 = 13.797` Gyr `= 4.3539e17` s using the Gregorian year of 365.2425 × 86400 s."

E. Executability. Every literal command in the document is executable as written; verified on this machine: python3 --version → Python 3.9.6; the sympy one-liner (equivalent executed here: sympy 1.14.0 imports and prints); shasum -a 256 $(command -v python3) → produced a digest of /usr/bin/python3. The V5 `sympy.__version__`-as-shell-command defect is genuinely fixed. No other literal commands exist (fallback-table entries are procedures, not commands).

F. Deferred-content verdict, plainly: the V6 comparator repair SUPPLIES content — values, formulas, tolerance and decision rule are all in-document and no file needs to be read; the failure mode that killed V3 (requiring content) and V4 (supplying values behind forbidden reads) is closed. Two residual content gaps: (i) condition 3's timeout branch still defers the decision — it points at fallback rows none of which decides its proposition, so a timeout never decides condition 3 and only defaults to BLOCKED (repair in finding 4); (ii) the sentence claiming every comparator interval is "derived from §2b" overclaims — three inputs are asserted, not derived, and must be named as such (repair in D). Everything else this round is supplied, including the manifest digests, which I verified byte-for-byte against the real files.

R3D_V6_KIMI_COMPLETE
