ACCESS_SHA=02c2495b14c6a0cd58708ff8b8a9a7a903c29bd1b05ac39c19e33e05f211ad11
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES

The scientific outcome classes are exhaustive and mutually exclusive once class 4's stated precedence is applied. The discriminators are: no reproduced binding relation (class 3); a binding relation permitting masses approaching zero or inconsistent printed relations (class 4); a completion-free floor with all admissible completions agreeing (class 1); a completion-free floor with a differing admissible floor and none permitting zero (class 2); or no completion-free floor but at least one completion-dependent positive floor and none permitting zero (class 5). Classes 6 and 7 are ordered administrative outcomes rather than competing scientific conclusions. I could construct neither a result fitting two final classes nor a result fitting none. `DYM_SOURCE_BLOCKED` and `R3D_NO_CLASS` make an inconclusive terminal result genuinely reachable.

Sound; no replacement.

2. CONTROLS

C1-C6 and C5b each have an exact code. C1, C2, C3, C4, and C5b require printed artefacts; C5 requires printed execution of three literal commands; C6 requires the condition-specific artefacts and completed comparison table. Section 9 extends `NOT_RUN` to every unreached control, so unreached controls cannot be asserted as passes. The three C5 commands execute exactly as written in this environment: `python3 --version`, `python3 -c "import sympy; print(sympy.__version__)"`, and `shasum -a 256 $(command -v python3)` all completed successfully. No control is passable merely by transcribing a value printed elsewhere: section 9 expressly rejects transcription for C1 and C5, while the other controls require derivation/run artefacts rather than expected answers.

There is, however, one substantive internal control contradiction. Verbatim sentence: “**Those four are asserted bounds and conventions, NOT derivations from §2b**; each is **recorded as an `ADDED_COMPLETION` in the C2 ledger**, and condition 5 may pass only if **C6 condition 2 accepts them under its provenance rule**.” C6 condition 2 passes only when every constant terminates in a manifest-source equation or the §2b list, and explicitly fails an assumed terminus. The four declared assertions satisfy neither allowed terminus. Consequently condition 2 cannot accept them, condition 5 cannot pass under this sentence, and C6 cannot pass on any `DYM_FLOOR_DERIVED` path. This is the same functional failure mode as the prior rounds, although the comparator numbers themselves are now supplied.

Exact replacement sentence: “**Those four are frozen comparator conventions, NOT constants in the derived floor and NOT derivations from §2b**; each is printed in the completed C6 condition-5 comparison artefact, does not enter the C2 completion ledger, and is outside C6 condition 2's provenance test of the derived magnitude.”

3. CIRCULARITY

The lane-authored pattern cannot select relations or outcomes under the frozen rules. The census accounts for every non-blank line with exactly one visible disposition, prints the complete line-numbered text, reconciles non-blank-line and disposition counts, and separately requires every numbered or displayed equation as its own row. Thus an omitted relation is distinguishable from an excluded one: omission breaks the reconciliation or equation list, whereas exclusion leaves a row, locator, verbatim text, and predeclared reason. This includes a bare displayed equation. Exclusions must be demonstrated from independent source text, and the pattern is barred from inclusion, exclusion, and outcome selection until post-derivation C6.

Sound; no replacement.

4. THE FALSIFIER

Conditions 1, 3, 4, and 5 have bounded decision procedures as written. Condition 2 is syntactically bounded by the reproduced C2 passages, but it cannot return PASS after the document requires it to accept four inputs that the same rule necessarily rejects. Therefore the five-condition breaker is not pass-capable as specified. The defect and exact replacement are given in finding 2.

Condition-5 path trace: for a hypothetical completion-free derived point floor of `2.0e11 kg`, the floor overlaps the Hawking interval `[1.729e11, 5.190e11]`; condition 5 FAILS, and—assuming the class-1 premises and other reached controls hold—the report files `DYM_FLOOR_DERIVED`, reports the failed breaker, and does not amend the pattern. For a hypothetical point floor of `1.0e20 kg`, none of the three numerical intervals overlaps and ΛCDM has no minimum-mass interval; the finite comparison itself would PASS and the scientific class would again be `DYM_FLOOR_DERIVED`. As frozen, however, the quoted dependency on condition 2 prevents condition 5/C6 from actually passing even on this non-matching path, so the pattern still cannot be amended.

Independent arithmetic from §2b:

- Planck mass: `sqrt(ħc/G) = 2.1764343420511267e-8 kg`; printed `2.176434e-8 kg` reproduces.
- Hawking lower value: `(t_0 ħ c^4/(5120πG^2))^(1/3) = 1.7298245132213753e11 kg`; times 3 is `5.189473539664126e11 kg`. The printed intermediate values and outward interval `[1.729e11, 5.190e11]` reproduce.
- TOV endpoints: `2.2 M_☉ = 4.375624e30 kg` and `2.9 M_☉ = 5.767868e30 kg`; the outward interval `[4.375e30, 5.768e30]` reproduces.

No comparator number is deferred. The pass criterion is nevertheless deferred to an impossible provenance acceptance; the exact replacement above closes it without an external read.

5. RE-RUN GUARD

The design expressly forbids assuming K6 repeats, and `DYM_FLOOR_DERIVED` is scientifically reachable. A substantive breaker failure still files that positive scientific class. The present C6 contradiction prevents the stronger counterexample/pattern-amendment consequence, but it does not force the earlier branch's underdetermined scientific outcome.

Sound as to re-run independence; no replacement beyond finding 2.

6. FAIRNESS

Negative findings are consistently framed as “unreproduced from the stated inputs,” including limb A, class 3, and the inconsistent-relations branch of class 4. The document expressly forbids “error.”

Sound; no replacement.

7. STALL

Timeouts have a bounded second attempt and then file `DYM_SOURCE_BLOCKED`; missing or nondeciding fallbacks do the same. Persistent control failure files `R3D_NO_CLASS`. Seat disagreements invoke a third seat, and all-three disagreement or inability to decide files `DYM_SOURCE_BLOCKED`. The special same-class/one-dirty-seat case is also resolved. Every terminal path therefore has a fileable class. A C6 substantive FAIL files `DYM_FLOOR_DERIVED`, so it is not misclassified as a control stall.

Sound; no replacement.

Overall: the census, outcome partition, commands, arithmetic, fairness, rerun guard, and terminal-state rules are sound. One exact repair is mandatory before execution: comparator conventions must be separated from the derived-floor provenance test. The document has supplied the comparator content, but its current cross-condition dependency still makes the decisive breaker impossible to pass.

R3D_V7_GATE_COMPLETE
