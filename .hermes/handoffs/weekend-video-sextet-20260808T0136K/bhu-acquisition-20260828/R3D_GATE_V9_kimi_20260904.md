ACCESS_SHA=b7883c25784cdbc2617ac7f17ae9accf90f75785866b8849cec4a272111387d2
GATE=PREREG_SOUND_WITH_REPAIRS

1. OUTCOME CLASSES
Mostly sound, one genuine gap. Classes 1–5 are pairwise exclusive given the stated discriminators: the 1/2/5 split on whether a completion-free floor exists is clean, class 4's "permit" is defined derivationally, the 4-over-2 precedence is stated, and the empty case is confined to class 3 by limb A's stop. I could not construct a result fitting two classes. An inconclusive result is genuinely reachable: DYM_SOURCE_BLOCKED fires on a C1 digest mismatch, an UNRESOLVED census row, a fallback failure, or a three-way seat split, and it is explicitly not a scientific verdict.

Defect — a reachable physical state with no fileable class. The document's own vocabulary, in class 5's reporting instruction, separates "a unique floor" from "no attained minimum":
  VERBATIM: "Report, for every admissible completion, whether it yields a unique floor, a set or range of positive floors, or no attained minimum"
But the completion-free discriminator has no such treatment: class 1 requires that "a unique positive floor follows from the printed relations." The physically central case for this exact metric family is an extremal mass: horizons exist only at or above a critical mass, with a degenerate horizon at the critical value. If the printed relations bound the mass strictly below by a positive critical value whose attainment is itself contested (degenerate horizon counted as a black hole or not), the completion-free result is a positive infimum with no attained minimum. It then fits no class: not 1 or 2 (no "floor" follows, on the document's own attained/unattained distinction), not 4 (a positive lower bound DOES follow, so it does not "permit masses approaching zero" under the stated definition), not 3 (a relation exists), and not 5 (whose discriminator — whether a completion-free floor exists — becomes a seat's judgement about attainment). Two obedient seats can file 1 and 5 for identical physics.
  EXACT REPLACEMENT (new sentence inserted at the head of §4, before class 1): "Throughout this section, a floor is any positive greatest lower bound on the black-hole mass that follows from the stated relations, whether or not that bound is itself attained; attainment status is reported with every floor value, and an unattained positive bound counts as a floor for every class discriminator in this section."

2. CONTROLS
Every control names an exact code (C0_REACHABILITY, C1_SOURCE_IDENTITY, C2_COMPLETION_LEDGER, C3_DELETION_PROBE, C4_GR_BENCHMARK, C5_HARNESS_PINNED, C5B_PATH_LIST, C6_BREAKER_TEST). Unreached controls are handled: NOT_RUN, with the exact underscore spelling mandated globally. C2, C3, C4, C5, C5b each require a printed artefact and explicitly fail a bare claim.

Defect — C0's quantifier names nothing after de-patterning.
  VERBATIM: "For **every declared outcome class of §4**, and for **every C6 breaker condition whose failure would refute this lane's own expectation**, **exhibit a concrete input that produces it**"
  §0 now forbids any prior lane expectation from entering the document, so "this lane's own expectation" is undefined; the set of conditions requiring exhibition reads as anything from empty to all five depending on the seat.
  EXACT REPLACEMENT: "For **every declared outcome class of §4**, and for **each of C6's five breaker conditions, in both its PASS and its FAIL direction**, **exhibit a concrete input that produces it**: a specific numeric value or stated configuration, and the path it takes through this document to that verdict."

Defect — C0 cannot be satisfied in the order the document specifies, and its required content is absent.
  VERBATIM: "**C0 — reachability, run BEFORE the freeze.**" and "**The exhibitions are authored by a seat and only verified by Tori**", against the header "**FROZEN pending the fresh referee gate.**" and §8a's own admission "**but that is my check and does not satisfy C0**, which requires the seat's exhibition."
  The artefact requires a seat; no seat has run; the only existing exhibition is disclaimed by the document itself; so the document asserts a frozen state its own freeze-precondition forbids, and no step says when the exhibition table is produced.
  EXACT REPLACEMENT: "**C0 — reachability, executed as a named pre-freeze step: after this gate and before dispatch, a seat authors the exhibition table and Tori verifies it; the completed table is appended to this document as §5a, and only then does the freeze take effect. As gated, this document is PRE-FREEZE.**"

Defect — the §9 scope rule is not a partition, but C5b requires a clause for every path and fails on any OUT_OF_SCOPE row.
  VERBATIM: "this lane's own reading tree (`../bhu-reading-20260823/sources/`) is **in scope**, another lane's files are not."
  This document itself, the seat's harness, interpreter and environment files are neither, so no clause exists for paths a seat must open.
  EXACT REPLACEMENT: "this lane's own reading tree (`../bhu-reading-20260823/sources/`), this document, and the seat's own harness, interpreter and environment files are **in scope**; another lane's files are not, and any other path is not."

Named limitation, no replacement: C1 remains passable by transcription in practice. §2a prints the expected digests beside the artefact that must contain them, and a printed digest cannot distinguish computed from copied. The anti-transcription sentence is honor-system; no printed scheme closes this without witnessed execution. Detection rests entirely on §7's "Tori re-runs everything," which is outside the control. I judge this acceptable-by-design because the independent re-run is mandated, but it is the one control whose artefact is forgeable from the document alone.

3. CIRCULARITY
Sound, no replacement. The reconciliation count (non-blank lines equal assigned dispositions, per source, else C2 fails) turns a silently omitted contrary row into visible arithmetic. The equation-list cross-check requires every numbered or displayed equation as its own row, so a contrary relation printed only as a bare displayed equation cannot be buried inside a bounded block or disposed under a prose disposition. An inline relation mis-disposed under a reason code still carries its own verbatim text for the second seat and the audit. Exclusion requires a predeclared code demonstrated from source text; disagreement is UNRESOLVED, may not be treated as absence, and forces DYM_SOURCE_BLOCKED. The pattern record is forbidden as evidence for inclusion, exclusion, or outcome selection, and enters only at C6, after DYM_FLOOR_DERIVED is reached on other grounds, as a hash-pinned verbatim test. The lane's pattern cannot reach the evidence.

4. THE FALSIFIER
Conditions 1–4 are each decidable with a bounded procedure: dimensional classification; provenance confined to the C2 artefact's reproduced passages with an explicit terminus rule; the free-symbol probe with a 120-second cap and a §9 fallback row ("free-symbol survival") that now exists and decides exactly condition 3's proposition, with UNDECIDED routed to DYM_SOURCE_BLOCKED; the fixity table with a source line per row. Condition 5's interval-overlap test is decidable and trivial.

Defect — row 1's printed interval violates the document's own rounding convention and does not contain its computed value.
  VERBATIM: "| 1 | semiclassical GR + QM: a Planck-scale remnant | `[2.176434e-8, 2.176434e-8]` | `m_P = sqrt(ħc/G)`; a point value, so the interval is degenerate |"
  and the convention: "**Comparator endpoints are rounded OUTWARD throughout the table.**"
  I re-derived m_P from §2b: sqrt(1.054571817e-34 x 2.99792458e8 / 6.67430e-11) = 2.1764343420511267e-8 kg (matching the V8 seat's value). Both printed endpoints equal the value rounded DOWN, so the interval excludes the computed Planck mass itself. A floor of exactly m_P stated at full computed precision does not overlap row 1, so the canonical case this comparator exists to catch returns condition-5 PASS — and §8a's self-check claim "m_P → FAIL overlapping Planck" is false against the printed table. This is V6's inward-rounding defect migrated to row 1, with the same bias direction: a narrowed comparator makes condition-5 failure less likely, biasing the study toward declaring a counterexample.
  EXACT REPLACEMENT: "| 1 | semiclassical GR + QM: a Planck-scale remnant | `[2.176434e-8, 2.176435e-8]` | `m_P = sqrt(ħc/G) = 2.1764343e-8 kg`; a point value, so the interval is its outward rounding — the lower endpoint rounded down, the upper endpoint rounded up |"

5. RE-RUN GUARD
Sound, no replacement. §6 forbids assuming K6's outcome, and the positive class is genuinely reachable: a derived floor of 1.0e15 kg with constants traced to §2b and the manifest, free-symbol probe clean, fixity table complete, four-row table printed, overlaps nothing — files DYM_FLOOR_DERIVED with C6_BREAKER_TEST=PASS and amends the pattern record. The opposite answer to K6 is a live outcome.

6. FAIRNESS
Sound, no replacement. The wording "unreproduced from the stated inputs" is held in limb A's exit, in class 3's definition, and in class 4's inconsistency branch ("a consistent solution, and hence a positive lower bound, were unreproduced from the stated inputs"). Class 4's "permit" is defined as "no positive lower bound on the mass follows" — derivational, not existential. No negative finding anywhere is called an error.

7. STALL
One residual, beyond the class-coverage gap in finding 1. Timeout, fallback failure, and seat disagreement all terminate in a named class: SYMBOLIC_TIMEOUT routes to a fallback row that now decides its proposition; a fallback that is absent, timed out, or undeciding files DYM_SOURCE_BLOCKED; the seat-split rule covers any terminal-class disagreement, with three-way splits filing DYM_SOURCE_BLOCKED.

Defect — C6's single FAIL code carries two different terminal consequences, and the rule that consumes it names no exception.
  VERBATIM: "**`C6_BREAKER_TEST=FAIL`** — the table is incomplete or missing, **or** any condition fails its decision rule; **name the condition and print its artefact**."
  and in class 7: "**A scientific class may be filed only from a seat report in which every reached control passed**"
  A substantive condition failure is supposed to leave DYM_FLOOR_DERIVED fileable (class 1 says so); an incomplete table is supposed to be a control failure engaging class 7. Both print the identical code C6_BREAKER_TEST=FAIL, and class 7's control-clean rule contains no exception for the substantive case — the exception exists only in parentheticals attached to class 1 and to C6's outcome list. On the one path the study exists to produce, a seat applying class 7 literally cannot file class 1 from a report whose C6 reads FAIL, while a seat applying class 1 files it. That is a terminal divergence on the primary path.
  EXACT REPLACEMENT: "**`C6_BREAKER_TEST=FAIL`** — printed as exactly one of `FAIL_SUBSTANTIVE` (a condition failed its decision rule; name the condition and print its artefact; §4 class 1 remains fileable and the report counts as control-clean for the purposes of §4 class 7) or `FAIL_CONTROL` (the comparison table is incomplete or missing; name what is missing; the report is not control-clean and §4 class 7's rule applies)."

ROUND-SPECIFIC CHECKS (comparator-content round)

Condition 5 pass/fail paths, traced:
  Matching case: floor 2.0e11 kg falls in [1.729e11, 5.190e11] — overlaps comparator 2, named. Condition 5 FAILS; C6 prints FAIL_SUBSTANTIVE (per the repair above); DYM_FLOOR_DERIVED is still filed, the floor stands, it is not a counterexample, the pattern record is not amended.
  Non-matching case: floor 1.0e15 kg overlaps nothing; condition 5 PASSES; with conditions 1–4 satisfied and the four-row table printed, C6=PASS; class 1 filed as a counterexample and the pattern record is amended. Floor 3.0e30 kg sits between the Hawking and TOV intervals and also passes. So yes — condition 5 can now genuinely pass and genuinely fail on reachable paths, with one exception: the m_P path is broken by the row-1 endpoint defect (finding 4).

Number re-derivation (all computed by me from §2b):
  t_0: 13.797e9 x 31,556,952 s (Gregorian) = 4.3539127e17 s — printed 4.3539e17, correct; Julian gives 4.3540e17, as stated.
  Planck mass: 2.1764343420511267e-8 kg — the derivation claim is correct, but the printed interval excludes this value (finding 4). DOES NOT REPRODUCE as printed.
  Hawking floor: (t_0 hbar c^4 / 5120 pi G^2)^(1/3) = 1.7298245e11 kg — printed "1.7298e11" correct, interval lower 1.729e11 correctly rounded down. Upper 3.0 x unrounded = 5.1894735e11 — printed "5.1895e11, rounded up" correct, interval upper 5.190e11 correctly outward. Interval contains both computed endpoints. Reproduces.
  TOV: 2.2 x 1.98892e30 = 4.375624e30, rounded down to 4.375e30, correct; 2.9 x 1.98892e30 = 5.767868e30, rounded up to 5.768e30, correct. Reproduces.

Census, omitted vs excluded, bare displayed equation: yes, distinguishable on all three axes. Omitted line: reconciliation counts differ, C2 fails. Excluded line: a row with a predeclared code and verbatim text. Bare displayed equation: must appear as its own row via the equation-list cross-check AND within the line accounting; a mangled PDF-extraction fragment that a seat cannot classify is UNRESOLVED and blocks. The V4 blind spot is closed.

Assertion/transcription: every control except the named C1 limitation requires a printed artefact whose absence fails the pass. C1's artefact is forgeable from §2a alone; enforcement is §7's independent re-run. No other control can be passed by transcribing a value printed in the document (the comparator values must be re-derived and are checked by the second seat; C5's expected outputs are not printed in the document).

Commands: all three §9 commands execute as written. Verified on this machine: python3 --version -> Python 3.9.6; python3 -c "import sympy; print(sympy.__version__)" -> 1.14.0; shasum -a 256 $(command -v python3) -> b8763cf2... /usr/bin/python3. All exit 0.

Deferral, plainly: the comparator content is genuinely supplied — every interval is computed in this document from §2b plus four named, printed conventions, and no comparator source needs to be read. The V3/V4 deferral mode is closed there. But the deferral pattern reappears inside the new control: C0's exhibition table is required content, referenced as the control's artefact, and not supplied — the document's own §8a says the only existing check "does not satisfy C0." That is requiring content rather than supplying it, in the one control added this round.

ROUND-SPECIFIC CHECKS (polarity-redesign round)

PASS and FAIL paths with floor values: PASS — floor 1.0e15 kg (or 3.0e30 kg), no overlap, condition 5 passes, C6 passes, class 1 filed as counterexample. FAIL — floor 2.0e11 kg overlaps [1.729e11, 5.190e11] (Hawking), condition 5 fails, class 1 filed with the floor standing and the pattern unamended. A floor at m_P should fail against row 1 and does not as printed (finding 4).

Any clause still preconditioning condition 5's PASS on something that can fail: no. "There is no other precondition on the pass" is now true of condition 5 itself — FAIL iff overlap, else PASS. C6's PASS remains a conjunction (complete table and conditions 1–4 each satisfied), but every conjunct has its own bounded decision rule and is independently passable; none is the structural trap condition 5 was.

C6's PASS/FAIL/NOT_RUN set: complete and mutually exclusive as control outcomes, yes. But FAIL is not single-valued downstream — one code, two terminal consequences (finding 7). The split into FAIL_SUBSTANTIVE / FAIL_CONTROL is the repair.

Incomplete comparison recorded as a counterexample without failing a control: the specific hole is closed — an incomplete or missing table fails C6 loudly by name, so an incomplete comparison cannot ride the default PASS into a counterexample. The residual hole is a complete-but-wrong table: four rows present with an incorrect no-overlap verdict fails no control inside the document, because no control re-verifies the seat's overlap arithmetic. Detection rests on the second seat's independent derivation and §7's "Tori re-runs everything." The document states this accepted cost openly rather than hiding it, and I confirm it is stated.

C0 well-defined, and can C0 fail: it can fail in its designed way — UNREACHABLE blocks the freeze, which is loud and safe. But as written it is not well-defined, twice: the quantifier over breaker conditions references a lane expectation the de-patterned document no longer contains (finding 2), and its freeze-before-exhibition sequencing contradicts the document's own header and its own §8a disclaimer (finding 2). As printed, C0 has already been bypassed: the document is labelled FROZEN while asserting its freeze-precondition is unsatisfied.

Summary of required repairs: (1) define "floor" to include unattained positive bounds at the head of §4; (2) row-1 comparator interval to [2.176434e-8, 2.176435e-8]; (3) split C6's FAIL into FAIL_SUBSTANTIVE / FAIL_CONTROL with the control-clean consequence in the rule; (4) enumerate C0's exhibition set as all §4 classes plus all five conditions in both directions; (5) make C0 a named pre-freeze step and label the gated document PRE-FREEZE until its exhibition table is appended; (6) extend the §9 scope rule to a full partition. None disables the design; all are one-sentence or one-token changes.

R3D_V9_KIMI_COMPLETE
