ACCESS_SHA=1ecb7ac7ae80352ea72bf2ab3c46faf699fe0c1f1e84761f72e4e54bd76748f5
GATE=PREREG_UNSOUND

1. OUTCOME CLASSES. QUOTED (confirmed present in the hashed file): “**DYM_FLOOR_UNDERDETERMINED** — admissible completions give different floors. Report the freedom; choose none.” QUOTED (confirmed present in the hashed file): “**DYM_NO_POSITIVE_FLOOR** — the relations exist and permit masses approaching zero. Report the family.” Defect: these classes overlap whenever two admissible completions give different results and at least one permits masses approaching zero. A real result consisting of one completion with a positive floor and another with no positive floor fits both classes 2 and 4. The six classes are therefore not mutually exclusive. The blocked and control-failure routes make inconclusive outcomes reachable, but there is also a stall risk addressed in finding 7. EXACT REPLACEMENT: “2. **DYM_FLOOR_UNDERDETERMINED** — the printed relations admit at least two positive but unequal floors under admissible completions, and no admissible completion permits masses approaching zero. Report the freedom; choose none. 3. **DYM_NO_SIZE_MASS_RELATION** — limb A’s exit: the branch supplies no relation binding size to mass. 4. **DYM_NO_POSITIVE_FLOOR** — the printed relations, either alone or under at least one admissible completion, permit masses approaching zero; this class takes precedence over classes 2 and 3. Report the family.”

2. CONTROLS. QUOTED (confirmed present in the hashed file): “**C2 — completion ledger.** Every added completion named as such, never as source-derived. `C2_COMPLETION_LEDGER=PASS`.” Defect: C2 can be passed by assertion. It requires neither the ledger itself nor source locators and quotations supporting every “source-derived” classification. That is fatal here because relabeling or omitting a candidate relation can manufacture the no-relation/underdetermined result. QUOTED (confirmed present in the hashed file): “**C5 — harness, LIVE**; **C5b — path list.** As R3A/R3B.” Defect: C5 and C5b do not each assert an exact named code in this preregistration; “As R3A/R3B” leaves the names implicit. C6 is materially stronger: its five individual printed evaluations are artefacts, not a bare assertion, and it correctly permits `NOT_RUN` off class 1. Section 9 also correctly says unreached controls are `NOT RUN`, never passes. EXACT REPLACEMENT: “- **C2 — completion ledger.** The seat must print a row for every candidate premise or relation considered, with status `SOURCE_DERIVED`, `ADDED_COMPLETION`, or `UNRESOLVED`; every `SOURCE_DERIVED` row must include the pinned source path, page/line locator, and verbatim supporting text, and every `ADDED_COMPLETION` row must name the added assumption. The full printed ledger is the artefact required for `C2_COMPLETION_LEDGER=PASS`; a summary assertion fails. An `UNRESOLVED` row may not be excluded and forces `DYM_SOURCE_BLOCKED`. - **C5 — harness, LIVE.** Execute and print the three commands specified in §9. `C5_HARNESS_PINNED=PASS`. - **C5b — path list.** Print every opened path and verify it against §9’s scope rule. `C5B_PATH_LIST=PASS`. Unreached C5/C5b controls are recorded `NOT_RUN`, never `PASS`.”

3. CIRCULARITY. QUOTED (confirmed present in the hashed file): “The metric and its stated stress-energy; the core scale parameter and what fixes it; which mass (ADM, Misner–Sharp, Komar); which surface; the regularity conditions; and the de Sitter-limit condition.” Defect: this is a shopping list, not an inclusion/exclusion protocol. The design never requires an exhaustive census of candidate printed relations and never requires independent evidence before excluding a row. A seat can simply say that a mass, surface, or core-scale relation is absent, omit the contrary row, pass C2 by assertion, and then use the lane-constructed pattern as indirect support for `DYM_NO_SIZE_MASS_RELATION` or `DYM_FLOOR_UNDERDETERMINED`. C3 only tests an injected relation after the seat has selected the source-pinned field equations; it cannot detect a source relation excluded before that selection. Thus the pattern can indirectly become evidence for itself. EXACT REPLACEMENT: “Before choosing a limb or outcome, each seat must construct and print an exhaustive census of every equation or sentence in the pinned Dymnikova sources that mentions or relates core scale, density, mass, mass function, radius, horizon, matching surface, regularity, or the de Sitter limit. Every included and excluded row must carry a source path, page/line locator, and verbatim text. Exclusion is allowed only for a predeclared reason code (`WRONG_BRANCH`, `DEFINITION_ONLY`, `NO_MASS_OR_SIZE_CONTENT`, or `DUPLICATE`) demonstrated by that independent source text; disagreement or missing evidence is `UNRESOLVED`, may not be treated as absence, and forces `DYM_SOURCE_BLOCKED`. The shape/magnitude pattern record and all prior lane conclusions are forbidden as evidence for inclusion, exclusion, or outcome selection. C2 passes only if this census artefact is printed in full.”

4. THE FALSIFIER. QUOTED (confirmed present in the hashed file): “**C6 — breaker test.** If class 1 is reached, all five pattern-breaker conditions are evaluated and printed individually.” Defect: conditions 1–4 are decidable by calculation, provenance tracing, a free-symbol probe, and a fixity derivation respectively. Condition 5 is not decidable as written: a seat cannot establish that the number is not shared with “any standard model” without a bounded comparator set and search rule. Printing an evaluation does not make the universal negative operational. EXACT REPLACEMENT: “- **C6 — breaker test.** If class 1 is reached, conditions 1–4 are evaluated by the stated calculation, full citation-chain provenance table, free-symbol probe, and fixity derivation. For condition 5, before the run freeze a finite named comparator set, the observable and tolerance defining ‘shared’, and the permitted source corpus; execute and print every comparison. A match fails condition 5; an unread comparator or incomplete frozen set yields `DYM_SOURCE_BLOCKED`; only a completed no-match table passes condition 5. Print all five results and the underlying artefacts. `C6_BREAKER_TEST=PASS`; otherwise `NOT_RUN` only when class 1 is not reached.”

5. RE-RUN. QUOTED (confirmed present in the hashed file): “K6's outcome may **not** be assumed to repeat: this is a different branch and the study must be able to return `DYM_FLOOR_DERIVED`.” Defect: none. This is a distinct Dymnikova branch and does not repeat the closed ECKS-density calculation. EXACT REPLACEMENT: NONE.

6. FAIRNESS. QUOTED (confirmed present in the hashed file): “The record's wording for any negative finding is **\"unreproduced from the stated inputs,\" not \"error.\"** No tier, token, standing or stamp moves.” Defect: none; no fairness slip appears elsewhere in the frozen design. EXACT REPLACEMENT: NONE.

7. STALL. QUOTED (confirmed present in the hashed file): “**Stall guard**: every symbolic operation under a 120-second cap; on timeout print `SYMBOLIC_TIMEOUT` and fall back.” Defect: neither the fallback nor its own failure outcome is specified. The design also gives no deterministic aggregation rule for two seats returning different scientific classes; “third seat via the wrapper” does not state when it is triggered or how it rules. Either path can stop without a fileable outcome class. EXACT REPLACEMENT: “- **Stall guard**: every symbolic operation has a 120-second cap. On timeout print `SYMBOLIC_TIMEOUT` and execute the named numerical/substitution fallback under a second 120-second cap. If that fallback times out or cannot decide the required proposition, file `DYM_SOURCE_BLOCKED`; do not pass the affected control. If the two blind seats return different scientific classes, the third seat must adjudicate exactly that split from the printed artefacts; its class is filed if it agrees with either seat, and if all three differ or the third seat cannot decide, file `DYM_SOURCE_BLOCKED`. Every terminal path therefore files exactly one declared class.”

R3D_CODEX_GATE_COMPLETE
