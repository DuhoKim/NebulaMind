# RQ-B reconciliation — the Popławski interior transfer function (BHU Lane 2, task 4)

**Tori, 2026-09-01.** Blind-double on codex (`RQ_B_codex_RESULT.md`, 6m50s) + agy (`RQ_B_agy_RESULT.md`).
**Tier move NOT made — returns to Duho as q4.** A genuine seats-split; a third read is in flight.

## What the seats AGREE on

- **The field equations.** Both assemble the same torsion-modified closed-FLRW Friedmann system with
  the spin term `ε̃ = ε − α n_f²`, `α = κ(ℏc)²/32`, and the effective density `−α n_f² ∝ a⁻⁶`.
- **The e-fold count is small.** The torsion bounce yields only a modest expansion — Popławski's own
  numbers: a factor √2 in the pure-torsion accelerating phase (entry 9), ~10¹⁰ ≈ **~23 e-folds** with
  particle production (entry 11). Both note this is ≪ the ~60 of inflation.
- **The mechanism has field equations only for the HOMOGENEOUS background,** and it maps parent
  mass/radius → daughter homogeneous initial data (entry 12). No source supplies a perturbation
  (Fourier-`k`) system.

## Where they SPLIT — the transfer function

- **codex → `UNDETERMINED_NEEDS_LINEAR_ECSK_PERTURBATION_ACTION`.** The papers fix the background
  bounce but **not** a perturbation transfer. The super-horizon curvature transfer needs a quadratic
  ECSK spin-fluid/Dirac perturbation action + a spin-correlation closure + bounce/junction matching —
  **none supplied.** Multiple closures consistent with the *same* background give *different* `T`
  (adiabatic vs spin-isocurvature; the PRD **cusp** has discontinuous `ȧ` → distributional matching
  not fixed by the Friedmann eqs; particle-production `K = β(κε̃)²` has an **undetermined β** the
  author says "must be derived from QFT in Riemann–Cartan spacetime"). Assigning `T=1` OR `T=0` is an
  extra boundary condition, not a derivation. **Decisive textual check:** entry 11 itself says
  *"Finally, we need to derive primordial density fluctuations … and compare them with the observed
  spectrum"* (`1410.3881:364`) — the exact RQ-B calculation, acknowledged **unfinished by the source.**
- **agy → `NEW_FALSIFIER_CANDIDATE`.** Claims the spin fluid's pressure `p_eff` is uniquely fixed by
  `n_f`, so the non-adiabatic pressure perturbation **δp_nad = 0**, so the super-horizon curvature `ζ`
  is **conserved across the bounce, T(k) ≈ 1**. Then, because the torsion expansion is only ~23
  e-folds, the parent's classical inhomogeneities (rotation, density gradients, multipoles) are **not
  diluted** beyond the observable horizon → they survive as a finite CMB signature (preferred axis /
  bulk flow / low-ℓ) → a falsifier candidate.

## The crux (for the third read — as on RQ-D)

**Is the perturbation closure UNIQUELY DETERMINED by the published Popławski model** — i.e. is
**δp_nad necessarily zero** (agy: `p_eff` a function of `n_f` alone → adiabatic → `T ≈ 1`) — **or is it
genuinely UNDERDETERMINED** (codex: spin-isocurvature, the cusp's distributional matching, and the
free particle-production β mean many closures share the background and give different `T`)?
- If **determined / δp_nad = 0** → agy: `T ≈ 1`, and with ~23 e-folds a finite parent signature
  survives → NEW_FALSIFIER_CANDIDATE.
- If **underdetermined** → codex: UNDETERMINED_NEEDS the ECSK perturbation action; the literature
  proves neither survival nor suppression. (Note: agy's e-fold argument survives *conditionally* on
  `T ≈ 1`; it is the `T ≈ 1` step that is contested.)

Under the corpus **ownership-of-proof** rule, a NEW_FALSIFIER needs the transfer *derived*, not a
closure *assumed*; that favours codex — but the call is substantive and goes to an independent seat,
then Duho.

## Source-identity fix (codex catch — real defect in my brief)

My brief mis-mapped **entry 10**: `1111.1017_clean.txt` is *"Quantization of the Universe as a Black
Hole"* (Fullana & Alfonso-Faus), **not** Popławski PRD 85. The correct paper is **arXiv 1111.4595**
(PRD 85, 107502), which codex found + pinned (`1111.4595v2_poplawski_prd85{.pdf,_clean.txt}`), along
with **entry 12** (`2509.11468v2_poplawski_ijmpa40{.pdf,_clean.txt}`). All four force-added here. The
bibliography's entry-10 source reference should be checked/corrected to 1111.4595 (factual pin fix,
tier-neutral).

## Status

Third read (kimi, outside the codex/agy split) dispatched on the crux. Both seat results + the four
corrected pins committed. Outcome → Duho q4 once the crux settles.
