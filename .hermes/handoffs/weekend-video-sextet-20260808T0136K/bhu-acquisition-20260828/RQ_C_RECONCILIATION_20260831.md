# RQ-C reconciliation — the Gaztañaga causal-horizon CMB cutoff (BHU Lane 2, task 3)

**Tori, 2026-08-31.** Blind-double on codex (`RQ_C_codex_RESULT.md`, 30m) + agy (`RQ_C_agy_RESULT.md`).
Both fetched/read Fosalba–Gaztañaga 2021 (arXiv 2011.00910; codex also pinned Planck 2018 VII,
1906.02552). **Tier move NOT made here — returns to Duho.**

## Both seats agree: CMB_FALSIFIER_CANDIDATE (a PREDICTED scale, not a fitted one)

- **PREDICTED, not fitted (the load-bearing call — both agree).** The cutoff scale is fixed from the
  background parameters, independent of the low-ℓ deficit: `r_S = c/(H_0 √Ω_Λ) = 2GM` (Part I Eq. 33),
  the early comoving causal radius `χ_* ≈ 3 r_S ≈ 15.4–15.6 Gpc`, subtending `θ_* ≈ 60°` at last
  scattering → `ℓ_cut ≈ π/θ ≈ 3`. Decisive receipt (both cite it): Fosalba–Gaztañaga state the BHU
  θ(Ω_Λ) curve has **"no free parameter … published before the CMB analysis"** — i.e. **out-of-sample
  with respect to the low-ℓ deficit**, which is the brief's definition of PREDICTED.
- **Same derived scale.** Both get `ℓ_cut ~ 3` (θ ≈ 60°); codex notes the fundamental-mode convention
  leaves it 3–6, unresolved by the sources.
- **Planck: consistent in scale and direction (both agree).** The low quadrupole (Planck's realization
  C_2 ≈ 226 μK², ~1/5 of ΛCDM) and the large-angle S_1/2 deficit — Planck 2018 VII's only excursion
  outside the 95% band is at a ~60° lower bound — sit exactly at the predicted scale. **Planck does
  not refute it; it is consistent.**

## The one nuance (caution, not disagreement)

codex adds — and agy does not contradict — that this earns only **candidate** status: the BHU papers
supply **no primordial cutoff kernel, boundary eigenmodes, cutoff sharpness, or C_ℓ suppression
amplitude**, so there is no calibrated likelihood-level rejection threshold (no "C_2 < X"). The broad
low-ℓ deficit toward ℓ~20–30 is not predicted by the simple ℓ~3 geometry, and the global anomaly
significance is modest (~1% tail). **The model owns the missing proof:** it must specify the boundary
conditions + primordial P(k)/transfer function that propagate the finite domain into C_ℓ. So it is a
**scale-level** falsifier candidate, not a precision power-spectrum prediction.

## Reconciled verdict

**The Gaztañaga causal-horizon cutoff is a genuine, PREDICTED (out-of-sample), scale-level CMB
falsifier candidate — ℓ_cut ~ 3 / θ ≈ 60°, parameter-free, consistent with Planck's large-angle
anomalies — but NOT yet a calibrated falsifier (no amplitude/threshold).** This is the strongest
positive result in Lane 2 so far (contrast RQ-A undetectable, RQ-D 0-kills): the only corpus claim
whose scale is fixed a priori and lands on a real, documented CMB feature. What separates "candidate"
from "calibrated" is a specific piece of missing theory the papers do not supply.

## What this means for the tier (Duho's call — OPEN_QUESTIONS)

Entries 25/26 are QUALITATIVE-DIRECTIONAL. RQ-C shows their cutoff claim is a *predicted, tested,
Planck-consistent scale* — arguably stronger than "directional," but short of CALIBRATED-FALSIFIER
(which needs the number + threshold the papers lack). Recommended: annotate 25/26 with the RQ-C
finding (predicted scale-level CMB candidate; consistent with Planck; missing the C_ℓ threshold),
**tier unchanged** — "PROSPECT-shaped": a real prediction awaiting the amplitude that would make it
calibrated. Every figure traces to a pinned source (Symmetry papers + Fosalba–Gaztañaga + Planck
2018 VII).
