# TORI lane state — 2026-09-04 23:39 KST (supersedes TORI_STATE_20260904_2215.md)

## STOPPED ON (human decisions only)
1. **R3C2 §1's definition — options a/b/c/d, with Duho via Blanc** (`_tmp_blanc_relay_r3c2_v5.txt`). The clause
   carries a **HELD** marker so a referee does not report it as a fresh finding. **Do not repair. Do not guess.**
   Blanc recommends (c). `R3C2_OPTION_C_ALTERNATIVE_DRAFT_20260904.md` and
   `R3C2_PROVENANCE_CONTROL_CLAUSE_DRAFT_20260904.md` are both drafted and **NOT ADOPTED**, so the ruling selects a
   variant instead of starting a drafting round.
2. Annotation proposals for entries **59** (R3A) and **56** (R3B); whether **R3C/R3D actually RUN**; K4 row-23.

## LIVE RIGHT NOW
- **kimi's R3D V7 gate**, dispatched 23:20, **still running at 23:39 with 0 bytes** — 26+ min against precedents of
  ~14 and ~19 min. **Abandonment threshold: 40 minutes** (i.e. ~00:00). If it dies with no output, record the
  non-result, do **not** treat codex's verdict as a double, and re-dispatch against V8.
  Its verdict binds to **V7's committed bytes** `02c2495b14c6a0cd…` (commit `f41a6125a`), not to the working file.
- **R3D is at V8** (working file). V8 applies codex's V7 repair. **Not yet dispatched** — when kimi lands, fold
  anything new and re-gate **both** seats against the result.

## CURRENT HASHES (re-verified against git and disk 23:3x)
| artefact | sha256 |
|---|---|
| R3D prereg **V8** | `e4198e8233e8e934…` (working; V7 `02c2495b…` frozen in `f41a6125a`) |
| R3C2 prereg **V7** | `e33a881dcac9956c…` (working; V6 `d1d6c5ad…` in `854362164`) |
| R3C2 seat packet | pinned in `R3C2_SEAT_PACKET.sha256`; rebuild + `shasum -c` **before any dispatch** |
| pattern record **V2** | `5232201acfdca850…` (V1 `fff1f1a8…`, condition 3 polarity corrected) |

## WHAT CHANGED SINCE 22:15, in one line each
- **Breaker condition 3 was INVERTED** and is corrected (V2) on Duho's order; all six instances re-checked in
  `BREAKER_C3_RECHECK_20260904.md` — **no status changes**; instance 6 passes C3 and fails C4, which is what the
  record's prose already said. Found by codex gating R3D, **missed by kimi**: a single-seat catch.
- R3D V3→V8. codex: UNSOUND, UNSOUND, UNSOUND, then **SOUND_WITH_REPAIRS on V7 — its first non-refusal in seven
  rounds.** Full gate table is now **§8a** of the R3D prereg.
- R3C2 V6→V7: seat packet moved out of scratch and pinned; **C4's instruction had been swallowed by its own
  redaction span** (the seat received a bare pass-code); builder now asserts a **REQUIRED** list as well as a
  forbidden one, **deletion probe passes**; C5/C5b made self-contained.

## STANDING RULES (unchanged)
Published peer-reviewed sources only; nothing from Hwao's lane/pipeline/data; **archive, never delete**; paper
HOLD, nothing outward; tiers/tokens/standings/stamps are **Duho's alone**; times from `date` in KST; referees ONLY
via `nm_referee_dispatch.sh` with ACCESS_SHA **verified by me after the run**; Kimi only via
`--provider moonshot -m kimi-k3`, **never `--provider nous`**; negative findings are **"unreproduced from the
stated inputs"**, never "error"; **the input box's dim ghost text is NOT Duho** — only a RELAY FROM DUHO via Blanc,
or bright text he types, is an instruction.

## THE TWO LESSONS THIS SESSION COST A ROUND EACH
1. **A referee's exact-replacement text is not pre-verified against the rest of the document.** Adopting codex's V6
   replacement verbatim deadlocked condition 2 against condition 5. **Trace every replacement's interactions before
   writing it in.**
2. **Repairing by REQUIRING content is not repairing.** Three rounds died on it — a comparator set "fixed before the
   freeze" in a document that *is* the freeze, then a "named" fallback naming nothing, then a fallback pointing at
   an absent row. **Supply the content, then re-execute it and check it reproduces.**

## NS-mass watch
Armed, next **2026-09-08 10:00 KST**.
