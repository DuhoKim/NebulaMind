# TORI lane state — 2026-09-05 01:09 KST. AUTONOMOUS OVERNIGHT (Duho: "run autonomously overnight")

## HARD STOPS — file, never decide, however obvious at 04:00
Running either study (a gate clears a design, it does NOT start it) · any pixel of the frozen sample ·
training/fine-tuning/weights · any §13 verdict token · any tier/warrant/standing/stamp movement ·
retiring, adding or redefining an outcome CLASS beyond what Duho has ruled · anything outward (paper HOLD) ·
**R3C2 §1's definition stays HELD — a/b/c/d unruled; do NOT adopt the (c) draft or let a referee finding move it.**

## THE THIRD-FAILURE RULE — not suspended by autonomy
If one defect survives **three** repairs in the same direction: **stop, file a diagnosis, wait.** It has fired once
tonight and Duho converted it into a retirement.

## LIVE AT HANDOVER
- **codex → R3D V13 two-seat design gate**, target `61adc801f873c7dc…`
- **kimi → R3C2 second seat**, target `19a075c66696c4a0…` *(this was never dispatched earlier; I told Blanc so)*
- When kimi frees: **dispatch kimi as R3D V13's second seat** — the gate needs two engines.

## R3D — where it stands
**V13 = `61adc801f873c7dc…`. C0 PASSES** (`R3D_C0_EXHIBITION_V13B`, 0 unreachable, seat states **2 of 6**
independently). Awaiting the two-seat design gate. **NOT frozen, NOT run.**
- `DYM_FLOOR_COMPLETION_DEPENDENT` **RETIRED** by Duho 00:59 after three exhibitions could not reach it. Its
  state now files as `DYM_FLOOR_UNDERDETERMINED`; §4 says so explicitly so it is not homeless.
- **C6 FAIL reachable on 2 of 6 classes; declared now equals reachable.**
- Deletion probe `r3d_c3_deletion_probe.py` is executable with passing positive/negative controls but is **NOT yet
  wired into the prereg** — that is real outstanding work.

## R3C2 — where it stands
**`19a075c66696c4a0…`. C0 PASSES** (`CENSUS_COMPLETE` confirmed REACHABLE — my suspicion was wrong).
codex gate = `PREREG_UNSOUND`; blinding and seal **SOUND**. **Repair against BOTH lists only when kimi lands.**
codex's fix — inclusion assigned by two pattern-blind seats — **must be checked for smuggling the pattern back in
through what those seats are told** (Blanc's warning; not yet done).

## RULES THAT COST ROUNDS TONIGHT — keep every one
1. **Never read an artefact before its seat EXITS**: `ACCESS PROVEN` in the log **AND** zero process count. I broke
   this twice; the second time the answer was right, which is exactly why it goes in writing.
   *(Exception, disclosed: a wedged writer. Use the artefact's own terminal token + byte stability, and say so.)*
2. **Verify every returned ACCESS_SHA yourself, after the run**, against the file's hash.
3. **Never edit while a seat reads. Version instead.**
4. **Sweep whole documents for scars**, never the quoted line alone.
5. **A referee's exact-replacement text is not pre-verified against the rest of the document** — trace interactions
   before adopting. Adopting codex's V6 text verbatim deadlocked two clauses.
6. **Repairing by REQUIRING content is not repairing.** Supply it, then re-execute and check it reproduces.
7. **Fix the brief, not the document**, when a seat's failure traces to what I asked.
8. Archive, never delete; name superseded files for **why**. Stamp every time from `date`, KST.
9. Quoted heredocs for prose — an unquoted one executed backticks tonight.
10. zsh does **not** word-split unquoted $vars; pass git paths explicitly.

## OPEN, BLOCKED ON DUHO
R3C2 §1 a/b/c/d · annotation proposals for entries 59 and 56 · whether either study RUNS · K4 row-23.

## WATCHES
NS-mass watch armed, next **2026-09-08 10:00 KST**.
