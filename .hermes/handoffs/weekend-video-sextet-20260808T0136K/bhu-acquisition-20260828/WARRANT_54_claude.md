W_DIRECTION_DERIVED
borrowed_inputs: constant ground-state density rho_G with P=-rho asserted from the quantum exclusion principle ("We have found that the quantum exclusion principle leads to a ground state", L250); Pradhan et al. 2025 for the same k>0 requirement (L49); amplitude only: chi_* = 15.93+-2.22 Gpc from Camacho-Quevedo & Gaztanaga 2022 (L317-325) and chi_CMB (L314)
With constant ground-state density, Friedmann equation (20) reads H^2 = r_S/R_G^3 - k/a^2, so "a gravitational bounce (adot=0 and addot>0)" (L280) exists only if k>0: "Note how it is critical that k > 0 (or chi_k^2 < inf) to have a bounce before the singularity" (L286); "an infinite FLRW cloud has no bounce" (L288). Since "k has to be constant" (L125) and Omega_k = -k/H0^2 (L331), "Inflation preceded by a bounce requires Omega_k < 0" (L336). Only the magnitude is conditional ("could be smaller", L336). k>0 is also called an assumption that "simply follow[s] from considering a finite over-density" (L47), with Pradhan et al. 2025 cited (L49), but Sec. V derives it; no free parameter selects the sign.
contested: no — the L47 "assumption" wording is reconciled by the Sec. V derivation (L280-288); the sign is not a free-parameter choice, only the magnitude is.
