K1 STAGE-2, STEP 2 — DRIVER + LAUNCH (one of two BLIND seats; do not open the other seat's K1S2_<other>_driver*, *_post*, *_RESULT*). Frozen prereg: K1S2_POPSYN_PREREG_20260903.md. MASTER PIN SHEET: K1S2_PIN_GATE_agy.md ("MASTER PIN SHEET" + "Driver Settings Needed") — use ONLY those pins. Binary: <lane>/_tmp_k1s2_codex/COMPAS/src/COMPAS (v03.29.05 at commit e728869, built 17:47 KST; smoke test in _tmp_k1s2_smoke/: 10^4 binaries = 214 CPU s, so ~6 CPU-h per 10^6). Machine: 32 cores; you may use AT MOST 10 concurrent COMPAS processes (other lanes share this machine).
GRID (per the master sheet): caps M_NS,max ∈ {1.97, 2.50, 3.50} M☉ × engine ∈ {DELAYED, RAPID} × metallicity ∈ {0.02 (=Z☉ per prereg), 0.0002 (=0.01 Z☉)} × CE/kick ∈ {code default; alternative LAMBDA_FIXED λ=0.1 + MAXWELLIAN σ=265 km/s} = 24 configurations; each = 3 batches × 10^6 binaries with seeds 104729, 130363, 155921 (master row 6); always `--remnant-mass-prescription FRYER2012` and the explicit cap. IMF slope α₃ ∈ {1.6, 2.3, 3.0} is NOT rerun: reweight in post-processing from Mass@ZAMS(1) (COMPAS samples a Kroupa primary; weight w = m^{-(α₃ − 2.3)} for m ≥ 1 M☉, unity below) — state this in your post-processing script and justify the secondary's treatment.
YIELD: Y_BH = (number of systems whose final Stellar_Type(1) or Stellar_Type(2) == 14) per unit star-forming mass, using the repo's own mass-evolved normalisation (postProcessing/PythonScripts totalMassEvolvedPerZ, cite the file path and function) so the unsampled IMF range is accounted for. Derivative ∂Y_BH/∂M_NS,max by central difference over the cap grid at every box point; Monte-Carlo error from the across-seed dispersion of the three batches.
CONTROLS (prereg §5): C1 — convolve the fiducial configuration's BBH merger-time distribution with the Madau & Dickinson 2014 SFH (master row 4; the repo's CosmicIntegration tools may be used, cite paths) to a z=0.2 rate; must lie in 17.9–44 Gpc⁻³ yr⁻¹. C2 — two-sample KS at α=0.05 between the fiducial-cap synthetic NS masses (near-birth: exclude recycled/accreted, state your cut) and the 15 pinned masses (master row 5). C3 — the same cap grid in single-star mode (`--mode SSE`, same seeds, 10^6 stars), sign of the derivative reported. C4 — the centre configuration rerun with three extra seeds (6×10^6 total) — sign must not change.
DELIVERABLES NOW: (1) K1S2_<seat>_driver.sh — generates every COMPAS command line (one output directory per configuration/batch under _tmp_k1s2_<seat>/runs/), runs them through `xargs -P 10`, writes _tmp_k1s2_<seat>/runs/DONE when all finish; (2) K1S2_<seat>_post.py — the full post-processing from HDF5 to yields, derivatives, C1–C4 and the class table, runnable on partial output; (3) LAUNCH the driver detached (`nohup zsh K1S2_<seat>_driver.sh > _tmp_k1s2_<seat>/driver.log 2>&1 &`) and RETURN IMMEDIATELY with: the number of processes, the PID of the driver, the expected wall time from the smoke-test rate, and a line confirming the first HDF5 file appeared. Do NOT wait for the grid. Do not write a RESULT file now; the RESULT (K1S2_<seat>_RESULT.md, prereg §3 vocabulary) is written by a later dispatch when DONE exists. No web.
