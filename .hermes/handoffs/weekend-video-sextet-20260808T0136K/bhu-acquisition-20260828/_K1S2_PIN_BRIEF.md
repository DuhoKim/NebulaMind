K1 STAGE-2, STEP 1 — PIN SHEET ONLY (no population-synthesis runs yet). Read K1S2_POPSYN_PREREG_20260903.md (frozen; do not edit). You are one of two blind seats; do not open the other seat's K1S2_*_pins.md. Produce K1S2_<seat>_pins.md with, each row carrying a receipt (file + line in ../bhu-reading-20260823/sources/, or for the code a git commit hash and file path inside the checkout):
(1) COMPAS: clone https://github.com/TeamCOMPAS/COMPAS into _tmp_k1s2_<seat>/COMPAS (network allowed for this clone only), record the exact commit hash you checked out (git rev-parse HEAD), the release tag if any, and whether it builds on this machine (attempt the documented build; record success/failure and the compiler line; do NOT run a population). (2) The option names that set the remnant-mass prescription (Fryer delayed/rapid) and the maximum neutron-star mass, with the source-file path and line numbers in the checkout, and their defaults. (3) The cap grid: three M_NS,max values — one below, one at, one above 2.5 M☉ — chosen from values the pinned sources actually state (Fryer 2012 1110.1726_clean.txt: their adopted cap; Özel & Freire 1603.02698_clean.txt: any stated maximum-mass bound); quote the lines. (4) A star-formation-history prescription for the C1 rate convolution: name a published form, fetch its arXiv text into the sources tree (curl + pdftotext -layout, record sha256), and cite the equation line. (5) The two-sample test and level for C2 (declare; e.g. a KS test at a stated level) and the Özel & Freire table lines that give the neutron-star masses you will compare against. (6) The declared number of binaries per grid point and the seeds, with an order-of-magnitude runtime estimate from the COMPAS documentation (cite the line). (7) The common-envelope and kick prescriptions: the code's default (option names + defaults, path + lines) and ONE declared alternative. If a row cannot be pinned, write UNPINNED and what is missing; never guess. Output the file and nothing else.
