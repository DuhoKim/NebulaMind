K1 STAGE-2 PIN-SHEET GATE. After the ACCESS_SHA line for the target (the frozen prereg), also print ACCESS_SHA_CODEX=<sha256 of K1S2_codex_pins.md> and ACCESS_SHA_CLAUDE=<sha256 of K1S2_claude_pins.md> computed by you. Read the prereg and both pin sheets. For every receipt in both sheets, open the cited line(s) (sources under ../bhu-reading-20260823/sources/; code lines inside _tmp_k1s2_codex/COMPAS or _tmp_k1s2_claude/COMPAS as cited) and confirm they state what is claimed; list every receipt that does not hold. Then decide row by row which sheet's pin (or a merge) is the MASTER for the driver step, noting: (a) both sheets pin the same COMPAS commit and both builds fail for missing Boost/GSL/HDF5 — record that as a resource fact, not a gate failure; (b) the cap grids differ (codex {1.97, 2.50, 3.50}; claude {2.0, 2.5, 3.5}) and the receipts for the outer points differ in quality (figure-label vs sentence) — pick the grid whose every value is sentence-pinned; (c) N per point differs (3×10^6 vs 10^6) — pick one and say why; (d) the code's default remnant prescription is not Fryer 2012 and its Z☉ is 0.0142 vs the prereg's 0.02 — state the driver settings that make the run match the prereg. Output file K1S2_PIN_GATE_agy.md: the ACCESS lines; then PIN_GATE=<PASS|PASS_WITH_REPAIRS|FAIL>; then the MASTER PIN SHEET (seven rows, receipts that held, driver settings); then failed receipts; then <=200 words. Change no file.
