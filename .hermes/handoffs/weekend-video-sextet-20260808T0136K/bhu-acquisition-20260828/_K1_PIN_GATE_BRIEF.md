K1 STAGE-1 PIN-SHEET GATE (codex). ACCESS PROOF first: compute sha256 of K1S1_claude_pins.md and K1S1_agy_pins.md with shasum -a 256 and print them as the first two lines in the form ACCESS_SHA_CLAUDE=<hex> and ACCESS_SHA_AGY=<hex>; if you cannot read either, print NO_ACCESS and stop.
Read K1S1_CNS_SIGN_PREREG_20260903.md (frozen) and both pin sheets. For EVERY receipt in both sheets (file + line in ../bhu-reading-20260823/sources/), open the cited line(s) with sed -n and confirm the line states what the sheet claims; list every receipt that does not hold. Check: (a) row-by-row agreement between the two sheets on value/range; (b) the σ(M) ∝ A_s^{1/2} row is a definitional pin, not a quoted line — say whether that is acceptable under the prereg ("exponent pinned with a receipt") and, if not, what receipt would satisfy it; (c) agy's UNPINNED rows — does claude's sheet pin them with valid receipts; (d) the Fryer 2012 note that pdftotext dropped squared exponents — confirm from the PDF (pdftotext is at /opt/homebrew/bin/pdftotext; 1110.1726.pdf is in the sources tree) which exponents are affected; (e) every fetched source has a sha256 recorded. Output file K1S1_PIN_GATE_codex.md: after the two ACCESS lines, line 3 PIN_GATE=<PASS|PASS_WITH_REPAIRS|FAIL>; then the MASTER PIN SHEET for phase 2 (the seven rows with the receipts that held, merged from both sheets, plus any repair), then the list of failed receipts, then <=150 words. Change no other file.
