You are a fresh adversarial referee gating a pre-registration for a reproduction census. Judge the DESIGN only. No
derivation has been run. You have seen no prior verdict on this document and there is none to infer; section 10 of
the master is history and BINDS NOTHING — do not report its narrated findings as yours, do not look for gate files,
and do not open any file other than the two named below.

STEP 0 (already done by the access proof above): print ACCESS_SHA of the master.

STEP 1 — PACKET FIRST, BEFORE ANYTHING ELSE. Open ONLY the seat packet:
  r3c2_seat_packet/R3C2_SEAT_PACKET.md
Print its sha256 as PACKET_SHA=<64 hex>. Then answer, from the packet ALONE and before you have read the master:
  Q5. CAN YOU TELL, FROM THE PACKET ALONE, THAT ANY OUTCOME IS MORE CONSEQUENTIAL THAN ANY OTHER — to the lane, to
      anyone? Quote the sentence that tells you, or state that no sentence does. Answer before Q3.
  Q6. CAN YOU STATE, FROM THE PACKET ALONE, WHAT THE RECORDED `origin` OF AN INPUT IS FOR — what the study does with
      it after you record it? Quote the sentence that tells you, or state that you cannot. Answer before Q3.
  Q3. WHAT CAN YOU INFER ABOUT WHAT THE LANE EXPECTS OR HOPES THE CENSUS WILL SHOW? State every inference you can
      draw about a hypothesis, a comparison model, a preferred outcome, or a "pattern" — with the packet sentence
      that leaks it. If you can infer nothing, say so and say why the packet gives you nothing. This is the
      hypothesis-reachability test; six leak findings preceded it. Be adversarial: try to reconstruct the stake.
STEP 2 — then read the master in full: R3C2_REPRODUCTION_CENSUS_PREREG_20260904.md (Version 27, the living draft; not signed). Judge the operative sections
  (1, 2, 3, 4, 5, 6, 7, 9, 11). Section 3's core definition is SETTLED by the principal's ruling (option (c): one
  pass, two tallies — reproduce mechanically from every input the paper states, record provenance, a script
  computes each claim's rests_on; REPRO_AFTER_CHOICE retired). Judge it as written.

JUDGE, each explicitly and in order:
1. OUTCOME CLASSES. Exhaustive and mutually exclusive? Construct a real claim fitting two per-claim outcomes, or
   none; a real tally fitting two study-level classes, or none. Is INCONCLUSIVE genuinely reachable?
   Q1 (ask directly): DOES SECTION 3's DEFINITION, AS WRITTEN UNDER OPTION (c), MAKE EVERY DECLARED OUTCOME REACHABLE
   AND KEEP THE REPRODUCTION QUESTION DECIDABLE? Those two pull against each other and four earlier rounds failed on
   that tension. Construct the hardest case you can — a printed-but-chosen input, a fitted parameter the paper
   states, an imported value — and trace it to exactly one outcome and one rests_on value, or show it cannot be.
2. CONTROLS. Can any control be passed by assertion rather than a printed artefact? Does each name an exact code
   with PASS/FAIL/NOT_RUN? Are unreached controls handled?
   Q2 (ask directly): IS THE PROVENANCE FIELD CHECKABLE RATHER THAN ASSERTED? origin must be cited with a
   machine-matched quotation; root_origins and rests_on are computed by script; the audit re-classifies origin from
   the sources. TEST IT: write a ledger record whose origin is wrong but plausible, and say which mechanism catches
   it and how. If the mechanism as written cannot catch it, that is the answer — say so.
3. CIRCULARITY. Can the lane's expectation reach the evidence — through inclusion, exclusion, origin classification,
   or the interpretation step? Can a contrary claim be quietly excluded?
4. THE BLIND. Is the packet's redaction machine-asserted as claimed? Does the C4 mechanism as described actually
   prevent what it claims to prevent, and does the document state its limit honestly?
5. THE SEAL (section 7). Does the receipted relay bind what it claims to bind, before the step it must precede?
6. FAIRNESS. Every negative outcome worded "unreproduced from the stated inputs", never "error"? Any class name or
   clause that asserts more than its definition?
7. STALL / EXECUTABILITY.
   Q4 (ask directly): CAN ANYTHING THE DOCUMENT DESCRIBES NOT BE EXECUTED AS DELIVERED? Every literal command, every
   named script, every file the document says exists, every field a script is said to compute, every path a seat is
   told to use. Name each thing you checked and how. A described artefact that does not exist as delivered is a
   finding.

For every defect: QUOTE the verbatim sentence, state the defect, give an EXACT replacement. Mark COSMETIC anything
that changes no class filed, no control's passability, no reachability, no conclusion and no tested value.

REPORT — write R3C2_GATE_V27_<seat>_20260907.md beside the master AND print it as your final answer:
  line 1: ACCESS_SHA=<64 hex of the master>
  line 2: PACKET_SHA=<64 hex of the packet>
  line 3: GATE=PREREG_SOUND | PREREG_SOUND_WITH_REPAIRS | PREREG_UNSOUND
  line 4: LEAK=NONE | LEAK=<one line: what the packet alone let you infer>
  line 5: CONSEQUENCE_VISIBLE=NO | CONSEQUENCE_VISIBLE=<one line: which outcome, from which sentence>
  line 6: ORIGIN_PURPOSE=CANNOT_STATE | ORIGIN_PURPOSE=<one line: what it is for, from which sentence>
  then sections 1-7 with Q1-Q4 answered where placed.
  final line: R3C2_V27_GATE_COMPLETE


Q7 (V27): this version carries the repairs from the two-seat gate on V26 (§10.22) — you may open R3C2_GATE_V26_codex_20260906.md and
R3C2_GATE_V26_kimi_20260906.md AFTER the master to check each. Operative changes versus V26: the audit's re-derivation file carries full
reconstructed input records (status, value, source coordinates, origin, derived_from); `audit compare` compares every field and
dependency edge against the sealed ledger, carries a declared origin alternative, and recomputes each input's chain of origins to its
roots from both sides (the graph bypass); limb B lives in a separate directory under the canonical file names, chain B bound to the agreed
limb-A seals; per-session `validate` is NOT_RUN and joined validation precedes limb-A agreement (the cross-batch lifecycle); `validate`
applies a shared evidence check to every non-ORIG_SILENT record before any status branch — an ABSENT input keeps the origin its evidence
supports; `compute` requires the candidate file (two arguments exit 2); C3's lane-tool pin and compute form, C5's manifest pin, the
manifest's root-symlink refusal, the local-import summary sentence, the printed build command with explicit arguments, tool docstrings,
the ownership list's provenance; and the `rests_on` label `DERIVED_STANDARD_OR_MEASURED_ONLY` (formerly `DERIVED_ONLY`, identical
membership, implemented as routine on both V26 reviewers' label-only finding). The kit is r3c2_staged_d1d7/ (pin sheet lane-relative);
the packet pin sheet R3C2_SEAT_PACKET.sha256 lists eight files; the packet's line 9 names the master it was built from.
  Answer: (a) verify both pin sheets from the working directory and that the packet's "Built from master" digest equals the ACCESS_SHA;
  run the kit (last three lines); then, with your OWN fixtures: the graph bypass — a sealed ledger and an auditor reconstruction that
  differ ONLY in one dependency edge with equal root sets (must FAIL, C6_AUDIT_SAMPLE=FAIL); an ABSENT/MEASURED record with an empty
  quotation (must FAIL) and one with its describing sentence quoted (must PASS); a limb-A chain, then a limb-B directory copied under
  canonical names, sealed as chain B bound to the limb-A seals, joined, and `census … final` on it; compute with two arguments (exit 2)
  and with three (NOT_COMPUTED row for a recordless included claim); the manifest on a symlinked root (must refuse); print tokens.
  (b) C5_EXECUTABLE_UNDER_SCOPE=YES|NO and NO_MASKED_STAGE=YES|NO for THIS text. (c) PRIOR_FINDINGS_CLOSED=YES|NO over the V26 gate's
  findings (codex F1–F7, C1–C3; kimi F1–F4, N1–N3): each CLOSED / PARTLY / NOT with your evidence. (d) Does the brief let a batch session
  execute every printed command as delivered, in both limbs, without a seal being overwritten? Add line 7: C5_EXECUTABLE_UNDER_SCOPE=…,
  line 8: NO_MASKED_STAGE=…, line 9: PRIOR_FINDINGS_CLOSED=….
  Nothing you write adopts, signs or runs anything.
