B7_REVIEW=RESULT_STANDS

Adversarial referee pass on `B7_CURVED_20260907.md` + `b7_curved.py` (Codex worker's artifacts). I attacked hardest first: the existence/openness claim, the zero-production control, constraint propagation, the Eq. (35) comparison, and scope/hygiene. Every load-bearing claim survived independent re-derivation: I re-derived the first-order KS system from the publisher's printed Eq. (10) and the matter laws from (14)+(34) myself, got B7's equations exactly; all 12 of my independent symbolic checks pass; my own integrations (LSODA at different tolerances and a hand-written fixed-step RK4, plus my own 5%/10% random perturbation sets and a direct openness test around B7's construction point) reproduce every reported number; the Eq. (35) quotation is byte-exact against the saved publisher HTML. A negative verdict is not warranted; no repair is needed. Report only; nothing modified.

## 0. What was read, and what every quotation is bound to

- **Publisher HTML**: `b4_springer_access.html`, SHA256 recomputed by me: `fb94da36db11823ed1081fd39226a7134dec64883a7811c4011a1ea7234efc88` — matches B7's pin and B6's pin. I parsed the raw bytes myself; MathJax TeX payloads live in `<span class="mathjax-tex">$$...$$</span>` inside `id="EquNN"` divs. I read the payloads for (1), (6), (10), (11), (14), (15), (16), (24), (25), (30)–(37) and the prose around (30), (35), and in §9 directly from those bytes. **Every source statement in this review is [publisher HTML]**, read from the saved publisher file; I did not use the arXiv copy or any pre-existing extraction for any claim.
- B7's single verbatim quotation (Eq. (35)) byte-compared against the publisher payload after normalizing only the `$$`/`\[\]` wrapper, the `&gt;` entity, and whitespace: **exact match** (`_tmp_kimi_b7_quote.py`, MATCH: True). B7's label of it as "publisher HTML, MathJax payload Equ35, own tag-stripping display" is accurate.
- Input custody: recomputed SHA256 of B2 (`d99f286f...`), B4 (`f4cf91f7...`), B6 (`7531c8ed...`), KIMI_B6 (`9faf751d...`) — all four match B7's pins exactly. Target hashes: B7 md `0d583614...`, script `15760f4c...` (match the script's own execution-time printout).
- `b7_curved.py` run by me with `/usr/bin/python3` (3.9.6, NumPy 1.26.4, SciPy 1.13.1, SymPy 1.14.0): exit 0, `OUTCOME=TURNS_OVER`, every number in B7's tables reproduced exactly (log `_tmp_kimi_b7_run.log`): the 9-row main table, both extra families, the representative block, 12/12 perturbations per closure, convergence deltas (5.639e-11/8.101e-10/5.520e-12/6.776e-10), control stop times and Kretschmann values, both REJECTED_TEST lines, residual maxima (b: abs 4.445610102266e-08 / rel 1.123816807965e-12; c: abs 2.850683813449e-07 / rel 8.540809244357e-12). The script writes no files (no `open(` in it).
- Independent work, all mine: `_tmp_kimi_b7_extract.py`/`_tmp_kimi_b7_eqs.txt` (publisher equations), `_tmp_kimi_b7_sympy.py` (12 symbolic checks, ALL_CHECKS_PASSED), `_tmp_kimi_b7_numeric.py` (independent solvers + my perturbations), `_tmp_kimi_b7_control.py` (clean zero-production control), `_tmp_kimi_b7_quote.py` (quote byte-compare).

## 1. ATTACK 1 — existence and openness. HELD; attack fails.

**Re-derivation of the turning condition in the source's variables.** From printed (10) [publisher HTML] — `(Ẏ²+1)/Y² + 2ẊẎ/(XY) = κε̃`, `(Ẏ²+1)/Y² + 2Ÿ/Y = -κp̃`, `Ẍ/X + Ÿ/Y + ẊẎ/(XY) = -κp̃` — I solved the algebraic system for `u̇, v̇` myself (not reading B7's formulas) and got exactly B7.1: `u̇ = -u²-uv+(v²+K-κP)/2`, `v̇ = -(3v²+K+κP)/2`. From printed (14) with (1) and printed (34) I got `ε̇ = 2αnβH⁴ - 4Hε` exactly. My SymPy checks then confirm: off-constraint `Ḣ = -2H² + (καn²-S-K)/3 + C/6`, so on `C=0, H=0` the contracting-to-expanding condition is exactly **B7.7: καn_f² > Δ²/3 + K = S+K**, and `ä = aḢ > 0` there. Degenerate-equality boundary verified independently: with `H=0, C=0, καn²=S+K`, `Ḧ = -4KΔ/9` (no crossing for Δ≠0) and at Δ=0 `Ḧ=0, H⁽³⁾ = -4K²/9 < 0` (expansion-to-contraction, not a bounce). Equality is not silently counted; the checks are real.

**Is the constructed point tuned?** The §4 construction (K_b=κε/4, S_b=κε/12, καn_b²=5κε/6) is one point, and I verified it sits exactly on the constraint (residual 0.0 at ε_b=2) with turn margin κε/2 = 1 > 0. The openness around it is the standard transversality + continuous-dependence argument on the constraint manifold (regular, since ∂C/∂ε = -κ ≠ 0) — mathematically sound, and I tested it directly rather than trusting it: back-integrated the constructed point 0.02 in time (H = -0.0067 < 0, contracting), applied 30 random 2% constraint-preserving perturbations of (u,v,n) with ε re-solved from C=0, integrated forward with LSODA: **30/30 turn**. The claim does not rest on the script's 12/12.

**Perturbing the distant datum myself.** Around the main datum (H0=-1, Δ0=3, K0=1, w0=0.1), 40 random ±5% perturbations of all six quantities (H,Δ,K,w,A,B), ε recomputed from the constraint: **40/40 turn**. At ±10%: **39/39** (one draw rejected as inadmissible, not a failure to turn). This is much stronger than the script's ±1% 12/12 and independently supports robustness. My own small-β probe: the main family still turns at B=1e-3, 1e-4, 1e-5 (tb drifting 0.3151→0.3155 toward the control's blow-up time) — B7 never claims this; it carefully claims existence for each finite β via its construction, not that one fixed datum turns for every β. No tuned α-β-data relation anywhere: the 9-row grid spans A∈[0.25,1], B∈[0.01,1], and the analytic construction works for any α in a compact positive interval and any finite β>0 with ε_b a free positive value.

**Independent reproduction of the representative turn.** LSODA (rtol 1e-11, my own RHS from my own derived equations): tb = 0.3139806918035, Ḣ_b = 1316.4810148, e_b = 4150.2445221, margin 3949.4430445 — B7 prints 0.3139806918085 / 1316.48101 / 4150.24452223. My hand-written fixed-step RK4 (h=2e-5, no scipy): tb = 0.313980691809 to printed precision. Turn is not a DOP853 artefact. At the turn u=+50, v=-25 (X expanding, Y contracting): a volume minimum of a=(XY²)^{1/3}, exactly as B7 defines and discloses; the source's own (32) is likewise an acceleration condition on this a [publisher HTML].

## 2. ATTACK 2 — the zero-production control. HONEST AND CORRECT; attack fails.

The numerics are labelled exactly right: `FINITE_CUTOFF_UNRESOLVED`, Kretschmann growing 2.5e11→2.5e14→2.5e17 across caps 1e3/1e4/1e5 with stop times approaching ~0.3155, no cap called a singularity, and `INTEGRATION_FAILURE`/`NO_EVENT_ON_FINITE_WINDOW` are distinct statuses. This is not hiding a failure to integrate: constraint residuals at the stops are ≤ 2e-14 normalized, and my own clean control run (β≡0, no `0×∞` artefact, DOP853, max_step 1e-4) confirms the analytic story point by point: H is monotone decreasing and **stays below the Riccati upper bound -1/(1-t) at every sample point** (so no turn is possible before the bound's blow-up at t=1); R=w/S decreases monotonically from 1/30, so w<S throughout (max w-S = -2.9 < 0); Δ>0 throughout and grows; Vn conserved to 7.7e-12 relative; at the cap, V≈3.4e-5, Y≈1e-3, K≈1e6, ε≈1e6, H≈-3.4e4 and falling. B7's three-step analytic argument checks out logically: (1) Δ>0 persists since d(VΔ)/dt = X > 0 and β=0 keeps R decreasing; (2) on C=0, Ḣ = -H² - (2/3)(S-w) - κε/3 < -H², Riccati comparison kills any turn; (3) bounded-curvature continuation is excluded (bounded invariants bound n via Ricci² = 4w², hence V bounded away from 0 via Vn = const, then X=VK, Δ, u, v, ε all bounded, contradicting (2)). The contrast B7 needs is established: the *same* datum turns for β>0 and develops unbounded curvature in finite proper time for β=0 — production is what makes the difference, for this datum, and that is all B7 claims. The verdict `FINITE_CUTOFF_UNRESOLVED` plus a separately-scoped analytic singularity argument is the honest presentation, not a hidden integration failure.

## 3. ATTACK 3 — constraint propagation. VERIFIED both ways; attack fails.

Symbolic (mine, from my own derived flow, chain rule per closure): `Ċ + 3HC = 0` identically in closures (b) and (c); energy residual `ε̇ - 2αnṅ + 3H(4ε/3 - 2αn²) = 0` identically in both; plus the signed shear `Δ̇+3HΔ=K`, comoving shear `Ṡ+6HṠ... = (2/3)KΔ`, Raychaudhuri off-constraint, turn-margin off-constraint, fractional race (B7.11), J-evolution (B7.13), and `J = 3(Ḣ+H²)` on C=0 (off-constraint: `Ḣ+H²-J/3 = -C/6`, i.e. J = 3(Ḣ+H²)+C/2) — 12/12 of my checks pass. B7.5 `Ċ=-3HC` is exactly right, and the honest consequence (numerical C-errors amplify by V(t0)/V(t) ≈ 22 during contraction) is stated by B7 itself.

Numerical residuals are the right quantity: normalized η_C = |C|/(1+v²+2|uv|+K+e+w) divides by the same magnitudes C is a difference of (my own sampling along the trajectory gives relC ~ 1.9e-11 with looser LSODA settings; the script's tight runs report ≤ 8.6e-12 max over 23 runs per closure, including continuation). The larger absolute values (up to 2.9e-7) sit where the individual terms are O(10³–10⁴) — relative is the meaningful measure and B7 supplies both. Independent quadrature checks (Vn and VΔ integrals) at ≤ 4.9e-12 normalized confirm the integral form of (B7.8). Coarse/fine convergence (Δtb ~ 5.6e-11, Δe/e ~ 8.1e-10) shows the turn times are resolved, not tolerance artefacts. The (b)/(c) coordinate agreement (tb differing at 1e-13) is expected — they are the same ODE up to a variable transform — and B7 correctly treats it as a consistency check, not independent confirmation.

## 4. ATTACK 4 — comparison with printed Eq. (35). B7 IS RIGHT, AND SAYS THE HARD THING PLAINLY; attack fails.

Printed (35) [publisher HTML, byte-verified above] is `(2βn_f/81)(Ẋ/X+2Ẏ/Y)⁴ > (2/3Y²)(Ẋ/X-Ẏ/Y)`, i.e. `2βn_fH⁴ > (2/3)KΔ`. The printed prose frames it as the production-vs-shear rate comparison ("To avoid a singularity, the rate of n²_f must exceed the rate of σ². Comparing (34) with (30) gives") and attaches it to "the instant when (32) is reached... to ensure that (32) continues to hold" [publisher HTML]. B7's characterization is exact: (35) compares the **absolute source terms** of printed (34) and (30) with dilution moved left; it is neither the fractional condition (B7.12, thresholds equal only where n_f² = Δ²/3 — I verified this algebra) nor the amplitude condition (B7.7).

The decisive fact: **at every positive-Δ turnaround H=0, so the LHS of printed (35) vanishes while the RHS (2/3)KΔ > 0 — B7's turnarounds violate the source's stated condition at the turn, strictly, in both closures and in the analytic construction.** B7 says this itself ("printed (35) fails, while (B7.7) can hold strictly"). What it means: (35) is a *persistence* condition for (32) comparing instantaneous absolute rates; it cannot be a necessary turn condition, because the production rate Ψ = βH⁴ is zero at the crossing by construction — the turn is carried by accumulated spin amplitude (the integral (B7.8)), not by the instantaneous source. The numeric trajectory shows the mechanism plainly: the fractional race Ṙ/R is won by production only between t≈0.0405 and t≈0.3094, curvature wins again before the turn (race = -2.68 at tb≈0.3140), yet the turn happens because R≈3.16 with margin w-S-K≈3949 > 0. B7's further observation that the two sides of printed (35) carry different dimensions (L⁻⁷ vs L⁻³ with β dimensionless from Ψ=βH⁴ ~ L⁻⁴; verified) is correct and is handled cleanly — B7 preserves (35) as printed instead of silently repairing it with κα. J-evolution (B7.13, my check passes) correctly shows persistence of (32) is undecided at J=0 by the boundary derivative — again distinguishing, not revising. No overstatement; if anything B7 does the source's referee work.

## 5. ATTACK 5 — scope and hygiene. CLEAN; attack fails.

- Flat leakage: the only occurrence of "flat"/"Bianchi"/"Dirac" in B7 is the exclusion sentence itself ("No flat Bianchi I result is used... The Dirac row is excluded throughout."). Every derivation carries K; the zero-production argument uses the KS Raychaudhuri forms (both verified). No flat result enters.
- Added assumptions: §8 lists them all — B2 closures with p=ε/3 retained and the equilibrium n(T) relation released (matching B2's determined (b)/(c)); smooth classical patch; fixed positive α, β, κ, h⋆; regular F branch for (c); dimensionless units and the exploratory A,B samples explicitly *not* source-calibrated; turnaround defined as minimum of a; no extra stress/production law; finite-window numerics with only the two named rigorous claims beyond the windows. I found no unlisted assumption in the script or text (the energy source 2αnΨ is derived from printed (14)+(1), and disclosed as retained).
- Overstatement hunt: the headline is scoped ("existence result... not... for all initial data or a prediction for an unspecified source trajectory"); "no class is filed and no general cosmological conclusion is drawn"; "It proves existence of open sets, not that a fixed initially weak-spin datum turns for every β>0"; negative effective ρ at the turn is disclosed as allowed by FLUID (1); the volume-vs-directional-minima distinction is disclosed; closure-(c) temperature reconstruction is explicitly conditional on the branch image. All hedges are accurate.
- B6 consistency: B6's adopted verdicts were FAILS (comparison-of-constants transfer) and UNDETERMINED (guaranteed-bounce transfer). B7 does not contradict either: it does not revive a comparison of constants, does not claim production wins the KS race (it shows the race is *lost* near the turn), and does not claim a guaranteed bounce — it proves the turning condition is amplitude-based and satisfiable, which is precisely the question B6 left undetermined ("the sign of the relevant rate, nor the turning-point condition" — KIMI_B6 F4). B7 states the relationship itself: "This calculation makes the distinction without revising B6." The decision memo's acceptance criteria 1–4 are each met (constraint propagation verified with printed residuals; explicit turning inequality in source variables; pre-declared outcome token TURNS_OVER-for-open-set; singular/integration-failure statuses separated); criterion 5 is this report.
- Admissibility hygiene: INADMISSIBLE_INITIAL_DATA (negative ε) and INADMISSIBLE_PARAMETER (β<0) are rejected in the script and printed, not silently dropped.

## 6. Findings (all minor; none verdict-changing)

- **F1 (semantics, disclosed).** The "open set" is open in the constraint manifold (constraint-preserving perturbations), the standard notion for a constrained system; B7 says exactly this ("open neighbourhood on the constraint manifold") and never claims openness in the unconstrained data space. No action.
- **F2 (regime note, disclosed).** All exhibited turns occur where the spin correction is comparable to or larger than the ordinary energy (αn²/ε = 5/6 at the construction, w/e ≈ 1.43, ρ < 0 at the representative turn) — the regime the route summary flags as physically unestablished. B7 stipulates the effective system rather than claiming physical validity; consistent with result 4 of the route summary. No action.
- **F3 (observation, in B7's favor).** The main family turns even at B=1e-5 (my probe), tb approaching the control's blow-up time from below — the production/zero-production contrast is sharp, not threshold-search-dependent. B7 does not claim this; it strengthens the numeric picture.
- **F4 (nit).** The identical-to-12-digits tb in the (b)/(c) table is a variable-transform consistency check, not independent evidence; B7 labels its own agreement check as such and reports the two closures' constraint errors separately. No action.

## 7. Failed attacks (what I tried that held)

- Tried to find a sign/factor error in B7.1 by solving printed (10) for u̇,v̇ myself — got B7's equations to the symbol. Held.
- Tried to break the turning condition off-constraint (maybe C/6 contributes at H=0) — on admissible data C=0, and the degenerate-equality boundary analysis (Ḧ=-4KΔ/9, H⁽³⁾=-4K²/9) is exactly right, so no hidden crossing at equality. Held.
- Tried to break openness: direct 30/30 perturbation test around the analytic construction, plus 40/40 (±5%) and 39/39 (±10%) my-own random perturbations of the distant datum. Held.
- Tried to expose the turn as a solver artefact: LSODA at different tolerances and a scipy-free fixed-step RK4 both reproduce tb, Ḣ_b, e_b to B7's printed digits. Held.
- Tried to read the zero-production control as a solver failure: my clean control run confirms the analytic mechanism pointwise (Riccati bound respected, w<S throughout, Vn conserved, all invariants diverging at t≈0.3155). Held.
- Tried to catch (35) mishandled: quotation byte-exact [publisher HTML]; absolute-vs-fractional-vs-amplitude distinction algebraically correct; dimension observation correct; B7 itself states the turnarounds violate (35). Held.
- Tried to find flat leakage, a Dirac claim, an unlisted assumption, or a B6 contradiction: none (§5 enumeration). Held.

## 8. Evidence ledger

- `shasum -a 256` on publisher HTML + all four pinned lane inputs + both targets: all match B7's pins (values in §0).
- `/usr/bin/python3 b7_curved.py` → exit 0, full log `_tmp_kimi_b7_run.log`; every printed table value equals B7's reported values.
- Publisher equations extracted from saved bytes: `_tmp_kimi_b7_extract.py`, output `_tmp_kimi_b7_eqs.txt` (Eqs. (1),(2),(6),(10),(11),(14),(15),(16),(24),(25),(30)–(37)).
- Independent algebra: `_tmp_kimi_b7_sympy.py` → ALL_CHECKS_PASSED 12 (derives the ODE from printed (10)/(14)/(34), then constraint propagation both closures, energy residuals, Raychaudhuri and turn-margin off-constraint, degenerate Ḧ/H⁽³⁾, fractional race, J-evolution, J=3(Ḣ+H²) on C=0, shear identities, both Ṫ equations).
- Independent numerics: `_tmp_kimi_b7_numeric.py` (LSODA + hand RK4 reproduction; ±5%/±10% random perturbation sets; direct construction-point openness test; small-β probe), `_tmp_kimi_b7_control.py` (clean β=0 control with Riccati bound check), `_tmp_kimi_b7_quote.py` (Eq. (35) byte-compare: MATCH True).
- Lane files read in full (contents): B7 target, b7_curved.py, B2_CLOSURE, B6_APPLICABILITY, KIMI_B6_CHALLENGE, BOUNCE_ROUTE_SUMMARY (decision memo acceptance criteria).

## 9. Uncertainties and what I did not inspect

- Claims bind to the saved publisher bytes (SHA matched by B7, B6, and me); I did not re-fetch Springer live. Internal coherence (metadata, numbering, §§1–9 structure) gives no sign of divergence; if the saved file diverges from the version of record, B7 and this review inherit that jointly.
- I verified B7.1 from printed (10) algebraically rather than re-deriving the KS Einstein equations from the metric; (10) is the printed system B7 is contracted to use.
- The Kretschmann formula (B7.15) was used only as a finite diagnostic; I sanity-checked its blow-up behaviour in the control but did not independently re-derive the orthonormal-frame invariant from the KS Riemann tensor. It is not load-bearing for any claim.
- The analytic control argument's step 3 is a standard continuation argument; I verified its logic and every quantitative ingredient numerically but did not formalize it.
- Constraint compliance: report only — I modified no lane artifact; my writes are the `_tmp_kimi_b7_*` files above and this report.
