# BOUNDED WORKER — B7: can collapse turn around in the source's own curved scenario? (Tori, 2026-09-07)

AUTHORISED by Duho on 2026-09-07 (relay `/Users/duhokim/work/Trio/duho-approvals-20260907-1939.md`): continue the curved
Kantowski–Sachs scenario; determine whether curvature-driven shear outpaces production-boosted spin, whether collapse can turn
around, and under what conditions. Negative and inconclusive results are explicitly accepted. You produce the derivation,
integration and tokens; you do NOT file a class or state a cosmological conclusion.

## (1) Governing source and the meaning of every ambiguous term
Governing text: Popławski, *Gen. Relativ. Gravit.* **53**, 18. **The PUBLISHER article HTML is saved at
`bounce/b4_springer_access.html` — prefer it, and state for each quotation whether you read it, the arXiv copy, or an extraction.**
- **KS** = the source's own Kantowski–Sachs geometry, its Eq. (6), curvature `K = 1/Y^2`; this is the geometry under study.
- **Flat Bianchi I** = where our earlier B1/B3 results live. They do NOT transfer: B6 (challenged, VERDICTS_STAND) established
  that. Never state a flat result as if it held here.
- **FLUID row** = Weyssenhoff spin fluid, Eq. (1). The Dirac row is EXCLUDED from this task entirely.
- **The established identity** (B6, independently re-derived): `d(a^6 S)/dt = (2/3) a^6 K Delta`, where `S` is the source's
  printed shear scalar and `Delta = Xdot/X - Ydot/Y`. In KS `a^6 S` is NOT conserved — that is why the flat argument fails.
- **Closures (b) and (c)** = the two determined closures from `B2_CLOSURE_20260907.md`. Run BOTH, separately; do not choose.
- **Production** = the source's own `Psi = beta H^4`, its Eqs. (33)–(35). `beta` has no numerical value in the source
  (`B4_BETA_COMPARISON_20260907.md`: BETA_VALUES=UNRESOLVABLE_FROM_SOURCE), so carry it as a parameter, and carry `alpha` as an
  interval as always.

## (2) The question and the ACCEPTANCE CRITERION (from the approved decision memo, verbatim in substance)
Answer: **does curvature-driven shear growth outpace the production-boosted spin term in KS, and can collapse turn around?**
The task is complete only when all five hold:
1. constraint propagation is VERIFIED for the exact system you integrate, in each closure, with the residual printed;
2. the turning-point condition is stated as an explicit inequality **in the source's own variables** (X, Y, Delta, K, n_f, beta);
3. the outcome is reported as exactly one of `TURNS_OVER` (for an open set of admissible data), `NEVER_TURNS_OVER`, or
   `STILL_UNDETERMINED` with the missing input NAMED — all three are acceptable results;
4. genuine singular behaviour is distinguished from integration failure, and inadmissible data from either;
5. every added assumption the sources do not print is listed explicitly.
Compare your turning-point inequality with the source's own Eq. (35) and say whether they agree, differ, or are different
conditions (B6's referee noted printed (35) uses absolute rates while a fractional-rate condition is a different inequality).

## (3) Allowed inputs and exclusions
Allowed: the saved publisher HTML, the arXiv copy, the local APS PDFs, this lane's `bounce/` files and scripts. EXCLUDED: any
pinned census file, the R3C2 kit, any Hwao lane or unopened evaluation data. Do NOT adopt a new closure, build a framework,
acquire a new corpus, re-open B1–B6, or assert anything about black-hole-universe cosmology generally.

## (4) Output and completion
Write `bounce/B7_CURVED_20260907.md` and `bounce/b7_curved.py` (runnable as `/usr/bin/python3 b7_curved.py`, deterministic, no
network, no writes outside `bounce/`). Print both sha256s, the per-closure constraint residuals, and the outcome token as your
final answer.
