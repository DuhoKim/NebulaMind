# Lana — V7 exact-hash gate: UNCONDITIONAL PASS

**Lana (science / claim-boundary seat), 2026-08-13.** Bound to exactly:

- `NARRATION_DRAFT_V7.md` — `3380497f0514e906db8463d0fdd2ffd1f0b02b37ac6825e3bfdec86011c2edc0`
- `STORYBOARD_DRAFT_V7.json` — `3077f0636385487bb4092d2032c18bbedaedb647780bbfc581e59027c87a8d2b`
- `CLAIM_LINE_LEDGER_V5.md` — `871a808c4f2af94e24ef68b19cef416b7f7e3295720dab25ef8676753c845b5a`
  (byte-identical carryover — correct for a referent swap; no claim moved, so no ledger change is due)

## VERDICT: **PASS — unconditional.** My seat's list is closed.

## 1. The card-06 delta, verified by my own diff

V6→V7 is exactly two fields, both card 06, in both channels where each lives: the assertion heading
("the packet" → "the closing record", narration and storyboard identically) and the diagram label
(PACKET DOES NOT ADJUDICATE → CLOSING RECORD DOES NOT ADJUDICATE). No other card, field, timing, or
top-level key moved — checked field-by-field, not taken from the report. Both new strings are sound:

- **Heading:** "the closing record" is the referent card 06's own narration earns in the same
  breath ("Lana's closing record does not decide which side applies"). Accuracy unchanged — the
  heading still asserts exactly the packet's refusal, no more.
- **Label:** the crew chose my offered alternative over my first recommendation; accepted without
  reservation. "ADJUDICATE" on screen is earned in-the-moment by the simultaneous spoken "does not
  decide which side applies" — and I twice proposed adjudicate-bearing labels myself, so the word
  carries my seat's precedent as well as the comprehension bar.

## 2. My own whole-artifact sweep — clean, with two hits adjudicated on the record

I ran an independent crew-vocabulary sweep over every viewer-facing string (headings, narration,
diagram-quoted labels, on-screen support, all eleven cards), with a broader term list than the S4
family. Result: no "packet", "hash", "receipt", "prereg", seat name, or other crew-internal referent
in any viewer-facing string. Two grep hits surfaced and are ruled, not passed over:

1. **Card 11 diagram, "gates" — false positive.** The instruction describes *drawn locked gates* the
   CONFIRMED SPIN ASYMMETRY token cannot unlock. That is the lay visual metaphor itself, not crew
   gate-vocabulary; the rendered labels are the REOPEN CONDITION strings. Clean.
2. **Card 07 narration, "The spin lane was not chasing a phantom" — ruled acceptable, with
   reasoning.** "Lane" is crew vocabulary, but unlike "the packet" it has a live plain-English
   metaphorical reading ("area of work") that lands the sentence's exact meaning for a stranger, and
   the surrounding sentences ("…led to our sky work… It followed an explicit, source-backed
   qualitative claim") anchor it. The sentence is also the script's carrier of my packet's
   fair-account finding, in nearly its original words. Five reviews have passed it consistently;
   flagging it at gate seven would be inventing work. If a V8 ever exists for other reasons,
   "our spin search" is a drop-in improvement; it does not block render.

Narration spoken lines: clean. The remaining "packet"/"hash" strings in the narration file are the
per-card "Source:" provenance annotations and the status scaffolding — never spoken, never rendered.

## 3. On Hwao's non-blocking observation — concur, with a filing note

`NARRATION_DRAFT_V7.md`'s H1 still reads "narration draft V5" and its scaffolding says "generated
from STORYBOARD_DRAFT_V5.json". Concur: invisible to any viewer, no render effect, and this verdict
binds hashes, not self-descriptions — no gate cycle. But Hwao's species identification is right
(stale self-description inside a certified chain), so it should not be left to memory: **fix it in
the same commit as the next byte change to these files if one ever occurs; otherwise, record the
discrepancy in the render receipt** so the archived chain explains itself to a future reader. Either
way it costs nothing tonight.

## 4. What this pass rests on (so the closure is auditable)

The V5 whole-read verdicts (accuracy against Revision 5; spine; opening 35 s; pacing 120–135 WPM;
the R11-lineage no-fabrication guarantee on card 05's non-scaled callout; Kun's replacements
adjudicated for comprehension) carry to these bytes because V5→V6→V7 changed exactly five strings
across two cards, each verified by my own diffs at each step, none touching a claim. Every repair I
raised across this chain — R1–R11, S1–S3, S4, and the card-06 pair — is now verified landed with
intent in the frozen bytes. **No known viewer-facing instance of crew-internal vocabulary remains.
My seat has nothing further; render is released as far as this seat is concerned.**

Boundary: this pass releases render only in concert with the other two seats' unconditional passes;
no upload or publication is authorized by it; publication remains Duho's decision on the finished
artifact.

— Lana, 2026-08-13.
