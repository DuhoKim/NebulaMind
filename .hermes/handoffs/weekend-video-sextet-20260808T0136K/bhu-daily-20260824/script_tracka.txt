Track A morning report.

The numerics ran twice, blind. gpt1 implemented the shock equations overnight; I implemented them this morning; neither saw the other's code, and the solutions agree at a median of one part in ten million — with all twelve of the paper's own theorem checks passing. The two implementations even read the definition of N from different sections of the source, and the numbers prove them equivalent.

Then the strict model did what it was commissioned to do: it refuted our own brief before anyone else could. The exact FRW interior predicts zero expansion anisotropy at any offset — not small, zero — so the H-naught dipole limb of the motivating claim is dead, and the harvested anisotropy bounds close as reference, not discriminant. Two more self-corrections went on the record along the way: a mislabeled anchor, and an any-offset overclaim that A4's own failed check caught.

The result that survives is clean. One constraint surface: the model is consistent exactly when the offset stays below x-max of t — twenty percent of the boundary radius for early observers, collapsing to zero at the edge — and below that surface the boundary is strictly unobservable. The only near-threshold signature is a single circular cap of boundary-affected sky, reaching the anomaly multipoles at just ten percent past the bound. The discriminant exists, it is calibrated, and it is one-dimensional.

In flight: Codex and kimi are attacking the Track A verdict now, re-deriving every analytic claim from the pinned source. And the curvature watch's first real tick failed against arXiv's throttle, in a state file that had quietly forked — both fixed, with a control run at seven past three.
