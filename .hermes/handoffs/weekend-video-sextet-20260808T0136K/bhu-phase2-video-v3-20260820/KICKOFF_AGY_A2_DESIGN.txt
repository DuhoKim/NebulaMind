You are agy, the design seat. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-phase2-video-v3-20260820 . Read OVERNIGHT_BRIEF_P2V3.md (Duho's verbatim verdict: "graphic quality too low" — this is the fix), and review ../bhu-phase2-video-v2-20260819/VISUALS.md plus its build/ output style for what was NOT good enough. Write DESIGN_SYSTEM.md with two parts. PART 1 — the visual language: full-bleed 1920x1080 layout grid; type scale (display headings 64-96px class, body annotations 36-44px class, chips 28px class — everything legible on a phone); a coherent dark cosmic palette (specify hex values, 4-6 colors + accent); animation vocabulary (staged builds synced to narration beats, highlight cursor style for plot walkthroughs, transition style between panels — no divider cards); the attribution-chip and illustration-chip designs; the band-ladder and magnitude-visual house style (labeled, both edges, no unlabeled log tricks). PART 2 — per-panel art briefs for Nano Banana Pro (assume the 14-16 panel plan from COMPREHENSION_AUDIT.md if present, else draft against the v2 12-panel structure and mark panels likely to split): for each panel needing illustration art, an EXACT generation prompt (style-consistent across all: specify the shared style descriptor string to prefix every prompt), what text if any appears in-image (only large legible display text), the layout slot the art fills, and a fallback programmatic-illustration spec if generation is unavailable. Concept art needed at minimum: spacetime twist from spin; the crushed-ball bounce; the black-hole nursery; the one-sentence card backdrop; the causality speed-limit; the helium fossil record; a cold-open cosmic backdrop for panel 01. Generated art illustrates CONCEPTS only, never data — say so in each brief. Finish with AGY_A2_DONE.md, first line AGY_A2_COMPLETE. No fetches. Never portal.nersc.gov.