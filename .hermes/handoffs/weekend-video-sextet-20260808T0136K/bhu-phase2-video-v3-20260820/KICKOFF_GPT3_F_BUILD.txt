You are gpt3, the build seat. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-phase2-video-v3-20260820 . Precondition (verify first): KIMI_P2V3_PACKET_GATE.md exists, first line PASS_P2V3_PACKET — else stop and write GPT3_F_DONE.md first line GPT3_F_BLOCKED with the reason. Read OVERNIGHT_BRIEF_P2V3.md (Duho's verdict on v2: "graphic quality too low" — THIS BUILD is the fix; do not reuse the v2 look), DESIGN_SYSTEM.md (the binding visual language: implement PART 1 exactly — type scale, grid, palette hexes, staged builds synced to narration beats, highlight-cursor plot walkthroughs, chip designs, band-ladder house style), SCRIPT.md, STORYBOARD.json, CLAIM_LEDGER.md, assets_v3/GENERATION_LOG.md (the fallback ruling: nbp_p01_cold_open.png is the ONLY generated image — verify its hash against assets_v3/PINS.sha256 and use it full-bleed for panel 01; every other art slot uses the panel brief's PROGRAMMATIC FALLBACK spec from DESIGN_SYSTEM.md PART 2, rendered in the design system's palette and quality bar, with its NebulaMind-rendering chip). The v2 build ../bhu-phase2-video-v2-20260819/build/ may be mined for toolchain plumbing ONLY (encoders, TTS/ASR glue) — not for panel design. Requirements: 1920x1080; the four paper plots embedded large and legible with attribution chips and animated highlight walkthroughs per the specs; staged element builds (nothing appears before its narration beat); assertion headings in the display type scale; only the three permitted equations; labeled band ladders both edges; Planck markers; no divider cards. Narration audio via hermes gateway TTS (gpt-4o-mini-tts, same voice), text byte-identical to STORYBOARD.json; measured pace ~125-135 wpm with breathing pauses at panel turns (insert explicit silence gaps; do NOT speed up the voice). Assemble build/BHU_PHASE2_EXPLAINER_V3_LOCAL_REVIEW.mp4, target 10-12 min. FULL ASR QA from the final mp4: per-panel alignment in ASR_QA.md; contract-bearing residuals (numbers, felt comparisons, caveat, Reading-1 clause, parent-spec clause, the B-17 quote, verdicts) unacceptable — re-render until clean; state finals with cosmetic-vs-contract judgment. ALSO write build/PANEL_STILLS/panel_NN.png (one representative frame per panel, 16 files) so the render gate can frame-audit cheaply. Freeze: sha256 of the mp4, each narration wav, and each panel still in GPT3_F_DONE.md, first line GPT3_F_COMPLETE. Rules: local pipeline only (gateway TTS/ASR the only network); no generation credits; writes in this lane (build/_tmp_*); audio never into the monitored cockpit root. Never portal.nersc.gov.