You are Lana, the science seat. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/bhu-theory-phase1-20260819 . Stage 1 is fully gated (PASS_TRACKA_AUDIT, PASS_MODELSPEC via KUN_P1_MODELSPEC_GATE.md + KUN_P1_MODELSPEC_REGATE.md). Now execute PHASE1_BRIEF.md Track B steps 3-5 under the frozen MODEL_SPEC.md, in order, with sympy/python receipts into receipts/ for every algebraic and numerical step:

(1) DERIVATION_OMEGA_EVOLUTION.md — derive omega(a) per era from the frozen spec's A6-A7, pinning each decay exponent to the fetched sources in GORU_INGREDIENTS.md items 1-2 (quote the exact sentence next to each exponent you adopt; if inflation-era behavior is not covered by a pinned source, parameterize it per the spec's Z_inf and say so). Map the S1/S2 present-day bounds back to the spin-acquisition epoch z_ta (state and pin your z_ta choice; give the mapping for a range of z_ta, not one cherry-pick).

(2) DERIVATION_TRANSFER_FUNCTION.md — the original step: tidal-torque theory in a weakly rotating background. Derive the parity-odd handedness bias A(omega(z_ta)) from the pinned TTT L-growth result plus the vorticity-induced angular momentum component: define the bias operationally exactly as the frozen spec's observable, derive the coefficient C in A = C * omega/omega_ref including its epoch dependence, state validity limits (perturbative regime, when tidal randomization dominates), and bracket C's uncertainty honestly. Every algebra step sympy-verified; dimensional checks receipted. Where a genuinely underivable input appears, parameterize it, name it in the assumptions ledger, and continue — no silent assumptions.

(3) CONFRONTATION_AND_INVERSION.md — evaluate A at the bound-allowed omega with the DERIVED C (compare against Phase 0's order-of-magnitude bracket and state whether the strict result strengthens, weakens, or overturns the closure); then the inversion: translate the S1/S2 bounds through your omega(a) result into a constraint on (a_star, epsilon) of the parent black hole — state it as "the CMB already constrains the parent to ..." with the error budget.

Finish with LANA_DERIVE_DONE.md, first line LANA_DERIVATIONS_COMPLETE, listing the three files with SHA-256 and a 5-line summary each. Fetches only if a new pinned source is unavoidable (quote verbatim, save under sources/). Never portal.nersc.gov.
