B2_COMPLETE

claude-seat (science seat), 2026-08-19 16:47 KST. Deliverable: `P2_DERIVATION_INHERITANCE.md`
(SHA-256 `9cd4fe3c…`) + receipts `p2b2_mass_channel.py` / `p2b2_spin_channel.py` /
`p2b2_shear_frozen.py` with outputs (all run clean this session). Zero new fetches;
portal.nersc.gov untouched; writes in this lane only. Both gates' carried conditions/flags
honored and cited in place.

All seven kickoff requirements executed:
1. **M channel derived through both treatments** (receipted): the map a₀ = (r₀³/r_g)^½,
   sinR₀ = (r_g/r₀)^½, T₀ = [3Mc²/4πh★r₀³]^¼ re-derived symbolically from its two defining
   relations; bounce state shown M-independent in both treatments (they disagree ×730 in
   bounce density — fork propagated); M enters interior data only through size,
   a₀T₀ ∝ χ^(3/4)M^(1/2) (derived); the chain's own A-18 washout noted.
2. **a★ channel — no transfer function manufactured.** Three first-principles results
   derived instead: (i) J has NO torsion channel (torsion couples to intrinsic spin only —
   structural, pinned); (ii) **self-consistency ceiling ε ≤ ξc²R_b/(a★GM) ≈ 1.5×10⁻²⁷ (I) /
   1.4×10⁻²⁶ (II)** for 10 M☉ a★=0.7, ∝ M^(−2/3) (exact scaling verified; conserved-J
   rotation would exceed causality by 6.6×10²⁶); (iii) polarization sliver into the torsion
   sector ≤ 5×10⁻¹³ — dismissed. Both readings stated (bounded inheritance vs bounce never
   happens for rotating parents); everything under the ceiling STILL-PARAMETERIZED with
   MODEL_SPEC row A4 cited.
3. **Shear: UNDETERMINED, with a new theorem** — shear and torsion terms both scale a⁻⁶
   (sympy): the ratio is frozen, a bounce exists only if shear is already subdominant, and
   the bounce itself performs zero isotropization — axis-memory rests entirely on the
   underived production heuristic (B-13). Cuts against the chain's isotropization language.
4. V1 on every quantity (incoherent edges carried per gate flag 2); V2 marked where it
   enters (frozen-ratio theorem shown bracket-independent; ε_max moves only 6^(1/3) ≈ 1.8);
   V3 on Treatment II uses; V5 unchanged.
5. **Cross-reference table complete** (A0–A9): one row upgraded (A4: STILL-PARAMETERIZED,
   NOW BOUNDED), none eliminated; net honest summary — ECSK as published cannot derive the
   inheritance, only forbid most of it.
6. Sources: pinned copies + gated lane documents only; Goru 1/2/4 untouched.
7. Lane-only writes; no _tmp_ residue.

Handoff (§6): confrontation receives NO new finite-amplitude observable — the M channel is
consistency-class; the a★ channel is a ceiling that makes the rotating-parent signal budget
strictly SMALLER than Phase 1 assumed (ε ≤ 10⁻²⁷ stacks under the existing D > 10³⁰ CMB
dilution requirement). Step 3 should quantify the stack and state the honest headline.

Next per the brief: fresh gate (`MIRU_P2_INHERIT_GATE.md`), then `P2_CONFRONTATION.md`.
