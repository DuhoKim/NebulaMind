# PHASE 2 — the Popławski torsion-bounce chain, strict treatment

Hwao (director), 2026-08-19 ~16:00 KST. Duho, verbatim: **"go ahead with target 1, the Popławski
chain"** — selecting rank 1 of the gated published bibliography
(`../bhu-published-bibliography-20260819/BHU_PUBLISHED_BIBLIOGRAPHY.md`, entries 8–12 spine).

Scope label: BHU is Duho's personal side-interest, not a NebulaMind research programme.
Standing rules in force: **published papers only as base** (preprints = context, cite the
bibliography's appendix); strict-model bar (audit + in-house derivations, no borrowed
normalizations as results); every number fetched and quoted; kill nothing by rhetoric;
external-theorist review required before any publication claim.

## The mission, from the gated ranking

Re-derive the Einstein–Cartan(–Sciama–Kibble) bounce and the Ω_S ≈ −8.6×10⁻⁷⁰ present-day
torsion number (erratum trail included); then derive, in-house, **the transfer function the
literature never wrote — parent-hole parameters (M, a★) through the spin-fluid bounce to interior
observables** — and state honestly whether ANY finite-amplitude signature survives. This
supersedes Phase 1's ε/f_b ignorance-parameterization wherever ECSK mechanics can actually
derive the inheritance; where it still cannot, the parameterization stays and is named. The axis
question is reached ONLY through this published chain.

## Tracks and seats (audits parallel, derivations after gates)

**Track A1 — core-mechanism audit (Lana):** equation-by-equation of the spine's bounce papers
(the PLB 2010 cosmology-with-torsion paper and the PRD spinor-torsion bounce paper, per
bibliography entries — read the entries for exact citations; fetch full texts from ar5iv/journal,
pin SHA-256 under sources/). Verdict rows CHECK/ERROR/UNSUPPORTED/POST-HOC with sympy receipts;
special attention: the spin-fluid averaging step (a known critique target), the sign and
magnitude chain to Ω_S, and the erratum's effect. → `TRACK_A1_AUDIT.md` + `LANA_A1_DONE.md`
(`LANA_A1_COMPLETE`).

**Track A2 — universe-in-a-black-hole audit (Lana-2, parallel seat):** the spine's
interior/collapse papers (the ApJ-class universe-in-BH paper and the 2025 collapse-with-torsion
paper, per bibliography entries; fetch + pin). Focus: the matching at the horizon/bounce, what
of the parent is claimed to imprint (mass, spin, anything), and every stated observable
consequence. Same verdict discipline. → `TRACK_A2_AUDIT.md` + `LANA2_A2_DONE.md`
(`LANA2_A2_COMPLETE`).

**Ingredients (Goru, parallel):** published critiques/replications of the ECSK bounce (the
spin-fluid averaging debate; any published parameter constraints on torsion cosmology); any
published parent→interior inheritance calculation in ECSK; the Ω_S erratum trail verified from
the journal record; and any published bound on interior vorticity/axis from this chain. Verbatim
quotes + venues (published only; preprint critiques go in a context list). →
`GORU_P2_INGREDIENTS.md` + `GORU_P2_DONE.md` (`GORU_P2_COMPLETE`).

**Gate 1 (Miru, fresh one-shot):** both audits + ingredients — recompute a sample of each
audit's receipts, spot-resolve the erratum record, adjudicate any cross-seat disagreement. →
`MIRU_P2_STAGE1_GATE.md` (`PASS_P2_STAGE1` / HOLD).

**Track B — strict derivations (Lana, after Gate 1):**
1. `P2_DERIVATION_BOUNCE.md` — the ECSK bounce re-derivation with our own algebra (sympy
   receipts), reproducing or correcting the published Ω_S with the erratum applied.
2. `P2_DERIVATION_INHERITANCE.md` — the original step: parent (M, a★) → spin-fluid state at the
   bounce → interior initial conditions (density, vorticity, torsion relic). Where ECSK
   determines a quantity Phase 1 parameterized, derive it and cross-reference the Phase 1 spec;
   where it cannot, keep ε-style parameterization and say so.
3. `P2_CONFRONTATION.md` — every derived observable against its best published bound (reuse
   Phase 0/1 pins where applicable; fetch anything new); the honest headline: does any
   finite-amplitude signature survive, at what magnitude, observable by what.
   Per-step Miru gates (`MIRU_P2_<STEP>_GATE.md`).

**Wrap:** `PHASE2_SUMMARY.md` after the last gate.

## Boundaries

Literature hosts only; **never `portal.nersc.gov`** — the acquisition chain is live and OWNS that
host; acquisition dispatches preempt this lane's attention. Writes in this lane only. Fresh Miru
one-shot per gate; grep-extraction, time-boxed checks, UNVERIFIED-AT-GATE over crawling. Budget
note: Moonshot wallet ~$27 — gates are metered; if it dips under $10, pause gating and report.
