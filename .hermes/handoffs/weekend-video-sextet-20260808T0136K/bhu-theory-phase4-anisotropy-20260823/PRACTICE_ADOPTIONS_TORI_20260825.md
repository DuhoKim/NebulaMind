# Lane adoptions from Blanc's external-practice relay (2026-08-25, Tori)

Source: blanc-ops-overhaul-20260820/EXTERNAL_PRACTICE_ADOPTIONS_20260825.md (Duho-surfaced
hellices/harness-workshop). Taken as offered — what fits this lane:

1. **Academic-gates checklist** — adopted as a freeze-gate item. The Track B apparatus already
   carries most of it (DOIs, venue classes, arXiv versions, source checksums, the verifier as
   the reproducible script, the span ledger); the checklist adds, for any FUTURE external-data
   freeze: bibcodes where ADS is the route, dataset/release identifiers when survey data is
   quoted, and archived query strings if any API harvest feeds a frozen number.
2. **One-change-per-iteration** — adopted for amendment cycles. Honesty note: today's v7 cycle
   bundled three changes (deletion rule, symmetric unit-splitting, sentence fallback); each
   carried its own hypothesis and self-test, but the bundle is the v8-refutation shape Blanc
   warned about. Next cycle: one change per rerun unless a documented hypothesis requires the
   combination.
3. **Name the observable after a failure-driven instruction** — adopted retroactively for the
   two standing fixes: the watch repair's observable is the LANE state stamping last_run with
   null last_error at the Aug 31 10:00 tick; the verifier v7's observable is codex's deletion
   corruption failing on its regate-4 retry. Both get checked on the named runs.
