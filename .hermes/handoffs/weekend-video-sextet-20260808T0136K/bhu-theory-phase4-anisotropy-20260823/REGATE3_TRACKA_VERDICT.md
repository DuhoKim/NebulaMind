PASS_TRACK_A_AMENDED

# Phase 4 Track A — third regate

Scope was limited to the single residue in `REGATE2_TRACKA_VERDICT.md`: the three conflicting passages in `A3_RECEIPT.md`. No other Track A content was re-examined.

## Verification

1. **Regimes paragraph — PASS.** `A3_RECEIPT.md:30–33` now states that keeping every direct CMB ray interior is a **sufficient** hiding condition, while boundary-crossing observational inconsistency remains uncalibrated and necessity is not established.

2. **`t_obs > t_1100` item — PASS.** `A3_RECEIPT.md:42–45` now limits the claim to crossing geometry, states that a crossing direction's observational appearance is uncalibrated, and explicitly says the regime is not excluded by this track.

3. **Correction-addendum dichotomy sentence — PASS.** `A3_RECEIPT.md:70–74` now scopes opacity to direct post-recombination photons under an approximate sharp `z_ls` screen and defines `x_off < x_max(t_obs)` only as a sufficient no-cap ray-containment condition, not an observational iff.

The REGATE2 Track A residue is cleared.