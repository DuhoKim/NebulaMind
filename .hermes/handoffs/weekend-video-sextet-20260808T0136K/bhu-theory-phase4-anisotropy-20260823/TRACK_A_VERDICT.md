# Track A verdict — the strict interior model, complete (2026-08-24, Tori; pre-gate draft)

Basis: A1 (blind-double-confirmed shock trajectory, 12/12 checks), A2 (light-cone geometry,
5/5, two §6 bounds hit from inside), A3 (observables; one self-correction), A4 (prediction
functions; second self-correction). All σ=1/3, all receipts and pins in-lane.

**Verdict (proposed, pending cross-engine gates):**

1. The anti-Copernican discriminant EXISTS and is calibrated — but it is one-dimensional, not
   three. Expansion-rate anisotropy: identically NULL (exact FRW interior — our own brief's
   claim refuted on this limb). Non-kinematic dipole: NOT PREDICTED (TOV optics unmodeled).
   The single surviving observable is CMB-sky uniformity.
2. The calibrated statement: consistency ⇔ x_off < x_max(t_obs) (P2 surface; 20% of the
   boundary radius for early observers, collapsing to zero at t_1100). Below the surface the
   boundary is STRICTLY UNOBSERVABLE (opacity + interior exactness) — the model is consistent
   exactly where it is untestable.
3. The only near-threshold signature is P3's single large-angle circular cap (ℓ ≲ 10 within
   10% past the bound) — a concrete morphology Track C can compare against claimed CMB
   anomalies.
4. Honesty ledger: two self-corrections (A1 anchor label; A3 trichotomy → dichotomy), one
   loud numerical failure kept on the record (forward shooting), the brief's motivating claim
   partially refuted by its own strict model. K3 respected: no parameters beyond
   (x_off, t_obs) with r_* fixed by the σ=1/3 orbit and gauge.

Class implication for the branch if gates concur: still CONSISTENCY-ONLY in the hidden
regime; acquires its first CALIBRATED exclusion surface; PROSPECT-grade only through the
cap-morphology question, which Track C must answer before any stronger word is used.

---

# AMENDMENT 1 (2026-08-24, post-gate; supersedes the conflicting language above)

Codex gate: HOLD_TRACK_A_UNCALIBRATED_OBSERVABLES_AND_OVERCLAIMS. kimi gate: PASS_TRACK_A.
Both engines confirmed every equation, derivation, and number; Codex's blocking objections
target the verdict's prose claiming more than the receipts establish. All four are accepted:

1. **"Calibrated exclusion surface" is withdrawn.** x_max(t_obs) is a GEOMETRIC CROSSING
   surface: x_off < x_max is SUFFICIENT for all direct CMB rays to remain in the exact-FRW
   region. It is NOT proven necessary for observational consistency — a TOV side that is
   non-disruptive across the cap has not been shown impossible (no transfer function,
   amplitude, or spectrum was derived; "conspiracy" was rhetoric, not proof). The "iff" is
   replaced by "sufficient". P3's cap is a set of crossing directions with an angular-scale
   heuristic ℓ ~ π/θ, not a predicted multipole spectrum.
2. **"Strictly unobservable" is withdrawn.** The correct statement: below the surface, the
   boundary is unobservable TO DIRECT POST-RECOMBINATION PHOTONS whose complete paths remain
   interior; the z_ls = 1100 sharp screen is an approximate photon-screen criterion (no
   visibility function computed), and other messengers (neutrinos, gravitational waves) were
   not analyzed.
3. **The H₀-anisotropy limb closes only partially.** Exact null holds for sources whose
   complete light paths remain interior (A0). For boundary-crossing or boundary-influenced
   probes — which TRACK_A_SETUP.md's own observable (b) included — the model is UNCALIBRATED,
   not null. "NOT-A-DISCRIMINANT" applies to wholly-interior expansion probes only.
4. **Validity domain restricted.** All results hold for pre-horizon observer epochs
   (t_obs ≤ t_crit, N ≥ 1). The post-exit regime (shock on the Schwarzschild geodesic,
   0210105 §7) was not derived and nothing here speaks to it.

Non-blocking corrections executed with this amendment: the t_over_t0 label in
a1_shock_trajectory.py and its CSV column corrected to t_over_tcrit (CSV regenerated,
re-pinned); A1_CROSSCHECK custody note refreshed with the post-addendum A1_RECEIPT hash;
A4_RECEIPT's t = 0.14 sample now cited to the in-script check rather than the delivered map.

The amended verdict: Track A delivers a verified strict-model geometry (blind-double-confirmed
numerics, four confirmed analytic laws) whose observational content is a SUFFICIENT
hiding condition and an uncalibrated cap morphology — a foundation for Track C, not a
finished discriminant. Upgrading it to necessity requires TOV-side optics, which is new work
beyond this brief unless Duho commissions it.
