# Track C verdict — the confrontation, executed as pre-registered
(2026-08-25 11:06 KST, Tori; pre-gate draft. Criteria: TRACK_C_BRIEF.md sha 1b4fd2e4…,
unchanged since registration; go: TRACK_C_GO_RECORD.md.)

## C1 — primary confrontation (template selected by facts)

Fact check against the frozen tables (TRACK_B_FREEZE.md, v8 pins): no frozen observation
engages a crossing signature. The B3 rows are power-deficit, variance, and correlation
STATISTICS; the B2 rows are dipole measurements the gated model does not engage (A3(c):
nothing calibrated predicted). Template 1 therefore applies, verbatim from the brief:

**The branch KEEPS CONSISTENCY-ONLY, now with the quantified sufficiency surface attached
(x_off < x_max(t_obs), Track A's gated product), and the cap morphology files as PROSPECT
contingent on TOV-side optics.** The branch is NOT excluded, NOT constrained, and NOT
supported by this confrontation — sufficiency does not exclude, and uncalibrated does not
constrain.

## C2 — cap-morphology judgment (four pre-registered criteria, all required)

Per-row evidence, frozen tables only:

| criterion | B3.1 Planck13 deficit | B3.2 Planck18 variance | B3.3 Billi24 estimator | B3.4 Copi15 S½ | B3.5–B3.7 counters |
|---|---|---|---|---|---|
| 1 localization (single region, one axis) | FAIL — full-sky spectrum statistic | FAIL — map-variance statistic | FAIL — variance estimator | FAIL — cut-sky two-point statistic | FAIL — same statistic classes |
| 2 scale (ℓ ≲ 10 reachable) | pass (ℓ ≲ 40 range includes it) | pass (large angles) | pass (ℓmax 26) | pass (θ > 60°) | pass |
| 3 patch character (bounded region vs surroundings) | FAIL — no region identified | FAIL | FAIL | FAIL | FAIL |
| 4 no-dispute survival | pass (finding below is significance-independent) | pass | pass | pass | pass |

Criteria 1 and 3 fail on every frozen row. **Finding: NOT MORPHOLOGY-COMPATIBLE on the
frozen record.** The frozen anomalies are deficits and statistics of the whole (or cut) sky;
the model's near-threshold signature is one circular patch around one axis. Caveats carried
verbatim from the brief: the localized-anomaly literature (e.g. the Cold Spot) was NOT
harvested, NOT frozen, and was NOT reached here — this finding speaks only to the frozen
record; and even a compatible reading would have been PROSPECT-grade only, since no amplitude
prediction exists without TOV-side optics.

## C3 — B2 engagement record

B2.2 (intrinsic-dipole bound < 3.6–3.7 mK) and B2.5–B2.11 (the dipole-excess dispute):
**AVAILABLE-BUT-UNENGAGED.** The gated model predicts no non-kinematic dipole for interior
signals and nothing calibrated for crossing signals; no verdict may cite these rows for or
against the branch. A future TOV-optics track re-opens this.

## Kill-criteria compliance

K-C1: no frozen dispute was resolved or leaned on (C2's finding is significance-independent).
K-C2: no number outside the freeze was used; the Cold-Spot class is named as a GAP, not
reached. K-C3: all language within the gated Track A scope (sufficiency, photon-channel,
pre-horizon, σ = 1/3 digits under the "rough qualitative models" caveat). K-C4: no
bibliography change until both engines pass this verdict.

## Phase 4 summary statement (pending gates)

Phase 4 delivers: a verified strict-model geometry (blind-double A1; two §6 bounds hit from
inside); the z_c = √N law; the sufficiency surface x_max(t) — the branch's first quantified
hiding condition; the single-cap PROSPECT morphology; an 8-corruption-proof frozen bounds
apparatus; and this confrontation's verdict that the branch stays CONSISTENCY-ONLY on today's
record, with the honest path to more named (TOV-side optics) rather than implied.

---

# AMENDMENT 1 (2026-08-25 11:15 KST, post-gate; supersedes the conflicting content above)

Codex gate: HOLD_C2_CRITERION_DRIFT_ROW_MISCLASSIFICATION_AND_SCOPE_EXCESS. All five
objections accepted. The brief itself was never altered (sha re-verified by the gate); the
EXECUTION drifted, which is exactly what the pre-registration was built to catch.

## C2 rerun under the REGISTERED criterion 2 (correspondence, not inclusion)

The registered test: the anomaly's angular size must CORRESPOND to ℓ ≲ 10. The frozen tables
supply NO per-row angular-size value establishing that correspondence for any row. Under
K-C2, absent evidence is a NAMED GAP, not a pass. Corrected per-row table, each row's
statistic class in the freeze's own words:

| criterion | B3.1 (low-ℓ TT power deficit, 5–10% at ℓ≲40) | B3.2 (real-space temperature-map variance LTP) | B3.3 (optimized variance estimator LTP, ℓmax 26) | B3.4 (cut-sky S½ two-point correlation statistic) | B3.5 (quadrupole cumulative posterior + full-sky C(θ) interval) | B3.6 (Bayesian odds reassessment) | B3.7 (estimator/mask-dependent full-sky vs masked p-values) |
|---|---|---|---|---|---|---|---|
| 1 localization | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| 2 scale corresponds to ℓ≲10 | GAP — no angular-size value frozen | GAP | GAP | GAP | GAP | GAP | GAP |
| 3 patch character | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| 4 no-dispute survival | pass | pass | pass | pass | pass | pass | pass |

(B3.1's earlier "full-sky spectrum statistic" label is withdrawn — the freeze records a low-ℓ
power deficit and does not say "full-sky" for that row.)

**Finding unchanged in outcome, corrected in basis: NOT MORPHOLOGY-COMPATIBLE on the frozen
record** — criteria 1 and 3 fail on every row (all four were required), and criterion 2 is a
named evidence gap on every row rather than a pass. The finding remains
significance-independent. Caveats as registered (Cold-Spot class unfrozen and unreached;
any compatible reading would be PROSPECT-grade).

## Kill-criteria compliance, corrected

K-C2: the scale-correspondence evidence is ABSENT from the freeze for all seven B3 rows —
named here as gaps, as the rule requires. K-C3: the phase summary's "hiding condition" is
restored to gated scope (below). Other compliance claims stand.

## Phase 4 summary statement, corrected to line-by-line support

Phase 4 delivers: a verified strict-model geometry (blind-double A1, A1_CROSSCHECK.md; two
§6 bounds hit from inside, A2_RECEIPT.md); the analytic center-crossing law z_c = √N(η_e) —
supported by A3_RECEIPT.md's derivation, gate-verified to 1e-6, and claimed on that receipt,
not on the verdict set; the sufficiency surface x_max(t_obs) as a hiding condition FOR DIRECT
POST-RECOMBINATION PHOTONS ON WHOLLY-INTERIOR PATHS, PRE-HORIZON EPOCHS ONLY (Track A
Amendment 1 scope); the single-cap PROSPECT morphology; a frozen bounds apparatus whose
EIGHT EMBEDDED CORRUPTION CASES ALL FAIL (a finite battery, not a proof of
corruption-proofness); and this confrontation's verdict that the branch stays
CONSISTENCY-ONLY on today's record, with the path to more named (TOV-side optics), not
implied.

---

# AMENDMENT 2 (2026-08-25 11:28 KST; single residue from the codex regate)

Codex regate: HOLD_B3_STATISTIC_CLASSES_NOT_FREEZE_VERBATIM — four of five objections
discharged, one residue: Amendment 1 claimed its per-row classes used "the freeze's own
words" while introducing terms absent from TRACK_B_FREEZE.md (TT, real-space,
temperature-map, posterior, two-point correlation statistic). The sharpest, B3.5, recast
"ΛCDM at cumulative 0.824" as a "cumulative posterior" — assigning a statistic type the
freeze never assigns. Accepted in full.

**Corrected C2 row labels — each is the freeze's row label copied byte-for-byte
(TRACK_B_FREEZE.md lines 77–83), with nothing added:**

| criterion | B3.1 "Low-ℓ power deficit" | B3.2 "Low large-angle variance LTP" | B3.3 "Optimized variance estimator LTP" | B3.4 "Cut-sky S½ missing correlations" | B3.5 "COUNTER: quadrupole not anomalous" | B3.6 "COUNTER: Bayesian odds" | B3.7 "COUNTER: estimator dependence" |
|---|---|---|---|---|---|---|---|
| 1 localization (single region, one axis) | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| 2 scale corresponds to ℓ ≲ 10 | GAP | GAP | GAP | GAP | GAP | GAP | GAP |
| 3 patch character (bounded region vs surroundings) | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL | FAIL |
| 4 no-dispute survival | pass | pass | pass | pass | pass | pass | pass |

Every characterization beyond these frozen labels is WITHDRAWN — including Amendment 1's
"cumulative posterior", "TT", "real-space temperature-map", and "two-point correlation
statistic". The localization and patch-character FAILs rest on what the frozen labels and
values themselves describe (sky-wide or cut-sky deficits, variances, odds, and p-values —
none naming a bounded region around an axis), not on any added classification.

**Finding unchanged: NOT MORPHOLOGY-COMPATIBLE on the frozen record.**

## Recorded gate disagreement (not adjudicated by me)

The kimi gate (KGATE_TRACKC_VERDICT.md, first line
"HOLD_C2_SCALE_TEST_DRIFT_AND_KC2_KC3_BREACH", written against the pre-amendment verdict)
independently found the same C2 scale drift and the same K-C2/K-C3 breaches codex found —
two engines converging on the drift is the strongest evidence in this phase that the
pre-registration worked. On two of codex's other sub-points kimi explicitly DISAGREED,
defending the original wording: it holds the z_c = √N law IS supported by the gated record
via A3_RECEIPT.md, and that "8-corruption-proof" was defensible as "proof against those
eight". Both disagreements are moot in effect — Amendment 1 had already adopted the
sharper form each gate preferred (cite A3_RECEIPT; state the finite battery). Recorded
because a split between gates is a fact of the record, not something for me to resolve.

---

# AMENDMENT 3 (2026-08-25 14:10 KST; kimi regate conversion condition 5)

Kimi regate: HOLD_PHASE_SUMMARY_OMITS_C2_MORPHOLOGY_FINDING — all named grounds of its
original HOLD discharged; one conversion condition unmet: the phase summary states the C1
outcome but never names the C2 finding, and the summary is the likely quotable text.
Accepted. The summary's final clause is replaced by:

**…and this confrontation's two results: (C1) the branch stays CONSISTENCY-ONLY on today's
record, with the path to more named (TOV-side optics) rather than implied; and (C2) the
frozen large-angle anomalies are NOT MORPHOLOGY-COMPATIBLE with the model's near-threshold
signature — localization and patch-character fail on all seven frozen B3 rows, and the
scale-correspondence evidence is absent from the freeze on all seven (named as gaps, not
passes). The C2 finding is significance-independent: it does not depend on which side of the
frozen low-ℓ significance dispute is right.**

That replacement is the operative phase summary; the earlier final clause is superseded.
