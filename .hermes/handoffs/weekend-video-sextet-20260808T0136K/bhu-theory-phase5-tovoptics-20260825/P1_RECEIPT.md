# P1 receipt — the geometry repaired, and the exterior is TRANSPARENT across every allowed
# assumption
(2026-08-25, stamped at commit. p1_optical_depth.py, log _tmp_p1_run.txt, 5/5 checks.
Blind double dispatched to gpt1 before this receipt was read by anyone; no P1 number is final
until the two agree.)

## The derivation the gate demanded

Inside the horizon A = 1−N < 0, so r̄ is timelike (pinned, eq. 3.5). The comoving fluid moves
along r̄ with u^r̄ = √(N−1). For a photon k^μ, −u·k = k^r̄/√(N−1), and dλ = dr̄/k^r̄, so

  **dτ_opt = σ n_e dr̄ / √(N−1)**

**The photon's trajectory cancels completely.** Inside the horizon the optical depth along ANY
null ray depends only on the r̄-interval it traverses, not on its direction — the angular
geometry that would matter outside a horizon is irrelevant here. This is the invariant integral
the gate required, and it is *simpler* than the discredited proxy, not more complex. My S0
error was not conservatism; it was using a timelike coordinate as a spatial column length.

## The exterior profile

Closing the pinned system (3.2)–(3.3) with A6's p̄ = wρ̄ and integrating outward from the
crossing (N = 6.501, r̄_s = 0.4047, ρ̄_s = 2.037, w_shock = 0.2456) reaches the horizon N = 1 at
r̄ = 2.096 = 5.18 × r̄_shock. **The integral converges there** despite the 1/√(N−1) factor —
shown, not asserted (LC4).

## Result, across the authorized ranges

τ at a Hubble-time anchor:

| w (A6) | f_b = 1 | f_b = 0.1 | f_b = 0.01 |
|---|---|---|---|
| 0.001 | 2.5e-4 | 2.5e-5 | 2.5e-6 |
| 0.05 | 1.3e-2 | 1.3e-3 | 1.3e-4 |
| 0.2456 (shock value) | **5.8e-2** | 5.8e-3 | 5.8e-4 |
| 0.30 | 6.6e-2 | 6.6e-3 | 6.6e-4 |

w → 0 forces vacuum (the Oppenheimer–Snyder limit) and τ = 0 exactly.

## The finding — and it is assumption-ROBUST

**τ ≤ 0.07 across the ENTIRE authorized assumption range**, attained only at maximal baryon
loading and the largest allowed equation-of-state parameter. **The exterior is optically thin.**

This settles the branch question the withdrawn S0 could not: there is no photosphere, and no
knife edge. The transparent branch is selected, and selected robustly — the plasma unknowns
A1–A6, which K4 forced us to commission a whole sub-brief for, turn out **not to be
load-bearing for this question**, because even their most opaque corner gives τ < 0.07.

Consequence for what follows: P2's transfer integral is in the optically-thin regime, where
absorption and emission are perturbative corrections to the kinematic shift rather than
competitors to it. S2b's photosphere branch is retired — correctly computed, wrong regime.

## Limits

σ = 1/3 interior, pre-horizon, photon channel. A6 (bulk equation of state) was added as a
dated addendum before any P1 number existed. The anchor conversion uses the same Hubble-time
scale as the withdrawn S0 for comparability; τ scales as 1/t_crit as before. Y_e = 1 in the
table (helium-rich Y_e = 0.5 halves every entry, deepening transparency).
