# Disposition of REGATE4 — the phase's positive finding is gone
(2026-08-27. HOLD_NULL_EXISTENCE_AND_FLATNESS_UNSUPPORTED. Both FAILs accepted without argument.)

## Claim 4 — the cancellation is NOT a property of the model. FAIL, and it empties the phase.

I asked whether the null's existence survives the loss of both thermal closures. It does not.

The gate supplies an explicit counterexample: **a source that is constant across crossing epochs
has zero source-gradient, so the coefficient stays at +0.615301 and never crosses zero.** Both
of our roots were outputs of closures the epoch ruling invalidated — my blackbody junction
anchor, and the seat's adiabatic carry. Reproducing a root under two assumed source maps proves
robustness *within those two maps*, not existence over the unrestricted set of positive source
fields.

**Permitted:** "the two tested closures each contain a cancellation, at different locations."
**Not permitted:** "the pinned model contains a silent configuration whose location alone is
unknown." **Both existence and location are closure-dependent.**

That was the phase's positive finding. It is withdrawn.

## Claim 5 — my flatness closure was a small-numbers illusion. FAIL.

I compared a 0.11% change in a ratio that sits near unity. Because the ratio is near 1, its
percent change **suppresses exactly the quantity being diagnosed**. The gate measured the
relevant residual instead: it changes by about 50%, and direct projection changes the
coefficient by 0.566675 **and reverses its sign**. So the shape is not flat at the anisotropy
scale, and FLATNESS_GAP_CLOSED.md does not close the gap. Withdrawn.

## What survives

- **Claim 1** (nothing reaches us from beyond the boundary): CONDITIONAL PASS, scope narrowed.
- **Claim 2** (the invariant optical-depth element, photon trajectory cancelling): PASS in its
  stated comoving-fluid scope.
- **Claim 3** (β_rel = −1/√N): CARRIED PASS, unchanged since it was first gated.

## The consequence I must act on immediately

**WHAT_IS_AT_STAKE_BHU_20260827.md now overclaims**, and it has been read aloud twice. Its
headline — "the model can produce no signal at all without being wrong" — is exactly the
sentence this ruling forbids. The supportable version is weaker and more careful:

> *Under the two source models we were able to test, the model produces no signal at a
> particular setting. Whether that silence is a property of the model itself, rather than of
> those source models, is not established — and cannot be, without thermodynamics the papers
> never supplied.*

The document must be corrected before it is quoted further. That is the next action.

## Standing lesson

I built a phase headline on a feature found under two closures and treated agreement between
them as evidence about the model. Two wrong closures agreeing is not evidence about the thing
they are both approximating. The gate's counterexample took one line.
