# Disposition of the S0–S2 gate: K4 has fired. Stopping for a decision.
(2026-08-25, stamped at commit. Codex verdict: HOLD_S0_OPTICAL_DEPTH_AND_S2_EXCLUSION_UNDERIVED.
kimi gate still running.)

**All seven objections accepted.** No argument is offered against any of them.

## The one that stops the phase — objection 2, and it is K4

The gate: setting n_e = ρ̄/m_p is an extra thermodynamic/baryon-loading assumption, and an
inconsistent one — at the crossing p̄/ρ̄ = 0.246, so the exterior is not a cold proton fluid.
The pinned matching fixes the exterior's energy density and pressure; it fixes neither its
baryon fraction, nor its pair content, nor its temperature.

That is exactly the brief's **K4**: *"if the junction requires physics outside the pinned
metric … STOP and record: that is a modelling choice, not a derivation, and it needs its own
brief and go."* K4 has fired. **I am stopping S0's line here rather than choosing a plasma
model myself**, and per the standing correction I am pinging before passing the stop
condition, not after.

## What is WITHDRAWN

- τ_R = 0.34 at a Hubble-time anchor, and the τ = 1 anchor at 1.47e17 s. These are arithmetic
  values of a **dimensional proxy, not an optical depth**. The deeper error is geometric and
  is mine: r̄ is TIMELIKE inside the horizon — my own S1 receipt quotes the pinned source
  saying so — and I nonetheless used it as a spatial column length in S0. A proper τ is the
  invariant line integral ∫σ_T n_e (−u·k) dλ along the null path, which neither implementation
  computed.
- "Marginally transparent", "knife edge", and every statement that the exterior's transparency
  is known. It is not known.
- The 3.86e-6 exclusion (already withdrawn by my own S3 amendment before this verdict).
- S2's label as "the transfer function": it computes a kinematic frequency shift only, with no
  absorption or emission along the path.

## What SURVIVES the gate

- **S1's analytic law β_rel = −1/√N.** The gate states its central algebra is correct; what it
  refutes is my verification hygiene — a generic Landau & Lifshitz citation with no
  edition/section/equation, and an LC1 "no-jump" check that passes on |β|<1 and therefore
  tests nothing. Both are real defects and are mine to fix; the identity stands.
- The PINNED/DERIVED mislabels (objection 6) are concrete and correctable: z_c = √N is a
  Phase-4 DERIVED result, not pinned; the crossing geometry likewise; and the "photon
  4-momentum is continuous, so ALL of the shift is fluid-velocity" step is an adapted optics
  claim that must be labelled DERIVED.

## The structural point that survives independently of τ

S2b showed the OPAQUE branch also produces an order-unity contrast, and S3 showed the pattern
is dipole-dominated with a frozen-row bound of ~1e-3. Those computations inherit the same
defects the gate names (no transfer integral, unfixed Doppler orientation, monopole handling),
so **no bound is claimed now**. But the shape of the argument — that both branches point the
same way, so the unknown τ is not load-bearing for the conclusion — is the thing worth
rebuilding correctly, and it is why the phase is not dead.

## The decision this needs (Duho's, not mine)

Rebuilding S0 correctly requires committing to a plasma model for the exterior — baryon
loading, pair content, and a source function — none of which the pinned metric supplies. Under
K4 that is a new modelling brief with its own go. Options as I see them, recommendation first:

1. **Commission the plasma sub-brief** — scope the minimal assumption set, state it as
   assumption rather than derivation, and rebuild S0/S2/S3 on the invariant transfer integral.
   Slower, and it is the only route to a defensible number.
2. **Rebuild only the τ-free part** — the geometric and kinematic content that needs no plasma
   model, and report the phase's result as conditional on transparency rather than resolving it.
3. **Close Phase 5 here** with S1's law as its durable product and the rest recorded as
   attempted-and-withdrawn.
