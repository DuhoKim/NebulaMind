**DEMOTE**

Entry 54 should be re-tiered from **CALIBRATED-FALSIFIER** to **QUALITATIVE-DIRECTIONAL**. The paper's model-hard observational content is the direction/sign `Ω_k < 0`; its nonzero numerical coefficient is tied to a conditional interpretation of the CMB homogeneity scale and does not calibrate the hard falsification boundary. I do not, however, uphold the opening finding's claim that an exactly flat universe is consistent with the model: the source explicitly requires strict negative curvature, so `Ω_k = 0` is excluded even though values can approach zero from below.

## Source answers

1. **Does the paper state `χ_k > χ_*`? — Yes.**

   Line 306 states verbatim:

   > “Recall from Eq. 8 that χ_k needs to be larger than the cloud boundary: χ_k > χ_*.”

   The same strict inequality also appears when the model is introduced at line 140. There is a source-level wording inconsistency worth recording: the abstract instead says `χ_k ≥ χ_*` at line 39.

2. **Does Eq. 27 therefore bound the magnitude rather than predict a value? — Yes.**

   Lines 329–333 give

   > `Ω_k ≡ −k(1/H_0)² = −(0.07 ± 0.02)(χ_*/χ_k)²`.

   With the strict body-text condition `χ_k > χ_*`, the ratio squared lies strictly between 0 and 1 for finite positive `χ_k`. Thus the `0.07 ± 0.02` term is a scale/magnitude ceiling, not a point prediction: `Ω_k` can approach zero from the negative side as `χ_k` increases. The abstract's compact range at line 39 — “`−0.07 ± 0.02 ≤ Ω_k < 0`” — is therefore a bounded interval, not a predicted value with uncertainty.

3. **Do the authors qualify or withdraw the numeric limits? — They qualify them; “withdraw” is too strong.**

   Line 336 states verbatim:

   > “The limits for Ω_k above assume that the homogeneity scale is the result of only χ_*.”

   It continues:

   > “However, if the homogeneity scale or the low value of C_2 has a different origin, then the value of Ω_k in the floating FLRW cloud could be smaller.”

   This makes the magnitude calibration conditional on an auxiliary interpretation. The authors do not literally say that Eq. 27 is withdrawn, and the brief/opening finding's word “withdraw” is not an accurate quotation or the most precise characterization.

4. **Is there a sentence stating the requirement as a sign? — Yes.**

   Line 336 states verbatim:

   > “Inflation preceded by a bounce requires Ω_k < 0.”

   The same model requirement is supported earlier by lines 286–288, which say `k > 0` (equivalently finite `χ_k`) is critical for a bounce and that an infinite FLRW cloud has no bounce.

5. **Does a confirmed flat universe refute this model? — Yes, if “confirmed flat” means the universe is exactly `Ω_k = 0`; no finite-precision result merely consistent with zero establishes that condition.**

   The source's strict requirement is `Ω_k < 0` (line 336). Exact flatness, `Ω_k = 0`, violates that requirement. The fact that allowed negative values can approach zero arbitrarily closely does not make zero itself allowed; a limit point is not a member of the strict interval. Accordingly, the bibliography sentence “a confirmed flat universe refutes it” is logically consistent with the pinned source.

   Operationally, however, a measurement centered on zero with finite uncertainty would not by itself refute the model, because it would still admit arbitrarily small negative curvature. The opening finding's examples such as `Ω_k = −0.0001` are allowed, but they do not establish that exact `Ω_k = 0` is allowed. Its statements that flatness is “consistent with the model” and that the falsification condition is “backwards” do not follow from the source.

6. **Does the entry meet “number + threshold”? — Not in the sense needed to distinguish CALIBRATED-FALSIFIER from QUALITATIVE-DIRECTIONAL.**

   Eq. 27 contains a number, but that number multiplies the unspecified ratio `(χ_*/χ_k)²`, and line 336 makes its observational calibration conditional on attributing the homogeneity scale/low quadrupole only to `χ_*`. The model-hard falsification boundary is simply the directional sign boundary at zero. Treating zero as the requisite calibrated number would collapse any sign prediction into the calibrated class and erase the stated distinction from **QUALITATIVE-DIRECTIONAL**.

   The best existing class is therefore **QUALITATIVE-DIRECTIONAL**: the model requires closed curvature (`Ω_k < 0`), while its quoted magnitude scale is conditional and can become arbitrarily small. A new tier is unnecessary.

7. **Testimony: what I could not verify.**

   I did not verify the external observational analyses cited by the paper (Planck, ACT, DESI, or the homogeneity-scale study), so I offer no finding about whether current data confirm, disfavor, or can operationally establish exact flatness. I adjudicated only the classification against the pinned primary-source text. I also did not compare the cleaned text byte-for-byte with the publisher's typeset PDF; line references above are to `2505.23877_clean.txt` as instructed.

## Quotation check against the pinned source

- The brief's shortened Eq. 27 formula is accurate (source lines 329–333), though it omits the preceding definition `Ω_k ≡ −k(1/H_0)²`.
- The quoted `χ_k > χ_*` statement is accurate (line 306).
- The quoted sign requirement `Ω_k < 0` is accurate (line 336).
- The brief's characterization that the authors “withdraw the ceiling” is not verbatim and overstates the source; the source conditionally qualifies the limits.
- The brief's inference that “a confirmed flat universe refutes it” is false is itself unsupported by the source's strict inequality. Exact flatness refutes; finite-precision consistency with flatness does not.
