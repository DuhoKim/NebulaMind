LANA_ANNOTATIONS_APPROVED

# Lana — review of Goru's three claim-bearing on-screen annotations

**Lana (claim-boundary seat), 2026-08-18 23:04 KST.** Reviewed against the gated Phase 0 sources
under the same discipline as `CLAIM_LEDGER.md` (source keys C/S/K identical; hashes already
pinned there this session). Placement note for the record: the 1/√N annotation is proposed on
**Panel 03** (VISUALS.md line 45, beneath "REAL IMBALANCE MUST BEAT COUNTING NOISE"); the
NEEDED/ALLOWED labels are on **Panel 06**'s log ladder (VISUALS.md lines 92–93). All three are
**approved as proposed — no amendments required.** Approved rows appended to `CLAIM_LEDGER.md`
§6.

## Approved strings (exact)

1. `Counting noise floor = 1/√N` — Panel 03, beneath the counting-noise line.
2. `NEEDED (~1/100)` — Panel 06 ladder, marker at 10⁻².
3. `ALLOWED (~5/10M)` — Panel 06 ladder, marker at 5×10⁻⁷.

## Per-annotation verdicts

**A1 — `Counting noise floor = 1/√N`: APPROVED.** Maps to C 109–110 verbatim in substance:
"For a handedness asymmetry A = (N_cw − N_ccw)/N, the statistical floor is σ_A = 1/√N."
"Counting noise floor" is the plain-words name for σ_A; the formula is exact for the asymmetry
statistic the panel describes ("compare the 2 totals … larger than ordinary counting noise").
Rule compliance: the 22:55 amendment permits an equation as an on-screen annotation when it
clarifies — this is the amendment's own worked example, and ledger note F15 pre-covered exactly
this annotation at exactly this source. Narration stays equation-free (the annotation is viewer
text only). Optional, also pre-approved if Goru wants it for accessibility: the suffixed variant
`Counting noise floor = 1/√N (N = galaxies counted)` — the gloss adds nothing beyond C 109–110's
own definition of N as the sample count.

**A2 — `NEEDED (~1/100)`: APPROVED.** Maps to C 111–112: "a 3σ detection requires A ≳ 1×10⁻²."
~1/100 = 1×10⁻² exactly; the tilde carries "about," safe. "NEEDED" without the 3σ qualifier is
acceptable for the same reason ledger note F5 accepted the narration's "convincing detection":
the label is read under the panel heading ("…needs about 1 extra in 100") and beside body text
"NEEDED · ABOUT 1 IN 100" already ledgered as P06#2, and the 3-standard-deviations digit is
spoken at P07#2 before the kill is stated. No new claim beyond the mapped sentence.

**A3 — `ALLOWED (~5/10M)`: APPROVED.** Maps to C 99/106 (A ≈ 5×10⁻⁷, bracket upper edge) with
K 70 certifying 5.24×10⁻⁷. ~5/10M = about 5 per 10 million = 5×10⁻⁷ exactly (ledger note F1);
"10M" as "10 million" is standard and unambiguous next to the body text's spelled-out "ABOUT 5
IN 10 MILLION". "ALLOWED" marks the *upper edge* of the allowed bracket [~10⁻¹², ~5×10⁻⁷] —
showing the model's best case, which is the generous (anti-overclaim) direction for the kill
argument, and Panel 05's plates ("AT MOST ABOUT 5 IN 10 MILLION", "UPPER-EDGE ESTIMATE") carry
that qualifier on screen immediately before this panel.

## Ladder honesty check (context for A2/A3, no change ordered)

The markers sit 4.28 orders apart (K 76). Goru's spec — labeled ticks at 10⁻²…10⁻⁷, "4 distinct
stacked ×10 blocks separating them," 5×10⁻⁷ placed between the 10⁻⁶ and 10⁻⁷ ticks, "NO
unlabeled logarithmic spacing tricks" — satisfies the brief's honesty rule: four full ×10 blocks
plus the visible partial step is consistent with the text plate's "MORE THAN 10,000 TIMES TOO
SMALL" (>10⁴, C 114; K 76). Stating "4 blocks" in text anywhere ON SCREEN would slightly
understate; the current spec keeps block count as geometry only, with the claim carried by the
already-ledgered "more than 10,000" string. Fine as specified.

## Boundary

Review only; `VISUALS.md` untouched (approval recorded here and in the ledger — Goru needs no
edit). No fetches; `portal.nersc.gov` untouched. All writes in this lane.

— Lana, 2026-08-18 23:04 KST.
