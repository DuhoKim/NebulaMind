# C41 Track-B Shape 1 — measurement design **v2** (Kun refutation folded; execution behind `APPROVE C41 TRACK-B SHAPE-1 MEASUREMENT START`)

> **Revision v2** — Kun DESIGN_SOUND_WITH_PATCHES (`KUN_SHAPE1_DESIGN_REFUTATION.md`), all five
> folded. **F1**: a frozen **pace-statistic contract** artifact precedes the forecast — exact
> M_UV threshold(s) with declared rest-frame wavelength, slice edges, pace functional form,
> per-slice photo-z covariance treatment, and a **named cosmic-variance term instantiated from a
> cited source at freeze time** (bootstrap covers Poisson only, never instead of CV — the F-T4-1
> lesson); the forecast states exclusion ranges in that statistic's units, CV included. **F2**:
> the primary pace is computed WITHIN each catalog; any cross-catalog merged pace is a labelled
> secondary gated on a named overlap-bin seam test (the A/A′ precedent); chain disclosure gains
> three fields (rest-UV wavelength convention + per-catalog conversion; Eddington-bias
> forward-modeling status; published contamination estimate); catalogs without machine-readable
> completeness at the frozen cuts enter census-only (Class-X-by-rule); consumption is
> object-level only — published LF tables may cross-check, never input. **F3**: prediction
> entries additionally carry, at composition: numeric value WITH uncertainty in the frozen frame
> (or explicit `not-numeric-in-frame`), the F6 calibration-overlap field, and the source-span
> receipt; the forecast pre-states the expected number of CONFRONTABLE entries. Named hazards
> honored: c41_065 is an integrated faint-end-dominated statistic — it confronts the bright-end
> pace ONLY through a declared frame bridge, never silently; c41_076 (z≈16 candidates) stays in
> the candidate-population lane. **F4**: stage-two hook reworded honestly — positions retained
> so A2's data gap reduces to masks/randoms and burst-indicator columns, all three named NOW as
> stage-two requirements. **F5**: micro-delta review is a RULE — any post-approval change to a
> reviewed script requires Kun's review before its next execution; the executor may not
> self-approve deltas; per-run console logs are receipts.

Lane: `c41-trackb-shape1-uvlf-20260804` · Gated: Duho 2026-08-04 21:52 KST (history #1).
Designed by Hwao from the map's A1/A2 content; Kun refutes before the START gate. Shape-2's entire
lesson set is INHERITED AS LAW, not re-derived: reviewed-script protocol (agents write and review
scripts; only reviewed scripts produce numbers; no lane ever types a result), rules-first assembly
with peek logs, frozen pre-fetch forecasts, per-row exclusion reasons, frame/direction-explicit
prediction entries, per-candidate fetch guards, uniform unquoting at the TAP layer, flux-vs-error
column-pick preference rules, and receipts at every stage boundary.

## The question (from the map)

A1 (`actively_debated`, 11 entries): how fast does the UV-bright census decline beyond z ≈ 8?
The axis's literal settle-line (spectroscopic confirmation of z≳15 candidates) is telescope work —
tracked via ingest, not measured here. The catalog-grade measurement: **assemble the bright-end UV
luminosity function over z ≈ 8–14 from public photometric catalogs and confront its PACE against
the ledger'd model predictions** (feedback-free efficiency, non-thermal/bursty, variable-IMF
tracks). Designed as **stage one of A2's joint test**: sky positions are retained in the sample so
a clustering-bias stage can follow without re-fetching.

## Measurement

1. **T1 recon (reviewed script)**: global VizieR enumeration for catalogs carrying M_UV (or
   rest-UV flux + z), photometric redshifts, survey area, and completeness information at z>7.5 —
   candidate families: JADES, CEERS, COSMOS-Web, NGDEEP-class releases. Assembly rules frozen
   BEFORE contents are read; per-catalog chain disclosure fields (reduction, photo-z code,
   completeness method — the Kun-F6 shared-pipeline clause from the original plan) are REQUIRED
   columns of the manifest.
2. **Frozen forecast (F4 law)**: expected N per M_UV bin per z slice from published catalog
   counts, pre-fetch; the null's information content defined (what decline-pace range the census
   can exclude at that N).
3. **T2 prediction entries (Lana)**: A1-specific prediction-class entries composed from the
   corpus's model papers — number-density/pace predictions with `scope.baseline` and
   `scope.direction` fields MANDATORY at composition (the Shape-2 retrofit is now the standard).
   The A1 ledger side (c41_065's non-accelerated-decline basis; the c41_076 candidate population;
   c41_046/067 completeness controls) enters as the observational counterweight.
4. **T3 (reviewed script)**: fetch per rules; per-object provenance; LF construction per z slice
   with completeness corrections AS PUBLISHED per catalog (never re-derived); the pace statistic
   (decline rate of the bright-end density between adjacent slices) with bootstrap uncertainties;
   predictions overlaid LAST, per-entry frame verdicts, nothing defaults to consistent.
5. **T4 Kun** reproduction + forensics (anchor-guard declared as before) · **T5 Tori** receipts.

## Contracts (inherited + shape-specific)

- Single declared magnitude convention; per-catalog photo-z chain disclosure; predictions
  calibrated on the same catalogs flagged (F6); contamination/completeness clauses per
  c41_046/067 written into the assembly rules; the z≳15 tail reported as candidate-population
  statements only (A7-style boundary discipline — never confirmed-population claims).
- Honest outcomes, defined now: a measured pace with model verdicts; OR a scale/completeness-
  limited bound (publishable per the F5 precedent); OR an honest census of what public LF data
  cannot yet constrain — each traces to the frozen forecast.

## Lanes under the post-Goru protocol

Scripts authored by Hwao, reviewed by Kun (initial + micro-deltas as needed), executed directly —
no mechanical seat. Lana: prediction entries + any contract semantics deltas. Kun: design
refutation, script review, T4 (reserved-usage discipline: these ARE his T4-grade moments).
Tori: receipts. All numbers computed; all failures diagnosed on evidence before rerun.
