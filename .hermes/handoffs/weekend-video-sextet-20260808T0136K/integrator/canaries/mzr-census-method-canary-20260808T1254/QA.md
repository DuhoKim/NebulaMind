# QA — mzr-census-method-canary-20260808T1254 (v1)

## Verdict: **PASS — as a method-only silent canary.**

A numeric-guard PASS is not semantic authorization; this lane additionally has NO freeze, so
nothing beyond method may be shown, and nothing beyond method is shown.

## Checks

1. **Numeric-source guard**: PASS 10/10 twice; 157/178/62 single-hit on the semantically
   correct lines; 19/21 derivation documented in RECEIPT (counted from dropped_candidates,
   asserted in the figure build).
2. **Pattern fidelity, not content copy**: all nine moves realized from lane-own artifacts;
   zero spin wording or spin numbers; the intellectual core (move 7) is the lane's own
   recall/decoy/precision logic, not the mirror argument.
3. **No manufactured claims**: no eligibility verdicts, no relation, no measurement, no
   eligible-table count; single-table and not-a-ruling boundaries stated on-screen.
4. **Silence**: single H.264 video stream; no audio exists.
5. **Machine QA** (`audit_canary.py`): 10 states / 10 cards, all 9 expected cuts, none
   unexpected, sha `07f08990…` matches hashes.txt, 107.0 s.
6. **Encoded-frame QA**: contact sheet reviewed — badge on 10/10 states (safe-area position),
   hold stated by card 2 (~13 s), spine figure legible, ends on the gate order.
7. **Standards inherited from the spin line's guard stack**: structural hold, safe-area badge,
   citation footers, no meaning by hue/geometry alone (counts are direct labels). Full
   transform-stack acceptance runs can be added on request; the deck uses the same renderer
   copy and design idiom that passed all fourteen guards on the spin canary.

## Known limits

- Character overlay + URL close pending Hwao's deck-of-record style ruling (as with spin).
- Concat close-card hold (+6 s) as in all canaries; re-time if ever narrated.
