# QA — spin-method-canary-20260808T0315 (v4)

## Verdict: **PASS — as a method-only silent canary.**

Authorizes nothing beyond itself. `video_reportable_now` remains `false`.

## Checks

1. **Numeric-source guard**: PASS 11/11, run twice; single-hit evidence on `rows_parsed`,
   `N_tie`, `probed`.
2. **Opening-order standard** (upheld by the spin lane's completed v8 audit): the scientific
   question is now stated in the first card, before any method language; the
   not-reportable boundary still lands by card 2 (~18 s). Consistent with Duho's
   stakes-first structure bar.
3. **Forbidden-term sweep**: clean (v3 standard carried forward; question wording introduces
   no excluded context).
4. **Semantic scope**: unchanged card→allowed_scope mapping; the question is framed as the
   test's design question, no result direction implied either way.
5. **Silence**: single H.264 video stream, 1920×1080 @ 30 fps; no audio stream exists.
6. **Machine QA**: 11 states / 11 cards, all 10 expected cuts, zero unexpected, sha matches
   `hashes.txt`, 116.0 s = 110.0 s + 6.0 s close hold.
7. **Encoded-frame QA**: corrected opening frame verified at full resolution; no text
   collision with the character overlay; remaining states match v3.

## Known limits / queued corrections (recorded in RECEIPT.md)

- Equation card lacks the one-A-per-readout bridge; "dominance" undefined on screen; source
  footers still show receipt paths — queued for the next pass(es), footer fix first (needs a
  bounded candidate-workspace renderer-copy edit).
- Character overlay + URL close remain pending Hwao's deck-of-record ruling.
- Concat close-card hold unchanged; re-time if ever narrated.
