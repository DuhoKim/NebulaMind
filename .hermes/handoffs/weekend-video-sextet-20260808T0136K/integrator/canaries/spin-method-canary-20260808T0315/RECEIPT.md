# RECEIPT — spin-method-canary-20260808T0315 (v4)

Seat: `yui-video-integration`. Rendered 2026-08-08 03:15–03:18 KST (stamps from `date`).
Freeze in force: unchanged — `spin-method-canary-pass1-20260808T0153K`.

## What this is

Version 4 of the silent, method-only galaxy-spin visual canary. **v1 (0204), v2 (0235), and
v3 (0256) are preserved unchanged** as the correction lineage.

## The one change, and its evidence

The spin lane's final independent audit completed with **PASS — EXACT-CURRENT V8 STATIC
PROPOSAL HANDOFF ONLY** (lane `SEALED_STATIC_PROPOSAL_V8_HANDOFF`), independently confirming
the full delayed-review repair set — which upholds those comprehension findings as this
weekend's accepted standard. The first-ranked upheld finding applicable to this canary:
**the opening must state the scientific question before method jargon** (their audit's first
confirmed item is "immediate paper-naive question"). v1–v3 never stated the question.

Correction (card 1 body only; +2 s duration floor):

> "The question: is an apparent excess of one spiral handedness a property of the sky, or of
> the people who labelled the images? This cut shows only how that test was built. The result
> is deliberately not in this video. NebulaMind Lab."

The question phrasing follows the frozen storyboard of record's own title card ("a
pre-registered test of whether an apparent handedness excess is in the sky, or in the people
who classified it") — read-only, quoted in substance, no result stated. All other cards,
figures, and claims are byte-identical in text to v3.

## Remaining upheld findings — queued, not silently dropped

Applicable to this canary but NOT applied this pass (one correction per pass): a
one-A-per-readout bridge on the equation card; an on-screen definition of "dominance"; source
footers show receipt paths (`sources/…`) where the sealed deck standard wants human-readable
citations with paths in receipts only — this last one needs a bounded edit to the
candidate-workspace renderer copy (allowed by DELEGATION) and is first in queue for pass 9.

## Verification

- Numeric-source guard: PASS 11/11 twice; single-hit evidence unchanged.
- Machine QA (`audit_canary.py`): PASS — 11 states, all expected cuts, none unexpected, single
  silent H.264 stream, sha `f67a07d7…` matches `hashes.txt`, 116.0 s = 110.0 s + 6.0 s close
  hold.
- Encoded-frame QA: corrected opening verified at full resolution; remaining states match v3.

## Gates untouched

No TTS, no Git, no upload/publication, no shared-tool or public-asset writes.
