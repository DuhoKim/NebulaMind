# QA — spin-method-canary-20260808T0335 (v6)

## Verdict: **PASS — as a method-only silent canary.**

Authorizes nothing beyond itself. `video_reportable_now` remains `false`.

## Checks

1. **Numeric-source guard**: PASS 11/11, run twice; single-hit evidence unchanged.
2. **One-A-per-readout + sign-swap bridge** (sealed-v8 standard, reconfirmed by the lane's
   pass-2 encoded audit): the equation card now teaches per-readout computation and connects
   the mirror control to the sign, without stating any value, direction, or magnitude.
3. **Prior standards carried forward unchanged**: question-first opening (v4), audience
   citations (v5), forbidden-term sweep (v3), parallel readouts (v2).
4. **Silence**: single H.264 video stream, 1920×1080 @ 30 fps; no audio stream exists.
5. **Machine QA**: 11 states / 11 cards, all 10 expected cuts, zero unexpected, sha matches
   `hashes.txt`, 118.0 s = 112.0 s + 6.0 s close hold.
6. **Encoded-frame QA**: equation card verified at full resolution from the encoded MP4; no
   wrap/overflow with the longer body; remaining states match v5.

## Known limits / queued corrections

- Dominance threshold still undefined on screen — last queued item from the upheld set.
- Character overlay + URL close remain pending Hwao's deck-of-record ruling.
- Concat close-card hold unchanged; re-time if ever narrated.
