# RECEIPT — spin-method-canary-20260808T0335 (v6)

Seat: `yui-video-integration`. Rendered 2026-08-08 03:35–03:39 KST (stamps from `date`).
Freeze in force: unchanged — `spin-method-canary-pass1-20260808T0153K`.

## What this is

Version 6 of the silent, method-only galaxy-spin visual canary. **v1–v5 preserved unchanged**
as the correction lineage.

## The one change, and its evidence

**One-A-per-readout and sign-swap bridge on the equation card.** The spin lane's sealed v8
standard names "one A per readout" and the "equation/sign/swap bridge" among the repaired
presentation mechanisms carrying an exact-current independent PASS, and its pass-2 encoded
audit (03:22 KST) confirms the held candidate fails precisely for lacking them. Canary v1–v5's
equation card taught the statistic as if it were single-valued and never connected mirroring to
the sign.

Correction (card 6 body only; +2 s floor): the statistic is stated as computed separately for
each of the three readouts, on that readout's decisively-labelled sample; sign meaning kept;
one new clause ties the mirror control to the sign ("mirroring the images should swap the sign
of a signal that lives in the sky" — the same predeclared logic already on the schematic card);
"every value is withheld" replaces the singular. No result, direction, or magnitude implied.

All other cards, figures, and claims are identical to v5.

## Remaining queued correction

On-screen definition of the dominance threshold (last item from the upheld set). Next pass.

## Verification

- Numeric-source guard: PASS 11/11 twice.
- Machine QA (`audit_canary.py`): PASS — 11 states, all expected cuts, none unexpected, single
  silent H.264 stream, sha `55860f27…` matches `hashes.txt`, 118.0 s = 112.0 s + 6.0 s close
  hold.
- Encoded-frame QA: corrected equation card verified at full resolution; remaining states
  match v5.

## Gates untouched

No TTS, no Git, no upload/publication, no shared-tool or public-asset writes. The renderer
copy is unchanged since its documented pass-9 edit.
