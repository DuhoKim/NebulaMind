# RECEIPT — spin-method-canary-20260808T0345 (v7)

Seat: `yui-video-integration`. Rendered 2026-08-08 03:45–03:48 KST (stamps from `date`).
Freeze in force: unchanged — `spin-method-canary-pass1-20260808T0153K`.

## What this is

Version 7 of the silent, method-only galaxy-spin visual canary. **v1–v6 preserved unchanged.**

## The one change, and its evidence

**On-screen definition of the dominance threshold** — the last queued item from the upheld
sealed-v8 comprehension set ("undefined readout terms"). One caption line added to the
readouts figure:

> dominance threshold: the larger of the archive's spin-vote fractions (P_CW / P_ACW) must
> reach 0.60 or 0.80 — above one half, a tie is impossible.

The definition is grounded in the pinned sources: P_CW/P_ACW are the frozen catalogue's
vote-fraction columns (T1_FUNNEL header record), and the recorded zero-tie counts at both
dominance readouts are what "above one half, a tie is impossible" states. Card text is
unchanged; the correction lives on the figure where the term appears.

**With this change the upheld-findings adoption queue is EMPTY.** The canary line now carries
all six standards accumulated this weekend: parallel readouts (v2), zero forbidden audience
terms (v3), question-first opening (v4), human-readable audience citations (v5),
one-A-per-readout + sign-swap bridge (v6), dominance definition (v7).

## Verification

- Numeric-source guard: PASS 11/11 twice.
- Machine QA (`audit_canary.py`): PASS — 11 states, all expected cuts, none unexpected, single
  silent H.264 stream, sha `c627a87d…` matches `hashes.txt`, 118.0 s = 112.0 s + 6.0 s close
  hold.
- Encoded-figure QA: definition line verified legible with no collisions at full figure
  resolution; contact sheet covers all 11 states.

## Gates untouched

No TTS, no Git, no upload/publication, no shared-tool or public-asset writes. Renderer copy
unchanged since the documented pass-9 edit.
