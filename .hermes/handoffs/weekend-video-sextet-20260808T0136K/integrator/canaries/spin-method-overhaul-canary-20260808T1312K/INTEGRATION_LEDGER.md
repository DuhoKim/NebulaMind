# Sextet integration ledger

Candidate: `spin-method-overhaul-canary-20260808T1312K`
Independent design snapshot: `INDEPENDENT_REPRESENTATION_DESIGN.md` (written before any overhaul review packet was read)
Integration state: pre-audio, pre-render

## Packet custody and recommendations

| Seat | Independent finding | Recommendation | Integration disposition |
|---|---|---|---|
| Lana | Claim boundary held; narrative failed because the mirror discriminant was buried and withholding sounded bureaucratic. Amendment correctly binds the watched artifact to 0204 and bans audience-facing `parity`. | Open on the scientific question, make the mirror intervention the intellectual peak, motivate the later machinery, frame withholding as discipline, close by re-arming the question. Keep sign convention symmetric and explicitly undetermined. | **APPLY with source-safe wording.** Move real mirroring before funnel/equation; use `handedness` only; state image-linked versus labeling-process logic without a result direction; make the final boundary return to the question. |
| Goru | 0204 has nine states longer than eight seconds, only 24/114 seconds of figure runtime (21.1%), and audience-facing internal JSON paths. | Measure every material state, exceed 75% graphic runtime, keep holds under eight seconds, use survey/release citations. | **APPLY.** Sentence-native state ledger; continuous motion; graphics targeted above 90%; on-screen citation `Galaxy Zoo 1 data release — Table 2`; filenames confined to receipts. |
| Kun | 0204 has no audio or sentence timing substrate, uses static hard cuts, and is not self-contained enough to reproduce. Post-encoded acceptance requires sampled audio/action deltas, WPM, loudness, clipping, and a rebuild recipe. | Synthesize sentence tracks first, make progressive builds real, snapshot renderer and dependencies inside the canary, verify the encoded artifact rather than logs. | **APPLY.** Local `build.py`, `synthesize.py`, source copies, exact command/env receipt, per-sentence audio, frame-quantized action starts, encoded QA. Final PASS remains pending Kun's post-encoded amendment. |
| Tori | Corrected primary artifact is 0204. Current status allows method-only Alloy narration at 1.18 while reportability remains false. Source hashes and closed gates pass pre-build; encoded checks remain pending. | Keep every result/interpretation out; recheck actual pixels, audio, playback, prior-attempt preservation, and all closed gates after build. | **APPLY.** Freeze current status/order sources, run affirmative and negative scans, decode full MP4, preserve all prior attempts. Final PASS remains pending Tori's post-build amendment. |

## Preserved disagreements and resolution

### 1. Order of scientific elements

The independent design placed funnel and equation before mirroring. Lana argues the mirror must be the causal turn and emotional peak before the machinery. No source gate requires the numbered §4 elements to appear in the listed order. **Resolution: adopt Lana's sequence** — question → why counting is insufficient → real mirror intervention → frozen funnel → estimator → controls → gate timeline → boundary.

### 2. Strength of the mirror inference

Lana's shorthand `flips ⇒ sky; survives ⇒ us` is rhetorically sharp but stronger than the frozen method boundary and risky while the stored-direction frame is unresolved. **Resolution: do not render the shorthand.** Use the source-compatible asymmetric statement: `an image-linked pattern must invert; a labeling-process effect need not.` No observed behavior or world is selected.

### 3. Stakes language

Lana proposes `a fact about the universe`. That phrase can invite a forbidden broader interpretation. **Resolution: narrow it** to `a pattern tied to the images` versus `an effect introduced by the labeling process`. The scientific question remains compelling without broader interpretation.

### 4. When the design was frozen

Lana says every cut/control was fixed before a number was computed; earlier copy says before data were pulled. The source packet safely supports predeclared rules and a frozen source run, but broad absolute chronology wording is unnecessary. **Resolution:** say `the logic was frozen before the analysis result was read.`

### 5. Sign convention

The independent design proposed a positive/zero/negative sign rail. Lana correctly flags that an isolated positive definition can be heard as a leaked direction. **Resolution:** keep the rail visually symmetric, show no active side, and combine the definition with `value withheld` in the same sentence/state. No positive or negative side receives emphasis.

### 6. The 36-of-36 integrity check

The rejected cut treated this routine check as co-equal with the mirror idea. Goru did not require it, and the order requires the control matrix rather than every QA result. **Resolution:** omit it from narration and the audience-facing sequence. Preserve it only in source custody and receipts.

### 7. Exact next gate versus all open blockers

Lana names the independent post-run verdict review as the exact next gate. Tori correctly records both that missing record and `FRAME_UNSTATED`, plus later evidence/receipt/referee stages. **Resolution:** the timeline shows every blocker. The close names the independent post-run verdict record as the next review gate and simultaneously states that the stored-direction frame must also be resolved before the opening question can be answered.

### 8. Funnel topology

The watched 0204 card implied a sequential narrowing; the 0648 supplement changed to three parallel readouts. The frozen funnel artifact stores each readout from the same source. **Resolution:** use one source reservoir feeding three parallel branches. Never draw `flag → 0.60 → 0.80` as a sequential population chain.

## Integrated hard contract

- Primary rejected artifact: 0204, exact hash `2b1db497…`; 0648 supplemental only.
- Audience term: `galaxy spin handedness`; never audience-facing `parity`.
- No presenter, giant-number card, paragraph-dominant frame, internal filename citation, or frozen decorative hold.
- Real horizontal mirror of one conceptual spiral with simultaneous label inversion and a persistent `CONCEPTUAL — illustration, not data` tag.
- Parallel animated source/readout funnel with exact row units and attached counts.
- Progressive symbolic equation, no computed value, symmetric unselected sign convention.
- Bias-control matrix is design only and shows control × failure mode/test.
- Review timeline visibly blocks result rendering.
- Final boundary states known, not reportable, and next gate, then returns to the opening question.
- Alloy, speed 1.18, sentence-aligned synthesis first, no music, delivered 105–125 WPM.
- Visual action starts within ±0.3 seconds of each substantive sentence.
- At least seven materially distinct states; no unchanged state longer than eight seconds; at least 75% animated scientific graphics.
- Self-contained local build and full receipts.
- No upload, public/shared replacement, Git, deletion, repo-tool edit, browser, cockpit, DB, deploy/restart, billing/provider/config, or secret action.

## Pass boundary

Yui may build after this ledger. Yui may report machine QA and `PENDING_SEXTET_POST_ENCODED_REVIEW`, but must not call the candidate PASS until Kun and Tori append post-encoded amendments and their findings are reconciled.

## Narrative-correction addendum — Duho via Hwao

Authority: `reviews/HWAO_NARRATIVE_CORRECTION.md`, read with Lana's preserved packet before rendering.

The first integrated script/audio lineage is now `REJECTED_PRE_RENDER_NARRATIVE_GRAMMAR` and remains byte-preserved under `narration_script.json` and `audio/`, with exact hashes in `REJECTED_NARRATION_V1.md`. No v1 audio is reused.

Applied corrections:

1. **Mirror becomes the peak:** two unresolved explanations are posed first; five dedicated mirror sentences and the longest real-animation interval then establish the discriminant. The funnel has two sentences.
2. **Discipline replaces ticketing:** the script says cuts, thresholds, mirror logic, and paired comparisons were fixed before any number was computed. The specific frame/verdict/evidence/receipt/referee gates are then shown as consequences of that self-imposed standard.
3. **The question is paid off:** the opening images-versus-labeling question returns, followed by what the audience now has—the mirror discriminant inside a frozen test—and the one answer that is still withheld.
4. **Passing fixes retained:** the opening contains no disclaimer; the neutral sign definition and value withholding remain in one sentence and one visual state.

Replacement build lineage is `narration_script_v2.json` → `audio_v2/` → re-derived v2 timeline → render. Claim boundary and all closed gates remain unchanged.
