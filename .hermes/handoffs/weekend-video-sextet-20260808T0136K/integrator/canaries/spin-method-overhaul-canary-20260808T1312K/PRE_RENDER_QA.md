# Pre-render QA — v2 narrative-corrected lineage

Status: `CLEARED_FOR_LOCAL_FULL_RENDER`
Post-encoded Sextet verdict: still pending.

## Tori pre-render frame note disposition

The inspected artifact was `preview-contact-sheet-v2.jpg`, generated at the final 1920×1080 design resolution from the PCM-derived v2 timeline.

1. **State 15 denominator:** initial midpoint preview was too dark. The denominator reveal now begins at 20% and reaches full contrast by 62% of the state. The regenerated native-resolution midpoint shows the numerator, fraction bar, and denominator legibly while `A =` remains the later progressive element.
2. **States 17–18 lower control rows:** row labels are 30 px and tested-failure labels are 28 px. State 17's third-row reveal was advanced; state 18 retains all three rows at full contrast under `DESIGN ONLY · NO OUTCOMES`.
3. **State 20 gate labels:** EVIDENCE, RECEIPT, and REFEREE are now at least 70% contrast throughout the state and use 27 px labels. The answer node remains intentionally dim behind `STANDARD HOLDS HERE`.
4. **Discipline wording:** removed the visual phrase `not tickets`. The state now says `consequences of a standard set in advance` under `requirements we imposed before calculation`.
5. **Captions:** renderer validation measures every exact sentence at 31 px inside the subtitle width and hard-fails if any caption exceeds two lines. Current maximum is two lines; 17 of 24 captions occupy one line and 7 occupy two.

## Preserved narrative requirements

- Mirror sequence remains the longest and strongest conceptual graphic: two explanations → one real horizontal transform → two predicted behaviors → discriminant.
- Funnel remains three parallel branches from one source, with counts attached to stages.
- Equation remains symbolic; sign convention and value withholding share state 16.
- Control matrix remains design-only.
- Gate state frames open checks as consequences of rules fixed before calculation.
- Closing three states return to images versus labeling process and land on the scientific discriminant.
- No measured value, direction, or interpretation is shown.

Full render is local to this canary. No upload, publication, repo-tool edit, Git write, or prior-attempt deletion is authorized or performed.
