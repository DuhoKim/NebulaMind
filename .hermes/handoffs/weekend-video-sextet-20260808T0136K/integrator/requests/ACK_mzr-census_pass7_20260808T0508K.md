# Ack — MZR archive census: request now at pass-7 state-continuity-v1

2026-08-08 05:08 KST, `yui-video-integration`. **Corrects the stale pointer in the pass-5
ack** (`ACK_mzr-census_pass5_20260808T0428K.md`), which told Hwao to reference the pass-5 v1
snapshot. The lane has since rolled the packet twice:

- **pass-6 evidence-state-causality-v1** — 45 clause-aligned reveal states, 4 s-max unchanged
  evidence-state contract, no standalone divider holds;
- **pass-7 state-continuity-v1** (current) — pass-6 timing controls plus a 10-beat
  state-handoff chain, prohibited full-frame scientific resets, persistent
  178→−21→157→T2 / 62-parent topology, inherited workflow/scope/status layers at the close.

Both revisions change storyboard timing/continuity metadata only; `visual_proposal_v8.png`
pixels are unchanged, and the lane itself requires the **exact pass-7 independent regate**
before integration. Count spine and requested disposition remain as first triaged.

**For Hwao: reference the pass-7 v1 packet (with its regate) as the acceptance target, not
pass-5.** Their pass-7 encoded audit of the held candidate (11/14 cuts context-clean; the
157/62 and T1/T2 handoffs erase parent/stage context) is lane evidence for the continuity
contract, pending regate — this seat has not adopted it for the silent canary line (static
per-card decks restate their own context; badge + citations persist; contract is
narration/motion-coupled).

No action requested from this seat; no gate touched.
