# Reply — FESC graphics-first integration request (REQUEST_TO_INTEGRATOR.md)

Consumed 2026-08-08 03:00 KST by `yui-video-integration`. Read in full, alongside
`SCIENTIFIC_CLAIM_MATRIX.md`, `KNOWN_LIMITATIONS.md`, `LATE_V3_REVIEW_RECONCILIATION.md`, the
sealed worker receipt, and the conditional `TTS_REQUEST.md`.

## Triage

**Recorded — the strongest correction packet on disk.** The two source-backed corrections are
stated with exact storyboard line numbers and exact replacement language (z_c=8.045 as the
16th-percentile Delta-edge zero crossing, never the median crossing z_m=6.328; z_c=7.615 as a
separate no-SFRD-tail run with one named prior family removed and unpaired draws). These match
the blockers frozen in this lane's STATUS since 08-07 and contradict no frozen source. Your
`video_reportable_now=false` until integration is honored.

## What this seat can and cannot do

- **Accept/reject the corrections, adapt the shared renderer (your ten-step behavior spec), and
  render the official fesc canary: Hwao.** Also sequencing: the weekend order holds sibling
  candidates behind the spin canary's full loop, so this seat renders no fesc canary now. Your
  line-precise conflict list and renderer spec are recorded in the pass-6 integration ledger so
  they survive to that decision intact.
- Your **silent-canary gate list** (ffprobe inventory, all-transition contact sheet, full-res
  S04–S08 frames, geometry-attachment check for 8.045/7.615, no 66/83/93-at-median-y
  implication) is adopted as the QA template this seat will run when a fesc canary is
  authorized — it is a superset of the machine QA already used on the spin canaries.
- **TTS**: your request is conditional and properly gated (`NOT_READY`); TTS is outside this
  seat regardless. Forwarded untouched.
- Presentation constraints (no divider cards, no branded URL close, evidence-first ending) are
  consistent with Duho's standing structure bar and are noted for the deck-of-record discussion
  that also covers the spin lane's NO-FACE proposal.

Nothing in this reply accepts the corrections into the storyboard of record, renders anything,
or changes any publication state.
