# RECEIPT — integration pass 10 (integrator/)

Seat `yui-video-integration`, 2026-08-08 03:35–03:42 KST (stamps from `date`).
Authority byte-unchanged: order `ac5d3531…`, DELEGATION, COORDINATION_UPDATE.

## What changed and why

- **Fresh QA on canary v5 (0325)** at pass start: PASS, bit-stable.
- **The one evidence-backed correction — canary v6
  (`canaries/spin-method-canary-20260808T0335/`)**: one-A-per-readout + sign-swap bridge on
  the equation card, per the sealed-v8 mechanism list and the spin lane's pass-2 encoded audit
  showing the held candidate failing for exactly this gap. Guard 11/11 twice; machine +
  encoded-frame QA PASS; full §5 receipts. **v1–v5 preserved.** Remaining queue: on-screen
  dominance-threshold definition.
- **Consumed**: spin pass-2 encoded audit of the held 0149 candidate (FAIL on presentation/
  source-gate grounds; blocker packet deepened; T4 blocker re-verified OPEN; their
  integrator-safe next action recorded for Hwao) and mzr-census's pass-4 packet revision with
  a hash-pinned custody snapshot (ack filed; no re-triage needed — asks structurally
  unchanged).
- **`INTEGRATION_LEDGER.md`** pass-10 entry appended; **`STATUS.json`** refreshed.

## Preserved, not touched

Canaries v1–v5 with all receipts; the held 0149 candidate (read-only to everyone); all lane
artifacts and snapshots; pre-order `lanes/*`; repo `tools/`; public MP4s; all prior replies.

## Hashes

See `hashes_pass10.txt` beside this receipt.

## Gates

No publication, shared/public asset writes, TTS, Git writes, or browser automation. No halt
condition hit. Window end 2026-08-10 07:00 KST.
