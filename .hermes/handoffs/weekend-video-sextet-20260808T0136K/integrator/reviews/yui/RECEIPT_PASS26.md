# RECEIPT — integration pass 26 (integrator/)

Seat `yui-video-integration`, 2026-08-08 06:17–06:22 KST (stamps from `date`).
Authority byte-unchanged: order `ac5d3531…`, DELEGATION, COORDINATION_UPDATE.

- Fresh machine QA on canary v8 (0448): PASS, bit-stable.
- The one evidence-backed correction: spin pass-8 **redundant-encoding guard** adopted into
  this seat's QA gate; canary v8 run through the five-mode color-vision review (new
  audit_cvd.py; color/grayscale/protan/deutan/tritan contact sheets, all human-reviewed):
  **PASS** on every applicable clause. No pixel change warranted — no v9, matching the
  lane's own no-churn conclusion for sealed v8.
- Consumed: spin pass-8 completion (guard + audit + blocker packet; science blockers
  unchanged) and mzr pass-7 delegated reviews (regate pending; pointer rule covers).
- INTEGRATION_LEDGER.md pass-26 entry appended; STATUS.json refreshed.
- Preserved: canaries v1–v8, all lane artifacts, pre-order lanes/*, repo tools/, public MP4s.
- Gates: no publication, shared/public asset writes, TTS, Git writes, or browser automation.
  No halt condition hit. Window end 2026-08-10 07:00 KST.
- Hashes: hashes_pass26.txt beside this receipt.
