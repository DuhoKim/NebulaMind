# RECEIPT — integration pass 7 (integrator/)

Seat `yui-video-integration`, 2026-08-08 03:04–03:10 KST (stamps from `date`).
Authority byte-unchanged: order `ac5d3531…`, DELEGATION, COORDINATION_UPDATE.

## What changed and why

- **Fresh QA on canary v3 (0256)**: PASS, bit-stable — sha `2c803bba…` unchanged.
- **The one evidence-backed correction**: the pass-3 mzr-census triage reply was superseded by
  `requests/REPLY_mzr-census_UPDATE_20260808T0308K.md` after the lane revised its request at
  03:04 KST (taxonomy/closure-boundary grounds, v7 visual, pass-3 10-beat design,
  render-only-`on_screen_copy` rule, exact opening/closing copy). Substance of the triage
  unchanged; the record now matches the request actually on disk.
- **Discipline call, recorded rather than acted on**: the spin lane's v5 seal was superseded by
  late-arriving full review summaries; v6/v7 repaired, v8 is the pixel-identical
  custody-corrected current target, final QA **PENDING**. None of the pending comprehension
  findings were adopted into the canary — same rule that governed pass 5. Canary v3's
  forbidden-terms correction is unaffected (the standard survives v6–v8).
- **Reconciliation recorded**: c41-uvlf `FINAL_RECEIPT.json` V2 cleanly supersedes the
  premature V1 (resolves the pass-6 stale-verdict flag); fesc's v4 proposal now carries
  independent paper-naive `PASS; C=none` and scientific `PASS; R=none`, making the
  two-correction acceptance decision ripe for Hwao.
- **`INTEGRATION_LEDGER.md`** pass-7 entry appended; **`STATUS.json`** refreshed.

## Preserved, not touched

Canaries v1/v2/v3 with all receipts (QA read-only this pass); all lane superseded iterations,
reseals, and held candidates; pre-order `lanes/*`; repo `tools/`; public MP4s; every prior
reply (superseded ones stand as history).

## Hashes

See `hashes_pass7.txt` beside this receipt.

## Gates

No publication, shared/public asset writes, TTS, Git writes, or browser automation. No halt
condition hit. Window end 2026-08-10 07:00 KST.
