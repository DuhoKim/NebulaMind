# RECEIPT — integration pass 9 (integrator/)

Seat `yui-video-integration`, 2026-08-08 03:25–03:31 KST (stamps from `date`).
Authority byte-unchanged: order `ac5d3531…`, DELEGATION, COORDINATION_UPDATE.
Quiet cycle: only mzr-census deepening-QA landed; no new requests or lane STATUS changes.

## What changed and why

- **Fresh QA on canary v4 (0315)** at pass start: PASS, bit-stable.
- **The one evidence-backed correction — canary v5
  (`canaries/spin-method-canary-20260808T0325/`)**: human-readable audience citations, the
  first queued upheld finding (sealed-v8 standard + three identical lane requests).
  `display_citation` on all 9 sourced cards mirroring the sealed deck's citation strings;
  figure provenance line likewise; internal paths/hashes now receipts-only. Includes the
  **bounded candidate-workspace renderer-copy edit** DELEGATION authorizes (footer
  `display_citation` support; pre-edit sha `919af6b1…` = freeze-pinned repo renderer; delta
  recorded in the canary's `hashes.txt`; repo `tools/` untouched). Guard 11/11 twice with
  unchanged coverage; machine + encoded-frame QA PASS; full §5 receipts. **v1–v4 preserved.**
- Remaining queue after this pass: one-A-per-readout bridge; on-screen dominance definition.
- **`INTEGRATION_LEDGER.md`** pass-9 entry appended; **`STATUS.json`** refreshed.

## Preserved, not touched

Canaries v1–v4 with all receipts; all lane artifacts; pre-order `lanes/*`; repo `tools/`;
public MP4s; all prior replies.

## Hashes

See `hashes_pass9.txt` beside this receipt.

## Gates

No publication, shared/public asset writes, TTS, Git writes, or browser automation. No halt
condition hit. Window end 2026-08-10 07:00 KST.
