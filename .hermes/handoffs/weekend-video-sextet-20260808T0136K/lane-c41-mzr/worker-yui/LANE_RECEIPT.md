# Worker-Yui lane receipt

Timestamp: 2026-08-07T17:09:34Z

## Authority applied

Read and applied:

- `HWAO_WEEKEND_ORDER.md`
- `COORDINATION_UPDATE.md`

All pre-order artifacts remain untouched under the former `lanes/mzr-anchor/` directory. Every subsequent write was confined to the authoritative `lane-c41-mzr/worker-yui/` directory.

Hwao remains the sole integrator and candidate/shared-tool/TTS writer. This paper lane completed evidence, visual/storyboard proposal, QA, and request work only.

## Source and science result

`SOURCE_FREEZE.json` and `EVIDENCE_FREEZE.json` record:

- source reportability: `PASS_WITH_STRICT_ARCHIVE_CENSUS_BOUNDARY`;
- frozen unit path: 79 archive tables → 23 candidate tables → 11 fetched tables across 8 catalogs → 95 z>3 rows → 5 contract-grade anchors;
- 12 candidate tables unreachable;
- same-unit row accounting: 64 + 12 + 6 + 8 exclusions plus 5 survivors = 95 rows;
- mass-bin counts: 2, 1, 0;
- two additional anchors below the frozen log-mass-eight floor;
- shared decision minimum: N=3 anchors per bin;
- result: no deficit verdict possible in every frozen bin.

The frozen result does not authorize a calibrated high-z relation, a deficit magnitude or direction, validation/invalidation of local diagnostics, sky absence, or an FMR claim.

## Exact current-media QA

The current public MP4 is SHA-256 `02a26fa3449dd5dfc070b21988430ec51bd8d69d40adcc883a4ff2cba7831ed8`.

Probe and decoded-frame review found:

- 1920×1080 H.264 at 30 fps;
- 81.0 seconds;
- no audio stream;
- nine observed scene states;
- current source storyboard has sixteen cards and 107.5 seconds of duration floors;
- no encoded scientific figure or mass-bin decision chart;
- 95 and 5 are not joined to their units in the primary metric hierarchy;
- the rendered `8 were reachable` wording conflates the evidence’s 11 fetched tables with 8 catalogs;
- internal filenames are displayed instead of scholarly citations.

Verdict: `FAIL_AS_CURRENT_CANDIDATE__ARTIFACT_LINEAGE_AND_REPRESENTATION_BOUNDARY`.

## Proposal-only output

`STORYBOARD_PROPOSAL.json` contains nine sentence/action beats. Validation returned:

- `PASS_PROPOSAL_ONLY`;
- 9/9 beats with explicit visual actions;
- 106.0 seconds of duration floors;
- 212 narration-proposal words;
- 120.0 WPM at those floors;
- no validation errors or warnings.

The first still contact sheet is preserved at `proposals/stills-v1/` with its title/axis/glyph defects. The corrected second pass is under `proposals/stills-v2/`:

- explicit 79/23/11/95/5 unit pipeline;
- exact 95-row accounting;
- bars 2/1/0 against a shared N=3 threshold;
- separate +2 below-floor anchor pool;
- final archive-census/non-relation boundary.

Full-resolution QA returned `PASS_AS_PROPOSAL_ONLY` with no material clipping or overlap. These PNGs are visual proposals, not a candidate bundle.

## Integration handoff

`INTEGRATOR_REQUEST.md` gives Hwao the implementable changes and the closed-gate conditions. Because the direct coordination update restricted this worker to its mapped lane, no file was written to `integrator/requests/`; Hwao may consume or copy the in-lane request.

## Verification

Local verification returned:

`verification_pass scripts=3 json=9`

This covered syntax compilation of the three worker-only scripts, parsing of all nine JSON files then present, proposal validation, current-video verdict, and second-pass still QA.

Artifact hashes are recorded in `RECEIPT_HASHES.txt` and `proposals/stills-v2/hashes.txt`.

## Closed gates preserved

- Shared renderer/plotter writes: none.
- Storyboard-of-record writes: none.
- TTS or narration invocation: none.
- Candidate MP4 bundle: none.
- Public/current artifact writes: none.
- Upload or publication action: none.

Exact next action: Hwao integrates the proposal into a candidate, then delegates review if desired.
