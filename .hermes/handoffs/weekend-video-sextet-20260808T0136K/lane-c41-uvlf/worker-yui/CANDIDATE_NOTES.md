# Worker-Yui proposal notes — C41 bright-end UVLF

Artifact class: `WORKER PROPOSAL / NOT AN OFFICIAL CANDIDATE`

Coordination state: C41 is a non-canary paper lane. Any future candidate waits behind acceptance of `lane-c31-sed/worker-tori` and a subsequent Hwao decision/delegation.

## Outcome

The current public MP4 is technically decodable but fails scientific presentation: ten static cards, no audio, no axes, no data marks, no archive-flow geometry, and internal filenames used as citations. Worker-Yui therefore did not ask Hwao to reuse it.

The strongest replacement concept is `visual_proposals/v10/`, a deterministic seven-state revision packet built from frozen archive artifacts:

1. name-query reach (`20 / 112`) inside the explicitly scoped frozen two-channel VizieR manifest;
2. physical-meaning/UCD-only recovery (`+92`) with other repositories explicitly outside exhaustive scope;
3. eligibility partition (`67 + 34 + 1 + 10 = 112`) and `6,417` raw rows;
4. actual `{table, reported UV-like magnitude, z}` rows for `10 <= z < 11.5`, with dominant-table provenance (`420 / 453` rows) and an explicit open upper bound;
5. visible reported `M_UV`-like `-20` cut, `176 / 453` subset/denominator, `161 / 176` dominant-table qualifier, and no-bandpass-homogenization caveat;
6. separate census and published-LF data-requirement planes, explicitly described as missing extractable data rather than zero galaxies or zero published LFs;
7. self-describing publication-packet checklist plus the paper-specific study-status boundary.

Every plotted point in states 4–6 comes directly from the frozen 6,417-row sample. No jitter, smoothing, interpolation, or invented geometry is used.

## Recommended Hwao integration

- Treat `STORYBOARD_CANDIDATE.json` as a storyboard proposal despite the filename required by the original lane brief; its `artifact_class` explicitly says it is not the official candidate.
- Use the v10 images as visual blueprints or source-grounded evidence assets, not as a pre-authorized candidate bundle.
- Preserve progressive revelation rather than holding each state as another static card.
- Keep the row-level evidence plane on screen while introducing the threshold and the published-LF boundary; do not reset to a blank title card between those beats.
- Keep audience-facing citations (paper title/section and VizieR) visible; do not expose internal artifact paths.
- Treat `AUDIENCE_DATA_SUPPLEMENT_PROPOSAL.json` as a local publication proposal only. Before public integration, Hwao must choose an audience-reachable location and independently verify the row-level supplement supporting `453`, `176`, and the six-table geometry.
- Keep the raw-census caveat visible through all row-plot scenes.
- Do not use `lit_uvlf_alpha.png` as the evidence spine. It is a faint-end-slope literature context figure and does not support the archive-gap geometry.
- If shared-tool changes are necessary, Hwao should make them; worker-Yui made no shared renderer or storyboard-of-record edits.
- Use the prepared Alloy request only after Hwao accepts the visual/semantic candidate. Worker-Yui invoked no TTS.

## Status boundary

The science is reportable for a local revision packet because the paper-specific review/history records Duho's clearance and the scientific numbers reproduce from frozen artifacts. The public Flagship Studies component still carries generic copy saying no study has human clearance and renders `not accepted`; its metadata also says `30` disqualified while the final frozen census says `34`. These stale representations must be reconciled by Hwao before approved integration, any public release claim, or any website change. The video must not imply journal review, independent validation, or publication.

Allowed line: “Descriptive archive census; human-cleared for Lab landing; not journal-refereed, not independently validated, and not published as a journal result.”

## Preserved review history

- `v1`: rejected — funnel-title clipping, published-LF sidebar clipping, crowded threshold label.
- `v2`: rejected — published-LF heading/body overlap.
- `v3`: passed the six-state evidence core.
- `v4`: rejected closing frame — title and transformations body clipped.
- `v5`: superseded after adversarial review exposed missing row-contribution and dominant-table provenance.
- `v6`: added `27 / 40` contributing-versus-zero-row catalogue status, `420 / 453` source-table dominance, and `161 / 176` bright-row dominance; passed corrected worker visual review.
- `v7`: added the no-bandpass-homogenization boundary, changed the axis/threshold to reported UV-like catalogue fields, and clarified Hubble-scaling language; later superseded when the delayed independent reviews exposed archive-scope, study-status, citation, and paper-naive wording gaps.
- `v8`: added frozen-VizieR scope, audience-supplement requirement, exact dominant-table identifier, open upper-bound marker, plain published-LF status, and study status; rejected for title/sidebar/card overflow.
- `v9`: fixed title and sidebar overflow; rejected because the skipped-candidate heading still overflowed its card.
- `v10`: contained the skipped-card detail and passed the final full-resolution worker visual delta review.

No failed visual-review version was deleted or overwritten after its disposition.

## Gates

No official candidate, narration audio, mux, shared-tool edit, public replacement, website wiring, Git write, DB/SQL write, deploy/restart, browser automation, provider/config change, or secret access occurred in this lane.
