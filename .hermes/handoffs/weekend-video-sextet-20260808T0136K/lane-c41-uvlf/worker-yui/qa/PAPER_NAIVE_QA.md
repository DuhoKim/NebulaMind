# Paper-naive QA — C41 worker-Yui proposal

Verdict: `INDEPENDENT_V5_PASS_WITH_REQUIRED_NOTES / V10_LOCAL_DELTA_PASS / APPROVED_INTEGRATION_BLOCKED`

## Independence and scope

A read-only delegated reviewer (`deleg_1abe3d79`, task 0) inspected the immutable v5 storyboard and seven-state visual sequence without modifying artifacts. The reviewer passed proposal-level handoff with required clarification notes. Worker-Yui reconciled every note into v10 and replayed full-resolution/contact-sheet delta checks. The independent reviewer did not inspect v10, so v10 remains a revision packet for Hwao review rather than an independently cleared integration candidate.

## Can a reader follow the story without the paper?

Yes, in this order:

1. a name-only query reaches 20 of 112 candidate tables in the frozen two-channel VizieR manifest;
2. physical-meaning metadata, defined as UCD tags, reveals the other 92 while other repositories remain outside exhaustive scope;
3. eligibility separates candidate tables, counted catalogues, and raw rows;
4. the target redshift slice becomes a real axis-based evidence plane;
5. the reported magnitude cut reveals 176 of 453 rows;
6. the census and published-LF data requirements are explicitly separated as missing extractable data, not zero galaxies or zero published LFs;
7. a publication-packet checklist and explicit study-status boundary close the procedural story.

No paper-cover hold is required. The count transitions are visible, and the conclusion is earned by the archive flow plus row-level geometry rather than by title cards alone.

## Naive-reader risks found and disposition

- `UCD` was archive jargon. v10 defines it as physical-meaning metadata before relying on the acronym. `PASS_LOCAL_DELTA`.
- The opening was audio-dependent and did not visibly identify bright-end UV luminosity functions. v10 names the topic in the first two visual kickers. `PASS_LOCAL_DELTA`.
- `The public archive` overread the searched universe. v10 scopes both pixels and narration to the frozen two-channel VizieR manifest and says other repositories were not exhaustively searched. `PASS_LOCAL_DELTA`.
- `67 counted catalogues` could look like 67 contributing sources. v10 states that 27 contribute rows and 40 contribute zero in the frozen slices. `PASS_LOCAL_DELTA`.
- `453` and `176` could look like galaxy counts. v10 repeatedly calls them raw rows and keeps the `176 / 453` denominator. `PASS_LOCAL_DELTA`.
- `M_UV` could look like one homogeneous 1500-angstrom quantity. v10 says catalogued UV-like magnitude and displays `no bandpass homogenization`. `PASS_LOCAL_DELTA`.
- State 6 used internal jargon and could be mistaken for zero galaxies or absence of published LFs. v10 says that no predefined-roster LF table met the machine-readable data requirements for the slice and interprets this as missing extractable data. `PASS_LOCAL_DELTA`.
- `phi` and `h` were unexplained. v10 defines phi as the luminosity-function value and labels Hubble-constant scaling. `PASS_LOCAL_DELTA`.
- Storyboard/render section citations mismatched. v10 aligns the cited sections. `PASS_LOCAL_DELTA`.
- The original 68-second plan was dense. v10 proposes 269 words over 132 seconds: 122.3 words per minute overall, with every scene between 120.0 and 123.5 words per minute. `PASS_LOCAL_DELTA`.
- The `453` denominator remains unavailable in the public paper. v10 visibly requires an audience-reachable supplement, and `AUDIENCE_DATA_SUPPLEMENT_PROPOSAL.json` provides a local six-table proposal. `BLOCK_APPROVED_INTEGRATION_UNTIL_HWAO_VERIFIES_AND_PUBLISHES_SUPPLEMENT`.
- The state-3 frame is information-dense as a still. The storyboard requires progressive revelation over 22 seconds; Hwao should preserve that reveal rather than hold the complete frame from the first beat. `HANDOFF NOTE`, not a blocker.

## Presentation boundary

The review authorizes transmission of the revision packet to Hwao only. It does not authorize:

- an official C41 candidate,
- approved integration,
- TTS,
- public video replacement,
- website copy changes,
- Git actions,
- or publication claims.

C41 remains behind the C31 canary gate and Hwao remains the sole integrator/candidate/shared-tool/TTS writer.
