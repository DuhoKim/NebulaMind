# Deepening pass 4 — fresh encoded-frame scientific-presentation audit

Audit timestamp: 2026-08-08T03:22:11+09:00

Exact read-only candidate: `/Users/duhokim/HermesOps/cockpit/videos/mzr-archive-census-narrated-20260808T0155.mp4`

Candidate SHA-256: `0bdfd12dcc87098e1034196bc973df4ace79044cadc4261f3f827b65d7cd162d`

Fresh verification: 13,989,937 bytes; 128.4 s; H.264 1920×1080 at 30 fps plus AAC mono 24 kHz. The candidate hash and mtime are unchanged. Thirty-two fresh encoded frames were sampled at 2 + 4n seconds, offset from the pass-3 cadence. Contact-sheet SHA-256: `f2f1ab19f1b38f6a5a8c813077d57a3e61e4f22290289f2a50b415c0f39d8c4c`. `FRAME_HASHES.json` pins all 32 frames.

All findings below are from encoded pixels. No audio, renderer intent, or unpublished state is inferred.

## Reconfirmed encoded blockers

- 006–010 s: the opening says catalogues are `carrying gas-phase metallicity, stellar mass and redshift together`, rather than identifying single-table search-axis metadata reach.
- 014–022 s: the literature metallicity scatter still says the census exists to measure the spread, although no metallicity/MZR measurement was run.
- 042–050 s: `157` dominates while 178−21 conservation and the 19/2 split remain prose or absent; an internal filename is rendered as a citation.
- 054–066 s: proportional 157/62 geometry and a giant `62` still imply filtration; all 157 do not visibly proceed directly to T2. Internal run filenames remain visible.
- 074–082 s: 7/7 and 0/3 are prose, not explicit evidence graphics, and the precision-not-certified boundary is absent.
- 102–118 s: the contamination card still collapses symbol/meaning collisions and target-domain mismatches into `the symbol Z, not the concept` and does not label the examples as recorded characterizations rather than T2 rulings.
- 122–126 s: the close still replaces scientific status with generic artifact provenance.

## New pass-4 representation findings

### 1. Target concept is conflated with pre-eligibility retrieval-axis reach

At 006 s the opening says:

> A pre-registered census of catalogues carrying gas-phase metallicity, stellar mass and redshift together.

The frozen T1 result is an abundance/mass/redshift **search-axis metadata intersection**. T1 explicitly makes no eligibility ruling, and its own findings show multiple redshift-axis meanings. The gas-phase term audit is separate. Therefore `gas-phase metallicity` or even unqualified `gas-phase abundance` cannot be used as the already-adjudicated identity of every abundance-axis match.

The safe unit is `single tables at the abundance-search, stellar-mass, and redshift-search metadata intersection`. Gas-phase abundance is the target concept, not a T1 eligibility result.

### 2. The 62 card upgrades vocabulary presence into evidence

At 062–066 s the frame says:

> 62 of those candidates carry explicit gas-phase evidence in their own recorded descriptions.

The frozen side audit says only that a regex matched terms such as `12+log`, `oxygen abundance`, `gas-phase`, `nebular`, or `emission-line` in concatenated recorded descriptions. Its explicit boundary is `vocabulary presence, not an E1–E4 verdict`. Calling this `explicit gas-phase evidence` changes the evidentiary class and encourages the audience to treat 62 as adjudicated physical content. The denominator is 62 of 157 descriptions, but the frame also omits the direct 157→T2 path.

Safe visible wording is `62 OF 157 RECORDED DESCRIPTIONS MATCHED THE FROZEN VOCABULARY REGEX · SIDE CHECK ONLY`.

### 3. Consecutive cards conflate two different control systems

At 074–082 s the generic `instrument` card says seven recall members returned and three controls stayed out. These are the **T1 retrieval-instrument** checks: recall 7/7 and controls appearing 0/3. The frame does not identify T1 and does not display `PRECISION NOT CERTIFIED`.

At 086–098 s the next generic `rules` card says the eligibility contract was frozen after seven gate rounds with twelve decoys. These are **T2 contract-design** decoys, not the preceding T1 controls. The frozen record also distinguishes 12 decoys plus 3 anchors. The encoded sequence provides no persistent T1/T2 phase label, and the T2 frame does not show application-not-completed or no-eligible-table-count status.

A viewer can therefore merge `three controls` and `twelve decoys` into one evaluation result, even though one is a completed T1 retrieval check and the other is provenance for a frozen-but-unapplied T2 rule contract.

Safe correction: label every beat with its stage. Use `T1 ENUMERATION`, `T1 RETRIEVAL-INSTRUMENT CHECK`, `T1 RECORDED CHARACTERIZATION`, and `T2 CONTRACT STATUS · APPLICATION NOT COMPLETED`. If T2 decoy counts are shown, explicitly label them `T2 CONTRACT-DESIGN DECOYS`; never merge them with T1's three controls.

### 4. Citation representation remains non-audience and under-scoped

The frames expose `source: T1_FINDINGS.md`, `source: T1E_GASPHASE_COUNT.json`, and `source: FREEZE_RECORD_T2.md`. These internal filenames do not support an audience-facing denominator or geometry by themselves and violate the scholarly-display-citation rule. The existing pass-3 audience-copy firewall remains necessary.

## Pass-4 verdict

`FAIL_FOR_SCIENTIFIC_REPRESENTATION_TARGET_VS_AXIS_EVIDENCE_CLASS_CONTROL_STAGE_AND_PRIOR_BOUNDARIES`

The candidate remains preserved and read-only. This verdict does not authorize replacement, rendering, TTS, upload, publication, or any shared/public change.

## Safe next correction

Keep the accepted v7 static overview pixels unchanged. Version the worker storyboard proposal to:

1. distinguish the gas-phase target concept from abundance-search-axis metadata reach in narration and visible copy;
2. label 62 as frozen vocabulary presence in recorded descriptions, never `explicit gas-phase evidence`;
3. add persistent T1/T2 stage labels so T1's 7/7 and 0/3 retrieval checks cannot be conflated with T2's contract-design decoys;
4. retain direct 157→T2 topology, the 62 side branch, precision-not-certified status, the two contamination mechanisms, and reportable-now versus pending closure;
5. prohibit internal paths, filenames, build directions, and verification labels from audience display.

This is an evidence-class and workflow-provenance correction. It adds no scientific result and does not clear the encoded candidate.
