# Candidate notes — proposal only

Current proposal identifiers: packet `pass7-exhaustive-receipt-and-neutral-scaffolding-v27`; storyboard `pass7-distinct-state-custody-v23`; accepted static visual `visual_proposal_v9.png` with SHA-256 `c07eb10a3652e088f8dfca9085b1e1cc35d6af4f84ccf58d4ab767efca78eebe`. These identify worker proposal records only and do not clear the failed encoded candidate.

## Implemented in the worker proposal

- The story is recast as an archive-informatics metadata census, not a metallicity measurement.
- The quantitative spine is per-axis retrieval → 178 three-axis reach → 21 modifier-filter drops → 157 recorded candidates → qualified T2 gate. A separate side inset reports frozen term-regex matches in 62 of 157 descriptions; it does not filter the T2 input.
- The single-table scope is persistent: cross-table joins and crossmatches were not assessed.
- The 21 dropped rows are split visibly into 19 redshift-search-axis and 2 abundance-search-axis losses, with modifier-column examples from the frozen manifest.
- The instrument check is expressed as `7/7` and `0/3`, followed immediately by a precision limitation: the frozen controls did not cover the recorded `src.redshift`-qualifier contamination documented in the manifest.
- Recorded precision-failure examples are split into symbol/meaning collisions (Galactic height; model-grid metal fraction) and target-domain mismatches (stellar gravitational redshift; gravitational-redshift velocity). They are characterization examples, not T2 rulings.
- Internal verification paths and audience display citations are separated.
- Every beat now carries explicit `on_screen_copy`; `visual_action`, verification sources, and rejection notes are non-audience fields.
- The opening says `single-table metadata census` and `metadata-reachable columns`, not that catalogues carry adjudicated measurements. The close explicitly separates reportable-now facts from the pending T2 application, eligible-table count, and any MZR measurement.
- The opening now calls all three pre-eligibility axes `abundance-search`, `mass-search`, and `redshift-search`; none is an adjudicated physical measurement at T1.
- The 62 inset now says `FROZEN VOCABULARY REGEX PRESENCE · NOT ADJUDICATED GAS-PHASE EVIDENCE`.
- Every beat carries a workflow-stage label. T1's three retrieval controls remain distinct from the complete T2 contract-design set of 12 decoys plus 3 anchors; those fifteen are not eligibility results.
- Pass-4 proposal v1 is preserved as an exact adversarial FAIL because the taxonomy headings were field names/build directions rather than visible values. Pass-4 v2 passed exact review. Pass-5 v1 preserves that correction and adds all-axis search scope plus balanced T2 design provenance.
- Static visual v9 is the current proposal and supersedes but preserves v8/v7. It retains the search-axis heading correction and additionally search-qualifies every visible 19/2 modifier-drop label; deterministic packet-local rerender and full-resolution QA pass.
- Internally derived count graphics require an audience-reachable methods/count ledger before any public release; internal custody labels are not a public supplement.
- Pass-6 adds 45 timed reveal states across 10 beats. The maximum planned gap and contract ceiling between substantive evidence changes are both 3.0 seconds.
- Standalone section-divider holds are prohibited; section identity must coexist with the evidence surface. The close reveals reportable-now, then pending, then the combined qualified summary.
- The literature metallicity scatter is rejected for this topic because it makes an infrastructure census look like a measurement.

## Rejected alternatives

1. Keep the existing 157-versus-62 bar and strengthen the title. Rejected: it still omits 178→157 accounting and can make 62 look eligible.
2. Retain the literature scatter as motivation. Rejected: the encoded card says the census exists to measure the spread, contradicting the frozen scientific status.
3. Put the 19/2 split in narration only. Rejected: the lane brief requires dropped-row reasons to be visual.
4. Treat 7/7 and 0/3 as a precision certificate. Rejected: the frozen controls did not cover the recorded `src.redshift`-qualifier contamination documented in T1 findings.
5. Render a worker MP4. Rejected by the authoritative coordination update: Hwao is the sole candidate/TTS/shared-tool writer.
6. Close on generic provenance such as “every number came from an artifact.” Rejected: provenance does not state which scientific claims are reportable or which gate remains pending.
7. Call abundance-axis reach `gas-phase abundance` or the 62 regex matches `explicit gas-phase evidence`. Rejected: both upgrade pre-eligibility metadata or vocabulary reach into adjudicated target-domain content.
8. Present T1's three controls and T2's contract-design decoys without stage labels. Rejected: they are different instruments and cannot be merged into one evaluation result.
9. Show only T2's twelve decoys. Rejected: the frozen design also includes three must-retain anchors; all fifteen are contract-design controls, not eligibility results.
10. Publish count cards with internal filenames or only a named-run label. Rejected: public denominators require an audience-reachable methods/count ledger.
11. Treat a long narrated card as progressive merely because its background moves. Rejected: scientific-presentation causality requires a new metric, label, topology, comparison, or status state, not decorative motion.
12. Use standalone `DATA`, `ANALYSIS`, or `SUMMARY` divider holds. Rejected: the exact candidate spends four 2.5-second states on dividers while evidence remains absent.
13. Replace the entire frame at every scientific transition. Rejected: pass-7 boundary pixels show 11/14 cuts with zero declared scientific anchors preserved; cut 7 isolates 62 from its 157 parent and cut 10 erases T1/T2 stage identity.
14. Count `hold`, `retain`, divider text, or any nonempty state label as a scientific change. Rejected: pass-6 exact review exposed a seven-second b07→b08 unchanged state hidden by this false-pass rule.
15. Claim clause-to-visual causality without declared clauses and state mappings. Rejected: every substantive clause now has an ID and at least one mapped reveal/transform state; future audio alignment remains a separate exact-candidate gate.
16. Clear integration with presence-only count/citation/order checks. Rejected: the validator must bind exact rendered values to frozen sources and reject the preserved mutation suite.
17. Validate selected fields while leaving other narration, screen examples, or build directions mutable. Rejected: v2 passed four demonstrated false mutations; v3 pins canonical full-story, audience, and build semantics and expands exact source/render bindings.
18. Call a review snapshot self-contained while validator replay still requires an absolute external source directory or while frame custody exposes only contact sheets. Rejected: v4 carries a verified eight-file source freeze and all 43 audited frame PNGs.
19. Treat search-axis qualification as satisfied when only opening/axis cards are qualified, count duplicate styling or empty taxonomy shells as scientific changes, call a packet projection-only despite review scaffolding, or stop mutation testing before exact adversarial attacks. Rejected: v5 search-qualifies b05, makes every b08 state scientifically distinct, declares scaffolding, and rejects 25/25 counterfactuals.
20. Re-reveal a path already made persistent across a beat boundary, let a visual action contradict ordered states, or treat regenerable co-located canonical hashes as an independent approval anchor. Rejected: v6 stages the 157-to-T2 extension only once, binds b01 action to state order, and authenticates immutable contract/storyboard/validator bytes through the externally pinned snapshot manifest.

## Integrator implementation notes

- Prefer one persistent funnel that progressively reveals stages instead of separate giant-number cards.
- Use equal-width boxes and label `counts, not area encoded`; do not invent proportional geometry.
- Keep all generated annotations outside any source graphic pixels.
- Connect 157 directly to T2. Use the exact side-inset phrases `62 OF 157 RECORDED DESCRIPTIONS MATCHED` and `SIDE CHECK ONLY · NOT AN ELIGIBILITY FILTER · T2 STILL APPLIES TO ALL 157`.
- Use the qualified T2 phrases `CONTRACT FROZEN`, `APPLICATION NOT COMPLETED`, and `NO ELIGIBLE-TABLE COUNT`.
- Do not describe all four `src.redshift` examples as `symbol Z, not the concept`; distinguish symbol/meaning collisions from genuine gravitational-redshift quantities that are target-domain mismatches for galaxy-MZR work.
- Display citation footer: `VizieR: Ochsenbein, Bauer & Marcout 2000 · IVOA UCD1+ v1.5 · NebulaMind pre-registered VizieR metadata census, snapshot 2026-08-05`.
- Preserve internal artifact paths only in the build receipt and numeric-source evidence audit.
- Render only `on_screen_copy` and `display_citation` as audience copy. Never render `visual_action`, `timed_reveal_states`, `state_handoff`, `verification_sources`, or `visual_rejections` verbatim.
- Persist `T1 ENUMERATION`, `T1 RETRIEVAL-INSTRUMENT CHECK`, `T1 RECORDED CHARACTERIZATION`, and `T2 CONTRACT STATUS · APPLICATION NOT COMPLETED` labels across the corresponding beats.
- In b09, render the explicit `SYMBOL / MEANING COLLISION` and `TARGET-DOMAIN MISMATCH` label values above their examples; do not infer audience copy from JSON key names or `visual_action`.
- Use `ABUNDANCE SEARCH`, `MASS SEARCH`, and `REDSHIFT SEARCH` on every pre-eligibility axis card.
- If T2 design controls are shown, display `12 DECOYS + 3 ANCHORS · NOT ELIGIBILITY RESULTS`; never show the decoys alone.
- Keep publication closed until an audience-reachable methods/count ledger exists for internally derived denominators.
- Implement every `timed_reveal_states` entry; never render its build text verbatim.
- Keep each substantive evidence-state gap at or below three seconds and re-audit hard cuts plus sentence/action alignment on the exact muxed candidate.
- Implement all 10 `state_handoff` objects and require all nine exit→entry state IDs to match. Do not use a full-frame scientific reset.
- After b05 constructs the funnel, keep the `178→−21→157→T2` main spine visible through b10. Whenever 62 is visible, retain its 157 parent, side-check boundary, and all-157-to-T2 path.
- Re-audit 0.25 seconds before and after every future hard cut for scientific-anchor continuity; timing compliance alone is not a continuity pass.
- Treat the 45 timed states as structured semantic deltas, not prose labels. Only `reveal` or `transform` with a nonempty evidence delta counts; enforce the gap globally across beat boundaries.
- Run the validator mutation suite and all three helper `--check` modes before integration. Check mode must not write.
- Build the closed-book packet only from exact narration, on-screen leaf strings, and display citations. Do not add build-derived explanatory sentences or package the answer key beside the review packet.
- Require `qa/APPROVED_STORYBOARD_CONTRACT.json` to match the canonical storyboard, audience-semantics, and build-semantics hashes. Any storyboard mutation requires a new preserved version and exact regate; never silently regenerate the contract to bless an unreviewed edit.
- Replay validation from `frozen_sources/pass7/`; verify its manifest before reading. Treat source-manifest changes as a new exact version. Review all 43 frame files against `FRAME_HASHES.json`, not only the contact sheets.
- Enforce the three-second global semantic ceiling on distinct evidence changes. In the packet receipt, separate the exact audience payload from title/directions/headings/questions review scaffolding; never call the whole packet projection-only.
- Treat the externally supplied immutable snapshot-manifest digest as the trust anchor. During snapshot replay, require validator PASS only when the approval contract, storyboard, and validator bytes match their manifest entries; a locally regenerated approval file never authorizes itself.

No shared tool, storyboard of record, TTS, candidate bundle, public video, website, cockpit, Git, or publication state was changed.
