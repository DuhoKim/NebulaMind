# Integrator request — MZR archive census

Requester: `lane-mzr-census/worker-yui`

Authority: proposal/request only. Hwao remains the sole integrator and sole candidate/shared-tool/TTS writer.

## Requested disposition

Reject the latest local narrated MZR candidate for scientific-representation, target-versus-search-axis, evidence-class, workflow-stage, graphics-grammar, static-evidence-state causality, cross-cut state-continuity, taxonomy, and closure-boundary defects. Do not publish or substitute it. Build the next canary only after Hwao accepts the bounded proposal below.

## Exact requested changes

1. Remove `lit_metallicity.png` and the card saying the census exists to measure a metallicity spread. T1 is an archive metadata census and reports no metallicity or MZR measurement.
2. Replace the prose/giant-number sequence with the progressive metadata graphic in `visual_proposal_v8.png` and the 10-beat pass-7 v4 design in `STORYBOARD_CANDIDATE.json`. Render only each beat's `on_screen_copy` plus `display_citation`; never render `visual_action`, `timed_reveal_states`, `state_handoff`, `narration_clauses`, `verification_sources`, or `visual_rejections` verbatim. In b09, render the explicit `SYMBOL / MEANING COLLISION` and `TARGET-DOMAIN MISMATCH` label values above their example lists; JSON field names are not audience copy.
3. Keep the complete direct count spine on screen: per-axis retrieval channels → `178` single-table three-axis reach → `−21` modifier-filter losses (`19 redshift`, `2 abundance`) → `157` recorded candidates → T2 `CONTRACT FROZEN · APPLICATION NOT COMPLETED · NO ELIGIBLE-TABLE COUNT`.
4. Connect 157 directly to T2. Render `62 of 157` only as a side inset: `FROZEN VOCABULARY REGEX PRESENCE IN RECORDED DESCRIPTIONS · NOT ADJUDICATED GAS-PHASE EVIDENCE · SIDE CHECK ONLY · T2 STILL APPLIES TO ALL 157`. Do not use “explicit gas-phase evidence” as the standalone label.
5. Keep `SINGLE-TABLE METADATA INTERSECTION · CROSS-TABLE JOINS AND CROSSMATCHES NOT ASSESSED` persistent.
6. Render `RECALL 7/7` and `CONTROLS APPEARING 0/3` as visible scorecards under `T1 RETRIEVAL-INSTRUMENT CHECK · 3 CONTROLS`. In the same beat, state `RETRIEVAL CHECK PASSED · PRECISION NOT CERTIFIED`. Never merge these T1 controls with T2 contract-design decoys. Group recorded examples under `T1 RECORDED CHARACTERIZATION` as `SYMBOL / MEANING COLLISION` (Galactic height; model-grid metal fraction) and `TARGET-DOMAIN MISMATCH` (stellar gravitational redshift; gravitational-redshift velocity), with `NOT T2 RULINGS` explicit.
7. Replace audience-facing internal paths with the display citations in `DISPLAY_CITATIONS.md`. Preserve internal paths only in integration receipts. Before any public release, provide an audience-reachable methods and count ledger for internally derived denominators; an internal named-run label is not a public supplement.
8. Open with `A SINGLE-TABLE METADATA CENSUS` and `abundance-search, mass-search, and redshift-search axes`; none of those T1 axes is an adjudicated physical measurement. Do not say catalogues/tables `carry` measurements. Close with the exact `REPORTABLE NOW VERSUS PENDING` copy, including `APPLICATION NOT COMPLETED · NO ELIGIBLE-TABLE COUNT` and `SINGLE-TABLE METADATA CENSUS · NOT AN MZR MEASUREMENT`.
9. Keep persistent workflow provenance: `T1 ENUMERATION`, `T1 RETRIEVAL-INSTRUMENT CHECK`, `T1 RECORDED CHARACTERIZATION`, and `T2 CONTRACT STATUS · APPLICATION NOT COMPLETED`.
10. If T2 contract-design counts are shown, display the complete `12 DECOYS + 3 ANCHORS` under `T2 CONTRACT DESIGN` and `NOT ELIGIBILITY RESULTS`; never show only the decoys or imply the 157-table application ran.
11. Implement every structured `timed_reveal_states` object. Only `operation=reveal|transform` plus a nonempty `evidence_delta` counts as a semantic change; `hold`, `retain`, divider text, or a merely nonempty label does not. Enforce the four-second ceiling on one global timeline across beat boundaries. Map every declared `narration_clauses` ID to at least one state. In b10 reveal `REPORTABLE NOW`, then `PENDING`, then the no-count gate, then transform into the combined qualified summary.
12. Implement every beat's `state_handoff`: each exit state must equal the next entry state, and no full-frame scientific reset may erase the active evidence. Keep the `178 → −21 → 157 → T2` spine visible after construction through the qualified close. Whenever 62 is visible, keep its 157 parent, side-check boundary, and all-157-to-T2 path visible. Persist T1/T2 stage identity across every handoff.
13. If Hwao approves the visual/storyboard proposal, use `ALLOY_NARRATION_MANIFEST_PROPOSAL.json` as a synthesis request: Nous-managed Alloy, speed `1.18`. The worker did not invoke TTS and generated no audio.
14. Before integration, run `validate_proposal.py --check`, `prepare_narration_manifest.py --check`, `prepare_paper_naive_packet.py --check`, and `qa/test_validate_proposal_mutations.py`; check mode must not write. The mutation suite must reject all thirteen counterfactuals, including a tampered local source file. `qa/APPROVED_STORYBOARD_CONTRACT.json` pins exact canonical audience/build semantics and the self-contained source manifest.
15. Keep card durations as visual floors. After synthesis/mux, run exact encoded-frame, cut-boundary continuity, global semantic evidence-state timing, clause/audio alignment, waveform, stream, readability, exact-projection paper-naive comprehension, adversarial-boundary, and human-listening gates before any publication decision.

## Worker artifacts

- `SOURCE_FREEZE.json` — qualified `video_reportable_now` and hard scientific/status gates.
- `NUMERIC_SOURCE_AUDIT.json` — claim-to-source and source-role mapping.
- `FRAME_DIAGNOSIS.md` — exact current public target and latest local candidate audit.
- `STORYBOARD_CANDIDATE.json` — pass-7 self-contained-contract-v4; 10 beats, 207 narration words, 105 visual-floor seconds, planned 118.3 WPM; 45 structured reveal/transform states, 21 declared clauses, 3.0-second maximum global semantic gap, a 10-beat state-handoff chain, persistent 178→−21→157→T2 and 62-parent topology, and inherited workflow/scope/status layers at the close.
- `visual_proposal_v8.png` — current worker static proposal, SHA-256 `5c36f6e1d4b63d215a0bb68545457376d961b168bff1f18686ad45e1d4d19df9`; v7 is preserved as the superseded pass-2/pass-4 artifact.
- `qa/visual_proposal_qa.json` — preserved v1–v7 lineage followed by visual v8 local PASS; pass-7 v4 adds self-contained source replay and keeps audience/build semantics and v8 pixels unchanged; exact pass-7 v4 independent regate is required before integration.
- `qa/deepening-pass2-encoded/ENCODED_FRAME_AUDIT.md` — fresh 20-frame exact-candidate audit and the evidence-backed contamination-taxonomy correction.
- `qa/deepening-pass3-encoded/ENCODED_FRAME_AUDIT.md` — fresh 32-frame exact-candidate audit and the evidence-backed opening/unit, T2-status, and final-closure blocker deepening.
- `qa/deepening-pass4-encoded/ENCODED_FRAME_AUDIT.md` — fresh offset 32-frame audit and the evidence-backed target-versus-search-axis, vocabulary-evidence-class, and T1/T2 control-stage correction.
- `qa/deepening-pass5-encoded/ENCODED_FRAME_AUDIT.md` — fresh 1+4n 32-frame audit and the evidence-backed all-axis search-scope, complete T2 control-design provenance, and public-denominator-ledger blocker deepening.
- `qa/deepening-pass6-encoded/ENCODED_FRAME_AUDIT.md` and `SCENE_HOLDS.json` — fresh 3+4n 32-frame audit plus hard-cut timing; 10 evidence-state holds exceed six seconds and the maximum is 16.133 seconds.
- `qa/deepening-pass7-encoded/ENCODED_FRAME_AUDIT.md`, `FRAME_HASHES.json`, `BOUNDARY_CONTINUITY.json`, and `OCR_DENSITY.json` — 43 fresh boundary/midpoint frames; 11/14 cuts preserve zero declared scientific anchors, while the 157/62 and T1/T2 handoffs erase necessary parent/stage context.
- `qa/proposal_validation.json` — exact-source and structural PASS.
- `qa/PASS7_VALIDATOR_MUTATION_TESTS.json` — baseline PASS plus thirteen expected-fail counterfactuals; check mode leaves the stored receipt unchanged.
- `qa/APPROVED_STORYBOARD_CONTRACT.json` — canonical exact-story, audience-semantics, build-semantics, and source-manifest hashes for v4; any unapproved storyboard or source mutation fails validation.
- `frozen_sources/pass7/` — eight exact source copies plus a hash manifest used by validator replay; no absolute prior-pack dependency remains.
- `qa/PAPER_NAIVE_PACKET_PROJECTION.json` and `prepare_paper_naive_packet.py` — deterministic packet projection containing 10/10 narration strings, all 58 on-screen leaf strings, and 10/10 display citations; no build-derived sentence or adjacent answer key is required.
- `ALLOY_NARRATION_MANIFEST_PROPOSAL.json` — manifest only; `tts_invoked=false`, no audio artifacts.
- `DISPLAY_CITATIONS.md` and `citation_ledger.json` — grounded audience citation basis.

## Closed gates

No shared renderer, source storyboard, candidate bundle, TTS, public video, website, cockpit, DB, Git, upload, or publication state change is authorized by this request.
