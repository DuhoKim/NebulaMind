# T2 eligibility contract — freeze record

Duho, 2026-08-06: "freeze the T2 contract".

| | |
|---|---|
| artifact | `T2_ELIGIBILITY_CONTRACT_V1_DRAFT.md` (v1.8) |
| sha256 | `f40e37240c1728dd1a4aedcb7ee47b39230dad8191e8ef73f54ad2500218f383` |
| perms | `-r--r--r--` |
| gate | `KUN_T2_REGATE7.md` → **T2 EVIDENCE RE-GATE 7: PASS** |
| rounds | 7 gate rounds, 12 decoys + 3 anchors, 49 quoted spans verified against the pinned manifest |

## What was frozen, and what deliberately was not

Only the header changed, recording the freeze itself. **No other byte was touched.** Kun's PASS
was issued on these bytes; editing a passed contract before freezing it is precisely the drift
the lane exists to prevent, and the spin lane already burned a gate on a header that said DRAFT
while the file sat at 444.

## Lana's ruling — the amendment agenda, NOT applied

`LANA_MZR_SCIENCE_RULING.md` cleared the freeze ("nothing here blocks it… the contract text is
the strongest artifact this lane has produced") while raising items she scoped to *before T2
runs*. They go through §2b as amendments, each with its own gate:

1. **Rename the destination.** The census is **infrastructure, not a flagship candidate** — a
   holdings map, not a study. Her line: z9–10 was retired for assembling published *values*; a
   census assembles *table listings*, one rung below. The lane should stop describing its
   endpoint as a paper.
2. **Pre-register the go/no-go before T2 emits its table** — on N_s (eligible tables per
   scale-key after de-duplicating common-source derivatives) and Δz_s. Declaring the stopping
   rule after seeing the numbers is how a null becomes a rescue attempt.
3. **Move §5.3 generality adjudication to Miru**, since Kun's involvement in the ruling logic
   makes him the wrong seat to grade its generality.
4. Add the survey-coherent-clause check to the adjudicator's list.
5. Add per-gate redshift character to the funnel output.

## What T2 must do at run time (from the frozen §5.5/§5.6 and Kun's conditions)

- **Per-clause fire-counts** over all 157 for every clause of the ruling logic; a clause firing
  only on control members is presumptive special-casing.
- **The complete 157-row disposition table** with quoted evidence, published *before* the gate is
  called — without it T2 cannot PASS whatever it does with the fifteen controls.
- The adjudicator re-derives independently: the 49-span diff, the manifest pin, the clipped list,
  the 62-count via `t1e_gasphase_count.py`, the fire-counts, and the full table.
- **Gate FAIL**: any of D1–D12 ELIGIBLE, any of P1–P3 excluded, any clause naming a table_id,
  missing fire-counts, or a missing/incomplete disposition table.

## The separable result

T1 surfaced one finding that exists nowhere else and does not depend on T2: `src.redshift` is
tagged on the **symbol Z, not the concept**, contaminating the three-axis intersection with at
least four semantically distinct quantities — Galactic height, gravitational redshift, model
composition, snapshot index — with 28 of 157 candidates carrying a fully disqualified redshift
axis. Lana's read: publishable as an **archive-informatics note** for the VO/IVOA community, on
that shelf, not as galaxy-evolution science.
