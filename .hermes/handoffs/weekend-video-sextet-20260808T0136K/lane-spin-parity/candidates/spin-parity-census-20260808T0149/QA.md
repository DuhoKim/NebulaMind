# QA — spin-parity alloy candidate, 2026-08-08T0149

## Verdict: **HELD — NOT AUTHORIZED FOR RELEASE OR ACCEPTANCE**

Superseded my earlier machine-only PASS (kept as `QA_SUPERSEDED_machine-only.md`). That verdict
said "cleared for Duho's watch/listen". **That was wrong**, and the error is worth naming: I read a
numeric-source-guard PASS as though it were semantic authorization. It is not. The guard proves a
number appears in the cited file; it cannot judge whether the lane is allowed to state the result
at all.

## What overrode it

`lanes/spin/SOURCE_FREEZE.json` (freeze `spin-method-canary-pass1-20260808T0153K`) and
`lanes/spin/STATUS.json` record `video_reportable_now: false`, decision
**`BLOCK_SUBSTANTIVE_RESULT_RENDER; ALLOW_METHOD_ONLY_CANARY`**, on four blockers:

1. **STATUS_RESULT_MISMATCH** — the deck renders the post-A4 MIXED result and its figures, but
   `AMENDMENT_A4_DRAFT.md` §5 requires an A3.8 verdict-record review *after* the run, and no
   post-run independent verdict record for `T4_PAIRED_FLIP.json` was found.
   `LANA_ZERO_CONCORDANCE.md` is science pressure, not a gate.
2. **FRAME_UNSTATED** — `KUN_FRAME_REVIEW.md` independently agrees. The stored mirrored-direction
   convention is unresolved, so no result may be represented as a sky, dipole, parity or
   cosmological finding.
3. **WORKFLOW_STATUS_NOT_RELEASE_READY** — evidence freeze, receipts, referee and video stages are
   all still pending in `WORKFLOW_CHECKLIST.json`.
4. **LATER_FREEZE_EXCLUDES_NEW_POINTERS** — the Ganalyzer/DESI/dipole/parity/GRB/SN Ia/DE/quasar/H0
   pointers are recorded questions and may not enter a weekend candidate.

## Why this specific candidate is blocked

`forbidden_scope` names **"T3 or T4 headline/result figures"**. This deck carries five of them —
cards 07 (verdict), 09 (significance), 10 (paired flip), 11 (decomposition) — plus result prose in
cards 12–15. It is squarely inside the forbidden scope, not adjacent to it.

Separately, the lane's `audio_contract` reads **"silent; narration/TTS not authorized"**. So the
alloy recut itself was outside this lane's authorized scope, regardless of voice quality.

## What is preserved, and what it is good for

The MP4 is **machine-valid and retained**, per the gate's instruction to preserve rather than
delete: `spin-parity-census-narrated-20260808T0149.mp4`, 243.3 s, 16/16 cards guard-verified,
single-voice alloy at the calibrated 1.18. Its narration work and pacing calibration remain
reusable. **It is held, not accepted, and not a release candidate.**

## What would actually unblock this lane

A post-run independent A3.8 verdict record for `T4_PAIRED_FLIP.json`, and resolution of
FRAME_UNSTATED — or a **method-only** deck that stays inside `allowed_scope`: frozen source/sample
funnel, the predeclared asymmetry equation, the handedness convention schematic, the predeclared
bias-control design, and a clearly labelled unresolved-result boundary. The second is achievable
this weekend; the first is not a video task.

## Standing note

Narration-only recuts do not answer Duho's original complaint, which was about **scientific
presentation structure**. Changing the voice changes nothing structural.
