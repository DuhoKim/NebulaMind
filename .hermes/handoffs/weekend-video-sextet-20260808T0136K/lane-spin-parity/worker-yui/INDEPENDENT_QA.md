# Independent QA ledger

Scope: static proposal handoff only. No entry in this ledger authorizes an MP4 candidate, science result, narration, upload, or publication.

## V2 — paper-naive review

Delegation: `deleg_d6e2c159`, task 0.
Verdict: `FAIL — proposal handoff only`.

Blocking comprehension findings retained from the review:

- the opening did not state the scientific question before method jargon;
- the sample branches did not explain overlap/non-additivity;
- the equation did not explain sign or connect the statistic to label-frame uncertainty;
- the six visible connector rows could be mistaken for all 36 probes;
- the control scene named conditions but not their comparison logic or no-outcome status;
- the independent-review blocker appeared only at the final frame.

Files written by reviewer: none.

## V2 — adversarial representation review

Delegation: `deleg_d6e2c159`, task 1.
Verdict: `FAIL_AS_IS_FOR_STATIC_PROPOSAL_HANDOFF`.

Blocking representation findings retained from the review:

- the final `NOT CLAIMED` list still displayed explicitly forbidden scientific contexts; negation did not erase the association;
- sample branching did not fully fence overlap/additivity;
- six example connector rows needed an explicit example-vs-36 label;
- control logic needed design/no-outcome wording.

Files written by reviewer: none.

## V3 — targeted re-reviews

Delegation: `deleg_e5f422b7`.

- Task 0 paper-naive verdict: `PASS — STATIC PROPOSAL HANDOFF ONLY`.
- Task 1 adversarial verdict: `FAIL — STATIC PROPOSAL HANDOFF ONLY`.

The adversarial task passed the overlap, six-of-36, control-design, no-result, no-path, and held-status checks. Its sole blocker was the persistent audience header `SPIN PARITY`, interpreted literally against the no-visible-`parity` test.

## Corrective action

- V4 restored the explicit 29,053 tie count while retaining `flag overlap not quantified`; all other v3 frames remained byte-identical.
- V5 changed the persistent header to `GALAXY SPIN` and the two audience citation footers to `galaxy-spin contract`, while preserving all semantic-body pixels from v4.

## Current v5 review

Delegation: `deleg_9299883e`.
Verdict: `PASS — STATIC PROPOSAL HANDOFF ONLY`.

The final reviewer inspected the freeze, current storyboard, v5 contact sheet, all seven full-resolution frames, custody hashes, storyboard-visible strings, and OCR output. It confirmed:

- immediate paper-naive question and both blockers;
- all exact sample counts, including 29,053 ties;
- explicit overlap/non-additivity/subset boundaries;
- equation, sign, tie handling, and label-frame limit;
- `FRAME UNSTATED` consequence;
- six examples versus 36-probe scope;
- within-object control design with no outcome;
- persistent `RESULT HELD`;
- zero result values/figures, internal paths, or forbidden audience terms.

Files written by reviewer: none.

This PASS is limited to Hwao's review of the static method-only proposal. It is not candidate, science, narration, release, upload, or publication authorization.

## Delayed full-summary reconciliation — v6/v7

The complete async v2 summaries arrived after the v5 seal and contained additional findings omitted from their earlier truncated delivery: opening-order mismatch, undefined readout terms, missing one-A-per-readout and sign-swap bridges, possible branch-probability implication, overbroad alignment language, unexplained column codes, generic control questions, internal workflow identifiers, and edge-safe concerns.

V6 and v7 correct those items. V8 is pixel-identical to v7 and changes only non-visible custody so the release boundary cites the official worker freeze.

V7 delegation: `deleg_f9c2155d`.
V7 verdict: `PASS — STATIC PROPOSAL HANDOFF ONLY`; later superseded solely for exact-current custody binding.

V8 delegation: `deleg_00a42452`.
V8 verdict: `PASS — EXACT-CURRENT V8 STATIC PROPOSAL HANDOFF ONLY`.

The exact-current reviewer reported no blockers. It independently confirmed the full delayed-v2 repair set, zero forbidden/internal audience terms, no result values or axes, and stable exact bytes across its audit-start plus three final hash samples. Custody matched exactly: storyboard `80c91bc5513ac6268501c02cac2e8354388e43bf0094569804e4bbf54f293bce`, renderer `cf1ba19c47d599166b98ab50098b9601108027cbca63b8ecf93136726904a3f8`, and official worker freeze `ecff0992015e0bea2b86c14fbf627b79348228bc387d4ad4523208f4925a56a1`. All seven frames plus the contact sheet were 8/8 byte-identical and pixel-identical v7→v8.

Files written by reviewer: none.

This PASS is not a candidate, science, narration, release, upload, or publication authorization.

## Isolated deepening pass 2 — encoded candidate and blocker packet

Exact-v4 delegation: `deleg_e066afae`.
Verdict: `PASS — PASS2 BLOCKER-DEEPENING HANDOFF ONLY`; blockers: none.

The reviewer verified candidate and snapshot custody at start and close, all 16 extracted frame pins, the scene-level scientific-presentation diagnosis, unchanged v8 proposal custody, absence of T4 science-result values from the blocker packet, the inherited A3.8 minimum record fields, and exact `FRAME_UNSTATED` status. It agreed that blocker deepening rather than cosmetic v9 work is safe within proposal-only scope.

Two minor census findings were corrected before seal: case-insensitive verdict naming adds pre-T4 `RUN_kun_verdict.txt` (5/5 verdict-named files still predate T4), and all-decodable-text scanning adds post-T4 `storyboard_spin_parity.json.pre-intro.bak` (4/4 T4-naming documents still contain zero A3.8 mentions). Neither changes the blocker or any science status. Final corrected snapshot: `qa/pass2_review_snapshot_v7.json`; deterministic verifier and 51-entry manifest pass.

Focused exact-v5 rechecks `deleg_0de510e1` and `deleg_9aa6be32` were pending at seal as non-gating late reviews. Any later finding can reopen only this pass-2 handoff. It cannot authorize a result, candidate, narration, release, upload, or publication.

### Late superseded-snapshot review

Delegation `deleg_7069a623` reviewed the first pass-2 snapshot after that snapshot had already been explicitly superseded. Its custody verdict was correctly `FAIL`: the old snapshot pin no longer matched the deepened blocker packet. This is expected historical custody evidence and does not apply to final snapshot v7.

Its substantive pixel/science checks passed. Its five-file case-insensitive verdict census and storyboard-backup finding were incorporated into v5 before seal. The additional companion execution log was rechecked directly: `T4_PAIRED_LOG.txt` is hash `c49af7b8…28a9e`, timestamped 80 microseconds after the result artifact, but contains zero literal `T4_PAIRED_FLIP.json` mentions and zero A3.8 mentions. It is therefore outside the packet's explicitly defined literal-name census and cannot clear the independent-review blocker. The review's stale-manifest finding also applied only mid-edit; current v7 manifest verification remains 51/51 PASS.

Delegation `deleg_4b9a08fc` likewise correctly returned custody `FAIL` for superseded snapshot v3: the audit text had already received its explicit count clarification and v3 had already been marked `SUPERSEDED_AUDIT_COUNT_CLARIFICATION` in favor of v4. It reported no minor findings; candidate custody and all 16 midpoint-frame hashes matched. This is expected supersession evidence and has no bearing on current final snapshot v7.

Delegation `deleg_69450782` correctly returned custody `FAIL` for superseded snapshot v2 and repeated the verdict/backup census gaps already repaired later. It also found one independent presentation-detail issue not yet reflected in the current audit: scene 9's rightmost provenance text was clipped. Worker full-resolution vision confirmed the line reaches the frame edge after `LANA_T3_REDERIV…` while the major chart remains legible. `PASS2_ENCODED_FRAME_AUDIT.md` now states that bounded defect instead of claiming no inspected clipping. Snapshot v6 first pinned that corrected audit and v7 supersedes it only for chronology reconciliation; candidate bytes, all extracted frames, blockers, v8 proposal pixels, and science status remain unchanged.

Delegation `deleg_abc7057b` reviewed a moving pre-v5 control plane and correctly returned custody `FAIL`; its stale target/manifest findings were already superseded by later snapshots. It additionally identified a real chronology ambiguity: the packet's 03:22 initial draft time preceded the current extraction receipt's 03:25 deterministic replay time. Snapshot v7 resolves this without inventing an earlier timestamp: the audit and blocker packet now explicitly distinguish initial draft, replay receipt, and 04:00 exact-current reverify, and `qa/verify_pass2_packet.py` mechanically enforces that order. Its substantive checks passed; no science or pixel state changed.

Delegation `deleg_0de510e1` completed its exact-v5 review after v7 was current and correctly returned custody `FAIL` for the same 221.383-second draft-versus-replay chronology ambiguity. It reported no minor findings; all artifact, candidate, frame, source, census, A3.8, FRAME_UNSTATED, and unchanged-v8 substantive checks passed. This finding is already resolved by snapshot v7's explicit three-time chronology and mechanical ordering assertions, so it does not reopen v7.

Focused delegation `deleg_9aa6be32` returned `PASS` for its narrower exact-v5 mechanical/census regate: no blockers, 5/5 pre-T4 verdict-named files, four post-T4 T4-naming documents with zero A3.8 mentions, 14/14 packet hash references, six faithful unblock fields, and exact FRAME_UNSTATED. Its sole metadata minor was that then-current `STATUS.json` still said “all four verdict-named files”; current status says all five and contains no stale four-file phrase. This narrow historical PASS does not override v5's chronology failure; it confirms the census correction carried into current v7. All dispatched pass-2 late reviews are now settled.
