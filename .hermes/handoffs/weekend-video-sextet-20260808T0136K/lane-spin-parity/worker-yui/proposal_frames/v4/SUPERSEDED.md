# SUPERSEDED — static proposal v4

Retained as the tie-count restoration receipt. It is not current, not a candidate, and not authorized for integration or release.

V4 restored `29,053 ties` while retaining `flag overlap not quantified`. The targeted v3 adversarial re-audit then interpreted the phrase `SPIN PARITY` in the persistent header as violating its no-explicit-parity test. V5 changes the audience header to `GALAXY SPIN` and the two audience citations to `galaxy-spin`; all method content is otherwise preserved. The root `STORYBOARD_PROPOSAL.json` now belongs to v5 and must not be used to reconstruct v4.
