# Yui integration/visual-QA brief

Read and obey `../../LANE_CONTRACT.md` and `../../USER_DIRECTION.md` relative to this brief.

Write root: `/Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/lanes/integration/`

You are the isolated integration and representation-QA pass, not a publisher. Read the actual encoded videos, `tools/nm_paper_video.py`, the five storyboards, and the independent scientific-presentation consults in `.hermes/handoffs/video-scipres-consult-20260808T0108K/` once their files exist. Preserve disagreements and grade each recommendation against decoded frames. Goru's report may be partial; do not upgrade it beyond its evidence.

Build shared renderer/storyboard candidates only as copies in this integration directory. Do not edit `tools/`, source storyboards, shared audio directories, or public MP4s. Define and machine-check:

- source/storyboard/renderer/audio lineage sidecars;
- a fixed scientific slide grid with evidence viewport, annotation panel, claim band, and scholarly citation layer;
- progressive figure states instead of one PNG held for 6–12 seconds;
- large video-safe axis/tick/legend/citation typography;
- display citations separated from internal verification paths;
- no decorative presenter over scientific evidence; presenter, if used later, is bookends only;
- ffprobe audio-stream, card-count, figure-runtime, static-hold, and no-raw-path QA gates.

Use Fesc as the shared renderer canary unless its source freeze fails. Run encoded-frame QA on the copied canary and make a second correction pass. Do not invoke TTS; prepare an Alloy manifest after visual acceptance. Reconcile paper-lane outputs as they land, but never copy a claim across papers.

Write `STATUS.json` immediately and maintain an `INTEGRATION_LEDGER.md`. End the current pass with standalone marker `YUI_INTEGRATION_WEEKEND_PASS1` in `INTEGRATION_RECEIPT.md` or an exact blocker. Hwao remains the coordinator and may supersede the sequencing in `HWAO_WEEKEND_ORDER.md`.
