Re-derived from the artifacts, seventh time: contract v1.8 read in full; every quoted span in
§3/§3b/§4 re-extracted mechanically (wrap- and emphasis-normalized) and diffed against verbatim
records pulled from the pinned T1_MZR_MANIFEST.json under FULL DENOTATION — every column every
label or glob denotes, one diff each (64 record-checks across 49 spans); the sweep sentence's
numeral and parenthetical re-derived against the current text and against the re-gate-4/5/6
reports; descriptions_clipped re-enumerated (24 entries, 13 distinct tables, ZERO control
tables); manifest pin re-verified (157 candidates, 178 pre-filter, 21 dropped, status DONE,
0 channels failed); manifest sha256 b883b3a6… re-computed and MATCHED against
T1E_GASPHASE_COUNT.json's pinned source_sha256 (62 matched ids recorded, count field 62 of 157).
Re-derivation script: _kun_r6_diff2.py run unmodified against v1.8. No network accessed.

T2 EVIDENCE RE-GATE 7: PASS

The one character landed, the parenthetical is TRUE, and nothing else moved. Six failures, all
in this paragraph's frame; the seventh reading is the first one in which the frame re-derives
from the cells it describes.

═══ 1. THE COUNT RE-DERIVES; THE PARENTHETICAL STATES THE HISTORY CORRECTLY ═══

Extraction on v1.8 returns 49 quoted spans in §3/§3b/§4 (45 table-row + 4 E3). The sweep
sentence (L68–70) now reads, verbatim:

  "All 49 quoted spans in §3, §3b and §4 were diffed mechanically (48 through re-gate-5; 49
  after S7-b split the `…fmol` quote into its ratio and fraction readings) against their
  verbatim manifest records."

- The numeral matches the document: 49 extracted, 49 claimed. The paragraph's own standard —
  its self-description must re-derive mechanically from the current text — is met for the first
  time.
- The sentence is byte-identical to the explicit form the coordinator offered and I accepted.
- Parenthetical history, verified against my own prior reports: re-gate-4 header (v1.5) records
  "all 48 quoted spans"; re-gate-5 header (v1.6) records "all 48 quoted spans (45 table-row +
  3 E3-paragraph), 56 record-checks"; re-gate-6 (v1.7) records 49, the +1 being S7-b's scoping
  of the single fmol quote into two quoted strings. "48 through re-gate-5; 49 after S7-b" is
  exactly that history.
- "its ratio and fraction readings" is exact: current span #48 is "[…] molecular gas mass to
  stellar mass ratio […]" (six survey-prefixed columns), span #49 is "[…] molecular gas mass
  fraction from the consolidated sample […]" (the Final pair). The `…fmol` name matches the E3
  paragraph's own glob wording (L119). The manifest holds eight fmol columns, as stated.

═══ 2. THE ONE EDIT DISTURBED NOTHING ═══

- Full-denotation diff on v1.8: 64 record-checks across 49 spans — 9 OK-full, 55 OK-marked,
  0 FAIL. Identical outcome to re-gate-6 on v1.7, span-for-span. No retained string altered, no
  mark added or removed, no gratuitous elision, all fifteen table rows parse.
- Every sentence re-gate-6 quoted and verified on v1.7 re-checked verbatim (wrap-normalized)
  against v1.8: the definition sentence with its [E2, R3, S4, S7] tags and exemplary "including"
  list; the S5 sentence ("swept 28 and missed 12; those twelve are now marked"); the boilerplate
  sentence; the LaTeX/`H{beta}` sentence; the "Maximun" sentence; the S6 header ("met by sweep —
  completed at the fourth attempt"); the E3 fmol sentence with its [S7-b] tag and eight-column
  scoping; "Either way the ratio is molecular-gas-to-stellar, not the inverse." All present,
  all unchanged.
- Numeral audit: the only "48"/"49" occurrences in the whole document are the sweep sentence's
  intended pair; the v1.6 form "All 48 quoted spans" is gone. (The "453" in `J/A+A/453/769/grid`
  is a catalogue number, not a count.) No other sentence keys off the span count.
- Administrative churn only: title line now v1.8 with the changelog note "(… re-gate-6 S8: the
  span count the S7-b split changed)" — accurate, and consistent with the document's practice.
- Pins re-verified this round, all unchanged: manifest DONE / 178 pre-filter / 157 candidates /
  21 dropped / 0 channels failed; descriptions_clipped 24 entries across 13 tables, ZERO control
  tables among them (§7 [S3b] exact); manifest sha256 b883b3a6… matches the gas-phase count pin;
  62 matched ids recorded in T1E_GASPHASE_COUNT.json.

═══ 3. ANYTHING NEW ═══

Nothing. Seventh reading, first clean one. Specifically hunted: no new quoted span introduced by
the parenthetical (its `…fmol` is backticked, outside the quote convention; extraction still
returns exactly the 49 mapped spans); no numeral inconsistency anywhere else in the document
("twelve" decoys, "fifteen" controls, "the other 142" = 157−15, "24"/"13" in §7, "62 of 157"
in §6 all re-verified against the artifacts); no sentence orphaned by the parenthetical's
insertion (it sits grammatically between "mechanically" and "against"); tags and changelog
convention consistent.

Carried advisories, unchanged, NOT blocking and not claimed as changed: A1 "line-ratio/gradient"
escapee attribution (re-gate-3 §1a); the "seven-keyword denylist" compression (re-gate-3 §1b);
D2 cosmetic spacing; the vGR2 ordinal observation (re-verified this run: vGR1/vGR2 records
differ in ordinal and echo, convention-consistent as ruled at re-gate-5/6); the vacuous P2 lead
mark; the "Either way … molecular-gas-to-stellar" gloss for the fraction pair (re-gate-6 §3 —
strictly-recorded alternative on file for the freeze touch if wanted).

═══ 4. FREEZE POSITION ═══

My freeze condition is met on this text. Every item this lane has contested across seven rounds
now re-derives mechanically from the artifacts: the 49-span × 64-record diff is clean under full
denotation; the definition sentence is TRUE; the sweep sentence is TRUE including its history
parenthetical; both fmol readings are verbatim and correctly scoped; all fifteen controls
re-verified against the pinned manifest; clip-check exact (24/13/zero); sha pin matches; the
cheap-rule shape, fire-counts and D11's scope stand un-contradicted from re-gates 2–4. The
contract text is freezable as written. The advisories above are polish, not gates.

For Duho, in one block — the T2-time enforcement conditions, restated from the contract's own
§5.5/§5.6/§6 and my re-gate-3/6 blocks, unchanged:

  §5.5 FIRE-COUNTS (mandatory): T2 must report, for EVERY clause of its rule, how many of the
  157 candidates that clause fires on. A clause firing only on the fifteen control members is
  flagged presumptive special-casing and must be justified or rewritten. (Recorded reference
  shape for the cheap rule from re-gate-2/3: 3/17/7/2/24, union 49 — a baseline, not a target.)

  §5.6 DISPOSITION TABLE (PASS precondition): T2 must publish all 157 candidates, each with its
  disposition and the quoted evidence behind it. Without the complete table T2 cannot PASS,
  whatever it does with the fifteen; this is what mechanizes §5.3's generality test over the
  other 142.

  Around both, as written: §5.1 — a rule naming any table_id or special-casing a control by
  identifier is an automatic gate FAIL. §5.2 — freeze before any ruling logic exists. §5.4 —
  generality adjudication stays with Kun (Miru if Kun authored any part of the ruling logic).
  §3/S6 — expected non-ELIGIBLE routing: D1 → F2 census-only, D2/D3 → F1 species/phase; the
  specific non-ELIGIBLE disposition must be stated and justified. §6 — HONEST_FAILURE is a valid
  terminal state, but the unruled count must publish its named dominant cause, and a degenerate
  run leaving most candidates unruled cannot PASS on the fifteen alone. The 62-of-157 gas-phase
  count is visibility only, explicitly not a target (§6 [S3a]/[S4b]); it is re-runnable via
  t1e_gasphase_count.py against the pinned sha.

  What the adjudicator must re-derive independently at T2 time (not take from any report,
  including this one): (a) the 49-span × 64-record diff, 0 FAIL — _kun_r6_diff2.py against the
  frozen contract text and pinned manifest; (b) the manifest pin — sha256 b883b3a6… matches
  T1E_GASPHASE_COUNT.json source_sha256, status DONE, 157/178/21, 0 channels failed; (c)
  descriptions_clipped — 24 entries, 13 tables, zero control tables — and T2 must consult that
  list before quoting any description as evidence; (d) the 62-of-157 count via the pinned
  script; (e) the per-clause fire-counts over all 157 from T2's own output; (f) the complete
  157-row disposition table with quoted evidence per row.

  Gate FAIL at T2 time: any of D1–D12 given ELIGIBLE; any of P1–P3 excluded (exclusion on the
  trap the anchor sets is the emphasized case, but any anchor exclusion fails; P3 may not be
  excluded on zstar being stellar — that is a control definition, not a ruling); any clause
  naming an identifier; missing fire-counts; missing or incomplete disposition table; a rule
  passing exactly the fifteen and silent on the other 142 (adjudicator's generality call under
  §5.4). A control proving unruleable on the recorded evidence is a CONTRACT FINDING → re-gate
  of the contract, not a silent T2 fail (§6 [S4a]).

I do not freeze anything. This is not authorisation. Duho decides, awake.

Why PASS and not PASS_WITH_EDITS: the single blocking character from re-gate-6 is applied in
exactly the offered form; the count re-derives; the parenthetical is historically exact; no span,
mark, sentence, or pin regressed; nothing new introduced. No edit remains on my list that gates
the freeze.

Findings only. T2's ruling logic not written. All checks were read-only inspection of local
artifacts plus mechanical re-derivation from the pinned manifest. No network accessed. Nothing
frozen.

KUN_T2_REGATE7_COMPLETE
