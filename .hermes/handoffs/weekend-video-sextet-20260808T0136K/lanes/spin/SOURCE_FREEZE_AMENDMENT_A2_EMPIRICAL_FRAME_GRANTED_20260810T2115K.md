# AMENDMENT A2 — GRANTED: empirical frame determination admissible as a FINDING

Granted 2026-08-10 21:34 KST. **Duho, directly: "grant the amendment with those three constraints."**

Amends `AMENDMENT_A3.9` §4/§5 for the spin lane. Drafted by Lana in
`SOURCE_FREEZE_AMENDMENT_EMPIRICAL_FRAME_DRAFT_20260810T2040K.md`, which is incorporated by
reference and remains the operative detail.

**`lanes/spin/SOURCE_FREEZE.json` is NOT modified.** It stays byte-identical at `f7204bd7aa9a96830e22b76456cd2e24…`.
This amendment sits beside it and binds only when read together with it.

## What is granted

An **empirical determination** of the mirrored-condition frame becomes admissible. Nothing else
changes.

This is a deliberate, on-the-record **lowering of the evidence bar** — not a claim that the
documentary bar was met. Lana's two prior rulings stand undisturbed: the documentary route is
**exhausted** across three surfaces, and A3.9 §4 as written is **documentary-only**. Neither is
softened or reinterpreted by this grant.

## The three constraints — Duho's words, binding

1. **FINDING only, never an ESTABLISHMENT.** The frame remains undocumented; a result here is an
   inference about the data, not a statement of the archival convention.
2. **Permanent inference label**, carried in every downstream artifact, with the draft's stated
   mechanism for stopping the label being lost.
3. **No automatic Land comparison.** No "contradicts", "confirms" or "in tension with" phrasing
   attaches to this result by default.

Lana's support was explicitly conditional on all three: *"If Duho would rather not accept those
constraints, my recommendation flips back to Path C — an inference recorded as more than an
inference is worse than an honest null."* All three are granted, so her conditional support holds.

## The gate that makes this honest, and it is absolute

**Pre-registration is frozen and independently gated BEFORE any number is seen.** Thresholds,
controls and the bias-confound control are bound first; the correlation is computed **once**, as a
fresh separately-receipted computation; any outcome not clearing every stopping condition is
**INCONCLUSIVE**, and uncertainty defaults to BLOCK.

Lana pre-committed: *"I would refuse to concur with any run whose pre-registration was not gated
first."* **That refusal is upheld as binding, not advisory.** If the gate is skipped or weakened,
the run is void regardless of what it produces.

## Granted knowing it may lose

Lana: *"Inconclusive is a genuinely likely honest outcome. The bias confound is real — the effect
under study can produce the same sign… Granting this is a bet that may honestly lose."*

Duho grants it on that basis. **An INCONCLUSIVE result is a successful execution of this
amendment**, returns the lane to Path C, and is to be reported as plainly as any other outcome.

## What this does NOT unblock

`STATUS_RESULT_MISMATCH`, `WORKFLOW_STATUS_NOT_RELEASE_READY` and the excluded-pointers blocker all
stand. `BLOCK_SUBSTANTIVE_RESULT_RENDER` survives even a successful test — this clears one of four.
`video_reportable_now` stays `false`. No video changes on this grant.

## Sequence

Lana binds the pre-registration and Kun gates it **before any computation**. Only then does the run
proceed, once, receipted. Tori binds the result. Nothing is accepted without Duho.
