REFUTED_PACING_AMENDMENT

The amendment's speed arithmetic is contradicted by the receipts and the transport code. In 35,962 accepted pre-amendment receipts through 2026-08-23 13:36:57Z, median throughput was 2.038 MB/s, but mean request-start spacing was 6.439 s (559.1 requests/hour), not ~4.9 s (~735/hour). `PACING_SECONDS` is only a minimum between request starts (`nm_image_transfer.py:816-823`); median transfer duration was already 5.963 s, so the old 2.0 s floor was nonbinding. Only 24/35,947 normal intervals were ≤2.1 s; changing the floor to 0.25 s projects ~559.1 requests/hour, essentially no acceleration. The harvest precedent does hold: 60,308/60,308 receipts are `OK_CONFIRMED` under a 1.0 s minimum, so the proposed load is conservative, but the amendment does not implement Duho's requested expedition and its stated effect is false.

Attacks that held: `git diff -- _tori_transfer_20260819/nm_image_transfer.py` changes exactly one source line, `PACING_SECONDS = 2.0` to `0.25` plus its comment; script SHA-256 is `a4e4c4af669520e63651c281eaede42e66109f06add976b8cb8ea8517a136ad2`. Dict comparison finds exactly `image_request_spacing_seconds`, `prose_record`, and `duho_verbatim` changed. The new approval is mode 0444 and its SHA-256 is `ff242cc42cf495e858e6857239165fdb8f2d9a7461c2c2f78cdb19a7e17ce6b3`, matching the amendment. “why? make it fast.” is a direct order to expedite.
