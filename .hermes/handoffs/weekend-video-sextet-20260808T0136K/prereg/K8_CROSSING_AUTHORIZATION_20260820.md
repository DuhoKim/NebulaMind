# K-8 crossing — AUTHORIZED, and the amendment frozen

**Duho, 2026-08-20 22:20 KST, verbatim: "freeze it and authorize the crossing"** — given after
reading the gated amendment and the revised recommendation in `K8_CROSSING_BRIEF_20260820.md`.

## 1. Frozen at this moment

    AMENDMENT_PREK8_20260820.md
    SHA-256  161547400e47ed66df616ba14756d9ab066c547f54b39bc161e6b4eaa26478c0
    mode     444   (verified at freeze: hash matched the gated Revision 2 exactly)
    gates    KUN_GATE_A_AMENDMENT_20260820.md  (HOLD, 4 repairs, 3 rulings)
             KUN_GATE_A2_REGATE_20260820.md    (PASS_PREK8_AMENDMENT, integrity sweep PASS)

Predecessor Revision 1 (`e832a341…`) is superseded and retained unedited. The parent
preregistration `PREREG_LONGO_AMPLITUDE_TEST_FROZEN_20260815_V3.md` (`b06901c8…`, 444) is
re-verified unmodified at freeze time.

## 2. What K-8 said, and what it says after this

Before: *"no real-sky statistic has been computed anywhere in this program — no chirality label,
no sky estimand, no unblinding of any kind, and no science cutout has been fetched."* The cutout
and fetch clauses were already spent under Duho's earlier authorizations. **This authorization
spends the chirality-label clause.** From the first real χ onward, F-9 binds absolutely: any
parameter change voids the run.

## 3. Conditions binding the crossing (all frozen before it)

1. **Partial-tertile prohibition.** Per-object χ may accumulate incrementally as cutouts land.
   The |χ| tertile boundaries — and therefore the strata — are computed **exactly once, on the
   complete accepted population**, after the last cutout. No partial tertile may be computed,
   printed, logged, or inspected, not even as a diagnostic: once it exists, it can be seen.
2. **No aggregation.** χ is a per-object measurement with a receipt. No sky statistic, no dipole,
   no summary over χ of any kind until the frozen order of work reaches it.
3. **The amendment governs**: sign convention (scientific + operational, AM-A), Jeffreys
   allocation priors with the estimate firewall (AM-B), sparse-cell rule defaulting to
   HOLD → INCONCLUSIVE-BY-POWER until a gated merge revision exists (AM-C).
4. **Sign discipline.** An apparent inverted polarity on real data is a bug (three enumerated
   defect classes), triggers a HOLD, and never licenses a sign flip. If confirmed after any real
   χ, F-9 voids the run — that is the cost, and it is accepted knowingly.
5. **Gated code only.** Inference runs the gated runner (`PASS_INFERENCE_BUILD`) with the gated
   plumbing (`PASS_PLUMBING_FIXES`) over gated cutouts (`PASS_CUTOUT_PIPELINE`) from
   digest-verified bricks. Any change to any of them after the crossing is a parameter change.
6. **Stop conditions.** Weights-hash mismatch, IC-contract failure, identity-property failure,
   or any refusal by a gated program stops the run and comes to Duho. Nothing is worked around.

## 4. What is NOT authorized by this

Computing the sky estimand; unblinding anything; the hand-check itself (that needs cutouts
complete, strata computed once, and Duho at the keyboard); publication of any kind. The pilot's
150 labels remain Duho's separate act under `PILOT_DECISION_20260818.md`.

## 5. Execution

Hwao arms the inference authorization file the gated runner requires, launches incremental χ
behind the cutter, and guards it like the transfer: per-object receipts, resumable, stop on any
refusal. First real χ marks the crossing; the receipt of the first object is the record of it.

— recorded by Hwao; the authorization and its verbatim wording are Duho's. K-8's chirality-label
clause is spent as of the first receipt written under this document.
