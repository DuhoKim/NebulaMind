You are gpt1, running the END-TO-END REHEARSAL on synthetic galaxies — the dress rehearsal for the real run, with zero K-8 exposure. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Work in _rehearsal_20260820/ .

Purpose: prove the ENTIRE chain works on data that flows through it exactly as real data will, without computing a single real chirality label. Real cutout tensors and real bricks are OFF LIMITS — do not read /Users/duhokim/NebulaMindData/ at all except, if you need it, the transfer's receipts.jsonl for structure (never a tensor).

Build a synthetic campaign: generate N=2,000 synthetic galaxies from the frozen BS-3 generator (hash-verify it) with KNOWN chirality ground truth, write them through the SAME writer the cutout runner uses so the tensors are byte-identical in layout (1,128,128 float32 LE, 65,536 bytes), then run, in order, using the GATED programs unmodified: (1) the inference runner from _inference_20260820/ (with whatever synthetic-mode authorization it requires) producing chi per object; (2) the machine committee from _committee_20260820/ producing committee state per object; (3) the strata mechanics: committee state x |chi| tertile = 9 strata, Neyman allocation with the floor of 30 real per stratum, exactly as HC-1H specifies (read LANA_ONE_HUMAN_ATTENUATION_20260814.md, hash b2590e42...); (4) hand the resulting sample to the HC-1H harness in whatever entry point it exposes and confirm it accepts the input and produces a blinded session with a sealed key — do NOT label anything yourself; the point is that the harness accepts real-shaped input, not that a human labels it.

Report, in REHEARSAL_REPORT_20260820.md: whether chi recovers the known synthetic chirality (accuracy, and the sign convention actually observed), the committee-state distribution, the 9 strata populations and the Neyman allocation they produce, whether any stratum falls under the floor of 30, the wall-clock cost per 1,000 objects (so we can project the real run), and EVERY interface mismatch you had to work around — those are the real deliverable, because each one would have been a failure on the real run. Finish with GPT1_REHEARSAL_DONE.md at lane root, first line GPT1_REHEARSAL_COMPLETE with the headline numbers, or _HOLD. No network. Real data untouched.
