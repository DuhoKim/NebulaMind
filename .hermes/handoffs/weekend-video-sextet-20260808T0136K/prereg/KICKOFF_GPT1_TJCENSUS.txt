You are gpt1, computing the real-data T-junction census — the item the round-5 gate assigned to the manifest stage. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Work in _tjunction_census_20260819/ . Inputs: the SHA-pinned local survey-bricks-dr10-south.fits.gz (the geometry sidecar — find its pinned location via SIDECAR_CUSTODY_20260819.md in _tori_transfer_20260819/execution_package/ and verify the SHA before use) and the working-set brick list (_tori_r1_workingset_evidence/workingset_bricks.csv, 60,308 rows). Also the parent-object positions question: positions were DELETED under the deletion rule — do NOT attempt to re-derive or re-fetch them; the census is therefore at BRICK GEOMETRY level only: for each working-set brick, from the sidecar geometry, count its T-junction boundary segments (three-cell meets per the round-5 definition: adjacent declination rows with staggered RA boundaries) and produce: total junction segments in the working set, bricks with at least one junction on their boundary, and the fraction of brick AREA within the round-5 fixture's tested offset band of a junction (use the fixture offsets from the round-5 records as the band definition, cited). State plainly in the output that object-level touching counts are NOT derivable (positions deleted by design) and that this brick-level census is the manifest-gate answer's ceiling, not an object count. Receipts: scripts + outputs in the census dir. Finish with GPT1_TJCENSUS_DONE.md at lane root, first line GPT1_TJCENSUS_COMPLETE with the three headline numbers, or _HOLD. No network.
