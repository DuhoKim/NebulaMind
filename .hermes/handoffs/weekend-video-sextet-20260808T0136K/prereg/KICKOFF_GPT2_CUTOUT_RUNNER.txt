You are gpt2, building the production cutout runner — the composition layer over already-gated pieces. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Build in _cutout_runner_20260820/ . The law, verify hashes/modes first: PREREG_LONGO_AMPLITUDE_TEST_FROZEN_20260815_V3.md (b06901c8..., 444) §6, and LANA_PC1_INPUT_AMENDMENT_20260815.md (519ab5ba...) §3 A2-A3 — the IC-1..IC-7 input contract is your spec, implemented to the letter. Composition: the gated production read path (_production_readpath_20260819/, PASS_PRODUCTION_READPATH) feeds bricks; the gated adapter (the resampler-gate-certified cutter from the 08-15..17 lanes — find it via KUN_RESAMPLER_GATE_20260816.md and reuse, never reimplement) produces the 128x128 r-band raster; your layer applies IC-1..IC-7 exactly: single plane check (fail closed on anything else), nanomaggies unconsumed-units rule, no background ops, IC-4 invalid-pixel handling with the cap as an EXPLICIT UNFILLED SLOT (parameter file _cutout_runner_20260820/ic_slots.json with ic4_invalid_fraction_cap=null, ic5_scaling_map=null — the R1-R5 rerun fills and pins them; the runner REFUSES real-sky objects while slots are null), IC-5 scaling map as a pluggable frozen-function slot, IC-6 float32 little-endian C-order (1,128,128) with one big-endian conversion at ingest, IC-7 np.fliplr mirror in tensor space only. Custody: per-object receipts at the Mittal-Singal standard (brick sha ref, adapter geometry receipt, IC pass/fail flags, invalid fraction, output tensor sha256). Positions input: accept an explicit positions file parameter (ra, dec, ls_id) — do NOT invent a selection query; the frozen selection's position provisioning is a separate step I will spec. Include tests: synthetic fixture through the full path; IC-1 fail-closed test (wrong shape); IC-4 null-slot refusal test; mirror bit-exactness test (fliplr twice = identity; chi-path antisymmetry left to the rerun). Real accepted bricks may be READ (read-only) for a smoke test of the read path composition WITHOUT producing science outputs (mark smoke outputs as NON-SCIENCE and delete them in the same run). Finish with GPT2_CUTOUTRUNNER_DONE.md at lane root, first line GPT2_CUTOUTRUNNER_COMPLETE with test results, or _HOLD. No network. Never portal.nersc.gov.
