You are gpt2 (hermes tori2 profile), building the execution package M1+M3+M4+M5 per KUN_TRANSPORT_GATE_20260819.md §6/§8 under the frozen binding TORI_ROUTE_BINDING_SUCCESSOR_20260817.md (verify 1371b110... mode 444 first) and ACQUISITION_PREAUTH_20260818.md. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Work in _tori_transfer_20260819/execution_package/ :

M1 — the §11.4d size sample: exactly 1,024 HEAD requests against manifest URLs (sample selection per the binding's clause — read it; if it specifies the sampling rule, follow it exactly; else use a seeded uniform sample over the manifest and record the seed), at the binding's §5.4 pacing (currently in-window; the pacing rule is a MINIMUM spacing — never faster), receipts.jsonl per request (url, status, content-length, last-modified), then derive the approved byte ceiling exactly as §11.4d prescribes (read the clause; show the arithmetic in a receipt script). HEAD requests only — any GET of an image URL is forbidden and is a lane-stopping violation. If any HEAD returns non-200, record and continue; >1% non-200 → STOP and report.

M3 — package the coverage census as a hash-pinnable artifact: COVERAGE_CENSUS_20260819.md stating required=60308, receipted=60308, absent-by-coverage=0, contradictions=0, with the receipts.jsonl SHA-256 and the harvest completion record quoted; pin the file (print its SHA-256 into EXEC_PACKAGE_INDEX.md).

M4 — package the digest-currency re-verification: DIGEST_CURRENCY_20260819.md carrying the cross-check numbers (598 identified / 397 in-WS / 397 late-pattern / 0 hazard / 0 anomaly / control 59,911 clean) with the verdict + gate first lines quoted and the underlying receipt hashes; pin it.

M5 — the geometry-sidecar route-B custody receipt (§4.3): locate the existing SHA-pinned local survey-bricks-dr10-south.fits.gz custody records from the adapter lanes; if the existing custody satisfies §4.3's fields (source URL, digest, size, local SHA), package it as SIDECAR_CUSTODY_20260819.md with quotes; if any §4.3-mandated field is missing from retained records, say exactly which — do NOT fetch anything to fill gaps without it being a single §5-scoped checksum/metadata operation, and record it if performed.

Write EXEC_PACKAGE_INDEX.md listing all artifacts with SHA-256s. Finish with GPT2_EXECPKG_DONE.md at lane root, first line GPT2_EXECPKG_COMPLETE (with the derived byte ceiling and sample stats) or GPT2_EXECPKG_HOLD with what blocked. The transfer itself remains forbidden.
