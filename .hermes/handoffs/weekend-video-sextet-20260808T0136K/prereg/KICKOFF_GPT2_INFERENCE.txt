You are gpt2, building the inference runner — CODE AND SYNTHETIC VALIDATION ONLY. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Build in _inference_20260820/ .

ABSOLUTE BOUNDARY, stated first because it is the point: this build MUST NOT compute chi for any real galaxy. Real cutout tensors under /Users/duhokim/NebulaMindData/cutouts_dr10_south/tensors/ are OFF LIMITS to execution — do not read them into the model, not even one, not for a smoke test. Computing real chi is the K-8 crossing and belongs to Duho. Your runner must REFUSE to run on real inputs unless an explicit authorization file exists (design it as: --authorization FILE whose SHA-256 you check and whose absence means the real-data path raises and exits nonzero). Synthetic validation only in this build.

The law: YUI_PRODUCTION_ESTIMATOR_APPENDIX_20260812.md (read it fully) — the frozen instrument: CE-ResNet-class trunk applied twice with SHARED weights, chi_net(x) = (f(x) - f(mirror(x)))/2; ResNet-18 topology, 1 channel, 128x128, stem 3x3 stride 1 (no 7x7/maxpool), stages [2,2,2,2] widths [32,64,128,256], GAP, single linear scalar; eval mode, float32, SINGLE-THREADED DETERMINISTIC kernels; mirror is PURE index reversal (tensor flip on width axis) — never resampling/affine/interpolation anywhere in the chi path. Weights: prereg/weights_frozen.pt, SHA-256 83008c1cbdae511af5d30020540e1e281c62c2bd95d3cb05527fc0687bf49e6d — verify before load and refuse on mismatch.

Requirements: (1) load the frozen weights, verify hash, eval mode, deterministic single-thread; (2) input path is the IC-6 tensor layout the cutout runner emits (float32 LE C-order (1,128,128), 65,536 bytes) — read the cutout runner's writer to match byte-for-byte; (3) chi_net with the antisymmetrization done exactly as frozen; (4) the identity property test on SYNTHETICS: chi_net(mirror(x)) == -chi_net(x) BIT-EXACTLY over >=1,000 synthetic tensors from the frozen BS-3 generator; (5) determinism: same tensor twice -> identical bits; multiprocessing determinism check; (6) the machine committee (from _committee_20260820/) runs alongside to emit each object's committee state, per the HC-1H strata definition — committee output is stratification metadata, NEVER inside chi; (7) per-object receipts (input tensor sha256, chi value, committee state, weights sha, code sha) and an append-only results ledger, resumable like the other runners; (8) tests as usual.

Finish with GPT2_INFERENCE_DONE.md at lane root, first line GPT2_INFERENCE_BUILD_COMPLETE with the synthetic identity-test result, or _HOLD. No network. Never portal.nersc.gov. Real tensors: untouched.
