You are gpt2, building the object-brick manifest builder — the upstream planner the cutout runner's --brick-manifest contract requires. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Build in _objmanifest_20260820/ . The runner's exact input contract is load_brick_manifest in _cutout_runner_20260820/cutout_runner.py (schema_version 1; objects: {ls_id: [{path, row:{ra,dec}, sha256, brickname}]}). Requirements: (1) REUSE the gated adapter's polygon source-set planner (the round-1..5 gated planner — the same one the resampler gate certified; find it via the adapter the cutout runner composes; never reimplement the geometry) to compute, for each object in _positions_20260820/positions_runner_view.csv (208,407 rows; verify sha 0edfdef0...-prefix against RUNNER_VIEW_PROVENANCE.md), its full candidate source-brick set from the SHA-verified geometry sidecar; (2) join against the transfer's receipts (/Users/duhokim/NebulaMindData/dr10_south_image_r/receipts.jsonl — READ ONLY; outcome==ACCEPTED and digest_verified==true) taking path = destination root + destination_relative_path and sha256 = local_sha256; (3) an object is READY only when EVERY brick in its candidate set is accepted — emit ready objects into the runner-schema JSON; report counts (objects ready / waiting / per missing-brick histogram top-10); (4) deterministic: same inputs -> byte-identical output (sort keys, stable ordering); include tests (synthetic sidecar fixture with edge/corner/T-junction cases exercising the reused planner; a receipts fixture; determinism test); (5) CLI: build_object_manifest.py --positions --receipts --destination-root --sidecar --output manifest.json, plus --only-bricks FILE to restrict to a brick subset (the wrapper's batching hook). Finish with GPT2_OBJMANIFEST_DONE.md at lane root, first line GPT2_OBJMANIFEST_COMPLETE with ready/waiting counts on the CURRENT real receipts, or _HOLD. No network. Never portal.nersc.gov.
