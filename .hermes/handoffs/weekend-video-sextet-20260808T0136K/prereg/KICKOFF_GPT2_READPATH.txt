You are gpt2, building the production read path — the named remaining production-tier item. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Build in _production_readpath_20260819/ : a reader for real DR10 South coadd files — .fits.fz image HDU 1 — feeding the gated adapter's expectations (the adapter and its contracts live in this lane's acquisition/cutout work from 08-15..17; read KUN_RESAMPLER_GATE_20260816.md and the adapter source to match its input interface exactly). Test against REAL accepted bricks already on disk under /Users/duhokim/NebulaMindData/dr10_south_image_r/coadd/ (READ-ONLY — never write, move, or rename anything under that root; the transfer owns it; copy test subjects into your build dir if you need mutable fixtures). Requirements: fpack/.fz decompression via astropy or cfitsio (record which and pin the version), HDU-1 selection with header receipts (WCS custody fields logged per the PC-3/PC-4 concerns recorded 2026-08-15: WCS custody transfers to our code and must be verified, not assumed), determinism test (two reads byte-identical arrays), and a multiprocessing determinism check (the second named production-tier item): N processes reading the same brick produce identical arrays and stable scheduling receipts. Tests offline-safe where possible; reading the real accepted local files is permitted (they are ours now). Finish with GPT2_READPATH_DONE.md at lane root, first line GPT2_READPATH_COMPLETE with test results, or _HOLD with what blocked. No network. Never touch portal.nersc.gov.
