You are the kimi gate seat (Nous route), fresh one-shot, gating TWO deliverables that stand between this program and the K-8 crossing. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Method: local files, grep, python3; read the code and the frozen documents; time-boxed; UNVERIFIED-AT-GATE over crawling.

DELIVERABLE 1 — AMENDMENT_PREK8_20260820.md (SHA e832a341f4c9c3937b8fcafc42d6dd7c15ca1e6ea32e5c9ae21856ab50e1380c, 562 lines; receipt CSEAT_AMENDMENT_DONE.md). It amends a FROZEN preregistration before the irreversible step, so gate it as such. Verify:
 (a) AM-A: the claim that the sign convention was ALREADY frozen by BS-5 §3 and merely CONFIRMED by the rehearsal — check BS-5 and the frozen V3 yourself; if the amendment is actually CHOOSING a convention rather than confirming one, that is a material finding, say so;
 (b) AM-A's Longo polarity arithmetic: A = (R-L)/(R+L) = -0.0408 mapping to +0.0408 in (L-R) polarity, and that REPRODUCED-LONGO therefore requires positive A_c — recompute the sign algebra and check it against the frozen decision regions in V3 F-6;
 (c) AM-B: recompute the Jeffreys formula against the rehearsal's hc1h_neyman_priors.json and confirm the claimed byte-for-byte string reproduction; verify the degeneracy claim (that Jeffreys makes nm_handcheck.py's zero-information refusal unreachable) by reading that refusal site; verify the FIREWALL claim that allocation priors never enter the estimate a — read HC-4's computation path and prove priors touch only n_s;
 (d) AM-C: verify the merge ladder is deterministic, within-committee-state only, evaluated once on the complete population; verify the rejection of the proceed-with-deficient-cell option against the gated safety check it cites (HC-5.2) and F-10.c k>=50; verify BOTH branches of the AM-C.3 precedence rule are fully specified now, so no discretion survives the crossing;
 (e) the amendment weakens NO existing guarantee (F-9, F-10 output rules, HC-3/HC-4, sealed key, K-8 itself) — check each explicitly;
 (f) rule on the THREE questions the amendment escalates to you (its "What Kun must rule on" section): the P7 nine-strata wording change, whether the merge implementation can be gated before the crossing given HC1H_STRATA is a hardcoded nine-tuple, and whether AM-A.4's FAIL_CLOSED consequence is entailed by PC-1/PC-4 or is a new freeze. Rule explicitly on each; these are the amendment's own open items and it correctly refused to self-approve them.

DELIVERABLE 2 — the six plumbing fixes (receipt GPT2_PLUMBING_DONE.md; F2,F3,F4,F5,F7,F8). Verify each from code and tests, run the test suites, and specifically: F3's manifest mode is byte-equivalent to the legacy path (not merely similar) and handles 20,000 paths; F4's batch CLI verifies member-B weights hash before torch.load and refuses on mismatch; F5's renderer is deterministic byte-identical and PNGs are structurally barred from the chi path; F7's bijection is total and injective and both sides import the SAME module; F8's provenance assertion actually refuses a synthetic pool passed as real population (test it); F2 introduced no package installs. CRITICAL BOUNDARY CHECK: grep the plumbing diffs for any smoothing rule, sparse-cell policy, or strata-count change — findings 1 and 6 were fenced OUT of gpt2's scope and belong to the amendment; code that quietly encodes either is an automatic HOLD.

Write KUN_AMEND_PLUMB_GATE_20260820.md at LANE ROOT, first line PASS_PREK8_AMENDMENT_AND_PLUMBING or HOLD_ + numbered repairs, with your three rulings from (f) stated separately and unambiguously. Findings-only; edit nothing else. No network. Never portal.nersc.gov.
