You are the kimi gate seat (Nous route), fresh one-shot, gating the object-brick manifest builder in _objmanifest_20260820/ . Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Method: local files, grep, python3; time-boxed; read the code. Verify: (1) PLANNER REUSE, not reimplementation — identify the import/call into the gated adapter's polygon source-set planner and quote it; any reimplemented geometry is an automatic HOLD; (2) run the test suite yourself (fixture must exercise edge/corner/T-junction cases through the REUSED planner); (3) readiness semantics: an object is emitted ONLY when every candidate brick is ACCEPTED+digest_verified in the transfer receipts — verify in code and spot-check 5 emitted objects (their bricks all in receipts) and 5 waiting objects (at least one brick absent) against the CURRENT receipts; (4) schema conformance: feed the emitted manifest.json to cutout_runner.py's load_brick_manifest in a python snippet (import and call it) — it must parse without ContractError; paths must resolve under /Users/duhokim/NebulaMindData/dr10_south_image_r and sha256 fields must equal the receipts' local_sha256 for the 5 spot-checked objects; (5) determinism: run the builder twice, byte-identical output; (6) counts: recompute ready=15203/waiting=193204 from the current receipts yourself (allow drift if the transfer advanced — verify the CODE logic, then state the fresh counts). Write KUN_OBJMANIFEST_GATE_20260820.md, first line PASS_OBJECT_MANIFEST or HOLD_ + numbered repairs. Findings-only. No network.
