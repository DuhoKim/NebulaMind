You are the kimi gate seat (Nous route), fresh one-shot. Re-gate ONLY the two repairs to the object-manifest builder. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Prior gate: _objmanifest_20260820/KUN_OBJMANIFEST_GATE_20260820.md (PASS_OBJECT_MANIFEST, 2 findings). Repair receipt: GPT2_OBJM_REPAIR_DONE.md. Method: local files, grep, python3, time-boxed; do NOT re-litigate what already passed (planner reuse, determinism, schema) beyond one quick confirmation each.

Verify: (1) R1 path rule — the builder computes active_root = DEST/accepted if it exists else DEST/staging, matching nm_image_transfer.py's own rule (quote both code sites side by side); emitted paths resolve under it; spot-check 5 emitted objects' brick paths EXIST as regular files on disk right now and their sha256 fields equal the receipts' local_sha256; (2) R1 fail-closed rule — confirm in code that an object with a receipt but a missing file becomes waiting with reason RECEIPTED_FILE_MISSING and is never emitted; construct a temp fixture proving it (a receipt pointing at a nonexistent file must yield waiting, not emitted); (3) R2 — a zero-intersecting-brick position now counts as waiting with reason ZERO_INTERSECTING_BRICKS and does not abort; run the added test; (4) determinism preserved: run the builder twice on the current inputs, byte-identical; (5) schema still conforms: import cutout_runner.load_brick_manifest and parse the emitted manifest.json without ContractError. Write KUN_OBJM_REGATE_20260820.md at the LANE ROOT (/Users/duhokim/.../prereg/KUN_OBJM_REGATE_20260820.md — not in a subdirectory), first line PASS_OBJECT_MANIFEST_REPAIRS or HOLD_ + numbered repairs. Findings-only. No network.
