You are the kimi gate seat, fresh one-shot, gating two independent deliverables. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Method: local files, grep extraction, python3 recomputes, time-boxed; read code, not just notes. (1) _production_readpath_20260819/ against GPT2_READPATH_DONE.md: run its tests yourself; verify the .fits.fz HDU-1 selection and decompression library+version are receipted; verify WCS custody receipts exist per the recorded PC-3/PC-4 concern; rerun the determinism check (two sequential reads byte-identical) and the multiprocessing determinism check on one real accepted brick (READ-ONLY on /Users/duhokim/NebulaMindData/dr10_south_image_r/); verify the reader's output interface matches the gated adapter's input contract (compare against the adapter interface the resampler gate certified); write KUN_READPATH_GATE_20260819.md, first line PASS_PRODUCTION_READPATH or HOLD_+repairs. (2) _tjunction_census_20260819/ against GPT1_TJCENSUS_DONE.md: verify the sidecar SHA was checked before use; recompute the census on a 500-brick random sample (seeded, receipted) and confirm consistency with the headline numbers (132,108 unique events / 60,308 bricks-with-junction / area fraction 2.0614e-5); verify the band definition cites the round-5 offsets faithfully; confirm the output plainly states object-level counts are non-derivable by design; write KUN_TJCENSUS_GATE_20260819.md, first line PASS_TJUNCTION_CENSUS or HOLD_+repairs. Findings-only. No network. Never portal.nersc.gov.
