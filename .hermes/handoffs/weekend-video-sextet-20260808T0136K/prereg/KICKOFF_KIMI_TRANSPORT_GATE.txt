You are the kimi gate seat, fresh one-shot — the FINAL gate before real image bytes move in the STOP-rule chain. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Gate the transport build in _tori_transfer_20260819/ against the FROZEN binding TORI_ROUTE_BINDING_SUCCESSOR_20260817.md (verify SHA-256 = 1371b11094a2765228a7deb1bbe1367117c9452dbea4513519bf99b7ce23fe8b and mode 444 first; the binding is the law). Method: local files only, grep extraction, time-boxed, python3 for recomputes; read the CODE, not just the build notes. Verify: (1) manifest integrity — recompute the manifest SHA-256 (expect ff75636cf8fe14f14bcd35721491cbdf225d31d706325c114ecba4e91cf0dde2), count 60,308 unique URLs, and verify a 10-sample of URLs is constructed exactly from the harvested checksum-file listings per the binding's manifested-URL rules (no invented paths); (2) digest verification path — the code must verify each file's SHA-256 against the harvested digest BEFORE acceptance, with mismatch → quarantine + BLOCK_EVENT semantics per the binding; run the test suite yourself (expect 18 pass) and confirm the corrupt-digest fixture genuinely trips quarantine; (3) pacing and windows — implementation matches the binding's numbers (quote the binding clause next to the code lines); (4) prohibitions — grep-prove absence of recursion/wildcards/range requests/cutout-service calls/unmanifested URL construction; (5) resume idempotency — receipts.jsonl append-only, receipted files never re-fetched; (6) the execution clause — the binding's §5/§11 execution-approval requirements: rule explicitly whether ACQUISITION_PREAUTH_20260818.md (Duho's recorded conditional pre-authorization, both conditions since gated: CROSSCHECK_PASS + PASS_CROSSCHECK_GATE) satisfies the binding's approval requirement for execution, and state exactly what if anything is still missing; (7) no network calls occurred during build (spot-check for any receipts/logs indicating fetches). Write KUN_TRANSPORT_GATE_20260819.md, first line PASS_TRANSPORT_BUILD (with the execution-clause ruling inside) or HOLD_ + numbered repairs. Findings-only. Never fetch from portal.nersc.gov.
