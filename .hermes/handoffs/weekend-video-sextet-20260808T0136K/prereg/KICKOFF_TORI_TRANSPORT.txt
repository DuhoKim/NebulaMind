You are Tori, the build seat, on the pre-authorized image transport — the STOP-rule build. Working directory (absolute, all reads/writes): /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg . Both pre-auth conditions are gated: CROSSCHECK_VERDICT_20260819.md first line CROSSCHECK_PASS and KUN_CC_GATE_20260819.md first line PASS_CROSSCHECK_GATE — verify both yourself before writing a line of code. Then read ACQUISITION_PREAUTH_20260818.md and the FROZEN binding TORI_ROUTE_BINDING_SUCCESSOR_20260817.md (verify SHA-256 = 1371b11094a2765228a7deb1bbe1367117c9452dbea4513519bf99b7ce23fe8b and mode 444; the binding governs every behavior below — where this kickoff and the binding disagree, the binding wins).

Build `_tori_transfer_20260819/nm_image_transfer.py` modeled on the proven nm_checksum_harvest.py pattern: the 60,308 working-set image-r files (URLs constructed exactly per the binding's manifested-URL rules from the harvested checksum files' listings); per-file SHA-256 verified against the harvested digest BEFORE acceptance (mismatch → quarantine file + BLOCK_EVENT semantics per the binding); receipts.jsonl append-only state (resume-idempotent, never re-fetch a receipted file); the binding's pacing and window rules; heartbeat.json; nohup-detached run mode; disk-space preflight (~0.7 TB needed — check the target volume per the binding's storage clause and STOP with a report if insufficient). NO prohibited operations: no recursion, no wildcards, no range requests, no cutout-service calls, no unmanifested URLs. Include a test suite with a MockTransport-style local fixture (network code excluded from tests; a deliberate corrupt-digest fixture must trigger the quarantine path). Do NOT execute any network fetch — building and testing only; execution is dispatched separately after the gate. If the acquisition/ package's BUILD_ONLY_STOP guard is implicated, document the exact change in the build notes rather than deleting anything outside your transfer directory. Finish with TORI_TRANSPORT_DONE.md at lane root, first line TORI_TRANSPORT_BUILD_COMPLETE (with test results + preflight result) or TORI_TRANSPORT_HOLD with what blocked. Never fetch from portal.nersc.gov during the build.
