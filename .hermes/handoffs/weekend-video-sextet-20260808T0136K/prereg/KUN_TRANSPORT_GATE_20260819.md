PASS_TRANSPORT_BUILD — with the execution-clause ruling inside: EXECUTION IS NOT AUTHORIZED. ACQUISITION_PREAUTH_20260818.md does NOT satisfy the binding's §11 step-5 approval requirement (its two conditions are met — verified — but the step-5 approval record and its mandated contents do not exist). Missing before any image byte: M1 the §11.4d size sample (1,024 paced HEAD requests, receipted) deriving the approved byte ceiling; M2 the frozen, SHA-pinned, mode-444 Duho approval record naming the URL manifest hash, destination, exact file count, the approved byte ceiling with its sampling receipt, and the §5.4 pacing values restated; M3 a hash-pinnable coverage-census artifact (§11.4b) — substance exists, packaging does not; M4 a hash-pinnable digest-currency re-verification artifact (§11.4c) — same state; M5 a route-B custody receipt for the geometry sidecar (§4.3). Two named findings (F1, F2, §9) require Duho's disposition before the manifest is sealed; neither weakens any custody control, and this verdict moves no bytes.

# Kun transport gate — `_tori_transfer_20260819/` against the frozen successor binding

Kun (kimi gate seat), 2026-08-19, fresh one-shot. Findings-only. Method: local files only, grep extraction, python3 recomputes; the code was read in full (1,199 lines implementation, 414 lines tests), not just the build notes. Zero network use by this lane; portal.nersc.gov never contacted.

## 0. The law, verified first

- `TORI_ROUTE_BINDING_SUCCESSOR_20260817.md`: recomputed SHA-256 = `1371b11094a2765228a7deb1bbe1367117c9452dbea4513519bf99b7ce23fe8b` (matches the kickoff value exactly); mode `444` (stat `%Lp` = 444). The binding read in full; all clause citations below are to it.
- Build artifacts present: `nm_image_transfer.py` (SHA-256 `5c19cf3646cdaa201c759dcf71a0aae66d6e469bb5f773181749c04a5b5e6db9`, mode 755, 1,199 lines — matches build notes), `tests/test_nm_image_transfer.py` (SHA-256 `9608a915d7906bd21e08a9f82bd73081e66b486021afc312ab9fd8b019da7e57`, 414 lines — matches), `candidate_image_manifest.jsonl` + `.sha256` sidecar, `BUILD_NOTES_20260819.md`. Python byte-compilation re-run: OK.

## 1. Manifest integrity — PASS

- Recomputed manifest SHA-256: `ff75636cf8fe14f14bcd35721491cbdf225d31d706325c114ecba4e91cf0dde2` — matches the sidecar and the expected value.
- Population recompute (python3, full parse of all rows): 60,308 rows; **60,308 unique `source_url`s**; 60,308 unique bricknames; 60,308 unique survey digests; **0 template violations** — every row's `source_url` equals exactly the §4.1 template `https://portal.nersc.gov/cfs/cosmo/data/legacysurvey/dr10/south/coadd/<AAA>/<BRICKNAME>/legacysurvey-<BRICKNAME>-image-r.fits.fz` with `AAA = BRICKNAME[0:3]`; every row carries `release=dr10.1-latest-byte-bound`, `portal_host=portal.nersc.gov`, `product=image-r`, `coverage_class=required`, the §5.1 checksum-provenance record, working-set inclusion hash, format version, and creation time.
- 10-sample against the harvested checksum-file listings (first row, last row, 8 seeded-random rows): **10/10 PASS** on all six checks per row — the harvested `.sha256sum`'s `image-r` line digest equals the manifest `survey_sha256`; `checksum_source.sha256` equals the local harvested file's recomputed SHA-256; `checksum_source.bytes` equals its size; `checksum_source.url` matches the binding's checksum-file URL pattern; `source_url` matches §4.1; destination path matches. Bricks sampled: 0001m395, 3598m515, 1358m857, 0996m545, 2639m770, 2957m700, 0328m567, 0687m475, 3076m450, 0935m397.
- No invented paths, verified in code: `build_manifest_records` (nm_image_transfer.py:184-248) derives URLs only via `image_url(brickname)` (106-117, exact template, brickname regex-validated) and digests only via `_listed_image_digest` (172-181, requires exactly one `image-r` line in the harvested file); it re-verifies each harvested checksum file against its harvest receipt (`sha256_of_checksum_file`, `bytes_received`, 214-219) and requires the harvest receipt set to equal the working set exactly (245-246). Working-set input pinned: recomputed `workingset_bricks.csv` SHA-256 = `78ee99d6824bf4f5126b9ffd9eb622ad8201df2c64c3f232d99c1791b5f36b74` (matches the hard pin at line 34); 60,308 data rows.
- The manifest is correctly labelled `UNSEALED_BUILD_CANDIDATE` (line 1080) — derivation tooling output, not retrieval authority.

## 2. Digest verification path — PASS

Binding §5.2 (named equivalent): "`verify_checksum = true` → post-receipt SHA-256 of every file against the sealed manifest digest; mismatch terminal." §5.3: "local SHA-256 equal to the approved manifest digest" before acceptance.

- Code: after transfer, the staged `.part` file is hashed and fsynced (nm_image_transfer.py:951-952); **on mismatch the bytes are moved to `quarantine/`, a terminal `SHA256_MISMATCH` receipt is appended, `BLOCK_EVENT.json` is written, and the campaign raises** (953-967). Acceptance (rename `.part` → final staging path, 968) happens only after digest equality. No FITS is ever opened by the transport (§5.3's "FITS opened only after digest equality" is structurally satisfied — the transport opens none at all).
- Test suite run by this seat: `python3 -W error::ResourceWarning -m unittest discover -s tests -p 'test_*.py' -v` → **18 tests, 18 passed, 0 failures** (0.035s).
- The corrupt-digest fixture genuinely trips quarantine: `test_corrupt_digest_is_quarantined_and_blocks_campaign` serves `b"corrupt"` against the record's digest of `b"expected"`, and asserts `CampaignBlocked`, `BLOCK_EVENT.json` reason `SHA256_MISMATCH`, exactly one quarantined file containing the corrupt bytes, and no accepted staging file. Observed passing.
- §5.2's other two named equivalents: "`sync_level = checksum` → digest re-verification after any resume" — `_load_state` re-hashes every receipted file and blocks on mismatch or receipt contradiction (695-708); "`skip_source_errors = false` → no skip code path exists at all" — grep-proved: zero skip paths; a required-file failure terminates the shard (945-948).

## 3. Pacing and windows — PASS (binding clause ↔ code)

| Binding §5.4 (frozen values) | Code |
|---|---|
| "1. Concurrency: 1. Strictly serial; one connection; no pipelining." | `campaign_lock` exclusive flock, second process blocks (577-595); strictly serial `for record in records` loop (783), one fetch in flight at a time; no threads/async anywhere |
| "2. image files: next request no sooner than max(2.0 s after the previous request started, completion of the previous transfer)" | `PACING_SECONDS = 2.0` (44); `RateLimiter.before_request` enforces ≥2.0 s between starts (334-345); serial loop guarantees completion-before-next-start; wall-clock floor `last_request_epoch` persists the 2.0 s spacing across restarts via receipted timestamps (817-823, restored at 672-678) |
| "2. checksum/metadata files (~6 KB each): 1.0 s" | Not this build's scope — this transport fetches image files only; the checksum harvest ran under its own §11.4a lane (its receipts verified, §7) |
| "3. Bandwidth ceiling: 25 MB/s sustained." | `BANDWIDTH_LIMIT = "25000000"` (46), passed as curl `--limit-rate` (401) |
| "4. bulk retrieval only 20:00–08:00 US/Pacific on weekdays, any hour on weekends" | `in_window`: weekend → open; weekday → `hour >= 20 or hour < 8` (292-299); `seconds_until_window_close` closes weekends Monday 08:00 and weekday windows at 08:00 (302-317); curl `--max-time` receives the remaining open window (837-839); out-of-window the runner waits receipted as `PAUSED_WINDOW` (734-741) |
| "5. backoff ladder 30 s / 60 s / 120 s, then terminal for that file" | `BACKOFF_SECONDS = (30.0, 60.0, 120.0)` (45); transient attempts receipted with received-byte accounting before sleeping the ladder rung (928-944); ladder exhausted → terminal `REQUIRED_FILE_TRANSFER_FAILED` + block (945-948); non-transient HTTP failures never retried (test 16) |
| "6. STOP-ON-FIRST-BLOCK: the first 429, 403, block page, challenge, or rate-limit signal … a custody event to receipt, not something to retry around … resumption requires a human decision by Duho … never an automatic retry" | 403/429 → terminal receipt + block (883-887); 503 + `Retry-After` → `RATE_LIMIT_SIGNAL` block (888-892); `text/html`/`text/plain` content-type → `CHALLENGE_OR_BLOCK_PAGE` block (893-898); `_block` stamps `"resumption": "requires Duho's decision; never retry automatically"` (635-648); any existing `BLOCK_EVENT.json` refuses all resumption (689-690) |

§5.1.1.3 ceiling enforcement: remaining campaign byte authority is passed to curl as `--max-filesize` before every request (801, 834-836); cumulative received bytes ledgered across transient attempts (845, verified by test 12); crossing the ceiling → terminal `BYTE_CEILING_EXCEEDED` custody event (868-872); curl 63 (file exceeds remaining authority) → terminal `FILE_EXCEEDS_REMAINING_CEILING` (873-882). "The campaign is structurally incapable of exceeding what Duho authorized" — holds.

## 4. Prohibitions — PASS (grep-proved against §5 "Still forbidden")

- **Recursive retrieval / mirroring / crawling**: no recursion constructs; the only `rglob` is local extra-file detection in the destination (716). curl invoked per single explicit URL.
- **Wildcard expansion at execution time**: curl `--globoff` (396); `validate_manifest_record` rejects `*`, `?`, `[`, `]` in URLs (151-152); `_validate_runtime_url` re-validates every runtime URL against the exact template before any curl invocation (392, 497-512).
- **HTTP range requests**: no `--range`/`-r`/`Range` header anywhere (grep: only Python `range()` builtins and the local `rglob`); test 8 asserts their absence from the curl command; `--max-filesize` caps whole-file size, not a byte range.
- **Cutout-service calls**: zero hits; the only URLs constructible are the §4.1 portal image template.
- **Unmanifested URL construction / retrieval of a URL not in the approved manifest**: URLs are built only from the brickname via the frozen template; the runner fetches only `record["source_url"]` from a manifest whose SHA-256 is pinned at load (269-289) and re-verified against the approval/runner pins (760-762, 1096-1098); every record is field-validated (120-152).
- Additional shape facts: no Python network libraries imported (only `/usr/bin/curl` via subprocess); no redirect following (no `-L`; a 3xx is non-200, non-transient → terminal); no HEAD/`--head`/`-I` (the §5.1.1 size sample is not part of this build — see §8 M1); curlrc and proxy/cert environment explicitly disabled (`--disable`, `--proxy ""`, `--proto =https`, env scrub 422-427).

## 5. Resume idempotency — PASS

- `receipts.jsonl` is append-only: `append_jsonl` opens `O_APPEND`, writes, fsyncs file and directory (530-543). No rewrite path exists.
- Receipted files are never re-fetched: accepted bricks are skipped in the run loop (785-786); `test_receipted_file_is_reverified_and_never_refetched` proves a second run makes zero transport calls with exactly one receipt line — observed passing.
- Resume is re-verified, not trusted: every receipted file is re-hashed before being counted (695-708); on-disk tamper → `RECEIPTED_FILE_DIGEST_MISMATCH` block with zero fetches (test 4). Receipt-stream integrity: parse failure, non-monotonic cumulative bytes, duplicate/finalized brick, or a pre-existing terminal receipt all block (658-690). A changed manifest cannot reuse receipts (`campaign_binding.json` compare, 772-774; test 15). Unresolved in-flight state blocks resume (691-692).

## 6. The execution clause — RULING

**Binding law.** §11 step 5: "Retrieval approval — AMENDED: Duho explicitly approves the URL manifest hash, destination, exact file count, the approved byte ceiling with its sampling receipt (§5.1.1), and the pacing plan (§5.4 values restated in the approval record)." §1: "…Duho's explicit approval of the URL manifest hash, byte total, AND pacing plan (§11 step 5) are all mandatory before any retrieval begins." §11: "Until step 5, no image byte may be requested."

**What the pre-authorization is and satisfies.** `ACQUISITION_PREAUTH_20260818.md` records Duho's verbatim "pre-authorize the acquisition, run it after the cross-check passes", conditional on (1) harvest complete and (2) the known-issues cross-check passing. Both conditions hold, verified directly by this seat: `HARVEST_COMPLETE.json` = 60,308/60,308; harvest `receipts.jsonl` = 60,308/60,308 parse-clean `OK_CONFIRMED`, zero `BLOCK_EVENT.json`; `CROSSCHECK_VERDICT_20260819.md` first line `CROSSCHECK_PASS`; `KUN_CC_GATE_20260819.md` first line `PASS_CROSSCHECK_GATE`. The pre-authorization pins the binding hash, the route, the 60,308-brick working set, and per-file digest verification — and by its own text does not waive the binding's rules or this gate.

**Ruling: it does NOT satisfy §11 step 5.** It predates and therefore cannot name the URL manifest hash (manifest created 2026-08-19T07:27:24Z; pre-auth 2026-08-18); it names no destination; it contains no approved byte ceiling, because the §5.1.1 size sample that derives one has never run; and it does not restate the §5.4 pacing values in an approval record. It is a recorded conditional authorization of the lane — the human decision that permitted the harvest and this build to proceed — not the step-5 approval artifact the binding makes mandatory before retrieval. The two artifacts coexist by design: the pre-auth itself sequences "transport build + Kun gate → paced transfer", and the executable requires the step-5 record as a separate, frozen, SHA-pinned file (`load_approval`, 1028-1069).

**Exactly what is still missing before any image byte (all five required; none waived by this verdict):**

- **M1 — the §11.4d size sample.** 1,024 manifest-listed image-URL `HEAD` requests, stratified across the `AAA` keyspace, paced per §5.4 tier-2, receipted, transferring zero body bytes, deriving `approved_byte_ceiling = sample mean × 60,308 × 1.25` (§5.1.1.2). Not built, not run. (It is the only image-URL operation permitted before step 6, and it is not part of this transport build — a separate gated operation is needed.)
- **M2 — the §11 step-5 approval record itself.** Frozen (mode 444), SHA-pinned, Duho's, naming: status, the URL manifest hash `ff75636cf8fe14f14bcd35721491cbdf225d31d706325c114ecba4e91cf0dde2` (or its sealed successor), destination, exact file count 60,308, the approved byte ceiling with the M1 sampling receipt pinned, and the §5.4 values (concurrency 1; 2.0 s image spacing; 25,000,000 bytes/s; weekday 20:00–08:00 US/Pacific; weekends any hour) restated. The executable additionally requires `kun_transport_gate` and pinned SHA-256s for the size-sample receipt, the coverage census, and the geometry-sidecar receipt (1056-1068). No file anywhere contains `APPROVED_FOR_IMAGE_RETRIEVAL` except the transport source itself (grep-proved).
- **M3 — packaged coverage census (§11.4b).** Substance exists and is clean: all 60,308 harvest receipts carry `image_r_listed: true` (incomplete count zero, so nothing position-correlated to assess), disclosed in `CROSSCHECK_VERDICT_20260819.md`. But no standalone census artifact exists whose SHA-256 the approval record can pin.
- **M4 — packaged digest-currency re-verification (§11.4c).** Substance exists: the cross-check censused all 60,308 harvested checksum Last-Modified values (397 replaced bricks at 26 Jul 2023; 59,911 controls Nov 2022; zero exceptions) on the tree as harvested through 2026-08-19T05:10:02Z — contemporaneous with manifest time. Not yet packaged as a pinnable artifact.
- **M5 — geometry-sidecar custody (§4.3).** No route-B receipt exists for `survey-bricks-dr10-south.fits.gz` bound by its survey-published digest. (The only local copy is a 2026-08-14 static evidence file from another lane — not route-B custody.) The approval record cannot be completed without it.

Until M1–M5 exist and this gate's pass is recorded inside M2, the executable cannot be armed: `run`/`launch` fail closed before constructing any transport — frozen gates re-verified (95-103), exact execution acknowledgement required (1092, 1126), approval file SHA-256 + mode 444 + every field enforced (1099-1106, 1133-1140), disk preflight before transport construction (1107-1109). Observed: `test_run_refuses_before_manifest_read_without_exact_execution_ack` passing.

## 7. No network calls during build — PASS

- `_tori_transfer_20260819/` contains exactly five artifacts: implementation, tests, manifest, sidecar hash, build notes. No `receipts.jsonl`, `heartbeat.json`, `BLOCK_EVENT.json`, `inflight.json`, campaign directory, detached log, or `transfer_stdout.log` (find-proved).
- No live curl/harvest/transfer processes (ps-proved).
- Code confirms no network at import, `build-manifest`, or `preflight`: the only network path is `CurlTransport.fetch`, reachable solely through `run`/`launch` after the approval record loads; the test suite uses `MockTransport` exclusively (its own docstring: "It never opens a socket").
- `acquisition/` untouched (mtimes 2026-08-14); `BUILD_ONLY_STOP` guard intact.
- The harvest lane's network use (60,308 checksum GETs, 2026-08-17→19) was separately authorized under §11.4a and is receipted in `_tori_harvest_20260817/` — not this build's act, recorded here for completeness.

## 8. Verdict scope

PASS_TRANSPORT_BUILD covers: the transport implementation, its test suite, and the candidate-manifest derivation integrity, against every custody clause of the frozen binding examined. It does **not**: seal the candidate manifest; constitute or substitute for the §11 step-5 approval; authorize the size sample, the sidecar retrieval, or any image byte; or amend the binding. Execution remains blocked on M1–M5 (§6) regardless of this pass.

## 9. Named findings for Duho's disposition (non-blocking to this verdict; blocking to sealing)

- **F1 — `reason` field vocabulary.** Every candidate row carries `reason: "working-set intersecting source"`; binding §5.1 enumerates `primary brick | edge neighbour | corner neighbour | geometry sidecar`. The primary/margin split is retained in no evidence artifact (grep-proved across the workingset evidence; position files were deleted under the deletion rule by design), so the binding's vocabulary cannot be populated from retained evidence. Custody impact: none — every brick is uniformly `coverage_class=required` and terminal-if-missing; the field is bookkeeping. Options: (a) Duho records acceptance of the uniform truthful reason as a deviation at the manifest gate; (b) a successor amendment restates the field. Not repairable by re-pulling positions — that path is forbidden.
- **F2 — geometry sidecar is not a manifest record here.** §4.3 makes the sidecar's digest/size/URL/local-SHA "mandatory manifest fields", and §5.1 contemplates a `geometry sidecar` product record. This build's validation fixes `product=image-r` and count=60,308 (127-138, 1097-1098), so the sidecar can never travel in the executable manifest; the build instead requires a pinned `geometry_sidecar_receipt_sha256` in the frozen approval record (1064) — a different, arguably stronger binding point, but not the binding's letter. Options: (a) extend the manifest format to carry the §4.3 sidecar record (small, mechanical); (b) Duho records the approval-pin design as the accepted disposition. Decide before the manifest is sealed.

## 10. Gate-lane receipt

- portal.nersc.gov contacted: 0; image bytes: 0; network calls of any kind by this lane: 0.
- Recomputes: binding SHA-256 + mode; manifest SHA-256; full-population URL/template/digest counts; 10-sample harvest cross-check; working-set SHA-256; both code-artifact SHA-256s; harvest receipt census (60,308/60,308 parse-clean); test suite executed (18/18); py-compile.
- Reads: binding (full), implementation (full), tests (full), build notes, pre-auth, crosscheck verdict, DONE marker, kickoff, workingset record.
- Binding, frozen prereg, harvest, acquisition package: unmodified. Writes by this lane: this report only.

— Kun (kimi gate seat), 2026-08-19. Findings-only; Kun rules, Duho decides.
