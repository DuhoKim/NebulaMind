# Yui — BS-3 production identity receipt, promised 1,000 probes

**Run:** 2026-08-14 KST  
**Slot:** BS-3, identity-count assembly repair only  
**Verdict:** **PASS — production identity is bit-exact 1,000/1,000.**

This is the exact production witness required by the binding-slot validity text in `PREREG_LONGO_AMPLITUDE_TEST_20260812.md:221`. It resolves the discrepancy Kun identified at `KUN_REGATE_BS1_BS3_20260814.md:114-119`: the slot promised 1,000 production probes, while the landed R1/R2 production witnesses had only 200.

## 1. One-shot result

The frozen primary estimator was evaluated once on the deterministic 1,000-probe extension of the already-landed production probe prefix.

| Test | Result | Validity requirement |
|---|---:|---:|
| R1, `mirror(mirror(x)).tobytes() == x.tobytes()` | **1,000/1,000** | 1,000/1,000 |
| R2, `chi(mirror(x)) == -chi(x)` by float32 value | **1,000/1,000** | 1,000/1,000 |
| R2, float32 bit pattern of `chi(mirror(x))` equals that of `-chi(x)` | **1,000/1,000** | 1,000/1,000 |
| Nonzero R2 cases | **1,000** | recorded |
| Zero R2 cases in the 1,000-probe grid | **0** | recorded separately |
| Maximum `abs(chi(mirror(x)) + chi(x))` | **0.0 exactly** | 0.0 |

**BS-3 identity validity: PASS.** No failed probe was dropped, replaced, or rerun. No alternative probe set was tried.

## 2. Probe custody

- source indices: **3,000,000 through 3,000,999**, inclusive;
- N = **1,000**;
- master seed: `LONGO-AMPLITUDE-FREEZE-M1`;
- seed per source index: `SHA-256(M || index) mod 2^63` as implemented by the landed production runner family;
- raster and inference dtype: **128 x 128 float32**;
- pure mirror: width-axis pixel-index reversal only;
- 1,000-image manifest SHA-256: `35d679d4955d3657866bd64fe309a9a42b30ff8a61d1952d2a3795ee59231024`.

The first 200 probe hashes reduce to `ab75d5f2ec08ad44fbcf1198d1612c23759f8d3aac29db044a181346ac43f9b2`, exactly matching `YUI_BS3_R4_R5_RECEIPTS.md` and `prereg/yui_bs3_r4_r5_20260813/results.json`. The 1,000 test is therefore an extension of the landed production population, not a substituted passing set.

The synthetic-only row receipt is `prereg/yui_bs34_20260814/identity_probe_records.jsonl`, 1,000 rows, SHA-256 `b5ddea6397947f74156cdb9bf62038d72f983b765b4ba54a8e00052eb6550380`.

## 3. Frozen production estimator

Direct file hashes measured before and after the run:

| Frozen object | Directly measured value |
|---|---|
| Generator `spike/yui_identity/w_chi.py` | `89da33ec6260e75e06eadb0f171da4c52f1478b59ff5e543d363dbf56fefcd75` |
| Frozen weights file `prereg/weights_frozen.pt` | `83008c1cbdae511af5d30020540e1e281c62c2bd95d3cb05527fc0687bf49e6d` |
| Canonical little-endian float32 parameter serialization after strict load | `1075a4d91c295d7f3256128534a0b8c4d097fb9d162169df1ac698843637a589` |
| Frozen primary tau | `4.4006456017494235` |
| Null-set manifest, N = 8,000 | `1963132f2f36e7aa42b08012aad02d2c541d6c0973740a5bbce6a6e7a2904bd1` |
| `prereg/train_results.json` | `c36cd33001e432c60df786da8c0ff95b8ef5ab350a458b29d71ff084178a41fd` |
| `prereg/receipt_results.json` | `d5d4a8bc005b031ed523e64a672237536896f37030722fd5cf71ff44a3405a04` |
| Corrected-retention result | `414fbc5cb6fa050390f0a6bca69e02e81795ed2a3585928be19767f4cb3a59e2` |

The model was loaded strictly from the frozen state dictionary. `yui_train_measure.py` was not imported because importing it would retrain. There was no retraining, re-export, fine-tuning, recalibration, tau change, training-set regeneration, null-set regeneration, or probe retry.

## 4. Signed-zero rule carried

The exactly mirror-symmetric primary probe produced `chi = +0.0`. Its mirrored value had float32 bits `0x00000000`, while `-chi` had bits `0x80000000`. They were value-equal and bit-different, and the ordered acceptance test `abs(chi) > tau` returned false. This is the expected R3 edge rule, not an identity failure in the 1,000 nonzero production probes.

## 5. Remaining BS-3 receipts carried without reopening design

The previously landed machine and prose receipts remain pinned:

- production R4/R5 result `prereg/yui_bs3_r4_r5_20260813/results.json`, SHA-256 `bb4eef8798893a0ce8e06c09768e20d683ca771c5b7dfb396fd7747a86efea78`;
- production paired rows `prereg/yui_bs3_r4_r5_20260813/paired_probe_records.jsonl`, SHA-256 `8f8b973abe4685f76049de16b907e16bbc8e3be7c9061f7d349b655bd55b7e82`;
- prose receipt `YUI_BS3_R4_R5_RECEIPTS.md`, SHA-256 `fd2fdc07143dc9e5d38d0c21b5639aab89d8fa67b6964c7e73bfdbebe9c20b44`.

Those receipts report the exact production path, R4 canary detection, and R5 `dA_raw = +0.015`; this 1,000-probe run repairs only the R1/R2 count named by the slot table.

## 6. Operative retention and slot arithmetic

The operative primary retention is the full admitted-range measurement, not the old 0-60 degree result:

- accepted: **10,349/12,000**;
- central retention: **86.24%**;
- one-sided lower 95% bound: **85.72%**;
- support: uniform in `cos(i)` through 69.3 degrees, covering the inclination range admitted by `b/a > 0.4`.

The older **96.44% / 96.15%** numbers sampled inclination only over 0-60 degrees and are not operative.

For the slot's arithmetic check only, Kun's aggregate re-gate records a frozen post-Cut-6 parent count of **832,393** at `KUN_REGATE_BS1_BS3_20260814.md:25-33`. Multiplying that aggregate by the measured lower bound gives:

`832,393 x 0.8571626782674123 = 713,496.2132510461266339`, or **713,496** after conservative flooring.

That exceeds the BS-3/BS-1 threshold of 100,000 by **613,496**. This arithmetic does not close BS-1's separate licence or route-assembly items.

## 7. Machine artifacts and independent verification

Workspace: `prereg/yui_bs34_20260814/`

| Artifact | SHA-256 |
|---|---|
| `run_bs34.py` | `264ff3b7a3904baea1e022433645da535cef10a6a7b22aaa39e92c4947cc9f8f` |
| `test_run_bs34.py` | `784febfe6eb9d7a3b915048e543211e019252d66611083b14fb4399576286b27` |
| `results.json` | `cfd11391f123e0caa054f0a3bdfab76b20eb7293c457bafd70563e90af07df33` |
| `identity_probe_records.jsonl` | `b5ddea6397947f74156cdb9bf62038d72f983b765b4ba54a8e00052eb6550380` |
| `independent_verify.py` | `75e6af0cecb7f63fdf9b835891bf02c6fd222b070e73a2b6d743e74db666c9e4` |
| `test_independent_verify.py` | `83dff437d512d048ee4b4b335cbd2d515df1145a4316ec9670697e03e0ddd83d` |
| `independent_verification.json` | `2a51aed8307e9ab3140d91eb74fdd2b0b973bb85d7c5db11157be23d5efcfeb3` |
| `run_stderr.log` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` (empty) |
| `independent_stderr.log` | `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` (empty) |

Focused tests: **5/5 PASS**. The independent verifier imported neither the model runner, training code, Torch, SciPy, nor the secondary algorithm. It reparsed all 1,000 records, verified the exact source-index sequence and probe manifests, recomputed bit-level identity from serialized float32 outputs, rehashed every frozen file, and returned `PASS_INDEPENDENT_BS34_REDUCTION`.

## 8. Boundary

No survey data, real-galaxy row, coordinate, real image, morphology/chirality label, or sky statistic was read or computed. Nothing was tuned, accepted, frozen, published, committed, or pushed. This receipt supplies the missing BS-3 production identity witness for Kun's re-gate; **Duho owns acceptance and Kun gates the slot.**
