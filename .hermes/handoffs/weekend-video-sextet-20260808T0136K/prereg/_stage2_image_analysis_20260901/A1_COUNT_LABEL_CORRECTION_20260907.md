# "122 CONSUMER CHECKS" WAS AN AGGREGATE WEARING A SUITE'S NAME (2026-09-07 18:44 KST)
Review 10 verified against the FILES this time, as instructed, and found two real problems. Both are mine.
**1. The count label.** Measured with the loader from the lane root: `test_core_consumer` **32**, `test_run_path` 42, `test_medium_perturbation` 32, `test_readiness` **14**, `test_evidence_gate` **5**, `test_select_sample` 16 (from its own directory), `test_verify_drand_v2` 4. The sheet said "**122 consumer checks**". 122 was the AGGREGATE of the whole agreement-run set at the v46 run, and I carried it forward as though it were one suite's total — then repeated it in later briefs, where it was copied rather than checked. Corrected to the measured per-suite figures, including the two suites the sheet had never listed at all (readiness 14, evidence-gate 5).
**2. A1's 28.** A1 stated "28 real entries" in the present tense, attributed to a 17:15 update with digest `cdac63a5…`. Review 10 is right that a reader would take it as current. It is now marked **HISTORICAL** in place, naming the current manifest `ec2e5158…` and its 30 / 5 — recorded, not rewritten.
**Where the reviewer earned it.** Reviews 8 and 9 confirmed the counts from prose and missed both. Review 10, given the artefacts and forbidden to echo, checked seven separate numbers against their sources and caught the one that was wrong — while confirming the six that were right (eligible 11,837, failed set 2,000, exclusions 2,644, post-exclusion 7,283, and the run-path/MEDIUM/selector/BLS counts). **The difference was not the reviewer; it was the brief.** A review that is handed the document and asked whether it is consistent will find prose consistent with prose.
Both changed artefacts go back for review: the sheet (`9d6ed807…`) and A1 (`d2049709…`). A1's bytes changed, so its earlier CANDIDATE-SOUND no longer covers it, and the assembler will say so.
