# OWNER DECISIONS ON A1's OPEN QUESTIONS (2026-09-07 12:11 KST)
The open-questions worker read the ORIGINAL documents, not the derived extracts (Blanc 11:48), and returned 4 ANSWERED, 3 NEEDS-OWNER, 1 UNCOMPUTABLE (`A1_OPEN_QUESTIONS_RESOLVED_20260907.md`). The four answered stand on their cited sources. Here are the owner decisions on the rest.

**Q4 — BLS verification vs two-host agreement. A DISCREPANCY IN A1, and it is resolved AGAINST the current text.** The worker, reading Duho's original drand-only direction and the relay, finds it requires ACTUAL BLS SIGNATURE VERIFICATION against the pinned chain identity. A1 line 36 currently accepts "identical 64-hex randomness from at least two of `api.drand.sh`, `api2.drand.sh`, `api3.drand.sh`, `drand.cloudflare.com`, confirmed by live re-fetch" — that is V15's two-host rule, not BLS. **DECIDED: the named round's randomness must be BLS-verified against the pinned chain public key, AND at least two pinned hosts must agree — the second is an additional check, not an alternative.** The verifier already exists and is tested (`_optionA_dev/drand_only/verify_drand_v2.py`, 4 tests); the cost is one dependency (py_ecc) in the run path. Reason: the seed is the one safeguard that cannot be weakened, and agreeing hosts prove agreement, not authenticity.

**Q5 — which rendering/gate artifacts belong in the manifest. DECIDED, by the narrowest rule that is checkable:** the manifest lists EXACTLY the files the run path reads — the eligible-id file, the exclusion file, the derived failed-set file and the CSV it came from, `select_sample.py`, the estimator identity (family file + configuration grid + the frozen winner's configuration) and the drand verifier — each with its SHA-256. Nothing that the run path does not read goes in the manifest; anything the run path does read and is not in the manifest is a defect, and that is the test.

**Q6 — how a render-refused identity reaches scoring. DECIDED: by an EXPLICIT STATUS FIELD, never a sentinel value in the data.** A render-refused identity is carried as `status = RENDER-REFUSED` with its cause, is UNSCORED, and counts as a MISS in the fixed denominator — which is V15's existing rule, unchanged. Reason: a magic tensor value is indistinguishable from a real measurement to every downstream consumer; a status field cannot be silently averaged.

**Q8 — the seal-helper status contradiction. DECIDED: MOOT FOR THIS RUN, and recorded rather than reconciled.** A1 defers the seal-append helper and step 2 entirely, so no clause of this run depends on whether the helper was written and tested. Both original statements stay exactly as filed; neither is edited to agree with the other. If the helper is ever brought back into scope, the contradiction must be reconciled THEN, from the artefacts.

**Q1 — UNCOMPUTABLE, and it stays open honestly.** The renderable population within the 7,410 post-exclusion identities cannot be counted without catalogue inputs this run deliberately does not read. It is not needed before the seed is named: the manifest fixes the eligible-id FILE, and its length is the population. The number will be reported from that file when it is fixed — it is not guessed here.
