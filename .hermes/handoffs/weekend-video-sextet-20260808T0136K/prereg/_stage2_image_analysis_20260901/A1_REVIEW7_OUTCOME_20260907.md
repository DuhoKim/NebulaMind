# CANDIDATE BYTES — **VERDICT: CANDIDATE-NOT-SOUND** (2026-09-07 18:28 KST). A1 is true; the sheet Duho reads is stale.
Reviewer agy / Gemini, ACCESS PROVEN for A1 `2c8f31828fa13de7…` — the gap that remained. Exit artefacts first: stderr 0 bytes, stdout 2,037 bytes, rc 0.
**A1 itself passed on every count**: its claims about the runtime representation, byte binding, obligations and manifest are true of the implementation as it stands; the TOCTOU limitation is restored verbatim and neither hidden nor widened; the obligation still requires byte binding for import artefacts AND shared-cache images and the document's claim that it is met by streaming SHA-256 over every byte of the cache files is supported by the V50 evidence and review 6; nothing in it contradicts the implementation.
## THE FATAL — the sheet still says the work is undone
`AGREEMENT_RUN_DECISION_SHEET_FOR_DUHO_20260907.md:11–12` reads "Runtime representation remains current preparation work to be done, not later-stage evidence." A1 line 57 says the opposite, and A1 is the one telling the truth. **A reader who has not followed today would conclude the critical runtime binding is still missing.**
**Why this matters more than its size.** The sheet is the ONLY document written for Duho. Everything else in this package — A1, the registers, the reviews — exists so that one page can be trusted. A package can be entirely sound and still mislead the person it is for, and that is exactly the failure here: the technical documents are accurate and the human-facing one is not. I have been refreshing the sheet all day and let it fall behind the very repair that closed the last obligation.
## DISPOSITION
The sheet is corrected to describe THIS candidate. **The decision sheet is added to the assembler's GOVERNING ARTEFACTS**, because it is the artefact presented — checking A1 and the code while leaving the presented page unchecked is precisely the gap this review found. Then one focused review of the corrected sheet. Nothing is presented until it passes.
