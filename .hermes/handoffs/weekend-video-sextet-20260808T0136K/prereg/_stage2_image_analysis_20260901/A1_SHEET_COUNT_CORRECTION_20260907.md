# THE SHEET SAID 28; THE MANIFEST SAYS 30 (2026-09-07 18:37 KST)
Measured, not inferred: `INPUT_MANIFEST_A1_CORE.json` (SHA-256 `ec2e51586d5664cded303112a02b7f1bc55fa114d3ede753fb390d70cfc67fbe`) holds **30 entries in `files`, every one `status=REAL`**, and **5 later-stage placeholders**. I also read the placeholders array rather than assuming its length. The sheet's line 11 asserted "28 real entries" in the PRESENT TENSE and is corrected to 30 / 5, now quoting the manifest digest so the number can be checked against the file instead of taken on trust. A1's 28 is explicitly attributed to an earlier owner update and an older digest; it stays as history and is not the current claim.

## THE PART THAT MATTERS MORE THAN THE NUMBER
**Reviews 8 and 9 stated that the sheet's counts match exactly. They do not.** Both reviewers read the count in the sheet's own prose and confirmed it against the surrounding prose — not against the manifest. That is the same defect this package has been finding all day, this time inside the review itself: **a check that confirms a number by reading the document that states it has verified nothing.** Their positive verdicts stand as reviewer records on the questions they actually examined; this particular claim was not among them, and I am not going to describe their findings as independently verified when a deterministic check found a real discrepancy they both passed over.
Consequence for the next review: the focused sheet re-review is given the MANIFEST AND ITS DIGEST and asked to verify the 30-entry claim **against the file**, explicitly forbidden from echoing the sheet's prose back.
Not repeated: the A1 and implementation reviews. Their bytes are unchanged, and a current-state count in the sheet is no reason to re-run them.
