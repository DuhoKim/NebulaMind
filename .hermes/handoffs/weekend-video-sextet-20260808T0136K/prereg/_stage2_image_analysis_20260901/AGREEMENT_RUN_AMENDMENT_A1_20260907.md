# Agreement run — narrow amendment A1 proposal, 2026-09-07

Authoring proposal only; no execution authority. Amends signed V15, SHA-256 `fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1`. Its historical draft heading does not change the signature recorded in the run record.

**Purpose.** Obtain one agreement number between machine chirality classifications and human GZ1 classifications on unseen images, with a Wilson interval and lower bound against 0.70. V15's orientation-agnostic convention remains: agreement allows a global sign reversal, so this is not necessarily agreement with a preassigned clockwise sign. Report that limitation with the number.

**Departures from the adopted plan — what the reader loses.** Replace V15 §3b's NIST-primary/24-hour-fallback path with drand only, and replace its signature-relative clock with a procedure/input-commit-relative clock; NIST authentication and fallback availability are given up. The exposed V15 round 6440756 cannot seed this successor. Replace E3/§7's prescribed builder/driver, chained seals and pushed-witness enforcement with digest-fixed selection, ordinary records and explicit access restrictions; the reader loses automated reconstruction/refusal coverage and independently witnessed chronology. Defer the additional machinery named below, and replace §9/§10's repeated two-seat package gates for this amendment with one independent review; this author supplies no verdict. Existing custody, pipeline restrictions, finite search and one-attempt limit remain. No unseen data access is authorized by this document.

**Eligibility.** Start with V15's 12,054-object guarded pool (`guarded_pool.csv`, SHA-256 `2cc94a29562270fcb5043f4ce942e303696f359b5fb0c59fdee48578ebb34155`). Exclude the 2,000 failed identities in `VALIDATION_SELECTION_V29_20260905.csv` (V15 pin `5643555c…`) and all 2,644 identities in `_optionA_dev/corpus_identity/dryrun_identities_to_exclude_20260905.txt`, SHA-256 `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95`, including its 44 skipped identities. These disjoint historical rank blocks leave **exactly 7,410 identities before renderability filtering**; check membership, uniqueness and exclusion disjointness against the committed inputs.

An identity is renderable only if its §9B.2b brick exists under the binary64 half-open test in the pinned DR9-north survey-bricks table (SHA-256 `2edd5c295fdad26852c6f224a3ff023cff43dd0e03a53acd35b767e726ee72fb`) and is absent from `validation_bricks/_bricks_without_r_coverage.txt` (SHA-256 `ba2eb9d16d0d1d47eef2e0d52497b56d44ac979ebe67dd54b33f57c117d7a2fe`). The latter excludes bricks without published r-band images. No pixels or checksum-catalogue membership decide eligibility. The eligible population is exactly this renderable subset of the 7,410; its numerical count is not supplied in these sources and must be recorded catalogue-only before the input commit.

**One selection rule.** Interpret SHA256 as an unsigned big-endian integer. Order the post-exclusion identities by `h' = SHA256(lowercase seed hex || "||" || ASCII decimal GZ1_OBJID) mod 2^32`, breaking ties by numeric GZ1_OBJID. Walk from rank 1, listing and skipping non-renderable identities; take the first 400 for tuning, next 200 for holdout, next 2,000 for fresh validation. All three sets are disjoint by construction. **Sizes 400/200/2,000, scored floors 380/190/1,900 and the strict Wilson lower-bound bar > 0.70 are CARRIED OVER UNCHANGED from V15.** Never replace an object because its raster or score fails. Preserve ordered identities, skipped ranks, seed, input digests and selection-code digest before any development fetch or label access. Six selection checks suffice: fixed-seed determinism, disjointness, exclusions, exact sizes, refusal of reuse across draws, and refusal of missing eligibility inputs.

**Prospective seed, in minutes.** After the separate amendment decision, fix the procedure and every input: final text, selection implementation, eligibility catalogues and exclusions with full digests, rendering/pipeline and estimator family, environment, 96-value search protocol, scoring/gate implementation, and drand chain metadata, relay/authentication rules and round-time calculation. The lane owner commits their SHA-256 manifest and preserves commit identifier and UTC time C. No seed is obtained during this authoring task.

After that commit, name and record the first default-mainnet drand round with scheduled time at least C + 600 seconds: using V15's genesis G = 1595431050 and period P = 30 seconds, `r = 1 + ceil((C + 600 - G)/P)` and `T = G + (r - 1)P`. Record r, T and the commit identifier before T exists, with a contemporaneous UTC record; if that deadline is missed, stop and record the abort, never silently substitute another round. Ten minutes gives time to record the completed commit and round designation and retains V15's ten-minute prospective separation; it is an operational margin, not proof of clock honesty. There is **no 24-hour wait**: that was solely the removed NIST fallback window.

Retain raw responses and accept only the named round with identical 64-hex randomness from at least two of V15's four pinned HTTPS hosts (api.drand.sh, api2.drand.sh, api3.drand.sh, drand.cloudflare.com), confirmed by live re-fetch; lowercase that randomness as seed. This retains V15's hostname-agreement authentication, not BLS verification or proof of operator independence. Unavailability means wait for this same round or record abandonment, never switch source or round. Whether the later drand-only direction requires the exhibited BLS verifier is an unresolved input-contract question that must be settled before C; the source choice itself is settled.

**Separation and one opening.** Keep V15 E1–E8's scientific restrictions, §6 pipeline, §7's fixed 96-configuration tuning search, objective `max(k,m-k)/400`, floors, tie-break and complete configuration records. Freeze by digest the selected estimator code, configuration, preprocessing, environment and tuning results **before holdout access**. “Opened” means the first access by any selection participant or their process/service to a holdout pixel, tensor, label or per-object outcome; mere catalogue identities are not opening. Record that access before it starts; a partial run or crash after access consumes the opening. Evaluate only the frozen winner, once: require m ≥ 190 and Wilson lower bound of `max(k,m-k)/200` > 0.70, with z = 1.959963984540054. No retuning or second opening. Preserve fresh-pixel custody and fetch-after-freeze/holdout-PASS restrictions; then evaluate fresh validation once. V15 §5's validation denominator is only incorporated by reference and is unresolved in these inputs: settle and digest the exact gate before C, without changing its 1,900 floor, bar or orientation convention.

**Attempts and failure, fixed beforehand.** Keep attempt 1, both step-1 collections, exposed round 6440756, fixtures, corrections and chronology intact. Register every draw, abort and re-draw with UTC, reason, governing digests, seed/round, access stage and outcome; a register does not authorize another draw. Exactly one further validation attempt remains; no secondary, no best-of-many and no unregistered restart. A seed/deadline/input mismatch, exclusion/reuse/overlap, premature access, changed frozen identity or repeat holdout invalidates this run and stops it. No eligible tuning winner, holdout floor/strength failure, validation m < 1,900 or validation Wilson lower bound ≤ 0.70 closes option A. Ties, non-finite/missing/error/non-repeat scores remain unscored. Report failures and unscored counts, not only successes.

**Deferred means OUT OF SCOPE FOR THIS RUN, not deleted or disproved.**
- Provenance/witness stack (GitHub PushEvent witness, history continuation, per-entry publication, receipts and Option-A′/B receipt identity): hostile-third-party chronology proof is not needed to compute this comparison, at the cost of relying on honest records.
- Composed provenance mode: combining those evidence paths is unnecessary for the agreement calculation.
- NSD/precedence/input-boundary refusal apparatus: adversarial loader hardening is unnecessary for this fixed-input comparison.
- The 132 differential controls, table verification and fail-first kit as run-gating machinery: they test the deferred framework; retain the six selection checks above.
- `verify_split`: deterministic selection and retained identities provide the necessary recomputation without that helper.
- `holdout_once` flag machinery: the explicit one-opening restriction and access record suffice here, with less automated enforcement.
- Seal-append helper: ordinary digest records suffice here without chained-seal append enforcement.
- Step 2 as designed: its seal/witness orchestration is unnecessary for recording the deterministic identity; its identity-before-access obligation remains.
