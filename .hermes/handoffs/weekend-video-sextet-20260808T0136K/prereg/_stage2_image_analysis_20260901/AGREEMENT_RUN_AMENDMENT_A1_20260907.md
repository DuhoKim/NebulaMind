# Agreement run — narrow amendment A1, 2026-09-07

**UNADOPTED PROPOSAL, returning for ONE independent re-review by a seat that did not author it.** Authoring only; no execution authority, draw, split or unseen-data access. Proposes amendments to V15 (`fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1`); its history remains intact.

**Purpose.** One machine–human GZ1 chirality agreement number on unseen images, with a Wilson interval against 0.70. V15 allows global sign reversal: this does not establish agreement with a preassigned clockwise sign.

## Input manifest — full SHA-256 or explicit unresolved placeholder

`UNFIXED_*` names denote missing values, never usable defaults; resolve before commit C. The manifest lists exactly the files the run path reads, each with SHA-256: any file read but absent is a defect; files not read do not belong. Include the eligible, exclusion and derived failed-ID files and source CSV, `select_sample.py`, estimator family file, configuration grid, frozen winner configuration and drand verifier. Expand runtime placeholders into individual files actually read, not aggregate digests. Producer-only catalogue provenance stays outside this manifest. Source: `A1_OWNER_DECISIONS_20260907.md` Q5.

| File / file placeholder | SHA-256 / digest placeholder |
|---|---|
| `UNFIXED_ELIGIBLE_IDS_PATH` | `UNFIXED_ELIGIBLE_IDS_SHA256` |
| `_optionA_dev/corpus_identity/dryrun_identities_to_exclude_20260905.txt` | `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95` |
| `UNFIXED_FAILED_IDS_PATH` | `UNFIXED_FAILED_IDS_SHA256` |
| `VALIDATION_SELECTION_V29_20260905.csv` (failed-ID provenance) | `5643555c75670a695cd9144455a956440125c1019ebd1fb028a7ee21889014c7` (documentary pin; current-byte verification pending) |
| `select_sample.py` | `UNFIXED_SELECTION_CODE_SHA256` |
| `UNFIXED_RENDERING_PIPELINE_ARTIFACT_PATHS` | `UNFIXED_RENDERING_PIPELINE_ARTIFACT_SHA256S` |
| `UNFIXED_ESTIMATOR_FAMILY_GRID_AND_FROZEN_WINNER_PATHS` | `UNFIXED_ESTIMATOR_FAMILY_GRID_AND_FROZEN_WINNER_SHA256S` |
| `UNFIXED_ENVIRONMENT_PATHS` (run environment includes `py_ecc`; version/pins unresolved) | `UNFIXED_ENVIRONMENT_SHA256S` |
| `UNFIXED_SCORING_ADAPTER_PATHS` | `UNFIXED_SCORING_ADAPTER_SHA256S` |
| `miniprereg_pins/validation_gate.py` | `65e241cad76b6150f625ac5aad085ed528004153d8590f8c32b07895c0c75e5b` |
| `_optionA_dev/drand_only/verify_drand_v2.py` | `UNFIXED_DRAND_VERIFIER_SHA256` |

CSV and gate pins: `MINI_PREREG_GZ_TIERC_DRAFT_V35_20260905.md`, “Pinned artefacts of this clause” (line 474) and §9B.7a, as cited in `A1_OPEN_QUESTIONS_RESOLVED_20260907.md` Q2; the gate hash was checked there. The derived failed-ID digest remains separate. Bind final procedure bytes in C independently of the runtime inventory.

**Eligibility as input.** Per `AGREEMENT_RUN_INPUT_CONTRACT_20260907.md`, selection consumes three pinned one-ID-per-line files. Eligible IDs are ascending and unique; failed IDs are the CSV’s GZ1_OBJID column, sorted ascending and deduplicated, with derivation recorded before C; the dry-run exclusion file retains its pin. Every selection input is passed with its expected digest, recomputed on read; absence, malformation or mismatch refuses, with no default or optional digest. Exclude the 2,000 failed identities and all 2,644 dry-run identities, including 44 skips; verify membership, uniqueness and exclusion disjointness. The guarded 12,054 leave exactly 7,410 before renderability; the eligible-ID file’s length IS the population and will be reported when the manifest is fixed, before C; no numerical renderable count is established here (`A1_OWNER_DECISIONS_20260907.md` Q1).

File provenance remains V15 §9B.2b: a brick must exist under the binary64 half-open test in the pinned DR9-north survey-bricks table and be absent from the pinned no-r registry, which lists bricks without published r-band images. Neither pixels nor checksum-catalogue membership determines eligibility. The file represents exactly that renderable subset of the 7,410. **The selection/run path does not reimplement the brick test or registry lookup.**

**Selection.** Order post-exclusion identities by unsigned big-endian `SHA256(lowercase seed hex || "||" || ASCII decimal GZ1_OBJID) mod 2^32`, ties by numeric ID; walk ranks, skipping IDs absent from the eligible file. Take up to 400 tuning, 200 holdout, 2,000 fresh validation in that order; if survivors cannot fill a target size but meet its floor (380, 190, 1,900 respectively), take what is available and record the target-minus-actual shortfall for each split in the returned `shortfalls` dict (zero when filled); refuse a selection shortage only below the corresponding floor. Never replace raster/score failures. Retain ordered IDs, bricks, skipped ranks, seed and input/code digests before development fetch or label access. Six checks remain: determinism, disjointness, exclusions, target sizes or recorded floor-permitted shortfalls, refusal of reuse across draws, and missing-eligibility refusal; digest/malformation refusal also applies.

**Prospective seed — REQUIRED third-party time anchor.** After the separate amendment decision, fix every manifest entry, including inputs, digests, procedure and code, in manifest commit C and retain its commit ID. The manifest and every attempt use the SAME minimal anchor mechanism: either (a) a commit pushed to the public remote whose server-side push timestamp a third party records, or (b) an external statement of the digest in the chat channel by someone who is not the lane owner, timestamped by the provider. For each anchor, the run records which mechanism was used and retains its reference and third-party UTC timestamp as the evidence; a local timestamp or the lane owner's own digest statement is insufficient. BEFORE the drand round is named, anchor manifest commit C by one of these mechanisms; its third-party timestamp is A. The round may only be one scheduled after that published anchor: name the first default-mainnet drand round at least **A + 600 seconds (ten minutes measured from the published anchor, never the local commit timestamp)**: `r = 1 + ceil((A + 600 - G)/P)`, `T = G + (r - 1)P`, with V15's `G = 1595431050`, `P = 30`. Record r, T, commit ID and the anchor reference with contemporaneous UTC after publication and before T; missed deadline means recorded abort, never substitution. A local commit clock alone is insufficient because it can be backdated to an already-published round, permitting seed grinding. The manifest needs one anchor; every attempt also needs its own anchor under the rule below. Beyond these minimal pushes/anchors, per-entry publication machinery, chained seals, receipts, history-continuation machinery and composed provenance stay deferred. No 24-hour NIST fallback wait remains. Exposed round 6440756 cannot be used.

Retain raw responses from chain-qualified URLs: accept only the named round with its randomness **BLS-VERIFIED against the pinned chain public key AND identical 64-hex randomness from at least two pinned hosts** (`api.drand.sh`, `api2.drand.sh`, `api3.drand.sh`, `drand.cloudflare.com`), confirmed by live re-fetch. Host agreement is an ADDITIONAL check, never an alternative. Use `_optionA_dev/drand_only/verify_drand_v2.py`; require round equality and randomness = SHA256(signature), then lowercase as seed. Its default-mainnet identity is chain hash `8990e7a9aaed2ffed73dbd7092123d6f289930540d7651336225dc172e51b2ce`, public key `868f005eb8e6e4ca0a47c8a77ceaa5309a47978a7c71bc5cce96366b5d7a569937c529eeda66c7293784a9402801af31`, scheme `pedersen-bls-chained`, DST `BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_NUL_`, G/P as above; verify over SHA256(previous_signature || eight-byte big-endian round). Sources: owner decisions Q4; original `.hermes/CODEX_DUHO_DRAND_ONLY_DECISION_20260906.md` lines 12–16 and `_tmp_relay_duho_drand_only.txt` lines 23–31, cited in resolved Q4; `PROPOSAL_DRAND_ONLY_SAMPLING_20260906.md` §§2–3 and `DRAND_ONLY_EXHIBIT_20260906.md` pinned identity. Unavailability means waiting for that round or recorded abandonment, never another source/round.

**Separation, scoring and failure.** Retain the disposition note's KEEP obligations except explicit replacements here: E1–E8 scientific restrictions, §6 pipeline, environment mismatch stop, input/tensor/label checks, custody and access restrictions. Retain the fixed 96-configuration search, complete records, objective `max(k,m-k)/400`, floor 380 and tie-break. Freeze winner code/configuration/preprocessing/environment/results by digest before holdout access. Opening means first participant/process/service access to a holdout pixel, tensor, label or per-object outcome; catalogue IDs alone are not opening. Record, push and anchor the holdout opening under the attempts rule before access; partial access/crash consumes the sole opening. Only that winner: m ≥ 190 and Wilson lower bound for `max(k,m-k)/200` > 0.70, z = 1.959963984540054. No retuning/reopening. After PASS, retain the unchanged freeze and custodian fetch-after-freeze restrictions; validate once using the inherited scored denominator: caller enforces n = 2,000, r counts refusals, m = 2,000 − r, k counts matches among m, p_raw = k/m, p_val = max(p_raw, 1 − p_raw). Fail first if m < 1,900; otherwise K = int(round(p_val × m)), p = K/m, and the two-sided 95% Wilson lower endpoint is `[p + z²/(2m) − z sqrt(p(1−p)/m + z²/(4m²))] / [1 + z²/m]`, z as above. PASS requires > 0.70. This validation denominator differs from fixed tuning/holdout denominators. Sources: V15 §5; `MINI_PREREG_GZ_TIERC_DRAFT_V35_20260905.md` §§9B.6–9B.7a and `miniprereg_pins/validation_gate.py` lines 34–78, cited in resolved Q3; the earlier review’s `/2000` answer is superseded. Selection’s floor-permitted validation shortfall conflicts with caller n = 2,000 and remains open below.

Keep attempt 1, both step-1 collections, fixtures, corrections and chronology. Register every draw, abort, re-draw and holdout opening with UTC, reason, digests, seed/round, access stage and outcome. **One push per attempt:** push each attempt record to the public remote when it happens, so a deleted attempt leaves a visible hole. Apply the SAME minimal anchor rule above to every record: a third party records the pushed commit's server-side timestamp, OR someone who is not the lane owner externally states the record's digest with a provider timestamp; the external-statement alternative does not waive the required push. Record the mechanism used and retain the reference and third-party timestamp as evidence. For the holdout opening, record, push and anchor before first access. No chained seals, receipts or history-continuation machinery are required; registration grants no extra attempt. Exactly one further validation attempt, no secondary or hidden restart. Seed/deadline/input mismatch, reuse/overlap/exclusion breach, premature access, changed freeze or repeated holdout invalidates and stops. No eligible winner or holdout/validation floor/bar failure closes option A. Ties, missing/non-finite/error/non-repeat scores stay unscored; report failures and unscored counts. A render refusal has an explicit `status = RENDER-REFUSED` field with its cause, is UNSCORED, and counts as a MISS in the fixed tuning/holdout denominator (V15 §7); validation uses r and m above. No sentinel value is written into the data. Keep the identity, bypass scoring, and never replace it; ordinary missing/corrupt tensors remain input-integrity failures. Sources: owner decisions Q6; V15 §7 and V39 §8.15b, cited in resolved Q6.


**Signing/precedence — documentary answer, not approval.** V15 §10 imports `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V2_20260905.md` §10 (resolved Q7): companion pre-commitment; Duho’s chat statement of its §17.2-style preimage digest with blank `DUHO SIGNATURE:` line; Blanc relays, Hwao records, neither signs; V36 (§9.1 identity) and the pipeline amendment cite its digest; two seats on different engines return SIGNABLE before signature. It does not restore V2’s two-candidate design. A1 proposes one independent re-review instead of that two-seat gate; C is not a signature. The replacement approval/preimage and companion-digest treatment still needs an explicit clause before adoption; no later-proposal precedence is assumed.

## WHAT THE READER LOSES

Departures proposed below; every deferral is out of scope for this run, not deleted or disproved.

- **Eligibility input:** correctness rests on the producing procedure rather than re-proof at selection, which matters if production is wrong; the file and digest must be fixed BEFORE the seed round is named.
- **Failed-ID input:** membership rests on recorded CSV extraction rather than rechecking the CSV during selection, which matters if extraction is wrong; the derived file, its digest and the CSV digest must be fixed BEFORE the seed round is named.
- **BLS authenticity cost:** one added run-path dependency, `py_ecc`, is required: agreeing hosts prove agreement, not authenticity; the seed safeguard cannot be weakened (owner decisions Q4).
- **Drand only:** NIST certificate/signature/link authentication, primary priority and the 24-hour fallback path disappear, which matters if drand's retained authentication or availability is inadequate.
- **Clock replacement:** signature-relative timing is replaced by published-anchor-relative timing; the local commit timestamp supplies no evidence of prospective designation.
- **Compounded fatal loss (repaired):** the commit-relative clock and deferred third-party witness were fatal IN COMBINATION, not separately: together they allowed an unwitnessed timestamp to be backdated to an already-published round, permitting invisible seed grinding.
- **DEFERRED → ESSENTIAL — manifest anchor:** a pushed commit whose server-side timestamp a third party records, OR an external statement of its digest by someone who is not the lane owner, is required BEFORE naming the round. Retain the mechanism, reference and third-party timestamp; measure the ten-minute margin FROM THIS ANCHOR. This moved because a local commit clock can be backdated, allowing invisible seed grinding; the anchor supplies independent evidence that inputs, procedure and code were fixed before seed designation.
- **DEFERRED → ESSENTIAL — attempts anchoring:** every draw, abort, re-draw and holdout opening is pushed to the public remote when it happens, one push per attempt, and anchored by the SAME minimal mechanism as the manifest; retain the mechanism, reference and third-party timestamp. The opening is recorded, pushed and anchored before access. This moved because a locally-held attempts register is asserted by the same party it constrains: that party could open the holdout, dislike the result, delete the entry and retry invisibly. The public attempt anchors make a deleted attempt leave a visible hole.
- **Legacy builders/driver:** exclusive executable/CLI pins, winner reconstruction, RUN ROOT reconciliation and refusal-token/exit enforcement disappear, which matters if replacement code misimplements retained rules.
- **Legacy schemas/beacon tools:** CORPUS-IDENTITY-2/BEACON-RECORD-3 enforcement and NIST collector/verdict/PKI/probe machinery disappear, which matters if ordinary records omit required bindings or misauthenticate a seed.
- **Witness/provenance stack beyond the two essential anchor requirements:** PushEvent/history machinery, per-entry publication beyond the minimal one push per attempt, chained seals, blob-equality checks, origin/ancestry checks, server protection/additional chat witnesses, Option-A′/B receipts, optional countersigning and history-continuation machinery remain deferred, which matters if history beyond the anchored manifest and attempt records is disputed or dishonest. Only the manifest anchor and attempts anchoring moved; the whole witness stack has not returned.
- **Composed provenance:** combined evidence-path checking is deferred, which matters if inconsistencies between paths conceal an error.
- **NSD/precedence/input-boundary apparatus:** adversarial evidence-loader refusal coverage is deferred, which matters if hostile evidence reaches this fixed-input run.
- **Broad test gates:** 132 differential controls, table verification, fail-first kit, prescribed fixture/generator/suite commands and beacon negative/behaviour probes cease gating, which matters if defects escape the focused selection checks.
- **`verify_split`:** helper-based split verification is deferred, which matters if retained-list recomputation misses a split error.
- **Holdout FLAG machinery (`holdout_once`):** flag enforcement is deferred; the open-once rule and anchored attempts records remain essential, but automatic prevention of a second opening is absent.
- **Seal-append/step 2:** chained append and seal/witness orchestration are deferred while identity-before-access remains, which matters if records beyond the anchored manifest and attempts can be altered or backdated. The seal helper is out of scope for this run; its written/tested versus not-written/tested status contradiction (`OPTION_A_RUN_RECORD_20260906.md` lines 30/161) is recorded, not reconciled (owner decisions Q8).
- **Review/release machinery:** repeated two-seat package gates, historical review questions and the V15 release cap are replaced by one independent amendment review, which matters if that seat misses a defect or imported precedence obligations conflict.
- **Two-candidate alternative:** the hypothetical familywise alternative is dropped, which matters if the remaining candidate fails; no substitute becomes available.

## OPEN QUESTIONS FOR THE REVIEW

Remaining prerequisites; owner decisions Q4–Q6/Q8 and documentary answers Q2–Q4/Q7 above are no longer open.

1. What are the eligible-ID filename, digest and length, and the remaining individual runtime filenames/digests, including the separate derived failed-ID pin and current-byte CSV verification? The population will be reported from the fixed eligible file, not guessed.
2. How is the frozen winner configuration added to the manifest, given that C precedes selection/development and the winner is only known afterward? The exact-read rule is decided; this timing is not.
3. How is a floor-permitted validation selection shortfall reconciled with the inherited caller requirement n = 2,000? Do not silently substitute another denominator.
4. What explicit replacement signing clause binds final-byte approval/preimage and companion digests while proposing one re-review in place of V15’s imported two-seat gate? Duho retains amendment authority.
