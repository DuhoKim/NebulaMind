# Agreement run — narrow amendment A1, 2026-09-07

**FINAL AUTHORING OUTPUT FOR INDEPENDENT REVIEW — UNADOPTED PROPOSAL.** One independent re-review by a seat that did not author A1 is proposed; no review verdict or adoption is asserted. Authoring only; no execution authority, draw, split or unseen-data access. Proposes amendments to V15 (`fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1`); its history remains intact.

**Purpose.** One machine–human GZ1 chirality agreement number on unseen images: validation `max(k, m−k)/m`, where m is the SCORED count and refusals are excluded from the denominator entirely and reported separately as r; require the SCORED-COUNT floor m ≥ 1,900 after the estimator runs on the exact DRAW size n = 2,000, refusals excluded from m, before computing the Wilson 95% lower bound, which must be > 0.70 with z = 1.959963984540054. V15 allows global sign reversal: this does not establish agreement with a preassigned clockwise sign.

## Manifest — input and later-stage inventories

**18 REAL entries; 7 PENDING named inventory obligations (3 INPUT, 1 POST-SEED, 1 STAGED ACCESS, 2 WINNER).** Multi-file placeholders must expand before their due stage; these are row counts, not a completed runtime file count. REAL means SHA-256 recomputed from existing bytes in this fold or the bounded selection reconciliation, not a freeze or operational validation. Pending inputs prevent INPUT FREEZE; future-stage values do not yet exist.

The manifest lists exactly every file read by the adopted run, each with its SHA-256. Resolve all INPUT entries before manifest commit C and its external anchor; no unresolved INPUT entry permits the round to be named. The input manifest fixes the procedure, configuration grid, code, environment, input files, and the schemas and deterministic production/access rules for future outputs. It does not claim that post-seed data or a post-tuning winner already exist. Maintain separate stage inventories for generated or access-controlled inputs and the WINNER record W under the two-stage freeze clause; these append actual paths/digests without replacing or changing C. An unlisted file read or a digest mismatch is a defect and stops the run. Expected hashes must come from the anchored record applicable to that stage, never a substituted local manifest.

The source CSV is included as the expressly required failed-ID derivation input, even though the selector itself reads only the three ID files. Producer-only catalogues and build-only requirements are provenance, not run inputs. The table below binds the named rendering components for the proposed replacement adapter; the adapter must actually use them, or the inventory must be corrected before C. Their existence does not establish a working adapter. Do not import the legacy driver, old corpus builder or seal helper merely to fill an empty pin.

The estimator family file itself contains CONFIG_GRID and FIXED (96 configurations). That single file digest binds both family and grid; DEFAULT_CFG is not the selected winner. No invented grid file is required. Hashing the source CSV was an opaque integrity operation only; no evaluation label or row was displayed or interpreted.

### REAL file entries — current-byte observations

| Status | File | SHA-256 | Freeze | Read purpose / limit |
|---|---|---|---|---|
| REAL | `_optionA_dev/corpus_identity/dryrun_identities_to_exclude_20260905.txt` | `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95` | INPUT | Dry-run exclusions; all 2,644 identities, including skips |
| REAL | `VALIDATION_SELECTION_V29_20260905.csv` | `5643555c75670a695cd9144455a956440125c1019ebd1fb028a7ee21889014c7` | INPUT | Failed-ID derivation source; opaque byte hash only, no CSV parsing or label access |
| REAL | `_optionA_dev/agreement_run/select_sample.py` | `8e517aab2715e6ec814a37f0dfb517b186f2f0ed7b64f045ae6a2a34120144b5` | INPUT | Selection implementation after bounded non-member reconciliation; recheck at C |
| REAL | `_optionA_dev/fourier_chirality/fourier_chirality.py` | `a026fe5fa168ca1eb2056829d86fb549dec4a61596b5d122f60c2a389e2d98a2` | INPUT | Estimator family AND CONFIG_GRID/FIXED; one file, one pin, no separate grid file is read |
| REAL | `study_renderer/__init__.py` | `659b763c83a948825d285e51671c28173b446f428bcd32d941811bf6cdaf5721` | INPUT | Package initializer imported by rendering chain |
| REAL | `study_renderer/render_chain_v3.py` | `7835fda76591bfe5cd9fd4d3413654e212ed7a0f73a45da29b6cc324bbd0cf5e` | INPUT | Rendering chain; imports renderer, rejection and protected-region modules below |
| REAL | `study_renderer/renderer_v4.py` | `dad904ffb0dbc68fcb350be6fbee7ea57a5bf66503d61bf77a4272e0cb812b02` | INPUT | Renderer implementation |
| REAL | `study_renderer/pixel_rejection_v2.py` | `075d73460645ab4c8353a468c8f3a7eabed8e9339198ea26f0dd51a35db02e54` | INPUT | Pixel-rejection implementation |
| REAL | `miniprereg_pins/protected_region_v2.py` | `4b4fb5ca17953e0933bd5c2286ca23063247a472fbe2be5f4fc6d4a57e195faf` | INPUT | Protected-region implementation |
| REAL | `miniprereg_pins/render_config_v2.json` | `b1c0c6d6dca9ee438a474a96b884b5164c1731c95392be04726b1e49f3d34db4` | INPUT | Geometry configuration read by renderer_v4 |
| REAL | `miniprereg_pins/validation_gate.py` | `65e241cad76b6150f625ac5aad085ed528004153d8590f8c32b07895c0c75e5b` | INPUT | Validation gate; caller must still enforce n = 2,000 and scored-count floor |
| REAL | `_optionA_dev/drand_only/verify_drand_v2.py` | `e982d9441f5c8d8b9d4cdf43b4bd44a03c5de42d4246c5bdc5531f4ba5d5ed7b` | INPUT | Chain-bound BLS verification function; collection/two-host/anchor adapter remains pending |
| REAL | `_optionA_dev/fourier_chirality/env_lock.json` | `4e2c851fe1f22f4bc28f121cf3ff4326f036be57fc100b34af9e5501d6c74251` | INPUT | Existing scientific environment lock, to be read and enforced by replacement scorer; not a complete BLS/runtime lock |
| REAL | `/Library/Developer/CommandLineTools/usr/bin/python3` | `bdea59019a38eb6600cc9e71e984a97fedadc406448431281e7657030f54987e` | INPUT | Pinned interpreter executable; hash does not cover its loaded libraries |
| REAL | `/Users/duhokim/Library/Python/3.9/lib/python/site-packages/numpy/core/_multiarray_umath.cpython-39-darwin.so` | `e870618cf0f8a4b73a928649aba064eee859f8e25a7912344c979f1d03679194` | INPUT | NumPy core binary named by existing environment lock |
| REAL | `/Users/duhokim/Library/Python/3.9/lib/python/site-packages/numpy/fft/_pocketfft_internal.cpython-39-darwin.so` | `cfc7c758921da384994a66578d289d6c8aa710756a6af64a28f964b90fd5f546` | INPUT | NumPy FFT binary named by existing environment lock |
| REAL | `_optionA_dev/agreement_run/inputs/eligible_ids_20260907.txt` | `15f34e4ef21b47a5393786a404ecc45aa07f347d548c4811fcc92932a258611d` | INPUT | 11,837 renderability-qualified IDs before historical exclusions; derivation in inputs/INPUTS_RECEIPT_20260907.md |
| REAL | `_optionA_dev/agreement_run/inputs/failed_set_ids_20260907.txt` | `f459d2fd996047ac8470ba2309062f98d9ce21c5adf5d28d609f14126bad3f5d` | INPUT | 2,000 global failed IDs derived from the pinned CSV; derivation in inputs/INPUTS_RECEIPT_20260907.md |

### Pending inputs — named placeholders requiring production

| Status | File placeholder | Digest placeholder | Due stage | Required producer/action |
|---|---|---|---|---|
| PENDING | `UNFIXED_A1_RUN_ADAPTER_PATHS` | `UNFIXED_A1_RUN_ADAPTER_SHA256S` | INPUT | Lane owner: before C, supply exact replacement entry points/modules for selection invocation, anchored-round scheduling, collection/live refetch and two-host agreement, rendering/fetch, scoring, same-process repeat checks, 96-configuration search, winner selection, holdout gate, validation caller and output writing. Expand into one path/hash per file actually read. Legacy driver and sentinel adapter cannot stand in as an asserted completed A1 path. |
| PENDING | `UNFIXED_A1_RUNTIME_DEPENDENCY_PATHS` | `UNFIXED_A1_RUNTIME_DEPENDENCY_SHA256S` | INPUT | Environment preparer: before C, supply a working BLS environment and the exact individually pinned loaded source/package-initializer/binary/library files for the selected interpreter and final adapters, including py_ecc and transitive dependencies, NumPy/Astropy where read, and the environment enforcement configuration. The prerequisite draft reports py_ecc is not discoverable under the pinned interpreter; runtime readiness remains unestablished. Existing env_lock does not enumerate this closure; expand this placeholder, never use a directory or requirements-file digest as a substitute. |
| PENDING | `UNFIXED_RENDER_COORDINATE_INPUT_PATHS` | `UNFIXED_RENDER_COORDINATE_INPUT_SHA256S` | INPUT | Lane owner: before C, name and hash the admissible label-free coordinate/brick metadata actually read by the rendering adapter. Eligible IDs alone do not supply coordinates. Producer-only survey catalogues remain outside the run manifest unless the final run actually reads them. |
| PENDING | `UNFIXED_DRAW_AND_BEACON_RECORD_PATHS` | `UNFIXED_DRAW_AND_BEACON_RECORD_SHA256S` | POST-SEED | Run operator: after the prospectively designated round, retain raw chain-qualified relay responses and deterministic ordered split/skipped-rank/brick records, each pinned individually before its downstream use; publish and anchor draw record before development access. C fixes their schema and deterministic production rule, not unknowable future bytes. |
| PENDING | `UNFIXED_STAGE_DATA_READ_PATHS` | `UNFIXED_STAGE_DATA_READ_SHA256S` | STAGED ACCESS | Authorized producer/custodian: enumerate each actual checksum manifest, image plane, tensor, label file and integrity/status input at its permitted stage, with individual expected/produced hashes before downstream consumption. C fixes source selection, expected-checksum acquisition and integrity/access rules; post-draw manifests must precede development-label access under V15 section 3a. Holdout data are not opened to obtain pins before WINNER FREEZE; fresh pixels are not fetched before holdout PASS and the retained custody gates. This author neither opens nor hashes pixel/label artifacts; stage inventories are not supplied. |
| PENDING | `UNFIXED_TUNING_EVIDENCE_PATHS` | `UNFIXED_TUNING_EVIDENCE_SHA256S` | WINNER | Tuning operator: after complete fixed-grid tuning and before holdout opening, produce and individually hash complete per-object/per-configuration results, tuning input/read inventories and selection calculation used to identify the sole winner; anchor their digests in W. |
| PENDING | `UNFIXED_WINNER_CONFIG_PATH` | `UNFIXED_WINNER_CONFIG_SHA256` | WINNER | Tuning operator: after tuning and before holdout opening, serialize the mechanically selected single configuration (all six keys and config_id), hash the actual file, bind it to family/grid/code/environment pins and tuning evidence in W. Not an INPUT-freeze prerequisite and not a predeclared default configuration. |

The rows containing PATHS/SHA256S are unresolved inventory obligations, not acceptable aggregate runtime entries. Expand them into individual files before their stated stage. No pixel/label content is authorized by this clause before its access gate. If the exact-read requirement cannot be implemented consistently with these access gates, the owner must resolve that concrete inventory conflict before C; no early access is implied.

All REAL pins must be checked against final adopted/runtime bytes at C. The selector pin above identifies the reconciled code; eligible and failed-ID outputs now exist with verified pins and recorded derivation. This authoring pass produces no producer output, BLS installation or replacement adapter.

## Eligibility and selection

**Eligibility as input.** Per `AGREEMENT_RUN_INPUT_CONTRACT_20260907.md`, selection consumes three pinned one-ID-per-line files. Eligible IDs are ascending and unique; failed IDs are the CSV’s GZ1_OBJID column, sorted ascending and deduplicated, with derivation recorded before C; the full dry-run exclusion file retains its pin, including skips. Every selection input is passed with its expected digest, recomputed on read; absence, malformation or mismatch refuses, with no default or optional digest.

**Pipeline order.** Renderability-qualified eligibility FIRST, then exclusion by the full historically pinned lists, then ranking and exact draws. The eligible file contains 11,837 IDs before historical exclusions. The full dry-run and failed-set lists are global history: IDs outside eligibility are legitimate non-members, are retained in those lists, and are counted rather than rejected. The selector returns their unique-ID counts as `counts.exclusion_nonmembers` and `counts.failed_nonmembers`; full-list `exclusion`/`failed` counts remain intact, while `removed` counts only eligible IDs removed. Exclusion/failed mutual overlap still refuses even outside eligibility. No excluded or failed ID may survive into any split. Mandatory digest verification, strictly ascending duplicate-free eligible IDs, and exact draws of 400/200/2,000 or a stopping shortfall error remain required.

**Measured input facts (2026-09-07).** Eligible: 11,837, SHA-256 `15f34e4ef21b47a5393786a404ecc45aa07f347d548c4811fcc92932a258611d`; failed-set: 2,000, SHA-256 `f459d2fd996047ac8470ba2309062f98d9ce21c5adf5d28d609f14126bad3f5d`; full dry-run exclusions: 2,644, SHA-256 `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95`. Exclusion/failed overlap is zero. There are 44 dry-run and 46 failed-set legitimate non-members of eligibility. Thus 4,554 eligible IDs are removed and the **post-exclusion population is 7,283**: 11,837 − (2,644 − 44) − (2,000 − 46) = 7,283. The 44 dry-run non-members are a measured set difference, not an inferred identification with the historical skips. These facts resolve A1's Q1 population placeholder; the old 7,410 pre-renderability figure is not the selection population.

Sources: `.hermes/CODEX_A1_ACTUAL_INPUT_COUNTS_20260907_1500.md` (measured set counts) and `_optionA_dev/agreement_run/inputs/INPUTS_RECEIPT_20260907.md` (eligible/failed derivation and pins). No selection on real inputs is needed to establish these facts.

File provenance remains V15 §9B.2b: a brick must exist under the binary64 half-open test in the pinned DR9-north survey-bricks table and be absent from the pinned no-r registry, which lists bricks without published r-band images. Neither pixels nor checksum-catalogue membership determines eligibility. The eligible file represents the renderability-qualified subset of the guarded 12,054 before historical exclusions. **The selection/run path does not reimplement the brick test or registry lookup.**

**Selection.** Order post-exclusion identities by unsigned big-endian `SHA256(lowercase seed hex || "||" || ASCII decimal GZ1_OBJID) mod 2^32`, ties by numeric ID; walk the ranked survivors of eligibility and both full exclusion lists. Take EXACT DRAW sizes of 400 tuning, 200 holdout and 2,000 fresh validation, in that order. If the eligible population after exclusions cannot fill any requested draw, STOP the run and raise an error naming the split and its shortfall; never shrink a draw. The 380/190/1,900 thresholds are SCORED-COUNT floors applied only after the estimator runs on those exact draws, with refusals and other unscored outcomes excluded from m. They are not selection parameters and cannot authorize smaller draws. Never replace raster/score failures. Retain ordered IDs, bricks, skipped ranks, seed and input/code digests before development fetch or label access. Six checks remain: determinism, disjointness, exclusions, exact draw sizes or a stopping shortfall error, refusal of reuse across draws, and missing-eligibility refusal; digest/malformation refusal also applies.

## Two-stage freeze and prospective seed

**Two separate mandatory events.** INPUT FREEZE and WINNER FREEZE are distinct records at different times. A reference to “freeze” must identify which event it means. A later winner record never modifies the earlier input record.

**(i) INPUT FREEZE, C, before naming any seed round.** After Duho adopts the final A1, and before a drand round is designated, fix the adopted procedure digest, complete fixed family/grid (all 96 configurations and enumeration), tuning objective/floors/tie-break, selection and all run code, preprocessing, environment, the eligible/exclusion/derived-failed input files and source CSV, and every other pre-existing input actually read. Resolve every INPUT-manifest path and digest, including the eligible-file length. No choice in C may depend on tuning, holdout or validation results. C also fixes the schemas, source-selection and deterministic production rules, access gates and integrity checks for files necessarily generated later; it contains no pretend digest for a future winner, draw, beacon response or result.

Anchor C's manifest digest, adopted procedure digest and commit ID using A1's minimal mechanism: a public-remote push whose server-side timestamp is recorded by a third party, or a provider-timestamped chat statement of the digest by someone other than the lane owner. Retain the mechanism, external reference and third-party UTC A. Only AFTER this anchor may the first default-mainnet round scheduled at least A + 600 seconds be named: r = 1 + ceil((A + 600 - G)/P), T = G + (r - 1)P, G = 1595431050, P = 30. Record designation after publication and before T; missing the deadline is a recorded abort, not a replacement round. The published digest is the authority for all checks against C. This event needs no seed or drawn-list digest: those values do not exist yet and must never be fabricated to satisfy a record template.

After the named round is authenticated under A1, derive the split using C's exact inputs/code. Retain, push and anchor the draw record with seed/round, C reference, three selection-input hashes, selector hash and drawn-list hashes before any development fetch or label access. Future data-read inventories are stage records under C's already fixed production rules; they cannot alter the population, split, procedure, grid or code. Run the complete fixed tuning search on the tuning set only, retaining all configuration and per-object results.

**(ii) WINNER FREEZE, W, after tuning and before holdout opens.** Once all prescribed tuning evaluations finish, apply the fixed eligibility, objective and tie-break to determine the sole winner; no eligible winner closes option A. Serialize the winning configuration and config_id, hash the actual file and retain it in W. W binds C's externally anchored digest/reference; the seed/round and drawn-list digests; the complete tuning results and every tuning-evidence file read to select the winner; the winner configuration file; and the identical family, grid, selection/scoring code, preprocessing and environment hashes already fixed at C. W adds the winner and completed evidence, not new code or tunable choices. The winner cannot be pre-declared at INPUT FREEZE because it is the result of the prescribed comparison of all eligible configurations on tuning data not yet drawn or evaluated.

Publish and externally anchor W before the first participant, process or service accesses any holdout pixel, tensor, label or per-object outcome (catalogue IDs alone are not opening). W's digest and file pins may be included in the already required holdout-opening attempt record, avoiding an extra publication mechanism: that record must identify W and C, carry seed/round, three selection-input hashes, selector hash and drawn-list hashes, and be pushed AND anchored under A1's attempt rule before access. Retain commit ID, mechanism, external reference and third-party UTC. The opening must post-date that evidence; partial access or a crash consumes the sole opening.

Evaluate only W's configuration once on holdout under the unchanged scientific gates. A holdout PASS confirms the already frozen identity; it is not permission to choose a new winner or alter C/W. Only after PASS and the retained V15 custody/fetch/lift conditions may fresh-validation pixels be fetched or opened as applicable. All validation uses the same W. A changed C/W binding, early access, retuning, replacement winner or repeated holdout invalidates and stops the run. Abort and attempt records remain required; no stage transition grants another attempt.

**Required order:** adoption → INPUT FREEZE and external anchor C/A → prospective round designation → authenticated seed → anchored deterministic draw → tuning → WINNER FREEZE and externally anchored W/holdout-opening record → sole holdout opening/evaluation → PASS and retained custody gates → sole fresh validation. Neither freeze can substitute for the other. Draw sizes, scored floors and denominator rules are not amended by this clause.

**Anchored-record authority and content.** Checks use the externally published digest applicable to C, the draw or W, never a substituted local manifest. C binds the procedure, input pins, code and future-output rules; it cannot carry an unavailable seed or drawn-list digest. Every post-draw attempt/opening record carries seed/round, the three selection-input hashes, selector hash and drawn-list hashes so outsiders can re-derive selection. An abort before these values exist records that fact and its stage, with C and all available bindings; it must not invent a value or permit another attempt. These are contents of the existing minimal records; control gates, `verify_split`, chained seals, per-entry publication beyond required attempt pushes, receipts, history-continuation machinery and composed provenance stay deferred. No 24-hour NIST fallback wait remains; exposed round 6440756 cannot be used.

**Seed authentication.** Retain raw responses from chain-qualified URLs: accept only the named round with its randomness **BLS-VERIFIED against the pinned chain public key AND identical 64-hex randomness from at least two pinned hosts** (`api.drand.sh`, `api2.drand.sh`, `api3.drand.sh`, `drand.cloudflare.com`), confirmed by live re-fetch. Host agreement is an ADDITIONAL check, never an alternative. Use `_optionA_dev/drand_only/verify_drand_v2.py`; require round equality and randomness = SHA256(signature), then lowercase as seed. Its default-mainnet identity is chain hash `8990e7a9aaed2ffed73dbd7092123d6f289930540d7651336225dc172e51b2ce`, public key `868f005eb8e6e4ca0a47c8a77ceaa5309a47978a7c71bc5cce96366b5d7a569937c529eeda66c7293784a9402801af31`, scheme `pedersen-bls-chained`, DST `BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_NUL_`, G/P as above; verify over SHA256(previous_signature || eight-byte big-endian round). Sources: owner decisions Q4; original `.hermes/CODEX_DUHO_DRAND_ONLY_DECISION_20260906.md` lines 12–16 and `_tmp_relay_duho_drand_only.txt` lines 23–31, cited in resolved Q4; `PROPOSAL_DRAND_ONLY_SAMPLING_20260906.md` §§2–3 and `DRAND_ONLY_EXHIBIT_20260906.md` pinned identity. Unavailability means waiting for that round or recorded abandonment, never another source/round.

## Separation, scoring and failure

 Retain the disposition note's KEEP obligations except explicit replacements here: E1–E8 scientific restrictions, §6 pipeline, environment mismatch stop, input/tensor/label checks, custody and access restrictions. Retain the fixed 96-configuration search, complete records, tuning objective `max(k, m−k)/400` (fixed denominator equal to the exact DRAW size 400, unscored = miss for both orientations), SCORED-COUNT floor m ≥ 380 after the estimator runs, refusals excluded from m, and tie-break. Complete and externally anchor WINNER FREEZE W and the opening record as specified above before holdout access. Only that winner: check the holdout SCORED-COUNT floor m ≥ 190 after the estimator runs on the exact DRAW size 200, refusals excluded from m, BEFORE any Wilson computation; failure closes option A. Then require the Wilson 95% lower bound for `max(k, m−k)/200` (fixed denominator equal to the exact DRAW size 200, unscored = miss for both orientations) > 0.70, z = 1.959963984540054. No retuning/reopening. After PASS, retain unchanged C/W and the custodian fetch-after-WINNER-FREEZE restrictions; validate once using the inherited scored denominator: caller enforces the exact DRAW size n = 2,000, refusals are excluded from the denominator entirely and reported separately as r, m = 2,000 − r is the SCORED count, k counts matches among m, p_raw = k/m, p_val = max(k, m−k)/m = max(p_raw, 1 − p_raw). After the estimator runs, fail first if the SCORED-COUNT floor m ≥ 1,900 is not met; otherwise K = int(round(p_val × m)), p = K/m, and the two-sided 95% Wilson lower endpoint is `[p + z²/(2m) − z sqrt(p(1−p)/m + z²/(4m²))] / [1 + z²/m]`, z as above. PASS requires > 0.70. The two stages use OPPOSITE conventions: tuning/holdout count unscored objects as misses in fixed denominators; validation excludes refusals from its scored denominator entirely. Sources: V15 §5; `MINI_PREREG_GZ_TIERC_DRAFT_V35_20260905.md` §§9B.6–9B.7a and `miniprereg_pins/validation_gate.py` lines 19–25 and 34–79, cited in resolved Q3; A1’s earlier Q3 answer incorrectly used `/2000`: the correct validation denominator is m, the SCORED count. Any unfillable exact draw stops selection before scoring; scored-count floors never permit shrinking it.

### Attempts and refusals

Keep attempt 1, both step-1 collections, fixtures, corrections and chronology. Register every draw, abort, re-draw and holdout opening with UTC, reason, digests, seed/round when available (otherwise explicit absence under the stage-content rule), access stage and outcome. **One push per attempt:** push each attempt record to the public remote when it happens, so a deleted attempt leaves a visible hole. Apply the SAME minimal anchor rule above to every record: a third party records the pushed commit's server-side timestamp, OR someone who is not the lane owner externally states the record's digest with a provider timestamp; the external-statement alternative does not waive the required push. Record the mechanism used and retain the reference and third-party timestamp as evidence. For the holdout opening, record, push and anchor before first access. No chained seals, receipts or history-continuation machinery are required; registration grants no extra attempt. Exactly one further validation attempt, no secondary or hidden restart. Seed/deadline/input mismatch, reuse/overlap/exclusion breach, premature access, changed C/W binding or repeated holdout invalidates and stops. No eligible winner or holdout/validation post-estimator scored-count floor (refusals excluded from m) or strength-bar failure closes option A. Ties, missing/non-finite/error/non-repeat scores stay unscored; report failures and unscored counts. A render refusal has an explicit `status = RENDER-REFUSED` field with its cause and is UNSCORED: apply the fixed-denominator misses for tuning/holdout and scored-denominator exclusion for validation, with the exact draws, post-estimator floors and Wilson gate specified above. No sentinel value is written into the data. Keep the identity, bypass scoring, and never replace it; ordinary missing/corrupt tensors remain input-integrity failures. Sources: owner decisions Q6; V15 §7 and V39 §8.15b, cited in resolved Q6.


## Final adoption and precedence — unadopted decision template

UNADOPTED DECISION TEMPLATE. Duho has NOT adopted this clause or the final A1 through this draft. The text below proposes both the amendment and its single-review departure; it records neither approval nor a completed review.

**Adoption of exact final bytes.** A1 takes effect only if Duho gives clear plain-language approval in his dialogue with Codex, bound to the exact final AGREEMENT_RUN_AMENDMENT_A1_20260907.md actually presented for adoption. Duho need not recite a digest, use any special sentence, repeat the authorization elsewhere or perform a separate signing ceremony. This approval channel follows Duho’s recorded decision in CODEX_DUHO_CONVERSATION_APPROVAL_RECORD_20260906.md (recorded 2026-09-06T11:08:13+09:00; SHA-256 f99fb08c0e14a6473b4ab76311abfadf1942322a28b99c72b3ff8ac03aeb409f). FINAL_A1_SHA256 is a named pending value, not a digest of this clause draft or an earlier A1. Before presenting final bytes for adoption, fold all accepted clauses, resolve INPUT prerequisites, and obtain one independent re-review by a seat that did not author A1, identifying those same final bytes and returning SIGNABLE. Any substantive edit after that verdict requires review of the changed final bytes before adoption. This single independent A1 review is itself a proposed replacement of V15's imported two-seat/repeated package gates and historical release cap for this narrow run; it becomes operative only through Duho's explicit adoption of this clause in final A1. Preparation authority, an author disposition, a draft review, a manifest commit or an anchor is not Duho's adoption and does not already waive V15's gate.

For the agents’ internal version-binding recordkeeping, retain the existing byte convention: final A1 is UTF-8/LF with a terminal LF and exactly one blank SIGNATURE UTC: line and one blank DUHO SIGNATURE: line. The agents compute its full-file SHA-256 and its section-17.2-style preimage digest (replace only the entire DUHO SIGNATURE: line with that blank form); both must agree with the digest presented in chat. If they differ, stop and correct the proposed package before asking for adoption. Keep the adopted A1 bytes unchanged thereafter. The agents record Duho's verbatim approving words, the approval time, provider chat reference/timestamp, the approved stage and exact final version with its full SHA-256, and any distinct relay UTC in a separate internal adoption record; do not insert a later timestamp or signature into the hashed A1. Blanc may relay and Hwao may record; neither signs for Duho. The record must distinguish provider time from relay time. This explicitly resolves the timestamp/preimage mismatch documented in SIGNATURE_RECORD_SELRULE_V15_20260906.md without rewriting V15 or its signature history.

**Limited supersession.** The base remains the signed OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V15_20260906.md, digest fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1. For this agreement run only, adopted A1 governs where its explicit operative replacements differ from V15: the three pinned selection inputs and trimmed implementation in place of inline eligibility/legacy builders; prospective anchored drand-only BLS-plus-host authentication in place of NIST/fallback and signature-relative beacon timing; the separate INPUT/WINNER records and minimal manifest/attempt/opening anchors in place of the expressly deferred seal/witness/orchestration machinery; explicit render-refusal status; and the final adoption/review procedure in this clause. A1's express deferrals are out of scope for this run, not claims that the historical mechanisms never existed or worked. A disposition table, owner note, code edit or later proposal has no independent power to supersede a signed requirement.

All V15 provisions not expressly replaced by adopted A1 continue to govern, including E1–E8 scientific substance, the family and 96-configuration search with its fixed objective and tie-break, preprocessing, environment mismatch stop, input integrity and repeat checks, exclusions and disclosures, custody and fetch-after-freeze restrictions, one holdout and exactly one further validation attempt, no secondary/replacement/retry, and the inherited scoring floors/bars/denominators. In particular tuning/holdout use fixed 400/200 denominators with unscored objects as misses; validation uses scored m with refusals reported separately, requires n = 2,000 and m ≥ 1,900, and the prescribed Wilson lower bound > 0.70. Exact draws remain 400/200/2,000 or a stopping shortfall error, as stated in Selection; 380/190/1,900 are post-estimator scored-count floors, never permission for smaller draws. These authored rules do not certify implementation readiness. Preserve attempt 1, both step-1 collections, dry-run knowledge/exclusions, corrections and all prior signature/review history.

**Companion digest treatment.** V15 remains the signed companion baseline. Any V36 identity or pipeline companion that relies on this amendment must cite BOTH that V15 digest and Duho's adopted FINAL_A1_SHA256 in its new adoption/reference record, specifying the A1 clauses it relies on. Do not edit an already signed companion merely to insert a new digest. This replaces V15 section 10's single selection-rule digest reference for the A1 route. A1 does not silently adopt an unsigned V36/pipeline document, change their unrelated scientific requirements or claim their approval; any still-required companion adoption must be completed before its protected action. Later draft version numbers alone have no precedence. Within A1's expressly amended scope A1 governs over V15 and contrary inherited procedural cross-references; outside that scope the signed base/companion provisions remain controlling.

**No retroactive migration of a begun run.** For this precedence rule a run has begun once its INPUT manifest is externally anchored, a seed round is designated, or protected data are accessed, whichever occurs first. A later proposal has no effect. A later adopted amendment applies prospectively only to a run not yet begun; it cannot change the governing bytes, winner, denominator, exclusions, access history, attempt count or verdict of an already begun run. That run stays bound to its recorded V15/A1/C/W identities. If it cannot continue under those bindings, stop and record abandonment or invalidity under its governing rules; a later amendment cannot erase it, cure prior exposure, reopen holdout or restore an expended attempt. No automatic restart or extra validation attempt is authorized. Existing CLOSED/no-further-attempt obligations remain controlling.

## WHAT THE READER LOSES

Departures proposed below; every deferral is out of scope for this run, not deleted or disproved.

- **Inherited denominator asymmetry / Q3 correction:** tuning `max(k, m−k)/400` and holdout `max(k, m−k)/200` use fixed denominators equal to the exact DRAW sizes 400 and 200, unscored = miss; apply SCORED-COUNT floors m ≥ 380 and m ≥ 190 respectively after the estimator runs, refusals excluded from m, and check the holdout floor BEFORE any Wilson computation. Validation uses `max(k, m−k)/m`, where m is the SCORED count; refusals are excluded from the denominator entirely and reported separately as r; require the SCORED-COUNT floor m ≥ 1,900 after the estimator runs on the exact DRAW size n = 2,000, refusals excluded from m, before computing the Wilson 95% lower bound, which must be > 0.70 with z = 1.959963984540054. These OPPOSITE conventions are INHERITED from V15 §§5/7 and its pinned `validation_gate.py`, not introduced by the simplification. A1 previously carried the wrong validation denominator in its Q3 answer (`/2000`); that answer is corrected to `/m`.
- **Draw-size / scored-count correction:** an earlier version of this package conflated exact DRAW sizes 400/200/2,000 with post-estimator SCORED-COUNT floors 380/190/1,900 (refusals excluded from m), permitting a thin eligible population to return a smaller draw down to a scored-count floor, including only 1,900 validation identities. That repair is superseded: any unfillable exact draw STOPS the run; the floors apply only after scoring.
- **Eligibility input:** correctness rests on the producing procedure rather than re-proof at selection, which matters if production is wrong; the file and digest must be fixed BEFORE the seed round is named.
- **Failed-ID input:** membership rests on recorded CSV extraction rather than rechecking the CSV during selection, which matters if extraction is wrong; the derived file, its digest and the CSV digest must be fixed BEFORE the seed round is named.
- **BLS authenticity cost:** one added run-path dependency, `py_ecc`, is required: agreeing hosts prove agreement, not authenticity; the seed safeguard cannot be weakened (owner decisions Q4).
- **Drand only:** NIST certificate/signature/link authentication, primary priority and the 24-hour fallback path disappear, which matters if drand's retained authentication or availability is inadequate.
- **Clock replacement:** signature-relative timing is replaced by published-anchor-relative timing; the local commit timestamp supplies no evidence of prospective designation.
- **Compounded fatal loss (repaired):** the commit-relative clock and deferred third-party witness were fatal IN COMBINATION, not separately: together they allowed an unwitnessed timestamp to be backdated to an already-published round, permitting invisible seed grinding.
- **DEFERRED → ESSENTIAL — manifest anchor:** a pushed commit whose server-side timestamp a third party records, OR an external statement of its digest by someone who is not the lane owner, is required BEFORE naming the round. Retain the mechanism, reference and third-party timestamp; measure the ten-minute margin FROM THIS ANCHOR. This moved because a local commit clock can be backdated, allowing invisible seed grinding; the anchor supplies independent evidence that inputs, procedure and code were fixed before seed designation.
- **DEFERRED → ESSENTIAL — attempts anchoring:** every draw, abort, re-draw and holdout opening is pushed to the public remote when it happens, one push per attempt, and anchored by the SAME minimal mechanism as the manifest; retain the mechanism, reference and third-party timestamp. The opening is recorded, pushed and anchored before access. This moved because a locally-held attempts register is asserted by the same party it constrains: that party could open the holdout, dislike the result, delete the entry and retry invisibly. The public attempt anchors make a deleted attempt leave a visible hole.
- **PAIRWISE deferred-column reading — anchor content:** a ledger read row by row cannot see an interaction between rows; the two fatal losses were survivable alone and fatal together. The PAIRWISE SWEEP in `A1_ADVERSARIAL_DEFERRAL_SWEEP_20260907.md` supplies both requirements: the published manifest digest is the authority, and every anchored record carries enough to re-derive the selection. These close the input-substitution and unverifiable-sample gaps while leaving the control gates, `verify_split` and the rest of the deferred machinery deferred.
- **Legacy builders/driver:** exclusive executable/CLI pins, winner reconstruction, RUN ROOT reconciliation and refusal-token/exit enforcement disappear, which matters if replacement code misimplements retained rules.
- **Legacy schemas/beacon tools:** CORPUS-IDENTITY-2/BEACON-RECORD-3 enforcement and NIST collector/verdict/PKI/probe machinery disappear, which matters if ordinary records omit required bindings or misauthenticate a seed.
- **Witness/provenance stack beyond the two essential anchor requirements:** PushEvent/history machinery, per-entry publication beyond the minimal one push per attempt, chained seals, blob-equality checks, origin/ancestry checks, server protection/additional chat witnesses, Option-A′/B receipts, optional countersigning and history-continuation machinery remain deferred, which matters if history beyond the anchored manifest and attempt records is disputed or dishonest. Only the manifest anchor and attempts anchoring moved; the whole witness stack has not returned.
- **Composed provenance:** combined evidence-path checking is deferred, which matters if inconsistencies between paths conceal an error.
- **NSD/precedence/input-boundary apparatus:** adversarial evidence-loader refusal coverage is deferred, which matters if hostile evidence reaches this fixed-input run.
- **Broad test gates:** 132 differential controls, table verification, fail-first kit, prescribed fixture/generator/suite commands and beacon negative/behaviour probes cease gating, which matters if defects escape the focused selection checks.
- **`verify_split`:** helper-based split verification is deferred, which matters if retained-list recomputation misses a split error.
- **Holdout FLAG machinery (`holdout_once`):** flag enforcement is deferred; the open-once rule and anchored attempts records remain essential, but automatic prevention of a second opening is absent.
- **Seal-append/step 2:** chained append and seal/witness orchestration are deferred while identity-before-access remains, which matters if records beyond the anchored manifest and attempts can be altered or backdated. The seal helper is out of scope for this run; its written/tested versus not-written/tested status contradiction (`OPTION_A_RUN_RECORD_20260906.md` lines 30/161) is recorded, not reconciled (owner decisions Q8).
- **Review/release machinery:** repeated two-seat package gates, historical review questions and the V15 release cap are replaced by one independent amendment review, which matters if that seat misses a defect or imported precedence obligations conflict.
- **Two-candidate alternative:** the hypothetical familywise alternative is dropped, which matters if the remaining candidate fails; no substitute becomes available.

## OPEN QUESTIONS FOR THE REVIEW

No unresolved clause-design question remains from the three prerequisite drafts: the manifest inventory, separate C/W timing and final-adoption/precedence template are now stated above. The seven remaining named pending inventory obligations remain in the manifest table with their producers and due stages; no missing path, digest, population length or future output is treated as a discretionary question or usable default.

Duho's adoption decision remains unmade. Independent review is still required; these are final authored bytes for that review, not an adoption-ready package while INPUT prerequisites remain unresolved. Resolving them changes A1's bytes and requires review of those changed final bytes before adoption. Any concrete exact-read/access-gate conflict must be resolved before C as required by the manifest clause.

SIGNATURE UTC:
DUHO SIGNATURE:
