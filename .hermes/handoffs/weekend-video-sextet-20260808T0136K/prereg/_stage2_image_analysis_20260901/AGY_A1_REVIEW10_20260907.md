ACCESS_SHA=c52ce05376b89cc4a9df798d7b520740daa667d960c5bed7976a39c80321a263

[MINOR] 1. THE COUNT. The sheet's stated CORE count is "30 real entries / 5 later-stage placeholders". I counted exactly 30 files in the "files" array with "status": "REAL" and 5 placeholders in the "placeholders" array of `INPUT_MANIFEST_A1_CORE.json` (SHA-256 `ec2e...`). The numbers perfectly match.

[MAJOR] 2. ANY OTHER PRESENT-TENSE NUMBER OR STATUS in the sheet:
- "Eligible 11,837": Matches the line count of `eligible_ids_20260907.txt` exactly.
- "failed-set 2,000": Matches the line count of `failed_set_ids_20260907.txt` exactly.
- "full dry-run exclusions 2,644": Matches the line count of `dryrun_identities_to_exclude_20260905.txt` exactly.
- "post-exclusion population 7,283": Matches the exact deterministic output (11,837 - 1954 - 2600 = 7,283).
- **"122 consumer checks": DISAGREES.** The source artifact `test_core_consumer.py` contains exactly 32 tests. Even aggregating all other `agreement_run` tests not separately broken out yields at most 60 (or 81 with other scripts), not 122.
- "42 run-path checks": Matches `test_run_path.py` (42 tests).
- "32 MEDIUM checks": Matches `test_medium_perturbation.py` (32 tests).
- "16 selector checks": Matches `test_select_sample.py` (16 tests).
- "4 BLS checks": Matches `test_verify_drand_v2.py` (4 tests).
- "All three current preparation obligations are resolved": Matches the manifest `current_unresolved_preparation_obligations: 0` and the 3 items having `resolved: true`.

[MAJOR] 3. HISTORICAL VERSUS CURRENT. A1 states in the present tense that the manifest "has 28 real entries" and explicitly attributes this to a lane owner update marked "2026-09-07 17:15 KST" (with digest `cdac...`). A reader would naturally take this as the current, authoritative count. It is not clearly contextualized as historical, creating a direct conflict with the sheet's claim of 30.

[MINOR] 4. OVERCLAIMING. The sheet successfully avoids overclaiming: it explicitly states "Duho has decided nothing", requires plain language approval bound to the exact version, clarifies that "Readiness grants no run permission," and acknowledges that five later-stage obligations remain due.

VERDICT: SHEET-NOT-SOUND
