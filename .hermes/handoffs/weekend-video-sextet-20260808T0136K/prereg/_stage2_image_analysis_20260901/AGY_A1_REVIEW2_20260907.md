ACCESS_SHA=61e253cef941de58b6be805faacc12822d0a31900ae8309f160ad976618ac069

**1. YOUR FATAL 1 and 2 (unwitnessed clock; ten minutes insufficient):**
The hole is actually closed. By requiring the manifest commit to be anchored by a third party (e.g., a git server push timestamp or a chat provider timestamp) *strictly before* the drand round is even named, and waiting 10 minutes (A + 600) from that external anchor, the lane owner can no longer backdate the clock. The seed is genuinely prospective and unknowable at the time the inputs are locked, making seed-grinding impossible.

**2. THE SECOND MOVE (anchoring attempt records):**
This definitively closes the "best-of-many" hole. By requiring every attempt (draw, abort, re-draw, and holdout opening) to be pushed to the public remote and anchored *before* first access to the holdout data, a deleted or abandoned attempt leaves a visible gap. The lane owner cannot silently open the holdout, dislike the result, and start over invisibly because the intention to open was already externally anchored.

**3. THE PAIRWISE SWEEP's two additions:**
Re-derivability by an outsider is genuinely sufficient to justify deferring the control gates and `verify_split`. Because the inputs (eligible, failed, excluded lists), the selection code (`select_sample.py`), and the seed are all firmly anchored, the split is entirely deterministic. An outsider can run the exact anchored code on the anchored inputs and drand seed. If the lane owner were to secretly modify the script or manipulate the IDs, the drawn-list hashes they publish in the draw record would not match the outsider's honestly derived hashes. The attack is fully contained.

**4. THE STATISTIC:**
I checked `miniprereg_pins/validation_gate.py` (lines 67-75). The code computes `m = n - r` (where `n` is 2,000 and `r` is refusals), and then evaluates the Wilson score bound using `m` as the denominator (`wilson_lower(..., m)`). Refusals are completely excluded from the denominator. The lane owner is exactly right, and my first review's claim (`/2000` with unscored counting as misses for validation) was wrong. The asymmetry is correctly inherited from V15. 

**5. SIZES vs FLOORS:**
I verified `select_sample.py` and V15. The `select_sample.py` code loops over the exact target sizes `(400, 200, 2000)` and checks `if available < size:`, immediately raising a `ValueError` for any shortfall. The code no longer contains any reference to the `380/190/1900` floors, ensuring it draws EXACTLY the requested sizes or stops the run. This perfectly aligns with V15, which treats the sizes as rigid selection parameters and the floors as post-estimator scoring thresholds.

**6. THE TWO-STAGE FREEZE:**
The two events cannot be conflated, nor can a winner be pre-declared. `INPUT FREEZE` (`C`) is anchored *before* the seed is designated, meaning the data isn't even split yet, so the winner cannot possibly be known. `WINNER FREEZE` (`W`) happens *after* tuning but before holdout access, binding the winner to `C`'s inputs. A1 expressly forbids `C` from carrying any pretend digests for future outputs. 

**7. THE MANIFEST:**
No digests are invented. Future or pending files are recorded with explicit placeholders (e.g., `UNFIXED_ELIGIBLE_IDS_SHA256`) that A1 strictly demands must be resolved to REAL digests (recomputed from actual bytes) before the manifest `C` is anchored. Furthermore, A1 explicitly asserts that the manifest lists "exactly every file read by the adopted run," meaning anything the run reads that is missing from the manifest constitutes a hard defect.

**8. THE ADOPTION/PRECEDENCE CLAUSE:**
It does not claim any approval Duho has not given; it explicitly states "Duho has NOT adopted this clause... it records neither approval nor a completed review," marking itself honestly as an unadopted decision template. It correctly and precisely identifies V15 by its SHA-256 digest as the governing base, enumerates the exact operative replacements (e.g., drand-only, separate freezes, attempt anchors), and states that V15 continues to govern on any conflicts outside this amended scope.

**9. ANYTHING THE CHANGES BROKE:**
Nothing was broken by these changes. The partial fix to `select_sample.py` regarding duplicates (allowing sets to naturally handle them for exclusion/failed lists while enforcing strict ascending/no-duplicates on the eligible list) is robust. The shift from local clocks to public third-party anchors is a strict security upgrade. The adversarial sweep correctly mapped out the necessary compensating controls (published drawn-list hashes) for the deferred elements.

VERDICT: REVIEWABLE-AND-SOUND
