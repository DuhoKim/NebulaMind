ACCESS_SHA=c0459ad1b16cfdec2333eabae4d78284f09f4c396e1781e489cbbf8cca6c7d12

[MAJOR] 1. THE MEDIUM PRODUCER.
The implementation in `medium_perturbation.py` exactly implements the stated method (computing chi with pixels kept versus replaced by the lower median, reporting sign flips). Label-blindness is strictly enforced by construction because `_FIELDS` strictly limits the input dictionary keys and omits any label data. The preprocessing rule evaluates all 96 configurations in a fixed, exhaustive order (`fourier_chirality.enumerate_configs()`), which cannot be varied. No search choices are smuggled.

[MINOR] 2. THE TWO REGISTERS.
Nothing the run path actually reads is missing from CORE. (FITS planes and checksum manifests are stage evidence acquired at runtime, not input-stage configuration). The runtime pins correctly state their limits, explicitly noting that they do not claim byte-level identity for dynamically resolved extensions, standard libraries, or transitive dependencies. They do not claim more than they can establish.

[FATAL] 3. READINESS.
The `ready_for_input_freeze` flag is enforced in `run_path.py` as a gate, but it can be evaluated as TRUE while required work remains absent. Specifically, the "runtime representation" is explicitly declared as a current preparation obligation that remains unresolved. However, this obligation was silently omitted from the `current_preparation_obligations` array in `INPUT_MANIFEST_A1_CORE.json`, allowing the readiness flag to be declared TRUE prematurely. 

[FATAL] 4. THE REFUSALS.
The readiness-false refusal check in `run_path.py` (line 301) only checks that `all(p.get("resolved") is True for p in obligations)`. A trivial tamper the checks would miss is deleting an unresolved obligation from the JSON manifest entirely (which was done for the runtime representation obligation). The checks pass on a broken/incomplete implementation because they do not verify the completeness of the obligations list. Furthermore, the file-hashing checks are susceptible to TOCTOU (Time of Check to Time of Use) tampers, as files like `numpy` extensions are hashed by `read_pin` and later loaded by Python's `import` mechanism without a persistent lock.

[MINOR] 5. COUNTS AND CLAIMS.
The check counts (122, 42, 32, 16, 4) are correctly stated separately and are not overstated for what they test. No sentence in A1 or the decision sheet claims completeness that is not true; both documents explicitly acknowledge that implementation preparation is not declared complete.

[MINOR] 6. ANYTHING THE CHANGES BROKE.
No previously sound mechanisms from the second review were broken. The validation gate properly computes the 5% attrition floor, and the run path correctly enforces exact-draw evaluations.

VERDICT: DELTA-NOT-SOUND
