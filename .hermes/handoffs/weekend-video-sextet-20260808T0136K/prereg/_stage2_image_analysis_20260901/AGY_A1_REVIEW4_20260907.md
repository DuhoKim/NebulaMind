ACCESS_SHA=53f8bef5cf24bdcf80f0658b0238639ab274ccb96f4aee3e95ab03fe2d39a906

[MINOR] 1. COMPLETENESS (run_path.py:374)
The `OBLIGATION_REGISTRY` now declares the exact set of obligations independent of the manifest. Lines 374-375 compute `missing` and `undeclared` sets against the manifest's list, and line 393 requires `not (missing or undeclared or duplicates)`. Deleting an obligation causes `missing` to populate; renaming causes both `missing` and `undeclared` to populate. All present obligations must be True. The gate cannot pass if an obligation is missing or manipulated.

[MINOR] 2. READINESS FALLS OUT OF THE PREDICATE (run_path.py:408)
The predicate `input_readiness(c)` independently evaluates readiness. Line 408 requires `c.get("ready_for_input_freeze") is True and computed["ready_for_input_freeze"]`, meaning a hand-set TRUE flag in the manifest cannot override a FALSE computed value. The computed value correctly evaluates `RUNTIME_REPRESENTATION` as false because the `A1_OBLIGATION_STATEMENTS` mapping statically bounds it to the A1 document, preventing a manifest-only edit from resolving it.

[MINOR] 3. THE A1 CROSS-CHECK (run_path.py:301)
Line 301 strictly enforces the SHA-256 hash of the pinned A1 document (`require(sha(raw) == A1_REVIEWED_SHA256`). Thus, the stated obligations in A1 cannot be altered to fool the parser without changing the document's hash and failing the `A1-OBLIGATION-SOURCE-CHANGED` refusal. The mapping in `A1_OBLIGATION_STATEMENTS` is tightly coupled to the exact reviewed text.

[MINOR] 4. THE TOCTOU DISCLOSURE (run_path.py:14)
The TOCTOU limitation is accurately disclosed. Lines 14-16 in `run_path.py` and lines 217-219 explicitly state that there is no persistent lock between hashing and loading, and that the pre-import recheck only evidences disk bytes at check time, not the bytes the loader used. It does not overstate the guarantee or quietly widen it.

[MINOR] 5. DID THE REPAIR BREAK ANYTHING (run_path.py:692)
Nothing previously cleared was broken. The exhaustive 96-configuration search remains intact (line 692), label-blindness is enforced by construction, the CORE consumer logic is unchanged, and the refusal mechanisms are intact. Test results in `_tmp_v47_after.txt` confirm all 122 existing checks pass alongside the 14 new readiness tests.

VERDICT: REPAIR-SOUND
