ACCESS_SHA=f5100a046ba63c9e043e7ff67ef6e01e7621fe12eaa1097ac68a0fd96b5ffeec

[FATAL 1] IS BYTE BINDING REAL? (run_path.py:408-418, runtime_binding.py:92-140)
Byte binding is now real for the disk copies. The implementation hashes the entire cache subfile on disk (`dyld_shared_cache_arm64e` and all its suffixes) instead of relying solely on the OS-reported identity. The binding covers the on-disk copy of the cache file rather than the specific image bytes the process actually loaded in memory. A change could evade this check via an alternative custom loader, via in-memory modifications after the image is loaded by the OS, or by reading native resources not passed through the disk file.

[FATAL 2] IS READINESS NOW EVIDENCE-DRIVEN? (run_path.py:428-472, 480-493)
Yes, readiness is completely evidence-driven. The `input_readiness` predicate now computes readiness exclusively by executing `EVIDENCE_PREDICATES` (such as `_runtime_representation_evidence`), which directly measure the runtime environment and cache byte bindings. A document edit can no longer flip readiness to TRUE; an obligation cannot be resolved without its evidence predicate actually returning true in code. The A1 cross-check (`_a1_consistency`) now only compares the mapped prose against the computed code evidence and can only raise a `Refused` exception if they disagree.

[MAJOR 4] THE DECISIVE TEST. (test_evidence_gate.py:99-133)
The decisive test (`test_real_cache_resident_image_bytes_change_same_uuid_and_os_build_refuses`) correctly proves exactly what it claims. It isolates a copy of the cache, mutates a single byte in the `__TEXT` segment payload of CoreFoundation, and verifies that the `SHARED-CACHE-BYTES-MISMATCH` exception is raised. The test explicitly verifies that the OS-reported UUID and OS build remain strictly unchanged before verifying the exception. The test passes because the implementation actually hashes the file bytes. It would correctly fail on a hollow implementation that only checked headers/UUIDs because the altered cache file would be accepted without raising the exception.

[MAJOR 3] THE TOCTOU DISCLOSURE. (AGREEMENT_RUN_AMENDMENT_A1_20260907.md:43-45)
The TOCTOU disclosure has been completely restored to the wording previously found accurate. The widened caveats (disclaiming custom loaders, arbitrary in-memory mutations, and unobserved native resource reads) have been removed.

[MINOR 5] WAS ANY OBLIGATION QUIETLY AMENDED (AGREEMENT_RUN_AMENDMENT_A1_20260907.md:47)
No obligation was quietly amended, nor was any refusal, disclosure, or check weakened to make this pass. The obligation text still properly requires "reproducible byte binding for selected import artifacts AND OS shared-cache images."

[MINOR 6] ANYTHING BROKEN THAT YOU PREVIOUSLY CLEARED (run_path.py:391-405)
Nothing broken. The scientific MEDIUM producer, CORE registers, completeness checks, and counts remain fully intact. The exhaustive 96-configuration search is still enforced. Unseen data remains unopened, and drand constraints are untouched.

VERDICT: FINAL-SOUND
