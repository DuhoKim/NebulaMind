ACCESS_SHA=6b9ecc79210046fa4c6611953184ece75818214bf4e962e4ac2e308243616e9f

### 1. BINDING CONSISTENCY
**EDITORIAL/HISTORICAL**: The bindings are completely consistent. The `run_path.py` A1 pin (`A1_REVIEWED_SHA256`) exactly matches the final A1 file digest (`6b9ecc79210046fa4c6611953184ece75818214bf4e962e4ac2e308243616e9f`). The CORE manifest (`INPUT_MANIFEST_A1_CORE.json`) binds this exact A1 hash. Readiness is strictly evidence-driven, derived by executing the consumer verification against the CORE manifest (`_tmp_v55_refresh_REPORT.md` confirms readiness is dynamically derived by execution, not from a stored text flag). All pins are fresh and correct. The Decision Sheet contains a dated observation of the CORE digest from prior to the V55 refresh, but this is explicitly labelled as a historical per-presentation observation and does not invalidate the technical bindings or readiness.

### 2. THE COMPARISON ITSELF
**EDITORIAL/HISTORICAL**: The statistical comparison is perfectly intact, correctly stated, and logically sound. 
- The eligible population (11,837), explicit exclusion subtractions, and the final post-exclusion population (7,283) are exact and reproducible.
- The 400/200/2,000 exact draws are hardcoded and must be fulfilled. 
- The 96-choice tuning is correctly constrained by code.
- Tuning/holdout separation strictly enforces an external WINNER FREEZE (`W`) before any holdout access, allowing only one holdout evaluation.
- The agreement statistics correctly implement the designed asymmetry: tuning (`/400`) and holdout (`/200`) use fixed exact draw sizes as denominators (treating unscored as misses), while validation uses the scored count `m` as its denominator (excluding refusals entirely).
- The scored-count floors (380/190/1,900) are accurately enforced only *after* the estimator runs, preventing their misuse to authorize smaller draws. 
- The one-further-attempt limit is plainly stated and enforced. 
This package describes a rigorous, heavily guarded pipeline that will produce a trustworthy number.

### 3. OVERCLAIMING AND READINESS
**EDITORIAL/HISTORICAL**: No document overclaims adoption or approval. A1 and the Decision Sheet explicitly and repeatedly state that "Duho has decided nothing", that readiness implies only that the input files are ready to freeze, and that readiness grants no run permission. All previous misstatements have been corrected.

### 4. REMAINING RESULT-INVALIDATING DEFECT
**EDITORIAL/HISTORICAL**: There is no remaining result-invalidating defect. All prior findings, including the count discrepancies and stale pins, have been methodically resolved. The package is fully consistent.

VERDICT: SNAPSHOT-SOUND
