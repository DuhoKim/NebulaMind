# BLANC ORDER — M5 is a provenance defect, not a housekeeping item (2026-09-06 22:58 KST)

You accepted all five V23 findings; good. I want M5 handled differently
from the rest, and M4 handled before Duho sees Q1.

## M5 — pins edited in place after review

Four files pinned at V22 (`history_v2.py`, `test_track1_fail_first.py`,
`coherent_attacks.py`, `test_track2_fail_first.py`) were edited IN PLACE
after the V22 review. So a V22 pin no longer describes what the V22
reviewers actually read.

This is the THIRD pin drift today: the README against its pin sheet, the
decision brief's stale reconciliation digest, and now this. Each was
harmless alone. Together they are a pattern, and the pattern attacks the
one property this whole programme is selling — that a stated digest tells
you what someone actually examined.

Required:
  1. Do NOT retro-edit the V22 pins to match. Record instead: for each of
     the four files, the digest as pinned at V22, the digest now, and what
     changed. A reader must be able to see that the V22 verdicts were
     rendered on the earlier bytes.
  2. Adopt a rule and write it down: once a file is pinned into a
     dispatched review, it is IMMUTABLE until that review's verdicts are
     filed. Successors get new filenames, as you already do for modules.
  3. Say in the record whether any V22 VERDICT depended on the edited
     bytes. If one did, that verdict is about superseded code and must be
     labelled so.

## M4 — before Duho rules on Q1

The questions file presents Option A as the expired-receipt workflow, but
A cannot carry a composed invocation past loss of the HISTORY-OPEN event —
there is no receipt path for it. Fix the options before he sees them. He
must not be choosing between a real option and one that does not work as
described; that is worse than offering him fewer options.

M1, M2, M3: repair in the next preparation round as you proposed.
Nothing adopted. Q1 stays open and un-presented until M4 is corrected.
