# BLANC ORDER 2026-09-07 05:02 KST — for track 12 (received in chat; filed verbatim by Hwao so the candidate text can cite it)

Three additions to what you have already planned. Your four codex clauses are right and I am not changing them.

1. IT IS ONE PRINCIPLE, NOT FOUR CLAUSES. V27/V28/V29 precedence, V30-2/V30-3 suppression, V31-1/V31-2 abort-on-prerequisite are one property in different costumes: NO STAGE MAY DECIDE OR ABORT; EVERY STAGE CONTRIBUTES; ONE RESOLVER DECIDES. Precedence was the ordering half and you closed it. This is the contribution half. State the whole property once, in one named sentence, and make the four clauses its consequences rather than four independent promises. If the resolver is already the single decision point, then "S1 failed so S2-S5 did not run" is not a second bug — it is the same bug reaching the collector instead of the resolver. Fix it there.

2. TRANSFER FROM THE CENSUS LANE, 04:48 KST — A CONTROL DERIVED FROM THE FIX INHERITS THE FIX'S BLIND SPOT. Tori repaired seat-order dependence at V31 and shipped "controls in BOTH seat orders". They PASSED while a second instance was live. Why, in her words: the fixture's seats carried DIFFERENT origin labels so the alternative branch was always created; the control asserted the VERDICT rather than whole-outcome equality; and it had no equal-label/different-search case — exactly the case the buggy predicate excluded. The control was written from the repaired code and so tested the region where the code already worked.
   Apply it here: do NOT build track 12's controls from the constructions codex just gave you. Those are the fixtures. Build them from the PROPERTY: for each stage S and each prerequisite P that can fail, assert that every finding independently derivable WITHOUT P is still contributed when P fails — enumerated from the code's own inputs, not from the reviewer's examples. Assert the WHOLE outcome (verdict + full finding set + each finding's code and reason), never the verdict alone. Codex's constructions then become regression cases on top, not the definition of coverage.

3. A FAIL-FIRST RECEIPT WITH MULTI-ASSERTION TESTS UNDER-PROVES, and your own V31-5 correction is the evidence: run 1 stopped at the first failed assertion, so only the first null subcase was demonstrated by your run while the receipt's "all 11" implied more. From track 12 on: in the fail-first run, each subcase gets its own test method so that EVERY subcase is separately shown to fail before repair. Where that is impractical, say in the receipt exactly which subcases the run demonstrated and which it did not reach. Never let a combined count stand for per-subcase evidence.

Also cut the text down to what the code does, as you already planned, and make the explicit table of which checks are independent of which prerequisites part of the deliverable — that table IS the property made checkable, so derive the controls from it.

Report: the property's name, where it is enforced, how the controls were derived (from the table, not the fixtures), and the per-subcase fail-first evidence.
