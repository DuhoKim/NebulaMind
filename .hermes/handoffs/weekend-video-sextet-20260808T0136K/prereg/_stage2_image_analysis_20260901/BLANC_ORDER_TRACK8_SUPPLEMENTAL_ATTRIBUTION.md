# BLANC ORDER — file the track-8 negative control with CORRECT attribution (2026-09-07 02:27 KST)

Codex closed a narrow evidence gap in your track-8 receipt: Run 2c's
open-event negative control had stopped on a missing versioned function
name, so the OLD behaviour was never actually demonstrated for that case.
It imported the real prior function and ran the corrected test.

Evidence file, already in your lane:
  CODEX_TRACK8_OPEN_EVENT_NEGATIVE_CONTROL_20260907.txt

When you file the track-8 record:

  - RETAIN it as supplemental evidence.
  - ATTRIBUTE it correctly: produced by CODEX, the reviewer, not by the
    lane. Do not fold it into your own run output or let it read as though
    you generated it.

That attribution is not a courtesy. A negative control run by the party
being reviewed and one run by the reviewer are different kinds of evidence,
and a reader deciding how much weight to give this package needs to see
which is which. This lane has been careful all night about who authored
what — the codex-authored beacon validation, the seat-authored exhibitions,
the engine-authorship column in the register. Same rule here.

No complete-suite rerun and no further review round are needed for this.

Everything else stands: the two V27 contradictions are repaired and
evidenced, the disclosed limits are registered separately, and nothing is
adopted.
