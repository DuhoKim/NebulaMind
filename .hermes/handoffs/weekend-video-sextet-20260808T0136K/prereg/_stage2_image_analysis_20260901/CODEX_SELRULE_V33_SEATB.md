ACCESS_SHA=f294cb55ac4c87daf156d2b9875d9bbd40434d93908f64c536cb1dbc751bff15

Completed independent review. V33 is NOT-SIGNABLE: remaining contradictions in NSD, table verification, receipt handling and evidence accounting prevent offering these exact bytes as a truthful adoption package.

This is a review of the exact staged, unadopted V33 package. Engine-lineage disclosure: the package attributes earlier review constructions and superseded builder work to Codex. I did not modify candidate code in this review. The retained regression fixtures are dependent evidence; the new fault injections, differential comparisons and table mutation below are additional executions, not independent implementations of the whole system. Fixture remotes are local bare repositories; event runners and explicitly identified failure injections are synthetic. No public repository was modified. The aggregate used the pinned CommandLineTools Python 3.9.6, the declared BLS site-packages, PYTHONDONTWRITEBYTECODE=1, -W error::ResourceWarning, and each suite's own directory. The inherited fourier_chirality.py:87 RuntimeWarning occurred; no ResourceWarning was reported.

1. [FATAL] UNREPAIRED CONTRADICTION: NSD still fails at actual-input boundaries. Clauses: candidate introduction, line 3, and §3c, line 38. Code: run_configurations_v17.py:451–452, 484–489, 503–512, 542–551; precedence_core.py:96–107; provenance_designs_v13.py:490–527.

Executed cases:

- With witness fetch failed, malformed T_pulse alone contributes IDENTITY-T-PULSE under identity-local. Adding malformed created_at removes that contribution and produces only IDENTITY-WITNESS-TIME. Both retained-event digests were coherent. _time_checks returns on its first parse failure, hiding the independently parseable second input.
- A coherent retained approval event with commits=[null] and head/before requiring delivery evaluation contributes LOCAL-TERMINAL MALFORMED-RETAINED-INPUT when the conjunction runs. Failing only witness fetch leaves IO-UNAVAILABLE at approval-delivery and MALFORMED-REMOTE-EVIDENCE at approval-live, with no local-terminal contribution. The live feed carries the genuine, different same-id event, but that contradiction is not collected either. A retained-data AttributeError is classified using the enclosing stage's git/remote label, and delivery aborts authentication before the independent same-id check.
- A five-publication history containing a final rewrite normally contributes HISTORY-NOT-AN-EXTENSION. Injecting OSError while reading the second committed history blob stops the entire extension loop. The later, independently readable adjacent relation is never checked. _prev is never installed, so working_tree defaults it to empty bytes and falsely reports PENDING-PUSH for 708 already-published bytes. No check is blocked; S5 is marked completed. The callback preserves findings already derived, but cannot preserve findings the aborted loop never derives.
- PermissionError reading the adoption file contributes IO-UNAVAILABLE, but adoption-identity is neither evaluated, blocked nor represented by a finding. S0h's named-block handler catches only DataIntegrityFail. Seed is blocked; that does not account for the missing adoption comparison.

These are not L-OFF, L-COV or availability limits. They contradict the explicit promises that the first failure never hides the next, operations are classified at their input boundaries, and blocked checks are named. They also corrupt the proposed failure-state explanation: the history probe's PENDING-PUSH is false.

2. [MAJOR] UNREPAIRED CONTRADICTION: the table is neither exact nor completely verified. Clauses: E3(i), introduction, §3c INDEPENDENCE TABLE. Code: run_configurations_v17.py:404–416, 433, 542, 768–784; test_track13_table_vs_code.py:12–52.

The 16 check rows and 10 prerequisite rows in the candidate exactly match the current Python table data; I regenerated and compared all 26. That establishes rendering fidelity, not correspondence with execution. An ancestry-based approval event accepts with git available. With only git launch failed, approval-live contributes RETRY-EVENTS-UNAVAILABLE instead of its empty baseline. Yet its needs omit git-launch. The normal controls use a same-id contradiction recipe that resolves before this ancestry-dependent branch. A correct contract needs to represent conditional dependencies rather than infer independence from that recipe.

The executed adoption and delivery probes also contribute IO-UNAVAILABLE under checks whose declared codes omit it. Actual local sub-check findings report stage='sub', although the table names S0e/S0f; history-remote also reports from S0b-root, although its table row lists only S5-history.

Mutation control: I removed EVENT-FORGED from approval-live.codes in memory, ran all four table tests, and restored it. All four passed. This code is selected through m[o], not a literal add call matched by the regex. Other uncovered sites include stage/sub exception contributions, dynamic history callback contributions, block()/info['blocked'] updates, and callees such as _conjunction_prefix outside the extracted source slice. Stage verification tests membership in a set, not equality of every contribution site's stage. Prerequisite verification checks declared-name membership and pair counts; it does not extract actual reads or validate every failure code/class/how. Helpers, resolver and split are explicitly excluded. Thus the claim 'every site/every code/every stage/every prerequisite' is false.

3. [MAJOR] UNREPAIRED CONTRADICTION: the built approval-receipt success path refuses. Clauses: E3(i) composed wiring, §3c receipt substitution, questions file's approval-only built mechanism. Code: run_configurations_v17.py:543–550.

On load_identity, I isolated approval authentication to return EXPIRED and the receipt verifier to return (True, valid), retaining genuine history evidence and the production seed re-deriver. The verifier was called once; every other stage completed. S3a then evaluated m['EXPIRED'], which does not exist, and contributed MALFORMED-REMOTE-EVIDENCE: KeyError: 'EXPIRED'. These are labelled boundary stubs, not a claim that a real OPS receipt was created. They establish that even successful receipt verification cannot complete the advertised branch. The disclosed absence of history-open/per-entry receipts does not disclose this separate broken approval branch.

4. [MAJOR] The differential controls are useful but weaker than the promised property. Clause: §3c 'whole outcomes' and per-subcase coverage. Code: test_track13_nsd_table.py:19, 57, 87, 102, 111–139 (DEFECTS, fail_prereqs, by_check, baseline and _make).

The differential idea is appropriate: failure of an unrelated input must preserve a check's contribution, and unavailable dependent checks must remain accounted for. Generation really gives 110 pairs plus six doubles, with identity-file pairs included. However, each check has one planted defect recipe, not one per predicate/refusal/input failure. Identity-file failure means deletion only; adoption-file failure means deletion only; the integrity recipe changes pool digest; the per-entry recipe is batching. Neither timestamp interaction, adoption PermissionError, middle-blob exception, successful receipt, nor ancestry-dependent approval is reached by those recipes.

by_check reduces findings to sets of code strings. It discards class, stage, reason, multiplicity and order. HISTORY-NOT-AN-EXTENSION and false PENDING-PUSH share HISTORY-CONTINUATION, so this comparison cannot distinguish them. Baselines come from the implementation and require only the designated planted code, no blocked checks, and its own resolver's winner. An incomplete baseline can therefore become the oracle. The needer assertion also permits silence whenever the name is blocked, without checking that unrelated findings under that name survived. Passing method counts establish these selected cases, not all actual-input subcases.

5. [MAJOR] Evidence and candidate text do not consistently name the same reviewed package. Clauses: introduction line 3, §3c, §9; STAGED_EVIDENCE_MANIFEST.txt:4.

The introduction still says 'run 2 on the successors: 16 OK' and contains literal {NCTRL}. The retained run-2 regression log says FAILED (failures=1). The receipt correctly leaves the claimed post-fix run-2 pass OPEN; runs 3 and 4 are new executions and do not recover it. The introduction must carry that correction too.

The introduction pins obsolete runner/gate hashes and says the gate suite has five tests; the staged suite has eight, all independently rerun successfully. Actual runner SHA-256 is 0a92eb4b14a9ab7f1d68ebf4c0abf76ac4ce2572dd3c0597398a93d4e2dec479; actual gate SHA-256 is ac844f7fd33ff983929c6b862ce15afc6332d63b4b7d9c2a0a7491d6331a0c25. The staging record's later correction has these values, but the candidate does not. The attack inspection's prose still says 105+6 controls; its 35 result rows remain reproducible. The aggregate invokes retained track-8–12 modules: for example track11/test_track11_fail_first.py:9,19 imports provenance v11/driver v15, and track12:11–12 imports v12/v16. Their passes are not executions of v13/v17 merely because they appear in the V33 aggregate; the introduction's separate claim about 15 earlier tests on the successors is not established by those aggregate blocks.

I rehashed all 104 manifest entries: 103 match. The staged suite spec actually hashes to 88449e3636171a70e69c60eefa7772eb066418747f17e873cf6a6c2eb43c8f36, not its manifest's a513bf3f5cc50431293ad6e1b2fde1540a1579eac96c589c64c5773ff981139e. Its paths are correctly relocated to this sandbox; this is stale verification metadata, not evidence of changed test logic. A scan of 100 explicit Python/text/data pins matched 98; the exceptions were the obsolete gate pin and the absent no-r registry. The runner pin was checked separately. The three principal implementation pins match.

The fresh aggregate completed at 08:27:47 KST: 32 suites, 365 tests run, 29 suites EXIT 0 and three EXIT 1. Gate v3 returned 2 (FAIL). Both driver suites ran 23/17 tests with one error each in test_real_builder_output_loads_through_load_identity; the original builder suite errored in setUpClass and ran zero instead of four. All three failures stem from absent scratch/survey-bricks-dr9-north.fits.gz; validation_bricks/_bricks_without_r_coverage.txt is absent too. These are additional missing fixture inputs beyond the disclosed guarded-pool/GZ1 limitation. The other suites pass, including track13 regressions 16, controls 116, table tests 4 and text test 1; track12 passes 84. No interpreter check was relaxed and no live-lane dataset was substituted. The fresh aggregate log hashes to ee535efa8bad7c633b7d8b0fe2c23f9ca85ec4b518c61edba5dbf9160fc431ea.

The retained RUN2 portable copy independently passes gate v3: 32 suites, 369 tests, every child EXIT 0, matching complete outputs. RUN1 certifies nothing. Eight gate tests pass, including a fresh child printing OK then exiting 17; v2 accepts the retained exit-17 display log and v3 refuses. Gate-test scratch was redirected to /tmp so the candidate tree was not altered.

The fail-first receipt's retained diagnostics support regression run 1's 14 behavioural failures plus two new-interface errors; v2 control run 1c's 19 behavioural failures plus 91 check-attribution errors; v3 runs 1d/1e's 111/116 baseline KeyError('check') errors, all new-interface; successor run 2d's 16 failures among 111 methods; and final successor logs' 16/116/4 passes. No v3 behavioural fail-first standing against v16 follows. Driver invocation failures are environment/import-path failures as disclosed. V32 had one completed seat; the supplied timeout evidence makes seat A's report provisional, not a verdict. The staged outcome's annotations and unsafe-wrapper withdrawal are retained, not retroactively treated as completion evidence.

The three states were exercised. The 35-row attack table matches the filed final table after normalizing run-dependent commit ids. Offline defaults still accept counter-cases 1, 2 and A and arbitrary coherent lists; they refuse second approval paths, CLOSED history, missing accepts and the other named conjunction defects. Standalone v13 helpers accept genuine published history and refuse local event mismatches, rewrites, pending/diverged history, batches, malformed evidence, and unavailable/incomplete/expired evidence under their stated dispositions. Composed load_identity accepts the genuine fixture and refuses attacks 1/2/A as EVENT-FORGED, HISTORY-NOT-AN-EXTENSION and OPEN-NOT-GENESIS-ONLY. It preserves the reproduced V32 repairs but also exhibits findings 1–3 above. PRODUCTION.rederive_seed is the actual production_rederive_seed; normal fixtures use that function, with real archived BLS evidence. The production CLI test ran; the full catalogue-to-loader test could not complete here.

The five disclosed limits in §3c are legitimate categories: L-OFF (offline authentication gaps), L-COV (publication evidence does not prove operation time or unpublished decision completeness), L-RCPT (the first OPS-labelled receipt still needs OPS's independently retained copy), L-INH (list trust with verify_split off), and L-AVAIL (bounded event availability and covenant-only study closure). GitHub documents the 300-event/30-day window and 30-second–6-hour latency. [GitHub Events documentation](https://docs.github.com/en/rest/activity/events). Those limits do not cover findings 1–3. The OPS label is lane-proposed, unadopted; delegation requires no per-receipt human confirmation. Option C is the recommended live-only policy. A′ lacks history-open/per-entry receipt support, and its approval branch is additionally broken as above. Option B is not implemented; stronger independent decision evidence is not implemented.

V32 disposition: V32-1 NOT REPAIRED as a general property, although the named identity/open/import/nonce/degraded-byte examples are repaired; V32-2 REPAIRED for partial-prefix absence deciding FORGED, including the completion control; V32-3 NOT REPAIRED as an operation-boundary/durable-collection guarantee, although previously recorded findings now survive the tested later exceptions; V32-4 REPAIRED by runner v2.1/gate v3.1; V32-5 dynamic refusal families and blob-equality wording REPAIRED, historical run-2 gap DISCLOSED-AS-OPEN in the receipt but NOT REPAIRED in the candidate's accounting. Seat A's two NSD/table observations remain NOT REPAIRED in their general form. Blanc's property has a clear name and intended enforcement point, although it is repeated in the accumulated text rather than stated literally once. Controls are genuinely table-generated, but the table and per-subcase oracle are incomplete.

Preserved: 400/200/2,000 sizes; 380/190/1,900 floors; strict Wilson lower-bound >0.70; failed-set exclusion; 2,644 dry-run ids; excluded rounds 6440756/6441904/6441924; E5(d)'s separate-account, point-in-time custody receipts and interval covenant; blindness and fetch/lift ordering; an actual prospective round derived from approval time, not an exhibited historical round; one further validation attempt. holdout_once remains prepared/unadopted, default False; the one-holdout covenant remains binding. verify_split remains unadopted, default False. E1's 17-test correction is a factual erratum, not a new scientific choice. Constants/defaults and exclusion count were checked; estimator tests pass 10/10 and the validation-gate suite passes 14/14. The property exhibit ran twice identically with internal digest 2160fa754ce3db25613389d58bacd3098763ea8536744558e9dae96c5b4f0ae1; the drand-only exhibit reports 45d6149a2ccf30cb167dc4331455f3403a733516a82399292b1e7ad2c029e531. No future study draw, custody operation or guarded-pool reconstruction was performed. Those operational proofs remain future obligations, not evidence supplied by these fixtures.

Cost remains one acknowledged push per collector/builder attempt/history entry, including failures, with pending-push reconciliation and refusal of batching/unrelated unpublished commits. PENDING-PUSH means publication is not acknowledged; HISTORY-DIVERGED refuses a reset/rebuild; RETRY-REMOTE-UNAVAILABLE means the remote cannot currently be examined. Development/fresh-fetch estimates remain about 7.8/26 GB. Finding 1 shows the validator can presently misreport the pending state.

New-execution evidence is retained outside the candidate tree: /tmp/codex_v33_seatb_run.log and /tmp/codex_v33_seatb_run/suites/status.tsv plus each suite's complete stdout/stderr; /tmp/codex_v33_seatb_attacks.txt; /tmp/codex_v33_seatb_probes.txt; /tmp/codex_v33_seatb_probes2.txt; /tmp/codex_v33_seatb_needs2.txt; /tmp/codex_v33_seatb_retained.txt; /tmp/codex_v33_seatb_standalone.txt. The fault probes use track13/test_track13_regressions.H13.build/build_plant and call rc.load_identity, inspect LAST_OUTCOME, and inject only the named failing input boundary. The ancestry probe sets before to the approval commit's parent, head to the last history commit and commits=[]; baseline ACCEPT becomes approval-live retry when subprocess.run rejects git launch. The middle-blob probe fails only _blob_at for evs[1].payload.head; the later rewrite's adjacent inputs remain readable. The table mutation changes only the in-memory codes set for the duration of the four verifier tests.

Required replacement clauses before asking Duho to adopt exact behaviour:

“Every independently evaluable predicate contributes a finding or a named blocked state. Timestamp parsing, each committed blob read, each adjacent extension relation, each publication predicate, and adoption-file versus identity comparison have separate input boundaries. An unavailable blob never substitutes empty acknowledged history. A local retained-data exception remains local regardless of the enclosing stage or another prerequisite's failure. Obtainable same-id contradictions are still collected when delivery parsing fails.”

“The check registry enumerates every contribution and block site, including helper callbacks, dynamically selected codes and exception outcomes. It records conditional input dependencies and actual owning stages. Coverage checks reject unregistered sites/codes/stages and wrong prerequisites. Controls cover every declared predicate and failure variant, compare class/code/check/stage and stable reason semantics, and retain per-subcase evidence; method totals alone are not completeness evidence.”

“A successfully verified expired-approval receipt completes approval authentication without a refusal. Missing or rejected receipts retain the stated class-3 loss policy. This mechanism substitutes for approval evidence only; other receipt coverage remains unimplemented.”

“The adoption packet identifies one exact candidate and executable set with matching current hashes. Unsupported regression run-2 success remains an open historical gap; only retained runs 3/4 are passes. Runner v2.1 and gate v3.1 judge complete raw outputs and actual child statuses. Missing catalogue inputs and non-reproduced tests are listed explicitly; relocated evidence has its own correct digest.”

After those repairs and executions, Duho still decides the proposed composed/live-only policy, whether to adopt holdout_once and verify_split, and any future receipt identity/key upgrade. No routine per-receipt confirmation is required. Nothing in this review adopts any option.

VERDICT: NOT-SIGNABLE
