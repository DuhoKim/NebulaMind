# DISPATCH DEFECT — a seat's leftover child keeps the referee wrapper alive; process absence becomes undecidable (filed 2026-09-07 06:43 KST; Blanc 06:35 item 4)

**Observed (V32 seat A, agy).** Report `AGY_SELRULE_V32_SEATA.md` complete and byte-stable at 5,347 bytes from 05:58:07; the seat process at 0.0% CPU; its ONLY child `tail -f test_output.log` (a log follower agy left running); the wrapper (pid 63711) therefore could not exit before its 90-minute print-timeout (~07:17). The lane's read rule — read a seat's report after its process exits — could not fire; the read was made under a recorded exception (`V32_REVIEW_OUTCOME_20260907.md`). This will recur for any seat that leaves a follower behind.

**Cause.** `nm_referee_dispatch.sh` v1 (sha256 prefix fd3aa3b9) ran the seat in the foreground and judged completion by process exit alone.

~~**Fix (v2, sha256 prefix fd3aa3b9433c8d6a, in place at `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh`; v1 retained beside it as `_nm_referee_dispatch_v1_RETAINED_20260907.sh`).** The seat runs in ITS OWN PROCESS GROUP (`perl -e 'setpgrp(0,0); exec @ARGV'`). A WATCHDOG judges liveness on the seat's OWN WRITING: once the report exists, carries the expected `ACCESS_SHA=` line and a `VERDICT:` line, and has been byte-stable (size and mtime) for STABLE_SECS (default 120 s), the whole process group is terminated (TERM, then KILL after 10 s) and the action is logged: `SEAT DONE BY REPORT STABILITY … terminating its process group <pgid> (pids: …)`. A seat that is still writing is never killed; a seat that has finished is never waited on for a stray child. Every other guarantee of the wrapper (ACCESS PROOF prepended, digest computed by the wrapper, quarantine + exit 3 on a missing proof) is unchanged.~~

**⟵ SUPERSEDED AND WITHDRAWN (2026-09-07 06:46 KST; Blanc 06:45). The paragraph above is WRONG in the dangerous direction and is left only as history: it describes a stability-killing watchdog as "the fix … in place" and pins it to a digest that belongs to the file that does NOT contain it. TRUE STATE, verified by me at 2026-09-07 06:46 KST: the live `/Users/duhokim/HermesOps/scripts/nm_referee_dispatch.sh` is sha256 fd3aa3b9433c8d6a2c2b0530a8a8ea1465885b6677afed97de58f644a9632635 — the RESTORED SAFE v1 — and contains 0 matches for kill / TERM / setpgrp / STABLE_SECS; `_nm_referee_dispatch_v1_RETAINED_20260907.sh` has the same digest (fd3aa3b9433c8d6a…); the v2 watchdog was installed by me at 06:41 KST, replaced by the coordinating Codex's restore of v1 at 06:42 with a compare-before-replace guard, NEVER used by any dispatch, and is archived UNSAFE at `_nm_referee_dispatch_v2_STABILITY_KILL_UNSAFE_20260907.txt` (sha256 5f4b0a39adf8b21b44928d255cc1b71ae48b8bd7259cb5bbd184f56cf6b6edbf); no process was signalled (agy 63737, wrapper 63711, follower 12612 ran on untouched). PROHIBITION, stated here so no later reader restores the killer believing it sanctioned: report byte-stability, low CPU and the shape of the process tree are NOT completion evidence; a watchdog that terminates a seat CONVERTS TRUNCATION INTO APPARENT COMPLETION — the seat's disappearance is the very signal the read-after-exit rule keys on — so it can never be the basis of a read. Completion = EXPLICIT completion evidence from the backend, or the GENUINE exit of the owning process. Child cleanup only AFTER proven completion. A deadline is a timeout / failure, never a pass. The defect in the OBSERVED section is real and STILL UNFIXED; the fix is STAGED, not applied; no new dispatch may use a modified wrapper until Blanc's active-backend test (b) passes.**

**Controls (kept in `scripts/dispatch_fixtures/`) — evidence of the DEFECT and of v2's behaviour, NOT of a validated fix.** A FAKE seat (`fake_seat_leaves_tail_f.sh`) writes a valid report (ACCESS_SHA + VERDICT) and then runs `tail -f` forever — the observed defect. (1) v1 wrapper + fake seat under a 150 s alarm: HUNG until the alarm (exit 142, SIGALRM), the report present but never read — the defect reproduced (`control_v1_wrapper_hung_150s.log`). (2) v2 wrapper + the same seat with STABLE_SECS=30: exited on its own after 40 s with `SEAT DONE BY REPORT STABILITY` and `ACCESS PROVEN`, no leaked `tail` process (`control_v2_wrapper_exited_40s.log`). Both logs retained.

**Scope.** This wrapper is shared tooling under Duho's 09-03 referee-dispatch rule; the change adds a completion criterion, removes none, and is disclosed to Blanc. The V32 seat A wrapper already running under v1 is left to its own timeout (not killed; no live review interrupted).


## WITHDRAWN (2026-09-07 06:44 KST; Blanc 06:44) — the v2 above is UNSAFE and is NOT installed
Blanc's 06:35 item 4 was, in his words, badly worded, and I built the wrong thing from it: v2 terminated the reviewer's whole process group once a report carrying ACCESS_SHA and a VERDICT line had been unchanged for 120 s. That is unsafe: a reviewer may write a PROVISIONAL report and revise it after a long test or a backend wait; a two-minute plateau would terminate valid work; and worst, a watchdog kill CONVERTS TRUNCATION INTO APPARENT COMPLETION — the seat's disappearance is exactly the signal the read-after-exit rule keys on, so the gate could be satisfied by killing the thing it measures. Low CPU and a follower child are not proof that the agent issued its final answer. State of play: the coordinating Codex atomically restored v1 for new dispatches (live wrapper sha256 fd3aa3b9433c8d6a2c2b0530a8a8ea1465885b6677afed97de58f644a9632635, zero kill/TERM statements — verified here); v2 is archived verbatim as \`_nm_referee_dispatch_v2_STABILITY_KILL_UNSAFE_20260907.txt\` (sha256 5f4b0a39…); no process was signalled; the V32 seat A wrapper, agy and its follower run on untouched; Tori's V35 C0 runs under the earlier wrapper, untouched. My HermesOps commit that installed v2 is superseded by the restoration commit.

**The safe repair stays STAGED until it meets all of Blanc's conditions:** (1) completion only from EXPLICIT completion evidence from the backend or the GENUINE exit of the owning process — never a file plateau, CPU or the process tree's shape; (2) child cleanup AFTER proven completion, never as a way to produce it; (3) the existing deadline remains a TIMEOUT / FAILURE — a timed-out seat's report is not a verdict; (4) tested on BOTH (a) a finished backend with a lingering follower (detected complete, cleaned up) and (b) an ACTIVE backend whose provisional report stays unchanged LONGER than any threshold — which must SURVIVE untouched; (5) both results filed with the change; no new dispatch uses a modified wrapper until (b) passes. The control results above therefore stand as evidence of the DEFECT (v1 hangs on a leaky seat) and of v2's unsafety, not as a validated fix.


## ADDENDUM 2026-09-07 07:22 KST (Blanc 07:21) — the seat this defect was observed on TIMED OUT; DISAPPEARANCE IS NOT COMPLETION
The V32 seat A whose follower kept wrapper 63711 alive did not finish: at exit (07:17:41 KST, rc=1) its artefacts were AGY_SELRULE_V32_SEATA.stderr = `Error: timeout waiting for response` (36 bytes, sha256 60aa3a5934a49f2ec268bf44a23c952efdb835b745a4d55ddcc45e092bf8ff97) and AGY_SELRULE_V32_SEATA.stdout = 0 bytes; the report file was a provisional write from before the timeout. STANDING RULE: a seat's own exit artefacts (.stdout / .stderr) distinguish a timeout from a finished answer; the process tree never does — the wrapper's exit, the seat's disappearance, a byte-stable report and 0.0% CPU are each compatible with a timeout. Check the exit artefacts every time before treating a report as final. The staged wrapper repair (conditions 1–5) must also record the backend's completion status in the report's provenance, so that a timeout can never be read as a verdict.
