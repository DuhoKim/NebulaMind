# CHANGE RECORD — option A V20 DRAFT (REFUSED) → V21 DRAFT — DRAND-ONLY sampling amendment of the signed V15 — 2026-09-06 20:18 KST

**Authority for this round:** Blanc, `BLANC_ORDER_V21_ROUND.md` (19:48 KST; the cap on this track is Blanc's, lifted for ONE round) and `BLANC_ORDER_V21_ROUND_AMENDMENT.md` (19:50 KST; inherited ≠ blocked: prospective fixes for inherited defects MAY be prepared, labelled INHERITED — PREPARED, NOT ADOPTED; the checklist must separate (a) a change that needs Duho from (b) preparation). Codex coordination notes read directly as the amendment asked: `.hermes/CODEX_REPAIR_PREPARATION_20260906_1948.md`, `.hermes/CODEX_PREPARATION_SCOPE_FOLLOWTHROUGH_20260906.md` — coordination under Duho's standing autonomy instruction, not approvals. **Source decision unchanged:** Duho, via codex voice, ~19:02 KST, "해" (`.hermes/CODEX_DUHO_DRAND_ONLY_DECISION_20260906.md`, copied into the lane as `CODEX_DUHO_DRAND_ONLY_DECISION_20260906_copy.md`). Nothing approved, signed, frozen, sealed, drawn, fetched or rendered; V15 remains operative on paper.
**Target:** `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V21_20260906.md` SHA-256 `1906d0bb717faa0b408188b33629fdf931363c093d4d5d0dbfdda475f64adbca` (= preimage, `signature_preimage.py`). **Diffs:** `V20_TO_V21.diff` `26cbbb3caa8f6460594cbbed4b5536fe7d6980d476cdf17d8c398b030e4fe95b` (54 lines); `V15_TO_V21.diff` `8baf6b4c95a7b45ef9a67d3919b8243aaefe4c90a34eb82fdcd3ea946ad65e45` (60 lines).

## THE TRACK'S HISTORY — five refusals — and the overstatement, on the record
| draft | agy | codex | why refused |
|---|---|---|---|
| V16 (11:16) | SIGNABLE | NOT-SIGNABLE | ACCEPT-NIST on a raising live re-fetch and before the clock check (INHERITED from the signed V15); chronology asserted |
| V17 (11:41) | SIGNABLE | NOT-SIGNABLE | git witness had no wall-clock upper bound; HTTP errors opened the fallback; no first-ACCEPT lock |
| V18 (12:26) | SIGNABLE | NOT-SIGNABLE (no fatal) | retention and API window overstated; wrong-time pulse opened the fallback; history unbound; adoption unverified; driver checked nothing; §7 command stale; two advertised tests absent |
| V19 (13:12) | SIGNABLE | NOT-SIGNABLE — FATAL on the property | with two sources the failing side leaves no authenticator, so the FIRST collection could still select the source (executed) |
| V20 (19:22) | SIGNABLE | NOT-SIGNABLE — FATAL on the production path | `PRODUCTION.rederive_seed = None`: the real driver refused every identity (`PROTOCOL-NO-REDERIVER`); only fixtures injecting a callback loaded one; plus M1 (fabricated evidence with a real seed accepted), M2 (unauthenticated, resettable history), byte-uniqueness overclaim, substring URL binding, W3/W5 gaps, adapter reconciliation absent, stale cost/label text, ResourceWarnings |
**The author's 12:52 answer OVERSTATED its own result and the OPS coordinator released V19 on it (12:54); both facts stay on record (V19 triage). The V20 fatal is the author's: text and tests asserted an enforcement the pinned production bytes lacked — the same root cause as V16–V19, in a new place.** Order of this round, as Blanc required: (1) checklist against the COMPLETE V20 report — `V20_REPORT_REPAIR_CHECKLIST_20260906.md`; (2) the fatal REPRODUCED in executed output BEFORE repair — `V20_FATAL_REPRODUCTION_20260906.md` (`e336ccad41b6ba0923d5666d5c17aec6e678bb17d6e803238074f3a9a45a8991`; sections (a)–(d) before, (e) after); (3) repair, exhibit re-run twice, ONE gate with both seats; (4) the cap re-arms.

## What V21 changes, finding by finding (codex V20 → disposition → evidence)
| codex finding | disposition | evidence (executed) |
|---|---|---|
| FATAL production path unexecutable | REPAIRED: `run_configurations_v5.py` `d338e33d…` binds `production_rederive_seed` (offline drand-only verdict, lazy verifier import → `VERIFIER-UNAVAILABLE`) into PRODUCTION; fixtures built from `PRODUCTION.__dict__`, re-deriver asserted by identity; CLI exercised under PRODUCTION | reproduction (e): the same identity LOADS through PRODUCTION's re-deriver and is still REFUSED with `rederive_seed=None`; `test_run_configurations_v5` Ran 22 tests OK |
| M1 driver conjunction shallow | REPAIRED: approval record read from the repo at the approval commit (added once, untouched, byte-equal, pushed), schema lines parsed and bound (rule/T_sign/nonce), T_pulse/round re-derived, event re-validated (type/ref/time/delivery incl. before..head/digest/provenance), nonce BLS re-verified from retained bodies, history validated as a chain, beacon record bound (T_sign/T_pulse/round/rule/statement bytes/≥2 pinned bodies), seed re-derived | twenty-five fabricated identities each refused by the named check (`test_v4_semantic_conjunction`) |
| M2 history unauthenticated / resettable; paths unlogged | REPAIRED: `corpus_identity/history.py` `8ac7a81c…` — genesis anchored to the approval record digest/T_pulse/rule/round, `prev_sha256` chain, closed stage set, file lock, ATOMIC first accept (`append_locked`), validate refuses reset/edit/reorder/second genesis; collector CLI initiates the genesis and refuses mismatch; builder logs every path incl. `builder-error` (BaseException) and `witness-pending/closed`; pre-genesis failures disclosed in a sidecar | `test_history` 4 OK; `test_build_corpus_identity_v21` 8 OK (deleted entry → HISTORY-INVALID; other exception → builder-error); `test_beacon_record_drand_v21` CLI genesis/mismatch/pregenesis |
| X exhibit case C "impossible" overclaim | REPAIRED in text and code: seed-uniqueness vs byte-uniqueness; same signature re-encoded = same value (live-confirmed, difference recorded); different verifying signature = `REFUSE-CONFLICTING-VALID-SIGNATURES` (a key defect, simulated only) | `PROPERTY_EXHIBIT_V21_20260906.md` digest `2160fa75…`, run twice identical; C1 ACCEPT same seed, C2 RETRY |
| U substring URL binding | REPAIRED: `verify_drand_v2.py` `e982d944…` exact membership; `source_url` required; relays counted by exact pinned URL in the verdict | `test_verify_drand_v2` 4 OK (None / look-alike host / suffix refused); `test_unpinned_urls_and_lookalike_hosts_are_not_counted` |
| W5 nonce set/bound/authentication; '00' signatures in fixtures | REPAIRED: `approval_witness_v4.py` `6181690e…` exact set, scheduled bound ≥ MIN_T_SIGN, before the seed round, bodies fetched from the chain-hash path and BLS-verified (≥2 relays), retained in the witness; fixtures use the REAL round 6441904 (pinned bodies `b787e446…`, a public historical round pinned for the fixture — NOT a beacon read for a run) | `test_approval_witness_v4` 3 OK |
| W3 before..head; PENDING/CLOSED not persisted; event digest unchecked | REPAIRED: `delivers()` via `git merge-base --is-ancestor`; state returned by the witness and persisted by the builder as chained entries; the driver recomputes the canonical event digest | witness + builder + driver fixtures |
| S adapter reconciliation | REPAIRED: `reconcile_sentinels` — sentinel rows ↔ journalled REFUSED entries both ways, render-end count, sentinels without a journal refused; `Protocol.render_journal` per group | `test_sentinel_tensor_is_counted_and_unscored` (three refusals + the accepted case) |
| T stale text (§3c "V16", "tried once", unverified-BLS cost, "3 tests plus 2", L3 cross-reference, docstring "−30 s"); ResourceWarnings | REPAIRED in V21 text and code; fixtures close files; the one remaining warning is the INHERITED estimator RuntimeWarning (disclosed, not repaired) | 69 tests under `-W error::ResourceWarning`, OK |
| P decision provenance | the decision file is copied into the lane (`CODEX_DUHO_DRAND_ONLY_DECISION_20260906_copy.md`) | — |
| inherited (signed V15): E1's false count; repeatable holdout; V15's first-collection lever / unverified fallback (superseded by the recorded design); the estimator's NaN-cast warning | NOT REPAIRED in the signed text. PREPARED, NOT ADOPTED, labelled in V21: the true E1 count stated beside the false one; `Protocol.holdout_once` (default off) with a persisted marker. Each is class (a) — Duho's call | `test_holdout_once_prepared_not_adopted`; checklist rows I1–I4 |

## What V21 is, and the evidence (unchanged elements from V20 not repeated)
Pinned V21 files: driver `run_configurations_v5.py` `d338e33dfbf84ed7127c6b32e529da6dcfd0cbf9704aba468cdc2aa2a426722c` (+ `test_run_configurations_v5.py` `3d63af01…`, 22); builder `build_corpus_identity_v21.py` `048b4221…` (+ test `63dfcad2…`, 8); `history.py` `8ac7a81c…` (+ test `9d658c09…`, 4); `approval_witness_v4.py` `6181690e…` (+ test `015e2c27…`, 3); `beacon_record_drand_v21.py` `24afe74e…` (+ test `ee8f519f…`, 7); `verify_drand_v2.py` `e982d944…` (+ test `62064e44…`, 4); `exhibit_property_v21.py` `14851316…`; nonce-round pins `round_6441904_api.drand.sh.json` / `…api2…` `b787e446…`. Every V20 file is retained unchanged (`beacon_record_drand.py` `7d77d746…`, `verify_drand.py` `a781b965…`, `approval_witness_v3.py`, `build_corpus_identity_v20.py`, `run_configurations_v4.py`, `exhibit_property_v20.py`). Sample sizes, floors, bar, exclusions, one holdout, custody, protection and the §6 identity are byte-for-byte V15.

## Plain-words summary for Duho (used only if both seats clear; READY-FOR-APPROVAL, held by Blanc)
The last version was refused because the real program could not run: the piece that recomputes the random seed had been left empty in the production settings, and only the test harness ever filled it in. This version first shows that failure happening, in a saved run, and then fixes it: the real program now carries its own seed check, the tests use that same check instead of a stand-in, and the program refuses to run at all if the checking library is missing. It also now reads the approval record and the randomness history back from the repository and checks each piece against the others — the approval time, the ten-minute-later tick, the round number, the nonce, the push time on GitHub's clock, the history's unbroken chain — instead of trusting what the identity file says about them. The history file can no longer be quietly reset. What has NOT changed: the sample sizes, the bars, the one holdout, custody, and the branch protection. Two things I could fix but did not, because they sit in the rule you signed: the wrong test count in E1, and the fact that the program does not itself stop a second holdout run — I prepared both fixes, labelled, and left them for you.
