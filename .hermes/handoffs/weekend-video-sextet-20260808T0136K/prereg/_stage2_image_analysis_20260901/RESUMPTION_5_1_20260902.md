# RESUMPTION NOTE — Hwao restart to Claude Code 2.1.258 / Fable 5.1

Written 2026-09-02 ~15:3x KST under Duho's restart order (via Blanc): "then
restart you Trio but as seemlessly as possible". You are Hwao's successor on
the DESI spiral-handedness lane. Everything below is committed on
`feat/paper-workflow-v2`.

## FIRST ACTION — relaunch the Tier-C fetch (before anything else)

The authorized Tier-C brick download was STOPPED CLEANLY for this restart at
**784/17,947 bricks, 784 receipts, 0 failures, no stray partials**. It is
resumable by design: a brick already on disk is skipped. Relaunch verbatim:

```
cd /Users/duhokim/NebulaMind/NebulaMind/.hermes/handoffs/weekend-video-sextet-20260808T0136K/prereg/_successor_build_20260824/acquire
nohup python3 fetch_bricks.py --manifest tier_c_manifest_v1.json --dest bricks_tier_c --journal tier_c_fetch_receipts.jsonl --workers 4 --delay 0.5 < /dev/null >> tier_c_fetch.log 2>&1 &
```

Then arm a completion watcher on the PYTHON PID (`kill -0 <pid>` loop, or
pattern `fetch_bricks[.]py --manifest` — the `[.]` is the self-exclusion
that keeps the watcher from matching itself; today two watchers deadlocked
on exactly that mistake, disclosed in `TIER_C_ACQUISITION_GO_20260902.md`).
Blanc gets ONE ping when it completes (Duho's standing ask), with brick
count / GiB / failure tally. ETA from 784: ~17.5 h at ~960/hr.

## SECOND ACTION — dispatch the agy re-referee of mini-prereg V2

Chain state: draft V1 → agy referee **NOT-SIGNABLE** (F1 FATAL missing pins,
F2 FATAL missing BS-4 mechanics, F3 MINOR wording) → codex repair **V2 filed**
(`MINI_PREREG_GZ_TIERC_DRAFT_V2_20260902.md`, 11 pins, `miniprereg_pins/`
with the verdict program + run fixture test + BS-4 spec + render config +
env-record schema). codex PID 77374 may still have been finalizing at
restart; the deliverable and its column-0 block are complete on disk — treat
V2 as landed, but `tail runner_codex_mpr2.log` to confirm a clean exit.

Re-referee brief (Blanc's standing order, verdict pings owed at each step):
agy, hostile posture, verifies per-finding that F1/F2/F3 landed (re-hash
every pin sha stated in V2 against disk yourself; run the fixture test), and
hunts NEW defects the repairs introduced. Verbatim-preservation check: V1's
PASSED dimensions (D1 blind safety, D4 stats, D5 geometry, D6 custody,
D7 scope) must be unchanged except where pins were inserted. Only a clean
re-referee goes to Duho for signature; ping Blanc either way. Referee V1
report: `AGY_MINIPREREG_REFEREE_20260902.md`. Kickoff patterns to reuse:
`_tmp_agy_miniprereg_referee.txt` (V1's brief).

## STANDING SCOPE — unchanged, do not re-derive

* Ruling #55 ("as Hwao's rec", choice 2 verify-then-go): Tier-C ONLY;
  **P0 blind untouched (Tier A never rendered/measured/labelled); Tier B
  HELD and excluded**; acquisition pre-authorized and running; the
  mini-prereg returns to **Duho for signature before ANY measurement**.
* agy BC-verify: **CONFIRMED** (`AGY_BC_VERIFY_20260902.md`) — Tier B exact
  8,465/6,770/845/346; Tier-C ceiling 23,257 hc; cost ~210 GiB.
* The paper HOLD stands (mid F2–F7 repair, banked). Stage one banked
  terminal; stage two re-open outcome filed (`STAGE2_REOPEN_OUTCOME_…`).
* Rulings arrive ONLY via Blanc's "RELAY FROM DUHO" messages.
* Seat dispatch hygiene: `< /dev/null` always; PATH needs
  `~/.local/bin` + `~/.nvm/versions/node/v24.13.0/bin`; watchers PID-based.
* Burn: weight heavy work to codex/agy/kimi; Fable for judgment.

## Machine state at restart

Claude Code 2.1.258 on Studio/MacPro/MacBook, default `claude-fable-5-1`
everywhere (Duho's order, done). Tier-C fetch stopped at 784; #52 closure
(12,117 bricks) intact and receipted.
