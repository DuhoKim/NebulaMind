# THIRD-FAILURE DIAGNOSIS — option A sampling amendment V16 → V17 → V18, all refused by codex (agy passed all three) — 12:50 KST

**Rule engaged:** three consecutive refusals of the same amendment track. STOPPED. No V19 is drafted until Blanc releases the track (as for Tier-C at 12:45) or Duho rules.

## Why three rounds, honestly
V16 shortened the fallback and asserted chronology; V17 witnessed chronology with git, which cannot bound push time; V18 bounded it with GitHub's server time — which codex confirms holds against the late-writer attack — and was refused on the completeness of the surrounding machinery: what is retained, how long it is queryable, what exactly opens the fallback, whether every collection attempt is logged and bound, whether the adoption file is itself witnessed, and whether the driver checks any of it. The pattern differs from Tier-C's: here each round repaired the named defect correctly and was refused because the REPAIR's own claims ran ahead of its execution ("retained verbatim", "90 days", "every attempt logged", "the driver refuses", "19 tests"). Two of the V18 findings are plain misses of mine (the §7 command still naming the V17 builder; the two driver tests that never got appended — the fixture ran 17). Codex's reports were correct each time.

## What V19 would contain (one round if released — larger than Tier-C's four, listed exactly)
1. Witness: retain the ENTIRE matching PushEvent JSON and the retrieval provenance (endpoint, page, UTC, `gh` version); match by commit inclusion; choose the EARLIEST qualifying event, not the first newest-first hit; state the real API limit (about 30 days / 300 events, 30 s–6 h latency) and require the retained receipt to be taken promptly; a pending state for latency that never authorises a new approval, timestamp, round or source; the nonce stated as "no earlier than round R".
2. Fallback predicate: eligibility requires a served, live-equal pulse whose `timestamp_is_t_pulse` check holds and whose signature/anchor checks fail; a different or absent pulse is RETRY. Fixture with the wrong-time counterexample.
3. Collection history: the collector AND the builder append EVERY attempt (RETRY, refusals, conflicting ACCEPT) with inputs and verdicts; the first ACCEPT is durably locked (fail closed on a missing, reset or inconsistent log after initiation); the identity carries the log digest; the driver verifies the log blob at the witnessed commit, that `beacon_outcome` starts with ACCEPT, and that the witness and lock are present and consistent; the e2e fixture runs the V19 builder, not V15's.
4. Adoption binding: the adoption file must be a blob at the verified approval commit, equal to the approved digest, exactly one line with no blank lines, and byte-bound at identity use; builder and driver verify committed-ness, not only local content.
5. Text: §7 command → the V19 builder and record; the removed VOID token no longer cited as observed; REFUSE-NIST-LIVE-DIFFERS named for the unequal-live case; equality claims scoped (PEM-normalised certificates; drand quorum randomness); "ONE rule changes" qualified; docstrings and §3c/§10 labels; the two driver tests actually appended and the count made true; "the driver refuses non-ACCEPT" made true by item 3.
Nothing in this list touches sample sizes, floors, the bar, exclusions, the one holdout, custody or protection.

## What is NOT affected
Tier-C V39 is under its own released gate. V15 and V35 remain operative on paper; no identity, draw, fetch, render or pixel under any of them.

## Filing defect, recorded (Blanc 12:51 KST)
The first filing of this document (commit 0b0742ddf, 12:49 KST) was POLLUTED: it was written through an unquoted shell heredoc, so two backticked words in item 1 and item 3 were executed as commands — `gh` printed its help text into item 1 ("Work seamlessly with GitHub from the command line. USAGE / CORE COMMANDS …") and two words were lost. Blanc caught it. The polluted copy is archived unchanged at `_archive/THIRD_FAILURE_DIAGNOSIS_OPTIONA_SAMPLING_AMENDMENT_20260906_POLLUTED_1249.md`; this file is the clean regeneration through a quoted heredoc (commit 437cbf9c4). The other files written in the same batch (the V18 change record, the run record, the lane state, and the Tier-C V39/V38 records) were checked for the same signature: nothing else.
