# TRACK 11 — THE FATAL OF THE LANE'S OWN, REPRODUCED THEN REPAIRED; THE COMPOSED PATH AS A STAGED FINDING COLLECTOR (NOT GATED, NOT ADOPTED) — 2026-09-07 04:30 KST

Routine repair under existing authority after the V30 split (`V30_REVIEW_OUTCOME_20260907.md`), still under Blanc's 03:20 order (one order, one resolver). Successor files only: provenance_designs_v11, run_configurations_v15, beacon_record_drand_v31, build_corpus_identity_v31 (+ tests v15/v31/v31), track11/test_track11_fail_first.py (5 tests), track11/test_track11_text_v31.py, track1/coherent_attacks_v31.py. Candidate text `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V31_20260907.md` (SHA-256 f94b45e626ec95f1d2a60da783eb23c70f02394bbbe2be7389583013dd00cc49; diff `V30_TO_V31.diff`). Nothing Duho has approved is touched.

**The FATAL (V30-1) is the lane's own regression**, introduced by the track-10 resolver: a JSON `null` in the history-open file parsed without error, the resolver treated the absent event as nothing to check, and the composed load ACCEPTED — even over a published history rewrite. Per the V20 rule it was REPRODUCED IN EXECUTED OUTPUT before any repair (receipt run 1: the null file LOADS on the v15 copy of v14; the rewrite + null LOADS). Repair by construction: the open-event file must hold a PushEvent object (HISTORY-OPEN-EVENT-INVALID, class 0, for null / list / scalar / wrong type) and ACCEPT requires every stage in `REQUIRED_STAGES` to have RUN and contributed nothing — a stage whose inputs failed is NOT RUN and can never be skipped into acceptance.

| codex V30 | answer (staged) | evidence |
|---|---|---|
| V30-1 [FATAL] | above; plus every non-object value and the rewrite + null case | receipt run 1 → run 2 |
| V30-2 [MAJOR] decides outside the resolver | `load_identity_composed` — S0 the identity read locally, both retained events swept, the open-event file's validity; S0b the list checks BEFORE anything remote; S1 the witness-bound conjunction (extracted unchanged into `_conjunction_prefix`) as ONE classified stage: WITNESS-FETCH-FAILED / VERIFIER-UNAVAILABLE → RETRY-UNAVAILABLE findings after the local sweep; every refusal classified by `classify_refusal` (exact codes, then prefixes, unknown codes FLAGGED), every exception by `classify_exception` (malformed retained input class 0; I/O and subprocess class 5) | wrong repository + failed witness fetch → EVENT-INCONSISTENT; missing list id + HTTP 503 → IDENTITY-tuning_objids-SIZE-OR-TYPE; the classification test over codex's inventory |
| V30-3 [MAJOR] helpers suppress; evidence unshared | `history_findings_v11`: the history stage as a collector (remote head, genesis-only, open-event delivery + authentication, extension / batch over the fetched objects, per-entry publication, working tree vs remote — none returning first); ONE evidence snapshot per invocation (S2) applied to S3 and S5; `authenticate_event` evaluates EXPIRED before UNAVAILABLE | rewrite + empty feed → HISTORY-NOT-AN-EXTENSION; undetermined + newer feed + remote down → EVENT-EXPIRED-NO-RECEIPT-PATH; exactly one retrieval per invocation |
| V30-4 [MINOR] offline wording | §3c describes offline exactly: REDERIVE-RETRY and WITNESS-FETCH-FAILED exist there; IDENTITY-WITNESS-COMMIT-UNDETERMINED is its named undetermined-delivery refusal, not its only unavailability disposition; class boundaries and policies (seed cryptography class 4/5; PENDING-PUSH / DIVERGED class 4 by policy; receipt refusal class 3 by policy) named | `V30_4_Text`; the classification table |
| Blanc 03:20 items | 1 order stated once (unchanged); 2 one enforcing place — now the whole composed path, not four stages beside a deciding prefix; 3 the 21-pair exhibit stands (resolver unchanged) + codex's V30 constructions executed on the complete path; 4 every existing precedence test of tracks 8–10 kept and passing | receipt runs 2, 2b, 2c |

Inspection: `coherent_attacks_v31.py` — every row's verdict equals V30's (35 rows). Aggregate: 147 tests / 26 suites, OK blocks 26, warning-strict. Nothing gated, adopted, signed, frozen, sealed or drawn. Next: the V31 complete-package review, both seats, fresh sandbox.
