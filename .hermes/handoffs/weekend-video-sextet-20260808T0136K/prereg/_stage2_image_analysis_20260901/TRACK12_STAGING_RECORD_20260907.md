# TRACK 12 — NSD ("No Stage Decides"): the contribution half of the one property, made checkable and controlled from its table (NOT GATED, NOT ADOPTED) — 2026-09-07 05:35 KST

Under Blanc's orders of 05:02 and 05:09 KST (`BLANC_ORDER_TRACK12_NSD_20260907.md`, `BLANC_ORDER_TRACK12_EVIDENCE_SCOPE_20260907.md`) after the V31 split (`V31_REVIEW_OUTCOME_20260907.md`). Successor files only: provenance_designs_v12, run_configurations_v16, beacon_record_drand_v32, build_corpus_identity_v32 (+ tests v16/v32/v32), track12/test_track12_fail_first.py (84 tests, one subcase each), track12/test_track12_text_v32.py, track1/coherent_attacks_v32.py. Candidate text `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V32_20260907.md` (SHA-256 101a0af8f5a0a54e85506a17120d26d1311fd0ee7895e6f08b665ec7ff96034e; diff `V31_TO_V32.diff`). Nothing Duho has approved is touched.

**The property's name:** NSD — "No Stage Decides": NO STAGE MAY DECIDE OR ABORT; EVERY STAGE CONTRIBUTES; ONE RESOLVER DECIDES. Stated once in `provenance_designs_v12` (header), `run_configurations_v16` (header and the table block), and §3c of V32. Precedence (V27–V29) is its ordering half (unchanged); V30-2/3 and V31-1/2 are its contribution half; codex's four V31 clauses are its consequences.

**Where it is enforced:** `run_configurations_v16.load_identity_composed` — every stage runs on the inputs it truly needs and contributes; a prerequisite failure is itself a finding contributed by the stage that meets it; checks that needed it are recorded BLOCKED with the reason; `provenance_designs_v12.resolve` decides; ACCEPT only when every REQUIRED stage completed, nothing is blocked and no finding exists; `LAST_OUTCOME` records the whole outcome. The one raise outside the collector is its own dependency (the provenance module), named class 5.

**How the controls were derived — from the table, not the fixtures:** the INDEPENDENCE / PREREQUISITES table (code data in `run_configurations_v16`, rendered verbatim into §3c) lists every check, its stage, the prerequisites it truly needs and the codes it can contribute, and for every prerequisite how it fails and the finding it contributes. `test_track12_fail_first.NSD_FromTheTable` generates one method per (prerequisite, check) pair where the check does not need the prerequisite — 66 pairs: plant that check's defect, make the prerequisite fail, assert the WHOLE outcome (the defect contributed with a reason; the prerequisite failure contributed; every check needing the prerequisite blocked or contributing; the winner equal to the resolver's over the recorded findings). Codex's V31 constructions are 17 regressions on top, one subcase each. The table was appended to the unrepaired copy as DATA before run 1, so the controls exercised the OLD behaviour.

**Per-subcase fail-first evidence:** the original nine-method run 1 is preserved unaltered (`TRACK12_FAIL_FIRST_RUN1_ORIGINAL_PRESERVED_20260907.md`) with a table of exactly which subcases it reached (five methods stopped at their first subcase; two errors demonstrated nothing). Run 1d (84 methods, full output kept): 42 BEHAVIOURAL failures, 37 NEW-INTERFACE (no fail-first standing, named as such), 1 ALREADY-RIGHT, 1 raw ImportError that is itself codex's V31-1 demonstration. Run 2b: 84 OK on the successors after three disclosed refinements of the control's whole-outcome clause and one code change (the seed check reads the retained record from the working tree with its digest when git is unavailable, so the table's claim is true). Run 2c: every existing precedence test of tracks 8–11 (15) OK, unchanged.

| codex V31 | answer (staged) |
|---|---|
| V31-1 | S1's refusal blocks nothing that does not need it (S2–S5 run on the locally readable context); S0 sweeps without git, S0d's delivery check is blocked (not failed) when git cannot launch; the helper import is named class 5 |
| V31-2 | `retrieve_events` keeps every obtained page; a later page's failure is a finding; partial snapshots yield affirmative findings only |
| V31-3 | `history_findings_v12` never stops at a first defect; both contradiction arms before availability; `validate_continuation_v12` sweeps locally first |
| V31-4 | `CLASS_ALLOWLIST` (every actual raise, SPLIT-* included; the test re-extracts the codes from the sources); unknown codes flagged, no family prefixes; `classify_exception(exc, source)` by provenance; receipt failures normalized to the receipt policy |
| V31-5 | stage states attempted / completed / refused / blocked; every finding keeps its reason; the track-11 receipt's wording corrected in the V31 outcome; this track's receipt states per-subcase reach |

Inspection: `coherent_attacks_v32.py` — every row's verdict equals V31's (35 rows). Aggregate: 232 tests / 28 suites, OK blocks 27, warning-strict. Nothing gated, adopted, signed, frozen, sealed or drawn. Next: the V32 complete-package review, both seats, fresh sandbox.

## CORRECTION (2026-09-07 05:39 KST; Blanc 05:30) — the aggregate figure above ("OK blocks 27" of 28) was a FAILED aggregate, filed on a partial read
The driver v16 suite had 12 errors (`NameError: load_manifest`): my v16 edit had swallowed the definition (v15 line 424) while two callers remained. Found by the coordinating Codex and Blanc by AST diff; I had the NameError lines in my own output and filed anyway. Repair: `load_manifest` restored verbatim from v15 (the callers are v16's tuning/holdout modes and are correct; the removal was an editing accident); v16's top-level definitions now equal v15's exactly; the driver suite is 23/23 OK post-repair (`_tmp_v32_driver_suite_RUN2_post_repair.txt`); the V32 text is regenerated with the repaired driver's digest (61e894567142a695861f4b46c7d7683050df6d19bdc9fc48999ca405439a9a12); the full aggregate is re-run to NEW files (RUN2) and must pass `scripts/aggregate_gate.py` before the commit and the dispatch. The failed RUN1 logs and the first V32 filing are preserved unaltered. Nothing was dispatched on the broken bytes.

## FINAL (2026-09-07 05:45 KST): aggregate RUN2 after the repair — 232 tests / 28 suites, gate PASS (`scripts/aggregate_gate.py`). Candidate text regenerated: SHA-256 7620673b90562ccf262c84aed0838b2cc31638d2de73224125accbdb9b87aa32. This record's earlier aggregate line is superseded by this one.

## GATE REPAIR (2026-09-07 05:49 KST; Blanc 05:48) — v1 of scripts/aggregate_gate.py (sha256 9a0813f3…) matched an 'OK' PREFIX and never rejected FAILED; it PASSED a synthetic log `== s / Ran 1 / FAILED (errors=1) / OK` while printing "every block OK". v1 is retained as `scripts/_aggregate_gate_v1_RETAINED_defective.py`. v2 (sha256 6c6977f513293894bbd205801a65acfa6dcd064a5ef5c5495da6665ab68856bb) PREDICATE: exactly --suites blocks; each block has exactly ONE 'Ran N' line and exactly ONE result line that is EXACTLY `OK` (full-line match; 'OK (…)' fails); no line anywhere matching FAILED / ERROR / errors= / failures= / Traceback / *Error / exit-code; the Ran totals equal --tests; the printed sentence names only those facts. CONTROLS (`scripts/gate_fixtures/`, `scripts/test_aggregate_gate.py`, 5 tests): the synthetic failing log → v1 PASS (fail-first shown), v2 FAIL (2 result lines; FAILED); the genuine 12-NameError RUN1 → v2 FAIL; the genuine RUN2 → v2 PASS; two results in one suite → FAIL; 'OK (skipped=1)' → FAIL. RUN2's standing is unchanged (10 code suites / 18 text suites, 232 tests, no FAILED block); the original 12-error RUN1 is preserved; no pass/fail reassigned. The V32 sandbox already dispatched carries v1; the reviewers are told in the outcome, not by touching the sandbox.
