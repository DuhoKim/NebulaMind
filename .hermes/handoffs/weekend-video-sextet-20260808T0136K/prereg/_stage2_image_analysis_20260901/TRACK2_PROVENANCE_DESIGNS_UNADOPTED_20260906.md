# TRACK 2 — RECOMMENDED, UNADOPTED DESIGNS for the two provenance boundaries (Blanc's order 20:45 KST; codex's note 20:43) — REVISED 21:07 KST after codex's probe (21:00) and Blanc's orders of 21:02 and 21:04: the executable module is now `provenance_designs_v2.py` (v1 retained beside it); the history validator ASKS THE REMOTE; the event paths are BUILT, not described; the cost line is corrected. Original stamp: 2026-09-06 20:55 KST

**REVISION SUMMARY (read this first).** (1) Codex reproduced a gap in v1's `validate_continuation`: it walked LOCAL HEAD and trusted the supplied open commit without asking the remote, so an unpushed accept and a local reset-and-replace (remote unchanged) both validated — reproduced in executed output in `TRACK2_FAIL_FIRST_RECEIPT_20260906.md` run 1b. v2 (`validate_continuation_v2`) obtains the CURRENT remote head with `git ls-remote <pinned url> <ref>` (independent of every local ref), fetches that ref's objects, requires the open commit to be an ancestor of THAT head, walks the file's commits on the REMOTE ref's history, and enforces a PUSH-ACKNOWLEDGEMENT BOUNDARY: the working tree must EQUAL the blob at the remote head — anything local-only is PENDING-PUSH and governs nothing; a stale operator-supplied head is refused; an unreachable remote is RETRY, never a pass. Codex's two counterexamples, a stale head, an unavailable remote, an unpublished open commit, a deleted suffix and a rebuild all REFUSE; honest push-per-entry continuation passes (8 tests, fail-first). (2) The event design's retrieval, pagination, error handling and expired-receipt path are BUILT: `retrieve_events` (runner-injected; production runner = `gh api` subprocess; pagination until a short page or max_pages; failure kinds RATE-LIMITED / SERVER-ERROR / AUTH / NOT-FOUND / HTTP / MALFORMED / TRANSPORT each shown taking its path), `authenticate_event_live`, `write_events_receipt` / `verify_events_receipt` (receipt added once by one commit, never touched, ancestor of the LIVE remote head, digest recomputed, origin as expected, event verbatim; a replaced receipt is RECEIPT-NOT-FIRST). (3) COST CORRECTED — Blanc's summary to Duho said 'one push per freeze'; the design requires ONE PUSH PER COLLECTOR/BUILDER ATTEMPT, i.e. per history entry. FAILED-PUSH / RETRY STATES, exactly: an UNCOMMITTED extension and a COMMITTED-BUT-UNACKNOWLEDGED commit are both PENDING-PUSH (the remote's answer decides; the builder retries the push, never rebuilds); a push the protected remote REJECTS (non-fast-forward) leaves the local state DIVERGED and dead — the only way forward is to discard the local rewrite and continue from the published head. (4) RESIDUAL TRUST of the receipt path, stated: with one GitHub account the receipt's pusher is indistinguishable from the lane to GitHub; independence rests on the DELEGATED workflow (OPS takes, binds and retains the receipt; no per-receipt human confirmation — Duho has asked for no routine chores and none is proposed); a REPLACED receipt is caught by code, a receipt FORGED by the lane as the first one under the OPS label is NOT caught by code — only by OPS's retained copy (a check outside the lane, outside the code); closing it BY CODE needs an expected-receipt identity the driver can check — a second GitHub identity or a custodian key — which only Duho can provide, ONCE: that is the one decision reserved for him (Q1 in the questions file; the lane recommends Option C, no expired path at all).

**REVISION 3 (22:13 KST, after codex's V22 review A/B/C/F):** the executable module is now `provenance_designs_v3.py` (v2 retained): the history-open anchor is AUTHENTICATED (genesis-only first commit delivered by a retained PushEvent that must be in the live feed — a rebuilt history published as its own first commit is refused), the PRODUCER BOUNDARY IS BUILT (`publish_entry`; collector v23 and builder v23 publish each entry and stop on PENDING-PUSH; the validator requires one entry per acknowledged commit), ONE delivery predicate (W3's) in every mode, and an EMPTY feed is UNAVAILABLE. THE COVENANT (codex V24 N1, replacing the V23 residual): the validator authenticates the published append-only history and distinct, ordered publication events; it does not authenticate when the underlying operations occurred, nor the completeness of unpublished observations or decisions — before the genesis was published or between later publications; an unpublished absence-based CLOSED decision can be removed before its publication; the fixed-round seed is unchanged by any of this and an authentic LATE approval event remains disqualifying; stronger decision completeness would require independently retained decision evidence, which is NOT implemented. Option B of the questions file is NOT IMPLEMENTED; the feed window is 30 days / 300 events. `TRACK3_STAGING_RECORD_20260906.md`, `TRACK3_FAIL_FIRST_RECEIPT_20260906.md`, `V23_CANDIDATE_ATTACK_INSPECTION_20260906.md`.

**REVISION 4 (23:42 KST, after codex's V23 review M1/M2/M3):** the executable module is now `provenance_designs_v4.py` (v3 retained): PER-ENTRY PUBLICATION IS PROVEN BY THE SERVER — every history commit needs its own PushEvent in the live feed delivering exactly it, server-timed in order (one late push of several single-entry commits is HISTORY-PUBLICATION-BATCH); `publish_entry` pushes an already-committed pending entry instead of re-committing; `reconcile_pending` runs before any collector/builder operation and outer collector refusal/error entries are published; the residual is restated exactly (below). `TRACK4_FAIL_FIRST_RECEIPT_20260906.md`.

**REVISION 5 (00:19 KST, after codex's V24 review N1/N2/N3):** module `provenance_designs_v5.py` (v4 retained): dispositions EVIDENCE-UNAVAILABLE / EVIDENCE-INCOMPLETE (retry) vs HISTORY-PUBLICATION-BATCH (terminal, only when the feed proves a multi-commit delivery); the producer refuses PUBLISH-UNRELATED-COMMITS; the residual is replaced by THE COVENANT below. `TRACK5_FAIL_FIRST_RECEIPT_20260907.md`.

**REVISION 6 (00:52 KST, after codex's V25 review P1/P2):** module `provenance_designs_v6.py` (v5 retained): batch classification uses the shared delivery predicate (before..head ancestry counts — GitHub PushEvents may omit `payload.commits`), so a proven batch is terminal even without a commits list; ABSENCE IS NEVER FORGERY — a retained approval/open event missing from a feed is EVIDENCE-INCOMPLETE (retry), FORGED only when the feed carries a push delivering the same commit with different bytes; all three retrieval stages use one retry vocabulary. `TRACK6_FAIL_FIRST_RECEIPT_20260907.md`.

**REVISION 7 (01:29 KST, after codex's V26 review V26-1/2/3):** module `provenance_designs_v7.py` (v6 retained): the contradiction predicate is stated (same event id or same delivered commit, different bytes → FORGED; a difference only in `before`/`head` counts); delivery is TRI-STATE (UNDETERMINED = git objects missing or a failed command → UNAVAILABLE, retry — never FORGED); positive input mismatches are INCONSISTENT-INPUT, not 'different bytes'; EVIDENCE-EXPIRED is a loss at every stage. `TRACK7_FAIL_FIRST_RECEIPT_20260907.md`.

Both boundaries are codex's V21 findings C1 and C3 that no mechanical repair closes: the driver, working OFFLINE from committed bytes, cannot tell an authentic GitHub push event from a coherently forged one, and cannot tell a history that ran from the start from one rebuilt before the first freeze. Closing them means trusting something OUTSIDE the repository. That is Duho's decision, so both designs are staged as executable exhibits (`_optionA_dev/track2/provenance_designs_v2.py`, tests `test_track2_fail_first.py` 8 OK fail-first + v1's `test_provenance_designs.py` 2 OK; the coherent-attack rows 9a–9h) and wired into the production driver ONLY behind `Protocol.provenance_mode = 'composed'` (default offline), UNADOPTED. Nothing here reads a beacon, draws, fetches, renders or touches a pixel.

## (a) Authentic GitHub event validation — `authenticate_event`
| | |
|---|---|
| **What it checks** | The retained PushEvent (the identity's witness) must appear VERBATIM — canonical SHA-256 equal — in a LIVE retrieval of the pinned repository's events (`gh api repos/DuhoKim/NebulaMind/events`, the same call the builder makes), must name the pinned repository, the protected ref and the approval commit, and must be the EARLIEST qualifying event in that retrieval. |
| **Outcomes** (v7) | AUTHENTIC; INCOMPLETE (absent from a feed that covers its time, nothing contradicting — RETRY); UNAVAILABLE (API error, empty feed, or a delivery that cannot be established because git objects are missing — RETRY); EXPIRED (the feed no longer reaches the event's time — a LOSS, not a retry); FORGED (an affirmative contradiction: a live push with the same event id or delivering the same commit whose bytes differ — terminal); INCONSISTENT-INPUT (the retained object is not a PushEvent for the pinned repository, or does not deliver the commit — terminal, named as an input mismatch); NOT-EARLIEST (an earlier qualifying event exists — terminal). |
| **When it runs** | In the driver at `load_identity`, i.e. at `tune` and at `holdout` — both must happen while GitHub's feed still serves the events (at most 300 events from the past 30 days; a busy repository loses them sooner). |
| **What it costs** | Network at driver time (read-only, the authenticated `gh` CLI, BUILT: `retrieve_events` with pagination and named failure kinds); a retry path when GitHub is down (UNAVAILABLE → RETRY); and, for a driver run after the window, a SECOND retrieval receipt taken independently inside the window (BUILT: `write_events_receipt` by the OPS session under the DELEGATED witness workflow of `QUESTIONS_FOR_DUHO_TRACK2_ORIGIN_AND_RECEIPTS_20260906.md` — origin label `{actor: ops-witness, session: OPS}`, proposed by the lane, unadopted, not a user decision — committed and pushed once, with OPS's own retained copy outside the lane; `verify_events_receipt` accepts EXPIRED only when the retained event is verbatim in that receipt, the receipt was added once and never touched, and its commit is an ancestor of the LIVE remote head). |
| **Trusted afterwards** | GitHub's server clock and the integrity of its events feed; the `gh` session's authenticity; for the EXPIRED path (if Duho keeps one), the OPS session's independence as evidenced by its retained copy — a step outside the code, stated in the questions file. |
| **No longer trusted** | The identity's own retained event bytes, their digest, and the provenance block — attack 1 becomes FORGED (row 9b). |
| **Unavailable / inconsistent** | UNAVAILABLE = RETRY (no seed, no verdict); FORGED = DATA-INTEGRITY-FAIL, the identity is dead; EXPIRED without a receipt = refused; the rule text would say all three. |
| **Recommendation** | ADOPT (a) with the live re-query mandatory; for the EXPIRED path the lane's preference is Question 1 Option C (no expired path: complete tune and holdout inside the window) — it adds no trusted party; Options A′/B are described with their costs and are NOT available until built and reviewed (the questions file marks them so). Cost is several read-only network operations per driver run (event retrievals with pagination; ls-remote and fetch). |

## (b) Externally witnessed history continuation — `validate_continuation`
| | |
|---|---|
| **What it checks** | The collection history's FIRST commit must be the witnessed HISTORY-OPEN commit (the collector's genesis, committed and pushed to the protected branch immediately, and witnessed by its own server-side PushEvent exactly like the approval record — that PushEvent is the external anchor); every later commit touching the file must carry a blob that is a strict byte-prefix EXTENSION of the previous one (append-only, verified through git objects, not through the file's own hashes); the working-tree bytes must extend the last committed blob. |
| **Why it closes C3** | A rebuilt history (fresh genesis) is not an extension of the committed one; a deleted suffix is shorter than the last committed blob; a reset before the first freeze is visible because the earlier commits cannot be removed from a branch protected against force pushes and deletion. Rows 9c/9d, and `test_b_history_continuation_through_git` (rebuilt, truncated, restored-after-rebuild, wrong open commit: all refused). |
| **What it costs** | ONE ACKNOWLEDGED PUSH PER HISTORY ENTRY — every entry, including genesis and failure entries, is committed, pushed and acknowledged AS IT HAPPENS (BUILT: collector v24 and builder v24 do it themselves via `publish_entry`, and reconcile a pending entry before any operation via `reconcile_pending`), NOT one push per freeze (Blanc's condensation to Duho was wrong; corrected here); one process invocation may publish several entries, each acknowledged in turn; the history-open commit needs its PushEvent retained like the approval record's (W3 machinery reused); the validator ASKS THE REMOTE (`ls-remote`) at every load — network at driver time; a history that was never pushed cannot be used; a failed push = PENDING-PUSH until acknowledged; a push REJECTED as non-fast-forward (a rewrite) = DIVERGED, dead; a temporarily rejected push of a valid entry = PENDING-PUSH, recoverable by retrying the same commit. |
| **Trusted afterwards** | The remote's answer to `ls-remote` over TLS to github.com for the pinned URL; server-side branch protection on `feat/paper-workflow-v2` (Duho's GitHub setting, already required by §7); GitHub's PushEvent for the open commit. In the tests the remote is a LOCAL BARE repository with non-fast-forward receives denied — labelled; it is the same git command minus TLS. |
| **No longer trusted** | The file's own hash chain as evidence of custody (it stays as tamper-evidence within a blob); the operator's clock. |
| **Unavailable / inconsistent** | Remote unreachable = RETRY-REMOTE-UNAVAILABLE (nothing governs); uncommitted or unacknowledged local bytes = PENDING-PUSH (retry the push, never rebuild); a stale supplied head = STALE-EXPECTED-HEAD; a local rewrite = HISTORY-DIVERGED, and any non-extension commit on the remote ref = HISTORY-NOT-AN-EXTENSION — the history is dead and so is every identity built on it. |
| **Recommendation** | ADOPT (b). It reuses the witness already required for the approval record and the branch protection already required by §7; the only new cost is one push per attempt. |

## What stays trusted after both (stated for Duho)
GitHub (its clock, feed and branch protection), the League of Entropy threshold and the pinned drand key (limits L1–L2, unchanged), and — only if Duho keeps an expired-receipt path — the OPS session's retained copy of the receipt (or, under Q1 Option B, nothing beyond GitHub: the second identity makes the check a code check). What is NOT closed by either: the INHERITED V15 driver limitation that the object lists are taken on trust (C11) — the staged `verify_split` check (driver v6, off by default) closes it from catalogue bytes and is a separate adoption.

## If Duho says no
The candidate can still be gated with both boundaries DISCLOSED as covenants in §3c ("the retained event and the history are attested by the builder's run; a coherent forgery or a pre-freeze rebuild is not detected by the driver"). That is a weaker rule, honestly labelled; it is not recommended.
