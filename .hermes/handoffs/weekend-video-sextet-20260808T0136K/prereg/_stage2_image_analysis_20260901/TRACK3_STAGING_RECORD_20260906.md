# TRACK 3 — STAGED REPAIRS OF CODEX'S V22 FINDINGS A–F (NOT GATED, NOT ADOPTED) — 2026-09-06 22:13 KST

Continued under the standing preparation notes after the V22 review split (codex 21:12: "carry routine repairs through to a concrete reviewable recommendation"); every item is class (b) preparation; no gate was dispatched — that is Blanc's call. Fail-first: `TRACK3_FAIL_FIRST_RECEIPT_20260906.md` — run 1 against byte-copies of the V22 modules: 10 of 10 fail (7 MISSING-INTERFACE AttributeErrors/TypeErrors for the new v3 functions and signatures; 3 BEHAVIOURAL — E render-end position, F empty feed classified FORGED, F the questions text); run 2 after repair: 10 OK.

| codex V22 | repair (staged) | evidence |
|---|---|---|
| A late first publication launders a rebuilt history | `provenance_designs_v3.validate_continuation_v3`: the first remote commit touching the history must carry a GENESIS-ONLY blob (OPEN-NOT-GENESIS-ONLY) and be delivered by a RETAINED PushEvent authenticated in the live feed (OPEN-EVENT-*); driver v7 reads `history_open_event_file` instead of a lane-written commit id | track 3 tests A ×2; attack rows 9i, 9k, 10d |
| B producer boundary promised, not built | `publish_entry` (one entry per commit, `ls-remote` acknowledgment or PENDING-PUSH; PUBLISH-BATCH refuses two unpublished entries); collector v23 `--publish-remote/--publish-ref` publishes the genesis as history-open and every attempt; builder v23 publishes every appended entry; validator requires one entry per acknowledged commit (HISTORY-BATCH-COMMIT) | track 3 tests B ×3 (incl. the collector CLI publishing two commits) |
| C composed predicate narrower than W3 | `provenance_designs_v3.delivers` = `approval_witness_v4.delivers` (before..head via git) in `authenticate_event`/`_live`; driver v7 passes the repo root | track 3 test C |
| D control failure unlogged | builder v23 appends `builder-control-refusal` before returning False; `history_v2` stages extended | track 3 test D (real builder fixture, control CSV substituted) |
| E render-end position | driver v7: the render-end must be the LAST relevant record (RENDER-JOURNAL-NOT-ENDED) | track 3 test E |
| F 90 days; Option B described-not-built; empty feed = FORGED; NOT-FOUND/HTTP untested | questions file: 30 days / 300 events, loss consequences at tune vs holdout (closure, no new attempt), Option B marked NOT IMPLEMENTED, a second token ≠ a second identity; `authenticate_event`: empty feed = UNAVAILABLE; tests for 404/418/429 | track 3 tests F ×2 |
| stale text / side effects | §9 counts, §7 commands (v23 builder/collector), §3b old verifier marked historical, driver comments ("origin Duho names") removed, track-2 test docstring corrected (MISSING-INTERFACE), track-1 argparse test uses a temp sidecar, `provenance_designs_v3` docstring says where it is wired | `V23_TO…` text test; grep |

Residual, stated in v3 and in the V23 text: attempts made before the genesis was published are undetectable by any validator; under drand-only they cannot change the seed and cannot hide a CLOSED witness state. Not repaired here (Blanc's/Duho's): the forged-first-receipt trust step (Option B not built); the inherited driver trust (verify_split UNADOPTED).

Suites on the V23 candidate (warning-strict): 23 + 17 + 4 + 3 + 8 + 4 + 7 + 4 + 10 + 8 + 2 + 10 = 100 tests OK. V23 staged text `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V23_20260906.md` digest `1dd822619f5831bf18d0c6e721a6d5a98b8ba171286569dad1b00a1354aa3b4c`; diff `V22_TO_V23.diff`. The in-tree `beacon_v2/collector_argparse_failures.jsonl` (5 lines) is untracked test residue from before the sidecar fix; left in place, not committed. The codex note omitted from the V22 sandbox is copied next time.
