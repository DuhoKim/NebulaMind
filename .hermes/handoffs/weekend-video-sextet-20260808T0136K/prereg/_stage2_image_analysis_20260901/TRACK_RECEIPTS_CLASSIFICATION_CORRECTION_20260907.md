# CORRECTION NOTE beside the pinned fail-first receipts (codex V24 N4) — 2026-09-07 00:2x KST
The receipts are pinned into dispatched reviews and are NOT edited (PIN_IMMUTABILITY_RULE). Their run-1 classifications are corrected here:
- TRACK4_FAIL_FIRST_RECEIPT (V24 pin): run 1 = 3 assertion failures + 4 unittest errors. Correct reading: 3 MISSING-INTERFACE (`validate_continuation_v4`, `reconcile_pending`; the collector test's new CLI path) and 4 BEHAVIOURAL — the pending-retry case is behavioural (the V23 `publish_entry` existed and raised PUBLISH-COMMIT-FAILED), plus the three text assertions. The receipt said "4 MISSING-INTERFACE, 3 behavioural"; that was wrong.
- TRACK3_FAIL_FIRST_RECEIPT (V23 pin): run 1 = 7 unittest errors + 3 failures; NOT all seven errors are missing interfaces: `authenticate_event(..., root=)` raised TypeError on an existing function (behavioural signature mismatch) and the D case errored on an existing builder path (behavioural: the control failure escaped unlogged). Correct reading: 5 missing-interface (validate_continuation_v3 ×3 including the collector CLI flag, publish_entry ×2) and 5 behavioural. The D case was adjusted between runs to lift the exhibit-round exclusion (disclosed in the receipt).
- TRACK2_FAIL_FIRST_RECEIPT (V22 pin): 8 AttributeErrors = 8 MISSING-INTERFACE; the old behaviour is established by run 1b. Correct as filed.
- TRACK1_FAIL_FIRST_RECEIPT (V22 pin): 9 assertion failures + 1 FileExistsError, all BEHAVIOURAL. Correct as filed.
- TRACK5_FAIL_FIRST_RECEIPT (this round, not yet pinned): 3 missing-interface (N2a/b/c) + 3 behavioural (N3, N1, N5), stated per test in the receipt itself.
Present passing runs cannot prove the historical bytes of earlier runs; the receipts' own digest lines are the record of what was run.
