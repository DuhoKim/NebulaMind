# TRIAGE of codex's complete V19 report (CODEX_SELRULE_V19_SEATB.md, 13:24 KST) — every finding, R3C2's shape — 2026-09-06 14:33 KST

Columns: **origin** = NEW (introduced by V16–V19) or INHERITED (present in the SIGNED V15, fdd9eedd…, signed 2026-09-06T00:04:07Z); **scope** = IN (the expedite Duho authorised: remove the 24 h wait and make that safe) or OUT (a defect of the signed V15, or a design change); **disposition** with one line of reason; **call** = Hwao (repair within the amendment) or Duho (a signed-V15 defect or a design choice). An INHERITED item is never quietly repaired inside the expedite: it is flagged for Duho, who signed V15 this morning.

## A. The two MAJOR findings, in my own words, and what each costs if left
| finding | origin | scope | in my own words | cost if left |
|---|---|---|---|---|
| **M1 — the driver does not enforce the conjunction** (report item 1) | NEW (the checks were introduced by V18/V19; V15's driver never had them) | IN | The V19 driver looks at the identity's fields — an ACCEPT prefix, a timestamp, digests — and never at the evidence behind them. Codex built an identity with an EMPTY committed log claiming 999 entries, a witness that was just a timestamp in a dict, an adoption file outside the repository, and the invented outcome "ACCEPT-INVENTED"; sealed it; the driver loaded it and would have tuned on it. | Everything the amendment promises about approval-before-seed, one binding collection and a bound rule digest is enforced only at the builder, and the builder is ours. An operator who writes the identity by hand skips the builder entirely; the freeze witness then certifies bytes that never passed the acceptance path. The honesty chain reduces to trusting us — the thing the whole design exists to avoid. |
| **M2 — the collection history is neither complete nor authenticated** (report item 2) | NEW (the log is a V18/V19 construct) | IN | Failures before parsing (bad JSON, bad digest, missing file) never reach the log; the collector only logs if asked; the log is an append-mode file with no authenticated prior head, so a deleted or reset log is indistinguishable from a first attempt; the lock keys on `builder-accept` only, so a collector ACCEPT or a verdict followed by a witness refusal establishes nothing; the driver checks the log's bytes, never its entries. | "Every attempt is on record" is a claim, not a fact. A repeated or suppressed collection leaves no trace; the "first ACCEPT is binding" rule can be reset by deleting a file; a reader cannot reconstruct what happened between approval and identity. Together with M1 this is why "no lever" is unproved even where the property would hold. |

## B. Every finding
| # | finding (report ref) | origin | scope | disposition — reason | call |
|---|---|---|---|---|---|
| P | Collection-time independence not established: fallback opens on failed AGGREGATE authentication (next-link mismatch, AIA failure, failing pulse), inputs not fixed by content; executed source flip between an early and a later first collection | INHERITED in mechanism (V15's fallback also keys on "not authenticable" = aggregate; the 24 h wait only delayed it) / NEW in claim (V19 asserted the property) | OUT for the claim's remedy (removing the source choice is a design change), IN for withdrawing the overclaim | **STOP — design decision.** Narrowing the predicate does not close the failing-pulse case (`ANSWER_TO_BLANC_DOES_NARROWING_CLOSE_THE_PROPERTY_20260906.md`); closing it means drand-only, NIST-only, or a standing void condition. **Also a finding about the signed V15:** its 24 h design has the same lever at 24 h + ε (first collection after the wait can be timed to NIST's state). | Duho |
| P-L1 | AIA chain selected at first collection, not fixed | INHERITED | OUT | Same as P: the chain is our archive, not NIST's; only a source-choice removal or a void condition resolves it | Duho |
| P-L2 | BLS immutability claimed by a path that neither verifies BLS nor binds the response to CHAIN_HASH | INHERITED (V15 disclosed no BLS; V15's `drand_round` does not bind CHAIN_HASH either) | OUT (V15 defect: relay agreement without chain binding) / IN for the wording | Flag for Duho: a drand response from the wrong chain with two agreeing relays would be accepted under V15 as under V19; binding the response's chain hash is a small code fix but it is a V15 pin. | Duho (V15) |
| P-L3 | after the GitHub window the approval conjunction is re-queryable only by trusting retained evidence | NEW | IN | ANSWERED — L3 states it; nothing to repair beyond keeping the retained event | Hwao |
| P-L4 | "outages give RETRY" true only for NIST exceptions; drand quorum loss → UNAVAILABLE; changed NIST bytes → REFUSE | NEW (wording) | IN | Wording fix in any successor; no code change | Hwao |
| P-L5 | "neither selects a seed or source" is the unproved conclusion | NEW | IN | WITHDRAW the sentence in any successor; the honest form is in the answer file | Hwao |
| P-X | exhibit shows conditional replay determinism only; the real-record case stops at the MIN_T_SIGN refusal; generator docstring says "RETRY both times" (wrong) | NEW | IN | Exhibit to be re-scoped to what it proves, and to include the first-collection case only if a design closes it; docstring fix | Hwao |
| C5-W5 | W5 admits the predecessor round rather than exactly round_for(T_sign); AW docstring "T_sign − 30 s" overclaims | NEW | IN | Exact rule to be chosen and stated (current round only, or the predecessor with its scheduled bound); docstring fix | Hwao |
| C5-match | a push delivering the approval commit as a non-head ancestor without a `commits` array may not match the event | NEW | IN | Matcher to accept `before`..`head` range membership (ancestry between the event's `before` and `head`) — code fix + test | Hwao |
| C5-state | PENDING/CLOSED for a missing event is prose, not a persisted deadline state | NEW | IN | Either persist the state in the history (M2) or label it a covenant | Hwao |
| 1 / M1 | driver conjunction unenforced (see A) | NEW | IN | Driver must parse the history, restrict outcomes to ACCEPT-NIST/ACCEPT-DRAND, validate the witness (record, event, nonce, T_sign→T_pulse), verify adoption bytes at the approval commit, rederive the seed from retained evidence | Hwao |
| 1b | "failing authentication" in §3b is broader than "signature/anchor checks fail" | NEW (wording) / INHERITED (V15's predicate is the aggregate) | IN for wording; OUT for the predicate itself (see P) | Wording to match code; predicate change is P | Hwao / Duho |
| 2 / M2 | history incomplete and unauthenticated (see A) | NEW | IN | Mandatory logging on every path incl. pre-parse; authenticated prior head (hash chain like the seal journal); atomic first-accept; driver parses entries | Hwao |
| 3 | reproductions all as required; 35 E3 digests + witness/exhibit/diff pins MATCH; 92 tests OK; guarded pool UNVERIFIABLE HERE | — | — | Confirmed; no action | — |
| 4-nonce | nonce arithmetic ANSWERED, literal current-round NOT REPAIRED | NEW | IN | = C5-W5 | Hwao |
| 4-log | logging PARTLY repaired; history binding PARTLY | NEW | IN | = M2 | Hwao |
| 4-adopt | adoption REPAIRED at builder, NOT at driver (blank lines tolerated; approval blob unverified) | NEW | IN | = M1 | Hwao |
| 4-RETRY | V19:33 still says a DIFFERENT served pulse is RETRY while the code refuses (REFUSE-NIST-LIVE-DIFFERS) | NEW (wording) | IN | Wording fix | Hwao |
| 4-inherited-examples | the unsigned/wrong-certificate → RETRY examples in §3b are V15 observations presented as current; under V19 they can be UNAVAILABLE or ACCEPT-DRAND | INHERITED text, NEW mismatch | IN | Re-observe under the successor's module and rewrite | Hwao |
| 4-labels | §3c labelled V16, costs "under V17"; §9 is a historical V15 checklist; "ONE rule" ANSWERED | NEW | IN | Wording | Hwao |
| 4-sentinel | render-refused representation ANSWERED as design; adapter refusal-cause reconciliation not exhibited | NEW (prerequisite) | IN | Adapter to emit the sentinel only for a journalled refusal and reconcile identities/causes/counts before tuning — code + fixture (the adapter exists, unreconciled) | Hwao |
| 4-warnings | new ResourceWarnings in the driver fixture | NEW | IN | Close fixture resources | Hwao |
| 5-once | "NIST tried once" and "holdout once" not mechanically enforced; the driver does not prevent a second holdout invocation | INHERITED ("holdout once" is a V15 covenant) / NEW ("tried once") | OUT for the V15 covenant; IN for the wording | Flag for Duho: V15's one-holdout rule is a covenant, not a mechanism; a second `holdout` invocation is possible and only the journal would show it | Duho (V15) / Hwao |
| 5-lock-vs-freeze | the builder's lock precedes a successfully frozen identity | NEW | IN | Wording: the lock is on the verdict, the freeze on the identity | Hwao |
| 5-E1 | E1 says `test_run_configurations.py` has 10 tests; 17 run | INHERITED (V15 E1 text) | OUT (V15 text defect) | Flag for Duho: a false count in the signed text; harmless but it is in the signed bytes | Duho (V15) |
| 5-frag | an obsolete "3 tests … plus … 2 tests" fragment in E3 | NEW | IN | Wording | Hwao |
| 6-rule | required clause substance (immutable-input selection rule incl. failure evidence; complete authenticated history; driver conjunction; exact W5; adapter reconciliation) | NEW | IN except the first, which is P | As tabled above; the first item is Duho's | Hwao / Duho |
| 6-metadata | "ONLY inputs and evidence" is accurate only as "no trusted stored verdict" — the record carries collected_utc and error metadata | INHERITED (V15 §3b wording) | OUT (V15 text) | Flag for Duho: harmless, but the signed sentence is literally false | Duho (V15) |
| agy | agy passed V16–V19 each time and asserted completeness the audit refutes | — | — | Recorded: agy's SIGNABLE verdicts are not evidence of completeness on this track | — |

## C. Findings that are defects in the SIGNED V15, flagged separately for Duho
1. **The first-collection lever exists in V15's own design** (P): after the 24 h wait, the first collection can be timed to NIST's state exactly as in V19 — the wait delays the lever, it does not remove it. (V15's beacon_record additionally accepts a raising live fetch and returns before its clock check — already on record from the V16 gate.)
2. **drand responses are not bound to the chain hash and BLS is not verified** (P-L2): two agreeing relays from the wrong chain would pass V15's `drand_round.agreement`.
3. **"Holdout once" is a covenant, not a mechanism** (5-once): V15's driver does not prevent a second `holdout` invocation.
4. **Two false statements in the signed text**: E1's "10 tests" (17 run); §3b's "ONLY inputs and evidence bytes" (the record also carries metadata).
None of these is repaired by any amendment draft; each is Duho's to rule on, by a further Tier-C-style amendment of V15 if he wishes.

## D. Status
No successor drafted (Blanc 13:29 step 3 and 14:32: the three-way choice is Duho's; narrowing does not close the property). V19 refused; V15 operative on paper with the defects above on record; V39 installed; no approval, seed, draw, fetch, render or pixel.
