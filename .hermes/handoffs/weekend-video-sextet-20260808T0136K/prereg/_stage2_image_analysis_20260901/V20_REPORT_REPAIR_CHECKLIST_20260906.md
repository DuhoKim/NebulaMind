# REPAIR CHECKLIST against the COMPLETE V20 report (CODEX_SELRULE_V20_SEATB.md) — Blanc order step 1 — 2026-09-06 19:50 KST
origin: NEW (V16–V20) / INHERITED (present in the SIGNED V15). scope: IN (the drand-only amendment Duho chose) / OUT (a signed-V15 defect or a design change). class (Blanc amendment 19:50): (a) an operational or scientific change that needs Duho / (b) preparation that does not. call: Hwao / Duho. "Inherited" is history, not a prohibition: inherited items carry a PREPARED, NOT ADOPTED fix where one can be prepared.

STATUS (20:2x KST): every row marked REPAIR is now DONE in V21 (evidence in `MINI_PREREG_SELRULE_V20_TO_V21_CHANGE_RECORD_20260906.md`); the reproduction is `V20_FATAL_REPRODUCTION_20260906.md`.

| # | finding | origin | scope | class | disposition (V21) | call |
|---|---|---|---|---|---|---|
| F | production driver not executable: `PRODUCTION.rederive_seed = None`, refused with PROTOCOL-NO-REDERIVER; fixtures injected callbacks | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPRODUCED first (`V20_FATAL_REPRODUCTION_20260906.md`); REPAIR: a real module-level production re-deriver (offline drand-only verdict on the committed record, BLS re-verified) bound into PRODUCTION; fixtures use PRODUCTION's own function with paths overridden only — no callback substitution | Hwao |
| M1 | driver accepts fabricated evidence with a real seed: no approval record, 2098 event, nonce −1, zero event digest, reset 999-entry history, record absent from the freeze commit | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: the driver reads the approval record (blob at the approval commit), validates W1/W4 via git, the schema lines, the event's digest/provenance/ref/commit inclusion (head, commits, or before..head ancestry) and time; re-verifies the nonce bodies by BLS; validates the history chain from genesis (count, stages, ordering, semantic conflicts); binds rule/statement/T_sign/T_pulse/round/seed across record, identity and approval; requires the record as a blob at the witnessed commit | Hwao |
| M2 | history unauthenticated append file; resettable; a live-fetch JSON error escaped logging; CLI parsing precedes logging | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: `history.py` — genesis entry anchored to the approval record digest and T_pulse, every entry hash-chained (`prev_sha256`), appended under a file lock, validated by the driver; every exception path in collector and builder logged (BaseException wrap) | Hwao |
| X | exhibit case C calls a re-encoded same-signature body "impossible"; the seed is unique, the bytes are not | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: text splits seed-uniqueness (cryptographic) from byte-uniqueness (false); code: a live body with the SAME signature counts as live-confirmed; a live body with a DIFFERENT VERIFYING signature is the impossible case (`REFUSE-CONFLICTING-VALID-SIGNATURES`); exhibit re-run | Hwao |
| U | URL binding by substring: `source_url=None` accepted, look-alike host accepted, suffixes accepted; relays counted by string | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: exact membership in the pinned URL set; `source_url` required; relays counted by exact pinned URL | Hwao |
| W5 | nonce: predecessor allowed, scheduled bound not enforced, docstring "−30 s" false; nonce fetched unqualified and never BLS-verified; e2e serves signature '00' | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: nonce set exactly {round_for(T_sign)−1, round_for(T_sign)} with scheduled time ≥ MIN_T_SIGN enforced; nonce bodies fetched by the chain-hash path, BLS-verified, retained in the witness; docstring corrected; fixtures serve real signatures | Hwao |
| W3 | no before..head ancestry match; PENDING/CLOSED not persisted; event digest not checked by the driver | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: before..head via `git merge-base --is-ancestor`; PENDING/CLOSED persisted as history entries; the driver recomputes the event digest | Hwao |
| L | limits: P-L2 partly (URL), P-L3 stale cross-reference, P-L4 malformed live JSON escaping | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR in text and code (above) | Hwao |
| S | adapter refusal-cause reconciliation absent from the driver | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: the driver reconciles sentinel rows against the render journal (REFUSED entries ↔ sentinel digests, and no REFUSED object without a sentinel) via `proto.render_journal`; fixture | Hwao |
| T | text: §3c "V16" label, "NIST tried once", "unverified BLS" cost text, "3 tests … plus 2" fragment; ResourceWarnings in the driver fixture | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | REPAIR: wording; fixture closes files | Hwao |
| I1 | E1's "10 tests" for `test_run_configurations.py` (true count 17, executed) and §3b's "ONLY inputs and evidence" wording | INHERITED | OUT | (a) change to the signed V15 — Duho | PREPARED, NOT ADOPTED: V21 states the true count beside the false one, labelled; §3b wording is already superseded by the drand-only paragraph (recorded design supersession) | Duho |
| I2 | V15's first-collection lever and unverified/unbound drand fallback | INHERITED | OUT | (a) design supersession — Duho chose it ("해") | superseded for the sampling path by the drand-only design (recorded, not silent); nothing further to prepare | Duho |
| I3 | repeatable holdout invocation (the one-holdout covenant is text only in V15) | INHERITED | OUT | (a) how a signed covenant is enforced — Duho | PREPARED, NOT ADOPTED: driver v5 `Protocol.holdout_once` (default False = V15 behaviour) + persisted marker; exhibited; adopting it = Duho | Duho |
| I4 | `fourier_chirality.py:87` RuntimeWarning (NaN cast when the sentinel is scored) | INHERITED | OUT | (a) a change to E3(i)'s pinned family hash — Duho | disclosed in V21 §3c; no fix prepared (any change alters the signed family hash; a prepared variant would need its own gate) | Duho |
| P | provenance of the decision file outside the sandbox | NEW | IN | (b) preparation — lane-owned repair of an unsigned draft | the decision file is copied into the lane record set for the gate | Hwao |
