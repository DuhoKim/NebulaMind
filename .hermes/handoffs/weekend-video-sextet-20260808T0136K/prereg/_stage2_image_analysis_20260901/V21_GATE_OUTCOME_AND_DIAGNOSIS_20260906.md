# V21 GATE OUTCOME — SPLIT (agy SIGNABLE, codex NOT-SIGNABLE) — THE CAP RE-ARMS; NO V22 — 2026-09-06 20:39 KST

**Outcome.** Seat A (agy 1.1.27, Gemini as configured, dispatched 2026-09-06T11:26:06Z): SIGNABLE-AS-PRECOMMITMENT, seven [MINOR] "nothing missing" entries, no probe of its own reported. Seat B (codex-cli 0.153.4, gpt-6-astra, 11:26:06Z): NOT-SIGNABLE, with independently executed probes. Codex states in its own words: "I do not find a recurrence of V20's specific unwired-production-verifier FATAL. The findings below are [MAJOR]/[MINOR] … Blanc's same-surface-FATAL stop condition is therefore not invoked on that basis. This bounded gate nevertheless fails; it grants no V22 or further round." Both seats proved access (ACCESS_SHA = 1906d0bb…). Per `BLANC_ORDER_V21_ROUND.md`: the cap RE-ARMS after this gate; V21 did not clear both seats; no READY-FOR-APPROVAL is filed; no V22 is drafted. The lane STOPS on this track and files this record. Nothing approved, signed, frozen, sealed, drawn, fetched, rendered.

**What the round established (codex confirms):** the V20 fatal is repaired — PRODUCTION carries a real re-deriver, `main([...])` under unchanged PRODUCTION reaches `load_identity`, no fixture-only seed callback remains; exact URL membership and BLS-authenticated nonce are real; the atomic first accept held under a real two-process race; all 58 distinct pinned digests match; 69 (+10 estimator) tests pass warning-strict; both diffs reproduce; V15 and V20 bytes intact; the inherited labels are honest.

## Codex's findings, classified by the author (quote-and-answer; no repair in this round)
| # | codex finding (its words, condensed) | class | author's answer |
|---|---|---|---|
| C1 | [MAJOR] "Fabricated event, coherently recomputed digest and invented provenance: ACCEPTED" — the driver checks the event's internal consistency, not GitHub provenance | ACCEPTED DEFECT (new, mine) + DESIGN LIMIT | True: offline, the driver can only check the retained event against itself; the builder's live `gh api` retrieval is not re-established by the driver. Options for the next release: (i) the driver re-queries the Events API when online and refuses on absence (events are served ~30 days); (ii) disclose the retained event as attested-by-builder and bind it by a second witness (Duho's chat statement). Which is a trust choice — flagged for Duho (a) |
| C2 | [MAJOR] W4 (unique approval-record path) is enforced by the witness, not reproduced by the driver; a second pushed V21 record passed `load_identity` | ACCEPTED DEFECT (new, mine) | The driver must run the same `--all --diff-filter=A` glob check; the fixture's per-call unique record names masked it (my fixture design) |
| C3 | [MAJOR] history reset BEFORE the first freeze passes (genesis names public data only; no externally witnessed head); suffix deletion validates; CLOSED persisted but not terminal; `H.genesis` tests existence before locking | ACCEPTED DEFECT (new, mine) | Correct: the chain proves internal continuity, not custody (see memory: hash chains launder tampering). The head must be witnessed outside the file (e.g. the approval record commits an expected genesis hash, or the seal journal witnesses the history head at collection). CLOSED must be terminal in `first_accept`/driver. Lock before the existence test |
| C4 | [MAJOR] "Every path" is false: `BEACON-VALIDATION-ARGS-MISSING`, `FileExistsError` after validation, collector argparse failures escape logging | ACCEPTED DEFECT (new, mine; text overclaim) | The wrapper covers `_validate_inner` only; `build()` and argparse are outside it |
| C5 | [MAJOR] the brief said round 6441904 is "excluded by name"; `BD.EXCLUDED_ROUNDS` lists only 6440756 and 6441924; collect/verdict for 6441904 gives ACCEPT-DRAND | BRIEF ERROR (mine) + a one-line code gap | The RULE text does not claim 6441904 is excluded; my BRIEF did — an overstatement in the dispatch, recorded here. Adding 6441904 to `EXCLUDED_ROUNDS` is a one-line change for the next release |
| C6 | [MAJOR] signature equality is STRING equality: uppercasing the live signature's hex triggers `REFUSE-CONFLICTING-VALID-SIGNATURES` without any key defect; the branch is reachable | ACCEPTED DEFECT (new, mine) | Compare decoded signature bytes; the text's "simulated only" claim is therefore false too |
| C7 | [MAJOR] draft line 33 still requires EQUAL live bytes, names `REFUSE-LIVE-DIFFERS-BUT-VERIFIES`, calls verifying different bytes impossible, and names the old verifier and six-test fixture as current | ACCEPTED DEFECT (text, mine) | I edited line 32 and missed the same claims in the (4) clause of line 33 — a surviving V20 sentence, not a marked quotation |
| C8 | [MAJOR] §3b W1–W4 instructs `APPROVAL_RECORD_SELRULE_V20_<date>.md`; the builder searches `…V21*`; the literal V20 name fails W4 | ACCEPTED DEFECT (text, mine) | A stale filename in line 33 |
| C9 | [MAJOR] sentinel reconciliation: an entry with no refusal cause and no render-end passes; the count is checked only if an end exists | ACCEPTED DEFECT (new, mine) | Require `reason` and a render-end |
| C10 | [MINOR] the seed is SHA256(decoded signature), lowercase hex; lines 32/57 call the signature the seed; uniqueness/timing claims should be stated conditional on the threshold/key assumptions; the predecessor chain is not verified | ACCEPTED (text) | Wording |
| C11 | inherited: the driver trusts pool/exclusion hashes, ordering/renderability metadata and the lists beyond size/type/disjointness (negative objids accepted); offline bodies prove signatures, not two physical hosts | INHERITED (V15 driver) — DISCLOSE, class (a) | Not a V21 defect; the "FULL conjunction" wording must not hide it — flagged for Duho with the other inherited items |
| C12 | checklist/change record "REPAIRED" for M1, M2, X, S, T overstates | ACCEPTED | The dispositions in `V20_REPORT_REPAIR_CHECKLIST` and the change record read as complete repairs; C1–C9 show they were partial. Recorded here; the checklist is not rewritten under the cap |

**Root cause, again mine:** partial repairs described as complete (C4, C7, C12), a fixture that masked a missing check (C2), and a brief that overstated the code (C5). Same family as V16–V20: prose ahead of bytes.

**What a released V22 would need (NOT started; the cap is Blanc's):** C2, C4, C5, C6, C9 are mechanical; C7, C8, C10 are text; C3 needs an external witness of the history head (design); C1 needs a trust decision (Duho, class (a)); C11 is an inherited disclosure for Duho.

## Plain words (for Blanc, and for Duho when he returns)
The one round you allowed did what it was for: the program that could not run now runs, and both referees agree on that. But the stricter referee then found real holes I had not closed — the program still believes a forged copy of GitHub's push record if the forgery is internally consistent, the randomness history can be wiped and rewritten before the first freeze, and three sentences of the rule still describe the old code. I had described these as repaired; they were partly repaired. Under your order the cap is back on, I have drafted nothing further, and this file is the stop. Two items need Duho, not me: whether the program should re-ask GitHub for the push record, and the list of things the old signed rule's driver takes on trust.
