# V22 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE) — the package is NOT yet ready for Duho — 2026-09-06 21:51 KST

**Dispatch.** Fresh mkdtemp sandbox (no deletion; codex's 21:34 safe-preparation note), reviewed file set in its digest list, V22 digest 75eceb10… verified in place; both seats dispatched 21:36:34 KST, both processes exited by 21:49 KST, both ACCESS_SHA proven. Seat A (agy 1.1.27, Gemini as configured): SIGNABLE-AS-PRECOMMITMENT — its own added attack (unpushed local entry: offline ACCEPTED, standalone PENDING-PUSH, composed refused) and "nothing missing". Seat B (codex-cli 0.153.4, gpt-6-astra): NOT-SIGNABLE — no FATAL; six lettered findings from its own executed attacks, a C1–C12 disposition table, and clause text for what is still missing. Codex's own words: "The disclosed offline limitations are not themselves my reason for refusing this gate." Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED; no repair in this round — Blanc holds the package)
| # | finding (codex, condensed) | class | what it means / what the next preparation round would do |
|---|---|---|---|
| A [MAJOR] | LATE FIRST PUBLICATION launders a rebuilt history even in COMPOSED mode: delete the unpublished history, rebuild genesis + one accept, then publish THAT as the first history commit and name it as the history-open commit → offline ACCEPTED, standalone True, composed ACCEPTED (real seed) | ACCEPTED DEFECT (new; design (b) incomplete) | The design said the history-open commit is "witnessed by its own PushEvent … before collection" — the CODE never checks that: it takes the open commit from a lane-supplied file, does not require its blob to be genesis-only, does not authenticate its PushEvent, does not establish publication before collection. This is a THIRD open boundary, absent from the attack inventory. Repair: the history-open commit's PushEvent authenticated like the approval event (live feed; created_at before the first collector attempt's T_pulse), the open blob = exactly the genesis line, and the open commit bound to expected evidence (the approval record names the history path; the seal journal witnesses the open commit) — not a file the lane writes |
| B [MAJOR] | Per-entry publication is a PROMISED workflow: no collector/builder code commits+pushes each entry and waits for acknowledgment; the fixtures publish several entries in one batch and are accepted | ACCEPTED (described-not-built — the defect Blanc named at 21:04, in a new place) | Build the producer boundary: collector and builder commit+push each entry and refuse to proceed until ls-remote acknowledges it; validator requires one acknowledged publication per entry (not merely strict extension); tests fail-first |
| C [MAJOR] | Composed event authentication uses a NARROWER delivery predicate than W3 (head or commits only; no before..head ancestry) → an honest ancestry-delivered push is EVENT-FORGED in composed mode while offline accepts it — a FALSE REFUSAL; "nothing else changes" on adoption is false | ACCEPTED DEFECT (new) | `authenticate_event` must use `approval_witness_v4.delivers` (with the git root) — one predicate for all modes |
| D [MAJOR] | A control failure (`CONTROL FAILURE … return False`) bypasses the exception wrapper: no history entry; §3c "nothing known unlogged" is false again | ACCEPTED DEFECT (C4 still incomplete) | Log `builder-control-refusal` before returning False; test |
| E [MINOR] | "journal ends with render-end" is not what the code checks (a render-end placed first with a later refusal passes) | ACCEPTED (text stronger than code) | Require the last relevant record to be the render-end, or weaken the sentence to the code |
| F [MAJOR] | The questions file says ~90 days; GitHub serves up to 300 events from the past 30 days (V22 §3b W3 says 30 correctly); Option B (second identity / key) is DESCRIBED, not implemented (no actor-login check, no signature interface in P) and must not be offered as available behaviour; an empty feed classifies a structurally valid absent event as FORGED rather than EXPIRED | ACCEPTED (mine: a wrong number put to Duho; and a described-not-built option) | Correct Q1 to 30 days/300 events and state what permanent loss at tune vs holdout means (no new approval/round/split/attempt); mark Option B "not implemented"; fix the empty-feed classification |
| receipt boundary | forged FIRST receipt under the OPS label with a backdated event: standalone EXPIRED → receipt verifies → composed ACCEPTED (real seed) — "exactly the disclosed remaining trusted step, not a new break"; what would close it: a distinct GitHub witness account with credentials unavailable to the lane and its PushEvent checked, or a custodian signing key with a pinned verification key; a second token for the same account is NOT a second identity; a macOS account name is not a GitHub identity; the second event also expires, so a durable binding (a signature) is needed | DISCLOSED-AS-OPEN, confirmed; the identity requirement sharpened | Q1 Option C (no expired path) judged SOUND as the lane's recommendation; Option B must not be offered until built |
| stale text | §9 still says 69 tests / driver 22; §7 still names the V21 builder/collector/approval inputs; track-2 prose and P:4 say "nothing wired" although D:318 wires composed mode; D:113/352 still say "origin Duho names / has not named"; TRACK1 staging record's 81/89 figures superseded; track-2 "every failure kind exercised" overstates (NOT-FOUND and generic HTTP not in the supplied test — codex executed them: they work); §3b still cites old `verify_drand`/`test_verify_drand` as current boundary evidence | ACCEPTED (mine: C7/C12 recurrence — prose behind bytes again) | A single text pass, then the C7 text test extended to catch each of these |
| sandbox | `CODEX_V21_REPAIR_AND_PROVENANCE_PREPARATION_20260906.md` was named in the brief but not copied into the sandbox | ACCEPTED (my dispatch omission) | copy it next time; the package must not claim it was supplied |
| C1–C12 table | C2, C5, C6, C8 (code) REPAIRED; C3 mechanical + old counterexamples REPAIRED, precollection anchoring NOT (finding A); C4 original three REPAIRED, complete claim NOT (D); C7/C9/C10 substantively repaired, text not fully; C1/C11 DISCLOSED-AS-OPEN; C12 not fully repaired (blanket "built/closed" statements contradicted by A–F) | agreed | — |

**Preserved and re-verified by codex, individually:** sample sizes, floors, bar, failed-set and dry-run exclusions, the three historical rounds, custody text byte-identical to V15, blindness text equal to V15, a real future round (its own probe: T_sign 2026-09-07T12:40:35Z → T_pulse 12:51:00Z → round 6445148, unexcluded), ONE holdout unchanged (holdout_once off), E1 erratum truthful, all 78 pinned digests match, 90 + 10 tests OK, exhibit twice 2160fa75…, V15/V20/V21 bytes intact. Codex removed only its own appended line from the in-tree argparse sidecar (the track-1 test writes there — a fixture side effect to fix: the test should use a temp sidecar).

**Root cause, again:** design prose ahead of code (A, B, F-Option-B), and text behind code (stale counts/commands/labels). The mechanisms that exist are sound; the ones that were promised are not all built; and Blanc's rule of 21:04 — disclosure is not completion — was met on the event paths and missed on the history-open anchor and the producer boundary.

**For Blanc (plain words).** The package is not ready to take to Duho: one more real hole in the recommended design (a history can still be rebuilt if it is rebuilt BEFORE its first publication and then published as if new), one promised mechanism not built (each attempt pushed and acknowledged before the next), one false refusal (an honest push shape the composed mode rejects), one wrong number in the question we drafted for Duho (30 days, not 90), and a sweep of stale sentences. None of it is a decision for Duho; all of it is preparation. The agy seat again passed everything and found nothing; it has now passed V16–V22 while codex refused each of them on executed probes. What Duho's questions file gets right, codex confirms: the label is the lane's, no per-receipt chore, "no expired path" is the sound recommendation, and the only code-level closure of the receipt gap is a second GitHub identity or a custodian key — described, not built, and must be labelled so.

**Next preparation round, if released (all class (b), no user decision):** A (history-open anchor authenticated and genesis-only, bound outside a lane-written file), B (producer publish-and-acknowledge per entry), C (one W3 predicate), D (control-failure entry), E (render-end position), F (30 days; loss-at-tune/holdout consequences; Option B labelled not built; empty-feed classification), text sweep + extended C7 test, fixture sidecar side effect, copy the named codex note. Then one review of the complete package again.
