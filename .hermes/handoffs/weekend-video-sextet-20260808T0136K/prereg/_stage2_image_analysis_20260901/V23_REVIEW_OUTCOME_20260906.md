# V23 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE, no fatal) — the package is still NOT ready for Duho — 2026-09-06 22:57 KST

**Dispatch.** Fresh mkdtemp sandbox (126 MB, 4,517 files; the codex note omitted at V22 included); V23 digest 1dd82261… verified in place; both seats dispatched 22:27 KST. Seat A (agy): SIGNABLE, three probes of its own refused in composed mode, "nothing missing"; it flagged two output digests (`negative_probes.py`, `observed_behaviour.py`) differing from the draft text — those lines are V15-era historical evidence pins, to be checked in the text sweep. Seat B (codex), first run: every suite passed in its own execution, then OpenAI's content filter stopped it at the writing step (no report; wrapper quarantine; the brief's "attack/defeat/forged" wording); brief reworded to "verification probes / counter-cases / fabricated", codex re-dispatched alone at 22:40 on unchanged bytes: NOT-SIGNABLE, access proven, 22:55. Codex: "No recurrence of V20's missing-production-rederiver FATAL"; "the disclosed offline limitations are not themselves grounds for this refusal". Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED)
| # | finding (codex, condensed) | class | what the next preparation round does |
|---|---|---|---|
| M1 [MAJOR] | per-entry COMMITS do not prove per-entry ACKNOWLEDGMENT: four single-entry commits pushed together at the end pass v3 and composed mode (the validator checks blob counts and current equality, not that each entry was acknowledged before the next operation) | ACCEPTED (new; design (b) still incomplete) | the validator must see a distinct publication boundary per entry — e.g. each history commit's own PushEvent (server-timed) in the live feed, ordered, one per entry; or the producer records the remote's acknowledgment (head after push) INSIDE the next entry (`ack_head`) so the chain itself proves each push preceded the next append, and the validator checks `ack_head` = the commit that carried the previous entry |
| M2 [MAJOR] | collector error/refusal entries are appended without publishing; restart does not reconcile the pending backlog first; `publish_entry` retries by making a new commit (PUBLISH-COMMIT-FAILED on an already-committed pending entry) | ACCEPTED DEFECT (new, mine) | publish the outer refusal/error entries too; `ensure_genesis` (and the builder) reconcile pending entries with the remote BEFORE any new operation; `publish_entry` pushes an existing pending commit instead of committing again |
| M3 [MAJOR] | the residual "cannot hide a CLOSED witness state" is false unqualified: an ABSENCE-based CLOSED (feed empty at T_pulse + 7 h) logged locally, erased before first publication, then history reconstructed genesis-first with a timely approval event → ACCEPTED in composed mode; only a LATE-event CLOSED is unrecoverable | ACCEPTED (my residual statement overclaimed) | restate the residual exactly: pre-publication work AND pre-publication absence-based terminal decisions are unauthenticated; a late approval event remains disqualifying; a stronger guarantee needs an independently retained decision anchor (not staged) |
| M4 [MAJOR] | Q1 Option A cannot carry a composed invocation past loss of the HISTORY-OPEN event (no receipt path for it); the questions file presents A as the expired workflow without saying so | ACCEPTED (questions file incomplete) | state it in Q1; Option C stays the recommendation, with "both events must authenticate live at tune and holdout; the deadline is actual feed availability" |
| M5 [MAJOR] | four V22-pinned files (`history_v2.py`, `test_track1_fail_first.py`, `coherent_attacks.py`, `test_track2_fail_first.py`) were edited IN PLACE after the V22 review, so the V22 implementation is not reproducible from the tree | ACCEPTED DEFECT (mine — the in-place-edit rule I already knew, broken for four staged files) | RESTORED NOW from the V22 review commit, hash-verified against codex's digests, under `_optionA_dev/_archive_v22_pins/`; rule going forward: every pinned file gets a versioned successor, never an in-place edit |
| M6 [MINOR] | text remnants: T2 "about 90 days", "NOT wired", "would do it themselves", "one network read"; §3c attributes v23 behaviour to builder v22 and omits new stages; §3b(4) still cites the old verifier as current; §9/§10 labels; driver comment "UNNAMED until Duho answers Q1"; track-3 docstring claims name markers | ACCEPTED (C7/C12 recurrence) | one sweep with the text test extended to each remnant |
| A–F | A: original attack repaired, residual NOT completely stated (M3), sequencing limited (M1); B: built, gaps M1/M2; C, D, E: REPAIRED; F: repaired for code and the 30-day bound, decision-facing consequences incomplete (M4) | agreed | — |

**Preserved, re-verified individually by codex:** sizes, floors, bar, failed-set and dry-run exclusions, the three rounds, custody and blindness lines byte-equal to V15, a real future round (its own: T_sign 2026-09-08T13:45:11Z → round 6448158), ONE holdout, E1 erratum, verify_split off; 100 requested tests OK (+ 29 extra); 77/77 file pins match; V15/V20/V21/V22 texts intact.

**Root cause, again:** a design sentence stronger than the code (M1 "acknowledged per entry", M3 "cannot hide CLOSED"), and an operational rule I knew and broke (M5). What is now firmly established after three complete-package reviews: the production path runs; the offline candidate's boundaries are exactly disclosed; the composed mode refuses every reviewer attack that has an anchor; what remains is publication sequencing/recovery and honest residual wording.

**For Blanc (plain words).** Third review, third refusal by codex, again no fatal and again nothing for Duho to decide. The new holes are narrower each time: the program cannot yet tell "each attempt was published before the next" from "all attempts were published at the end"; the collector does not tidy up after a crash before starting again; one sentence promised more than the log can prove; and I edited four files I had pinned, which is a rule of this lane I knew. The four files are restored from git. Recommendation: one more preparation round on M1–M6 (fail-first, no user decision), then review again. Agy has now passed V16–V23 without a finding of its own that codex did not also find.
