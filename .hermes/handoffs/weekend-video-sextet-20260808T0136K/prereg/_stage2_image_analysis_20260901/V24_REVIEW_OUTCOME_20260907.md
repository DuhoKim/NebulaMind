# V24 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE, no fatal) — 2026-09-07 00:11 KST

**Dispatch.** Fresh mkdtemp sandbox (127 MB, 4,543 files), V24 digest f3987cbe… verified in place; both seats dispatched 23:52 KST (2026-09-06), both exited by 00:10 KST, both ACCESS_SHA proven. Seat A (agy): SIGNABLE, "nothing missing". Seat B (codex): NOT-SIGNABLE with five findings N1–N5, its own probes, all 107 tests and every pin verified; codex: "No recurrence of V20's missing-production-rederiver FATAL"; "the disclosed offline limitations are not themselves grounds for this verdict". Register: 24 written, 23 gated, 22 refused, 0 signed. Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED)
| # | finding (condensed) | class | disposition for the next round |
|---|---|---|---|
| N1 [MAJOR] | publication ORDER does not prove operation ORDER: entries committed while the remote held only the genesis, then pushed individually in order, pass; an absence-based CLOSED obtained AFTER genesis publication and erased before its own publication also passes — the residual names only pre-genesis work | ACCEPTED (my residual still too narrow; this is a STRUCTURAL limit) | restate the residual as codex's clause: the validator authenticates the published append-only history and distinct ordered publications, NOT when operations occurred nor the completeness of unpublished observations/decisions before or between publications; the seed is unchanged and a late approval event stays disqualifying; stronger completeness needs independently retained decision evidence, not implemented. No mechanism is proposed to close it — it is a covenant, labelled |
| N2 [MAJOR] | V24's live-evidence contract = approval event + open event + a push event for EVERY history commit at every load; Q1 says "two events"; v4's second retrieval treats an EMPTY feed as HISTORY-PUBLICATION-BATCH (regression of F); a delayed honest event gets the same diagnostic; temporary incompleteness needs a RETRY disposition distinct from proven inconsistency | ACCEPTED DEFECT (new, mine) | v5: empty feed on the second retrieval → UNAVAILABLE/RETRY; a missing per-commit event → EVIDENCE-INCOMPLETE (retry within the window), BATCH only when the feed shows a multi-commit delivery of that commit; Q1 restated for the full contract |
| N3 [MAJOR] | the producer can push a history entry together with an unrelated local commit; the push is acknowledged but v4 refuses (before ≠ parent) — producer precondition not enforced; §7 commands still name the v23 collector/builder | ACCEPTED DEFECT (new, mine; plus text) | `publish_entry` refuses when HEAD has unpublished non-history commits (PUBLISH-UNRELATED-COMMITS); §7 → v24/v25 names |
| N4 [MINOR] | track-4 run 1 = 3 assertion failures + 4 unittest errors, of which the pending-retry case is BEHAVIOURAL (v3 raised PUBLISH-COMMIT-FAILED), so 3 missing-interface + 4 behavioural, not 4+3; track 3 likewise must not be all "missing interface" | ACCEPTED (my classification) | a correction note beside the receipts (receipts themselves are pinned; not edited) |
| N5 [MINOR] | §3c still self-labels V22/v3; §9 names `coherent_attacks.py`; the inspection table header says V22/v6; driver comments name old helpers; track-4 text assertions weak, one vacuous; the pin-drift record cites `_tmp_v22_gate_COPIED_DIGESTS.txt` which was NOT in the sandbox (my rsync excludes `_tmp_*`) | ACCEPTED (text; and a dispatch omission — the cited list exists in the lane, committed at 63401b013) | text sweep with stronger assertions; include the historical digest lists in the next sandbox |
| trust/cost/pins | receipt trust step disclosed accurately; Option B correctly unavailable; cost = one acknowledged publication per entry incl. genesis/failure entries, several retrievals per load; all 81 V24 pins and 80 V23 pins resolve; the four V22 archives match; preserved items re-verified individually (a future round 6453776 from its own clock) | agreed | — |

**Reading, honestly.** Four complete-package reviews: the findings are now about (i) a chronology limit no validator can close without an external decision anchor (N1 — to be stated as a covenant, not repaired), (ii) two producer/validator consistency defects (N2, N3) and (iii) labels. Agy has passed V16–V24 without a finding of its own. Codex's clause text in its §6 is the shape the next candidate should take.

**Next round (preparation, class b):** v5 helpers (N2 empty/incomplete feed dispositions; N3 producer precondition), collector/builder v25 names in §7, residual restated as the covenant (N1), Q1 for the full contract (N2), receipt-classification correction note (N4), text sweep with stronger assertions and the historical digest lists in the sandbox (N5). Then one more review.
