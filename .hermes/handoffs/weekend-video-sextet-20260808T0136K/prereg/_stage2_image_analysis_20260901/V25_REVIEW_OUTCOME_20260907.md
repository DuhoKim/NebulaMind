# V25 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE, no fatal) — 2026-09-07 00:49 KST

**Dispatch.** Fresh mkdtemp sandbox (134 MB, 4,569 files; the historical digest lists and the preserved failed log INCLUDED), V25 digest d29aabac… verified in place; both seats dispatched 00:32 KST, both exited by 00:48 KST, both ACCESS_SHA proven. Seat A (agy): SIGNABLE, "nothing missing". Seat B (codex): NOT-SIGNABLE with four findings P1–P4 (two MAJOR, two MINOR); all 113 tests, every pin (83 values), the four V22 archives and the three historical digest lists verified; codex: "No recurrence of the missing-production-rederiver FATAL"; "Neither defect demonstrated an inconsistent identity loading in composed mode"; "the disclosed offline boundaries and the honestly stated chronology covenant are not themselves reasons for refusal". Register: 25 written, 24 gated, 23 refused, 0 signed. Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED)
| # | finding (condensed) | class | disposition for the next round |
|---|---|---|---|
| P1 [MAJOR] | a proven batch delivered by a push event WITHOUT `payload.commits` (GitHub's own documented shape) is classified EVIDENCE-INCOMPLETE (retry) instead of HISTORY-PUBLICATION-BATCH — batch classification searches only `payload.commits`, not the before..head ancestry the package already uses as delivery evidence | ACCEPTED DEFECT (new, mine) | v6: a commit is "carried" if any push event delivers it by the shared `delivers` predicate (head/commits/before..head) with head ≠ commit or before ≠ parent → BATCH (terminal); absence with no such event → EVIDENCE-INCOMPLETE (retry) |
| P2 [MAJOR] | absence from a non-empty feed is still called FORGED for the approval and history-open events (`authenticate_event`); a temporary omission does not prove fabrication; the empty-open case lacks the RETRY prefix | ACCEPTED DEFECT (mine; the F/N2 principle not carried to the first two retrievals) | v6: absent from the feed → EVIDENCE-INCOMPLETE (retry) unless the feed contains a push delivering the same commit with DIFFERENT canonical bytes → FORGED (affirmative contradiction); all three retrieval stages return uniform RETRY-prefixed dispositions in the driver |
| P3 [MINOR] | remnants: "on the V22 candidate" before the 113-test sum; driver comments naming v2 helpers; the v25 inspection docstring saying driver v6 / V22; T2 "offered with their costs" for A/B; "a rejected push = DIVERGED, dead" (a temporary rejection is recoverable PENDING-PUSH) | ACCEPTED (text) | sweep + assertions for each |
| P4 [MINOR] | the questions file does not carry the N1 covenant (present in code, design doc, §3c) | ACCEPTED (text) | add the covenant to Q1's contract paragraph |
| trust/cost/pins | receipt trust step, Option C recommendation, A′/B unavailability, cost per entry, all pins, archives and historical lists: agreed; retry loops cannot load an inconsistent identity (three complete→complete→empty sequences all refused) | agreed | — |

**Reading.** Five reviews; the findings are now two disposition defects in the same family (absence ≠ proof, ancestry as evidence) and text. Agy has passed V16–V25 without a finding of its own. Codex's §6 clause text is the shape of the next candidate.

**Next round (preparation, class b):** track 6 = P1 (ancestry-aware batch classification), P2 (absence → retry unless contradicted; uniform RETRY prefixes), P3 (sweep + assertions), P4 (covenant in Q1). Fail-first, successors only, then one more review.
