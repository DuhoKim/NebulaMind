# V26 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE, no fatal) — 2026-09-07 01:18 KST

**Dispatch.** Fresh mkdtemp sandbox (136 MB, 4,587 files; all historical digest lists included), V26 digest 9a13afd1… verified in place; both seats dispatched 01:02 KST, both exited by 01:18 KST, both ACCESS_SHA proven. Seat A (agy): SIGNABLE, "nothing missing". Seat B (codex): NOT-SIGNABLE with V26-1, V26-2 (MAJOR) and V26-3 (MINOR); all 118 tests, 85 pin values, the archives and the historical lists verified; codex: "No new FATAL or composed acceptance bypass was demonstrated"; P1 REPAIRED; P4 REPAIRED; the covenant and the receipt trust step now stated in all four places. Register: 26 written, 25 gated, 24 refused, 0 signed. Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED)
| # | finding (condensed) | class | disposition for the next round |
|---|---|---|---|
| V26-1 [MAJOR] | a SAME-EVENT contradiction (same id, same head, different `before`) is classified INCOMPLETE because the contradiction test requires head AND before to equal the retained event's; an affirmative contradiction became a retry | ACCEPTED DEFECT (mine — the contradiction predicate was too narrow) | v7: contradiction = a live event with the same event id, or delivering the same commit under the shared predicate, whose canonical bytes differ from the retained → FORGED (terminal); absence stays INCOMPLETE; predicate stated and tested |
| V26-2 [MAJOR] | `delivers` collapses "cannot establish ancestry" (missing git objects, failed command) into False, which `authenticate_event` reads as proven non-delivery → FORGED; a clone lacking the descendant object turns an AUTHENTIC event into FORGED | ACCEPTED DEFECT (mine) | v7: a tri-state delivery check — DELIVERED / NOT-DELIVERED / UNDETERMINED (missing objects, command failure) → UNDETERMINED is EVIDENCE-UNAVAILABLE (retry); wrong type / wrong repo / earlier-event violations get their own accurately named inconsistency outcomes, distinct from "different bytes" |
| V26-3 [MINOR] | remnants: T2 outcomes row still says FORGED on absence and omits INCOMPLETE; provenance docstring calls EXPIRED retry-class; Q names OPEN-EVENT-EXPIRED; A′ paragraph both accepts and retries EVIDENCE-UNAVAILABLE; R:33 names `beacon_record_drand_v22.collect` and `verify_drand.py` as current; driver v10's historical lines were over-renamed to v6 (my regex) | ACCEPTED (text; the over-broad rename is a new self-inflicted inaccuracy) | sweep with per-item assertions; historical lines restored to their true names |
| covenant / receipt / cost / pins / preserved | all stated in the four places; cost per entry; 85/82 pins; archives and lists consistent; preserved items re-verified (future round 6448440 from its clock; BEACON-NOT-YET before the pulse with zero network calls) | agreed | — |

**Reading.** Six reviews. Codex confirms P1, P4, N1–N5, M1–M6, A–F as repaired or covenant; what remains is the exact boundary of "affirmative contradiction" (too narrow in one direction, too broad in the other) and text. Agy has passed V16–V26 without a finding of its own.

**Next round (preparation, class b):** track 7 = V26-1 (same-event contradiction terminal), V26-2 (tri-state delivery; UNDETERMINED = retry), V26-3 (sweep incl. restoring the over-renamed historical driver lines). Fail-first, successors only, the new V26 text test carried as a pin, then one more review.
