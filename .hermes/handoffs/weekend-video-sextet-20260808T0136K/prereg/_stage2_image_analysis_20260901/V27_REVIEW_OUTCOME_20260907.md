# V27 COMPLETE-PACKAGE REVIEW — OUTCOME (split: agy SIGNABLE, codex NOT-SIGNABLE, no fatal) — 2026-09-07 02:00 KST

**Dispatch.** Fresh mkdtemp sandbox (137 MB, 4,613 files; every historical digest list and every preserved aggregate log included), V27 digest 154a680c… verified in place; both seats dispatched 01:44 KST, both exited by 02:00 KST, both ACCESS_SHA proven. Seat A (agy): SIGNABLE, "nothing missing". Seat B (codex): NOT-SIGNABLE with V27-1, V27-2 (MAJOR) and V27-3 (MINOR); all 124 tests, 89 pin values, the archives, the historical lists and both aggregate logs verified; codex: "No new FATAL and no undisclosed successful composed identity bypass were demonstrated"; V26-1's before/head cases and V26-2's stale-clone case REPAIRED; the honest re-delivery cases it tried did NOT reach FORGED. Register: 27 written, 26 gated, 25 refused, 0 signed. Nothing adopted; no beacon read, draw, fetch, render, pixel.

## Codex's findings, classified by the author (all ACCEPTED)
| # | finding (condensed) | class | disposition for the next round |
|---|---|---|---|
| V27-1 [MAJOR] | PRECEDENCE: (a) a same-id contradiction whose changed head needs an unavailable ancestry object returns UNAVAILABLE before the contradiction is examined; (b) a verbatim copy present alongside a differently-encoded same-id copy returns AUTHENTIC without the contradiction check; (c) a locally decidable mismatch (wrong repository) becomes RETRY-EVENTS-UNAVAILABLE when the retrieval fails (HTTP 503) because `authenticate_event_live` retrieves first; the same-commit contradiction arm must be scoped to the pinned protected history | ACCEPTED DEFECT (mine — order of checks) | v8: evaluate locally decidable input mismatches BEFORE any retrieval; evaluate same-id contradictions in the available feed BEFORE the unresolved-delivery and verbatim-presence shortcuts; state the precedence and the protected-history scope in the docstring and the text; tests for each |
| V27-2 [MAJOR] | tri-state delivery is not propagated through the production path's earlier Boolean prerequisites: the driver's IDENTITY-WITNESS-COMMIT precheck (Boolean `AW.delivers`) fires before composed provenance, so an undetermined ancestry claim is a non-retry refusal; the open-event stage (v3 code via v6/v7) uses Boolean `delivers` → OPEN-EVENT-DOES-NOT-DELIVER; positive non-delivery is not named EVENT-INCONSISTENT on the full path; a git launch exception raises OSError instead of UNDETERMINED | ACCEPTED DEFECT (mine — the tri-state helper was added but the older prerequisites were left Boolean) | v8/driver v12: in composed mode the driver's witness-commit precheck and the open-event stage use `delivery()`; UNDETERMINED → RETRY-EVENTS-UNAVAILABLE; NOT-DELIVERED → EVENT-INCONSISTENT; subprocess launch failures → UNDETERMINED |
| V27-3 [MINOR] | names/counts: D:6 lineage table says v10 composed on provenance_designs_v7 (it used v6 — my table error); D:80/291 comments say v6; D:327 names validate_continuation_v6 for a v7 call; the inspection header says driver v10 / standalone v6; P:49 "pure decision unchanged", P:13 historical sentence grouping EXPIRED with retry; T2 row 9b attribution; R §9 calls the V22 inspection "THIS candidate"; "seventeen suites" is false — both logs carry 18 blocks and §3c lists 18 modules | ACCEPTED (text; the lineage-table error is a new self-inflicted inaccuracy) | sweep with per-item assertions; count corrected to 18 |
| honest re-delivery | codex tried an honest push of the approval commit to another ref and an ordinary advancing push: EXPIRED, not FORGED; no honest false-FORGED case within the protected-ref covenant | confirmed | keep the same-commit arm scoped to the pinned ref (V27-1) |
| covenant / receipt / cost / pins / preserved / attribution | all confirmed; retry loops never load an inconsistent identity; the attribution file read correctly; both aggregate logs preserved | agreed | — |

**Reading.** Seven reviews. The remaining defects are the ORDER in which the checks run (a contradiction or an input mismatch must be decided before a retry is declared) and the propagation of the tri-state helper into two older Boolean prerequisites; plus names. Agy has passed V16–V27 without a finding of its own.

**Next round (preparation, class b):** track 8 = V27-1 (precedence + scope), V27-2 (tri-state through the driver precheck and the open-event stage; git exceptions), V27-3 (sweep incl. the lineage table and the 18-suite count). Fail-first, successors only, then one more review.
