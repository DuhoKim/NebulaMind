# V29 COMPLETE-PACKAGE REVIEW — OUTCOME AND CLASSIFICATION — 2026-09-07 03:17 KST

Candidate: `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V29_20260907.md` (preimage 4458b821e3a1249c0646ce2790491f4d04e7224d0ef60721144354e216450599), fresh sandbox selrule_v29_gate_63ir5esk (4,671 files; both seats ACCESS PROVEN; dispatched 03:01, both exited by 03:16 KST). Seat A agy (Gemini): **VERDICT: SIGNABLE-AS-PRECOMMITMENT** — all MINOR, all ACCEPTED. Seat B codex (gpt-6-astra): **VERDICT: NOT-SIGNABLE** — two MAJOR, two MINOR, no FATAL; 136/22 reproduced, both exhibits, all 35 inspection rows, the seven track-9 combined cases reproduced on the complete path, fail-first independently re-run against the V28 modules, no acceptance bypass. SPLIT, as V22–V28 (agy passed all fourteen; codex refused all fourteen). Nothing gated, adopted, signed or drawn.

## Classification — contradictions and limits kept apart (Blanc 02:06)

| finding | class | what codex executed | disposition |
|---|---|---|---|
| **V29-1 [MAJOR]** seed re-derivation returns a retry before approval precedence runs | **UNREPAIRED CONTRADICTION** (column C) | a coherently sealed identity whose retained seed-signature bodies are unverifiable (offline verdict RETRY → driver REDERIVE-RETRY at D:303) combined with an approval event that is undetermined + wrong/repo, or undetermined + a genuine conflicting same-id event in the feed: the complete load returns REDERIVE-RETRY with ZERO event retrievals, where the authentication control answers INCONSISTENT-INPUT / FORGED | ACCEPTED. The V29 text's clause (1) ("locally decidable retained-event mismatches are terminal before ANY evidence-unavailability return") is contradicted by stage order: the seed re-deriver's retry precedes composed provenance. Track 10: in composed mode the local sweep of BOTH retained events and one live retrieval with same-id authentication run BEFORE seed re-derivation and before any other stage's retry. |
| **V29-2 [MAJOR]** remote availability and the approval retrieval still bypass local open-event mismatches | **UNREPAIRED CONTRADICTION** (column C) | a retained open event naming wrong/repo (undetermined delivery): ls-remote failing → HISTORY-CONTINUATION: RETRY-REMOTE-UNAVAILABLE (v3 returns before the open event is inspected; the v9 wrapper only acts on an UNDETERMINED delivery field, which a remote failure never sets); approval-stage HTTP 503 first → RETRY-EVENTS-UNAVAILABLE before the open file is even read; only when the stage is reached → OPEN-EVENT-INCONSISTENT-INPUT | ACCEPTED. Track 10: the open event is read and locally checked (type / repository / protected ref) before any retrieval or remote call; its same-id authentication uses the one retrieval already made for the approval event; the history stage's remote failure can then only retry for what is genuinely unavailable. Codex's proposed clause carried verbatim. |
| **V29-3 [MINOR]** names and pin text | ACCEPTED DEFECTS (text) | the §3c aggregate sentence still says "driver v12 / builder v28 / verdict v28" (lane check: 1 occurrence); four literal backreferences `\1` / `\2` stand where the digests of track7/test_track7_fail_first.py, track7/test_track7_text_v27.py, track8/test_track8_fail_first.py and track8/test_track8_text_v28.py belong (lane check: 4 in V29, 2 already in V28 — the V28 and V29 edit scripts used regex backreference syntax inside a plain string replacement; the V28 review did not catch it) | Track 10: the four digests written out; the labels corrected; the text test asserts no backreference token remains and the current-module labels match the executed suites. |
| **V29-4 [MINOR]** rule (iii) equal-time edge | ACCEPTED DEFECT (predicate) | two distinct-id qualifying events with EQUAL created_at: reversing feed order flips AUTHENTIC into NOT-EARLIEST, whose explanation asserts an earlier event that does not exist (a predicate stress test, not an honest-history case) | Track 10: NOT-EARLIEST requires a STRICTLY earlier qualifying event; equal timestamps are not "earlier"; feed order is no tie-break. Codex's clause carried verbatim. |

Codex also: (a) confirmed the exact V28-1/V28-2 filed cases REPAIRED and judged the UNIVERSAL complete-path claim NOT REPAIRED (V29-1/2) — the lane's section D "none known" is superseded; column C reopened below; (b) judged column A's qualifications "necessary and now substantially honest" (L-OFF a default-mode boundary, L-INH a staged-but-disabled check, L-AVAIL external + stateless enforcement, L-COV an evidence limit of this design, L-RCPT removable by a different identity/key design) — none excusing a decidable mismatch receiving a retry; (c) executed the covenant (unpublished CLOSED erased in each interval → ACCEPTED offline and composed, as disclosed) and the fabricated first receipt (verify_events_receipt True; A′ approval-only path ACCEPTED; Option C EVENT-EXPIRED-NO-RECEIPT-PATH) — the disclosed trusted step, nothing undisclosed; (d) Option C remains the sound recommendation, subject to repairing the misleading precedence claims; (e) the V23 historical digest list carries successors, not all four V22 values — the drift record's reliance on the V22 list is supported, and the V23 list must not be described as old-byte evidence; (f) read track 9's run 1 correctly: one complete-path failure at the FIRST sub-case, one header-phrase failure, one missing-file error — not seven separately observed old-path failures (the lane's receipt says the same and cites codex's V28 report for the rest); (g) preserved items each re-verified; actual-future probe BEACON-NOT-YET, zero fetches; 91 full digests, 88 resolve to retained bytes, the other three generated outputs; literal backreferences counted as defects, not pins.

## Codex's clause texts (verbatim; to be carried into V30 and the code)
"Before any retry is returned from load_identity, the implementation checks locally decidable type, repository and protected-ref mismatches in both retained approval and history-open events. This obligation applies before seed re-derivation availability returns, approval-feed availability returns, and history remote-head or fetch availability returns. Delivery checks that require unavailable remote objects remain undetermined; they do not excuse other locally decidable mismatches. In composed mode, deferred approval or open-event delivery must not bypass obtainable same-ID authentication merely because another stage has queued a retry. A retry is emitted only after the applicable higher-priority terminal checks have run. Offline undetermined delivery retains its explicitly named refusal."
"NOT-EARLIEST requires a strictly earlier qualifying event under the explicitly specified ordering. Equal timestamps alone do not prove that another event is earlier; feed iteration order is not an unstated tie-break."

## Next (routine repair under existing authority; no new gate, no question for Duho)
Track 10: driver v14 + provenance v10 (+ collector v30 / builder v30, tests): a LOCAL SWEEP of both retained events and ONE live retrieval with same-id authentication for both, placed before seed re-derivation and before any other stage's retry in composed mode; strict-earlier ordering; the four digests written out; labels; fail-first with codex's three V29-1 rows and four V29-2 rows on the complete path; V30 text; review.
