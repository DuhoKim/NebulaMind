1c1
< # OPTION (A) — INSTRUMENT SELECTION RULE, DRAFT V29 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED) — DRAND-ONLY SAMPLING AMENDMENT OF THE SIGNED V15 (SOURCE DECISION BY DUHO, VIA CODEX VOICE, ~19:02 KST: "해")
---
> # OPTION (A) — INSTRUMENT SELECTION RULE, DRAFT V30 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED) — DRAND-ONLY SAMPLING AMENDMENT OF THE SIGNED V15 (SOURCE DECISION BY DUHO, VIA CODEX VOICE, ~19:02 KST: "해")
3c3
< V29 of 2026-09-07 is V28 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V28_REVIEW_OUTCOME_20260907.md`), with codex's V28-1/2/3 answered FAIL-FIRST (`TRACK9_FAIL_FIRST_RECEIPT_20260907.md`: 3 tests failing against byte-copies of the V28 modules — the complete-path case reproducing codex's exact retry, the rule-(iii) guard, the text case; 3 passing after; the complete-path case carries seven sub-cases: approval delivery undetermined + genuine same-id contradiction in the feed → EVENT-FORGED; + wrong repository, with a healthy feed and with HTTP 503 → EVENT-INCONSISTENT; undetermined with nothing contradicting → RETRY-EVENTS-UNAVAILABLE, unchanged; the same three on the history-open stage → OPEN-EVENT-FORGED / OPEN-EVENT-INCONSISTENT-INPUT / EVIDENCE-UNAVAILABLE). V28-1 and V28-2 were UNREPAIRED CONTRADICTIONS the lane's own V27-2 repair had introduced — a tri-state precheck that returned a retry before the precedence rules could run — filed in column two of the register at the V28 outcome and repaired here; codex's clause text is carried verbatim into the code header, this paragraph and §3c: (V28-1) "On both the approval and history-open paths, locally decidable retained-event mismatches are terminal before any evidence-unavailability return. In composed mode, unresolved delivery does not bypass live authentication: a same-ID canonical contradiction obtained from the live feed is terminal before an undetermined-delivery retry. Only when no higher-priority inconsistency is established may undetermined delivery or unavailable retrieval return the stage's retry disposition. Offline mode retains its explicitly named undetermined-delivery refusal." — implemented by driver v13's witness-commit precheck DEFERRING an undetermined delivery to composed provenance (local_precheck → retrieval → same-id contradictions → only then the retry) while positive non-delivery stays terminal and offline keeps IDENTITY-WITNESS-COMMIT-UNDETERMINED; (V28-2) `validate_continuation_v9` consults the feed the same way before returning EVIDENCE-UNAVAILABLE; (V28-3) "Same-commit contradiction is scoped to the pinned protected ref and evaluated only when the retained event is absent. If it is present verbatim, distinct qualifying events are resolved by earliest-event ordering; a later event does not invalidate an authentic earliest event. Conflicting same-ID evidence is refused even beside a verbatim copy, without asserting that this alone identifies which producer supplied false evidence." — rule (iii) restated (the V28 phrasing 'another delivering push is an ordering question — NOT-EARLIEST' was too short: a later distinct qualifying push beside a verbatim earliest genuine event is AUTHENTIC; NOT-EARLIEST needs an EARLIER qualifying event), the two false filename↔digest associations corrected (the V28 edit's token replacement had renamed the historical builder-fixture clause; the V27 edit's line-33 replacement had put the historical verifier's digest beside the pinned verifier's name), the driver fixture header names itself, the inspection's row-1 note says INCONSISTENT-INPUT for the wrong-repository forgery, the questions file's contract sentence names both arms of FORGED with the absent-only qualification, the design document's outcomes row is restated for v9 (revision 9), and L-INH / L-AVAIL carry their mode qualifications. V28 of 2026-09-07 was V27 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V27_REVIEW_OUTCOME_20260907.md`), with codex's V27-1/2/3 answered FAIL-FIRST (`TRACK8_FAIL_FIRST_RECEIPT_20260907.md`: 7 tests failing against byte-copies of the V27 modules, classified per test — 3 behavioural, 2 missing-interface, 1 complete-path, 1 text; run 1b executing the V27 functions: a same-id contradiction with an undeterminable head called UNAVAILABLE, a differently encoded same-id copy beside a verbatim one called AUTHENTIC, a wrong-repository event under HTTP 503 called UNAVAILABLE; run 1c executing the V27 driver: an undeterminable witness delivery called IDENTITY-WITNESS-COMMIT; 7 passing after). BLANC'S ORDER OF 02:06 KST (`BLANC_ORDER_V27_CONTRADICTIONS_NOT_LIMITS.md`): V27-1 and V27-2 were CONTRADICTIONS between the stated contract and the implemented path — REPAIRED, not disclosed; the categories are kept apart in §3c and in `LIMITS_VS_CONTRADICTIONS_20260907.md`. (V27-1) PRECEDENCE, stated as three rules and tested: locally decidable input mismatches (not a PushEvent, wrong repository, positive non-delivery) are decided BEFORE any retrieval (`local_precheck`); a same-id contradiction is decided BEFORE the undetermined-delivery return and BEFORE the verbatim-presence shortcut; the same-commit arm is scoped to the pinned protected ref and evaluated only when the retained event is absent (when it is present, another delivering push is an ordering question — NOT-EARLIEST); no other precedence exception exists. (V27-2) TRI-STATE THROUGH EVERY STAGE: the driver's witness-commit precheck and the validator's open-event stage (`validate_continuation_v8`) use `delivery` — UNDETERMINED is RETRY-EVENTS-UNAVAILABLE / EVIDENCE-UNAVAILABLE (offline mode, which has no retry vocabulary, names it IDENTITY-WITNESS-COMMIT-UNDETERMINED), positive non-delivery is EVENT-INCONSISTENT / OPEN-EVENT-INCONSISTENT-INPUT, and a git process-launch exception is UNDETERMINED. (V27-3) driver v12's lineage table re-verified row by row against each retained driver's own import line (v10 composed on provenance_designs_v6, not v7), the helper docstrings corrected where they described current code (historical paragraphs kept and marked), the inspection header names the modules it executes, the design document's row 9b says INCONSISTENT-INPUT, §9 no longer calls the V22 inspection an inspection of this candidate, and the suite count is taken from the executed logs (20 suites). V27 of 2026-09-07 was V26 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V26_REVIEW_OUTCOME_20260907.md`), with codex's V26-1/2/3 answered FAIL-FIRST (`TRACK7_FAIL_FIRST_RECEIPT_20260907.md`: 4 tests failing against byte-copies of the V26 modules, classified per test; run 1b executing the V26 functions — a same-event contradiction called INCOMPLETE, missing git objects called FORGED; 4 passing after): (V26-1) the CONTRADICTION PREDICATE is stated — a live event with the same event id, or delivering the same commit, whose canonical bytes differ (a difference only in `before` or `head` counts) is FORGED, terminal; (V26-2) DELIVERY IS TRI-STATE — DELIVERED / NOT-DELIVERED / UNDETERMINED; UNDETERMINED (a git object missing locally, a failed git command) is UNAVAILABLE, a retry, never forgery; a retained event that positively does not deliver the commit or names the wrong repository is INCONSISTENT-INPUT; a verbatim-present but not-earliest event is NOT-EARLIEST; EVIDENCE-EXPIRED is a loss, not a retry, at every stage; (V26-3) the remnants swept, and driver v11 states its lineage in a table instead of the over-renamed historical lines of v10; the text tests are attributed (`TEXT_TEST_ATTRIBUTION_20260907.md`): the V26-specific test is retained as pinned and a V27-specific test is added. V26 of 2026-09-07 was V25 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V25_REVIEW_OUTCOME_20260907.md`), with codex's P1–P4 answered FAIL-FIRST (`TRACK6_FAIL_FIRST_RECEIPT_20260907.md`: 5 tests failing against byte-copies of the V25 modules, classified per test; run 1b executing the V25 functions; 5 passing after): (P1) batch classification uses the SAME delivery evidence as authentication — `delivers` (head, payload.commits, or before..head ancestry; GitHub PushEvents may omit the commits list) — so a proven batch is terminal HISTORY-PUBLICATION-BATCH even without a commits list; (P2) ABSENCE IS NEVER FORGERY — a retained approval or history-open event missing from a feed is EVIDENCE-INCOMPLETE (retry), FORGED only on an affirmative contradiction (a push delivering the same commit with different bytes), and all three retrieval stages use one EVIDENCE-* vocabulary (driver v10 prefixes UNAVAILABLE/INCOMPLETE with RETRY-; EXPIRED is a loss, not a retry); (P3) labels swept; (P4) THE COVENANT is carried into the questions file. V25 of 2026-09-07 was V24 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V24_REVIEW_OUTCOME_20260907.md`), with codex's N1/N2/N3/N5 answered FAIL-FIRST (`TRACK5_FAIL_FIRST_RECEIPT_20260907.md`: 6 tests failing against byte-copies of the V24 modules — 3 missing-interface, 3 behavioural — run 1b executing the V24 functions; 6 passing after) and N4 by `TRACK_RECEIPTS_CLASSIFICATION_CORRECTION_20260907.md`: (N1) THE COVENANT replaces the residual (§3c) — the validator authenticates the published append-only history and distinct ordered publications, NOT when the operations occurred nor the completeness of unpublished observations or decisions before the genesis was published or between later publications; (N2) `validate_continuation_v5` dispositions — EVIDENCE-UNAVAILABLE and EVIDENCE-INCOMPLETE are RETRY, HISTORY-PUBLICATION-BATCH is terminal only when the feed proves a multi-commit delivery — and the questions file states the FULL live contract (approval event, history-open event and one push event per history commit); (N3) the producer refuses PUBLISH-UNRELATED-COMMITS and §7 names the v25 collector/builder; (N5) labels swept. V24 of 2026-09-06 was V23 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V23_REVIEW_OUTCOME_20260906.md`), with codex's M1/M2/M3/M6 repaired FAIL-FIRST (`TRACK4_FAIL_FIRST_RECEIPT_20260906.md`: 7 tests failing against byte-copies of the V23 modules, 7 passing after; run 1b executes codex's late-push case on the V23 validator): (M1) PER-ENTRY PUBLICATION IS PROVEN BY THE SERVER — `validate_continuation_v4` requires one PushEvent per history commit in the live feed delivering exactly it (head = commit, before = its parent), server-timed in order; several single-entry commits delivered by one late push are HISTORY-PUBLICATION-BATCH; (M2) `publish_entry` pushes an already-committed pending entry instead of re-committing; collector v24 and builder v24 RECONCILE a pending entry before any operation (`reconcile_pending`) and the collector publishes its outer refusal/error entries; (M3) the residual is restated exactly (§3c); (M5) the four V22-pinned files edited in place are recorded in `PIN_DRIFT_RECORD_V22_M5_20260906.md` and archived under `_optionA_dev/_archive_v22_pins/`; the PIN IMMUTABILITY RULE (`PIN_IMMUTABILITY_RULE_20260906.md`) governs every later change; (M4) the questions file presents Option A′ as not built for the history-open event and Option C as the only working option. V23 of 2026-09-06 was V22 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V22_REVIEW_OUTCOME_20260906.md`), with codex's findings A–F repaired FAIL-FIRST (`TRACK3_FAIL_FIRST_RECEIPT_20260906.md`: 10 tests failing against byte-copies of the V22 modules, 10 passing after): (A) the history-open anchor is AUTHENTICATED — the first remote commit touching the history must carry a GENESIS-ONLY blob and be delivered by a retained PushEvent authenticated in the live feed, so a rebuilt history published as its own first commit is refused; (B) the PRODUCER BOUNDARY is BUILT — collector and builder publish each history entry and require the remote's acknowledgment before proceeding, and the validator requires one entry per acknowledged commit; (C) ONE delivery predicate (W3's, before..head included) in every mode; (D) a control failure appends a named history entry; (E) the render-end must be the last relevant journal record; (F) the questions file says 30 days / 300 events and marks Option B NOT IMPLEMENTED; an EMPTY live feed is UNAVAILABLE, never FORGED. The complete object is `V23_CANDIDATE_ATTACK_INSPECTION_20260906.md` (rows 9c–9k, 10a–10d). V22 was a STAGED CANDIDATE prepared under Blanc's two-track order of 20:45 KST (cap lifted) and codex's coordination note of 20:43: TRACK 1 — the mechanical repairs of codex's V21 findings, each with a test that FAILED against byte-copies of the V21 modules before the repair and passes after (`TRACK1_FAIL_FIRST_RECEIPT_20260906.md`); TRACK 2 — the two provenance boundaries codex left open (a coherently forged GitHub event, a history rebuilt before the first freeze) are NOT closed by this candidate offline; their RECOMMENDED designs are staged as executable exhibits, UNADOPTED, in `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md` and `_optionA_dev/track2/`; the candidate DISCLOSES both as open (§3c) until Duho adopts a design. The complete object — the successful production path AND codex's coherent attacks re-run against these pinned bytes — is `V22_CANDIDATE_ATTACK_INSPECTION_20260906.md`. V21 was REFUSED (agy SIGNABLE; codex NOT-SIGNABLE: no recurrence of the V20 fatal, nine MAJORs — `V21_GATE_OUTCOME_AND_DIAGNOSIS_20260906.md`). V21 of 2026-09-06 (Blanc's bounded round, order of 19:48 KST + amendment 19:50) was V20 with the PRODUCTION PATH MADE EXECUTABLE and the conjunction computed from committed evidence. V20 was REFUSED by codex (FATAL: the pinned driver's production protocol carried `rederive_seed = None`, so the real entry point refused every identity with `PROTOCOL-NO-REDERIVER` and only fixtures injecting a callback ever loaded one; agy passed V20). Per the order the fatal was REPRODUCED in executed output BEFORE any repair (`V20_FATAL_REPRODUCTION_20260906.md`: `PRODUCTION.rederive_seed = None`; the real `load_identity` refuses a production-shaped sealed identity; the same identity loads only with an injected callback), then repaired: driver v5 binds a real module-level re-deriver into PRODUCTION and the fixtures use PRODUCTION's own function (§E3, §3c); every other V20 finding is tabled in `V20_REPORT_REPAIR_CHECKLIST_20260906.md` (new / inherited; in / out of scope; (a) a change needing Duho vs (b) preparation; disposition). Inherited defects of the SIGNED V15 are not repaired here; where a fix could be prepared it is carried in this candidate LABELLED "INHERITED — PREPARED, NOT ADOPTED" with what it would change, and adopting it is Duho's call. V20 replaced the two-source design of V15–V19 with ONE public randomness source, by Duho's decision: via codex voice at about 19:02 KST, after Codex presented the drand-only direction and its availability trade-off (wait, never switch source), Duho answered "해" ("Do it") — recorded by Codex in `.hermes/CODEX_DUHO_DRAND_ONLY_DECISION_20260906.md`; a source-direction decision, not a signature, not a review verdict, not a draw. History: V16–V21 were refused at gates and V22–V28 at complete-package reviews (each by codex; agy passed all thirteen); V19 fell on the property it claimed — with two sources, the failing side (NIST) leaves no authenticator, so the FIRST collection could still select the source; the author's 12:52 answer overstated this and the OPS coordinator released on it (recorded in the change record). V20 removes the choice: the seed is the drand mainnet default chain's threshold BLS signature for a prospectively fixed round, verified cryptographically under the pinned public key, bound to the pinned chain hash — a function of (round, key) that no relay, operator or collection time can change; unavailability is RETRY, never a different value. The signed V15 (fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1, T_sign 2026-09-06T00:04:07Z) stays on file unchanged with its RETRY record 1c1d9d4bacc35319e91d46420b8f892e1126cc48059ec69156a0a718428aeb4b; its own defects are on record (\`V19_REPORT_AUDIT_TRIAGE_20260906.md\` §C) and are not repaired here. Preserved byte-for-byte from V15: sample sizes 400/200/2,000, floors 380/190/1,900, the 0.70 bar, the exclusions, ONE holdout, the label/pixel boundaries, custody, branch protection, the §6 pipeline identity (executing under the approved Tier-C V39). The V19 audit's two MAJOR findings are repaired here because every option needed them: the driver verifies the acceptance conjunction semantically (§3c) and the collection history is complete on every path (§3c). V15 REMAINS OPERATIVE on paper until a candidate is approved on its final bytes; a clear pair is READY-FOR-APPROVAL, not permission; nothing is drawn under a draft.
---
> V30 of 2026-09-07 is V29 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V29_REVIEW_OUTCOME_20260907.md`), with codex's V29-1/2/3/4 answered under BLANC'S ORDER OF 03:20 KST (`BLANC_ORDER_PRECEDENCE_IS_ONE_PATTERN.md`): V27-1, V27-2, V28-1/2, V29-1 and V29-2 were ONE pattern — a check returning before the rule that governs it — four found by a reviewer constructing a counter-case and one introduced by the lane's own repair; so V30 is NOT a sixth point fix. PRECEDENCE ORDER, STATED ONCE (Blanc's order of 03:20 KST, `BLANC_ORDER_PRECEDENCE_IS_ONE_PATTERN.md`; the constant `PRECEDENCE` in `provenance_designs_v10`; when two or more verdict classes are simultaneously derivable the higher one wins, top to bottom, no exceptions): 0 LOCAL-TERMINAL (locally decidable refusals needing no external evidence — identity integrity, retained-event type / pinned repository / protected ref / positive non-delivery, a missing retained file) > 1 FORGED (an affirmative contradiction in obtained live evidence) > 2 NOT-EARLIEST (a strictly earlier qualifying event) > 3 EXPIRED (a loss) > 4 DERIVED-TERMINAL (refusals proven from obtained remote or retained evidence: history not-an-extension / diverged / batch / not-genesis-only, REDERIVE-<non-retry>, seed mismatch) > 5 RETRY-UNAVAILABLE (feed or remote down, undetermined delivery with nothing contradicting, seed re-derivation RETRY) > 6 RETRY-INCOMPLETE (absent, nothing contradicts) > 7 ACCEPT. ONE PLACE ENFORCES IT: `provenance_designs_v10.resolve` — driver v14's `composed_resolver` runs four CONTRIBUTING stages (A local sweep of both retained events, B one retrieval with the approval authentication and the open event's same-id check, C seed re-derivation with PRODUCTION's own re-deriver, D history continuation) and none decides; class-0 findings raised in the fixed offline check sequence before the resolver are consistent with the order because nothing outranks class 0; within a class the fixed stage sequence decides, never retrieval order; every finding is recorded in the identity's `_findings`. EXHIBITED PAIRWISE: `V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class) and the complete-path pairs of tracks 8–10. FAIL-FIRST (`TRACK10_FAIL_FIRST_RECEIPT_20260907.md`): run 1 against byte-copies of the V29 modules — codex's V29-1 row reproduces its exact `REDERIVE-RETRY`, the equal-time case flips, the four literal placeholders are found, the resolver does not exist; run 2: the complete-path case with codex's seven V29 rows (unverifiable seed bodies + undetermined approval + wrong repository → EVENT-INCONSISTENT; + genuine same-id contradiction → EVENT-FORGED; genuine approval → REDERIVE-RETRY, the legitimate retry; open event wrong repository with ls-remote down / with the approval feed at HTTP 503 → OPEN-EVENT-INCONSISTENT-INPUT; open event same-id contradiction with ls-remote down → OPEN-EVENT-FORGED; genuine open event with ls-remote down → RETRY-REMOTE-UNAVAILABLE), the strict-earlier case in both feed orders, and the resolver's own pairwise test all OK; run 2b: EVERY existing precedence test of tracks 8 and 9 re-run against the v10/v14 successors, OK, and the 23-test driver fixture OK. (V29-1) and (V29-2) are repaired by the resolver, not separately; codex's clause carried verbatim: "Before any retry is returned from load_identity, the implementation checks locally decidable type, repository and protected-ref mismatches in both retained approval and history-open events. This obligation applies before seed re-derivation availability returns, approval-feed availability returns, and history remote-head or fetch availability returns. Delivery checks that require unavailable remote objects remain undetermined; they do not excuse other locally decidable mismatches. In composed mode, deferred approval or open-event delivery must not bypass obtainable same-ID authentication merely because another stage has queued a retry. A retry is emitted only after the applicable higher-priority terminal checks have run. Offline undetermined delivery retains its explicitly named refusal." (V29-4) `authenticate_event`: NOT-EARLIEST only on a STRICTLY earlier qualifying event; codex's clause verbatim: "NOT-EARLIEST requires a strictly earlier qualifying event under the explicitly specified ordering. Equal timestamps alone do not prove that another event is earlier; feed iteration order is not an unstated tie-break." (V29-3) the four literal backreference tokens that stood where digests belong (the V28 and V29 edit scripts had used regex backreference syntax inside a plain string replacement; present since V28, caught by codex at V29) are replaced by the digests; the aggregate labels name the executed modules (driver v14, builder v30, verdict v30); the V30 edit script builds those clauses with a function, not a backreference, and the V30-specific text test asserts that no such token remains. V29 of 2026-09-07 was V28 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V28_REVIEW_OUTCOME_20260907.md`), with codex's V28-1/2/3 answered FAIL-FIRST (`TRACK9_FAIL_FIRST_RECEIPT_20260907.md`: 3 tests failing against byte-copies of the V28 modules — the complete-path case reproducing codex's exact retry, the rule-(iii) guard, the text case; 3 passing after; the complete-path case carries seven sub-cases: approval delivery undetermined + genuine same-id contradiction in the feed → EVENT-FORGED; + wrong repository, with a healthy feed and with HTTP 503 → EVENT-INCONSISTENT; undetermined with nothing contradicting → RETRY-EVENTS-UNAVAILABLE, unchanged; the same three on the history-open stage → OPEN-EVENT-FORGED / OPEN-EVENT-INCONSISTENT-INPUT / EVIDENCE-UNAVAILABLE). V28-1 and V28-2 were UNREPAIRED CONTRADICTIONS the lane's own V27-2 repair had introduced — a tri-state precheck that returned a retry before the precedence rules could run — filed in column two of the register at the V28 outcome and repaired here; codex's clause text is carried verbatim into the code header, this paragraph and §3c: (V28-1) "On both the approval and history-open paths, locally decidable retained-event mismatches are terminal before any evidence-unavailability return. In composed mode, unresolved delivery does not bypass live authentication: a same-ID canonical contradiction obtained from the live feed is terminal before an undetermined-delivery retry. Only when no higher-priority inconsistency is established may undetermined delivery or unavailable retrieval return the stage's retry disposition. Offline mode retains its explicitly named undetermined-delivery refusal." — implemented by driver v13's witness-commit precheck DEFERRING an undetermined delivery to composed provenance (local_precheck → retrieval → same-id contradictions → only then the retry) while positive non-delivery stays terminal and offline keeps IDENTITY-WITNESS-COMMIT-UNDETERMINED; (V28-2) `validate_continuation_v9` consults the feed the same way before returning EVIDENCE-UNAVAILABLE; (V28-3) "Same-commit contradiction is scoped to the pinned protected ref and evaluated only when the retained event is absent. If it is present verbatim, distinct qualifying events are resolved by earliest-event ordering; a later event does not invalidate an authentic earliest event. Conflicting same-ID evidence is refused even beside a verbatim copy, without asserting that this alone identifies which producer supplied false evidence." — rule (iii) restated (the V28 phrasing 'another delivering push is an ordering question — NOT-EARLIEST' was too short: a later distinct qualifying push beside a verbatim earliest genuine event is AUTHENTIC; NOT-EARLIEST needs an EARLIER qualifying event), the two false filename↔digest associations corrected (the V28 edit's token replacement had renamed the historical builder-fixture clause; the V27 edit's line-33 replacement had put the historical verifier's digest beside the pinned verifier's name), the driver fixture header names itself, the inspection's row-1 note says INCONSISTENT-INPUT for the wrong-repository forgery, the questions file's contract sentence names both arms of FORGED with the absent-only qualification, the design document's outcomes row is restated for v9 (revision 9), and L-INH / L-AVAIL carry their mode qualifications. V28 of 2026-09-07 was V27 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V27_REVIEW_OUTCOME_20260907.md`), with codex's V27-1/2/3 answered FAIL-FIRST (`TRACK8_FAIL_FIRST_RECEIPT_20260907.md`: 7 tests failing against byte-copies of the V27 modules, classified per test — 3 behavioural, 2 missing-interface, 1 complete-path, 1 text; run 1b executing the V27 functions: a same-id contradiction with an undeterminable head called UNAVAILABLE, a differently encoded same-id copy beside a verbatim one called AUTHENTIC, a wrong-repository event under HTTP 503 called UNAVAILABLE; run 1c executing the V27 driver: an undeterminable witness delivery called IDENTITY-WITNESS-COMMIT; 7 passing after). BLANC'S ORDER OF 02:06 KST (`BLANC_ORDER_V27_CONTRADICTIONS_NOT_LIMITS.md`): V27-1 and V27-2 were CONTRADICTIONS between the stated contract and the implemented path — REPAIRED, not disclosed; the categories are kept apart in §3c and in `LIMITS_VS_CONTRADICTIONS_20260907.md`. (V27-1) PRECEDENCE, stated as three rules and tested: locally decidable input mismatches (not a PushEvent, wrong repository, positive non-delivery) are decided BEFORE any retrieval (`local_precheck`); a same-id contradiction is decided BEFORE the undetermined-delivery return and BEFORE the verbatim-presence shortcut; the same-commit arm is scoped to the pinned protected ref and evaluated only when the retained event is absent (when it is present, another delivering push is an ordering question — NOT-EARLIEST); no other precedence exception exists. (V27-2) TRI-STATE THROUGH EVERY STAGE: the driver's witness-commit precheck and the validator's open-event stage (`validate_continuation_v8`) use `delivery` — UNDETERMINED is RETRY-EVENTS-UNAVAILABLE / EVIDENCE-UNAVAILABLE (offline mode, which has no retry vocabulary, names it IDENTITY-WITNESS-COMMIT-UNDETERMINED), positive non-delivery is EVENT-INCONSISTENT / OPEN-EVENT-INCONSISTENT-INPUT, and a git process-launch exception is UNDETERMINED. (V27-3) driver v12's lineage table re-verified row by row against each retained driver's own import line (v10 composed on provenance_designs_v6, not v7), the helper docstrings corrected where they described current code (historical paragraphs kept and marked), the inspection header names the modules it executes, the design document's row 9b says INCONSISTENT-INPUT, §9 no longer calls the V22 inspection an inspection of this candidate, and the suite count is taken from the executed logs (20 suites). V27 of 2026-09-07 was V26 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V26_REVIEW_OUTCOME_20260907.md`), with codex's V26-1/2/3 answered FAIL-FIRST (`TRACK7_FAIL_FIRST_RECEIPT_20260907.md`: 4 tests failing against byte-copies of the V26 modules, classified per test; run 1b executing the V26 functions — a same-event contradiction called INCOMPLETE, missing git objects called FORGED; 4 passing after): (V26-1) the CONTRADICTION PREDICATE is stated — a live event with the same event id, or delivering the same commit, whose canonical bytes differ (a difference only in `before` or `head` counts) is FORGED, terminal; (V26-2) DELIVERY IS TRI-STATE — DELIVERED / NOT-DELIVERED / UNDETERMINED; UNDETERMINED (a git object missing locally, a failed git command) is UNAVAILABLE, a retry, never forgery; a retained event that positively does not deliver the commit or names the wrong repository is INCONSISTENT-INPUT; a verbatim-present but not-earliest event is NOT-EARLIEST; EVIDENCE-EXPIRED is a loss, not a retry, at every stage; (V26-3) the remnants swept, and driver v11 states its lineage in a table instead of the over-renamed historical lines of v10; the text tests are attributed (`TEXT_TEST_ATTRIBUTION_20260907.md`): the V26-specific test is retained as pinned and a V27-specific test is added. V26 of 2026-09-07 was V25 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V25_REVIEW_OUTCOME_20260907.md`), with codex's P1–P4 answered FAIL-FIRST (`TRACK6_FAIL_FIRST_RECEIPT_20260907.md`: 5 tests failing against byte-copies of the V25 modules, classified per test; run 1b executing the V25 functions; 5 passing after): (P1) batch classification uses the SAME delivery evidence as authentication — `delivers` (head, payload.commits, or before..head ancestry; GitHub PushEvents may omit the commits list) — so a proven batch is terminal HISTORY-PUBLICATION-BATCH even without a commits list; (P2) ABSENCE IS NEVER FORGERY — a retained approval or history-open event missing from a feed is EVIDENCE-INCOMPLETE (retry), FORGED only on an affirmative contradiction (a push delivering the same commit with different bytes), and all three retrieval stages use one EVIDENCE-* vocabulary (driver v10 prefixes UNAVAILABLE/INCOMPLETE with RETRY-; EXPIRED is a loss, not a retry); (P3) labels swept; (P4) THE COVENANT is carried into the questions file. V25 of 2026-09-07 was V24 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V24_REVIEW_OUTCOME_20260907.md`), with codex's N1/N2/N3/N5 answered FAIL-FIRST (`TRACK5_FAIL_FIRST_RECEIPT_20260907.md`: 6 tests failing against byte-copies of the V24 modules — 3 missing-interface, 3 behavioural — run 1b executing the V24 functions; 6 passing after) and N4 by `TRACK_RECEIPTS_CLASSIFICATION_CORRECTION_20260907.md`: (N1) THE COVENANT replaces the residual (§3c) — the validator authenticates the published append-only history and distinct ordered publications, NOT when the operations occurred nor the completeness of unpublished observations or decisions before the genesis was published or between later publications; (N2) `validate_continuation_v5` dispositions — EVIDENCE-UNAVAILABLE and EVIDENCE-INCOMPLETE are RETRY, HISTORY-PUBLICATION-BATCH is terminal only when the feed proves a multi-commit delivery — and the questions file states the FULL live contract (approval event, history-open event and one push event per history commit); (N3) the producer refuses PUBLISH-UNRELATED-COMMITS and §7 names the v25 collector/builder; (N5) labels swept. V24 of 2026-09-06 was V23 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V23_REVIEW_OUTCOME_20260906.md`), with codex's M1/M2/M3/M6 repaired FAIL-FIRST (`TRACK4_FAIL_FIRST_RECEIPT_20260906.md`: 7 tests failing against byte-copies of the V23 modules, 7 passing after; run 1b executes codex's late-push case on the V23 validator): (M1) PER-ENTRY PUBLICATION IS PROVEN BY THE SERVER — `validate_continuation_v4` requires one PushEvent per history commit in the live feed delivering exactly it (head = commit, before = its parent), server-timed in order; several single-entry commits delivered by one late push are HISTORY-PUBLICATION-BATCH; (M2) `publish_entry` pushes an already-committed pending entry instead of re-committing; collector v24 and builder v24 RECONCILE a pending entry before any operation (`reconcile_pending`) and the collector publishes its outer refusal/error entries; (M3) the residual is restated exactly (§3c); (M5) the four V22-pinned files edited in place are recorded in `PIN_DRIFT_RECORD_V22_M5_20260906.md` and archived under `_optionA_dev/_archive_v22_pins/`; the PIN IMMUTABILITY RULE (`PIN_IMMUTABILITY_RULE_20260906.md`) governs every later change; (M4) the questions file presents Option A′ as not built for the history-open event and Option C as the only working option. V23 of 2026-09-06 was V22 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE, no fatal — `V22_REVIEW_OUTCOME_20260906.md`), with codex's findings A–F repaired FAIL-FIRST (`TRACK3_FAIL_FIRST_RECEIPT_20260906.md`: 10 tests failing against byte-copies of the V22 modules, 10 passing after): (A) the history-open anchor is AUTHENTICATED — the first remote commit touching the history must carry a GENESIS-ONLY blob and be delivered by a retained PushEvent authenticated in the live feed, so a rebuilt history published as its own first commit is refused; (B) the PRODUCER BOUNDARY is BUILT — collector and builder publish each history entry and require the remote's acknowledgment before proceeding, and the validator requires one entry per acknowledged commit; (C) ONE delivery predicate (W3's, before..head included) in every mode; (D) a control failure appends a named history entry; (E) the render-end must be the last relevant journal record; (F) the questions file says 30 days / 300 events and marks Option B NOT IMPLEMENTED; an EMPTY live feed is UNAVAILABLE, never FORGED. The complete object is `V23_CANDIDATE_ATTACK_INSPECTION_20260906.md` (rows 9c–9k, 10a–10d). V22 was a STAGED CANDIDATE prepared under Blanc's two-track order of 20:45 KST (cap lifted) and codex's coordination note of 20:43: TRACK 1 — the mechanical repairs of codex's V21 findings, each with a test that FAILED against byte-copies of the V21 modules before the repair and passes after (`TRACK1_FAIL_FIRST_RECEIPT_20260906.md`); TRACK 2 — the two provenance boundaries codex left open (a coherently forged GitHub event, a history rebuilt before the first freeze) are NOT closed by this candidate offline; their RECOMMENDED designs are staged as executable exhibits, UNADOPTED, in `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md` and `_optionA_dev/track2/`; the candidate DISCLOSES both as open (§3c) until Duho adopts a design. The complete object — the successful production path AND codex's coherent attacks re-run against these pinned bytes — is `V22_CANDIDATE_ATTACK_INSPECTION_20260906.md`. V21 was REFUSED (agy SIGNABLE; codex NOT-SIGNABLE: no recurrence of the V20 fatal, nine MAJORs — `V21_GATE_OUTCOME_AND_DIAGNOSIS_20260906.md`). V21 of 2026-09-06 (Blanc's bounded round, order of 19:48 KST + amendment 19:50) was V20 with the PRODUCTION PATH MADE EXECUTABLE and the conjunction computed from committed evidence. V20 was REFUSED by codex (FATAL: the pinned driver's production protocol carried `rederive_seed = None`, so the real entry point refused every identity with `PROTOCOL-NO-REDERIVER` and only fixtures injecting a callback ever loaded one; agy passed V20). Per the order the fatal was REPRODUCED in executed output BEFORE any repair (`V20_FATAL_REPRODUCTION_20260906.md`: `PRODUCTION.rederive_seed = None`; the real `load_identity` refuses a production-shaped sealed identity; the same identity loads only with an injected callback), then repaired: driver v5 binds a real module-level re-deriver into PRODUCTION and the fixtures use PRODUCTION's own function (§E3, §3c); every other V20 finding is tabled in `V20_REPORT_REPAIR_CHECKLIST_20260906.md` (new / inherited; in / out of scope; (a) a change needing Duho vs (b) preparation; disposition). Inherited defects of the SIGNED V15 are not repaired here; where a fix could be prepared it is carried in this candidate LABELLED "INHERITED — PREPARED, NOT ADOPTED" with what it would change, and adopting it is Duho's call. V20 replaced the two-source design of V15–V19 with ONE public randomness source, by Duho's decision: via codex voice at about 19:02 KST, after Codex presented the drand-only direction and its availability trade-off (wait, never switch source), Duho answered "해" ("Do it") — recorded by Codex in `.hermes/CODEX_DUHO_DRAND_ONLY_DECISION_20260906.md`; a source-direction decision, not a signature, not a review verdict, not a draw. History: V16–V21 were refused at gates and V22–V29 at complete-package reviews (each by codex; agy passed all fourteen); V19 fell on the property it claimed — with two sources, the failing side (NIST) leaves no authenticator, so the FIRST collection could still select the source; the author's 12:52 answer overstated this and the OPS coordinator released on it (recorded in the change record). V20 removes the choice: the seed is the drand mainnet default chain's threshold BLS signature for a prospectively fixed round, verified cryptographically under the pinned public key, bound to the pinned chain hash — a function of (round, key) that no relay, operator or collection time can change; unavailability is RETRY, never a different value. The signed V15 (fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1, T_sign 2026-09-06T00:04:07Z) stays on file unchanged with its RETRY record 1c1d9d4bacc35319e91d46420b8f892e1126cc48059ec69156a0a718428aeb4b; its own defects are on record (\`V19_REPORT_AUDIT_TRIAGE_20260906.md\` §C) and are not repaired here. Preserved byte-for-byte from V15: sample sizes 400/200/2,000, floors 380/190/1,900, the 0.70 bar, the exclusions, ONE holdout, the label/pixel boundaries, custody, branch protection, the §6 pipeline identity (executing under the approved Tier-C V39). The V19 audit's two MAJOR findings are repaired here because every option needed them: the driver verifies the acceptance conjunction semantically (§3c) and the collection history is complete on every path (§3c). V15 REMAINS OPERATIVE on paper until a candidate is approved on its final bytes; a clear pair is READY-FOR-APPROVAL, not permission; nothing is drawn under a draft.
21c21
< - **E3** Pinned identity, TWO freezes, and an EXECUTABLE SEARCH IDENTITY frozen by this rule. (i) FAMILY: `_optionA_dev/fourier_chirality/fourier_chirality.py`, SHA-256 `a026fe5fa168ca1eb2056829d86fb549dec4a61596b5d122f60c2a389e2d98a2` (unchanged from V5) — configuration-driven, all state rebuilt per configuration, grid `CONFIG_GRID` and fixed `FIXED` in the file, refuses out-of-grid values and any key set other than the grid's. DRIVER: `run_configurations_v13.py`, SHA-256 `d59198d2342fb2d35d7ce2035c1a84d038645b16ba7b09d1dbf986a77f7b6ca3` (V29 STAGED: `run_configurations_v12.py` — reviewed at V28, retained — plus composed mode on `provenance_designs_v9` (the witness-commit precheck defers an UNDETERMINED delivery to live authentication in composed mode; the open-event stage consults the feed before any retry; positive non-delivery terminal; offline keeps its named undetermined refusal); V28 STAGED was: `run_configurations_v11.py` — reviewed at V27, retained — plus composed mode on `provenance_designs_v8` (locally decidable mismatches before any retrieval; same-id contradictions before any retry; tri-state delivery through the witness-commit precheck — RETRY-EVENTS-UNAVAILABLE / EVENT-INCONSISTENT in composed mode, IDENTITY-WITNESS-COMMIT-UNDETERMINED / IDENTITY-WITNESS-COMMIT offline — and through the open-event stage); V27 STAGED was: `run_configurations_v10.py` — reviewed at V26, retained — plus composed mode on `provenance_designs_v7` (EVENT-FORGED on a same-event contradiction; RETRY-EVENTS-UNAVAILABLE on an undeterminable delivery; EVENT-INCONSISTENT / EVENT-NOT-EARLIEST named); V26 was: `run_configurations_v9.py` — reviewed at V25, retained — plus composed mode on `provenance_designs_v6` (RETRY- for UNAVAILABLE/INCOMPLETE at every stage; absence of the approval event is RETRY-EVENTS-INCOMPLETE); V25 was: `run_configurations_v8.py` — reviewed at V24, retained — plus composed mode on `provenance_designs_v5` (EVIDENCE-* dispositions are RETRY); V24 was: `run_configurations_v7.py` — reviewed at V23, retained — plus composed mode on `provenance_designs_v4` (one server-timed push event per history commit); V23 was: `run_configurations_v6.py` — reviewed at V22, retained — plus (codex V22 A/B/C/E) composed mode on `provenance_designs_v3` with a retained history-open PushEvent (`history_open_event_file`), one delivery predicate, render-end last; V22 was: `run_configurations_v5.py` `d338e33dfbf84ed7127c6b32e529da6dcfd0cbf9704aba468cdc2aa2a426722c` — refused at the V21 gate, retained — plus (codex V21 C2) W4 reproduced in the driver (`Protocol.approval_glob`, exactly one approval-record path in all history), (C3) a `witness-closed` history entry is terminal (`COLLECTION-CLOSED`), (C9) sentinel reconciliation requires a non-empty refusal cause on every journalled refusal and a render-end with the matching count, (C11, UNADOPTED, default off) `Protocol.verify_split` — recompute the §3b split from the pinned catalogue files and require the identity's lists to equal it; and (Blanc 21:14, UNADOPTED, default `offline`) `Protocol.provenance_mode = 'composed'` — the track-2 v9 helpers called INSIDE `load_identity` on the production call path (live event authentication through the `gh` runner with W3's delivery predicate; history continuation against the live remote head from a RETAINED history-open PushEvent that must be authentic and must deliver a genesis-only first commit; one entry per acknowledged commit; the EXPIRED path closed unless a receipt path is configured), so that adopting the design is a flag and its inputs, not new wiring — exhibited in `test_composed_mode_on_the_production_call_path` and attack rows 10a–10c (the THREE STATES: offline candidate accepts attacks 1–2; standalone helpers refuse them; composed mode refuses them on the same call); fixture `test_run_configurations_v13.py` `df4e04ea9546f780087afd3ae556a5dd6427518adb2d9b44684a991440a70f6e`, 23 tests (its composed test's expired fixture is an unrelated later push: under v7's contradiction predicate a later push delivering the same commit is FORGED, not EXPIRED) (composed test: one fixture push event per published commit, before = parent) (composed test: per-entry publication, retained open event, codex's V21 attack 2 and V22 attack A), ONE approval commit per repository (the v5 fixture's per-call records had masked W4), the end-to-end test exhibiting `verify_split` refusing a coherently sealed swapped list that the default driver accepts. V21 was: `run_configurations_v4.py` `d55ea70ddb27ba3b849204addeec3da0551657b7a0bb3d53a422d61f9a7d7993` — superseded and retained — plus the PRODUCTION WIRING: `PRODUCTION = Protocol(rederive_seed=production_rederive_seed)`, a module-level function that recomputes the drand-only verdict OFFLINE from the retained record (`beacon_record_drand_v21.verdict`, fetch=None, bound to the adopted digest and the committed approval record bytes; the verifier stack is imported lazily and its absence is `VERIFIER-UNAVAILABLE`, never a pass); the FULL CONJUNCTION of §3c computed from COMMITTED evidence; ADAPTER RECONCILIATION (§7); an unreachable `IDENTITY-MISSING` branch of v2–v4 made reachable; and `holdout_once` [INHERITED — PREPARED, NOT ADOPTED, §7]. Fixture `test_run_configurations_v5.py` `3d63af01bff40f663caa2ac5a36ebf5792876c35eefd2ac95e81ca9e9a688794`, 22 tests, whose test protocol is built FROM `PRODUCTION.__dict__` with paths, sizes and pinned digests overridden ONLY — the re-deriver is PRODUCTION's function, asserted by identity (`assertIs`), no callback substitution — and whose identity fixture carries REAL evidence: a committed approval record and adoption file at one approval commit, a real BEACON-RECORD-4 for the public round 6441924 (BLS-verified; the exhibit-round exclusion lifted inside the fixture only), a real BLS-verified nonce (round 6441904 = round_for(T_sign) for that exhibit T_sign, its bodies pinned from two relays: `drand_only/round_6441904_api.drand.sh.json` and `…api2…`, both `b787e446384c59f610d86b7b2c005f6ee04713ca1c59cb5f2b52a99a4623571f`), an authenticated history, a PushEvent with canonical digest and provenance; twenty-five fabricated identities are refused each by the check that names its defect, the real entry point `main([...])` is exercised under PRODUCTION, and the end-to-end test drives the collector CLI (genesis) → V21 builder → driver v5. V20 was: V15's `run_configurations.py` `d486ae58f40d220e8d2a9a1d875e008059cdc3bb1d9aee0dffc7dc974c7d53aa` plus the adoption binding, the render-refused sentinel accounting and the SEMANTIC acceptance conjunction of §3c — fixture `test_run_configurations_v4.py` `512384b0112aec56a4113db9d42dd0f9acc3c0df0d95eb1ddc3c53ab9502e196`, 20 tests — REFUSED at the V20 gate for injecting the re-deriver; the V19 driver `2028ee3a73367f9783fb243b26b557c0ed1eea97832590deff27149bbc37e8c0` and its fixture `642805b2b47a8d45017db3077cac5ec9c303425fb9c3490a5b8c8ca45f8902d1` are superseded and retained) — the ONLY path from tensors to a selected candidate; two closed modes, both requiring `--identity`: **`tune`** verifies the environment (below) before any read; loads the CORPUS IDENTITY file, validates its schema (CORPUS-IDENTITY-2; pool digest EQUAL to the pinned guarded pool; exclusion-file digest EQUAL to the pinned exclusion file; split mode `beacon-seeded`; a 64-hex beacon-record digest present; `rule_sha256` EQUAL to the signed rule's digest pinned in the driver's production protocol at adoption; ordering rule, seed, renderability inputs; integer objids; exactly 400/200/2,000; three-way disjoint — a TEST-SEED identity is refused) and refuses unless its SHA-256 is recorded in the lane's chained seal journal under operation `corpus-identity-freeze` AND that journal record is WITNESSED: a file `<journal>.witness.corpus-identity-freeze.json` names a 40-hex git commit; the driver requires (`verify_witness`) that the commit exists, that `git cat-file` of the journal at that commit contains the record line, that the IDENTITY FILE'S BYTES are the blob at that commit (and, for `tuning-freeze`, that the receipts, per-object journal and summary are blobs at that commit — agy V12: the pushed commit must carry the artefacts themselves, not only a digest line), that `git remote get-url origin` equals the pinned `https://github.com/DuhoKim/NebulaMind.git` (agy V8: a clone whose `origin` was redirected to a local bare repository is refused), and — after `git fetch` — that the commit is an ancestor of `origin/feat/paper-workflow-v2` (a remote whose branch is protected server-side against force pushes and deletion — Duho's GitHub setting, evidenced as §7 requires; no local hook is relied on); any failure is DATA-INTEGRITY-FAIL; refuses any manifest whose ORDERED objids differ from the identity's `tuning_objids` (as well as size ≠ 400, duplicates, missing tensor, tensor hash mismatch, label ∉ {±1}); runs all 96 configurations in lexicographic order; UNSCORED = tie/non-finite/missing/exception/non-repeat; ELIGIBLE iff m ≥ 380 (literal, no flag); OBJECTIVE = max(k, m−k)/400; tie-break fewer unscored, then earlier index; writes per-object journal, per-configuration receipts (each carrying the lock hash, observed environment incl. thread variables, manifest digest, tensor-set digest and identity digest) and a summary. **`holdout`** verifies the environment; loads the same sealed identity; verifies the tuning summary's journal and receipts digests; recomputes the tuning RUN ROOT (SHA-256 over the digests of the per-object journal, the receipts, the manifest, the tensor set, the corpus identity, the estimator, the driver and the environment lock) and refuses unless it equals the summary's and is sealed under operation `tuning-freeze` AND witnessed (pushed commit verified by `verify_witness`); RECONCILES every receipt against the per-object journal (recomputing k, m and unscored per configuration; refuses `JOURNAL-MISMATCH`); refuses if the estimator file changed since tuning; RECONSTRUCTS the winner from the 96 receipts — exactly 96 in order, each configuration and id equal to the live enumeration, counts consistent, eligibility/objective/tie-break re-derived — and refuses (`WINNER-SUBSTITUTED`) if any summary winner field differs; refuses any manifest whose ordered objids differ from `holdout_objids`; refuses overlap with tuning; evaluates that one configuration once; `CLOSED: holdout floor` if m < 190, else `PASS` iff Wilson lower bound on max(k, m−k)/200 > 0.70, else `CLOSED: holdout strength`. Any deviation is DATA-INTEGRITY-FAIL, exit 2, no selection, no verdict. ENVIRONMENT: thread variables set to 1 before NumPy import; `env_lock.json` SHA-256 `4e2c851fe1f22f4bc28f121cf3ff4326f036be57fc100b34af9e5501d6c74251` (literal in the driver) must hash-match and the live interpreter, Python 3.9.6, NumPy 1.26.4, platform, machine arm64, NumPy core digest, pocketfft digest and thread variables must all equal the lock; stop on inequality. CORPUS IDENTITY SCRIPTS: `_optionA_dev/corpus_identity/build_guarded_pool.py`, SHA-256 `e9b00ef4f50ce93c980cf6c5e0581276c46e6fa9a56ed46daa6d4eb95d2f52f3`, re-derives the §9B.2–9B.4 guarded pool from the GZ1 table, the completeness receipt's terminal dispositions and the protected-object pins, with controls 12,100 / 46 / 12,054 asserted; `build_corpus_identity_v29.py`, SHA-256 `3e6bdc911eb078a0e7892149663f062db425216784bc2a7ba53d39f808d49808` (V29 STAGED: on provenance_designs_v9; fixture `test_build_corpus_identity_v29.py` `0d2992b4c1b0f5782baa807f5254086f53e9751608d5fdb3510ae071b1015a59`, 8 tests; V28 was: on provenance_designs_v8; fixture `test_build_corpus_identity_v28.py` `fd581106fae0475112337d3b7677c95d805f73a4bd9db7bb9f87268baf4f5691`, 8 tests; V27 was: on provenance_designs_v7; fixture `test_build_corpus_identity_v27.py` `4fbf91fe4de84d2e221b25fdd446aee35693b48a29fb4980d44935f6238a3a12`, 8 tests; V26 was: on provenance_designs_v6; fixture `test_build_corpus_identity_v26.py` `87e514fd6097835cf5091c878acccd721d3cf87b17eaf2bcf1c0caa2adf28267`, 8 tests; V25 was: on provenance_designs_v5; fixture `test_build_corpus_identity_v25.py` `da08d85fd95deac71d5fa845313ba658355a875035598d10e56f77299c44ce70`, 8 tests; V24 was: `build_corpus_identity_v23.py` retained; plus (codex V23 M2) `reconcile_pending` before any operation; fixture `test_build_corpus_identity_v24.py` `47708417a627573e2f1cd6d781286c3e1bbf5e84b5de18603d199b8f6c558d58`, 8 tests; V23 was: `build_corpus_identity_v22.py` retained; plus (codex V22 D) `builder-control-refusal` appended before a control failure returns, and (B) per-entry publication when `--publish-remote/--publish-ref` are given; V22 was: `build_corpus_identity_v21.py` `048b42211a23f47ee4d97a9f6b7658b169736dbc2c4aa2f922f14dbb9d6a65d0` retained, plus (codex V21 C4) the argument refusal disclosed in the sidecar and every failure after validation — catalogue, control, classification, output — logged as `builder-error` before refusing (the boundary wraps `build()`), approval glob V22; fixture `test_build_corpus_identity_v22.py` `eb9d0a858e8eec26ab37f654a3e662e691895d2c8cb08341c66c0e91b3d07993`, 8 tests; history `history_v2.py` `374d468a8a57f9f3fcb558c874688be8180371650f600f9902cbe1c95fcdbbb3` (stages `builder-control-refusal`, `builder-args-refusal` added; fixture `test_history_v2.py` `1585c48a961056975cef36b383384c9e3dbcb753722fa2b9af7c271a72b365bb`) (exclusive genesis creation; `witness-closed` terminal in `first_accept`; fixture `test_history_v2.py` `1585c48a961056975cef36b383384c9e3dbcb753722fa2b9af7c271a72b365bb`, 4 tests). V21 was: `build_corpus_identity_v20.py` `f3aa4955c150798fb255d6d9d89217e0872981b09fca6d8e845d2106358d70e0` — superseded and retained — plus the AUTHENTICATED HISTORY module `corpus_identity/history.py` `8ac7a81cc9ee5c250c2eea0ad05400ec72b6640e3d09b51c43ad67b81ffccde1` (fixture `test_history.py` `9d658c09f8ca0de9edd66fdeea99adbc0eaedf8a1204817369928a54eee37d24`, 4 tests), `approval_witness_v4`, `beacon_record_drand_v21`, and every exception path logged; V20 was: V15's `build_corpus_identity.py` `3090af770f0329beddb2d8b64acae6d4a1b1da06025e616b6fb145bff47ca46c` plus the drand-only verdict import, the approval-witness-v3 call, the complete collection history and the adoption-at-approval-commit binding; the V19 builder `27c88669bf413185d4d24da16d5e174f88c1c404e4b0bc80e40cf24dc2f6a814` is superseded and retained) (its beacon boundary is one call to `beacon_record.verdict` with live re-fetch; refuses `BEACON-NOT-ACCEPTED (<token>)`) (HISTORY: the earlier builder field-validation code was AUTHORED BY THE CODEX SEAT 2026-09-06 00:13–00:18 from Hwao's specification and no longer participates — every V16–V18 module is the author's; its refusal vocabulary is enumerated in `_optionA_dev/beacon/CODEX_BEACON_BUILD_RESULT_20260906.md`) (fixture `test_build_corpus_identity_v20.py` `3eb80459d9ba9e8226a2367e1d2a7426e12895bc9f9cad8a28b48de9e0172c07` (6 tests, listed in §3c; V19's fixture `fba963557b8fb961b15b0eebf627460fd8b1cf70d1a248bd70c76a1fe81d046b` retained); the V16/V17/V18 fixtures are retained as history (V16's 'both refuse' test exercised only its builder; V17's witness proved no upper bound; V18's lock logged only accepts — each as codex showed) beside V15's `test_build_corpus_identity.py` `2cd65283a1e227cd0511f5c7fc1135ec0b365cb76a9608ed94e988ed3148440c` (4 tests; every beacon record in it is PRODUCED BY `beacon_record.collect` over the three-tier test network, never hand-built), 4 tests (`test_build_corpus_identity.py`: control; seeded split; ordering/renderability; exclusion — V20's "3 tests" was a stale fragment, codex V20 5-frag) plus `test_build_guarded_pool.py` 2 tests, incl. the seeded split's determinism, case-insensitivity of the seed, exclusion of the failed set and of the dry-run identities, and that a different seed gives a different split; builds into a temporary directory and leaves nothing in the lane), orders the pool by §9B.5's hash rule, asserts that ranks 1–2,000 reproduce the failed set AS A SET with equal labels (the selection CSV is GZ1_OBJID-ordered), marks RENDERABILITY by exactly this rule and no other — the brick exists in the pinned DR9-north survey-bricks table (`2edd5c295fdad26852c6f224a3ff023cff43dd0e03a53acd35b767e726ee72fb`, half-open test) AND is not in the pinned no-r registry `validation_bricks/_bricks_without_r_coverage.txt` (`ba2eb9d16d0d1d47eef2e0d52497b56d44ac979ebe67dd54b33f57c117d7a2fe`); the sealed checksum catalogue is NOT consulted, and the identity records both input digests — and emits, in the schema the driver consumes (`schema_version` CORPUS-IDENTITY-2: top-level ordered `tuning_objids` 400, `holdout_objids` 200, `fresh_validation_objids` 2,000, `pool_sha256` = `2cc94a29562270fcb5043f4ce942e303696f359b5fb0c59fdee48578ebb34155`, per-set detail with bricks and digests, skipped ranks); their receipts state every control number obtained (pool 12,100 / guard dropped 46 / guarded pool 12,054 (guarded_pool.csv SHA-256 2cc94a29562270fcb5043f4ce942e303696f359b5fb0c59fdee48578ebb34155); the 2,000 smallest h mod 2^32 equal the failed set as a SET with equal labels (the selection CSV is GZ1_OBJID-ordered, so the control is set equality — Hwao's first brief wrongly asked for row order and the codex seat correctly stopped rather than tune); dry run of the identity script: tuning 400, holdout 200, fresh validation 2,000 renderable objects at ranks 2,001–4644, 44 non-renderable ranks skipped, 7410 pool objects unused — the dry-run outputs are NOT the sealed identity and were moved to _scratch; the sealed identity is produced once, after signature, and journalled). FIXTURES: `test_fourier_chirality.py` SHA-256 `c30b3af46b3a855c499469c4249e487a765c7015b46126291cc7a4c134072811` (10 tests; antisymmetry for all 96 asserts a tie in BOTH orders or exact negation) and `test_run_configurations.py` SHA-256 `2fa83512b36788345541e84ff013c4f5ae3a9a9a5f0df0cbacc4b519945707f1` (17 tests; the end-to-end test obtains its beacon record from the pinned fetcher under a mocked network, builds the seeded identity with the real pinned pool and exclusion digests, and loads it through the driver with the rule digest checked: REDIRECTED REMOTE refused `WITNESS-REMOTE-URL`; TEST-SEED identity refused in production mode; the end-to-end test builds a SEEDED identity from a test beacon record with the real pinned pool and exclusion digests; FORGED RECEIPTS — a coherently rewritten receipt set with a matching new seal is refused `JOURNAL-MISMATCH`; PRODUCTION-SHAPED identity in every test; WITNESS — a freeze committed but NOT pushed is refused `WITNESS-NOT-PUSHED`, pushed is accepted, a wrong commit and a missing witness file are refused, exercised against a real bare git remote created by the fixture; END-TO-END — the real builder's output loads through `load_identity` at 400/200/2,000 and is refused until sealed; environment mismatch refusal; manifest size/duplicate/missing/hash/order refusal; no floor flag and `--identity` required; unscored = miss and the floor both sides; tie-break levels; identity unsealed and identity-of-other-objects refused; holdout reconstructs the winner, refuses an unsealed tuning run and a COHERENT substitution; floor-CLOSED before Wilson, overlap and identity refusal; `CLOSED: holdout strength` reached deterministically; PASS reached; production literals; fft digest and thread variables observed) — 27 tests, command `cd _optionA_dev/fourier_chirality && python3 -W error::ResourceWarning -m unittest test_fourier_chirality test_run_configurations`; corpus fixtures 4 + 2 tests from `_optionA_dev/corpus_identity/`; beacon_v2 19 tests from `_optionA_dev/beacon_v2/` (`python3 -W error::ResourceWarning -m unittest test_beacon_v2`); probes `python3 negative_probes.py` (offline, mocked network); behaviour record `python3 observed_behaviour.py`; corpus `python3 -W error::ResourceWarning -m unittest test_build_corpus_identity test_build_guarded_pool` — the SAME warning-strict form for every fixture command in this document; beacon fixture: `test_beacon_v2.py` under `_optionA_dev/beacon_v2/` (19 tests; pinned in the BEACON PATH block). BEACON PATH (`_optionA_dev/beacon_v2/`, the one rebuild): `beacon_record_drand.py` `7d77d7465163a1ee4158506e00d49ee92e9e332a58d3f6452049a7a538bddcd8` (V20: the ONE verdict, drand-only — see §3b (3)–(4); fixture `test_beacon_record_drand.py` `beff3592d6f31dc75c6dc88b26d0ea03e974a9b0a2ebafac00a9d0787e9f6099`, 6 tests; the V16–V19 module `beacon_record_expedited.py` `7b82fa32fc2d55dc967806295e40434d7c0464f1c138b3868fcf1ddff1d8c627` and its fixture `77d5bb9961683ecdc9d6b3bf7b34f1f96fe05e585c6803545a5fc26367b67d64` are superseded and retained; collect + the single verdict, live equality over all four retained NIST inputs), `nist_pulse.py` `c725dd6ab4830a2dc709e55195507c1739250851498f3384dfd10c8732b43cb9` (evidence + authentication + anchor), `drand_round.py` `3403aee0d5d8c57ba0878bc5b716000faf93eab7a0fba0c7613613a501300882` (pinned relays + agreement), pinned root `pinned_root_DigiCertGlobalRootG2.pem` `5d550643b6400d4341550a9b14aedd0b4fac33ae5deb7d8247b6b4f799c13306`; fixtures `test_beacon_v2.py` `c8689eef76e8f035fed12d3f61c3ecd5c11eaa695b1e16b31c9f77e81ed83e38` (19 tests, ending in token coverage) with the test-only three-tier PKI `test_pki.py` `a7a9c85347db34e20f207380972a72703e322bad6790672df02fb3114b682c47` (production never imports it); probes `negative_probes.py` `839f6f54b165fb879fdf4f3699e425728737340c8662948c29ee79941d76dd0c` and receipt `53ff8213c34435c7c093324218597bbc9355ac68b0c42f02bd887c9efdb0fd22` (deterministic — no wall clock; regenerating reproduces the digest); behaviour record generator `observed_behaviour.py` `0911dfb87778a54efc1bef552d50d0d4d8cf2ee6a50046a4070ddf05c5145be0` and its output `BEACON_V2_OBSERVED_BEHAVIOUR_20260906.md` `c68d49e22427a328ce090b615c8bf715eb1e8f11b0bfdd503e18916dbbf7f858` (deterministic — no timestamps or durations; regenerating reproduces the digest); retained live sample `_sample_pulse_last.json` `08f07600adf6ae1d1655aac33f6976d48798a3de66153a4d92df78560ca828d9`, `_sample_certificate.pem` `c342339ca0fe5f1c522e03471b32811310371c3adbb8f107c8d77b5f336bfce9`, `_digicert_intermediate.pem` `6601f41fceefbe7523a6a2e746938de57fc24e99426b7bea58d1867dbee1be5e`. The previous implementation (`_optionA_dev/beacon_v1_retired_20260906/`) is pinned by nothing. EXCLUSION FILE: `_optionA_dev/corpus_identity/dryrun_identities_to_exclude_20260905.txt`, SHA-256 `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95`, 2,644 objids (the dry-run tuning 400 + holdout 200 + fresh 2,000 + 44 skipped, from the archived dry-run output). Vendored generator `w_chi_vendored.py` SHA-256 `89da33ec6260e75e06eadb0f171da4c52f1478b59ff5e543d363dbf56fefcd75`. (ii) CANDIDATE IDENTITY = family hash + the reconstructed winner's `config_id` + the environment lock hash, produced by `tune`, tested once by `holdout`, receipted and SEALED before the fresh validation set's pixels are fetched by the custodian account. No third freeze exists.
---
> - **E3** Pinned identity, TWO freezes, and an EXECUTABLE SEARCH IDENTITY frozen by this rule. (i) FAMILY: `_optionA_dev/fourier_chirality/fourier_chirality.py`, SHA-256 `a026fe5fa168ca1eb2056829d86fb549dec4a61596b5d122f60c2a389e2d98a2` (unchanged from V5) — configuration-driven, all state rebuilt per configuration, grid `CONFIG_GRID` and fixed `FIXED` in the file, refuses out-of-grid values and any key set other than the grid's. DRIVER: `run_configurations_v14.py`, SHA-256 `264a3897e335bb5bc9f83fc63ac7d3cabef7815f9f563e97a02d93ad8800b22a` (V30 STAGED: `run_configurations_v13.py` — reviewed at V29, retained — with composed mode rebuilt as ONE RESOLVER (`composed_resolver`: four contributing stages, `provenance_designs_v10.resolve` decides by the stated order; the v13 stage-by-stage `composed_provenance` removed, not kept as dead code); V29 STAGED was: `run_configurations_v12.py` — reviewed at V28, retained — plus composed mode on `provenance_designs_v9` (the witness-commit precheck defers an UNDETERMINED delivery to live authentication in composed mode; the open-event stage consults the feed before any retry; positive non-delivery terminal; offline keeps its named undetermined refusal); V28 STAGED was: `run_configurations_v11.py` — reviewed at V27, retained — plus composed mode on `provenance_designs_v8` (locally decidable mismatches before any retrieval; same-id contradictions before any retry; tri-state delivery through the witness-commit precheck — RETRY-EVENTS-UNAVAILABLE / EVENT-INCONSISTENT in composed mode, IDENTITY-WITNESS-COMMIT-UNDETERMINED / IDENTITY-WITNESS-COMMIT offline — and through the open-event stage); V27 STAGED was: `run_configurations_v10.py` — reviewed at V26, retained — plus composed mode on `provenance_designs_v7` (EVENT-FORGED on a same-event contradiction; RETRY-EVENTS-UNAVAILABLE on an undeterminable delivery; EVENT-INCONSISTENT / EVENT-NOT-EARLIEST named); V26 was: `run_configurations_v9.py` — reviewed at V25, retained — plus composed mode on `provenance_designs_v6` (RETRY- for UNAVAILABLE/INCOMPLETE at every stage; absence of the approval event is RETRY-EVENTS-INCOMPLETE); V25 was: `run_configurations_v8.py` — reviewed at V24, retained — plus composed mode on `provenance_designs_v5` (EVIDENCE-* dispositions are RETRY); V24 was: `run_configurations_v7.py` — reviewed at V23, retained — plus composed mode on `provenance_designs_v4` (one server-timed push event per history commit); V23 was: `run_configurations_v6.py` — reviewed at V22, retained — plus (codex V22 A/B/C/E) composed mode on `provenance_designs_v3` with a retained history-open PushEvent (`history_open_event_file`), one delivery predicate, render-end last; V22 was: `run_configurations_v5.py` `d338e33dfbf84ed7127c6b32e529da6dcfd0cbf9704aba468cdc2aa2a426722c` — refused at the V21 gate, retained — plus (codex V21 C2) W4 reproduced in the driver (`Protocol.approval_glob`, exactly one approval-record path in all history), (C3) a `witness-closed` history entry is terminal (`COLLECTION-CLOSED`), (C9) sentinel reconciliation requires a non-empty refusal cause on every journalled refusal and a render-end with the matching count, (C11, UNADOPTED, default off) `Protocol.verify_split` — recompute the §3b split from the pinned catalogue files and require the identity's lists to equal it; and (Blanc 21:14, UNADOPTED, default `offline`) `Protocol.provenance_mode = 'composed'` — the track-2 v10 helpers called INSIDE `load_identity` on the production call path (live event authentication through the `gh` runner with W3's delivery predicate; history continuation against the live remote head from a RETAINED history-open PushEvent that must be authentic and must deliver a genesis-only first commit; one entry per acknowledged commit; the EXPIRED path closed unless a receipt path is configured), so that adopting the design is a flag and its inputs, not new wiring — exhibited in `test_composed_mode_on_the_production_call_path` and attack rows 10a–10c (the THREE STATES: offline candidate accepts attacks 1–2; standalone helpers refuse them; composed mode refuses them on the same call); fixture `test_run_configurations_v14.py` `fbaf17c0d828f666c7f9228532042b0bdbb861b0d41fb13632540251fa2af240`, 23 tests (its composed test's expired fixture is an unrelated later push: under v7's contradiction predicate a later push delivering the same commit is FORGED, not EXPIRED) (composed test: one fixture push event per published commit, before = parent) (composed test: per-entry publication, retained open event, codex's V21 attack 2 and V22 attack A), ONE approval commit per repository (the v5 fixture's per-call records had masked W4), the end-to-end test exhibiting `verify_split` refusing a coherently sealed swapped list that the default driver accepts. V21 was: `run_configurations_v4.py` `d55ea70ddb27ba3b849204addeec3da0551657b7a0bb3d53a422d61f9a7d7993` — superseded and retained — plus the PRODUCTION WIRING: `PRODUCTION = Protocol(rederive_seed=production_rederive_seed)`, a module-level function that recomputes the drand-only verdict OFFLINE from the retained record (`beacon_record_drand_v21.verdict`, fetch=None, bound to the adopted digest and the committed approval record bytes; the verifier stack is imported lazily and its absence is `VERIFIER-UNAVAILABLE`, never a pass); the FULL CONJUNCTION of §3c computed from COMMITTED evidence; ADAPTER RECONCILIATION (§7); an unreachable `IDENTITY-MISSING` branch of v2–v4 made reachable; and `holdout_once` [INHERITED — PREPARED, NOT ADOPTED, §7]. Fixture `test_run_configurations_v5.py` `3d63af01bff40f663caa2ac5a36ebf5792876c35eefd2ac95e81ca9e9a688794`, 22 tests, whose test protocol is built FROM `PRODUCTION.__dict__` with paths, sizes and pinned digests overridden ONLY — the re-deriver is PRODUCTION's function, asserted by identity (`assertIs`), no callback substitution — and whose identity fixture carries REAL evidence: a committed approval record and adoption file at one approval commit, a real BEACON-RECORD-4 for the public round 6441924 (BLS-verified; the exhibit-round exclusion lifted inside the fixture only), a real BLS-verified nonce (round 6441904 = round_for(T_sign) for that exhibit T_sign, its bodies pinned from two relays: `drand_only/round_6441904_api.drand.sh.json` and `…api2…`, both `b787e446384c59f610d86b7b2c005f6ee04713ca1c59cb5f2b52a99a4623571f`), an authenticated history, a PushEvent with canonical digest and provenance; twenty-five fabricated identities are refused each by the check that names its defect, the real entry point `main([...])` is exercised under PRODUCTION, and the end-to-end test drives the collector CLI (genesis) → V21 builder → driver v5. V20 was: V15's `run_configurations.py` `d486ae58f40d220e8d2a9a1d875e008059cdc3bb1d9aee0dffc7dc974c7d53aa` plus the adoption binding, the render-refused sentinel accounting and the SEMANTIC acceptance conjunction of §3c — fixture `test_run_configurations_v4.py` `512384b0112aec56a4113db9d42dd0f9acc3c0df0d95eb1ddc3c53ab9502e196`, 20 tests — REFUSED at the V20 gate for injecting the re-deriver; the V19 driver `2028ee3a73367f9783fb243b26b557c0ed1eea97832590deff27149bbc37e8c0` and its fixture `642805b2b47a8d45017db3077cac5ec9c303425fb9c3490a5b8c8ca45f8902d1` are superseded and retained) — the ONLY path from tensors to a selected candidate; two closed modes, both requiring `--identity`: **`tune`** verifies the environment (below) before any read; loads the CORPUS IDENTITY file, validates its schema (CORPUS-IDENTITY-2; pool digest EQUAL to the pinned guarded pool; exclusion-file digest EQUAL to the pinned exclusion file; split mode `beacon-seeded`; a 64-hex beacon-record digest present; `rule_sha256` EQUAL to the signed rule's digest pinned in the driver's production protocol at adoption; ordering rule, seed, renderability inputs; integer objids; exactly 400/200/2,000; three-way disjoint — a TEST-SEED identity is refused) and refuses unless its SHA-256 is recorded in the lane's chained seal journal under operation `corpus-identity-freeze` AND that journal record is WITNESSED: a file `<journal>.witness.corpus-identity-freeze.json` names a 40-hex git commit; the driver requires (`verify_witness`) that the commit exists, that `git cat-file` of the journal at that commit contains the record line, that the IDENTITY FILE'S BYTES are the blob at that commit (and, for `tuning-freeze`, that the receipts, per-object journal and summary are blobs at that commit — agy V12: the pushed commit must carry the artefacts themselves, not only a digest line), that `git remote get-url origin` equals the pinned `https://github.com/DuhoKim/NebulaMind.git` (agy V8: a clone whose `origin` was redirected to a local bare repository is refused), and — after `git fetch` — that the commit is an ancestor of `origin/feat/paper-workflow-v2` (a remote whose branch is protected server-side against force pushes and deletion — Duho's GitHub setting, evidenced as §7 requires; no local hook is relied on); any failure is DATA-INTEGRITY-FAIL; refuses any manifest whose ORDERED objids differ from the identity's `tuning_objids` (as well as size ≠ 400, duplicates, missing tensor, tensor hash mismatch, label ∉ {±1}); runs all 96 configurations in lexicographic order; UNSCORED = tie/non-finite/missing/exception/non-repeat; ELIGIBLE iff m ≥ 380 (literal, no flag); OBJECTIVE = max(k, m−k)/400; tie-break fewer unscored, then earlier index; writes per-object journal, per-configuration receipts (each carrying the lock hash, observed environment incl. thread variables, manifest digest, tensor-set digest and identity digest) and a summary. **`holdout`** verifies the environment; loads the same sealed identity; verifies the tuning summary's journal and receipts digests; recomputes the tuning RUN ROOT (SHA-256 over the digests of the per-object journal, the receipts, the manifest, the tensor set, the corpus identity, the estimator, the driver and the environment lock) and refuses unless it equals the summary's and is sealed under operation `tuning-freeze` AND witnessed (pushed commit verified by `verify_witness`); RECONCILES every receipt against the per-object journal (recomputing k, m and unscored per configuration; refuses `JOURNAL-MISMATCH`); refuses if the estimator file changed since tuning; RECONSTRUCTS the winner from the 96 receipts — exactly 96 in order, each configuration and id equal to the live enumeration, counts consistent, eligibility/objective/tie-break re-derived — and refuses (`WINNER-SUBSTITUTED`) if any summary winner field differs; refuses any manifest whose ordered objids differ from `holdout_objids`; refuses overlap with tuning; evaluates that one configuration once; `CLOSED: holdout floor` if m < 190, else `PASS` iff Wilson lower bound on max(k, m−k)/200 > 0.70, else `CLOSED: holdout strength`. Any deviation is DATA-INTEGRITY-FAIL, exit 2, no selection, no verdict. ENVIRONMENT: thread variables set to 1 before NumPy import; `env_lock.json` SHA-256 `4e2c851fe1f22f4bc28f121cf3ff4326f036be57fc100b34af9e5501d6c74251` (literal in the driver) must hash-match and the live interpreter, Python 3.9.6, NumPy 1.26.4, platform, machine arm64, NumPy core digest, pocketfft digest and thread variables must all equal the lock; stop on inequality. CORPUS IDENTITY SCRIPTS: `_optionA_dev/corpus_identity/build_guarded_pool.py`, SHA-256 `e9b00ef4f50ce93c980cf6c5e0581276c46e6fa9a56ed46daa6d4eb95d2f52f3`, re-derives the §9B.2–9B.4 guarded pool from the GZ1 table, the completeness receipt's terminal dispositions and the protected-object pins, with controls 12,100 / 46 / 12,054 asserted; `build_corpus_identity_v30.py`, SHA-256 `aec5834cd6e944c4911ff9f5c614e12d4d87970c513a022bb84b6915c447064f` (V30 STAGED: on provenance_designs_v10; fixture `test_build_corpus_identity_v30.py` `2a3eeb0e5e50b2b9fe408369984c342861fe650a443d01649b95d55dc1d56224`, 8 tests; V29 was: on provenance_designs_v9; fixture `test_build_corpus_identity_v29.py` `0d2992b4c1b0f5782baa807f5254086f53e9751608d5fdb3510ae071b1015a59`, 8 tests; V28 was: on provenance_designs_v8; fixture `test_build_corpus_identity_v28.py` `fd581106fae0475112337d3b7677c95d805f73a4bd9db7bb9f87268baf4f5691`, 8 tests; V27 was: on provenance_designs_v7; fixture `test_build_corpus_identity_v27.py` `4fbf91fe4de84d2e221b25fdd446aee35693b48a29fb4980d44935f6238a3a12`, 8 tests; V26 was: on provenance_designs_v6; fixture `test_build_corpus_identity_v26.py` `87e514fd6097835cf5091c878acccd721d3cf87b17eaf2bcf1c0caa2adf28267`, 8 tests; V25 was: on provenance_designs_v5; fixture `test_build_corpus_identity_v25.py` `da08d85fd95deac71d5fa845313ba658355a875035598d10e56f77299c44ce70`, 8 tests; V24 was: `build_corpus_identity_v23.py` retained; plus (codex V23 M2) `reconcile_pending` before any operation; fixture `test_build_corpus_identity_v24.py` `47708417a627573e2f1cd6d781286c3e1bbf5e84b5de18603d199b8f6c558d58`, 8 tests; V23 was: `build_corpus_identity_v22.py` retained; plus (codex V22 D) `builder-control-refusal` appended before a control failure returns, and (B) per-entry publication when `--publish-remote/--publish-ref` are given; V22 was: `build_corpus_identity_v21.py` `048b42211a23f47ee4d97a9f6b7658b169736dbc2c4aa2f922f14dbb9d6a65d0` retained, plus (codex V21 C4) the argument refusal disclosed in the sidecar and every failure after validation — catalogue, control, classification, output — logged as `builder-error` before refusing (the boundary wraps `build()`), approval glob V22; fixture `test_build_corpus_identity_v22.py` `eb9d0a858e8eec26ab37f654a3e662e691895d2c8cb08341c66c0e91b3d07993`, 8 tests; history `history_v2.py` `374d468a8a57f9f3fcb558c874688be8180371650f600f9902cbe1c95fcdbbb3` (stages `builder-control-refusal`, `builder-args-refusal` added; fixture `test_history_v2.py` `1585c48a961056975cef36b383384c9e3dbcb753722fa2b9af7c271a72b365bb`) (exclusive genesis creation; `witness-closed` terminal in `first_accept`; fixture `test_history_v2.py` `1585c48a961056975cef36b383384c9e3dbcb753722fa2b9af7c271a72b365bb`, 4 tests). V21 was: `build_corpus_identity_v20.py` `f3aa4955c150798fb255d6d9d89217e0872981b09fca6d8e845d2106358d70e0` — superseded and retained — plus the AUTHENTICATED HISTORY module `corpus_identity/history.py` `8ac7a81cc9ee5c250c2eea0ad05400ec72b6640e3d09b51c43ad67b81ffccde1` (fixture `test_history.py` `9d658c09f8ca0de9edd66fdeea99adbc0eaedf8a1204817369928a54eee37d24`, 4 tests), `approval_witness_v4`, `beacon_record_drand_v21`, and every exception path logged; V20 was: V15's `build_corpus_identity.py` `3090af770f0329beddb2d8b64acae6d4a1b1da06025e616b6fb145bff47ca46c` plus the drand-only verdict import, the approval-witness-v3 call, the complete collection history and the adoption-at-approval-commit binding; the V19 builder `27c88669bf413185d4d24da16d5e174f88c1c404e4b0bc80e40cf24dc2f6a814` is superseded and retained) (its beacon boundary is one call to `beacon_record.verdict` with live re-fetch; refuses `BEACON-NOT-ACCEPTED (<token>)`) (HISTORY: the earlier builder field-validation code was AUTHORED BY THE CODEX SEAT 2026-09-06 00:13–00:18 from Hwao's specification and no longer participates — every V16–V18 module is the author's; its refusal vocabulary is enumerated in `_optionA_dev/beacon/CODEX_BEACON_BUILD_RESULT_20260906.md`) (fixture `test_build_corpus_identity_v20.py` `3eb80459d9ba9e8226a2367e1d2a7426e12895bc9f9cad8a28b48de9e0172c07` (6 tests, listed in §3c; V19's fixture `fba963557b8fb961b15b0eebf627460fd8b1cf70d1a248bd70c76a1fe81d046b` retained); the V16/V17/V18 fixtures are retained as history (V16's 'both refuse' test exercised only its builder; V17's witness proved no upper bound; V18's lock logged only accepts — each as codex showed) beside V15's `test_build_corpus_identity.py` `2cd65283a1e227cd0511f5c7fc1135ec0b365cb76a9608ed94e988ed3148440c` (4 tests; every beacon record in it is PRODUCED BY `beacon_record.collect` over the three-tier test network, never hand-built), 4 tests (`test_build_corpus_identity.py`: control; seeded split; ordering/renderability; exclusion — V20's "3 tests" was a stale fragment, codex V20 5-frag) plus `test_build_guarded_pool.py` 2 tests, incl. the seeded split's determinism, case-insensitivity of the seed, exclusion of the failed set and of the dry-run identities, and that a different seed gives a different split; builds into a temporary directory and leaves nothing in the lane), orders the pool by §9B.5's hash rule, asserts that ranks 1–2,000 reproduce the failed set AS A SET with equal labels (the selection CSV is GZ1_OBJID-ordered), marks RENDERABILITY by exactly this rule and no other — the brick exists in the pinned DR9-north survey-bricks table (`2edd5c295fdad26852c6f224a3ff023cff43dd0e03a53acd35b767e726ee72fb`, half-open test) AND is not in the pinned no-r registry `validation_bricks/_bricks_without_r_coverage.txt` (`ba2eb9d16d0d1d47eef2e0d52497b56d44ac979ebe67dd54b33f57c117d7a2fe`); the sealed checksum catalogue is NOT consulted, and the identity records both input digests — and emits, in the schema the driver consumes (`schema_version` CORPUS-IDENTITY-2: top-level ordered `tuning_objids` 400, `holdout_objids` 200, `fresh_validation_objids` 2,000, `pool_sha256` = `2cc94a29562270fcb5043f4ce942e303696f359b5fb0c59fdee48578ebb34155`, per-set detail with bricks and digests, skipped ranks); their receipts state every control number obtained (pool 12,100 / guard dropped 46 / guarded pool 12,054 (guarded_pool.csv SHA-256 2cc94a29562270fcb5043f4ce942e303696f359b5fb0c59fdee48578ebb34155); the 2,000 smallest h mod 2^32 equal the failed set as a SET with equal labels (the selection CSV is GZ1_OBJID-ordered, so the control is set equality — Hwao's first brief wrongly asked for row order and the codex seat correctly stopped rather than tune); dry run of the identity script: tuning 400, holdout 200, fresh validation 2,000 renderable objects at ranks 2,001–4644, 44 non-renderable ranks skipped, 7410 pool objects unused — the dry-run outputs are NOT the sealed identity and were moved to _scratch; the sealed identity is produced once, after signature, and journalled). FIXTURES: `test_fourier_chirality.py` SHA-256 `c30b3af46b3a855c499469c4249e487a765c7015b46126291cc7a4c134072811` (10 tests; antisymmetry for all 96 asserts a tie in BOTH orders or exact negation) and `test_run_configurations.py` SHA-256 `2fa83512b36788345541e84ff013c4f5ae3a9a9a5f0df0cbacc4b519945707f1` (17 tests; the end-to-end test obtains its beacon record from the pinned fetcher under a mocked network, builds the seeded identity with the real pinned pool and exclusion digests, and loads it through the driver with the rule digest checked: REDIRECTED REMOTE refused `WITNESS-REMOTE-URL`; TEST-SEED identity refused in production mode; the end-to-end test builds a SEEDED identity from a test beacon record with the real pinned pool and exclusion digests; FORGED RECEIPTS — a coherently rewritten receipt set with a matching new seal is refused `JOURNAL-MISMATCH`; PRODUCTION-SHAPED identity in every test; WITNESS — a freeze committed but NOT pushed is refused `WITNESS-NOT-PUSHED`, pushed is accepted, a wrong commit and a missing witness file are refused, exercised against a real bare git remote created by the fixture; END-TO-END — the real builder's output loads through `load_identity` at 400/200/2,000 and is refused until sealed; environment mismatch refusal; manifest size/duplicate/missing/hash/order refusal; no floor flag and `--identity` required; unscored = miss and the floor both sides; tie-break levels; identity unsealed and identity-of-other-objects refused; holdout reconstructs the winner, refuses an unsealed tuning run and a COHERENT substitution; floor-CLOSED before Wilson, overlap and identity refusal; `CLOSED: holdout strength` reached deterministically; PASS reached; production literals; fft digest and thread variables observed) — 27 tests, command `cd _optionA_dev/fourier_chirality && python3 -W error::ResourceWarning -m unittest test_fourier_chirality test_run_configurations`; corpus fixtures 4 + 2 tests from `_optionA_dev/corpus_identity/`; beacon_v2 19 tests from `_optionA_dev/beacon_v2/` (`python3 -W error::ResourceWarning -m unittest test_beacon_v2`); probes `python3 negative_probes.py` (offline, mocked network); behaviour record `python3 observed_behaviour.py`; corpus `python3 -W error::ResourceWarning -m unittest test_build_corpus_identity test_build_guarded_pool` — the SAME warning-strict form for every fixture command in this document; beacon fixture: `test_beacon_v2.py` under `_optionA_dev/beacon_v2/` (19 tests; pinned in the BEACON PATH block). BEACON PATH (`_optionA_dev/beacon_v2/`, the one rebuild): `beacon_record_drand.py` `7d77d7465163a1ee4158506e00d49ee92e9e332a58d3f6452049a7a538bddcd8` (V20: the ONE verdict, drand-only — see §3b (3)–(4); fixture `test_beacon_record_drand.py` `beff3592d6f31dc75c6dc88b26d0ea03e974a9b0a2ebafac00a9d0787e9f6099`, 6 tests; the V16–V19 module `beacon_record_expedited.py` `7b82fa32fc2d55dc967806295e40434d7c0464f1c138b3868fcf1ddff1d8c627` and its fixture `77d5bb9961683ecdc9d6b3bf7b34f1f96fe05e585c6803545a5fc26367b67d64` are superseded and retained; collect + the single verdict, live equality over all four retained NIST inputs), `nist_pulse.py` `c725dd6ab4830a2dc709e55195507c1739250851498f3384dfd10c8732b43cb9` (evidence + authentication + anchor), `drand_round.py` `3403aee0d5d8c57ba0878bc5b716000faf93eab7a0fba0c7613613a501300882` (pinned relays + agreement), pinned root `pinned_root_DigiCertGlobalRootG2.pem` `5d550643b6400d4341550a9b14aedd0b4fac33ae5deb7d8247b6b4f799c13306`; fixtures `test_beacon_v2.py` `c8689eef76e8f035fed12d3f61c3ecd5c11eaa695b1e16b31c9f77e81ed83e38` (19 tests, ending in token coverage) with the test-only three-tier PKI `test_pki.py` `a7a9c85347db34e20f207380972a72703e322bad6790672df02fb3114b682c47` (production never imports it); probes `negative_probes.py` `839f6f54b165fb879fdf4f3699e425728737340c8662948c29ee79941d76dd0c` and receipt `53ff8213c34435c7c093324218597bbc9355ac68b0c42f02bd887c9efdb0fd22` (deterministic — no wall clock; regenerating reproduces the digest); behaviour record generator `observed_behaviour.py` `0911dfb87778a54efc1bef552d50d0d4d8cf2ee6a50046a4070ddf05c5145be0` and its output `BEACON_V2_OBSERVED_BEHAVIOUR_20260906.md` `c68d49e22427a328ce090b615c8bf715eb1e8f11b0bfdd503e18916dbbf7f858` (deterministic — no timestamps or durations; regenerating reproduces the digest); retained live sample `_sample_pulse_last.json` `08f07600adf6ae1d1655aac33f6976d48798a3de66153a4d92df78560ca828d9`, `_sample_certificate.pem` `c342339ca0fe5f1c522e03471b32811310371c3adbb8f107c8d77b5f336bfce9`, `_digicert_intermediate.pem` `6601f41fceefbe7523a6a2e746938de57fc24e99426b7bea58d1867dbee1be5e`. The previous implementation (`_optionA_dev/beacon_v1_retired_20260906/`) is pinned by nothing. EXCLUSION FILE: `_optionA_dev/corpus_identity/dryrun_identities_to_exclude_20260905.txt`, SHA-256 `77b29eafe18e02d4dee621a6e748e8f698381c6e23098e7db2db190aee270c95`, 2,644 objids (the dry-run tuning 400 + holdout 200 + fresh 2,000 + 44 skipped, from the archived dry-run output). Vendored generator `w_chi_vendored.py` SHA-256 `89da33ec6260e75e06eadb0f171da4c52f1478b59ff5e543d363dbf56fefcd75`. (ii) CANDIDATE IDENTITY = family hash + the reconstructed winner's `config_id` + the environment lock hash, produced by `tune`, tested once by `holdout`, receipted and SEALED before the fresh validation set's pixels are fetched by the custodian account. No third freeze exists.
33c33
< **Order, provable.** (1) This amendment is APPROVED (procedure adopted by Blanc 11:11 KST from Duho's ruling through the codex conversation): the exact FINAL bytes of this file and their SHA-256 are presented in the codex conversation; Duho approves THAT version there; the approval is recorded as "approved by Duho in the codex conversation, attested by Codex, <UTC>" — never as "signed" or "witnessed by Blanc" — with the version, the digest and the UTC; Blanc independently recomputes the digest from the bytes on disk and REFUSES on any difference. What that check proves: that the approved bytes are the bytes on disk. What it does not prove: who spoke — that stays attested by Codex's record and Duho's chat confirmations of the channel. T_sign is the UTC of that approval; the retained approval record (rule digest + T_sign, verbatim) is the `statement_bytes` the collector binds to. (1a) ORDER MADE VISIBLE — WITNESSED BY A SERVER CLOCK, NOT BY OURS (codex V16 C5; V17 FATAL 1; V18 item 1). The approval record is the file `APPROVAL_RECORD_SELRULE_V22_<date>.md` (the name the builder and the driver search for: glob `APPROVAL_RECORD_SELRULE_V22*` — V21's text still said V20, codex V21 C8), written by Hwao immediately after the approval, with a STRICT SCHEMA: exactly one line `RULE_SHA256: <the approved digest>`, exactly one line `APPROVAL_UTC: <T_sign>`, exactly one line `DRAND_AT_APPROVAL: round R randomness <64 hex>` quoting the drand mainnet round current at T_sign — EXACTLY: R ∈ {round_for(T_sign) − 1, round_for(T_sign)}, the round just closed or the one closing, because the latest CLOSED round at the instant of writing is one of these two; R's scheduled time is at or after MIN_T_SIGN, and the attestation text verbatim; it is committed TOGETHER WITH the adoption file `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line and one newline) and PUSHED to the protected branch. `approval_witness_v4.verify` (`_optionA_dev/corpus_identity/approval_witness_v4.py` `6181690ed5df23b60a2337cf9b2e285cf11a18b51e55c864e4f8579a4e95f581`; v3 `c41d516e75a8a4c408cf5f29abe6ea8e12e61d0235bdb6b72d4619da76e2671a` superseded and retained), called by the builder after an ACCEPT verdict and before any identity is written, REFUSES unless: (W1) exactly one commit adds the record, none modifies it, the committed blob equals the bytes on disk; (W2) `origin` is the pinned URL and, after a fetch, that commit is an ancestor of `origin/feat/paper-workflow-v2`; (W3) **the EARLIEST GitHub PushEvent that delivered that commit to the branch — `created_at` stamped by GitHub's server — is BEFORE T_pulse**; "delivered" = `head` is the commit, or the commit is in `payload.commits`, or (V21) the push's `before..head` range covers it — `before` a strict ancestor of the commit and the commit an ancestor of `head`, decided by `git merge-base --is-ancestor`, never by substring; the ENTIRE event JSON is retained in the identity with its retrieval provenance (endpoints, retrieval UTC, `gh` version) and its own digest; a missing event is PENDING while now < T_pulse + 6 h (the API's stated publication latency) and CLOSED afterwards; the state is RETURNED by the witness (`APPROVAL-PUSH-EVENT-PENDING` / `APPROVAL-PUSH-EVENT-CLOSED` / `APPROVAL-PUSHED-AFTER-T-PULSE`) and PERSISTED by the builder as a chained history entry (`witness-pending` / `witness-closed`) — never a licence for a new approval, time, round or source; GitHub serves this record for roughly the last 30 days or 300 events, so the receipt is taken promptly and, afterwards, is the lane's retained copy (limit W3-L — V20 cited "L3" here, which is the relay-availability limit; corrected); (W4) FIRST APPROVAL IS FINAL: exactly one approval-record path for this version anywhere in history — checked by the witness at build time AND reproduced by the driver at every load (V22, codex V21 C2); (W5) the nonce round R is EXACTLY one of {round_for(T_sign) − 1, round_for(T_sign)}, R's scheduled time is ≥ MIN_T_SIGN (enforced: `round_time(R) < MIN_T_SIGN` refuses), R is before the seed round, and the nonce is AUTHENTICATED: each pinned relay's body for R is fetched from the chain-hash-qualified path, BLS-verified under the pinned key with exact URL membership, ≥ 2 distinct relays must verify with `randomness` equal to the recorded nonce (`APPROVAL-NONCE-UNAUTHENTICATED` otherwise), and the verifying bodies are RETAINED in the witness (`nonce_bodies_b64`) so the driver re-verifies them offline — it proves the record was written no earlier than R's scheduled time (≥ T_sign − 60 s; V20's docstring said "T_sign − 30 s", corrected); the record's `APPROVAL_UTC` equals the record's T_sign and its `RULE_SHA256` the approved digest. What this PROVES: the record reached the protected branch before T_pulse by the server's clock and was written no earlier than round R. What stays ATTESTED: who spoke; that the approval statement was made at T_sign (Codex's attestation and Duho's chat confirmations). Fixtures: `test_approval_witness_v4.py` `015e2c2752b994e8cd270a5166f9d2296234bf4c7e6b42b22ac38343cc7cc133`, 3 tests over a real git clone with a bare origin, the REAL nonce round 6441904 (pinned bodies) and a mock events feed: accepted with retained verified nonce bodies and their offline re-verification; delivery by before..head ancestry accepted and `before == commit` refused; PENDING then CLOSED by the clock; pushed-after-T_pulse CLOSED; a non-verifying nonce body refused; a predecessor round scheduled before MIN_T_SIGN refused; earliest-event choice; the v3 fixture (9 tests, `aff92daf…`) stays with v3. In addition `beacon_record_drand_v29.collect` REFUSES any T_sign earlier than `MIN_T_SIGN` = 2026-09-06T02:20:00Z and REFUSES BY NAME the round of 2026-09-06T00:15:00Z (drand round 6440756) and the historical fixture rounds 6441904 and 6441924 — lower bounds and exclusions only. (2) T_pulse = the first whole minute ≥ T_sign + 600 s (`verify_drand.pulse_time`; observed: 01:00:00 → 01:10:00, 01:00:01 → 01:11:00); ROUND = ⌊(T_pulse − 1595431050)/30⌋ + 1 (`verify_drand.round_for`); the round's scheduled time is ≥ T_pulse > T_sign, so it does not exist at approval (exhibited for a hypothetical approval 2026-09-07T01:00:07Z → T_pulse 01:11:00Z → round 6443748, `DRAND_ONLY_EXHIBIT_20260906.md`). (3) `beacon_record_drand_v29.collect(fetch, T_sign, rule_sha256, statement_bytes, now)` (`_optionA_dev/beacon_v2/beacon_record_drand_v29.py` `e46b300852d430d7ddc7281b0390a5fa8537ed4b63c15c1b61c447d16295bb57` (V29: on provenance_designs_v9; v28 retained; V28: on provenance_designs_v8; v27 retained; V27: on provenance_designs_v7; v26 retained; V26: on provenance_designs_v6; v25 retained; V25: on provenance_designs_v5 — PUBLISH-UNRELATED-COMMITS refused; v24 retained; V24: outer refusal/error entries published; `reconcile_pending` before any operation; v23 retained; V23: `--publish-remote/--publish-ref` publish the genesis as the history-open commit and every attempt, one acknowledged commit each; v22 retained); V21's `beacon_record_drand_v21.py` `24afe74eb2310c68ed707a0110d428e2053285ba460a861008a0c0b6e1b36cf3` and V20's `beacon_record_drand.py` `7d77d7465163a1ee4158506e00d49ee92e9e332a58d3f6452049a7a538bddcd8` superseded and retained; verifier `drand_only/verify_drand_v2.py` `e982d9441f5c8d8b9d4cdf43b4bd44a03c5de42d4246c5bdc5531f4ba5d5ed7b`, fixture `test_verify_drand_v2.py` `62064e4461260cf898b7d23cbbc948a6e5ac3fc104c057b472d34728690b74db`, 4 tests incl. None / look-alike host / suffix URLs refused; V20's `verify_drand.py` `a781b965e50ea947969f4559c267b88505b55d292b04d0b4739381216da6c20d` (the pinned verifier is `verify_drand_v2.py` (`e982d9441f5c8d8b9d4cdf43b4bd44a03c5de42d4246c5bdc5531f4ba5d5ed7b`); codex V28-3: the V27 edit had put the historical digest beside the pinned name) is HISTORICAL, retained, not a current boundary) REFUSES to run before T_pulse (`BEACON-NOT-YET`), refuses a T_sign before `MIN_T_SIGN` = 2026-09-06T02:20:00Z and the public 2026-09-06T00:15:00Z pulse/round and BOTH historical fixture rounds by name — the exhibit's 6441924 and the nonce fixture's 6441904 (`EXCLUDED_ROUNDS = (6440756, 6441904, 6441924)`; V22, codex V21 C5) — (`T-SIGN-PREDATES-AMENDMENT`, `T-PULSE-EXCLUDED`), refuses a statement that lacks the rule digest or T_sign (`STATEMENT-DOES-NOT-BIND`), and writes a record (`BEACON-RECORD-4`) that contains ONLY inputs (T_sign, T_pulse, round, the pinned chain hash and public key, rule digest, statement bytes, collection UTC) and EVIDENCE BYTES — each pinned relay's raw body from the chain-hash-qualified path `/<chain hash>/public/<round>` (api, api2, api3.drand.sh; drand.cloudflare.com), or the error it returned — and no verdict, flag, seed or boolean. (4) `beacon_record_drand_v29.verdict(record, now, fetch, rule_sha256, statement_bytes)` recomputes everything from those bytes — only relay entries keyed by an EXACT pinned URL are considered, and distinct pinned relays are counted by URL — in this order: schema and pinned-chain identity, T_pulse/round derivation, MIN_T_SIGN and exclusions, rule digest and statement binding, then the CLOCK (`now < T_pulse` → `RETRY`), then for each retained body: JSON, `round` equal, signature 96 bytes (G2), `randomness = SHA-256(signature)`, **BLS12-381 verification under the pinned public key** (`verify_drand.verify`, `_optionA_dev/drand_only/verify_drand.py` `a781b965e50ea947969f4559c267b88505b55d292b04d0b4739381216da6c20d`, py_ecc G2Basic, DST BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_NUL_, message SHA-256(previous_signature ‖ round)), URL bound to the chain hash; ≥ 2 relays must verify and agree (they cannot verify and disagree); with `fetch` given, each verifying relay is re-fetched live and judged by the VALUE it carries (THE PROPERTY paragraph): equal bytes, or different bytes whose DECODED signature equals the retained one, confirm it live (the representation difference is recorded); a body that does not verify, or is absent, drops that relay from the live count; different signature BYTES that verify are `REFUSE-CONFLICTING-VALID-SIGNATURES`; fewer than 2 live confirmations is `RETRY`. Outcomes: `ACCEPT-DRAND` (seed = the round's randomness, verified relays listed) | `RETRY` | `REFUSE-<token>`. The same function is the builder's boundary. Every REFUSE token the code can emit is exercised by `test_beacon_record_drand_v29.py` (`6189f0a330889d26fc4156b3c08e1cfb1dbc2ca4ab492bef7f29c695f2ad6394`, 7 tests on the real retained round — the exclusion of that round is lifted only inside the fixture — including quorum, transport faults, tampered bodies, clock-first, bindings, and that the CLI logs every attempt including pre-parse errors); `verify_drand` itself by `test_verify_drand.py` (`c87ffb73b6fab411a2dcffcc9489a18ea13b1547410b175b993a9c4707c362d3`, 4 tests) and the drand-only exhibit `exhibit_drand_only.py` (`922cf28f7cc29d789566f01ff4abe60da87dee603252dd21adae16a9b798f62c`, output digest `45d6149a2ccf30cb167dc4331455f3403a733516a82399292b1e7ad2c029e531`). NIST is not consulted; `nist_pulse.py`, `beacon_record.py` (V15) and `beacon_record_expedited.py` (V16–V19, `7b82fa32fc2d55dc967806295e40434d7c0464f1c138b3868fcf1ddff1d8c627`) are superseded and retained.
---
> **Order, provable.** (1) This amendment is APPROVED (procedure adopted by Blanc 11:11 KST from Duho's ruling through the codex conversation): the exact FINAL bytes of this file and their SHA-256 are presented in the codex conversation; Duho approves THAT version there; the approval is recorded as "approved by Duho in the codex conversation, attested by Codex, <UTC>" — never as "signed" or "witnessed by Blanc" — with the version, the digest and the UTC; Blanc independently recomputes the digest from the bytes on disk and REFUSES on any difference. What that check proves: that the approved bytes are the bytes on disk. What it does not prove: who spoke — that stays attested by Codex's record and Duho's chat confirmations of the channel. T_sign is the UTC of that approval; the retained approval record (rule digest + T_sign, verbatim) is the `statement_bytes` the collector binds to. (1a) ORDER MADE VISIBLE — WITNESSED BY A SERVER CLOCK, NOT BY OURS (codex V16 C5; V17 FATAL 1; V18 item 1). The approval record is the file `APPROVAL_RECORD_SELRULE_V22_<date>.md` (the name the builder and the driver search for: glob `APPROVAL_RECORD_SELRULE_V22*` — V21's text still said V20, codex V21 C8), written by Hwao immediately after the approval, with a STRICT SCHEMA: exactly one line `RULE_SHA256: <the approved digest>`, exactly one line `APPROVAL_UTC: <T_sign>`, exactly one line `DRAND_AT_APPROVAL: round R randomness <64 hex>` quoting the drand mainnet round current at T_sign — EXACTLY: R ∈ {round_for(T_sign) − 1, round_for(T_sign)}, the round just closed or the one closing, because the latest CLOSED round at the instant of writing is one of these two; R's scheduled time is at or after MIN_T_SIGN, and the attestation text verbatim; it is committed TOGETHER WITH the adoption file `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line and one newline) and PUSHED to the protected branch. `approval_witness_v4.verify` (`_optionA_dev/corpus_identity/approval_witness_v4.py` `6181690ed5df23b60a2337cf9b2e285cf11a18b51e55c864e4f8579a4e95f581`; v3 `c41d516e75a8a4c408cf5f29abe6ea8e12e61d0235bdb6b72d4619da76e2671a` superseded and retained), called by the builder after an ACCEPT verdict and before any identity is written, REFUSES unless: (W1) exactly one commit adds the record, none modifies it, the committed blob equals the bytes on disk; (W2) `origin` is the pinned URL and, after a fetch, that commit is an ancestor of `origin/feat/paper-workflow-v2`; (W3) **the EARLIEST GitHub PushEvent that delivered that commit to the branch — `created_at` stamped by GitHub's server — is BEFORE T_pulse**; "delivered" = `head` is the commit, or the commit is in `payload.commits`, or (V21) the push's `before..head` range covers it — `before` a strict ancestor of the commit and the commit an ancestor of `head`, decided by `git merge-base --is-ancestor`, never by substring; the ENTIRE event JSON is retained in the identity with its retrieval provenance (endpoints, retrieval UTC, `gh` version) and its own digest; a missing event is PENDING while now < T_pulse + 6 h (the API's stated publication latency) and CLOSED afterwards; the state is RETURNED by the witness (`APPROVAL-PUSH-EVENT-PENDING` / `APPROVAL-PUSH-EVENT-CLOSED` / `APPROVAL-PUSHED-AFTER-T-PULSE`) and PERSISTED by the builder as a chained history entry (`witness-pending` / `witness-closed`) — never a licence for a new approval, time, round or source; GitHub serves this record for roughly the last 30 days or 300 events, so the receipt is taken promptly and, afterwards, is the lane's retained copy (limit W3-L — V20 cited "L3" here, which is the relay-availability limit; corrected); (W4) FIRST APPROVAL IS FINAL: exactly one approval-record path for this version anywhere in history — checked by the witness at build time AND reproduced by the driver at every load (V22, codex V21 C2); (W5) the nonce round R is EXACTLY one of {round_for(T_sign) − 1, round_for(T_sign)}, R's scheduled time is ≥ MIN_T_SIGN (enforced: `round_time(R) < MIN_T_SIGN` refuses), R is before the seed round, and the nonce is AUTHENTICATED: each pinned relay's body for R is fetched from the chain-hash-qualified path, BLS-verified under the pinned key with exact URL membership, ≥ 2 distinct relays must verify with `randomness` equal to the recorded nonce (`APPROVAL-NONCE-UNAUTHENTICATED` otherwise), and the verifying bodies are RETAINED in the witness (`nonce_bodies_b64`) so the driver re-verifies them offline — it proves the record was written no earlier than R's scheduled time (≥ T_sign − 60 s; V20's docstring said "T_sign − 30 s", corrected); the record's `APPROVAL_UTC` equals the record's T_sign and its `RULE_SHA256` the approved digest. What this PROVES: the record reached the protected branch before T_pulse by the server's clock and was written no earlier than round R. What stays ATTESTED: who spoke; that the approval statement was made at T_sign (Codex's attestation and Duho's chat confirmations). Fixtures: `test_approval_witness_v4.py` `015e2c2752b994e8cd270a5166f9d2296234bf4c7e6b42b22ac38343cc7cc133`, 3 tests over a real git clone with a bare origin, the REAL nonce round 6441904 (pinned bodies) and a mock events feed: accepted with retained verified nonce bodies and their offline re-verification; delivery by before..head ancestry accepted and `before == commit` refused; PENDING then CLOSED by the clock; pushed-after-T_pulse CLOSED; a non-verifying nonce body refused; a predecessor round scheduled before MIN_T_SIGN refused; earliest-event choice; the v3 fixture (9 tests, `aff92daf…`) stays with v3. In addition `beacon_record_drand_v30.collect` REFUSES any T_sign earlier than `MIN_T_SIGN` = 2026-09-06T02:20:00Z and REFUSES BY NAME the round of 2026-09-06T00:15:00Z (drand round 6440756) and the historical fixture rounds 6441904 and 6441924 — lower bounds and exclusions only. (2) T_pulse = the first whole minute ≥ T_sign + 600 s (`verify_drand.pulse_time`; observed: 01:00:00 → 01:10:00, 01:00:01 → 01:11:00); ROUND = ⌊(T_pulse − 1595431050)/30⌋ + 1 (`verify_drand.round_for`); the round's scheduled time is ≥ T_pulse > T_sign, so it does not exist at approval (exhibited for a hypothetical approval 2026-09-07T01:00:07Z → T_pulse 01:11:00Z → round 6443748, `DRAND_ONLY_EXHIBIT_20260906.md`). (3) `beacon_record_drand_v30.collect(fetch, T_sign, rule_sha256, statement_bytes, now)` (`_optionA_dev/beacon_v2/beacon_record_drand_v30.py` `43f7c1f7b9335425849aefab420ea0e727f5db63749a7fc8b7145222525c0111` (V30: on provenance_designs_v10; v29 retained; V29: on provenance_designs_v9; v28 retained; V28: on provenance_designs_v8; v27 retained; V27: on provenance_designs_v7; v26 retained; V26: on provenance_designs_v6; v25 retained; V25: on provenance_designs_v5 — PUBLISH-UNRELATED-COMMITS refused; v24 retained; V24: outer refusal/error entries published; `reconcile_pending` before any operation; v23 retained; V23: `--publish-remote/--publish-ref` publish the genesis as the history-open commit and every attempt, one acknowledged commit each; v22 retained); V21's `beacon_record_drand_v21.py` `24afe74eb2310c68ed707a0110d428e2053285ba460a861008a0c0b6e1b36cf3` and V20's `beacon_record_drand.py` `7d77d7465163a1ee4158506e00d49ee92e9e332a58d3f6452049a7a538bddcd8` superseded and retained; verifier `drand_only/verify_drand_v2.py` `e982d9441f5c8d8b9d4cdf43b4bd44a03c5de42d4246c5bdc5531f4ba5d5ed7b`, fixture `test_verify_drand_v2.py` `62064e4461260cf898b7d23cbbc948a6e5ac3fc104c057b472d34728690b74db`, 4 tests incl. None / look-alike host / suffix URLs refused; V20's `verify_drand.py` `a781b965e50ea947969f4559c267b88505b55d292b04d0b4739381216da6c20d` (the pinned verifier is `verify_drand_v2.py` (`e982d9441f5c8d8b9d4cdf43b4bd44a03c5de42d4246c5bdc5531f4ba5d5ed7b`); codex V28-3: the V27 edit had put the historical digest beside the pinned name) is HISTORICAL, retained, not a current boundary) REFUSES to run before T_pulse (`BEACON-NOT-YET`), refuses a T_sign before `MIN_T_SIGN` = 2026-09-06T02:20:00Z and the public 2026-09-06T00:15:00Z pulse/round and BOTH historical fixture rounds by name — the exhibit's 6441924 and the nonce fixture's 6441904 (`EXCLUDED_ROUNDS = (6440756, 6441904, 6441924)`; V22, codex V21 C5) — (`T-SIGN-PREDATES-AMENDMENT`, `T-PULSE-EXCLUDED`), refuses a statement that lacks the rule digest or T_sign (`STATEMENT-DOES-NOT-BIND`), and writes a record (`BEACON-RECORD-4`) that contains ONLY inputs (T_sign, T_pulse, round, the pinned chain hash and public key, rule digest, statement bytes, collection UTC) and EVIDENCE BYTES — each pinned relay's raw body from the chain-hash-qualified path `/<chain hash>/public/<round>` (api, api2, api3.drand.sh; drand.cloudflare.com), or the error it returned — and no verdict, flag, seed or boolean. (4) `beacon_record_drand_v30.verdict(record, now, fetch, rule_sha256, statement_bytes)` recomputes everything from those bytes — only relay entries keyed by an EXACT pinned URL are considered, and distinct pinned relays are counted by URL — in this order: schema and pinned-chain identity, T_pulse/round derivation, MIN_T_SIGN and exclusions, rule digest and statement binding, then the CLOCK (`now < T_pulse` → `RETRY`), then for each retained body: JSON, `round` equal, signature 96 bytes (G2), `randomness = SHA-256(signature)`, **BLS12-381 verification under the pinned public key** (`verify_drand.verify`, `_optionA_dev/drand_only/verify_drand.py` `a781b965e50ea947969f4559c267b88505b55d292b04d0b4739381216da6c20d`, py_ecc G2Basic, DST BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_NUL_, message SHA-256(previous_signature ‖ round)), URL bound to the chain hash; ≥ 2 relays must verify and agree (they cannot verify and disagree); with `fetch` given, each verifying relay is re-fetched live and judged by the VALUE it carries (THE PROPERTY paragraph): equal bytes, or different bytes whose DECODED signature equals the retained one, confirm it live (the representation difference is recorded); a body that does not verify, or is absent, drops that relay from the live count; different signature BYTES that verify are `REFUSE-CONFLICTING-VALID-SIGNATURES`; fewer than 2 live confirmations is `RETRY`. Outcomes: `ACCEPT-DRAND` (seed = the round's randomness, verified relays listed) | `RETRY` | `REFUSE-<token>`. The same function is the builder's boundary. Every REFUSE token the code can emit is exercised by `test_beacon_record_drand_v30.py` (`985f1784c5e4bf1a09561147a47a39540faeda37d0cdda3977c40efa7317178b`, 7 tests on the real retained round — the exclusion of that round is lifted only inside the fixture — including quorum, transport faults, tampered bodies, clock-first, bindings, and that the CLI logs every attempt including pre-parse errors); `verify_drand` itself by `test_verify_drand.py` (`c87ffb73b6fab411a2dcffcc9489a18ea13b1547410b175b993a9c4707c362d3`, 4 tests) and the drand-only exhibit `exhibit_drand_only.py` (`922cf28f7cc29d789566f01ff4abe60da87dee603252dd21adae16a9b798f62c`, output digest `45d6149a2ccf30cb167dc4331455f3403a733516a82399292b1e7ad2c029e531`). NIST is not consulted; `nist_pulse.py`, `beacon_record.py` (V15) and `beacon_record_expedited.py` (V16–V19, `7b82fa32fc2d55dc967806295e40434d7c0464f1c138b3868fcf1ddff1d8c627`) are superseded and retained.
38c38
< **3c. V29 — WHAT THE AMENDMENT COSTS, WHAT IS PRESERVED, HOW THE ORDER STAYS VISIBLE, AND WHAT IS STILL OPEN.** TWO CATEGORIES, KEPT APART (Blanc 02:06 KST; register `LIMITS_VS_CONTRADICTIONS_20260907.md`). DISCLOSED LIMITS — a precommitment may carry these and reviewers confirm their disclosure: L-OFF, the OFFLINE candidate (PRODUCTION default) accepts counter-cases 1, 2 and A (a coherently forged approval event, a history rebuilt before the freeze, a rebuilt history published as its own first commit) — only the UNADOPTED composed mode refuses them; L-COV, THE COVENANT below (published history and ordered publications are authenticated, not when operations occurred nor the completeness of unpublished observations/decisions; the seed is unchanged; a late approval stays disqualifying; independent decision evidence is NOT implemented); L-RCPT, the fabricated-first-receipt step of Option A′ (moot under Option C, the recommended no-receipt path); L-INH, the inherited V15 trust in the object lists and pool/exclusion digests — a default-mode implementation limit, not an irreducible one: `verify_split` is staged (UNADOPTED, off) and would refuse a coherently swapped list if adopted (codex V28); L-AVAIL, GitHub's event window (300 events / 30 days; 30 s–6 h latency) — an EXTERNAL availability limit — combined with the rule's stateless closure enforcement (a genuinely unavailable identity can stay pending; loss closes the approval under the rule; no persisted loader state); this never covers an already-decidable mismatch, which is terminal on every path (codex V28). REPAIRED CONTRADICTIONS — the text said one thing and the code did another; each was repaired fail-first in the candidate that followed its review, never disclosed as a limit: V27-1 (precedence) and V27-2 (tri-state propagation) at V28 (`TRACK8_FAIL_FIRST_RECEIPT_20260907.md`); V28-1 and V28-2 — the witness-precheck and open-event retries that pre-empted those very rules on the complete paths, introduced by the V27-2 repair itself and filed as unrepaired at the V28 outcome — in THIS candidate (`TRACK9_FAIL_FIRST_RECEIPT_20260907.md`, combined-condition tests on the complete paths); the earlier ones in tracks 1–7. THE TWO PRECEDENCE CLAUSES IN FORCE: "On both the approval and history-open paths, locally decidable retained-event mismatches are terminal before any evidence-unavailability return. In composed mode, unresolved delivery does not bypass live authentication: a same-ID canonical contradiction obtained from the live feed is terminal before an undetermined-delivery retry. Only when no higher-priority inconsistency is established may undetermined delivery or unavailable retrieval return the stage's retry disposition. Offline mode retains its explicitly named undetermined-delivery refusal." "Same-commit contradiction is scoped to the pinned protected ref and evaluated only when the retained event is absent. If it is present verbatim, distinct qualifying events are resolved by earliest-event ordering; a later event does not invalidate an authentic earliest event. Conflicting same-ID evidence is refused even beside a verbatim copy, without asserting that this alone identifies which producer supplied false evidence." UNREPAIRED CONTRADICTIONS: none known; anything a reviewer finds of that kind goes to this column and is repaired, never to the first. COLLECTION HISTORY, TAMPER-EVIDENT WITHIN THE FILE (V19 audit M2; codex V20 M2; codex V21 C3): `_optionA_dev/collection_log.jsonl` is a hash chain (`corpus_identity/history_v2.py`): the collector CLI (`--log` mandatory) writes the GENESIS on the first collect — anchored to SHA-256(approval record bytes), T_pulse, the rule digest and the round — and refuses to initiate over ANY existing file (exclusive creation — the creation is the lock; V22) or to log into a history initiated for another approval (`HISTORY-EXISTS`, `HISTORY-GENESIS-MISMATCH`); every later entry carries `prev_sha256` of the previous line and a stage from a closed set (`collector-collect`, `collector-verify`, `collector-refusal`, `collector-error`, `witness-pending`, `witness-closed`, `builder-pre-parse-refusal`, `builder-verdict`, `builder-witness-or-adoption`, `builder-error`, `builder-accept`, `builder-conflict`, `builder-control-refusal`, `builder-args-refusal`); appends happen under an exclusive file lock after re-validating the chain, and the FIRST ACCEPT is taken ATOMICALLY (`history.append_locked`: read-check-append under one lock, so two racing builders cannot both be first); an interior deletion, edit or reorder, or a second genesis, fails `history_v2.validate` and is refused (`HISTORY-INVALID`), disclosed beside the log (`<log>.pregenesis.jsonl`); a `witness-closed` entry is TERMINAL (`first_accept` reports it as a conflict; the driver refuses `COLLECTION-CLOSED`); WHAT THE CHAIN DOES NOT PROVE, stated plainly (codex V21 C3): custody — a history DELETED AND REBUILT before the first freeze (fresh genesis, one accept, consistent lock and count) validates internally and LOADS, and a deleted SUFFIX that leaves an accept in place validates; closing this needs the head anchored OUTSIDE the file — the track-2 design (b) v3, `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md`: the history-open commit carries EXACTLY the genesis and is delivered by a retained PushEvent authenticated live; every entry is committed, PUSHED and ACKNOWLEDGED as it happens (one push per attempt — the collector and builder do it themselves, `publish_entry`, and refuse to proceed otherwise); `validate_continuation_v3` obtains the current remote head independently (`ls-remote` against the pinned URL), walks the file's history on the REMOTE ref, requires one entry per acknowledged commit, and treats anything the remote has not acknowledged as PENDING-PUSH; THE COVENANT (codex V24 N1, replacing the V23 residual): the validator authenticates the published append-only history and distinct, ordered publication events; it does not authenticate when the underlying operations occurred, nor the completeness of unpublished observations or decisions — before the genesis was published or between later publications; an unpublished absence-based CLOSED decision can be removed before its publication; the fixed-round seed is unchanged by any of this and an authentic LATE approval event remains disqualifying; stronger decision completeness would require independently retained decision evidence, which is NOT implemented — UNADOPTED, Duho's call; until adopted this is an OPEN BOUNDARY of the candidate, not a closed one; a failure before the genesis inputs exist is written to that sidecar by name. The builder `build_corpus_identity_v29.py` (`3e6bdc911eb078a0e7892149663f062db425216784bc2a7ba53d39f808d49808`) requires the collector's genesis for THIS approval record and logs: the argument refusal (sidecar, `builder-args-refusal`), pre-parse refusals, every verdict, witness refusals WITH their PENDING/CLOSED state, adoption refusals, the first ACCEPT (binding), conflicts (`builder-conflict`, then `COLLECTION-LOCKED`; a later accept-bearing entry naming a different record or seed is a semantic conflict, refused with or without the label), and any other exception anywhere in `build()` — validation, catalogue, control, classification, output — as `builder-error` before `BUILDER-ERROR` (V22: the boundary wraps `build()`), and a CONTROL FAILURE — which returns False rather than raising — as `builder-control-refusal` before returning (V23, codex V22 D); the collector CLI discloses argument failures in the sidecar named by `--log`, or in `beacon_v2/collector_argparse_failures.jsonl` when no `--log` was given; what is still NOT logged: nothing known — the seats are asked to look; the identity carries the log's digest and first ACCEPT, its T_pulse, round, source and the adoption digest; the log is committed with the identity and the retained beacon record (E3 witness). ADOPTION BINDING: `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line and one newline) is committed WITH the approval record; the builder refuses unless it is a byte-equal blob at the witnessed approval commit and equals `--rule-sha256`. THE DRIVER'S CONJUNCTION, FROM COMMITTED EVIDENCE — WITH ITS TWO OPEN BOUNDARIES NAMED (V19 audit M1; codex V20 FATAL + M1; codex V21 C1/C2/C3): `run_configurations_v13.py` (`d59198d2342fb2d35d7ce2035c1a84d038645b16ba7b09d1dbf986a77f7b6ca3`) refuses to load an identity unless — in addition to the seal and freeze witness — its outcome is exactly `ACCEPT-DRAND` and its source the pinned chain; the APPROVAL RECORD named by the witness EXISTS in the repository, is a byte-equal blob at the approval commit, was added once by that commit and never touched (git log), that commit is an ancestor of the protected remote ref, it is the ONLY approval-record path for this version's glob in all history (W4, `APPROVAL-NOT-FIRST`), its digest equals the witness's, and its schema lines PARSE to: `RULE_SHA256` = the adopted digest, `APPROVAL_UTC` = the identity's T_sign, the one nonce line = the witness's round and randomness; T_pulse and the round are RE-DERIVED from T_sign and must equal the identity's, MIN_T_SIGN and the exclusions hold; the PushEvent is re-validated from its own bytes (type, protected ref, server time before T_pulse, delivery by head / commits / before..head ancestry via git, canonical digest equal to the witness's, retrieval provenance present) — OPEN BOUNDARY (codex V21 C1): this checks the retained event against ITSELF; a coherently forged event with a recomputed digest and invented provenance LOADS (attack 1 of the inspection); the driver does not re-query GitHub; the track-2 design (a) — live re-query of the pinned repository's events (BUILT: `retrieve_events` with pagination and named failure paths; an EMPTY feed is UNAVAILABLE), verbatim presence, earliest qualifying under W3's own delivery predicate, plus an independent OPS receipt for the expired case (BUILT: `write_events_receipt` / `verify_events_receipt`, receipt added once, published at the live remote head, origin = the label `{actor: ops-witness, session: OPS}` PROPOSED BY THE LANE for the OPS session's receipts, unadopted, not a user decision; the recommended default is the DELEGATED witness workflow of `QUESTIONS_FOR_DUHO_TRACK2_ORIGIN_AND_RECEIPTS_20260906.md` — no per-receipt human confirmation; the residual trusted step, stated there, is that a first receipt forged by the lane under the OPS label is caught only by OPS's retained copy, not by code; closing it by code needs a second GitHub identity or a custodian key, the one step only Duho can provide, and the lane recommends having no expired path at all) — is staged UNADOPTED; until adopted, the event is ATTESTED by the builder's run, not authenticated by the driver; the NONCE is in the exact set with the scheduled bound and before the seed round, and ≥ 2 retained relay bodies BLS-verify for it with the recorded randomness (offline); the adoption file is well-formed, equals the adopted digest, is a byte-equal blob at the approval commit, and matches the identity's adoption digest; the committed collection log's bytes match the identity, are the blob at the witnessed commit, VALIDATE as a chain whose genesis names this approval record's digest, T_pulse, rule and round, have the recorded entry count, contain no `witness-closed` entry, and whose EARLIEST `builder-accept` names the identity's record, outcome and seed with no semantic conflict (the chain's custody limit above applies here too); the retained beacon record matches the identity's digest, is a blob at the witnessed commit, binds the same T_sign/T_pulse/round/rule and the committed approval record's bytes as its statement, retains ≥ 2 distinct pinned relay bodies, and the seed RE-DERIVED from it by PRODUCTION's own re-deriver (`production_rederive_seed`, the offline drand-only verdict) equals the identity's seed. Any absent verifier is a refusal. INHERITED LIMITATION, disclosed (codex V21 C11; the signed V15 driver): beyond size, type and disjointness the driver takes the three object lists, the pool and exclusion digests and the ordering/renderability metadata ON TRUST from the sealed identity — a coherently sealed swapped or arbitrary list loads; the staged `Protocol.verify_split` (default off) recomputes the split from the pinned catalogue files and the seed and refuses `SPLIT-NOT-REPRODUCED` (exhibited end-to-end on real builder output); adopting it changes what the signed driver trusts and is Duho's call. Offline, the retained relay bodies prove valid signatures under the pinned key, not that two physical hosts served them; the builder's live confirmation is not reproduced by the driver. Fixtures (V22): `test_track1_fail_first.py` (`34cf9c3528e1221156e70d8bbdba5e5a063050f2fdfa4418d26c144d192e5752`, 10 tests, each covering one codex V21 finding — C2, C3 ×2, C4 ×3, C5, C6, C7/C8/C10 (the rule text itself), C9 — each shown FAILING against byte-copies of the V21 modules before the repair, `TRACK1_FAIL_FIRST_RECEIPT_20260906.md`); `coherent_attacks_v29.py` (`ba4cb1ba251e5e7ea9dfdd1647a6954c42019c98104f88bf01a1291a6d92a371`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v13 / provenance v9 — `V29_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict in every row as V28; earlier scripts retained as pinned; codex's coherent substitutions re-run against this candidate, output filed); `track2/provenance_designs_v9.py` (`cbebc41767149caa45ad97e57bfae8c9d37adb1b40cf46b4065ece04f10b49ae`; UNADOPTED; v9 = v8 + codex's two precedence clauses on the COMPLETE paths (`validate_continuation_v9` consults the feed before any retry; driver v13's precheck defers) and rule (iii) restated; fixture `track9/test_track9_fail_first.py` `c881060647240a9ddea37b209c2e5fd17685c657d444b47ed47e671ea97ba4ab`, 3 tests fail-first (1 complete-path case with seven sub-cases through driver v13, 1 rule-(iii) guard, 1 text); text tests `track9/test_track9_text_v29.py` `9fa4b7bab11313360b0259e9341f15bf851e7fa37d1fc51fa5f00c83aae49f96` (written against THIS text; 1 test), the retained V28-specific `track8/test_track8_text_v28.py` `\2` (written against V28, run against V28 only), v8 (`track2/provenance_designs_v8.py`, fixture `track8/test_track8_fail_first.py` `\1`, 7 tests — its text case is V28-specific and runs against the V28 text) retained; v8 = v7 + the three precedence rules with `local_precheck` before retrieval, the tri-state open-event stage of `validate_continuation_v8`, and UNDETERMINED for a git process-launch exception, the retained V27-specific `track7/test_track7_text_v27.py` `\2` (written against V27, run against V27 only), v7 (`track2/provenance_designs_v7.py`, fixture `track7/test_track7_fail_first.py` `\1`, 4 tests) retained; v7 = v6 + the stated contradiction predicate, tri-state delivery and named input-mismatch outcomes and the retained V26-specific `track6/test_track6_text_v26.py` `ed882a918e1ec34b974037450a34958728fe222c0208de4a0224b963553a8252` (written against V26, run against V26 only); v6 (`track6/test_track6_fail_first.py`, 5 tests) retained; v6 = v5 + ancestry-aware batch classification and absence-is-not-forgery at every stage; fixture `track6/test_track6_fail_first.py` `20ead17b4e29cf60eb10db7ee4fa2356d7cabaf0a35c9aedace8bca05523605f`, 5 tests fail-first; v5 (`track5/test_track5_fail_first.py`, 6 tests) retained; v5 = v4 + the N2 dispositions, the N3 producer precondition and THE COVENANT; fixture `track5/test_track5_fail_first.py` `0f0ce4780581026d1336219f8187e8dd744cb0561854e9802f8376ba388d58d0`, 6 tests fail-first; v4 (`track4/test_track4_fail_first.py`, 7 tests) retained; v4 = v3 + per-commit push-event proof of publication, pending-commit retry, `reconcile_pending`, the exact residual; fixture `track4/test_track4_fail_first.py` `f101e956ab92ae2853bb7545cd6f432d1d1b3b057663e609e9ec94cfbfacfb7b`, 7 tests fail-first; v3 (`track3/test_track3_fail_first.py`, 10 tests) retained; v3 = v2 + the authenticated genesis-only history-open anchor, the producer boundary `publish_entry`, one delivery predicate, empty-feed = UNAVAILABLE; fixture `track3/test_track3_fail_first.py` `4a6044b2e6d8578bcb2affa8ddbc0d3255cdc6bdfd2b816375ef9749680c484c`, 10 tests fail-first; v2 REPAIRED after codex's 21:00 probe — the history validator ASKS THE REMOTE with `ls-remote` and enforces a push-acknowledgement boundary; the event retrieval, pagination, error handling and expired-receipt paths are BUILT, runner-injected) + `test_track2_fail_first.py` (`a65599e2cf68136a5a8cfcec912e7ba7d397968d8d4b0266edfb2fecd67674b1`, 8 tests, fail-first against a copy of v1: codex's two counterexamples, stale head, unavailable remote, unpublished open commit, deleted suffix/rebuild, retrieval+pagination, every failure kind, the expired-receipt path incl. a replaced receipt; the live feed and the `gh` runner are fixture-supplied and the remote is a local bare repository with non-fast-forward receives denied — labelled); v1 `provenance_designs.py` (`6ff0f7dcb4c7a4170b876074c63ae981c4ad0603fa806ba11d25db614f02b3ae`) + `test_provenance_designs.py` (`ec23e5ae648f50391fea1bd68093a26461111d30de0f1c3d3a5ff47c4c37da82`, 2 tests) retained; `test_build_corpus_identity_v21.py` (`63dfcad2058e2643f1a7e6f90f1eb0929a75b3b9c722c990cdf548d7ec77c179`, 8 tests on the real round, the collector CLI initiating the genesis and a REAL BLS-verified nonce: accepted with a chained history and witness v4; every attempt logged incl. RETRY, PENDING, CLOSED and pushed-after; a deleted entry breaks the chain and is refused; a second collection locked out; adoption must be a blob at the approval commit; identity fields; pre-parse failure logged; any other exception logged as builder-error; a history for another approval refused); `test_beacon_record_drand_v21.py` (`ee8f519f757870020d5cb0af36a6e7e9cb570d95411e686e63c8223386d060cc`, 7 tests: same-signature re-encoding accepted as the same value, the conflicting-signature branch by simulation, malformed live JSON never an escape, unpinned and look-alike URLs not counted, every token exercised, the CLI's genesis/mismatch/pregenesis paths); `test_run_configurations_v5.py` (`3d63af01bff40f663caa2ac5a36ebf5792876c35eefd2ac95e81ca9e9a688794`, 22 tests; the conjunction refuses each of twenty-five defective identities — RETRY, invented and NIST outcomes, wrong source, missing/late witness (incl. a 2098 event), lock mismatch, log digest, logged conflict, malformed adoption, a consistently fabricated seed, an unverifying nonce, a wrong T_pulse, a missing approval record, a nonce line mismatch, a zeroed event digest, a rewritten event, missing provenance, a wrong entry count, a wrong round, a wrong T_sign line, a beacon record absent from the freeze commit, a reset 999-entry history, look-alike relay URLs — and an emptied log after the seal; delivery by before..head ancestry accepted; `rederive_seed = None` still refused; the real CLI entry point under PRODUCTION; sentinel reconciliation both ways; `holdout_once`; end-to-end collector CLI → V21 builder → driver v5; run under the pinned interpreter with the venv's packages on PYTHONPATH — E2's interpreter lock is unchanged). Run warning-strict (`-W error::ResourceWarning`) on this candidate: driver v12 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v28 8 + V15 builder 4 + verdict v28 7 + verifier v2 4 + track 1 10 + track 2 v2 8 + track 2 v1 2 + track 3 10 + track 4 7 + track 5 6 + track 6 5 + track 6 text (V26-specific) 1 + track 7 4 + track 7 text (V27-specific) 1 + track 8 7 + track 8 text (V28-specific) 1 + track 9 3 + track 9 text (V29-specific) 1 = 136 tests — TEXT-TEST ATTRIBUTION (Blanc 01:05): each text test is run against the document version its assertions were WRITTEN against (`TEXT_TEST_ATTRIBUTION_20260907.md`): the track-5 label test against the V25 text, the V26-specific text test against the V26 text, the V27-specific text test against the V27 text, the V28-specific text test AND track 8's V27-3 text case (it asserts the V28 counts) against the V28 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions) plus the V29-specific test against THIS text; a pass of an older label test says nothing about this text, no ResourceWarning. One RuntimeWarning remains and is INHERITED: `fourier_chirality.py:87` ("invalid value encountered in cast") when the sentinel NaN tensor is scored — the pinned V15 estimator; not repaired (a change to E3(i)'s family hash is Duho's call (a)). COST (V22; unchanged from V21 in substance): (i) NIST is not consulted at all: there is no primary/fallback pair, no live NIST attempt, no "tried once"; the seed's provenance is a threshold BLS signature VERIFIED under the pinned public key by py_ecc 8.0.0 (limit L4), transported by ≥ 2 of 4 pinned relays — a STRONGER provenance than V15's (where the drand fallback was unverified), obtained by giving up the NIST route entirely; what is given up with it is any independence from the League of Entropy's threshold (limit L1) and from the pinned key's authenticity (L2); (ii) the V15 signature and its RETRY record become history: one signed commitment whose seed was never drawn, on file forever, and the register shows a V15 signed-then-superseded row; (iii) nothing else — no sample size, floor, bar, exclusion, attempt count, custody or protection rule moves. PRESERVED: V15's bytes and digest, its signature record, the RETRY beacon record, every pin V15 carries except the files superseded and retained above. ORDER: the server-side push event (W3) and the nonce (W5) of step (1a) bound the approval record on both sides; MIN_T_SIGN and EXCLUDED_T_PULSE are lower bounds only; the change record carries the timeline (finding 11:01, dispatch 11:06, confirmation 11:08, drand-only decision ~19:02, V20 refusal 19:37, Blanc's bounded order 19:48/19:50, reproduction 19:48, repairs 19:50–20:1x, this text, gate, approval, T_pulse) so a reader sees request → authorisation → approval → pulse in that order.
---
> **3c. V30 — WHAT THE AMENDMENT COSTS, WHAT IS PRESERVED, HOW THE ORDER STAYS VISIBLE, AND WHAT IS STILL OPEN.** TWO CATEGORIES, KEPT APART (Blanc 02:06 KST; register `LIMITS_VS_CONTRADICTIONS_20260907.md`). DISCLOSED LIMITS — a precommitment may carry these and reviewers confirm their disclosure: L-OFF, the OFFLINE candidate (PRODUCTION default) accepts counter-cases 1, 2 and A (a coherently forged approval event, a history rebuilt before the freeze, a rebuilt history published as its own first commit) — only the UNADOPTED composed mode refuses them; L-COV, THE COVENANT below (published history and ordered publications are authenticated, not when operations occurred nor the completeness of unpublished observations/decisions; the seed is unchanged; a late approval stays disqualifying; independent decision evidence is NOT implemented); L-RCPT, the fabricated-first-receipt step of Option A′ (moot under Option C, the recommended no-receipt path); L-INH, the inherited V15 trust in the object lists and pool/exclusion digests — a default-mode implementation limit, not an irreducible one: `verify_split` is staged (UNADOPTED, off) and would refuse a coherently swapped list if adopted (codex V28); L-AVAIL, GitHub's event window (300 events / 30 days; 30 s–6 h latency) — an EXTERNAL availability limit — combined with the rule's stateless closure enforcement (a genuinely unavailable identity can stay pending; loss closes the approval under the rule; no persisted loader state); this never covers an already-decidable mismatch, which is terminal on every path (codex V28). REPAIRED CONTRADICTIONS — the text said one thing and the code did another; each was repaired fail-first in the candidate that followed its review, never disclosed as a limit: V27-1 (precedence) and V27-2 (tri-state propagation) at V28 (`TRACK8_FAIL_FIRST_RECEIPT_20260907.md`); V28-1 and V28-2 — the witness-precheck and open-event retries that pre-empted those very rules on the complete paths, introduced by the V27-2 repair itself and filed as unrepaired at the V28 outcome — at V29 (`TRACK9_FAIL_FIRST_RECEIPT_20260907.md`); V29-1 and V29-2 — the seed re-derivation, remote-head and approval-feed retries that pre-empted the same rules, the fifth and sixth instances of ONE pattern — in THIS candidate, not by a point fix but by the single resolver (`TRACK10_FAIL_FIRST_RECEIPT_20260907.md`); the earlier ones in tracks 1–7. THE TWO PRECEDENCE CLAUSES IN FORCE: "On both the approval and history-open paths, locally decidable retained-event mismatches are terminal before any evidence-unavailability return. In composed mode, unresolved delivery does not bypass live authentication: a same-ID canonical contradiction obtained from the live feed is terminal before an undetermined-delivery retry. Only when no higher-priority inconsistency is established may undetermined delivery or unavailable retrieval return the stage's retry disposition. Offline mode retains its explicitly named undetermined-delivery refusal." "Same-commit contradiction is scoped to the pinned protected ref and evaluated only when the retained event is absent. If it is present verbatim, distinct qualifying events are resolved by earliest-event ordering; a later event does not invalidate an authentic earliest event. Conflicting same-ID evidence is refused even beside a verbatim copy, without asserting that this alone identifies which producer supplied false evidence." UNREPAIRED CONTRADICTIONS: none known; anything a reviewer finds of that kind goes to this column and is repaired, never to the first. PRECEDENCE ORDER, STATED ONCE (Blanc's order of 03:20 KST, `BLANC_ORDER_PRECEDENCE_IS_ONE_PATTERN.md`; the constant `PRECEDENCE` in `provenance_designs_v10`; when two or more verdict classes are simultaneously derivable the higher one wins, top to bottom, no exceptions): 0 LOCAL-TERMINAL (locally decidable refusals needing no external evidence — identity integrity, retained-event type / pinned repository / protected ref / positive non-delivery, a missing retained file) > 1 FORGED (an affirmative contradiction in obtained live evidence) > 2 NOT-EARLIEST (a strictly earlier qualifying event) > 3 EXPIRED (a loss) > 4 DERIVED-TERMINAL (refusals proven from obtained remote or retained evidence: history not-an-extension / diverged / batch / not-genesis-only, REDERIVE-<non-retry>, seed mismatch) > 5 RETRY-UNAVAILABLE (feed or remote down, undetermined delivery with nothing contradicting, seed re-derivation RETRY) > 6 RETRY-INCOMPLETE (absent, nothing contradicts) > 7 ACCEPT. ONE PLACE ENFORCES IT: `provenance_designs_v10.resolve` — driver v14's `composed_resolver` runs four CONTRIBUTING stages (A local sweep of both retained events, B one retrieval with the approval authentication and the open event's same-id check, C seed re-derivation with PRODUCTION's own re-deriver, D history continuation) and none decides; class-0 findings raised in the fixed offline check sequence before the resolver are consistent with the order because nothing outranks class 0; within a class the fixed stage sequence decides, never retrieval order; every finding is recorded in the identity's `_findings`. EXHIBITED PAIRWISE: `V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class) and the complete-path pairs of tracks 8–10. COLLECTION HISTORY, TAMPER-EVIDENT WITHIN THE FILE (V19 audit M2; codex V20 M2; codex V21 C3): `_optionA_dev/collection_log.jsonl` is a hash chain (`corpus_identity/history_v2.py`): the collector CLI (`--log` mandatory) writes the GENESIS on the first collect — anchored to SHA-256(approval record bytes), T_pulse, the rule digest and the round — and refuses to initiate over ANY existing file (exclusive creation — the creation is the lock; V22) or to log into a history initiated for another approval (`HISTORY-EXISTS`, `HISTORY-GENESIS-MISMATCH`); every later entry carries `prev_sha256` of the previous line and a stage from a closed set (`collector-collect`, `collector-verify`, `collector-refusal`, `collector-error`, `witness-pending`, `witness-closed`, `builder-pre-parse-refusal`, `builder-verdict`, `builder-witness-or-adoption`, `builder-error`, `builder-accept`, `builder-conflict`, `builder-control-refusal`, `builder-args-refusal`); appends happen under an exclusive file lock after re-validating the chain, and the FIRST ACCEPT is taken ATOMICALLY (`history.append_locked`: read-check-append under one lock, so two racing builders cannot both be first); an interior deletion, edit or reorder, or a second genesis, fails `history_v2.validate` and is refused (`HISTORY-INVALID`), disclosed beside the log (`<log>.pregenesis.jsonl`); a `witness-closed` entry is TERMINAL (`first_accept` reports it as a conflict; the driver refuses `COLLECTION-CLOSED`); WHAT THE CHAIN DOES NOT PROVE, stated plainly (codex V21 C3): custody — a history DELETED AND REBUILT before the first freeze (fresh genesis, one accept, consistent lock and count) validates internally and LOADS, and a deleted SUFFIX that leaves an accept in place validates; closing this needs the head anchored OUTSIDE the file — the track-2 design (b) v3, `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md`: the history-open commit carries EXACTLY the genesis and is delivered by a retained PushEvent authenticated live; every entry is committed, PUSHED and ACKNOWLEDGED as it happens (one push per attempt — the collector and builder do it themselves, `publish_entry`, and refuse to proceed otherwise); `validate_continuation_v3` obtains the current remote head independently (`ls-remote` against the pinned URL), walks the file's history on the REMOTE ref, requires one entry per acknowledged commit, and treats anything the remote has not acknowledged as PENDING-PUSH; THE COVENANT (codex V24 N1, replacing the V23 residual): the validator authenticates the published append-only history and distinct, ordered publication events; it does not authenticate when the underlying operations occurred, nor the completeness of unpublished observations or decisions — before the genesis was published or between later publications; an unpublished absence-based CLOSED decision can be removed before its publication; the fixed-round seed is unchanged by any of this and an authentic LATE approval event remains disqualifying; stronger decision completeness would require independently retained decision evidence, which is NOT implemented — UNADOPTED, Duho's call; until adopted this is an OPEN BOUNDARY of the candidate, not a closed one; a failure before the genesis inputs exist is written to that sidecar by name. The builder `build_corpus_identity_v30.py` (`aec5834cd6e944c4911ff9f5c614e12d4d87970c513a022bb84b6915c447064f`) requires the collector's genesis for THIS approval record and logs: the argument refusal (sidecar, `builder-args-refusal`), pre-parse refusals, every verdict, witness refusals WITH their PENDING/CLOSED state, adoption refusals, the first ACCEPT (binding), conflicts (`builder-conflict`, then `COLLECTION-LOCKED`; a later accept-bearing entry naming a different record or seed is a semantic conflict, refused with or without the label), and any other exception anywhere in `build()` — validation, catalogue, control, classification, output — as `builder-error` before `BUILDER-ERROR` (V22: the boundary wraps `build()`), and a CONTROL FAILURE — which returns False rather than raising — as `builder-control-refusal` before returning (V23, codex V22 D); the collector CLI discloses argument failures in the sidecar named by `--log`, or in `beacon_v2/collector_argparse_failures.jsonl` when no `--log` was given; what is still NOT logged: nothing known — the seats are asked to look; the identity carries the log's digest and first ACCEPT, its T_pulse, round, source and the adoption digest; the log is committed with the identity and the retained beacon record (E3 witness). ADOPTION BINDING: `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line and one newline) is committed WITH the approval record; the builder refuses unless it is a byte-equal blob at the witnessed approval commit and equals `--rule-sha256`. THE DRIVER'S CONJUNCTION, FROM COMMITTED EVIDENCE — WITH ITS TWO OPEN BOUNDARIES NAMED (V19 audit M1; codex V20 FATAL + M1; codex V21 C1/C2/C3): `run_configurations_v14.py` (`264a3897e335bb5bc9f83fc63ac7d3cabef7815f9f563e97a02d93ad8800b22a`) refuses to load an identity unless — in addition to the seal and freeze witness — its outcome is exactly `ACCEPT-DRAND` and its source the pinned chain; the APPROVAL RECORD named by the witness EXISTS in the repository, is a byte-equal blob at the approval commit, was added once by that commit and never touched (git log), that commit is an ancestor of the protected remote ref, it is the ONLY approval-record path for this version's glob in all history (W4, `APPROVAL-NOT-FIRST`), its digest equals the witness's, and its schema lines PARSE to: `RULE_SHA256` = the adopted digest, `APPROVAL_UTC` = the identity's T_sign, the one nonce line = the witness's round and randomness; T_pulse and the round are RE-DERIVED from T_sign and must equal the identity's, MIN_T_SIGN and the exclusions hold; the PushEvent is re-validated from its own bytes (type, protected ref, server time before T_pulse, delivery by head / commits / before..head ancestry via git, canonical digest equal to the witness's, retrieval provenance present) — OPEN BOUNDARY (codex V21 C1): this checks the retained event against ITSELF; a coherently forged event with a recomputed digest and invented provenance LOADS (attack 1 of the inspection); the driver does not re-query GitHub; the track-2 design (a) — live re-query of the pinned repository's events (BUILT: `retrieve_events` with pagination and named failure paths; an EMPTY feed is UNAVAILABLE), verbatim presence, earliest qualifying under W3's own delivery predicate, plus an independent OPS receipt for the expired case (BUILT: `write_events_receipt` / `verify_events_receipt`, receipt added once, published at the live remote head, origin = the label `{actor: ops-witness, session: OPS}` PROPOSED BY THE LANE for the OPS session's receipts, unadopted, not a user decision; the recommended default is the DELEGATED witness workflow of `QUESTIONS_FOR_DUHO_TRACK2_ORIGIN_AND_RECEIPTS_20260906.md` — no per-receipt human confirmation; the residual trusted step, stated there, is that a first receipt forged by the lane under the OPS label is caught only by OPS's retained copy, not by code; closing it by code needs a second GitHub identity or a custodian key, the one step only Duho can provide, and the lane recommends having no expired path at all) — is staged UNADOPTED; until adopted, the event is ATTESTED by the builder's run, not authenticated by the driver; the NONCE is in the exact set with the scheduled bound and before the seed round, and ≥ 2 retained relay bodies BLS-verify for it with the recorded randomness (offline); the adoption file is well-formed, equals the adopted digest, is a byte-equal blob at the approval commit, and matches the identity's adoption digest; the committed collection log's bytes match the identity, are the blob at the witnessed commit, VALIDATE as a chain whose genesis names this approval record's digest, T_pulse, rule and round, have the recorded entry count, contain no `witness-closed` entry, and whose EARLIEST `builder-accept` names the identity's record, outcome and seed with no semantic conflict (the chain's custody limit above applies here too); the retained beacon record matches the identity's digest, is a blob at the witnessed commit, binds the same T_sign/T_pulse/round/rule and the committed approval record's bytes as its statement, retains ≥ 2 distinct pinned relay bodies, and the seed RE-DERIVED from it by PRODUCTION's own re-deriver (`production_rederive_seed`, the offline drand-only verdict) equals the identity's seed. Any absent verifier is a refusal. INHERITED LIMITATION, disclosed (codex V21 C11; the signed V15 driver): beyond size, type and disjointness the driver takes the three object lists, the pool and exclusion digests and the ordering/renderability metadata ON TRUST from the sealed identity — a coherently sealed swapped or arbitrary list loads; the staged `Protocol.verify_split` (default off) recomputes the split from the pinned catalogue files and the seed and refuses `SPLIT-NOT-REPRODUCED` (exhibited end-to-end on real builder output); adopting it changes what the signed driver trusts and is Duho's call. Offline, the retained relay bodies prove valid signatures under the pinned key, not that two physical hosts served them; the builder's live confirmation is not reproduced by the driver. Fixtures (V22): `test_track1_fail_first.py` (`34cf9c3528e1221156e70d8bbdba5e5a063050f2fdfa4418d26c144d192e5752`, 10 tests, each covering one codex V21 finding — C2, C3 ×2, C4 ×3, C5, C6, C7/C8/C10 (the rule text itself), C9 — each shown FAILING against byte-copies of the V21 modules before the repair, `TRACK1_FAIL_FIRST_RECEIPT_20260906.md`); `coherent_attacks_v30.py` (`3deb4802d3dd89c9ec121789df9d1aced80d17b950f3e8a06274836ba5dbf896`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v14 / provenance v10 — `V30_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict in every row as V29; earlier scripts retained as pinned; codex's coherent substitutions re-run against this candidate, output filed); `track2/provenance_designs_v10.py` (`f2c878f2611a840a1bf8865a02023621b54ccf5ffb8a0b0b008a983586b0d8d3`; UNADOPTED; v10 = v9 + THE PRECEDENCE ORDER STATED ONCE (`PRECEDENCE`) and ONE RESOLVER (`resolve`, `finding`, `classify_history`, `local_precheck_event`; `validate_continuation_v10` is a contributor with v9's decision) and strictly-earlier ordering; fixture `track10/test_track10_fail_first.py` `8206328178624949068c9fee2fa0652fc6ece625a68ce8f75a2926ed49fc1aa7`, 4 tests fail-first (1 complete-path case with codex's seven V29 rows through driver v14, 1 strict-earlier case in both feed orders, 1 resolver pairwise case — 21 class pairs in both orders — and 1 text); exhibit `track10/exhibit_precedence_pairs.py` `d44bc2860e167842b6e561221dfc678d22d552b39d4f5f36ef575368ec8f65f8` (`V30_PRECEDENCE_EXHIBIT_20260907.md`); text tests `track10/test_track10_text_v30.py` `e57fcb8191017c47fa500b7e94bb8d2fa1d25545e65be005bb4fabef217738e7` (written against THIS text; 1 test), the retained V29-specific `track9/test_track9_text_v29.py` `9fa4b7bab11313360b0259e9341f15bf851e7fa37d1fc51fa5f00c83aae49f96` (written against V29, run against V29 only), v9 (`track2/provenance_designs_v9.py`, fixture `track9/test_track9_fail_first.py` `c881060647240a9ddea37b209c2e5fd17685c657d444b47ed47e671ea97ba4ab`, 3 tests — its text case written against V29) retained; v9 = v8 + codex's two precedence clauses on the COMPLETE paths (`validate_continuation_v9` consults the feed before any retry; driver v13's precheck defers) and rule (iii) restated, the retained V28-specific `track8/test_track8_text_v28.py` `4f1dc05521aec6588a70a5df5286b479c670f6824d1c40a65ca4bdca2db1f81e` (written against V28, run against V28 only), v8 (`track2/provenance_designs_v8.py`, fixture `track8/test_track8_fail_first.py` `05710cb371138a34825c53aa9c80e9370be935a5a428201c4366329eaa4cc494`, 7 tests — its text case is V28-specific and runs against the V28 text) retained; v8 = v7 + the three precedence rules with `local_precheck` before retrieval, the tri-state open-event stage of `validate_continuation_v8`, and UNDETERMINED for a git process-launch exception, the retained V27-specific `track7/test_track7_text_v27.py` `0a3ba58d11451e42b411dd125e5bf33d18fc7a0431ca68a01a8921d0154d2caa` (written against V27, run against V27 only), v7 (`track2/provenance_designs_v7.py`, fixture `track7/test_track7_fail_first.py` `dec20a7c678b00c61edefa033b3964238b7280f15411ec28725fa3b5a07fc7eb`, 4 tests) retained; v7 = v6 + the stated contradiction predicate, tri-state delivery and named input-mismatch outcomes and the retained V26-specific `track6/test_track6_text_v26.py` `ed882a918e1ec34b974037450a34958728fe222c0208de4a0224b963553a8252` (written against V26, run against V26 only); v6 (`track6/test_track6_fail_first.py`, 5 tests) retained; v6 = v5 + ancestry-aware batch classification and absence-is-not-forgery at every stage; fixture `track6/test_track6_fail_first.py` `20ead17b4e29cf60eb10db7ee4fa2356d7cabaf0a35c9aedace8bca05523605f`, 5 tests fail-first; v5 (`track5/test_track5_fail_first.py`, 6 tests) retained; v5 = v4 + the N2 dispositions, the N3 producer precondition and THE COVENANT; fixture `track5/test_track5_fail_first.py` `0f0ce4780581026d1336219f8187e8dd744cb0561854e9802f8376ba388d58d0`, 6 tests fail-first; v4 (`track4/test_track4_fail_first.py`, 7 tests) retained; v4 = v3 + per-commit push-event proof of publication, pending-commit retry, `reconcile_pending`, the exact residual; fixture `track4/test_track4_fail_first.py` `f101e956ab92ae2853bb7545cd6f432d1d1b3b057663e609e9ec94cfbfacfb7b`, 7 tests fail-first; v3 (`track3/test_track3_fail_first.py`, 10 tests) retained; v3 = v2 + the authenticated genesis-only history-open anchor, the producer boundary `publish_entry`, one delivery predicate, empty-feed = UNAVAILABLE; fixture `track3/test_track3_fail_first.py` `4a6044b2e6d8578bcb2affa8ddbc0d3255cdc6bdfd2b816375ef9749680c484c`, 10 tests fail-first; v2 REPAIRED after codex's 21:00 probe — the history validator ASKS THE REMOTE with `ls-remote` and enforces a push-acknowledgement boundary; the event retrieval, pagination, error handling and expired-receipt paths are BUILT, runner-injected) + `test_track2_fail_first.py` (`a65599e2cf68136a5a8cfcec912e7ba7d397968d8d4b0266edfb2fecd67674b1`, 8 tests, fail-first against a copy of v1: codex's two counterexamples, stale head, unavailable remote, unpublished open commit, deleted suffix/rebuild, retrieval+pagination, every failure kind, the expired-receipt path incl. a replaced receipt; the live feed and the `gh` runner are fixture-supplied and the remote is a local bare repository with non-fast-forward receives denied — labelled); v1 `provenance_designs.py` (`6ff0f7dcb4c7a4170b876074c63ae981c4ad0603fa806ba11d25db614f02b3ae`) + `test_provenance_designs.py` (`ec23e5ae648f50391fea1bd68093a26461111d30de0f1c3d3a5ff47c4c37da82`, 2 tests) retained; `test_build_corpus_identity_v21.py` (`63dfcad2058e2643f1a7e6f90f1eb0929a75b3b9c722c990cdf548d7ec77c179`, 8 tests on the real round, the collector CLI initiating the genesis and a REAL BLS-verified nonce: accepted with a chained history and witness v4; every attempt logged incl. RETRY, PENDING, CLOSED and pushed-after; a deleted entry breaks the chain and is refused; a second collection locked out; adoption must be a blob at the approval commit; identity fields; pre-parse failure logged; any other exception logged as builder-error; a history for another approval refused); `test_beacon_record_drand_v21.py` (`ee8f519f757870020d5cb0af36a6e7e9cb570d95411e686e63c8223386d060cc`, 7 tests: same-signature re-encoding accepted as the same value, the conflicting-signature branch by simulation, malformed live JSON never an escape, unpinned and look-alike URLs not counted, every token exercised, the CLI's genesis/mismatch/pregenesis paths); `test_run_configurations_v5.py` (`3d63af01bff40f663caa2ac5a36ebf5792876c35eefd2ac95e81ca9e9a688794`, 22 tests; the conjunction refuses each of twenty-five defective identities — RETRY, invented and NIST outcomes, wrong source, missing/late witness (incl. a 2098 event), lock mismatch, log digest, logged conflict, malformed adoption, a consistently fabricated seed, an unverifying nonce, a wrong T_pulse, a missing approval record, a nonce line mismatch, a zeroed event digest, a rewritten event, missing provenance, a wrong entry count, a wrong round, a wrong T_sign line, a beacon record absent from the freeze commit, a reset 999-entry history, look-alike relay URLs — and an emptied log after the seal; delivery by before..head ancestry accepted; `rederive_seed = None` still refused; the real CLI entry point under PRODUCTION; sentinel reconciliation both ways; `holdout_once`; end-to-end collector CLI → V21 builder → driver v5; run under the pinned interpreter with the venv's packages on PYTHONPATH — E2's interpreter lock is unchanged). Run warning-strict (`-W error::ResourceWarning`) on this candidate: driver v14 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v30 8 + V15 builder 4 + verdict v30 7 + verifier v2 4 + track 1 10 + track 2 v2 8 + track 2 v1 2 + track 3 10 + track 4 7 + track 5 6 + track 6 5 + track 6 text (V26-specific) 1 + track 7 4 + track 7 text (V27-specific) 1 + track 8 7 + track 8 text (V28-specific) 1 + track 9 3 + track 9 text (V29-specific) 1 + track 10 4 + track 10 text (V30-specific) 1 = 141 tests — TEXT-TEST ATTRIBUTION (Blanc 01:05): each text test is run against the document version its assertions were WRITTEN against (`TEXT_TEST_ATTRIBUTION_20260907.md`): the track-5 label test against the V25 text, the V26-specific text test against the V26 text, the V27-specific text test against the V27 text, the V28-specific text test AND track 8's V27-3 text case (it asserts the V28 counts) against the V28 text, the V29-specific text test against the V29 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions, track 10's V29-3 assertions) plus the V30-specific test against THIS text; a pass of an older label test says nothing about this text, no ResourceWarning. One RuntimeWarning remains and is INHERITED: `fourier_chirality.py:87` ("invalid value encountered in cast") when the sentinel NaN tensor is scored — the pinned V15 estimator; not repaired (a change to E3(i)'s family hash is Duho's call (a)). COST (V22; unchanged from V21 in substance): (i) NIST is not consulted at all: there is no primary/fallback pair, no live NIST attempt, no "tried once"; the seed's provenance is a threshold BLS signature VERIFIED under the pinned public key by py_ecc 8.0.0 (limit L4), transported by ≥ 2 of 4 pinned relays — a STRONGER provenance than V15's (where the drand fallback was unverified), obtained by giving up the NIST route entirely; what is given up with it is any independence from the League of Entropy's threshold (limit L1) and from the pinned key's authenticity (L2); (ii) the V15 signature and its RETRY record become history: one signed commitment whose seed was never drawn, on file forever, and the register shows a V15 signed-then-superseded row; (iii) nothing else — no sample size, floor, bar, exclusion, attempt count, custody or protection rule moves. PRESERVED: V15's bytes and digest, its signature record, the RETRY beacon record, every pin V15 carries except the files superseded and retained above. ORDER: the server-side push event (W3) and the nonce (W5) of step (1a) bound the approval record on both sides; MIN_T_SIGN and EXCLUDED_T_PULSE are lower bounds only; the change record carries the timeline (finding 11:01, dispatch 11:06, confirmation 11:08, drand-only decision ~19:02, V20 refusal 19:37, Blanc's bounded order 19:48/19:50, reproduction 19:48, repairs 19:50–20:1x, this text, gate, approval, T_pulse) so a reader sees request → authorisation → approval → pulse in that order.
50c50
< - **Corpus identity, sealed first**: after signature and the §3b beacon read, the pinned `PYTHONPATH=_optionA_dev/_venv_bls/lib/python3.9/site-packages /usr/bin/python3 _optionA_dev/corpus_identity/build_corpus_identity_v29.py --beacon-record <record> --rule-sha256 <the approved digest> --signature-statement <the witnessed APPROVAL_RECORD_SELRULE_V22 file> --exclude <exclusion file> [--publish-remote <pinned URL> --publish-ref refs/heads/feat/paper-workflow-v2, in composed mode]` (after the collector CLI `beacon_record_drand_v29.py collect … --log _optionA_dev/collection_log.jsonl [--publish-remote … --publish-ref …]` has written and, in composed mode, published the genesis as the history-open commit and the record) (E3(i)) produces `corpus_identity.json` — ordered `tuning_objids` (the first 400 RENDERABLE objects of the seeded order), `holdout_objids` (the next 200), `fresh_validation_objids` (the next 2,000), skipped ranks, seed, beacon-record digest, exclusion digest, pool digest and ordering rule — and its SHA-256 is recorded in the chained seal journal (operation `corpus-identity-freeze`) BEFORE any development brick is fetched or label opened. The driver refuses unsealed or non-matching identities. Tuning and holdout bricks are fetched by the seal-only discipline (§9B.2a–c) after this rule is signed; labels may be used in development on the TUNING set only.
---
> - **Corpus identity, sealed first**: after signature and the §3b beacon read, the pinned `PYTHONPATH=_optionA_dev/_venv_bls/lib/python3.9/site-packages /usr/bin/python3 _optionA_dev/corpus_identity/build_corpus_identity_v30.py --beacon-record <record> --rule-sha256 <the approved digest> --signature-statement <the witnessed APPROVAL_RECORD_SELRULE_V22 file> --exclude <exclusion file> [--publish-remote <pinned URL> --publish-ref refs/heads/feat/paper-workflow-v2, in composed mode]` (after the collector CLI `beacon_record_drand_v30.py collect … --log _optionA_dev/collection_log.jsonl [--publish-remote … --publish-ref …]` has written and, in composed mode, published the genesis as the history-open commit and the record) (E3(i)) produces `corpus_identity.json` — ordered `tuning_objids` (the first 400 RENDERABLE objects of the seeded order), `holdout_objids` (the next 200), `fresh_validation_objids` (the next 2,000), skipped ranks, seed, beacon-record digest, exclusion digest, pool digest and ordering rule — and its SHA-256 is recorded in the chained seal journal (operation `corpus-identity-freeze`) BEFORE any development brick is fetched or label opened. The driver refuses unsealed or non-matching identities. Tuning and holdout bricks are fetched by the seal-only discipline (§9B.2a–c) after this rule is signed; labels may be used in development on the TUNING set only.
64,66c64,66
< ## 9. Questions for the seats at the V29 complete-package review (STAGED candidate; nothing adopted)
< AUTHORSHIP: all V21/V22 code is Hwao's; the earlier builder field-validation code (codex, 00:13–00:18 on 09-06) no longer participates. Seat A (agy) and Seat B (codex): codex's V21 findings are answered in `V21_GATE_OUTCOME_AND_DIAGNOSIS_20260906.md` (classification), `TRACK1_FAIL_FIRST_RECEIPT_20260906.md` (each repair fail-first), `V22_CANDIDATE_ATTACK_INSPECTION_20260906.md` (codex's coherent attacks against the V22 candidate, historical; the inspection of THIS candidate is `V29_CANDIDATE_ATTACK_INSPECTION_20260907.md` — offline, counter-cases 1, 2 and A still load and are DISCLOSED LIMITS, column one of §3c) and `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md` (their recommended, unadopted closures). Seat B: state your engine's earlier authorship at the top and mark dependent findings.
< 0. THE COMPLETE OBJECT: judge the candidate WITH its stated open boundaries — are they stated exactly, and is anything else open that the inspection does not list? Re-run `coherent_attacks_v29.py` and add counter-cases of your own. 1. THE PRODUCTION PATH: is `PRODUCTION.rederive_seed` a real, executable re-deriver, and does every fixture reach it WITHOUT callback substitution (`assertIs`)? Does `main([...])` under PRODUCTION run `load_identity` for real? 2. THE CONJUNCTION: build your own fabricated identity with a REAL seed (as at the V20 gate) and show which named check refuses it; any acceptance condition read rather than recomputed from committed evidence? 3. Run everything warning-strict — `-W error::ResourceWarning`; 136 tests across the 22 suites named in §3c; the exhibit twice (digest 2160fa75…); the reproduction script's claims; hash every pinned file. 4. HISTORY: can a reset, edit, reorder, deletion or second genesis pass? can two builders both be first? 5. URLS and NONCE: any path where a non-pinned URL is counted, or a nonce is accepted unverified? 6. TEXT: any sentence of §3b/§3c not supported by the pinned bytes? any stale NIST/V16/"once" wording left? 7. THE TRACK-2 DESIGNS: are (a) and (b) sufficient to close C1/C3, is the trust they introduce stated completely, and what would you change before Duho is asked to adopt them? 8. WHAT IS STILL MISSING for approval, as clause text; or state that nothing is.
---
> ## 9. Questions for the seats at the V30 complete-package review (STAGED candidate; nothing adopted)
> AUTHORSHIP: all V21/V22 code is Hwao's; the earlier builder field-validation code (codex, 00:13–00:18 on 09-06) no longer participates. Seat A (agy) and Seat B (codex): codex's V21 findings are answered in `V21_GATE_OUTCOME_AND_DIAGNOSIS_20260906.md` (classification), `TRACK1_FAIL_FIRST_RECEIPT_20260906.md` (each repair fail-first), `V22_CANDIDATE_ATTACK_INSPECTION_20260906.md` (codex's coherent attacks against the V22 candidate, historical; the inspection of THIS candidate is `V30_CANDIDATE_ATTACK_INSPECTION_20260907.md` — offline, counter-cases 1, 2 and A still load and are DISCLOSED LIMITS, column one of §3c) and `TRACK2_PROVENANCE_DESIGNS_UNADOPTED_20260906.md` (their recommended, unadopted closures). Seat B: state your engine's earlier authorship at the top and mark dependent findings.
> 0. THE COMPLETE OBJECT: judge the candidate WITH its stated open boundaries — are they stated exactly, and is anything else open that the inspection does not list? Re-run `coherent_attacks_v30.py` and add counter-cases of your own. 1. THE PRODUCTION PATH: is `PRODUCTION.rederive_seed` a real, executable re-deriver, and does every fixture reach it WITHOUT callback substitution (`assertIs`)? Does `main([...])` under PRODUCTION run `load_identity` for real? 2. THE CONJUNCTION: build your own fabricated identity with a REAL seed (as at the V20 gate) and show which named check refuses it; any acceptance condition read rather than recomputed from committed evidence? 3. Run everything warning-strict — `-W error::ResourceWarning`; 141 tests across the 24 suites named in §3c; the exhibit twice (digest 2160fa75…); the reproduction script's claims; hash every pinned file. 4. HISTORY: can a reset, edit, reorder, deletion or second genesis pass? can two builders both be first? 5. URLS and NONCE: any path where a non-pinned URL is counted, or a nonce is accepted unverified? 6. TEXT: any sentence of §3b/§3c not supported by the pinned bytes? any stale NIST/V16/"once" wording left? 7. THE TRACK-2 DESIGNS: are (a) and (b) sufficient to close C1/C3, is the trust they introduce stated completely, and what would you change before Duho is asked to adopt them? 8. WHAT IS STILL MISSING for approval, as clause text; or state that nothing is.
68c68
< ## 10. Signing and precedence — as V2 §10. Costs, total: 600 development objects ≈ 7.8 GB (now); 2,000 validation objects ≈ 26 GB (only after a holdout pass); one two-seat review on this amendment (V29, if ordered; V16–V28's counted), one on the pipeline amendment (Tier-C V39, APPROVED and installed 13:01 KST), one on the final Tier-C version; render + inference minutes.
---
> ## 10. Signing and precedence — as V2 §10. Costs, total: 600 development objects ≈ 7.8 GB (now); 2,000 validation objects ≈ 26 GB (only after a holdout pass); one two-seat review on this amendment (V30, if ordered; V16–V29's counted), one on the pipeline amendment (Tier-C V39, APPROVED and installed 13:01 KST), one on the final Tier-C version; render + inference minutes.
