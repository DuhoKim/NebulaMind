# V30 COMPLETE-PACKAGE REVIEW — OUTCOME AND CLASSIFICATION — 2026-09-07 04:13 KST

Candidate: `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V30_20260907.md` (preimage 98881c7ecba0ac3bb718983f8534aaa55d325efeb4314d72b31b64b95af2abb9), fresh sandbox selrule_v30_gate_wmmjztjt (4,698 files). Seat A agy (Gemini): **VERDICT: SIGNABLE-AS-PRECOMMITMENT** (dispatched 03:40, ACCESS PROVEN; every item REPAIRED). Seat B codex (gpt-6-astra): first dispatch 03:40 ran the package for ~8 min and was then BLOCKED by the OpenAI provider content filter before writing (no report; wrapper quarantine; stderr retained in the sandbox); second dispatch 03:49 into the SAME sandbox bytes with the brief's free-text wording softened (both briefs retained: `_tmp_v30_brief_first_dispatch.txt`, `_tmp_v30_brief_second_dispatch.txt`) — ACCESS PROVEN, **VERDICT: NOT-SIGNABLE**: ONE FATAL, two MAJOR, one MINOR; 141/24 reproduced, both exhibits, all 35 inspection rows, all 21 exhibit rows, every filed V27–V29 combined row, tracks 8/9 aliased to v10/v14. SPLIT (agy passed all fifteen; codex refused all fifteen). V30 is REFUSED at review; nothing gated, adopted, signed or drawn.

## THE FATAL IS THE LANE'S OWN REGRESSION
**V30-1 [FATAL] — JSON `null` in the history-open file disables the composed history boundary.** `json.loads("null")` returns None without error; `composed_resolver` (driver v14) treats `open_event is None` as "nothing to check" — no local finding, no same-id check, no history continuation — and with no other finding records composed success and ACCEPTS. Codex executed it: a coherently sealed identity with a genuine approval, a published fast-forward history REWRITE then restoration of the original bytes — with the genuine open-event file the complete path refuses HISTORY-CONTINUATION: HISTORY-NOT-AN-EXTENSION; with the open-event file changed to `null` the SAME rewritten history is ACCEPTED (and a normal history with `null` is ACCEPTED). This is a bypass of published-history authentication in the recommended composed mode, introduced by the track-10 resolver (the v13 path raised HISTORY-OPEN-EVENT-MISSING only for an unreadable file and then passed None into a stage that refused it). Class: **UNREPAIRED CONTRADICTION** with §3c, with Q1 Option C's all-events requirement and with the claimed composed repair of A/B. It must be REPRODUCED IN EXECUTED OUTPUT before any repair (the V20 rule) — track 11's run 1.

## The MAJORs — the resolver is not yet the one place that decides
| finding | class | what codex executed | disposition |
|---|---|---|---|
| **V30-2 [MAJOR]** load_identity still decides outside the resolver, before and after it | UNREPAIRED CONTRADICTION (column C) | BEFORE: `verify_witness` performs a real fetch and raises WITNESS-FETCH-FAILED (evidence unavailability, not class 0) before the local sweep — wrong approval repository + failed freeze-witness fetch → WITNESS-FETCH-FAILED; `_drand_modules` can raise VERIFIER-UNAVAILABLE; WITNESS-NOT-PUSHED / APPROVAL-COMMIT-NOT-PUSHED depend on fetched evidence. AFTER: list size/type, disjointness and (if enabled) split checks run after the resolver — one tuning id missing + HTTP 503 → RETRY-EVENTS-UNAVAILABLE, the class-0 defect unchecked. Unnormalized exceptions (JSON, KeyError, path, subprocess) bypass the resolver. Codex supplied the full pre-entry raise inventory with a class assessment per line. | ACCEPTED. Track 11: local list/shape/overlap checks move BEFORE the resolver; the witness fetch and the verifier import become contributed RETRY-UNAVAILABLE findings after a degraded local sweep of both retained events; every stage body wrapped so exceptions are classified (malformed retained input → LOCAL-TERMINAL; I/O and subprocess → RETRY-UNAVAILABLE); an explicit code→class table covering the inventory. |
| **V30-3 [MAJOR]** contributors still suppress higher findings internally; evidence not shared across retrievals | UNREPAIRED CONTRADICTION (column C) | `validate_continuation` (v3 core) returns the open-event outcome before checking the already-fetched history extension: a published rewrite → HISTORY-NOT-AN-EXTENSION with a healthy open retrieval, but RETRY-HISTORY-CONTINUATION: EVIDENCE-UNAVAILABLE when that retrieval was empty — the lower finding suppressed the higher; `authenticate_event` returns UNAVAILABLE for undetermined delivery before evaluating expiry (EXPIRED outranks it). A normal path makes THREE retrievals (B; D's open authentication; D's per-entry checks) and obtained evidence is not accumulated: a conflicting same-id approval copy obtained only in a later retrieval → ACCEPTED, obtained in B → EVENT-FORGED; a strictly earlier open event obtained only in B → ACCEPTED, in D → OPEN-EVENT-NOT-EARLIEST. The 21-row exhibit tests a minimum over manufactured findings, not derivability or evidence collection; A/D on the complete path does not contribute the independent history-rewrite finding. | ACCEPTED. Track 11: ONE evidence snapshot per invocation applied to every predicate; the history stage rewritten as a finding COLLECTOR (`validate_continuation_v11` returns every derivable finding — remote head, genesis-only, open-event delivery/authentication, extension/batch, per-entry publication — none suppressing another); expiry evaluated before unavailability; ACCEPT only when every required stage ran and reported ACCEPT. |
| **V30-4 [MINOR]** offline vocabulary overstated | ACCEPTED DEFECT (text) | offline returns REDERIVE-RETRY for unverifiable seed bodies and can raise WITNESS-FETCH-FAILED; "offline has no retry vocabulary" and "every offline raise is class 0" are false; IDENTITY-WITNESS-COMMIT-UNDETERMINED is the named undetermined-DELIVERY refusal, not the only unavailability disposition; the seed mismatch is class 4. | Track 11 text. |

Codex also judged: the order is TOTAL over the eight classes but not a total classifier of program outcomes (resolve assumes a nonempty list; ACCEPT is implemented separately); EXPIRED above DERIVED-TERMINAL and RETRY-UNAVAILABLE above RETRY-INCOMPLETE are defensible policies needing explicit mappings; receipt failures are all mapped to EXPIRED regardless of cause (must be named as a policy or classified by cause); PENDING-PUSH as class 4 must be named as "terminal for this invocation, remedy is publication"; the seed-cryptography boundary between class 0 and 4/5 must be spelled out. Blanc's items: 1 REPAIRED as a class list (coverage/overlap need qualification); 2 NOT REPAIRED (V30-1/2/3); 3 REPAIRED as the exhibit, NOT as complete-path/retrieval-order proof; 4 REPAIRED. The 24 actual stage-pair combinations (all six pairs, both feed orders, ABCD vs DCBA) were invariant; the passing winners do not establish "every finding recorded". Column A qualifications judged honest; none licenses V30-1/2/3 into the limits column; section F's "none known" is superseded. Covenant executed and agrees; the fabricated first receipt exercises the disclosed trusted step; Q1 C remains the sound recommendation once repaired and "is not accurately described as fully working today while V30-1 exists".

## Codex's clause texts (verbatim; to be carried into V31 and the code)
"In composed mode, every required retained event must be a valid PushEvent object. Missing, unreadable, null and other invalid values produce a named LOCAL-TERMINAL finding; none disables history verification. Acceptance requires affirmative success of approval authentication, seed re-derivation, published-history continuation and required per-entry publication checks. No required stage can be skipped into acceptance."
"The precedence order applies to the entire load_identity conjunction, including freeze-witness availability, local list shape/disjointness and enabled split checks. Unavailable evidence is not a proven local inconsistency. Every independently derivable higher-priority finding is collected before a lower-priority result is emitted, including within history and authentication helpers. All live evidence obtained during an invocation is evaluated for every applicable contradiction and ordering predicate under an explicitly defined snapshot/reconciliation policy; changing retrieval order cannot suppress an obtained contradiction."
"The mapping explicitly distinguishes local integrity, retained cryptographic validity, remote-derived publication findings, receipt refusal causes, evidence loss and temporary unavailability. Offline retains IDENTITY-WITNESS-COMMIT-UNDETERMINED for unresolved approval delivery and separately exposes its existing seed-re-derivation and witness-availability refusals; no broader claim that all offline refusals are class 0 is made."

## Next (routine repair under existing authority; the FATAL reproduced first)
Track 11: run 1 = reproduce V30-1 in executed output on driver v14 (null open file → ACCEPTED; rewritten history + null → ACCEPTED); then successors v11/v15: valid-PushEvent requirement with LOCAL-TERMINAL findings for null/invalid; ACCEPT only when every required stage ran and reported ACCEPT; local checks before the resolver; witness fetch / verifier import as contributed retries after a degraded local sweep; exception normalization with an explicit code→class table; one evidence snapshot; history stage as a finding collector; expiry before unavailability; receipt and PENDING-PUSH policies named; offline wording corrected; codex's stage-pair and retrieval-order cases as fail-first tests; V31 text; review.
