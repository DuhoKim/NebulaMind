# V31 COMPLETE-PACKAGE REVIEW — OUTCOME AND CLASSIFICATION — 2026-09-07 05:00 KST

Candidate: `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V31_20260907.md` (preimage f94b45e626ec95f1d2a60da783eb23c70f02394bbbe2be7389583013dd00cc49), fresh sandbox selrule_v31_gate_bva0owfg (4,719 files; both seats ACCESS PROVEN; dispatched 04:32, both exited by 04:59 KST). Seat A agy (Gemini): **VERDICT: SIGNABLE-AS-PRECOMMITMENT** — everything REPAIRED. Seat B codex (gpt-6-astra): **VERDICT: NOT-SIGNABLE** — NO FATAL; four MAJOR, one MINOR; the FATAL repair confirmed ("I independently reproduced both acceptances on retained v14 and both refusals on v15; the repair is real"); 147/26 reproduced; the five V30 constructions take their expected dispositions; the three-defect example (rewrite + wrong open repository + empty feed) collects every finding and class 0 wins. SPLIT (agy passed all sixteen; codex refused all sixteen). V31 is REFUSED; nothing gated, adopted, signed or drawn.

## One message in four findings: the text's claims are stronger than the collector
| finding | class | what codex executed | disposition |
|---|---|---|---|
| **V31-1 [MAJOR]** a failed S1 still suppresses independent checks | UNREPAIRED CONTRADICTION (column C) — with the carried V29/V30 clauses | any S1 refusal (e.g. only `git fetch origin` failing → WITNESS-FETCH-FAILED) leaves S2–S5 NOT RUN: a conflicting same-id live event (FORGED, obtainable) is never contributed, zero Events API requests; a sealed identity with schema_version 'WRONG' + witness-fetch failure → WITNESS-FETCH-FAILED, the local schema defect never contributed; a rewrite + fetch failure → the remote-history finding never contributed; S0 launches `git rev-parse` BEFORE its approval precheck, so a git launch OSError + wrong repository → IO-UNAVAILABLE, no EVENT-INCONSISTENT; `_P()` is called outside the wrapper — an unavailable provenance module raises a raw ImportError | ACCEPTED. Track 12: every independently readable input / obtainable source contributes even when another prerequisite fails; the collector records BLOCKED checks and why; the local sweep must not depend on launching git; the helper import inside the wrapper. |
| **V31-2 [MAJOR]** a failed later page erases obtained contradictions | UNREPAIRED CONTRADICTION (column C) — with the snapshot clause | `retrieve_events` accumulates pages in a local list and raises on a later HTTP 503 / malformed page, dropping page one — which held a valid same-id contradiction: RETRY-EVENTS-UNAVAILABLE instead of EVENT-FORGED | ACCEPTED. Track 12: one logical retrieval keeps every obtained page with provenance; a later page's failure contributes unavailability WITHOUT discarding earlier events; partial retrieval never establishes absence, expiry or acceptance. |
| **V31-3 [MAJOR]** helpers still stop before higher findings | UNREPAIRED CONTRADICTION (column C) | the per-entry loop stops at the first missing publication (entry 1 EVIDENCE-INCOMPLETE) while a later batch proves entry 2 HISTORY-PUBLICATION-BATCH (class 4, derivable, absent); the extension loop breaks; ordering evaluated only if the loop completes; `authenticate_event` returns EXPIRED/UNAVAILABLE for an undetermined retained delivery before the absent-only same-commit arm (a distinct-id genuine event demonstrably delivering the commit → FORGED derivable, UNAVAILABLE returned); the standalone `validate_continuation_v11` returns on remote failure before the open event's local check (the composed path catches it in S0; the two paths must be distinguished); a scalar `payload` open event blocks the rewrite finding | ACCEPTED. Track 12: loops evaluate every independently checkable commit/publication/ordering relation, preserving findings already derived; both contradiction arms before any availability disposition; the standalone path shares S0's local sweep or is labelled as not enforcing clause (1). |
| **V31-4 [MAJOR]** classification not total; exception type ≠ provenance | UNREPAIRED CONTRADICTION (column C) | SPLIT-INPUTS / SPLIT-NOT-REPRODUCED (enabled split verification) are missing from the table → flagged class 0 (a real raise exercised); a new EVENT-NEW-UNKNOWN code is silently class 0 via the family prefix, so "every unknown code is flagged" is false; a PushEvent with scalar `payload` from the REMOTE feed → class-0 MALFORMED-RETAINED-INPUT (it is remote evidence); a receipt whose `origin` is a list raises inside `verify_events_receipt` → class-0 MALFORMED-RETAINED-INPUT instead of the class-3 receipt policy | ACCEPTED. Track 12: an explicit allowlist of every actual raise (extracted from the code, including SPLIT-*), family prefixes validated against it; malformed remote evidence classified by provenance (class 5); receipt exceptions normalized to the receipt policy; unknown-code handling defined precisely. |
| **V31-5 [MINOR]** bookkeeping and wording overstate | ACCEPTED DEFECTS | `ran` includes a stage whose body raised (so "NOT RUN" is never recorded — with a null open file S5 says its inputs prevent execution yet `_stages_run` lists it); `_findings` omits reasons and collapses distinct history refusals to one code; receipt wording: run 1 shows the first null load only (the rewrite + null subcase was not reached by the lane's run 1 — codex reproduced both), and "run 2b" was 10 passes + 1 wording failure with run 2c the re-run | ACCEPTED. Track 12: stage states attempted / completed / blocked / successful; findings retain code and reason; receipt wording corrected in this outcome and in the next receipt. |

Codex confirmed: the FATAL is repaired; the five V30 constructions; the three-defect collection; the named policies "intelligible" (policies, not consequences of a theorem); the covenant, receipt trust step and Option C stance unchanged; 147/26; pins and preserved items as before.

## Codex's clause texts (verbatim; to be carried into V32 and the code)
"Each independently readable input and independently obtainable evidence source contributes findings even when another prerequisite fails. A failed freeze-witness fetch or verifier import does not prevent local integrity checks, obtainable approval/open-event contradiction checks, seed verification whose retained inputs are available, or independently obtainable history checks. The collector records which individual checks are blocked and why; a blocked check never permits acceptance. Local retained-event type/repository/ref checks do not depend on launching git."
"One logical event retrieval accumulates every successfully obtained page with provenance. A later page's transport, HTTP, schema or JSON failure contributes unavailability without discarding earlier events. Every obtained event is evaluated for applicable affirmative contradictions and ordering predicates. Partial retrieval never establishes complete presence requirements, absence, expiry or acceptance merely because an unrequested or failed page may contain missing evidence."
"History validation evaluates every independently checkable commit/publication and ordering relation despite an earlier missing or invalid entry, preserving all findings already derived if another check fails. Authentication evaluates both defined contradiction arms before a lower-priority availability disposition whenever their inputs are available. The standalone and composed paths share these obligations; unavailable remote state does not excuse locally decidable open-event mismatches."
"Classification is explicit for every actual enabled refusal, including split verification. Malformed remote evidence is classified by its remote provenance, not as malformed retained input. All receipt-verification failures, including malformed nested values and exceptions, follow the stated receipt-loss policy. Unknown-code handling is defined precisely, including how code families are validated. Diagnostics distinguish attempted, completed and blocked checks and retain each finding's specific code and reason."

## Correction to the track-11 receipt's wording (V31-5), recorded here
Run 1 showed the FIRST null load only; the rewrite + null subcase was not reached by the lane's run 1 (the test stops at its first failed assertion) — codex reproduced both acceptances on v14 and both refusals on v15 (seat B, category 2). Run 2b was 10 passes and one wording failure; run 2c re-ran that test to a pass; "all 11" is that combined evidence.

## Next (routine repair under existing authority)
Track 12: implement codex's four clauses as stated, with the text's claims cut down to exactly what the code does (an explicit table of which checks are independent of which prerequisites, and which are blocked by what); fail-first with codex's V31 constructions; V32; review.
