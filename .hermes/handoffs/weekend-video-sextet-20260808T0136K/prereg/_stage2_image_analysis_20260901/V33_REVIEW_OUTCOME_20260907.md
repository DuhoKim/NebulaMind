# V33 COMPLETE-PACKAGE REVIEW — OUTCOME (2026-09-07 08:33 KST). BOTH SEATS COMPLETED. **NOT-SIGNABLE.**
Candidate `OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V33_20260907.md` sha256 f294cb55ac4c87daf156d2b9875d9bbd40434d93908f64c536cb1dbc751bff15 (PINNED; not edited — every repair goes into V34). Nothing approved, signed, frozen, adopted or run. V15 (fdd9eedd…) remains the operative rule; Tier-C V39 remains the installed pipeline identity.

## HOW EACH SEAT WAS READ — EXIT ARTEFACTS FIRST (the V32 rule, applied)
| seat | engine | wrapper | exit artefacts | completed? | report | verdict |
|---|---|---|---|---|---|---|
| A | agy / Gemini | 17482, dispatched 08:11:31, exited 08:27–08:28 KST, rc=0, ACCESS PROVEN | **stderr 0 bytes** (no timeout line); stdout 5,638 bytes | YES — normally completed | `AGY_SELRULE_V33_SEATA.md` sha256 9640093ca7dce9d155505d4e15f9ceca69ab4de1ae45aae7a1fa8426bf4451f1, written 08:18:02 | **SIGNABLE-AS-PRECOMMITMENT** |
| B | codex-cli 0.153.4 / gpt-6-astra (shim) | 22010, dispatched 08:12:05, exited by 08:31:29 KST, rc=0, ACCESS PROVEN | stdout 19,599 bytes; stderr 911,158 bytes ending `VERDICT: NOT-SIGNABLE` / `tokens used 211,352` | YES — normally completed | `CODEX_SELRULE_V33_SEATB.md` sha256 75fed23994d8fefe… (= its stdout), written 08:28:48 | **NOT-SIGNABLE** (1 FATAL, 4 MAJOR) |

MY OWN DETECTOR WAS WRONG ABOUT SEAT B, AND I CHECKED BEFORE BELIEVING IT. `_tmp_v33_waiter.sh` greps a seat's stderr for the substring `timeout waiting for response` and printed "TIMED OUT — provisional, NOT a verdict" for seat B. That was a FALSE POSITIVE: the phrase occurs 8 times inside seat B's 911 KB stderr because the seat echoed the package text, which QUOTES the V32 timeout record. Seat A's genuine V32 timeout was a stderr of exactly 36 bytes equal to `Error: timeout waiting for response`; seat B's stderr is 911 KB ending in its verdict, and its wrapper exited rc=0 with ACCESS PROVEN. SAME CLASS OF DEFECT AS THE ONES BLANC HAS BEEN NAMING ALL NIGHT: a control that matches a phrase ANYWHERE cannot distinguish the artefact from a document about the artefact. REPAIRED in the waiter: the timeout test is now whole-artefact — stdout empty AND stderr's entire content equal to the timeout line (`$(cat …) == "Error: timeout waiting for response"`), never a substring anywhere.

## SEAT A — A COMPLETED SEAT, BUT A READING-LEVEL REVIEW (evidence weight; `V33_SEATA_EXECUTION_WEIGHT_20260907.md`)
Its specifics are grounded (L-OFF / L-COV / L-RCPT / L-INH / L-AVAIL, the 7.8 / 26 GB figures, `rederive_seed`'s offline default all genuinely appear in the pinned text — checked). But dispatch 08:11:31 → report written 08:18:02 = 6 min 31 s, while the 32-suite aggregate takes ~15 min under the pinned interpreter (controls alone ~5 min 55 s), so it CANNOT have executed the package; its §9 is hedged accordingly ("run successfully WHEN the complete sandbox environment … is supplied"). ITS VERDICT COUNTS AS ONE COMPLETED SEAT'S OPINION; NO EXECUTION CLAIM RESTS ON IT. Seat A declared every V32 item repaired and no unrepaired contradiction; seat B, executing, found four. Where they conflict, the executed evidence governs, and I verified it myself.

## SEAT B'S FINDINGS — EVERY LABEL SWEPT, EACH VERIFIED BY ME IN THE CODE (not taken at face value)
| label | claim | my independent check | class | disposition |
|---|---|---|---|---|
| **B-1 [FATAL]** | NSD still fails at actual-input boundaries (four executed cases) | **CONFIRMED in all four**, see rows 1a–1d | UNREPAIRED CONTRADICTION | REPAIR in V34 |
| 1a | `_time_checks` returns on the first parse failure, hiding an independently parseable second input | CONFIRMED — `run_configurations_v17.py:485–489`: one `try` chain, each branch `raise`s; the second time input is never parsed | contradiction of "the first failure never hides the next" | each time input its own sub-check |
| 1b | a retained-data `AttributeError` is classified with the ENCLOSING stage's git/remote label, and delivery aborts authentication before the independent same-id check | CONFIRMED — `stage()` classifies by the stage's declared source, so a retained-input exception inside a remote-labelled stage is filed as remote | contradiction of "classified at ITS input boundary" | classify by the failing input, not the stage |
| 1c | an `OSError` on the second committed blob stops the whole extension loop; `_prev` is never installed, so `working_tree` defaults it to empty bytes and falsely reports PENDING-PUSH for 708 already-published bytes; no check is blocked and S5 is marked completed | CONFIRMED — `provenance_designs_v13.py:499` sets `info["_prev"]` only at the end of `extension_checks`; `:520` reads `info.get("_prev", b"")` | contradiction — and it CORRUPTS A FAILURE STATE the text advertises | per-blob sub-checks; absence of `_prev` must block, never default |
| 1d | `PermissionError` on the adoption file leaves `adoption-identity` neither evaluated, blocked nor represented — S0h's named-block handler catches only `DataIntegrityFail` | CONFIRMED — `run_configurations_v17.py:503–512`: the `except DataIntegrityFail` blocks `adoption-identity`; any other exception escapes before `sub(...)` runs | contradiction of "blocked checks are named" | block on ANY failure to obtain the adopted digest |
| **B-2 [MAJOR]** | the table is neither exact nor completely verified: the mutation control (remove `EVENT-FORGED` from `approval-live.codes`) passes all four table tests because that code is selected through `m[o]`, not a literal `add(...)`; `approval-live`'s needs omit `git-launch` though its ancestry branch uses git; sub-check findings report `stage='sub'` while the table names S0e/S0f; `history-remote` also reports from S0b-root | **CONFIRMED** — `m = {...}; cls, code = m[o]` at `:543,551` is invisible to the regex scanner; `sub()` files findings with the literal stage `"sub"`; the mutation control is a legitimate fail-first exhibit | UNREPAIRED CONTRADICTION ("every site / every code / every stage / every prerequisite" is false) | a real registry (contribution sites enumerated, conditional dependencies, owning stages), not a regex over source text |
| **B-3 [MAJOR]** | the built approval-receipt SUCCESS path refuses: with `authenticate_event` returning EXPIRED and the receipt verifier returning `(True, valid)`, S3a falls through to `m[o]` → `KeyError: 'EXPIRED'` → MALFORMED-REMOTE-EVIDENCE | **CONFIRMED** — `run_configurations_v17.py:545–551`: the EXPIRED branch adds findings on the two failure paths but has NO `return` on success, then executes `cls, code = m[o]` where `m` has no `EXPIRED` key. The advertised Q1-Option-A′ receipt substitution cannot complete | UNREPAIRED CONTRADICTION (a built mechanism the text presents as working) | return on a verified receipt; a test for the success path |
| **B-4 [MAJOR]** | the differential controls are weaker than the property: one planted defect per check (identity-file = deletion only, adoption-file = deletion only, per-entry = batching); `by_check` reduces findings to code sets, discarding class, stage, reason, multiplicity and order — so HISTORY-NOT-AN-EXTENSION and the false PENDING-PUSH are indistinguishable; the baseline comes from the implementation, so an incomplete baseline can become the oracle | **CONFIRMED** — `test_track13_nsd_table.py`: `DEFECTS` has one recipe per check; `by_check` builds `{check: {code}}`; `baseline()` records the implementation's own output | my controls' reach was overstated | compare class/code/check/stage and reason; multiple failure variants per input; an oracle that does not come from the implementation |
| **B-5 [MAJOR]** | the candidate text and the evidence do not name the same package: a literal `{NCTRL}` survives; the introduction still says "run 2 on the successors: 16 OK" though that run is the OPEN historical gap; the runner/gate hashes and "5 tests" are obsolete; the inspection prose still says 105+6 controls; the aggregate's track-8–12 blocks import v11/v15 and v12/v16, so they do not evidence "15 earlier tests on the successors"; the manifest's spec row carries the pre-relocation hash | **CONFIRMED, every part** — `{NCTRL}` ×1 (my f-string bug: the table block is a plain string); the "16 OK" sentence is present and contradicts §1 of the receipt; text pins runner `431d5ab7…` / gate `aca5f939…` vs actual `0a92eb4b…` / `ac844f7f…` and "5 tests" vs the staged 8; the inspection says "105 pairs"; the spec row was already filed as 103 + 1 in `V33_STAGED_EVIDENCE_PROVENANCE_20260907.md` (Blanc 08:19/08:27) | UNREPAIRED CONTRADICTIONS of accounting | V34 regenerates every pin AFTER the code freezes, and the text's history sentence is corrected to the open gap |

NONE OF THESE BECOMES A DISCLOSED LIMIT. Every one is a text≠code contradiction and is repaired fail-first in V34 (Blanc's standing rule: contradictions are repaired, never disclosed).

## WHAT SEAT B CONFIRMED (recorded so the repair does not undo it)
V32-2 REPAIRED including the completion control; V32-4 REPAIRED (runner + gate v3.1; its own fresh exit-17 child through the real runner refused by v3, accepted by v2; the 8 gate tests pass); V32-5's dynamic families and blob-equality wording REPAIRED. The retained RUN2 portable copy **independently passes gate v3: 32 suites, 369 tests, every child EXIT 0, matching complete outputs**; RUN1 certifies nothing. The fail-first receipt's per-run classification is supported by the retained logs (14+2, 19+91, 111/116 KeyError('check') as new-interface, 2d's 16 of 111, and the final 16/116/4); v3 has no behavioural standing against v16, as the receipt states. The three states behave as described; the 35-row attack table reproduces after normalising run-dependent commit ids; the preserved items (400/200/2,000; 380/190/1,900; the 0.70 Wilson bar; the exclusions; E5(d); blindness; a prospective round; one holdout; `verify_split` unadopted) are all intact; the estimator suite 10/10 and the validation-gate suite 14/14 pass; the property exhibit reproduces digest 2160fa75…. V32 had ONE completed seat and seat A's V32 report is provisional — seat B read that record correctly.

## A STAGING DEFECT THIS REVIEW EXPOSED (mine, not the seat's)
Seat B's FRESH aggregate in the sandbox ran 32 suites / **365** tests with **three suites EXIT 1** (gate v3 → FAIL, correctly): both driver suites errored in `test_real_builder_output_loads_through_load_identity` and the original builder suite errored in `setUpClass`, all three from the ABSENT `scratch/survey-bricks-dr9-north.fits.gz` (and `validation_bricks/_bricks_without_r_coverage.txt`), which my rsync excludes drop. So the sandbox I gave the reviewers cannot reproduce the 369-test aggregate, while the brief invites them to re-run it. That is a real gap between what the package claims and what the staged copy supports — an additional missing-input limitation beyond the disclosed guarded-pool/GZ1 one. V34's dispatch either stages those inputs or the text declares exactly which suites a sandbox cannot run and why. Seat B's own log: `ee535efa…`; its evidence lives outside the candidate tree (`/tmp/codex_v33_seatb_*`).

## DISPOSITION
V33 is NOT-SIGNABLE. Both seats completed; the one that executed refused. The repairs listed above become V34 (successor files only; V33 and every file named in its brief stay byte-frozen). Seat A's SIGNABLE is recorded as a completed seat's reading-level opinion and is NOT evidence that the code does what the text says — seat B's executed counter-cases show it does not.
