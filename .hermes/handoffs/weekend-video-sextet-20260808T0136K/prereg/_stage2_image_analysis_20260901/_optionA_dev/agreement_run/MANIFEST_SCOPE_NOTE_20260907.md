# A1 manifest scope — 2026-09-07

The owner decision in `A1_MANIFEST_SCOPE_DECISION_20260907.md` establishes two registers because the input freeze must be checkable by a person.

**INPUT_MANIFEST_A1_CORE.json** identifies the run's data, our code and the prose now consumed by the readiness cross-check: **30 real files and 5 future binding groups** after V49. These comprise seven data inputs, twelve source modules including the real `medium_perturbation.py` and `runtime_binding.py`, eight observed bytecode caches, `env_lock.json`, one runtime-register reference and one A1 obligation source. Each digest is recomputed from final readable bytes. The schema remains `A1-INPUT-CORE-DRAFT-1`; `files` is authoritative and `inputs`/`code` are matching lookup aliases. Every row is verified, including render configuration and cached code. Required code/input aliases and discoverable standard caches cannot be omitted. No installed-library tree is expanded.

**RUNTIME_PINS_A1_CORE.json** retains the three compact artifact pins, versions, invocation and scientific lock and adds mandatory `A1-RUNTIME-REPRESENTATION-1` evidence: 1,480 individually pinned readable import/native artifacts, observed module origins/cache candidates, and 423 dyld image identities, including 346 confirmed shared-cache residents. `RUNTIME_REPRESENTATION_20260907.md` names each mechanism and the offline reproduction command. All evidence is checked at stage entry and before a completed stage record. Historical inventories are not authorities; no installed-library tree sweep is required. The BLS chain/key, authentication calls and host/refetch checks are unchanged.

`INPUT_MANIFEST_A1.json`, `RUNTIME_DEPENDENCIES_A1_FINAL.json` and `RUNTIME_LIBRARY_BYTES_A1_FINAL.md` are retained, unchanged and unrenamed, as **inventory artefacts. They are NOT the manifest.** Their historical 1,529 real / 359 placeholder rows and 1,521 runtime files / 422 loaded images are not freeze-readiness operands.

`ready_for_input_freeze` is **true**, computed by `run_path.input_readiness` after the ordered V49 implementation, A1 revision and digest re-pin. Every real CORE entry must match, no INPUT-due placeholder may remain, the manifest must carry every declared id exactly once with no undeclared ids, every present obligation must be resolved, and A1 statuses must agree. The additional `runtime_representation_evidenced` check requires the runtime obligation evidence pin to match the registered runtime input and calls the actual environment verifier. Missing or mismatched evidence cannot be overridden by resolved prose or TRUE flags. All three declared preparation obligations now satisfy the predicate. COMPLETE still means every declared member is present; it is not a synonym for resolved. A recorded FALSE still stops the consumer, and recorded checks must exactly match the recomputed checks. Counts and stored readiness values are never predicate inputs.

`OBLIGATION_REGISTRY` independently defines the three current preparation obligations. `_a1_obligations` reads the actual A1, enforces its declared full-file digest, and checks its mapped status statements. Missing/extra/duplicate ids, contradictory statuses and unfamiliar A1 revisions still refuse. The owner explicitly authorized V49 to revise only runtime representation after implementing and evidencing it, then re-pin `A1_REVIEWED_SHA256`. That historical constant name does not confer an independent review verdict. Older A1 inventory and owner-attachment digests remain historical; the final current pins and both A1 digests are in `_tmp_v49_runtime_REPORT.md`. No five-stage future group or adoption gate changed.

The real producer is `_optionA_dev/agreement_run/medium_perturbation.py`, unchanged from V44. `RunPath._render` calls it only for tuning, passing numeric source planes and losslessly checked TAN geometry before opening labels. It computes both fixed arms with the existing components. The driver streams objects, combines their actual receipts and retains `OUT/tuning.medium.json` in tuning outputs, so existing downstream output/W bindings include it. Missing published checksums retain an unresolved identity; they do not become zero-flip evidence. Unsupported/distorted or lossy WCS conversion refuses. The historical approval recorder is not called. Real tuning evidence remains future-stage evidence without an invented current digest.

The pins establish individually checked on-disk byte identity for readable artifacts and separately checked platform identity for cache-only images. The latter is the active dyld cache UUID, kernel OS build, process/cache architecture and per-image loaded path, LC_UUID, CPU type/subtype and confirmed cache residency; it is not per-image byte binding. **TOCTOU limit:** extension modules are hashed by `read_pin` and later imported with no lock spanning the two operations. Even the retained immediate pre-NumPy-import recheck evidences disk bytes at check time, not bytes the loader used or an extension already loaded. It narrows the interval without eliminating it or attesting relocated pages. Disabled bytecode writing does not disable reading existing caches. CORE retains its local cache/source-code equality checks; runtime evidence pins observed third-party source/cache files as well. The exact fileless-module, embedding-caller, custom-loader, native-resource-read and memory-mutation limits are stated in A1 and `RUNTIME_REPRESENTATION_20260907.md`. Additional observed modules/images must match the register or refuse. This is not a native-loader sandbox, complete possible-path proof, scientific correctness or cross-process bit-exactness guarantee.

The five placeholder groups cover the prospective input anchor, beacon evidence, draw records, staged access/data/reused outputs, and winner freeze. Each names its producing stage and concrete consumer/template. They are obligations, not predicted file counts, and must expand to individual paths and real digests before the corresponding read. They do not block pre-seed INPUT readiness. OUT and operator-supplied paths remain undetermined.

CORE remains an authoring register, not adoption, an anchor or permission to run. V45/V46/V47 evidence remains historical. Ordered V49 work, fail-first/after/kit results, final pins and the evidence-backed readiness result are recorded at the lane root in `_tmp_v49_runtime_REPORT.md`.