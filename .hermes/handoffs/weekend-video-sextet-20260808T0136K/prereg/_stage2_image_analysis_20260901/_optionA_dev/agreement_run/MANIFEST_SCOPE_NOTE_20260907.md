# A1 manifest scope — 2026-09-07

The owner decision in `A1_MANIFEST_SCOPE_DECISION_20260907.md` establishes two registers because the input freeze must be checkable by a person.

**INPUT_MANIFEST_A1_CORE.json** identifies the run's data, our code and the prose now consumed by the readiness cross-check: **29 real files and 5 future binding groups** after V47. These comprise seven data inputs, eleven source modules including the real `medium_perturbation.py`, eight observed bytecode caches, `env_lock.json`, one runtime-register reference and one A1 obligation source. Each digest is recomputed from final readable bytes. The schema remains `A1-INPUT-CORE-DRAFT-1`; `files` is authoritative and `inputs`/`code` are matching lookup aliases. Every row is verified, including render configuration and cached code. Required code/input aliases and discoverable standard caches cannot be omitted. No installed-library tree is expanded.

**RUNTIME_PINS_A1_CORE.json** records three artifact digests: the invoked interpreter and the two explicitly checked NumPy extensions. The consumer verifies each artifact, the interpreter binding, the extensions' loaded paths, Python/py_ecc/NumPy/Astropy versions, every recorded invocation setting, the scientific lock and its independently recorded enforced values. Exact PYTHONPATH, disabled bytecode writing, all five thread settings, interpreter identity and py_ecc 8.0.0 remain required. The BLS verification call, pinned chain/key and additional two-host/live-refetch checks are unchanged. Checks occur at stage entry and before a completed stage record.

`INPUT_MANIFEST_A1.json`, `RUNTIME_DEPENDENCIES_A1_FINAL.json` and `RUNTIME_LIBRARY_BYTES_A1_FINAL.md` are retained, unchanged and unrenamed, as **inventory artefacts. They are NOT the manifest.** Their historical 1,529 real / 359 placeholder rows and 1,521 runtime files / 422 loaded images are not freeze-readiness operands.

`ready_for_input_freeze` is **false**, computed by `run_path.input_readiness` from rehashed CORE pins, the code's declared obligation registry, and the pinned reviewed A1 prose. Every real entry must match, no INPUT-due placeholder may remain, the manifest must carry every declared id exactly once with no undeclared ids, every present obligation must be resolved, and A1's mapped ids/statuses must agree. COMPLETE means every declared member is present; it does not mean resolved. The current list is complete: `CORE_CONSUMER_RECONCILIATION` and `MEDIUM_CURRENT_PREPARATION` are resolved; `RUNTIME_REPRESENTATION` is unresolved. Its unresolved state alone makes the computed predicate FALSE. V46 omitted that third obligation and therefore reported premature TRUE. A recorded FALSE still stops the consumer; a recorded TRUE cannot override the computed predicate. The consumer retains its exact recorded-check comparison. Counts and stored readiness values are never inputs to the computation.

`OBLIGATION_REGISTRY` in `run_path.py` defines each id's meaning independently of the manifest. `_a1_obligations` reads `AGREEMENT_RUN_AMENDMENT_A1_20260907.md` itself, requires its independently declared reviewed digest, and checks the mapped statements for each id/status. Missing, extra or duplicate manifest entries fail by name; differing statuses report `A1-OBLIGATION-MISMATCH`. Any unfamiliar A1 revision, including an additional obligation outside the mapped excerpts, fails `A1-OBLIGATION-SOURCE-CHANGED` even if its CORE pin is updated. This is a bounded interpretation of the reviewed prose, not arbitrary natural-language parsing. A subsequent A1 revision requires an explicit reviewed mapping/digest update. The original A1 is unchanged in this bounded repair; its historical TRUE statement and prior CORE/runtime digests are stale. FATALs 3/4 and the owner's `A1_REVIEW3_OUTCOME_20260907.md` govern this correction. The cross-check concerns current preparation obligations, not the five later-stage groups or historical digest statements.

The real producer is `_optionA_dev/agreement_run/medium_perturbation.py`, unchanged from V44. `RunPath._render` calls it only for tuning, passing numeric source planes and losslessly checked TAN geometry before opening labels. It computes both fixed arms with the existing components. The driver streams objects, combines their actual receipts and retains `OUT/tuning.medium.json` in tuning outputs, so existing downstream output/W bindings include it. Missing published checksums retain an unresolved identity; they do not become zero-flip evidence. Unsupported/distorted or lossy WCS conversion refuses. The historical approval recorder is not called. Real tuning evidence remains future-stage evidence without an invented current digest.

The pins establish named on-disk byte identity and agreement of reported versions/settings at check time. **TOCTOU limit:** extension modules are hashed by `read_pin` and later imported by Python with no lock between. Pins evidence what was on disk at check time, not what the loader used or what is already loaded. The consumer re-verifies both named NumPy extension digests immediately before the imports that can open them. This narrows the interval but does not remove it or attest loaded bytes. `-B` and `PYTHONDONTWRITEBYTECODE=1` prevent writes, not reads of existing caches. CORE checks recorded local caches and compares their decoded code objects against compilation of pinned source. This is not an import sandbox or attestation of already executing code: custom import loaders, changes between checks and arbitrary in-memory mutation are outside the guarantee. Package version strings do not pin or authenticate all package bytes. Only the two named NumPy extensions have explicit artifact and loaded-path checks; dynamically resolved Astropy/cytoolz extensions, shared libraries, dyld images, the Python framework/stdlib, OS and third-party bytecode remain outside that byte identity. No dependency closure, cross-process bit-exactness or scientific correctness is claimed. This documentation and the pre-import recheck do not resolve A1's outstanding runtime-representation work.

The five placeholder groups cover the prospective input anchor, beacon evidence, draw records, staged access/data/reused outputs, and winner freeze. Each names its producing stage and concrete consumer/template. They are obligations, not predicted file counts, and must expand to individual paths and real digests before the corresponding read. They do not block pre-seed INPUT readiness. OUT and operator-supplied paths remain undetermined.

CORE is an authoring register, not adoption, an anchor or permission to run. V45/V46 retain their historical evidence. V47 deletion/divergence tests, current readiness and final digests are reported in `_optionA_dev/agreement_run/_tmp_v47_readiness_REPORT.md`.
