# Build task (codex): make the beacon-record path VALIDATE, not merely require, and prove it end to end. Python 3.9 stdlib + numpy + astropy only. NO network calls in tests (mock `fetch_beacon.fetch`). Do not touch the estimator (`fourier_chirality.py`) or `run_configurations.py` except where item 3 says. Work only inside _optionA_dev/. Write RESULT.md in _optionA_dev/beacon/ when done, first line = PASS/FAIL of the full fixture run with counts.

Read first: OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V10_20260905.md §3b and E3(i); CODEX_SELRULE_V10_SEATB.md items 1, 3, 4, 7 (your own findings); V10_GATE_OUTCOME_AND_STOP_20260905.md "The complete repair, specified".

1. `_optionA_dev/corpus_identity/build_corpus_identity.py`: production seeding takes `--beacon-record R --rule-sha256 D --signature-statement S --exclude X --out O` and REFUSES (SystemExit with a named token) unless ALL hold:
   - R has every field fetch_beacon.py writes, including `rule`; R.rule_sha256 == D (64 hex); R.signature_statement_sha256 == SHA-256 of S's bytes; S's bytes contain D and R.T_sign.
   - T_pulse recomputed from R.T_sign by fetch_beacon.pulse_time equals R.T_pulse (import fetch_beacon from ../beacon).
   - NIST: R.source == "NIST-beacon-2.0"; R.pulse.timeStamp == T_pulse + ".000Z" form; int(R.pulse.statusCode) == 0; str(R.pulse.chainIndex) == "2"; R.uri == R.pulse.uri and matches ^https://beacon\.nist\.gov/beacon/2\.0/chain/2/pulse/\d+$; R.pulse.outputValue is 128 hex and equals R.seed_hex (case-insensitive).
   - drand: R.source == "drand-mainnet-default"; R.chain_hash == fetch_beacon.DRAND_CHAIN; R.round == fetch_beacon.drand_round(T_pulse) == R.pulse.round; R.pulse.randomness is 64 hex and equals R.seed_hex; R.fallback_condition present; R.fetched_utc >= T_pulse + 24 h.
   - R.body_sha256 is 64 hex (cannot be recomputed without the body — say so in a comment; it is re-verifiable by third-party re-fetch).
   The identity JSON records: beacon_record_sha256, rule_sha256 (D), signature_statement_sha256, seed_hex, T_sign, T_pulse, source. Keep `--seed-hex` ONLY with `--test-seed` (split_mode "TEST-SEED …"). Keep the unseeded dry-run branch only when neither is given (split_mode says DRY RUN). Fix the two unclosed-file spots (use `with`).
2. `_optionA_dev/beacon/fetch_beacon.py`: close files with `with`; otherwise unchanged in behaviour (RETRY before 24 h; 404-only absence after; VOID note). If you find a defect, fix it and list it in RESULT.md.
3. `_optionA_dev/fourier_chirality/run_configurations.py`: in `load_identity`, additionally require `rule_sha256` present and equal to a new Protocol field `rule_sha256` when that field is not None (production will pin the signed rule's digest there; tests pass their own). Nothing else changes in the driver.
4. Fixtures (unittest), all offline:
   - `_optionA_dev/beacon/test_fetch_beacon.py`: add a test that runs `fetch_beacon.main` with a MOCKED `fetch` returning a NIST body whose pulse timeStamp equals the computed T_pulse (build the body from the computed time), and asserts the written record has every field including `rule`, `rule_sha256`, `signature_statement_sha256`; and one for the drand path (mock: NIST raises HTTPError 404; drand body with the computed round) with T_sign 30 h ago.
   - `_optionA_dev/corpus_identity/test_build_corpus_identity.py`: END-TO-END — produce a REAL record via `fetch_beacon.main` with mocked fetch (both NIST and drand variants), pass it to `build(...)` with the matching D and S, assert success and that the identity's beacon_record_sha256 equals sha256(record bytes) and rule_sha256 == D; then NEGATIVE cases, each asserting the named refusal: wrong rule digest; statement lacking T_sign; pulse timeStamp off by one minute; statusCode 1; uri host wrong / not matching pulse.uri; drand wrong round; drand chain hash wrong; thin record; bare seed without --test-seed. Remove any hand-built "production-shaped" record that is NOT produced by fetch_beacon.main (the reviewer's point: no fixture should hand-build what production produces).
   - `_optionA_dev/fourier_chirality/test_run_configurations.py`: make the end-to-end test (`test_real_builder_output_loads_through_load_identity`) obtain its record from `fetch_beacon.main` (mocked network) instead of a hand-written dict, and pass rule_sha256 to the test Protocol; keep every other test passing. Do NOT weaken any assertion; do not assert an observed defect anywhere.
   Run: `cd _optionA_dev/fourier_chirality && python3 -W error::ResourceWarning -m unittest test_fourier_chirality test_run_configurations`; `cd ../corpus_identity && python3 -W error::ResourceWarning -m unittest test_build_corpus_identity`; `cd ../beacon && python3 -W error::ResourceWarning -m unittest test_fetch_beacon`. All must pass with zero ResourceWarnings. Report exact counts in RESULT.md.
5. RESULT.md: first line PASS/FAIL + counts; then every refusal token the builder can emit; every file you changed with its new SHA-256; anything you could not do. Byte-honesty over completeness.
