# Build two pinned scripts, in THIS directory (_optionA_dev/corpus_identity/), with fixtures. Python 3.9 + numpy + astropy (+ pandas if present). Nothing else. Do not modify any other file. Do not fetch anything from the network.

## 1. build_guarded_pool.py  → guarded_pool.csv (columns: GZ1_OBJID, RA, DEC, G) + guarded_pool_receipt.json
Re-derive the §9B.2–§9B.4 pool of the signed preregistration MINI_PREREG_GZ_TIERC_DRAFT_V35_20260905.md (read §4.7–4.11 for the label threshold, §9B.2 for the pool definition, §9B.3–9B.4 for the guard, §3.5 for the great-circle formula, §9B.5 for the ordering) from these inputs ONLY:
- scratch/gz1_parsed.csv.gz — GZ1 table (columns OBJID, ra_deg, dec_deg, P_CW, P_ACW, …). Label: clockwise iff P_CW >= 0.8 and P_ACW < 0.8 → G=+1; anticlockwise iff P_ACW >= 0.8 and P_CW < 0.8 → G=-1; both ≥ 0.8 = contradictory (count and exclude, report); neither = below threshold.
- completeness_gate/artifacts_full/checkpoint.jsonl (+ completeness_receipt_20260903T122712Z.json for context) — per-GZ1-object terminal dispositions; the pool needs the ones whose terminal disposition is exactly `NO-DR10-WITHIN-1ARCSEC`. Discover the field names yourself and state them in the receipt.
- Protected objects for the guard: ../_successor_build_20260824/acquire/positions_selected_cut.csv (Tier A; columns ls_id,brickid,objid,ra,dec,…), ../_successor_build_20260824/acquire/positions_selected.csv (protected parent, contains Tier B), TIER_C_SAMPLE_MANIFEST_V13_20260904.csv (frozen Tier-C sample; RA, DEC columns).
- Guard: DROP a candidate if any protected object lies at angular separation <= r_guard = (33.536/2)*sqrt(2) arcsec, evaluated in binary64 from that expression (never a decimal literal), inclusive at the boundary, great-circle formula in binary64. Use a proper spatial index or sorted-dec windowing; the parent has 65,060 rows and the pool 12,100 — a naive N×M loop is fine too (≈ 0.8 G pairs is too slow in pure Python; vectorise with numpy in dec-sorted blocks).
CONTROLS the receipt MUST print and the fixture MUST assert (from VALIDATION_SELECTION_RECEIPT_20260905.json): pool = 12,100; guard dropped = 46; guarded pool = 12,054. If a control fails, print the actual numbers and STOP — do not tune anything to hit them; a mismatch is a finding.

## 2. build_corpus_identity.py → corpus_identity.json + corpus_identity_receipt.json
Inputs: guarded_pool.csv; VALIDATION_SELECTION_V29_20260905.csv (the FAILED set — control); scratch/survey-bricks-dr9-north.fits.gz (DR9-north survey-bricks table: RA1<=RA<RA2, DEC1<=DEC<DEC2 half-open, binary64) and validation_bricks/_bricks_without_r_coverage.txt (bricks with no published r-band).
Ordering (§9B.5): h = SHA256(ASCII base-10 GZ1_OBJID) as unsigned big-endian integer; order by h mod 2^32 ascending, ties by GZ1_OBJID ascending, over the guarded pool.
CONTROL: ranks 1–2,000 must equal, in order, the GZ1_OBJID column of VALIDATION_SELECTION_V29_20260905.csv (2,000 rows). Assert it; on mismatch print the first differing rank and STOP.
RENDERABLE(object) := its brick under the half-open test exists in the DR9-north table AND that brick is not in _bricks_without_r_coverage.txt. (This is catalogue-only.)
Output lists (ordered): tuning = the first 400 RENDERABLE objects at ranks ≥ 2,001; holdout = the next 200 RENDERABLE; skipped non-renderable ranks listed separately; fresh_validation = the next 2,000 RENDERABLE after those (ranks continue upward). Record for each list: the objids in order, their bricks, count, and a SHA-256 of the newline-joined objids. Record pool_sha256 (sha of guarded_pool.csv), the ordering rule text, the last rank consumed, and how many of the pool remain unused.
Also emit tuning_manifest_template.csv / holdout_manifest_template.csv / fresh_validation_selection.csv with columns objid,g,tensor_sha256 (tensor_sha256 left EMPTY — filled only when tensors exist) resp. GZ1_OBJID,RA,DEC,G,DR9N_BRICK.

## 3. Fixtures: test_build_guarded_pool.py and test_build_corpus_identity.py (unittest), including a SMALL synthetic-pool test for the ordering and renderability logic and the two CONTROL assertions above on the real data. Print `Ran N tests … OK`.

## 4. Write RESULT.md here: the control numbers you got (all of them, matching or not), the exact disposition field names you used, run times, and anything that did not reproduce. Byte-honesty: if 12,100/46/12,054 or the 2,000 do NOT reproduce, say so in the first line.
