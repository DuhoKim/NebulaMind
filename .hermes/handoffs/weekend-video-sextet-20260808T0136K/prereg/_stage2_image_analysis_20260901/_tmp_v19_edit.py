import os, re; E=os.environ; p='OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V19_20260906.md'; s=open(p).read()
def rep(old,new,count=1):
    global s; assert s.count(old)==count, (s.count(old), old[:90]); s=s.replace(old,new)
lines=s.split('\n')
lines[0]='# OPTION (A) — INSTRUMENT SELECTION RULE, DRAFT V19 (NOT ADOPTED, NOT APPROVED) — PROSPECTIVE SAMPLING AMENDMENT OF THE SIGNED V15, FOURTH DRAFT UNDER A NARROW RELEASE (V16, V17 AND V18 REFUSED AT THEIR GATES)'
assert lines[2].startswith('V18 of 2026-09-06 is the third draft')
lines[2]="V19 of 2026-09-06 is the fourth draft of the NARROW, PROSPECTIVE amendment of the SIGNED V15 (fdd9eedd9938c2ba7df312cdc9b82a06612772ccc087e9365b33fd7d9d9a97d1, T_sign 2026-09-06T00:04:07Z; on file unchanged with its RETRY beacon record 1c1d9d4bacc35319e91d46420b8f892e1126cc48059ec69156a0a718428aeb4b), written under Blanc's narrow release of 12:54 KST after the third-failure rule engaged (V16, V17, V18 refused — each by codex, agy passing all three; the history and the pattern are in `THIRD_FAILURE_DIAGNOSIS_OPTIONA_SAMPLING_AMENDMENT_20260906.md` and the change record). Authority: Duho, via codex voice, confirmed in chat 2026-09-06 11:08 KST; Duho's away-window instruction 12:04 (drafting and gating only — NOT approval). **What V19 changes in kind, not in degree: it states a PROPERTY before any mechanism (§3b, first paragraph) — the source decision is a function of archived public bytes for the fixed T_pulse plus the pinned root, recomputable by anyone at any later time — makes the code honour it, EXHIBITS it (the same verdict and the same seed at two different wall-clock times, `PROPERTY_EXHIBIT_V19_20260906.md`), and names the limits where an input is not genuinely archived.** Every V18 finding is repaired (change record): the fallback opens only on the served, live-equal, T_pulse-timestamped pulse failing authentication; the witness retains the ENTIRE GitHub push event with its retrieval provenance and takes the EARLIEST qualifying event, and the real API window is stated; the collection HISTORY logs every attempt from both the collector and the builder and is bound into the identity and verified by the driver; the adoption file must be a blob at the approval commit; the driver refuses any identity without an ACCEPT outcome, a timely witness, a matching lock and a committed log; the §7 command names the V19 builder; the removed VOID re-check is no longer cited as observed; the tests advertised are the tests that run. ONE sampling rule changes from V15 — the drand fallback is permitted from T_pulse itself — and, to make that safe, the acceptance machinery around it changes as listed. Sample sizes 400/200/2,000, floors 380/190/1,900, the 0.70 bar, exclusions, ONE holdout, label/pixel boundaries, custody, branch protection and the §6 pipeline identity (now executing under the approved Tier-C V39) are byte-for-byte V15. Disclosure about the operative rule: V15's pinned `beacon_record.py` accepts a NIST pulse when the live re-fetch raises and before the clock check; no identity will be built under V15 as signed. V15 REMAINS OPERATIVE until V19 is approved on its final bytes; a clear pair is READY-FOR-APPROVAL, not permission."
s='\n'.join(lines)
rep("**Order, provable.** (1) This amendment is APPROVED","**THE PROPERTY (V19, stated first).** The source decision for the fixed T_pulse — which of NIST or drand is binding, and therefore the seed — is a FUNCTION of archived public bytes and the pinned root: f(NIST pulse body served for T_pulse; the certificate named by its `certificateId`, which is the SHA-512 of that certificate's DER and so fixes its bytes; the issuer chain reached from the leaf, anchored to the pinned DigiCert Global Root G2; the next pulse, chained through `precommitmentValue`; the drand mainnet round round_for(T_pulse) and its randomness), and it is RECOMPUTABLE BY ANYONE AT ANY LATER TIME to the same verdict and the same seed. WHEN a verdict is asked changes only whether it is available yet (RETRY), never what it is; a later NIST \"recovery\" cannot exist for archived bytes — a change in served bytes is refused as `REFUSE-NIST-LIVE-DIFFERS`, and a change in the certificate would fail `certificate_id_is_sha512_of_leaf_der`. EXHIBITED, not asserted: `_optionA_dev/beacon_v2/exhibit_property.py` (SHA-256 `"+E['EXP']+"`) runs the decision twice at different wall-clock times from the same archived inputs — a synthetic record through the three-tier test PKI at T_pulse + 1 min and at T_pulse + 30 days, with and without live re-fetch (ACCEPT-DRAND, same seed, identical), and the REAL filed RETRY record of 2026-09-06T00:15:00Z at two times with the production root and no network (identical) — printed verbatim in `PROPERTY_EXHIBIT_V19_20260906.md`, output digest `"+E['EXD']+"`, byte-reproducible (`test_v19_property_exhibit_reproduces`). LIMITS of the property, named: (L1) NIST's issuer chain is fetched through the leaf's AIA URL at collection; a re-issued intermediate with the same key still anchors, a different chain fails live equality — the chain is archived by US in the record, not by NIST; (L2) the drand round's randomness is served by relays that archive historic rounds; its BLS signature under the chain's public key is what makes it immutable and any third party can verify that signature — this lane does not (disclosed), it verifies relay agreement; (L3) GitHub's push-event record (§3b (1a)) is served for about 30 days / 300 events with 30 s–6 h latency — after that the retained event is OUR copy, attested; (L4) availability is time-dependent — outages give RETRY, never a different answer; (L5) the operator can DELAY or ABANDON; both are visible (a signed rule with a passed T_pulse and no filed identity is a public event) and neither selects a seed or a source. The consequence: the operator's freedom over WHEN collection starts is not policed — it is made irrelevant.\n**Order, provable.** (1) This amendment is APPROVED")
i=s.index("(1a) ORDER MADE VISIBLE — WITNESSED BY A SERVER CLOCK, NOT BY OURS"); end="these are lower bounds only and are not claimed to order the approval against the pulse."; j=s.index(end, i)+len(end)
s=s[:i]+f"(1a) ORDER MADE VISIBLE — WITNESSED BY A SERVER CLOCK, NOT BY OURS (codex V16 C5; V17 FATAL 1; V18 item 1). The approval record is the file `APPROVAL_RECORD_SELRULE_V19_<date>.md`, written by Hwao immediately after the approval, with a STRICT SCHEMA: exactly one line `RULE_SHA256: <the approved digest>`, exactly one line `APPROVAL_UTC: <T_sign>`, exactly one line `DRAND_AT_APPROVAL: round R randomness <64 hex>` quoting the drand mainnet round CURRENT at T_sign (round_for(T_sign) − 1 ≤ R ≤ round_for(T_sign)), and the attestation text verbatim; it is committed TOGETHER WITH the adoption file `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line and one newline) and PUSHED to the protected branch. `approval_witness_v3.verify` (`_optionA_dev/corpus_identity/approval_witness_v3.py` `{E['AW3']}`), called by the builder after an ACCEPT verdict and before any identity is written, REFUSES unless: (W1) exactly one commit adds the record, none modifies it, the committed blob equals the bytes on disk; (W2) `origin` is the pinned URL and, after a fetch, that commit is an ancestor of `origin/feat/paper-workflow-v2`; (W3) **the EARLIEST GitHub PushEvent that delivered that commit to the branch — `created_at` stamped by GitHub's server — is BEFORE T_pulse**; the ENTIRE event JSON is retained in the identity with its retrieval provenance (endpoints, retrieval UTC, `gh` version) and its own digest; a missing event is PENDING within the API's publication latency (30 s to 6 h) and CLOSES the commitment if the window passes — never a licence for a new approval, time, round or source (`APPROVAL-PUSH-EVENT-MISSING` / `APPROVAL-PUSHED-AFTER-T-PULSE`); GitHub serves this record for roughly the last 30 days or 300 events, so the receipt is taken promptly and, afterwards, is the lane's retained copy (limit L3); (W4) FIRST APPROVAL IS FINAL: exactly one approval-record path for this version anywhere in history; (W5) the nonce round is the one current at T_sign and before the seed round, confirmed live by ≥ 2 pinned relays — it proves the record was written NO EARLIER than round R; the record's `APPROVAL_UTC` equals the record's T_sign and its `RULE_SHA256` the approved digest. What this PROVES: the record reached the protected branch before T_pulse by the server's clock and was written no earlier than round R. What stays ATTESTED: who spoke; that the approval statement was made at T_sign (Codex's attestation and Duho's chat confirmations). Fixture `test_approval_witness_v3.py` `{E['TAW3']}`, 9 tests over a real git clone with a bare origin and a mock events feed: accepted-and-what-it-proves; the late writer with a backdated commit REFUSED by the server time; no event; old nonce round; duplicate lines; wrong digest; unpushed / modified / second record; the event matcher; entire-event retention and earliest-event choice. In addition `beacon_record_expedited.collect` REFUSES any T_sign earlier than `MIN_T_SIGN` = 2026-09-06T02:20:00Z and REFUSES BY NAME the pulse of 2026-09-06T00:15:00Z (NIST chain-2 pulse 1928801, drand round 6440756) — lower bounds only."+s[j:]
rep("then the fallback, which opens ONLY on positive public evidence — the record holds a NIST pulse served for T_pulse, the live copy EQUALS it, and it fails authentication — an absent, erroring or never-collected NIST is `RETRY` (observed: `test_v18_http_error_is_retry_and_absent_nist_never_seeds` shows HTTP 404 and 500 → RETRY under V18 and → ACCEPT-DRAND under V15's module;",
    "then the fallback, which opens ONLY on positive public evidence — the retained pulse IS the T_pulse pulse of chain 2 (`timestamp_is_t_pulse` and `uri_is_nist_chain2` hold), the live copy EQUALS it, and its signature/anchor checks fail — an absent, erroring, never-collected or DIFFERENT served pulse is `RETRY` (V19, codex V18 item 2; observed: `test_v19_wrong_time_pulse_is_not_fallback_evidence` — a served, live-equal pulse whose body says T_pulse − 60 s → RETRY; `test_v18_http_error_is_retry_and_absent_nist_never_seeds` — HTTP 404 and 500 → RETRY under V19 and → ACCEPT-DRAND under V15's module;")
rep("`RETRY` when a verdict is asked before T_pulse, or when ANY live NIST fetch raises (HTTP errors included), or when no NIST pulse is retained for T_pulse, or when the live copy is not confirmed equal — never a seed in any of these; from T_pulse itself (`FALLBACK_AFTER_H = 0`), if the retained, live-equal NIST pulse is not authenticable",
    "`RETRY` when a verdict is asked before T_pulse, or when ANY live NIST fetch raises (HTTP errors included), or when no NIST pulse is retained for T_pulse, or when the retained pulse is not the T_pulse pulse of chain 2 — never a seed in any of these; a live copy that DIFFERS from the retained bytes is `REFUSE-NIST-LIVE-DIFFERS` (not RETRY); from T_pulse itself (`FALLBACK_AFTER_H = 0`), if the retained, live-equal T_pulse pulse is not authenticable")
i=s.index("observed `REFUSE-DRAND-VOID-PRIMARY-AUTHENTICABLE`"); j=s.index("exercised)", i)+len("exercised)")
s=s[:i]+"V19: the separate VOID re-check is REMOVED as unreachable — with live equality required first, a live authenticable pulse is either EQUAL to the retained one, ACCEPT-NIST, or DIFFERENT, REFUSE-NIST-LIVE-DIFFERS; `REFUSE-DRAND-VOID-PRIMARY-AUTHENTICABLE` is a V15 token that V19's module cannot emit)"+s[j:]
rep("With `fetch` supplied, `verdict` re-fetches every public input live and requires equality with the retained bytes","With `fetch` supplied, `verdict` re-fetches the public inputs live and requires equality with the retained bytes — for NIST the four collected representations (pulse body, leaf certificate and issuer chain as PEM-normalised by the collector, next pulse body), for drand the quorum randomness of the pinned relays, not every relay body byte")
rep("SOURCE-DECISION PROCEDURE, EXECUTABLE (codex V16 item 2; V17 [MAJOR] 5): every collection attempt at or after T_pulse and its verdict is appended, in order, to the append-only `_optionA_dev/collection_log.jsonl`; the FIRST verdict that is ACCEPT-* is BINDING — `build_corpus_identity_v18.py` (`"+E['B18']+"`) refuses any later build whose record, outcome or seed differs from that first logged ACCEPT (`COLLECTION-LOCKED`), so a delayed or repeated collection cannot choose between source states; the log is committed and pushed with the identity (E3 witness). A NIST pulse that becomes authenticable AFTER the first ACCEPT does not reopen it. ADOPTION BINDING (codex V16 item 6, V17 carried): at approval the approved digest is written to the committed file `_optionA_dev/ADOPTED_RULE_SHA256.txt` (exactly one 64-hex line); the builder refuses a `--rule-sha256` that differs from it and refuses if it is absent (`ADOPTION-MISSING` / `-MALFORMED` / `-MISMATCH`); the driver `run_configurations_v2.py` (`"+E['RC2']+"`) reads the same file and refuses an identity whose `rule_sha256` differs (fixtures `test_build_corpus_identity_v18.py` `"+E['TB18']+"`, 5 tests; `test_run_configurations_v2.py` `"+E['TRC2']+"`, 19 tests).",
    "SOURCE-DECISION PROCEDURE (codex V16 item 2; V17 item 5; V18 item 3): by THE PROPERTY, the source decision for T_pulse is a function of archived bytes and cannot depend on when collection happens; the COLLECTION HISTORY therefore RECORDS what was done rather than removing a lever — and it is complete: the collector CLI (`beacon_record_expedited.py collect/verify --log`) and the builder append EVERY attempt, in order, to the append-only `_optionA_dev/collection_log.jsonl` — RETRY, refusals, witness and adoption refusals, the first ACCEPT, and any later conflicting ACCEPT (logged as `builder-conflict`, then refused `COLLECTION-LOCKED`); the FIRST builder ACCEPT is binding; the identity carries the log's digest, entry count and first ACCEPT (`collection_lock`), its T_pulse (`beacon_t_pulse`) and the adoption digest; the log is committed with the identity (E3 witness) and the driver verifies it as a byte-equal blob at the witnessed commit. ADOPTION BINDING (codex V16 item 6; V18 item 4): the adoption file `_optionA_dev/ADOPTED_RULE_SHA256.txt` is committed WITH the approval record; `build_corpus_identity_v19.py` (`"+E['B19']+"`) refuses unless it is a byte-equal blob at the witnessed approval commit, exactly one 64-hex line and one newline, equal to `--rule-sha256` (`ADOPTION-MISSING` / `-MALFORMED` / `-MISMATCH` / `-NOT-IN-REPO` / `-NOT-AT-APPROVAL-COMMIT`); the driver `run_configurations_v3.py` (`"+E['RC3']+"`) reads the same file and REFUSES any identity whose `rule_sha256` differs, whose `beacon_outcome` does not start with ACCEPT, whose approval push event is missing or not before its T_pulse, whose first logged ACCEPT does not name its record, outcome and seed, or whose collection log is not the committed blob (`IDENTITY-BEACON-NOT-ACCEPTED`, `IDENTITY-WITNESS-MISSING/-LATE`, `IDENTITY-LOCK-MISMATCH`, `COLLECTION-LOG-DIGEST`, `COLLECTION-LOG-NOT-IN-WITNESS-COMMIT`). Fixtures: `test_build_corpus_identity_v19.py` `"+E['TB19']+"` (5 tests: accepted-witnessed-locked with the history; every attempt logged including RETRY and witness refusals; a conflicting later ACCEPT logged and locked out; adoption must be a blob at the approval commit; identity carries lock/T_pulse/adoption); `test_run_configurations_v3.py` `"+E['TRC3']+"` (20 tests: V15's 17, plus adoption-fails-closed, sentinel-counted-and-unscored, and the acceptance-path refusals; its end-to-end test drives the V19 builder over a real git scaffold into the driver).")
rep("build_corpus_identity_v17.py --beacon-record <record> --rule-sha256 <approved V17 digest> --signature-statement <the witnessed APPROVAL_RECORD_SELRULE_V17 file>","build_corpus_identity_v19.py --beacon-record <record> --rule-sha256 <the approved V19 digest> --signature-statement <the witnessed APPROVAL_RECORD_SELRULE_V19 file>")
rep(f"`build_corpus_identity_v18.py`, SHA-256 `{E['B18']}` (V18: V15's `build_corpus_identity.py` `3090af770f0329beddb2d8b64acae6d4a1b1da06025e616b6fb145bff47ca46c` plus the expedited beacon import, the approval-witness-v2 call, the collection-log lock and the adoption binding — the removed lines are the boundary only, asserted by its fixture)",
    f"`build_corpus_identity_v19.py`, SHA-256 `{E['B19']}` (V19: V15's `build_corpus_identity.py` `3090af770f0329beddb2d8b64acae6d4a1b1da06025e616b6fb145bff47ca46c` plus the expedited beacon import, the approval-witness-v3 call, the complete collection history and the adoption-at-approval-commit binding; the V18 builder `{E['B18']}` is superseded and retained)")
rep(f"`test_build_corpus_identity_v18.py` `{E['TB18']}` (5 tests: accepted-witnessed-locked; late push closes the commitment; a second, different ACCEPT is locked out; adoption binding; boundary-only diff); the V16/V17 fixtures are retained as history (the V16 'both refuse' test exercised only the V16 builder, as codex noted; the V17 witness proved no upper bound, as codex showed)",
    f"`test_build_corpus_identity_v19.py` `{E['TB19']}` (5 tests, listed in §3c); the V16/V17/V18 fixtures are retained as history (V16's 'both refuse' test exercised only its builder; V17's witness proved no upper bound; V18's lock logged only accepts — each as codex showed)")
pat=re.compile(r"`beacon_record_expedited\.py` `[0-9a-f]{64}` \(V18: V15's `beacon_record\.py` `023c4d7dfd18d2d6818ea09d41e78b44835dc95d20ff4ba69f36c0afd61bbb73` with `FALLBACK_AFTER_H = 0`, `MIN_T_SIGN`, `EXCLUDED_T_PULSE`, the clock-first RETRY, any-exception RETRY, the live-equality requirement, the positive-evidence fallback and the removed VOID block — the 18 removed lines are FROZEN in its fixture `test_beacon_record_expedited\.py` `[0-9a-f]{64}`, 15 tests ending in token coverage, including codex's V16 and V17 counterexamples run against BOTH modules;")
assert len(pat.findall(s))==1
s=pat.sub(f"`beacon_record_expedited.py` `{E['BX']}` (V19: V15's `beacon_record.py` `023c4d7dfd18d2d6818ea09d41e78b44835dc95d20ff4ba69f36c0afd61bbb73` with the property stated in its docstring, `FALLBACK_AFTER_H = 0`, `MIN_T_SIGN`, `EXCLUDED_T_PULSE`, the clock-first RETRY, any-exception RETRY, the live-equality requirement, the T_pulse-pulse fallback predicate, the removed VOID block and the collector's `--log` — the 23 removed lines are FROZEN in its fixture `test_beacon_record_expedited.py` `{E['TBX']}`, 18 tests ending in token coverage, including codex's V16, V17 and V18 counterexamples run against BOTH modules and the exhibit's reproducibility;", s)
pat2=re.compile(r"DRIVER: `run_configurations_v2\.py`, SHA-256 `[0-9a-f]{64}` \(V18: V15's `run_configurations\.py` `d486ae58f40d220e8d2a9a1d875e008059cdc3bb1d9aee0dffc7dc974c7d53aa` plus the adoption binding and the render-refused sentinel accounting — fixture `test_run_configurations_v2\.py` `[0-9a-f]{64}`, 19 tests, the V15 fixture's 17 plus adoption-fails-closed and sentinel-counted-and-unscored\)")
assert len(pat2.findall(s))==1
s=pat2.sub(f"DRIVER: `run_configurations_v3.py`, SHA-256 `{E['RC3']}` (V19: V15's `run_configurations.py` `d486ae58f40d220e8d2a9a1d875e008059cdc3bb1d9aee0dffc7dc974c7d53aa` plus the adoption binding, the render-refused sentinel accounting and the acceptance-path checks of §3c — fixture `test_run_configurations_v3.py` `{E['TRC3']}`, 20 tests; the V18 driver `{E['RC2']}` and its fixture `{E['TRC2']}` are superseded and retained — the latter's advertised count was false, as codex found)", s)
rep("one two-seat gate on this amendment (V17; V16's counted), one on the pipeline amendment (Tier-C V37; V36's counted), one on the final Tier-C version;","one two-seat gate on this amendment (V19; V16–V18's counted), one on the pipeline amendment (Tier-C V39, APPROVED and installed 13:01 KST), one on the final Tier-C version;")
open(p,'w').write(s); print("V19 text edited")
