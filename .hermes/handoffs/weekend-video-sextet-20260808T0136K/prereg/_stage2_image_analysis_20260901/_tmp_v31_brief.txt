You are an independent referee of a COMPLETE UNADOPTED PACKAGE, not of a happy path: the STAGED candidate OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V31_20260907.md (SHA-256 f94b45e626ec95f1d2a60da783eb23c70f02394bbbe2be7389583013dd00cc49; NOT GATED before now, NOT ADOPTED, NOT APPROVED). V31 = V30 after ITS complete-package review (agy SIGNABLE; codex NOT-SIGNABLE with ONE FATAL and V30-2/3/4 — both reports and the author's classification V30_REVIEW_OUTCOME_20260907.md are here; codex's first V30 dispatch was blocked by its provider's content filter after executing the package and was re-dispatched into the same sandbox bytes, both briefs retained). THE FATAL WAS THE LANE'S OWN REGRESSION (V30-1): a JSON null in the history-open file parsed without error, the track-10 resolver treated the absent event as nothing to check, and the composed load ACCEPTED — even over a published history rewrite. Per the V20 rule it was REPRODUCED IN EXECUTED OUTPUT before any repair (TRACK11_FAIL_FIRST_RECEIPT_20260907.md run 1: the null file LOADS on the v15 byte-copy of driver v14; the rewrite + null LOADS). WHAT V31 CLAIMS ON TOP OF V30 (judge each, executed): the composed path is now a STAGED FINDING COLLECTOR — run_configurations_v15.load_identity_composed, stages S0 (the identity file read locally; BOTH retained events swept; the history-open file must hold a PushEvent OBJECT — null, a list, a scalar or a wrong type is HISTORY-OPEN-EVENT-INVALID, class 0, and nothing downstream is skipped), S0b (list shape / disjointness, class 0, BEFORE anything remote), S1 (the witness-bound offline conjunction extracted UNCHANGED into _conjunction_prefix and run as ONE classified stage — WITNESS-FETCH-FAILED / VERIFIER-UNAVAILABLE become RETRY-UNAVAILABLE findings after the local sweep; every refusal classified by provenance_designs_v11.classify_refusal, every exception by classify_exception; an unknown code is FLAGGED, never silently placed), S2 (ONE evidence snapshot per invocation), S3 (the approval event authenticated on that snapshot; the open event's same-id contradictions on the same snapshot), S4 (seed re-derivation with PRODUCTION's own re-deriver), S5 (history_findings_v11 — the history stage as a collector on the same snapshot: remote head, genesis-only, open-event delivery and authentication, extension / batch over the fetched objects, per-entry publication, working tree vs remote — none returning before the others are derived); ACCEPT only when every stage in REQUIRED_STAGES RAN and contributed nothing — a stage whose inputs failed is NOT RUN and can never be skipped into acceptance; every finding recorded in I['_findings']; authenticate_event evaluates EXPIRED before UNAVAILABLE; class boundaries and policies are named in §3c (seed cryptography class 4/5; PENDING-PUSH / HISTORY-DIVERGED class 4 by policy; a receipt-path refusal class 3 by policy); the offline wording is corrected (offline exposes REDERIVE-RETRY and WITNESS-FETCH-FAILED; IDENTITY-WITNESS-COMMIT-UNDETERMINED is its named undetermined-delivery refusal, not its only unavailability disposition); codex's three V30 clauses are carried verbatim. Fail-first (TRACK11_FAIL_FIRST_RECEIPT_20260907.md): run 1 reproduces the FATAL and codex's V30-2/3 rows on the byte-copies; run 2: null and every non-object open value refused, the rewrite + null refused, wrong repository + failed witness fetch → EVENT-INCONSISTENT, one tuning id missing + HTTP 503 → IDENTITY-tuning_objids-SIZE-OR-TYPE, a rewrite with an empty feed → HISTORY-NOT-AN-EXTENSION, undetermined approval + a feed that no longer reaches it + remote down → EVENT-EXPIRED-NO-RECEIPT-PATH, exactly one retrieval per invocation, every named refusal of your V30 inventory classified; run 2b: every existing precedence test of tracks 8–10 (11 tests) re-run against v11/v15, OK (one disclosed wording restoration in S0, no test changed); run 4: 147 tests / 26 suites OK (_tmp_v31_all_suites_aggregate_RUN1.txt). JUDGE THE COLLECTOR ITSELF: (a) enumerate every raise on the composed path that still decides before or outside load_identity_composed — is any of them not class 0 by cause? (b) can ANY required stage be skipped into acceptance — construct inputs that make a stage 'not run' or contribute nothing while a defect exists (an identity file that is JSON but not an object; an open-event file that is an object without 'type'; a missing approval_witness block; an events_runner that returns malformed JSON; an unreadable beacon record); (c) does the one-snapshot policy hold — count retrievals on the happy path and on every refusal path, and test whether an obtained contradiction can still be hidden by where it appears; (d) does history_findings_v11 collect every derivable finding — construct a rewrite + a wrong open repository + an empty feed together and check every finding is recorded and the winner is the highest class; (e) is classify_refusal's table total over the actual raises (read the code, not the list) and are the class placements and the named policies sound; (f) offline: is its described behaviour exactly its executed behaviour (list checks after the conjunction; REDERIVE-RETRY; WITNESS-FETCH-FAILED). Column C of LIMITS_VS_CONTRADICTIONS_20260907.md (V30-1/2/3) is claimed resolved in section H — judge that claim. The questions file (SHA-256 prefix 4e6dc490485cf956) is unchanged. V30 = V29 after ITS complete-package review (history, still in force, judge as before): WHAT V29 CLAIMED ON TOP OF V28 (still in force, judge as before): (V28-1) driver v13's witness-commit precheck, in composed mode, DEFERS an UNDETERMINED delivery to composed provenance instead of raising the retry — `authenticate_event_live` runs local_precheck (wrong repository → EVENT-INCONSISTENT before any retrieval, so HTTP 503 changes nothing), then retrieval, then same-id contradictions (→ EVENT-FORGED), and only then RETRY-EVENTS-UNAVAILABLE; positive non-delivery stays terminal; offline keeps IDENTITY-WITNESS-COMMIT-UNDETERMINED (no feed, no repository pinning there); codex's clause (1) is carried verbatim in the v9 header, §3c and the V29 paragraph. (V28-2) `validate_continuation_v9` — when v8 reports an undetermined open-event delivery it consults the feed through `authenticate_event_live` for the open commit: OPEN-EVENT-INCONSISTENT-INPUT / OPEN-EVENT-FORGED / OPEN-EVENT-NOT-EARLIEST terminal, EVIDENCE-EXPIRED a loss, EVIDENCE-UNAVAILABLE / EVIDENCE-INCOMPLETE only when nothing higher is established. (V28-3) the historical builder-fixture clause names test_build_corpus_identity_v27.py with its own digest; the verifier citation names verify_drand.py a781b965… as historical and verify_drand_v2.py with its own digest; the v13 driver fixture opens by naming itself (its inherited header text follows, marked); the inspection's row-1 note says INCONSISTENT-INPUT; rule (iii) restated with codex's clause (2) — a later distinct qualifying push beside a verbatim earliest genuine event is AUTHENTIC, NOT-EARLIEST needs an EARLIER qualifying event (test_track9_fail_first.V28_3_RuleIII executes both). V28's claims on top of V27 (still in force, judge as before): (V27-1) PRECEDENCE — three rules, stated in provenance_designs_v8's header and §3c, claimed to be the ONLY precedence rules: (i) locally decidable input mismatches (not a PushEvent, wrong repository, positive non-delivery) are decided BEFORE any retrieval by `local_precheck`, so a failed retrieval cannot turn them into a retry (authenticate_event_live prechecks, then retrieves); (ii) a SAME-ID contradiction (a live PushEvent with the retained id and different canonical bytes) is decided BEFORE the undetermined-delivery return and BEFORE the verbatim-presence shortcut — FORGED; (iii) the SAME-COMMIT arm is scoped to the pinned protected ref (through `delivery`, which answers NOT-DELIVERED for any other ref) and evaluated only when the retained event is ABSENT — when it is present verbatim, another delivering push is an ORDERING question decided by the earliest-qualifying rule (NOT-EARLIEST), not a contradicting copy. (V27-2) TRI-STATE THROUGH EVERY STAGE — driver v12's witness-commit precheck (D: `_P().delivery`) in every mode: UNDETERMINED → RETRY-EVENTS-UNAVAILABLE in composed mode / IDENTITY-WITNESS-COMMIT-UNDETERMINED offline (offline has no retry vocabulary), positive non-delivery → EVENT-INCONSISTENT: IDENTITY-WITNESS-COMMIT composed / IDENTITY-WITNESS-COMMIT offline; `validate_continuation_v8` re-derives the retained history-open event's tri-state for the open commit after v3's fetch — EVIDENCE-UNAVAILABLE (retry; RETRY-HISTORY-CONTINUATION in the driver) / OPEN-EVENT-INCONSISTENT-INPUT (terminal) — replacing v3's Boolean OPEN-EVENT-DOES-NOT-DELIVER on this path; `delivery` answers UNDETERMINED for an OSError launching git. (V27-3) driver v12's lineage table re-verified against each retained driver's own `import provenance_designs_vN as P` line (v10 → v6; the v11 row was wrong); D comments v6→v8; P docstrings: seven outcomes, the historical EXPIRED-as-retry sentence marked HISTORICAL; the inspection header names the modules it executes; the design document's row 9b says INCONSISTENT-INPUT (revision 8); §9 no longer calls the V22 inspection an inspection of THIS candidate; the suite count is taken from the executed logs (132 / 20). Consequence: every inspection row keeps its V27 verdict; only the refusal wording of rows 10b/10c changed. V27's claims on top of V26 (still in force, judge as before): (V26-1) the CONTRADICTION PREDICATE is stated and implemented — a live event with the SAME event id, or DELIVERING the same commit under the tri-state check, whose canonical bytes differ (a difference only in `before` or `head` included) is FORGED, terminal; plain absence stays INCOMPLETE; (V26-2) DELIVERY IS TRI-STATE — provenance_designs_v7.delivery() returns DELIVERED / NOT-DELIVERED / UNDETERMINED; UNDETERMINED (a git object missing locally, a failed git command, no repository root for an ancestry claim) yields UNAVAILABLE (retry), never FORGED; a retained event that positively does not deliver the commit, or names the wrong repository/type, is INCONSISTENT-INPUT; a verbatim-present but not-earliest event is NOT-EARLIEST; driver v11 names them EVENT-INCONSISTENT / EVENT-NOT-EARLIEST; EVIDENCE-EXPIRED is a loss, not a retry, at every stage; (V26-3) the design doc's outcomes row rewritten, the provenance docstrings corrected, the questions file's open-event expiry named EVIDENCE-EXPIRED and A′ receipts limited to EXPIRED, §3b(3) names the v27 collector, the clause-(4) verifier cite is verify_drand_v2, and driver v11 states its LINEAGE IN A TABLE instead of the over-renamed historical lines of v10 (a defect of my own, codex V26-3). Consequence: composed row 10b now refuses the backdated contradicting approval event as EVENT-FORGED (a contradiction), not via the EXPIRED path. V26's claims on top of V25 (still in force, judge as before): (P1) batch classification uses the SAME delivery evidence as authentication — `delivers` (head, payload.commits, or before..head ancestry; GitHub's documented PushEvent shape may omit the commits list) — so a proven multi-commit delivery is terminal HISTORY-PUBLICATION-BATCH with or without a commits list; absence with no such event stays EVIDENCE-INCOMPLETE (retry); (P2) ABSENCE IS NEVER FORGERY at any of the three retrievals: a retained approval or history-open event missing from a feed that covers its time is INCOMPLETE (retry), FORGED only when the feed carries a push delivering the same commit with DIFFERENT bytes; the open-event stage speaks the uniform EVIDENCE-UNAVAILABLE / EVIDENCE-INCOMPLETE / EVIDENCE-EXPIRED vocabulary; driver v10 prefixes UNAVAILABLE/INCOMPLETE with RETRY- (EXPIRED is a loss, not a retry); (P3) labels swept (the '113 on the V22 candidate' phrase, driver comments, the inspection docstring, T2's A′/B availability and the qualified 'rejected push' wording); (P4) THE COVENANT is in the questions file. V25's claims on top of V24 (still in force, judge as before): (N1) THE COVENANT — NOT a repair — replaces the residual in provenance_designs_v5, the design doc and §3c: the validator authenticates the published append-only history and distinct ordered publications; it does not authenticate when the underlying operations occurred nor the completeness of unpublished observations or decisions, before the genesis was published or between later publications; an unpublished absence-based CLOSED can be removed before its publication; the seed is unchanged; a late approval event stays disqualifying; stronger completeness needs independently retained decision evidence, NOT implemented; (N2) validate_continuation_v5 DISPOSITIONS — an empty/unavailable per-entry retrieval is EVIDENCE-UNAVAILABLE (retry), a history commit without its own push event and no evidence of batching is EVIDENCE-INCOMPLETE (retry within the feed window), HISTORY-PUBLICATION-BATCH only when the feed shows the commit delivered inside a multi-commit push (terminal); driver v9 prefixes EVIDENCE-* refusals RETRY-; the questions file states the FULL live contract and these dispositions; (N3) publish_entry refuses PUBLISH-UNRELATED-COMMITS when unpublished local commits other than the pending entry exist (nothing pushed); §7 names the v25 collector/builder; (N5) §3c self-label, §9 script name, driver comments swept; stronger text assertions. V24's claims (M1/M2/M3/M6) and V23's (A–F) remain in force — judge as before: (A) the history-open anchor is AUTHENTICATED — validate_continuation_v3 requires the first remote commit touching the history to carry a GENESIS-ONLY blob and to be delivered by a RETAINED PushEvent (Protocol.history_open_event_file) authenticated in the live feed; codex's late-first-publication counter-case now refuses OPEN-NOT-GENESIS-ONLY (rows 9k, 10d); RESIDUAL STATED: attempts made before the genesis was published are undetectable by any validator — under drand-only they cannot change the seed and cannot hide a CLOSED witness state; (B) the PRODUCER BOUNDARY IS BUILT — provenance_designs_v3.publish_entry (one entry per commit; ls-remote acknowledgment or PENDING-PUSH; PUBLISH-BATCH refuses two unpublished entries); collector v23 --publish-remote/--publish-ref publishes the genesis as the history-open commit and every attempt; builder v23 publishes every appended entry; the validator requires one entry per acknowledged commit (HISTORY-BATCH-COMMIT); (C) ONE delivery predicate — provenance_designs_v3.delivers = approval_witness_v4.delivers (before..head via git) in every mode; (D) a control failure appends builder-control-refusal before returning False; (E) the render-end must be the LAST relevant journal record; (F) QUESTIONS_FOR_DUHO_TRACK2_ORIGIN_AND_RECEIPTS_20260906.md says 30 days / 300 events with the consequences of loss at tune vs holdout, marks Option B NOT IMPLEMENTED, states that a second token on the same account is not a second identity; an EMPTY live feed is UNAVAILABLE (never FORGED); 404/418/429 paths tested. Text sweep: §9 counts, §7 commands, §3b old verifier marked historical, driver comments, track-2 test docstring, track-1 test no longer writes into the lane.
THE THREE STATES (exercise them on the production call path): (1) OFFLINE candidate — PRODUCTION defaults, provenance_mode='offline' — STILL ACCEPTS counter-cases 1, 2 and A (disclosed in §3c); (2) STANDALONE v11 helpers; (3) COMPOSED mode — Protocol.provenance_mode='composed' — load_identity_composed, the staged finding collector on the v11 helpers (test_composed_mode_on_the_production_call_path; rows 10a–10d). Say for each what it accepts and refuses. The origin label {actor: ops-witness, session: OPS} is PROPOSED BY THE LANE, unadopted, not a user decision; the recommended default is the delegated witness workflow with NO per-receipt human confirmation; the residual trusted step (a first receipt lane-produced under the OPS label is caught only by OPS's retained copy) is DISCLOSED; Option B (second GitHub identity / custodian key) is NOT IMPLEMENTED — assess exactly that step and whether Q1 Option C (no expired path) remains the sound recommendation. COST: one push per collector/builder attempt (per history entry), failure states PENDING-PUSH / DIVERGED / RETRY-REMOTE-UNAVAILABLE; flag any remaining 'one push per freeze' outside quotations.
PRESERVED AND RE-VERIFIED (state each): sample sizes 400/200/2,000; floors 380/190/1,900; the 0.70 bar; exclusions (failed set, 2,644 dry-run identities, rounds 6440756/6441904/6441924); custody E5(d); blindness; an ACTUAL FUTURE drand round; the ONE holdout (holdout_once PREPARED NOT ADOPTED, default False); E1 as a FACTUAL ERRATUM; verify_split UNADOPTED (default off).
ENVIRONMENT: venv with py_ecc 8.0.0 at _optionA_dev/_venv_bls (system site packages); drand/beacon/builder/witness fixtures with _optionA_dev/_venv_bls/bin/python; DRIVER, track-1/2/3 fixtures with PYTHONPATH=_optionA_dev/_venv_bls/lib/python3.9/site-packages /usr/bin/python3; PYTHONDONTWRITEBYTECODE=1; -W error::ResourceWarning (expect ONE inherited RuntimeWarning at fourier_chirality.py:87). Read-only network to public drand relays and the GitHub Events API permitted; publishing not. GZ1 table absent (guarded pool: UNVERIFIABLE HERE). Fixture remotes are LOCAL BARE repositories with non-fast-forward receives denied; the gh runner is fixture-supplied — labelled.
RUN: (a) cd _optionA_dev/fourier_chirality && PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_run_configurations_v15 (23) test_run_configurations (17); (b) cd ../corpus_identity && /usr/bin/python3 -W error -m unittest test_history_v2 (4); ../_venv_bls/bin/python -W error::ResourceWarning -m unittest test_approval_witness_v4 (3) test_build_corpus_identity_v31 (8) test_build_corpus_identity (4); (c) cd ../beacon_v2 && ../_venv_bls/bin/python -W error -m unittest test_beacon_record_drand_v31 (7); ../_venv_bls/bin/python exhibit_property_v22.py TWICE (digest 2160fa754ce3db25613389d58bacd3098763ea8536744558e9dae96c5b4f0ae1); (d) cd ../drand_only && ../_venv_bls/bin/python -W error -m unittest test_verify_drand_v2 (4); (e) cd ../track1 && RULE_TEXT=<abs V31 path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track1_fail_first (10); PYTHONPATH=<venv sp> /usr/bin/python3 coherent_attacks_v31.py (compare with the filed V31 table — every verdict equal to V30's); (f) cd ../track2 && PYTHONPATH=<venv sp> /usr/bin/python3 -W error -m unittest test_track2_fail_first (8) test_provenance_designs (2); (g) cd ../track3 && QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track3_fail_first (10); cd ../track4 && RULE_TEXT=<abs V31 path> DESIGN_TEXT=<abs TRACK2_PROVENANCE_DESIGNS path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track4_fail_first (7); cd ../track5 && RULE_TEXT=<abs V25 path — the pinned V25 label test, run against the text it was written for> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track5_fail_first (6); cd ../track6 && RULE_TEXT=<abs V31 path> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track6_fail_first (5); RULE_TEXT=<abs V26 path — the pinned V26 label test> /usr/bin/python3 -m unittest test_track6_text_v26 (1); cd ../track7 && RULE_TEXT=<abs V31 path> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track7_fail_first (4); RULE_TEXT=<abs V27 path — the pinned V27 label test> /usr/bin/python3 -m unittest test_track7_text_v27 (1); cd ../track8 && RULE_TEXT=<abs V28 path — track 8's text case asserts the V28 counts> DESIGN_TEXT=<abs design path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track8_fail_first (7); RULE_TEXT=<abs V28 path — the pinned V28 label test> /usr/bin/python3 -m unittest test_track8_text_v28 (1); cd ../track9 && RULE_TEXT=<abs V30 path — its V28-3 text case holds against V30> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track9_fail_first (3); RULE_TEXT=<abs V29 path — the pinned V29 label test> /usr/bin/python3 -m unittest test_track9_text_v29 (1); cd ../track10 && RULE_TEXT=<abs V30 path — track 10's text case asserts the V30 labels> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track10_fail_first (4); RULE_TEXT=<abs V30 path — the pinned V30 label test> /usr/bin/python3 -m unittest test_track10_text_v30 (1); /usr/bin/python3 exhibit_precedence_pairs.py (21 pairs; the resolver is unchanged in v11); cd ../track11 && RULE_TEXT=<abs V31 path> DESIGN_TEXT=<abs design path> QUESTIONS_TEXT=<abs questions path> PYTHONPATH=<venv sp> /usr/bin/python3 -W error::ResourceWarning -m unittest test_track11_fail_first (5); RULE_TEXT=<abs V31 path> /usr/bin/python3 -m unittest test_track11_text_v31 (1) — pass the variables SEPARATELY (the lane's own aggregate runner once passed two as one env argument; see TRACK4_SUITE_LOG_RECONCILIATION); (h) python3 miniprereg_pins/signature_preimage.py <V31> (must print f94b45e626ec95f1d2a60da783eb23c70f02394bbbe2be7389583013dd00cc49) python3 miniprereg_pins/signature_preimage.py <V30> (must print 98881c7ecba0ac3bb718983f8534aaa55d325efeb4314d72b31b64b95af2abb9) python3 miniprereg_pins/signature_preimage.py <V29> (must print 4458b821e3a1249c0646ce2790491f4d04e7224d0ef60721144354e216450599) python3 miniprereg_pins/signature_preimage.py <V28> (must print c9e65f1bfc2bdac10716760670c85866d20c1ec0ca1d9aebdfa5b83842804fc2); (i) hash every file the V31 text pins, AND verify that every file pinned by V30, V29, V28, V27, V26, V25, V24 and V23 is present at its path or under _optionA_dev/_archive_v22_pins/ with its pinned digest (the immutability rule), AND that the pin-drift record's cited historical digest lists (_tmp_v22/v23/v24_gate_COPIED_DIGESTS.txt, now supplied) carry the V22-pinned digests of the four archived files; (j) ADD VERIFICATION PROBES (this is a preregistration integrity review of local fixture repositories and fixture event feeds; nothing here targets any live system) OF YOUR OWN against all three states — internally consistent counter-cases built in your temporary fixtures, as the filed inspection does — and report each with the named refusal or ACCEPTED; in particular exercise THE STAGED COLLECTOR on the COMPLETE composed path — your V30 constructions first (the null open file over a rewrite; wrong approval repository + failed witness fetch; a missing list id + HTTP 503; a rewrite with an empty feed; an undetermined approval whose feed no longer reaches it with the remote down), then every pair of stages S0–S5 with both findings derivable at once in both derivation and retrieval orders, then inputs meant to make a stage 'not run' or contribute nothing while a defect exists; then the resolver pairs as before: every pair of contributing stages (A local sweep / B retrieval + same-id / C seed re-derivation / D history) with both findings derivable at once, in both derivation orders — including codex's V29 rows (unverifiable seed bodies + wrong repository / same-id contradiction; ls-remote down + wrong repository / same-id; approval feed 503 + open-event wrong repository) — and your own (e.g. FORGED vs HISTORY-NOT-AN-EXTENSION; EXPIRED vs RETRY-REMOTE-UNAVAILABLE; NOT-EARLIEST vs REDERIVE-RETRY; LOCAL-TERMINAL open-file-missing vs everything); look for ANY raise on the composed path that still decides before the resolver; then the V28-1/V28-2 cases as before: a coherently sealed identity whose retained approval event keeps the genuine id but claims delivery through an unknown `before` and a real later head (no commits shortcut) while the live feed serves the genuine conflicting same-id event (expect EVENT-FORGED, never RETRY); the same payload with repo.name = wrong/repo, with a healthy feed and with HTTP 503 (expect EVENT-INCONSISTENT both times); the same payload with the feed serving the retained event itself and nothing contradicting (expect RETRY-EVENTS-UNAVAILABLE); the three analogues on the retained history-open event (expect HISTORY-CONTINUATION: OPEN-EVENT-FORGED / OPEN-EVENT-INCONSISTENT-INPUT / RETRY-HISTORY-CONTINUATION: EVIDENCE-UNAVAILABLE); offline for the undetermined payload (expect IDENTITY-WITNESS-COMMIT-UNDETERMINED); then ask whether ANY other precheck on load_identity's path can still return a retry before local_precheck and the same-id check have run, whether codex's clause (1) is implemented completely (both paths, every stage), and whether rule (iii) as restated can be misused; then the V27-1/V27-2 cases as before: a retained event with the SAME id as a genuine live event but a changed head whose ancestry is undeterminable in a stale clone (expect FORGED, not UNAVAILABLE); the retained event verbatim PLUS a differently encoded copy with the same id (expect FORGED, not AUTHENTIC); a coherently sealed identity whose approval event names the wrong repository, loaded in composed mode with the runner returning HTTP 503 (expect EVENT-INCONSISTENT, never RETRY); an approval event whose `before` object no clone holds, on the complete composed path (expect RETRY-EVENTS-UNAVAILABLE) and offline (expect IDENTITY-WITNESS-COMMIT-UNDETERMINED); positive non-delivery on the complete path (expect EVENT-INCONSISTENT: IDENTITY-WITNESS-COMMIT); the same for the history-open event (expect RETRY-HISTORY-CONTINUATION: EVIDENCE-UNAVAILABLE / HISTORY-CONTINUATION: OPEN-EVENT-INCONSISTENT-INPUT); git unlaunchable (expect UNDETERMINED); and ask whether any precedence exception remains UNSTATED, whether rule (iii)'s absent-only scope can be misused (an author who keeps a verbatim genuine event while a second push re-delivers the commit), and whether GitHub can honestly serve two events with one id (if not, is the FORGED reading of that case sound?); then the V26-1/V26-2 cases as before: the retained event differing from the live event only in `before` or only in `head` (expect FORGED); a before..head delivery judged in a clone missing the descendant object (expect UNAVAILABLE, never FORGED); a retained event that positively does not deliver the commit, or names the wrong repository (expect INCONSISTENT-INPUT); a not-earliest verbatim event (expect NOT-EARLIEST); a later unrelated push with the approval event older than the feed's oldest (expect EXPIRED); and your own variations — including whether FORGED can be reached by anything other than an affirmative contradiction, whether any retry disposition can keep an inconsistent identity alive indefinitely, and whether the contradiction predicate can be triggered by an HONEST second push that legitimately re-delivers the same commit.
JUDGE, [FATAL]/[MAJOR]/[MINOR] with clause and code line: 1. the three states, exercised; 2. V30-1 (FATAL) / V30-2 / V30-3 / V30-4, Blanc's four items (order stated once / one enforcing place — now the whole composed path / pairwise exhibit / tests kept + added), V29-1/2/3/4, V28-1/2/3, V27-1/2/3, V26-1/2/3, P1–P4, N1–N5, M1–M6 and A–F: REPAIRED / DISCLOSED-AS-OPEN / NOT REPAIRED / COVENANT — and, per Blanc's 02:06 order, THE TWO CATEGORIES: is every entry in §3c's DISCLOSED LIMITS column a genuine limit (something no implementation in this design could establish) and not a contradiction between text and code? Is there any place where the code still does something other than what the text promises that is NOT in the REPAIRED column — i.e. an UNREPAIRED CONTRADICTION? Name it as such, fail-first evidence read correctly (per-test classification; the correction note); 3. THE COVENANT and the remaining trusted step (lane-produced first receipt) — are both stated exactly and completely, in the code, the design doc, §3c and the questions file? Do the Q1 options now describe themselves accurately (Option C = ALL required events; A′ = its real requirements, not built; B not implemented), with the temporary-retry vs terminal-inconsistency distinction and the actual availability window preserved? 4. cost and failure states; 5. preserved items; 6. every count and name in the text TRUE (run them); anything described-not-built; 7. the questions file: exactly what needs Duho, no more, with true consequences; 8. WHAT IS STILL MISSING before Duho can be asked to adopt exact reviewed behaviour with concrete consequences — as clause text; or state that nothing is.
VERDICT line, exactly one of: VERDICT: SIGNABLE-AS-PRECOMMITMENT   or   VERDICT: NOT-SIGNABLE   (SIGNABLE means: the package is complete and truthful enough to be put to Duho for adoption decisions; it is not approval of the offline baseline as a rule to run under.)
NOTE ON WORDING: the refusal names EVENT-FORGED / OPEN-EVENT-FORGED / FORGED are fixed identifiers in the reviewed code and text; they denote 'an affirmative contradiction between a retained record and the live feed'. Do not modify any file in this directory. Write your report ONLY to the path named in the access-proof instruction above.
