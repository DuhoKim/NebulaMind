# -*- coding: utf-8 -*-
import os, re; E=os.environ
p='OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V30_20260907.md'; s=open(p, encoding='utf-8').read()
def rp(pat, new):
    global s; m=re.search(pat, s); assert m, pat[:80]; s=s.replace(m.group(0), new, 1)
def rpf(pat, fn):
    global s; m=re.search(pat, s); assert m, pat[:80]; s=s.replace(m.group(0), fn(m), 1)
def sub(old, new):
    global s; assert old in s, old[:80]; s=s.replace(old, new, 1)
C1="In composed mode, every required retained event must be a valid PushEvent object. Missing, unreadable, null and other invalid values produce a named LOCAL-TERMINAL finding; none disables history verification. Acceptance requires affirmative success of approval authentication, seed re-derivation, published-history continuation and required per-entry publication checks. No required stage can be skipped into acceptance."
C2="The precedence order applies to the entire load_identity conjunction, including freeze-witness availability, local list shape/disjointness and enabled split checks. Unavailable evidence is not a proven local inconsistency. Every independently derivable higher-priority finding is collected before a lower-priority result is emitted, including within history and authentication helpers. All live evidence obtained during an invocation is evaluated for every applicable contradiction and ordering predicate under an explicitly defined snapshot/reconciliation policy; changing retrieval order cannot suppress an obtained contradiction."
C3="The mapping explicitly distinguishes local integrity, retained cryptographic validity, remote-derived publication findings, receipt refusal causes, evidence loss and temporary unavailability. Offline retains IDENTITY-WITNESS-COMMIT-UNDETERMINED for unresolved approval delivery and separately exposes its existing seed-re-derivation and witness-availability refusals; no broader claim that all offline refusals are class 0 is made."
NEWORDER="ONE PLACE ENFORCES IT (v15): `load_identity_composed` is a STAGED FINDING COLLECTOR — S0 the identity file read locally and BOTH retained events swept (the history-open file must hold a PushEvent object: null, a list, a scalar or any other value is HISTORY-OPEN-EVENT-INVALID, class 0, and nothing downstream is skipped), S0b the list shape / disjointness checks (class 0, before anything remote), S1 the witness-bound offline conjunction as ONE classified stage (WITNESS-FETCH-FAILED and VERIFIER-UNAVAILABLE become RETRY-UNAVAILABLE findings AFTER the local sweep; every other refusal is classified by `classify_refusal`, every exception by `classify_exception` — malformed retained input is class 0, I/O and subprocess failure class 5, an unknown code is FLAGGED, never silently placed), S2 one evidence snapshot (one retrieval per invocation), S3 the approval event and the open event's same-id contradictions on that snapshot, S4 seed re-derivation with PRODUCTION's own re-deriver, S5 the history stage as a collector (`history_findings_v11`: remote head, genesis-only, open-event delivery and authentication, extension / batch over the fetched objects, per-entry publication on the same snapshot, working tree vs remote — none returning before the others are derived). ACCEPT only when every stage in `REQUIRED_STAGES` RAN and contributed nothing; a stage whose inputs failed is NOT RUN and can never be skipped into acceptance; the winner is `provenance_designs_v11.resolve`; every finding is recorded in the identity's `_findings`; within a class the fixed stage sequence decides, never retrieval order. Boundaries and policies, named: retained cryptographic validity of the seed record (REDERIVE-<non-retry>, IDENTITY-SEED-NOT-REDERIVED) is class 4 — proven from retained bytes by verification, not by shape; REDERIVE-RETRY is class 5; WITNESS-NOT-PUSHED / APPROVAL-COMMIT-NOT-PUSHED are class 4 (publication findings from fetched evidence); PENDING-PUSH and HISTORY-DIVERGED are class 4 BY POLICY — terminal for this invocation, the remedy is publication and a new invocation; a receipt-path refusal is class 3 BY POLICY whatever the receipt's failure cause — an expired approval without an acceptable substitute is a loss; `authenticate_event` evaluates EXPIRED before UNAVAILABLE. OFFLINE MODE (PRODUCTION default) is unchanged in behaviour and is described exactly: its conjunction, then seed re-derivation, then the list checks; it names IDENTITY-WITNESS-COMMIT-UNDETERMINED for an unresolved approval delivery and separately exposes REDERIVE-RETRY (too few verifying seed bodies) and WITNESS-FETCH-FAILED (the freeze-witness fetch) — no claim that every offline refusal is class 0 is made."
sub('DRAFT V30 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED)','DRAFT V31 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED)')
sub('V30 of 2026-09-07 is V29 after its complete-package review',
 "V31 of 2026-09-07 is V30 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE with ONE FATAL — `V30_REVIEW_OUTCOME_20260907.md`; codex's first V30 dispatch was blocked by its provider's content filter after executing the package and re-dispatched into the same sandbox bytes). THE FATAL IS THE LANE'S OWN REGRESSION: (V30-1) a JSON null in the history-open file parsed without error, the track-10 resolver treated the absent event as nothing to check, and the composed load ACCEPTED — even over a published history rewrite that the genuine open event refuses. It was REPRODUCED IN EXECUTED OUTPUT before any repair (`TRACK11_FAIL_FIRST_RECEIPT_20260907.md` run 1: the null file loads on the v15 copy of driver v14; the rewrite + null loads) and is repaired by construction: the open-event file must hold a PushEvent object (HISTORY-OPEN-EVENT-INVALID, class 0, for null and every non-object value) and ACCEPT requires every stage in `REQUIRED_STAGES` to have RUN and contributed nothing — a stage whose inputs failed is NOT RUN and can never be skipped into acceptance. (V30-2) the resolver was not yet the one place that decides: WITNESS-FETCH-FAILED and VERIFIER-UNAVAILABLE were raised before the local sweep; the list shape / disjointness checks ran after it; unnormalized exceptions bypassed it — repaired by making the whole composed path a staged finding collector (`load_identity_composed`, stages S0–S5) with an explicit code→class table (`classify_refusal`, `classify_exception`; an unknown code is flagged). (V30-3) the history helper suppressed a higher finding behind a lower one, `authenticate_event` returned UNAVAILABLE before evaluating EXPIRED, and three unshared retrievals let retrieval order hide an obtained contradiction — repaired by `history_findings_v11` (every derivable finding collected, none returning first), expiry before unavailability, and ONE evidence snapshot per invocation applied to every predicate. (V30-4) the offline wording corrected: offline exposes REDERIVE-RETRY and WITNESS-FETCH-FAILED; IDENTITY-WITNESS-COMMIT-UNDETERMINED is its named undetermined-delivery refusal, not its only unavailability disposition. Codex's three clauses, verbatim, in force: \"" + C1 + "\" \"" + C2 + "\" \"" + C3 + "\" Fail-first: run 1 reproduces the FATAL and codex's V30-2/3 rows on the v15/v11 copies; run 2: all four code cases OK (null and every non-object open value refused; the rewrite + null refused; wrong repository + witness fetch failing → EVENT-INCONSISTENT; one tuning id missing + HTTP 503 → IDENTITY-tuning_objids-SIZE-OR-TYPE; a rewrite with an empty feed → HISTORY-NOT-AN-EXTENSION; undetermined approval + a feed that no longer reaches it + remote down → EVENT-EXPIRED-NO-RECEIPT-PATH; exactly one retrieval per invocation; every named refusal of the inventory classified, an unknown code flagged); run 2b: every existing precedence test of tracks 8–10 re-run against v11/v15, OK (one wording restoration in S0, disclosed: the composed name EVENT-INCONSISTENT: IDENTITY-WITNESS-COMMIT for positive non-delivery). V30 of 2026-09-07 was V29 after its complete-package review")
sub('History: V16–V21 were refused at gates and V22–V29 at complete-package reviews (each by codex; agy passed all fourteen);','History: V16–V21 were refused at gates and V22–V30 at complete-package reviews (each by codex; agy passed all fifteen);')
rp(r"DRIVER: `run_configurations_v14\.py`, SHA-256 `[0-9a-f]{64}` \(V30 STAGED:", f"DRIVER: `run_configurations_v15.py`, SHA-256 `{E['RC15']}` (V31 STAGED: `run_configurations_v14.py` — reviewed at V30, retained — with the composed path rebuilt as a STAGED FINDING COLLECTOR (`load_identity_composed`, S0–S5; the offline conjunction extracted unchanged into `_conjunction_prefix`; the v14 `composed_resolver` removed, not kept as dead code); V30 STAGED was:")
rp(r"fixture `test_run_configurations_v14\.py` `[0-9a-f]{64}`, 23 tests", f"fixture `test_run_configurations_v15.py` `{E['TRC15']}`, 23 tests")
sub('the track-2 v10 helpers called INSIDE `load_identity` on the production call path','the track-2 v11 helpers called INSIDE `load_identity` on the production call path')
rp(r"`build_corpus_identity_v30\.py`, SHA-256 `[0-9a-f]{64}` \(V30 STAGED:", f"`build_corpus_identity_v31.py`, SHA-256 `{E['B31']}` (V31 STAGED: on provenance_designs_v11; fixture `test_build_corpus_identity_v31.py` `{E['TB31']}`, 8 tests; V30 was:")
rp(r"`_optionA_dev/beacon_v2/beacon_record_drand_v30\.py` `[0-9a-f]{64}` \(V30:", f"`_optionA_dev/beacon_v2/beacon_record_drand_v31.py` `{E['BD31']}` (V31: on provenance_designs_v11; v30 retained; V30:")
rp(r"exercised by `test_beacon_record_drand_v30\.py` \(`[0-9a-f]{64}`, 7 tests", f"exercised by `test_beacon_record_drand_v31.py` (`{E['TBD31']}`, 7 tests")
nb=s.count('beacon_record_drand_v30'); s=s.replace('beacon_record_drand_v30','beacon_record_drand_v31'); nbu=len(re.findall(r'(?<!test_)build_corpus_identity_v30', s)); s=re.sub(r'(?<!test_)build_corpus_identity_v30','build_corpus_identity_v31', s); print('token replacements: beacon', nb, 'builder (non-fixture)', nbu)
rp(r"`coherent_attacks_v30\.py` \(`[0-9a-f]{64}`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v14 / provenance v10 — `V30_CANDIDATE_ATTACK_INSPECTION_20260907\.md`, same verdict in every row as V29;", f"`coherent_attacks_v31.py` (`{E['ATT31']}`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v15 / provenance v11 — `V31_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict in every row as V30;")
rpf(r"`track2/provenance_designs_v10\.py` \(`[0-9a-f]{64}`; UNADOPTED; v10 = v9 \+ THE PRECEDENCE ORDER STATED ONCE \(`PRECEDENCE`\) and ONE RESOLVER \(`resolve`, `finding`, `classify_history`, `local_precheck_event`; `validate_continuation_v10` is a contributor with v9's decision\) and strictly-earlier ordering; fixture `track10/test_track10_fail_first\.py` `([0-9a-f]{64})`, 4 tests fail-first \(1 complete-path case with codex's seven V29 rows through driver v14, 1 strict-earlier case in both feed orders, 1 resolver pairwise case — 21 class pairs in both orders — and 1 text\); exhibit `track10/exhibit_precedence_pairs\.py` `([0-9a-f]{64})` \(`V30_PRECEDENCE_EXHIBIT_20260907\.md`\); text tests `track10/test_track10_text_v30\.py` `([0-9a-f]{64})` \(written against THIS text; 1 test\)",
    lambda m: f"`track2/provenance_designs_v11.py` (`{E['P11']}`; UNADOPTED; v11 = v10 + the CLASSIFICATION TABLE (`classify_refusal`, `classify_exception`, exact codes then prefixes, unknown codes flagged), `history_findings_v11` (the history stage as a finding collector on the one snapshot), `validate_continuation_v11` (its standalone form), and EXPIRED before UNAVAILABLE in `authenticate_event`; fixture `track11/test_track11_fail_first.py` `{E['T11']}`, 5 tests fail-first (the FATAL reproduced in executed output, codex's V30-2 and V30-3 rows on the complete path, the classification of every named refusal, 1 text); text tests `track11/test_track11_text_v31.py` `{E['T11T']}` (written against THIS text; 1 test), the retained V30-specific `track10/test_track10_text_v30.py` `{m.group(3)}` (written against V30, run against V30 only), v10 (`track2/provenance_designs_v10.py`, fixture `track10/test_track10_fail_first.py` `{m.group(1)}`, 4 tests — its text case asserts the V30 labels and runs against V30; exhibit `track10/exhibit_precedence_pairs.py` `{m.group(2)}`, `V30_PRECEDENCE_EXHIBIT_20260907.md`, still valid for `PRECEDENCE`/`resolve`, unchanged in v11) retained; v10 = v9 + THE PRECEDENCE ORDER STATED ONCE (`PRECEDENCE`) and ONE RESOLVER (`resolve`, `finding`, `classify_history`, `local_precheck_event`; `validate_continuation_v10` is a contributor with v9's decision) and strictly-earlier ordering")
assert re.findall(r"`\\[0-9]`", s) == []
rp(r"`run_configurations_v14\.py` \(`[0-9a-f]{64}`\) refuses to load an identity unless", f"`run_configurations_v15.py` (`{E['RC15']}`) refuses to load an identity unless")
rp(r"The builder `build_corpus_identity_v31\.py` \(`[0-9a-f]{64}`\)", f"The builder `build_corpus_identity_v31.py` (`{E['B31']}`)")
sub("**3c. V30 — WHAT THE AMENDMENT COSTS","**3c. V31 — WHAT THE AMENDMENT COSTS")
k=s.index("**3c. V31 — WHAT THE AMENDMENT COSTS"); i=s.index("ONE PLACE ENFORCES IT: `provenance_designs_v10.resolve`", k); j=s.index("EXHIBITED PAIRWISE:", i); s=s[:i]+NEWORDER+" "+s[j:]   # the §3c occurrence, not the historical V30 paragraph's
k2=s.index("**3c. V31 — WHAT THE AMENDMENT COSTS"); pre, post = s[:k2], s[k2:]; assert "`V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class) and the complete-path pairs of tracks 8–10." in post; s = pre + post.replace("`V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class) and the complete-path pairs of tracks 8–10.", "`V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class — the resolver is unchanged in v11) and the complete-path pairs of tracks 8–11, including codex's V30 constructions (null open file over a rewrite; wrong repository + failed witness fetch; a missing list id + HTTP 503; a rewrite with an empty feed; an undetermined approval whose feed no longer reaches it with the remote down).", 1)   # §3c occurrence only
sub("V29-1 and V29-2 — the seed re-derivation, remote-head and approval-feed retries that pre-empted the same rules, the fifth and sixth instances of ONE pattern — in THIS candidate, not by a point fix but by the single resolver (`TRACK10_FAIL_FIRST_RECEIPT_20260907.md`); the earlier ones in tracks 1–7.",
    "V29-1 and V29-2 — the seed re-derivation, remote-head and approval-feed retries that pre-empted the same rules — at V30 by the single resolver (`TRACK10_FAIL_FIRST_RECEIPT_20260907.md`); V30-1 (the FATAL: a null open-event file skipped into acceptance), V30-2 (retries before the local sweep, class-0 checks after the resolver, unnormalized exceptions) and V30-3 (helpers suppressing higher findings, unavailability before expiry, three unshared retrievals) — in THIS candidate by the staged finding collector (`TRACK11_FAIL_FIRST_RECEIPT_20260907.md`); the earlier ones in tracks 1–7.")
sub("driver v14 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v30 8 + V15 builder 4 + verdict v30 7","driver v15 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v31 8 + V15 builder 4 + verdict v31 7")
sub("+ track 10 4 + track 10 text (V30-specific) 1 = 141 tests","+ track 10 4 + track 10 text (V30-specific) 1 + track 11 5 + track 11 text (V31-specific) 1 = 147 tests")
sub("the V29-specific text test against the V29 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions, track 10's V29-3 assertions) plus the V30-specific test against THIS text",
    "the V29-specific text test against the V29 text, the V30-specific text test AND track 10's V29-3 text case (it asserts the V30 labels) against the V30 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions, track 11's V30-4 assertions) plus the V31-specific test against THIS text")
sub("141 tests across the 24 suites named in §3c;","147 tests across the 26 suites named in §3c;")
sub("Re-run `coherent_attacks_v30.py` and add counter-cases of your own.","Re-run `coherent_attacks_v31.py` and add counter-cases of your own.")
sub("## 9. Questions for the seats at the V30 complete-package review (STAGED candidate; nothing adopted)","## 9. Questions for the seats at the V31 complete-package review (STAGED candidate; nothing adopted)")
sub("(V30, if ordered; V16–V29's counted)","(V31, if ordered; V16–V30's counted)")
sub("the inspection of THIS candidate is `V30_CANDIDATE_ATTACK_INSPECTION_20260907.md`","the inspection of THIS candidate is `V31_CANDIDATE_ATTACK_INSPECTION_20260907.md`")
for bad in ("3c. V30", "141 tests across", "run_configurations_v14.py`, SHA-256", "V30_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict"): assert bad not in s, bad
open('OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V31_20260907.md','w',encoding='utf-8').write(s); print("V31 text written")
