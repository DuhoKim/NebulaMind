# -*- coding: utf-8 -*-
import os, re, sys; E=os.environ
sys.path.insert(0, '_optionA_dev/fourier_chirality'); sys.path.insert(0, '_optionA_dev/beacon_v2'); sys.path.insert(0, '_optionA_dev/drand_only'); sys.path.insert(0, '_optionA_dev/corpus_identity'); sys.path.insert(0, '_optionA_dev/track2')
import run_configurations_v16 as rc
p='OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V31_20260907.md'; s=open(p, encoding='utf-8').read()
def rp(pat, new):
    global s; m=re.search(pat, s); assert m, pat[:80]; s=s.replace(m.group(0), new, 1)
def rpf(pat, fn):
    global s; m=re.search(pat, s); assert m, pat[:80]; s=s.replace(m.group(0), fn(m), 1)
def sub(old, new):
    global s; assert old in s, old[:80]; s=s.replace(old, new, 1)
NSD = "THE PROPERTY, STATED ONCE AND NAMED — NSD, \"No Stage Decides\" (Blanc's order of 05:02 KST, `BLANC_ORDER_TRACK12_NSD_20260907.md`): NO STAGE MAY DECIDE OR ABORT; EVERY STAGE CONTRIBUTES; ONE RESOLVER DECIDES. Precedence (V27–V29; `PRECEDENCE` / `resolve`, unchanged) is its ordering half; suppression (V30-2/3) and abort-on-prerequisite (V31-1/2) are its contribution half, and codex's four V31 clauses are its consequences, not four promises. WHERE IT IS ENFORCED: `run_configurations_v16.load_identity_composed` — every stage runs on the inputs it truly needs and CONTRIBUTES; a prerequisite failure is itself a finding, contributed by the stage that meets it; the checks that needed it are recorded BLOCKED with the reason; the winner is `provenance_designs_v12.resolve`; ACCEPT only when every REQUIRED stage completed, nothing is blocked and no finding exists; the whole outcome (winner, every finding with its class, code, stage and reason, every stage's state — attempted / completed / refused / blocked — and every blocked check) is recorded in `LAST_OUTCOME`. WHAT THE CODE DOES, exactly (no more is claimed): S0 reads the identity file and sweeps BOTH retained events by type / pinned repository / protected ref WITHOUT git, and requires the history-open file to hold a PushEvent object with an object payload (null and every other value → HISTORY-OPEN-EVENT-INVALID, class 0; the open-event checks are then BLOCKED, not skipped); S0d checks positive non-delivery of the approval commit if git launches (else the check is BLOCKED and IO-UNAVAILABLE is contributed); S0b the list shape / disjointness checks; S0c the local identity integrity (schema, required fields, pool / exclusion / rule digests, beacon shape, outcome, source, witness fields and commit format, event digest, created_at before T_pulse); S1 the witness-bound conjunction (`_conjunction_prefix`, unchanged since v14) as ONE classified stage whose refusal blocks nothing that does not need it — when it refuses, S2–S5 run on the LOCALLY READABLE context (the identity's witness fields, the local repository without fetch, the retained beacon record, the retained approval record accepted only when its digest is the witness's); S2 ONE retrieval that KEEPS every obtained page — a later page's failure is a RETRY finding and the obtained events stay (a partial snapshot yields affirmative findings only; presence, absence, expiry and acceptance are never established from it); malformed remote events are excluded and reported as MALFORMED-REMOTE-EVIDENCE (class 5, remote provenance); S3 the approval event on the snapshot — BOTH contradiction arms (same id; absent + same delivered commit on the pinned ref) before any availability disposition, then the receipt policy for EXPIRED (every receipt failure, exceptions included, is class 3) — and the open event's same-id contradictions on the same snapshot; S4 seed re-derivation with PRODUCTION's own re-deriver on the retained records; S5 `history_findings_v12` — remote head, genesis-only, the open event's delivery and authentication, every adjacent extension relation, every per-entry publication and the ordering relation over the entries found, the working tree against the remote — NO LOOP STOPS at a first defect, and the open-event checks are BLOCKED (recorded) when no valid open event exists. CLASSIFICATION is an ALLOWLIST (`CLASS_ALLOWLIST`: every refusal code the driver and the helpers actually raise, with its class; the track-12 test re-extracts the codes from both sources and requires each to be placed unflagged; any other code — including a new member of a known family — is FLAGGED); exceptions are classified by PROVENANCE (`classify_exception(exc, source)`: retained input → MALFORMED-RETAINED-INPUT class 0; remote evidence → MALFORMED-REMOTE-EVIDENCE class 5; I/O and subprocess → IO-UNAVAILABLE class 5). The one raise outside the collector is its own dependency: an unimportable provenance module is named VERIFIER-UNAVAILABLE, class 5 by cause. The standalone `validate_continuation_v12` sweeps the open event locally before consulting the remote (sharing the composed path's obligation). "
def table():
    rows = ["| check | stage | needs | can contribute |", "|---|---|---|---|"]
    for c, spec in rc.INDEPENDENCE.items(): rows.append(f"| {c} | {spec['stage']} | {', '.join(sorted(spec['needs']))} | {', '.join(sorted(spec['codes']))} |")
    pre = ["| prerequisite | how it fails | the finding it contributes |", "|---|---|---|"] + [f"| {k} | {v['how']} | {v['failure_code']} ({v['class']}) |" for k, v in rc.PREREQUISITES.items()]
    return "THE INDEPENDENCE TABLE — the property made checkable (rendered from `run_configurations_v16.INDEPENDENCE` / `PREREQUISITES`, the code's own data; the controls of track 12 are DERIVED from it: for each prerequisite that can fail and each check that does not need it, one test plants that check's defect, makes the prerequisite fail and asserts the WHOLE outcome — 66 (prerequisite, check) pairs; codex's V31 constructions are regressions on top, not the definition of coverage):\n\n" + "\n".join(rows) + "\n\n" + "\n".join(pre) + "\n\n"
sub('DRAFT V31 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED)','DRAFT V32 — STAGED CANDIDATE (NOT GATED, NOT ADOPTED, NOT APPROVED)')
sub('V31 of 2026-09-07 is V30 after its complete-package review',
 "V32 of 2026-09-07 is V31 after its complete-package review (agy SIGNABLE; codex NOT-SIGNABLE with four MAJORs and one MINOR, NO fatal, the V30 FATAL confirmed repaired — `V31_REVIEW_OUTCOME_20260907.md`), with codex's V31-1/2/3/4/5 answered under BLANC'S ORDERS OF 05:02 and 05:09 KST (`BLANC_ORDER_TRACK12_NSD_20260907.md`, `BLANC_ORDER_TRACK12_EVIDENCE_SCOPE_20260907.md`). " + NSD + "(V31-1) a failed prerequisite no longer suppresses independent checks (S1's refusal blocks nothing that does not need it; the local sweep needs no git; the helper import is named); (V31-2) obtained pages are kept, partial snapshots yield affirmative findings only; (V31-3) no loop stops, both contradiction arms precede availability, the standalone path sweeps locally; (V31-4) the allowlist, provenance-classified exceptions, the receipt policy for exceptions; (V31-5) stage states and reasons recorded, and the track-11 receipt's wording corrected in the V31 outcome. FAIL-FIRST, PER SUBCASE (Blanc 05:02 item 3, 05:09; `TRACK12_FAIL_FIRST_RECEIPT_20260907.md`, with the original nine-method run preserved unaltered in `TRACK12_FAIL_FIRST_RUN1_ORIGINAL_PRESERVED_20260907.md` and its reach stated per subcase): run 1d against byte-copies of the V31 modules with the table appended as data only — 84 methods, each one subcase: 42 BEHAVIOURAL failures (the verdict assertion failed on the old bytes), 37 NEW-INTERFACE (the old bytes gave the right verdict; the method failed only at the whole-outcome name — no fail-first standing), 1 ALREADY-RIGHT, 1 raw ImportError that is itself codex's V31-1 demonstration; run 2/2b against the successors: 84 OK after three disclosed refinements of the control's whole-outcome clause and one code change that made the table's seed claim true; run 2c: every existing precedence test of tracks 8–11 (15) OK, unchanged. V31 of 2026-09-07 was V30 after its complete-package review")
sub('History: V16–V21 were refused at gates and V22–V30 at complete-package reviews (each by codex; agy passed all fifteen);','History: V16–V21 were refused at gates and V22–V31 at complete-package reviews (each by codex; agy passed all sixteen);')
rp(r"DRIVER: `run_configurations_v15\.py`, SHA-256 `[0-9a-f]{64}` \(V31 STAGED:", f"DRIVER: `run_configurations_v16.py`, SHA-256 `{E['RC16']}` (V32 STAGED: `run_configurations_v15.py` — reviewed at V31, retained — with the composed path rebuilt under NSD (`load_identity_composed`: independent contributing stages S0/S0d/S0b/S0c/S1/S2/S3/S4/S5, the INDEPENDENCE / PREREQUISITES table, LAST_OUTCOME); V31 STAGED was:")
rp(r"fixture `test_run_configurations_v15\.py` `[0-9a-f]{64}`, 23 tests", f"fixture `test_run_configurations_v16.py` `{E['TRC16']}`, 23 tests")
sub('the track-2 v11 helpers called INSIDE `load_identity` on the production call path','the track-2 v12 helpers called INSIDE `load_identity` on the production call path')
rp(r"`build_corpus_identity_v31\.py`, SHA-256 `[0-9a-f]{64}` \(V31 STAGED:", f"`build_corpus_identity_v32.py`, SHA-256 `{E['B32']}` (V32 STAGED: on provenance_designs_v12; fixture `test_build_corpus_identity_v32.py` `{E['TB32']}`, 8 tests; V31 was:")
rp(r"`_optionA_dev/beacon_v2/beacon_record_drand_v31\.py` `[0-9a-f]{64}` \(V31:", f"`_optionA_dev/beacon_v2/beacon_record_drand_v32.py` `{E['BD32']}` (V32: on provenance_designs_v12; v31 retained; V31:")
rp(r"exercised by `test_beacon_record_drand_v31\.py` \(`[0-9a-f]{64}`, 7 tests", f"exercised by `test_beacon_record_drand_v32.py` (`{E['TBD32']}`, 7 tests")
nb=s.count('beacon_record_drand_v31'); s=s.replace('beacon_record_drand_v31','beacon_record_drand_v32'); nbu=len(re.findall(r'(?<!test_)build_corpus_identity_v31', s)); s=re.sub(r'(?<!test_)build_corpus_identity_v31','build_corpus_identity_v32', s); print('token replacements: beacon', nb, 'builder (non-fixture)', nbu)
rp(r"`coherent_attacks_v31\.py` \(`[0-9a-f]{64}`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v15 / provenance v11 — `V31_CANDIDATE_ATTACK_INSPECTION_20260907\.md`, same verdict in every row as V30;", f"`coherent_attacks_v32.py` (`{E['ATT32']}`; rows 9m–9u = codex's V24 N2/N3, V25 P1/P2 and V26-1/2 cases, re-executed on driver v16 / provenance v12 — `V32_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict in every row as V31;")
rpf(r"`track2/provenance_designs_v11\.py` \(`[0-9a-f]{64}`; UNADOPTED; v11 = v10 \+ the CLASSIFICATION TABLE \(`classify_refusal`, `classify_exception`, exact codes then prefixes, unknown codes flagged\), `history_findings_v11` \(the history stage as a finding collector on the one snapshot\), `validate_continuation_v11` \(its standalone form\), and EXPIRED before UNAVAILABLE in `authenticate_event`; fixture `track11/test_track11_fail_first\.py` `([0-9a-f]{64})`, 5 tests fail-first \(the FATAL reproduced in executed output, codex's V30-2 and V30-3 rows on the complete path, the classification of every named refusal, 1 text\); text tests `track11/test_track11_text_v31\.py` `([0-9a-f]{64})` \(written against THIS text; 1 test\)",
    lambda m: f"`track2/provenance_designs_v12.py` (`{E['P12']}`; UNADOPTED; v12 = v11 + page-keeping `retrieve_events` (partial snapshots), both contradiction arms before availability and the `complete` flag in `authenticate_event`, `history_findings_v12` (no loop stops; blocked open-event checks), `validate_continuation_v12` (local sweep first), `CLASS_ALLOWLIST` / `classify_refusal` (no family prefixes) / `classify_exception(exc, source)` (provenance), receipt failures normalized; fixture `track12/test_track12_fail_first.py` `{E['T12']}`, 84 tests fail-first, one subcase each (66 controls DERIVED from the INDEPENDENCE table + 17 regressions from codex's V31 constructions + 1 text); text tests `track12/test_track12_text_v32.py` `{E['T12T']}` (written against THIS text; 1 test), the retained V31-specific `track11/test_track11_text_v31.py` `{m.group(2)}` (written against V31, run against V31 only), v11 (`track2/provenance_designs_v11.py`, fixture `track11/test_track11_fail_first.py` `{m.group(1)}`, 5 tests — its text case written against V31) retained; v11 = v10 + the CLASSIFICATION TABLE (`classify_refusal`, `classify_exception`, exact codes then prefixes, unknown codes flagged), `history_findings_v11` (the history stage as a finding collector on the one snapshot), `validate_continuation_v11` (its standalone form), and EXPIRED before UNAVAILABLE in `authenticate_event`")
assert re.findall(r"`\\[0-9]`", s) == []
rp(r"`run_configurations_v15\.py` \(`[0-9a-f]{64}`\) refuses to load an identity unless", f"`run_configurations_v16.py` (`{E['RC16']}`) refuses to load an identity unless")
rp(r"The builder `build_corpus_identity_v32\.py` \(`[0-9a-f]{64}`\)", f"The builder `build_corpus_identity_v32.py` (`{E['B32']}`)")
sub("**3c. V31 — WHAT THE AMENDMENT COSTS","**3c. V32 — WHAT THE AMENDMENT COSTS")
k=s.index("**3c. V32 — WHAT THE AMENDMENT COSTS"); i=s.index("ONE PLACE ENFORCES IT (v15):", k); j=s.index("EXHIBITED PAIRWISE:", i); s=s[:i]+NSD+table().replace("\n", " ⏎ ")+" "+s[j:]
k2=s.index("**3c. V32 — WHAT THE AMENDMENT COSTS"); pre, post = s[:k2], s[k2:]
old_ex="`V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class — the resolver is unchanged in v11) and the complete-path pairs of tracks 8–11, including codex's V30 constructions (null open file over a rewrite; wrong repository + failed witness fetch; a missing list id + HTTP 503; a rewrite with an empty feed; an undetermined approval whose feed no longer reaches it with the remote down)."
assert old_ex in post
s = pre + post.replace(old_ex, "`V30_PRECEDENCE_EXHIBIT_20260907.md` (21 class pairs, both derivation orders, same winner, always the higher class — the resolver is unchanged in v12) and, for the contribution half, the 66 table-derived controls of track 12 (every (prerequisite, check) pair with the check independent of the prerequisite: the defect is contributed, the prerequisite failure is contributed, the checks that needed it are blocked or contribute unavailability, the winner is the resolver's), with codex's V30 and V31 constructions as regressions on top.", 1)
sub("V30-1 (the FATAL: a null open-event file skipped into acceptance), V30-2 (retries before the local sweep, class-0 checks after the resolver, unnormalized exceptions) and V30-3 (helpers suppressing higher findings, unavailability before expiry, three unshared retrievals) — in THIS candidate by the staged finding collector (`TRACK11_FAIL_FIRST_RECEIPT_20260907.md`); the earlier ones in tracks 1–7.",
    "V30-1 (the FATAL: a null open-event file skipped into acceptance), V30-2 and V30-3 — at V31 by the staged finding collector (`TRACK11_FAIL_FIRST_RECEIPT_20260907.md`); V31-1 (a failed S1 suppressing independent checks; S0's git launch before its local sweep; the helper import outside the wrapper), V31-2 (a failed later page discarding obtained contradictions), V31-3 (loops stopping early; the same-commit arm not evaluated for an undetermined delivery; the standalone path returning on remote failure) and V31-4 (the class table not total; family prefixes silencing unknown codes; remote malformed evidence classed as retained; receipt exceptions escaping the receipt policy) — in THIS candidate under NSD (`TRACK12_FAIL_FIRST_RECEIPT_20260907.md`); the earlier ones in tracks 1–7.")
sub("driver v15 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v31 8 + V15 builder 4 + verdict v31 7","driver v16 23 + V15 driver 17 + history_v2 4 + witness v4 3 + builder v32 8 + V15 builder 4 + verdict v32 7")
sub("+ track 11 5 + track 11 text (V31-specific) 1 = 147 tests","+ track 11 5 + track 11 text (V31-specific) 1 + track 12 84 + track 12 text (V32-specific) 1 = 232 tests")
sub("the V30-specific text test AND track 10's V29-3 text case (it asserts the V30 labels) against the V30 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions, track 11's V30-4 assertions) plus the V31-specific test against THIS text",
    "the V30-specific text test AND track 10's V29-3 text case (it asserts the V30 labels) against the V30 text, the V31-specific text test AND track 11's V30-4 text case against the V31 text, and only the version-agnostic tests (tracks 1/3/4, the track-6 sweep, track 7's fail-first, track 9's V28-3 assertions, track 12's V31 text assertions) plus the V32-specific test against THIS text")
sub("147 tests across the 26 suites named in §3c;","232 tests across the 28 suites named in §3c;")
sub("Re-run `coherent_attacks_v31.py` and add counter-cases of your own.","Re-run `coherent_attacks_v32.py` and add counter-cases of your own.")
sub("## 9. Questions for the seats at the V31 complete-package review (STAGED candidate; nothing adopted)","## 9. Questions for the seats at the V32 complete-package review (STAGED candidate; nothing adopted)")
sub("(V31, if ordered; V16–V30's counted)","(V32, if ordered; V16–V31's counted)")
sub("the inspection of THIS candidate is `V31_CANDIDATE_ATTACK_INSPECTION_20260907.md`","the inspection of THIS candidate is `V32_CANDIDATE_ATTACK_INSPECTION_20260907.md`")
sec = s[s.index("**3c. V32"):]; sec = sec[:sec.index("\n")]
for bad in ("every derivable finding", "nothing downstream is skipped", "none returning before the others are derived"): assert bad not in sec, bad
for bad in ("3c. V31", "147 tests across", "run_configurations_v15.py`, SHA-256", "V31_CANDIDATE_ATTACK_INSPECTION_20260907.md`, same verdict"): assert bad not in s, bad
open('OPTION_A_INSTRUMENT_SELECTION_RULE_DRAFT_V32_20260907.md','w',encoding='utf-8').write(s); print("V32 text written")
