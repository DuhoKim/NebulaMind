# AGY V136 REFEREE REPORT

## TASK A — MINIMALITY
PASS. The diff between V135 and V136 contains exactly and only the allowed changes:
- Preamble update (version + amendment paragraph with V136 substituted).
- §7 BS-2a row updated.
- §10 generated V134 → V135 row only (no V135 → V136 row is in the draft, as the sidecar carries it).
- §11 (BS-2a schema entry; deferred build item discharged; known limit paragraph truthfully updated).
- Signature lines.
V135's committed bytes are unchanged, and the P0 manifest is intact (30/30 OK).

## TASK B — THE FILL IS TRUE
PASS (with wording findings).
- `bs2a_quality_gate.py` sha `dfbd63d146b472f194f74d01b313874f23c9a4264f26903b22837ae32aa18508` is correct.
- The three constants match exactly digit by digit: `flux_ivar_r > 8.4000532`, `psfsize_r < 1.5699703`, `nobs_r >= 3`.
- `receipt_strict.py` sha `27e88520ad982ea83330b8147c44dda842d1217f85c1fffb73ae147cc6630701` matches.
- Evidence-schema and verifier digests are exactly as pinned.
- The `BS-2a` candidate validates through `receipt_strict("BS-2a", ...)` with exactly the seven fields the row names.
- Classification is `DESIGN-IDENTITIES-ONLY`. It is honest that no catalogue row was evaluated.
- The filling respects the VALUE-vs-DESIGN rule.
- The row's history is preserved below the FILLED line.
- **Defect**: The slot's `blocks` column still lists `BS-2f, BS-6`. Because no catalogue row is evaluated, it does not need cutouts, meaning it no longer blocks BS-6. The prompt specifically asked if it now correctly blocks BS-2f and nothing else. It still formally claims to block BS-6 in the column.

## TASK C — THE VERSION CHAIN OF receipt_strict.py
PASS (but UNSOUND).
- The BS-2v candidate is still valid under `27e88520` and the `BS2V-V1` entry is byte-identical between the two versions (`f50d8c1d` vs `27e88520`).
- The chain is disclosed, but the mechanism is **unsound**. Because the entire file's SHA is pinned per slot sequentially, a later fill (like BS-2a) produces a new global file SHA (`27e88520`). If a later fill silently alters an earlier slot's schema (e.g. `BS-2v`), the later file SHA will accept a forged receipt or silently enforce the altered fields. A future reader verifying BS-2a checks `27e88520`, but the safety of BS-2v within that same codebase is compromised unless they manually verify structural equivalence with `f50d8c1d`. 
- **Repair**: Require per-entry digests for `SLOT_SCHEMA_SUCCESSOR`, or formally assert in §11 that any `receipt_strict.py` version must be structurally identical to its predecessor for all earlier entries.

## TASK D — TOOLING
PASS.
- `tools/prereg_trace.py . --check V136`: 135 computed transition(s); 0 problem(s).
- `tools/prereg_lint.py V136`: 97 finding(s), 0 blocking.
- `tools/prereg_counts.py V136`: prose already matches the table.
- `receipt_strict` fixtures pass (8/8 PASS).
- `gates/FINDINGS_MAP.md` correctly cites direction #58 and the BS-2a pins.
- `ref/STRING_FIELD_REGISTRY.md` changed only its provenance label.

## FINDINGS

1. SEVERITY: BLOCKING (Wording-only)
   FILE: PREREG_SUCCESSOR_DRAFT_V136_20260903.md:930
   CLAUSE: BS-2a row blocks column
   REPAIR: Remove `BS-6` from the `blocks` column in the BS-2a row, as BS-2a evaluated no catalogue row and needs no cutouts, and therefore does not block BS-6. The column should read `BS-2f`.

2. SEVERITY: BLOCKING (Wording-only / Architecture)
   FILE: PREREG_SUCCESSOR_DRAFT_V136_20260903.md:1260
   CLAUSE: Strict receipt schema chaining mechanism
   REPAIR: The sequential accumulation in `receipt_strict.py` is unsound because a later file pin could silently alter an earlier slot's schema. Add a requirement in §11 for per-entry digests of the schema dict, OR an explicit rule that any updated `receipt_strict.py` must preserve the exact byte-for-byte schema of all prior entries to prevent silent alterations of earlier schemas.

SEAT: AGY
VERSION: V136-REFEREE-V1
VERDICT: SIGNABLE-AFTER-REPAIRS
MINIMALITY: PASS
P0_MANIFEST: 30/30 OK
BS2V_STILL_VALID: yes
COUNT: 2
