ACCESS_SHA=6b3ff1301546f6595582c0f5d5afe8e729f187e753fc1b63653af6eaf7b75377

**REFEREE REPORT — V136 Amendment (BS-2a fill) Round 2**

**TASK A — CLOSURE**
*   **F1 (CLOSED):** Verified in §7. The BS-2a row's `blocks` cell has been repaired to name `BS-2f` only, correctly reflecting that the design-identity candidate evaluates no catalogue row and does not require C2/E processes to run.
*   **F2 (CLOSED):** Recomputed the canonical JSON entry digests for BS-2v, BS-2a, and BS-3g according to the §11 rule (sorted keys, no spaces, specific structure) and confirmed they exactly match the recorded digests in the text (`c5931c40...`, `e3c08ea6...`, `eb8589f5...`). Executed `run/receipt_strict.py`, confirming 10/10 fixtures pass. The fixtures internally verify `assert_entries_preserved()` refuses an execution where the `BS-2v` schema list is mutated, and confirm both BS-2v and BS-2a candidates validate successfully. The §11 rule explicitly mandates that subsequent versions MUST preserve entries using `assert_entries_preserved()`.

**TASK B — MINIMALITY**
*   **PASS:** Inspected the amendments to the BS-2v row (line 932) and BS-3g §11 entry (line 1279). Both are strictly append-only insertions of their respective entry digests into the text. The V135 signed texts remain exactly preserved up to those insertions. 

**TASK C — TOOLING**
*   `tools/prereg_trace.py --check PREREG_SUCCESSOR_DRAFT_V136_20260903.md .` executed with 0 problems.
*   `tools/prereg_lint.py PREREG_SUCCESSOR_DRAFT_V136_20260903.md` executed with 0 blocking findings (97 advisory).
*   `tools/prereg_counts.py` aligns with prose claims.
*   The `gates/FINDINGS_MAP.md` sidecar includes the correct V135 → V136 transition and properly documents the R1 and R2 repairs (including entry digests). 
*   The `ref/STRING_FIELD_REGISTRY.md` sidecar shows the generated label `V136` correctly.

**TASK D — NEW DEFECTS**
*   None.

SEAT: AGY
VERSION: V136-REFEREE-V2
VERDICT: SIGNABLE
F1: CLOSED
F2: CLOSED
MINIMALITY: PASS
COUNT: 0
