# FORM-ECHO STOPPING RULING — FREEZE WITH DISCLOSURE — 2026-08-31 ~00:40 KST

**The principal's selection, verbatim option label (relayed by the coordinator, who put
the round-6 non-monotone turn to him with three options):**

> Freeze with disclosure (Recommended)

**Scope, as presented and ruled — nothing wider:** STOP repairing the FORM ECHO checker;
QUOTE BOTH round-6 findings VERBATIM (CODEX-V122A F1, the character-offset-vs-byte-
distance measure; GPT56-V122A F1, the nested-parenthesis `[^)]*` gap) in the appendix as
NAMED LIMITATIONS of the FORM ECHO tripwire; freeze the instrument on that basis. No
further repair-verify mini-rounds on the FORM lineage. Checker hardening stays available
later via a repaired-and-refereed successor revision during build-out, per the appendix's
own exit clause.

**Reconciliation of the relay race (the lane's mechanical call, disclosed):** V123 — an
un-refereed FORM-echo hardening staged at ~00:40 with read-7 dispatched at 00:41 — is
REVERTED: read-7 was stopped mid-flight with zero outputs
(`gates/READ7_STOPPED_BY_RULING_20260831.md`); the frozen instrument is the V122-state
checker, so the disclosed limitations are exactly the frozen bytes' limitations; the
V123 code is parked in git history (commit 2c4d06b3e) as the candidate successor
revision, admissible later only through the exit clause.

**Recorded also in the track's human-direction history** (direction #14).
