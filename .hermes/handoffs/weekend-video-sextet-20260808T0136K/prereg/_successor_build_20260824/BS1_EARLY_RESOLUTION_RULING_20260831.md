# BS-1 EARLY RESOLUTION RULING — 2026-08-31, DISCLOSED SUPERSEDING FORM

**The principal's words, verbatim (relayed via Blanc/OPS):**

> are you sure dr11 will be available sep-5? start from the data already availalbe first.

**Reading, as put to him and not contradicted:** BS-1 resolves toward the committed
fallback — **Branch B, DR10.1, the already-available release** — NOW, rather than
waiting out the Sep-5 rule date. The DR11 wait ends.

## Why this is a SUPERSEDING ruling and not an application — from the frozen bytes

The frozen resolution rule (draft §2.1, quoted exactly):

> BS-1 is filled on the earlier of (i) the day DR11 photo-z is confirmed
> available, or (ii) **2026-09-05**. On (ii) with photo-z still absent, Branch B
> is selected and the choice-point closes; waiting further requires a gated
> amendment.

A principal resolution on 2026-08-31 is **neither trigger (i)** (DR11 photo-z was
measured ABSENT at today's 06:59 daily check) **nor trigger (ii)** (it is not the
date). The frozen bytes therefore do not admit this as a plain application, and it
is recorded in the established superseding form instead — which is **exactly what
the brief promised him in advance** (`BS1_RELEASE_CHOICE_BRIEF_20260830.md`, line
59: *"override from you before that date supersedes the rule; silence lets it
fire as designed"*). No divergence between what he was told and what this is.

## Why early resolution does NOT damage the freeze candidate — checked before executing

**Branch B is the draft's own pinned branch.** §2.1's frozen text: *"For Branch B —
the pinned branch — nothing else in this document changes"* — the geometry
sidecar, count table, selection, parent, and 12,117-brick closure are all DR10
artifacts already; the release-universe identity (`863e5ded…`, cardinality
366,912) is pinned **on Branch B**. The order selects the branch every pin in the
freeze candidate already assumes. It closes the choice-point five days early and
changes no other byte's meaning. (The dangerous direction — Branch A — would have
voided the §0 pin; that is the direction this ruling forecloses.) The STOP
condition in the relay was checked and does not obtain.

**Convergence note, for the record:** DR11 photo-z is absent today, so the branch
this ruling selects is the same branch trigger (ii) would have selected on Sep-5
had the absence persisted — the ruling removes the wait and the residual
uncertainty, not the outcome the measurement was pointing at.

## What is superseded, exactly

The two-trigger fill clause quoted above — its enumeration no longer governs; the
choice-point is **CLOSED on Branch B by this ruling**, recorded at V134 with the
superseded clause quoted dead in place. The **branch-invariance requirement is
NOT superseded** and stands: BS-1's receipt must still show every §7 downstream
artifact produced by the same code path, differing only in recorded input
paths/versions.

## What this unblocks

The run preparation no longer waits on Sep-5: **the calibration rehearsal moves
into the P0 queue** — the package (V124 text + final appendix + pins V125–V131 +
transitions V132–V134 + the commitments) awaits only the P0 freeze signature.

## The daily DR11 check — disposition (my call, disclosed)

RETIRED as a gate input (the choice-point it fed is closed). It continues
**information-only** through P0 — one line in the morning report, because a later
DR11 photo-z appearance is now pure information: it changes nothing, and saying
so on the record beats silently dropping a measurement Duho was told about.

Recorded as human direction #19 in the spin-parity history.
