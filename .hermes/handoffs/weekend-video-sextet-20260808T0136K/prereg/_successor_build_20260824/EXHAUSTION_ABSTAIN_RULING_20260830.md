# REPLAY EXHAUSTION → ABSTAIN — RULED — 2026-08-30 20:54 KST

**The principal's word, verbatim (direct message to Hwao's session, answering the one-word
ask "halt, abstain, or replace"; the fourth and final parked decision):**

> abstain

**What this rules.** The V94 flag ("exhaustion has a named terminal: the run HALTS
pre-BS-8f... softening the halt — an ABSTAIN label, or member replacement — is YOURS to
rule") is resolved as **ABSTAIN**, by the person who designed the hand-check procedure:

- When interruptions consume ALL `R_max` committed renders of one allocated object before
  the checker reaches a decision, that object receives an **ABSTAIN label** from that
  member, and **the run CONTINUES**. The hard pre-BS-8f halt is SUPERSEDED for this case.
- **ABSTAIN joins the sealed hand-check label vocabulary** — one more legal value in Row H's
  ONE persisted label set, sealed χ-side with every other label. It is a labeling outcome,
  not a new event class.
- **The chain pattern is unchanged**: every object still shows exactly `R_max` contiguous
  render commits and NO per-object label event (the Row-H-TRUE pattern), so the
  no-timing-channel and constant-multiplicity arguments stand exactly as built. Abstention
  is caused by interruption count, which is procedural and fixed-budget — handedness-blind
  by the same argument that justified padding.
- **Sample completeness re-reads as every-object-labeled**, with ABSTAIN a label:
  `require_complete_sample()`'s refusal still guards a partial traversal; an abstained
  object is a labeled object whose label contributes no handedness call and drops out of
  the analysis sample, honestly recorded.
- The BS-2k labeling deadline backstop is unchanged.

**Scope guard.** Member replacement (option c) is NOT taken. The hard halt survives for
everything else it covered — this ruling touches only the one-object replay-exhaustion
case. Nothing here changes the ruled refusal vocabulary, the render/traversal rules, v9
(`6a9abbbd…`), BS-6, or the first image byte.

**Fold plan (V113 — V112 is sha-pinned in a live round):** the §5-derived hand-check
passage (the "exhaustion HALTS the run pre-BS-8f" clause, GPT56-V93 F6 lineage) re-derived
to the ABSTAIN rule with the old halt quoted dead; the spec's §5 multiplicity/exhaustion
text updated to match; the label-set declaration gains ABSTAIN; completeness wording
re-derived; DECISIONS_FOR_DUHO.md updated (this commit); sweep token for the dead
halt-on-exhaustion phrasing.

**Recorded also in the track's human-direction history** (direction #12 in
`spin-parity_history.json`).
