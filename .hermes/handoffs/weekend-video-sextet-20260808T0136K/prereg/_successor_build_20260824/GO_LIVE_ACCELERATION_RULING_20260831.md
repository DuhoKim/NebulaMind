# GO-LIVE ACCELERATION RULING — 2026-08-31, DISCLOSED SUPERSEDING FORM

**The principal's selection, verbatim option label (relayed via Blanc/OPS,
presented with the Sep-5 default and both options):**

> Go-live when staging is done

**What this rules.** The frozen slot table's "BS-1 blocks everything" dependency
edge — which the codex RUNOPEN-V1 review identified as barring any live start
before the BS-1 receipt — is EXPRESSLY SUPERSEDED for the run's start: the chain
goes live and the first real data touch happens as soon as staging-v2 completes
AND verifies, not at the 2026-09-05 default. His selection is the ruling; the
board's silence-default (Option 1) is superseded by this express choice.

**What this does NOT supersede, stated so it cannot be mistaken later:** the
frozen `v9.resolve_branch` date-gate on the BS-1 receipt itself. No ruling can
make a truthful pre-Sep-5 Branch-B stamp exist (the probe result and the
resolution date are the only inputs the frozen function accepts; codex confirmed
no third path). The run therefore opens with BS-1 in the
**resolved-by-ruling / receipt-pending-at-its-date** state — the branch's
substance governs (Branch B, the early-resolution ruling), and the literal
receipt lands 2026-09-05 by v9's own guard, joining the chain then. Every gate
that binds the receipt does so from that date; everything earlier records the
pending state honestly.

**Sequence from here:** staging-v2 (morning block, as paced) → its verify passes
(codex/agy per burn discipline) → ON VERIFICATION, go-live: Row B starts, the
epoch-1 opening appends, provisioning completes, and the P1+ machine sequence
begins on the already-available Branch-B data. Human waypoints at go-live
surface via Blanc in plain words.

Recorded as human direction #22.
