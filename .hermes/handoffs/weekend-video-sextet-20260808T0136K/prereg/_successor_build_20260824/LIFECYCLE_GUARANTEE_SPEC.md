**STATUS: SPEC — the access-log lifecycle's guarantee, stated as invariants before any further
construction. Written at the THREE-FAILURE THRESHOLD (V67 F1/F2, V68 F1/F2, V69 F1/F2 — one object,
three consecutive rounds, a new corner each time), under the rule this lane wrote after the citation
check failed the same way: the third failure means stop patching, because the corners keep appearing
when the purpose was never stated. The draft's lifecycle text is now DERIVED from this file; a
conflict between them is a defect in the draft.**

# WHAT THE ACCESS-LOG LIFECYCLE GUARANTEES — invariants by crash window and by reader

## 0. Objects and definitions

- **TOUCH** — bytes leaving a sealed store, or landing in one. Two kinds of leaving: **CONVEYANCE**
  (bytes to another row's process) and **RENDER** (bytes to Row G's sealed interface for display —
  **the render's store effect is the conveyance-to-interface, a DISTINCT kind with its own
  store-qualified operation token, one event of one kind**; the human's perception is never an
  event, and the display itself is bounded by G6's session, not logged as a second touch —
  GPT56-V81 F3: wording that made a render event "assert conveyance" read as one event serving two
  kinds). One kind of landing: **WRITE**.
- **TOUCH COMMIT** — one atomic commit in the BS-2k transactional domain:
  `{store effect, its one event carrying the effect's true outcome, Row B's identifier binding}`.
- **REFUSAL COMMIT** — the same commit with no store effect: `{event, binding}`.
- **DELIVERY** — what happens to already-conveyed bytes after their commit (a row's process consuming
  them; a human perceiving a rendered frame). **Delivery is not a touch.**
- **READERS** — the four parties who consult the record: the **requester** (external), the
  **enumeration/BS-L verifiers** (read chain + checkpoint materials at gates), the **post-hoc
  auditor** (reads everything after the run), and **Row B itself on recovery** (reads its own committed bindings for CUSTODY — never deciding custody from the chain, the hash-chains-launder-tampering shape — and reads the chain's ARRIVAL events for WORK DISCOVERY, which is the write-ahead receipt's designed purpose: GPT56-V99 F2 caught the flat never-the-chain wording contradicting the recovery duty the arrival class exists to serve; custody and discovery answer different questions and read different objects).

## 1. The guarantees

| # | invariant | holds for |
|---|---|---|
| G1 | **No unlogged touch**: no bytes leave or land in a sealed store without a committed event | every touch kind |
| G2 | **No false event**: a touch event's outcome field is true of the store effect it records; **a refusal event truthfully records that a request was refused and NO store effect occurred — AND its reason token is true of the refusal: a request with no completed permission verdict may carry only `REFUSED-UNCLASSIFIED`, and any specific code asserts its condition was actually established** (GPT56-V71 F2: without this, a false specific code bypasses the catch-all enumeration entirely) — **and an ARRIVAL event is under this invariant too: it truthfully records RECEIPT — its identifying facts are those of one real request, appended before any processing of that request (extended over the arrival class, GPT56-V88 F1)** | every event |
| G3 | **One TOUCH event per touch** — and **the event classes PARTITION: every committed event is exactly one of an ARRIVAL event (§1c), one touch's event, or one refusal's event — no event is two of these and no event is none** (re-derived over the arrival class — GPT56-V88 F1: the pre-arrival form said every event is a touch's or a refusal's and no event is neither, which forbade the class the same sitting authorised; V70's lesson — an event partition must name ALL its classes, GPT56-V70 F1, CODEX-V70 F1 — applied to three) | every event |
| G4 | **No double decision**: one request never yields two DECISION events — a touch commit or a refusal commit — and, since the WRITE-AHEAD ARRIVAL RECEIPT was authorised as its own event class (principal ruling, 2026-08-30 10:46), a completed request carries exactly ONE arrival event and ONE decision event; this invariant counts decisions only. The pre-arrival form said two EVENTS bare, which condemned the arrival receipt the same sitting authorised — swept as a set with GPT56/CODEX-V87 F1. | every request |
| G6 | **A view is the display session of one render commit**: it ends at the first of position advance, interface clear, or any interruption of continuous display — visibility loss, blanking, occlusion, navigation away; duration alone does not multiply views, nothing displayed after an interruption is the same view, and commit↔session ownership is one-to-one — each render commit opens at most one session and every session is opened by exactly one commit | Row G only |
| G5 | **Render = touch.** Every render is its own touch with its own committed event. **Row G's *"any unlogged view"* void clause is a consumer of G5, not an exception to N1** | Row G only |

## 1b. Operations that are in NO cell because they are FORBIDDEN, not unmodelled

**Delete, truncate, and custody-relevant metadata mutation of a sealed object are not touch kinds —
they are forbidden operations** (GPT56-V71 F7 asked which cell they land in; the answer is none, on
purpose, and here is the purpose). No row's stated surface includes destroying or mutating a sealed
object, so **via Row B such a request is a REFUSAL COMMIT (`REFUSED-ROW-NOT-AUTHORISED` or
`REFUSED-OUTSIDE-STATED-SURFACE`)**, fully covered by the refusal invariants; **outside Row B it is a
bypass of the mediator — `VOID-5-FORBIDDEN-ACT` / digest deviation**, which the covenant already
claims at phase Any. **The commit domain does not model operations whose occurrence voids the run; it
refuses the request or the run dies.** An unmodelled-but-possible operation would be a spec hole; a
forbidden one is a wall.

## 1c. The ARRIVAL RECEIPT — the second event class, authorised by ruling (2026-08-30 10:46)

**Every request's ARRIVAL is durably logged BEFORE any processing begins** — a write-ahead event
carrying the request's identifying facts (row, operation, object identity, timestamp — and the AUTHENTICATED CLOCK PAIR `boot_epoch` + `monotonic_reading`, the durable start the §3b deadline is measured from), appended by
Row B on receipt. **This is a second event class, and it changes what the access log records: the
principal authorised exactly that.** What it buys: **no real request can vanish** — a request that
dies anywhere after arrival has its arrival event, so the crash-before-commit case is no longer
invisible; the pre-verdict death that N2 spent eleven revisions naming as a residue is now a logged
arrival with no terminal event, which recovery and the auditor can SEE and the deadline machinery
can close. An arrival event is not a touch and satisfies no touch invariant; a touch without a
preceding arrival is refused by the verifier as malformed history. **One ARRIVAL per request, appended exactly once at receipt by the single serialised writer; recovery resumes from the logged arrival and never re-appends it — a second arrival for one request is malformed history, refused the same way (GPT56-V88 F4's duplicate branch).** **THE RECEIPT TRANSITION (GPT56-V89 F2, CODEX-V89 F5 — "before any processing" needs a boundary, or a half-arrived request sits on an undefined side of it): a request EXISTS exactly when (1) its complete framed unit — length-prefixed and digest-closed under the BS-2k wire schema — has been fully read and decoded, and (2) its ARRIVAL event's framed unit is durably committed. Frame decode is part of RECEIPT, not processing; "processing" begins at authorisation. A truncated, malformed or partial frame is BY DEFINITION not a request — wire noise outside the lifecycle, in the only sense the log can speak — and a crash mid-arrival-append fails the event's digest closure and is no arrival. The no-vanish guarantee is scoped to completely framed, schema-decodable requests, and this sentence is that scoping.**

## 2. The non-guarantees, with equal weight

| # | non-guarantee | why it cannot be otherwise |
|---|---|---|
| N1 | **Delivery is outside the custody claim** — the event records the store effect, never the requester's receipt or the human's perception | the requester and the human are external to any commit domain; three orderings failed trying to include them |
| N2 | **RETIRED BY RULING (2026-08-30 10:46): the WRITE-AHEAD ARRIVAL RECEIPT makes every real request durably visible** — arrival is logged BEFORE any processing, as a second event class the principal explicitly authorised, so no request can vanish and the lifecycle promise becomes true instead of narrowed. Kept in the table as the record of what was a non-guarantee for eleven revisions | the second event class it needed WAS authorised (principal ruling, 2026-08-30 10:46) — this cell carried the impossibility for eleven revisions; the authorisation dissolved it, and the cell now records that resolution (GPT56-V88 F3, CODEX-V88 F2: the sweep retired the body and left this cell still refusing) |
| N3 | **The log can over-report delivery, never under-report a touch** | the safe direction for a custody log, consequence of G1 + N1 |

## 3. The invariant table — crash window × reader

**Windows (re-cut at V91 — the arrival class created a window the table did not have, GPT56-V90 F4):** W0 = before the arrival commit (the pre-arrival wire; §1c's receipt transition is the boundary) · W1 = after the arrival commit, before the decision commit (the PENDING span, §3b) · W2 = at a commit (empty by atomicity — no partial state exists, arrival and decision commits alike) · W3 = after the decision commit, before delivery · W4 = during delivery · W5 = after delivery.

| window | requester sees | verifier/auditor sees | Row B on recovery sees |
|---|---|---|---|
| W0 | nothing (no bytes ever left) | nothing — a request that dies before its arrival commit never entered the log's world (§1c scopes the guarantee to completely framed, receipted requests) | nothing — no arrival, no work item |
| W1 | nothing (no bytes ever left) | **the ARRIVAL event (§1c)** — visible with no terminal event: the named PENDING state (§3b), which the deadline machinery closes; its invariant is exactly-one-terminal's existence half | the arrival is the work item; recovery resumes it to ONE decision, no second arrival |
| W2 | — | — | — (atomic: no such window) |
| W3 | nothing yet | a TRUE event: the effect happened | binding present → never re-decide; **conveyance**: may re-deliver from the committed buffer, no new event; **render**: NO re-render without a NEW touch commit (G5) |
| W4 | partial bytes / partial frame | same true event | same as W3 |
| W5 | the bytes / the frame | same true event | nothing to do |

**Reading the table is the completeness argument: every corner the three failed rounds found lives in
one cell.** V67's under-logged read = W1 mislabelled as safe for a resolve-first ordering — killed by
making the effect part of the commit. V68's outcome-change-after-event = a claimed W3→W5 transition
that could alter the terminal fact — killed by deleting the `TRANSFER` state: **after W2 there is no
state whose failure changes what the log claims.** V69's unsatisfiable Row G rule = classing renders
as N1-delivery — killed by G5.

## 3b. The deadline — lifecycle semantics, so it lives HERE (GPT56-V81 F2, CODEX-V81 F4: the
deadline was added to the draft and absent from this spec, invisible to the derivation checker — the
single-home rule violated by the repair that extended the lifecycle)

**Every request carries a deadline.** Its value is a BS-2k design constant; its clock is the
**monotonic** clock (wall-clock manipulation does not extend it); it is **fixed at request receipt
and never resets** — no state transition, retry-internal step or partial progress renews it. A live
request past its deadline **is** a processing failure under a live Row B and receives the
`REFUSED-UNCLASSIFIED` refusal commit. A request that is neither terminal nor within deadline is a
state this spec does not admit. **The admitted nonterminal state has a NAME — `PENDING`: arrival committed, no decision event, deadline unexpired (named for the existence half of exactly-one-terminal, GPT56-V88 F4, CODEX-V88 F3). A past-deadline zero-terminal arrival is NOT PENDING and not admitted — it is the state whose absence §6.1's five verifier gates check, and finding one refuses the gate as a custody failure; the last gate refuses any zero-terminal arrival at all — the run does not end over an open request.** **THE AUTHENTICATED CLOCK BASIS (GPT56-V89 F1, CODEX-V89 F2 — a verifier cannot read the monotonic clock it is asked to enforce): the ARRIVAL event carries `(boot_epoch, monotonic_reading)` — Row B's own monotonic reading at append, authenticated with the event — and every checkpoint's materials carry a CLOCK RECORD `(boot_epoch, monotonic_reading)` of their production. Overdue is COMPUTED FROM THOSE BYTES ONLY: an arrival is overdue at a gate iff the gate checkpoint's epoch exceeds the arrival's — a restart happened after it; readings do not compare across epochs, so the conservative closure is the deadline refusal at the first later-epoch pass — or the epochs are equal and the checkpoint reading minus the arrival reading exceeds the BS-2k deadline constant. **THE WINDOW JOINS THE RESIDUE (GPT56-V95 F1: an inter-anchor rollback can erase a committed arrival and REUSE its chain position as a new request_key — the no-vanish claim cannot exceed what the anchors prove): the no-vanish guarantee, the deadline laws and the key-uniqueness claim all hold AS OF THE ANCHORS; a rollback inside the window is platform-level custody failure in the same family as §3b's storage clause — the run ends by operator observation, not lifecycle rule, if it surfaces, and the residue sentence names the window beside the pre-arrival wire rather than claiming the wire is alone.** The wall-clock ISO timestamp is human-facing and NEVER the comparison basis; no verifier reads a clock at verification time.** **THE SEMANTICS THE SIGNATURES DO NOT CARRY (CODEX-V90 F1, GPT56-V90 F2 — a signature proves authorship, not monotonicity): `boot_epoch` is a BS-2k-provisioned RESTART COUNTER — incremented exactly once at every Row B start, committed in the transactional domain BEFORE any event of the new epoch, never reused and never decreased; it is a sequence number and owes nothing to any clock. Monotonicity is an INVARIANT OVER THE SIGNED EVENT ORDER, checkable by any verifier from the chain bytes alone: the invariants quantify over CLOCK-BEARING RECORDS — arrival events and checkpoint clock records — BY DESIGN and not by omission (CODEX-V91 F1: the V91 wording quantified over every event while decision events carry no clock pair; scoping is the honest form, with the reason stated): a decision event's epoch is not a compared quantity anywhere in this spec, and no decision can straddle an epoch transition because EVERY EPOCH OPENS WITH A RECOVERY CHECKPOINT — Row B's first act after restart, its clock record the epoch's first clock-bearing record, committed before any arrival or decision of that epoch, while the old epoch's in-flight commits were aborted by the transactional domain. The invariants: (a) over clock-bearing records in chain order, epochs are non-decreasing, each epoch's first clock-bearing record is its recovery checkpoint's clock record — THE OPENING — and **the openings are ANCHORED AND LINKED (GPT56-V92 F1, CODEX-V92 F3: a counter-commit/pre-opening crash skips a number and the V92 invariants accepted the skip silently; the first epoch had no anchor at all): each opening NAMES ITS PREDECESSOR and DECLARES any skipped numbers, UNDER AN AUTHENTICATED SCHEMA (GPT56-V93 F1, CODEX-V93 F1: the V93 declaration had no fields to live in — the checkpoint clock record is a closed two-field schema): **an OPENING is the extended record `(boot_epoch, monotonic_reading, predecessor_epoch, gap_declaration)` — the two clock decimals, the previous opening's epoch, and the possibly-empty ascending list of declared skipped epochs, all bounded decimals** — and an undeclared gap, a predecessor mismatch, or two openings for one epoch is malformed history. The FIRST epoch is 1, the BS-2k provisioning constant, with **predecessor 0 — the reserved pre-first value, since a bounded-decimal field cannot encode NONE (GPT56-V94 F2, CODEX-V94 F6); no real epoch is 0**. **AND THE DECLARATION NEEDS NO TRUST, because the chain itself proves a declared gap EMPTY (GPT56-V93 F2, CODEX-V93 F1: self-declaration cannot distinguish crash from rollback — so nothing is entrusted to it): what a gap could hide is an epoch that HAD events, but every appended event extends the running chain digest, so an epoch with events cannot vanish without breaking continuity — the verifier checks that no chain event carries a declared-gap epoch, and chain continuity does the rest. A rollback that appended nothing is byte-identical to a crash that appended nothing, and equally harmless: there is nothing to launder. The declaration is therefore a naming convenience over a fact the chain proves — **AS OF THE EXTERNAL ANCHORS, because internal continuity alone survives suffix truncation (GPT56-V94 F1, CODEX-V94 F1: a prefix-valid chain with its newest epoch dropped is internally consistent; the proof needs heads bound OUTSIDE the chain): the chain head (position, running digest) is externally bound at every RECEIPTED anchor — BS-2f's running-checkpoint receipt, BS-L's sealed materials, and every verifier gate's PASS RECORD, **which is BUILT, not named (GPT56-V95 F2, CODEX-V95 F4: an anchor without schema, signer, custody or a predecessor check was a word): closed schema `(gate, head_position, head_digest, verifier_digest, predecessor_record_digest, partition_cut_position, signature)` — synced with the draft's (ii-e) at V101 (GPT56-V100 F2: the V98 field landed draft-side only, and the derivation checker binds invariants, not schemas — it now checks this tuple too) — the gate token from the closed five-gate set, the head pair, the pinned enumeration verifier's own digest, **THE PREDECESSOR RECORD'S DIGEST INSIDE THE SIGNED BODY (GPT56-V96 F2, CODEX-V96 F2: prose said the records verify their predecessors while the signature bound none — two valid disclosure records could each claim the terminal head; the anchors now chain by construction: the first record's predecessor is BS-2f's running-checkpoint receipt digest, every later record binds exactly one predecessor, EXACTLY ONE record exists per gate in the five-gate order, a REFUSING pass emits no record — the refusal blocks the gate instead — and a duplicate gate token or two records sharing a predecessor is malformed history)**, and a detached signature under the enumerator keypair PROVISIONED AT BS-2k; custody: the record lands in that gate's receipted materials beside its other receipts. **THE TRUST BOUNDARY, NAMED RATHER THAN LAUNDERED (GPT56-V97 F1/F4, CODEX-V97 F1: the chain roots in a machine-receipted checkpoint and one enumerator key signs entries, anchors and the terminated record — asked what a compromised key forges, the honest answer is EVERYTHING BETWEEN HUMAN SIGNATURES): the anchors' independence comes from the THREE HUMAN WAYPOINTS, not from machine keys — the freeze signature (P0), BS-L's canonical body (P6, which BINDS BS-2f's checkpoint-receipt digest, retroactively rooting the chain under the principal's key), and the opening authorization (P7, binding bsl_digest). Between waypoints, every anchor is the provisioned key's testimony; a forged interval must still agree with the waypoint on each side. **AND THE ENDGAME SUFFIX HAS ITS CLOSING WAYPOINT NAMED — it is not inside this run (GPT56-V98 F1, CODEX-V98 F1: the opening authorization binds bsl_digest and nothing after it; P7→P9 has no human signature, and pretending otherwise would be the laundering this paragraph exists to refuse): within this run the suffix after the opening authorization is machine testimony to the end, SAID SO; the closing human eye WAS named as the successor preregistration's freeze review — and both seats broke that in one round (GPT56-V99 F1, CODEX-V99 F1: a programme-ending run has no successor, and an existing one reads only the enumerator key's self-consistent suffix, so the compromise argument moved one run right instead of closing). **The honest resolution is a TERMINAL HUMAN SIGNATURE — over a digest the signing ceremony RECOMPUTES, never one the enumerator presents (V100, both seats' F1: a signature over the presented digest notarizes testimony — the require_authorization/gamma_bound family, an attestation worth only the independence of its derivation): the ceremony reruns the terminal-head derivation from the chain bytes and the anchor chain under a pinned verifier — **ROOTED OUTSIDE THE ENUMERATOR'S TRUST DOMAIN (GPT56-V101 F1, CODEX-V101 F1: a recomputation launched from the compromised environment inherits the compromise): the verifier is a PINNED BUILD ITEM — `gates/terminal_review_verifier.py`, REQUIRED, DOES NOT EXIST, digest set AND printed here when built; until then the ceremony cannot run and this sentence says so instead of claiming a printed digest that is not there (CODEX-V102 F4, GPT56-V102 F1). The ceremony script (a BS-2k design artifact) names the ACQUISITION PATH — the principal copies the verifier at the printed digest and checks the sha256 himself with an OS tool on HIS environment — and has him CHECK, not read: the recomputed chain head against an independent copy of the chain, the receipt digest against the receipt store, and the verifier digest against the printed pin, before signing the CANONICAL TERMINAL-REVIEW BODY — domain-tagged `(kind, terminal_checkpoint_digest, drain_start_position, recomputed_head, verifier_digest, transcript_digest)` — field order chosen so its two-field prefix is distinct from the terminal checkpoint's (the generalized L08 keys on that prefix and correctly refused the collision on this build's own battery)**. Adding the act is his to rule: FILED, recommended, awaiting. Until ruled, the suffix boundary stands as machine testimony with NO closing waypoint, said in exactly those words.** The successor's freeze review keeps its predecessor-enumeration duty regardless. Extending Clause 6's opening-authorization body with the issuance pass-record digest would close P6→P7 under the principal's own P7 signature; that changes what he signs and is FILED, not taken.** A second machine key from the same BS-2k provisioning would be co-located trust, not independence, and is NOT taken — the repair is this paragraph: the boundary stated, the pre-BS-L span named as the freeze-to-lock window where anchors rest on provisioned keys alone.** THE FINAL SUFFIX IS CLOSED BY THE LAST GATE (CODEX-V95 F4's second half): the disclosure pass record is the run's TERMINAL HEAD — the chain ends where it says, anything after it is post-run and outside every custody claim, stated as the boundary rather than left dangling.** Each anchor's recorded head must match the chain at that position, so a truncation is caught at the first anchor after it. What is NAMED rather than claimed away: a suffix younger than the newest anchor could be dropped — the INTER-ANCHOR WINDOW — and the emptiness proof holds as of the anchors bracketing the gap.** What is provable stays honest: one opening per chain-visible epoch, strictly increasing, every gap declared AND provably empty** (b) within one epoch, `monotonic_reading` is non-decreasing over clock-bearing records in chain order; (c) every arrival's epoch equals the epoch of the most recent OPENING at its position — chain-checkable, replacing the unverifiable committed-counter clause. A violation of any of the three is MALFORMED HISTORY and refuses the CHAIN, not the request. **THE CONSUMER IS NAMED (GPT56-V91 F1: an invariant no verifier consumes is prose): the enumeration verifier's five gates each run a CLOCK PASS over the chain as it then stands — §6.1's §11 item carries the contract and the fixtures (rollback, reuse, arrival-epoch mismatch, within-epoch regression, missing or unlinked opening, undeclared gap).** **THE ORDER AND LATENCY LAWS — lifecycle semantics, so they live HERE (GPT56-V92 F3: the FIFO rule was stated in the covenant table outside this spec's single normative home, the V81 deadline lesson repeating): (i) FIFO — Row B commits decisions in ARRIVAL ORDER; an out-of-order decision is malformed history. (ii) DECIDE-WITHIN-D — every request's decision must commit within the deadline constant D of its arrival, and the decision's clock evidence is its BINDING-MAP entry's `(boot_epoch, monotonic_reading)` — the map, NOT the ruled access-log event schema, carries the clock (CODEX-V92 F1; the widening is FILED with the coordinator); the verifier refuses a same-epoch decision reading exceeding its arrival reading + D. **THE READING IS BOUND TO THE COMMIT BY THE DOMAIN'S OWN BOUND — the V94 ordering argument is DEAD (CODEX-V94 F2 killed it exactly: Row B is the single serialised writer, so while it stalls NOTHING intervenes to order against; queued work does not help because the queue only moves through Row B): the reading is taken at COMMIT START — a BS-2k implementation obligation with its own stall fixture (the harness forces a read-then-stall and expects the abort) — and §3b's bounded-commit rule aborts any commit exceeding the commit bound, so a recorded reading understates true commit time by AT MOST the commit bound. DECIDE-WITHIN-D is therefore checked against `D − commit_bound` **FOR SUBSTANTIVE DECISIONS; the DEADLINE REFUSAL is the law's enforcement and necessarily commits AFTER D (CODEX-V95 F3) — its bound is `D + enforcement_lag`, where `enforcement_lag ≥ detection + Q · commit_bound` joins BS-2k's stated obligations with `Q` the provisioned queue bound (GPT56-V96 F3: one lag covering one commit cannot close a multi-request overdue backlog serialized behind the single writer; the backlog is bounded by Q, so the lag is sized to drain it). **AND THERE ARE NO TIERS TO CLASSIFY — TWO ACCEPTANCE PREDICATES, BECAUSE CLASSIFYING MISFILED LEGITIMATE REFUSALS (GPT56-V97 F6, CODEX-V97 F3: a real substantive refusal committing inside the slack window was deterministically labelled deadline enforcement): the verifier checks timeliness, not kind — a TOUCH commit is timely iff its map-entry reading ≤ arrival + D − commit_bound; a REFUSAL commit is timely iff its reading ≤ arrival + D + enforcement_lag, whatever its reason (the longer allowance covers enforcement and slow-but-real refusals alike); anything beyond its predicate is malformed. **THE STRETCH CHANNEL THE LONG ALLOWANCE OPENS IS BOUNDED BY THE BUDGET, NOT THE LAG (CODEX-V98 F6: a hostile row could otherwise encode in authenticated decision times): the allowance is long for QUEUE reasons, but each request's own PROCESSING is head-budget-bounded, so a row cannot stretch its own refusal beyond `budget` — the channel is at most `log₂(⌈budget/g⌉ + 1)` bits per refusal, joining the named clock channels under the same gate review.** Deadline and substantive refusals share event bytes and now also share a predicate — the distinction die entirely.**** — with `D > commit_bound` joining BS-2k's stated design obligations. **AND EVERY RETRY RE-READS (CODEX-V95 F5: abort/retry said nothing about re-reading): the reading obligation is PER ATTEMPT — each retry reads at its own commit start, the recorded reading is the committing attempt's, so staleness stays ≤ one commit_bound regardless of aborted predecessors; the stall fixture exercises the retry path too.** What no byte can verify — that the read truly happened at commit start — is the fixture's to test and is SAID to be testimony-plus-fixture, not proof; map entries still join invariant (b) under the slack rule, which catches gross staleness across commits.** **AND THE RECORDED READINGS ARE QUANTIZED — with the DIRECTIONS FIXED PER ROLE, because rounding both endpoints up was NOT conservative (GPT56-V94 F3, CODEX-V94 F3: ceil(d) − ceil(a) can undercount the true gap and pass a late decision): an ARRIVAL reading rounds DOWN and a DECISION reading rounds UP to the BS-2k granularity `g`, so the recorded gap bounds the true gap from above and decide-within-D on recorded values is conservative by construction; openings and checkpoint records round UP. Mixed directions cost the ordering invariant its exactness, stated rather than hidden (GPT56-V94 F4): invariant (b) is checked WITH SLACK `g` — a recorded decrease exceeding `g` is malformed, inversions within one quantum are expected — and the channel bound is `log₂(⌈D/g⌉ + 1)` bits per decision, well-defined and never negative (GPT56-V94 F10: the V94 form went negative for D < g).** (iii) THE HEAD BUDGET — Row B's per-request processing is bounded by a BS-2k budget constant; an overrun head receives the catch-all refusal AT ITS TURN (the worker-timeout class above), so a slow head cannot silently push later arrivals past D — and BS-2k's design carries the stated obligation **D ≥ Q·(budget + commit_bound) + commit_bound — the FULL-QUEUE DRAIN inequality (GPT56-V98 F7: D ≥ budget·Q sized the refusal lag but left a tail TOUCH behind a full queue unable to meet its own stricter D − commit_bound predicate; the corrected bound drains Q requests, each budget plus commit, before the tail's commit)** — with its own fixture (CODEX-V92 F1).** Bounds, stated where the fields live: `boot_epoch` is a decimal integer in [0, 10^6]; `monotonic_reading` is a decimal integer nanosecond count in [0, 2^63 − 1] (GPT56-V90 F3: both were called bounded with no bound stated).** **And the COMMIT itself is bounded (CODEX-V82 F7: a live Row B
stalled INSIDE the atomic commit could not append the deadline refusal, leaving a past-deadline
nonterminal request): the transactional domain aborts any commit that neither completes nor
aborts within its own BS-2k commit bound — transactional semantics make abort always available —
and the aborted request then receives the deadline refusal. A stalled commit resolves to
abort-then-refusal, never to a wait — **within what the platform serves: where the STORAGE ITSELF
never returns (GPT56/CODEX-V83 F5: an fsync that hangs forever), no domain can conjure liveness,
and that terminal case is the platform failing — the same shape as the RETIRED N2's old residue, kept as a comparison only — the run ends by operator
observation, not by lifecycle rule, and the lifecycle is TOTAL over the states the platform
serves, which is the honest quantifier.**

## 3c. TERMINATION — three properties, and the construction derived from them

Stated as properties first (the coordinator's directive after V102), each a labelled row the
draft must quote byte-for-byte; the construction is what the properties force, not a design to
be patched beside them.

| # | property | holds for |
|---|---|---|
| T1 | **Admission CLOSES at DRAIN-START**: when any TERMINATED-family condition fires, Row B's first act is appending the DRAIN-START record — a checkpoint-family, clock-bearing record — and after it the receipt transition is CLOSED: no frame is received, no arrival is appended, and the no-vanish guarantee is scoped to requests received before it; a post-drain-start frame is wire residue by definition (V102's checkpoint never closed admission — GPT56-V102 F3) | termination |
| T2 | **The drain is IDEMPOTENTLY RESUMABLE from the log**: DRAIN-OPEN — drain-start present, terminal checkpoint absent — is a chain state; recovery in that state opens a DRAINING EPOCH, lawful and constrained: it refuses each remaining PENDING arrival under `REFUSED-LOCK-OR-CEREMONY-STATE` (the ceremony state does not permit completion — truthful for every drained arrival, no false deadline, no catch-all flood), skips arrivals already terminal, admits nothing, and appends the terminal checkpoint when none remain; ordinary admission never resumes (V102 deadlocked here — the terminal receipt made every recovery opening malformed, GPT56-V102 F2, CODEX-V102 F2; the opening rule now reads: an opening is malformed for ORDINARY admission when a drain-start or TERMINATED receipt stands — a DRAINING opening under T2 is the one lawful continuation) | termination |
| T3 | **The TERMINAL CHECKPOINT is a first-class artifact and THE AUTHENTICATED LOG IS THE ROOT**: closed non-χ schema `(kind, drain_start_position, receipt_digest, chain_head_position, chain_head_digest, boot_epoch, monotonic_reading)` — every field value-domained, the record ON the exhaustive non-χ list, IN the domain-kind set, and UNDER the clock pass like every clock-bearing record; its digest is what the terminal ceremony signs AFTER recomputation; and the artifact set names the log itself as the trust root, because claiming any other root would name a thing that is not there (GPT56-V102 F6, CODEX-V102 F3, and both F1s' root complaint) | termination |

The new-chain rule follows from T1–T3 and is stated once: **an OPENING for ordinary admission is
malformed while any drain-start or TERMINATED-family receipt stands under this freeze; the only
lawful post-drain-start openings are T2's draining epochs.** A successor study is a new
preregistration, never a new chain here.

## 4. The states, derived — this list REPLACES every previous statement of the state machine

`RECEIVED` → `PENDING-AUTHORISATION` → (writes only: `PENDING-SURFACE-CHECK`) → **one commit**
(refusal or touch) → (conveyance only: delivery, outside the custody claim).

**There is no `TRANSFER` state.** V69 deleted it in one paragraph and left it declared in another —
a deletion that did not delete, the incomplete-retraction shape this corpus knows best. **The draft's
state declaration now points here, so the fact has one home and cannot drift against itself.**

## 5. The Row G analysis — why GPT56-V69 F2's fork DISSOLVES, and what it costs

**The asserted fork:** delivery cannot be inside the atomic domain, AND Row G voids any unlogged view
— so unlogged re-views (V69's carve-out) make the two rules unsatisfiable together.

**The dissolution: the covenant had already decided, and V69's carve-out was written over it.** Row
G's cell voids *"any unlogged view"* — views were **always** logged events. The N1 carve-out is
correct for **conveyance** (a machine re-consuming already-conveyed bytes) and was **over-broad** in
covering renders. Under **G5**: every render is a fresh touch commit — logged, satisfying Row G —
while what remains outside the claim is the human's **perception** of a rendered frame, which no log
can capture and Row G's clause never claimed. **Both rules hold simultaneously. No normative choice
is needed, because no rule changed: G5 restates what Row G's void clause already required.**

**The costs, stated so the dissolution is not mistaken for free:**
- **The V65 schedule sentence is recut**: *"re-viewing the current object is unrestricted"* remains
  true in **schedule** terms (a re-render is not a request for a different object and never violates
  the traversal) and is **corrected in custody terms: every re-render is a logged touch.** **A VIEW is the display session of ONE render commit** — it begins when the committed conveyance
  is displayed and ends at the FIRST of: the traversal position advancing, the interface clearing,
  or **ANY interruption of continuous display — visibility loss, blanking, occlusion, navigation
  away** (CODEX-V71 F2: a visibility-loss-and-restore redisplay fired neither named ender and kept
  one commit alive across views). **A session is one continuous uninterrupted display**; duration
  alone does not multiply views — one long look is one view — but **nothing displayed after an
  interruption is the same view** (GPT56-V71 F3: an operator could otherwise hold a session open
  indefinitely across repeated redisplays under one event). Within a
  session, dwell and magnification of the already-rendered frame move no store bytes and are the
  SAME view — one commit, one event. **Displaying the object again after the session ends is a NEW
  view and requires a new render commit** (CODEX-V70 F2: without the session boundary, "magnification
  is no touch" and "any unlogged view voids" contradicted on cached frames). **If the principal means
  "view" per-glance rather than per-session, that is his reading to impose; the spec's definition is
  the session, stated so the word has one meaning.**
  **BUFFER LIFETIME AND CACHE, because G5 and G6 composed into two contradictions in one round.**
  A render buffer lives **from its touch commit until its VIEW SESSION ends — or, if no session
  ever opens (a crash between commit and first frame), until its REQUEST ends — then must be
  destroyed** (GPT56-V77 F6: a commit that opened no session left the buffer with no named
  destruction trigger; the request boundary is the trigger every commit already has) — V74 said it dies "with its commit", which made delivering after the commit from a
  dead buffer an impossibility (CODEX-V74 F2); a buffer outliving the SESSION would be a cache.
  And **the sealed interface renders EXCLUSIVELY from Row B conveyances and holds NO redisplayable
  surface beyond the live session — compositor, framebuffer or otherwise** (GPT56-V74 F3: occlusion
  ends the session under G6, and a compositor restore would create the new view with no new commit;
  the interface must re-request instead, which is a new touch). **A BS-2k interface design
  requirement with fixtures: occlude-and-restore must produce a second committed render event or no
  image.** **MULTIPLICITY IS A CONSTANT, BY PADDING (V93 — GPT56-V92 F4 and CODEX-V92 F2 broke V92's single-pass rule against the restore requirement two sentences up: an interrupted member had NO route to a terminal label, and "no re-render operation" contradicted "must produce a second committed render event"): the interface delivers EXACTLY `R_max` committed render events per allocated object per member — `R_max` a BS-2k design constant ≥ 2 — where interruptions consume replays from the fixed allowance, **and the padding is PER-OBJECT AND PRE-LABEL, because end-clustered padding timed the very count it hides (CODEX-V93 F4) and "session-set end" was undefined and evadable (GPT56-V93 F5, CODEX-V93 F3): when the member proceeds AWAY from an object — to the next object or to label entry — the interface FIRST issues that object's unconsumed replays as PADDING commits (real conveyances opening no session; the no-session buffer path above destroys each at request end). **Labels enter the interface per object and Row H persists THE ONE LABEL SET exactly as its row states — the V94 text presumed per-object label writes Row H does not define (GPT56-V94 F6), so the chain pattern claim is now Row-H-true: every object shows exactly `R_max` contiguous render commits and NO per-object label event; the single label-set write lands once, at the end, as ruled.** The member-level backstop is the BS-2k LABELING DEADLINE. **The residual, named (CODEX-V94 F5): inter-object PACE remains visible in event timing — pace was never inside the multiplicity claim, it is bounded by the labeling deadline, and hiding it would need batching that changes what the log records; it is the stated leftover, reviewable at the gates like the other named channels.** Every object's per-member render multiplicity therefore equals `R_max` regardless of member behaviour, and neither counts nor padding timing carry anything. **EXHAUSTION HAS ITS FULL MACHINERY, not a name (CODEX-V94 F4: a terminal with no token, producer, schema, verifier or registry entry was prose; GPT56-V94 F7: its record carried χ-adjacent identities and the member-controlled halt needed declaring): the outcome is `TERMINATED-BY-LABEL-EXHAUSTION` — the second member of the terminated family, neither VOID nor INCONCLUSIVE, sibling to the unnameable-refusal-class ending and produced the same way: the detecting instrument's refusal IS the production. The producer is the sealed interface via Row B; the record is a RECEIPTED ARTIFACT outside the chain (no third event class — what the log records is ruled and untouched), with a CANONICAL BODY `(kind, chain_head, freeze_signature_digest, first_opening_digest)` — freeze-bound AND chain-bound, because one freeze can govern a resumed run (GPT56-V98 F5, CODEX-V98 F2) — carried under a DETACHED signature envelope (CODEX-V96 F4: the authenticator is the envelope, not a body field) — and the object and member identities SEALED into the committee store, χ-adjacent as found; the enumeration verifier refuses every gate after a halt receipt exists — **and TERMINATION IS GOVERNED BY §3c's T-ROWS — the construction was re-derived there in one pass after V102's drain attacks (the coordinator's directive: state the properties, derive the construction; both seats broke the V102 paragraph four ways — a mid-drain crash was unrecoverable under its own opening rule, admission never closed, the checkpoint sat off every schema, and it claimed a trust root the artifact set does not hold). The V102 paragraph is superseded whole; §3c is the single home and the draft derives from it label-bound.** — **and the receipt is AUTHENTICATED, not merely named (GPT56-V95 F3, CODEX-V95 F2): a detached signature under the sealed interface's keypair PROVISIONED AT BS-2k, over the two-field canonical body; the gates verify signature, roster membership, and that the recorded chain head matches the chain — a forged or replayed halt receipt fails one of the three. The outcome token joins the draft's canonical run-outcome enumeration and the receipt's face joins the exhaustive non-χ list, because a terminal absent from the registries was a name again.** **The member-controlled channel is DECLARED: a hostile member can force the halt at will by burning replays — a DENIAL channel, not a disclosure channel: it can stop the study, it cannot bias a verdict that is never produced; hardening it is protocol territory and the principal's.** `R_max ≥ 2` keeps the accidental case rare; softening (ABSTAIN, member replacement) stays flagged in the decisions file.** V92's single-pass (R_max = 1, no replay) is superseded — it satisfied the counting rule by forbidding what this section requires.**
- **The committed buffer is GOVERNED, answering CODEX-V69 F2**: for **renders there is no buffer
  reuse** — each render re-conveys under its own commit; for **conveyance** the buffer is part of the
  committed touch, destroyed on delivery completion or request end, and **its existence, bounds and
  destruction are BS-2k design requirements with fixtures** — it is Row B's surface inside the
  transactional domain, not an ungoverned χ-bearing holding area.

**If the principal reads Row G's re-view practice differently — wanting re-views unlogged — that IS
the normative fork Blanc named, it weakens a void clause, and it is his. The spec's position is that
the covenant as written already contains G5.**

## 6. What this spec deliberately leaves open

- **N2 is RETIRED** — the arrival receipt (§1c) is the second event class, authorised 2026-08-30; nothing here is referred any more.
- **The post-`BS-L` enumeration surface** is the enumeration mechanism's problem, not the
  lifecycle's; it is repaired in the draft where the enumeration lives.
- **Cross-run recurrence** stays a successor-preregistration duty.

**v9 untouched at `6a9abbbd`. Nothing here is evidence about the sky.**
