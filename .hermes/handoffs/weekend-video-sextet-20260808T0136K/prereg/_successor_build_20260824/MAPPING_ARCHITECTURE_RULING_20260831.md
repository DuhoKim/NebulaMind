# MAPPING ARCHITECTURE — RULED — 2026-08-31 (relayed ~10:1x KST)

**The principal's selection, verbatim option label (presented with CODEX's DIVERGENT
argument summarized):**

> Sweep runner owns it (Recommended)

**Scope as presented, nothing wider:** MappingA stays a ONE-DRAW PRIMITIVE — one
committed draw mapped across the γ grid. The sweep/replay runner owns the 99×51 verdict
matrix and the HELD reduction (within-draw comparison of every cell (i, j) to that
draw's own (i, j₀=25) γ=0 cell). The module does NOT perform the worst-case reduction.
This confirms the architecture as built and answers CODEX-MAPCONV Q3.

**Explicitly NOT covered by this ruling (its own "nothing wider"):** the four-value
convention commitment — a₀ = cal a_hat, c̄ = mask mean, the physical clamp, the cal′
transform — committed blind in `ref/MAPPING_CONVENTION_COMMIT_20260831.md` and STILL
FILED for the principal's separate confirmation. `mapping_id` stays
`MAPPING-NOT-PREREGISTERED` and the replay manifest entry stays PENDING-CONFIRMATION
until that confirm lands.

**Recorded in the track history (direction #15).** The agy re-verify of
`gain_mapping_a.py` dispatches on this ruling per the coordinator's green light.
