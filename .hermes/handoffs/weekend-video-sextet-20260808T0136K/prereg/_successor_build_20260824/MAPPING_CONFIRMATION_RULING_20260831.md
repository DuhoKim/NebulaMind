# MAPPING FOUR-VALUE CONFIRMATION RULING — 2026-08-31 ~13:2x KST

**The principal's selection, verbatim option label (relayed via Blanc/OPS, presented
exactly as `DECISIONS_FOR_DUHO.md` framed it):**

> Confirmed as committed (Recommended)

**What this rules.** The four blind-committed conventions in
`ref/MAPPING_CONVENTION_COMMIT_20260831.md` (sha256
`ff7b2cdb0441702ae471530b794ec43b62d0f9c07e776e308a26a76984fe0ebc`) STAND as
committed: a₀ = cal["a_hat"]; c̄ = the mean of the mask's c values; the physical
clamp to [0.5 + 1e-9, 1.0] with clip diagnostics; and the per-bin-means cal′
transform with preserved margins. They were committed BLIND — before any data could
be seen — with written reasons why none was shopped; the codex convention pass had
returned DIVERGENT-as-underdetermination (defensible-but-underdetermined), which is
exactly why the confirmation was the principal's and not mine.

**The chain the draft itself demanded is now complete:** the V125 pin held
`mapping_id` for "a focused referee pass judg[ing] the module against the ruling's
own words" → that pass ran and surfaced the underdetermination → the blind
commitment was filed → the architecture around it was ruled ("Sweep runner owns
it", 2026-08-31 morning) → the module was re-verified SOUND on the
commitment-bound bytes (AGY MAPA-V2, zero findings) → **the principal confirmed.**

**Scope, as presented — nothing wider:**
1. `mapping_id` transitions `MAPPING-NOT-PREREGISTERED` →
   `MAPPING-A-CRN-PCG64-20260830-v1`.
2. The replay manifest's `gain_mapping_a` entry flips PENDING-CONFIRMATION →
   ACTIVE with digest
   `8bc693ffae7009e0967a0b433b9bc7787494da8742457ad381443d4b210b4aa1` — which
   changes `replay_harness.py`'s bytes and therefore its pinned sha. **This is
   the visible re-pin KGATE_REPLAY designed** ("the pin arms nothing while making
   the eventual mapping confirmation a visible drift event"), not a silent swap;
   the re-pin lands at V132 with the diff verified by a narrow seat pass.
3. The BS-3g sweep machinery unblocks (the sweep runner still refuses until the
   run-time calibration artifacts exist — that refusal is design, not debt).

**Remaining on the principal after this, unchanged:** the BS-2k constants sheet,
the reviewer/hand-check rosters, the Sep-5 BS-1 release rule, the P0 signature.

Recorded as human direction #16 in
`.hermes/handoffs/spin-parity-census-20260805T1922K/spin-parity_history.json`.
