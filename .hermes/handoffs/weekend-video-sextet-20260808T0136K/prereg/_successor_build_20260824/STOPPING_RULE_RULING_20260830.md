# THE STOPPING RULE — RULED — 2026-08-30 21:54 KST

**The principal's words, verbatim (direct message to Hwao's session, choosing among three
presented options — the fifth ruling of the evening):**

> option 2

**What this rules.** The referee loop's stopping rule is the KIMI GATE's recommendation
(`gates/KGATE_PREREG_NECESSITY_20260830.md`, recommendation 1), selected by the principal
over (1) loop-until-CLEAR×2 and (3) freeze-and-watch:

- **The text-referee loop is CAPPED AT THREE MORE ROUNDS — V114, V115, V116 — all under
  the scope freeze** (no new record kinds, generators, or controls except controls on
  existing generators; repairs only).
- If **CLEAR ×2 arrives before the cap**, the loop ends there — better than planned.
- Otherwise, after V116's repairs are folded, **the text loop ENDS**, and every remaining
  finding or acknowledged limitation becomes the **KNOWN-DEBT APPENDIX**: enumerated,
  GENERATED from the repair ledger (dispositions, never restated prose), quoted in full,
  frozen WITH the document and signed with it. An honest bounded list, not a hidden one.
- **The cap ends text refereeing, not the build.** The freeze itself still waits on the
  build items that gate the science: the six promised tools (decoder, terminal-review
  verifier + ceremony script, executable mapping module, replay harness, BS-SI schema),
  the platform constants, the two human rosters, the UNFILLED slots — above all the
  calibration-robustness run returning HELD — and BS-1's release choice (rule date
  Sep 5). Referee effort redirects to that build-out, per the same kimi recommendation.
- The final round's seats see the appendix draft, so the debt list itself gets one
  adversarial read before it freezes.

**Supersession, disclosed.** This supersedes the previously declared convergence criterion
(zero-HIGH round → consolidation pass → CLEAR×2 on a no-debt draft) as the CONDITION for
freeze. The consolidation pass survives as the LAST build's discipline (all sweeps and
seeded controls re-run, history regenerated) regardless of verdict. The coordinator's
(Blanc's) consolidation plan is honoured inside the new rule, and this record is the
disclosure to him.

**Recorded also in the track's human-direction history** (direction #13 in
`spin-parity_history.json`).
