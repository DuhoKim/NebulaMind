with open("PREREG_SUCCESSOR_DRAFT_V16_20260827.md", "a") as f:
    f.write("\n## §11 Code-side inventory for the next atomic revision\n\n")
    f.write("- **`SLOT_SCHEMA` entries (Row A, Row N):** Add one explicit code-side item requiring exact pinned `SLOT_SCHEMA` entries and canonical receipt fields for **BS-L and BS-2k**. Name the **BS-2a** schema addition as required work deferred with the already-refused BS-2a design. Bind those schema bytes into the implementation/schema digest item. A general `SLOT_SCHEMA` update to capture access-log checkpoints and archive seal-state digests for BS-2f/BS-L. **Do not change BS-5f** (route b stands).\n")
    f.write("- **`verify_lock()` enforcement (route b):** Require the pinned `verify_lock()` to resolve the BS-L-bound BS-8f bytes and independently recompute `all(a_LB_b >= 0.85)`. Pin the implementation/schema digest for this route. Add a negative fixture demonstrating that a low-bound BS-8f cannot produce a passing lock.\n")
    f.write("- **Row-J calibration guard:** Implement the guard to emit `INCONCLUSIVE-BY-CALIBRATION` and halt pre-unblinding if `a_LB_b < 0.85`.\n")
    f.write("- **Row B access mediation:** Implement Row B's hard block on Row D prior to C2's exact-parent stage-completion receipt verification. Implement the enforceable-mediation gate checks.\n")
    f.write("- **Row C2 and Integrity:** Implement the hermetic worker profile allowlist and adversarial producer fixtures for C2. Implement `recompute_acceptance_ledger` to compute statuses and reasons from the evidence projections, atomically writing the evidence ledger and realised partition.\n")
    f.write("- **Verifiers (Row O, Q):** Implement `verify_unblinding_receipt`, `verify_archive_seal`, and the opening-authorization / replay verifier.\n")
