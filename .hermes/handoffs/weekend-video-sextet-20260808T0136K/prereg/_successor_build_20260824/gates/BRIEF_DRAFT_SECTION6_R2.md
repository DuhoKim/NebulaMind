# DRAFTING BRIEF — §6, second pass. Your draft was refereed; revise it.

You drafted `SECTION6_DRAFT_KIMI.md`. GPT56 and CODEX refereed it and both returned **NOT
CLEAR**: `SECTION6_REVIEW_GPT56.md`, `SECTION6_REVIEW_CODEX.md`. Nine blocking findings and one
major between them. Revise into `SECTION6_DRAFT_KIMI_R2.md`.

## First, what your draft achieved, because it should not be lost in the revision

GPT56 opens: *"the draft finally states a universal access ban and makes table-authorized acts
survive the void rule."* That is round 1's first blocker — the covenant forbade disclosure, not
access — which survived four of my revisions and which my V14 made worse by voiding its own
exceptions. Your table closed it in one pass, and the hand-check path is visible in one place for
the first time. **Do not trade that away to satisfy a narrower finding.** If a prescribed repair
would reintroduce role-scoping or make an authorised act void the run, say so and propose the
alternative instead of complying.

## The nine blockers

From GPT56:

1. The kept Disclosure clause forbids the BS-8f export that Stage C requires — the scope calls
   BS-8f aggregates a permitted surface, row G writes them and row H reads them pre-lock, and the
   opening clause forbids every real-χ-derived summary leaving the sealed stores before BS-L.
2. "A process not in the table may not run" forbids the machinery that creates and maintains the
   table's own artifacts.
3. The scope classifies execution receipts as both χ-bearing and non-χ-bearing.
4. Row C has no named pre-BS-2f producer, while the document and code place one elsewhere.
5. BS-L names a "signature" without defining a signable or verifiable lock artifact.

From CODEX:

6. The scope both classifies execution receipts as χ-bearing and exempts all receipts, logs and
   digests — **an access carve-out recreated through object naming rather than role naming.** Its
   prescribed repair: delete the categorical exemption; define as non-χ-bearing only a closed list
   of authenticated schemas provably unable to encode or bind an outcome value; anything unlisted,
   any opaque digest of χ-bearing bytes, and any schema permitting outcome payloads is χ-bearing
   by default.
7. The lifecycle's "under which receipt" condition is self-dependent for the mandatory rows.
8. The table omits the process that is supposed to make violations detectable.
9. Clause 3(d) incorrectly says §0's code-precedence rule does not reach the missing piece.

Plus CODEX's major: rows E and F do not define one closed label path.

Read both reports in full rather than working from my summary — **summarising findings is the
operation I keep getting wrong**, and this list is my summary.

## Constraints unchanged from the first brief

- The table is the normative object; anything not in it is forbidden by default.
- Keep Part 2 (conforming edits outside §6). It is the discipline whose absence produced three
  of my own cross-section defects, and no referee asked for it — you added it.
- Keep Part 3 (choices and their alternatives), Part 4 (your draft's residual risks), Part 5
  (requirement mapping, including anything you judge unsatisfiable as stated).
- You still do not referee your own text. GPT56 and CODEX judge the revision.
- If one of the nine is wrong, refuse it and say why. You refused a requirement of mine last
  round — every automation row carrying a pinned code symbol that does not yet exist — and you
  were right to; I would have fabricated the pin.

Do not modify the preregistration. Do not read `/Users/duhokim/NebulaMindData/`.
