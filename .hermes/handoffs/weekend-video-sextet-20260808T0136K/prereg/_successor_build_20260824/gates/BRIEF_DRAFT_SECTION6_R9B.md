# ADDITION — R9b. One sentence. Change nothing else.

`SECTION6_DRAFT_AGY_R9.md` (sha256
`ac0a91b973e017172d453a873fd2a72bdc915e215847f7507e9d31838e4b0308`) is correct and complete. Both
reachable branches now terminate:

- the inapplicable 962/1,000 case emits `INCONCLUSIVE-BY-POWER`, with V15 lines 570–573 cited;
- non-committee attrition fails closed to `INCONCLUSIVE-BY-CALIBRATION`, **because the applicability
  predicate does not exist in the frozen record** — you declared the missing quantity rather than
  composing one, which was the right call and is exactly what the previous round got wrong.

**Do not change any of that.**

## The one addition

The principal asks that the draft state, in its own text, *why* an unterminated branch is fatal —
not merely that this one has been terminated. His words:

> Every path in a preregistration must terminate in a stated outcome, because an unterminated branch
> is exactly where someone chooses what it means AFTER seeing the data. That is the failure the whole
> document exists to prevent.

Add that as a **normative sentence in §6**, placed where a future editor will meet it before adding
a branch — the clause list is the natural home. Write it in the document's own voice, as a rule that
binds, not as a quotation or a note about this round. Something to the effect that every branch of
every row must terminate in one stated outcome, and that a branch whose consequence depends on a
judgement made later is not terminated.

Add nothing else. No new rows, no new predicates, no restatement of the R9 repairs.

## Deliverable

`SECTION6_DRAFT_AGY_R9B.md` — complete, self-contained, five parts. In Part 5, record this as the
addition it is.

Do not modify the preregistration. Do not read `/Users/duhokim/NebulaMindData/`. Nothing is
authorised to fetch.
