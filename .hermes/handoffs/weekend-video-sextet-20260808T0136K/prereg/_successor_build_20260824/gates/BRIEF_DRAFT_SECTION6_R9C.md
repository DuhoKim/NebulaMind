# ADDITION — R9c. Clause 10 needs its reason. Nothing else changes.

`SECTION6_DRAFT_AGY_R9B.md` (sha256
`6361a16bb3f29b61b5ae0e46f6fa8a14f07a2c15b9537730a7e907dd01169043`) is correct. **Change only
clause 10.**

You wrote:

> 10. **Branch termination.** Every branch of every row must terminate in one stated outcome,
> because a branch whose consequence depends on a judgement made later is not terminated.

The rule binds, and the second half is a good catch — a deferred judgement is the same hole with a
longer sentence. But the "because" is **definitional, not explanatory**. It restates what
"terminated" means. It does not tell a future editor *why the requirement exists*, which is the
whole point of putting it in the clause list where they will meet it before adding a branch.

The reason, in the principal's words:

> an unterminated branch is exactly where someone chooses what it means AFTER seeing the data. That
> is the failure the whole document exists to prevent.

Rewrite clause 10 to carry **both**: the binding rule, and that reason — that an unterminated branch
is where the meaning gets chosen after the data is visible, which is the failure this
preregistration exists to prevent. Keep your "judgement made later" clause; it is doing real work.

Write it in the document's own voice as a rule that binds, not as a note about how it came to be
written. Two sentences is fine if one will not hold it.

**Change nothing else.** No other clause, row, predicate, threshold or part.

## Deliverable

`SECTION6_DRAFT_AGY_R9C.md` — complete, self-contained, five parts. Part 5's addition item should
reflect the fuller clause.

Do not modify the preregistration. Do not read `/Users/duhokim/NebulaMindData/`. Nothing is
authorised to fetch.
