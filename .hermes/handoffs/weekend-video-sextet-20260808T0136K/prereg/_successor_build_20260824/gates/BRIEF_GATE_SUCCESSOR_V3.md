# ADVERSARIAL GATE BRIEF — successor preregistration draft V3 (re-gate)

You are an adversarial gate. Your job is to REFUSE this document if you can. You (or a seat of
your family) refused V2 with blocking findings; V3 claims to repair the union of both refusals.
A repair that merely responds without repairing is a REFUSAL. New holes count as much as old ones.

## Pin first (custody)

The file under review is `../PREREG_SUCCESSOR_DRAFT_V3_20260824.md` (relative to this brief).
Compute its sha256 FIRST and print it in your report. It must equal
`1c4788c5555a9f7ed00c83d0431c6ad4c4730faa79da5da6e7f2580371730532`.
If it does not, STOP and report the mismatch — review nothing.

## Context (read-only)

- `GATE_GPT56_SUCCESSOR_V2.md` and `GATE_CODEX_SUCCESSOR_V2.md` (this directory) — the two V2
  refusals whose finding-union V3 must repair (V3 §9 has the trace)
- `../../SUCCESSOR_SCOPE_20260821.md` — the seven design requirements
- `../../PREREG_LONGO_AMPLITUDE_TEST_FROZEN_20260815_V3.md` — the frozen predecessor
- `../../LANA_BS6_PHOTOMETRIC_CUTS_20260814.md` — the hash-pinned Cut-6 receipt V3 restates
  (verify the §2 predicate list against it byte-for-byte)
- `../agy/REVIEW_AGY_20260824.md` — the earlier independent review
- Anything else under `../../` a claim cites. Do not read `/Users/duhokim/NebulaMindData/`.

## Attack surfaces, minimum set (add your own)

1. **Each V2 blocking finding:** is the V3 repair a real mechanism? Re-derive the math yourself
   (attenuation algebra incl. the σ_ours delta-method term; the greedy optimizer's determinism
   and its behaviour on the counterexample; CP lower-bound arithmetic at n_trials = 1,000;
   plus-one p resolution at n_perm = 100,000).
2. **The axis vector:** recompute the ICRS unit vector from (l, b) = (52°, 68.5°) with the
   stated IAU constants; confirm the printed 15-digit components; check the display-only rule
   actually forbids every runtime conversion path.
3. **The two-stage power gate:** any remaining circularity or unfillable slot in the §7 state
   machine? Walk every slot: producer, consumer, order.
4. **Quotation fidelity:** every value attributed to the predecessor or BS6-pred.
5. **Loopholes:** laziest compliant reading of every MUST, especially Stage C input
   admissibility, the blind-double no-reconciliation rule, and the BS-7 digest protocol.
6. **Internal consistency:** symbols defined before use (β̂, Â_L, σ_β, σ_ours, σ_comb, a_LB,
   L_min_plan, L_plan), §-references, slot classes, threshold identity between §3/§4/§5.

## Report (write ONLY your own report file in this directory)

Write `GATE_<YOURSEAT>_SUCCESSOR_V3.md` — seat name per your dispatch instruction. Structure:
pinned sha as computed; numbered findings (severity, quote, why, minimal repair); a verdict
line: **PASS** (freeze-candidate grade) or **REFUSED** (blocking findings named). Author
statements you cannot back with a shown command go under a Testimony heading.
