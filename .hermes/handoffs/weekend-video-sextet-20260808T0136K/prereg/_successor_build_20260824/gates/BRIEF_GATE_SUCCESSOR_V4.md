# ADVERSARIAL GATE BRIEF — successor preregistration draft V4 (re-gate, round 3)

You are an adversarial gate. Your job is to REFUSE this document if you can. Seats of your
families refused V2 and V3; V4 claims to repair the union of both V3 refusals. A repair that
responds without repairing is a REFUSAL. New holes count as much as old ones.

## Pin first (custody)

The file under review is `../PREREG_SUCCESSOR_DRAFT_V4_20260824.md` (relative to this brief).
Compute its sha256 FIRST and print it in your report. It must equal
`1ea8bb8d8e236049b2e73091770f3a7f58dbee5a3b8385ba3f30cb11fd31adcb`.
If it does not, STOP and report the mismatch — review nothing.

## Context (read-only)

- `GATE_GPT56_SUCCESSOR_V3.md`, `GATE_CODEX_SUCCESSOR_V3.md` (this directory) — the V3
  refusals whose finding-union V4 must repair (V4 §9 traces each)
- `../../SUCCESSOR_SCOPE_20260821.md` — design authority, INCLUDING its new Amendment 1
  (2026-08-24) which V4 §2 relies on
- `../../PREREG_LONGO_AMPLITUDE_TEST_FROZEN_20260815_V3.md` and
  `../../LANA_BS6_PHOTOMETRIC_CUTS_20260814.md` — quotation sources
- Anything else under `../../` a claim cites. Do not read `/Users/duhokim/NebulaMindData/`.

## Attack surfaces, minimum set (add your own)

1. **Each V3 blocking finding:** is the V4 repair a real mechanism? Execute the math:
   the BS-2c→2o→5p→2s chain's acyclicity (walk producers/consumers slot by slot); the greedy
   update identity and its float determinism; the local-improvement pass's determinism and its
   behaviour on all three published counterexamples; the x ≥ 962 CP arithmetic; the seed-spawn
   contract's reproducibility; the retention floor; the calibration-gate fallback estimator's
   algebra (does β̂/ŵ actually deconvolve a piecewise-constant a(c)?).
2. **Amendment legitimacy:** does Amendment 1 actually license what §2 does, and is the
   no-global-claim discipline held everywhere (no leftover "maximize"/"optimal" claims)?
3. **Quotation fidelity:** every predecessor/BS6-pred value, byte-level where the brief for V3
   demanded it (predicate 6).
4. **Loopholes:** laziest compliant reading of every MUST — especially the count-oracle
   completeness proof, Stage-C input admissibility, the calibration-bin fixing, the
   no-reconciliation rule, and the digest/environment contract.
5. **Internal consistency:** every symbol defined before use; every slot reference resolves;
   σ_ours(a*) evaluation points named at every use; §3/§4/§5 threshold identity; class-P/E
   assignments consistent with the preamble.

## Report (write ONLY your own report file in this directory)

Write `GATE_<YOURSEAT>_SUCCESSOR_V4.md` — seat name per your dispatch instruction. Structure:
pinned sha as computed; numbered findings (severity, quote, why, minimal repair); verdict line:
**PASS** (freeze-candidate grade) or **REFUSED** (blocking findings named). Author statements
you cannot back with a shown command go under a Testimony heading.
