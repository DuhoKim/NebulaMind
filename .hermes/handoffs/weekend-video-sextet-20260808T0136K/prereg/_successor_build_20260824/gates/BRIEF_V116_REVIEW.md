# BRIEF — V116 whole-document referee round — THE LAST TEXT ROUND

**Subject:** `PREREG_SUCCESSOR_DRAFT_V116_20260830.md`
**sha256 `315162bd158b0f608503f26c96e48dd568c26cb90343bd23442f5e371d8fc886`**
**Companion, in scope: `LIFECYCLE_GUARANTEE_SPEC.md`** — the lifecycle's invariants now live there and the draft is derived from it.
Verify this digest before reading. If it does not match, stop and report the mismatch.

**Last refereed draft: V115** `8ed151b74f9b2689` — **NOT CLEAR ×2** (GPT56 4: 2 HIGH 2 MEDIUM; CODEX 3: 2 HIGH 1 MEDIUM — eight → seven). All seven dispositioned REPAIRED. **THIS IS ROUND 3 — THE LAST TEXT ROUND under the principal's option-2 ruling. After your reports: findings you mark appendix-SAFE become entries in the generated KNOWN-DEBT APPENDIX, frozen and signed with the document; findings you mark DEBT-INELIGIBLE go to the principal with your own ineligibility argument quoted verbatim — he decides sign-or-fix with your words in front of him. So for EVERY finding, state its debt eligibility explicitly and argue it — that judgment is now load-bearing, not advisory.** Before that, V114 `721e41ebfbbb6653` — **NOT CLEAR ×2** (GPT56 3: 2 HIGH 1 MEDIUM; CODEX 5: 4 HIGH 1 MEDIUM — eleven → eight, softening). All eight dispositioned REPAIRED. **THIS IS ROUND 2 OF THE CAPPED THREE — after the NEXT round the text loop ends and remaining findings become the generated known-debt appendix. You both flagged every V114 HIGH as freeze-poisoning and all are repaired this build; keep flagging debt-ineligible classes explicitly — that is what rounds here on are FOR.** Before that, V113 `8a04e549e4a25315` — **NOT CLEAR ×2** (GPT56 6: 3 HIGH 2 MEDIUM 1 LOW; CODEX 5: 2 HIGH 2 MEDIUM 1 LOW — softening after the V112 spike, nothing structural). All eleven dispositioned REPAIRED in gates/REPAIR_LEDGER.md. **PROCESS RULING you must know: the principal ruled the STOPPING RULE (`STOPPING_RULE_RULING_20260830.md`) — THIS IS ROUND 1 OF AT MOST THREE; after round 3 the text loop ends and remaining findings become a generated KNOWN-DEBT APPENDIX signed with the freeze. A SCOPE FREEZE is in force: no new record kinds, generators or controls except controls on existing generators (one new kind, VERIFICATION-CLOSE, was admitted this build under the freeze's finding-driven exception, per the coordinator's explicit prescription). Weigh your findings accordingly: HIGHs that touch χ-integrity or verifiability first.** Before that, V112 `22cf104327282adc` — **NOT CLEAR from both seats** (GPT56 9, CODEX 8; the headline was the coordinator's diagnosis — repairs landing in generated outputs while generator inputs kept the old regime, so regeneration un-repaired repairs). All seventeen dispositioned REPAIRED in gates/REPAIR_LEDGER.md. Before that, V111 `b3e147ee4e6f3ad2` — **NOT CLEAR from both seats** (GPT56 8, CODEX 7 — the count ticked up because V110's repairs introduced record types without integration, and two coordinator prescriptions broke on contact). All fifteen dispositioned in gates/REPAIR_LEDGER.md. Before that, V110 `a5f7b1b2b1294989` — **NOT CLEAR from both seats** (GPT56 4,
CODEX 6). All dispositioned in gates/REPAIR_LEDGER.md; this build also folds the
coordinator's four V109 directives, which crossed with the V110 dispatch. Before that,
V109 `69e726ffef1f1468` — **NOT CLEAR from both seats** (GPT56 4,
CODEX 4 — perfectly paired on four objects). All dispositioned in
gates/REPAIR_LEDGER.md. Before that, V108 `ae54ecdf9118d4c3` — **NOT CLEAR from both seats** (GPT56 4,
CODEX 5). All dispositioned in gates/REPAIR_LEDGER.md. Before that, V107 `d7b57cdaa24fc51f` — **NOT CLEAR from both seats** (GPT56 3,
CODEX 3 — the narrowest round of the day, five of six findings on the same three
objects). All dispositioned in gates/REPAIR_LEDGER.md. Before that, V106 `b0bfded176fdfb17` — **NOT CLEAR from both seats** (GPT56 3,
CODEX 4 — seven findings, the smallest round yet, all refining V106's own repairs). All
dispositioned in gates/REPAIR_LEDGER.md. Before that, V105 `bbf3dd45e22e818d` — **NOT CLEAR from both seats** (GPT56 4,
CODEX 5 — the smallest round yet). All dispositioned in gates/REPAIR_LEDGER.md. Before
that, V104 `7414666df54d2757` — **NOT CLEAR from both seats** (GPT56 6,
CODEX 5). All dispositioned in gates/REPAIR_LEDGER.md (v4: strict (round,seat,finding)
key, EVERY parseable block covered, pre-convention debt counted per round). Before that,
V103 `a9506e37e85e593d` — **NOT CLEAR from both seats** (GPT56 7,
CODEX 5). All dispositioned in gates/REPAIR_LEDGER.md (v3 — coverage back to V88 by
citation scan). Before that, V102 `a990cbc7d494cf3d` — **NOT CLEAR from both seats** (GPT56 7,
CODEX 8). All dispositioned in gates/REPAIR_LEDGER.md (now v2 — round discovery from the
directory, block contracts checked, its limit on its face). Before that, V101 `9a5c612ebb02ad4f` — **NOT CLEAR from both seats** (GPT56 6,
CODEX 8). All dispositioned in gates/REPAIR_LEDGER.md — READ IT: three V100 findings
returned unaddressed because the V101 build repaired from a digest without enumerating
the raw blocks; the ledger is the fix and completeness claims now QUOTE it. Before that,
V100 `8cdc89cc495ca380` — **NOT CLEAR from both seats** (GPT56 5,
CODEX 7, severity softening: 2 HIGH). All are answered below. Before that, V99 `55062e8ae7696353` — **NOT CLEAR from both seats** (GPT56 8,
CODEX 6). All are answered below. Before that, V98 `1bf168c0d31e19a3` — **NOT CLEAR from both seats** (GPT56 8,
CODEX 7). All are answered below. Before that, V97 `2a469a2f2adb239e` — **NOT CLEAR from both seats** (GPT56 7,
CODEX 5). All are answered below. Before that, V96 `ca49c2a2842f4dc0` — **NOT CLEAR from both seats** (GPT56 6,
CODEX 5). All are answered below. Before that, V95 `1262c2767cf1cfe6` — **NOT CLEAR from both seats** (GPT56 6,
CODEX 7). All are answered below. Before that, V94 `74efc959dc8c9b9f` — **NOT CLEAR from both seats** (GPT56 10,
CODEX 7). All are answered below. Before that, V93 `fbafd1c47abf145b` — **NOT CLEAR from both seats** (GPT56 9,
CODEX 6). All are answered below. Before that, V92 `1607525cf9e52a9f` — **NOT CLEAR from both seats** (GPT56 6,
CODEX 6; three exact pairs). All are answered below. Before that, V91 `38fe971a0e0fd586` — **NOT CLEAR from both seats** (GPT56 9,
CODEX 6). All are answered below. Before that, V90 `4ab89668382e291c` — **NOT CLEAR from both seats** (GPT56 8,
CODEX 4; three convergent pairs). All are answered below. Before that, V89 `c608ca9a798e1571` — **NOT CLEAR from both seats** (GPT56 7,
CODEX 6; five pairs). All are answered below. Before that, V88 `1a7810442b6d7743` — **NOT CLEAR from both seats** (GPT56 8,
CODEX 4 — the narrowest CODEX report in fifteen rounds; five findings were pairs). All are
answered below as three threads. NOTE: both V88 report blocks self-declared `VERSION: V87`
because THIS brief's template carried that literal — both seats flagged it and preserved it;
the token is corrected above an annotation in each report, and the template is fixed in this
brief. Before that, V87 `700fd8eb810c46c6` — **NOT CLEAR from both seats** (GPT56 7,
CODEX 6, six of them the same event in two mouths). All seven pairs are answered below as ONE
repair. Before that, V56 `c0743b40698e75b6` — **NOT CLEAR from both seats**, 20:22 (GPT56 5,
CODEX 6). Before that, V54 `b0ccbecc46e21677` — **NOT CLEAR from both seats**, 19:58 (GPT56 5,
CODEX 3). Three are answered below, four are referred. Before that, V53 `cc4e289578b129e4` — **NOT CLEAR from both seats**, 19:32 (GPT56 3,
CODEX 4). Five of those seven are answered below. Before that, V52 `a825e5d2045721c4` — **NOT CLEAR from both seats**, 19:18 (GPT56 3,
CODEX 5). Six of those eight are answered below. Before that, V49 `d8a9501e0653dd84` — **NOT CLEAR from both seats**, 15:47 (GPT56 7,
CODEX 3). Eight of those ten are answered below. Before that, V46 `c5afba31f909dcda` — **NOT CLEAR from both seats**, 14:06 (2 findings
each). Both are answered. Before that, V44 `4faa2564ba093ae4` — **NOT CLEAR from both seats**, 13:49 (2 findings
each). Both are answered. Before that, V43 `7b2e9a701c38c570` — **NOT CLEAR from both seats**, 13:33 (GPT56 3
findings, CODEX 2). All five are answered in V44. Before that, V40 `531d3f40f06130e792ff474e660fde931038e2d7bd8e573612b90c8ec624c1f6`
— **NOT CLEAR from both seats**, 11:26 (GPT56 7 findings, CODEX 4). V41 and V42 have not been
refereed.

## What changed since V40

**V41** `5270452ff9a54caf` — two V40 findings:
- **GPT56-V40 F4 / CODEX-V40 F4 (both seats).** `BS-3g` was missing from §6.1's closed
  non-χ-bearing receipt list. That list is exhaustive and everything off it is χ-bearing by default,
  so the slot's `blocks BS-6` edge was not receiptable. Added, plus a **§11 code-side item** — a
  receipt class with no producer or verifier is still not receiptable, and that goes slightly past
  the letter of the finding.
- **GPT56-V40 F6.** The §2.7 evidence citations were wrong for a second consecutive draft. V38 named
  §6.1/§6.2; V39's repair replaced those with **absolute line numbers**, which were already stale by
  V40 because the option-C edit added nine lines to §5. Both references are now by section and
  quoted content.

**V42** `6c9cc2fca67d5aff` — one miscitation found by hand-verification, not by any tool:
the Stage-P dual-valued passage cited `KIMI-V11 F4`, which is a **§6.1 access finding**. KIMI's
Stage-P finding is **F7** (*"the exact-null Stage P is not implemented in the file §0 pins"*), which
is the argument the passage actually makes. Corrected.

**V43** `7b2e9a701c38c570` — **the principal ruled option A on the rerun procedure: delete it.**
`INCONCLUSIVE-BY-COMPUTATION` is now a **terminal halt**; the operator's recourse is a new run
under a new preregistration, not a retry inside this one. This dissolves GPT56-V40 F1/F2/F3 and
CODEX-V40 F1/F2/F3 together — **no seed schedule, no attempt log, no verifier, no attempt cap, no
new slot, and class counts stay 16/8.** §6.1 Row J's one-outcome contract stops being contradicted
rather than being defended. **GPT56-V40 F5** is also closed: `INCONCLUSIVE-BY-COMPUTATION` is now
explicitly **subordinate to `INCONCLUSIVE-BY-CALIBRATION`** where both would apply. F5 was
re-checked against the deletion rather than assumed to dissolve with it — **it survived**, because
the overlap concerns which code fires, not the rerun.

## What is deliberately unrepaired — do not re-derive

**All V40 findings are answered.** Out of scope here:

- **The gain control's γ → sign-vector mapping** (`OPEN_QUESTION_GAIN_SIGN_MAPPING.md`) — with the
  principal. `ref/gain_counterfactual_path.py` is built and **refuses to run without a mapping**;
  supplying one is a modelling assumption requiring preregistration.
- **`require_authorization`** — deprioritised by the principal. Its limit is recorded accurately in
  §5 and is deliberately not repaired; `successor_ref_v9.py` stays frozen.

## THE LINT'S CITATION BEHAVIOUR HAS CHANGED SINCE V40 — read before running it

At V38 and V40 the citation check was **quarantined**: it emitted `repair-citations-advisory` and,
contrary to its own comment, **still failed the lint**. Both are now false.

The principal ruled option C, then option D. The check reads `FINDINGS-BLOCK v1` and indexes reports
**by the blocks they declare, never by filename** — removing the defect that made it call a real
citation fabricated. Compound citations (`SEAT/SEAT-Vn Fk`) are expanded and **every named seat is
checked**; previously only the last was seen, which is what hid the V42 miscitation above. Three
categories, two of which block: `repair-citation-fabricated` and `repair-citation-malformed` fail;
**`repair-citation-legacy` is advisory and does not.**

**On V87 the lint exits 0 — 97 advisory findings, 0 blocking.** The 96 are citations whose reports
predate the block format. **Under the principal's option D ruling that is their permanent answer, not
outstanding work.** Do not report them as unresolved.

Other checkers on V87: counts **16 class P / 8 class E** prose-matched; `prereg_trace` 86 transitions,
0 problems; `void_registry` self-test 6 controls, 0 failures.

## What V116 changed — the last repairs before the appendix

- **FORM_SCHEMAS (GPT56 F1):** one kind literal → one exact ordered field set for both
  export forms and both terminal-review forms; a FORM-SCHEMA ECHO holds each form's tuple
  byte-equal in its home corpus; "field-for-field" is per-kind, the producer selecting by
  the chain-derived kind. The flat-union reading is dead.
- **The count-oracle prose is true against the frozen bytes (GPT56 F2 — your oldest-quiet
  attack, landed):** the real symbol `validate_count_table()` named; its
  None-conditionality SAID; the closure moved to the slot — BS-2c's receipt requires the
  three proof objects non-null with digests carried, the harness refuses None before
  `_plan`, and missing/extra/disagreement controls run through `build_plan()` itself. v9
  untouched at `6a9abbbd…`.
- **Exact close-class sets (GPT56 F3 + CODEX F3):** the echo now extracts the note's
  domain segment and compares EXACT token sets both domains; STALLED-widening and
  EXPIRED-deletion are seeded controls (shared extractor — the control tests the shipped
  function, not a twin).
- **R02 demoted and widened, sentence-scoped (GPT56 F4):** a literal-shape tripwire in
  its own words; your two counterexample sentences now fire when planted (demonstrated);
  the negative control still passes; checker re-pinned.
- **The five-gate ORDER is a successor predicate (CODEX F1):** each pass record's gate
  equals the unique successor of its predecessor record's gate, first predecessor BS-2f;
  skip-BS-V, swap, and premature-boundary fixtures.
- **T1 keeps its universal subject (CODEX F2):** the mid-hold decoded-frame execution IS
  a T1 violation and is said to be — undetectable-but-violative, testimony-plus-fixture,
  the W0 consequence bounded, never blessed.

Counts, quoted from generated bytes: `total=315 nonslot=10 pending=7` · ` kinds` · `rows: 20`.

## Known and parked — do not spend the round re-deriving these

- **CODEX-V64 F2 — the four availability codes describe the logged object.** **Confirmed, unrepaired,
  and with the principal**: they are part of the vocabulary he ruled at 22:18, and changing them is
  his. **Say so if you think it is worse than recorded, but do not re-derive it.**
- **The durable pre-verdict state** (GPT56-V66 F1) — needs a second event class; changing what the
  log records is not authorised. **Referred, and re-finding it does not move it.**
- **The strata AND their producer** — `FINDING_ROW_F_STRATA.md` + `OPEN_QUESTION_STRATUM_PRODUCER.md`, **with the principal, coupled**. Row F's surface is ruled and applied; the strata decision and the producer row/surface decision are open together. **Do not re-derive; report only if the analysis is wrong.**
  Established: **both stratum axes are χ-derived**, so there is no χ-free version of the inherited
  stratification, and `N_HC_STRATA = 9` is a frozen constant in v9. **Do not re-derive; report only if
  you find the analysis wrong.**

- **The VOID/numerical partition** (GPT56-V63 F1, CODEX-V63 F1). Broken by both of you, **referred to
  the principal**, three constructions failed.
- **`REFUSED-OUTSIDE-STATED-SURFACE` + logged object identity still publishes membership**
  (GPT56-VOCAB F4, CODEX-VOCAB F4). **Confirmed and unrepaired**: the channel is the identity field,
  and changing what the log records is **not authorised**. It is bound up with the χ-adaptive access
  leak now with the principal (`OPEN_QUESTION_CHI_ADAPTIVE_ACCESS_LEAK.md`).
- **`REFUSED-INTEGRITY-MISMATCH`** — flagged, unresolved, with the principal: indistinguishable from
  tampering at emission and colliding with the phase-Any digest-deviation VOID antecedent.
- **The BS-3g lifecycle cycle** (GPT56-V63 F3): pre-BS-6 verification needing the later P3 mask and P4
  calibration. **Unrepaired and referred** — it decides whether the control gates BS-6 at all.
- **A tension I recorded rather than resolved:** a verifier timeout is foreseeable, and routing a
  foreseeable class into a category defined as a defect is in tension with itself. Flagged for the
  principal; **say so if you think it is worse than a tension.**

- **GPT56-V49 F1, the access log's unconstrained refusal reasons.** Confirmed; a closed eight-code
  vocabulary is **drafted and with the principal** (`PROPOSAL_ACCESS_LOG_REFUSAL_VOCABULARY.md`).
  **No draft text has been changed for it.** Report only if the drafted set is itself wrong.
- **GPT56-V52 F3 / CODEX-V52 F3 — the ledger classifies per raise statement, not per call site.**
  Confirmed and **referred**: the fix is a call-graph build. **Enumeration sound, unit wrong.**
- **CODEX-V53 F4 — the freeze-signature exemption is unbounded**, because no canonical body or
  verifier defines which signed bytes qualify. Confirmed and **referred**
  (`OPEN_QUESTION_V53_RESIDUE.md`); defining it is new normative machinery.
- **GPT56-V53 F2 — BS-2v is UNRESOLVED for a self-reference its own checker disproves.** Confirmed
  and **referred**; moving a slot off UNRESOLVED is a claim about what the study has settled.
- **CODEX-V52 F4 — `VOID-6.1L-WRONG-SIGNATURE` is P7-only while Row L signs at P0/P6/P7.**
  Confirmed and **referred**; changing an antecedent's phase changes what voids a run.
- **The γ → sign-vector mapping** (`OPEN_QUESTION_GAIN_SIGN_MAPPING.md`), open with the principal.
- **`require_authorization`**, deprioritised; its limit is recorded and deliberately unrepaired.

- **The four V54 findings referred to the principal** (`OPEN_QUESTION_V54_RESIDUE.md`): the evidence bar lets sampling establish a status named for a proof; the post-unblinding double-claim has recurred; `NUMERICAL-PLANNING` is a category the rule never authorised; and the 80,000-execution rerun has no pinned harness. **All confirmed — do not re-derive them.**

## Attack these specifically

- **Did V41's `BS-3g` additions actually make the edge receiptable**, or only appear to? The §11 item
  is named and not specified — say if that is insufficient.
- **THIS ROUND'S REAL DELIVERABLE IS THE DEBT MAP.** Attack anything, but for every
  finding SAY: appendix-safe (and why the freeze survives it) or debt-ineligible (and
  exactly what it poisons). Your eligibility arguments go verbatim to the principal.
- **Attack the successor predicate's ends.** First gate's predecessor is BS-2f; disclosure
  is last. **Break it**: a run with a legitimate re-boundary retry at gate k — the retry's
  boundary is "premature" for gate k+1 rules but is gate k again: does the
  next-undischarged-gate rule read retries as legal?; the reconciliation pass at
  disclosure — same gate token as disclosure's pass or a sixth consultation?
- **Attack the slot-side count-oracle closure.** The harness refuses None pre-_plan.
  **Break it**: the harness is a REQUIRED-DOES-NOT-EXIST build item — until built, what
  refuses None? (say whether the slot contract alone suffices pre-build); proof objects
  non-null but STALE (yesterday's universe digest — which check binds them to the current
  release?).
- **Attack the form-schema echo.** Tuples byte-equal in the home corpus. **Break it**: the
  same tuple string appearing in a HISTORY row (does count<1 vs ==1 matter?); a form
  renamed in FORM_SCHEMAS but not in the draft kind literals.
- **The oldest-quiet question, tenth asking** — one §2-era clause, re-derived against the
  current machinery.
- **Attack the request_key.** On-chain, unique per request, joining arrival to exactly one terminal.
  **Break the uniqueness or the join** — key reuse across restarts, a terminal whose binding names a
  key with no arrival, two arrivals one key.
- **Attack the grid-by-construction.** Endpoints pending ratification — **what happens to a receipt
  produced BEFORE ratification?** Say whether the blocker chain actually prevents it.
- **Attack the count correction.** 16/9 now, and the §10 V86 → V87 row is present with the count
  move in its generated column. **Verify every prose statement of the counts agrees with the
  generated rows.** Seven rulings entered the text in one
  revision. **Find the application that is narrower or wider than its ruling** — an ordering rule
  that quietly decides cases the ruling left open, an arrival event carrying more than arrival, a
  D2 surface wider than "runs the committee and writes the index", a terminated-class ending that a
  clause elsewhere still routes to VOID or INCONCLUSIVE.
- **The draw discipline is UNFROZEN — attack it fresh.** Complete spec: 99 draws, CRN, a-priori
  ±0.25 at Δγ=0.01, committed seed/generator. **Is anything still undefined, contradictory, or
  gameable?**
- **Break the ordering rule.** "What does the failure demonstrate" — construct the post-unblinding
  failure where demonstrates-impeachment and demonstrates-non-resolution are BOTH wrong readings.
- **Break the arrival receipt.** Arrival before any processing — **what is "arrival" for a request
  that half-arrives** (truncated on the wire, malformed framing)? Which side of the receipt does it
  fall? The cross-check extracts clause phrases
  by pattern. **Reword a clause so a leaf disappears from the extraction while remaining normative**
  — the checked declaration's own blind spot — or find the declared set the check still does not
  reach.
- **Break the issuance point.** One serialised step — **what does the single writer do if it dies
  MID-step?** Say whether the recovery-by-bindings rule covers a half-issued BS-L.
- **Break the listed-predecessors domain.** This document lists V3-pred. **What obligates the NEXT
  document to list THIS one** — and where is that obligation checkable rather than customary?
- **Break the blocker inventory.** Count the named BS-3g blockers in §11 and in the packet. **If any
  two disagree, that is the drift class; if the count is right, say whether every blocker has an
  owner** — the principal, or a build. v9 executes first and is registered under its name;
  the path binds to it. **What ELSE does executing either buffer import?** — numpy at v9's top
  arrives via real import machinery from the roots; trace the boundary where pre-binding's
  guarantee ends and the roots' re-verification is the only cover, and say whether anything falls
  between.
- **Break the temporal partition.** Pre-BS-L events sealed, post-BS-L events continued. **Find the
  event the partition orphans** — appended in the same transaction as BS-L issuance itself, or
  during the checkpoint's own assembly.
- **Break the commit bound.** Abort is always available — **is it?** A commit stalled inside the
  store's own fsync, an abort that cannot run because the domain state is the thing wedged.
- **Break the cross-run recurrence rule.** The successor reads prior enumerations — **which prior?**
  A run aborted at P2, a run voided, a run whose enumeration was itself the failure. The two pinned modules never touch the import
  machinery. **What do they import TRANSITIVELY?** — v9 imports numpy at module top; that import
  still runs inside the exec. Find the path by which executing the verified buffer reaches
  unverified code beyond the re-verified roots — or a compile()/exec semantic that differs from
  import in a way that changes a verdict.
- **Break the harness pin.** `replay_harness_sha256` pins the harness — **what pins the thing that
  CHECKS the harness pin?** Say where that regress properly stops, or find the level where it stops
  silently.
- **Break the store-qualified operations.** (row, operation) → store. **Find the touch where the
  qualification is ambiguous or forged** — an operation legitimately valid against two stores, a row
  whose surface changes by phase.
- **Break unique-JSON.** NFC + minimal escapes + integers only. **Two logically equal values, two
  byte strings, inside the rules** — or a value the rules cannot encode that a canonical body needs. The replay builds its own inputs from pinned
  artifacts. **Find the executable that still arrives from outside** — an artifact whose
  deserialization runs code, a type-exact check on a class whose module an earlier stage shadowed, a
  pinned-module mapping whose import executes more than a definition.
- **Break the flags check.** `sys.flags.optimize == 0` verified in-process. **Find the optimization
  or interpreter mode the flag does not report** — or a way the check itself is stripped.
- **Break the deadline.** A BS-2k constant bounds pending time. **Construct the request that is
  neither pending nor terminal** — clock manipulation, a deadline that resets, a refusal commit that
  cannot be appended because the chain is mid-checkpoint.
- **Third time on the opening body:** the field list now ends `schema_version`, verified against
  Clause 6. **If it is still wrong, say so with the bytes** — twice-corrected is where §8 item 9
  says to look hardest. The claim: one dynamic-load site,
  unreachable from the verdict path, and no room for an unloader under `-I -S`. **Find the reachable
  path the name-based call graph cannot see** — an attribute call, a dispatch table, an exception
  handler — from `run_production_verdict` or `perm_record` to any dynamic-load construct. The
  call-graph ledger's own limit says paths are a lower bound; **use that limit against this claim.**
- **Break the eight-field opening authorization.** Clause 6 binds it; the registry enumerates it.
  **Find the field Clause 6 implies but does not name** — or a store-identity encoding that is not
  actually closed-vocab.
- **Break the monotone-presence disjunction.** Both disjuncts assume the prior touch was of THE SAME
  object. **Attack the identity** — keys reused, an object rewritten under the same identity by an
  authorised write, a join the auditor cannot make.
- **Break the payload encodings.** Canonical JSON for structured values — **construct two logically
  equal values with different canonical bytes** (float representation, unicode normalisation), and
  say whether any canonical body can carry one. The process refuses its own result if
  a loaded object is outside the manifest. **Defeat the enumeration itself** — an object loaded and
  unloaded before the check, a loader API that under-reports, a verdict influenced by a load the
  interpreter does not count as an image.
- **Break the named residual.** log₂5 bits per catch-all emission, reviewed as enumerated defects.
  **Compose emissions across a run into more than the review can see** — or show the bound is wrong.
- **Break the falsifiable-from-history rule.** `OBJECT-ABSENT` is contradicted by a prior committed
  touch. **Construct the absence that is real DESPITE history** — an object legitimately removed?
  The stores are append-only and mutation is forbidden; if you find the path, it is a spec finding,
  not a code one.
- **Break the NAMES-CLASS template.** A parseable line that names without naming — the token member
  but wrong, the key present but another class's. The dynamic-load manifest stops at
  the linker and kernel by declaration. **Name what loads BETWEEN the manifest and the declaration**
  — dlopen at runtime after replay start, locale/codec data, a lazily-loaded numpy submodule — and
  whether any of it can touch a verdict after the manifest was recorded.
- **Break the precedence-ordered causes.** The highest applicable cause is mandatory. **Construct
  the failure where WHICH causes apply is itself χ-correlated** — the channel moved from selection
  to applicability, or it did not; say which.
- **Break the testimony split.** Availability-code truth is attributable, not detectable. **Show an
  availability refusal whose falsity is provable from the chain after all** — then the split is
  drawn in the wrong place.
- **Break the 24-byte timestamp.** Leap seconds, timezone smuggling, or a second timestamp source
  diverging from it. Printable ASCII ≤ 64 bytes on three
  interpreter strings. **Say what 64 bytes of chosen-value channel buys an adversary who controls
  the interpreter build** — and whether the honest answer is "nothing the trust declaration did not
  already concede."
- **Break the canonical-body rows.** Six canonical bodies are registry rows as digest-refs. **Which
  one lacks a defined field-order encoding somewhere in this draft** — a digest-ref to an encoding
  nobody wrote is the SCHEMA-PENDING defect wearing a canonical name.
- **Break the BS-7p sub-schema.** interpreter_path + sha256 + ordered roots. **What loads outside
  the roots** — the dynamic linker's view versus the verifier's view.
- **Break the activation guard.** The word list is finite (`reinstat|restored|reactivat|is active|in
  force|hereby|applies again|governs`). **Reactivate without any of them.** Three environment leaves are declared
  RECORDED-UNPINNED and six non-slot classes SCHEMA-PENDING. **Show either honesty is load-bearing
  for something that needs more than honesty** — a pre-unblinding reader that consumes an unpinned
  leaf, a SCHEMA-PENDING class whose producer is NOT actually blocked by the slot the stub names.
- **Break the pinned bootstrap.** Absolute interpreter path + explicit sys.path + frozen roots.
  **What still loads that the roots do not pin** — shared libraries, a numpy plugin, locale data —
  and does any of it touch a verdict?
- **Break the one-to-one session rule.** Construct the interface behaviour where a commit and a
  session cannot be paired bijectively — a crash between commit and first frame, a session that
  outlives its interface process.
- **Break the known-retired rule.** The retired set is a dict in the checker. **Can the draft's
  prose and the checker's dict diverge** — a code retired in text but absent from `RETIRED`, and
  which side wins? Envelope, environment, signatures and
  non-slot classes are in. **What still is not?** A value produced by v9 outside `receipt()`, a
  field the environment record composes at runtime, a non-slot class whose per-class schema does not
  exist yet (the digest-ref points at what, exactly?).
- **Break the isolated interpreter.** `-I -S`, cleared env, pinned cwd, trust boundary declared.
  **Find the configuration vector that survives those flags**, or show the declared boundary is
  wider than stated.
- **Break the session-bounded buffer.** It lives to the session's end. **Construct the overlap** —
  two sessions, one buffer; a session end the interface cannot observe; a fixture occlude-and-restore
  passes while a real compositor would not.
- **Break the one-retirement-per-fragment rule.** Punctuation, nesting, quotation — smuggle a second
  active token through a retiring fragment. It now claims the operative corpus. **Find
  the string-bearing surface it still misses** — a field created at runtime rather than declared, a
  v9 structure outside `SLOT_SCHEMA` that reaches a pre-unblinding reader, a classification that is
  wrong rather than absent (is `BS-8p.allocation` really a digest-ref?).
- **Break the full-body rule.** Quotes must equal the whole spec row. **Reverse meaning anyway** — a
  spec row whose own wording admits two readings, normative force smuggled outside any labelled row,
  a pin refreshed without re-reading.
- **Break the fresh-interpreter claim.** A new process hashing-then-importing — **find the rebinding
  window that survives** (sitecustomize, PYTHONSTARTUP, an import hook in the environment the
  process inherits), and say what pinning the ENVIRONMENT would take.
- **Break the fragment-scoped retirement.** Two tokens, one fragment, one retirement word — which
  reading wins, and can punctuation smuggle an activation? It is label-bound, deletion-detecting in both
  directions, and blocking in the lint. **Diverge anyway**: normative lifecycle semantics stated
  outside any `G…/N… —` label (its stated blind spot — find an instance that matters), a quote that
  is a true substring of its row but reverses meaning by truncation, a battery path that never runs
  the lint.
- **BREAK THE REGISTRY GENERATOR.** It extracts from schema blocks by pattern. **Find the declared
  string-bearing field its extraction cannot see** — a field defined in prose rather than a list, a
  schema block whose format drifted, a field name the token pattern still misses.
- **Attack the bijection.** Entry↔emission both directions — construct the surviving orphan: an
  entry and an emission that pair under the join but should not, or a chain rewrite the recompute
  cannot distinguish.
- **DO NOT attack the draw discipline** — frozen pending the principal's sitting, reason in the ⛔
  banner. Findings against it will be recorded, not repaired, so spend the round elsewhere. It pins the spec by digest and requires
  quoted invariant bodies to be spec bytes. **Diverge without tripping it**: a paraphrase that avoids
  the `G… —` marker, normative lifecycle text living outside the quoted block, a stale pin the
  battery never runs. The check names its own blind spot — an unlabelled reworded invariant — **find
  one that matters.**
- **BREAK THE STRING RULE.** Every string field closed or bounded, no third kind. **Find the string
  the rule misses** — a field someone would not call a string, a bounded encoding that can smuggle
  (the class-key components? the identifiers?), a closed vocabulary whose members compose into prose.
- **Break the five-cause vocabulary.** A real failure it cannot express forces `NAMED-AS-DEFECT` and
  a vocabulary re-derivation — is that loop livable, or does every run die of its first novel
  failure? If the vocabulary is too small, say what is missing rather than that it is small.
- **Break the session boundary.** Continuous uninterrupted display — construct the practice that is
  plainly several views under one commit, or plainly one view that the rule fragments into many. G2/G3 were restated on your finding; the
  view-session definition is new. **Attack the restatements**: an event that is neither a touch's nor
  a refusal's under some failure; a render session whose boundary (position advance / interface
  clear) can be gamed to keep one commit alive across what any reader would call several views.
- **Attack the production-equal binding.** Verdict cells must replay under `n_perm = 100,000` and the
  frozen address contract. **Find the degree of freedom that survives** — an address parameter not in
  the frozen set, a replay the verifier cannot afford and therefore samples, a path digest that pins
  the module but not its imports.
- **Attack the within-draw rule.** `HELD` iff every `(i,j)` equals `(i,0)`. Construct the case where
  that is satisfied while the gradient demonstrably matters, or where draw-level comparison hides an
  aggregate shift every draw shows weakly.
- **Attack (iv-b).** Explanations are non-χ by constraint, not by construction. **Write the
  explanation that passes the constraint and still leaks** — the constraint is a sentence, and you
  have beaten sentence-constraints all night. Read `LIFECYCLE_GUARANTEE_SPEC.md` as the
  object under review. **Find a failure that lands in no cell of its window × reader table, a reader
  it forgot, or a touch kind that is neither conveyance, render nor write.** A corner already covered
  by a G or an N is not a finding.
- **Attack G5's dissolution of the Row G fork.** The spec claims no normative choice was needed
  because Row G's clause already required logged views. **If the fork survives — if some permitted
  re-view practice cannot be a fresh touch commit — say so plainly; that returns it to the principal
  as the open question the spec says it would be.**
- **Attack the continuation segment.** Post-`BS-L` entries authenticate independently by signature +
  chain join. Forge one, orphan one, or find the verifier unable to distinguish continuation entries
  from checkpoint entries where it matters.
- **Attack the conditional `HELD`.** Its three named conditions — can a receipt satisfy every
  verifier clause while a condition is violated in a way the text claims is detectable? G1–G4 and N1–N2
  claim to partition every failure. **Find a failure that is neither guaranteed against nor named as
  a non-guarantee** — that would show the guarantee itself is incomplete, which is the only defect
  left that a new corner-case can reveal. A corner already covered by G or N is not a finding.
- **Attack N1's edge.** Delivery is outside the custody claim — but Row G's interface renders bytes
  to a human. **Is a rendered view "delivery" or a store touch?** If the committed buffer outlives
  its delivery, who holds it and under what surface?
- **Defeat the disposition bindings.** Mint a re-derivation digest that names nothing, resolve an
  `explanation_ref` to an artifact that explains a different emission, or find the dangling-ref check
  unable to see across the checkpoint boundary.
- **Test the coarsened key.** `(row, operation)` merges more — **does it merge too much?** If two
  genuinely distinct defects share a key, the recurrence rule forces a re-derivation naming what,
  exactly?
- **Test `k_gamma`'s rule.** The bound is `|γ̂| + k·σ` — construct the case where that formula is the
  wrong shape (σ underestimated, γ̂ biased toward zero) and say whether the formula or only the value
  is at fault. It claims a touch commits
  entirely or not at all across three effects — store, event, binding. **Attack the commit domain's
  edges**: a read whose bytes reach the requester outside the domain, a write acknowledged before its
  commit, recovery replaying a committed touch, the contract being unimplementable for the
  predecessor archive Row B also mediates. **The stated residue is the invisible pre-commit death;
  anything worse is a finding.**
- **Defeat the enumeration entry.** Forge the join, smuggle free text through `explanation_ref`,
  discharge a key with two `EXPLAINED` entries under two different digests, or find an emission the
  chain recompute cannot see.
- **Split a class anyway.** The key is `(row, operation, lifecycle state)`. **Can a caller vary its
  requests so one defect presents as many keys?** If yes, say whether that is a real evasion or the
  honest grain of the data.
- **Evade the manifest constraints as now executed.** Endpoints, three values, spacing, field
  equality — construct a conforming manifest that still dodges the invariance question.
- **Test the discharge split.** Find the path where a `FAILED` or `NOT-EVALUATED` receipt still
  advances anything.
- **Test the recomputation predicate.** Can boundaries built from a stratum-contaminated input equal
  the full-set recomputation without being harmless? The claim: the event always
  carries a true outcome, nothing is delivered unlogged, and one request never produces two events.
  **Find the counterexample** — a write whose staging is itself the commit, a read whose resolution
  cannot be held, a recovery that re-resolves and appends twice. **The stated residue is that a
  crash between append and release over-reports a touch; anything WORSE than that is a finding.**
- **Defeat the enumeration verifier.** It recomputes emissions from the chain and is consulted at
  `BS-L` and at the opening. **Find the catch-all event that reaches unblinding anyway** — appended
  between the two consultations, or invisible to a recompute, or discharged by an entry that says
  nothing.
- **Defeat the recurrence rule.** Explanation no longer discharges a recurring class. **Find the way
  to keep a class routine anyway** — by re-labelling it each time so it never "recurs", or by keeping
  each instance formally distinct.
- **Attack the manifest's honesty, not its coverage.** The draft says `HELD` is bounded by grid
  resolution and refutation is decisive. **Check that every other place the invariance outcome is
  used respects that asymmetry** — a downstream reader treating `HELD` as proof would defeat the
  statement.
- **Is Row F now coherent?** Its surface admits a χ-bearing input and its void clause forbids one
  reaching **bin construction**. **Find the path by which the stratum index reaches
  `calibration_bins()`**, or by which the widened surface admits more than the allocation needs. The claim: under append-before-release, no
  request is both undecided and delivered, and none is decided twice. **Find the counterexample** — a
  partial release, a write committed before its append, a retry that produces two events for one
  touch, a lease handover that appends twice. **The stated residue is that a crash between decide and
  append is indistinguishable from a request that never arrived; anything WORSE than that is a
  finding.**
- **Is the blocking invariant actually blocking?** `BS-L` and the lock opening are blocked by an
  unenumerated `REFUSED-UNCLASSIFIED`. **Find the path to unblinding with one outstanding** — a
  refusal that never reaches the chain, an enumeration that can be satisfied vacuously, a gate that
  does not consult it.
- **Can BS-3g still be emitted?** Three separate blockers now claim to prevent it — unset `n_draws`,
  unset `draw_master_seed`, empty `draw_generator_id` set. **Find the emission path that survives all
  three**, or show one of them is not actually enforced.
- **Does the manifest rule bind?** It must span `±gamma_bound` with three distinct values. **Construct
  a conforming manifest that still evades the invariance question.**
- **BREAK THE REQUEST LIFECYCLE more broadly**, beyond atomicity. It claims every
  request ends in exactly one state with one terminal treatment, and that no request can end undecided
  or unlogged. **Construct one that does** — a failure between states, a write whose surface check
  cannot complete, a crash exactly at the log boundary, a request that is in two states at once.
  **CODEX: this is the repair you named; try to break your own prescription.**
- **Does the catch-all guard actually bind, or is it a wish?** The claim is that freeze-time
  enumeration keeps `REFUSED-UNCLASSIFIED` from becoming routine. **Find the path where it becomes
  routine anyway** — a class of refusal that lands there every run and passes the enumeration because
  it is always the same one.
- **Is the eleven-code set still leaking?** You both found that `REFUSED-OUTSIDE-STATED-SURFACE` plus
  the logged object identity still publishes membership. **That is NOT repaired here** — see the
  parked list. If you find a *different* leak, that is new.
- **Can the frozen-seed pre-commitment be defeated?** `n_draws` and `draw_master_seed` are frozen in
  the document and the verifier refuses unless the receipt matches. **Find the way to still choose
  favourably** — a generator whose id admits a family, a manifest reorder, anything that varies the
  draws while the frozen values stay put.
- **BREAK THE PARTITION — only if you have something new.** Already broken and referred; see above. §5 claims every
  post-unblinding failure is owned by exactly one clause and resolvable **without precedence**.
  Construct a counterexample: a post-unblinding failure that is genuinely ambiguous, or one both
  clauses claim, or one neither claims. **If you need the precedence rule to decide any case, the
  repair has not landed and the draft says so itself.** Attack the exhaustiveness ("every failing
  quantity was either verified before or computed by the run") and the exclusivity separately.
- **Does the narrowing of `VOID` let a real deviation be reported as arithmetic?** VOID now covers a
  post-unblinding non-finite only when it shows a pinned/sealed/verified object wrong. Try to build a
  protocol breach that presents as an ordinary failed computation. The draft's answer is that
  forbidden acts and protocol/digest deviation remain `Any` phase and catch it independently — test
  that answer rather than accept it.
- **Is any of L963, L973 or L986 reachable during a run?** The ruling was about planning failures, not
  about those three line numbers. **If you find a run-time path, say so plainly — that goes straight
  back to the principal**, and the draft commits to that.
- **Can the draw set be satisfied and still not bound the gate?** Construct a BS-3g receipt that
  passes all four draw fields and the verifier's clause (e) while reporting something that is not the
  worst of the draws — or that fixes `n_draws` after seeing a verdict.
- **Does any text still admit measurement-only promotion?** Basis (i) is dropped in §5; check §11, §7
  and the ledger for language that still lets a count promote a guard.
- **Does the scoped binding leave a slot receipt unbound?** Find a producer of a `SLOT_SCHEMA`
  artefact that can still reach `v9.receipt()` directly. **If one exists, say so plainly — that is
  the unfreeze path and it is the principal's.**
- **Can BS-3g's mask binding be defeated?** The claim is that equality with BS-2f's `mask_digest`
  prevents certifying a subset. Try to construct a receipt that satisfies it while covering less.
- **Is BS-3g's per-object claim actually a property?** §11 argues no field can carry a per-object
  quantity *because of the field list itself*. Try to defeat it — a field that admits an
  object-indexed value, or a route by which an extra field survives `receipt()`'s both-directions
  check.
- **Is the producer binding complete?** Find a producer named in §6.1 or §7 that could construct a
  receipt without going through `receipt_strict()`. **If one exists, say so plainly — it is the only
  path back to unfreezing v9 and it is the principal's call, not mine.**
- **Does the suspension actually suspend?** Check no clause elsewhere still relies on the eight-code
  set being in force, or on the withdrawn closure argument.
- **BS-3g still has no pinned schema, producer or verifier** (CODEX-V56 F2, HIGH, unrepaired). It is
  the slot authorised this morning, still not doing its job — say if V57 makes that worse.
- **Is the withdrawal complete?** No site should hold `UNREACHABLE-BY-CONSTRUCTION` anywhere in
  the draft or the ledger. Check both, not one.
- **Is the restated evidence bar actually sufficient?** It now demands varying every argument in
  the callable's documented surface. Is there a way to satisfy that literally and still be wrong —
  a dimension of reachability neither measurement nor a subsuming condition captures?
- **Is the Row L exemption narrow enough, and wide enough?** It names two objects. Is there a third
  signature the row mandates that the condition still catches — or does the exemption reach anything
  it should not?
- **Is `UNREACHABLE-BY-CONSTRUCTION` safe?** Test the falsification clause: if a guard so marked
  fires, does the document actually terminate the run in a named outcome, or does it only say so?
- **Is the class rule actually a rule?** Test whether it would terminate a raise site nobody has
  enumerated, or whether it only appears general while relying on the sites §5 names.
- **Is the caller-error boundary right in both directions?** Too wide and it swallows input
  validation; too narrow and real failures stay unterminated. Try to find a raise site the test
  classifies wrongly.
- **Can the new code go unreachable**, as its predecessor did at V44? The precedence clause is meant
  to prevent that; check it does.
- **Is the closed-enumeration argument in §5 sound?** It rests on Row R's default-forbidden clause
  and on §2.7(c) disposing of a per-object non-finite output as an exclusion. If either premise
  fails, the deletion's justification fails with it.
- **Is the V43 rerun deletion complete?** The five-step allowance is gone; check no clause elsewhere
  still assumes a rerun exists.
- **Does any other row still describe a ruled question as open?** F2 was stale for three hours
  because a ruling landed and the row was not re-derived. Look for others.
- **Is the V42 citation correction right?** `KIMI-V11 F7` is asserted to support the Stage-P claim.
  Check it against the report rather than taking my word.
- **The §5 VOID split from V40 still stands unreviewed in part.** Verify the misconduct conditions
  (`forbidden acts`, `protocol/digest deviation`) remain at `Any` everywhere in the prose — a quiet
  narrowing there would be unauthorised.
- **Class counts moved 15/8 → 16/8 at V37.** Check nothing still assumes the old inventory.

## Carry the absence-clause lens

**A narrow pattern is safe for presence and dangerous for absence.** For each universal negative
("no X can…", "nothing may…", "cannot create"), ask what construct would make it false and whether
the document *enforces* the exclusion or merely asserts it. That lens has produced a real finding in
every round it has been used.

## REQUIRED REPORT FORMAT

Write to `gates/V116_WHOLE_REVIEW_<SEAT>.md`, `<SEAT>` being `GPT56` or `CODEX`. End with exactly:

    <!-- FINDINGS-BLOCK v1 -->
    SEAT: <GPT56|CODEX>
    VERSION: V116
    VERDICT: <CLEAR|NOT CLEAR>
    COUNT: <n>
    F1 | <HIGH|MEDIUM|LOW> | <REPAIR-REQUIRED|ADVISORY|HELD> | <§ and line> | <one-line summary>
    <!-- END FINDINGS-BLOCK -->

The marker must start at **column 0**. Number findings contiguously from 1; `COUNT` equals the number
of `F` lines; `COUNT: 0` and no `F` lines if you find nothing. A confirmation of a previous repair is
`HELD`, **not** a finding — do not number it.

**A verdict of CLEAR means the text is a correct preregistration that is honest about being an
unfinished programme.** It does not mean the study may proceed. BS-6 and the first image byte remain
blocked; BS-2a stays DESIGN, UNFILLED.
