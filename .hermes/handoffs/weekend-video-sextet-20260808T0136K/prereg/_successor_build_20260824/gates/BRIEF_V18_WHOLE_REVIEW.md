# REFEREE BRIEF — V18, whole document. Third assembled round.

Subject: **`../PREREG_SUCCESSOR_DRAFT_V18_20260827.md`**, sha256
`ce144dc23ba8605df1a3b7590464fc3de09c313a597168f91c80d4b29ab302f4`. **Verify before opening, and
record the result.** 50 lines changed from V17.

## What your V17 round produced

Both NOT CLEAR, three convergent HIGH blockers plus one you alone caught, CODEX. All four repaired:

1. **§2.7's reason (d) is genuinely removed** — the two remaining occurrences are in §10's repair
   trace, recording the deletion. **One confidence predicate, one authority; Row P applies it and is
   not an alternative place it may be defined.** Verify that holds.
2. **The registry is split by cardinality.** §5 now says `run_production_verdict()` emits **exactly
   one run-level outcome**; per-attempt terminal states are **zero or more per run, never a run
   outcome**. The seat also reports adding a **missing Row I abort** while doing it — that was not in
   the brief; **check it.**
3. **The scalar/profile threshold now states the calibration gate's precedence.** Verify against
   `../ref/successor_ref_v9.py` lines 1492–1496 yourself.
4. **CODEX 4 — the self-violating clause is closed.** §6.3 required every gated revision to carry its
   finding→change map in §10, and V17 had no V16→V17 entry. V18 adds V16→V17 and V17→V18. **Check
   both entries are accurate**, not merely present — a trace that misdescribes its own change is
   worse than a missing one.
5. The chronology fix is applied to the fold record as well as the banner, and §10's historical claim
   is removed.

## What to judge

1. **Digest first**, recorded.
2. **Clause 10 across §§0–11, both directions, against both registry sets.** The split is new. A
   partition asserted in prose can still overlap in fact — check the two sets are disjoint **by
   construction**, and that every state in each is reachable.
3. **The unbriefed Row I abort.** A seat adding something nobody asked for is exactly where an
   unreviewed change enters. Judge it on its merits and say whether it belongs.
4. **Every threshold: value, phase, failure effect**, whole document.
5. **Are the two new §10 trace entries true?** Compare them against what actually changed.
6. **Did these repairs break anything adjacent?** Four of the last five rounds introduced a defect
   while fixing one. 50 lines changed.
7. **Does the document overclaim?**

## Standing state

Findings 1, 2, 2b and 3 **UNRESOLVED**; **BS-2a REFUSED**; rows C2 and E cannot run; **BS-6 and the
first image byte blocked**; `verify_lock()` and the unblinding-receipt schema required work, not
implemented. Any single post-unblinding removal emits `INCONCLUSIVE-BY-CALIBRATION`.

V15, V16 and V17 are held at their reviewed digests and were verified immutable across this run.

Do not read `/Users/duhokim/NebulaMindData/`. Nothing is authorised to fetch. No deadline.

## Verdict

`V18_WHOLE_REVIEW_<YOURSEAT>.md`. Numbered findings with severity, section and line, why it fails,
smallest sufficient repair. Unverified assertions under `Testimony`. Final line exactly `**CLEAR**`
or `**NOT CLEAR**`.

The blocker count has fallen every round: four and five, then three and four, now this. **If it is
sound, say so. If it is not, say what is wrong.** Judge independently; do not converge.
