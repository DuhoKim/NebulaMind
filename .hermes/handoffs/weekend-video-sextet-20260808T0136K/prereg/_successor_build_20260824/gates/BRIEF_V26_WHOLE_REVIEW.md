# REFEREE BRIEF — V26, whole document. A refuted claim replaced, and a slot walked back.

Subject: **`../PREREG_SUCCESSOR_DRAFT_V26_20260827.md`**. Its sha256 is pinned in
`runner_v26_chain.log` after the drafting seat exited. **Verify it and state what you compared.**

## You refuted the coordinator's argument and V26 accepts it

Both of you found, independently and as CRITICAL, that **temporal precedence is not independence
from handedness.** CODEX: *"Chronology establishes only that this study's later instrument output did
not cause those earlier catalogue values… A pre-existing variable may correlate with a later outcome
through sky position… The receipt itself establishes substantial correlations with the tested axis."*

That was my argument, not the drafting seat's. The receipt reports
`corr(psfsize_r, cos θ) = +0.3659`, and the Longo hypothesis is that handedness correlates with
position on that axis — so the cut may reach handedness *through position*, which is the circularity
the design exists to prevent. **The refutation is accepted in full.**

V26 should now claim only that the predicate is **outcome-blind with respect to this study's
unobserved χ**: columns and absolute thresholds fixed **without reading χ and before any image byte**,
so it cannot be tuned post hoc. **Check that no claim of independence from handedness survives
anywhere** — not softened, not moved to another section.

**And check the open question is stated as open.** Whether the predicate is independent of handedness
*conditional on position* — the property the dipole estimator actually needs — should be recorded as
**not established**, either with a preregistered check or as a named assumption carrying its risk. **A
confident weaker claim substituted for a refuted stronger one is the failure mode here.**

## What else V26 was told to change

1. **The predicate moves to before BS-2f** (CODEX's repair), so the **P3 sealed mask genuinely holds
   49,211 rows** while the **65,060-row parent identity is unchanged**. Rows C2/E/F, the terminal-state
   vocabulary, BS-2f and Clause-10 phases follow. Post-unblinding instrument-confidence stays
   separate. **Previously §4 and BS-5f printed a P5 population that did not exist until P8.**
2. **BS-2a walked back from FILLED to DESIGN-defined-UNFILLED.** Your rule: a DESIGN slot gated as
   text *and* code cannot be FILLED while its code, schema and digest are deferred. **One of fifteen
   class-P slots is filled, not two.** Check every status statement and count agrees — that
   propagation is what failed last round.
3. **Stage-P marked superseded** — 995/1000 was computed on 65,060 and cannot stand for a changed
   geometry.
4. **`tools/prereg_trace.py` no longer truncates** the changed-section list; CODEX found it dropping
   sections silently. The document still owes a **non-self-referential coverage contract** and either
   populated or explicitly exempted historical mappings.

## What to judge

1. **Digest first**, comparison stated.
2. **Is the narrow claim actually narrow?** This is the round's central question.
3. **Does the P3 mask now genuinely hold 49,211**, with every phase, row and vocabulary entry agreeing?
4. **Did the BS-2a walk-back propagate everywhere**, or only to §7's row?
5. **Clause 10 across §§0–11 both directions; every threshold for value, phase and failure effect;
   read the neighbours.**
6. **Is the V25→V26 findings mapping honest** — citing only findings this delta demonstrably answers?

## Standing state

**BS-2a DESIGN/UNFILLED. One of fifteen class-P slots filled.** BS-2v UNRESOLVED; findings 1, 2, 2b
and 3 UNRESOLVED; rows C2 and E cannot run; **BS-6 and the first image byte remain blocked.** No image
byte fetched or authorised; the catalogue metadata query was separately authorised and is complete.

Do not read `/Users/duhokim/NebulaMindData/`. No deadline.

## Verdict

`V26_WHOLE_REVIEW_<YOURSEAT>.md`. Numbered findings with severity, section and line, why it fails,
smallest sufficient repair. Unverified assertions under `Testimony`. Final line exactly `**CLEAR**`
or `**NOT CLEAR**`. **Judge independently; do not converge.**
