# REFEREE BRIEF — V27, whole document.

Subject: **`../PREREG_SUCCESSOR_DRAFT_V27_20260827.md`**. Its sha256 is pinned in
`runner_v27_chain.log` after the drafting seat exited. **Verify it and state what you compared.**

## What you validated in V26, and must not be lost

Both of you confirmed independently that the central language repair held: **no claim of statistical
independence from handedness survives**, and the conditional-independence gap at §2.7 line 378 is
**stated as open** rather than replaced by a confident weaker claim. The drafting seat was told not to
touch that sentence. **Confirm it is intact and still says what it said.**

## The three repairs, two of which you ordered twice

**1. Catalogue quality added to §2.7's closed pre-lock exclusion vocabulary** (CRITICAL, both), with
its authenticated fields, source digest, one-to-one join keys, verifier and failure effect in the
BS-2a/Row-E path — and **removed from Row P's P8 decision precedence and the adequacy receipt's fatal
`EXCLUDED-BY-*` set**, or carried there only as an already-resolved pre-lock status that cannot
constitute a P8 removal. **Check which was chosen and that it is consistent.** P8 absence,
non-finiteness and instrument-confidence stay separate.

**2. Stage P marked superseded** (HIGH, both). **This was in the V26 brief and did not happen.**
995/1000 was computed on 65,060; the analysed population is 49,211. The document should now say
superseded, and that **BS-5p cannot be filled until Stage P is rerun on the post-exclusion mask.**
**Check it says that, not that it "may need revisiting".**

**3. The false preamble closure claim deleted, and the orphan `VOID` registry ID resolved** (HIGH,
CODEX) — either the C2 attestation-failure antecedent and effect are defined in Row C2, or the ID is
removed. **Reverse reachability must remain explicitly unresolved and BS-6 blocked.** An orphan ID
reads as coverage, which is why it is worse than an admitted gap.

## Now enforced, so judge the claim

`tools/prereg_trace.py --check` returns **zero** on V26 under a contract now encoded in the checker:
in-band coverage stops at the subject's predecessor; the current transition lives in the sidecar
`gates/FINDINGS_MAP.md`; **V1→V15 are exempt by a named rule in code, not by silence.** A V25→V26
mapping was added, and removing any mapping makes the check fire on exactly that transition.

**§10 may now claim the mapping is enforced. Verify the claim rather than accepting it** — run the
checker yourself, and say whether the three rules are the ones the document describes.

## What to judge

1. **Digest first**, comparison stated.
2. **Did all three land in the document**, not in the intention? Two of them were ordered in the
   previous round and did not.
3. **Clause 10 across §§0–11, both directions** — with attention to whether moving catalogue quality
   out of P8 orphaned anything at P8.
4. **Every threshold: value, phase, failure effect.** **Read the neighbours.**
5. **Is §2.7 line 378 unchanged?**

## Standing state

**BS-2a DESIGN/UNFILLED. One of fifteen class-P slots filled.** BS-2v UNRESOLVED; findings 1, 2, 2b
and 3 UNRESOLVED; rows C2 and E cannot run; **BS-6 and the first image byte remain blocked.** Stage P
superseded pending rerun. No image byte fetched or authorised.

Do not read `/Users/duhokim/NebulaMindData/`. No deadline.

## Verdict

`V27_WHOLE_REVIEW_<YOURSEAT>.md`. Numbered findings with severity, section and line, why it fails,
smallest sufficient repair. Unverified assertions under `Testimony`. Final line exactly `**CLEAR**`
or `**NOT CLEAR**`. **Judge independently; do not converge.**
