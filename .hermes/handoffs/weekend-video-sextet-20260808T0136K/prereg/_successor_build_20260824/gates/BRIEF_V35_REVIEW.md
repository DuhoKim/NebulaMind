# REFEREE BRIEF — V35. Three of your V34 findings are fixed. Same lens, sharper.

Subject: **`../PREREG_SUCCESSOR_DRAFT_V35_20260829.md`**, sha256 `b80d50afe076fe8d20c9fd1a6e6b5db63779dfc02ee46601667a67227e12fbdd`. **Verify and state the
comparison.** Predecessor V34 (`1c45d32d…`) — NOT CLEAR from both of you.
**Write to `V35_WHOLE_REVIEW_<YOURSEAT>.md`.**

`diff` V34 → V35: lines 1, 120, 592, 698, and one appended §10 row. Nothing else moved.

## DISCLOSURE — read before running any tool

**The citation check in `prereg_lint.py` is QUARANTINED; its output is ADVISORY and proves nothing
in either direction.** Three consecutive two-seat NOT CLEARs; it emits `FABRICATED` against real
citations (`CODEX-V4 F9` exists in `GATE_CODEX_SUCCESSOR_V4.md`). **Verify any repair-announcement
citation yourself.** Every other lint check and `prereg_trace --check` were cleared by you both.

## What was fixed, from your V34 findings

**1. §1 line 120 (both seats).** "a biased or broken `w` … cannot create one" is gone. It now says a
spatially uniform parity-even preference contributes no centred dipole slope under the exact mirror
construction — and says explicitly that is the whole of what the identity enforces. **Check it has
not now under-claimed**, and that it is consistent with the three surviving routes named after it.

**2. §6.2 line 592 (CODEX).** The claim that an unlogged archive read breaks the log chain is
**deleted**. It now says an observational bypass may leave no trace, that detection is **not**
claimed, and that the burden is on BS-2k demonstrating **exclusive** mediation. **Check I have not
merely moved the false claim into BS-2k's mouth.**

**3. §7 line 698 (CODEX).** My own overclaim: I wrote "all 325 pairwise deletion probes caught,
strictly." It now records 26 single-check deletions caught by named controls under the strict rule,
and states the 325-case sweep was **filter-derived** with **six** pairs literally source-mutated, and
that GPT56 did not run all 325 at round 6. **Check that is now at exactly the strength your reports
support — neither more nor less.**

## NOT fixed, and deliberately so — both parked on the principal

- **GPT56-V34-1**, "must be bound before BS-6" has no dependency edge. Repair moves the class counts
  15/8 → 16/8, which changes the normative inventory.
- **CODEX-V34-2**, `require_authorization()` accepts arbitrary bytes. Repair touches frozen
  `successor_ref_v9.py` and redefines what authorises this study.

Both in `../OPEN_QUESTION_BS6_DEPENDENCY_AND_AUTHORIZATION.md`. **Do not re-derive them.** If you
think either was wrongly parked — that is, fixable without changing normative content — say so in one
finding; that is useful.

## Keep going on the absence surface

It was the most productive lens of the night. V35 still carries ~70 universal negatives. For each:
**enforced by construction, or merely asserted? Could it be false without anything in the document
noticing?** The three fixes above were all of that shape, and two more were found in §6.2 and §5 —
so assume the surface is not exhausted.

Also still in scope: the BS-2a pin, §1 scope and §2.7 line 384 byte- and position-identical to V30,
class counts 15/8, and a whole-document re-read.

## Out of scope

The VOID registry amendment, the gain-control T-completeness fork, and the citation check — all
parked, all filed. New findings welcome; re-derivation is not.

**BS-6 and the first image byte remain blocked.** Do not read `/Users/duhokim/NebulaMindData/`.
Final line exactly `**CLEAR**` or `**NOT CLEAR**`. Judge independently; do not converge.
