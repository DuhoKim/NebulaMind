# REFEREE BRIEF — V36. Both your V35 MEDIUMs are fixed. Same lens.

Subject: **`../PREREG_SUCCESSOR_DRAFT_V36_20260829.md`**, sha256 `e4d7b175ac270f4cdc0bc4af3a16af0e834aa3e4eacc174a73d10798cd4b6177`. **Verify and state the
comparison.** Predecessor V35 (`b80d50af…`). **Write to `V36_WHOLE_REVIEW_<YOURSEAT>.md`.**

`diff` V35 → V36: line 1, line 698, and one appended §10 row. Nothing else.

## DISCLOSURE

**The citation check in `prereg_lint.py` is QUARANTINED; its output is ADVISORY and proves nothing
in either direction** (three two-seat NOT CLEARs; it emits `FABRICATED` against real citations).
**Verify any repair-announcement citation yourself.** Every other lint check and
`prereg_trace --check` were cleared by you both.

## What was fixed — both were mine, both in the BS-2a pin

**GPT56-V35-1.** My V35 edit left "all 26 single and 26 single-check deletion probes", which reads
as 52. I substituted over the wrong half of "26 single and 325 pairwise". It now states **one strict
single-deletion sweep over the 26 unique checks, all 26 caught by a named control, zero crash-only,
zero undetected.**

**CODEX-V35-1.** "a crash fails closed, never a PASS" omitted the exit-status boundary. You flagged
this half in V34 and I repaired only the other half. It now says **no builder-produced row reached a
crash in the 65,060-row census, every observed crash exited nonzero, and consumers must gate on exit
status — because a post-verification emit failure can print the true `MATCH` summary and then exit
1.**

**Check both are now at exactly the strength your round-6 reports support, neither more nor less.**
This row has carried an overclaim of mine three times; assume a fourth.

## Still in scope

- The **absence surface** — V36 still carries ~70 universal negatives. Enforced by construction, or
  merely asserted? Could it be false without anything in the document noticing? It has produced a
  real finding every round it has been applied.
- §1 scope and §2.7 line 384 byte- and position-identical to V30; class counts 15/8; BS-2a pin;
  whole-document re-read.

## Out of scope — parked on the principal, do not re-derive

The BS-6 dependency edge (repair moves counts 15/8 → 16/8), `require_authorization()` accepting
arbitrary bytes (repair touches frozen v9), the VOID registry amendment, the gain-control
T-completeness fork, and the citation check. All filed as `OPEN_QUESTION_*.md`. New findings welcome.

**BS-6 and the first image byte remain blocked.** Do not read `/Users/duhokim/NebulaMindData/`.
Final line exactly `**CLEAR**` or `**NOT CLEAR**`. Judge independently; do not converge.
