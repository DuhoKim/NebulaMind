# FINDINGS MAP — which referee finding each transition answers

Human-written. `tools/prereg_trace.py` reads this and **fails** when a transition changed a normative
section while citing nothing here. The tool computes the bytes; it refuses to decide which finding a
change answers, because that is a judgement.

Format: `V<from>→V<to>: <finding ids, comma separated>`

V15→V16: FOLD — §6 replaced from SECTION6_DRAFT_AGY_R15 (d2c388a4) on principal's instruction 21:48
V16→V17: GPT56-V16-1, GPT56-V16-2, GPT56-V16-3, GPT56-V16-4, CODEX-V16-1, CODEX-V16-2, CODEX-V16-3
V17→V18: GPT56-V17-1, GPT56-V17-2, GPT56-V17-3, CODEX-V17-1, CODEX-V17-2, CODEX-V17-3, CODEX-V17-4
V18→V19: GPT56-V18-1, CODEX-V18-1, CODEX-V18-2
V19→V20: GPT56-V19-1, CODEX-V19-1
V20→V21: GPT56-V20-1, GPT56-V20-2, GPT56-V20-3, CODEX-V20-1, CODEX-V20-2
V21→V22: GPT56-V21-1, GPT56-V21-2, GPT56-V21-3, CODEX-V21-1, CODEX-V21-2, CODEX-V21-3, CODEX-V21-4
V22→V23: CODEX-V22-1 (class-E count 8 not 7), CODEX-V22-2, CODEX-V22-3, CODEX-V22-4, GPT56-V22-1, GPT56-V22-2, GPT56-V22-3
V23→V24: CODEX-V23-1, CODEX-V23-2, CODEX-V23-3, GPT56-V23-1, GPT56-V23-2, GPT56-V23-3, plus BLANC-20260828 (compute the counts and the trace)
V24→V25: BS2A-ADOPTION-20260828 (quality-cut exclusion predicate; principal's instruction), GPT56-V24-1, GPT56-V24-2, CODEX-V24-4, CODEX-V24-5, CODEX-V24-6
V25→V26: GPT56-V25-1, GPT56-V25-2, CODEX-V25-1, CODEX-V25-2, CODEX-V25-3, CODEX-V25-4
V26→V27: GPT56-V26-1, GPT56-V26-2, GPT56-V26-3, CODEX-V26-1, CODEX-V26-2, CODEX-V26-4
V27→V28: GPT56-V27-1, GPT56-V27-2, GPT56-V27-3, CODEX-V27-1
V28→V29: CODEX-V28-1 (current-transition scope rule)
V29→V30: PRINCIPAL-20260828-LAND-NULL (human direction: "add the land 2008 null to the prereg motivation"; no referee finding — the change answers an instruction, and inventing a finding ID for it would be a lie)
V30→V31: GPT56-V30-1, GPT56-V30-2, GPT56-V30-3, CODEX-V30-1, CODEX-V30-2
V31→V32: GPT56-V31-1, CODEX-V31-1, plus PRINCIPAL-20260828-COUPLING (human direction: "the cut raised the coupling — flag it in §2.7"; measured figures, not a referee finding)
V32→V33: GPT56-V32-6, CODEX-V32-5 (§2.7 conditional-independence overreach). The five gain-control findings — GPT56-V32-1..5, CODEX-V32-1..4 — were repaired in gates/GAIN_GRADIENT_CONTROL_DESIGN_20260828.md, which is a sidecar and not part of this document's bytes.
V33→V34: BS2A-R6-CLEAR-20260828 (the quality-predicate component cleared its code gate at round 6 from both seats and is pinned by digest in the BS-2a row, with its recorded robustness limit; the slot remains DESIGN, UNFILLED and no class count moves)
V34→V35: GPT56-V34-2, CODEX-V34-3 (the antisymmetry identity does not forbid a biased w creating a signal), CODEX-V34-1 (an unlogged archive read need not break the chain), CODEX-V34-4 (the BS-2a pin overstated the pairwise probe evidence)
V35→V36: GPT56-V35-1 (the repaired BS-2a row duplicated the 26-probe class, reading as 52), CODEX-V35-1 (the crash clause omitted the exit-status boundary; a post-verification emit failure prints MATCH then exits 1)
V36→V37: PRINCIPAL-20260829-VOID-OPTION-A and PRINCIPAL-20260829-BS6-OPTION-A (human decisions relayed by Blanc 09:20 KST, on GPT56-V34-1 and the three §7.1 coverage gaps GPT56 named in the VOID gate round). Closes the degenerate and digest gaps as separate antecedents, extends the §2.7 ID to cover a threshold *chosen* as well as *moved*, and adds class-P slot BS-3g blocking BS-6 — the first row-count change since V4 (15/8 → 16/8). **The §2.7 phase reconciliation was explicitly NOT authorised and remains open.**
V37→V38: PRINCIPAL-20260829-2.7-REFUSED-AS-PUT (the principal declined the §2.7 phase question as not his to answer — the clause was authored by this lane at V11, commit 4d99d1d93; the instant was recovered from that commit's own §2.7 preamble and the cell left unchanged) and PRINCIPAL-20260829-AUTH-DEPRIORITISED (record CODEX-V34-2 accurately in §5; do not build the typed authorisation record; do not touch frozen v9).
V38→V39: GPT56-V38-1 and CODEX-V38-1 (agreed: the DESIGN-slot inventory omitted BS-3g), CODEX-V38-3 (DECISIONS_FOR_DUHO.md cited as the record of a ruling it does not record), CODEX-V38-4 (§6.1/§6.2 mis-cited for the χ-ordering evidence, and commit metadata does not prove lane authorship). CODEX-V38-2 is NOT repaired here — it changes what voids a run and is referred to the principal.
V39→V40: PRINCIPAL-20260829-VOID5-OPTION-C (ruling on CODEX-V38-2: a pre-unblinding numerical failure does not void the run; qualify §5's numerical trigger to post-unblinding and route pre-unblinding permutation/statistic failures to INCONCLUSIVE-BY-COMPUTATION with a binding rerun procedure. Misconduct conditions deliberately unmoved at Any).
V40→V41: GPT56-V40-4 and CODEX-V40-4 (BS-3g absent from §6.1's closed non-χ-bearing receipt list, so its blocks-BS-6 edge was not receiptable; added, plus a §11 code-side item because a receipt class with no producer is still not receiptable), GPT56-V40-6 (the §2.7 evidence citations were wrong for a second consecutive draft; absolute line numbers replaced with section-and-quoted-content references that survive renumbering). GPT56-V40-1/2/3 and CODEX-V40-1/2/3 are NOT repaired here — they concern the rerun procedure and are referred to the principal.
V41→V42: HWAO-HANDCHECK-20260829 (hand-verification of the 11 pre-format prose citations found one wrong: the Stage-P dual-valued passage cited KIMI-V11 F4, which is a §6.1 access finding. KIMI's Stage-P finding is F7 — 'the exact-null Stage P is not implemented in the file §0 pins' — which is the argument the passage actually makes. Corrected to KIMI-V11 F7. The compound form 'KIMI/GPT56-V11 F4' was also invisible to the citation checker, which only ever saw the last seat; prereg_lint now expands compound citations.)
V42→V43: PRINCIPAL-20260829-RERUN-OPTION-A (delete the rerun allowance; INCONCLUSIVE-BY-COMPUTATION is a terminal halt and the operator's recourse is a new run under a new preregistration — dissolves GPT56-V40-1/2/3 and CODEX-V40-1/2/3 together), GPT56-V40-5 (aggregate calibration failures overlapped both inconclusive codes; COMPUTATION is now explicitly subordinate to CALIBRATION).
V43→V44: GPT56-V43-1 and CODEX-V43-1 (a pre-unblinding Stage-C numerical failure mapped to both INCONCLUSIVE-BY-COMPUTATION and Row J's POWER route, and the new code had no named producer; COMPUTATION is now residual by construction with producers named and a §11 implementation item requiring evaluation AFTER the power and calibration guards), GPT56-V43-2 and CODEX-V43-2 (the BS-3g row still presented the three-way completeness fork the principal ruled on at 10:15; only the γ-to-sign-vector mapping remains open), GPT56-V43-3 (V43 wrote its own V42→V43 transition in-band with no result digest, which §10 lines 850 and 854 forbid; removed — the row belongs to the draft that follows it).
V44→V45: GPT56-V44-2 and CODEX-V44-2 (§11's BS-3g item still called the completeness semantics open; the principal settled them at 10:15 and only the γ-to-sign-vector mapping remains. The seats cited §7 line 719 AND §11 line 920 at V43 and I repaired only the first). GPT56-V44-1 and CODEX-V44-1 are NOT repaired here — the COMPUTATION/POWER reachability question has now failed twice and is referred to the principal.
V45→V46: PRINCIPAL-20260829-COMPUTATION-OPTION-D (delete INCONCLUSIVE-BY-COMPUTATION; the principal ruled that INCONCLUSIVE-BY-POWER is the named inconclusive code his option C ruling meant, so the existing route satisfies it and the second code was redundant. Resolves GPT56-V43-1, CODEX-V43-1, GPT56-V44-1 and CODEX-V44-1. The deletion is recorded in §5 rather than made silently, together with how a closed-row enumeration can establish the absence).
V46→V47: GPT56-V46-2 and CODEX-V46-2 (§5 falsely routed a non-finite instrument output through §2.7 reason (c), which is catalogue quality; §2.7 defers instrument non-finiteness to post-unblinding handling), and the completeness argument resting on it is retracted. GPT56-V46-1 and CODEX-V46-1 (Row F's degenerate-bin and infeasible-allocation FAIL branches have no named outcome, and Stage-C numerical exceptions have no named conversion in §11, so the claimed existing POWER route is not executable) are NOT repaired here — naming outcomes for those branches changes what the study claims and is referred to the principal.
V47→V48: GPT56-V46-1 partial (the Stage-C exception has no named conversion to an outcome). §11 now requires the exception-to-outcome conversion generally, having established that the gap is systemic rather than Stage-C-specific: 111 raise sites in the pinned reference, 3 of them typed to a named outcome. The item deliberately does NOT decide which outcome any branch receives — Row F's branches remain referred to the principal, and option A has failed on its own terms.
V48→V49: PRINCIPAL-20260829-NUMERICAL-OPTION-B-RERULED (option A withdrawn after it failed on its own terms; option B ruled as a rule for the class rather than a code for Row F). Adds INCONCLUSIVE-BY-NUMERICAL-FAILURE stated as a CONDITION covering sites not yet enumerated, at every phase rather than pre-unblinding only, with the caller-error boundary made checkable and the two rulings (D deleted a redundant claimant, B adds for genuine absence) reconciled in the row. Resolves GPT56-V46-1 and CODEX-V46-1.
V49→V50: GPT56-V49-2 and CODEX-V49-3 (the class rule and the VOID clause both claimed post-unblinding non-finite/degenerate failures; the VOID antecedents are now named in the precedence and the rule never fires where one applies), GPT56-V49-4 (raise inventory recounted by AST — 112 nodes including 39 ManifestClosureError the grep missed entirely), CODEX-V49-3 (the 31–79 range stated as an unsubtracted candidate partition rather than a count), GPT56-V49-3 (the false §2.7(c) route removed rather than left standing beside its retraction), CODEX-V49-2 (the §11 classification attaches to failure paths and call sites, not to raise statements). NOT repaired: GPT56-V49-1, -5, -6, -7 and CODEX-V49-1.
V50→V51: GPT56-V49-6 (§2.1's unqualified "nothing else changes with the branch" contradicted its own text four lines later; qualified to Branch B, with Branch A's pin-voiding recovered from the V11 commit rather than legislated) and GPT56-V49-7 (the predecessor decision memo was amended by append after signing — b202645cd signed b4a1f1fc, ac603e4c2 corrected the signing time to produce the current 76cc25e5 — so the pinned digest no longer matched the file; both digests and the reason are now recorded).
V51→V52: PRINCIPAL-20260829-ROWL-QUALIFY (Row L's wrong-signature VOID condition now exempts the freeze signature and the canonical opening authorization — the two the row itself mandates that the unqualified rule caught; the BS-L detached signature is over the canonical lock digest and was never caught. Closes CODEX-V24-1 / CODEX-V49-1, carried open 25 drafts. Recorded as making the path executable, not verified) and PRINCIPAL-20260829-THIRD-STATUS (UNREACHABLE-BY-CONSTRUCTION added, promoted only by named per-site evidence — execution count over a stated harness, a structural subsumption argument, or both — with a falsification clause making it a claim about the record rather than about routing). Closes GPT56-V49-5.
V52→V53: GPT56-V52-1 and CODEX-V52-1 (V52's UNREACHABLE example named no sites and the ledger marked all eight guards NUMERICAL, so the worked example violated the rule it illustrates; the five are now named per site, with L1411/L1435/L1437/L1439 promoted on measurement AND structure as post-allocation guards, and L1401 promoted on measurement ONLY because it is a branch of the feasibility decision itself rather than a guard subsumed by it — V52 wrongly implied the structural argument covered it). GPT56-V52-2 and CODEX-V52-2 (stale 29/31/48-unread and 111 inventories withdrawn, not merely superseded; the AST count of 112 with 39 ManifestClosureError is stated with the reason the grep figure arose).
V53→V54: GPT56-V53-1 and CODEX-V53-1 (L1401 is directly reachable — allocate_handcheck takes budget as an argument and the harness froze it at HC_REAL_LABELS=500; it fires at budget=200), CODEX-V53-2 (the remaining four lack per-site predecessor conditions). ALL FIVE PROMOTIONS WITHDRAWN; no site holds UNREACHABLE-BY-CONSTRUCTION and the evidence bar is restated so a third attempt is not a third failure. GPT56-V53-3 and CODEX-V53-3 (§11's 48-unread status withdrawn; §5 and the ledger both at NUMERICAL 22). NOT repaired: CODEX-V53-4 and GPT56-V53-2.
V54→V55: GPT56-V54-4 (the Stage-P dual-valued passage cited KIMI, which never supported it — KIMI-V11 F13 says the promise is 'now single-valued' and its F7 is a v7-subject disclosure finding; V42 had corrected an earlier KIMI miscitation by substituting F7, which is also wrong. Now cites two seats, GPT56-V11 F4 and CODEX-V11 4), CODEX-V54-2 (L1464 reclassified CALLER; numerical class 21), GPT56-V54-5 (the numerical-routes artifact's STATUS line still carried 48-unread and 31–79). NOT repaired: GPT56-V54-1, GPT56-V54-2/CODEX-V54-1, GPT56-V54-3, CODEX-V54-3.
V55→V56: PRINCIPAL-20260829-REFUSAL-VOCABULARY-OPTION-A (the eight-code closed refusal set, no catch-all, options B and C refused and recorded as refused; the principle that a reason may describe the request and authorisation state but never the object is written in as the test rather than the list; the derivation from §6.1's closed row table is stated and pinned by fingerprint, with tools/refusal_vocabulary_check.py failing when the table moves). Closes GPT56-V49-1.
V56→V57: PRINCIPAL-20260829-REDO-DERIVATION (the closure argument is withdrawn and option A is SUSPENDED — neither reaffirmed nor reversed; the derivation is to be redone from scratch across both the authorisation and availability axes with a tested exhaustiveness argument, not extended by a ninth code; REFUSED-IDENTITY-OUTSIDE-PERMITTED-SET does not survive as written). Resolves GPT56-V56-1/2/3 and CODEX-V56-1/3/4 by withdrawal rather than repair. GPT56-V56-4, CODEX-V56-6 (the hand-copied totals removed in favour of the generated ledger), GPT56-V56-5 and CODEX-V56-5 (inject_signs supplied-accuracy and mask-c admissibility reclassified CALLER). NOT repaired: CODEX-V56-2, BS-3g still has no pinned schema, producer or verifier.
V57→V58: PRINCIPAL-20260829-BS3G-SCHEMA (CODEX-V56-2 half one — BS-3g's SLOT_SCHEMA entry is specified as nine fields with types written for an independent implementer, plus producer, independent verifier and failure behaviour; the receipt is shown unable to carry a per-object field as a property of the field list rather than an undertaking, with the limit stated that this holds only for slots present in SLOT_SCHEMA and four others remain absent).
V58→V59: CODEX-V56-2 half two — receipt construction bound to a strict constructor that refuses slots absent from the pinned SLOT_SCHEMA, with every named producer required to use it and nothing else, plus a separately pinned verifier making a permissive-path receipt detectable rather than merely discouraged. v9 not modified. The four remaining SLOT_SCHEMA entries are deliberately not specified: each is written as part of filling its slot, and no receipt may be emitted for a slot lacking an entry, which turns the gap into a blocker rather than a hole. A producer that cannot be routed through the constructor is a STOP and returns the unfreeze question to the principal.
V59→V60: GPT56-V59-3 and CODEX-V59-3 (BS-3g bound the code the control ran and not the data it ran on, so a conforming receipt could certify a different sample or a favourable subset; mask_sha256, calibration_sha256 and perturbation_manifest_sha256 added, with the verifier refusing unless the mask digest equals BS-2f's pin and n_perturbations equals its manifest length).
V60→V61: PRINCIPAL-20260829-ROWL-PHASES (VOID-6.1L-WRONG-SIGNATURE gets Row L's own phases P0/P6/P7, with the exemption re-anchored to canonical body digests so widening cannot re-catch the exempt signatures), PRINCIPAL-20260829-FREEZE-SIG-BODY (CODEX-V53-4 — the canonical freeze-signature body, its digest and its verifier are specified, and the reusable lesson recorded: naming an object is not narrowing when the object has no canonical form), PRINCIPAL-20260829-BS2V-REASON (GPT56-V53-2 — the false self-reference reason is corrected while the slot stays UNRESOLVED and pinning is stated necessary-not-sufficient). Also: the unreachability harness bar now requires recording which arguments were varied, after the third frozen-parameter failure in one day.
V61→V62: CODEX-V59-2 and GPT56-V59-1 (the receipt_strict binding is scoped to slot-receipt producers; the V59 wording swept in seven rows emitting non-slot artefacts and fired V59's own STOP condition on its own overbreadth rather than on an unbindable producer, so v9 stays frozen; BS-7p is explicitly bound), GPT56-V59-5 and CODEX-V59-4 (the row fingerprint and the tool file's sha256 were conflated by a wrong label, not a wrong value), GPT56-V59-7 and CODEX-V59-5 (§5 now quotes the generated ledger rather than restating a count that has drifted three times).
V62→V63: PRINCIPAL-20260829-UNREACHABLE-DROP-BASIS-I (GPT56-V54-2 and CODEX-V54-1 — basis (i), promotion on an execution count alone, is DROPPED; UNREACHABLE-BY-CONSTRUCTION now requires a structural proof and measurement is corroboration only. CODEX's sentence is carried into the text verbatim, both seats' answer that the old bar could be met in full by a false claim is recorded, and §5 states explicitly that a permanently empty category is a real answer rather than a defect to be fixed by loosening the bar), PRINCIPAL-20260829-VOID-PARTITION (GPT56-V54-1 — the class rule and the VOID clause are made DISJOINT rather than ordered for a third time; §5 states the partition, on whether the failing quantity was pinned/sealed/verified before the failure or computed by the run, so any post-unblinding failure resolves without a precedence rule, and the falsification test is written in: if precedence is still needed the sets are not disjoint. §7.1 records that VOID-5-NONFINITE and VOID-5-DEGENERATE are narrowed accordingly), PRINCIPAL-20260829-PLANNING-NOT-AN-OUTCOME (GPT56-V54-3 — NUMERICAL-PLANNING is removed as an outcome class: nothing has started, so nothing can be voided or declared inconclusive. Its three sites L963/L973/L986 are MOVED to CALLER, not deleted, and the per-call-site ledger was checked rather than assumed — three reaching paths each, none through run_production_verdict, with the graph's lower-bound limit stated and the escalation named if any is ever shown run-reachable), PRINCIPAL-20260829-WITHDRAWAL-ON-RULE (CODEX-V54-3 — the five withdrawn promotions stand on the rule change and on the missing per-site predecessor condition, not on the unpinned ad hoc harness; the execution count is recorded as history and carries no weight, which also disposes of its 80,000/60,000 drift), PRINCIPAL-20260829-GAIN-MAPPING-OPTION-A-WORST-CASE (the γ → sign-vector mapping is ruled: option A with worst case over draws. BS-3g's schema grows by the four draw-set fields that make the count, generator and stopping rule checkable; mapping_id stays MAPPING-NOT-PREREGISTERED because a family is not a mapping until its draw set is frozen; option B is discarded as the gate mapping but NOT retired from the record until the margin is re-derived at the real calibration; and what a worst-case failure would mean is stated in advance), HWAO-REGISTER-CITATION-CHAIN (§8 gains the one defect in that register the lane committed rather than inherited: V42 repaired a wrong KIMI citation by substituting another wrong one, which survived twelve drafts — a correction that is itself unverified is not a correction, and that is the argument for citation_block_check.py).
V63→V64: PRINCIPAL-20260829-CATCHALL-NON-CLOSURE (the no-catch-all decision of 19:52 is FORMALLY REVERSED after both seats broke the second closure derivation in the VOCAB-R1 round; the vocabulary is eleven codes including REFUSED-UNCLASSIFIED, carrying a code and nothing else, under a guard making every emission a defect enumerated at freeze rather than a routine outcome, because a catch-all whose count is never reviewed becomes the vocabulary. The sequence is recorded as load-bearing: the ruling was made AFTER the argument was attacked, and one made against an argument that had only been written would have collapsed as the first one did. Resolves GPT56-V63-6, the suspended set still normative and still hard-enforced by the checker; tools/refusal_vocabulary_check.py is rewritten to the eleven codes, its fingerprint control INVERTED loudly - pinning a derivation fingerprint is now a failure, because there is no closure claim for it to protect - and its closure test SCOPED after an unscoped version fired on invariance_outcome's token set, with a negative control asserting it does not), CODEX-VOCAB-1 and GPT56-VOCAB-1 (the request lifecycle: permission is decided durably before any transfer work, writes get a PENDING-SURFACE-CHECK state because conformance cannot be judged before the payload is decoded, every state has one terminal treatment so no request ends undecided or unlogged, and the log boundary sits at the verdict. The catch-all makes an undecided permission loggable; the lifecycle makes it decided. Nothing is added to what the log records), REFUSED-SCHEMA-NONCONFORMING RESTORED (its deletion rested on receipt_strict() covering the fact, and V62 scoped receipt_strict() to slot receipts only - my own repair removed the basis for the deletion, and Rows C2 and H write non-slot field-constrained objects through Row B), GPT56-V63-2 (L986 is MOVE_CAP, an internal cap firing after a feasible prefix exists, so calling it a caller error violated this section's own supplied-argument boundary; dispositioned PLANNING-INTERNAL, explicitly NOT an outcome class and carrying no terminal consequence - this corrects a premise of the ruling rather than the ruling), GPT56-V63-4 (BS-3g's entry lives in the successor layer's schema because frozen v9 cannot gain one; the producer is bound to receipt_strict() and a BS-3g receipt through v9.receipt() is a protocol deviation - specification is not enforcement), GPT56-V63-5 and CODEX-V63-2 and CODEX-V63-3 (four draw-set gaps closed: n_draws and draw_master_seed are FROZEN IN THIS PREREGISTRATION so pre-commitment is witnessed by the freeze rather than asserted by the receipt it constrains; the draw x perturbation matrix gets canonical row-major addressing and a stated serialization; the categorical worst case is defined as HELD iff every cell equals baseline_verdict, avoiding an invented ordering over outcome names; and identifier, seed and count encodings are bounded so no field can carry an object-indexed payload), CODEX-V63-4 (require_complete_sample compares two caller-supplied integers and verifies no parent-to-receipt partition; recorded as a limit, not repaired, because v9 stays frozen). NOT repaired: GPT56-V63-1 and CODEX-V63-1, the partition - it failed the falsification test written into section 5 at the principal's instruction, three constructions have now failed on the same defect, and a fourth is not mine to attempt unannounced. GPT56-V63-3, the BS-3g lifecycle cycle, is also unrepaired and referred.
V64→V65: PRINCIPAL-20260829-CHI-BLIND-ACCESS-SCHEDULE (option (a) on the χ-adaptive access leak both seats found — GPT56 via Row G, CODEX via Row D. §2.7(3) constrains the exclusion predicates and does not reach the access schedule, so the χ-blindness this covenant relied on was inherited from something that does not cover it: the universe of eligible objects is χ-blind, the set and sequence actually requested are not. Every row that may read a χ-bearing object now has a precommitted χ-blind traversal — order, multiplicity, retries and stopping — and Row B refuses any request that is not the next unserved position. THE CLAUSE FORBIDS RATHER THAN OBSERVES: nothing built today is adaptive, but the covenant PERMITTED it and permission is what leaks, so a clause recording that nobody currently does it would leave the channel open to the first implementation that did. The traversal is derived from already-pinned objects and creates no new slot; re-viewing the CURRENT object is unrestricted, because dwell is not a request for a different object), PRINCIPAL-20260829-FLAG-RULE-NO-CONNECTION (flag → discard → replace stays unmodified, and is recorded as a FINDING rather than a permission: recognising a specific galaxy and judging its handedness are separate judgements, and nothing about handedness is visible in the image. The WHY is written into the text because an unexplained exemption invites a later editor to widen or delete it and both would be wrong for reasons nobody could reconstruct. The dependency is written so it can be checked rather than assumed — the exemption holds only while flagging is uncorrelated with handedness, and it is BOUND TO THE PINNED DIGEST of the Row G interface specification, so a different interface, visible orientation cues or a checker who also sees instrument output lapses it. Bound to a name it would widen with the name; bound to a digest it cannot widen silently. The last dependency of this kind was assumed and the seats found it unpaid, which is why this one is a condition to preserve rather than a fact that stays true by itself).
V65→V66: GPT56-V64-1 and CODEX-V64-1 (the lifecycle asserted a log boundary without making it atomic - reaching a verdict and appending it are two operations and a crash between them loses the event, which is the ordering-assertion defect committed inside the clause written to repair it. Now THE APPEND IS THE VERDICT: no verdict exists until its event is durably in the chain; Row B appends BEFORE releasing any byte, so a crash before the append leaves no event and no delivery and re-processing is safe; the cost is stated - the log can over-report a touch and can never under-report one; Row B is a single serialised writer with one decision per request, owns recovery, and a lease-losing worker may not append. NOT fixed and referred: a durable PRE-verdict state needs a second event class, and Row B's cell commits to exactly one event per touch, so adding one changes what the log records and is not authorised), GPT56-V64-2 and CODEX-V64-3 (the catch-all guard was unenforceable because the freeze is P0 and every refusal is P1 or later - a freeze-time review cannot police emissions that do not exist yet. The enumeration moves to the pre-unblinding lock checkpoint, which Row B's cell already produces, with a BLOCKING INVARIANT: BS-L may not be issued while any REFUSED-UNCLASSIFIED event is unenumerated. Deliberately NOT a VOID condition - blocking the lock stops the run repairably and matches what was ruled, where voiding is stronger than was ruled and would be his sentence. The checker now verifies the MECHANISM rather than phrase-matching the guard: R06 fires if the enumeration is anchored at freeze, R07 if it has no blocking invariant), GPT56-V64-3 and CODEX-V64-5 (V64 claimed the freeze witnesses n_draws and draw_master_seed while no frozen value was written anywhere, so the verifier had nothing to compare against. Both are class-P and CURRENTLY UNSET, and their absence blocks BS-3g emission the way the four empty SLOT_SCHEMA entries do; the closed sets are declared - mapping_id admits only MAPPING-NOT-PREREGISTERED, draw_generator_id's set is currently EMPTY and therefore also a blocker), CODEX-V64-4 (a singleton gamma=0 manifest satisfied every stated check while perturbing nothing and reporting HELD - the digest bound the manifest to the receipt and nothing bound the manifest to the question. The manifest must contain both endpoints and at least three distinct values, and the verifier refuses any whose largest |gamma| is below gamma_bound and refuses the singleton {0} by name), GPT56-V64-4 and CODEX-V64-6 (L986 was PLANNING-INTERNAL in the draft and still CALLER in the generated ledger; the generator is fixed and the ledger reads CALLER 25, PLANNING-INTERNAL 1), GPT56-V64-5 and CODEX-V64-7 (the checker digest was that file's value before the same revision rewrote it; recomputed - and it went stale a second time inside this repair when the checker gained R06 and R07, which is now recorded with the rule that the digest is computed last). NOT repaired and referred: CODEX-V64-2, that the four availability codes describe the logged object and create a per-object state leak - they are part of the vocabulary ruled at 22:18 and changing them is the principal's.
V66→V67: GPT56-V66-2 and CODEX-V66-1 (V66's append-before-release kept the log ahead of delivery but made the event's own outcome field a claim Row B could not yet have - one pre-transfer event cannot truthfully record a later COMPLETED or FAILED. A defect the previous repair introduced. Now RESOLVE, THEN APPEND, THEN RELEASE: Row B resolves the touch into storage it alone controls so the outcome is known, appends the single true event, and only then releases. The residue is unchanged and still stated - the log can over-report a touch and never under-report one - but the event is no longer false in the failure case. Still exactly one event per touch), CODEX-V66-2 ("never re-decided" was unenforceable without saying what a request is; Row B assigns an internal identifier held in its own recovery state and NOT written to the log, and a legal retry is a NEW request with its own event, because a retry is a second touch - what is forbidden is one request producing two events), GPT56-V66-3 and CODEX-V66-3 (V66 declared the enumeration obligation continuous and named nothing that checks it, and its own control matched the phrase BS-L rather than the mechanism - the phrase-matching defect reproduced inside the control written to remove it. A separately pinned ENUMERATION VERIFIER now recomputes the emissions from the access-log chain and is consulted TWICE, at BS-L issuance and again at the opening of the lock over the chain as it then stands, because an enumeration complete at BS-L does not discharge an event appended after it. Checker gains R08 for the verifier and its second consultation), CODEX-V66-4 (a class that fires every run could be explained every run and stay formally enumerated - the catch-all becoming the vocabulary by the route the guard exists to block, while passing it. Explanation now stops discharging a RECURRING class: the vocabulary must be re-derived to name it, which the set being open makes ordinary maintenance. Checker gains R09), GPT56-V66-4 and CODEX-V66-5 (a three-value endpoint-spanning manifest can step over an interior flip. NOT engineered away: no finite manifest can prove no interior flip exists, so the asymmetry is stated - a flip found anywhere is decisive, HELD means only that none was found on the evaluated grid. The grid's maximum spacing delta-gamma is a class-P parameter, CURRENTLY UNSET, and a further blocker on BS-3g emission), GPT56-V66-5 and CODEX-V66-6 (section 5 still said all three planning sites are CALLER, three revisions after L986 moved - the drift recorded in the sentence written to record it; the durable fix is to quote the generated ledger rather than restate it), PRINCIPAL-20260829-ROWF-WIDEN (Row F's read surface is widened to what its emission requires. The input is the per-object HC stratum index and BOTH its axes are chi-derived, so the expected escape does not exist. Widening puts the chi-bearing input inside the covenant now rather than at freeze, and makes Row F's void clause load-bearing instead of vacuous: the stratum index may reach the allocation and may never reach calibration_bins). NOT repaired and referred: GPT56-V66-1, the durable pre-verdict state, which needs a second event class; CODEX-V64-2, the availability codes describing the logged object; and whether this study's strata should BE chi-derived, with the cost established and surfaced rather than absorbed.
V67→V68: GPT56-V67-1 and CODEX-V67-1 and GPT56-V67-2 and CODEX-V67-2 (three orderings have failed and the third failure is structural: for a READ, resolving IS the touch, so V67's crash-before-append left a completed sealed-store read unlogged; for a WRITE, staging success cannot establish the later commit. One event, two fallible operations - no ordering of two fallible operations can make one always-true about the other, which is the partition's evidence-shape again: the axis was wrong, not the arrangements. The repair is the ATOMIC TOUCH CONTRACT, a named BS-2k design obligation: the stores and the access-log chain share one transactional commit domain, and a touch - store effect, its one event, and Row B's identifier-binding recovery update - commits entirely or not at all. Closes the under-logged read, the falsely-reported write, bytes-preceding-event, and the recovery duplication/suppression pair, since recovery consults committed bindings. Still exactly one event per touch, nothing new in the log; the pre-verdict residue stays referred), GPT56-V67-3 and CODEX-V67-3 (the enumeration verifier gets its object: an ENUMERATION ENTRY with chain_position, event_digest, computed class_key, one-token disposition, explanation_ref and signature; join by (chain_position, event_digest), both already in the chain; entries live in the lock-checkpoint materials under Row B's existing duty; wiring into section 5's BS-L guard at issuance and a fresh pass at opening; section 11 build item gates/enumeration_verifier.py. And the limit of the lint is stated in the draft and in the checker's own docstring: a lint verifies the text states the mechanism, never that it exists), GPT56-V67-4 and CODEX-V67-4 (the class key is COMPUTED, not assigned: (row, operation, lifecycle state at failure), all fields the event carries, so relabelling cannot split a class; the verifier recomputes the key and refuses a second EXPLAINED for the same key. Checker gains entry and key controls, 17 total), GPT56-V67-5 and CODEX-V67-6 (delta_gamma_max enters the schema - eighteen fields - and the verifier executes every manifest constraint the prose stated: both endpoints, three distinct values, adjacent gap at most the frozen delta, field equality with the frozen class-P value), CODEX-V67-5 (only invariance_outcome = HELD fills the slot and discharges the BS-6 edge; a verifier-valid FAILED receipt is a true record that BLOCKS and goes to the principal per the pre-stated reading; NOT-EVALUATED discharges nothing; verifier clause (f) evaluates discharge separately from validity), GPT56-V67-6 and CODEX-V67-7 (the widening did its job: no covenant row produces the stratum index - Row D emits only the primary instrument's receipts, nothing runs the committee architectures, and the exhaustive table sends an unnamed producer to Row R. Filed as OPEN_QUESTION_STRATUM_PRODUCER.md with options and costs rather than built, coupled to the strata decision; the allocation is a blocker meanwhile. And the bin/allocation separation gets a predicate: BS-2f's verifier recomputes boundaries from the full sealed accepted positions and refuses inequality - verification, not typing, because v9 is frozen; the seats had executed calibration_bins on stratum indices successfully), GPT56-V67-7 and CODEX-V67-8 (the two clauses still invoking freeze-time enumeration are corrected to the run-time gates, with the drift noted in one of them).
V68→V69: GPT56-V68-1/2 and CODEX-V68-1/2 (four findings, one defect: the commit domain cannot include delivery to an external requester, and a request could change terminal outcome after its sole event was durable. Two consecutive rounds failing on the same object, one short of the three-failures rule, so per Blanc's direction the GUARANTEE is stated before any construction: G1 no unlogged touch, G2 no false event, G3 one event per touch, G4 no double decision - and with equal weight what is NOT guaranteed: N1 delivery is outside the custody claim, N2 a request dying before any commit is invisible and referred. The construction derives: a touch commit is atomic {store effect, true-outcome event, binding}, a refusal commit is {event, binding}, delivery is a separate act from the committed buffer, re-sending is the same conveyance and never a second event, bytes cannot escape an aborted commit because the requester receives only from a committed buffer, and the TRANSFER state is DELETED - after the commit no failure alters anything the log claims. The corners now enumerate, which is what stating the guarantee buys. Terminal treatment distinguishes WHO died: worker fails while Row B survives = refusal commit REFUSED-UNCLASSIFIED; Row B dies pre-commit = N2 said plainly, no clause can make a dead writer log), GPT56-V68-3 and CODEX-V68-4 (class_key coarsened to (row, operation), the fields the closed schema actually carries; adding a field is not authorised, and coarser merges more so the recurrence rule fires sooner), GPT56-V68-4 (NAMED-AS-DEFECT must carry the digest of the re-derived vocabulary revision naming the class; EXPLAINED must resolve to a signed artifact; dangling references refused - a disposition that cannot be followed to its object is the citation-chain defect in a new coat), GPT56-V68-5 (recurrence scoped WITHIN-RUN as a stated limit; cross-run closure is an obligation on any successor preregistration to read this run's enumeration at its freeze review), CODEX-V68-3 (the ledger's own auto-typing violated the planning ruling: _plan L1331/L1341 raise InconclusiveByPower AT PLANNING; moved to PLANNING-INTERNAL - the typed exception is the operator-stop mechanism, the outcome class is produced only by the run-time guards; TYPED-OUTCOME 3 to 1, PLANNING-INTERNAL 1 to 3; the AST inventory's type counts stay true), CODEX-V68-5 (the three claimed build items now EXIST in section 11: enumeration_verifier.py with its refusal set, the atomic-touch commit domain as a BS-2k design requirement with fixtures, and the BS-2f boundary recomputation verifier - a claim without the item was the self-describing defect in the inventory built to prevent it), CODEX-V68-6 (the recomputation predicate stated at its true strength: it certifies the ARTIFACT, not the process - chi-influence that changed nothing is invisible to a product check and stays a conduct matter under the void clause; what it cannot do is move a boundary undetected), CODEX-V68-7 (gamma_bound gets an origin outside the receipt: the verifier recomputes it as |gamma_hat| + k_gamma * sigma_gamma with k_gamma a frozen class-P multiplier, CURRENTLY UNSET, a further blocker - a bound the author declares is require_authorization's shape), CODEX-V68-8 (the outcome is a total function of the evaluation: NOT-EVALUATED admissible only at zero evaluated cells; an evaluated matrix must carry HELD iff every cell equals baseline and FAILED otherwise - a decisive failure filed as a non-test is concealment wearing a valid schema).
V69→V70: THREE-FAILURE THRESHOLD INVOKED on the lifecycle (V67 F1/F2, V68 F1/F2, V69 F1/F2 - one object, three consecutive rounds, per Blanc's relay of 00:37 and this lane's own citation-check rule). No fourth patch: LIFECYCLE_GUARANTEE_SPEC.md is written as its own artifact - guarantees G1-G5 and non-guarantees N1-N3, the five-crash-window by four-reader invariant table, the derived state machine with no TRANSFER state, and the Row G analysis showing GPT56-V69 F2's asserted fork DISSOLVES: the covenant had already decided, Row G's any-unlogged-view clause always required logged views, so V69's delivery carve-out was correct for machine conveyance and over-broad in covering renders. G5: every render is its own touch with its own committed event. If the principal reads re-view practice differently, that is the normative fork and it is his - stated in the spec. GPT56-V69-1 and CODEX-V69-1 (the state declaration still routed through TRANSFER after its deletion - a deletion that did not delete; the state machine now has ONE home, the spec, and the draft quotes it), GPT56-V69-2 and CODEX-V69-2 (the V65 re-view sentence recut: unrestricted in SCHEDULE terms, logged in CUSTODY terms; renders get no buffer reuse; the conveyance buffer is destroyed on completion, its bounds a BS-2k design requirement - the ungoverned chi-bearing holding surface is governed), GPT56-V69-3 and CODEX-V69-4 (the entry schema gains the two disposition-conditional fields the prose demanded - rederivation_digest required exactly under NAMED-AS-DEFECT, explanation_ref exactly under EXPLAINED - and the signed explanation must itself name the (chain_position, event_digest) it explains, because an unbound explanation can discharge any emission it is pointed at; the stale lifecycle-state naming in the blocking invariant is corrected), GPT56-V69-4 (post-BS-L entries live in an authenticated continuation segment outside the sealed checkpoint materials, each independently authenticated by signature plus chain join), CODEX-V69-3 (the post-opening window: the continuous rule's instantiation is named - BS-7f, BS-V and disclosure each require a fresh enumeration pass, so a catch-all during opening blocks at BS-7f, not never), GPT56-V69-5 (the key is the TRIGGER granularity, not the naming granularity: the re-derivation names what it finds by reading the joined emissions, one class or several), CODEX-V69-5 (the manifest must contain gamma=0 and the verifier recomputes baseline_verdict by replaying that column - a producer-chosen baseline is require_authorization's shape at the reference point), GPT56-V69-6 and CODEX-V69-6 (gamma_bound's guarantee stated at the second recurrence: |gamma-hat| + k*sigma is a sampling-error bound under three named conditions - linear model, unbiased estimator, honest sigma - and NOT a bound under violation of any; HELD is conditional and says so; the shape choice between measurement-derived and a-priori bounds joins the BS-3g parameter sitting with the principal), CODEX-V69-7 (the ledger generator's header said all three local_pass sites are CALLER two revisions after L986 moved - the header/table drift one paragraph above the table; fixed and regenerated).
V70→V71: GPT56-V70-1 and CODEX-V70-1 (the round's first finding scored against the spec itself: refusal events have no store effect, so G3's one-touch-per-event contradicted them and G2 said nothing about their truth. G2 and G3 restated - a refusal event truthfully records that NO effect occurred, and every event is exactly one touch's or one refusal's, never both, never neither), CODEX-V70-2 (a VIEW is defined as the display session of one render commit: within it, dwell and magnification are the same logged view; displaying again after the session ends is a new view and a new commit. If the principal means per-glance, the spec says that is his reading to impose), GPT56-V70-2 and CODEX-V70-4 (post-BS-L EXPLAINED entries' signed explanations follow their entries into the authenticated continuation - the V70 text sent entries there and left explanations pointing at a sealed surface, the same defect one reference deeper), CODEX-V70-3 (the enumeration entries, continuation segment and explanation artifacts join the closed non-chi list as (iv-b) - the pre-unblinding verifier must read them and the closed-list default would have made them chi-bearing; explanations are bound by the same never-the-object principle as refusal reasons), GPT56-V70-3 and CODEX-V70-5 (the section-11 enumeration-verifier item now names all FIVE gates - BS-L, opening, BS-7f, BS-V, disclosure - and the checker's R08 requires the post-opening passes; the item said two after the text said five, the claim-without-the-item defect scored against the item built to fix it), CODEX-V70-6 (the class key's operation component is a token from the BS-2k event schema's closed operation set, or the key is splittable by phrasing), GPT56-V70-5 (the comparison is WITHIN-DRAW: HELD iff verdict(i,j) equals verdict(i,0) for every draw and perturbation, because one scalar baseline confounds stochastic draw variation with gradient sensitivity; baseline_verdict carries the token when the gamma-0 column is constant and PER-DRAW otherwise, informational either way), CODEX-V70-7 (the verdict-producing computation is bound, not just its output: counterfactual_path_sha256 enters the schema - nineteen fields - and every verdict cell must be produced under the PRODUCTION permutation contract, n_perm 100,000 and the frozen stage/trial/address parameters, verifier-replayed; the path had been defaulting to 2,000 with caller-supplied overrides, so a producer could change the matrix while every receipt field stayed fixed. A cheaper counterfactual resolution is a preregistered parameter for the principal's sitting, not a producer's default), CODEX-V70-8 (the verifier refuses any manifest point with |gamma| above gamma_bound - out-of-bound points could report a failure the allowed range never produced), GPT56-V70-4 (the checker's member-parse is decoration-independent with a retirement-line rule for non-member mentions, an identifier-prefix guard so finding IDs are not tokens, and a contradiction scan that refuses an affirmative may-describe-the-object line even when the principle also appears; the draft's enforcement claim softened to the lint's true strength - it checks that text STATES the set).
V71→V72: GPT56-V71-1 and CODEX-V71-1 (both F1s the same sentence - the draft did not inherit the spec's G2/G3 repair, the mirror of the one-round death warned about at V70. Per Blanc's directive the derivation became CHECKABLE before the content was repaired: tools/lifecycle_derivation_check.py fails when the draft's lifecycle-spec sha256 pin goes stale or any quoted G/N invariant body diverges from the spec's bytes, with a 4-control self-test; the draft's duplicated guarantee block is REPLACED by digest-pinned verbatim quotation, so the class of finding is impossible rather than unlikely. The control caught a real prose paraphrase of N2 on its first run), GPT56-V71-2 (the spec's G2 gains the reason-token truth condition: a request with no completed verdict may carry only REFUSED-UNCLASSIFIED, and any specific code asserts its condition was established - a false specific code was bypassing the catch-all enumeration), GPT56-V71-3 and CODEX-V71-2 (the view session is bounded by CONTINUOUS UNINTERRUPTED DISPLAY - ends at position advance, interface clear, or ANY interruption including visibility loss; duration alone does not multiply views, and nothing displayed after an interruption is the same view), GPT56-V71-4 and CODEX-V71-4 (Blanc's global STRING RULE: every string field in every non-chi artifact is closed-vocabulary or bounded-encoding, no third kind pre-unblinding - the seats found the refusal-reason leak one surface over in explanation prose. The explanation artifact is redesigned closed-vocabulary: a cause token from a five-member set plus bounded numeric parameters, no free prose; a failure inexpressible in the vocabulary cannot be EXPLAINED and must be NAMED-AS-DEFECT. The one field in violation was this lane's own three-revision-old construction, never ruled, so tightened rather than filed; any future field the rule constrains is filed before it is built), GPT56-V71-5 (what the verifier checks is consistency - cause membership, bounds, join, class_key coherence - and what it cannot check is whether the cause is what happened, which is TESTIMONY, signed and named as such), CODEX-V71-3 (signatures bind a named signer: detached over the canonical entry body under the BS-2k-provisioned enumerator keypair with roster identity - a signature with no trust root is a checkbox), GPT56-V71-6 (the verifier's clause (a) recomputes all FOUR module digests - counterfactual_path_sha256 was added at V71 while the clause still said three), CODEX-V71-5 (the two surviving scalar-baseline clauses become within-draw), CODEX-V71-6 (what THE SAME DRAW means across gamma is a preregistration semantics choice, not a generator detail: common random variates isolate the gradient, independent redraw contaminates the comparison with noise. The within-draw rule was built assuming the first; assuming is not preregistering. CLASS-P, UNSET, a blocker, and it joins n_draws in the principal's file because it decides what a draw IS), GPT56-V71-7 (delete, truncate and metadata mutation land in no crash-window cell because they are FORBIDDEN, not unmodelled: via Row B a refusal commit, outside Row B a bypass already claimed by VOID-5-FORBIDDEN-ACT - an unmodelled-but-possible operation would be a spec hole, a forbidden one is a wall). Also composed in one place: WHAT THE GAIN GATE IS FOR - the bound places the true gradient inside the swept interval, the manifest spans exactly that interval, the within-draw rule isolates the gradient, so HELD asserts the systematic that actually exists cannot have flipped this draw set's verdicts, up to grid resolution, conditional on the model.
V72→V73: BANKED-NOT-DISPATCHED (built after 01:46 under Blanc's prefer-banking instruction). GPT56-V72-1 (the derivation predicate is WIRED into prereg_lint as blocking - an unwired check is an unstated spec one level up; demonstrated failing on V72's own bytes before any repair: L02 stale pin, L04 G6 unquoted), GPT56-V72-2 and CODEX-V72-3 (the checker binds labels - a quote must match ITS OWN spec row - and detects deletion in both directions, L04/L05, with swapped-label and deleted-invariant controls; the session boundary becomes spec row G6 and the draft's unlabelled paraphrase - which named only two session-enders after the spec gained the interruption clause, CODEX's found divergence - becomes a labelled bound quote), CODEX-V72-2 and GPT56-V72-3 (the universal string rule is replaced by an ENUMERATED registry: ref/STRING_FIELD_REGISTRY.md generated from the schema blocks by ref/gen_string_field_registry.py, a field with no row forbidden by default, generator nonzero on unclassified fields; explanation_ref becomes a digest-ref; value-domain enforcement for slot receipts is a receipt_strict/verifier obligation with the section-11 item actually written; the honest limit stated - bounded numerics carry bits, the registry bounds capacity and removes free prose. And the first extractor version proved the point against itself: its [a-z_]+ token pattern silently dropped all seven sha256 fields - Row F's keyword omission committed by the tool built to prevent it - caught by its own stale-row report and recorded in the module), CODEX-V72-1 (the third scalar-baseline survivor in item (3) becomes within-draw), FIVE-ROUND FREEZE (the draw discipline is marked UNSTABLE-PENDING-RULING after V68 F7, V69 F6, V70 F5/F8, V71 F5/F6, V72 F1 - past the three-round threshold, and the reason is structural: the object's defining parameters await the principal. Repaired only where re-derivable; design findings recorded not patched; excluded from briefs' attack surface with the banner as stated reason), CODEX-V72-7 (NOT-EVALUATED deleted as unreachable-by-conformance - zero-cell receipts are non-conformant under the schema's own bounds, so an outcome no conforming receipt can carry is a promise the schema cannot keep; outcome set HELD/FAILED), GPT56-V72-4 (the entry-emission relation is a BIJECTION checked both directions - orphan entries joined to non-catch-all or nonexistent events refused), CODEX-V72-4 (existence is not naming: the re-derivation revision must CONTAIN the entry's class_key; adequacy of the naming is the re-deriver's testimony, named as such), GPT56-V72-5 (the replay must assert in-process that the imported successor_ref_v9 module hashes to the frozen 6a9abbbd - pinning the caller of a computation pins a name), CODEX-V72-6 (the retirement exemption is negation-guarded: REFUSED-X is-not-deleted no longer exempts; control added, 21 total), CODEX-V72-8 (the ledger paragraph said 'each is a setup error against a caller-supplied l_plan' three clauses after L986 stopped being one; generator fixed and regenerated).
V73→V74: GPT56-V73-2 and CODEX-V73-1 (the registry enumerated only the draft-declared schemas and omitted all 86 fields of frozen v9's operative SLOT_SCHEMA - the universal-sentence defect surviving into the enumeration built to replace it. The generator now extracts v9's SLOT_SCHEMA by AST, read-only; 123 fields, all classified, BS-1b.provenance - the free-prose field GPT56 named - now a digest-ref), CODEX-V73-4 (stale registry rows exit nonzero: a stale row is extractor/schema drift, the signal that caught the digit-blind bug, and zero exit made that omission nonblocking), GPT56-V73-3 and CODEX-V73-2 and CODEX-V73-5 (the derivation checker refuses TRUNCATED quotes - full-row-body equality, L06, since a true substring can reverse meaning by omission - and refuses a second pin, L07; the lint no longer silently skips a missing companion spec: absence is a blocking lifecycle-derivation-L00 finding, because unchecked is not passed; 9-control self-test), GPT56-V73-4 and CODEX-V73-6 (the retirement exemption is per-SENTENCE-FRAGMENT, so one token's retirement no longer exempts a second active token on the same line; em-dash deliberately not a splitter after it orphaned a legitimate retired mention from its own retirement two asides later; 22 controls), GPT56-V73-1 and CODEX-V73-7 (the delivery-retry allowance is scoped CONVEYANCE-ONLY where the allowance lives - the unscoped sentence re-authorised a no-new-event render contrary to G5; render buffers do not outlive their commit), CODEX-V73-3 (the replay's v9 assert runs in a FRESH INTERPRETER - a new process that hashes the pinned file, imports it, and computes, leaving no moment in which rebinding code can have run; an in-process assert left the file hash true while perm_record pointed elsewhere, the V6 monkeypatch seam wearing a digest).
V74→V75: GPT56-V74-1 and CODEX-V74-1 (the registry reaches 138 fields: v9's receipt ENVELOPE and environment fields extracted from the constructor itself, the nine declared non-slot artifact classes as digest-refs to their per-class schemas, and the enumerator SIGNATURE - constrained to a deterministic scheme mandated at BS-2k, because a valid ECDSA signature still carries a nonce, a covert channel wearing a validity proof), GPT56-V74-2 and CODEX-V74-4 (a merely fresh interpreter still runs site hooks, sitecustomize, PYTHONSTARTUP and path-injected import machinery before the pinned import; the replay is now python -I -S with a cleared environment and pinned cwd, and the trust boundary is stated rather than implied: the interpreter binary and OS are trusted BY DECLARATION exactly as for v9's own runtime - isolation removes every rebinding vector that rides configuration rather than ownership), GPT56-V74-3 and CODEX-V74-2 (G5 and G6 composed into two contradictions: a render buffer that died with its commit could not deliver after it - the buffer now lives until its VIEW SESSION ends, then is destroyed - and a compositor restore after occlusion would create G6's new view with no G5 commit - the sealed interface renders exclusively from Row B conveyances and holds no redisplayable surface beyond the live session, a BS-2k interface requirement whose fixture is occlude-and-restore producing a second committed render event or no image), CODEX-V74-3 (the V73 em-dash fix's mirror: with em-dash no longer splitting, one token's retirement could exempt a second non-member in the fragment; a retiring fragment may retire AT MOST ONE token, 23 controls), GPT56-V74-4 and CODEX-V74-5 (the refusal-checker digest went stale a THIRD time by the same mechanism - a later same-cycle edit postdating the hand-update; the compute-it-last rule is MECHANISED: prereg_lint recomputes the live file's sha256 and blocks on mismatch with the draft's claim, so the class dies as a check rather than surviving as a resolution).
V75→V76: GPT56-V75-1 and CODEX-V75-2 (the environment's six leaf fields enter the registry, extracted from environment_record() itself; three are pinned by require_environment - a genuinely closed vocabulary of one frozen value each - and three (python, platform, machine) are RECORDED-UNPINNED: interpreter-supplied text refused by nothing, a real residual surface named rather than dressed as closed; the container row becomes a digest-ref over the leaves), GPT56-V75-2 and CODEX-V75-1 (the nine non-slot rows stop being pseudo-fields: three classes are inventoried field-by-field in the registry itself - the event rows, the enumeration surface, the projection bits - and six are honest SCHEMA-PENDING stubs whose fields are unenumerable until their defining slots fill; a stub that says so, not a constraint it does not have), GPT56-V75-3 and CODEX-V75-4 (the replay interpreter is pinned by ABSOLUTE PATH recorded in BS-7p's environment - a bare python is PATH-rebindable before isolation begins - and carries an explicit sys.path to the pinned dependency roots, because -S skips site and NumPy does not import from nowhere; the roots join the frozen environment require_environment checks), CODEX-V75-5 (G6 gains one-to-one commit-session ownership: each render commit opens at most one session and every session is opened by exactly one commit - one commit serving two simultaneous sessions was the bijection defect at the interface), CODEX-V75-3 (the registry records its generating draft and prereg_lint blocks when that is not the draft under lint; regenerated), GPT56-V75-4 (the retirement exemption applies ONLY to tokens in the KNOWN retired set - a novel token cannot buy legality by standing next to the word deleted, because retirement is a fact about this vocabulary's history, not a property of nearby prose; 24 controls), CODEX-V75-6 (ledger: require_complete_sample's raise at 1649 moved INTEGRITY to CALLER - it compares two caller-supplied integers, which section 5's own recorded limit had said for four revisions while the ledger contradicted it).
V76→V77: GPT56-V76-1 and CODEX-V76-2 (bounded-encoding with no declared bound was a contradiction inside one registry row; the three value-unpinned environment leaves now carry a REAL bound - printable ASCII, 64 bytes, refused by the successor-layer envelope verifier - unpinned in VALUE, bounded in ENCODING, both halves now true), GPT56-V76-3 and CODEX-V76-1 (the draft's hand counts said nine classes and six pending while the generator says ten and seven - the hand-copied-count drift on the surface built while writing about that drift; the counts are now the generator's, quoted from ref/_registry_counts.txt, and the declared canonical bodies the extraction missed - freeze-signature body, lock body, opening authorization, entry body, explanation body, provenance record - are registry rows), GPT56-V76-2 (the bootstrap pins are receiptable: BS-7p's environment field carries a canonical sub-schema - interpreter_path, interpreter_sha256, dependency_roots as ordered (path, digest) pairs - and a separately pinned verifier recomputes from disk and refuses mismatch), GPT56-V76-4 and CODEX-V76-3 (a known retired token could be REACTIVATED in the same fragment as its historical retirement word and ride the exemption; an activation word anywhere in the fragment now defeats it, 25 controls).
V77→V78: GPT56-V77-1 and CODEX-V77-2 (BS-7p's sub-schema fields join the registry - interpreter_path bounded to an absolute POSIX path of 256 printable-ASCII bytes with no traversal segments, the digests as digest-refs - and BS-7p.environment's false closed-vocab label becomes digest-ref one revision after the sub-schema was defined), GPT56-V77-2 and CODEX-V77-3 (the dynamic_load_manifest joins BS-7p: the linker-resolved transitive closure of shared objects as ordered path-digest pairs recorded at freeze, verifier-recomputed - listed-root hashing left native dependencies able to alter verdict computation while every listed digest matched; what remains above the manifest is the linker itself and the kernel, which are the trust declaration, and each closure level names the next as trusted with this one stating where the naming stops), GPT56-V77-3 and CODEX-V77-1 (canonical.provenance_record was force-added as digest-ref with no written encoding - the SCHEMA-PENDING defect wearing a canonical name; it is now an honest pending row and BS-1b.provenance's note says the field is unfillable until the encoding is written; the canonical explanation body's field order IS now written, so its digest-ref has a preimage), GPT56-V77-4 (timestamp bound: ISO-8601 UTC, exactly 24 bytes), GPT56-V77-5 (what can check reason-token truth is stated: codes with chain-recomputable conditions are audit-checkable post hoc, availability codes assert unreplayable store states and are testimony under Row B's signature - a false code is detectable in one class and attributable in the other, silent in neither), GPT56-V77-6 (a render commit that opens no session destroys its buffer at request end - the trigger every commit already has), GPT56-V77-7 (a re-derivation whose digest equals the prior revision's changed nothing and named nothing - vacuous discharge refused), GPT56-V77-8 (the cause set is an ordered precedence, not a menu: when several truthfully apply the highest is mandatory, removing the five-way selection channel), GPT56-V77-9 and CODEX-V77-4 (the activation guard scans a 160-char window after the token - now-mandatory rode a pronoun across the fragment boundary while whole-line scanning false-fired on a legitimate mention three sentences later; member tombstones are divergences; the finite word list's limit is stated in the checker, 27 controls), GPT56-V77-10 (the perturbation manifest's byte serialization is written: ascending decimal ASCII, shortest round-trip, newline-separated, UTF-8), GPT56-V77-11 and CODEX-V77-5 (the last hand counts - nine, 145 - are stripped; ref/_registry_counts.txt is the only source).
V78→V79: GPT56-V78-1 and CODEX-V78-1 (the manifest is enforced at replay END, not only recorded at freeze: after the verdict computes, the process enumerates its actually-loaded objects and REFUSES its own result if any is outside the manifest; the frozen reference and path contain no dynamic-load constructs, auditable once by AST, so a late load is a violation never a need), CODEX-V78-2 (the container ENTRY fields join the registry - roots_entry/dlm_entry path bounded to the same 256-byte POSIX form, digests as digest-refs - unbounded leaves had been hiding behind bounded wrappers), GPT56-V78-2 (explanation parameters get a per-cause CLOSED schema - five parameter names, arity exactly as declared per cause; names and arity had been free strings, the string rule's own surface one level down), CODEX-V78-3 (the applicability channel is a NAMED RESIDUAL, not claimed away: which causes apply can correlate with what the process touched, at most log2-5 bits per emission, bounded by the existing enumerated-defect review - the price of the token having diagnostic content at all, like N3), GPT56-V78-4 and CODEX-V78-5 (the checkability split refined per code with its CONSUMER named: authorisation codes and SCHEMA-NONCONFORMING recomputable; OBJECT-ABSENT falsifiable-from-history since a prior committed touch contradicts it; UNREADABLE/INCOMPLETE/INTEGRITY-MISMATCH testimony; the section-11 audit pass recomputes every recomputable and history-falsifiable condition for EVERY specific refusal at the same five gates), GPT56-V78-3 and CODEX-V78-6 (ONE canonical byte encoding for every canonical body, defined by frozen v9's field() constructor - 4-byte LE name length, UTF-8 name, 8-byte LE payload length, payload - in the body's declared order; the opening authorization's order is declared, closing its digest-without-preimage), CODEX-V78-4 (the naming is a checkable TEMPLATE: NAMES-CLASS: key AS token with membership checked - a changed revision containing only the bare key still discharged vacuously), GPT56-V78-5 (future-tense reactivation forms join the activation list, 28 controls, limit restated).
V79→V80: GPT56-V79-1 and CODEX-V79-1 (verbatim-convergent, and RIGHT: V79's no-importlib claim about the frozen bytes was FALSE - v9's _frozen_planner() loads the frozen cutout planner via importlib.util at L269-277. The corrected statement is the checked one: exactly one dynamic-load site, loading a module that pins its own adapter digest, UNREACHABLE from the verdict path - the call graph from run_production_verdict reaches neither _frozen_planner nor frozen_plan_object, AST-verified at V80, and the counterfactual path imports neither importlib nor ctypes. The load-then-unload gap closes by composition: under -I -S with a cleared environment the only code running before the end-snapshot is the pinned code the AST audit covers - an unload needs an unloader and there is nowhere for one to live), GPT56-V79-2 and CODEX-V79-2 (V79 invented a three-field opening-authorization body for an object Clause 6 had already bound - one object, one body: the V79 declaration is WITHDRAWN and the canonical order is Clause 6's eight-field enumeration), GPT56-V79-3 and CODEX-V79-3 (a prior touch proves PAST presence, not present absence's falsity; but presence is monotone in append-only stores with mutation forbidden, so history plus a later ABSENT proves the DISJUNCTION - either the code is false or a forbidden removal occurred, both audit-findings, the audit surfacing the contradiction rather than adjudicating it), GPT56-V79-4 and CODEX-V79-4 (the canonical-body string leaves join the registry - Clause 6's eight opening-authorization fields and the freeze body's five components; 177 fields), CODEX-V79-5 (the NAMES-CLASS token's own definition in the revision must contain the class_key - a template naming an unrelated existing member proved nothing), CODEX-V79-6 (the payload encodings under field() are declared: raw bytes, UTF-8 text, decimal-ASCII integers, lowercase-hex digests, canonical JSON for structured values - the convention v9's own envelope already uses), CODEX-V79-7 (remains-operative and its cousins join the activation guard, 29 controls, the finite-list limit still stated).
V80→V81: GPT56-V80-1 and CODEX-V80-3 (the V80 withdrawal substituted timestamp for Clause 6's schema/version - a SECOND wrong correction on the same object, section 8 item 9's citation-chain shape exactly; this one is verified against Clause 6's bytes and the registry follows), GPT56-V80-2 (SCHEMA-NONCONFORMING moved to TESTIMONY: the chain is payload-free by design, so nobody can recompute what the refused payload contained - V80's table had it on the wrong side), GPT56-V80-3 (a committed render event asserts CONVEYANCE to the interface, never that a human saw a frame: a crash before first frame leaves a true event about a conveyance whose session never opened, handled by the no-session buffer path), GPT56-V80-4 and CODEX-V80-1 (the composition proof fell to the two caller-supplied surfaces - an unpinned mapping callback and SealedMask subclass dispatch through isinstance; THE REPLAY NOW CONSTRUCTS ITS OWN INPUTS: type-exact mask built from pinned artifacts, no mapping callback until a preregistered mapping enters the manifest as a pinned module rather than an argument - caller-supplied executables were the entire remaining code surface and now there are none), GPT56-V80-5 and CODEX-V80-2 (-O survives -I -S and strips v9's load-bearing calibration-stability assert at L1622: the launch contract forbids -O/-OO, the cleared environment kills PYTHONOPTIMIZE, the harness verifies sys.flags.optimize == 0 in-process, and the assert-as-guard is recorded as a v9 limit since a frozen file cannot trade its assert for a raise), CODEX-V80-4 (the monotone-presence audit joins on STORE identity plus brickid/objid - object keys alone can pair different objects across stores), CODEX-V80-5 (every request carries a DEADLINE, a BS-2k design constant, after which a live pending request IS a failure and receives the refusal commit - indefinitely-stuck was a state with no terminal treatment, the V64 defect returning through patience), CODEX-V80-6 (the token regex gains its right boundary so a suffixed non-member cannot pass as prose, and the activation list's finiteness stays a stated limit; 30 controls).
V81→V82: GPT56-V81-1 and CODEX-V81-2 and CODEX-V81-3 (three attacks, one mechanism: the harness reads v9 and the path ONCE into memory, hashes THOSE buffers, and compile(buffer, optimize=0)-executes them into fresh namespaces - no import machinery, no bytecode cache so -X pycache_prefix substitution dies, no second read so the verified/consumed swap window dies, and the optimize level is an argument we set so a stripped-assert code object dies; the dependency roots still travel imports so their digests are re-verified after computation), CODEX-V81-1 (replay_harness_sha256 enters the schema - twenty fields - because the harness carrying every replay obligation was the one executable left outside the receipt), GPT56-V81-2 and CODEX-V81-4 (the deadline moves into the spec's 3b as lifecycle semantics - monotonic clock, fixed at receipt, never reset - the single-home rule had been violated by the repair that extended the lifecycle), GPT56-V81-3 (the spec's render definition tightened: the render's store effect is conveyance-to-interface as a DISTINCT kind with a store-qualified operation token, one event of one kind; perception is never an event), GPT56-V81-4 and CODEX-V81-5 (the BS-2k operation vocabulary is STORE-QUALIFIED so (row, operation) determines the store - Row I touches multiple stores and the presence-audit join was non-derivable without it), GPT56-V81-5 (the detached signature fields join the registry - sig.freeze/bsl_lock/opening/explanation/checkpoint, 64-byte deterministic), GPT56-V81-7 and CODEX-V81-7 (canonical JSON made unique: bytewise-sorted keys, NFC strings, minimal escapes, integers only with floats forbidden in structured payloads), GPT56-V81-6 (required/authoritative join the activation list, 31 controls, the finite-list limit standing), GPT56-V81-8 (the ten-and-seven sentence marked HISTORICAL - it read as current one revision after pending moved to eight), CODEX-V81-8 (the registry header states its two provenances plainly: extracted sets and declared sets, instead of claiming generated-from-schemas globally). FILED FOR THE PRINCIPAL: CODEX-V81-6, the collision between the catch-all naming obligation and the section-6.3 void rule - OPEN_QUESTION_VOCAB_REDERIVATION_VS_VOID.md, DECISIONS item 6.
V82→V83: GPT56-V82-1 and CODEX-V82-1 (the path buffer's own first line - import successor_ref_v9 - re-entered the import machinery the mechanism claimed to remove: the harness now executes the verified v9 buffer FIRST, registers it as sys.modules['successor_ref_v9'], and the path's import binds to the already-verified in-memory module, touching no disk), CODEX-V82-2 (replay_harness_sha256 gets an INDEPENDENT expected pin frozen in the preregistration, not the receipt's own claim; clause (a) recomputes all FIVE module digests - the fifth joined at V82 while the clause said four, the one-revision lag a third time; the who-checks-the-checker regress stops at the freeze, where every pin already stops), CODEX-V82-3 (temporal partition of the enumeration surfaces: a continuation entry may join only post-BS-L events, every pre-BS-L emission must be in the SEALED checkpoint materials, and a continuation entry claiming a pre-BS-L chain_position is refused - a post-hoc entry could otherwise retroactively heal a lock-time enumeration failure), CODEX-V82-4 (lowercase escape hex; post-NFC key collisions refused as invalid rather than silently merged), CODEX-V82-5 (lockbody.bound_digests enters the registry pointing at clause 3(a)'s enumeration - one enumeration, not two, the provenance-record lesson - and openauth's closed sets are named as BS-2k design artifacts), GPT56-V82-2 (the five explanation parameters get real ranges - the V76 environment-leaf defect on the parameter surface), GPT56-V82-3 (a class explained in ANY prior run counts as RECURRING at the successor's freeze review - once-per-run pacing could otherwise explain a class forever), CODEX-V82-6 (R08's polarity blindness repaired with negated-FORM patterns after two proximity attempts false-fired on neighbouring clauses' nots; 32 controls), CODEX-V82-7 (the COMMIT itself is bounded in the spec: the transactional domain aborts any commit that neither completes nor aborts within its BS-2k bound, and the aborted request receives the deadline refusal - a stalled commit resolves to abort-then-refusal, never a wait).
V83→V84: GPT56-V83-1 and CODEX-V83-1 (the harness pin was a name with no artifact: gates/replay_harness.py is now a REQUIRED build item in section 11 carrying every replay obligation, its sha256 the class-P expected value set when built and frozen at freeze - UNSET until then, one more named blocker on BS-3g, exactly like n_draws; a pin without an artifact was a name, a named artifact with an unset pin is a blocker), GPT56-V83-2 and CODEX-V83-2 (the window inside the platform loader's reads joins the trust declaration by name: a swap-import-restore race around the loader's own read defeats before-and-after hashing, and no userland check closes a race against the loader it runs on - an adversary who can race the loader owns the machine, which the declaration already concedes for linker and kernel), GPT56-V83-3 and CODEX-V83-3 (the temporal partition has no seam: Row B is a single serialised writer and checkpoint-seal-through-issuance is ONE serialised step, so no event can be appended during it by construction - the boundary is a point, not a window), GPT56-V83-4 and CODEX-V83-4 (the recurrence domain is the LISTED predecessors' surviving enumerations - each preregistration lists its known predecessors, this one lists V3-pred, and a run that died before recording its failures contributes nothing, a named limit because such a run cannot teach), GPT56-V83-5 and CODEX-V83-5 (the lifecycle is TOTAL over the states the platform serves, the honest quantifier: where storage itself never returns no domain conjures liveness, and that terminal case is the platform failing - N2's sibling, ended by operator observation), GPT56-V83-6 and CODEX-V83-6 (the lock-body pointer said clause 3(a), which contains no enumeration - the unverified-pointer defect inside the row written as that lesson; the body lives in 3(b), READ this time, its twelve leaves plus signer_identity enumerated in the registry AND cross-checked against the clause text at generation - and the cross-check caught the hand list missing signer_identity before it shipped), GPT56-V83-7 and CODEX-V83-7 (the three richest declared sets - openauth, lockbody, and their clause sources - are now CHECKED declarations: the generator extracts the clause text's own phrases and fails on a missing or stranger member; the remaining declared sets stay declaration with the two-provenance header saying so).
V84→V85: THE SEVEN-RULING SITTING OF 2026-08-30 10:46, applied. PRINCIPAL-20260830-PARTITION-OPTION-C (the overlap is REAL and both rulings stand recorded: a computed-then-sealed artefact honestly carries two failure meanings, and the open ORDERING rule decides by what the failure demonstrates - impeached record means VOID wins, unresolved arithmetic means the inconclusive side wins, both-true means VOID - superseding make-them-disjoint after three constructions died on genuinely dual-natured objects), PRINCIPAL-20260830-BS3G-SITTING (n_draws 99; the bound is an A-PRIORI FROZEN RANGE with k-gamma moot, endpoints plus-minus 0.25 PROPOSED from the calibration floor and awaiting ratification; COMMON RANDOM variates; seed/generator/delta-gamma committed blind in ref/DRAW_MECHANICS_COMMIT_20260830.md; the draw discipline UNFROZEN with the five-round freeze banner kept as history), PRINCIPAL-20260830-INTEGRITY-REFUSAL-OWNS (log and continue; every mismatch enumerated at freeze; an unexplained mismatch BLOCKS the freeze; the digest-deviation VOID antecedent scoped by the ordering rule - a read-time mismatch demonstrates served-differs-from-certified and does not impeach the certification, while VOID-5-DIGEST-DEVIATION claims the mismatch that does), PRINCIPAL-20260830-PRINCIPLE-REBUILT (storage state allowed, content-derived forbidden, safe only under the precommitted chi-blind schedule; the stale never-the-object sentence gone; checker R03 rebuilt to match with its contradiction control updated), PRINCIPAL-20260830-ARRIVAL-RECEIPT (the write-ahead second event class, explicitly authorised: arrival durably logged before any processing, no real request can vanish, N2 retired after eleven revisions with the spec table keeping the record; Row B's cell amended), PRINCIPAL-20260830-COLLISION-OPTION-A (a post-chi recurring catch-all class TERMINATES the run as TERMINATED-UNNAMEABLE-REFUSAL-CLASS - a third thing, neither VOID nor INCONCLUSIVE, both laws standing; in the section-5 registry), PRINCIPAL-20260830-STRATA-OPTION-A (chi-derived accepted; Row D2 stratum-index producer MAY READ chi, writing the sealed pinned independently verified artifact; slot BS-SI class P UNFILLED; Row F's void clause amended to its real inputs; CLASS COUNTS MOVED 16/8 TO 17/8, reported as required; the typed/capability barrier keeps the artifact out of calibration_bins with the positions-only recomputation refusing contamination). Also applied: the gamma_bound field text under the a-priori shape. The V84 round's findings were returned against pre-ruling text and fold into the next cycle.
V85→V86: the V84 round's twelve findings triaged against the ruled text - two were photographs of the mid-application state (CODEX-V84 F1 the half-migrated spec, CODEX-V84 F2's digest staleness), resolved by V85's committed form; the live ten repaired. GPT56-V84-1 (issuance is a COMMIT in the transactional domain, not merely serialised - a half-issued lock is not a state that can exist, recovery finds the committed BS-L or nothing), GPT56-V84-2 and CODEX-V84-6 (the predecessor listing is rule-bound: any successor over the same PINNED_PARENT_SHA256 must list every prior preregistration sharing it, checkable by its own gate rounds by search over the lane; what no document can do is enforce its successor, so the teeth live in the successor's briefs and the sentence is the instruction those briefs inherit), GPT56-V84-3 (the freeze-body crosscheck exists and clause-6 STRANGER detection added - every comma-segment must map to a known member, so an added normative field is visible; CODEX-V84-4 same repair), GPT56-V84-4 and CODEX-V84-5 (baseline_verdict carries PRODUCTION verdict tokens - the matrix's cells are run verdicts, HELD/FAILED is the comparison's outcome not a cell's; the registry row and the section-11 text both corrected), CODEX-V84-3 (the generator takes the draft path as an argument, newest only the default - older drafts were unregeneratable once a successor existed), CODEX-V84-7 (calibration_sha256's byte encoding stated: float64-LE row-major arrays in field() records), the four pipe-broken registry rows sanitized (GPT56-V84 F4's malformation half), GPT56-V84-5 and the it-controls phrasing joining the activation guard (34 controls). The spec's stale N2-sibling phrasing updated to name N2 as retired.
V86→V87: the ruling-application gaps the V86 round was asked to find, found. GPT56-V86-1 and CODEX-V86-1 (Row D2's void branches gain their canonical antecedents - VOID-6.1D2-OUTSIDE-STORE, -LATE-WRITE, -REACHES-BINS - and void_registry.py, which REFUSED V86 on V05, is green and back in the battery), CODEX-V86-2 (BS-SI's class-P label violated section 0's own definition - a class-P slot holds its receipt BEFORE the freeze and a chi-derived artifact cannot exist until P2; the row moves to the Class-E table and the generated row records the move: class-P rows 17 -> 16, class-E rows 8 -> 9 - the 17/8 state was my erroneous intermediate classification, and the error is mine, not the ruling's), GPT56-V86-2 and CODEX-V86-3 (the N2 retirement propagated: the spec's W1 row now shows the arrival event, the section-6 leftover and the terminal-treatment sentence updated - the quote checker stayed green through this because the stale text was unlabelled prose, its stated blind spot), GPT56-V86-3 and CODEX-V86-4 (the arrival event's schema is authenticated and closed with an ON-CHAIN request_key joining each arrival to exactly one terminal event, the binding-to-key map committed in the transactional domain and exposed at checkpoint), GPT56-V86-4 and CODEX-V86-6 (the stale never-describe-the-object text at the iv-b site and the checker docstring rebuilt to the ruled storage-state principle), GPT56-V86-5 and CODEX-V86-8 (the draw block's self-contradictions resolved: UNSET/EMPTY claims updated to ruled or committed values with pointers, and the superseded measurement-derived bound passages marked HISTORY with the shape choice recorded as decided), GPT56-V86-6 (TERMINATED-UNNAMEABLE-REFUSAL-CLASS has its producer: the enumeration verifier at whichever gate detects the recurrence - the refusal IS the production, emitting the terminated-verdict record), CODEX-V86-5 (the issuance commit's own mediated writes are the last pre-partition events, sealed-side by construction), CODEX-V86-7 (the vocabulary block's flagged-and-unresolved label updated to RESOLVED BY RULING), CODEX-V86-9 (the manifest is the GRID BY CONSTRUCTION - recomputed from the ratified endpoints and committed delta, nothing left for a producer to choose), CODEX-V86-10 (the committed seed rule's one-based indexing addressed spawn(99)[99], out of range; AMENDED in the commit artifact per its own post-hoc rule, the addressing rule now zero-based with values unchanged).
V87→V88: THE SUPERSESSION SWEEP (Blanc's directive, 2026-08-30 11:38 KST) - the V87 round's six paired findings plus GPT56-V87 F7 treated as ONE event, a morning ruling landing without its predecessor text dying, and repaired AS A SET rather than as point fixes. For each ruling of 2026-08-29/30 the old regime was grep-enumerated across draft, spec, registry generator and CONTROLS, then killed or re-derived with the ruling cited on each site. GPT56-V87 F1 + CODEX-V87 F1 (G4: the spec row and the draft's label-bound quote restated over the arrival class - one request never yields two DECISION events, arrival its own class per the 2026-08-30 10:46 sitting; the draft quote DERIVED from the swept row, not retyped). GPT56-V87 F2 + CODEX-V87 F2 (the old never-describe-the-object principle tail at the PRINCIPLE-STILL-BINDS site replaced with the rebuilt storage-state/content-derived form). GPT56-V87 F3 + CODEX-V87 F3 (the two live-N2 corner sentences replaced with arrival-event corners; the spec N2 row is a retirement record). GPT56-V87 F4 + CODEX-V87 F4 (registry generator notes: n_draws RULED = 99, generator committed blind, seed COMMITTED = 20260830, gamma bound a-priori with k-gamma moot; ARRIVAL field set added with request_key = the arrival event's own chain position). GPT56-V87 F5 + CODEX-V87 F5 (stale pre-correction count prose: grep found zero live sites - the V87 correction had already swept it; recorded as verified-clean, not repaired). GPT56-V87 F6 + CODEX-V87 F6 (arrival fields present in the draft schema block: verified-clean by grep). GPT56-V87 F7 (the freeze-time mismatch guard re-anchored to the five-gate machinery and the successor-facing freeze review, citing the V64-era finding that a freeze-time enumeration cannot police run-time emissions). AND THE SWEEP'S OWN CATCH, the eighth stale site: tools/refusal_vocabulary_check.py R03 still REQUIRED the pre-ruling sentence and had stayed green off the dead draft tail - the control and the dead text were masking each other; R03 rebuilt to three prongs (storage-state permission, content-derived ban, chi-blind-schedule dependency), fixture rebuilt, 34 controls green. The G4 spec/draft pair had silently failed to land in the first sweep pass and was caught by the re-grep receipts - the re-grep is part of the sweep, not decoration.
V88→V89: ARRIVAL AS ONE INTEGRATION, THE SWEEP AS A RECEIPT, TWO DESIGN ITEMS (Blanc's three-thread relay, 2026-08-30 12:06 KST; CODEX at four findings, the convergence signal). THREAD 1, one pass over every exhaustive partition rather than the two the seats named - GPT56-V88 F1 (spec G3 re-derived as a THREE-WAY partition: every committed event is exactly one of ARRIVAL, a touch's, a refusal's; G2 extended so an arrival event is under the truth invariant too, recording receipt of one real request; both draft quotes re-derived label-bound from the swept rows), GPT56-V88 F2 + CODEX-V88 F1 (the ARRIVAL schema admitted to the exhaustive non-chi list AT the list as item (ii-b), value domains bound to BS-2k; the registry generator grew a source parameter and the six arrival declarations now cite item (ii-b) - they shipped sourced to v9 SLOT_SCHEMA, which never contained an arrival; access_log_chain's inventory note names both event classes), GPT56-V88 F5 (the section-11 enumeration-verifier item now carries the full arrival contract: both join halves, deadline states, six named fixtures - the claim-without-the-item defect, scored a third time). THREAD 2 - GPT56-V88 F3 + CODEX-V88 F2 (the request/recovery block REPLACED, not tail-patched: the death-before-commit corner is visible-not-vacuous, the write-dying corner is PENDING, the two-identities rule replaces not-written-to-the-log, the residue re-drawn at the pre-arrival wire, the what-changed ledger names both authorised log changes; spec N2's why-cell no longer refuses what its body retired, W1's cell resumes-to-one-decision; AND the sweep is now a COMPUTED RECEIPT - ref/gen_sweep_receipt.py, 15 superseded tokens x 4 files, every hit printed with a DEAD/LIVE verdict, LIVE exits nonzero, token list append-only, paraphrase named as the round's residual - emitting ref/SUPERSESSION_SWEEP_RECEIPT.md, in the battery). THREAD 3 - GPT56-V88 F4 + CODEX-V88 F3 (the EXISTENCE half: spec section-3b names the PENDING state - arrival committed, no decision, deadline unexpired; recovery is the actor that drives open arrivals to their one decision via the normal path or the deadline refusal; the verifier refuses past-deadline zero-terminal arrivals at any gate and ANY zero-terminal arrival at disclosure; duplicate arrivals refused-though-impossible), GPT56-V88 F6 + CODEX-V88 F4 (TWO CUTS NAMED ONCE: the signed-checkpoint cut is clause 4's, before canonicalization, nothing after it in the signed body; the partition cut is issuance-commit end; issuance's own writes lie between - no enumerable event can exist there because a refused issuance is no issuance - authenticated by the chain and the opening pass's exactness check; the extends-through-issuance sentence, the circular half, is dead; Row L's phase set P0/P6/P7 UNTOUCHED, per the caution). MINORS - GPT56-V88 F7 (the BS-SI narrative and the map's own V86->V87 sentence now QUOTE the generated move, class-P 17->16 / class-E 8->9, naming 17/8 as my erroneous intermediate; the section-10 row regenerated from the edited map), GPT56-V88 F8 (L1462 and L1468 resolved CALLER under the table's own as-supplied boundary - the move the soft marking predicted and L1464 already made; counts 28/18, section-5 quotes 18). BUILD RECEIPTS: both V88 report blocks declared VERSION: V87 because my brief template carried the literal - both seats flagged it and preserved it; the control-plane token is corrected to V88 above an annotation in each report (findings and prose untouched) after prereg_lint proved the collision misresolved citations (two false FABRICATED verdicts against real V87 findings); the template is fixed in BRIEF_V89.
V89→V90: THE CLOCK, THE WIRE, AND THE GENERATORS (Blanc's five-thread relay, 2026-08-30 12:37 KST). GPT56-V89 F1 + CODEX-V89 F2 (the deadline gains its AUTHENTICATED CLOCK BASIS: the ARRIVAL event carries (boot_epoch, monotonic_reading) - Row B's own reading at append, the durable start - and every checkpoint's materials carry a CLOCK RECORD of their production; overdue is computed from those bytes only, cross-epoch arrivals overdue by definition at the first later-epoch pass, the wall timestamp demoted to human-facing; spec 3b carries the comparison rule, the schema at (ii-b) and Row B carries the pair, the registry gains four clock fields, 203 -> 207). GPT56-V89 F2 + CODEX-V89 F5 (the RECEIPT TRANSITION defined in spec 1c: a request exists exactly when its complete framed unit - length-prefixed, digest-closed - is read and decoded AND its arrival's framed unit is durably committed; frame decode is receipt, not processing; truncated/malformed frames are by definition not requests; a crash mid-arrival-append fails digest closure and is no arrival; the no-vanish guarantee explicitly scoped to completely framed schema-decodable requests; five framing fixtures added to the verifier item). CODEX-V89 F1 (the binding-to-key map follows the SAME partition as enumeration entries: sealed map for pre-partition decisions, AUTHENTICATED CONTINUATION MAP ENTRIES for post-partition ones, same temporal cut, every later pass reads sealed plus continuation - a PENDING arrival crossing BS-L is decidable without touching sealed bytes; two fixtures added). GPT56-V89 F4 + CODEX-V89 F3 (the two live pre-sitting draw survivors killed from the committed mechanics - the CURRENTLY-UNSET spacing paragraph and the CLASS-P-UNSET within-draw choice, both now quoting their dead forms; the sweep gained four draw/grid tokens with DRAFT scope, an honest per-token-scope header replacing the implied cross-product, and a --check byte-equality mode). CODEX-V89 F4 (the raise ledger's two V89 resolutions ENCODED IN ITS GENERATOR - gen_raise_classification.py now owns RESOLVED={1462,1468} with annotations, regenerates byte-equal, and gained --check; one pattern two instances with the sweep receipt, per the relay). GPT56-V89 F3 + F5 (the grid RE-EXPRESSED AS A STEP COUNT by DRAW_MECHANICS AMENDMENT 2: n_steps=50 even committed, endpoints constrained symmetric, delta-gamma DERIVED = 2*Gamma/50 - 0.01 at the proposed 0.25, unchanged - j zero-based over the baseline-inclusive manifest, n_perturbations=51, baseline j0=25 verified exactly zero, every (i,0) comparison now (i,j0); PROPOSAL_GAMMA_RANGE v2 pre-reconciled: the ratification asks one number, Gamma). GPT56-V89 F6 (Row B appends one DECISION event per completed request - touch's event or refusal's event, never a refusal-as-touch). GPT56-V89 F7 + CODEX-V89 F6 (the title line says V90 - the V89 copy carried V88's title, both seats' last finding).
V90→V91: SEMANTICS, SEAMS, AND THE CONTROLS HONOURED (Blanc's relay, 2026-08-30 13:06 KST, full autopilot). CODEX-V90 F1 + GPT56-V90 F2/F3 (the clock pair gains the semantics signatures cannot carry: boot_epoch DEFINED as the BS-2k restart counter - a sequence number owing nothing to any clock, incremented once per Row B start, committed before any event of the new epoch; monotonicity stated as INVARIANTS OVER THE SIGNED EVENT ORDER - epochs non-decreasing along the chain, readings non-decreasing within an epoch, an event's epoch equal to the committed counter - any violation MALFORMED HISTORY refusing the chain; bounds stated where the fields live: epoch decimal in [0,10^6], reading decimal ns in [0,2^63-1]; registry notes carry both). CODEX-V90 F3 + GPT56-V90 F5 (the map seam closed BY RULE, bindings unmoved: the map's two segments are cut at the SIGNED-CHECKPOINT cut - sealed for decisions at or before it, continuation for everything after, the issuance commit's own bindings the continuation's first members by definition; the asymmetry with enumeration entries stated - no refusal can exist between the cuts but decisions do; seam fixtures added both ways). CODEX-V90 F2 (the binding-to-key map DECLARED as exhaustive-list item (iv-c) with its closed schema - key, position, digest, signature, never payloads or outcomes; declares what the map already carried, widens nothing, so the ruled vocabulary is untouched and nothing is filed; registry gains four bindmap fields, 207 -> 211). GPT56-V90 F1 (the strata ruling propagated: the no-producer paragraph re-derived - Row D2 IS the producer, OPEN_QUESTION_STRATUM_PRODUCER closed-by-ruling with its stale class-P/count header corrected, the undecided-list line drops the strata question; two sweep tokens added). GPT56-V90 F6 (the live verifier contract follows AMENDMENT 2: manifest recomputed from (Gamma, n_steps=50) with delta-gamma DERIVED; the adjacent-gap and delta_gamma_max refusals compare the derived value; the 1209 committed-list names the step count, not the spacing; two more sweep tokens). GPT56-V90 F7 (exact-zero is TOTAL: manifest values are canonical DECIMAL STRINGS by exact decimal arithmetic - delta = Gamma*4/100, a digit shift - compared as strings, j0's value string-equal to the canonical zero; delta_gamma_max's schema row re-typed decimal-string; mechanics clarification appended, values untouched). GPT56-V90 F8 + CODEX-V90 F4 (the receipt --check control went RED over five stale line numbers from V90's post-generation section-10 insertion - honoured: receipt regeneration is now the LAST build step after every draft byte is final, like the pins; regenerated and committed here). GPT56-V90 F4 (the crash-window table gains the window the arrival class created: W0 pre-arrival wire / W1 the PENDING span, spec first, W2 covering both commits).
V91→V92: THE CLOCK GETS ITS VERIFIER, THE INTERFACE FIXES MULTIPLICITY, ONE GRAMMAR FOR THE GRID. GPT56-V91 F1 + CODEX-V91 F1 (the clock invariants re-scoped to CLOCK-BEARING records BY DESIGN with the reason stated - decision events carry no pair and compare nowhere, and no decision can straddle a transition because EVERY EPOCH OPENS WITH A RECOVERY CHECKPOINT, Row B's first act after restart, whose clock record is the epoch's first clock-bearing record - the chain evidence the restart counter lacked; arrival-epoch-equals-latest-opening replaces the unverifiable committed-counter clause; and THE CONSUMER IS NAMED: the enumeration verifier's five gates each run a CLOCK PASS, seven fixtures including the scoping one). GPT56-V91 F3 (the re-view collision resolved: the sealed interface delivers each allocated object's render EXACTLY ONCE per member - single-pass, forward-only, no re-render operation - so per-object multiplicity is the constant 1; the labeling-ergonomics cost is FLAGGED to the coordinator in the decisions file rather than decided silently, reversible until BS-2k fills). GPT56-V91 F7 (decisions commit FIFO in arrival order - the join permutation is the IDENTITY; the position-gap residual is NAMED like the cause-token channel; out-of-order decision refused as malformed history; head-of-line cannot deadlock under 3b's bounded commits). GPT56-V91 F2 (the lock-checkpoint preimage now REQUIRES the sealed enumeration entries and the sealed binding-map segment, whatever else the pending schema enumerates). GPT56-V91 F4 + CODEX-V91 F2 (ONE canonical decimal grammar for every gamma-grid byte - no plus, no exponent, no trailing zeros, no -0, canonical zero, exact decimal arithmetic, never a binary float; shortest-round-trip language dead; the registry's delta_gamma_max row re-typed to the grammar and DERIVED). GPT56-V91 F5 (the BS-3g row: the draw set is DERIVED not missing, and the dead NOT-EVALUATED token it still spoke is quoted dead). GPT56-V91 F6 (the unlabelled summary says six windows and G1-G6). GPT56-V91 F8 (v9 1677/1681 moved INTEGRITY -> CALLER in the GENERATOR - both test the supplied choice-point pair pre-run, no VOID antecedent owns them; CALLER 30 / INTEGRITY 57, regenerated byte-equal). GPT56-V91 F9 (FINDING_ROW_F_STRATA's header corrected: class E, generated count move quoted). CODEX-V91 F3 (bindmap.signature typed on-enum: 64-byte detached signature, signer roster-bound, no envelope leaves). CODEX-V91 F4 (the registry generator now EXTRACTS the (ii-b) arrival tuple and the (iv-c) bindmap tuple from the draft bytes and crosschecks them against the declared sets - the hand-declared sets that could drift silently now cannot). CODEX-V91 F5 (R01's completeness is BLOCK-SCOPED: the eleven codes must each appear inside the operative vocabulary block, outside retiring fragments - a stray mention elsewhere no longer counts; 35 controls green with the new block-scope control). CODEX-V91 F6 (the stay-16/8 sentence quotes the generated 16/9). BUILD NOTE: two of my new sentences formed accidental G-label patterns the derivation checker rightly bound (G6-dash, G5-dash) - caught by the battery, repunctuated; the checker's label-binding worked exactly as designed.
