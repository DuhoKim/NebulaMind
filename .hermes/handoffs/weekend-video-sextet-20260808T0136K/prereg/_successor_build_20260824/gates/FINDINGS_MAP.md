# FINDINGS MAP — which referee finding each transition answers

Human-written. `tools/prereg_trace.py` reads this and **fails** when a transition changed a normative
section while citing nothing here. The tool computes the bytes; it refuses to decide which finding a
change answers, because that is a judgement.

Format: `V<from>→V<to>: <finding ids, comma separated>`

V15→V16: FOLD — §6 replaced from SECTION6_DRAFT_AGY_R15 (d2c388a4) on principal's instruction 21:48
V16→V17: GPT56-V16-1, GPT56-V16-2, GPT56-V16-3, GPT56-V16-4, CODEX-V16-1, CODEX-V16-2, CODEX-V16-3
V17→V18: GPT56-V17-1, GPT56-V17-2, GPT56-V17-3, CODEX-V17-1, CODEX-V17-2, CODEX-V17-3, CODEX-V17-4
V18→V19: GPT56-V18-1, CODEX-V18-1, CODEX-V18-2
V19→V20: GPT56-V19-1, CODEX-V19-1
V20→V21: GPT56-V20-1, GPT56-V20-2, GPT56-V20-3, CODEX-V20-1, CODEX-V20-2
V21→V22: GPT56-V21-1, GPT56-V21-2, GPT56-V21-3, CODEX-V21-1, CODEX-V21-2, CODEX-V21-3, CODEX-V21-4
V22→V23: CODEX-V22-1 (class-E count 8 not 7), CODEX-V22-2, CODEX-V22-3, CODEX-V22-4, GPT56-V22-1, GPT56-V22-2, GPT56-V22-3
V23→V24: CODEX-V23-1, CODEX-V23-2, CODEX-V23-3, GPT56-V23-1, GPT56-V23-2, GPT56-V23-3, plus BLANC-20260828 (compute the counts and the trace)
V24→V25: BS2A-ADOPTION-20260828 (quality-cut exclusion predicate; principal's instruction), GPT56-V24-1, GPT56-V24-2, CODEX-V24-4, CODEX-V24-5, CODEX-V24-6
V25→V26: GPT56-V25-1, GPT56-V25-2, CODEX-V25-1, CODEX-V25-2, CODEX-V25-3, CODEX-V25-4
V26→V27: GPT56-V26-1, GPT56-V26-2, GPT56-V26-3, CODEX-V26-1, CODEX-V26-2, CODEX-V26-4
V27→V28: GPT56-V27-1, GPT56-V27-2, GPT56-V27-3, CODEX-V27-1
V28→V29: CODEX-V28-1 (current-transition scope rule)
V29→V30: PRINCIPAL-20260828-LAND-NULL (human direction: "add the land 2008 null to the prereg motivation"; no referee finding — the change answers an instruction, and inventing a finding ID for it would be a lie)
V30→V31: GPT56-V30-1, GPT56-V30-2, GPT56-V30-3, CODEX-V30-1, CODEX-V30-2
V31→V32: GPT56-V31-1, CODEX-V31-1, plus PRINCIPAL-20260828-COUPLING (human direction: "the cut raised the coupling — flag it in §2.7"; measured figures, not a referee finding)
V32→V33: GPT56-V32-6, CODEX-V32-5 (§2.7 conditional-independence overreach). The five gain-control findings — GPT56-V32-1..5, CODEX-V32-1..4 — were repaired in gates/GAIN_GRADIENT_CONTROL_DESIGN_20260828.md, which is a sidecar and not part of this document's bytes.
V33→V34: BS2A-R6-CLEAR-20260828 (the quality-predicate component cleared its code gate at round 6 from both seats and is pinned by digest in the BS-2a row, with its recorded robustness limit; the slot remains DESIGN, UNFILLED and no class count moves)
V34→V35: GPT56-V34-2, CODEX-V34-3 (the antisymmetry identity does not forbid a biased w creating a signal), CODEX-V34-1 (an unlogged archive read need not break the chain), CODEX-V34-4 (the BS-2a pin overstated the pairwise probe evidence)
V35→V36: GPT56-V35-1 (the repaired BS-2a row duplicated the 26-probe class, reading as 52), CODEX-V35-1 (the crash clause omitted the exit-status boundary; a post-verification emit failure prints MATCH then exits 1)
V36→V37: PRINCIPAL-20260829-VOID-OPTION-A and PRINCIPAL-20260829-BS6-OPTION-A (human decisions relayed by Blanc 09:20 KST, on GPT56-V34-1 and the three §7.1 coverage gaps GPT56 named in the VOID gate round). Closes the degenerate and digest gaps as separate antecedents, extends the §2.7 ID to cover a threshold *chosen* as well as *moved*, and adds class-P slot BS-3g blocking BS-6 — the first row-count change since V4 (15/8 → 16/8). **The §2.7 phase reconciliation was explicitly NOT authorised and remains open.**
V37→V38: PRINCIPAL-20260829-2.7-REFUSED-AS-PUT (the principal declined the §2.7 phase question as not his to answer — the clause was authored by this lane at V11, commit 4d99d1d93; the instant was recovered from that commit's own §2.7 preamble and the cell left unchanged) and PRINCIPAL-20260829-AUTH-DEPRIORITISED (record CODEX-V34-2 accurately in §5; do not build the typed authorisation record; do not touch frozen v9).
V38→V39: GPT56-V38-1 and CODEX-V38-1 (agreed: the DESIGN-slot inventory omitted BS-3g), CODEX-V38-3 (DECISIONS_FOR_DUHO.md cited as the record of a ruling it does not record), CODEX-V38-4 (§6.1/§6.2 mis-cited for the χ-ordering evidence, and commit metadata does not prove lane authorship). CODEX-V38-2 is NOT repaired here — it changes what voids a run and is referred to the principal.
V39→V40: PRINCIPAL-20260829-VOID5-OPTION-C (ruling on CODEX-V38-2: a pre-unblinding numerical failure does not void the run; qualify §5's numerical trigger to post-unblinding and route pre-unblinding permutation/statistic failures to INCONCLUSIVE-BY-COMPUTATION with a binding rerun procedure. Misconduct conditions deliberately unmoved at Any).
V40→V41: GPT56-V40-4 and CODEX-V40-4 (BS-3g absent from §6.1's closed non-χ-bearing receipt list, so its blocks-BS-6 edge was not receiptable; added, plus a §11 code-side item because a receipt class with no producer is still not receiptable), GPT56-V40-6 (the §2.7 evidence citations were wrong for a second consecutive draft; absolute line numbers replaced with section-and-quoted-content references that survive renumbering). GPT56-V40-1/2/3 and CODEX-V40-1/2/3 are NOT repaired here — they concern the rerun procedure and are referred to the principal.
V41→V42: HWAO-HANDCHECK-20260829 (hand-verification of the 11 pre-format prose citations found one wrong: the Stage-P dual-valued passage cited KIMI-V11 F4, which is a §6.1 access finding. KIMI's Stage-P finding is F7 — 'the exact-null Stage P is not implemented in the file §0 pins' — which is the argument the passage actually makes. Corrected to KIMI-V11 F7. The compound form 'KIMI/GPT56-V11 F4' was also invisible to the citation checker, which only ever saw the last seat; prereg_lint now expands compound citations.)
V42→V43: PRINCIPAL-20260829-RERUN-OPTION-A (delete the rerun allowance; INCONCLUSIVE-BY-COMPUTATION is a terminal halt and the operator's recourse is a new run under a new preregistration — dissolves GPT56-V40-1/2/3 and CODEX-V40-1/2/3 together), GPT56-V40-5 (aggregate calibration failures overlapped both inconclusive codes; COMPUTATION is now explicitly subordinate to CALIBRATION).
V43→V44: GPT56-V43-1 and CODEX-V43-1 (a pre-unblinding Stage-C numerical failure mapped to both INCONCLUSIVE-BY-COMPUTATION and Row J's POWER route, and the new code had no named producer; COMPUTATION is now residual by construction with producers named and a §11 implementation item requiring evaluation AFTER the power and calibration guards), GPT56-V43-2 and CODEX-V43-2 (the BS-3g row still presented the three-way completeness fork the principal ruled on at 10:15; only the γ-to-sign-vector mapping remains open), GPT56-V43-3 (V43 wrote its own V42→V43 transition in-band with no result digest, which §10 lines 850 and 854 forbid; removed — the row belongs to the draft that follows it).
V44→V45: GPT56-V44-2 and CODEX-V44-2 (§11's BS-3g item still called the completeness semantics open; the principal settled them at 10:15 and only the γ-to-sign-vector mapping remains. The seats cited §7 line 719 AND §11 line 920 at V43 and I repaired only the first). GPT56-V44-1 and CODEX-V44-1 are NOT repaired here — the COMPUTATION/POWER reachability question has now failed twice and is referred to the principal.
V45→V46: PRINCIPAL-20260829-COMPUTATION-OPTION-D (delete INCONCLUSIVE-BY-COMPUTATION; the principal ruled that INCONCLUSIVE-BY-POWER is the named inconclusive code his option C ruling meant, so the existing route satisfies it and the second code was redundant. Resolves GPT56-V43-1, CODEX-V43-1, GPT56-V44-1 and CODEX-V44-1. The deletion is recorded in §5 rather than made silently, together with how a closed-row enumeration can establish the absence).
V46→V47: GPT56-V46-2 and CODEX-V46-2 (§5 falsely routed a non-finite instrument output through §2.7 reason (c), which is catalogue quality; §2.7 defers instrument non-finiteness to post-unblinding handling), and the completeness argument resting on it is retracted. GPT56-V46-1 and CODEX-V46-1 (Row F's degenerate-bin and infeasible-allocation FAIL branches have no named outcome, and Stage-C numerical exceptions have no named conversion in §11, so the claimed existing POWER route is not executable) are NOT repaired here — naming outcomes for those branches changes what the study claims and is referred to the principal.
V47→V48: GPT56-V46-1 partial (the Stage-C exception has no named conversion to an outcome). §11 now requires the exception-to-outcome conversion generally, having established that the gap is systemic rather than Stage-C-specific: 111 raise sites in the pinned reference, 3 of them typed to a named outcome. The item deliberately does NOT decide which outcome any branch receives — Row F's branches remain referred to the principal, and option A has failed on its own terms.
V48→V49: PRINCIPAL-20260829-NUMERICAL-OPTION-B-RERULED (option A withdrawn after it failed on its own terms; option B ruled as a rule for the class rather than a code for Row F). Adds INCONCLUSIVE-BY-NUMERICAL-FAILURE stated as a CONDITION covering sites not yet enumerated, at every phase rather than pre-unblinding only, with the caller-error boundary made checkable and the two rulings (D deleted a redundant claimant, B adds for genuine absence) reconciled in the row. Resolves GPT56-V46-1 and CODEX-V46-1.
V49→V50: GPT56-V49-2 and CODEX-V49-3 (the class rule and the VOID clause both claimed post-unblinding non-finite/degenerate failures; the VOID antecedents are now named in the precedence and the rule never fires where one applies), GPT56-V49-4 (raise inventory recounted by AST — 112 nodes including 39 ManifestClosureError the grep missed entirely), CODEX-V49-3 (the 31–79 range stated as an unsubtracted candidate partition rather than a count), GPT56-V49-3 (the false §2.7(c) route removed rather than left standing beside its retraction), CODEX-V49-2 (the §11 classification attaches to failure paths and call sites, not to raise statements). NOT repaired: GPT56-V49-1, -5, -6, -7 and CODEX-V49-1.
V50→V51: GPT56-V49-6 (§2.1's unqualified "nothing else changes with the branch" contradicted its own text four lines later; qualified to Branch B, with Branch A's pin-voiding recovered from the V11 commit rather than legislated) and GPT56-V49-7 (the predecessor decision memo was amended by append after signing — b202645cd signed b4a1f1fc, ac603e4c2 corrected the signing time to produce the current 76cc25e5 — so the pinned digest no longer matched the file; both digests and the reason are now recorded).
V51→V52: PRINCIPAL-20260829-ROWL-QUALIFY (Row L's wrong-signature VOID condition now exempts the freeze signature and the canonical opening authorization — the two the row itself mandates that the unqualified rule caught; the BS-L detached signature is over the canonical lock digest and was never caught. Closes CODEX-V24-1 / CODEX-V49-1, carried open 25 drafts. Recorded as making the path executable, not verified) and PRINCIPAL-20260829-THIRD-STATUS (UNREACHABLE-BY-CONSTRUCTION added, promoted only by named per-site evidence — execution count over a stated harness, a structural subsumption argument, or both — with a falsification clause making it a claim about the record rather than about routing). Closes GPT56-V49-5.
V52→V53: GPT56-V52-1 and CODEX-V52-1 (V52's UNREACHABLE example named no sites and the ledger marked all eight guards NUMERICAL, so the worked example violated the rule it illustrates; the five are now named per site, with L1411/L1435/L1437/L1439 promoted on measurement AND structure as post-allocation guards, and L1401 promoted on measurement ONLY because it is a branch of the feasibility decision itself rather than a guard subsumed by it — V52 wrongly implied the structural argument covered it). GPT56-V52-2 and CODEX-V52-2 (stale 29/31/48-unread and 111 inventories withdrawn, not merely superseded; the AST count of 112 with 39 ManifestClosureError is stated with the reason the grep figure arose).
V53→V54: GPT56-V53-1 and CODEX-V53-1 (L1401 is directly reachable — allocate_handcheck takes budget as an argument and the harness froze it at HC_REAL_LABELS=500; it fires at budget=200), CODEX-V53-2 (the remaining four lack per-site predecessor conditions). ALL FIVE PROMOTIONS WITHDRAWN; no site holds UNREACHABLE-BY-CONSTRUCTION and the evidence bar is restated so a third attempt is not a third failure. GPT56-V53-3 and CODEX-V53-3 (§11's 48-unread status withdrawn; §5 and the ledger both at NUMERICAL 22). NOT repaired: CODEX-V53-4 and GPT56-V53-2.
V54→V55: GPT56-V54-4 (the Stage-P dual-valued passage cited KIMI, which never supported it — KIMI-V11 F13 says the promise is 'now single-valued' and its F7 is a v7-subject disclosure finding; V42 had corrected an earlier KIMI miscitation by substituting F7, which is also wrong. Now cites two seats, GPT56-V11 F4 and CODEX-V11 4), CODEX-V54-2 (L1464 reclassified CALLER; numerical class 21), GPT56-V54-5 (the numerical-routes artifact's STATUS line still carried 48-unread and 31–79). NOT repaired: GPT56-V54-1, GPT56-V54-2/CODEX-V54-1, GPT56-V54-3, CODEX-V54-3.
V55→V56: PRINCIPAL-20260829-REFUSAL-VOCABULARY-OPTION-A (the eight-code closed refusal set, no catch-all, options B and C refused and recorded as refused; the principle that a reason may describe the request and authorisation state but never the object is written in as the test rather than the list; the derivation from §6.1's closed row table is stated and pinned by fingerprint, with tools/refusal_vocabulary_check.py failing when the table moves). Closes GPT56-V49-1.
V56→V57: PRINCIPAL-20260829-REDO-DERIVATION (the closure argument is withdrawn and option A is SUSPENDED — neither reaffirmed nor reversed; the derivation is to be redone from scratch across both the authorisation and availability axes with a tested exhaustiveness argument, not extended by a ninth code; REFUSED-IDENTITY-OUTSIDE-PERMITTED-SET does not survive as written). Resolves GPT56-V56-1/2/3 and CODEX-V56-1/3/4 by withdrawal rather than repair. GPT56-V56-4, CODEX-V56-6 (the hand-copied totals removed in favour of the generated ledger), GPT56-V56-5 and CODEX-V56-5 (inject_signs supplied-accuracy and mask-c admissibility reclassified CALLER). NOT repaired: CODEX-V56-2, BS-3g still has no pinned schema, producer or verifier.
V57→V58: PRINCIPAL-20260829-BS3G-SCHEMA (CODEX-V56-2 half one — BS-3g's SLOT_SCHEMA entry is specified as nine fields with types written for an independent implementer, plus producer, independent verifier and failure behaviour; the receipt is shown unable to carry a per-object field as a property of the field list rather than an undertaking, with the limit stated that this holds only for slots present in SLOT_SCHEMA and four others remain absent).
V58→V59: CODEX-V56-2 half two — receipt construction bound to a strict constructor that refuses slots absent from the pinned SLOT_SCHEMA, with every named producer required to use it and nothing else, plus a separately pinned verifier making a permissive-path receipt detectable rather than merely discouraged. v9 not modified. The four remaining SLOT_SCHEMA entries are deliberately not specified: each is written as part of filling its slot, and no receipt may be emitted for a slot lacking an entry, which turns the gap into a blocker rather than a hole. A producer that cannot be routed through the constructor is a STOP and returns the unfreeze question to the principal.
V59→V60: GPT56-V59-3 and CODEX-V59-3 (BS-3g bound the code the control ran and not the data it ran on, so a conforming receipt could certify a different sample or a favourable subset; mask_sha256, calibration_sha256 and perturbation_manifest_sha256 added, with the verifier refusing unless the mask digest equals BS-2f's pin and n_perturbations equals its manifest length).
V60→V61: PRINCIPAL-20260829-ROWL-PHASES (VOID-6.1L-WRONG-SIGNATURE gets Row L's own phases P0/P6/P7, with the exemption re-anchored to canonical body digests so widening cannot re-catch the exempt signatures), PRINCIPAL-20260829-FREEZE-SIG-BODY (CODEX-V53-4 — the canonical freeze-signature body, its digest and its verifier are specified, and the reusable lesson recorded: naming an object is not narrowing when the object has no canonical form), PRINCIPAL-20260829-BS2V-REASON (GPT56-V53-2 — the false self-reference reason is corrected while the slot stays UNRESOLVED and pinning is stated necessary-not-sufficient). Also: the unreachability harness bar now requires recording which arguments were varied, after the third frozen-parameter failure in one day.
V61→V62: CODEX-V59-2 and GPT56-V59-1 (the receipt_strict binding is scoped to slot-receipt producers; the V59 wording swept in seven rows emitting non-slot artefacts and fired V59's own STOP condition on its own overbreadth rather than on an unbindable producer, so v9 stays frozen; BS-7p is explicitly bound), GPT56-V59-5 and CODEX-V59-4 (the row fingerprint and the tool file's sha256 were conflated by a wrong label, not a wrong value), GPT56-V59-7 and CODEX-V59-5 (§5 now quotes the generated ledger rather than restating a count that has drifted three times).
V62→V63: PRINCIPAL-20260829-UNREACHABLE-DROP-BASIS-I (GPT56-V54-2 and CODEX-V54-1 — basis (i), promotion on an execution count alone, is DROPPED; UNREACHABLE-BY-CONSTRUCTION now requires a structural proof and measurement is corroboration only. CODEX's sentence is carried into the text verbatim, both seats' answer that the old bar could be met in full by a false claim is recorded, and §5 states explicitly that a permanently empty category is a real answer rather than a defect to be fixed by loosening the bar), PRINCIPAL-20260829-VOID-PARTITION (GPT56-V54-1 — the class rule and the VOID clause are made DISJOINT rather than ordered for a third time; §5 states the partition, on whether the failing quantity was pinned/sealed/verified before the failure or computed by the run, so any post-unblinding failure resolves without a precedence rule, and the falsification test is written in: if precedence is still needed the sets are not disjoint. §7.1 records that VOID-5-NONFINITE and VOID-5-DEGENERATE are narrowed accordingly), PRINCIPAL-20260829-PLANNING-NOT-AN-OUTCOME (GPT56-V54-3 — NUMERICAL-PLANNING is removed as an outcome class: nothing has started, so nothing can be voided or declared inconclusive. Its three sites L963/L973/L986 are MOVED to CALLER, not deleted, and the per-call-site ledger was checked rather than assumed — three reaching paths each, none through run_production_verdict, with the graph's lower-bound limit stated and the escalation named if any is ever shown run-reachable), PRINCIPAL-20260829-WITHDRAWAL-ON-RULE (CODEX-V54-3 — the five withdrawn promotions stand on the rule change and on the missing per-site predecessor condition, not on the unpinned ad hoc harness; the execution count is recorded as history and carries no weight, which also disposes of its 80,000/60,000 drift), PRINCIPAL-20260829-GAIN-MAPPING-OPTION-A-WORST-CASE (the γ → sign-vector mapping is ruled: option A with worst case over draws. BS-3g's schema grows by the four draw-set fields that make the count, generator and stopping rule checkable; mapping_id stays MAPPING-NOT-PREREGISTERED because a family is not a mapping until its draw set is frozen; option B is discarded as the gate mapping but NOT retired from the record until the margin is re-derived at the real calibration; and what a worst-case failure would mean is stated in advance), HWAO-REGISTER-CITATION-CHAIN (§8 gains the one defect in that register the lane committed rather than inherited: V42 repaired a wrong KIMI citation by substituting another wrong one, which survived twelve drafts — a correction that is itself unverified is not a correction, and that is the argument for citation_block_check.py).
V63→V64: PRINCIPAL-20260829-CATCHALL-NON-CLOSURE (the no-catch-all decision of 19:52 is FORMALLY REVERSED after both seats broke the second closure derivation in the VOCAB-R1 round; the vocabulary is eleven codes including REFUSED-UNCLASSIFIED, carrying a code and nothing else, under a guard making every emission a defect enumerated at freeze rather than a routine outcome, because a catch-all whose count is never reviewed becomes the vocabulary. The sequence is recorded as load-bearing: the ruling was made AFTER the argument was attacked, and one made against an argument that had only been written would have collapsed as the first one did. Resolves GPT56-V63-6, the suspended set still normative and still hard-enforced by the checker; tools/refusal_vocabulary_check.py is rewritten to the eleven codes, its fingerprint control INVERTED loudly - pinning a derivation fingerprint is now a failure, because there is no closure claim for it to protect - and its closure test SCOPED after an unscoped version fired on invariance_outcome's token set, with a negative control asserting it does not), CODEX-VOCAB-1 and GPT56-VOCAB-1 (the request lifecycle: permission is decided durably before any transfer work, writes get a PENDING-SURFACE-CHECK state because conformance cannot be judged before the payload is decoded, every state has one terminal treatment so no request ends undecided or unlogged, and the log boundary sits at the verdict. The catch-all makes an undecided permission loggable; the lifecycle makes it decided. Nothing is added to what the log records), REFUSED-SCHEMA-NONCONFORMING RESTORED (its deletion rested on receipt_strict() covering the fact, and V62 scoped receipt_strict() to slot receipts only - my own repair removed the basis for the deletion, and Rows C2 and H write non-slot field-constrained objects through Row B), GPT56-V63-2 (L986 is MOVE_CAP, an internal cap firing after a feasible prefix exists, so calling it a caller error violated this section's own supplied-argument boundary; dispositioned PLANNING-INTERNAL, explicitly NOT an outcome class and carrying no terminal consequence - this corrects a premise of the ruling rather than the ruling), GPT56-V63-4 (BS-3g's entry lives in the successor layer's schema because frozen v9 cannot gain one; the producer is bound to receipt_strict() and a BS-3g receipt through v9.receipt() is a protocol deviation - specification is not enforcement), GPT56-V63-5 and CODEX-V63-2 and CODEX-V63-3 (four draw-set gaps closed: n_draws and draw_master_seed are FROZEN IN THIS PREREGISTRATION so pre-commitment is witnessed by the freeze rather than asserted by the receipt it constrains; the draw x perturbation matrix gets canonical row-major addressing and a stated serialization; the categorical worst case is defined as HELD iff every cell equals baseline_verdict, avoiding an invented ordering over outcome names; and identifier, seed and count encodings are bounded so no field can carry an object-indexed payload), CODEX-V63-4 (require_complete_sample compares two caller-supplied integers and verifies no parent-to-receipt partition; recorded as a limit, not repaired, because v9 stays frozen). NOT repaired: GPT56-V63-1 and CODEX-V63-1, the partition - it failed the falsification test written into section 5 at the principal's instruction, three constructions have now failed on the same defect, and a fourth is not mine to attempt unannounced. GPT56-V63-3, the BS-3g lifecycle cycle, is also unrepaired and referred.
V64→V65: PRINCIPAL-20260829-CHI-BLIND-ACCESS-SCHEDULE (option (a) on the χ-adaptive access leak both seats found — GPT56 via Row G, CODEX via Row D. §2.7(3) constrains the exclusion predicates and does not reach the access schedule, so the χ-blindness this covenant relied on was inherited from something that does not cover it: the universe of eligible objects is χ-blind, the set and sequence actually requested are not. Every row that may read a χ-bearing object now has a precommitted χ-blind traversal — order, multiplicity, retries and stopping — and Row B refuses any request that is not the next unserved position. THE CLAUSE FORBIDS RATHER THAN OBSERVES: nothing built today is adaptive, but the covenant PERMITTED it and permission is what leaks, so a clause recording that nobody currently does it would leave the channel open to the first implementation that did. The traversal is derived from already-pinned objects and creates no new slot; re-viewing the CURRENT object is unrestricted, because dwell is not a request for a different object), PRINCIPAL-20260829-FLAG-RULE-NO-CONNECTION (flag → discard → replace stays unmodified, and is recorded as a FINDING rather than a permission: recognising a specific galaxy and judging its handedness are separate judgements, and nothing about handedness is visible in the image. The WHY is written into the text because an unexplained exemption invites a later editor to widen or delete it and both would be wrong for reasons nobody could reconstruct. The dependency is written so it can be checked rather than assumed — the exemption holds only while flagging is uncorrelated with handedness, and it is BOUND TO THE PINNED DIGEST of the Row G interface specification, so a different interface, visible orientation cues or a checker who also sees instrument output lapses it. Bound to a name it would widen with the name; bound to a digest it cannot widen silently. The last dependency of this kind was assumed and the seats found it unpaid, which is why this one is a condition to preserve rather than a fact that stays true by itself).
V65→V66: GPT56-V64-1 and CODEX-V64-1 (the lifecycle asserted a log boundary without making it atomic - reaching a verdict and appending it are two operations and a crash between them loses the event, which is the ordering-assertion defect committed inside the clause written to repair it. Now THE APPEND IS THE VERDICT: no verdict exists until its event is durably in the chain; Row B appends BEFORE releasing any byte, so a crash before the append leaves no event and no delivery and re-processing is safe; the cost is stated - the log can over-report a touch and can never under-report one; Row B is a single serialised writer with one decision per request, owns recovery, and a lease-losing worker may not append. NOT fixed and referred: a durable PRE-verdict state needs a second event class, and Row B's cell commits to exactly one event per touch, so adding one changes what the log records and is not authorised), GPT56-V64-2 and CODEX-V64-3 (the catch-all guard was unenforceable because the freeze is P0 and every refusal is P1 or later - a freeze-time review cannot police emissions that do not exist yet. The enumeration moves to the pre-unblinding lock checkpoint, which Row B's cell already produces, with a BLOCKING INVARIANT: BS-L may not be issued while any REFUSED-UNCLASSIFIED event is unenumerated. Deliberately NOT a VOID condition - blocking the lock stops the run repairably and matches what was ruled, where voiding is stronger than was ruled and would be his sentence. The checker now verifies the MECHANISM rather than phrase-matching the guard: R06 fires if the enumeration is anchored at freeze, R07 if it has no blocking invariant), GPT56-V64-3 and CODEX-V64-5 (V64 claimed the freeze witnesses n_draws and draw_master_seed while no frozen value was written anywhere, so the verifier had nothing to compare against. Both are class-P and CURRENTLY UNSET, and their absence blocks BS-3g emission the way the four empty SLOT_SCHEMA entries do; the closed sets are declared - mapping_id admits only MAPPING-NOT-PREREGISTERED, draw_generator_id's set is currently EMPTY and therefore also a blocker), CODEX-V64-4 (a singleton gamma=0 manifest satisfied every stated check while perturbing nothing and reporting HELD - the digest bound the manifest to the receipt and nothing bound the manifest to the question. The manifest must contain both endpoints and at least three distinct values, and the verifier refuses any whose largest |gamma| is below gamma_bound and refuses the singleton {0} by name), GPT56-V64-4 and CODEX-V64-6 (L986 was PLANNING-INTERNAL in the draft and still CALLER in the generated ledger; the generator is fixed and the ledger reads CALLER 25, PLANNING-INTERNAL 1), GPT56-V64-5 and CODEX-V64-7 (the checker digest was that file's value before the same revision rewrote it; recomputed - and it went stale a second time inside this repair when the checker gained R06 and R07, which is now recorded with the rule that the digest is computed last). NOT repaired and referred: CODEX-V64-2, that the four availability codes describe the logged object and create a per-object state leak - they are part of the vocabulary ruled at 22:18 and changing them is the principal's.
