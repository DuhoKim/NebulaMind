KGATE_BUILDOUT VERDICT: HARNESS=HOLD MAPPING=PIN-READY
1. Harness fails pin-readiness because its decisive surface is real build_plan behavior on pinned data; tier-1 proves _plan is not reached on synthetic refusals, but tier-2 missing/extra/disagreement through the real planner is skipped and the docstring admits BS-2c may not fill.
2. The five repaired harness defects are precisely integration-class defects; after TOCTOU, universe-form, collision, falsy-passthrough, and vacuous-fixture repairs, the digest should not be frozen before the real pinned-data refusal taxonomy and recomputed-digest receipts are exercised.
3. Mapping holds under the stated battery: CRN identity across gammas, cross-draw distinctness, zero-based bounds, gamma-zero flat-a0, mask-drift guard, behavioral clamp controls, identity sensitivity, and one real counterfactual-path run all test behavior rather than self-report.
4. Mapping v2's identity_record binds seed, draws, clip bounds, A_LONGO by value, v9 sha, and numpy version, so the pin captures the executable convention and makes later environment/constant drift detectable.
RISKS:
HARNESS: unrun tier-2 may still show wrong refusal classification or receipt encoding for missing vs extra vs disagreement despite monkeypatched proof that dispatch was blocked.
MAPPING: the battery verifies mechanics and digests inputs, but cannot by itself prove the digested a0/gamma/cbar/A_LONGO convention and two-uniform order are the intended preregistered ones rather than a consistently pinned wrong convention.
TIER2: WAIT — loud skips preserve visibility but not evidence, and the module's own BS-2c caveat means the harness pin should wait for the real build_plan tier-2 result.
