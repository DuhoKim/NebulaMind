KGATE_PREREG_NECESSITY VERDICT: NECESSITY=OVERBUILT DIRECTION=OSCILLATING

Q1 — NECESSITY (OVERBUILT)
1. The study's actual integrity risks are already closed by cheap, receipted means: parity-even bias is zero by architecture, the 12,117-brick closure is pinned, the eight galaxy-cut predicates and z<0.15 are frozen — none of these facts depend on lifecycle crash-window laws, authenticated clock bases, or a human-waypoint trust chain to remain true.
2. The last ~6 rounds' own findings are defects inside machinery that exists only for the process: a generated registry contradicting the repaired text, a checker blind to drift in the very records it guards, admission tuples lagging schema, an unparsable sidecar — none of these can move the measured amplitude by a single unit; the loop is auditing artifacts that exist only to be audited.
3. The predecessor was declined on 2026-08-25 partly of process weight, and the successor's response was strictly more process: ~10 generated artifacts, a 13-check battery, and a standing rule forcing every repair to add yet another planted-red control.
4. The budget is misallocated against the dossier's own gate list: six promised tools unbuilt, platform constants unvalued, six UNFILLED slots, and a calibration-robustness control that must run and return HELD — a failed calibration vetoes the science; a malformed repair ledger never will.
5. Concrete cut list with no integrity loss: authenticated clock bases and the human-waypoint trust chain (git history plus one signed sha-pinned freeze receipt carries the same evidentiary weight), the terminal signature ceremony (the principal signing a pinned sha IS the ceremony), the supersession-sweep-receipt generator, and the mandatory planted-red control per repair; keep the non-chi surface as one handwritten list inside the prereg text.

Q2 — DIRECTION (OSCILLATING)
1. Finding counts do not decay: 12→8→10→15→17 across V88→V112, with the last three rounds rising monotonically — a converging loop trends toward zero, not up 70% from V110 to V112.
2. The defect-class shift is the signature of self-reference: early rounds found study-design gaps (blinding holes, crash windows, deadline laws); recent rounds find contract drift in artifacts the loop itself generated — its output has become its own input.
3. Surface growth is guaranteed by standing rule: every generated-artifact repair must land in generator source plus a new control, so each fix ships new reviewable bytes and the next round has strictly more to attack — positive feedback written into the process.
4. The declared stopping rule gates on severity (zero-HIGH) and a double CLEAR with no known debt, but says nothing about volume or surface area; after 26 rounds in ~36h across three paid seats there is still no zero-HIGH round, and the repair rule keeps manufacturing new debt classes.
5. The calendar is being spent against the study's own deadlines: the release choice resolves by rule on Sep 5 while the calibration control and six tools — the items that actually gate freeze — receive zero loop throughput.

RECOMMENDATIONS:
1. Declare a scope freeze today — no new machinery, registries, generators, or controls, repairs to existing text only — and cap the loop at a fixed number of further rounds (three), after which the principal signs with an enumerated known-debt appendix rather than waiting for a double CLEAR the current repair rule makes unreachable.
2. Redirect the two referee seats this week from text rounds to build-out: land the six promised tools and run the calibration-robustness control to a HELD-or-failed verdict, since those gate both the science and the Sep 5 release rule.
3. Execute the cut list in Q1.5 now, collapsing the trust-chain apparatus to one signed, sha-pinned freeze receipt plus plain git history and inlining the non-chi surface as a handwritten prereg section, so the remaining rounds review only the study, not the scaffolding.
