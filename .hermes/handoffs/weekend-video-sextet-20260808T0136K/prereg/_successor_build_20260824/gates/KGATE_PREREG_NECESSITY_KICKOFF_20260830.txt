You are the KIMI GATE SEAT — an outside auditor with no stake in the design under review.
The principal (the human investigator) asked for your independent verdict on whether a
preregistration effort is NECESSARY as run and going in the RIGHT DIRECTION. Be adversarial;
do not defer to the incumbent design; if the honest answer is "stop refereeing text and go
do X," say exactly that.

== THE STUDY ==
A preregistered replication test of Longo's galaxy spin-handedness dipole claim (published
amplitude −0.0408 at a fixed axis) on DESI Legacy Surveys DR10-south imaging. Instrument: an
automated antisymmetric classifier chi with chi(mirror(x)) = −chi(x) by architecture
(parity-even bias zero by construction, receipted). Sample: a pinned 12,117-brick closure,
redshift cut z_phot < 0.15, eight frozen galaxy-cut predicates. Blinding: strict chi-custody
— no measured handedness value may influence any pre-unblinding decision; the design
enumerates a non-chi surface (what records may exist outside the sealed store) and forbids
everything else by default. The PREDECESSOR direct-run study was DECLINED by the principal's
signature on 2026-08-25 (sample archived); this successor preregistration was authorized the
same day: writing only, own gates before any data touch.

== THE LOOP UNDER REVIEW ==
Since Aug 24 the prereg text (~1500 dense lines + a lifecycle-guarantee spec) has moved
V1→V113. Since V88 (~36h ago) every build is reviewed by TWO independent adversarial referee
seats (different model providers), sha-pinned, whole-document, findings in a structured
block; the coordinator repairs and re-dispatches. About 26 rounds so far. Verdicts: NOT
CLEAR every round. Finding counts per round-pair recently: V88=12, V109=8, V110=10, V111=15,
V112=17. The CHARACTER shifted: early rounds = structural design gaps (blinding holes,
lifecycle crash windows, deadline laws); the last ~6 rounds = instrument-and-contract
defects — a generated registry contradicting the repaired text, a checker blind to drift in
the very records it was built to guard, admission tuples lagging the schema, a sidecar the
trace checker cannot parse. The draft now ships ~10 GENERATED artifacts (field registry,
domain-kind map, non-chi surface registry, repair ledger, supersession sweep receipt) and a
13-check battery with seeded positive controls and deletion probes; a new standing rule says
every repair to a generated artifact lands in the generator's source with a control that
plants the superseded definition and proves red.
Stopping rule as declared: a zero-HIGH round, then one consolidation pass (all sweeps and
controls re-run, history regenerated), then CLEAR from both seats on a draft with no known
debt, then the principal signs the freeze.
Remaining before freeze besides the loop: six promised-but-unbuilt tools (canonical decoder,
terminal-review verifier + ceremony script, the executable perturbation mapping module, a
replay harness, one schema), platform constants to value, two human rosters, six UNFILLED
slots including a calibration-robustness control that must run and return HELD, and a
release choice that resolves by rule on Sep 5.
The principal has ruled on every open decision (4 rulings tonight, verbatim recorded).
Cost so far: ~26 two-seat referee rounds in ~36h plus coordinator time; the loop consumes
three paid seats.

== THE TWO QUESTIONS ==
1. NECESSITY. Is this degree of machinery — chi-custody lifecycle spec with crash-window
laws, generated non-chi surface registries, authenticated clock bases, human-waypoint trust
chain, a terminal signature ceremony — NECESSARY and proportionate for THIS study (a
replication test of a contested amplitude, predecessor dead partly of process weight)? Or
has the loop become self-serving — finding defects mostly in machinery that exists only to
be found defective? Name concretely what could be CUT without losing scientific integrity,
if anything.
2. DIRECTION. Given the trajectory (counts 12→8→10→15→17; classes structural →
instrument-and-contract; each repair adding new attack surface that the next round mines),
is the loop CONVERGING toward a freezable preregistration, or OSCILLATING? Is the declared
stopping rule right, or should it change — e.g., a SCOPE FREEZE (no new machinery, repairs
only), a cap on rounds, or shifting effort to the six unbuilt tools and the calibration run
that actually gate the science?

== REQUIRED OUTPUT FORM ==
Line 1: `KGATE_PREREG_NECESSITY VERDICT: NECESSITY=<PROPORTIONATE|OVERBUILT|UNDERBUILT> DIRECTION=<CONVERGING|OSCILLATING|WRONG-TARGET>`
Then: for each of the two questions, at most 5 numbered reasons, concrete, citing the
dossier's own facts. Then `RECOMMENDATIONS:` — at most 3, each one sentence, actionable
this week. Nothing else.
