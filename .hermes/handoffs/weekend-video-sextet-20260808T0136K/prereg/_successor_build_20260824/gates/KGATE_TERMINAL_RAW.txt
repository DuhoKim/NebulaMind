All probes complete. The picture is clear: the verifier file is sound on every axis I probed, but the ceremony script's documented CLI invocation is broken — and that region was never laddered. Ruling:

KGATE_TERMINAL: HOLD: repair terminal_ceremony.py's __main__ arg parsing so the documented `--transcript PATH` invocation actually runs the ceremony (today it exits 2 on the docstring; `--transcript=PATH` silently uses the default path), drive both forms through __main__ in the selftest, rerun green with a targeted re-verify of the changed region, then pin at V130.

1. Ladder verified on disk and by my own re-run: AGY TRV-V1 DEFECTIVE 2 → TRV-V2 DEFECTIVE 3 → TRV-V3 DEFECTIVE 1 → TRV-V4 SOUND 0 reports all present and match the claim; fixtures 29/29 and ceremony selftest 5/5 green on this machine; tree clean at v4 commit cf1e17973 — the bytes I probed are the ladder-final bytes.
2. The verifier file itself is PIN-READY: verify_prefix genuinely recomputes body_digest/running/anchors (not presentation-trust), ending XOR + CHAIN-UNMOORED + TRAILING-RECORDS + ENDING-COMMIT-MALFORMED + DUPLICATE-DRAIN-START + the export closing checks all refuse correctly; REVIEW_FORM_SCHEMAS mirrors ref/gen_string_field_registry.py:515-520 exactly; record model imported, never twinned.
3. KGATE-TERMINAL F1 (the hold): the pinned docstring's usage line `... STORES.json --transcript TRANSCRIPT.md` can never run — the argv filter drops the flag but keeps its value, leaving 4 positionals → docstring + exit 2 (probed: rc=2, no transcript written). Worse, `--transcript=X` runs rc=0 but silently writes the DEFAULT CEREMONY_TRANSCRIPT.md — transcript misdirection at the exact human waypoint P9 exists for.
4. This region is unladdered: the selftest calls run_ceremony() directly, so no round ever exercised __main__. The pin freezes these bytes+sha into the spec ("any byte change voids the pin") — the defect dies before the pin, not after.
5. It fails safe (exit 2, nothing signed), so no soundness hole — but a pinned ceremony driver whose documented interface is dead/misdirecting fails the artifact's own contract at the one moment it is run.
6. Exact unblocking condition: (i) fix __main__ parsing — documented --transcript PATH form runs and writes to PATH; =-form honored or explicitly rejected, never silently defaulted; 3-arg default preserved; (ii) selftest drives BOTH documented forms through the real CLI (subprocess), asserting rc=0 + transcript at the named path; (iii) verifier file untouched — any change there means full re-ladder; ceremony logic outside __main__ untouched, one targeted seat pass over the diff; (iv) 29/29 + selftest green re-run; _tmp_v130_pin.py recomputes the new ceremony sha, pin lands at V130 as scripted.
7. Observation, NOT a condition: non-record-model JSON input crashes with a traceback (fail-safe, nothing signed) — consistent with residue (b)'s provisioning binding and TRV-V4's record-model scoping; a catch→refusal hardening is a nice-to-have in the same repair.
8. Residues (a)(b)(c) confirmed in code: clock_pass lives in enumeration_verifier (five gates' job), the JSON input model is declared, the script prints its own/verifier/enum shas for the principal's OS-tool check and holds no key.

Current shas for the record (will change on repair): verifier 22599359c7178d…, ceremony 45c451a974f08d…
kimi exit=0
