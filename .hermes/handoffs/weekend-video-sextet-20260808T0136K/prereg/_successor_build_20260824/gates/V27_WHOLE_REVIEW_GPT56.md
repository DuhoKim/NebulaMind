# V27 WHOLE-DOCUMENT REVIEW — GPT56

## Verdict

**NOT CLEAR.** The three ordered document repairs landed, the V26 §2.7 conditional-independence sentence is byte-identical in V27, and the pinned subject digest matches. However, §10's new claim that findings mapping is enforced is false on the reviewed bytes: the required live `tools/prereg_trace.py --check` invocation exits 1, and the checker neither requires nor finds the current V26→V27 sidecar mapping. This is a document-contract blocker, not a lint nit.

## Digest-first identity

I compared the SHA-256 recomputed directly from the bytes of `../PREREG_SUCCESSOR_DRAFT_V27_20260827.md` with the value written after the drafting seat exited in `runner_v27_chain.log` line 5.

- runner pin: `e801a18bb7c489f0e4924695a13ba2f97f65a1b768c6dcc54a515cd5b31fb064`
- independently recomputed subject SHA-256: `e801a18bb7c489f0e4924695a13ba2f97f65a1b768c6dcc54a515cd5b31fb064`
- comparison: **exact 64-hex match**

For the preservation comparison, I also recomputed V26 as `2eec8da41ee69374fcc9c3fca2de150b29c04ca7b921848e908fa97a20bffd52`, matching V27 preamble line 4.

## Numbered findings

### 1. CRITICAL — §10 line 815; the asserted enforcement check fails on V27

**Evidence.** I ran the required checker myself from the repository root:

`python3 tools/prereg_trace.py <successor-build-dir> --check <.../PREREG_SUCCESSOR_DRAFT_V27_20260827.md>`

It returned exit code **1**:

```text
prereg trace check — PREREG_SUCCESSOR_DRAFT_V27_20260827.md
  MISSING: no written row for V25 → V26
  26 computed transition(s); 1 problem(s)
```

V27 §10 line 815 nevertheless says, “The findings mapping is enforced.” The table ends at V23→V24 (line 841); it has no V25→V26 row. The drafting runner's “no inconsistencies found” line came from lint, not from a successful trace check on V27.

**Why it fails.** The brief expressly permits §10 to make the enforcement claim only if the checker verifies it. The executable check is nonzero on the pinned subject. A preregistration cannot claim a mechanical obligation is enforced while its named enforcement command rejects the document.

**Smallest sufficient repair.** Regenerate/extend V27's in-band §10 table through its predecessor transition V25→V26 using the checker-generated byte columns and the human findings map, then rerun the exact V27 `--check` command and require exit 0 before retaining the enforcement sentence.

### 2. HIGH — §10 line 815, `gates/FINDINGS_MAP.md` lines 18–19, checker lines 222–238; the “current transition lives in the sidecar” rule is neither true nor enforced

**Evidence.** `gates/FINDINGS_MAP.md` ends at `V25→V26`; it contains **no `V26→V27` mapping**. In `tools/prereg_trace.py`, both the written-row check (lines 203–207) and findings-map check (lines 231–238) skip every transition whose destination is greater than or equal to the subject version. For V27 that skips V26→V27, exactly the transition §10 says the sidecar owns.

I also mutation-tested the rule in a temporary, removed-after-use mirror under this gate directory:

- V26 baseline sidecar: exit 0.
- Remove V24→V25, an in-band predecessor mapping: exit 1, exactly `NO FINDING CITED: V24 → V25`.
- Remove V25→V26, V26's current transition: exit 0, zero problems.

Thus the historical exemption is genuinely named in code (`HISTORIC_EXEMPT_BEFORE = 15`), and predecessor mappings are enforced, but the claimed current-transition sidecar rule is not. The current mapping is absent and deletion of a current mapping does not fire.

**Why it fails.** Of the three rules V27 describes, only two are represented operationally: the V1→V15 named exemption and predecessor-only in-band checking. The sidecar-current-transition obligation exists only in comments/prose. This is precisely the silent coverage gap the repair was meant to eliminate.

**Smallest sufficient repair.** Add `V26→V27: <the actual V26 finding IDs answered>` to `FINDINGS_MAP.md`, and change the checker so the current transition is excluded from the in-band table check but **required in the sidecar findings check**. Add a fixture that deletes the current mapping and asserts one `NO FINDING CITED: V26 → V27` failure.

### 3. HIGH — §10 lines 817–845 and checker lines 209–215; written-row coverage is not scoped to the trace table

**Evidence.** V27 has no V24→V25 table row, yet the checker does not report that row missing. Its regex searches the entire document for the text `V24→V25`; §6.3 line 611 contains “The V24→V25 mapping must...”, so that prose mention satisfies the written-row presence test. The endpoint-pin test uses OR and searches the entire document; the V24 digest already appears as the result endpoint of the preceding V23→V24 row. Together those unrelated strings impersonate a complete V24→V25 trace row.

**Why it fails.** “In-band coverage stops at the subject's predecessor” is not actually enforced as table coverage. Any prose mention of a transition plus either endpoint digest anywhere can mask a missing trace row. This also explains why V27 reports only V25→V26 missing although two table rows, V24→V25 and V25→V26, are absent.

**Smallest sufficient repair.** Parse only the §10 markdown table; require exactly one row for each required transition; compare both endpoint digests and the computed byte/count columns in that row. Do not search the whole document, and require both endpoint pins rather than either endpoint appearing anywhere.

## Ordered-repair verification and failed attacks

### Repair 1 — catalogue quality: held

The chosen route is consistent: catalogue quality is a closed **pre-lock** exclusion, not a P8 removal.

- §2.7 lines 336–343 adds catalogue quality as reason (c) and extends the blindness rule to (a)–(c).
- §2.7 lines 372–382 freezes `flux_ivar_r > 8.4000532`, `psfsize_r < 1.5699703`, and `nobs_r >= 3`, names `acquire/quality_selected.csv`, pins SHA-256 `61214b...ec28a3`, and places application before BS-2f.
- Row E line 539 names the exact authenticated fields, source digest, one-to-one `brickid`/`objid` keys, BS-2a pinned verifier, and ordinary-exclusion failure effect.
- §5 line 489 removes catalogue quality from the fatal post-unblinding `EXCLUDED-BY-*` set.
- Row P line 550 removes it from P8 precedence and carries it only as an already-resolved pre-lock status. P8 absence, non-finiteness, and confidence remain separate states.

Independent source checks held: the named CSV's live SHA-256 is exactly `61214b59d7b35a1e5004a39c6381d08b354ec1f7be6af6b60b23474d02ec28a3`; it contains all six expected columns, 65,060 rows, 65,060 unique `(brickid,objid)` pairs, zero duplicate keys, zero non-finite threshold fields, and exactly 49,211 rows pass the three frozen predicates (15,849 excluded).

### Repair 2 — Stage P superseded: held

The document no longer calls 995/1000 a current PASS. §2.6 line 292 and §4 lines 448–450 mark it **SUPERSEDED / NON-APPLICABLE TO THE 49,211 MASK**. §2.6 line 318 and §4 line 450 state unambiguously that BS-5p cannot be filled until Stage P is rerun on the actual post-exclusion mask. This is stronger than “may need revisiting.”

### Repair 3 — false closure/orphan VOID ID: held

The V26 preamble's claim that “VOID reachability [is] repaired here” is deleted. The orphan `VOID-6.1C2-ATTESTATION-FAIL` registry row is removed rather than given a fabricated Row-C2 antecedent. Clause 10 line 580 still explicitly states that `VOID` reverse reachability is unresolved, clause 10 is not executable, and BS-6 plus the first image byte remain blocked. BS-2v remains DESIGN/UNRESOLVED at line 692.

### V26 §2.7 line 378 preservation: held

I compared V26 line 378 and V27 line 378 as encoded bytes. They are byte-equal. V27 still says only that the predicate is outcome-blind with respect to this study's unobserved χ, and explicitly says independence from handedness conditional on position “is **not established**.” No statistical-independence claim has reappeared.

### Threshold-neighbour sweep: no additional blocker found

I checked threshold statements with their neighbouring phase and failure-effect language across §§0–11. The repaired catalogue thresholds act pre-BS-2f and later movement voids the run; the N_eq 100,000 floor and Stage-C failure halt as `INCONCLUSIVE-BY-POWER`; `a_LB_b < 0.85` halts pre-unblinding as `INCONCLUSIVE-BY-CALIBRATION`; spread `> 0.03` selects profile rather than failing; post-unblinding removal emits calibration-inconclusive with no Stage-C rerun; and the p/amplitude/floor thresholds act only in the numeric verdict phase. Independently, the one-sided 95% Clopper–Pearson lower bounds are 0.950487 at 962/1000 and 0.949366 at 961/1000, confirming the stated `x ≥ 962` boundary. The quality receipt's `N_eq = 110982.5` supports the displayed rounded 110,983 and clears the 100,000 floor by 10,982.5.

## Standing-state check

The standing posture remains explicit in the document: BS-2a is DESIGN/UNFILLED; one of fifteen class-P slots is filled; BS-2v and findings 1, 2, 2b and 3 remain unresolved; Rows C2 and E cannot run; Stage P is superseded pending rerun; BS-6 and the first image byte remain blocked. `prereg_lint.py` and `prereg_counts.py` both independently report 15 class-P rows, 8 class-E rows, and only BS-2m claimed filled.

## Testimony / deliberately unverified

- `runner_v27_chain.log` says the drafting seat exited 0 and records “no inconsistencies found.” I treat the exit chronology as testimony; the digest was independently verified, and the lint/count claims were independently rerun. The trace-enforcement claim did not survive rerun.
- I did not inspect `/Users/duhokim/NebulaMindData/`, as prohibited. Therefore I do not independently attest the document's “no image byte fetched” factual statement from that tree. This review establishes only that V27 keeps the authorization/blocking language.
- I did not execute any image fetch, study run, unblinding action, git mutation, or change to the subject, sidecar, checker, source CSV, or receipts. The temporary trace mutation mirror was deleted after the test. The only durable write is this report.

## Evidence ledger

Files read for content: `BRIEF_V27_WHOLE_REVIEW.md`; pinned V27 subject; `runner_v27_chain.log`; `tools/prereg_trace.py`; `gates/FINDINGS_MAP.md`; `acquire/quality_cut_receipt.json`. V26 was read only for the V26→V27 diff and exact line-378 comparison. The quality CSV was parsed mechanically for schema, key closure, finiteness, and threshold counts; its row payloads were not printed.

Independent executions: SHA-256 of V27, V26, and the quality CSV; full V26→V27 diff; required V27 trace check; V26 trace baseline; sidecar-removal mutation tests; prereg lint; prereg counts; quality-key/threshold recount; Clopper–Pearson boundary calculation.

**NOT CLEAR**