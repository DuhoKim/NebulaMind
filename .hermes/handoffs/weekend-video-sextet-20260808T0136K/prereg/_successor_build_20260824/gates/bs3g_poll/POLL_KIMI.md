1. CHOICE — A. The control did exactly what it was frozen to do. With floor headroom fixed (min_a_lb_b ≈ 0.864 > 0.85, zero calibration-inconclusive cells), it asked whether the frozen production verdict is invariant over the declared Γ = ±0.10, and returned a reproducible counterexample at the declared edge: draw 94, γ = −0.10, INCONCLUSIVE → REPRODUCED-LONGO, byte-identical across producers, 20/20 verifier PASS. Scientifically that is a true sensitivity of a thin categorical boundary inside the advertised operating envelope — not a calibration artifact and not a near-zero instability. A worst-case invariance gate that is waived when it fires is not a gate; the honest reading is that the robustness claim over [−0.10,+0.10] is presently false as stated. Accepting the FAILED receipt (DESIGN/UNFILLED, BS-6 blocked) is the only response that preserves the prereg's evidential value. D may follow as a clearly-labelled prospective diagnostic of prevalence/mechanism, but it cannot substitute or cure.

2. OBJECTION — strongest hostile line: "One cell in 5,049 (0.0198%), at a 2.09σ edge with γ̂ ≈ 0, is a stochastic tail event; across 5,049 cells some boundary crossing is expected by chance, so A converts a multiple-comparisons artifact into a permanent block of the image half." Answer: that severity argument had to be won before the freeze. The worst-case reduction was chosen blind precisely so that frequency cannot dilute a flip ("a flip found anywhere is DECISIVE"); re-weighting severity after seeing the adverse cell is post-hoc by definition. The cell is deterministic and verifier-authenticated, not noise. If the gate's severity is judged miscalibrated, the remedy is a later, independently-justified a-priori redesign (B done properly, insulated from draw 94), never a reinterpretation of this receipt.

3. DISALLOWED — B now: squarely post-hoc; the failing cell and its location are known, so any edge-flip tolerance or shrunken Γ would be drawn around the observed counterexample. Clauses (as quoted in the packet; I could not verify them): "The resolution is preregistered, not chosen afterwards"; "It is not a cue to look for a fourth mapping." C now: post-hoc endpoint-moving and outside the V137-H signature — the amendment "does not replace or alter P0's ssh-signed V134 manifest," while §7 binds the control's "statistic … acceptance rule, and failure consequence" before BS-6; C requires a new P0 freeze. D is allowed only as non-curative follow-up. A is the literal predeclared path: "ONLY invariance_outcome = HELD CAN FILL THIS SLOT"; "a verifier-valid FAILED receipt is a TRUE RECORD THAT BLOCKS."

4. ERRORS — NONE in verifiable numbers: 99×51 = 5,049; gamma_bound 0.1; 0.10/0.004 = 25; 1/5,049 = 0.0198%; γ̂ and σ_γ match the receipt exactly; 0.10/0.04790176316993866 = 2.0876 ≈ 2.09; counterfactual_path_sha256 prefix 92cbbdf; FAILED/INCONCLUSIVE tokens. Per instructions I skipped clause-quote verification against the V137 draft. Unverifiable from the provided receipt body (replay diagnostics or other artifacts, not errors): inconclusive_count = 0, the draw-94/γ=−0.10/REPRODUCED-LONGO detail, min_a_lb_b, all 0.88-receipt fields, and the truncated 19ffcbab… SHA.

SEAT: KIMI
CHOICE: A
CONFIDENCE: high
