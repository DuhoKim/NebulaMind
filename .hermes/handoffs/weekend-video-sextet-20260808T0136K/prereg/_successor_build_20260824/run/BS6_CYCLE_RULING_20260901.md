# BS-6 DEPENDENCY-CYCLE RULING — 2026-09-01, DISCLOSED SUPERSEDING FORM

**The principal's selection, verbatim option label (relayed via Blanc/OPS,
chat-verified AskUserQuestion ~15:30):**

> Option 1 — disclosed superseding ruling (Recommended)

**Nature of this record.** The preregistration draft is FROZEN at V134 under the
P0 signature (digest `d1be4a3b…`) and cannot change. This ruling is therefore a
RUN-TIME CUSTODY-CHAIN RECORD, not a draft edit: it discloses that the principal
reordered execution, quotes the frozen clause it supersedes, and travels with the
run's evidence. Nothing in the frozen bytes is altered or hidden.

## The cycle, from the frozen bytes

The frozen text requires BS-3g's receipt BEFORE BS-6 opens ("A missing or
non-conforming BS-3g receipt leaves the `blocks BS-6` edge undischarged, and
**BS-6 does not open**"). But BS-3g's receipt requires the measured per-bin
calibration (its `calibration_sha256`, and `gamma_hat`/`sigma_gamma` semantics),
which the successor MEASURES ITSELF via `accuracy_from_handcheck()` at BS-8f —
which requires the hand-check, which requires cutouts, which require imagery,
which requires BS-6. The dependency is circular:

    BS-3g receipt ← real calibration ← BS-8f ← hand-check ← BS-6 ← BS-3g

Found by the run's own stop-and-report discipline (codex BS3G-V1 built the tools
to 14/14 fixtures then refused to invent the missing calibration or the barred
γ̂); confirmed by four independent reads finding no resolution sentence.

## What this rules

The BS-3g→BS-6 edge is discharged AT BS-6-TIME by what genuinely exists before
BS-6, exactly the seven bindings the frozen text itself demands "before BS-6"
(statistic, sample, positional stratification, uncertainty, bound, acceptance
rule, failure consequence — all ruled and built) PLUS the machinery-HELD
robustness rehearsal (99×51, zero verdict flips, the honest fixture-scope
statement on record). BS-6 may open — the first image byte.

## What is preserved, stated so it cannot be mistaken later

- **The REAL BS-3g sweep + receipt still runs**, the moment BS-8f exists (the
  calibration it needs). Only its ORDER moved — from before-BS-6 to
  after-BS-8f.
- **A FAILED invariance outcome STILL HALTS the run**, per the frozen
  consequence: `invariance_outcome = FAILED` is a true record that blocks; only
  `HELD` discharges the BS-6 edge for successor purposes, and a `FAILED` receipt
  discharges nothing. The gate is not weakened; it is not skipped; it is
  relocated in time with its teeth intact.
- **No gate weakens. Nothing χ-bearing is touched.** γ̂ remains unmeasured until
  the real sweep at BS-8f-time; v9 stays frozen at `6a9abbbd…`.

## Downstream

BS-6 opening begins the P1+ image sequence (cutouts → instrument inference →
BS-2f → hand-check → BS-8f → the relocated BS-3g sweep → BS-L lock). Human
waypoints — the Row G hand-check committee surface and the P6 lock (BS-L) —
surface through Blanc in plain words when reached.

Recorded as human direction #26.
