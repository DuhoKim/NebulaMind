# CLOSED OPERATION SET COMMITMENT (X2) — 2026-08-31

**The discipline the principal ruled** ("constants and rosters approved as
proposed", X2): the class-key operation set is fixed by EXTRACTION from the
frozen §6.1 row table, never a hand list. Discharged tonight by BLIND DOUBLE:
two seats derived independently from the frozen bytes — codex (RUNOPEN-V1 §1,
sha256 `f972818ffc1751b338c413c59f61143c953c6720645a141e5ff391f0bf84a6cc`) and agy (OPSET-V1, sha256 `19a0dffbd56879ec52561e3153a8225f9c24aacc7e5ca413a5db072d8406d84c`) — with NEITHER
seeing the other's result. The coordinator's own half stood down as
contaminated (he read codex's before deriving) and is NOT part of this record.

**Convergence:** six tokens each, pairwise semantically identical — the same
mediator-traverse filter (workflow/compute verbs excluded), the same
store-qualification argument (an unqualified READ would merge unrelated
recurrence classes), the same conveyance-as-primitive judgment (Row G's
"rendered through the sealed interface"), the same metadata-vs-content
separation for the predecessor archive. Divergence: SPELLING ONLY.

**Adjudication rule, stated:** on semantic identity, the FIRST-DERIVED set is
adopted VERBATIM — no token blending, no third spelling invented. Adopted
(codex's, identity-sorted):

```
ARCHIVE-METADATA-READ
COMMITTEE-READ
COMMITTEE-WRITE
MAIN-READ
MAIN-RENDER-TO-SEALED-INTERFACE
MAIN-WRITE
```

**Canonical encoding** (count-prefixed, identity-sorted, comma-joined):
`6:ARCHIVE-METADATA-READ,COMMITTEE-READ,COMMITTEE-WRITE,MAIN-READ,MAIN-RENDER-TO-SEALED-INTERFACE,MAIN-WRITE`

**Set digest (binds into the BS-2k seal object at staging-v2, per codex F7):**
`c520596b6233d2d68ceb40bb86800d7b693cdafd82d99aff33eb3569a0c8db8b`

agy's confirming spellings (READ-MAIN-STORE, WRITE-MAIN-STORE,
READ-COMMITTEE-STORE, WRITE-COMMITTEE-STORE, READ-METADATA-ARCHIVE-STORE,
VIEW-MAIN-STORE) map one-to-one and are recorded here as the independent half.
