# STAGE-ONE TERMINAL — the clean stop at the pre-image boundary

**2026-09-01. Ruled by the principal (chat-verified, verbatim option label):**

> Expected halt — bank & stop clean (Recommended)

This record is the run's stage-one terminal: what was frozen, what was measured,
and the explicit named boundary where the frozen preregistration's own design
ends. Nothing here fills a DESIGN slot; nothing here touches imagery or χ.

## What this preregistration is, confirmed

A **PRE-IMAGE (stage-one) preregistration**. It froze and validated the
sampling, instrument-identity and robustness design — and deliberately leaves
the image-analysis design to a **separate successor preregistration**. The run
did not fail and was not abandoned: its own stop-and-report discipline found the
boundary, two independent seats confirmed it, and the principal ruled it the
expected halt.

## What was frozen (P0, 2026-08-31 19:33 KST)

Draft **V134** + KNOWN_DEBT_APPENDIX + the spec + v9 (`6a9abbbd…`, frozen
throughout) + thirteen pinned executables + three blind commitments + nine
ruling records + two rehearsal receipts — 30 files under manifest digest
`d1be4a3b61975c79f75d6bfafa75e117f69ae86e00dc81ea139a4884f62dc72a`, signed
ed25519 under namespace `nmpr-p0`, verified three ways (principal, Blanc, Hwao).

## What was measured — real, green, banked

- **Stage-P exact power battery** on the authenticated 49,211-object mask, every
  trial against its own 20,000-permutation null through frozen v9:
  **prefix battery 984/1000**, **final re-pass 996/1000** (frozen floor 962).
- **Instrument antisymmetry (BS-3)**: 1000/1000 bit-exact identity passes,
  1000/1000 byte-exact mirror involutions, max residual 0.0, verdict PASS;
  weights `83008c1c…` located and hashed on disk.
- **Synthetic absolute-sign anchor (BS-4)**: fresh v9 battery, BATTERY-SIGN PASS
  and BATTERY-POS PASS (REPRODUCED-LONGO on the positive control).
- **Machinery robustness rehearsal**: 99 draws × 51 γ = 5,049 evaluations
  through the confirmed MappingA and frozen v9 — **zero verdict flips** against
  each draw's own γ=0 baseline. (Machinery statement on fixture data; NOT
  `invariance_outcome = HELD`, fills no slot — stated in its own receipt.)
- **Calibration-machinery rehearsal**: seven-stage chain green end to end.
- **Custody chain**: epoch 1 opened 2026-09-01 01:14:00 KST after two voided
  attempts (both caught by post-live verification, preserved with reasons);
  Row-A seal signed by the enumerator key, verified independently.

## Class-P slot state at the terminal

**Banked candidates** (through frozen `v9.receipt()` unless noted; candidates,
never live-store fills): BS-1b · BS-2o · BS-2s · BS-3 · BS-4 · BS-5p · BS-7p ·
BS-2v (via the successor-layer `receipt_strict` path, since v9 is frozen and
cannot gain the entry — the BS-3g precedent). **BS-2m** stands filled
(2026-08-26) in the frozen table. **BS-2c** was computing through the pinned
count-oracle harness at the terminal; if it completes it joins the bank, and its
absence changes nothing about this boundary.

**Pending by rule, not by failure**: **BS-1** — resolved to Branch B by ruling,
its receipt date-gated to 2026-09-05 by frozen v9's own guard (no truthful
earlier stamp exists).

## THE BOUNDARY — the stage-one terminal, named explicitly

The following are **DESIGN / UNFILLED in the frozen table** and are the reason
the image phase does not open. They gate the first image byte, and by the frozen
text's own rule they can only be filled by a **new text revision with a fresh
text gate** — which the P0 freeze forbids:

- **BS-2a — acceptance design.** The quality thresholds exist and are pinned
  (`flux_ivar_r > 8.4000532`, `psfsize_r < 1.5699703`, `nobs_r ≥ 3`), but the
  row requires the evidence/ledger schemas, `verify_cutout_integrity` (Row C2),
  confidence threshold, retry and failure semantics, and adversarial producer
  fixtures under transformed cutouts — and states the quality component is
  "not a fill authorization."
- **BS-9 — input-path rebinding.** The production single-band HDU/plane input
  function, its tensor layout, the gated replacement runner, and the R1–R5
  checks — **absent from frozen v9 entirely**; the predecessor's acquisition
  code is expressly prohibited.
- **BS-8p — hand-check allocation.** Requires the realized 3×9 stratum cells
  from the BS-SI artifact, which is χ-derived and fills only at P2–P3, i.e.
  after imagery — the frozen text blocks its emission until then.

**Both codex and agy independently confirmed all three blocks are real.** No
partial fill, no threshold-only predicate, and no synthesized receipt was
produced for any of them: hand-filling a DESIGN slot is precisely the defect the
freeze exists to prevent.

## What the successor preregistration inherits

The image-analysis design (BS-2a, BS-9, BS-8p) plus the relocated real BS-3g
sweep and receipt (which, per the BS-6 cycle ruling, runs once BS-8f's measured
calibration exists — with a FAILED outcome still halting). It inherits this
run's frozen pins, its measured pre-image results above, its ruling records, and
the honest limits stated in the KNOWN_DEBT_APPENDIX and the rehearsal receipts.

## Status

**STAGE-ONE COMPLETE AND BANKED. THE RUN STOPS CLEAN AT THIS BOUNDARY.**
Nothing forced; no imagery touched; no χ measured; γ̂ unmeasured; v9 frozen at
`6a9abbbd…` throughout. Recorded as human direction #29.
