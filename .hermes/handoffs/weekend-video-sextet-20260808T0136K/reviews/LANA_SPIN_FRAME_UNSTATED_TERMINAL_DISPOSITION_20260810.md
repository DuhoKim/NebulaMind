# LANA — Part A: FRAME_UNSTATED recorded TERMINAL (Path C), + the lane's permitted statement

Per `HWAO_RESEARCH_PLAN_20260810T1715K.md` step 1, Part A. Filed **2026-08-10 17:15 KST**. This records a
disposition and the lane's permitted statement; it edits nothing frozen (`SOURCE_FREEZE.json` stays
byte-identical `f7204bd7…`) and **unblocks nothing** (see scope note).

## Disposition: FRAME_UNSTATED is TERMINAL, not pending
Both documents A3.9 §2 authorised for the frame question — Lintott et al. 2011 (the GZ1 data release) and
Land et al. 2008 (the bias study) — were retrieved whole and searched, and both are **silent** as
establishments of the recording convention; the CDS ReadMe is established silent across three disjoint
full-file searches. A3.9 §5's FRAME_UNSTATED branch is therefore reached, and it declares the avenue
**closed**: *"no third source is authorised by this amendment, and the lane records that its result's
meaning is not recoverable from the published record available to it."* Accordingly the frame question is
**terminal** — not an open loose end awaiting more effort. This is Path C, the near-zero-cost
recommendation I made this morning, now taken.

## The lane's only permitted statement (verbatim from `LANA_T3_REDERIVATION.md` §4.3)
A3.9 §5 fixes the lane's sole permissible statement as the fenced conditional instrument reading. Verbatim:

> "On a two-rung dominance ladder, over the 77.6% of the bias-study sample matched to zooSpec, in the
> stored-label frame and conditional on unverified direction columns, the handedness asymmetry flips sign
> under the mirror condition (>6σ) with a same-direction but individually non-significant (≤1.7σ) magnitude
> deficit."

Every clause is load-bearing. Per §4.3, the lane **may not** say it *confirms, contradicts, supersedes, or
"is in tension with"* Land et al. — **no Land-comparative phrasing in any artifact, paper, video, dashboard
or summary.** The reading is *"a verified reading of an instrument whose sign convention is unconfirmed."*

## Scope — this changes nothing about the result
Recording FRAME_UNSTATED terminal does **not** release the render. The decision stays
`BLOCK_SUBSTANTIVE_RESULT_RENDER; ALLOW_METHOD_ONLY_CANARY`, `video_reportable_now` stays **false**, and the
other three blockers — `STATUS_RESULT_MISMATCH`, `WORKFLOW_STATUS_NOT_RELEASE_READY`,
`LATER_FREEZE_EXCLUDES_NEW_POINTERS` — are **untouched**. The forbidden scope binds unchanged (no T3/T4
result figures, no dipole-axis interpretation, no parity violation, no BHU support). The fenced sentence
above is the permitted *form* of any statement about the instrument reading; it is **not** authorised for
render, and it is deliberately kept out of the Part B provenance finding, which asserts nothing about
handedness.